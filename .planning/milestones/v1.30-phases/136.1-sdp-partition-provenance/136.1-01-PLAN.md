---
phase: 136.1-sdp-partition-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md
  - .planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py
  - .planning/phases/136.1-sdp-partition-provenance/136.1-01-BLAST-RADIUS.md
  - firestarter_app/tools/build_db.py
  - firestarter_app/firestarter/data/chip_database.json
autonomous: true
requirements: [PROV-01]
must_haves:
  truths:
    - "chip_database.json carries, for every upstream-decoded entry, three new explicit fields decoded straight from infoic.xml's own flags/page_size attributes for that exact <ic> element -- never a cross-reference or token match: protect_off_before (bit 14, MP_OFF_PROTECT_BEFORE), protect_on_after (bit 15, MP_PROTECT_AFTER), and infoic_page_size_raw (the un-curated upstream page_size, PROV-06's corroborating axis, deliberately not the same key as the existing datasheet-curated page_size)."
    - "The regeneration is a REAL, live HTTPS fetch of the pinned minipro commit -- python tools/build_db.py, unmodified invocation, the same recipe firestarter_app/CLAUDE.md already names -- never a read from the gitignored/absent local tools/infoic*.xml or the planning scratchpad's cached copy."
    - "The diff this task produces is mechanically PROVEN additive-only: zero manufacturers or chip entries added or removed, zero pre-existing key's value changed, and the only new keys are the three named above -- proven by a committed, re-runnable script, not eyeballed."
    - "The 84/43/41 SDP partition (test_sdp_db_invariant.py, unmodified by this plan) stays exactly as it was before this plan ran -- this plan changes provenance metadata only, it does not touch sdp_capability.py or any ALLOW/REFUSE decision."
    - "tools/extra_chips.json's two non-upstream supplement chips (TEXAS INSTRUMENTS 2516/2532) do NOT gain the three new keys -- they have no infoic.xml <ic> element to decode from, and this is the expected, correct shape, not a gap."
  artifacts:
    - path: "firestarter_app/tools/build_db.py"
      provides: "flags bit 14/15 + raw page_size decode, cited to minipro database.c @ a8efaed"
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "protect_off_before / protect_on_after / infoic_page_size_raw fields, additive-only diff"
    - path: ".planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py"
      provides: "re-runnable, committed structural diff proof (additive-only) -- the phase's own blast-radius instrument"
    - path: ".planning/phases/136.1-sdp-partition-provenance/136.1-01-BLAST-RADIUS.md"
      provides: "verbatim record of the blast-radius script's output"
    - path: ".planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md"
      provides: "pre-edit and post-PROV-01 CI-parity/mypy record"
  key_links:
    - "Plan 136.1-02's re-pointed GATE-08 and Plan 136.1-03's b15-vs-page_size refutation both read protect_on_after/infoic_page_size_raw directly from the file this plan regenerates -- a missing or misnamed field here breaks both downstream plans."
    - "tools/build_db.py's own MINIPRO_XML_URL (already pinned to a8efaedc236c1d9718bd28299dfbb99536b010ff) is the fetch this plan runs unmodified -- Plan 136.1-02's tools/derive_sdp_partition.py duplicates this same constant and must be kept in lockstep with it."
---

<objective>
Decode infoic.xml flags bits 14/15 (and the raw upstream page_size, needed downstream by PROV-06) into
chip_database.json via tools/build_db.py, regenerate the database from a real live fetch of the pinned
minipro commit, and mechanically prove the resulting diff is additive-only -- this phase's single riskiest
change, isolated and measured before anything else in the phase builds on it.

Purpose: PROV-01 requires these fields to exist as committed, in-tree facts so PROV-02/03's gate and
PROV-06's refutation can be built from data already in the repo, with no live fetch required at CI time.
chip_database.json is read by every other gate in this codebase (GATE-08's 84/43/41 partition, the whole
0x0D dispatch path, dev test's derived SDP leg) -- hard requirement 8 names this plan's blast radius as
something to measure, not assume.
Output: chip_database.json regenerated with three new per-chip fields; a committed, re-runnable
blast-radius proof; a pre-edit/post-PROV-01 CI-parity record.
</objective>

<repo_topology>
The build_db.py edit and the chip_database.json regeneration live inside the firestarter_app git
submodule -- cd /workspaces/firestarter_app and commit THERE, on gsd/v1.30-sdp-surface-retirement (the
meta repo is on gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof -- the names deliberately diverge,
per standing project convention; do not "fix" this). Verify with
`git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` before the first edit, and confirm
`git -C /workspaces/firestarter_app status --porcelain -- firestarter tests tools` is empty before
starting (there may be unrelated ambient untracked files elsewhere in the working tree -- e.g. .coverage,
SECURITY.md -- that are NOT this plan's concern; do not touch them).

.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md, 136.1-check-blast-radius.py, and
136.1-01-BLAST-RADIUS.md are **meta-repo** artifacts, committed in /workspaces.

The top-level /workspaces/firestarter/ directory is the **Arduino firmware submodule and is not touched
by this plan at all** -- v1.30 is host-only.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-VALIDATION.md
@.planning/notes/infoic-xml-protection-flags-research.md
@.planning/todos/pending/decode-infoic-flags-bits-14-15-protect-metadata.md
@.planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:** PROV-01.

**Contributes to but MUST NOT tick:** none -- PROV-01 is fully discharged by this plan (the fields exist,
decoded and committed, with their provenance comment).

**MUST NOT touch, re-tick, or re-evidence:** PROV-02, PROV-03, PROV-04, PROV-05, PROV-06 (Plans
136.1-02/136.1-03), and none of GATE-*, RETIRE-*, LEG-*, RELOCK-*, CHAN-*, or CLOSE-* -- all either
already [x] (regression floor) or another phase's scope. This plan must not edit
firestarter/sdp_capability.py or tests/test_sdp_db_invariant.py at all -- those are Plan 136.1-02's.

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-edit CI-parity + CI-replica baseline</name>
  <files>.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md</files>
  <read_first>
    - .planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md's "## After" section (the most
      recent recorded state on this branch: mypy errors 33, watermark 35, checked 130 source files,
      1494 passed, coverage 82.14%) -- the shape to mirror, and the number this task must re-measure
      fresh rather than assume still holds.
    - firestarter_app/tools/ci_parity.sh -- confirm the leg list and that leg 1 exports
      FIRESTARTER_FW_ROOT at an empty dir.
    - firestarter_app/tools/ci_replica_venv.sh -- the ONLY local path to a real mypy count (the
      devcontainer's own mypy exits 2 against numpy; the devcontainer is py3.12, CI is py3.9/3.11).
  </read_first>
  <action>
Create .planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md with a "## Before (pre-edit)"
section only; "## After PROV-01 (DB regen only)" is Task 3's, and "## After (phase close)" is Plan
136.1-04's.

Run, from /workspaces/firestarter_app:
1. tools/ci_parity.sh -- record every leg's exit status verbatim.
2. tools/ci_replica_venv.sh -- record all five legs and the two certifying lines
   "mypy errors: N (watermark: 35)" and "checked N source files".
3. .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q | tail -5 -- record the passed count
   and coverage percentage.

Record explicitly, as prose:
- This phase INHERITS a starting point -- unlike Phase 136 (which had none) -- but this task still
  MEASURES it fresh rather than copying 136-CI-PARITY.md's numbers uncritically; state whether the
  measured numbers match that record and, if not, name the delta and why.
- The observed board state (ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null) -- ci_parity.sh has no no-board
  leg; the no-board condition is ambient, record it as an observation, never a claimed leg.
- The devcontainer is Python 3.12 while CI is 3.9/3.11, so only the ci-replica numbers are quotable.

Do not modify any file under firestarter_app/ in this task. Commit in the **meta** repo only.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md &amp;&amp; grep -q 'watermark: 35' /workspaces/.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md</automated>
  </verify>
  <acceptance_criteria>
    - 136.1-CI-PARITY.md exists, contains a literal "mypy errors: N (watermark: 35)" line copied from
      a real tools/ci_replica_venv.sh run (not retyped from memory), and a "checked N source files"
      line.
    - It contains a "## Before (pre-edit)" heading and NO "## After" heading of any kind yet.
    - `git -C /workspaces/firestarter_app status --porcelain -- firestarter tests tools` prints nothing
      after this task.
  </acceptance_criteria>
  <done>The phase's pre-edit budget is measured and written down before any production line moves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decode flags bits 14/15 + raw page_size into build_db.py; regenerate; prove additive-only</name>
  <files>firestarter_app/tools/build_db.py, firestarter_app/firestarter/data/chip_database.json, .planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py, .planning/phases/136.1-sdp-partition-provenance/136.1-01-BLAST-RADIUS.md</files>
  <read_first>
    - firestarter_app/tools/build_db.py :476-486 (the raw flags/voltages/mem_size decode block --
      `flags = int(ic.get("flags"), 16)` at :479 has NO default; there is no upstream case in this
      dataset where flags is absent) and :738 ("# [VERIFIED: minipro database.c#L921-L923 @
      a8efaedc]" -- the exact citation style to mirror) and :746-767 (chip_entry["programming"]
      construction: chip_id_check at :751, chip_id_value at :752, the conditional curated
      page_size spread at :757-766).
    - firestarter_app/doc/infoic-field-dictionary.md :24-25 -- bit 14 MP_OFF_PROTECT_BEFORE /
      bit 15 MP_PROTECT_AFTER, both CONFIRMED against database.c L39-50 @ a8efaedc.
    - .planning/todos/pending/decode-infoic-flags-bits-14-15-protect-metadata.md -- the exact field
      names this plan uses (protect_off_before, protect_on_after) and the "metadata only, no
      behavior change" acceptance framing.
    - .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-derive-sdp-allowset.py
      :16, :25-26 -- `B14, B15 = 0x4000, 0x8000` and the `pg = int(ic.get("page_size", "0x0"), 16)`
      default-safe pattern to mirror for the NEW raw field (this is a DIFFERENT page_size than the
      existing curated _PAGE_SIZE_BY_PART/programming.page_size -- do not conflate the two).
    - firestarter_app/tools/extra_chips.json -- confirm both supplement entries carry
      "algorithm": 11, not 13 -- they are merged after this decode loop and correctly will NOT gain the
      three new keys.
  </read_first>
  <behavior>
    - After this edit, every upstream-decoded chip_entry["programming"] dict carries three new keys:
      protect_off_before (bool of flags &amp; 0x4000), protect_on_after (bool of flags &amp; 0x8000),
      infoic_page_size_raw (int; the raw page_size XML attribute, default 0x0 if absent).
    - The two tools/extra_chips.json supplement chips (algorithm 11, TEXAS INSTRUMENTS 2516/2532) do
      NOT carry any of the three new keys after regeneration -- they bypass this decode loop entirely.
    - Regenerating from the SAME pinned commit reproduces every pre-existing key/value byte-identically --
      the only diff is the three new keys' addition.
  </behavior>
  <action>
In tools/build_db.py, immediately after the existing `flags = int(ic.get("flags"), 16)` line (:479),
add `raw_page_size = int(ic.get("page_size", "0x0"), 16)` -- the un-curated upstream page_size attribute
straight off the same &lt;ic&gt; element, needed downstream only as PROV-06's corroborating axis. Comment
this explicitly as distinct from the existing datasheet-curated _PAGE_SIZE_BY_PART/programming.page_size
mechanism a few lines below -- same English word, two different sources, never to be confused.

In the chip_entry["programming"] dict construction (:746-767), add three new keys, computed directly
from the flags/raw_page_size values already parsed for THIS exact &lt;ic&gt; element -- never a
cross-reference or token match, and state that distinction in the comment (Phase 120's script needed
token-matching because it worked from raw infoic.xml against an already-built chip_database.json with no
per-row flags field; this decode has direct access to the source element and needs none of that machinery):

- protect_off_before: bit 14 (0x4000), minipro MP_OFF_PROTECT_BEFORE.
- protect_on_after: bit 15 (0x8000), minipro MP_PROTECT_AFTER -- the SAME bit sdp_capability.py's
  static SDP_CAPABLE_TOKENS transcription encodes, now committed as an explicit per-chip field for the
  first time.
- infoic_page_size_raw: raw_page_size -- PROV-06's corroborating axis only; not consulted by any
  ALLOW/REFUSE decision anywhere in this codebase.

Cite minipro src/database.c lines 39-50 @ commit a8efaedc236c1d9718bd28299dfbb99536b010ff in a comment
block, mirroring the citation style already at :738, and cross-reference
doc/infoic-field-dictionary.md's CONFIRMED bit 14/15 row.

State explicitly, in a comment: this decode is UNIVERSAL -- every upstream-decoded entry gets all three
keys, not just the 84 protocol-0x0D bucket (mirrors the existing unconditional chip_id_check/
chip_id_value fields a few lines above). This task does not gate on protocol, pinout, or family -- that
would be exactly the "structural regeneration" hard requirement 6 forbids; this is a flat decode of one
XML attribute into one JSON field, nothing more.

Regenerate: from /workspaces/firestarter_app, run `python tools/build_db.py` unmodified -- a REAL live
HTTPS fetch of infoic.xml at the pinned commit from gitlab.com (the exact recipe
firestarter_app/CLAUDE.md already names as the normal regeneration command). Do NOT point this at the
planning scratchpad's cached XML copy -- build_db.py's own MINIPRO_XML_URL has no local-file override,
and substituting one here would misrepresent what the committed recipe actually does when a future
maintainer runs it verbatim.

Write .planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py (a meta-repo, one-off
verification script, in the spirit of .planning/phases/120-.../120-derive-sdp-allowset.py): by default,
loads the PRE-regen chip_database.json via `git -C /workspaces/firestarter_app show HEAD~1:firestarter/data/chip_database.json`
and the POST-regen file from the working tree (both refs overridable via env vars, so the script stays
re-runnable later as a standing regression proof, not a one-shot with hardcoded temp paths); for every
manufacturer, asserts the chip list has the same length and the same part_number sequence in the same
order; for every chip entry, asserts every top-level key OTHER than "programming" is byte-identical, and
within "programming", asserts every pre-existing key/value is byte-identical and the only NEW keys are a
subset of {protect_off_before, protect_on_after, infoic_page_size_raw}; separately confirms the two
extra_chips.json supplement entries (TEXAS INSTRUMENTS 2516/2532) do NOT carry any of the three new
keys; prints a summary (total entries compared, total entries that gained new keys, the 2 supplement
entries named explicitly) and exits non-zero, naming the offending entry, on ANY violation.

Run this script. If it exits non-zero, STOP and investigate -- do not force the diff through. This task's
entire acceptance rests on a clean, additive-only diff.

Independently confirm the SDP partition is untouched: run the EXISTING, not-yet-modified
`pytest tests/test_sdp_db_invariant.py -o addopts=""` -- must stay green, unchanged, since this task does
not touch sdp_capability.py or any ALLOW/REFUSE decision.

Record the blast-radius script's verbatim output into
.planning/phases/136.1-sdp-partition-provenance/136.1-01-BLAST-RADIUS.md.

Commit the build_db.py edit and the regenerated chip_database.json TOGETHER in the submodule. Commit
the two meta-repo artifacts separately in /workspaces.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; python3 /workspaces/.planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_sdp_db_invariant.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - The blast-radius script exits 0 and names zero violations.
    - Diff shows ONLY additions of the three named keys; no manufacturer or chip entry added or removed;
      the 2 extra_chips.json supplement entries confirmed to lack the new keys.
    - `pytest tests/test_sdp_db_invariant.py -o addopts=""` (unmodified by this plan) still reports the
      84/43/41 partition unchanged.
    - 136.1-01-BLAST-RADIUS.md records the verbatim summary output.
    - `git -C /workspaces/firestarter_app diff --stat HEAD~1 HEAD -- firestarter tools` (for this task's
      commit) touches exactly firestarter/data/chip_database.json and tools/build_db.py, nothing else.
    - `python3 -c "import ast; ast.parse(open('tools/build_db.py').read())"` exits 0 (still valid Python).
  </acceptance_criteria>
  <done>chip_database.json is regenerated with three new, universally-emitted, per-chip decode fields, and
  the diff is mechanically proven additive-only -- zero existing values changed, zero entries added or
  removed, the 84/43/41 SDP partition untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Record the post-PROV-01 CI-parity state and compare against the baseline</name>
  <files>.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md</files>
  <read_first>
    - This plan's own 136.1-CI-PARITY.md "## Before (pre-edit)" section (Task 1) -- the exact metrics
      to re-measure and compare against.
    - firestarter_app/.github/workflows/ci.yml :63-70 -- ruff/ruff-format/mypy all scope explicitly to
      `firestarter/ tests/`, NEVER `tools/` -- ground the expectation below in this fact, not a guess.
  </read_first>
  <action>
Append a "## After PROV-01 (DB regen only)" section to 136.1-CI-PARITY.md (edit the existing file; do not
overwrite "## Before"). Run, from /workspaces/firestarter_app:

1. tools/ci_parity.sh -- record every leg's exit status verbatim, compare against "## Before".
2. tools/ci_replica_venv.sh -- record all five legs and the two certifying lines. State the expectation
   explicitly, grounded in ci.yml's own scoping: this task's only production edit is inside tools/, which
   is OUTSIDE both mypy's scanned path (`sys.executable -m mypy firestarter/ tests/`, GATE-04) and ruff's
   CI-scanned path (`ruff check firestarter/ tests/`) -- so the mypy count and the `checked N source
   files` count are expected to be IDENTICAL to "## Before". Measure and confirm this explicitly; do not
   assume it without the real run.
3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q | tail -5` -- record the passed count
   and coverage. State the expectation explicitly: this task adds and edits zero test files, so the
   passed count must be EXACTLY the "## Before" count, not merely "greater than or equal" -- a stronger,
   more specific claim than a plan that adds new tests could make.
4. Re-confirm the observed board state and repeat the "no no-board leg" note from Task 1.

State plainly: no file under /workspaces/firestarter/ (firmware submodule) was touched at any point in
this task, and no new pip/npm/cargo package was installed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; tools/ci_replica_venv.sh</automated>
  </verify>
  <acceptance_criteria>
    - 136.1-CI-PARITY.md contains both "## Before (pre-edit)" and "## After PROV-01 (DB regen only)"
      headings.
    - The "## After PROV-01" section states the mypy count and checked-file count are IDENTICAL to
      "## Before", with the tools/-is-out-of-scope reasoning stated explicitly, and confirms this by a
      real run rather than asserting it a priori.
    - The full suite's passed count is EXACTLY equal to the "## Before" count (0 delta), stated explicitly.
    - `git -C /workspaces/firestarter_app status --porcelain -- firestarter` (the firmware submodule)
      prints nothing.
  </acceptance_criteria>
  <done>PROV-01's own blast radius on CI health is measured and recorded as zero mypy/lint/test-count
  delta, isolating this highest-risk change's cost before any other plan builds on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_db.py → gitlab.com (unauthenticated HTTPS fetch) | The regeneration recipe pulls infoic.xml live from a pinned commit SHA over HTTPS, with no content checksum pinning beyond the SHA itself |
| infoic.xml upstream content → chip_database.json | Upstream drift at the pinned commit is not possible (SHA-pinned), but a bug in this task's own decode could still misattribute a field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136.1-01 | Tampering | `tools/build_db.py`'s unauthenticated HTTPS fetch of infoic.xml | low | accept | Unchanged, pre-existing mechanism (this task adds no new fetch); commit-SHA pinning is the established, unmodified risk posture |
| T-136.1-02 | Tampering | chip_database.json regeneration silently mutating or reordering unrelated fields | high | mitigate | 136.1-check-blast-radius.py mechanically proves additive-only; task STOPS on any violation rather than forcing the diff |
| T-136.1-03 | Repudiation | An "additive-only" claim asserted without evidence | medium | mitigate | Verbatim script output recorded in 136.1-01-BLAST-RADIUS.md |
| T-136.1-04 | Denial of Service | mypy/ruff watermark headroom silently consumed by this task's edit | medium | mitigate | Task 3 measures fresh; reasoned and confirmed zero-impact since `tools/` is outside both CI gates' scanned paths (ci.yml :63-70) |
| T-136.1-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this task |
</threat_model>

<verification>
- `python3 .planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py` -- exits 0, zero
  violations.
- `pytest tests/test_sdp_db_invariant.py -o addopts="" -q` -- green, 84/43/41 unchanged.
- `tools/ci_replica_venv.sh` -- 5/5 legs, mypy count and checked-file count identical to "## Before".
- `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` -- passed count identical to
  "## Before".
- No file under `/workspaces/firestarter/` (firmware submodule) is touched.
</verification>

<success_criteria>
- chip_database.json carries `protect_off_before`/`protect_on_after`/`infoic_page_size_raw` for every
  upstream-decoded entry, decoded straight from the same `<ic>` element's own `flags`/`page_size`.
- The regeneration's diff is mechanically proven additive-only, with verbatim evidence recorded.
- The 84/43/41 SDP partition is unchanged; `sdp_capability.py` and the ALLOW/REFUSE decision are untouched.
- 136.1-CI-PARITY.md records a real "## Before" and "## After PROV-01" with an explicit, reasoned,
  zero-delta mypy/lint/test-count comparison.
</success_criteria>

<output>
Create `.planning/phases/136.1-sdp-partition-provenance/136.1-01-SUMMARY.md` when done.
</output>
