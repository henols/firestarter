---
phase: 136.1-sdp-partition-provenance
plan: "04"
subsystem: chip-database-provenance
tags: [sdp, chip_database, ci-parity, mypy-watermark, honesty-ledger, phase-close]

requires:
  - phase: 136.1-sdp-partition-provenance
    plan: "01"
    provides: "chip_database.json's committed protect_on_after/infoic_page_size_raw
      fields and 136.1-CI-PARITY.md's ## Before / ## After PROV-01 sections, the
      exact shape this plan's ## After (phase close) section extends"
  - phase: 136.1-sdp-partition-provenance
    plan: "02"
    provides: "GATE-08 leg 1 (infoic-derived-field comparison) and
      tools/derive_sdp_partition.py, both re-run independently by this plan"
  - phase: 136.1-sdp-partition-provenance
    plan: "03"
    provides: "the two new test modules (test_lockable_proms_doc_claims.py,
      test_b15_page_size_corroboration.py) whose mypy-scanned-file-count delta
      (+2) this plan measures rather than assumes zero"
provides:
  - "136.1-CI-PARITY.md's closing ## After (phase close) section: a real,
    measured whole-phase mypy/lint/test-count/coverage delta table, never
    inherited or estimated"
  - "136.1-RECORD.md: the phase's close-out record carrying forward all five
    findings from the three prior plans, the six PROV requirements' final
    state, and a whole-phase cost ledger for Phase 137's honesty ledger to cite"
  - "A ninth independent confirmation (across the whole phase, counting this
    plan's own three) that the 43 ALLOW / 41 REFUSE / 84-total SDP partition
    never moved -- including a byte-for-byte diff against the ORIGINAL,
    pre-phase, pre-milestone 120-sdp-partition.json"
affects: ["137-close (honesty ledger cites this phase's measured cost + all
  five carried-forward findings directly, never re-derives them)"]

tech-stack:
  added: []
  patterns:
    - "Whole-phase CI-parity three-section record (Before / After-<first-plan> /
      After-phase-close), same shape 136-CI-PARITY.md established -- the
      closing section always re-measures fresh, never trusts an inherited
      number, and states every delta explicitly including non-zero ones
      (checked-file count, test count) rather than only the headline metric
      (mypy error count)"

key-files:
  created:
    - .planning/phases/136.1-sdp-partition-provenance/136.1-RECORD.md
  modified:
    - .planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md

key-decisions:
  - "Authored 136.1-RECORD.md even though it is not in this plan's own
    files_modified frontmatter list -- Rule 2 (auto-add missing critical
    functionality): the dispatching orchestrator's own success criteria for
    this plan explicitly required a close-out record carrying forward all
    five findings, and the phase's own goal statement ('provenance, never
    verdicts') deserves a record that states what was proven AND what was not,
    not just a CI-parity number table"
  - "Ran tools/ci_parity.sh and tools/ci_replica_venv.sh in parallel background
    processes (independent scripts, no shared state) to shorten wall-clock
    time, then verified each completion via its own background-task
    notification before reading full output -- never assumed completion from
    elapsed time alone"
  - "Independently re-measured the byte-for-byte diff against
    120-sdp-partition.json's ORIGINAL committed allow array via a fresh
    one-off script reading chip_database.json and calling
    sdp_capability_for_entry directly, rather than trusting any prior plan's
    own recorded diff -- the task's own hard requirement (T-136.1-12) names
    this as the phase's most independent check"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "136.1-CI-PARITY.md's ## After (phase close) section: whole-phase
      mypy/lint/test-count/coverage delta table, measured fresh via
      tools/ci_parity.sh and tools/ci_replica_venv.sh, never inherited"
    verification:
      - kind: other
        ref: "tools/ci_replica_venv.sh (CI-REPLICA: PASS; mypy errors: 33
          (watermark: 35); checked 132 source files; 1504 passed; coverage
          82.14%) -- recorded verbatim in 136.1-CI-PARITY.md"
        status: pass
      - kind: other
        ref: "tools/ci_parity.sh (CI-PARITY: FAIL (legs:4), identical shape to
          ## Before / ## After PROV-01 -- leg 4's exit 2 is the documented
          devcontainer-numpy shape, not a regression)"
        status: pass
    human_judgment: false
  - id: D2
    description: "43 ALLOW / 41 REFUSE / 84-total SDP partition re-confirmed
      unchanged, independently, one final time -- via a byte-for-byte diff
      against the ORIGINAL 120-sdp-partition.json, a fresh from-scratch
      tools/derive_sdp_partition.py re-run, and tests/test_sdp_db_invariant.py's
      9/9 green (both partition-comparison legs agreeing)"
    verification:
      - kind: other
        ref: "one-off diff script comparing chip_database.json's
          sdp_capability_for_entry-derived ALLOW set against
          120-sdp-partition.json's committed allow array -- BYTE-FOR-BYTE
          MATCH: True, zero entries differ either direction"
        status: pass
      - kind: other
        ref: "INFOIC_XML_PATH=<cached> python3 tools/derive_sdp_partition.py --
          PASS: 43/41/84, zero disagreement against both sdp_capability_for_entry
          and the committed protect_on_after field, exit 0"
        status: pass
      - kind: unit
        ref: "tests/test_sdp_db_invariant.py -o addopts=\"\" -q (9 passed);
          -v confirms both
          test_sdp_partition_matches_committed_allow_list_element_wise and
          test_sdp_partition_matches_infoic_derived_field_element_wise green"
        status: pass
    human_judgment: false
  - id: D3
    description: "136.1-RECORD.md authored, carrying forward all five findings
      from Plans 136.1-01/02/03 (PROV-05 stale premise, PROV-02
      static-transcription decision, PROV-06 cross-methodology corroboration,
      the GATE-02 blast-radius fix, the 'committed NOTHING' process failure),
      the six PROV requirements' final state, and the whole-phase cost ledger"
    verification: []
    human_judgment: true
    rationale: "Narrative correctness -- whether the record faithfully carries
      forward each finding without softening or omission -- is a judgment call
      no automated check can make; the underlying facts it cites (commit
      hashes, test names, measured counts) are each independently verified
      above (D1/D2) and in the prior plans' own SUMMARYs."

duration: 21min
completed: 2026-08-05
status: complete
---

# Phase 136.1 Plan 04: SDP Partition Provenance — Phase Close Summary

**Closed the phase with a fresh, measured whole-phase CI-parity delta table (mypy headroom flat at 2,
+2 mypy-scanned files, +10 tests, 0.00pp coverage change) and a `136.1-RECORD.md` close-out that
re-confirms the 43/41/84 SDP partition unchanged via three independent methods, including a
byte-for-byte diff against the pre-milestone `120-sdp-partition.json`.**

## Performance

- **Duration:** 21 min
- **Started:** 2026-08-05T16:31:00Z
- **Completed:** 2026-08-05T16:52:00Z
- **Tasks:** 1 completed (1/1) — plus one Rule-2 deviation (136.1-RECORD.md)
- **Files modified:** 2 (1 modified, 1 created), both meta-repo only — no submodule commit, per this
  plan's own `<repo_topology>` (pure verification capstone)

## Accomplishments

- Ran `tools/ci_parity.sh` and `tools/ci_replica_venv.sh` fresh from `/workspaces/firestarter_app`
  (parallel background runs, independently verified complete via each one's own notification):
  `ci_parity.sh` — legs 1-3 exit 0, leg 4 exit 2 (the documented devcontainer-numpy shape, identical
  to `## Before` and `## After PROV-01`), `CI-PARITY: FAIL (legs:4)`. `ci_replica_venv.sh` —
  `CI-REPLICA: PASS`, `mypy errors: 33 (watermark: 35)`, `checked 132 source files` (up from 130 —
  Plan `136.1-03`'s two new test files, which sit inside mypy's CI-scanned `tests/` path), full suite
  **1504 passed**, coverage **82.14%**, ruff clean.
- Re-ran `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` standalone: **1504 passed, 1
  warning in 219.77s**, matching the `ci_replica_venv.sh` leg 5 count exactly (same interpreter, same
  test set, independently invoked a second time).
- Re-ran `tests/test_sdp_db_invariant.py -o addopts="" -q` explicitly: **9 passed**; verbose (`-v`)
  output confirms both `test_sdp_partition_matches_committed_allow_list_element_wise` (Phase 131's
  hand-curated snapshot) and `test_sdp_partition_matches_infoic_derived_field_element_wise` (Plan
  `136.1-02`'s infoic-derived DB field) green, both independently agreeing on **43 ALLOW / 41 REFUSE /
  84 total**.
- **Independently re-derived and diffed the ALLOW set against the ORIGINAL
  `120-sdp-partition.json`** — read `chip_database.json` directly, called the production
  `sdp_capability_for_entry` predicate over all 84 `algorithm==13` entries, and compared the resulting
  43-entry key set against `120-sdp-partition.json`'s own committed `allow` array (pre-Phase-136.1,
  pre-v1.30): **BYTE-FOR-BYTE MATCH: True, zero entries differ in either direction**.
- Re-ran `tools/derive_sdp_partition.py` once more (`INFOIC_XML_PATH` pointed at the cached
  pinned-commit scratchpad copy, avoiding a redundant live fetch): **PASS — 43 ALLOW / 41 REFUSE / 84
  total, zero disagreement against both `sdp_capability_for_entry` and the committed
  `protect_on_after` field, exit 0** — the phase's closing, independent, from-scratch confirmation.
- Confirmed `git -C /workspaces/firestarter status --porcelain` empty (firmware submodule untouched
  across the whole phase) and `git -C firestarter_app log --diff-filter=A --name-only
  f294821^..HEAD -- '*.txt' pyproject.toml setup.py` returns nothing (no new pip/npm/cargo package
  installed anywhere in this phase).
- Appended `## After (phase close)` to `136.1-CI-PARITY.md`: a whole-phase summary table stating every
  delta explicitly — mypy headroom **0**, checked-files **+2**, full suite **+10**, coverage
  **0.00pp**, SDP partition **0** — none estimated, all measured fresh this task.
- **[Rule 2 deviation]** Authored `136.1-RECORD.md`, the phase's close-out record: re-states the
  43/41/84 re-confirmation, carries forward all five findings from Plans `136.1-01`/`02`/`03` (the
  PROV-05 stale-premise finding, the PROV-02 static-transcription decision, the PROV-06
  cross-methodology corroboration, the GATE-02 blast-radius fix, and the "committed NOTHING" process
  failure), tabulates the six `PROV-0X` requirements' final state, and the whole-phase cost ledger —
  for Phase 137's honesty ledger to cite directly rather than re-derive.

## Task Commits

Both commits are meta-repo only (this plan makes no submodule commit, per its own
`<repo_topology>`):

1. **Task 1: Re-assert the 43/41/84 split is unchanged and record the phase's final CI-parity
   state** - meta `fd783947` (docs) — appended `## After (phase close)` to `136.1-CI-PARITY.md`

**Deviation commit (Rule 2):**
- meta `94877d6d` (docs) — authored `136.1-RECORD.md`

**Plan metadata:** commits for STATE.md/ROADMAP.md updates and this SUMMARY follow, see completion
report.

## Files Created/Modified

- `.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md` - appended `## After (phase
  close)`: verbatim `ci_parity.sh`/`ci_replica_venv.sh` output, the standalone full-suite re-run, the
  explicit `test_sdp_db_invariant.py` re-run, the byte-for-byte `120-sdp-partition.json` diff, the
  closing `derive_sdp_partition.py` re-run, the phase's opening promise re-confirmed, the
  firmware/package-manager confirmation, and a whole-phase summary table
- `.planning/phases/136.1-sdp-partition-provenance/136.1-RECORD.md` - new: the phase close-out record
  (Rule 2 deviation, see below)

## Decisions Made

- Authored `136.1-RECORD.md` even though not in this plan's own `files_modified` frontmatter list —
  the dispatching orchestrator's success criteria for this plan explicitly required it, and the
  phase's central promise ("provenance, never verdicts") warranted a record stating what was proven
  and what was not, distinct from the pure numeric CI-parity table.
- Ran `tools/ci_parity.sh` and `tools/ci_replica_venv.sh` as parallel background processes (fully
  independent scripts, no shared state, no file contention) to shorten wall-clock time; verified each
  one's completion via its own background-task notification rather than polling on a fixed schedule.
- Performed the byte-for-byte `120-sdp-partition.json` diff via a fresh, independent one-off script
  (reading `chip_database.json` directly and calling `sdp_capability_for_entry`) rather than trusting
  any prior plan's own recorded diff — per the task's own instruction that this is the phase's *most*
  independent check, and per threat `T-136.1-12`'s disposition.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Authored `136.1-RECORD.md`, not in this plan's own
`files_modified` list**
- **Found during:** Task 1, after the CI-parity record was appended
- **Issue:** This plan's `136.1-04-PLAN.md` frontmatter names only `136.1-CI-PARITY.md` under
  `files_modified`, and its own `<output>` spec names only the SUMMARY. The dispatching orchestrator's
  prompt for this execution, however, explicitly listed a `136.1-RECORD.md`-equivalent close-out
  (carrying all five findings, stating what was proven and what was not) as a required success
  criterion — a correctness requirement for the phase's own close, not an optional nicety.
- **Fix:** Authored `.planning/phases/136.1-sdp-partition-provenance/136.1-RECORD.md`: the 43/41/84
  re-confirmation, all five carried-forward findings named with their source plan and commit hash, a
  `PROV-0X` requirement traceability table, the whole-phase CI-parity/cost ledger, the phase's own
  "no ICs refused" vs. "provenance, not verdicts" framing, and an explicit hand-off section for Phase
  137.
- **Files modified:** `.planning/phases/136.1-sdp-partition-provenance/136.1-RECORD.md` (new)
- **Verification:** File exists on disk; every fact it cites (commit hashes, test names, measured
  counts, requirement traceability) is independently checked against `git log`, `REQUIREMENTS.md`, and
  this task's own fresh measurements before being written.
- **Committed in:** `94877d6d` (separate commit from the CI-parity append, since it is a distinct
  artifact)

---

**Total deviations:** 1 auto-fixed (Rule 2 — missing critical functionality, the phase-close record
the dispatching orchestrator's own success criteria required).
**Impact on plan:** Necessary for a complete phase close — the CI-parity table alone states numbers,
not what those numbers mean or what the phase proved/did not prove; `136.1-RECORD.md` is the artifact
Phase 137's honesty ledger needs to cite this phase without re-deriving its findings. No scope creep:
the record is documentation-only, cites facts already independently verified, and makes no code or
production-file change.

## Issues Encountered

None beyond the deviation above. No blockers, no auth gates, no checkpoints. `tools/ci_parity.sh`'s
leg 2 (and `ci_replica_venv.sh`'s leg 5) took longer than the `## Before` measurement's own recorded
runtime because `tests/test_skip_census.py` spawns a full-suite subprocess re-run internally — this is
expected, pre-existing test-suite behavior, not a regression this plan caused.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

Phase 136.1 is now plan-complete (4/4) with all six `PROV-0X` requirements Complete (verified `[x]` in
`REQUIREMENTS.md` at this plan's start, untouched by this plan). The 43 ALLOW / 41 REFUSE / 84-total
SDP partition is proven unchanged by nine independent measurements across the whole phase (six from
Plans `136.1-01`/`02`/`03`, three more by this plan). `136.1-CI-PARITY.md` carries a complete Before /
After-PROV-01 / After-phase-close record with every whole-phase delta stated explicitly (mypy headroom
flat at 2, checked-files +2, full suite +10, coverage 0.00pp, partition 0). `136.1-RECORD.md` gives
Phase 137's honesty ledger a single citable artifact for this phase's five findings and its measured
cost. Project-wide requirement state: **49 ticked / 7 open** (CLOSE-01…06 + RELOCK-07 — all Phase
137's own scope), unchanged by this plan. No blockers for Phase 137.

---
*Phase: 136.1-sdp-partition-provenance*
*Completed: 2026-08-05*

## Self-Check: PASSED

All created/modified files confirmed present on disk (`136.1-CI-PARITY.md`, `136.1-RECORD.md`,
`136.1-04-SUMMARY.md`). All referenced commit hashes confirmed present via `git log --oneline --all`
(meta: `fd783947`, `94877d6d`, `31832378`). No missing items.
