# Plan 136.1-01 Task 2 — Blast-Radius Proof (verbatim)

Records the verbatim output of `.planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py`
proving PROV-01's `chip_database.json` regeneration is additive-only.

Two runs were made:

1. **Pre-commit run** (`PRE_DB_REF=HEAD` override, comparing the working tree against the
   still-committed pre-regen state) — the "run this script, stop if non-zero" check the
   plan's action step names, executed before the submodule commit existed.
2. **Post-commit run** (default invocation, no env override — `PRE_DB_REF` defaults to
   `HEAD~1`) — the literal reproducible command this plan's own `<verify>` block names,
   executed after commit `f294821` (`firestarter_app`, "feat(136.1-01): decode infoic.xml
   flags b14/b15 + raw page_size into chip_database.json"). Both runs agree exactly.

## Run 1 — pre-commit (`PRE_DB_REF=HEAD`)

```
$ cd /workspaces/firestarter_app && PRE_DB_REF=HEAD python3 /workspaces/.planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py
==============================================================================
136.1-01 BLAST-RADIUS PROOF -- chip_database.json regeneration, additive-only
==============================================================================
Total chip entries compared:        746
Entries that gained new key(s):      744
Supplement entries (TEXAS INSTRUMENTS 2516/2532) confirmed present, WITHOUT new keys: ['2516', '2532']

VIOLATIONS: 0
RESULT: PASS -- diff is additive-only (only the three named keys added).
```

## Run 2 — post-commit (default invocation, `PRE_DB_REF` defaults to `HEAD~1`)

```
$ cd /workspaces/firestarter_app && python3 /workspaces/.planning/phases/136.1-sdp-partition-provenance/136.1-check-blast-radius.py
==============================================================================
136.1-01 BLAST-RADIUS PROOF -- chip_database.json regeneration, additive-only
==============================================================================
Total chip entries compared:        746
Entries that gained new key(s):      744
Supplement entries (TEXAS INSTRUMENTS 2516/2532) confirmed present, WITHOUT new keys: ['2516', '2532']

VIOLATIONS: 0
RESULT: PASS -- diff is additive-only (only the three named keys added).
```

**746 total entries compared** (744 upstream-decoded + 2 `extra_chips.json` supplement
entries) — matches `build_db.py`'s own regeneration print exactly ("744 upstream chips
processed + 2 non-upstream supplement chip(s) = 746 total"). **744 entries gained the
three new keys** — every upstream-decoded entry, none of the 2 supplement entries.
**0 violations** on both runs: no manufacturer added or removed, no chip added, removed,
or reordered within a manufacturer, no pre-existing key/value changed, and the only new
keys anywhere are `protect_off_before` / `protect_on_after` / `infoic_page_size_raw`,
confirmed absent from both TEXAS INSTRUMENTS supplement entries (`2516`, `2532`).

## Independent confirmation — `git diff --stat`

```
$ git -C /workspaces/firestarter_app diff --stat HEAD~1 HEAD -- firestarter tools
 firestarter/data/chip_database.json | 2232 +++++++++++++++++++++++++++++++++++
 tools/build_db.py                   |   34 +
 2 files changed, 2266 insertions(+)
```

**Zero deletions** (`+2266 insertions, 0 deletions`) — corroborates the blast-radius
script's structural finding from git's own line-level diff, independently. `git diff
--diff-filter=D --name-only HEAD~1 HEAD` (this task's commit) is empty — no file deleted.

## Independent confirmation — the 84/43/41 SDP partition, unmodified by this plan

```
$ .venv/ci-replica/bin/python -m pytest tests/test_sdp_db_invariant.py -o addopts="" -q
.......
7 passed in 0.08s
```

`tests/test_sdp_db_invariant.py` (unedited by this plan) stays green after the
regeneration — the 84/43/41 SDP ALLOW/REFUSE partition is unchanged. This plan changes
provenance metadata only; `firestarter/sdp_capability.py` and the ALLOW/REFUSE decision
were not touched.
