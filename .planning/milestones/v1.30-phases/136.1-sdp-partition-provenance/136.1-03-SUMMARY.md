---
phase: 136.1-sdp-partition-provenance
plan: "03"
subsystem: chip-database-provenance
tags: [infoic-xml, chip_database, sdp, provenance, documentation, honesty-ledger]

requires:
  - phase: 136.1-sdp-partition-provenance
    plan: "01"
    provides: "chip_database.json's committed programming.protect_on_after and
      programming.infoic_page_size_raw fields (infoic.xml flags bit 15 and raw
      upstream page_size) -- the hard data dependency this plan's PROV-06 test reads
      directly, with no .get() default"
  - phase: 121-close-gap-warning-5-at28c256-64-5v-eeprom-override-12v-on-we
    provides: "doc/lockable-proms.md section 17's AT28C16/AT28C64B/AT28C256
      correction, commit c3c9424 (plan 121-13) -- already landed before this
      phase (136.1) or this milestone (v1.30) were scoped; this plan verifies
      and durably gates it, and does not re-author it"
provides:
  - "tests/test_lockable_proms_doc_claims.py: a durable, automated regression
    gate for doc/lockable-proms.md section 17's AT28C16/AT28C64B/AT28C256
    SDP-capability distinction, plus a whole-tree negative scan for the wrong
    blanket-claim shorthand (PROV-05)"
  - "tests/test_b15_page_size_corroboration.py: a measured, named, non-vacuous
    in-tree refutation of the 'b15 approximates a page-write family marker'
    equivalence, built on Plan 136.1-01's protect_on_after/infoic_page_size_raw
    fields (PROV-06)"
affects: ["136.1-04 (phase close, final 43/41/84 re-assertion)", "137-close (honesty ledger -- this plan's own stale-premise finding is itself ledger-relevant)"]

tech-stack:
  added: []
  patterns:
    - "Self-contained, narrow test modules that deliberately duplicate small
      selection helpers (_select_0x0d_chips) rather than cross-import between
      test files -- matches this project's existing house convention"
    - "Verify-before-re-author: when a requirement's premise turns out to
      already be satisfied, the plan records that fact plainly (naming the
      prior commit) instead of silently re-authoring correct prose to look
      like new work"

key-files:
  created:
    - firestarter_app/tests/test_lockable_proms_doc_claims.py
    - firestarter_app/tests/test_b15_page_size_corroboration.py
  modified:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "PROV-05's premise was found stale at verification time: doc/lockable-proms.md
    section 17 already stated the corrected AT28C16/AT28C64B/AT28C256 fact,
    landed by Phase 121 plan 121-13 (commit c3c9424) well before v1.30 was
    scoped. This plan did NOT re-author the doc -- it verified the correction is
    present, confirmed (whole-tree grep) no other stale copy of the wrong claim
    exists anywhere in firestarter_app, and added the durable automated gate
    that did not exist before."
  - "PROV-06's '12 of 84' was measured FRESH via a per-row, single-value
    methodology (this test's own approach) rather than copied from Phase 120's
    cross-token-set-matching methodology. The two measurements agree (12), which
    is recorded as a corroborating finding across independently different
    methodologies, not an assumption that they had to agree."
  - "firestarter/sdp_capability.py was never touched by this plan (Plan
    136.1-02's file this wave) -- confirmed via git diff --stat empty across
    both task commits, plus a structural regression test asserting its
    existing '12 of the 84' docstring prose stays unedited."

patterns-established:
  - "A requirement whose stated defect turns out to have already been fixed by
    a prior, unrelated phase is recorded as a finding (stale premise), not
    hidden or silently re-claimed -- the test module's docstring names the
    exact prior commit and plan."

requirements-completed: [PROV-05, PROV-06]

coverage:
  - id: D1
    description: "doc/lockable-proms.md section 17's AT28C16/AT28C64B/AT28C256
      SDP-capability correction verified present (landed by Phase 121 c3c9424,
      pre-dating this phase) and durably gated by an automated test; whole-tree
      scan confirms no other stale copy of the wrong blanket claim exists"
    requirement: "PROV-05"
    verification:
      - kind: unit
        ref: "firestarter_app tests/test_lockable_proms_doc_claims.py -o addopts=\"\" -q (4 passed, commit c9f98b8)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The 'b15 approximates a page-write family marker' equivalence
      refuted in-tree: 12 of 84 algorithm==13 entries disagree between
      protect_on_after and (infoic_page_size_raw > 1), every disagreeing chip
      named, non-vacuity proven on a hand-counted synthetic set first"
    requirement: "PROV-06"
    verification:
      - kind: unit
        ref: "firestarter_app tests/test_b15_page_size_corroboration.py -o addopts=\"\" -q (4 passed, commit 31b5d74)"
        status: pass
    human_judgment: false

duration: "~16min this session (bookkeeping/verification only -- see Deviations)"
completed: 2026-08-05
status: complete
---

# Phase 136.1 Plan 03: SDP Partition Provenance -- doc/lockable-proms.md Correction Verification + b15-vs-page_size Refutation Summary

**Verified (not re-authored) `doc/lockable-proms.md` section 17's AT28C16/AT28C64B/AT28C256 correction --
already landed by Phase 121, pre-dating this phase -- and durably gated it with a new test; separately,
measured fresh and named all 12 chips where bit 15 (`protect_on_after`) disagrees with `infoic_page_size_raw
> 1` over the 84-entry SDP bucket, refuting the "b15 ≈ page-write family marker" equivalence in-tree.**

## Performance

- **Duration:** ~16 min this session (verification, full-suite/CI-parity re-measurement, and all
  plan-level bookkeeping: requirement ticks, ROADMAP/STATE updates, gitlink bump, this SUMMARY).
- **Started:** 2026-08-05T16:14:00Z (approx.)
- **Completed:** 2026-08-05T16:27:50Z
- **Tasks:** 2/2 already complete on disk at session start (see "How this plan was actually executed"
  below) -- this session's own work was verification + bookkeeping, not authorship.
- **Files modified:** 2 created (submodule, by the prior session), 3 meta-repo docs updated
  (`REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`), 1 gitlink bump, this SUMMARY.

## How this plan was actually executed (read before trusting the task list below)

**The orchestrator's dispatch note for this session said a previous executor attempt "died to a transient
API 529 partway through and committed NOTHING... Start fresh; do not assume any prior work exists."
That claim was checked, not assumed, and found to be about the META repo only -- not the submodule.**

At session start, `git -C firestarter_app log --oneline -5` showed both of this plan's task commits
already present and dated **2026-08-05T14:49:52Z** (`c9f98b8`, Task 1 / PROV-05) and
**2026-08-05T14:50:07Z** (`31b5d74`, Task 2 / PROV-06) -- roughly 90 minutes before this session began.
`REQUIREMENTS.md` still showed both `PROV-05`/`PROV-06` as `Pending`, no `136.1-03-SUMMARY.md` existed, and
the meta repo's `firestarter_app` gitlink still pointed at Plan `136.1-02`'s tip (`dc5bfbe`), one commit
*behind* the submodule's own checked-out HEAD (`git submodule status` reported it `+31b5d748...-dirty` --
ahead of the recorded pointer). So the true prior state was: **a previous session did complete both of
this plan's tasks and commit them correctly inside the submodule, then died before any of the meta-repo
bookkeeping (gitlink bump, requirement ticks, SUMMARY, STATE update) ran.** The dispatch note's "committed
NOTHING" was accurate about the meta repo's own working tree (which was genuinely clean, and is exactly
what an orchestrator checking `git status`/`REQUIREMENTS.md` from the meta repo alone would see) but did
not hold for the submodule, which has its own independent commit history.

This session did **not** redo Task 1 or Task 2. Instead, it:
1. Read both new test files in full and cross-checked every acceptance criterion in `136.1-03-PLAN.md`
   against them (docstring citations, negative-regex scope, non-vacuity ordering, named disagreement list,
   `sdp_capability.py` untouched) -- all satisfied, no gaps found.
2. Re-ran `pytest tests/test_lockable_proms_doc_claims.py tests/test_b15_page_size_corroboration.py -o
   addopts="" -q` fresh: **8 passed**.
3. Re-ran the full suite and `tools/ci_replica_venv.sh` fresh (see below) to confirm no regression and to
   produce this plan's own CI-parity numbers, since neither had been recorded anywhere yet.
4. Completed the plan-level work the crashed session never reached: bumped the `firestarter_app` gitlink,
   ticked `PROV-05`/`PROV-06` in `REQUIREMENTS.md` (with the stale-premise finding recorded in the
   requirement text itself, not just here), ticked `ROADMAP.md`'s plan checkbox, updated `STATE.md`, and
   authored this SUMMARY.

No code was re-written; no credit is claimed here for authoring the test modules -- that work, and its own
task-level correctness, belongs to the prior session's two submodule commits, cited by hash throughout.

## Accomplishments

- **PROV-05 verified, not re-fixed.** `doc/lockable-proms.md:295-296` already states the AT28C16 /
  AT28C64B / AT28C256 SDP-capability distinction correctly -- landed by **Phase 121 plan `121-13`, commit
  `c3c9424`** ("docs(121-13): ship the corrected lockable-proms reference..."), which pre-dates Phase 136.1
  and the entire v1.30 milestone. `PROV-05`'s own text (a maintainer memory note claiming the file *still*
  said "Atmel AT28C16 / 64 / 256" uniformly) was itself the stale artifact, not the doc. This is recorded
  plainly in both the new test module's docstring and in `REQUIREMENTS.md`'s own `PROV-05` entry, naming
  the prior commit explicitly rather than letting this plan silently look like the origin of the fix.
- `firestarter_app` commit `c9f98b8` added `tests/test_lockable_proms_doc_claims.py` (4 tests): asserts the
  `:295` AT28C64B/AT28C256 SDP-capable row, the `:296` AT28C16/plain-AT28C64 not-SDP-capable row plus its
  explicit "unlike AT28C64B/AT28C256" contrast clause, a narrow negative regex for the historical wrong
  shorthand (`AT28C16 / 64 / 256`) absent from the doc, and the same regex absent from every other
  `*.md`/`*.py`/`*.txt` file in the whole `firestarter_app` tree (checked, zero hits outside the corrected
  row itself, which draws the distinction rather than repeating the error).
- **PROV-06 measured fresh, not copied.** `firestarter_app` commit `31b5d74` added
  `tests/test_b15_page_size_corroboration.py` (4 tests): reads `chip_database.json`'s own
  `programming.protect_on_after` (bit 15, Plan 136.1-01) and `programming.infoic_page_size_raw` (raw
  upstream page_size, also Plan 136.1-01) for all 84 `algorithm==13` entries, with no `.get()` default (a
  missing key raises `KeyError` loudly). A non-vacuity check against 5 hand-constructed synthetic pairs
  (2 hand-counted disagreements) passes *before* the real-data assertion is trusted. Measured against the
  real, committed `chip_database.json`: **12 of 84 entries disagree**, every one named by
  `manufacturer/part_number` key in the module's own docstring and cross-checked by exact set (not just
  count) in the test body. This confirms Phase 120's original "12 of 84" figure via an **independently
  different methodology** -- this test's per-row single-value comparison vs. Phase 120's cross-token-set
  matching across possibly-multiple XML `<ic>` elements per alias -- named as a corroborating agreement,
  never assumed to have to hold.
- `firestarter/sdp_capability.py` (Plan 136.1-02's file this wave) was never touched: `git diff --stat
  dc5bfbe..HEAD -- firestarter/sdp_capability.py` is empty across both task commits, and
  `test_sdp_capability_module_untouched_this_plan` asserts its existing "12 of the 84" docstring prose
  stays unedited.
- **Re-verified this session, fresh:** the two new test files pass in isolation (8/8); the **full suite
  passes 1504 (up from 1496 after Plan 136.1-02, +8 -- exactly the two new test modules' test counts, 0
  unrelated delta)**; `tools/ci_replica_venv.sh` (all 5 legs, `CI-REPLICA: PASS`) measured **mypy errors: 33
  (watermark: 35), checked 132 source files** (up from 130 -- the two brand-new test files mypy now scans;
  the error *count* and watermark headroom are unchanged at 2) and `ruff check`/`ruff format --check` clean
  on both new files.

## Task Commits

Both task commits were made by the prior (crashed) session, inside the `firestarter_app` submodule, before
this session began. Cited here by hash and independently re-verified this session (see "How this plan was
actually executed" above); no new task-level commits were made this session.

1. **Task 1: Verify and durably gate doc/lockable-proms.md section 17's AT28C16/64/256 correction
   (PROV-05)** - submodule `c9f98b8` (test), dated 2026-08-05T14:49:52Z
2. **Task 2: Refute the "b15 ≈ page-write family marker" equivalence in-tree, measured (PROV-06)** -
   submodule `31b5d74` (test), dated 2026-08-05T14:50:07Z

**This session's own bookkeeping commits (meta repo):**
- `7991748` (chore: bump `firestarter_app` gitlink to `31b5d74`)
- `a36206a` (docs: mark `PROV-05`, `PROV-06` Complete in `REQUIREMENTS.md` + tick `ROADMAP.md`)
- (SUMMARY + STATE.md commits follow this one, see completion report)

## Files Created/Modified

- `firestarter_app/tests/test_lockable_proms_doc_claims.py` - durable regression gate for
  `doc/lockable-proms.md` section 17's AT28C16/AT28C64B/AT28C256 correction (created by the prior session,
  commit `c9f98b8`; verified this session)
- `firestarter_app/tests/test_b15_page_size_corroboration.py` - measured, named, non-vacuous refutation of
  the b15-vs-page_size equivalence (created by the prior session, commit `31b5d74`; verified this session)
- `.planning/REQUIREMENTS.md` - `PROV-05`/`PROV-06` ticked Complete, with the stale-premise finding
  recorded in `PROV-05`'s own text and both entries' `Evidence:` lines citing the exact commits/tests
- `.planning/ROADMAP.md` - `136.1-03-PLAN.md` checkbox ticked
- `.planning/STATE.md` - Current Position, plan-03 narrative section, frontmatter `stopped_at` /
  `last_activity_desc` / `progress` (41/42 plans, 98%) updated

## Decisions Made

- Did not redo, re-author, or re-commit Task 1/Task 2's work after confirming both commits independently
  satisfy every acceptance criterion in `136.1-03-PLAN.md` -- redoing already-correct, already-committed
  work would have been wasteful and would have obscured the true authorship of the prior session's commits.
- Recorded the "committed NOTHING" discrepancy plainly (see "How this plan was actually executed") rather
  than silently absorbing the prior session's commits as if this session had produced them, or silently
  trusting the dispatch note's stale claim without checking the submodule directly.
- PROV-05's stale-premise finding was written into `REQUIREMENTS.md`'s own requirement text (not just this
  SUMMARY) so a future reader of the requirements file alone sees the honest history without needing to
  cross-reference this SUMMARY.
- Ran the full suite and `tools/ci_replica_venv.sh` fresh this session (neither had been recorded for this
  plan anywhere) rather than assuming the prior session's local, unrecorded test run (which produced the
  8-test pass implied by the commit messages) was sufficient evidence on its own.

## Deviations from Plan

**None in the code sense** -- both task commits, independently reviewed line-by-line against
`136.1-03-PLAN.md`'s acceptance criteria, satisfy every one exactly as written: PROV-05's docstring names
commit `c3c9424`/plan `121-13` explicitly, the whole-tree grep is recorded with zero hits, the SUMMARY (this
one) states which branch fired ("already-correct, verified" -- the expected branch per the plan's own
`read_this_before_you_start` framing); PROV-06's non-vacuity check runs before the real-data assertion,
the measured count (12) is asserted as a literal with every disagreeing chip named, the methodology
discrepancy discussion is present, and `firestarter/sdp_capability.py`'s diff-stat stayed empty throughout.

**The one real deviation is procedural, not code-level:** this plan's execution was split across two
sessions by an unrelated infrastructure failure (a transient API 529 in the first session, per the
dispatch note), and the second session (this one) found the first session's task-level work already
correct and complete but its plan-level bookkeeping entirely undone. This is documented in full under "How
this plan was actually executed" above rather than filed as a Rule 1/2/3 auto-fix, since no bug was fixed
and no code changed -- the deviation is entirely about *which session did which part of the work*, and is
recorded for the same reason this project records every other stale premise: so no later reader
mis-attributes authorship or re-verifies work that was already correct.

**Total deviations:** 0 code-level; 1 procedural (session-split bookkeeping gap, documented above).
**Impact on plan:** None on correctness -- every acceptance criterion in `136.1-03-PLAN.md` is satisfied
by the commits already on disk, independently re-verified this session.

## Issues Encountered

None beyond the procedural session-split noted above. No blockers, no auth gates, no checkpoints.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`PROV-05` and `PROV-06` are Complete -- all six `PROV-0X` requirements this phase owns are now Complete
(`PROV-01`..`PROV-04` from Plans `136.1-01`/`136.1-02`, `PROV-05`/`PROV-06` from this plan). The 43/41/84
SDP partition remains unchanged and re-confirmed (this plan touches no partition-affecting file). Plan
`136.1-04` (phase close) can now run its final CI-parity record and independent 43/41/84 re-assertion
against a clean 4/4-plans-complete state. No blockers for the next plan.

**One fact Plan `136.1-04` or Phase 137 may want to fold into the honesty ledger:** `PROV-05` as originally
written in `REQUIREMENTS.md` described a defect that did not exist in the file at verification time --
the error had already been corrected by Phase 121, five phases and one milestone number earlier. This is
not a criticism of this milestone's scoping (the requirement was written from a good-faith, if stale,
memory of a real historical error, and the durable test this plan adds has genuine, independent value going
forward) but it is a fact worth stating in any close-time accounting of "what did this milestone actually
fix" versus "what did this milestone verify was already fixed."

---
*Phase: 136.1-sdp-partition-provenance*
*Completed: 2026-08-05*

## Self-Check: PASSED

All created/referenced files confirmed present on disk (`136.1-03-SUMMARY.md`,
`firestarter_app/tests/test_lockable_proms_doc_claims.py`,
`firestarter_app/tests/test_b15_page_size_corroboration.py`). All referenced commit hashes confirmed
present via `git cat-file -e` in their respective repos (meta: `7991748`, `a36206a`, `82342474`;
submodule `firestarter_app`: `c9f98b8`, `31b5d74`). No missing items.
