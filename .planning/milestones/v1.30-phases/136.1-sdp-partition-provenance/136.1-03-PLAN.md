---
phase: 136.1-sdp-partition-provenance
plan: 03
type: execute
wave: 2
depends_on: ["136.1-01"]
files_modified:
  - firestarter_app/doc/lockable-proms.md
  - firestarter_app/tests/test_lockable_proms_doc_claims.py
  - firestarter_app/tests/test_b15_page_size_corroboration.py
autonomous: true
requirements: [PROV-05, PROV-06]
must_haves:
  truths:
    - "doc/lockable-proms.md section 17's AT28C16/plain-AT28C64 correction (already landed by Phase 121 plan 121-13, commit c3c9424, PRE-DATING this phase) is verified present and is now durably gated by an automated test -- this plan does not silently re-claim credit for a fix Phase 121 already shipped; it names that prior commit explicitly."
    - "No OTHER stale copy of the wrong blanket claim ('AT28C16 / 64 / 256' uniformly SDP-capable) exists anywhere else in firestarter_app -- checked, not assumed."
    - "The b15-vs-page_size disagreement count on the 84 protocol-0x0D entries is MEASURED fresh from chip_database.json's own protect_on_after / infoic_page_size_raw fields (shipped by Plan 136.1-01), never copied uncritically from Phase 120's or this plan's own prose -- if the measured count differs from Phase 120's originally-reported 12, the discrepancy is named and reasoned about (methodology: per-row single value here vs. Phase 120's cross-token-set matching), never silently forced to match."
    - "Every disagreeing chip is named explicitly (auditable), and a non-vacuity check proves the counting logic itself is not accidentally hardcoded or vacuously correct."
  artifacts:
    - path: "firestarter_app/tests/test_lockable_proms_doc_claims.py"
      provides: "durable, automated regression gating PROV-05's doc correction"
    - path: "firestarter_app/tests/test_b15_page_size_corroboration.py"
      provides: "the in-tree, measured refutation of the b15-approx-page-write-family-marker equivalence (PROV-06)"
  key_links:
    - "This plan reads chip_database.json's protect_on_after and infoic_page_size_raw fields, which only exist once Plan 136.1-01 has run -- a hard data dependency."
    - "This plan does NOT touch firestarter/sdp_capability.py (Plan 136.1-02 owns that file this phase) -- it deliberately leaves that module's existing '12 of the 84' docstring comment (:32-36) unedited, citing the archived meta-repo doc; the NEW test this plan adds is the enforceable, in-tree proof, and the docstring's supplementary color is left as-is rather than creating a same-wave file conflict with Plan 136.1-02."
---

<objective>
Verify (and durably gate, rather than silently re-author) `doc/lockable-proms.md` section 17's
AT28C16/plain-AT28C64 correction, and refute the "b15 approximates a page-write family marker" equivalence
in-tree with a measured, named, non-vacuous test built on Plan 136.1-01's new
`protect_on_after`/`infoic_page_size_raw` fields.

Purpose: PROV-05 and PROV-06 both name honest-correction requirements -- one that turns out to have already
been fixed once (Phase 121), and one whose only existing evidence today is a docstring comment, not an
enforceable test. This plan makes both durable and this phase's own honest record about which is which.
Output: two new, narrow, single-purpose test modules; `doc/lockable-proms.md` unchanged if it is already
correct (recorded as such), corrected if it is not.
</objective>

<repo_topology>
All code in this plan lives inside the `firestarter_app` git submodule -- `cd /workspaces/firestarter_app`
and commit THERE, on `gsd/v1.30-sdp-surface-retirement`. Verify with
`git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` before the first edit, and confirm Plan
`136.1-01`'s fields are present -- `grep -c infoic_page_size_raw firestarter/data/chip_database.json` must
be non-zero -- before starting Task 2.

The top-level `/workspaces/firestarter/` directory is the **Arduino firmware submodule and is not touched
by this plan at all.**
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-VALIDATION.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-01-SUMMARY.md
@.planning/notes/infoic-xml-protection-flags-research.md
</context>

## Requirements this plan may mark Complete

**MAY tick:** PROV-05, PROV-06.

**Contributes to but MUST NOT tick:** none -- both are fully discharged here.

**MUST NOT touch, re-tick, or re-evidence:** PROV-01 (already `[x]`), PROV-02, PROV-03, PROV-04 (Plan
`136.1-02`'s), and none of `GATE-*`, `RETIRE-*`, `LEG-*`, `RELOCK-*`, `CHAN-*`, or `CLOSE-*`. This plan must
NOT edit `firestarter/sdp_capability.py` -- that file is Plan `136.1-02`'s this wave; touching it here
would create a same-wave file conflict with no real dependency behind it.

<tasks>

<task type="auto">
  <name>Task 1: Verify and durably gate doc/lockable-proms.md section 17's AT28C16/64/256 correction (PROV-05)</name>
  <files>firestarter_app/doc/lockable-proms.md, firestarter_app/tests/test_lockable_proms_doc_claims.py</files>
  <read_first>
    - `firestarter_app/doc/lockable-proms.md` `:289-306` (full section 17) -- the current, already-corrected
      text at `:295-296`.
    - `git -C /workspaces/firestarter_app log -p --follow -- doc/lockable-proms.md` (search for
      `AT28C16`/`AT28C64`) -- confirms commit `c3c9424` ("docs(121-13): ship the corrected lockable-proms
      reference and state the always-writes reality...") already landed this exact correction, well before
      this phase existed.
    - REQUIREMENTS.md's PROV-05 text -- the claim this task must verify, not blindly re-satisfy.
  </read_first>
  <action>
State plainly, up front, in the new test module's docstring: measured 2026-08-05,
`doc/lockable-proms.md` section 17 (`:295-296`) ALREADY states the corrected fact -- `AT28C16`
(incl. `AT28C16E,F`) and plain `AT28C64` measure b15=0 / not SDP-capable, distinct from `AT28C64B`/
`AT28C256` (b15=1, SDP-capable) -- landed by Phase 121 plan 121-13, commit `c3c9424`, well before this
phase (136.1) existed. This task's job is to VERIFY that correction is still present and make it a
durable, automated regression, not to re-author already-correct prose. Name the prior commit explicitly;
do not silently claim this phase originated the fix.

Grep the WHOLE `firestarter_app` tree (not just this one doc -- README, other `doc/*.md`, CLI help
strings) for the wrong blanket-claim shape (all three of `AT28C16`, `AT28C64`, `AT28C256` named together
with no qualifying distinction between the B-suffix/non-B-suffix parts) to confirm no OTHER stale copy
exists. Record the (expected) zero-hit result in the test module's docstring.

Author `tests/test_lockable_proms_doc_claims.py`: reads `doc/lockable-proms.md` as text; asserts BOTH
corrected facts are present verbatim-in-substance: (a) a line naming `AT28C64B` and `AT28C256` together
with wording indicating SDP capability (the existing `:295` row), and (b) a line naming `AT28C16` and
"plain AT28C64" together with wording indicating NO SDP command decoder / not SDP-capable (the existing
`:296` row). Also assert the ABSENCE of a precisely-defined wrong-claim regex (three part numbers named
together as uniformly capable, with no B-suffix/non-B-suffix distinction) -- define this regex narrowly
enough that it is a real, checkable negative, not a vague aspiration that could never fire.

If (surprisingly, contrary to the read_first finding) either assertion fails against the live file, fix
`doc/lockable-proms.md` section 17 to state the correction -- but the SUMMARY must state explicitly
which branch fired: "already correct, verified" (expected) or "found stale, corrected here" (unexpected;
if this happens, name exactly what was wrong).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_lockable_proms_doc_claims.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_lockable_proms_doc_claims.py -o addopts="" -q` passes.
    - The test module's docstring names commit `c3c9424` and plan `121-13` explicitly as the prior,
      already-landed fix.
    - The whole-tree grep for the wrong blanket-claim shape (outside the one corrected row) returns 0,
      recorded in the docstring.
    - The SUMMARY states explicitly which branch fired (already-correct-verified, or found-and-corrected).
  </acceptance_criteria>
  <done>doc/lockable-proms.md section 17's AT28C16/64/plain-AT28C64 correction is verified present and is
  now durably gated by an automated test, with its true prior authorship (Phase 121, not this phase)
  recorded honestly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refute the "b15 ≈ page-write family marker" equivalence in-tree, measured (PROV-06)</name>
  <files>firestarter_app/tests/test_b15_page_size_corroboration.py</files>
  <read_first>
    - `firestarter_app/firestarter/sdp_capability.py` `:32-36` -- the existing docstring claim ("Bit 15 is
      not a page-write proxy -- it disagrees with page_size &gt; 1 on 12 of the 84 entries. See
      120-SDP-PARTITION.md section 4...") -- a prose claim with no enforcing test today; this task adds
      one WITHOUT editing this file (Plan `136.1-02` owns it this wave).
    - Plan `136.1-01`'s new `chip_database.json` fields: `programming.protect_on_after` (bit 15) and
      `programming.infoic_page_size_raw` (the raw upstream page_size, NOT the existing curated
      `programming.page_size`).
    - REQUIREMENTS.md's PROV-06 text -- "12 of 84" is Phase 120's claim, computed via a DIFFERENT
      methodology (cross-token-set matching across possibly-multiple XML entries per alias) than this
      task's row-level single-value comparison; state this distinction, do not assume the two must agree.
  </read_first>
  <behavior>
    - A helper function counts, over the 84 algorithm==13 chip_database.json entries, how many have
      `protect_on_after != (infoic_page_size_raw &gt; 1)`.
    - Against a small synthetic list of `(protect_on_after, infoic_page_size_raw)` pairs with a
      hand-counted, KNOWN disagreement count, the helper returns exactly that hand-counted number --
      proving the counter is not accidentally hardcoded or vacuously correct.
    - Against the real, Plan-136.1-01-regenerated chip_database.json, the helper's count is asserted as a
      literal, MEASURED value (expected, but not blindly assumed: 12) and every disagreeing chip is named.
  </behavior>
  <action>
Author `tests/test_b15_page_size_corroboration.py`. Read `firestarter/data/chip_database.json`; select the
84 algorithm==13 entries (duplicate the selection logic locally with a one-line comment noting the
deliberate duplication rather than importing from `test_sdp_db_invariant.py` -- consistent with Task 1's
own house convention of not cross-importing between test modules). For each entry, read BOTH
`programming.protect_on_after` and `programming.infoic_page_size_raw` -- `KeyError`-loud if either is
absent (a regression check that Plan `136.1-01`'s fields survived intact).

Write a small, named counting helper: given the 84 `(protect_on_after, infoic_page_size_raw)` pairs, return
the list of `(mfr, part_number)` keys where `protect_on_after != (infoic_page_size_raw &gt; 1)`.

Non-vacuity first: call this helper against a small synthetic list of pairs you construct by hand (e.g. 5
pairs, with a hand-counted 2 disagreements), and assert the helper returns exactly that hand-counted count
and set -- proving the counting logic itself can distinguish agreement from disagreement, before trusting
it against the real data.

Then call it against the real 84-entry data. Assert the measured count as a literal in the test -- MEASURE
IT FRESH by running the helper, do not copy "12" into the assertion uncritically. If the measured count is
12, the test's docstring states this confirms Phase 120's original finding via an INDEPENDENTLY DIFFERENT
methodology (this test's per-row single value vs. Phase 120's cross-token-set matching) -- name both
methodologies explicitly, since they could in principle disagree and happen not to here. If the measured
count is NOT 12, do not force it: assert the actual measured number, name every disagreeing chip in the
test's own docstring, and record the discrepancy against Phase 120's "12" claim explicitly in this plan's
SUMMARY, reasoning about why (methodology difference, or a real finding).

Name every disagreeing chip explicitly in the test's docstring (an auditable list), matching this
project's preference for concrete facts over vague summaries.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_b15_page_size_corroboration.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_b15_page_size_corroboration.py -o addopts="" -q` passes.
    - The non-vacuity check (hand-counted synthetic pairs) is present and passes before the real-data
      assertion.
    - The test asserts a literal count that is the ACTUAL measured value against the real
      chip_database.json, documented as measured (not assumed).
    - Every disagreeing chip is named in the test's docstring.
    - The SUMMARY states explicitly whether the measured count matched Phase 120's "12" claim, and if not,
      explains the methodology difference.
    - `git -C /workspaces/firestarter_app diff --stat -- firestarter/sdp_capability.py` is empty -- this
      task never touches that file.
  </acceptance_criteria>
  <done>The "b15 ≈ page-write family marker" equivalence is refuted in-tree by a measured, named,
  non-vacuous test -- a reader can no longer substitute page_size for b15 and believe they share an
  axis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc/lockable-proms.md prose → operator/community reader belief | A documentation claim with no enforcing test can drift stale silently; this has already happened twice on this exact fact per REQUIREMENTS.md's own PROV-05 note |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136.1-09 | Repudiation | Re-claiming credit for a correction Phase 121 already shipped | medium | mitigate | Task 1's test docstring names commit `c3c9424` / plan `121-13` explicitly |
| T-136.1-10 | Tampering | The AT28C16/64/256 doc claim drifting stale a THIRD time (REQUIREMENTS.md notes it has already recurred twice) | high | mitigate | Task 1's test durably gates the fact in CI, not merely in prose review |
| T-136.1-11 | Repudiation | Asserting the "12 of 84" refutation without ever measuring it, or copying Phase 120's number uncritically | high | mitigate | Task 2 measures fresh, non-vacuously, and names every disagreeing chip |
| T-136.1-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this plan |
</threat_model>

<verification>
- `pytest tests/test_lockable_proms_doc_claims.py tests/test_b15_page_size_corroboration.py -o addopts="" -q`
  -- both green.
- `git -C /workspaces/firestarter_app diff --stat -- firestarter/sdp_capability.py` empty throughout.
- No file under `/workspaces/firestarter/` (firmware submodule) touched.
</verification>

<success_criteria>
- doc/lockable-proms.md section 17's correction is verified (and durably gated), with true prior
  authorship recorded honestly.
- The b15-vs-page_size refutation is measured fresh from Plan 136.1-01's own fields, named per-chip, and
  proven non-vacuous.
</success_criteria>

<output>
Create `.planning/phases/136.1-sdp-partition-provenance/136.1-03-SUMMARY.md` when done.
</output>
