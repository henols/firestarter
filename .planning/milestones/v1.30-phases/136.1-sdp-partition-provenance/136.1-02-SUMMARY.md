---
phase: 136.1-sdp-partition-provenance
plan: "02"
subsystem: chip-database-provenance
tags: [sdp, chip_database, gate-hardening, provenance, ci-gate]

requires:
  - phase: 136.1-sdp-partition-provenance
    plan: "01"
    provides: "chip_database.json's committed programming.protect_on_after field
      (infoic.xml flags bit 15, MP_PROTECT_AFTER), decoded and shipped by
      Plan 136.1-01 -- the hard data dependency this plan reads directly"
  - phase: 131-gate-hardening-ci-parity
    provides: "GATE-08's original committed-snapshot form
      (test_sdp_partition_matches_committed_allow_list_element_wise), which
      this plan adds a second, independent proof alongside, never replaces"
  - phase: 120-host-cli-surface-wire-emission-capability-refusal
    provides: "120-derive-sdp-allowset.py's exact token-matching rules
      (exact part_number token, strip only @PACKAGE, keep parentheticals),
      preserved verbatim in the new tools/derive_sdp_partition.py"
provides:
  - "GATE-08 leg 1: test_sdp_partition_matches_infoic_derived_field_element_wise,
    a genuinely infoic.xml-derived comparison (SDP_CAPABLE_TOKENS transcription
    vs. chip_database.json's own protect_on_after field), added alongside
    (never instead of) the hand-curated snapshot check"
  - "A live, verbatim seen-to-fail demonstration on the real committed
    chip_database.json (136.1-02-SEEN-TO-FAIL.md), proving the re-pointed
    gate is capable of catching a real planted single-chip re-bucketing"
  - "firestarter_app/tools/derive_sdp_partition.py -- a committed,
    fetch-based, reproducible-from-a-clean-checkout independent re-derivation
    script, run once and proven zero-disagreement at 43/41/84"
affects: ["136.1-04 (phase close, final 43/41/84 re-assertion)", "137-close (claim gate honesty ledger)"]

tech-stack:
  added: []
  patterns:
    - "Two-independent-proofs GATE-08 pattern: the hand-curated
      _COMMITTED_SDP_ALLOW_ENTRIES snapshot and the new
      _partition_from_protect_on_after_field DB-field derivation are BOTH
      kept, comparing production transcription against each independently
      -- neither replaces the other, since two proofs are strictly stronger
      than one and the snapshot costs nothing to keep"
    - "_assert_two_partitions_match generalizes _assert_partition_matches_committed
      for comparing two MEASURED sources (as opposed to a measured source
      against a committed literal), naming both directions of the symmetric
      difference"

key-files:
  created:
    - firestarter_app/tools/derive_sdp_partition.py
    - .planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md
  modified:
    - firestarter_app/tests/test_sdp_db_invariant.py
    - firestarter_app/firestarter/sdp_capability.py

key-decisions:
  - "Took PROV-02's static-transcription-plus-equality-gate branch, NOT the
    runtime-derive branch: SDP_CAPABLE_TOKENS stays an untouched frozenset
    literal, because tools/check_sdp_capability_invariants.py's Class 2(b)
    AST gate mechanically forbids any other binding shape, and reading
    chip_database.json at runtime would reopen the exact permit-by-default
    hole that gate exists to close (a user's ~/.firestarter/database.json
    override could otherwise vote itself ALLOW)"
  - "Kept both GATE-08 comparisons (hand-curated snapshot + DB-field
    derivation) rather than replacing the snapshot -- two independent
    proofs are strictly stronger, and the snapshot is cheap insurance"
  - "PROV-03's seen-to-fail demonstration was performed on the REAL
    committed chip_database.json (not only a synthetic in-memory fixture),
    per hard requirement 7 -- flipped ATMEL/AT28C256's protect_on_after
    true->false, captured the verbatim FAIL naming that exact chip, then
    reverted byte-identically (git diff --stat empty) and re-confirmed 9/9
    green"
  - "tools/derive_sdp_partition.py duplicates _select_0x0d_chips locally
    rather than importing it from tests/ -- the script must be
    self-contained and never coupled to the test suite's internals"

patterns-established:
  - "A DB-field-derived partition helper reads the field with NO .get()
    default (chip['programming']['protect_on_after'], not .get(...)) so a
    future regeneration that regresses and drops the field raises KeyError
    loudly instead of silently reading as REFUSE"

requirements-completed: [PROV-02, PROV-03, PROV-04]

coverage:
  - id: D1
    description: "GATE-08 re-pointed at a genuinely infoic.xml-derived source:
      the production SDP_CAPABLE_TOKENS transcription is proven equal,
      element-wise, to chip_database.json's own protect_on_after field
      (Plan 136.1-01), both independently measuring 43/41/84 -- ADDED
      alongside, not instead of, the existing hand-curated snapshot check"
    requirement: "PROV-02"
    verification:
      - kind: unit
        ref: "tests/test_sdp_db_invariant.py::test_sdp_partition_matches_infoic_derived_field_element_wise (9 passed total, firestarter_app commit 73739d5)"
        status: pass
      - kind: unit
        ref: "tools/check_sdp_capability_invariants.py -- PASS, SDP_CAPABLE_TOKENS bound exactly 1 time, unchanged (D-14 Class 2(b) gate untouched)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The re-pointed gate is SEEN to fail under a real, planted
      single-chip re-bucketing on the actual committed chip_database.json
      (ATMEL/AT28C256's protect_on_after flipped true->false), message
      recorded verbatim, then the file restored byte-identically"
    requirement: "PROV-03"
    verification:
      - kind: other
        ref: ".planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md -- verbatim FAIL naming ATMEL/AT28C256,..., clean revert (git diff --stat empty), 9/9 re-pass"
        status: pass
      - kind: unit
        ref: "tests/test_sdp_db_invariant.py::test_partition_flags_a_moved_chip_via_db_field_non_vacuous (synthetic non-vacuity proof, firestarter_app commit 73739d5)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The Phase 120 derivation is committed into firestarter_app
      (not stranded in the archived 120-* phase directory), pinned to
      minipro a8efaedc236c1d9718bd28299dfbb99536b010ff, documented as
      requiring a fetch, and proven today to reproduce 43/41/84 with zero
      disagreement against both sdp_capability_for_entry and the committed
      protect_on_after field"
    requirement: "PROV-04"
    verification:
      - kind: other
        ref: "INFOIC_XML_PATH=<cached> python3 tools/derive_sdp_partition.py (firestarter_app commit dc5bfbe) -- PASS: 43/41/84, zero disagreement against both comparison targets, exit 0; grep -c scratchpad tools/derive_sdp_partition.py == 0"
        status: pass
    human_judgment: false

duration: 22min
completed: 2026-08-05
status: complete
---

# Phase 136.1 Plan 02: Re-point GATE-08 at Infoic-Derived Provenance Summary

**GATE-08 now proves the static `SDP_CAPABLE_TOKENS` transcription equal, element-wise, to
`chip_database.json`'s own infoic-derived `protect_on_after` field -- observed live to catch a real
planted single-chip re-bucketing -- and the underlying Phase 120 derivation ships as a committed,
fetch-based script proven today to reproduce 43/41/84 with zero disagreement.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-08-05T14:14:37Z
- **Completed:** 2026-08-05T14:36:32Z
- **Tasks:** 3 completed (3/3)
- **Files modified:** 6 (2 submodule modified, 1 submodule created, 2 meta-repo created, 1 meta-repo
  gitlink bump; plus REQUIREMENTS.md/ROADMAP.md ticks and this SUMMARY)

## Accomplishments

- `tests/test_sdp_db_invariant.py` gained GATE-08 leg 1: `_partition_from_protect_on_after_field(db)`
  reads `chip["programming"]["protect_on_after"]` directly (no `.get()` default -- a missing key must
  raise `KeyError`, never silently read as REFUSE) and `_assert_two_partitions_match` compares it
  against the production `SDP_CAPABLE_TOKENS`-based transcription
  (`test_sdp_partition_matches_infoic_derived_field_element_wise`) -- both sides independently measure
  43 ALLOW / 41 REFUSE / 84 total. Its own non-vacuity proof
  (`test_partition_flags_a_moved_chip_via_db_field_non_vacuous`) flips a synthetic chip's
  `protect_on_after` field and confirms the raised message names the moved chip and not the control.
  The existing hand-curated `_COMMITTED_SDP_ALLOW_ENTRIES` snapshot and its comparator
  (`_assert_partition_matches_committed`) are kept byte-identical (verified: `git diff` for this file
  shows additions only, zero removed lines) -- both proofs stay; this closes the gap that constant's
  own comment has named since Phase 131 ("leg 1... not implementable... chip_database.json contains
  ZERO occurrences of the string flags").
- `firestarter/sdp_capability.py`'s comment block above `SDP_CAPABLE_TOKENS` gained one sentence
  documenting the new equality proof and restating why the frozenset still cannot read that field at
  runtime. `SDP_CAPABLE_TOKENS`'s literal contents and binding shape are untouched;
  `tools/check_sdp_capability_invariants.py` re-confirmed `PASS` (bound exactly 1 time, 0
  permit-by-default, 0 widenable-allow-set violations) and its own diff-stat is empty -- the file was
  never edited.
- **PROV-03's seen-to-fail demonstration ran on the real committed file, not only a synthetic
  fixture.** `ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L`'s
  `protect_on_after` was flipped `true`->`false` directly in `firestarter/data/chip_database.json`;
  the new gate FAILED, naming that exact chip key verbatim (captured in full in
  `136.1-02-SEEN-TO-FAIL.md`); the file was then reverted (`git checkout --`), confirmed
  byte-identical (`git diff --stat` empty), and the full `test_sdp_db_invariant.py` suite re-ran green
  (9/9) before any further commit.
- `firestarter_app/tools/derive_sdp_partition.py` (PROV-04): a standalone, network-capable script,
  never imported by production code or the pytest suite, pinned to minipro
  `a8efaedc236c1d9718bd28299dfbb99536b010ff` (duplicating `build_db.py`'s `MINIPRO_XML_URL` verbatim,
  with a lockstep comment). Reads `INFOIC_XML_PATH` if set (a caller convenience, never hardcoded in
  the script's own source -- `grep -c scratchpad tools/derive_sdp_partition.py` returns 0) or fetches
  live otherwise, mirroring `build_db.py`'s own default. Preserves Phase 120's exact token rule
  verbatim (exact `part_number` token, strip only `@PACKAGE`, keep parentheticals), cross-references
  both `sdp_capability_for_entry` (production) and the committed `protect_on_after` field
  independently. Run once against the cached, verified pinned-commit XML copy: **43 ALLOW / 41
  REFUSE / 84 total, zero disagreement against both comparison targets, exit 0.**
- `tools/ci_replica_venv.sh` re-measured this wave: **mypy errors: 33 (watermark: 35), checked 130
  source files** -- byte-identical to Plan 136.1-01's post-PROV-01 baseline, headroom flat at 2. Full
  suite via the ci-replica python: **1496 passed** (up from 1494 by exactly the two new tests), 30
  snapshots passed, coverage **82.14%** -- unchanged (the added lines are either test-only or
  non-executable comments).

## Task Commits

Two repos are involved (meta + `firestarter_app` submodule); hashes are qualified.

1. **Task 1: Re-point GATE-08 -- add the infoic-derived-field comparison, keep the existing snapshot
   check** - submodule `73739d5` (test)
2. **Task 2: Live seen-to-fail demonstration on the real committed file, then byte-identical restore**
   - meta `c897b187` (docs, `136.1-02-SEEN-TO-FAIL.md`) -- no submodule commit: the plant was reverted
     before any commit, per the task's own instruction
3. **Task 3: Commit the reproducible, fetch-based independent re-derivation script (PROV-04)** -
   submodule `dc5bfbe` (feat)

**Bookkeeping:** meta `3d322102` (chore: bump `firestarter_app` gitlink to `dc5bfbe`)

**Plan metadata:** meta `a334a0c3` (docs: mark PROV-02/03/04 Complete)

## Files Created/Modified

- `firestarter_app/tests/test_sdp_db_invariant.py` - added
  `_partition_from_protect_on_after_field`, `_assert_two_partitions_match`, and two new tests (8/9);
  existing 7 tests and the committed snapshot constant left byte-identical
- `firestarter_app/firestarter/sdp_capability.py` - one sentence added to the `SDP_CAPABLE_TOKENS`
  comment block; the frozenset itself untouched
- `firestarter_app/tools/derive_sdp_partition.py` - new, standalone, fetch-based independent
  re-derivation script (PROV-04)
- `.planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md` - verbatim seen-to-fail
  transcript (PROV-03)

## Decisions Made

- Took PROV-02's static-transcription-plus-equality-gate branch, not the runtime-derive branch (see
  `key-decisions` in frontmatter for the full mechanical reason:
  `tools/check_sdp_capability_invariants.py`'s Class 2(b) AST gate).
- Kept both GATE-08 comparisons rather than replacing the hand-curated snapshot -- two independent
  proofs are strictly stronger than one, and the snapshot costs nothing to keep.
- Ran PROV-03's seen-to-fail demonstration on the real committed `chip_database.json`, not only a
  synthetic in-memory fixture, matching the requirement's explicit "seen to fail... with the observed
  message recorded" wording.
- `tools/derive_sdp_partition.py` duplicates `_select_0x0d_chips` locally (never imports from
  `tests/`) so the script stays fully standalone.

## Deviations from Plan

None - plan executed exactly as written. No Rule 1/2/3 auto-fixes were needed; no architectural
questions arose; no authentication gates were hit.

## Issues Encountered

None. The background `tools/ci_replica_venv.sh` run took longer than usual (coverage instrumentation
overhead) but completed cleanly with the expected byte-identical numbers.

## User Setup Required

None - no external service configuration required. `tools/derive_sdp_partition.py`'s live-fetch path
was not exercised this run (the cached, previously-verified pinned-commit XML copy was used instead,
per the plan's own instruction to avoid a redundant fetch); the script's fetch path mirrors
`build_db.py`'s already-proven-working default and needs no new setup.

## Next Phase Readiness

PROV-02, PROV-03, and PROV-04 are Complete. `chip_database.json`'s `protect_on_after` field, GATE-08's
new leg 1, and `tools/derive_sdp_partition.py` are all available for Plan `136.1-03` (PROV-05/06,
independent, wave 2, no file overlap with this plan) and Plan `136.1-04`'s phase-close CI-parity
record and final 43/41/84 re-assertion. The 43/41/84 split is unchanged and re-confirmed by three
independent measurements this plan alone: the production transcription, the DB field, and the fresh
XML re-derivation. No blockers for the next plan.

---
*Phase: 136.1-sdp-partition-provenance*
*Completed: 2026-08-05*

## Self-Check: PASSED

All created files confirmed present on disk (`firestarter_app/tools/derive_sdp_partition.py`,
`.planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md`); all referenced commit
hashes confirmed present via `git cat-file -e` in their respective repos (submodule `firestarter_app`:
`73739d5`, `dc5bfbe`; meta: `c897b187`, `3d322102`, `a334a0c3`). No missing items.
