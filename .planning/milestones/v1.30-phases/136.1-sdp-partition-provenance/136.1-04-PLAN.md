---
phase: 136.1-sdp-partition-provenance
plan: 04
type: execute
wave: 3
depends_on: ["136.1-02", "136.1-03"]
files_modified:
  - .planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The 43 ALLOW / 41 REFUSE / 84 total SDP partition is re-confirmed unchanged at phase close, via BOTH gates -- the production-transcription-vs-hand-curated-snapshot check AND the new production-transcription-vs-infoic-derived-DB-field check (Plan 136.1-02) -- and the ALLOW list is diffed byte-for-byte against the ORIGINAL 120-sdp-partition.json committed entries one final time."
    - "tools/derive_sdp_partition.py (Plan 136.1-02) is re-run once more as the phase's closing, independent, from-scratch confirmation, and its PASS output at 43/41/84 with zero disagreement is recorded."
    - "136.1-CI-PARITY.md carries all three sections -- Before, After PROV-01, After (phase close) -- with an explicit, measured mypy/lint/test-count delta across the WHOLE phase, never inherited or estimated."
    - "No file under /workspaces/firestarter/ (firmware submodule) was touched anywhere in this phase; no new pip/npm/cargo package was installed."
  artifacts:
    - path: ".planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md"
      provides: "the complete Before / After-PROV-01 / After-phase-close CI-parity record"
  key_links:
    - "This plan's whole purpose is the promise stated at the top of this phase's ROADMAP entry and REQUIREMENTS.md section: 'this phase changes provenance, never verdicts.' This is the single task in the whole phase that re-proves that promise one more time, independently, after every other plan has landed."
---

<objective>
Close the phase: re-assert, one final time and independently, that the 43/41/84 SDP partition is
byte-for-byte unchanged from before this phase began, and record the phase's full post-close CI-parity
and mypy state so the milestone's own honesty ledger (Phase 137) has a real, measured number for this
phase's total cost -- never an estimate.

Purpose: this phase's entire premise, stated in its own ROADMAP entry and in REQUIREMENTS.md's PROV
section header, is that it changes PROVENANCE and never VERDICTS. Every prior plan in this phase proved
this LOCALLY (Plan 136.1-01's blast-radius check, Plan 136.1-02's re-pointed gate and live seen-to-fail
demonstration). This plan proves it GLOBALLY, one more time, with the whole phase's changes landed,
because a promise this central to the phase's own justification deserves a check that runs after
everything else, not only after each individual step.
Output: `136.1-CI-PARITY.md`'s closing `## After (phase close)` section, and a final, recorded
confirmation that the ALLOW/REFUSE partition never moved.
</objective>

<repo_topology>
All verification in this plan runs inside the `firestarter_app` git submodule --
`cd /workspaces/firestarter_app`, on `gsd/v1.30-sdp-surface-retirement`. `136.1-CI-PARITY.md`'s closing
section is a **meta-repo** edit, committed in `/workspaces` on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof` -- the two commits are separate, in separate
repos, and must not be conflated into one. This plan makes NO production or test-file edits in the
submodule at all -- it is a pure verification capstone.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-VALIDATION.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-02-SUMMARY.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-03-SUMMARY.md
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json
</context>

## Requirements this plan may mark Complete

**MAY tick:** none new. All six `PROV-01`..`PROV-06` are already ticked by plans `136.1-01`/`136.1-02`/
`136.1-03` before this plan runs.

**Contributes to but MUST NOT tick:** none.

**MUST NOT touch, re-tick, or re-evidence:** any `PROV-*` row (all already `[x]`), and none of `GATE-*`,
`RETIRE-*`, `LEG-*`, `RELOCK-*`, `CHAN-*`, or `CLOSE-*`.

<tasks>

<task type="auto">
  <name>Task 1: Re-assert the 43/41/84 split is unchanged and record the phase's final CI-parity state</name>
  <files>.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md</files>
  <read_first>
    - This plan's own `136.1-CI-PARITY.md`'s `## Before (pre-edit)` and `## After PROV-01 (DB regen only)`
      sections (Plan `136.1-01`) -- the exact metrics to re-measure and compare against, across the WHOLE
      phase now.
    - `firestarter_app/tests/test_sdp_db_invariant.py` (as landed by Plan `136.1-02`) -- both the original
      hand-curated-snapshot tests and the new infoic-derived-field test.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json` --
      the ORIGINAL committed 43-entry ALLOW list, from before this phase, and before Phase 131's own
      `_COMMITTED_SDP_ALLOW_ENTRIES` snapshot existed. The final, most independent check this task can run
      is a byte-for-byte diff against THIS file, not merely against the in-repo snapshots this phase's own
      plans have been building on.
    - `firestarter_app/tools/derive_sdp_partition.py` (Plan `136.1-02`) -- re-run once more here.
  </read_first>
  <action>
Append a `## After (phase close)` section to `.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md`
(edit the existing file; do not overwrite the `## Before` or `## After PROV-01` sections). Run, from
`/workspaces/firestarter_app`:

1. `tools/ci_parity.sh` -- record every leg's exit status verbatim, compare against both prior sections.
2. `tools/ci_replica_venv.sh` -- record all five legs and the two certifying lines; compare the mypy count
   and `checked N source files` against `## Before`; state explicitly whether headroom grew, shrank, or
   stayed flat, and by how much, across the WHOLE phase (Plans `136.1-02`/`136.1-03` add new test files
   inside `tests/`, which IS inside mypy's/ruff's CI-scanned path -- unlike Plan `136.1-01`'s `tools/`-only
   edit, this delta must be MEASURED, not assumed zero).
3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q | tail -5` -- record the passed count
   and coverage; the count must be &gt;= the `## Before` count (Plans `136.1-02`/`136.1-03` add new tests;
   this phase deletes none).
4. Re-run `pytest tests/test_sdp_db_invariant.py -o addopts="" -q` explicitly and confirm all 9 tests pass
   -- BOTH the original hand-curated-snapshot comparison AND the new infoic-derived-field comparison agree
   the partition is 43 ALLOW / 41 REFUSE / 84 total.
5. Independently diff the measured ALLOW list one final time against
   `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json`'s
   committed `allow` array (sorted, "MFR/PART_NUMBER" keys) -- byte-for-byte, zero entries added or
   removed on either side. State explicitly: this is the SAME 43-chip list that was true before this
   phase (indeed before Phase 131) ever started.
6. Re-run `tools/derive_sdp_partition.py` once more (`INFOIC_XML_PATH` pointed at the cached scratchpad
   copy if still present, else a fresh live fetch) and record its PASS output as the phase's closing,
   independent, from-scratch confirmation of 43/41/84 with zero disagreement.
7. State plainly, quoting this phase's own opening promise: "the split stays 43 ALLOW / 41 REFUSE / 84 --
   this phase changed provenance, never verdicts" -- and confirm it, one last time, with evidence from
   steps 4-6 above.
8. State plainly: no file under `/workspaces/firestarter/` (firmware submodule) was touched at any point
   across this whole phase, and no new pip/npm/cargo package was installed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `136.1-CI-PARITY.md` contains all three headings: `## Before (pre-edit)`, `## After PROV-01 (DB
      regen only)`, `## After (phase close)`.
    - The `## After (phase close)` section contains a literal `mypy errors: &lt;N&gt; (watermark: 35)` line
      from a real `tools/ci_replica_venv.sh` run, with N &lt;= 35, and states the whole-phase headroom
      delta against `## Before` explicitly as a number.
    - The full suite passes with a count &gt;= the `## Before` count.
    - `tests/test_sdp_db_invariant.py` reports 9/9 green, both partition-comparison legs agreeing at
      43/41/84.
    - The byte-for-byte diff against `120-sdp-partition.json`'s committed `allow` array is recorded as
      EMPTY (zero entries differ).
    - `tools/derive_sdp_partition.py`'s final re-run output is recorded, confirming 43/41/84 and zero
      disagreement.
    - `git -C /workspaces/firestarter_app status --porcelain -- firestarter` (the firmware submodule)
      prints nothing across the whole phase.
  </acceptance_criteria>
  <done>The phase closes having proven, one more time and independently, that it changed provenance and
  never verdicts -- the 43/41/84 split is exactly what it was before Phase 136.1 began, and the phase's
  real cost (mypy headroom, test count, coverage) is measured and recorded, never estimated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's cumulative production edits → the milestone's mypy watermark (headroom 2 entering the phase) | The phase-wide delta, not just any single plan's, is what could exhaust the watermark |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136.1-12 | Repudiation | Claiming "verdicts unchanged" without a final, byte-for-byte, whole-phase re-check | high | mitigate | Step 5's diff against the ORIGINAL 120-sdp-partition.json, independent of every in-phase snapshot |
| T-136.1-13 | Denial of Service | Cumulative mypy/test-count drift across the whole phase silently exceeding the watermark | high | mitigate | `tools/ci_replica_venv.sh` re-run and recorded; GATE-01..04 (already shipped) fail closed if it does |
| T-136.1-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this phase |
</threat_model>

<verification>
- `pytest tests/test_sdp_db_invariant.py -o addopts="" -q` -- 9/9 green, both legs agree 43/41/84.
- `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` -- full suite green, count &gt;=
  phase-entry baseline.
- `tools/ci_replica_venv.sh` and `tools/ci_parity.sh` -- both recorded in `## After (phase close)`, mypy
  &lt;= 35.
- Byte-for-byte diff against `120-sdp-partition.json`'s committed `allow` array -- empty.
- `git -C /workspaces/firestarter_app status --porcelain -- firestarter` -- empty across the whole phase.
</verification>

<success_criteria>
- The 43/41/84 SDP partition is re-confirmed unchanged, independently, one final time.
- `136.1-CI-PARITY.md` carries Before / After-PROV-01 / After-phase-close, with a stated whole-phase
  mypy/test-count delta.
- `tools/derive_sdp_partition.py`'s closing re-run confirms zero disagreement against both comparison
  targets.
</success_criteria>

<output>
Create `.planning/phases/136.1-sdp-partition-provenance/136.1-04-SUMMARY.md` when done.
</output>
