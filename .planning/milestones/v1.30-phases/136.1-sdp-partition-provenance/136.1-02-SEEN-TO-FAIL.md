# 136.1-02: PROV-03 Seen-to-Fail Demonstration

**Requirement:** PROV-03 — "A fail-closed gate proves the committed partition equals the
`infoic.xml`-derived partition at 43/41/84, and is seen to fail under a planted single-chip
re-bucketing, with the observed message recorded."

**Date:** 2026-08-05
**Plan:** 136.1-02, Task 2
**Gate under test:**
`firestarter_app/tests/test_sdp_db_invariant.py::test_sdp_partition_matches_infoic_derived_field_element_wise`
(GATE-08 leg 1, added by this plan's Task 1, commit `73739d5`)

---

## What was planted

The real, committed `firestarter/data/chip_database.json` was edited in place — not a synthetic
in-memory fixture — flipping ONE field on ONE real ALLOW chip:

- **Chip:** `ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L`
  (the same part this milestone's own `dev test` SDP leg exercises elsewhere — LEG-06's evidence)
- **Field:** `programming.protect_on_after`
- **Change:** `true` -> `false`

```diff
--- a/firestarter/data/chip_database.json
+++ b/firestarter/data/chip_database.json
@@ -2313,7 +2313,7 @@
         "infoic_page_size_raw": 64,
         "protect_off_before": true,
-        "protect_on_after": true,
+        "protect_on_after": false,
         "pulse_duration": "Algorithm Controlled"
```

This chip's `SDP_CAPABLE_TOKENS`-based production transcription (`_partition_0x0d`) still says
ALLOW (the static frozenset was NOT touched) — only the newly-shipped DB field
(`protect_on_after`, Plan 136.1-01) was flipped. This is exactly the "single-chip re-bucketing"
scenario PROV-03 requires be observed, not merely asserted capable of.

## Command run

```bash
cd /workspaces/firestarter_app
.venv/ci-replica/bin/python -m pytest \
  tests/test_sdp_db_invariant.py::test_sdp_partition_matches_infoic_derived_field_element_wise \
  -o addopts="" -q
```

## Observed result: FAIL (exit 1)

Verbatim captured output:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_sdp_partition_matches_infoic_derived_field_element_wise _________

    def test_sdp_partition_matches_infoic_derived_field_element_wise() -> None:
        """GATE-08 leg 1 (Phase 136.1 Plan 02, PROV-02/03): the production
        transcription (`_partition_0x0d`, `SDP_CAPABLE_TOKENS`-based) must equal,
        element-wise, the partition derived directly from chip_database.json's
        own `protect_on_after` field (Plan 136.1-01) -- a genuinely
        infoic.xml-derived source, not a hand-curated snapshot.

        Closes the gap `_COMMITTED_SDP_ALLOW_ENTRIES`'s own comment has named
        since Phase 131: leg 1 was "not implementable" because chip_database.json
        contained ZERO `flags`-derived fields at that time. Plan 136.1-01 changed
        that measurement; this is leg 1, implemented for the first time.
        """
        db = json.loads(_DB_FILE.read_text(encoding="utf-8"))
        allow_transcription, refuse_transcription = _partition_0x0d(db)
        allow_field, refuse_field = _partition_from_protect_on_after_field(db)

>       _assert_two_partitions_match(
            allow_transcription,
            allow_field,
            "SDP_CAPABLE_TOKENS transcription",
            "protect_on_after field",
        )

tests/test_sdp_db_invariant.py:600:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

allow_a = ['ATMEL/AT28BV256,AT28LV256', 'ATMEL/AT28BV64B,AT28LV64B', 'ATMEL/AT28C010,AT28C010E', 'ATMEL/AT28C040,AT28C040E', 'ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L', 'ATMEL/AT28C64B,AT28HC64B,AT28HC64BF', ...]
allow_b = ['ATMEL/AT28BV256,AT28LV256', 'ATMEL/AT28BV64B,AT28LV64B', 'ATMEL/AT28C010,AT28C010E', 'ATMEL/AT28C040,AT28C040E', 'ATMEL/AT28C64B,AT28HC64B,AT28HC64BF', 'ATMEL/AT28LV010', ...]
label_a = 'SDP_CAPABLE_TOKENS transcription', label_b = 'protect_on_after field'

    def _assert_two_partitions_match(
        allow_a: list[str], allow_b: list[str], label_a: str, label_b: str
    ) -> None:
        """Raise AssertionError naming both directions of the symmetric
        difference between two MEASURED ALLOW partitions (neither one a
        "committed literal" -- a generalization of
        `_assert_partition_matches_committed` for comparing two independently
        measured sources against each other rather than against a snapshot).
        """
        set_a = set(allow_a)
        set_b = set(allow_b)
        only_in_a = sorted(set_a - set_b)
        only_in_b = sorted(set_b - set_a)
        offenders = only_in_a or only_in_b
>       assert not offenders, (
            f"GATE-08 leg 1: the '{label_a}' and '{label_b}' ALLOW partitions "
            f"disagree. Only in '{label_a}': {only_in_a}. Only in '{label_b}': "
            f"{only_in_b}."
        )
E       AssertionError: GATE-08 leg 1: the 'SDP_CAPABLE_TOKENS transcription' and 'protect_on_after field' ALLOW partitions disagree. Only in 'SDP_CAPABLE_TOKENS transcription': ['ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L']. Only in 'protect_on_after field': [].
E       assert not ['ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L']

tests/test_sdp_db_invariant.py:572: AssertionError
=========================== short test summary info ============================
FAILED tests/test_sdp_db_invariant.py::test_sdp_partition_matches_infoic_derived_field_element_wise
1 failed in 0.07s
```

The message names the exact planted chip key,
`ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L`, verbatim, in the
"Only in 'SDP_CAPABLE_TOKENS transcription'" clause — the direction where the transcription still
says ALLOW but the (planted-REFUSE) DB field now disagrees. No other chip is named. This is a real,
provable RED, not an assertion of capability.

## Revert and re-pass

```bash
cd /workspaces/firestarter_app
git checkout -- firestarter/data/chip_database.json
git diff --stat -- firestarter/data/chip_database.json   # empty
.venv/ci-replica/bin/python -m pytest tests/test_sdp_db_invariant.py -o addopts="" -q
```

- `git diff --stat -- firestarter/data/chip_database.json` printed **nothing** — the file is
  byte-identical to commit `f294821` (Plan 136.1-01's regeneration commit) after the revert.
- Re-run of the full `test_sdp_db_invariant.py` suite: **9 passed** — all 9 tests green again,
  including the re-pointed GATE-08 leg 1 test.

```
.........                                                                [100%]
9 passed in 0.06s
```

## Conclusion

PROV-03's re-pointed GATE-08 gate has been observed, on the real committed data file, to:

1. Detect a real, planted single-chip re-bucketing (a `protect_on_after` field flip) and FAIL,
   naming the exact chip and no other.
2. Return to a clean, fully-green state once the plant is reverted byte-identically.

The plant was never committed in its flipped state — the working tree returned to a clean diff
before this record was written, and before any commit for this plan's later tasks.
