---
phase: 136-dev-tools-channel-gating
plan: 03
type: execute
wave: 3
depends_on: ["136-02"]
files_modified:
  - firestarter_app/tests/test_dev_group_channel_gating.py
  - firestarter_app/tests/test_dev_gate_reads_no_firmware_source.py
autonomous: true
requirements: [CHAN-01, CHAN-02, CHAN-03, CHAN-04, CHAN-06, CHAN-07]
must_haves:
  truths:
    - "A subprocess-only harness — mirroring `tests/test_py32_channel_gating.py`'s proven `_CHILD_PROGRAM`/`_run_cli` shape — assigns `firestarter.__version__` BEFORE `firestarter.cli_handlers` is ever imported in the child process, so the simulated-stable and simulated-prerelease channels are structural facts, not in-process assumptions (D-04)."
    - "On simulated-stable, `dev --help` lists ONLY `read` and `test` (CHAN-01), `sorted(cli_handlers.dev.commands.keys())` literally excludes all six gated names (CHAN-02, proven by direct registry introspection, not by help-text absence alone), and invoking a gated name refuses with the channel-specific message at a non-zero exit while a genuine typo still gets Click's own generic message (CHAN-03)."
    - "On simulated-prerelease, `dev --help` lists all eight and `dev.commands` contains all six gated names — the positive control without which the stable-side assertions would be unfalsifiable."
    - "`dev --help` text is PINNED (asserted equal to a captured literal) on both simulated channels in the same test module (CHAN-04)."
    - "On simulated-stable WITH `FIRESTARTER_DEV_TOOLS=1` set in the child's environment before import, all six gated names ARE registered — the bench override overrides the channel signal (CHAN-06)."
    - "The gate's own new code — `channel.py`'s four symbols plus `cli_handlers.py`'s `_DevGroup`/`_DEV_TOOLS_ENABLED`/the six conditional blocks — is proven to call no `open()` and reference no firmware path, comprehensively across both files (CHAN-07)."
    - "Two named mutations — removing `cls=_DevGroup`, and hardcoding `_DEV_TOOLS_ENABLED = True` — are each observed to turn a specific assertion RED, then the source is restored byte-identically, proving the gate is load-bearing and not a coincidentally-passing test."
  artifacts:
    - firestarter_app/tests/test_dev_group_channel_gating.py
    - firestarter_app/tests/test_dev_gate_reads_no_firmware_source.py
  key_links:
    - "The child program's own preamble MUST assert `\"firestarter.cli_handlers\" not in sys.modules` before assigning `firestarter.__version__` — exactly `test_py32_channel_gating.py`'s guard — otherwise `_DEV_TOOLS_ENABLED` would be frozen against whatever channel happened to be live first, and the test would pass for the wrong reason."
    - "CHAN-02's proof must read `cli_handlers.dev.commands.keys()` directly (registry introspection), not just the absence of a name from `--help` text — a command could theoretically be registered but hidden from help while still being invokable, and only the registry check rules that out."
---

<objective>
Prove, from the outside, everything plans `136-01`/`136-02` built: the six requirements that can only
be discharged by simulating both channels in separate subprocesses. Author the dual-channel harness,
the comprehensive no-firmware-read assertion, and the two non-vacuity mutations that show the gate is
load-bearing.

Purpose: D-04 states an in-process assertion that "stable hides `reg`" tests nothing, because this
checkout's own `__version__` is always a pre-release. Every requirement this plan ticks depends on a
subprocess, never an in-process shortcut.
Output: `tests/test_dev_group_channel_gating.py`, `tests/test_dev_gate_reads_no_firmware_source.py`, six
requirement ticks, two recorded non-vacuity mutations.
</objective>

<repo_topology>
All code in this plan lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`
and commit THERE, on `gsd/v1.30-sdp-surface-retirement`. The meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`; the names deliberately diverge. The top-level
`/workspaces/firestarter/` directory is the Arduino firmware submodule and is not touched by this phase
at all.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-dev-tools-channel-gating/136-CONTEXT.md
@.planning/phases/136-dev-tools-channel-gating/136-RESEARCH.md
@.planning/phases/136-dev-tools-channel-gating/136-VALIDATION.md
@.planning/phases/136-dev-tools-channel-gating/136-01-SUMMARY.md
@.planning/phases/136-dev-tools-channel-gating/136-02-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:**
- **CHAN-01** — stable `dev --help` lists only `read`/`test`. Proven here via subprocess; the mechanism
  landed in `136-02`.
- **CHAN-02** — beta-only subcommands are gated by non-registration. Proven here via direct
  `dev.commands` registry introspection in a simulated-stable child.
- **CHAN-03** — invoking a gated subcommand refuses informatively at non-zero exit. Proven here; a
  genuine typo control rules out the "swallows all errors" failure mode.
- **CHAN-04** — `dev --help` is pinned on both channels via subprocess. This plan's harness IS that pin.
- **CHAN-06** — `dev reg`'s bench role survives via the env override. Proven here with the override set
  in a simulated-stable child.
- **CHAN-07** — the gate reads no firmware source. Proven here comprehensively across `channel.py` and
  `cli_handlers.py`'s new symbols.

**Contributes to but MUST NOT tick:** none — this plan is where the six mechanism-only contributions from
`136-01`/`136-02` become provable facts.

**MUST NOT touch, re-tick, or re-evidence:** any `GATE-*`, `RETIRE-*`, `LEG-*`, `RELOCK-*`, `PROV-*`, or
`CLOSE-*` row. `CHAN-05` is already `[x]` from plan `136-02` — do not re-tick it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subprocess dual-channel harness — CHAN-01, CHAN-02, CHAN-03, CHAN-04, CHAN-06</name>
  <files>firestarter_app/tests/test_dev_group_channel_gating.py</files>
  <read_first>
    - `firestarter_app/tests/test_py32_channel_gating.py` in full (358 lines) — the `_CHILD_PROGRAM`
      string, the `_run_cli` cached subprocess runner, and the module docstring's rationale for why a
      subprocess (not an in-process monkeypatch, not a module reload) is the only sound approach. This
      plan's harness is a direct structural adaptation of this file for the `dev` group instead of
      `_BOARD_CHOICES`.
    - `firestarter_app/firestarter/channel.py` (post plan `136-01`) — `BETA_ONLY_DEV_COMMANDS`,
      `dev_tools_enabled_by_env`, `is_dev_tools_enabled`, `dev_command_gate_message`.
    - `firestarter_app/firestarter/cli_handlers.py` (post plan `136-02`) — `_DevGroup`,
      `_DEV_TOOLS_ENABLED`, the six conditionally-registered blocks, the rewritten `dev()` docstring.
  </read_first>
  <behavior>
    - Simulated stable (`__version__ = "3.0.0"`, no env override): `dev --help` output equals a captured
      literal listing only `read` and `test`; `dev.commands.keys()` (reported by the child) excludes all
      six gated names; invoking `dev reg` exits non-zero with `channel.dev_command_gate_message("reg")`'s
      text present; invoking `dev totally-bogus-name` exits non-zero with Click's generic
      "No such command" text present and this module's refusal text ABSENT.
    - Simulated prerelease (`__version__ = "3.0.0b1"`, no env override): `dev --help` output equals a
      captured literal listing all eight commands; `dev.commands.keys()` includes all six gated names.
    - Simulated stable WITH `FIRESTARTER_DEV_TOOLS=1` in the child's environment: `dev.commands.keys()`
      includes all six gated names — the override wins over the channel signal.
  </behavior>
  <action>
Author `firestarter_app/tests/test_dev_group_channel_gating.py`, adapting
`test_py32_channel_gating.py`'s `_CHILD_PROGRAM`/`_run_cli` pattern. The child program must, in order:
import `firestarter` bare; assert `"firestarter.cli_handlers" not in sys.modules`; assign
`firestarter.__version__` to the simulated value; THEN import `click.testing.CliRunner` and
`firestarter.cli_handlers`; invoke the given argv via `CliRunner().invoke(cli_handlers.cli, argv)`; print
a JSON report containing at minimum: `version`, `dev_tools_enabled` (`cli_handlers._DEV_TOOLS_ENABLED`),
`dev_commands` (`sorted(cli_handlers.dev.commands.keys())`), `exit_code`, and `output`.

Extend the cached runner helper to accept an optional `env_overrides: dict[str, str] | None` parameter
(default `None`) that is merged into the `env=` mapping passed to `subprocess.run`, on top of
`os.environ`, so a test can additionally set `FIRESTARTER_DEV_TOOLS` in the child's environment before
Python starts (not inside the `-c` program text — the value must be visible to `os.environ.get(...)`
inside `channel.dev_tools_enabled_by_env` when it is called during import). Note explicitly in a comment
that the base `os.environ` a test does NOT override could ambiently already carry
`FIRESTARTER_DEV_TOOLS` on some developer's shell — the stable-no-override tests must explicitly pass
an env mapping that OMITS the key (build a clean copy, do not rely on the ambient shell being clean).

Write the test functions named for each behavior in the `<behavior>` block above. Additionally include:
- One test asserting the two channels' `dev --help` texts differ, and each independently equals the
  literal captured for that channel (CHAN-04's pin, both directions in one assertion body, mirroring
  `test_py32_channel_gating.py`'s
  `test_board_choices_are_computed_at_import_not_cached_across_a_version_change`).
- One test asserting `dev.commands.keys()` on simulated-stable is exactly `{"read", "test"}` (not just
  "excludes the six" — the stronger, exact-set assertion) and on simulated-prerelease is the full set of
  eight.

Cache each `(version, argv, env_overrides)` combination through the shared runner exactly once per test
function, mirroring the existing file's `functools.lru_cache` pattern (extend the cache key to include a
hashable representation of `env_overrides`, e.g. a sorted tuple of items, since `dict` is unhashable).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_group_channel_gating.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_dev_group_channel_gating.py -o addopts="" -q` collects and passes at least 10 tests.
    - At least one test asserts `dev.commands.keys()` on simulated-stable equals exactly `{"read", "test"}`.
    - At least one test asserts all six gated names ARE present when `FIRESTARTER_DEV_TOOLS=1` is set on
      a simulated-stable child.
    - At least one test asserts a genuine typo's output contains `No such command` and does NOT contain
      any of the six gated names' refusal text.
    - `pytest tests/test_py32_channel_gating.py -o addopts="" -q` remains green (regression floor for the
      pattern this plan adapts).
  </acceptance_criteria>
  <done>All six CHAN requirements this plan owns are proven true on both simulated channels, from
  outside the process, never by an in-process shortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CHAN-07 — comprehensive no-firmware-read assertion</name>
  <files>firestarter_app/tests/test_dev_gate_reads_no_firmware_source.py</files>
  <read_first>
    - `firestarter_app/firestarter/channel.py` (post plan `136-01`, full file) and
      `firestarter_app/firestarter/cli_handlers.py`'s `_DevGroup` class and `_DEV_TOOLS_ENABLED`
      assignment (post plan `136-02`) — the exact symbols this task's source-scan must cover.
    - `.planning/phases/136-dev-tools-channel-gating/136-RESEARCH.md` §"Per-requirement touch notes"
      row CHAN-07 and Pitfall #7.
  </read_first>
  <behavior>
    - `inspect.getsource` of each of: `channel.is_prerelease_build`, `channel.dev_tools_enabled_by_env`,
      `channel.is_dev_tools_enabled`, `channel.dev_command_gate_message`, and
      `cli_handlers._DevGroup.get_command` contains no `open(` substring.
    - The same five sources contain none of the forbidden tokens naming a firmware path or file
      extension (`firestarter/include`, `.h"`, `.ino`, `serial_comm`, `frame_parser`).
    - Planting `open(` into one of the five sources makes the test fail.
  </behavior>
  <action>
Author `firestarter_app/tests/test_dev_gate_reads_no_firmware_source.py`. Build a list of the five
callables named in `<behavior>` above (import both modules, reference each callable object directly —
`channel.is_prerelease_build`, etc., and `cli_handlers._DevGroup.get_command` as an unbound function).
For each, call `inspect.getsource(fn)` and assert: no `"open("` substring; none of a named forbidden-token
tuple (firmware path fragments and file extensions) appears. Parametrize over the five callables so a
failure names which one violated the property, rather than one monolithic assertion.

Add one whole-module-source check too: `"open("` does not appear anywhere in
`inspect.getsource(channel)` (channel.py in full) — the stronger, file-wide claim CHAN-07's own touch
note asks for ("channel.py reads the package's own `__version__` and opens no file at all").

**Non-vacuity obligation.** Temporarily insert a call to `open("/dev/null")` inside the body of
`channel.is_dev_tools_enabled` (assign its result to an unused local, or discard it — the point is
purely to trip the scan, not to change behavior in any way that matters for this proof). Run this
task's test module and confirm it now fails, naming `is_dev_tools_enabled` as the offending callable.
Capture the failure output verbatim into the SUMMARY. Then restore `channel.py` byte-identically
(`git diff firestarter/channel.py` empty for this task before commit) and re-run to green.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_gate_reads_no_firmware_source.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_dev_gate_reads_no_firmware_source.py -o addopts="" -q` collects and passes at
      least 6 tests (5 parametrized callables + 1 whole-module check).
    - The SUMMARY contains verbatim RED output from the `open("/dev/null")` plant, naming
      `is_dev_tools_enabled`, and confirms `firestarter/channel.py` was restored byte-identically before
      commit.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` passes with count ≥ the plan-02
      baseline; `tools/ci_replica_venv.sh` reports `mypy errors: N (watermark: 35)`, N ≤ 35.
  </acceptance_criteria>
  <done>The gate's own new code is comprehensively proven to read no firmware source, and the proof has
  been seen to fail under a planted violation.</done>
</task>

<task type="auto">
  <name>Task 3: Non-vacuity — the gate is load-bearing, not a coincidentally-passing test</name>
  <files>firestarter_app/tests/test_dev_group_channel_gating.py</files>
  <read_first>
    - `firestarter_app/tests/test_op_registration_parity.py` `:753-778`
      (`test_altered_registry_copy_fails_parity_non_vacuous`) — the house non-vacuity idiom: positive
      control passes, then a real source mutation is observed to fail the SAME test, then the source is
      reverted.
    - The two production sites this task mutates: `cli_handlers.py`'s `@cli.group(name="dev",
      cls=_DevGroup)` line (plan `136-02` Task 1) and its `_DEV_TOOLS_ENABLED: bool =
      is_dev_tools_enabled()` line (same task).
  </read_first>
  <action>
Perform two temporary mutations, one at a time, each followed by a full restore before the next:

**Mutation A — remove the class wiring.** Temporarily change `@cli.group(name="dev", cls=_DevGroup)`
back to `@cli.group(name="dev")`. Re-run this plan's Task 1 test module. Confirm the specific assertion
that invoking a gated name on simulated-stable gets the channel-specific refusal text now FAILS (Click's
own generic "No such command" appears instead, because the plain `click.Group` has no informative
override). Capture the failing test name and output verbatim into the SUMMARY. Restore
`cli_handlers.py`'s decorator line byte-identically.

**Mutation B — force the flag.** Temporarily change `_DEV_TOOLS_ENABLED: bool = is_dev_tools_enabled()`
to `_DEV_TOOLS_ENABLED: bool = True` (a hardcoded literal). Re-run Task 1's test module. Confirm the
specific assertion that `dev.commands.keys()` on simulated-stable equals exactly `{"read", "test"}` now
FAILS (all eight are registered regardless of simulated channel). Capture the failing test name and
output verbatim into the SUMMARY. Restore the line byte-identically.

After both restores, run `git -C /workspaces/firestarter_app diff --stat firestarter/cli_handlers.py`
and confirm it prints nothing (empty diff) before this task's own commit — this task adds no permanent
source change, only the SUMMARY record of the two observed failures.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; git diff --stat firestarter/cli_handlers.py | wc -l | grep -q '^0$' &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_group_channel_gating.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains verbatim RED output from BOTH Mutation A and Mutation B, each naming the
      specific test that failed.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/cli_handlers.py` prints nothing before
      this task's commit.
    - `pytest tests/test_dev_group_channel_gating.py -o addopts="" -q` is green again after both restores.
  </acceptance_criteria>
  <done>Both mechanisms — the informative-refusal wiring and the conditional-registration flag — are
  proven load-bearing: removing either observably breaks a specific, named assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → child process | Each simulated channel is a genuinely separate OS process; no state leaks between them except via the explicit `env=` mapping this plan's harness controls |
| gate proof → gate implementation | A proof authored against a coincidentally-correct implementation is worse than no proof — it certifies nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-08 | Repudiation | An in-process test asserting "stable hides `reg`" that can only ever observe the beta channel (vacuous by construction, D-04) | critical | mitigate | Subprocess harness assigns `firestarter.__version__` before `cli_handlers` is ever imported in the child, guarded by the child's own `sys.modules` pre-assertion |
| T-136-09 | Tampering | The informative-refusal / conditional-registration mechanisms silently becoming decorative (a test that passes for the wrong reason) | critical | mitigate | Two named non-vacuity mutations, each observed to fail a specific assertion, then restored byte-identically |
| T-136-10 | Information Disclosure | The gate reading firmware source (comprehensive, both files) | high | mitigate | Source-scan assertion over `channel.py` and the new `cli_handlers.py` symbols; non-vacuity plants an `open(` call and observes it fail |
| T-136-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this phase |
</threat_model>

<verification>
- `pytest tests/test_dev_group_channel_gating.py tests/test_dev_gate_reads_no_firmware_source.py tests/test_py32_channel_gating.py -o addopts="" -q` — all green.
- `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — full suite green, count ≥ plan-02 baseline.
- `tools/ci_replica_venv.sh` — mypy ≤ 35, `checked` ≥ 120.
- `git -C /workspaces/firestarter_app diff --stat firestarter/cli_handlers.py firestarter/channel.py` — empty after Task 3's restores, before this plan's own commits land.
</verification>

<success_criteria>
- CHAN-01, CHAN-02, CHAN-03, CHAN-04, CHAN-06, CHAN-07 are all proven true from outside the process.
- Both non-vacuity mutations were observed to fail a named assertion and were restored byte-identically.
- No firmware file touched; no new package installed.
</success_criteria>

<output>
Create `.planning/phases/136-dev-tools-channel-gating/136-03-SUMMARY.md` when done.
</output>
