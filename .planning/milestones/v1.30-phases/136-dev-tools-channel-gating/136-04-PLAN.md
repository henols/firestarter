---
phase: 136-dev-tools-channel-gating
plan: 04
type: execute
wave: 4
depends_on: ["136-03"]
files_modified:
  - firestarter_app/tests/__snapshots__/test_characterization.ambr
  - .planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md
autonomous: true
requirements: [CHAN-05]
must_haves:
  truths:
    - "The `test_help_dev` syrupy snapshot is re-baselined DELIBERATELY, with the change named and justified in the SUMMARY — never a silent `--snapshot-update` (hard requirement 6 / RESEARCH §4)."
    - "The re-baseline diff touches ONLY the docstring header lines the CHAN-05 rewrite changed — the `Commands:` enumeration stays byte-identical (still all 8, in the same order, since this checkout's own channel is pre-release and conditional registration removes nothing here)."
    - "`136-CI-PARITY.md`'s `## After` section records a real post-edit mypy count via `tools/ci_replica_venv.sh` — never inherited, never estimated."
    - "The full test suite, including every file in RESEARCH §5's blast-radius table and both new channel-gating test modules, is green at the phase's close."
  artifacts:
    - firestarter_app/tests/__snapshots__/test_characterization.ambr
    - .planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md
  key_links:
    - "The re-baseline task's own RED-before-update run is itself a cheap non-vacuity check on CHAN-05: if `test_help_dev` did NOT fail before the update, the docstring rewrite did not actually change anything observable, and the whole plan-02 Task 3 claim would be false."
---

<objective>
Close the phase: re-baseline the one pre-existing snapshot the CHAN-05 docstring rewrite invalidated
(named and justified, never silent), and record the phase's post-edit CI-parity/mypy state so the
138-open-question of "what did this phase cost" has a real number, not an estimate.

Purpose: RESEARCH §4 is explicit that `test_help_dev` WILL change once the gate lands and warns against
a blind `--snapshot-update`. Hard requirement 6 makes this a named task, not a side effect buried in an
earlier plan's commit.
Output: a deliberately-updated `test_help_dev` snapshot, and `136-CI-PARITY.md`'s `## After` section.
</objective>

<repo_topology>
`tests/__snapshots__/test_characterization.ambr` lives inside the `firestarter_app` git submodule —
`cd /workspaces/firestarter_app` and commit THERE, on `gsd/v1.30-sdp-surface-retirement`.
`136-CI-PARITY.md`'s `## After` section is a **meta-repo** edit, committed in `/workspaces` on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof` — the two commits are separate, in separate
repos, and must not be conflated into one.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-dev-tools-channel-gating/136-CONTEXT.md
@.planning/phases/136-dev-tools-channel-gating/136-RESEARCH.md
@.planning/phases/136-dev-tools-channel-gating/136-VALIDATION.md
@.planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md
@.planning/phases/136-dev-tools-channel-gating/136-02-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:** none new. `CHAN-05` is listed in frontmatter because this plan's Task 1 exists solely as
a downstream consequence of that requirement's docstring rewrite (plan `136-02`) — it is already ticked
there and is NOT re-ticked here.

**Contributes to but MUST NOT tick:** none.

**MUST NOT touch, re-tick, or re-evidence:** any `GATE-*`, `RETIRE-*`, `LEG-*`, `RELOCK-*`, `PROV-*`, or
`CLOSE-*` row, and none of `CHAN-01`..`CHAN-07` — all seven are already ticked by plans `136-02`/`136-03`
before this plan runs.

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline `test_help_dev`, named and justified</name>
  <files>firestarter_app/tests/__snapshots__/test_characterization.ambr</files>
  <read_first>
    - `firestarter_app/tests/test_characterization.py` `:328-333` (`test_help_dev`) and `:142-161`
      (`run_firestarter` — confirm it runs the INSTALLED entry point as a real subprocess with a clean
      `FIRESTARTER_CONFIG_DIR`, so it observes this checkout's own current `__version__`, i.e. beta,
      i.e. all 8 commands remain listed).
    - `firestarter_app/tests/__snapshots__/test_characterization.ambr`'s current `test_help_dev` entry
      (`:124-141` at time of research) — the exact text that will change.
    - `.planning/phases/136-dev-tools-channel-gating/136-02-SUMMARY.md` for the exact new docstring text.
  </read_first>
  <action>
First, run `pytest tests/test_characterization.py -k test_help_dev -o addopts="" -q` and confirm it now
FAILS against the OLD snapshot (the docstring changed in plan `136-02`, so the captured text no longer
matches) — capture this RED output verbatim into the SUMMARY. If it unexpectedly still passes, STOP and
report this as a discrepancy rather than proceeding to update a snapshot that apparently didn't need it.

Then run `pytest tests/test_characterization.py -k test_help_dev --snapshot-update -o addopts="" -q` to
record the new snapshot.

Diff the `.ambr` file (`git -C /workspaces/firestarter_app diff tests/__snapshots__/test_characterization.ambr`)
and confirm the changed lines are confined to the docstring header text (the two lines below `Usage:
firestarter dev [OPTIONS] COMMAND [ARGS]...`) — the `Commands:` block listing all 8 subcommands, in the
same order, with the same one-line summaries, must be byte-identical before and after. If the diff
touches anything in the `Commands:` block, STOP — that would mean something other than the docstring
changed, which is out of this task's scope and must be investigated, not silently accepted.

Record the full diff verbatim in the SUMMARY, and state explicitly: this re-baseline is named and
justified by the CHAN-05 docstring rewrite (plan `136-02` Task 3); it is not a `--snapshot-update`
run-and-commit without explanation.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_characterization.py -k test_help_dev -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains verbatim RED output from the pre-update run (the old snapshot failing).
    - `pytest tests/test_characterization.py -k test_help_dev -o addopts="" -q` passes after the update.
    - `git -C /workspaces/firestarter_app diff --unified=0 tests/__snapshots__/test_characterization.ambr`
      shows changed lines ONLY within the docstring header region, not within the `Commands:` block.
    - The SUMMARY names the change and its justification explicitly (CHAN-05 / plan `136-02` Task 3).
  </acceptance_criteria>
  <done>The snapshot is re-baselined deliberately, with the change proven scoped to exactly the
  docstring rewrite and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Record the phase's `## After` CI-parity state + full regression</name>
  <files>.planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md</files>
  <read_first>
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md`'s `## After`
      section (if present) or its closing-plan analog, for the shape to mirror.
    - `.planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md`'s own `## Before (pre-edit)`
      section (plan `136-01` Task 1) — the exact metrics to re-measure and compare against.
    - `.planning/phases/136-dev-tools-channel-gating/136-RESEARCH.md` §5 (the blast-radius test file
      table) for the specific files to name in the regression confirmation.
  </read_first>
  <action>
Append a `## After` section to `.planning/phases/136-dev-tools-channel-gating/136-CI-PARITY.md`
(edit the existing file; do not overwrite the `## Before` section). Run, from
`/workspaces/firestarter_app`:
1. `tools/ci_parity.sh` — record every leg's exit status verbatim, compare against the `## Before` record.
2. `tools/ci_replica_venv.sh` — record all five legs and the two certifying lines, compare the mypy
   count against the `## Before` count; state explicitly whether headroom grew, shrank, or stayed flat,
   and by how much.
3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q | tail -5` — record the passed count
   and coverage; the count must be ≥ the `## Before` count (new test modules only add tests, this phase
   deletes none).
4. Explicitly re-run the RESEARCH §5 blast-radius file set (the specific test files covering the six
   gated commands) and confirm each is unchanged and green.
5. Explicitly re-run `tests/test_py32_channel_gating.py` and confirm it is unchanged and green — the
   pattern this phase's harness adapted must itself remain a working regression floor.

State plainly in the `## After` section: no file under `/workspaces/firestarter/` (firmware submodule)
was touched at any point in this phase, and no new pip/npm/cargo package was installed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `136-CI-PARITY.md` contains both a `## Before (pre-edit)` and a `## After` heading.
    - The `## After` section contains a literal `mypy errors: <N> (watermark: 35)` line from a real
      `tools/ci_replica_venv.sh` run, with N ≤ 35, and states the headroom delta against `## Before`
      explicitly (as a number, not "similar" or "about the same").
    - The full suite passes with a count ≥ the `## Before` count.
    - The RESEARCH §5 blast-radius file set and `tests/test_py32_channel_gating.py` are confirmed
      unchanged and green, named explicitly in the SUMMARY.
    - `git -C /workspaces/firestarter_app status --porcelain -- firestarter` (the firmware submodule)
      prints nothing across the whole phase.
  </acceptance_criteria>
  <done>The phase's real post-edit cost is measured and recorded, never estimated or inherited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot update tooling → committed fixture | `--snapshot-update` can silently absorb an unrelated regression if run broadly; scoping it to one named test and diffing the result is this task's whole guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-11 | Repudiation | Silently re-recording the `test_help_dev` snapshot, losing the ability to tell a docstring change from a command-list regression | high | mitigate | Diff-scoped proof that only the docstring lines changed; the pre-update run is confirmed RED first |
| T-136-12 | Denial of Service | The phase's production edits silently exceeding the mypy watermark (headroom was only 2 entering the phase) | high | mitigate | `tools/ci_replica_venv.sh` re-run and recorded; the gate itself (GATE-01..04, already shipped) fails closed if it does |
| T-136-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this phase |
</threat_model>

<verification>
- `pytest tests/test_characterization.py -k test_help_dev -o addopts="" -q` — green after the deliberate update.
- `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — full suite green, count ≥ phase-entry baseline.
- `tools/ci_replica_venv.sh` and `tools/ci_parity.sh` — both recorded in `## After`, mypy ≤ 35.
- `git -C /workspaces/firestarter_app status --porcelain -- firestarter` — empty across the whole phase.
</verification>

<success_criteria>
- `test_help_dev` is re-baselined deliberately, diff-scoped to the docstring only.
- `136-CI-PARITY.md` carries both `## Before` and `## After`, with a stated mypy delta.
- Full regression (including RESEARCH §5's blast radius and the py32 channel-gating pattern) is green.
</success_criteria>

<output>
Create `.planning/phases/136-dev-tools-channel-gating/136-04-SUMMARY.md` when done.
</output>
