---
phase: 136-dev-tools-channel-gating
plan: 02
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
autonomous: true
requirements: [CHAN-05, CHAN-01, CHAN-02, CHAN-03, CHAN-06, CHAN-07]
must_haves:
  truths:
    - "The `dev` group's Click command class is `_DevGroup(click.Group)` (D-01's exact name), holding the gated NAMES only (`channel.BETA_ONLY_DEV_COMMANDS`) — never a callback — and its `get_command` override is the empirically-settled hook from plan `136-01`'s spike."
    - "`_DEV_TOOLS_ENABLED` is computed ONCE, at import time, from `channel.is_dev_tools_enabled()` — mirroring the existing `_PY32_ENABLED` pattern already in this file — so a stable wheel's registration decision is fixed at build time, not re-evaluated per invocation."
    - "Each of the six gated `@dev.command` blocks (`reg`, `addr`, `consistency-check`, `write-cycle`, `fault-inject`, `validate-family`) is wrapped in `if _DEV_TOOLS_ENABLED:` at module scope — when the gate is closed, the function name itself never binds in the module namespace (CHAN-02 satisfied literally, not by `hidden=`)."
    - "The `dev reg` block carries a tripwire comment, in the `D-14 / RETIRE-07` style already used elsewhere in this file, naming WHY the `FIRESTARTER_DEV_TOOLS` override exists (the held-erase-rail DMM proxy) so a future edit to this gate is forced to read the dependency first (CHAN-06)."
    - "The `dev` group's docstring no longer implies `read`/`test` are for developers only — it states plainly that those two are the stable-supported subset, and the rest are beta/bench-only (CHAN-05, D-05)."
    - "Every one of the eight `dev` subcommands' existing tests still passes in-process, unchanged — this checkout's own `__version__` is a pre-release, so `_DEV_TOOLS_ENABLED` is True in every in-process test run and nothing observable changes yet."
  artifacts:
    - firestarter_app/firestarter/cli_handlers.py
  key_links:
    - "`@cli.group(name=\"dev\", cls=_DevGroup)` replaces the bare `@cli.group(name=\"dev\")` — omitting `cls=_DevGroup` silently reverts to Click's own generic refusal (this exact regression is planted and observed in plan `136-03`'s non-vacuity task)."
    - "`_DEV_TOOLS_ENABLED` must be read by every one of the six `if` guards from the SAME module-level name — six independent re-computations of `channel.is_dev_tools_enabled()` would still be correct today but would silently diverge if a future edit made the function non-deterministic; freezing it once at import time is the load-bearing choice, matching `_PY32_ENABLED`'s own precedent one screen above."
---

<objective>
Wire the two D-01 mechanisms into `firestarter_app/firestarter/cli_handlers.py`: the `_DevGroup`
subclass (informative refusal) and conditional registration (genuine non-registration) of the six
beta-only `dev` subcommands, plus the CHAN-05 docstring rewrite and the CHAN-06 tripwire comment.

Purpose: this is the phase's only production-code plan. Every later plan's proof depends on this file's
shape being final before it is tested from the outside (subprocess) in plan `136-03`.
Output: `_DevGroup`, `_DEV_TOOLS_ENABLED`, six conditionally-registered command blocks, a rewritten
`dev()` docstring, and a tripwire comment at `dev reg`.
</objective>

<repo_topology>
All code in this plan lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`
and commit THERE, on `gsd/v1.30-sdp-surface-retirement`. The meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`; the names deliberately diverge. The top-level
`/workspaces/firestarter/` directory is the Arduino firmware submodule and is not touched by this phase
at all.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-dev-tools-channel-gating/136-CONTEXT.md
@.planning/phases/136-dev-tools-channel-gating/136-RESEARCH.md
@.planning/phases/136-dev-tools-channel-gating/136-VALIDATION.md
@.planning/phases/136-dev-tools-channel-gating/136-01-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:**
- **CHAN-05** — the `dev` group docstring no longer warns the stable users `read`/`test` are kept for.
  Fully discharged here: this is an in-process, channel-independent text fact, provable without a
  subprocess.

**Contributes to but MUST NOT tick:**
- **CHAN-01, CHAN-02, CHAN-03** — the mechanisms land here (conditional registration; `_DevGroup`'s
  informative refusal), but proving them for a STABLE install requires simulating that channel, which
  is vacuous in-process (D-04) and needs the subprocess harness. **Closed by plan `136-03`.**
- **CHAN-06** — the tripwire comment lands here, but the requirement is that the override actually
  works end to end on a simulated-stable install, which is `136-03`'s subprocess proof.
- **CHAN-07** — this plan's new code is written to read no firmware source, but the comprehensive,
  cross-file assertion (covering both `channel.py` and this file's new symbols) is `136-03`'s.

**MUST NOT touch, re-tick, or re-evidence:** any `GATE-*`, `RETIRE-*`, `LEG-*`, `RELOCK-*`, `PROV-*`, or
`CLOSE-*` row.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `_DevGroup` subclass + `_DEV_TOOLS_ENABLED` + group wiring</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py` `:1-16` (module docstring — do not edit; it is a
      module-wide summary, out of this plan's scope) and `:36` (the existing
      `from firestarter.channel import available_boards` line to extend).
    - `firestarter_app/firestarter/cli_handlers.py` `:144-185` (`_BOARD_CHOICES` / `_PY32_ENABLED` /
      `_reject_py32_only_option` — the import-time-freeze pattern and the existing `click.UsageError`
      refusal convention to mirror exactly, including that no `from __future__ import annotations` is
      present in this file, so `Optional[click.Command]` must be spelled out, never `click.Command |
      None`).
    - `firestarter_app/firestarter/cli_handlers.py` `:1200-1213` (today's `@cli.group(name="dev")` /
      `dev()` definition and the stale "Wave 3 / D-12 step 5" section banner above it).
    - `.planning/phases/136-dev-tools-channel-gating/136-01-SUMMARY.md` for the exact `channel.py` symbol
      names and the settled Click-hook finding (`get_command`, not `resolve_command`).
    - `firestarter_app/tests/test_click_group_gate_hook.py` — the empirical proof this task's
      `get_command` override must match exactly (same fallback shape: try `super().get_command()` first,
      raise only for a recognized gated name, else return `None`).
  </read_first>
  <behavior>
    - `_DevGroup().get_command(ctx, "read")` (with `"read"` actually registered on the instance) returns
      the real command object.
    - `_DevGroup().get_command(ctx, "reg")`, when `"reg"` is NOT registered on the instance, raises
      `click.UsageError` whose message equals `channel.dev_command_gate_message("reg")`.
    - `_DevGroup().get_command(ctx, "totally-not-a-real-command")` returns `None` (falls through to
      Click's own generic error), because that name is not in `channel.BETA_ONLY_DEV_COMMANDS`.
    - `_DEV_TOOLS_ENABLED` equals `channel.is_dev_tools_enabled()` evaluated at import time.
  </behavior>
  <action>
Extend the existing import at `:36` (`from firestarter.channel import available_boards`) to also import
`BETA_ONLY_DEV_COMMANDS`, `dev_command_gate_message`, and `is_dev_tools_enabled` from
`firestarter.channel`.

Immediately above the existing "dev group + N sub-commands" section banner (around `:1200`), add a new
banner comment introducing the CHAN-01..07 gate, then:

1. `_DEV_TOOLS_ENABLED: bool = is_dev_tools_enabled()` — a module-level constant, computed once at
   import time. Comment states this mirrors `_PY32_ENABLED` above: a wheel's `__version__` is fixed
   when built, so the choice a stable install renders is decided once and decided correctly.
2. `class _DevGroup(click.Group):` with a docstring naming D-01 and stating explicitly that it holds
   gated NAMES only (via `channel.BETA_ONLY_DEV_COMMANDS`), never a callback, and citing
   `tests/test_click_group_gate_hook.py` as the empirical basis for overriding `get_command` and nothing
   else. Implement `get_command(self, ctx: click.Context, cmd_name: str) -> Optional[click.Command]:` —
   call `super().get_command(ctx, cmd_name)` first; if not `None`, return it; if `cmd_name` is a member
   of `BETA_ONLY_DEV_COMMANDS`, raise `click.UsageError(dev_command_gate_message(cmd_name), ctx=ctx)`;
   otherwise return `None`. Do not override `list_commands` — plan `136-01`'s spike proved it needs no
   override once conditional registration (Task 2) makes non-registration genuine.

Change `@cli.group(name="dev")` to `@cli.group(name="dev", cls=_DevGroup)`. Do not touch the `dev()`
function body or docstring in this task — the docstring rewrite is Task 3, kept separate so this task's
diff is reviewable as "wiring only."
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_cli_handlers.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class _DevGroup' firestarter/cli_handlers.py` returns 1.
    - `grep -c '_DEV_TOOLS_ENABLED: bool = is_dev_tools_enabled()' firestarter/cli_handlers.py` returns 1.
    - `grep -c 'cls=_DevGroup' firestarter/cli_handlers.py` returns 1.
    - `python -c "import sys; sys.path.insert(0,'.'); from firestarter.cli_handlers import dev; print(type(dev).__name__)"` prints `_DevGroup`.
    - `pytest tests/test_cli_handlers.py -o addopts="" -q` passes unchanged (this checkout's own version
      is a pre-release, so `_DEV_TOOLS_ENABLED` is `True` and nothing observable changed yet).
    - `mypy` via `tools/ci_replica_venv.sh` still reports `mypy errors: N (watermark: 35)`, N ≤ 35 (the
      new class is fully annotated: `ctx: click.Context`, `cmd_name: str`, return `Optional[click.Command]`).
    - `ruff check firestarter/` and `ruff format --check firestarter/` both exit 0.
  </acceptance_criteria>
  <done>`_DevGroup` exists, is wired onto the `dev` group, and its `get_command` override matches the
  plan-01 spike's empirically-proven shape exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional registration of the six gated commands + CHAN-06 tripwire</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - Re-grep this file for the current line numbers of the six `@dev.command(name=...)` blocks before
      editing — Task 1 inserted new lines above `dev()`, so the numbers read during planning
      (`:1245` `reg`, `:1307` `addr`, `:1344` `consistency-check`, `:1434` `write-cycle`, `:1487`
      `fault-inject`, `:1714` `validate-family`) have shifted. Anchor every edit on the exact
      `@dev.command(name="...")` decorator line text, never on a remembered line number.
    - `firestarter_app/firestarter/cli_handlers.py`'s `RETIRE-07` tripwire comments at `:570-574` (option
      default) and `:644-659` (`write()`'s decision-site comment) — the exact shape (names the specific
      test, names the specific companion location, states what changing the default invalidates) to
      mirror for the new CHAN-06 tripwire.
    - `firestarter_app/firestarter/cli_handlers.py`'s `read`-command block (`:1214-1243`, UNCHANGED —
      `read` and `test` are the two stable-kept commands and are NEVER wrapped) for contrast: this is
      what a NOT-gated block looks like, so the six gated blocks can be distinguished from it correctly.
  </read_first>
  <action>
For each of the six gated command blocks — `reg`, `addr`, `consistency-check`, `write-cycle`,
`fault-inject`, `validate-family` — wrap the ENTIRE block (every `@click...` decorator line, the
`@dev.command(name=...)` line itself, the `def dev_xxx(...) -> None:` line, its docstring, and its full
body) in a module-level `if _DEV_TOOLS_ENABLED:` guard, indenting every line of the block by exactly
four spaces. Change nothing else about the block's content — same decorators, same argument names, same
docstring text, same body. Leave one blank line before and after each wrapped block, matching the file's
existing spacing convention between top-level definitions.

Do NOT wrap `read` (`:1214-1243`) or `test` (`:2266+` at time of research) — those two stay unconditional
at module scope; they are the two commands CHAN-01 keeps on every channel.

At the `reg` block specifically, add a tripwire comment immediately above its (now-indented)
`@dev.command(name="reg")` line, inside the `if` block, in the `RETIRE-07` shape: name that
`FIRESTARTER_DEV_TOOLS` exists BECAUSE `dev reg` is load-bearing bench tooling (the held-erase-rail DMM
proxy) that would otherwise silently disappear from an editable devcontainer install at any stable
version cut or between betas; name the companion functions this depends on
(`channel.dev_tools_enabled_by_env`, `channel.is_dev_tools_enabled`) and state that narrowing the accepted
override value, or removing the OR with the env check, strands that bench tooling without warning
(CHAN-06).

After wrapping, confirm with `python -c "import ast; ast.parse(open('firestarter/cli_handlers.py').read())"`
that the file is still syntactically valid before running any test.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_cli_handlers.py tests/test_consistency_check.py tests/test_eprom_operations.py tests/test_serial_comm.py tests/test_validate_oracle.py tests/test_diagnostic_report.py tests/test_matrix_artifact.py tests/test_validate_family_cmd.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import sys; sys.path.insert(0,'.'); from firestarter import cli_handlers; assert {'reg','addr','consistency-check','write-cycle','fault-inject','validate-family'} <= set(cli_handlers.dev.commands.keys())"` exits 0 (this checkout is a pre-release, so all six remain registered in-process).
    - `grep -n 'if _DEV_TOOLS_ENABLED:' firestarter/cli_handlers.py | wc -l` returns 6.
    - `grep -c 'FIRESTARTER_DEV_TOOLS' firestarter/cli_handlers.py` returns at least 1 (the tripwire comment).
    - `python -c "import ast; ast.parse(open('firestarter/cli_handlers.py').read())"` exits 0.
    - The blast-radius suite (the command in `<verify>`) passes unchanged — RESEARCH §5's small file set
      for these six commands.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` passes with count ≥ the plan-01
      baseline; `tools/ci_replica_venv.sh` reports `mypy errors: N (watermark: 35)`, N ≤ 35.
    - `ruff check firestarter/` and `ruff format --check firestarter/` both exit 0.
  </acceptance_criteria>
  <done>The six beta-only commands are genuinely absent from the module namespace when
  `_DEV_TOOLS_ENABLED` is `False`, and the `dev reg` tripwire names its bench dependency.</done>
</task>

<task type="auto">
  <name>Task 3: CHAN-05 — rewrite the `dev` group's docstring</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py`'s current `dev()` docstring (post-Task-1, still
      two lines: "Debug command for development purposes." / "USR button will break command and
      return.").
    - `.planning/phases/136-dev-tools-channel-gating/136-CONTEXT.md` D-05 and
      `136-RESEARCH.md` §6 in full — the exact defect being fixed.
    - `firestarter_app/tests/__snapshots__/test_characterization.ambr`'s `test_help_dev` entry — read it
      to understand exactly what text this rewrite will change in that snapshot (plan `136-04` re-baselines
      it; this task does not touch the snapshot).
  </read_first>
  <action>
Rewrite the `dev()` group function's docstring. State, in the first line, what this group is without
the word "purposes" doing the scaring: development and diagnostic commands. In a second paragraph,
state plainly that on a stable install only `read` and `test` are available, that both are fully
supported for end users despite living in this group, and that the remaining subcommands are
development/bench tooling available only on a pre-release install. Keep the existing "USR button will
break command and return" line — it is a real hardware behavior note, not the defect CHAN-05 targets;
D-05 is specifically about the "for development purposes" framing discouraging `read`/`test`'s own
intended stable audience, not about removing accurate hardware notes.

Do not touch the top-of-file module docstring's stale "6 sub-commands" enumeration (`:14-15`) — it is a
pre-existing inaccuracy (the true count is 7 gated+ungated plus `test`) unrelated to CHAN-05's scope,
and touching it here would be uncontrolled scope creep against this plan's single concern.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; python3 -c "import sys; sys.path.insert(0,'.'); from firestarter.cli_handlers import dev; doc = dev.callback.__doc__ or ''; assert 'stable' in doc.lower(); assert 'read' in doc.lower() and 'test' in doc.lower()"</automated>
  </verify>
  <acceptance_criteria>
    - The new docstring's text names both `read` and `test` as available/supported on a stable install.
    - The new docstring keeps the USR-button line verbatim.
    - `pytest tests/test_cli_handlers.py -o addopts="" -q` still passes (no test currently pins the exact
      old docstring text — confirmed by grep in the SUMMARY).
    - `ruff check firestarter/` and `ruff format --check firestarter/` both exit 0.
  </acceptance_criteria>
  <done>The `dev` group's own help text no longer discourages the two commands stable users are meant
  to use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → command resolution | A user-typed subcommand name crosses into `_DevGroup.get_command`, which decides refusal vs. execution |
| wheel build time → runtime | `_DEV_TOOLS_ENABLED` is frozen at import time from the wheel's own fixed `__version__` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-05 | Elevation of Privilege | `_DevGroup.get_command` misclassifying a genuine typo as "beta-only" | medium | mitigate | Membership check against `channel.BETA_ONLY_DEV_COMMANDS` only; an unknown name still returns `None` and falls through to Click's own generic error (proven in plan `136-01`'s spike, confirmed end-to-end in `136-03`) |
| T-136-06 | Repudiation | `hidden=True`-style cosmetic gating masquerading as a real gate | critical | mitigate | Conditional registration means a gated command's `@dev.command` decorator never executes on stable — the function name itself does not exist in the module namespace |
| T-136-07 | Denial of Service | The bench override silently losing `dev reg` at a stable version cut, stranding the held-erase-rail DMM proxy | high | mitigate | Tripwire comment at the `dev reg` block names the dependency; end-to-end env-var proof lands in `136-03` |
| T-136-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this phase |
</threat_model>

<verification>
- `pytest tests/test_cli_handlers.py -o addopts="" -q` — unchanged, green.
- The RESEARCH §5 blast-radius suite (8 files across the 6 gated commands) — unchanged, green.
- `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — full suite green, count ≥ plan-01 baseline.
- `tools/ci_replica_venv.sh` — mypy ≤ 35, `checked` ≥ 120.
- `ruff check firestarter/` and `ruff format --check firestarter/` exit 0.
</verification>

<success_criteria>
- `_DevGroup`, `_DEV_TOOLS_ENABLED`, and the six conditionally-registered blocks exist in `cli_handlers.py`.
- The `dev reg` block carries a tripwire comment naming the bench dependency.
- The `dev()` docstring no longer discourages `read`/`test`'s own stable audience.
- Every existing in-process test remains green (this checkout's channel is pre-release throughout).
</success_criteria>

<output>
Create `.planning/phases/136-dev-tools-channel-gating/136-02-SUMMARY.md` when done.
</output>
