---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 03
type: execute
wave: 3
depends_on: ["134-02"]
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/tests/test_op_registration_parity.py
  - firestarter_app/tests/test_chip_test.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
autonomous: true
requirements: [LEG-01, LEG-02, LEG-04]
must_haves:
  truths:
    - "`derive_plan` emits the SDP leg from `sdp_capability(chip_name, db)` with **no new command-line option** — `dev test` keeps zero options (LEG-01)."
    - "The leg is **SIX** steps in this order: `write-baseline-b` · `write-baseline-a` · `sdp-lock` · `write-inhibited` · `sdp-unlock` · `write-restored` (D-06). Criterion 1's / LEG-01's / LEG-02's inherited **four** is measured-wrong; BOTH readings go in the record, and the count assertion plus the REFUSE-chip NA test each pin **six**."
    - "`write-restored` is the only step producing evidence the part was left writable, on a family whose protection state cannot be read back — dropping it would end the run on `sdp-unlock OK`, an emission claim with nothing behind it (D-06)."
    - "Two baseline ops, not one folded `sdp-baseline`, because `render()`'s console table shows only op/verdict/error_code/fingerprint and `reason` reaches only the markdown table and the JSON — the failing baseline DIRECTION must be legible in the op string itself (D-07, the `write-partial` precedent)."
    - "The 41 capability-REFUSED chips get six `Step(supported=False)` entries carrying `sdp_capability()`'s own refusal prose — zero new machinery, `run_plan:877-879`'s existing NA path (LEG-02)."
    - "The leg is gated on `write_execute` (`chip_test.py:463`); `write_scope=\"none\"` is unreachable from `dev test` since Phase 121's reversal, so that branch is library/test surface and is described as such, never as a live gate (D-18)."
    - "Phase 133's LEG-09/10/11 named proofs and the three shipped `locked_destructive` exact-equality assertions stay byte-identically green — the D-18 emission shape is chosen so they do."
  artifacts:
    - firestarter_app/firestarter/chip_test.py
    - firestarter_app/tests/test_chip_test.py
    - firestarter_app/tests/test_chip_test_sdp_leg.py
  key_links:
    - "`tests/test_chip_test.py:1939-1958` (`test_devtest01_0x0d_all_ok_sweep_no_longer_tags_community_fail`) runs `run_plan` on AT28C256 — an ALLOW chip — with `_mock_operator()`, whose `read_eprom` returns `True` and writes NO file. The instant `derive_plan` emits the leg, the oracle sees `actual = b\"\"`, the length gate fires, and its `assert VERDICT_BAD not in verdicts` goes RED. That test MUST be repaired in the same commit."
    - "`tests/test_chip_test_sdp_leg.py::test_empty_registry_noop` is LEG-10's NAMED evidence and calls `derive_plan(\"M8720\", _REAL_DB)` at the DEFAULT `write_scope=\"none\"`, asserting `len_results == 3` — so nothing may be emitted for a REFUSE chip at `write_scope=\"none\"`."
---

<objective>
Make `derive_plan` emit the six-step SDP leg, derived from `sdp_capability()`, for the 43 ALLOW chips —
and six NA steps carrying the refusal reason for the 41 REFUSE chips — with no new CLI option. Discharge
the last of Phase 133's parity exemption rows in the same commits, and repair the one shipped test the
emission necessarily changes.

Purpose: LEG-01/LEG-02/LEG-04's derivation half, and the point at which the leg becomes reachable from
a real `dev test` run.
Output: six emitted steps in D-06 order, a green parity gate with zero temporary rows left, and the
shipped 0x0D sweep tests repaired rather than weakened.
</objective>

<repo_topology>
All code lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`, commit
THERE on `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`). `/workspaces/firestarter/` is the Arduino
firmware submodule and is **not touched by this phase at all**.
`firestarter_app/firestarter/eprom_operations.py` is **RING-FENCED** — call only.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-PATTERNS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-02-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:**
- **LEG-01** — `dev test` derives the SDP leg from `sdp_capability()` with no new CLI option.
  ⚠ Tick it with the D-06 correction stated in the evidence clause: the leg is **six** steps, not the
  requirement text's four. Do not silently satisfy "four".
- **LEG-02** — REFUSED chips get NA/SKIPPED steps carrying the refusal reason, never a silent omission.
- **LEG-04** — the baseline proves a write **transition** (B, verify, A, verify) before any lock. The
  transition *semantics* landed in plan `134-02`; the *ordering* proof lands here, which completes it.

**Contributes to but MUST NOT tick:**
- **LEG-06** — closed by plan `134-05` (needs D-14's exit precedence).
- **LEG-13** — the M 4→10 change becomes measurable here, but LEG-13 needs its **pinning test**, which
  is plan `134-10`'s. Do **not** tick it and do **not** edit `count_applicable` (D-15 measured the
  ratio already drops, and editing it would trip the parity gate's inversion guard).
- LEG-12 → `134-07`. LEG-14 → `134-09`. LEG-17 → `134-10`. LEG-18 → `134-11`.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09/10/11/15 (already `[x]`, Phase 133 — the regression
floor). No RELOCK-\*, CHAN-\*, CLOSE-\* row.

## Artifacts this phase produces (new symbols introduced by THIS plan)

| Symbol | File | Kind |
|---|---|---|
| the six-step SDP emission block inside `derive_plan` | `firestarter_app/firestarter/chip_test.py` | code block |
| `_SDP_LEG_STEP_ORDER` (the D-06 op tuple, single-sourced) | `firestarter_app/firestarter/chip_test.py` | module constant |
| `_SDP_LOCKED_REASON` (the `write_scope="none"` advisory prose) | `firestarter_app/firestarter/chip_test.py` | module constant |
| read-back-capable operator double for the 0x0D sweeps | `firestarter_app/tests/test_chip_test.py` | test helper |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `derive_plan` emits the six-step leg; parity discharged; the 0x0D sweep repaired</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_op_registration_parity.py, firestarter_app/tests/test_chip_test.py</files>
  <read_first>
    - `firestarter_app/firestarter/chip_test.py` `:409-596` (`derive_plan` in full) — especially the `write_execute` gate at `:463`, the destructive write arm `:513-534` (`locked_destructive` shape), the `write-partial` comment `:517-519`, and the erase arm `:553-588` (the supported/NA fork and its family-fact reason prose).
    - `firestarter_app/firestarter/chip_test.py` `:314-408` (`Step`, `Plan`, `is_uv_eprom` `:378`, `_WRITE_SCOPE_*` `:403-406`).
    - `firestarter_app/firestarter/sdp_capability.py` `:180-184` (`REASON_*`), `:201` (`sdp_capability_for_entry`), `:266` (`sdp_capability(chip_name, db) -> tuple[bool, str]`).
    - `firestarter_app/firestarter/chip_test.py` `:773-972` (`run_plan`) — specifically the NA arm at `:877-879`, which is why LEG-02 needs **zero** new machinery.
    - `firestarter_app/tests/test_chip_test.py` `:560-600` (the exact-equality `locked_destructive` assertion at `:581-584`), `:615-676` (the `locked_ops == {...}` set assertions and the **NA-erase precedent** at `:662-671`), `:1700-1730`, and `:1908-1958` (the two AT28C256 0x0D sweeps and `_mock_operator`).
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` `:246-300` (`_SHIPPED_OPS_SEQUENCE` and `test_shipped_ops_sequence_unchanged`) and `:973-1006` (`test_empty_registry_noop` — LEG-10's named evidence).
    - `firestarter_app/tests/test_op_registration_parity.py` `:415-430` (the two `Phase 134 surface` rows at `:422-427`) and `:792-806` (the disposition pins at `:801-802`).
  </read_first>
  <behavior>
    - `derive_plan("AT28C256", db, write_scope="full").steps` ends with exactly the six op strings `write-baseline-b`, `write-baseline-a`, `sdp-lock`, `write-inhibited`, `sdp-unlock`, `write-restored`, in that order, all `supported=True`.
    - `derive_plan("M8720", db, write_scope="full").steps` contains those same six ops, all `supported=False`, each carrying `sdp_capability("M8720", db)[1]` as its reason.
    - `derive_plan("M8720", db)` (default `write_scope="none"`) yields `[s.op for s in plan.steps] == ["id", "read", "blank-check"]` and `plan.locked_destructive == [(OP_WRITE, …), (OP_VERIFY, …), (OP_ERASE, …)]` — byte-identical to today.
    - `derive_plan("AT28C256", db, write_scope="none").locked_destructive` contains six `(op, reason)` entries for the SDP leg in addition to the shipped ones, and `plan.steps` contains none of the six.
    - `len([s for s in derive_plan(<allow chip>, db, write_scope="full").steps if s.op in _SDP_LEG_STEP_ORDER or s.op in ("sdp-lock", "sdp-unlock")]) == 6`.
  </behavior>
  <action>
**A. `firestarter_app/firestarter/chip_test.py` — the emission block in `derive_plan`.**

Add a module constant `_SDP_LEG_STEP_ORDER` holding the D-06 order as a tuple —
`(OP_WRITE_BASELINE_B, OP_WRITE_BASELINE_A, OP_SDP_LOCK, OP_WRITE_INHIBITED, OP_SDP_UNLOCK,
OP_WRITE_RESTORED)` — so the order is single-sourced and every count assertion derives **six** from it
rather than restating the number (P-08's derive-don't-restate discipline).

Append the emission at the **end** of `derive_plan`'s step list, after the erase arm, so the six ops
form a contiguous appended block and no shipped step's index moves. `test_derive_plan_*`'s existing
`d_ops.index(OP_VERIFY) < d_ops.index(OP_ERASE)` comparisons must remain true.

Call `sdp_capability(name, db)` — the injected decision source (LEG-01). Do **not** re-implement a
protocol/pinout heuristic; `sdp_capability` is fail-closed and count-pinned at 43/41/84. Also do not add
any CLI option: `dev test` keeps zero options, and `derive_plan`'s signature gains no parameter.

Emission rules, in this order:

1. **`write_execute` gate first** (`chip_test.py:463`, `write_scope in (_WRITE_SCOPE_FULL,
   _WRITE_SCOPE_PARTIAL)`).
2. **When `write_execute` is True:** append six `Step`s in `_SDP_LEG_STEP_ORDER`.
   - ALLOW chip (`sdp_capability(...)[0] is True`): `supported=True`, `reason=""`,
     `destructive=True`, `write_region=write_region` (the same region the shipped write arm already
     computed — do **not** re-derive UV-ness; ALLOW chips are non-UV, so the region is `(0, 256)`).
   - REFUSE chip: `supported=False`, `reason=<sdp_capability(...)[1]>`, `destructive=True`.
     `run_plan:877-879` turns each into `_skip_result(..., verdict=VERDICT_NA)` with **no operator
     call** — zero new machinery. The 41 refusal reasons are already user-facing prose; pass them
     through verbatim, do not re-word them.
3. **When `write_execute` is False:**
   - ALLOW chip: append six `(op, _SDP_LOCKED_REASON.format(op=...))` entries to `locked_destructive`
     and **no** steps (D-18). Add `_SDP_LOCKED_REASON` as a module constant in the
     `'write_scope="none": write omitted (D-01)'` prose shape.
   - REFUSE chip: emit **nothing** — no steps and no `locked_destructive` entries.

   ⚠ **This last clause is a plan-time refinement of D-18 (a `Claude's Discretion` decision), taken on
   four measurements. Record it with both readings in the SUMMARY:**
   - `tests/test_chip_test_sdp_leg.py::test_empty_registry_noop` is **LEG-10's named evidence in
     `REQUIREMENTS.md`** and calls `derive_plan("M8720", _REAL_DB)` at the default `write_scope="none"`,
     asserting `op_sequence == ["id","read","blank-check"]` and `len_results == 3` against
     `_SHIPPED_OPS_SEQUENCE`. Emitting NA steps for a REFUSE chip at `"none"` turns LEG-10's proof RED —
     a breach of this phase's regression floor.
   - `tests/test_chip_test.py:581-584` asserts **exact list equality** on M8720's `"none"`
     `locked_destructive` (three entries); `:643-644` asserts `locked_ops == {"write","verify","erase"}`;
     `:670` asserts `locked_ops == {"write","verify"}` for AM2716. **All three chips are REFUSE**
     (measured at plan time via `sdp_capability`). Adding six entries breaks all three.
   - The house rule at `tests/test_chip_test.py:662-671` is explicit: an **unsupported** step "must NOT
     be fabricated as a runnable/locked step" — it stays an unsupported `Step` in `steps`. A REFUSE
     chip's SDP steps are unsupported by construction, so `locked_destructive` is the wrong home for
     them.
   - `write_scope="none"` is **unreachable from `dev test`** since Phase 121's reversal
     (`_resolve_write_scope` returns only `"full"`/`"partial"`). On every reachable `dev test` run,
     REFUSE chips DO receive the six NA steps, so **LEG-02 is fully satisfied on the live path.**
     Describe this branch as library/test surface, never as a live gate.

**B. Correction 2, in code and in the record.** Carry a comment beside `_SDP_LEG_STEP_ORDER` stating
both readings: ROADMAP criterion 1, LEG-01 and LEG-02 say a **four**-step leg; the leg is **six**. The
inherited four was written before LEG-04 mandated two transition directions, and the ROADMAP's own
enumeration omits `write-restored` — the only step producing evidence the part was left writable, on a
family whose protection state cannot be read back. Do not quietly ship six while asserting four.

**C. Parity discharge, SAME commit** (`firestarter_app/tests/test_op_registration_parity.py`):
- Remove the four `TEMPORARY — discharged by plan 134-03` rows for `(new_op, "derive_plan")`.
- Remove the two shipped `Phase 134 surface -- not derived as a plan step in 133 (D-11).` rows at
  `:422-427` — `derive_plan` now emits both SDP ops.
- Flip `("derive_plan", OP_SDP_LOCK): False` → `True` and `("derive_plan", OP_SDP_UNLOCK): False` →
  `True` at `:801-802`, and add the four new ops' `derive_plan` memberships to that matrix.
- Re-run the gate and follow its own failure list; do not predict row counts.

**D. Repair the one shipped test the emission necessarily changes, SAME commit**
(`firestarter_app/tests/test_chip_test.py:1908-1958`). Both AT28C256 sweeps now run the real six-step
leg. `_mock_operator()`'s `read_eprom` returns `True` and writes **no file**, so the oracle sees
`actual = b""`, the length gate fires and every leg step reports BAD —
`test_devtest01_0x0d_all_ok_sweep_no_longer_tags_community_fail`'s `assert VERDICT_BAD not in verdicts`
at `:1954` goes RED. **Repair it, do not weaken it:** give both tests a read-back-capable operator
double (the same shape as `tests/test_chip_test_sdp_leg.py::_readback_operator` from plan `134-02` —
`read_eprom.side_effect` writes the expected payload to the `output_file` kwarg) so an all-OK 0x0D sweep
is genuinely all-OK. Do **not** delete the assertion, do not add `pytest.mark.skip`, and do not narrow
the verdict set it inspects. Record the repair and its cause in the SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_op_registration_parity.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/chip_test.py` contains `_SDP_LEG_STEP_ORDER` and `sdp_capability(` inside `derive_plan`'s body.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.database import EpromDatabase;from firestarter.chip_test import derive_plan,_SDP_LEG_STEP_ORDER;db=EpromDatabase(skip_local_override=True);p=derive_plan('AT28C256',db,write_scope='full');ops=[s.op for s in p.steps];assert ops[-6:]==list(_SDP_LEG_STEP_ORDER),ops"` exits 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.database import EpromDatabase;from firestarter.chip_test import derive_plan,_SDP_LEG_STEP_ORDER;from firestarter.sdp_capability import sdp_capability;db=EpromDatabase(skip_local_override=True);p=derive_plan('M8720',db,write_scope='full');ss=[s for s in p.steps if s.op in _SDP_LEG_STEP_ORDER];assert len(ss)==6;assert all(not s.supported for s in ss);assert all(s.reason==sdp_capability('M8720',db)[1] for s in ss)"` exits 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.database import EpromDatabase;from firestarter.chip_test import derive_plan;db=EpromDatabase(skip_local_override=True);p=derive_plan('M8720',db);assert [s.op for s in p.steps]==['id','read','blank-check'];assert len(p.locked_destructive)==3"` exits 0.
    - `grep -c 'TEMPORARY — discharged by plan 134-0' tests/test_op_registration_parity.py` returns 0.
    - `grep -c 'Phase 134 surface -- not derived as a plan step in 133' tests/test_op_registration_parity.py` returns 0.
    - `grep -c 'pytest.mark.skip\|pytest.mark.xfail' tests/test_chip_test.py` is unchanged from before this task (`git diff` shows no added skip/xfail marker).
    - `pytest tests/test_chip_test.py -o addopts="" -q` passes, including `test_devtest01_0x0d_all_ok_sweep_no_longer_tags_community_fail` with its `VERDICT_BAD not in verdicts` assertion intact.
    - `pytest tests/test_chip_test_sdp_leg.py -o addopts="" -q` passes — `test_shipped_ops_sequence_unchanged`, `test_empty_registry_noop`, `test_shipped_ops_never_reach_sdp_arm` and every LEG-09/10/11 named proof still green.
    - `pytest tests/test_op_registration_parity.py -o addopts="" -q` — 7 passed.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0.
  </acceptance_criteria>
  <done>The leg is derived, the parity table carries zero temporary rows, and no shipped proof was weakened.</done>
</task>

<task type="auto">
  <name>Task 2: LEG-01 / LEG-02 / LEG-04 proofs across the full ALLOW and REFUSE populations</name>
  <files>firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` — `_REAL_DB`, `_readback_operator` (plan `134-02`), and the existing derive-side tests.
    - `firestarter_app/tests/test_sdp_db_invariant.py` — the committed 43/41/84 ALLOW/REFUSE population snapshot and how it is iterated (Phase 131 F-01/F-02: DB-only count legs live here, not in `test_sdp_table_parity.py`, which is `requires_fw`-skipped under CI-parity leg 1).
    - `firestarter_app/firestarter/sdp_capability.py` `:266`.
    - `firestarter_app/firestarter/chip_test.py` — `_SDP_LEG_STEP_ORDER` as written in Task 1.
  </read_first>
  <action>
Add tests to `firestarter_app/tests/test_chip_test_sdp_leg.py`, named so VALIDATION.md's selectors
resolve (`-k "derive and allow"`, `-k "derive and refuse"`, `-k "baseline_transition"`).

**LEG-01 — `derive and allow`:**
- Iterate the ALLOW population (source it from `sdp_capability` over the DB, the same way
  `tests/test_sdp_db_invariant.py` does; do not hard-code a name list) and assert every ALLOW chip's
  `write_scope="full"` plan carries exactly the six ops of `_SDP_LEG_STEP_ORDER`, all `supported=True`.
  Assert the count as `len(_SDP_LEG_STEP_ORDER)`, never as a literal `6` — the order tuple is the single
  source (P-08).
- Assert **no new CLI option**: `dev test`'s Click command still exposes zero options. Assert it
  structurally — inspect `firestarter.cli_handlers.dev_test.params` (or the registered Click command's
  `params`) and assert it contains no `click.Option` instances. An exit-code-only test would pass
  vacuously.
- Assert `sdp_capability` is the derivation source: patch it to return a REFUSE tuple for a chip that
  is really ALLOW and assert the derived steps flip to `supported=False`. This proves derivation, not
  coincidence.

**LEG-02 — `derive and refuse`:** iterate the REFUSE population and assert every chip's
`write_scope="full"` plan carries the same six ops, all `supported=False`, each `reason` **equal to**
`sdp_capability(name, db)[1]` (identity against the live function, not a substring match), and that
`run_plan` turns each into `VERDICT_NA` with **no operator call** — assert
`operator.sdp_lock.assert_not_called()` and `operator.write_eprom.assert_not_called()` on at least one
representative REFUSE chip. Pin the count at **six** via `len(_SDP_LEG_STEP_ORDER)`.

**LEG-04 — `baseline_transition`:** assert the ordering contract for an ALLOW chip's `write_scope="full"`
plan:
- `write-baseline-b` and `write-baseline-a` both appear **strictly before** `sdp-lock`;
- `write-inhibited` appears strictly after `sdp-lock` and strictly before `sdp-unlock`;
- `write-restored` is the **last** step in the plan;
- both baseline directions are present (a single-direction baseline is what D-07 rejected).
Add a docstring stating why the B direction is load-bearing: pattern A is already what the shipped
`write` step writes, so a chip already holding A with a dead write path would pass an A-only baseline —
the B direction is the entire discriminating power.

**Population-count sanity:** assert the ALLOW and REFUSE populations sum to the full SDP-classified
population and that ALLOW is non-empty, deriving both numbers from the live DB rather than restating
43/41/84 as literals in an assertion (a comment recording the measured 43/41/84 is fine).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test_sdp_leg.py -k "derive or baseline_transition" -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_chip_test_sdp_leg.py -k "derive and allow" -o addopts="" -q` selects ≥ 2 tests, all pass.
    - `pytest tests/test_chip_test_sdp_leg.py -k "derive and refuse" -o addopts="" -q` selects ≥ 2 tests, all pass.
    - `pytest tests/test_chip_test_sdp_leg.py -k "baseline_transition" -o addopts="" -q` selects ≥ 1 test, passes.
    - `grep -c '== 6\b' tests/test_chip_test_sdp_leg.py` shows no new literal-six count assertion; the count is asserted via `len(_SDP_LEG_STEP_ORDER)`.
    - `grep -q 'params' tests/test_chip_test_sdp_leg.py` — the zero-CLI-option assertion is structural, inspecting the Click command's `params`.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` green, coverage ≥ 70%.
  </acceptance_criteria>
  <done>LEG-01, LEG-02 and LEG-04's derivation halves are proven across the whole population.</done>
</task>

<task type="auto">
  <name>Task 3: D-18's `write_scope="none"` proofs and a byte-identical regression-floor check</name>
  <files>firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/firestarter/chip_test.py` — the `write_execute` branch as written in Task 1.
    - `firestarter_app/tests/test_chip_test.py` `:560-600`, `:615-676`, `:1700-1730` — the shipped `locked_destructive` assertions this branch must leave byte-identical.
    - `firestarter_app/firestarter/cli_handlers.py` `:2019-2070` (`_resolve_write_scope`) — the proof that `"none"` is unreachable from `dev test`.
    - `.planning/REQUIREMENTS.md` §LEG — the four `[x]` rows (LEG-09/10/11/15) and their named tests.
  </read_first>
  <action>
Add two tests to `firestarter_app/tests/test_chip_test_sdp_leg.py`:

1. **ALLOW chip at `write_scope="none"`** — assert `plan.steps` contains **none** of
   `_SDP_LEG_STEP_ORDER`, and that `plan.locked_destructive` contains all six as `(op, reason)` pairs
   with non-empty reasons. Assert the entries count toward `count_applicable`'s `M` (call
   `count_applicable(plan, results)` and assert `n_ran < m_applicable`) so D-18's stated polarity —
   the banner fires — is measured rather than asserted in prose. **Do not edit `count_applicable`.**
2. **REFUSE chip at `write_scope="none"`** — assert the derived plan is byte-equivalent to today's:
   `[s.op for s in plan.steps] == ["id", "read", "blank-check"]` and `plan.locked_destructive` has
   exactly its three shipped entries. Docstring: this is the branch that keeps LEG-10's named proof
   (`test_empty_registry_noop`) green, and `"none"` is unreachable from `dev test` since Phase 121's
   reversal, so this is library/test surface.

Then verify the regression floor **explicitly, by name**, and record the result in the SUMMARY:

```
.venv/ci-replica/bin/python -m pytest \
  "tests/test_chip_test_sdp_leg.py::test_unlock_exempt_from_destructive" \
  "tests/test_chip_test_sdp_leg.py::test_gate_closed_from_start" \
  "tests/test_chip_test_sdp_leg.py::test_lock_ran_then_gate_closes" \
  "tests/test_chip_test_sdp_leg.py::test_finally_drains_on_exception" \
  "tests/test_chip_test_sdp_leg.py::test_keyboard_interrupt_drains_and_propagates" \
  "tests/test_chip_test_sdp_leg.py::test_system_exit_drains_and_propagates" \
  "tests/test_chip_test_sdp_leg.py::test_empty_registry_noop" \
  "tests/test_chip_test_sdp_leg.py::test_drain_continues_after_failure" \
  "tests/test_chip_test_sdp_leg.py::test_drain_does_not_mutate_results" \
  "tests/test_chip_test_sdp_leg.py::test_serial_timeout_degrades_one_step" \
  "tests/test_chip_test_sdp_leg.py::test_hardware_error_degrades_one_step" \
  "tests/test_chip_test_sdp_leg.py::test_run_fatal_escapes" \
  "tests/test_chip_test_sdp_leg.py::test_assertion_error_propagates" \
  -o addopts="" -q
```

Also run `git diff --stat` against the fork point for `tests/test_chip_test_sdp_leg.py` and confirm the
diff is **additive only** in the regions holding those tests — no line inside a Phase-133 named proof
was edited. Record the confirmation. If any of those tests required a change to stay green, **stop and
report it rather than editing it**: that is a regression-floor breach, not a routine fix.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test_sdp_leg.py tests/test_chip_test.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - The two new `write_scope="none"` tests exist and pass.
    - The 13 named Phase-133 tests above all pass, and the SUMMARY quotes the pytest count line verbatim.
    - `git diff -U0 <fork-point> -- tests/test_chip_test_sdp_leg.py | grep '^-' | grep -v '^---'` shows no deletion inside any Phase-133 named proof; the SUMMARY records this check and its output.
    - `pytest tests/test_chip_test.py -k "locked_destructive or advisory or strip_default" -o addopts="" -q` passes with the three exact-equality assertions unchanged.
    - `tools/ci_replica_venv.sh` exits 0; `mypy errors: N (watermark: 35)` with N ≤ 35 recorded in the SUMMARY.
    - `tools/ci_parity.sh` exits 0 (leg 1 with `FIRESTARTER_FW_ROOT` pointed at an empty dir).
  </acceptance_criteria>
  <done>D-18's refinement is proven on both branches and the Phase-133 regression floor is byte-identically intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `chip_database.json` → `sdp_capability` → `derive_plan` | a DB entry decides whether six destructive steps are emitted at a stranger's part |
| `derive_plan` → `run_plan` | an emitted `supported=True` step becomes a real bus operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-10 | Elevation of Privilege | a REFUSE chip receiving a real SDP write | critical | mitigate | Capability is derived from `sdp_capability()` (fail-closed, count-pinned); REFUSE ⇒ `supported=False` ⇒ `run_plan:877-879` makes no operator call, asserted by `assert_not_called` |
| T-134-11 | Tampering | a DB field widening the write window for the leg | high | mitigate | `write_region` is the region `derive_plan` already decided from module constants; `_write_region_for` never re-derives UV-ness |
| T-134-12 | Repudiation | shipping six steps while asserting four | medium | mitigate | `_SDP_LEG_STEP_ORDER` single-sources the count; both readings recorded in code and SUMMARY (D-06) |
| T-134-13 | Denial of Service | six extra destructive steps on an unreachable code path | low | accept | `write_scope="none"` is unreachable from `dev test`; the branch is library/test surface and is documented as such |
| T-134-SC | Tampering | package-manager installs | n/a | accept | No new package installed by this phase |
</threat_model>

<verification>
- `pytest tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_op_registration_parity.py tests/test_dev_test_cmd.py -o addopts="" -q` green.
- The 13 named Phase-133 proofs pass, quoted by name in the SUMMARY.
- `tools/ci_replica_venv.sh` and `tools/ci_parity.sh` both exit 0.
</verification>

<success_criteria>
- All 43 ALLOW chips derive the six-step leg from `sdp_capability()`; all 41 REFUSE chips derive six NA steps carrying the refusal reason.
- `dev test` still exposes zero CLI options, asserted structurally.
- D-06's correction (six, not four) is recorded with both readings.
- The parity table carries zero temporary or stale Phase-134 rows.
- Phase 133's regression floor is byte-identically green.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-03-SUMMARY.md` when done.
</output>
