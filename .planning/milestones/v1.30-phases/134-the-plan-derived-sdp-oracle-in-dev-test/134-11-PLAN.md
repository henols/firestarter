---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 11
type: execute
wave: 9
depends_on: ["134-09", "134-10"]
files_modified:
  - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-GH20-TRIAGE.md
  - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md
  - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md
  - .planning/todos/pending/at28c256-write-path-failure-gh20.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LEG-18]
must_haves:
  truths:
    - "gh#20 is triaged against the new baseline-transition gate and the finding is **recorded** — not posted. D-16 routes the public reply to Phase 137 behind CLOSE-06's blocking operator wording review, so all outward-facing wording clears one review at close."
    - "The finding names the measured facts: gh#20 OPEN since 2026-07-30, host `3.0.0b14`, `Rev 2.3`, chip `at28c256`, protocol `13`, `schema_version 1.2`, steps `id NA / read OK / blank-check BAD / write BAD (indeterminate) / verify BAD (indeterminate) / erase NA`, banner `n_ran 4, m_applicable 4`, vpp 11800 mV / vpe 13700 mV, dedup `00e121446ceb`, db_diff `supported` / `community-fail`."
    - "Under this phase's leg, `write-baseline-b` goes BAD on that bench, the baseline gate closes, and **no lock is ever emitted** — whereas a leg without the gate would emit `sdp-lock` at a part that cannot be rewritten. Its banner drops from `4 of 4` to **5 of 10**."
    - "D-11's cost is named concretely inside the finding: the six added steps orphan gh#20's `dedup_fingerprint` `00e121446ceb`, every ALLOW chip re-keys, and the outward description is handed to Phase 137's release notes (CLOSE-05)."
    - "The underlying AT28C256 write/verify/blank-check failure is filed as a **backlog item with a named owner** (D-16), so it does not become another unowned acknowledgement. It is a separate, still-open defect this phase only triages."
    - "The record carries BOTH readings of every correction: criterion 1 / LEG-01 / LEG-02's 'four-step leg' versus the measured **six** (D-06); the exit precedence's source comment and docstring versus the shipped max (D-14); D-08's 'sdp-unlock is never attempted' clause versus D-20's supersession; and the plan-time refinement of D-18's write-scope-none branch."
    - "The Evidence Ceiling is restated verbatim from `133-RECORD.md` §6 and is not smoothed over anywhere: a locked die is unrepresentable in either repo's stubs, no fixture simulates real inhibition, and the causal claim 'the lock inhibited the write' is NOT provable this milestone."
    - "`134-CI-PARITY.md`'s after-half is recorded from real `tools/ci_parity.sh` and `tools/ci_replica_venv.sh` runs, with the board state recorded as an observation because `ci_parity.sh` has no no-board leg."
  artifacts:
    - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-GH20-TRIAGE.md
    - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md
    - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md
    - .planning/todos/pending/at28c256-write-path-failure-gh20.md
  key_links:
    - "No `gh issue comment`, `gh issue close`, `gh pr`, `git push`, `git tag` or `gh workflow run` appears anywhere in this plan. The gh#20 reply is Phase 137's (CLOSE-06); `gh` is used read-only, if at all."
    - "`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are edited with SCOPED `Edit` calls only — never a whole-file `Write`. A whole-file write destroys phase entries outside the diff window, and `phase.complete` has been observed clobbering an unrelated phase's `**Plans:**` line."
---

<objective>
Close LEG-18: record the gh#20 triage finding against the new baseline gate, file the underlying
AT28C256 write-path failure as a backlog item with a named owner, capture the after-half of the
CI-parity record, and write the phase record with both readings of every correction.

Purpose: gh#20 is the milestone's argument, not an aside — a real community report whose bench is the
live instance of "lock a part whose baseline write never worked". Recording it is how this phase
demonstrates the gate has a purpose outside a fixture.
Output: `134-GH20-TRIAGE.md`, a backlog item with an owner, the completed `134-CI-PARITY.md`, and
`134-RECORD.md`.
</objective>

<repo_topology>
**This plan writes ONLY meta-repo files** under `/workspaces/.planning/`. Commit in `/workspaces` on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`. It touches **no** file inside the
`firestarter_app` submodule and **no** file inside `/workspaces/firestarter/` (the Arduino firmware
submodule, untouched by this phase).
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/133-sdp-leg-mechanism/133-RECORD.md
@.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:**
- **LEG-18** — gh#20 triaged against the baseline gate, with the finding **recorded**. ⚠ Tick it only
  when **both** deliverables exist: the finding artifact AND the backlog item naming an owner. LEG-18's
  text says the finding is *"recorded"*, not posted — do not treat a public comment as evidence, and do
  not post one.

**Contributes to but MUST NOT tick:** nothing. Every other requirement is closed by an earlier plan.
Re-read the tick ledger before editing `REQUIREMENTS.md`: LEG-03 to `134-01`; LEG-05/07/08/16 to
`134-02`; LEG-01/02/04 to `134-03`; LEG-06 to `134-05`; LEG-12 to `134-07`; LEG-14 to `134-09`;
LEG-13/17 to `134-10`.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09/10/11/15 (already `[x]`, Phase 133). No RELOCK-\*,
CHAN-\*, CLOSE-\* row. ⚠ **No release notes, no honesty ledger, no claim gate, no public reply, no
`tools/check_*.py` scanner** — all Phase 137.

## Artifacts this phase produces (new symbols introduced by THIS plan)

| Artifact | Path | Kind |
|---|---|---|
| `134-GH20-TRIAGE.md` | `.planning/phases/134-…/` | the LEG-18 finding |
| `134-RECORD.md` | `.planning/phases/134-…/` | the phase's closing record |
| `at28c256-write-path-failure-gh20.md` | `.planning/todos/pending/` | the owned backlog item |
| the `## After (post-edit)` section | `.planning/phases/134-…/134-CI-PARITY.md` | record section |
| a `999.NN` ROADMAP Backlog row | `.planning/ROADMAP.md` | backlog entry |

<tasks>

<task type="auto">
  <name>Task 1: the gh#20 triage finding and the owned backlog item</name>
  <files>.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-GH20-TRIAGE.md, .planning/todos/pending/at28c256-write-path-failure-gh20.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md` §4.8 — the live-captured gh#20 body, field by field.
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md` D-11 and D-16 in full.
    - `.planning/REQUIREMENTS.md` §"⚠ Evidence Ceiling" (lines 14–40) and §LEG's LEG-18 row.
    - `.planning/phases/133-sdp-leg-mechanism/133-RECORD.md` §6 — the Evidence Ceiling wording to reuse **verbatim**.
    - `.planning/todos/pending/write-sdp-relock-deferred.md` — the shape and front matter of an existing pending todo.
    - `.planning/ROADMAP.md` section `## Backlog` — read it to find the next free `999.NN` number; do not guess.
  </read_first>
  <action>
**A. Write `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-GH20-TRIAGE.md`** with six
sections:

1. **The report, as measured.** Every field from RESEARCH §4.8: state OPEN, created 2026-07-30, the
   title, host `3.0.0b14`, `hw_revision Rev 2.3`, chip `at28c256`, protocol `13`, `schema_version 1.2`,
   the six step verdicts (`id NA`, `read OK`, `blank-check BAD`, `write BAD` with fingerprint
   `indeterminate`, `verify BAD` with fingerprint `indeterminate`, `erase NA`), banner
   `n_ran 4, m_applicable 4`, voltage `vpp 11800 mV` / `vpe 13700 mV` with before equal to after,
   `dedup_fingerprint 00e121446ceb`, `db_diff supported / community-fail`.
   Re-verify the issue state **read-only** with `gh issue view 20 --repo henols/firestarter_prom` and
   record whether anything changed since the research pass.

2. **The triage against the baseline gate.** State the mechanism concretely: on that bench
   `write-baseline-b` reports BAD, `_baseline_closes_sdp_gate` closes, and `sdp-lock`,
   `write-inhibited`, `sdp-unlock` and `write-restored` all render SKIPPED — **no lock is ever
   emitted**. A leg without the gate would emit `sdp-lock` at a part that cannot be rewritten. Cite the
   committed proof by test name (plan `134-04`'s dead-write-path gate test).

3. **The banner change.** `4 of 4 ran` today — which reads as complete — becomes **`5 of 10`** under
   this leg. Cite plan `134-10`'s pinning test by name.

4. **D-11's cost, named.** gh#20's `dedup_fingerprint` `00e121446ceb` is **orphaned** by the six added
   steps: b14/b15-era reports stop grouping with v1.30-era ones and accumulated N-greater-or-equal-2
   promotion counts reset for all 43 ALLOW chips. State the cost is **accepted and recorded**, that
   excluding the SDP steps from the hash was rejected because a leaked lock would then dedup identically
   with a held one, and that the **outward description is Phase 137's release notes (CLOSE-05)**.

5. **What this does NOT establish — the Evidence Ceiling.** Restate `133-RECORD.md` §6's wording
   verbatim. This triage explains what the tool would now do on that bench; it does **not** diagnose the
   chip and does **not** establish that any lock inhibited any write. A locked die is unrepresentable in
   either repo's stubs and no fixture in this phase simulates real inhibition. State explicitly that the
   leg is **not** protected by the chip-ID gate — all 43 SDP-ALLOW chips have `chip-id == 0`, so that
   gate is structurally vacuous for this population, and saying otherwise is the v1.22 C-5 overclaim
   class.

6. **Hand-off.** The **public reply is Phase 137's** (CLOSE-06, behind the blocking operator
   wording-review gate), alongside the gh#12 reply. This phase records only (D-16).

**B. File the underlying defect as a backlog item with a named owner** (D-16). It is a separate,
still-open defect this phase only triages. File it consistently in two places:

1. `.planning/todos/pending/at28c256-write-path-failure-gh20.md`, following the front matter and shape
   of `.planning/todos/pending/write-sdp-relock-deferred.md`. It must carry an explicit
   **`Owner: henols`** line (the repo owner / operator), state that an owner is named precisely so this
   does not become another unowned acknowledgement, and note it may be reassigned. Summarise the
   symptom — `blank-check`, `write` and `verify` all BAD with `indeterminate` fingerprints on Rev 2.3 at
   vpp 11800 / vpe 13700 mV — link the triage artifact, and state that the public reply is Phase 137's.
2. A `## Backlog` row in `.planning/ROADMAP.md` at the next free `999.NN` number, inserted with a
   **scoped `Edit`**. Read the section first to find the number; do not guess, and **never** use a
   whole-file `Write` on `ROADMAP.md`.

⚠ **No privileged command anywhere in this plan's execution or in any file it writes:** no
`gh issue comment`, `gh issue close`, `gh issue edit`, `gh pr`, `gh workflow run`, `gh release`,
`git push`, `git merge`, `git tag`, or `twine upload`. `gh` is read-only here.
  </action>
  <verify>
    <automated>grep -l '00e121446ceb' /workspaces/.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-GH20-TRIAGE.md /workspaces/.planning/todos/pending/at28c256-write-path-failure-gh20.md</automated>
  </verify>
  <acceptance_criteria>
    - `134-GH20-TRIAGE.md` exists and contains the literals `3.0.0b14`, `Rev 2.3`, `00e121446ceb`, `11800`, `13700`, `4 of 4` and `5 of 10`.
    - It contains the phrase `no lock is ever emitted` (or an equivalent explicit statement) in its triage section, and names the committed gate test.
    - It contains the Evidence Ceiling restatement and the explicit statement that the leg is not protected by the chip-ID gate.
    - `.planning/todos/pending/at28c256-write-path-failure-gh20.md` exists and contains a line matching `Owner: henols`.
    - `grep -c '999\.' /workspaces/.planning/ROADMAP.md` increased by exactly 1 versus before the edit, and `git diff --stat .planning/ROADMAP.md` shows a single-hunk change confined to the `## Backlog` section.
    - `grep -rnE 'gh (issue (comment|close|edit)|pr|workflow run|release)|git (push|merge|tag)|twine upload' /workspaces/.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-GH20-TRIAGE.md /workspaces/.planning/todos/pending/at28c256-write-path-failure-gh20.md` returns nothing.
    - `git -C /workspaces/firestarter_app status --porcelain` prints nothing — no submodule file touched.
  </acceptance_criteria>
  <done>LEG-18's two deliverables exist: the recorded finding and an owned backlog item.</done>
</task>

<task type="auto">
  <name>Task 2: the after-half of the CI-parity record</name>
  <files>.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md</files>
  <read_first>
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md` — the `## Before (pre-edit)` section written by plan `134-01`.
    - `.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md` §4 and §5 — the certifying-numbers and file-count-accounting shape.
    - `firestarter_app/tools/ci_parity.sh` and `firestarter_app/tools/ci_replica_venv.sh`.
    - `.planning/phases/134-…/134-VALIDATION.md` §"Sampling Rate".
  </read_first>
  <action>
Append a `## After (post-edit)` section to
`.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md`, using a **scoped
`Edit`** rather than rewriting the file.

Run, from `/workspaces/firestarter_app`, and transcribe verbatim:
1. `tools/ci_parity.sh` — every leg's exit status. Record that leg 1 points `FIRESTARTER_FW_ROOT` at an
   **empty dir**, because the devcontainer's sibling layout otherwise masks CI-only test defects, and
   that this recipe has **no no-board leg** — record the observed board state
   (`ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null`) as an ambient observation, never as a leg.
2. `tools/ci_replica_venv.sh` — all five legs, plus the certifying `mypy errors: N (watermark: 35)` and
   `checked N source files` lines.
3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — the passed count and coverage.

Then write a short delta table against the `## Before` section:
- mypy errors before vs after, with the **remaining headroom** stated against the watermark 35 (the
  phase started at 2);
- `checked` source files before vs after, restating that `MIN_CHECKED_SOURCE_FILES = 120` is a **FLOOR**
  so an increase is safe (correction 4);
- test count and coverage before vs after;
- the number of new test files and new production symbols this phase added.

If the mypy count exceeds the watermark, **stop and report it** — do not edit `pyproject.toml`. The
watermark is explicitly out of scope for this phase; this phase spends against it. Ratcheting it to the
measured count remains unowned (133-RECORD residual 4) and is not adopted here.
  </action>
  <verify>
    <automated>grep -c 'After (post-edit)' /workspaces/.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `134-CI-PARITY.md` contains both a `## Before (pre-edit)` and a `## After (post-edit)` heading.
    - The after-half contains a literal `mypy errors: <N> (watermark: 35)` line copied from `tools/ci_replica_venv.sh` output, with N ≤ 35, and a `checked <N> source files` line with N ≥ 120.
    - It contains a delta table naming mypy errors, checked files, test count and coverage before and after.
    - It records the observed board state and states in prose that `ci_parity.sh` has no no-board leg.
    - `git diff --stat .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md` shows an additive change only — the `## Before` section is byte-unchanged.
    - `git -C /workspaces/firestarter_app status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>The phase's budget spend is measured and recorded against its own pre-edit baseline.</done>
</task>

<task type="auto">
  <name>Task 3: `134-RECORD.md` — every correction with both readings, and the requirement tick</name>
  <files>.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/133-sdp-leg-mechanism/133-RECORD.md` in full — the record's section shape (§2 non-literal honourings, §3 criteria, §4 corrections, §5 residuals, §6 Evidence Ceiling).
    - Every `134-NN-SUMMARY.md` produced by plans `134-01` through `134-10`.
    - `.planning/ROADMAP.md` §"Phase 134" — the goal, the five success criteria, and the cross-cutting rule.
    - `.planning/REQUIREMENTS.md` §LEG — all fourteen rows plus the four already `[x]`.
    - `.planning/phases/134-…/134-VALIDATION.md` §"Non-Vacuity Obligations" — all seven rows.
  </read_first>
  <action>
Write `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md` in the
`133-RECORD.md` shape.

**§1 — Requirements.** All fourteen (LEG-01…08, 12, 13, 14, 16, 17, 18) with, for each, the plan that
ticked it and the named test or artifact that is its evidence. State explicitly that LEG-09/10/11/15
were **not** re-ticked and that their named Phase-133 proofs were verified byte-identically green.

**§2 — Decisions honoured, including non-literally.** All twenty (D-01…D-20). For each of the following,
state what was decided, what was built, and why they differ:
- **D-08's `sdp-unlock` clause** — measured wrong and **superseded by D-20**; record both readings and
  the LEG-09 distinction (destructive gate vs baseline gate) that makes the supersession safe.
- **D-18's `write_scope="none"` branch** — refined at plan time so a REFUSE chip emits nothing on that
  branch, because otherwise LEG-10's named proof `test_empty_registry_noop` and three shipped
  `locked_destructive` exact-equality assertions go RED. Record the four measurements behind the
  refinement and note that `write_scope="none"` is unreachable from `dev test`, so LEG-02 is fully
  satisfied on the live path.
- **P-03 prevention 4's `(False, A) ⇒ OK`** — recorded as **overturned** by correction 1 and D-01/D-03,
  with the reason (the `0x86` ack is unobservable from `chip_test.py`).
- **Research truth-table branch 5** — not implementable as written; recorded as overturned.

**§3 — The five ROADMAP success criteria**, each discharged with named evidence, and each stated at the
Evidence Ceiling. ⚠ Criterion 1 says a **four-step** leg; the leg is **six**. Record both readings and
why the four is wrong (it predates LEG-04's two transition directions, and the ROADMAP's own enumeration
omits `write-restored`, the only step producing evidence the part was left writable). ⚠ Criterion 2's
"exit code 1" was **unreachable on any marginal-bearing run** until D-14 landed; record that.

**§4 — Corrections, with both readings.** At minimum:
1. the four-versus-six step count (D-06);
2. the exit precedence — the source comment and docstring versus the shipped `max` (correction 3, D-14);
3. `MIN_CHECKED_SOURCE_FILES` is a FLOOR, not a spent budget (correction 4, inverting 133 D-15);
4. `_DECLARED_REGISTRY_COUNT` does not exist — the real names are `_POLICED_REGISTRY_COUNT` and
   `_DECLARED_NON_REGISTRY_COUNT` — and only **two** parity exemption rows were dischargeable, not five;
5. the stale-row guard fails closed on a **non-existent** op or registry name but does **not** catch a
   row that has become redundant, so discharging the temporary rows was a discipline obligation with a
   grep criterion, not a gate outcome;
6. `derive_plan`'s emission necessarily changed `tests/test_chip_test.py`'s AT28C256 0x0D sweep, which
   was **repaired** (a read-back-capable double) rather than weakened — no skip, no xfail, no narrowed
   assertion;
7. the chip-ID destructive gate is **structurally vacuous** for the whole SDP population (all 43 ALLOW
   chips have `chip-id == 0`), so R1/R2 are unreachable in production today and no artifact claims the
   leg is gated by chip ID.

**§5 — The seven non-vacuity obligations.** One row each, naming the planted break, the verbatim RED
evidence quoted in the owning plan's SUMMARY, and the confirmation of byte-identical restoration. Also
name the **seventh route** to a non-running oracle (the baseline gate) beyond research's R1–R6, and
state that it fails closed under D-08 + D-15.

**§6 — Residuals, carried not closed.**
- 133 D-07's forfeited report on Ctrl-C: after an interrupt mid-leg, `run_plan` never returns, so neither
  recovery form prints and there is no report at all. Mitigated by the up-front notice, **not** closed;
  fixing it needs `run_plan`'s signature and all twelve call sites. **No owner.**
- The mypy watermark ratchet — still unowned (133-RECORD residual 4), and headroom is now whatever
  `134-CI-PARITY.md` §After measured.
- D-11's `dedup_fingerprint` discontinuity — accepted here, outward description handed to Phase 137
  CLOSE-05.
- `.planning/codebase/TESTING.md` is severely stale and will mislead any agent that reads it.
- gh#20's underlying AT28C256 defect — now owned (Task 1), still open.

**§7 — The Evidence Ceiling**, restated verbatim from `133-RECORD.md` §6. No fixture in this phase
simulates real inhibition; every fixture pins the host's **response** to a scripted read-back. The
causal claim "the lock inhibited the write" is **not provable this milestone**, real silicon is missing
with no fallback, and nothing in this phase's artifacts may say otherwise.

**Then tick LEG-18** in `.planning/REQUIREMENTS.md` with a **scoped `Edit`**: change `- [ ] **LEG-18**`
to `- [x] **LEG-18**` and append an evidence clause naming `134-GH20-TRIAGE.md`, the backlog item and
its owner, and the honest qualifier that the finding is recorded and the public reply is Phase 137's.
**Do not touch any other requirement row.** Before committing, run
`git diff .planning/REQUIREMENTS.md` and confirm the diff is confined to the LEG-18 row.

Finally, verify the full tick ledger: exactly fourteen LEG rows are `[x]` for this phase's set plus the
four from Phase 133 — eighteen `[x]` LEG rows in total — and no RELOCK-\*, CHAN-\* or CLOSE-\* row
changed.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*LEG-' /workspaces/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `134-RECORD.md` exists with sections covering requirements, decisions, the five success criteria, corrections, the seven non-vacuity obligations, residuals, and the Evidence Ceiling.
    - It names all twenty decisions D-01 through D-20 at least once.
    - It records both readings for the four-versus-six step count and for the exit precedence.
    - `grep -c '^- \[x\] \*\*LEG-' /workspaces/.planning/REQUIREMENTS.md` returns 18.
    - `git diff .planning/REQUIREMENTS.md` is confined to the LEG-18 row (the SUMMARY quotes the diff).
    - `grep -c '^- \[ \] \*\*RELOCK-\|^- \[ \] \*\*CHAN-\|^- \[ \] \*\*CLOSE-' /workspaces/.planning/REQUIREMENTS.md` is unchanged from before this task.
    - `git -C /workspaces/firestarter_app status --porcelain` prints nothing.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing — the firmware submodule is untouched by this phase.
  </acceptance_criteria>
  <done>The phase record exists, every correction carries both readings, and LEG-18 is the only requirement this plan ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase record → future readers and Phase 137's ledger | the durable claim about what this phase proved |
| triage artifact → a community member's open issue | a statement about someone else's hardware |
| planning-file edits → the whole roadmap | a whole-file write destroys entries outside the diff window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-42 | Repudiation | claiming the lock was proven to inhibit a write | critical | mitigate | Evidence Ceiling restated verbatim in the record and the triage; every criterion phrased at that ceiling |
| T-134-43 | Repudiation | an unowned acknowledgement of a real community defect | high | mitigate | The backlog item carries `Owner: henols` and is filed in both the todo store and the ROADMAP backlog |
| T-134-44 | Tampering | a whole-file `Write` on ROADMAP.md destroying unrelated phase entries | high | mitigate | Scoped `Edit` only, with a single-hunk `git diff` assertion in the acceptance criteria |
| T-134-45 | Elevation of Privilege | posting an outward-facing reply before the claim gate is armed | high | mitigate | No privileged `gh` or `git` command; the reply is Phase 137's behind CLOSE-06's blocking operator review, asserted absent by grep |
| T-134-46 | Repudiation | over-ticking requirements (this project did it 4x in Phase 116) | high | mitigate | This plan ticks LEG-18 only; the tick ledger is verified at 18 `[x]` LEG rows and the diff confined to one row |
| T-134-SC | Tampering | package-manager installs | n/a | accept | No new package installed by this phase |
</threat_model>

<verification>
- `134-GH20-TRIAGE.md`, `134-CI-PARITY.md` (before + after), `134-RECORD.md` and the owned backlog item all exist.
- `grep -c '^- \[x\] \*\*LEG-' .planning/REQUIREMENTS.md` returns 18.
- No privileged command appears in any artifact; `gh` was used read-only.
- Both submodules are clean — this plan writes meta-repo files only.
</verification>

<success_criteria>
- gh#20 is triaged against the baseline gate with the finding recorded and the underlying defect owned.
- The CI-parity record has both halves with real `ci-replica` numbers and a delta table.
- The phase record carries both readings of every correction and all seven non-vacuity observations.
- LEG-18 is ticked and is the only requirement row this plan changed.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-11-SUMMARY.md` when done.
</output>
