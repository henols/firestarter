---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 07
type: execute
wave: 6
depends_on: ["134-05", "134-06"]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/test_dev_test_cmd.py
autonomous: true
requirements: [LEG-12, LEG-13]
must_haves:
  truths:
    - "Every run against an ALLOW chip renders `HELD` / `NOT-HELD` / `NOT-RUN(reason)` in **both** the human report and the JSON artifact (LEG-12), because `cli_handlers.py` assigns `chip_test.sdp_hold_state(plan, results)` to the report field at the derive-in-engine / assign-in-handler seam."
    - "A NOT-RUN oracle on an ALLOW chip keeps verdict `SKIPPED` and gets an exit FLOOR of 2 (D-15) — `marginal` was rejected because `_RAN_VERDICTS = {OK, BAD, MARGINAL}` counts marginal as *ran*, which would hold `N == M` and defeat LEG-13 outright."
    - "The floor composes AFTER D-14's precedence and BAD OUTRANKS IT: a run that is both BAD and NOT-RUN exits **1**, not 2 — otherwise the floor re-creates the very laundering D-14 removed. A naive `max(code, 2)` gets this WRONG (`max(1, 2) = 2`); the floor contributes a candidate code into the precedence selection instead."
    - "The floor applies only when the chip is ALLOW — a REFUSE chip legitimately reads NOT-RUN and must not be floored (D-15's own scoping; LEG-13 says 'for ALLOW chips')."
    - "Stated cost, pinned by a test: the exit code stops being a pure function of step verdicts. It gains exactly ONE non-verdict term (D-15)."
    - "`count_applicable` is NOT edited — D-15 measured that for an ALLOW chip the SDP steps already carry `supported=True` and count toward M while a SKIPPED result is already excluded from N, so the ratio ALREADY drops; and editing it would add op vocabulary to a declared non-registry and trip the parity gate's inversion guard."
  artifacts:
    - firestarter_app/firestarter/cli_handlers.py
    - firestarter_app/tests/test_dev_test_cmd.py
  key_links:
    - "The assignment seam is `cli_handlers.py:2161-2175` — `results = run_plan(...)`, `report.results = results`, `report.banner = count_applicable(plan, results)`, `report.db_diff = build_db_diff(...)`. Every derived value is computed in an engine module and *assigned* here; `sdp_hold_state` follows the same shape."
    - "133 D-07's residual is NOT closed here and must not be claimed closed: after a Ctrl-C mid-leg, `results = run_plan(...)` never returns, so there is no report at all and neither recovery form prints. The mitigation is plan `134-08`'s rewritten up-front notice, not a `finally` handler."
---

<objective>
Wire the engine's `sdp_hold_state` derivation into the report at the existing derive-in-engine /
assign-in-handler seam, and add D-15's single non-verdict exit term so a non-running oracle on an ALLOW
chip cannot exit 0.

Purpose: LEG-12 exists so a non-running oracle is visible to a community reporter **even at exit 0** —
and D-15 exists so that run does not return 0 at all, because `firestarter dev test at28c256` returning
0 gets filed as PASS (the P-04 shape).
Output: LEG-12 closed in both surfaces, and the exit floor composed in the one order that keeps BAD on
top.
</objective>

<repo_topology>
All code lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`, commit
THERE on `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`). `/workspaces/firestarter/` is the Arduino
firmware submodule and is **not touched by this phase at all**.
`firestarter_app/firestarter/eprom_operations.py` is **RING-FENCED** — call only.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-PATTERNS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-05-SUMMARY.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-06-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:**
- **LEG-12** — the `HELD` / `NOT-HELD` / `NOT-RUN(reason)` field in **both** surfaces on every ALLOW-chip
  run. Both halves now exist: the field and its serialisation (plan `134-06`) and the assigned value
  (here). ⚠ Tick it against an **end-to-end CLI run** on an ALLOW chip that asserts the value in the
  rendered output **and** in the JSON artifact, including the `NOT-RUN` case's reason.

**Contributes to but MUST NOT tick:**
- **LEG-13** — the exit floor and the SKIPPED-drops-the-ratio behaviour are wired here, but LEG-13's
  discharging evidence is a **pinning test** on `count_applicable` for an ALLOW chip, which is plan
  `134-10`'s. **Closed by plan `134-10`.** LEG-13 is one of the phase's three highest-risk premature
  ticks.
- **LEG-06** — already closed by plan `134-05`; do not re-tick.
- **LEG-14** — closed by plan `134-09`. **LEG-17** — closed by plan `134-10`.
  **LEG-18** — closed by plan `134-11`.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09/10/11/15 (already `[x]`, Phase 133). No RELOCK-\*,
CHAN-\*, CLOSE-\* row.

## Artifacts this phase produces (new symbols introduced by THIS plan)

| Symbol | File | Kind |
|---|---|---|
| `_dev_test_exit_code(results, *, sdp_oracle_not_run: bool) -> int` | `firestarter_app/firestarter/cli_handlers.py` | fully-annotated helper (STRICT island) |
| the `report.sdp_hold_state = sdp_hold_state(plan, results)` assignment | `firestarter_app/firestarter/cli_handlers.py` | seam wiring |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assign the hold state at the seam, and compose D-15's floor beneath D-14's precedence</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py` `:2160-2219` — the seam (`run_plan` call `:2164`, `report.results`, `report.banner = count_applicable(...)` `:2166`, `report.render(console)` `:2181`, `json.dumps(report.to_dict(), …)` `:2192`, the markdown table loop `:2194-2205`, `report.db_diff = build_db_diff(...)` `:2178`) and the exit computation at `:2216-2219`.
    - `firestarter_app/firestarter/cli_handlers.py` `:1885-1902` plus `_EXIT_CODE_PRECEDENCE` / `_overall_exit_code` as written by plan `134-05`.
    - `firestarter_app/firestarter/chip_test.py` — `sdp_hold_state`, `sdp_oracle_applicable`, `SDP_HOLD_NOT_RUN` from plan `134-04`; `count_applicable` `:1520-1544` (**read only, do not edit**) and `_RAN_VERDICTS` `:1500`.
    - `firestarter_app/firestarter/diagnostic_report.py` — the `sdp_hold_state` field from plan `134-06`.
    - `.planning/phases/134-…/134-CONTEXT.md` D-12's "Honest residual" paragraph and D-15 in full.
    - `.planning/phases/134-…/134-RESEARCH.md` §4.6 (the composed ordering and assumption A3).
  </read_first>
  <behavior>
    - An ALLOW-chip run whose oracle did not run and whose other steps are all clean exits **2**.
    - An ALLOW-chip run that is both BAD and NOT-RUN exits **1**, not 2.
    - An ALLOW-chip run with a `marginal` step and a NOT-RUN oracle exits **2**.
    - A REFUSE-chip run whose hold state reads NOT-RUN exits **0** when no step is BAD or `marginal` — the floor is ALLOW-only.
    - `report.to_dict()["sdp_hold_state"]` is non-empty on every `dev test` run that produces a report.
  </behavior>
  <action>
**A. Assign the hold state at the seam** (`cli_handlers.py:2161-2175`). Immediately after
`report.banner = count_applicable(plan, results)`, add
`report.sdp_hold_state = sdp_hold_state(plan, results)` and import `sdp_hold_state` /
`sdp_oracle_applicable` from `firestarter.chip_test`. This is the shipped derive-in-engine /
assign-in-handler shape — every derived value here is computed in an engine module and assigned, never
computed inline. It also keeps the logic off the mypy STRICT island (headroom **2**) and inside the
module P-07 requires (`chip_test.py` is scanned in full; `cli_handlers.py` helpers sit behind a
fail-open allow-list, and P-07's `_HANDLER_FUNCTION_NAMES` derived-subset gate landed in Phase 131 so a
new handler helper IS now caught — keep the handler-side addition to constants plus one thin annotated
function).

**B. Compose the exit floor** — replace the call site's `code = _overall_exit_code(results)` with a new
fully annotated helper:

`_dev_test_exit_code(results: list[StepResult], *, sdp_oracle_not_run: bool) -> int`

It computes the observed code set exactly as `_overall_exit_code` does, adds the code `2` to that set
when `sdp_oracle_not_run` is `True`, and then selects by `_EXIT_CODE_PRECEDENCE`. Call it as
`_dev_test_exit_code(results, sdp_oracle_not_run=sdp_oracle_applicable(plan) and
report.sdp_hold_state.startswith(SDP_HOLD_NOT_RUN))`.

⚠ **Do NOT write `code = max(code, 2)`.** `max(1, 2) = 2`, so a run that is both BAD and NOT-RUN would
exit 2 — re-creating exactly the laundering plan `134-05` just removed, and making criterion 2's
"exit code 1" conditional again. Contributing a candidate code into the precedence selection is the
only composition consistent with both D-14's "BAD outranks marginal" and D-15's "at least 2".

⚠ **The floor is ALLOW-only.** `sdp_oracle_applicable(plan)` gates it, so a REFUSE chip — which
legitimately reads NOT-RUN because it has no oracle at all — is not floored. D-15 and LEG-13 are both
scoped to ALLOW chips; record that reading explicitly rather than silently extending it.

Add an in-code comment recording D-15's **stated cost**: the exit code stops being a pure function of
step verdicts and gains exactly one non-verdict term. Also record why the step stays `SKIPPED` rather
than becoming `marginal`: `_RAN_VERDICTS = {OK, BAD, MARGINAL}` counts marginal as *ran*, so recording a
non-running oracle as marginal would hold `N == M` and defeat LEG-13 outright.

**C. Do NOT edit `count_applicable`.** Two independent reasons: D-15 measured that for an ALLOW chip the
six SDP steps already carry `supported=True` and are counted in `M`, while a `SKIPPED` result is already
excluded from `N` — so the ratio already drops — and editing it would add op vocabulary to a declared
non-registry and trip `test_non_registry_still_has_no_ops`.

**D. Record 133 D-07's residual honestly, do not close it.** After a Ctrl-C mid-leg,
`results = run_plan(...)` at `:2164` never returns, so **neither the report nor any recovery line
prints**. The mitigation is plan `134-08`'s rewritten up-front notice, printed where it is guaranteed to
be seen — not a `finally` handler. Fixing it properly needs `run_plan`'s signature and all twelve call
sites and has **no owner**. State the residual in the SUMMARY; do not claim otherwise anywhere.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_cli_handlers.py tests/test_op_registration_parity.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/cli_handlers.py` contains `def _dev_test_exit_code(results: list[StepResult], *, sdp_oracle_not_run: bool) -> int:` and `report.sdp_hold_state = sdp_hold_state(plan, results)`.
    - `grep -c 'max(code, 2)\|max(2, code)' firestarter/cli_handlers.py` returns 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.cli_handlers import _dev_test_exit_code as f;from firestarter.chip_test import StepResult,VERDICT_BAD,VERDICT_OK,VERDICT_MARGINAL;ok=[StepResult(op='a',verdict=VERDICT_OK,run_count=1)];bad=ok+[StepResult(op='b',verdict=VERDICT_BAD,run_count=1)];marg=ok+[StepResult(op='c',verdict=VERDICT_MARGINAL,run_count=1)];assert f(ok,sdp_oracle_not_run=True)==2;assert f(bad,sdp_oracle_not_run=True)==1;assert f(marg,sdp_oracle_not_run=True)==2;assert f(ok,sdp_oracle_not_run=False)==0"` exits 0.
    - `git diff firestarter/chip_test.py` is empty for `count_applicable` (this plan does not touch `chip_test.py` at all — `git diff --stat firestarter/chip_test.py` is empty).
    - `pytest tests/test_op_registration_parity.py::test_non_registry_still_has_no_ops -o addopts="" -q` passes.
    - `tools/ci_replica_venv.sh` exits 0; `mypy errors: N (watermark: 35)`, N ≤ 35, recorded in the SUMMARY.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0.
  </acceptance_criteria>
  <done>The hold state reaches the report, and a non-running oracle can no longer exit 0 without BAD losing its rank.</done>
</task>

<task type="auto">
  <name>Task 2: LEG-12 end to end in both surfaces, plus the exit-floor composition pins</name>
  <files>firestarter_app/tests/test_dev_test_cmd.py</files>
  <read_first>
    - `firestarter_app/tests/test_dev_test_cmd.py` `:144-160` (`_off_tty`, `_reports_dir`, `_load_report`) and the ALLOW-chip read-back operator builder added by plan `134-05`.
    - `firestarter_app/tests/conftest.py` `:229-334` (`make_app_context`, `app_context`).
    - `firestarter_app/firestarter/chip_test.py` — `SDP_HOLD_HELD` / `SDP_HOLD_NOT_HELD` / `SDP_HOLD_NOT_RUN`.
    - `.planning/phases/134-…/134-VALIDATION.md` §"Per-Task Verification Map" rows LEG-12 and LEG-13.
  </read_first>
  <action>
Add tests to `firestarter_app/tests/test_dev_test_cmd.py`, named so `pytest -k "hold"` and
`pytest -k "exit"` both select the relevant ones.

**A. LEG-12 end to end, all three values, both surfaces.** For each of `HELD`, `NOT-HELD` and
`NOT-RUN`, drive the real CLI on the ALLOW chip with an operator shaped to produce that outcome, then
assert:
- the value appears in `result.output` (the human report), and
- `_load_report(<chip>)["sdp_hold_state"]` carries the same value,
- and for the `NOT-RUN` case, that the **reason** survives into both surfaces — LEG-12 says
  `NOT-RUN(reason)`, and a reason reaching only the JSON is half the requirement.
Assert against the imported `SDP_HOLD_*` constants, never against retyped literals.

**B. The NOT-RUN case must be the gated one.** Produce it with the dead-write-path operator shape (the
baseline gate closes), so the test also demonstrates the phase's central safety property end to end:
`operator.sdp_lock.assert_not_called()` on that run. Assert the banner shows the dropped ratio — the
run's `banner` in the JSON artifact must have `n_ran < m_applicable`.

**C. Exit-floor composition pins (D-15, assumption A3).** Four end-to-end assertions:
1. ALLOW chip, oracle NOT-RUN, no BAD and no `marginal` ⇒ `exit_code == 2` (never 0 — the P-04 shape
   this closes is `firestarter dev test at28c256` returning 0 and being filed as PASS).
2. ALLOW chip, oracle NOT-RUN **and** a BAD step ⇒ `exit_code == 1`. Docstring: a naive `max(code, 2)`
   would return 2 here and re-create the laundering D-14 removed.
3. ALLOW chip, oracle NOT-RUN and a `marginal` step ⇒ `exit_code == 2`.
4. REFUSE chip whose hold state reads NOT-RUN, with no BAD or `marginal` step ⇒ `exit_code == 0` — the
   floor is ALLOW-only.

**D. The non-purity cost, pinned.** Add a test whose docstring states D-15's accepted cost outright:
`dev test`'s exit code is no longer a pure function of step verdicts. Construct two runs with the
**identical** verdict multiset that differ only in the hold state, and assert their exit codes differ.
That is the cost, made mechanical.

**E. Evidence Ceiling.** In the new tests' docstrings, state that these fixtures pin the host's
**response** to a scripted read-back and simulate no real inhibition; a locked die is unrepresentable in
either repo's stubs, and the causal claim "the lock inhibited the write" is **not provable this
milestone**.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py -k "hold or exit" -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_dev_test_cmd.py -k "hold" -o addopts="" -q` selects ≥ 4 tests, all pass, covering all three hold values.
    - A test asserts the `NOT-RUN` reason appears in BOTH `result.output` and `_load_report(...)["sdp_hold_state"]`.
    - Four exit-composition tests exist and pass, including the BAD+NOT-RUN ⇒ 1 case and the REFUSE-chip ⇒ 0 case.
    - A test asserts `n_ran < m_applicable` in the JSON artifact's banner for the gated ALLOW run, and `operator.sdp_lock.assert_not_called()` on that run.
    - A test with two identical-verdict-multiset runs asserts differing exit codes, with D-15's cost stated in its docstring.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` green, coverage ≥ 70%.
    - `tools/ci_replica_venv.sh` exits 0; mypy ≤ 35 recorded in the SUMMARY.
  </acceptance_criteria>
  <done>LEG-12 is discharged in both surfaces, and the exit floor is proven in every composition order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine derivation → report field → stranger | the value a community reporter reads as a statement about their part |
| composed exit code → CI / reporter | the number that decides whether a non-running oracle looks like a pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-27 | Repudiation | a non-running oracle exiting 0 and being filed as PASS | critical | mitigate | D-15's ALLOW-only floor of 2, pinned end to end |
| T-134-28 | Repudiation | the floor re-laundering a BAD run into exit 2 | critical | mitigate | Floor contributes a candidate code into `_EXIT_CODE_PRECEDENCE`, never `max`; pinned by the BAD+NOT-RUN ⇒ 1 test |
| T-134-29 | Information Disclosure | a REFUSE chip's legitimate NOT-RUN inflating its exit code | medium | mitigate | `sdp_oracle_applicable(plan)` gates the floor; pinned by the REFUSE ⇒ 0 test |
| T-134-30 | Repudiation | claiming the Ctrl-C report residual is closed | medium | accept | 133 D-07's residual is stated as open, with the up-front notice named as the mitigation and no owner claimed |
| T-134-SC | Tampering | package-manager installs | n/a | accept | No new package installed by this phase |
</threat_model>

<verification>
- `pytest tests/test_dev_test_cmd.py tests/test_cli_handlers.py tests/test_diagnostic_report.py tests/test_op_registration_parity.py -o addopts="" -q` green.
- `tools/ci_replica_venv.sh` 5/5, mypy ≤ 35.
- `git diff --stat firestarter/chip_test.py firestarter/diagnostic_report.py` empty — this plan touches neither.
</verification>

<success_criteria>
- Every ALLOW-chip run renders the hold state in both surfaces, reason included on `NOT-RUN`.
- A clean ALLOW run with a non-running oracle exits 2; a BAD one still exits 1.
- A REFUSE chip is never floored.
- `count_applicable` is untouched and the inversion guard is green.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-07-SUMMARY.md` when done.
</output>
