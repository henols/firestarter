---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 05
type: execute
wave: 5
depends_on: ["134-04"]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/test_dev_test_cmd.py
autonomous: true
requirements: [LEG-06]
must_haves:
  truths:
    - "BAD outranks `marginal` in `dev test`'s exit code (D-14) — restoring the behaviour the source comment at `cli_handlers.py:1885-1887` and `dev_test`'s docstring at `:2119-2121` **already claim**, rather than changing a designed contract."
    - "The shipped code was inverted against both of its own prose claims: `_VERDICT_EXIT_CODES` maps `marginal → 2` and `BAD → 1`, and `code = max(_verdict_code(r.verdict) for r in results)` makes `max(1, 2) = 2`, so marginal beat BAD (CONTEXT.md correction 3). Both readings go in the record."
    - "A write that unexpectedly succeeds after the lock reports BAD **and exits 1** end-to-end through the real CLI (LEG-06) — never SKIPPED, NA, or OK. The truth table has three `marginal` arms, so the collision is common, not hypothetical; before this plan LEG-06's exit 1 was provably unreachable on any marginal-bearing run."
    - "D-14's blocking audit is re-verified live, not inherited: there are ZERO existing tests asserting exit 2 on a **mixed BAD+marginal** run — `test_marginal_disagreement_exits_2` produces `[NA, OK, OK, marginal, OK, OK]` with no BAD anywhere, and every other `exit_code == 2` site is Click's own no-such-option code or an unrelated command."
    - "No sixth verdict status is introduced. `_verdict_code` is `.get(verdict, 0)`, so an unrecognised verdict exits **0** — the existing five stay the whole vocabulary."
    - "The explicit-precedence form follows the in-repo precedent the false comment itself points at — `dev_validate_family`'s `if verdict_int > overall_verdict` (`cli_handlers.py:1620-1621`) — not another `max`."
  artifacts:
    - firestarter_app/firestarter/cli_handlers.py
    - firestarter_app/tests/test_dev_test_cmd.py
  key_links:
    - "`cli_handlers.py` is in the mypy STRICT island (`disallow_untyped_defs = true`) with **2** errors of headroom (33 vs watermark 35) — every new helper needs full annotations or it is an error, not a watermark contribution. Measure only with `tools/ci_replica_venv.sh`."
    - "D-15's NOT-RUN exit floor composes AFTER this precedence (plan `134-07`) and BAD must outrank it: a run that is both BAD and NOT-RUN exits **1**, not 2, or the floor re-creates the very laundering this plan removes."
---

<objective>
Fix `dev test`'s exit precedence so BAD outranks `marginal`, and close LEG-06 with an end-to-end CLI
proof that a write succeeding after the lock reports BAD and exits 1.

Purpose: criterion 2's "exit code 1" is provably unreachable on a marginal-bearing run until this lands,
and the oracle's truth table has three `marginal` arms. The temptation this plan exists to defeat is
testing the exit code on a marginal-free happy path and calling LEG-06 discharged.
Output: explicit exit precedence, the mixed-run pin, the end-to-end leaked-lock proof, and non-vacuity
obligation #5.
</objective>

<repo_topology>
All code lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`, commit
THERE on `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`). `/workspaces/firestarter/` is the Arduino
firmware submodule and is **not touched by this phase at all**.
`firestarter_app/firestarter/eprom_operations.py` is **RING-FENCED** — call only.

⚠ This plan runs in the same wave as `134-06`. `134-06` owns `firestarter/diagnostic_report.py` and
`tests/test_diagnostic_report.py`; this plan owns `firestarter/cli_handlers.py` and
`tests/test_dev_test_cmd.py`. Do not touch the other plan's files.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-PATTERNS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-04-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick:**
- **LEG-06** — a write that unexpectedly succeeds after the lock reports **BAD** and exits **1**, never
  SKIPPED, NA, or OK. Both halves are now present: the engine BAD arm (plan `134-02`) and the exit-1
  contract (here). ⚠ Tick it only against a test that runs the **real CLI end to end** on an ALLOW chip
  and asserts `result.exit_code == 1`, plus the mixed BAD+marginal pin. A marginal-free happy-path exit
  assertion does **not** discharge it.

**Contributes to but MUST NOT tick:**
- **LEG-12** — closed by plan `134-07`. **LEG-13** — closed by plan `134-10`.
  **LEG-14** — closed by plan `134-09`. **LEG-17** — closed by plan `134-10`.
  **LEG-18** — closed by plan `134-11`.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09/10/11/15 (already `[x]`, Phase 133). No RELOCK-\*,
CHAN-\*, CLOSE-\* row.

## Artifacts this phase produces (new symbols introduced by THIS plan)

| Symbol | File | Kind |
|---|---|---|
| `_EXIT_CODE_PRECEDENCE` | `firestarter_app/firestarter/cli_handlers.py` | module constant (ordered codes, most severe first) |
| `_overall_exit_code(results: list[StepResult]) -> int` | `firestarter_app/firestarter/cli_handlers.py` | fully-annotated helper (STRICT island) |
| a read-back-capable operator builder for ALLOW-chip CLI runs | `firestarter_app/tests/test_dev_test_cmd.py` | test helper |

<tasks>

<task type="auto">
  <name>Task 1: re-verify D-14's blocking audit live, then land explicit exit precedence</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py` `:1885-1902` — the comment falsely claiming *"BAD beats marginal via `max`"*, `_VERDICT_EXIT_CODES`, and `_verdict_code`'s `.get(verdict, 0)`.
    - `firestarter_app/firestarter/cli_handlers.py` `:1615-1630` — `dev_validate_family`'s `if verdict_int > overall_verdict` explicit-precedence precedent, which the false comment itself points at.
    - `firestarter_app/firestarter/cli_handlers.py` `:2085-2125` — `dev_test`'s decorators, its zero options, its docstring exit contract at `:2119-2121`, and `click.echo(_ALWAYS_WRITES_NOTICE)` at `:2123`.
    - `firestarter_app/firestarter/cli_handlers.py` `:2210-2219` — the exit computation.
    - `firestarter_app/pyproject.toml` `:150-200` — the mypy global settings and the STRICT island list including `cli_handlers`.
    - `.planning/phases/134-…/134-CONTEXT.md` correction 3 and D-14; `134-RESEARCH.md` §2.5 and §4.6.
  </read_first>
  <action>
**A. Re-run D-14's audit live and record it in the SUMMARY** — do not inherit RESEARCH's discharge.
Enumerate every `exit_code == 2` assertion in the suite
(`grep -rn 'exit_code == 2' tests/`), and for each one state whether its run contains a BAD verdict.
Expected result (measured at research time, re-confirm): the `test_dev_test_cmd.py` sites at `:202` and
`:217` are Click's own *"no such option"* code, `test_marginal_disagreement_exits_2` and its
parametrised twin produce `[NA, OK, OK, marginal, OK, OK]` with **no BAD anywhere**, and the
`test_cli_handlers.py` / `test_py32_channel_gating.py` / `test_validate_oracle.py` sites belong to
unrelated commands. If any site *does* mix BAD and marginal, **stop and report** rather than editing it.

**B. Replace the `max` with explicit precedence** in `firestarter_app/firestarter/cli_handlers.py`:
- Add `_EXIT_CODE_PRECEDENCE: tuple[int, ...] = (1, 2, 0)` — the observed exit codes ordered
  most-severe-first — beside `_VERDICT_EXIT_CODES` at `:1891`.
- Add a fully annotated `_overall_exit_code(results: list[StepResult]) -> int` that computes the set of
  `_verdict_code(r.verdict)` values and returns the first code in `_EXIT_CODE_PRECEDENCE` present in
  that set, else `0`. Keeping `_verdict_code` and `_VERDICT_EXIT_CODES` as the single vocabulary source
  preserves the `.get(verdict, 0)` behaviour for an unrecognised verdict — which is exactly why **no
  sixth verdict status may be introduced**: an unknown verdict exits 0.
- Replace `code = max(_verdict_code(r.verdict) for r in results)` at `:2218` with
  `code = _overall_exit_code(results)`. Leave the `if not results: sys.exit(0)` guard at `:2216-2217`
  untouched.
- Amend the comment at `:1888-1890` so its **mechanism** clause is true (explicit precedence, not
  `max`), and leave its **contract** clause ("BAD beats marginal") and `dev_test`'s docstring at
  `:2119-2121` unchanged — they were already correct and are what this change restores. Add a short
  in-code note recording correction 3 with both readings: the shipped code and both prose claims
  disagreed, and the code was the wrong one.

⚠ **STRICT mypy island.** `_overall_exit_code` needs full annotations on parameters and return, or it
is an *error*, not a watermark contribution. Headroom is **2** (33 vs watermark 35). Measure with
`tools/ci_replica_venv.sh` only — the devcontainer's own mypy exits 2 against numpy and the devcontainer
is py3.12 while CI is py3.9/3.11.

⚠ **Do not add a new CLI option, a new verdict status, or a watermark edit.** `dev test` keeps zero
options; the `35` in `pyproject.toml` is not touched — this phase spends against it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_cli_handlers.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/cli_handlers.py` contains `def _overall_exit_code(results: list[StepResult]) -> int:` and `_EXIT_CODE_PRECEDENCE`.
    - `grep -c 'max(_verdict_code' firestarter/cli_handlers.py` returns 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.cli_handlers import _overall_exit_code;from firestarter.chip_test import StepResult,VERDICT_BAD,VERDICT_MARGINAL,VERDICT_OK;r=[StepResult(op='a',verdict=VERDICT_MARGINAL,run_count=1),StepResult(op='b',verdict=VERDICT_BAD,run_count=1)];assert _overall_exit_code(r)==1"` exits 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.cli_handlers import _overall_exit_code;from firestarter.chip_test import StepResult,VERDICT_MARGINAL,VERDICT_OK;assert _overall_exit_code([StepResult(op='a',verdict=VERDICT_MARGINAL,run_count=1)])==2;assert _overall_exit_code([StepResult(op='a',verdict=VERDICT_OK,run_count=1)])==0"` exits 0.
    - `git diff firestarter/cli_handlers.py` shows no change to `dev_test`'s docstring exit contract at `:2119-2121` and no added `click.Option`.
    - `git diff --stat pyproject.toml` is empty — the watermark is not edited.
    - `pytest tests/test_dev_test_cmd.py tests/test_cli_handlers.py tests/test_py32_channel_gating.py tests/test_validate_oracle.py -o addopts="" -q` all pass — the audited exit-2 sites are unaffected.
    - `tools/ci_replica_venv.sh` exits 0 with `mypy errors: N (watermark: 35)`, N ≤ 35, recorded in the SUMMARY.
    - The SUMMARY carries the live audit table: every `exit_code == 2` site in the suite with its BAD/no-BAD classification.
  </acceptance_criteria>
  <done>BAD outranks marginal, restoring what the code's own comment and docstring already claimed.</done>
</task>

<task type="auto">
  <name>Task 2: LEG-06 end-to-end — the leaked lock exits 1, plus the mixed-run pin and non-vacuity #5</name>
  <files>firestarter_app/tests/test_dev_test_cmd.py</files>
  <read_first>
    - `firestarter_app/tests/test_dev_test_cmd.py` `:55-105` (`_CHIP_NO_ID`, `_CHIP_WITH_ID`, `_CHIP_UV`, `make_clean_operator` — note it uses `Mock(spec=EpromOperator)`, the **real class**, so `sdp_lock`/`sdp_unlock` exist automatically and `assert_not_called()` needs no builder change; its docstring explains why the return annotation stays `Mock` — do **not** "fix" it, that relocates errors onto every mock-assertion site and the strict-island headroom is 2).
    - `firestarter_app/tests/test_dev_test_cmd.py` `:144-160` (`_off_tty`, `_reports_dir`, `_load_report`) and `:620-700` (the existing exit-code tests and the `write_eprom.assert_not_called()` idiom at `:662`).
    - `firestarter_app/tests/conftest.py` `:229-334` (`make_app_context` — fully annotated, six keyword-only params — and the `app_context` fixture). Reuse it; do **not** add a conftest fixture.
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` — `_readback_operator` from plan `134-02`, for the read-back `side_effect` shape.
    - `.planning/phases/134-…/134-VALIDATION.md` §"Non-Vacuity Obligations" row 5.
  </read_first>
  <action>
Add tests to `firestarter_app/tests/test_dev_test_cmd.py`, named so `pytest -k "exit"` selects them
(VALIDATION.md's LEG-06 command).

**A. A read-back-capable ALLOW-chip operator builder.** `make_clean_operator`'s `read_eprom` returns
`True` and writes no file, so an ALLOW chip's leg would only ever exercise the length gate. Add a
builder that sets `read_eprom.side_effect` to write a caller-supplied payload to the `output_file`
keyword argument. Use `AT28C256` as the ALLOW chip — verified at plan time to resolve through
`resolve_chip` (`algorithm: 13`, `chip-id: 0`, `memory-size: 32768`) — and define it as a named module
constant beside `_CHIP_NO_ID` / `_CHIP_WITH_ID` rather than inline.

**B. LEG-06 end to end — `test_..._leaked_lock_exits_1`.** Drive the real CLI
(`runner.invoke(cli, ["dev", "test", <ALLOW chip>], obj=app)` under `_off_tty()`) with an operator whose
baseline steps succeed, whose `sdp_lock` returns `True`, and whose read-back after the inhibited write
returns **pattern B** (the write got through). Assert:
- `result.exit_code == 1` (not 2, not 0);
- the `write-inhibited` step's verdict in the JSON artifact (`_load_report`) is `BAD` — never `SKIPPED`,
  `NA` or `OK`;
- `operator.sdp_unlock` was called, so the part is not left locked.
The docstring must state that this is LEG-06's discharging test and that the exit assertion alone would
not be enough — the verdict assertion is what stops a laundering route from passing it.

**C. The mixed BAD+marginal pin (D-14's own acceptance).** A run that contains **both** a BAD step and a
`marginal` step must exit **1**. Construct it end to end (e.g. a `marginal` write disagreement plus a
BAD leg step) rather than by calling `_overall_exit_code` directly, so the pin covers the wiring and not
just the helper. Add a comment recording that before D-14 this run exited 2.

**D. Non-vacuity obligation #5 (VALIDATION.md row 5) — OBSERVE, do not assert.** Temporarily revert
`cli_handlers.py`'s exit computation to `code = max(_verdict_code(r.verdict) for r in results)`, run the
mixed-run test, and **capture the RED output verbatim in the SUMMARY** — it must fail with `2 != 1`.
Then restore `cli_handlers.py` byte-identically (`git diff` empty for that file) and re-run green.

**E. Guard the shipped exit-2 contract.** Confirm `test_marginal_disagreement_exits_2` and its
parametrised twin still pass unchanged — a marginal-only run still exits 2. Do not edit them.

⚠ **Do not add a `--sdp` or any other option to `dev test`** — it keeps zero options. Assert nothing
about a flag; drive the shipped invocation.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py -k "exit" -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_dev_test_cmd.py -k "exit" -o addopts="" -q` selects ≥ 5 tests, all pass, including the leaked-lock and mixed-run tests.
    - A test asserts `result.exit_code == 1` for the leaked-lock end-to-end run AND asserts the `write-inhibited` step's verdict is `BAD` in the JSON artifact.
    - A test asserts `result.exit_code == 1` for a run containing both a BAD and a `marginal` step.
    - `test_marginal_disagreement_exits_2` passes unchanged (`git diff` shows no edit to it).
    - The SUMMARY contains the verbatim RED output of obligation #5 (`2 != 1`) and confirms byte-identical restoration of `firestarter/cli_handlers.py`.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` green, coverage ≥ 70%.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0.
  </acceptance_criteria>
  <done>LEG-06 is discharged end to end: BAD, exit 1, with the part unlocked and the gate seen RED once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step verdicts → process exit code | the number CI and a community reporter read as pass/fail |
| CLI → JSON artifact | the machine-readable claim a stranger pastes into an issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-19 | Repudiation | the milestone's headline finding arriving wearing the inconclusive exit code | critical | mitigate | Explicit precedence: BAD ≻ marginal ≻ clean; pinned by an end-to-end mixed-run test; non-vacuity #5 observed RED |
| T-134-20 | Spoofing | an unrecognised verdict exiting 0 | high | mitigate | `_verdict_code`'s `.get(verdict, 0)` retained as the single vocabulary source, and no sixth status is introduced |
| T-134-21 | Tampering | an exit-code-only test passing while the verdict is SKIPPED | high | mitigate | The LEG-06 test asserts the artifact verdict is BAD alongside the exit code |
| T-134-22 | Denial of Service | a leaked-lock run leaving the part locked | critical | mitigate | The end-to-end test asserts `sdp_unlock` was called on the BAD path |
| T-134-SC | Tampering | package-manager installs | n/a | accept | No new package installed by this phase |
</threat_model>

<verification>
- `pytest tests/test_dev_test_cmd.py tests/test_cli_handlers.py tests/test_py32_channel_gating.py tests/test_validate_oracle.py -o addopts="" -q` green.
- The live `exit_code == 2` audit table recorded in the SUMMARY.
- Non-vacuity obligation #5 observed RED and restored byte-identically.
- `tools/ci_replica_venv.sh` 5/5, mypy ≤ 35, recorded.
</verification>

<success_criteria>
- BAD outranks marginal in `dev test`'s exit code, via explicit precedence, not `max`.
- A leaked lock reports BAD and exits 1 end to end, with the part unlocked.
- A mixed BAD+marginal run exits 1; a marginal-only run still exits 2.
- `dev test` still has zero CLI options; no watermark edit; no sixth verdict status.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-05-SUMMARY.md` when done.
</output>
