---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 08
type: execute
wave: 7
depends_on: ["134-07"]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/tests/test_dev_test_cmd.py
autonomous: true
requirements: [LEG-14, LEG-12]
must_haves:
  truths:
    - "`_ALWAYS_WRITES_NOTICE` stays ONE static notice printed FIRST, unconditionally — before the SAFE-04 absent-chip hard-fail, before `write_scope` resolution and before `derive_plan` — so it cannot carry a per-chip derived count (D-09). Its shipped ordering pin `test_always_writes_notice_is_the_first_line_unconditionally` stays green."
    - "The rewritten notice names the **SDP lock** in prose, states the part is left unlocked on a **completed** run, and gives the aborted-run recovery in the word **rewrite** (D-09, LEG-14)."
    - "Its write-pass number is pinned by a DERIVED test: the test derives the plan for a representative ALLOW chip, computes the write-pass count, and asserts the notice's number equals it — the number is never written twice (P-08 prevention 2). Measured today: **6** passes over the 256-byte region (A twice from the shipped `write` step at `runs=2`, then B, A, B, A) against a shipped notice promising 'written twice'."
    - "The notice contains **NO hyphenated op literal**. All four new op strings auto-join `_MULTIWORD_OP_VALUES`, and `_ALWAYS_WRITES_NOTICE` is a declared non-registry measured by a live SUBSTRING test — writing the lock op's hyphenated string there turns `test_non_registry_still_has_no_ops` RED (RESEARCH §4.5)."
    - "Two recovery forms, and a line prints on the happy path (D-12): a loud form when the lock was emitted and the run did not confirm the part writable again, and a NEUTRAL one-liner on the happy path affirming the part was left unlocked and that this is evidence, not a guarantee, on a family whose state is unreadable. An unconditional warning trains dismissal; silence is not a statement."
    - "Both forms go through `click.echo`, matching `_ALWAYS_WRITES_NOTICE`'s precedent, so they reach console and `CliRunner` capture regardless of log-level wiring (D-12)."
    - "The recovery wording lives in NAMED module-level string constants in `cli_handlers.py` so plan `134-09`'s scoped pytest scans exactly those, and so Phase 137's CLOSE-03 scanner EXTENDS rather than duplicates them (D-13)."
    - "133 D-07's Ctrl-C residual is NOT closed: after an interrupt mid-leg neither recovery form prints and there is no report at all. The mitigation is this up-front notice, not a `finally` handler — do not claim otherwise (D-12's honest residual)."
  artifacts:
    - firestarter_app/firestarter/cli_handlers.py
    - firestarter_app/firestarter/chip_test.py
    - firestarter_app/tests/test_dev_test_cmd.py
  key_links:
    - "`_ALWAYS_WRITES_NOTICE` survives today only because `write`/`verify`/`erase` are single-word ops, which `_MULTIWORD_OP_VALUES` deliberately excludes. Write `SDP lock` with a space; never the hyphenated op string."
    - "`test_always_writes_notice_is_the_first_line_unconditionally` (`tests/test_dev_test_cmd.py:227`) asserts identity against the IMPORTED constant, so a rewrite keeps it green — which is exactly why D-09 additionally needs the derived-count test (P-08: that pin checks no content)."
    - "P-07's `_HANDLER_FUNCTION_NAMES` derived-subset gate landed in Phase 131 (GATE-10), so a NEW handler helper IS now caught. Keep the handler-side addition to constants plus ONE thin annotated function, and derive the decision in `chip_test.py`."
---

<objective>
Rewrite the always-writes notice so it tells the truth about this phase's run, add D-12's two recovery
forms behind named module-level constants, and pin the notice's write-pass number to a derived count.

Purpose: the notice is the only thing guaranteed to be seen — it prints before anything can energise the
shield, and it is the sole mitigation for the Ctrl-C case where no report exists at all. Today it
promises the device is "written twice" while this phase's leg writes it six times, and it says nothing
about the SDP lock it now applies.
Output: an honest notice with a derived number, two recovery forms, and the named constants Phase 137's
CLOSE-03 will extend.
</objective>

<repo_topology>
All code lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`, commit
THERE on `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`). `/workspaces/firestarter/` is the Arduino
firmware submodule and is **not touched by this phase at all**.
`firestarter_app/firestarter/eprom_operations.py` is **RING-FENCED** — call only.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-PATTERNS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-07-SUMMARY.md
</context>

## Requirements this plan may mark Complete

**MAY tick: NONE.**

**Contributes to but MUST NOT tick:**
- **LEG-14** — the recovery wording and its named constants land here, but LEG-14's discharging evidence
  is the **committed scoped gate plus its planted-violation non-vacuity leg**, which is plan `134-09`'s
  new test file. **Closed by plan `134-09`.**
- **LEG-12** — already closed by plan `134-07`; the recovery lines are an additional surface, not
  a second discharge. Do not re-tick.
- **LEG-13** — closed by plan `134-10`. **LEG-17** — closed by plan `134-10`.
  **LEG-18** — closed by plan `134-11`.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09/10/11/15 (already `[x]`, Phase 133). No RELOCK-\*,
CHAN-\*, CLOSE-\* row. ⚠ **Do NOT author Phase 137's `tools/check_*.py` string-literal scanner** — that
is CLOSE-03, explicitly out of scope (D-13). Do not write release notes, a claim gate, an honesty
ledger, or any public reply.

## Artifacts this phase produces (new symbols introduced by THIS plan)

| Symbol | File | Kind |
|---|---|---|
| `_ALWAYS_WRITES_PASS_COUNT` | `firestarter_app/firestarter/cli_handlers.py` | module constant (the notice's single-sourced number) |
| `_SDP_RECOVERY_LOUD` | `firestarter_app/firestarter/cli_handlers.py` | module constant (D-12 loud form) |
| `_SDP_RECOVERY_NEUTRAL` | `firestarter_app/firestarter/cli_handlers.py` | module constant (D-12 happy-path form) |
| `SDP_RECOVERY_CONSTANT_NAMES` | `firestarter_app/firestarter/cli_handlers.py` | tuple of the scanned constant names (LEG-14's scan target; Phase 137 CLOSE-03 extends it) |
| `_sdp_recovery_line(...) -> str` | `firestarter_app/firestarter/cli_handlers.py` | the one thin annotated helper (STRICT island) |
| `sdp_left_writable(results) -> bool` | `firestarter_app/firestarter/chip_test.py` | pure engine predicate |

<tasks>

<task type="auto">
  <name>Task 1: rewrite `_ALWAYS_WRITES_NOTICE` with a single-sourced pass count, and add the engine predicate</name>
  <files>firestarter_app/firestarter/cli_handlers.py, firestarter_app/firestarter/chip_test.py</files>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py` `:2064-2078` (`_ALWAYS_WRITES_NOTICE` and its D-04 printed-FIRST comment) and `:2119-2125` (the docstring exit contract and `click.echo(_ALWAYS_WRITES_NOTICE)` at `:2123`).
    - `firestarter_app/firestarter/cli_handlers.py` `:2126-2160` — everything between the echo and `derive_plan` (`:2138`) / `read_hardware_revision_value` (`:2150`, the first thing that energises), which is why the notice cannot carry a per-chip count.
    - `firestarter_app/tests/test_dev_test_cmd.py` `:226-250` (`test_always_writes_notice_is_the_first_line_unconditionally` — it asserts identity against the imported constant).
    - `firestarter_app/tests/test_op_registration_parity.py` `:142-148` (`_MULTIWORD_OP_VALUES`), `:276-330` (the `_ALWAYS_WRITES_NOTICE` constant-locator non-registry row) and `:608-652` (`_measure_op_vocabulary`'s live substring test at `:613-617`).
    - `firestarter_app/firestarter/chip_test.py` — `_SDP_LEG_STEP_ORDER`, `OP_WRITE_RESTORED`, `_MULTI_RUN_OPS`, `derive_plan`.
    - `.planning/phases/134-…/134-CONTEXT.md` D-09 in full.
  </read_first>
  <action>
**A. `_ALWAYS_WRITES_PASS_COUNT`.** Add a module constant in `firestarter_app/firestarter/cli_handlers.py`
holding the number of write passes a `dev test` run performs over the write region for a representative
ALLOW chip, and interpolate it into `_ALWAYS_WRITES_NOTICE` so the number exists in exactly ONE place
(P-08 prevention 2: derive it, do not write it twice). Measured at plan time: **6** — the shipped
`write` step writes pattern A twice at `runs=2`, then the leg writes B, A, B, A once each. Task 3 adds
the test that derives this from a live `derive_plan` and asserts equality; if that test measures a
different number, change the constant, not the test.

**B. Rewrite the notice's prose.** It must:
- keep the existing warning that `dev test` ALWAYS WRITES and should be run only on a blank or scratch
  part the user is willing to sacrifice;
- state the corrected pass count via `_ALWAYS_WRITES_PASS_COUNT`;
- name the **SDP lock** it now applies, in prose;
- state that on a **completed** run the part is left unlocked;
- give the aborted-run recovery in the word **rewrite** — protocol `0x0D` has no bulk-clear operation at
  all, so the recovery is to write the part again.

⚠ **Two structural constraints, both non-negotiable:**
1. **No hyphenated op literal anywhere in the notice.** All four new op strings are hyphenated and
   auto-join `_MULTIWORD_OP_VALUES`; `_ALWAYS_WRITES_NOTICE` is a declared non-registry whose value is
   measured by a live **substring** test (`_measure_op_vocabulary`, `:613-617`). Write `SDP lock` with a
   space. The shipped notice survives only because `write`/`verify`/`erase` are single-word ops, which
   that derivation deliberately excludes.
2. **It stays ONE static notice, printed FIRST, unconditionally.** Do not add a second line after
   `derive_plan`, do not make it conditional on the chip, and do not move the `click.echo` at `:2123`.
   Its D-04 ordering guarantee — printed before the SAFE-04 absent-chip hard-fail and before anything
   that could energise the shield — is pinned by a committed test and is the entire reason an unknown
   chip seeing this notice is harmless and honest.

**C. `sdp_left_writable(results: list[StepResult]) -> bool` in `firestarter_app/firestarter/chip_test.py`**
— a pure predicate returning `True` iff the `write-restored` result exists and its verdict is OK. This
is the "the run confirmed the part writable again" term D-12's loud form keys on. Put it in the engine
for the same three reasons as `sdp_hold_state`: P-07 (`chip_test.py` is scanned in full), the mypy
budget (`chip_test.py` bodies are unchecked; `cli_handlers.py` is STRICT with **2** of headroom), and
keeping op vocabulary out of the handler.

Do **not** add a CLI option, a second output surface, or a `finally` handler.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_op_registration_parity.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/cli_handlers.py` contains `_ALWAYS_WRITES_PASS_COUNT` and `_ALWAYS_WRITES_NOTICE` interpolates it.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.cli_handlers import _ALWAYS_WRITES_NOTICE as n;assert 'rewrite' in n.lower();assert 'SDP lock' in n"` exits 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.cli_handlers import _ALWAYS_WRITES_NOTICE as n;from firestarter.chip_test import _SDP_LEG_OPS;assert not any(op in n for op in _SDP_LEG_OPS)"` exits 0.
    - `pytest tests/test_op_registration_parity.py::test_non_registry_still_has_no_ops -o addopts="" -q` passes.
    - `pytest "tests/test_dev_test_cmd.py::TestAlwaysWritesNotice::test_always_writes_notice_is_the_first_line_unconditionally" -o addopts="" -q` passes, and `git diff` shows no edit to that test.
    - `grep -c 'click.echo(_ALWAYS_WRITES_NOTICE)' firestarter/cli_handlers.py` returns 1 and its line number is still below the `derive_plan(` call site's line number.
    - `firestarter/chip_test.py` contains `def sdp_left_writable(`.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0.
  </acceptance_criteria>
  <done>The notice tells the truth about this phase's run, with its number in exactly one place.</done>
</task>

<task type="auto">
  <name>Task 2: D-12's two recovery forms behind named constants, echoed after the report</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py` `:2160-2219` — the seam, `report.render(console)` `:2181`, `json.dumps(report.to_dict(), …)` `:2192`, the markdown table loop `:2194-2205`, `console.print(...)` `:2207`, and the exit computation.
    - `firestarter_app/firestarter/cli_handlers.py` `:2064-2078` — `_ALWAYS_WRITES_NOTICE`'s `click.echo` precedent.
    - `firestarter_app/firestarter/sdp_honesty.py` `:25-92` — `unreadable_state_caveat()` and `emission_summary(mode, chip_name)`. Phase 132 built these as **this phase's** forward contract; **call them, do not re-author the sentences**, and do not extend the module (STRICT island, deliberately imports no `click`).
    - `firestarter_app/firestarter/chip_test.py` — `sdp_hold_state`, `sdp_left_writable`, `SDP_HOLD_HELD` / `SDP_HOLD_NOT_HELD` / `SDP_HOLD_NOT_RUN`.
    - `firestarter_app/tools/check_devtest_orchestrator.py` — `_HANDLER_FUNCTION_NAMES` and its derived-subset gate (Phase 131 GATE-10), which now catches a new handler helper.
    - `.planning/phases/134-…/134-CONTEXT.md` D-12 and D-13 in full.
  </read_first>
  <action>
<!-- planner-discipline-allow: erase -->

Add to `firestarter_app/firestarter/cli_handlers.py`:

**A. Two named module-level string constants**, sited beside `_ALWAYS_WRITES_NOTICE`:
- `_SDP_RECOVERY_LOUD` — printed when the lock was emitted and the run did **not** confirm the part
  writable again. It must contain the word **rewrite**, must state plainly what the user should do, and
  must carry the honesty caveat by calling `sdp_honesty.unreadable_state_caveat()` at composition time
  rather than restating it.
- `_SDP_RECOVERY_NEUTRAL` — the happy-path one-liner. It affirms the part was left unlocked and states
  that this is **evidence, not a guarantee**, on a family whose protection state cannot be read back.

Both constants — and every other string plan `134-09`'s gate will scan — must contain the word
**rewrite** where they describe recovery, and must **not** contain the five-letter word for bulk
clearing (`0x0D` has no such operation at all, which is why the word would be actively wrong advice).
Neither may contain a hyphenated op literal.

**B. `SDP_RECOVERY_CONSTANT_NAMES`** — a module-level tuple naming exactly the constants above. This is
LEG-14's scan target: plan `134-09`'s scoped pytest imports the module, resolves each name, and scans
only those values. Add a comment handing Phase 137's **CLOSE-03** these constant names so its
`tools/check_*.py` scanner **extends** this tuple rather than duplicating the pytest. ⚠ **Do not author
that scanner here** — it is CLOSE-03 (D-13).

⚠ **Why a whole-report grep is ruled out, recorded in the comment:** the report legitimately contains
the bulk-clear word in at least three places — `derive_plan`'s `0x0D` NA reason
(`chip_test.py:577-580`), the shipped single-word op string itself in both the markdown table and the
JSON, and `_ALWAYS_WRITES_NOTICE`'s own step enumeration. A whole-report grep goes RED on correct text
and would need exemptions on day one (the 133 D-14 `_sample` shape).

**C. `_sdp_recovery_line(*, hold_state: str, left_writable: bool) -> str`** — ONE thin, fully annotated
helper (STRICT island, headroom **2**). It returns `_SDP_RECOVERY_LOUD` when the lock was emitted (the
hold state is `HELD` or `NOT-HELD`) and `left_writable` is `False`; otherwise it returns
`_SDP_RECOVERY_NEUTRAL`. Keep the op-string knowledge in `chip_test.sdp_left_writable`, not here.

**D. The echo site.** After `report.render(console)` (`:2181`) and before the exit computation, call
`click.echo(_sdp_recovery_line(hold_state=report.sdp_hold_state,
left_writable=sdp_left_writable(results)))`. Use `click.echo`, **not** a logger, matching
`_ALWAYS_WRITES_NOTICE`'s precedent so the line reaches console and `CliRunner` capture regardless of
log-level wiring.

⚠ **A line prints on the happy path.** Silence is not a statement, and an unconditional warning trains
dismissal — it would spend the signal on the case it exists for (D-12). Do not gate the neutral form
behind a flag or a verbosity level.

⚠ **Record the honest residual, do not close it.** After a Ctrl-C mid-leg, `results = run_plan(...)`
(`:2164`) never returns, so **neither form prints and there is no report at all**. The mitigation is
Task 1's up-front notice, not a `finally` handler; fixing it properly needs `run_plan`'s signature and
all twelve call sites and has **no owner**. State this in the SUMMARY and do not claim otherwise
anywhere.

**E. Check the handler gate.** Run the full suite; if `tests/test_check_devtest_orchestrator.py`'s
derived-subset gate flags `_sdp_recovery_line`, add it to `_HANDLER_FUNCTION_NAMES` in
`firestarter_app/tools/check_devtest_orchestrator.py` and record why in the SUMMARY. Never loosen the
gate.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_check_devtest_orchestrator.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/cli_handlers.py` contains `_SDP_RECOVERY_LOUD`, `_SDP_RECOVERY_NEUTRAL`, `SDP_RECOVERY_CONSTANT_NAMES` and `def _sdp_recovery_line(`.
    - `python -c "import sys;sys.path.insert(0,'.');import firestarter.cli_handlers as m;names=m.SDP_RECOVERY_CONSTANT_NAMES;assert len(names)>=2;assert all(isinstance(getattr(m,n),str) and getattr(m,n) for n in names);assert all('rewrite' in getattr(m,n).lower() or 'unlocked' in getattr(m,n).lower() for n in names)"` exits 0.
    - `python -c "import sys;sys.path.insert(0,'.');import firestarter.cli_handlers as m;from firestarter.chip_test import _SDP_LEG_OPS,_SDP_OPS;bad=[n for n in m.SDP_RECOVERY_CONSTANT_NAMES for op in (_SDP_LEG_OPS|_SDP_OPS) if op in getattr(m,n)];assert not bad,bad"` exits 0.
    - `grep -c 'click.echo(_sdp_recovery_line' firestarter/cli_handlers.py` returns 1, and its line number is greater than `report.render(console)`'s and less than the `sys.exit(` line's.
    - `grep -rn 'tools/check_' firestarter/cli_handlers.py` returns nothing — no Phase-137 scanner was authored here.
    - `pytest tests/test_check_devtest_orchestrator.py -o addopts="" -q` passes.
    - `tools/ci_replica_venv.sh` exits 0; `mypy errors: N (watermark: 35)`, N ≤ 35, recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Both recovery forms exist behind named constants and one of them prints on every completed run.</done>
</task>

<task type="auto">
  <name>Task 3: D-09's derived-count test and the two-form behavioural pins</name>
  <files>firestarter_app/tests/test_dev_test_cmd.py</files>
  <read_first>
    - `firestarter_app/tests/test_dev_test_cmd.py` `:226-250` (the notice-first pin) and the ALLOW-chip read-back operator builder from plan `134-05`.
    - `firestarter_app/firestarter/chip_test.py` — `derive_plan`, `_SDP_LEG_STEP_ORDER`, `_MULTI_RUN_OPS`, `OP_WRITE`, `OP_WRITE_PARTIAL`, and `run_plan`'s default `runs`.
    - `firestarter_app/firestarter/cli_handlers.py` — `_ALWAYS_WRITES_PASS_COUNT`, the two recovery constants, `_sdp_recovery_line`.
    - `.planning/phases/134-…/134-CONTEXT.md` D-09's "add a test that DERIVES the plan … and asserts the notice's number equals it".
  </read_first>
  <action>
Add tests to `firestarter_app/tests/test_dev_test_cmd.py`.

**A. D-09's derived-count test.** Derive the plan for a representative ALLOW chip at the write scope
`dev test` actually resolves, then compute the number of write passes over the write region **from the
plan**: for each step that writes, add `runs` when the op is in `_MULTI_RUN_OPS` and `1` when it is one
of the leg's single-run write ops. Assert the computed value equals `_ALWAYS_WRITES_PASS_COUNT`, and
assert that the same number appears in `_ALWAYS_WRITES_NOTICE`'s rendered text. The docstring must state
P-08's point explicitly: the shipped notice-first pin asserts identity against the imported constant and
therefore **checks no content at all**, so a rewrite keeps it green — this test is what makes the number
true. Never restate `6` as a literal in the assertion; derive it.

**B. The notice's content pins.** Assert the notice contains the word **rewrite**, names the SDP lock in
prose, and states the completed-run outcome. Assert it contains none of `_SDP_LEG_OPS`'s hyphenated
strings. Keep `test_always_writes_notice_is_the_first_line_unconditionally` untouched and green.

**C. D-12's two forms, behaviourally.** Three end-to-end CLI runs on the ALLOW chip under `_off_tty()`:
1. Happy path — the leg completes and `write-restored` reports OK ⇒ `result.output` contains
   `_SDP_RECOVERY_NEUTRAL` and does **not** contain `_SDP_RECOVERY_LOUD`.
2. Lock emitted, part not confirmed writable — `write-restored` reports non-OK ⇒ `result.output`
   contains `_SDP_RECOVERY_LOUD`.
3. Gated run — the baseline gate closed and no lock was emitted ⇒ the neutral form prints (nothing was
   locked), and `operator.sdp_lock.assert_not_called()`.
Assert against the imported constants, never against retyped prose.

**D. The Ctrl-C residual, recorded not closed.** Add a test **or** an explicit module comment stating
that when `run_plan` raises `KeyboardInterrupt`, neither recovery form prints and no report is produced.
If you write it as a test, assert the observed behaviour truthfully (no recovery line in the output) —
do **not** assert a behaviour the code does not have, and do **not** add a `finally` handler to make it
pass. This is 133 D-07's residual, with no owner.

**E. Evidence Ceiling.** State in the new tests' docstrings that no fixture here simulates real
inhibition; a locked die is unrepresentable in either repo's stubs, and the causal claim "the lock
inhibited the write" is **not provable this milestone**.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_dev_test_cmd.py -k "notice or recovery" -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_dev_test_cmd.py -k "notice" -o addopts="" -q` selects ≥ 3 tests, all pass, including the derived-count test.
    - `pytest tests/test_dev_test_cmd.py -k "recovery" -o addopts="" -q` selects ≥ 3 tests, all pass.
    - The derived-count test contains no literal `6` in its assertion; it computes the count from a live `derive_plan` result.
    - A test asserts the happy-path run prints `_SDP_RECOVERY_NEUTRAL` and not `_SDP_RECOVERY_LOUD`.
    - A test asserts the not-confirmed-writable run prints `_SDP_RECOVERY_LOUD`.
    - The Ctrl-C residual is recorded (as a truthful test or an explicit comment) and no `finally` handler was added to `dev_test`.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` green, coverage ≥ 70%.
    - `tools/ci_replica_venv.sh` exits 0; mypy ≤ 35 recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The notice's number is derived and true, and both recovery forms are proven to print in the right cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static notice → user, before any hardware energises | the only guaranteed-seen statement on an aborted run |
| recovery wording → user with a possibly-locked part | advice that must be actionable and correct for `0x0D` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-31 | Repudiation | the notice under-describing the run (promising two writes for six) | high | mitigate | Single-sourced `_ALWAYS_WRITES_PASS_COUNT` plus a derived-count test over a live `derive_plan` |
| T-134-32 | Tampering | wrong recovery advice naming a bulk-clear operation `0x0D` does not have | high | mitigate | Named recovery constants use the rewrite wording; plan `134-09`'s scoped gate plus a planted-violation leg enforces it |
| T-134-33 | Information Disclosure | a hyphenated op literal entering `_ALWAYS_WRITES_NOTICE` | medium | mitigate | Prose form `SDP lock`; asserted against `_SDP_LEG_OPS` and re-measured by the parity gate's substring test |
| T-134-34 | Repudiation | claiming a recovery line prints after Ctrl-C | medium | accept | 133 D-07's residual recorded as open with the up-front notice named as the mitigation; no `finally` handler added |
| T-134-SC | Tampering | package-manager installs | n/a | accept | No new package installed by this phase |
</threat_model>

<verification>
- `pytest tests/test_dev_test_cmd.py tests/test_op_registration_parity.py tests/test_check_devtest_orchestrator.py -o addopts="" -q` green.
- `test_non_registry_still_has_no_ops` green — the notice acquired no op vocabulary.
- `tools/ci_replica_venv.sh` 5/5, mypy ≤ 35.
</verification>

<success_criteria>
- The notice is one static first-printed string, names the SDP lock in prose, gives the rewrite recovery, and carries a derived pass count.
- Two recovery forms exist as named module constants, one of which prints on every completed run.
- No Phase-137 scanner, no release notes, no claim gate, no public reply was authored.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-08-SUMMARY.md` when done.
</output>
