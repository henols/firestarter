---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 04
type: execute
wave: 4
depends_on: ["134-03"]
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/tests/test_op_registration_parity.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
autonomous: true
requirements: [LEG-06, LEG-12, LEG-17]
must_haves:
  truths:
    - "`_baseline_closes_sdp_gate` lives in `run_plan`, mirroring `_id_step_closes_gate`'s shape, keyed on the leg's baseline ops, with its OWN reason constant — never `_DESTRUCTIVE_GATE_REASON`'s chip-ID wording, which would tell a reader the chip ID closed the gate when the write path did (D-08)."
    - "It closes on ANY baseline verdict that is not OK — BAD, marginal, SKIPPED, NA — because a contact fault is as disqualifying as a dead write path (D-08). Strictly wider than `_id_step_closes_gate`'s `(BAD, SKIPPED)` tuple; the widening is stated in the docstring."
    - "`sdp-unlock` JOINS the baseline-gate set and renders SKIPPED when that gate closed (D-20, operator decision 2026-08-04, superseding D-08's clause that it 'is never attempted because nothing was locked'). Without it a dead-write-path run ends on `sdp-unlock OK` — an emission claim on a step whose premise did not hold, the P-06 shape."
    - "D-20 does NOT violate LEG-09: LEG-09 is scoped to the **destructive** gate (`_DESTRUCTIVE_OPS` membership + `test_unlock_exempt_from_destructive`), a structurally different mechanism from the new baseline gate. A NEW test pins that a *destructive*-gate closure still never skips the unlock, so Phase 133's LEG-09 proof stays byte-identically green."
    - "Phase 133's frozen `_dispatch_sdp(op, name, eprom_data, operator)` signature is NOT reopened (133 D-01)."
    - "A completed leg emits `sdp_unlock` exactly ONCE: the explicit plan step de-registers the handle registered by a successful lock via `cleanup.remove(handle)`, never `cleanup.clear()`, because the registry declares itself deliberately GENERIC. A leg that raises before the explicit unlock still emits it once via the drain; a FAILED explicit unlock leaves the handle registered so the drain retries."
    - "The `HELD`/`NOT-HELD`/`NOT-RUN` value is derived by a pure function in `chip_test.py`, NOT inside `DiagnosticReport` — the parity gate's AST inversion guard re-measures every run that `DiagnosticReport` carries zero op vocabulary (D-10, RESEARCH §4.4)."
    - "This is the SEVENTH route to a non-running oracle, beyond research's R1–R6. Under D-08 + D-15 it fails closed, so it is not a laundering route — but it is tested in the same family and named as the seventh in the record."
    - "`count_applicable` is NOT edited: D-15 measured that a SKIPPED SDP step is already excluded from N while still counted in M, and editing it would add op vocabulary to a declared non-registry and trip the inversion guard. Two independent reasons."
  artifacts:
    - firestarter_app/firestarter/chip_test.py
    - firestarter_app/tests/test_chip_test_sdp_leg.py
  key_links:
    - "The ID (destructive) gate at `run_plan:881-883` fires FIRST and renders its own chip-ID wording; the baseline guard goes immediately after it. Order is load-bearing — swapping them makes a dead-write-path run claim the chip ID closed the gate."
    - "The baseline gate flag must be STICKY: a failing `write-baseline-b` followed by a passing `write-baseline-a` must not reopen it."
    - "The chip-ID destructive gate is structurally VACUOUS for the whole SDP population — all 43 ALLOW chips have `chip-id == 0`, so `derive_plan` emits an NA id step and `_id_step_closes_gate` never fires. The baseline gate is the live one in production. No artifact may claim 'the leg is gated by chip ID' (a v1.22 C-5 class overclaim)."
---

<objective>
Wire the run-time baseline gate that refuses to lock a chip whose write path did not transition, fix the
cleanup registry's double-fire, and add the pure `HELD`/`NOT-HELD`/`NOT-RUN` derivation the report layer
will carry.

Purpose: gh#20 is the argument, not an aside. A real community report — AT28C256, host `3.0.0b14`,
Rev 2.3, `blank-check`/`write`/`verify` all BAD — is the live instance of "lock a part whose baseline
write never worked". Without this gate, running this phase's leg on that bench emits a lock at a part
that cannot be rewritten.
Output: `_baseline_closes_sdp_gate`, its three `run_plan` wiring sites, the de-registration handle,
`sdp_hold_state`, and `sdp_oracle_applicable`.
</objective>

<repo_topology>
All code lives inside the `firestarter_app` git submodule — `cd /workspaces/firestarter_app`, commit
THERE on `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`). `/workspaces/firestarter/` is the Arduino
firmware submodule and is **not touched by this phase at all**.
`firestarter_app/firestarter/eprom_operations.py` is **RING-FENCED** — call only.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-PATTERNS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-03-SUMMARY.md
@.planning/phases/133-sdp-leg-mechanism/133-CONTEXT.md
</context>

## Requirements this plan may mark Complete

**MAY tick: NONE.** Every requirement this plan touches is closed elsewhere. Do not tick any box in
`.planning/REQUIREMENTS.md` from this plan.

**Contributes to but MUST NOT tick:**
- **LEG-06** — the gate is what stops a lock being emitted at a dead write path, but LEG-06's
  BAD + exit-1 contract is closed by plan `134-05`.
- **LEG-12** — `sdp_hold_state` is the derivation half; the field lands in plan `134-06`, the two
  rendered surfaces in plan `134-07`, which closes it.
- **LEG-17** — this gate is the **seventh** route to a non-running oracle; the six R1–R6 route tests are
  plan `134-10`'s, which closes it.
- **LEG-13** — the SKIPPED steps that drop the N-of-M ratio originate here; the pinning test is plan
  `134-10`'s.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09/10/11/15 (already `[x]`, Phase 133). ⚠ This plan is
the highest-risk one for LEG-09 specifically, because D-20 makes `sdp-unlock` skippable by a
**different** gate. Do not edit `test_unlock_exempt_from_destructive` or any other Phase-133 named
proof — add a new test beside them instead. No RELOCK-\*, CHAN-\*, CLOSE-\* row.

## Artifacts this phase produces (new symbols introduced by THIS plan)

| Symbol | File | Kind |
|---|---|---|
| `_baseline_closes_sdp_gate(result) -> bool` | `firestarter_app/firestarter/chip_test.py` | pure predicate |
| `_SDP_BASELINE_OPS` | `firestarter_app/firestarter/chip_test.py` | frozenset (gate inputs) |
| `_SDP_LEG_GATED_OPS` | `firestarter_app/firestarter/chip_test.py` | frozenset (gate outputs) |
| `baseline_gate_closed` local, `unlock_cleanup` handle | `firestarter_app/firestarter/chip_test.py` | `run_plan` locals |
| `SDP_HOLD_HELD` / `SDP_HOLD_NOT_HELD` / `SDP_HOLD_NOT_RUN` | `firestarter_app/firestarter/chip_test.py` | value constants |
| `sdp_hold_state(plan, results) -> str` | `firestarter_app/firestarter/chip_test.py` | pure derivation |
| `sdp_oracle_applicable(plan) -> bool` | `firestarter_app/firestarter/chip_test.py` | pure predicate |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `_baseline_closes_sdp_gate`, its three `run_plan` wiring sites, and the cleanup de-registration</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_op_registration_parity.py</files>
  <read_first>
    - `firestarter_app/firestarter/chip_test.py` `:773-972` (`run_plan` in full) — the init block `:853-869`, the NA arm `:877-879`, the destructive-gate guard `:881-883`, the `_run_step` call `:885`, the lock-to-cleanup registration `:890-918`, the ID-gate set clause `:920-921`, the `finally` drain `:967-971`, and `_UNLOCK_CLEANUP_SWALLOWED` `:770`.
    - `firestarter_app/firestarter/chip_test.py` `:974-985` (`_id_step_closes_gate` — the structural template and its narrow `(BAD, SKIPPED)` tuple) and `:705-707` (`_DESTRUCTIVE_GATE_REASON`'s chip-ID wording).
    - `firestarter_app/firestarter/chip_test.py` `:734-737` (`_skip_result` and its default verdict).
    - `firestarter_app/firestarter/chip_test.py` — `_SDP_LEG_OPS`, `_SDP_BASELINE_GATE_REASON`, `_SDP_UNLOCK_GATE_REASON` from plan `134-01`; `_SDP_LEG_STEP_ORDER` from plan `134-03`.
    - `.planning/phases/134-…/134-CONTEXT.md` D-08 and D-20 in full, including D-08's own ⚠ block marking its `sdp-unlock` clause measured-wrong.
    - `.planning/phases/133-sdp-leg-mechanism/133-CONTEXT.md` D-01, D-06, D-11.
    - `.planning/phases/134-…/134-RESEARCH.md` §4.1 and §4.2.
  </read_first>
  <behavior>
    - A run whose `write-baseline-b` reports BAD yields SKIPPED for `sdp-lock`, `write-inhibited`, `sdp-unlock` and `write-restored`, and `operator.sdp_lock` is never called.
    - Those four SKIPPED reasons contain the substring `no lock was emitted` and do NOT contain the chip-ID gate wording.
    - A `marginal` `write-baseline-a` closes the gate identically — any non-OK verdict closes it.
    - A failing `write-baseline-b` followed by a passing `write-baseline-a` leaves the gate CLOSED.
    - A fully successful leg calls `operator.sdp_unlock` exactly once.
    - A leg that raises after a successful `sdp-lock` but before the explicit unlock step still calls `operator.sdp_unlock` exactly once, via the `finally` drain.
    - A leg whose explicit `sdp-unlock` step reports non-OK calls `operator.sdp_unlock` twice — once explicitly, once from the drain retry.
    - For gh#20's shape on an ALLOW chip, `count_applicable` returns `n_ran=5, m_applicable=10`.
  </behavior>
  <action>
**A. `_baseline_closes_sdp_gate(result: StepResult) -> bool`** — add immediately after
`_id_step_closes_gate` (`:974-985`), mirroring its shape. Body: `return result.verdict != VERDICT_OK`.
The docstring must state the **widening** rather than copying the analog's narrow tuple: it closes on
BAD, `marginal`, SKIPPED **and** NA, because a contact fault is as disqualifying as a dead write path —
a lock must not be emitted at a part whose write path was not demonstrated to transition in both
directions.

**B. Two module frozensets** beside `_SDP_LEG_OPS`:
- `_SDP_BASELINE_OPS = frozenset({OP_WRITE_BASELINE_B, OP_WRITE_BASELINE_A})` — the gate's **inputs**.
- `_SDP_LEG_GATED_OPS = frozenset({OP_SDP_LOCK, OP_WRITE_INHIBITED, OP_SDP_UNLOCK, OP_WRITE_RESTORED})`
  — the gate's **outputs**. `OP_SDP_UNLOCK`'s membership is D-20; carry its justification inline,
  including the explicit statement that this is a different mechanism from `_DESTRUCTIVE_OPS` and does
  not weaken LEG-09.

Add both names to `firestarter_app/tests/test_op_registration_parity.py`'s `_REGISTRY_CONSTANT_NAMES`
(`:138-140`) so `_op_names_referenced_in` resolves them transitively. Do **not** add them to
`_POLICED_REGISTRIES`: that dict polices *dispatch and derivation* surfaces, where a missing op means an
op silently takes a wrong path, whereas these two are policy subsets of the already-policed
`_SDP_LEG_OPS`, and policing them would add roughly eighteen exemption rows asserting that read-only ops
are not gated by a write-path gate. Then **run `pytest tests/test_op_registration_parity.py -o
addopts="" -q` and follow its own failure output** — if a guard demands they be declared, declare them
and record why in the SUMMARY. Never loosen a guard.

**C. Three wiring sites in `run_plan`** (measured, RESEARCH §4.1):

1. *Init* — `baseline_gate_closed = False` beside `destructive_gate_closed = False` at `:854`, and
   `unlock_cleanup: Callable[[], None] | None = None` beside the `cleanup` list at `:869`.
2. *Guard clause* — immediately **after** the destructive-gate guard ending at `:883` and **before**
   the `_run_step` call at `:885`:
   `if step.op in _SDP_LEG_GATED_OPS and baseline_gate_closed:` appends
   `_skip_result(step.op, _SDP_UNLOCK_GATE_REASON if step.op == OP_SDP_UNLOCK else
   _SDP_BASELINE_GATE_REASON)` and `continue`s.
   ⚠ **Order is load-bearing.** The ID gate fires first and renders its own wording;
   `_DESTRUCTIVE_GATE_REASON` reads *"chip-ID mismatch — destructive steps gated (chip left pristine)"*,
   and telling a reader the chip ID closed the gate when the write path did is exactly what D-08
   forbids. Never reference that constant from the new guard.
3. *Set clause* — immediately **after** the ID-gate set clause at `:920-921`:
   `if step.op in _SDP_BASELINE_OPS: baseline_gate_closed = baseline_gate_closed or
   _baseline_closes_sdp_gate(result)`. **Sticky** by construction; comment that the stickiness is
   deliberate so a failing B followed by a passing A cannot reopen it.

**D. Cleanup de-registration** (RESEARCH §4.2, assumption A2). With this phase's explicit `sdp-unlock`
step in the plan, a successful lock registers a cleanup **and** the plan step runs it — two unlock
emissions. 133 D-11 rejected the both-paths shape precisely because of the double-count and the
endurance notice. Hold the handle:
- in the lock-to-cleanup block at `:890-918`, after `cleanup.append(_unlock_cleanup)`, also assign
  `unlock_cleanup = _unlock_cleanup`;
- add a sibling clause: when `step.op == OP_SDP_UNLOCK` and `result.verdict == VERDICT_OK` and
  `unlock_cleanup is not None`, call `cleanup.remove(unlock_cleanup)` and reset `unlock_cleanup = None`.

Use `cleanup.remove(handle)`, **never** `cleanup.clear()` — the registry's own comment at `:855-868`
declares it *"deliberately GENERIC rather than a hardcoded lock-to-unlock window"*, so `.clear()` would
over-reach the moment any other op registers something. A **failed** explicit unlock must leave the
handle registered so the drain still retries.

Do **not** touch `_dispatch_sdp`'s signature, `_UNLOCK_CLEANUP_SWALLOWED`, the `finally` drain loop, or
`count_applicable`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test_sdp_leg.py tests/test_chip_test.py tests/test_op_registration_parity.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/chip_test.py` contains `def _baseline_closes_sdp_gate(`, `_SDP_BASELINE_OPS`, `_SDP_LEG_GATED_OPS`, `baseline_gate_closed` and `unlock_cleanup`.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.chip_test import _SDP_LEG_GATED_OPS,OP_SDP_UNLOCK,_SDP_BASELINE_OPS;assert OP_SDP_UNLOCK in _SDP_LEG_GATED_OPS;assert not (_SDP_BASELINE_OPS &amp; _SDP_LEG_GATED_OPS)"` exits 0.
    - `grep -c 'cleanup.clear()' firestarter/chip_test.py` returns 0.
    - `grep -n '_DESTRUCTIVE_GATE_REASON' firestarter/chip_test.py` shows it referenced only by its definition and the pre-existing destructive-gate guard — the new baseline guard does not reference it.
    - `git diff firestarter/chip_test.py` shows no change to the `def _dispatch_sdp(` signature line, to `count_applicable`, or to the `finally` drain loop.
    - `git diff --stat firestarter/eprom_operations.py` is empty.
    - `pytest tests/test_op_registration_parity.py tests/test_chip_test_sdp_leg.py tests/test_chip_test.py -o addopts="" -q` all pass.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0.
  </acceptance_criteria>
  <done>A dead write path can no longer produce a lock emission, and a completed leg unlocks exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `sdp_hold_state` — the three-valued derivation, in the engine and never in the report</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/firestarter/chip_test.py` `:974-985` (the pure `StepResult`-to-value function shape) and `:960-963` (this module has **no logger and no `logging` import** — keep it that way).
    - `firestarter_app/firestarter/chip_test.py` `:1500-1544` (`_RAN_VERDICTS`, `count_applicable`) — **read only, do not edit**.
    - `firestarter_app/tests/test_op_registration_parity.py` `:276-330` (the declared non-registries, including `diagnostic_report.py renderer` — a **class locator over the whole `DiagnosticReport` body**) and `:579-652` + `:733-745` (`_count_op_vocabulary_references`, `_measure_op_vocabulary`, `test_non_registry_still_has_no_ops` and its failure message).
    - `firestarter_app/firestarter/sdp_honesty.py` `:25-92` — Phase 132 built this as **this phase's** forward contract; call it, do not re-author the sentences, do not extend the module (it is in the mypy STRICT island and deliberately imports no `click`).
    - `.planning/phases/134-…/134-CONTEXT.md` D-10, D-12 and D-15.
  </read_first>
  <behavior>
    - `sdp_hold_state` returns `SDP_HOLD_HELD` when the `write-inhibited` result verdict is OK.
    - It returns `SDP_HOLD_NOT_HELD` when that verdict is BAD.
    - It returns a string beginning with `SDP_HOLD_NOT_RUN` and carrying a non-empty reason when the step is absent from `results`, or its verdict is NA, SKIPPED or `marginal`.
    - The returned value is always a `str` — never `True`, `False`, `None`, or any other type.
    - `sdp_oracle_applicable(plan)` is `True` for an ALLOW chip's plan (leg in `steps` or in `locked_destructive`) and `False` for a REFUSE chip's plan.
  </behavior>
  <action>
Add to `firestarter_app/firestarter/chip_test.py`, beside `_id_step_closes_gate`:

- Three value constants — `SDP_HOLD_HELD = "HELD"`, `SDP_HOLD_NOT_HELD = "NOT-HELD"`,
  `SDP_HOLD_NOT_RUN = "NOT-RUN"`. These are **report values, not op strings**: they carry no `OP_`
  prefix and therefore never join `_ALL_OPS` or `_MULTIWORD_OP_VALUES`. State that in a comment so a
  later reader does not "helpfully" register them.
- `sdp_oracle_applicable(plan: Plan) -> bool` — derived structurally: `True` iff the plan carries an
  `OP_WRITE_INHIBITED` entry that is either a `supported=True` step **or** a `locked_destructive` pair.
  A REFUSE chip's `supported=False` step yields `False`. Do not call `sdp_capability` here; the plan
  already encodes the decision and a second source of truth is how the two drift.
- `sdp_hold_state(plan: Plan, results: list[StepResult]) -> str` — pure, no logger, no I/O:
  - `write-inhibited` verdict OK → `SDP_HOLD_HELD`
  - verdict BAD → `SDP_HOLD_NOT_HELD`
  - verdict NA / SKIPPED / `marginal`, or the step absent from `results` entirely →
    `f"{SDP_HOLD_NOT_RUN}: {reason}"`, where `reason` is that result's own `reason` when present and
    otherwise fixed prose naming the family fact (the oracle did not run; the chip's protection state
    cannot be read back, so nothing about it is being claimed).
  - Compose the honesty caveat by calling `sdp_honesty.unreadable_state_caveat()` rather than writing a
    new sentence.

⚠ **This derivation may NOT live in `DiagnosticReport`.** `tests/test_op_registration_parity.py`
declares `diagnostic_report.py renderer` a non-registry with a class locator over the whole
`DiagnosticReport` body, and `test_non_registry_still_has_no_ops` (`:733-745`) re-measures by AST every
run that it carries zero op vocabulary. Any `if result.op == OP_WRITE_INHIBITED` — or the bare
hyphenated op literal — inside `to_dict`, `render` or `_step_dict` fails that guard with *"A declared
non-registry has acquired op vocabulary — PROMOTE it to `_POLICED_REGISTRIES`, do not loosen this
guard."* Three independent reasons put the logic here instead: the inversion guard, P-07
(`chip_test.py` is scanned in full while `cli_handlers.py` helpers sit behind a fail-open allow-list),
and the mypy budget (`chip_test.py` bodies are unchecked and cost nothing, while `cli_handlers.py` is
STRICT with **2** errors of headroom).

⚠ **Never a boolean.** P-06 prevention 3: a JSON `true` on a key like `locked` or `protection_enabled`
reads as ground truth for a state this family cannot report. This function returns `str` only; plan
`134-06` adds the committed assertion that no such boolean exists anywhere in `to_dict()`.

Add unit tests to `firestarter_app/tests/test_chip_test_sdp_leg.py` named so `pytest -k "hold"` selects
them, covering every branch above — including the "step absent from results entirely" case, which is
laundering route R6 — and asserting `isinstance(value, str)` in each.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test_sdp_leg.py -k "hold" tests/test_op_registration_parity.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/chip_test.py` contains `def sdp_hold_state(`, `def sdp_oracle_applicable(`, `SDP_HOLD_HELD`, `SDP_HOLD_NOT_HELD`, `SDP_HOLD_NOT_RUN`.
    - `grep -c 'OP_WRITE_INHIBITED\|OP_SDP_LOCK\|OP_SDP_UNLOCK\|OP_WRITE_BASELINE' firestarter/diagnostic_report.py` returns 0.
    - `pytest tests/test_op_registration_parity.py::test_non_registry_still_has_no_ops -o addopts="" -q` passes.
    - `pytest tests/test_chip_test_sdp_leg.py -k "hold" -o addopts="" -q` selects ≥ 5 tests, all pass, including one asserting the value is a `str` and one covering the step-absent case.
    - `grep -c 'import logging' firestarter/chip_test.py` returns 0 — the module still has no logger.
    - `grep -q 'unreadable_state_caveat' firestarter/chip_test.py` — the honesty sentence is called, not re-authored.
  </acceptance_criteria>
  <done>The three-valued hold state is derived in the engine, as a string, with the inversion guard still green.</done>
</task>

<task type="auto">
  <name>Task 3: gate + de-registration proofs, the LEG-09 distinction pin, and non-vacuity #6</name>
  <files>firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` — Phase 133's named proofs: `test_unlock_exempt_from_destructive`, `test_gate_closed_from_start`, `test_lock_ran_then_gate_closes`, `test_finally_drains_on_exception`, `test_empty_registry_noop`, `test_drain_continues_after_failure`, `test_drain_does_not_mutate_results`; plus `_readback_operator` and `_dead_write_path_operator` from plan `134-02`.
    - `firestarter_app/firestarter/chip_test.py` — the gate and de-registration as written in Task 1.
    - `.planning/phases/134-…/134-VALIDATION.md` §"Non-Vacuity Obligations" row 6.
    - `.planning/REQUIREMENTS.md` §LEG — LEG-09's evidence clause and its named tests.
  </read_first>
  <action>
Add tests to `firestarter_app/tests/test_chip_test_sdp_leg.py`. Do **not** edit any Phase-133 named
proof; add beside them.

**A. Baseline-gate closure (D-08).** For an ALLOW chip's `write_scope="full"` plan driven by
`_dead_write_path_operator()`:
- assert `sdp-lock`, `write-inhibited`, `sdp-unlock` and `write-restored` are all `VERDICT_SKIPPED`;
- assert `operator.sdp_lock.assert_not_called()`;
- assert each of the four SKIPPED reasons contains `no lock was emitted` and does **not** contain the
  chip-ID gate wording (assert against the imported `_DESTRUCTIVE_GATE_REASON` constant, so a reword of
  that constant cannot silently satisfy the test);
- assert `count_applicable(plan, results)` returns `n_ran == 5` and `m_applicable == 10` — gh#20's
  measured shape, the run whose banner today misleadingly reads "4 of 4" and under this leg reads
  "5 of 10". Do **not** edit `count_applicable` to make this pass.

**B. Any-non-OK closure.** Parametrise over baseline verdicts BAD, `marginal`, SKIPPED and NA and assert
the gate closes in every case — the widening over `_id_step_closes_gate`'s two-verdict tuple.

**C. Stickiness.** A failing `write-baseline-b` followed by a passing `write-baseline-a` must still
leave the four gated steps SKIPPED.

**D. D-20's LEG-09 distinction pin — a NEW test, not an edit.** Assert both halves:
1. `OP_SDP_UNLOCK` is still absent from `_DESTRUCTIVE_OPS` (LEG-09's structural claim, unchanged);
2. in a run where the **destructive** gate is closed but the baseline gate is OPEN and the lock ran OK,
   the explicit `sdp-unlock` step still runs and `operator.sdp_unlock` is called — i.e. the *destructive*
   gate never skips the unlock.
Docstring: LEG-09 is scoped to the destructive gate (`_DESTRUCTIVE_OPS` membership +
`test_unlock_exempt_from_destructive`), a structurally different mechanism from the baseline gate, so
D-20 does not weaken it. Name the distinction explicitly for a later reader.

**E. Cleanup de-registration (RESEARCH §4.2's three properties).**
1. A completed leg calls `operator.sdp_unlock` **exactly once**.
2. A leg that raises after a successful `sdp-lock` but before the explicit unlock still calls it
   **exactly once**, via the drain — and Phase 133's `test_finally_drains_on_exception` stays green.
3. A **failed** explicit unlock (verdict non-OK) leaves the handle registered, so `sdp_unlock` is called
   **twice** (explicit + drain retry).

**F. Non-vacuity obligation #6 (VALIDATION.md row 6) — OBSERVE, do not assert.** Temporarily remove
`OP_SDP_LOCK` from `_SDP_LEG_GATED_OPS` in `firestarter_app/firestarter/chip_test.py`, run the
dead-write-path gate test, and **capture the RED output verbatim in the SUMMARY** — the run must be
observed calling `operator.sdp_lock`, i.e. emitting a lock at a part whose baseline write never worked
(gh#20's exact bench). Then restore `chip_test.py` byte-identically (`git diff` empty for that file) and
re-run green.

**G. Name the seventh route.** Add a module-level comment beside these tests stating that the baseline
gate is a **seventh** route to a non-running oracle beyond research's R1–R6, that under D-08 + D-15 it
fails closed (exit 1 from the baseline BAD, or at least 2 via the NOT-RUN floor) so it is not a
laundering route, and that plan `134-10` tests it in the same family as R1–R6.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test_sdp_leg.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_chip_test_sdp_leg.py -k "baseline_gate or sticky or deregist or unlock_once" -o addopts="" -q` selects ≥ 7 tests, all pass.
    - A test asserts `n_ran == 5` and `m_applicable == 10` for the dead-write-path ALLOW run, and `git diff firestarter/chip_test.py` shows `count_applicable` unchanged.
    - The LEG-09 distinction test exists as a NEW test; `git diff -U0 -- tests/test_chip_test_sdp_leg.py | grep '^-' | grep -v '^---'` shows no deletion inside `test_unlock_exempt_from_destructive`, `test_lock_ran_then_gate_closes`, `test_finally_drains_on_exception`, `test_empty_registry_noop`, `test_drain_continues_after_failure` or `test_drain_does_not_mutate_results`.
    - The SUMMARY contains the verbatim RED output of obligation #6 showing `sdp_lock` observed as called, and confirms byte-identical restoration of `firestarter/chip_test.py`.
    - `pytest tests/test_chip_test_sdp_leg.py tests/test_chip_test.py tests/test_dev_test_cmd.py tests/test_op_registration_parity.py -o addopts="" -q` all pass.
    - `tools/ci_replica_venv.sh` exits 0; `mypy errors: N (watermark: 35)` with N ≤ 35 recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The gate is proven in every closure direction, the unlock fires exactly once, and the gate has been seen absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline step outcome → lock emission | the decision to send an irreversible-on-this-family command |
| cleanup registry → wire | how many unlock emissions a part actually receives |
| engine derivation → report field | the string a stranger reads as a statement about their chip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-14 | Denial of Service | emitting a lock at a part that cannot be rewritten (gh#20's bench) | critical | mitigate | `_baseline_closes_sdp_gate` closes on any non-OK baseline verdict; non-vacuity #6 observes the gate absent |
| T-134-15 | Repudiation | `sdp-unlock OK` as the run's last word when nothing was locked | high | mitigate | D-20 puts `OP_SDP_UNLOCK` in the baseline-gate set and renders SKIPPED with its own reason |
| T-134-16 | Tampering | wrong-cause wording: chip-ID gate text on a write-path closure | medium | mitigate | Separate reason constants, asserted against the imported `_DESTRUCTIVE_GATE_REASON` |
| T-134-17 | Denial of Service | double unlock emission (endurance cost on a limited-cycle part) | medium | mitigate | `cleanup.remove(handle)` on a successful explicit unlock; three committed properties |
| T-134-18 | Spoofing | a JSON boolean read as ground truth for an unreadable protection state | high | mitigate | `sdp_hold_state` returns `str` only; plan `134-06` adds the no-boolean assertion over `to_dict()` |
| T-134-SC | Tampering | package-manager installs | n/a | accept | No new package installed by this phase (RESEARCH §9) |
</threat_model>

<verification>
- `pytest tests/test_chip_test_sdp_leg.py tests/test_chip_test.py tests/test_op_registration_parity.py tests/test_dev_test_cmd.py -o addopts="" -q` green.
- `test_non_registry_still_has_no_ops` green — `DiagnosticReport` still carries zero op vocabulary.
- Phase 133's named LEG-09/10/11 proofs green, with no deletions inside them.
- Non-vacuity obligation #6 observed RED and restored byte-identically.
- `tools/ci_replica_venv.sh` 5/5, mypy ≤ 35.
</verification>

<success_criteria>
- A dead write path closes the gate and no lock is emitted; the four gated steps render SKIPPED with the right cause.
- `sdp-unlock` is gated by the baseline gate (D-20) while remaining exempt from the destructive gate (LEG-09).
- A completed leg emits exactly one unlock; an interrupted one still emits exactly one; a failed one retries.
- `sdp_hold_state` is a pure engine function returning a string, and `DiagnosticReport` is untouched.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-04-SUMMARY.md` when done.
</output>
