---
phase: 134-the-plan-derived-sdp-oracle-in-dev-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/tests/test_op_registration_parity.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
autonomous: true
requirements: [LEG-03]
must_haves:
  truths:
    - "The four new engine-local op strings exist as module constants in `chip_test.py` and the op-registration parity gate is GREEN in the SAME commit that adds them (D-06's six-step vocabulary; the gate's module-level `assert len(_ALL_OPS) == 9` fires at COLLECTION, so there is no green intermediate state)."
    - "The inhibited-write payload comes from its own named generator `generate_inhibited_pattern`, NOT from a second `generate_pattern` call (D-19) — proven by assertions computed against the LIVE generators for the real region `(0, 256)`, never against a byte literal."
    - "Pattern B differs from pattern A at EVERY byte, and neither A nor B is all-`0x00` or all-`0xFF` (D-19 / LEG-03) — a one-page lock leak stays detectable."
    - "A fully-B read-back cannot reclassify as `blank/contact`: B's `ff_ratio` is measured against the live `_FF_RATIO_THRESHOLD`, not a hard-coded 0.98 (D-05)."
    - "The pre-edit CI-parity + CI-replica record exists with a REAL mypy count before any production line moves — mypy headroom is 2 (33 vs watermark 35), and `MIN_CHECKED_SOURCE_FILES = 120` is a FLOOR, so adding files is safe (CONTEXT.md correction 4)."
    - "`_ALWAYS_WRITES_NOTICE` and `DiagnosticReport` acquire NO op vocabulary in this plan — all four new op strings are hyphenated and auto-join `_MULTIWORD_OP_VALUES`, arming the inversion guard against them from this commit on."
  artifacts:
    - .planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md
    - firestarter_app/firestarter/chip_test.py
    - firestarter_app/tests/test_op_registration_parity.py
    - firestarter_app/tests/test_chip_test_sdp_leg.py
  key_links:
    - "`_SDP_LEG_OPS` must be added to `tests/test_op_registration_parity.py`'s `_REGISTRY_CONSTANT_NAMES` (`:138-140`) or `_op_names_referenced_in` will NOT resolve it transitively, and every later plan's `_dispatch_step` / `run_plan` reference will read as 'the new ops are unregistered'."
    - "`generate_inhibited_pattern` must call `generate_pattern` exactly once and complement its output — deriving B by calling `generate_pattern` again with any region makes A == B and the milestone's central assertion a tautology (P-01, the headline pitfall)."
---

<objective>
Land the four new engine-local op strings, the `_SDP_LEG_OPS` registry, the `FLAG_SKIP_SDP_UNLOCK`
import, and the D-19 inhibited-write pattern generator in `firestarter_app/firestarter/chip_test.py`
— together with, in the SAME commit, the six coupled edits `tests/test_op_registration_parity.py`
needs to survive them. Then prove LEG-03's five pattern assertions against the live generators, and
observe non-vacuity obligation #1 RED once.

Purpose: this is the phase's vocabulary foundation. Everything downstream keys on these four strings.
The parity gate fires at COLLECTION time, so the constants and the gate cannot be split across commits.
Output: 4 op constants, 1 frozenset, 1 pattern generator, 2 gate-reason constants, a green parity gate
at 13 ops, LEG-03's proofs, and the phase's pre-edit CI-parity record.
</objective>

<repo_topology>
**All code in this plan lives inside the `firestarter_app` git submodule.** `cd /workspaces/firestarter_app`
and commit THERE, on its own milestone branch `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof` — the names deliberately diverge). Verify with
`git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` before the first edit.

`134-CI-PARITY.md` is a **meta-repo** artifact and is committed in `/workspaces`, not in the submodule.

The top-level `/workspaces/firestarter/` directory is the **Arduino firmware submodule and is not
touched by this phase at all.** Every `firestarter/…` path in CONTEXT.md/RESEARCH.md means
`firestarter_app/firestarter/…`.
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CONTEXT.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RESEARCH.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-PATTERNS.md
@.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md
@.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md
</context>

## Requirements this plan may mark Complete

**MAY tick (in `.planning/REQUIREMENTS.md`):**
- **LEG-03** — the inhibited-write payload's own named generator, bitwise complement, differs in every
  byte, neither all-`0x00` nor all-`0xFF`. Fully discharged here; nothing later adds to it.

**Contributes to but MUST NOT tick:**
- LEG-04 — closed by plan `134-03` (the derive_plan step ordering is what proves "transition before lock").
- LEG-05, LEG-07, LEG-08, LEG-16 — closed by plan `134-02` (the oracle dispatcher).
- LEG-01, LEG-02 — closed by plan `134-03`.

**MUST NOT touch, re-tick, or re-evidence:** LEG-09, LEG-10, LEG-11, LEG-15 are already `[x]` from
Phase 133. They are this phase's **regression floor** — their named tests must stay byte-identically
green. Nor any RELOCK-\*, CHAN-\*, or CLOSE-\* row.

## Artifacts this phase produces (new symbols introduced by THIS plan)

Newly-created symbols — these do not exist at `firestarter_app@57e8eb5` and must be excluded from any
source-drift verification:

| Symbol | File | Kind |
|---|---|---|
| `OP_WRITE_BASELINE_B = "write-baseline-b"` | `firestarter_app/firestarter/chip_test.py` | op constant |
| `OP_WRITE_BASELINE_A = "write-baseline-a"` | `firestarter_app/firestarter/chip_test.py` | op constant |
| `OP_WRITE_INHIBITED = "write-inhibited"` | `firestarter_app/firestarter/chip_test.py` | op constant |
| `OP_WRITE_RESTORED = "write-restored"` | `firestarter_app/firestarter/chip_test.py` | op constant |
| `_SDP_LEG_OPS` | `firestarter_app/firestarter/chip_test.py` | frozenset registry |
| `generate_inhibited_pattern(start, length) -> bytes` | `firestarter_app/firestarter/chip_test.py` | pure function |
| `_SDP_BASELINE_GATE_REASON` | `firestarter_app/firestarter/chip_test.py` | module constant |
| `_SDP_UNLOCK_GATE_REASON` | `firestarter_app/firestarter/chip_test.py` | module constant |
| `_NOT_SDP_LEG_REASON` | `firestarter_app/tests/test_op_registration_parity.py` | exemption-reason template |
| `134-CI-PARITY.md` | `.planning/phases/134-…/` | phase record |

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-edit CI-parity + CI-replica baseline</name>
  <files>.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md</files>
  <read_first>
    - `.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md` — the exact document shape to copy (§1 board state, §4 the certifying mypy/file counts, §5 file-count accounting).
    - `.planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md` — the original four-leg recipe record.
    - `firestarter_app/tools/ci_parity.sh` — read it to confirm the leg list and that leg 1 exports `FIRESTARTER_FW_ROOT` at an empty dir.
    - `firestarter_app/tools/ci_replica_venv.sh` — the ONLY local path to a real mypy count.
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-VALIDATION.md` §"Test Infrastructure".
  </read_first>
  <action>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md` with a
`## Before (pre-edit)` section only; the `## After` section is written by plan `134-11`.

Run, from `/workspaces/firestarter_app`:
1. `tools/ci_parity.sh` — record every leg's exit status verbatim.
2. `tools/ci_replica_venv.sh` — record all five legs and the certifying two lines
   `mypy errors: N (watermark: 35)` and `checked N source files`.
3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q | tail -5` — record the passed count
   and the coverage percentage.

Record, explicitly and as prose rather than as a claimed leg:
- **`ci_parity.sh` has NO no-board leg.** The no-board condition is ambient. Record the actual board
  state (`ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null`) as an observation; do NOT describe it as a leg.
- **Leg 1 points `FIRESTARTER_FW_ROOT` at an empty dir** — state that this is what stops the
  devcontainer's sibling layout from masking CI-only test defects.
- The devcontainer is Python 3.12 while CI is 3.9/3.11, and the devcontainer's own `mypy` exits 2
  against numpy — so only the `ci-replica` numbers are quotable.
- The starting budget: **mypy headroom 2** (33 errors vs watermark 35) and `checked` vs
  `MIN_CHECKED_SOURCE_FILES = 120`, stating that 120 is a **FLOOR** (`checked < 120` ⇒ fail,
  `tools/check_mypy_watermark.py:48`), so adding source/test files moves further ABOVE it. Cite this as
  correcting Phase 133 D-15's inverted "file budget spent" reading (CONTEXT.md correction 4).

Do not modify any file under `firestarter_app/` in this task. Commit in the **meta** repo.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md &amp;&amp; grep -q 'watermark: 35' /workspaces/.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-CI-PARITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `134-CI-PARITY.md` exists and contains a literal line matching `mypy errors: <N> (watermark: 35)` copied from `tools/ci_replica_venv.sh` output, not retyped from the record.
    - It contains a literal `checked <N> source files` line and states in prose that 120 is a floor.
    - It contains a `## Before (pre-edit)` heading and NO `## After` heading.
    - It contains the sentence fragment "no no-board leg" and records the observed board state.
    - `git -C /workspaces/firestarter_app status --porcelain -- firestarter tests` prints nothing (no submodule source touched by this task).
  </acceptance_criteria>
  <done>The phase's pre-edit budget is measured and written down before any production line moves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Op vocabulary + `_SDP_LEG_OPS` + pattern-B generator, with the parity gate in the SAME commit</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_op_registration_parity.py</files>
  <read_first>
    - `firestarter_app/firestarter/chip_test.py` `:20-40` (module docstring contract line "sets no VPP, builds no wire dict" and the `from firestarter.constants import FLAG_CAN_ERASE` import at `:37`).
    - `firestarter_app/firestarter/chip_test.py` `:53-72` (`address_fold_byte`, `generate_pattern` — the source the B generator must NOT re-call for a second region).
    - `firestarter_app/firestarter/chip_test.py` `:290-310` (the `write-partial` vocabulary-stop comment, and the Phase-133 `OP_SDP_LOCK`/`OP_SDP_UNLOCK` block whose comment discipline to mirror).
    - `firestarter_app/firestarter/chip_test.py` `:663-710` (`_DESTRUCTIVE_OPS`, `_MULTI_RUN_OPS`, `_SDP_OPS` and its "documented-but-dead frozenset" self-warning; `_DESTRUCTIVE_GATE_REASON` at `:705-707`).
    - `firestarter_app/firestarter/constants.py` `:130-140` (`FLAG_SKIP_SDP_UNLOCK = 0x100` at `:137`).
    - `firestarter_app/tests/test_op_registration_parity.py` `:118-160` (`_OP_CONSTANT_NAMES` `:122`, `_ALL_OPS` `:128`, `_REGISTRY_CONSTANT_NAMES` `:138-140`, `_MULTIWORD_OP_VALUES` `:148`, the module-level `assert len(_ALL_OPS) == 9` at `:150`).
    - `firestarter_app/tests/test_op_registration_parity.py` `:231-246` (`_POLICED_REGISTRIES`, `_POLICED_REGISTRY_COUNT = 6`).
    - `firestarter_app/tests/test_op_registration_parity.py` `:375-445` (the exemption table, its `_NOT_SDP_REASON` templated rows, and the two dischargeable `derive_plan` rows at `:422-427`).
    - `firestarter_app/tests/test_op_registration_parity.py` `:495-545` (`_assert_op_parity`, `_stale_exemption_rows`) and `:786-806` (the membership pin matrix).
    - `.planning/phases/134-…/134-PATTERNS.md` §"chip_test.py — new op constants + `_SDP_LEG_OPS`" for the verbatim comment shapes to mirror.
  </read_first>
  <behavior>
    - `generate_inhibited_pattern(0, 256)` returns bytes of the same length as `generate_pattern(0, 256)`.
    - Every byte of `generate_inhibited_pattern(0, 256)` differs from the corresponding byte of `generate_pattern(0, 256)` — 256 of 256, not "somewhere".
    - `generate_inhibited_pattern(0, 256) != generate_pattern(0, 256)`.
    - Neither pattern is `b"\x00" * 256` nor `b"\xFF" * 256`.
    - `len(_SDP_LEG_OPS) == 4` and every member is also a member of `_DESTRUCTIVE_OPS`.
    - `pytest tests/test_op_registration_parity.py` collects and passes with `len(_ALL_OPS) == 13`.
  </behavior>
  <action>
**This task is ONE commit. Do not commit between the `chip_test.py` edit and the parity-gate edit** —
`assert len(_ALL_OPS) == 9` at `tests/test_op_registration_parity.py:150` is a bare MODULE-LEVEL assert
that fires at COLLECTION, so the first new `OP_*` constant turns all seven tests in that module into
collection errors. There is no green intermediate state.

**A. `firestarter_app/firestarter/chip_test.py`**

1. Extend the existing import at `:37` to also import `FLAG_SKIP_SDP_UNLOCK` from
   `firestarter.constants` (value `0x100`, `constants.py:136`). Keep the existing
   `# 0x02 -- do NOT redefine; import` comment discipline. Do not redefine the value locally.
   Note the flag is imported here but not yet *used*; plan `134-02` passes it. `ruff`'s `F` rules do not
   flag unused imports of names referenced nowhere — verify with `ruff check firestarter/` and, if it
   does flag it, move the import into plan `134-02` instead and say so in the SUMMARY.
2. Immediately after the Phase-133 op block ending at `:310`, add four op constants in the D-06 leg
   order: `OP_WRITE_BASELINE_B = "write-baseline-b"`, `OP_WRITE_BASELINE_A = "write-baseline-a"`,
   `OP_WRITE_INHIBITED = "write-inhibited"`, `OP_WRITE_RESTORED = "write-restored"`. Carry a comment in
   the `:302-308` shape stating: these are **engine-local op strings, NOT wire constants** — no
   `constants.py` / `firestarter.h` mirroring is triggered, so this phase needs no firmware lockstep and
   no `.hex` re-cut. State that D-07 chose **two** baseline ops rather than one folded `sdp-baseline`
   because `DiagnosticReport.render()`'s console table shows only `op`/`verdict`/`error_code`/
   `fingerprint` — `reason` reaches only the markdown table and the JSON, so a failing baseline
   *direction* hidden in `reason` is invisible to whoever reads the terminal, on the very step that
   decides whether a lock is emitted at all.
3. Add `generate_inhibited_pattern(start: int, length: int) -> bytes` next to `generate_pattern`
   (`:64`). It must call `generate_pattern(start, length)` exactly **once** and return the bitwise
   complement of its output (`~b & 0xFF` per byte). Its docstring must state P-01 explicitly:
   `generate_pattern` is a pure function of `(start, length)`, so deriving B by calling it a second time
   — with the same region, or with a "different seed" that reduces to the same region — makes A and B
   byte-identical and the leg's central assertion a tautology that reads as correct in review. Also
   record why a nonce/timestamp is rejected (D-19): it breaks reproducibility and re-keys
   `dedup_fingerprint` on every run.
4. Add `_SDP_LEG_OPS = frozenset({OP_WRITE_BASELINE_B, OP_WRITE_BASELINE_A, OP_WRITE_INHIBITED,
   OP_WRITE_RESTORED})` next to `_SDP_OPS` (`:703`), with the `:692-698` module-constant-not-DB-field
   justification and the same "documented-but-dead frozenset" self-warning.
5. Widen `_DESTRUCTIVE_OPS` (`:663`) to include all four new ops — they mutate the part, so the
   chip-ID destructive gate must cover them. Keep `OP_SDP_UNLOCK` OUT (LEG-09's shipped asymmetry; do
   not touch that line's intent).
6. Add two module constants beside `_DESTRUCTIVE_GATE_REASON` (`:705-707`), consumed by plan `134-04`:
   `_SDP_BASELINE_GATE_REASON` and `_SDP_UNLOCK_GATE_REASON`. Both must contain the exact substring
   `no lock was emitted` and must name the family fact (the baseline write/read-back transition did not
   complete; the part is left as found), never a mechanism name and never `_DESTRUCTIVE_GATE_REASON`'s
   chip-ID wording. `_SDP_UNLOCK_GATE_REASON` additionally states there is nothing to unlock.
   These follow the reason-string discipline at `chip_test.py:569-580`.
7. Widen the module docstring's contract line at `:20-25` ("sets no VPP, builds no wire dict") to
   **deliberately narrow** it: this module will pass exactly one `operation_flags` bit
   (`FLAG_SKIP_SDP_UNLOCK`) on exactly one op. Narrow it explicitly rather than silently violating it.

**B. `firestarter_app/tests/test_op_registration_parity.py` — six coupled edits, same commit**

1. `:150` — `assert len(_ALL_OPS) == 9` → `== 13`, and update its message text to match.
2. `:138-140` — add `"_SDP_LEG_OPS"` to `_REGISTRY_CONSTANT_NAMES`. Without this,
   `_op_names_referenced_in` will not resolve the frozenset transitively and every later plan's
   `_dispatch_step`/`run_plan` reference will read as "the new ops are unregistered".
3. `:231-246` — add `"_SDP_LEG_OPS": chip_test_mod._SDP_LEG_OPS` to `_POLICED_REGISTRIES` and bump
   `_POLICED_REGISTRY_COUNT` 6 → 7.
4. Exemption rows. Do **not** trust a predicted count: run the gate, read `_assert_op_parity`'s own
   failure list, and add exactly one reasoned row per reported pair. The expected target shape is the
   full 7-registry × 13-op grid, i.e. roughly 21 new rows —
   `_SDP_LEG_OPS` × the 9 pre-existing ops (add a `_NOT_SDP_LEG_REASON` template mirroring
   `_NOT_SDP_REASON`), `_MULTI_RUN_OPS` × 4 new ops (D-03: the leg's ops are single-run and fold their
   own read-back, so the marginal-on-disagreement policy does not apply), `_SDP_OPS` × 4 new ops (they
   are write-shaped and need a source path plus a read-back, which `_dispatch_sdp`'s frozen four-positional
   signature cannot carry), `_dispatch_multi_run` × 4 new ops (structurally never routed there), plus
   **temporary** rows for `_dispatch_step` × 4 and `derive_plan` × 4 that plans `134-02` and `134-03`
   discharge. Mark those eight rows with the literal marker `TEMPORARY — discharged by plan 134-02` /
   `TEMPORARY — discharged by plan 134-03` inside the reason string so the later plans can grep them.
   Every row is a real sentence naming the decision (`:400-411`'s prose pattern), never a placeholder.
5. `:801-802` — leave the two `("derive_plan", OP_SDP_LOCK): False` / `("derive_plan", OP_SDP_UNLOCK):
   False` pins as-is; plan `134-03` flips them. Extend the `expected_membership` matrix at `:792-805`
   with the new ops' memberships as they stand after THIS plan.
6. Update the module docstring's op census so it states 13, not 9.

⚠ **Measured correction to record in the SUMMARY.** CONTEXT.md says the stale-row guard "fails CLOSED —
a half-done discharge is a RED gate". That is only half true: `_stale_exemption_rows` (`:519-545`) fails
on a row naming a **non-existent op or registry**, and nothing catches a row that has become
*redundant* because the pair is now a member. Discharging the temporary rows in `134-02`/`134-03` is
therefore a **manual discipline obligation with a grep-provable criterion**, not something the gate
enforces. Record both readings.

⚠ **`_DECLARED_REGISTRY_COUNT` does not exist.** The two real constants are `_POLICED_REGISTRY_COUNT`
(`:246`) and `_DECLARED_NON_REGISTRY_COUNT` (`:328`). Do not search for the CONTEXT.md name.

⚠ **Do not add op vocabulary to any declared non-registry.** All four new op strings are hyphenated and
therefore auto-join `_MULTIWORD_OP_VALUES` (`:148`), which `_measure_op_vocabulary` (`:613-617`) applies
as a plain substring test to `_ALWAYS_WRITES_NOTICE` and as an AST scan to `DiagnosticReport`,
`count_applicable` and `dedup_fingerprint`. Touch none of them in this plan.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_op_registration_parity.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OP_WRITE_BASELINE_B\|OP_WRITE_BASELINE_A\|OP_WRITE_INHIBITED\|OP_WRITE_RESTORED' firestarter/chip_test.py` returns at least 4.
    - `firestarter/chip_test.py` contains `def generate_inhibited_pattern(` and `_SDP_LEG_OPS = frozenset(`.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.chip_test import _SDP_LEG_OPS,_DESTRUCTIVE_OPS;assert len(_SDP_LEG_OPS)==4;assert _SDP_LEG_OPS<=_DESTRUCTIVE_OPS"` exits 0.
    - `python -c "import sys;sys.path.insert(0,'.');from firestarter.chip_test import generate_pattern as a,generate_inhibited_pattern as b;A=a(0,256);B=b(0,256);assert len(A)==len(B)==256;assert sum(1 for x,y in zip(A,B) if x!=y)==256"` exits 0.
    - `grep -n 'no lock was emitted' firestarter/chip_test.py` returns two hits (`_SDP_BASELINE_GATE_REASON`, `_SDP_UNLOCK_GATE_REASON`).
    - `pytest tests/test_op_registration_parity.py -o addopts="" -q` passes all 7 tests with zero collection errors.
    - `grep -c 'TEMPORARY — discharged by plan 134-0' tests/test_op_registration_parity.py` returns 8.
    - `pytest tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_dev_test_cmd.py -o addopts="" -q` passes — Phase 133's LEG-09/10/11 proofs and the shipped baselines are byte-identically green.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `git log -1 --name-only` for this task lists BOTH `firestarter/chip_test.py` and `tests/test_op_registration_parity.py` in one commit.
  </acceptance_criteria>
  <done>13 ops exist, the parity gate is green at 13, and no shipped behaviour changed.</done>
</task>

<task type="auto">
  <name>Task 3: LEG-03 proofs against the live generators + non-vacuity obligations 1 and 7</name>
  <files>firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` `:186-232` (`_REAL_DB`, `_OPERATOR_METHODS`, `_mock_operator`, `_plan_with_steps`, `_result`) and the module header stating it carries no syrupy capture and no firmware-sibling decorator.
    - `firestarter_app/firestarter/chip_test.py` `:98-130` (`_diff_offsets`, `_FF_RATIO_THRESHOLD`) and `:143-210` (`classify_fingerprint`'s locked arm order).
    - `firestarter_app/tests/test_op_registration_parity.py` `:753-778` (`test_altered_registry_copy_fails_parity_non_vacuous` — the house non-vacuity idiom: positive control → mutate an in-memory copy → `try/except AssertionError: pass / else: raise`).
    - `.planning/phases/134-…/134-VALIDATION.md` §"Non-Vacuity Obligations" rows 1 and 7.
  </read_first>
  <action>
Add a `class TestInhibitedPattern` block to `firestarter_app/tests/test_chip_test_sdp_leg.py` with
test names containing `pattern_b` so `pytest -k "pattern_b"` selects them (VALIDATION.md's command).

Every assertion is computed from the **live generators for the real region**, never from a byte
literal. Derive the region the same way production will: for a non-UV ALLOW chip the plan's write
region is `(0, 256)` — obtain it from `chip_test._DEFAULT_REGION` (`:1006`) rather than hard-coding it,
so a future region change fails the test loudly instead of silently passing.

Assertions required (D-19's five plus D-05's):
1. `len(B) == len(A)`.
2. `sum(1 for x, y in zip(A, B) if x != y) == len(A)` — differ at **every** byte. Phrase the failure
   message around a one-page lock leak needing to be detectable; a "differ somewhere" assertion is
   explicitly not acceptable.
3. `B != A` and `B != generate_pattern(*region)` — the direct anti-tautology assertion.
4. `A` is neither `bytes(len(A))` nor `b"\xFF" * len(A)`; same for `B`.
5. **D-05's non-laundering leg:** call the live `classify_fingerprint(A, B, addr_base=region[0])` and
   assert its classification is NOT `FP_BLANK_CONTACT`, and that the measured `ff_ratio` on the result
   is strictly below the live `_FF_RATIO_THRESHOLD` imported from the module — do not write `0.98` or
   `0.0039` as literals. Add a comment recording the measured values at this commit (B's ff_ratio is
   ~0.0039 against a 0.98 threshold) as context, not as the assertion.

**Non-vacuity obligation #1 (VALIDATION.md row 1) — must be OBSERVED, not asserted.** Temporarily edit
`generate_inhibited_pattern` in `firestarter_app/firestarter/chip_test.py` to `return
generate_pattern(start, length)`, run the new tests, and **capture the RED output verbatim into the
plan SUMMARY** — the every-byte assertion and the `B != generate_pattern(region)` assertion must both
fail. Then restore the function **byte-identically** (`git diff firestarter/chip_test.py` must be empty
before the task's commit) and re-run to green. A pre-authored gate proves nothing until it has been
seen to fail; this project has shipped unreachable-green gates twice (v1.23 P129/P130).

**Non-vacuity obligation #7 (VALIDATION.md row 7).** Add no new leg. Run
`pytest tests/test_op_registration_parity.py::test_altered_registry_copy_fails_parity_non_vacuous
-o addopts="" -q` and record in the SUMMARY that it still passes now that `_ALL_OPS` has grown to 13.

⚠ **Do NOT add a test asserting `classify_fingerprint` returns the `address-line` label for this
region.** Measured: that arm requires `cmp_len > 256` (`chip_test.py:199`) and the leg's region is
exactly 256, so such a test would be unreachable-green.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_chip_test_sdp_leg.py -k "pattern_b" -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_chip_test_sdp_leg.py -k "pattern_b" -o addopts="" -q` selects at least 5 tests and all pass.
    - `grep -c '0\.98' tests/test_chip_test_sdp_leg.py` returns 0 inside the new block — the threshold is imported, not literal (a comment quoting the measured value is permitted and is not an assertion).
    - `grep -q '_DEFAULT_REGION\|_WRITE_REGION_LENGTH' tests/test_chip_test_sdp_leg.py` — the region is derived from the module, not hard-coded in the assertions.
    - The plan SUMMARY contains the verbatim RED pytest output from obligation #1 naming at least two failing tests, plus the confirmation that `firestarter/chip_test.py` was restored byte-identically (`git diff --stat` empty for that file before commit).
    - The plan SUMMARY records `test_altered_registry_copy_fails_parity_non_vacuous` as still passing at 13 ops.
    - `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` passes with a count ≥ the pre-edit baseline recorded in Task 1, and coverage ≥ 70%.
    - `tools/ci_replica_venv.sh` exits 0 and its `mypy errors: N (watermark: 35)` line is recorded in the SUMMARY with N ≤ 35.
  </acceptance_criteria>
  <done>LEG-03 is proven against live generators and its gate has been seen RED once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB entry → engine | `chip_database.json` is generated; a malicious/misconfigured entry must not widen a write window or supply an op string |
| engine → operator → serial wire | `operation_flags` bits reach the firmware; only bits this module names may be set |
| engine → community report artifact | report text reaches strangers and is quoted back as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-01 | Tampering | `_SDP_LEG_OPS` sourced from a DB field | high | mitigate | Module constant only, never a DB field — the `_WRITE_REGION_LENGTH` precedent; asserted by the parity gate's registry membership |
| T-134-02 | Elevation of Privilege | `generate_inhibited_pattern` writing outside the derived region | medium | mitigate | Generator takes `(start, length)` from `_write_region_for`'s already-decided region; never re-derives UV-ness |
| T-134-03 | Repudiation | A vacuous oracle shipping a tautology that reads as correct | critical | mitigate | Non-vacuity obligation #1 observed RED and restored byte-identically; every-byte assertion against live generators |
| T-134-04 | Information Disclosure | New op strings leaking into a wire constant / firmware header | low | accept | Engine-local strings only; `constants.py` and `firestarter.h` untouched — asserted by the absence of any `firestarter/` (firmware) diff |
| T-134-SC | Tampering | package-manager installs | n/a | accept | This phase installs **no** new npm/pip/cargo package (RESEARCH §9: no framework install needed), so no legitimacy audit or blocking checkpoint applies |
</threat_model>

<verification>
- `pytest tests/test_op_registration_parity.py -o addopts="" -q` — 7 passed, zero collection errors.
- `pytest tests/test_chip_test_sdp_leg.py tests/test_chip_test.py tests/test_dev_test_cmd.py -o addopts="" -q` — green; Phase 133's LEG-09/10/11 named proofs unchanged.
- `tools/ci_replica_venv.sh` — 5/5 legs, mypy ≤ 35, `checked` ≥ 120.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0.
</verification>

<success_criteria>
- Four op constants, `_SDP_LEG_OPS`, `generate_inhibited_pattern`, and the two gate-reason constants exist.
- The parity gate is green at `len(_ALL_OPS) == 13`, in the same commit as the constants.
- LEG-03's five assertions pass against live generators and have been observed RED once.
- `134-CI-PARITY.md` records the pre-edit budget with a real `ci-replica` mypy count.
- No firmware (`/workspaces/firestarter/`) file is touched.
</success_criteria>

<output>
Create `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-01-SUMMARY.md` when done.
</output>
