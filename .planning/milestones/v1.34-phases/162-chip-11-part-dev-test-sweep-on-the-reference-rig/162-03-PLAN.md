---
phase: 162-chip-11-part-dev-test-sweep-on-the-reference-rig
plan: 03
type: execute
wave: 3
depends_on: ["162-02"]
files_modified:
  - .planning/v1.34/tools/render_chip_evidence.py
  - .planning/v1.34/bench/CHIP-EVIDENCE.md
  - .planning/v1.34/tools/run_gates.sh
autonomous: true
requirements: [CHIP-01, CHIP-03, CHIP-04]
tags: [bench, rig-tooling, host-only, wave-0, renderer, gate-suite, no-hardware]

must_haves:
  truths:
    - "`CHIP-EVIDENCE.md` is regenerated from `CHIP-EVIDENCE.jsonl` and byte-compared by `--check`, so a hand-edit to the divergence table is structurally visible instead of being trusted (D-08's rejected markdown-only alternative closed)."
    - "Two consecutive renders of the same JSONL produce byte-identical markdown — no timestamp, no hostname, no value not derived from the file's own schema and rows."
    - "The sibling file is gated by the suite: `run_gates.sh` gains a `render_chip_evidence.py --check` gate and a `gate_record.py --jsonl CHIP-EVIDENCE.jsonl` gate, so `CHIP-EVIDENCE.jsonl` is never the one record in the milestone with no byte-identical-regeneration proof."
    - "The suite is measured at **14/14 tool selftests and 7/7 live gates, exit 0 read directly** — the target this phase is graded against."
    - "The rendered document states SC#4's reconciliation as `10 + N` arithmetic with the deviation from the roadmap's `11 + N` named on the same line, computed over rows rather than written by a reader (D-06)."
    - "The exclusion mechanism is driven entirely from the chip schema, with no literal fallback that could silently mis-count a control re-run or the named absence."
  prohibitions:
    - "No product code. Both sub-repo porcelains are empty at the end of this plan. A task needing a source edit in either sub-repo must STOP and report."
    - "No hardware command. This plan touches no device, no serial port, no avrdude, no `pio`, and no arm binary."
    - "No timestamp, hostname, locale-dependent formatting or clock-derived value in the renderer's output — the prior milestone's generated-on line is the specific thing not to copy, because it would make `--check` permanently red."
    - "No hardcoded bring-up-prefix default in the chip renderer. The chip file's exclusion is a different mechanism and must be driven from the chip schema with **no** literal fallback."
    - "No new eighth live gate for the procedure's second step-list section — that is plan 162-04's work and it extends the existing `render_steps.py` gate block, keeping the count at 7 (PD-2)."
    - "No append to `bench/EVIDENCE.jsonl`, and no change to its `_schema` or its `position_count_expected: 20`."
    - "`run_gates.sh`'s exit code is never read through a pipe."
    - "No third-party import — `run_gates.sh` runs each selftest with the system `python3`."
    - "This plan must not run under `--auto` / `--chain` / any auto-advance mode."
  artifacts:
    - .planning/v1.34/tools/render_chip_evidence.py
    - .planning/v1.34/bench/CHIP-EVIDENCE.md
    - .planning/v1.34/tools/run_gates.sh
  key_links:
    - "`render_chip_evidence.py --check` -> `run_gates.sh` live gate 6: the append-then-re-render pair the bench plans run per position becomes a suite-enforced invariant, not a habit."
    - "`gate_record.py --jsonl CHIP-EVIDENCE.jsonl` -> `run_gates.sh` live gate 7: field presence, the not-measured shape, the outcome domain, the forbidden-flag argv scan and the live config-dir SHA recomputation, all applied to every chip row on every wave."
    - "`_schema.chip_sc04_rule` -> the rendered Reconciliation section: SC#4 evaluated as an equation over rows, which is exactly what D-08's rejected markdown-only alternative could not provide."
    - "Explicit LF line endings in the atomic write -> the `--check` byte comparison: half of the determinism contract; the absence of any timestamp is the other half."
---

<objective>
Build the chip sweep's renderer and put both new gates into the suite, then measure the suite at
its phase target.

Purpose: D-08 rejected a markdown-table-only record precisely because Phase 166's CLOSE-01 wants
the chip sweep reconciled as **arithmetic over rows**, and a hand-maintained table is what
`render_evidence.py` exists to prevent. But RESEARCH found that `run_gates.sh`'s two record gates
name the WRV file **literally**, so without an edit `CHIP-EVIDENCE.jsonl` would never be gated and
`CHIP-EVIDENCE.md`'s `--check` would never run — the sibling would be the one record in the
milestone with no byte-identical-regeneration proof, which is the exact defect the rejected
alternative was rejected for.

Output: `tools/render_chip_evidence.py` with a `--check` mode and a deterministic render;
`bench/CHIP-EVIDENCE.md` rendered from the schema-only JSONL; `run_gates.sh` carrying two new live
gates; and a measured **14/14 selftests, 7/7 live gates, exit 0**.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-01-PLAN.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-02-PLAN.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-PATTERNS.md
</context>

## Standing bench rule 7 — read before executing

This phase must **not** run under `--auto`, `--chain`, or any auto-advance mode. This plan is
host-only and carries no checkpoint, but the mode is set for the whole phase run, not per plan.

## Planner decisions

`162-01-PLAN.md`'s "Planner decisions — canonical for the whole phase" applies unchanged. The ones
this plan implements are **PD-2** (the live-gate count stays at 7; the procedure's second section
extends the existing `render_steps.py` gate rather than adding an eighth), **PD-10** (the named
absence is a counted position) and **PD-12** (flat columns; the count is asserted from the schema,
never pinned to a magic number).

## Requirement completion

This plan builds the checking mechanism. It closes **none** of CHIP-01…CHIP-05. Do not mark any
CHIP requirement complete here.

## Artifacts this phase produces

Created by **this plan**:

- **New file:** `.planning/v1.34/tools/render_chip_evidence.py`.
- **New functions:** `load_schema_and_rows()`, `render_markdown()`, `_format_cell()`,
  `_reconciliation_text()`, `_sc04_text()`, `atomic_write()`, `build_argparser()`, `main()`,
  `_run_selftest()`.
- **New CLI flags (all on `render_chip_evidence.py`):** `--jsonl`, `--target`, `--check`,
  `--selftest`.
- **New generated document:** `.planning/v1.34/bench/CHIP-EVIDENCE.md`.
- **New `run_gates.sh` live-gate names:** `render_chip_evidence.py --check` and
  `gate_record.py (CHIP-EVIDENCE.jsonl)`.

Symbols created by **other plans in this phase** and referenced here:
`append_chip_evidence.py` and `bench/CHIP-EVIDENCE.jsonl` (162-02); `render_steps.py --section`
and `PROCEDURE.md`'s `## Chip-sweep step list` with ids `C-01`…`C-09` (162-04).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build render_chip_evidence.py — deterministic render plus byte-compare</name>
  <files>.planning/v1.34/tools/render_chip_evidence.py, .planning/v1.34/bench/CHIP-EVIDENCE.md</files>
  <read_first>
    - `.planning/v1.34/tools/render_evidence.py` — **read in full**. Specifically: the determinism-contract docstring and its four neutralised non-determinism sources; `load_schema_and_rows`; `render_markdown`'s document shape (H1, the generated-by blockquote naming the exact `--check` command, the provenance paragraph, the counting-rule section printing the schema string verbatim, the main positions table, the excluded-rows table, the reconciliation section); `_format_cell`'s fixed-separator sorted-key JSON encoding for nested values; `_reconciliation_text`; `atomic_write`'s explicit LF newline argument; the `--check` branch's three exit shapes; and `append_row_to_file`, which this renderer owns for the appender to import.
    - `.planning/v1.34/bench/CHIP-EVIDENCE.jsonl` line 1 — the schema this renderer drives from, including `close01_counting_rule`, `chip_sc04_rule`, `control_rerun_exclusion`, `named_absence_convention`, `primary_arm` and `position_count_expected`.
    - `.planning/v1.34/bench/EVIDENCE.md` — the rendered WRV document, as the target shape.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-PATTERNS.md` — the `render_chip_evidence.py` section, including the "do not copy" note about the hardcoded bring-up prefix fallback.
    - `.planning/v1.34/tools/run_gates.sh` — the discovery loop and the literal double-quoted `--selftest` grep.
  </read_first>
  <behavior>
    The `--selftest` must carry these legs, in the analog's accumulate-then-report style, each
    driving the real render and asserting on a named reason where it is negative:

    - Positive 1: a fixture JSONL with a schema and three rows — one `v133` `same`, one `v133`
      `diverges`, one `control` arbitrating the second — renders to markdown, and a second render
      of the same input is **byte-identical** to the first.
    - Positive 2: `--check` is **green** against the freshly rendered target.
    - Positive 3: `--check` is **red** against a byte-mutated target, prints a unified diff to
      stderr, exits 1, and writes nothing. Both directions of the comparison are therefore observed.
    - Positive 4: the reconciliation text over the fixture states the total-runs arithmetic with N
      equal to the fixture's control-row count, and names the roadmap's reading on the same line.
    - Positive 5: a fixture including a named-absence row counts that row in the close-out
      arithmetic and excludes it from the SC#4 arithmetic.
    - Negative 1: a JSONL whose row carries a key outside `record_keys` is refused by name.
    - Negative 2: a JSONL whose line 1 lacks a schema record, or whose schema lacks `record_keys`,
      is refused by name.
    - Negative 3: `--check` against a target path that does not exist is refused by name and
      exits 1.
    - Negative 4: a schema with no control-re-run discriminator and no named-absence column is
      refused rather than silently falling back to a literal — the leg that proves there is no
      hidden default.
  </behavior>
  <action>
Create `.planning/v1.34/tools/render_chip_evidence.py` by copying `render_evidence.py`'s skeleton
and replacing the document body. **Stdlib only.** Open the module docstring with the same
determinism contract, stating the four neutralised non-determinism sources and naming explicitly
the one thing not to copy: the prior milestone's generated-on ISO-8601 line, which would make
`--check` permanently red.

Four arguments: `--jsonl` defaulting to `bench/CHIP-EVIDENCE.jsonl`, `--target` defaulting to
`bench/CHIP-EVIDENCE.md`, `--check`, and `--selftest` — spelled with **double quotes** in the
source for the discovery grep.

Determinism, by the same four mechanisms as the analog: emit no timestamp, hostname or
clock-derived value anywhere; order rows by `position_id`; emit cells in `record_keys` order;
format every nested value with a fixed-separator, sorted-key JSON encoding; and write through an
atomic temp-then-replace that opens the file with an explicit LF newline argument.

Document body, mirroring the analog's sections but with this file's own arithmetic:

- an H1 naming the milestone and the pinned phase, both read from the schema;
- a generated-by blockquote naming the **exact** `--check` command a reader can run;
- a provenance paragraph;
- a counting-rule section printing `close01_counting_rule` **verbatim** from the schema;
- the main positions table over rows whose `arm` equals the schema's `primary_arm` — the sweep
  positions, including the named absence;
- an excluded-rows table over the control rows — the divergence-arbitration re-runs — with
  `control_rerun_exclusion` printed verbatim above it;
- a **divergence table** section: one row per primary position with `chip`,
  `prior_disposition_source`, `prior_disposition`, `step_verdicts`, `divergence_verdict` and
  `known_carried`. This is the table SC#3 is graded against, so every cell must be present with no
  blank; a not-measured-with-reason string is a value, a blank is not;
- a Reconciliation section carrying **two** computed statements. The close-out statement follows
  the analog's shape: validated plus skipped-with-reason equals the accounted count against
  `position_count_expected`, computed over primary-arm rows only. The SC#4 statement evaluates
  `chip_sc04_rule` as an equation — the control-row count against the count of primary rows whose
  `divergence_verdict` begins with the diverging prefix, whether every control row's
  `control_rerun_for` names an existing diverging primary row, and whether any two control rows
  name the same one — and states total runs as ten plus N with the arithmetic shown and the
  deviation from the roadmap's eleven-plus-N reading named **on the same line**.

Drive the row partition **entirely from the schema**: which rows are sweep positions and which are
arbitration re-runs comes from the schema's `primary_arm` and its `control_rerun_exclusion`
semantics, and the named absence is identified by its `named_absence` column being set. **Do not**
copy the analog's schema-lookup-with-literal-fallback idiom for the exclusion prefix — a literal
fallback in a file whose exclusion mechanism is different could silently mis-count a control re-run
or the named absence. A schema missing the discriminator is a named refusal.

Three exit shapes, as in the analog: 0 when a plain render succeeds or `--check` is green; 1 on
drift **with the unified diff printed to stderr**; 1 on a missing `--check` target. Nothing is
written under `--check`.

Finally, render the real file once, with `--jsonl` pointing at `bench/CHIP-EVIDENCE.jsonl` and
`--target` at `bench/CHIP-EVIDENCE.md`, producing the zero-row document. It must render cleanly with
no rows — the close-out reconciliation reads zero validated plus zero skipped of eleven positions
accounted for, with eleven not yet recorded, and the SC#4 identity holds vacuously at N equal to
zero. Then run `--check` against it and confirm green.
  </action>
  <acceptance_criteria>
    - `python3 .planning/v1.34/tools/render_chip_evidence.py --selftest` exits 0 with a `PASS:` line for every leg named in `<behavior>` and no `FAIL:` line.
    - The file contains the token for the selftest flag spelled with double quotes; the source parses with `ast.parse` and imports only standard-library modules.
    - `bench/CHIP-EVIDENCE.md` exists, is non-empty, and contains no ISO-8601 timestamp.
    - Rendering twice in a row produces byte-identical output: two renders to different temp paths compare equal.
    - Running the renderer against the real JSONL and the real target with `--check` exits 0.
    - The same command against a target with one byte changed exits 1 and prints a unified diff on stderr, and the target file is left unmodified by the `--check` run.
    - `CHIP-EVIDENCE.md` contains the literal `10 + N` and names `11 + N` on the same line, and contains a section printing the schema's `close01_counting_rule` verbatim.
    - `CHIP-EVIDENCE.md` contains a divergence-table section heading, even with zero rows.
    - The renderer source carries no bring-up prefix string literal and no lookup supplying a string default for the row-partition discriminator.
    - `ls .planning/v1.34/tools/*.py | wc -l` is `14`.
    - Both sub-repo porcelains are empty.
  </acceptance_criteria>
  <verify>
    <automated>python3 /workspaces/.planning/v1.34/tools/render_chip_evidence.py --selftest
echo "selftest_rc=$?"</automated>
    <automated>python3 -c "
import ast,sys
p='/workspaces/.planning/v1.34/tools/render_chip_evidence.py'
src=open(p).read(); tree=ast.parse(src)
std=set(sys.stdlib_module_names); mods=set()
for n in ast.walk(tree):
    if isinstance(n,ast.Import):
        for a in n.names: mods.add(a.name.split('.')[0])
    elif isinstance(n,ast.ImportFrom) and n.level==0 and n.module:
        mods.add(n.module.split('.')[0])
extra=sorted(m for m in mods if m not in std)
assert not extra, extra
assert src.count(chr(34)+'--selftest'+chr(34))>=1, 'selftest flag not advertised in double quotes'
body='\n'.join(l for l in src.splitlines() if not l.lstrip().startswith('#'))
assert 'BRINGUP' not in body.upper(), 'a bring-up prefix literal leaked into the chip renderer'
print('PASS: stdlib-only, advertises the selftest flag, no bring-up literal')
"</automated>
    <automated>T=/workspaces/.planning/v1.34/tools/render_chip_evidence.py
J=/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl
python3 "$T" --jsonl "$J" --target /tmp/162-03-r1.md
python3 "$T" --jsonl "$J" --target /tmp/162-03-r2.md
cmp /tmp/162-03-r1.md /tmp/162-03-r2.md
echo "PASS: two renders byte-identical"</automated>
    <automated>T=/workspaces/.planning/v1.34/tools/render_chip_evidence.py
J=/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl
M=/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.md
python3 "$T" --jsonl "$J" --target "$M" --check
echo "check_green_rc=$?"
cp "$M" /tmp/162-03-mut.md
printf 'x\n' >> /tmp/162-03-mut.md
python3 "$T" --jsonl "$J" --target /tmp/162-03-mut.md --check > /tmp/162-03-mut.out 2> /tmp/162-03-mut.err
RC=$?
echo "check_red_rc=$RC"
test "$RC" -eq 1
grep -q '^---' /tmp/162-03-mut.err || grep -q '+++' /tmp/162-03-mut.err
echo "PASS: --check red on drift with a unified diff on stderr"</automated>
    <automated>python3 -c "
import re
M='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.md'
t=open(M).read()
assert t.strip(), 'empty render'
assert not re.search(r'\d{4}-\d\d-\d\dT\d\d:\d\d', t), 'timestamp in the rendered document'
lines=[l for l in t.splitlines() if '10 + N' in l]
assert lines, 'the 10 + N arithmetic line is missing'
assert any('11 + N' in l for l in lines), 'the roadmap deviation is not named on the same line'
assert 'iverg' in t, 'no divergence table section'
print('PASS: rendered document carries the arithmetic, the deviation and the divergence section')
"</automated>
    <automated>ls /workspaces/.planning/v1.34/tools/*.py | wc -l
git -C /workspaces/firestarter status --porcelain
git -C /workspaces/firestarter_app status --porcelain
echo "submodule_porcelain_checked"</automated>
  </verify>
  <done>`render_chip_evidence.py` renders `CHIP-EVIDENCE.md` deterministically, is red on a one-byte drift with a unified diff, states both counting rules as computed arithmetic, and drives its row partition from the schema with no hidden literal.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two chip live gates to run_gates.sh and measure 14/14 with 7 live gates</name>
  <files>.planning/v1.34/tools/run_gates.sh</files>
  <read_first>
    - `.planning/v1.34/tools/run_gates.sh` — the whole file: the header comment's `WHAT THIS RUNS` live-gate list, the `set -euo pipefail` line, the discovery loop with its exit-2-on-zero-tools branch, all five existing live-gate blocks in order, the `--quick` skip list, and the summary block's exit codes (0 all passed, 1 one or more failed, 2 bad usage or zero tools).
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-PATTERNS.md` — the `run_gates.sh` section, which quotes the four-part live-gate block shape and states the placement rule.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md` R3 §"Second finding: `run_gates.sh`'s live gates are hardcoded to `EVIDENCE.jsonl`".
  </read_first>
  <action>
Add exactly **two** new live-gate blocks, copying the existing four-part shape verbatim: a banner
line naming the gate, an `if python3 …; then` form (never a chained-and, never a pipe), a
`FAILURES+=("<tool>: <named reason>")` on the else branch, and a FAIL echo redirected to stderr.

Gate 6 runs `render_chip_evidence.py` with `--jsonl` at `$BENCH_DIR/CHIP-EVIDENCE.jsonl`, `--target`
at `$BENCH_DIR/CHIP-EVIDENCE.md` and `--check`. Its named failure reason states that
`bench/CHIP-EVIDENCE.md` diverges from a fresh render and that a hand-edit is suspected.

Gate 7 runs `gate_record.py` with `--jsonl` at `$BENCH_DIR/CHIP-EVIDENCE.jsonl` and `--pins` at
`$PINS_FILE`. `validate_jsonl_file` is fully schema-driven — it reads `record_keys` and
`outcome_values` from the file's own line-1 schema — so it gates the sibling with **no new
argument**. Its named failure reason states that `bench/CHIP-EVIDENCE.jsonl` failed its own
record-shape gate.

Place both **outside** the `--quick` skip block, exactly as their two WRV analogs are placed:
neither touches an arm binary, a device or an image file, so `--quick` must still run them.

Update the header comment's `WHAT THIS RUNS` live-gate list to enumerate all seven — the amendment
discipline applies to this file's own docstring, and a header that still claims five while the
script runs seven is the drift this project keeps naming.

Then run the **full** suite and capture `$?` on the very next line. Never pipe it: `set -euo
pipefail` is on, and a pipe hands you the last stage's status instead of the gate's. The expected
result is **14 tool selftests and 7 live gates, exit 0** — 14 because `append_chip_evidence.py` and
`render_chip_evidence.py` are both discovered by `find -maxdepth 1 -name '*.py'`, and 7 because of
the two blocks added here. A selftest count of 13 means one of the two new tools was not
discovered; 15 means a scratch `.py` was left under `tools/` and must be removed before anything
else proceeds.

Commit `render_chip_evidence.py`, `CHIP-EVIDENCE.md` and `run_gates.sh` as one change.
  </action>
  <acceptance_criteria>
    - `run_gates.sh` contains two new live-gate banner lines naming `render_chip_evidence.py --check` and the `gate_record.py` run over `CHIP-EVIDENCE.jsonl`, each followed by an `if python3 …; then` form with a `FAILURES+=` else branch carrying a named reason.
    - Neither new gate is inside the `--quick` skip block: `bash run_gates.sh --quick` runs both and reports 7 live gates.
    - The header comment's live-gate list enumerates seven gates.
    - `bash .planning/v1.34/tools/run_gates.sh` exits **0** with its code captured directly from the next line, reporting **14** tool selftests and **7** live gates.
    - Both `append_chip_evidence.py` and `render_chip_evidence.py` appear by name among the reported selftests.
    - `ls .planning/v1.34/tools/*.py | wc -l` is `14`.
    - Deliberately mutating `CHIP-EVIDENCE.md` by one byte makes the suite exit **1** and name `render_chip_evidence.py --check` in the failure list; the file is restored afterwards and the suite is green again. The negative control is recorded as having **fired**, not as having been configured.
    - `git diff --quiet .planning/v1.34/bench/EVIDENCE.jsonl` succeeds and `bench/EVIDENCE.md` is byte-unchanged.
    - Both sub-repo porcelains are empty.
  </acceptance_criteria>
  <verify>
    <automated>bash /workspaces/.planning/v1.34/tools/run_gates.sh > /tmp/162-03-gates.out 2> /tmp/162-03-gates.err
RC=$?
echo "run_gates_rc=$RC"
python3 -c "
t=open('/tmp/162-03-gates.out').read()
n=t.count('selftest PASS')
g=t.count('live gate PASS')
print('selftest_pass_count', n)
print('live_gate_pass_count', g)
assert n==14, n
assert g==7, g
assert 'append_chip_evidence.py' in t
assert 'render_chip_evidence.py' in t
print('PASS: 14/14 selftests and 7/7 live gates')
"
test "$RC" -eq 0</automated>
    <automated>bash /workspaces/.planning/v1.34/tools/run_gates.sh --quick > /tmp/162-03-quick.out 2> /tmp/162-03-quick.err
RC=$?
echo "run_gates_quick_rc=$RC"
python3 -c "
t=open('/tmp/162-03-quick.out').read()
assert 'render_chip_evidence.py' in t, 'chip render gate skipped under --quick'
assert 'CHIP-EVIDENCE.jsonl' in t, 'chip record gate skipped under --quick'
print('PASS: both new gates run under --quick')
"
test "$RC" -eq 0</automated>
    <automated>M=/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.md
cp "$M" /tmp/162-03-chipmd.bak
printf 'drift\n' >> "$M"
bash /workspaces/.planning/v1.34/tools/run_gates.sh --quick > /tmp/162-03-neg.out 2> /tmp/162-03-neg.err
RC=$?
cp /tmp/162-03-chipmd.bak "$M"
echo "negative_control_rc=$RC"
test "$RC" -eq 1
grep -q 'render_chip_evidence.py' /tmp/162-03-neg.err
echo "NEGATIVE CONTROL FIRED: the suite went red on a one-byte CHIP-EVIDENCE.md drift and named the gate"
bash /workspaces/.planning/v1.34/tools/run_gates.sh --quick > /tmp/162-03-restored.out 2> /tmp/162-03-restored.err
RC2=$?
echo "restored_rc=$RC2"
test "$RC2" -eq 0</automated>
    <automated>python3 -c "
src=open('/workspaces/.planning/v1.34/tools/run_gates.sh').read()
assert 'CHIP-EVIDENCE.jsonl' in src and 'CHIP-EVIDENCE.md' in src
assert 'render_chip_evidence.py' in src
head=src.split('PY_TOOLS=')[0]
assert head.count('live gate') >= 1 or 'WHAT THIS RUNS' in head
print('PASS: both gates wired and the header mentions the gate list')
"
ls /workspaces/.planning/v1.34/tools/*.py | wc -l</automated>
    <automated>git -C /workspaces diff --quiet -- .planning/v1.34/bench/EVIDENCE.jsonl .planning/v1.34/bench/EVIDENCE.md
echo "WRV record byte-unchanged rc=$?"
git -C /workspaces/firestarter status --porcelain
git -C /workspaces/firestarter_app status --porcelain
echo "submodule_porcelain_checked"</automated>
  </verify>
  <done>The suite gates the sibling record on every wave, the negative control has been **observed to fire** rather than merely configured, and the phase's tooling target is measured at 14/14 selftests and 7/7 live gates with exit 0 read directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHIP-EVIDENCE.jsonl` rows -> the rendered document | the markdown is what a human reads; a render that silently drops or mis-partitions a row is an undetected evidence gap |
| a hand edit to `CHIP-EVIDENCE.md` -> the milestone record | the only thing standing between a retroactively "corrected" table and the record is the byte comparison |
| the gate suite -> the phase's own claim of correctness | a suite that discovers nothing, or that runs a gate the header does not list, passes vacuously |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-13 | Repudiation | a hand-edited `CHIP-EVIDENCE.md` | high | mitigate | `--check` byte-compares against a fresh render on every wave, and the negative control is observed firing on a one-byte drift before the sweep begins |
| T-162-14 | Repudiation | an ungated sibling record | high | mitigate | two new live gates wired outside the `--quick` block, with the header's gate list updated so the script and its own documentation cannot drift |
| T-162-15 | Tampering | non-deterministic render making `--check` meaningless | high | mitigate | no timestamp or hostname emitted; deterministic row order; fixed nested-value encoding; explicit LF endings; a selftest leg asserting two renders are byte-identical |
| T-162-16 | Repudiation | a row-partition literal that silently mis-counts | medium | mitigate | the partition is schema-driven with **no** literal fallback, and a schema missing the discriminator is a named refusal with its own selftest leg |
| T-162-17 | Tampering | the WRV record disturbed by a sibling edit | high | mitigate | `EVIDENCE.jsonl` and `EVIDENCE.md` asserted byte-unchanged at the end of the plan |
| T-162-SC | Tampering | package installs | low | accept | stdlib-only by requirement; no `pip`/`npm` step exists anywhere in the phase; no slopsquatting surface exists |
</threat_model>

<verification>
- `bash .planning/v1.34/tools/run_gates.sh` exits 0, read directly, at 14/14 selftests and 7/7 live gates.
- `bash .planning/v1.34/tools/run_gates.sh --quick` also runs both new gates and exits 0.
- A one-byte drift in `CHIP-EVIDENCE.md` makes the suite exit 1 and name the render gate; restoring the file makes it green again.
- Two consecutive renders of `CHIP-EVIDENCE.jsonl` are byte-identical, and the document carries no timestamp.
- `CHIP-EVIDENCE.md` carries the `10 + N` arithmetic with the `11 + N` deviation on the same line, the verbatim close-out counting rule, and a divergence-table section.
- `.planning/v1.34/tools/` holds exactly 14 `.py` files.
- `EVIDENCE.jsonl` and `EVIDENCE.md` are byte-unchanged; both sub-repo porcelains are empty.
</verification>

<success_criteria>
The chip sweep's record is now regenerable and gated to the same standard as the WRV record, the
suite is at its phase target of 14/14 selftests and 7/7 live gates with the exit code read
directly, and the gate that protects the divergence table has been seen to go red before a single
part is seated. No part has been seated and no row exists.
</success_criteria>

<output>
Create `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-03-SUMMARY.md`
when done. Record: the renderer's document section list as built and any divergence from this plan;
the nine selftest leg names with pass/fail; the two new live-gate names exactly as they appear in
`run_gates.sh`'s output; the measured selftest and live-gate counts with the exit code; the
negative-control transcript showing the suite going red on the one-byte drift and green again after
restore; and the zero-row reconciliation text as first rendered.
</output>
