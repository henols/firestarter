---
phase: 162-chip-11-part-dev-test-sweep-on-the-reference-rig
plan: 04
type: execute
wave: 4
depends_on: ["162-03"]
files_modified:
  - .planning/v1.34/tools/render_steps.py
  - .planning/v1.34/PROCEDURE.md
  - .planning/v1.34/tools/run_gates.sh
autonomous: true
requirements: [CHIP-01, CHIP-02]
tags: [bench, rig-tooling, host-only, wave-0, procedure-amendment, render-steps, no-hardware]

must_haves:
  truths:
    - "`PROCEDURE.md` stops claiming to be executed unchanged by Phase 162: §Scope and the header both name **two** cell shapes, and the chip-sweep shape has its own prescriptive step list."
    - "The chip-sweep step list lives under its own H2 and its `C-NN` ids are **gated** — `render_steps.py` renders it per arm and the suite diffs the two, so D-07's 're-confirm the gate' is not a vacuous pass over text the gate cannot see."
    - "`## Step list`'s eleven `P-NN` steps still render arm-identically at 11 lines per arm, and their headings are byte-unchanged from `HEAD` — the text `A1`, `A2` and `A3/B2` cited is intact."
    - "The `~/.firestarter/config.json` mtime baseline is re-pinned to a value measured at the moment the amendment is written, and the transition is recorded as the fourth recurrence — without this, P-11 assertion (1) is unconditionally red before the phase starts and would book a false `P-H1` at every position."
    - "The config-dir copy-out invariant is a prescribed step obligation, asserted before **and** after every `dev test`, not an implementation detail of one tool."
    - "The amendment names the `[arm: …]` **marker** as the mechanism that makes the render diff non-empty, correcting the two prior amendments that put `$ARM_BIN` first — the token is emitted literally and is diff-neutral."
    - "The suite is still 14/14 selftests and 7/7 live gates: the C-section diff extends the existing `render_steps.py` gate rather than adding an eighth (PD-2)."
  prohibitions:
    - "No product code. Both sub-repo porcelains are empty at the end of this plan. A task needing a source edit in either sub-repo must STOP and report."
    - "No hardware command. This plan touches no device, no serial port, no avrdude, no `pio`, and no arm binary. The `~/.firestarter` measurement is a stat, not a device operation."
    - "**No `C-NN` heading inside `## Step list`.** `_STEP_ID_RE` is anchored to the `P-NN` shape and `validate_steps()` raises on anything else, which would red the existing live gate for the rest of the milestone."
    - "**No renumbering of the P steps.** Continuing the `P-NN` sequence into the chip list is RESEARCH R6 option (C), rejected: it puts steps that do not apply to a WRV cell inside the WRV step list with no marker distinguishing them, reintroducing the exact defect Amendment 4 exists to fix."
    - "No `[arm: control]` or `[arm: v133]` annotation marker in any new text, in either section. That marker is the only thing that makes the render diff non-empty."
    - "**Do not drop the mtime from P-11's four-value assertion.** It is the only one of the four values that has ever changed; dropping it makes the assertion vacuous, which is the shape the two-assertion design exists to close."
    - "**Do not attempt to remove or repair `~/.firestarter`.** The sandbox denies it, and Phase 160's one unlogged kill during a removal attempt is what contaminated the directory."
    - "No eighth live gate. `run_gates.sh` stays at seven."
    - "`run_gates.sh`'s exit code is never read through a pipe."
    - "This plan must not run under `--auto` / `--chain` / any auto-advance mode."
  artifacts:
    - .planning/v1.34/tools/render_steps.py
    - .planning/v1.34/PROCEDURE.md
    - .planning/v1.34/tools/run_gates.sh
  key_links:
    - "`render_steps.py --section C` -> the extended live-gate block -> Amendment 4's closing re-confirmation sentence: the gate now covers the text the amendment adds, which is what makes the re-confirmation non-vacuous."
    - "Amendment 4 clause on the config-dir copy-out -> `C-01` and `C-07` -> `append_chip_evidence.py`'s two pristine assertions -> `check_arms.py --expect-config-sha` inside the per-wave gate: one invariant, prescribed in the procedure and enforced in three places."
    - "The re-pinned `~/.firestarter` mtime -> every position's `P-11`-by-reference teardown: a further change **during** the sweep becomes this phase's own `P-H1` finding instead of a false one inherited from a stale pin."
    - "`$CHIP_TOKEN` in `## Arm substitution` -> the `dev test` report filename -> `append_chip_evidence.py`'s copy-out path: the case-sensitive token is declared once, in the document both the operator and the tool read."
---

<objective>
Teach `render_steps.py` a second gated section, then write `PROCEDURE.md` Amendment 4 — the
chip-sweep step list `C-01`…`C-09`, the §Scope correction, the widened token table, the two
supersessions, the re-pinned `~/.firestarter` baseline, and the copy-out obligation — and extend
the arm-agnosticism gate to cover the new text.

Purpose: `PROCEDURE.md` currently says, in two places, that it is executed **unchanged** by Phase
162, while describing only the write-read-verify cell shape. Roughly half its step list applies
verbatim to a chip position and the other half does not apply at all — `P-07` and `P-09` write a
pre-computed image and judge a full-device SHA, and `dev test` does neither. D-07's answer is one
document with two shapes, never a second document, because a second document is a second place
every standing-rule change has to be applied to.

Two findings make this more than prose. First, a `C-NN` heading placed inside `## Step list` does
not merely fail to render — it raises and **reds the live gate for the rest of the milestone**, so
the new list needs its own H2 and the renderer needs to learn about it. Second, the
`~/.firestarter/config.json` mtime has **already** drifted from the Amendment-3 pin, so P-11's
first teardown assertion is unconditionally red before this phase starts and would book a false
`P-H1` at every one of the ten positions.

Output: `render_steps.py` with a `--section` argument and two new selftest legs; `PROCEDURE.md`
with Amendment 4 and a `## Chip-sweep step list`; `run_gates.sh` diffing both sections inside one
gate; the suite still at 14/14 and 7/7.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/v1.34/PROCEDURE.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-01-PLAN.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-02-PLAN.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-PATTERNS.md
</context>

## Standing bench rule 7 — read before executing

This phase must **not** run under `--auto`, `--chain`, or any auto-advance mode. This plan is
host-only and carries no checkpoint, but the mode is set for the whole phase run, not per plan.

## Planner decisions

`162-01-PLAN.md`'s "Planner decisions — canonical for the whole phase" applies unchanged. This plan
implements **PD-2** (option A, one gate covering two sections), **PD-3** (the copy-out invariant as
a prescribed step obligation), **PD-9** (the `--no-image-plan` deviation recorded in the amendment),
**PD-14** (the ceilings the `C-05` step body cites) and **PD-17** (the per-part pot supersession).
One decision is new here:

**PD-19 — `## Step list` is *not* claimed byte-unchanged; the claim is scoped to what the three
already-run cells actually cited.** Amendment 3 pinned the `~/.firestarter` baseline **inside**
`P-11`'s body, so re-pinning the mtime necessarily edits that section. A byte-identity criterion
would break the moment that edit lands, which is exactly the brittle-criterion shape this project
has recorded before. The claim Amendment 4 makes instead is checkable and honest: the section still
parses to **exactly eleven** steps; every `### P-NN — <title>` heading line is **byte-equal** to
`HEAD`'s; the only changed lines inside the section are the pinned-mtime lines in `P-11`'s baseline
block; and both arms still render section P identically at 11 lines. No prescription any of `A1`,
`A2` or `A3/B2` executed has changed — only a baseline value that had already been overtaken by
reality.

## Requirement completion

This plan writes the procedure the bench plans execute. It closes **none** of CHIP-01…CHIP-05.
Do not mark any CHIP requirement complete here.

## Artifacts this phase produces

Created or extended by **this plan**:

- **New `render_steps.py` CLI flag:** `--section`, taking `P` or `C`, defaulting to `P` so every
  existing invocation is unchanged. Two new selftest legs.
- **New `PROCEDURE.md` H2:** `## Chip-sweep step list`.
- **New `PROCEDURE.md` step ids:** `C-01`, `C-02`, `C-03`, `C-04`, `C-05`, `C-06`, `C-07`, `C-08`,
  `C-09`.
- **New `PROCEDURE.md` substitution token:** `$CHIP_TOKEN`. The existing `$CHIP` token row is
  widened from two parts to the eleven-part inventory.
- **New `PROCEDURE.md` amendment:** Amendment 4, with eight numbered clauses.
- **Extended `run_gates.sh` gate:** the existing `render_steps.py` live gate now renders and diffs
  **both** sections. The gate count stays at seven.

Symbols created by **other plans in this phase** and cited by the `C-NN` bodies:
`append_chip_evidence.py`, `render_chip_evidence.py`, `bench/CHIP-EVIDENCE.jsonl`,
`bench/CHIP-EVIDENCE.md`, `bench/cells/CHIP/`, and `rig-pins.json`'s eleven-entry `chips` map with
its `chip_token` field.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach render_steps.py a second, gated section</name>
  <files>.planning/v1.34/tools/render_steps.py</files>
  <read_first>
    - `.planning/v1.34/tools/render_steps.py` — **read in full**. Specifically: the docstring paragraph stating that substitution tokens are emitted **as literal token text** and never expanded, and that `--arm` selects *inclusion* via the annotation marker; `_STEP_LIST_HEADING_RE`; `_NEXT_H2_RE`; `extract_step_list_section()`; `_STEP_ID_RE`; `_ANNOTATION_RE`; `_ARM_CHOICES`; `parse_steps()`; `validate_steps()` and each `ProcedureParseError` it raises; `render_for_arm()`'s inclusion filter; and `main()`'s argument surface and exit codes.
    - `.planning/v1.34/PROCEDURE.md` — the `## Arm substitution` section in full (it states the same arm-agnosticism property in prose and declares the non-arm-dependent tokens), and the `## Step list` heading with the first two step bodies, for the heading shape the parser expects.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md` R6 — "Blocking finding: `C-NN` step ids cannot live inside `## Step list`", the three options with their costs, and "Correction: `$ARM_BIN` is not what makes the diff non-empty".
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-PATTERNS.md` — the `render_steps.py` section, which quotes the two regexes and `validate_steps`' docstring.
  </read_first>
  <behavior>
    Two new `--selftest` legs, in the file's existing accumulate-then-report style, each asserting
    on a named reason where it is negative:

    - Positive: a fixture procedure containing both an eleven-step `P` section and a nine-step `C`
      section renders nine lines under the chip section and eleven under the default section, and
      each section's two arm renders are byte-identical.
    - Negative: a fixture whose `## Step list` contains a `C-NN` heading is refused, and the reason
      names the offending id and the id pattern the section requires; and, symmetrically, a fixture
      whose chip section contains a `P-NN` heading is refused under `--section C` with its own
      named reason.
  </behavior>
  <action>
Add a `--section` argument taking exactly two values, `P` and `C`, **defaulting to `P`** so every
existing invocation — including `run_gates.sh`'s current two — behaves byte-identically to today.

Generalise the two hardcoded facts the parser holds, and only those two: the section heading it
extracts, and the step-id pattern it validates. Section `P` keeps the existing heading and the
existing two-digit `P-NN` pattern; section `C` uses the heading `## Chip-sweep step list` and a
two-digit `C-NN` pattern. Every other property of `validate_steps()` is unchanged and applies to
both sections: duplicate ids are refused, ids must be **strictly ascending** by their numeric part,
and an annotation naming an arm outside the arm vocabulary is refused. `_NEXT_H2_RE` already
terminates a section at the next H2, so the two sections cannot bleed into each other.

Keep the substitution semantics exactly as they are and re-state them in the docstring for the new
section: tokens such as the arm-binary token, the port token and the cell-id token are emitted **as
literal token text**, never expanded — expanding one to a concrete path would make the two renders
differ by construction and defeat the property the gate exists to measure. What `--arm` actually
selects is **inclusion**, via the annotation marker. That marker is the only thing that can make
the diff non-empty, and it must appear in neither section's new text.

Refuse a missing section by name rather than rendering empty: the existing behaviour for an absent
`## Step list` is a named `ProcedureParseError`, and section `C` must behave identically, because a
renderer that emits nothing and exits 0 is the vacuous-pass shape this suite already guards against
with its zero-tools-discovered exit 2.

Change nothing else. `render_steps.py` is load-bearing for the milestone's only arm-agnosticism
proof.
  </action>
  <acceptance_criteria>
    - `python3 .planning/v1.34/tools/render_steps.py --selftest` exits 0 with a `PASS:` line for both new legs and no `FAIL:` line.
    - `render_steps.py --arm control --procedure .planning/v1.34/PROCEDURE.md` with **no** `--section` argument behaves exactly as before: exit 0, non-empty, 11 lines.
    - `--section P` and the no-argument form produce byte-identical output for the same arm.
    - `--section C` against a procedure with no chip section exits non-zero with a named reason, not an empty render and exit 0.
    - `--section` rejects any value other than `P` and `C` with an argparse usage error and exit 2.
    - The file still contains the selftest flag token spelled with double quotes.
    - The source contains no arm-annotation marker literal added to a default or a fallback.
    - `ls .planning/v1.34/tools/*.py | wc -l` is still `14` — no new tool file.
    - Both sub-repo porcelains are empty.
  </acceptance_criteria>
  <verify>
    <automated>python3 /workspaces/.planning/v1.34/tools/render_steps.py --selftest
echo "selftest_rc=$?"</automated>
    <automated>T=/workspaces/.planning/v1.34/tools/render_steps.py
P=/workspaces/.planning/v1.34/PROCEDURE.md
python3 "$T" --arm control --procedure "$P" > /tmp/162-04-p-default.txt
RC_A=$?
python3 "$T" --arm control --procedure "$P" --section P > /tmp/162-04-p-explicit.txt
RC_B=$?
echo "rc_default=$RC_A rc_explicit=$RC_B"
test "$RC_A" -eq 0
test "$RC_B" -eq 0
cmp /tmp/162-04-p-default.txt /tmp/162-04-p-explicit.txt
test "$(wc -l < /tmp/162-04-p-default.txt)" -eq 11
echo "PASS: default equals --section P, 11 lines"</automated>
    <automated>T=/workspaces/.planning/v1.34/tools/render_steps.py
out=$(python3 "$T" --arm control --procedure "$T" --section bogus 2>&1)
RC=$?
echo "bad_section_rc=$RC"
test "$RC" -eq 2
case "$out" in *"invalid choice"*) echo "PASS: --section rejects an unknown value";; *) echo "FAIL: unknown section not refused"; exit 1;; esac</automated>
    <automated>python3 -c "
src=open('/workspaces/.planning/v1.34/tools/render_steps.py').read()
assert src.count(chr(34)+'--selftest'+chr(34))>=1
assert '--section' in src
print('PASS: --section present and the selftest flag is still advertised in double quotes')
"
ls /workspaces/.planning/v1.34/tools/*.py | wc -l</automated>
  </verify>
  <done>`render_steps.py` parses and validates a second section under its own H2, defaults to the existing behaviour so nothing already green changes, refuses a missing section by name rather than rendering empty, and carries a positive and a negative leg for the new path.</done>
</task>

<task type="auto">
  <name>Task 2: Write PROCEDURE.md Amendment 4 and the C-01…C-09 chip-sweep step list</name>
  <files>.planning/v1.34/PROCEDURE.md</files>
  <read_first>
    - `.planning/v1.34/PROCEDURE.md` — **read in full**, and specifically: the header sentence claiming the document is executed unchanged by Phases 161, 162 and 163; `## Scope`'s first paragraph and its amendment-discipline paragraph; `## Standing bench rules` (all nine); `## Arm substitution`'s token table and its statement that tokens are never expanded; `P-01`, `P-02`, `P-04`, `P-06` and `P-11`'s full bodies including their literal command blocks, `Performer:` clauses and `Record field(s):` clauses; `P-03`, `P-05`, `P-07`, `P-08`, `P-09` and `P-10`'s bodies, so the not-applicable list is accurate; `## Outcome taxonomy`; `## Halt policy` with `P-H1` and `P-H2`; `## Forbidden invocations`; `## Recording discipline`; and Amendments 1, 2 and 3 at the file bottom, for the house format.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md` R6 in full — the exact §Scope and header replacements, the five shared steps with what each contributes and how it differs for this shape, the six not-applicable steps with their reasons, the Amendment 3 clause (c) precedent, the measured `~/.firestarter` drift, and the "formatting Amendment 4 to match" list.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-PATTERNS.md` — the `PROCEDURE.md` section: the amendment house style quoted from Amendment 2, and the `P-NN` step-body shape quoted from `P-02`.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-01-PLAN.md` — PD-1's part table (slug, token, JP4, VPP target per part), PD-3, PD-7, PD-9, PD-14's ceiling table and PD-17.
    - `.planning/v1.34/rig-pins.json` — the eleven-entry `chips` map with `chip_token`, `config_dir`, `arms.*.venv_bin`, `targets.leonardo.hex_span_expected_by_arm`.
  </read_first>
  <action>
Two surgical corrections outside `## Step list`, one new H2 section, one amendment paragraph, and
exactly one value changed inside `## Step list`.

**Correction A — the header sentence.** Replace the claim that the document is executed unchanged
by Phases 161, 162 and 163 with the truth: unchanged by Phases 161 and 163, whose cells are
write-read-verify cells; Phase 162 runs a different command and executes the chip-sweep step list
instead; and every cell either phase runs cites this document's step ids rather than re-describing
the run.

**Correction B — §Scope.** Leave the existing WRV cell definition paragraph intact and insert a new
paragraph after it, headed "Two cell shapes, not one". It states that the paragraph above defines
the **WRV cell** — the shape Phases 161 and 163 run and the only shape the `P-NN` list describes —
and that Phase 162 runs a **chip-sweep position** instead: one part, one arm, one `dev test`
invocation, on the standing Leonardo and Rev 2.0 rig Phase 161 leaves assembled. A chip position
has no pre-computed image, no `IMAGE-PLAN.json` row, no full-device SHA judgement and no per-cell
arm rotation, so `P-07` and `P-09` do not apply to it at all. Its steps are `C-01`…`C-09` under
`## Chip-sweep step list`, and its evidence goes to `bench/CHIP-EVIDENCE.jsonl`, never to
`bench/EVIDENCE.jsonl`, whose position count covers WRV positions only. Close the paragraph with
the sentence that matters most: the standing bench rules, the arm-substitution token table, the
halt policy, the outcome taxonomy, the forbidden invocations and the recording discipline bind
**both** shapes identically.

**Correction C — `## Arm substitution`.** Widen the `$CHIP` token row from the two bench chips to
the eleven-part inventory, and declare a new token `$CHIP_TOKEN` alongside it: the case-sensitive
CLI token as typed, distinct from the lower-case rig slug. State why it is declared separately —
the report path is built from the token as typed and the token sanitiser does not normalise case,
so the same part invoked in two casings writes two different files and the appender's copy-out
reads only one of them. Both tokens are non-arm-dependent, like the rest of the table.

**The new `## Chip-sweep step list` section**, placed immediately after `## Step list` ends and
before the next existing H2, so the `P` section's extractor terminates exactly where it does today.
Open the section with a short paragraph stating that these steps are the chip-sweep position's
prescription, that `P-01`, `P-02`, `P-04`, `P-06` and `P-11` are shared **by reference and never by
copy**, and that `P-03`, `P-05`, `P-07`, `P-08`, `P-09` and `P-10` are **explicitly not applicable**
— naming the reason for each: the first two are Uno-class-only and the Leonardo is chip-out-exempt;
`P-07` and `P-09` write a pre-computed image and judge a full-device SHA; `P-08` is the two-chip
rotation within a cell; and `P-10` is the per-cell arm switch, which D-17 replaces with a
per-divergence interleave. Name them rather than leaving it to inference — Amendment 2's own lesson
is that a step whose literal command must be inferred by analogy is the failure mode this
document's prescriptive contract exists to prevent.

Then nine steps, each in the established body shape: an `### C-NN — <Title>` heading, prose, a
**literal fenced command block** (Amendment 2 made this mandatory — no prose-only steps), a
`Performer:` clause and a `Record field(s):` clause. Ids are two-digit and strictly ascending, and
**no step carries an arm-annotation marker**.

- **`C-01` — Assert the frozen config dir is pristine.** Runs `check_arms.py` with the expected
  config-dir SHA before the position's `dev test`. A dirty digest here means the *previous*
  position's report was never copied out and the row about to be written would carry a wrong
  `config_dir_sha`. Performer: Claude. Record: `config_dir_sha`.
- **`C-02` — Seat the part; set JP4 and the pot as this position requires.** The single operator
  handover for the position, folding what `P-05`, `P-06` and `P-08` do for the WRV shape into one
  stop, because the standing requirement is one operator stop per part and not two. State the part,
  the pin count, the JP4 position and — only when this position is a pot boundary — the target and
  the request for **one** meter reading. The operator adjusts the pot and reads the meter solo:
  state the target, wait, take exactly **one** read, never a monitor loop. One clean re-seat is
  permitted per position and **both** the discarded attempt and the re-run are recorded. Performer:
  operator. Record: `jp4_position`, `reseat_count`, `vpp_real_mv`.
- **`C-03` — One firmware VPP reading for this part.** The arm's `vpp` subcommand, run **exactly
  once**, with the config-dir variable inline. A blank or `0x303` reading is a **contact fault, not
  a voltage** — that is `P-H1`, not a pot adjustment, because the VPP and VPE monitors do not route
  to the socket. Never chase the database target upward: the firmware guard is asymmetric, a
  reading above the target plus 500 mV is a blocking error downgradeable only by a permanently
  withdrawn flag, while a reading below 95% of target is a non-blocking warning. Performer: Claude.
  Record: `vpp_target_mv`, `vpp_firmware_mv`, `vpp_shortfall_mv`.
- **`C-04` — Capture provenance for this position.** `capture_provenance.py` with this position's
  cell id, position id, arm, target, port, chip slug and declared shield revision, plus
  `--no-image-plan` because a chip position writes the chip but writes no pre-computed image and
  therefore has no image row to resolve. Performer: Claude. Record: the nineteen provenance columns.
- **`C-05` — Run `dev test` under the size-class ceiling, fully logged.** One invocation, one
  argument, no flags — the command takes exactly one argument and one optional flag, and that flag
  is forbidden here because it disables the two measurements this sweep most needs. The config-dir
  variable is inline; the arm binary is the pinned absolute path, never the bare name on `PATH`.
  The invocation runs under a timeout derived from the size class, with full stdout **and** stderr
  into a numbered log pair and the wrapper's exit code recorded. If the ceiling fires, the kill is
  logged with the last progress frame and **which step was in flight**, and recorded as timed out
  at N seconds against a measured baseline of M — write progress is time-keyed per block with the
  clock restarting each block, so the last frame names a block and not a byte offset. Performer:
  Claude. Record: `exit_code`, the per-step maps, `write_target`, `write_coverage`, `banner`,
  `dedup_query_outcome`.
- **`C-06` — Read-divergence follow-up, only when the report says the two read runs diverged.**
  `dev test` destroys its read bytes, so only the divergence metric survives; when it is set, one
  `dev consistency-check` at three runs with the keep-files and output-directory arguments captures
  the actual bytes on the same seating and the same arm, before any re-flash. This is recorded as a
  data point against the standing read-instability question and does **not** attempt to close it.
  Performer: Claude. Record: `read_divergence`, `read_consistency_followup`.
- **`C-07` — Append the row, then re-render and check.** `append_chip_evidence.py` copies the report
  out of the frozen config dir to a position-keyed path, verifies both copies' digests, removes the
  two source files — the files only, never the directory — asserts the pristine digest again, and
  appends. `render_chip_evidence.py --check` runs immediately after, as one pair. This ordering is
  the invariant: without the removal the config-dir assertion reds from the first position onward
  and every already-appended row's recorded digest starts failing at the next gate run. Performer:
  Claude. Record: the artifact paths and digests, `outcome`, `divergence_verdict`.
- **`C-08` — Divergence arbitration, with the chip unmoved.** When and only when this position's
  verdict begins with the diverging prefix: re-flash to the control arm per `P-04` by reference
  including its independent read-back proof judged against the **arm-keyed** expected span, re-run
  the same part on the same seating, append the control row naming the position it arbitrates, then
  re-flash back per `P-04` again. Two flashes per divergence and **zero extra chip handlings**. The
  Leonardo is chip-out-exempt so the part stays seated throughout. On a UV part the control re-run
  lands on the **next slot down**, because slot selection is stateless and keyed on chip content —
  record that, do not fight it. Performer: Claude. Record: `control_rerun_for`,
  `fw_readback_sha_judged`, `fw_readback_judged_span_bytes`.
- **`C-09` — Per-wave gate.** `run_gates.sh` with its exit code taken **directly**, never through a
  pipe. Performer: Claude. Record: the measured selftest and live-gate counts and the exit code.

**The one change inside `## Step list`.** Measure `~/.firestarter/config.json` at the moment the
amendment is written — its size, its content digest, the tree digest and its mtime — and re-pin the
**mtime** in `P-11`'s baseline block to the measured value. Change **nothing else** in that block:
keep all four values asserted, because the mtime is the only one of the four that has ever changed
and dropping it would make the assertion vacuous. Change no other line inside `## Step list`, and
no `### P-NN — <title>` heading.

**Amendment 4**, appended at the file bottom in the house format — a bold-led paragraph headed with
the amendment number, the date and this plan, with inline `(a) What changed:`, `(b) Why:` and
`(c) Which cells ran under which text:` clauses, and numbered items inside (a) mirrored by numbered
reasons inside (b). Eight items:

1. The header sentence and §Scope corrected to name two cell shapes.
2. A new `## Chip-sweep step list` H2 carrying `C-01`…`C-09`, sharing five `P` steps by reference
   and listing six as explicitly not applicable.
3. `## Arm substitution`'s `$CHIP` row widened to the eleven-part inventory and `$CHIP_TOKEN`
   declared, with the case-sensitivity reason.
4. `P-06`'s once-per-cell pot rule **superseded per-part for the chip-sweep shape only**, per
   D-11/D-13: every part records its own firmware VPP reading, the meter comes out only at the two
   pot-group boundaries, and every other position records its real-rail figure as a
   named absence naming the position where the meter was read and the group ratio that makes it
   derivable. The `P` step text is unchanged; the supersession lives in `C-02`/`C-03` and here.
5. `P-11`'s completeness assertion **retargeted for the chip-sweep shape** to
   `bench/CHIP-EVIDENCE.jsonl` and `render_chip_evidence.py --check`, and its assertion (2) noted as
   now load-bearing for the copy-out invariant rather than merely confirmatory. The `P` step text is
   unchanged; the retarget lives in `C-07`/`C-09` and here.
6. The `~/.firestarter/config.json` mtime baseline re-pinned, recording the transition from the
   Amendment-3 value to the measured one as the **fourth** recurrence — the first three were Phase
   161's cells `A1`, `A2` and `A3/B2`. State that content and tree digests are unchanged across all
   four, that a further change **during** this phase is this phase's own `P-H1` finding, and that
   removal is never attempted.
7. The config-dir copy-out invariant stated as a step obligation: assert pristine before, copy out,
   verify, remove the two source files, assert pristine after.
8. `--no-image-plan`'s documented rationale widened to cover a chip-sweep position, which writes the
   chip but has no pre-computed image row — the mechanism already fits and only the help text's
   scope did not.

Clause (b) gives the measured trigger for each of the eight, including for (2) the fact that a
`C-NN` id inside the `P` section raises and reds the live gate, and for (6) the measured drift.

Clause (c) is materially harder than its predecessors', because **three real sweep cells have now
run**. It must say, truthfully: `A1`, `A2` and `A3/B2` ran under the Amendment-3 text and are
unaffected — Amendment 4 adds a new section, corrects §Scope and the header, and changes exactly
one value inside `## Step list`, the `~/.firestarter` mtime baseline, which those three cells
asserted against a value reality had already overtaken; no prescription any of them executed has
changed and every `P-NN` heading is byte-identical. `B1` and `B3`, Phase 163's cells, will run under
`## Step list` at the Amendment-4 text. No chip-sweep position has run under any text; Amendment 4
lands before the first one.

Close with the re-confirmation sentence, and **name the mechanism correctly**: the arm-agnostic
empty-diff gate was re-confirmed after this edit for **both** sections, and it stays empty because
no new text in either section carries an `[arm: …]` annotation marker — the marker, not the
arm-binary token, is what makes a render differ, since substitution tokens are emitted literally
and are diff-neutral. Amendments 2 and 3 both put the token first; say the marker first here, and
if the token is mentioned at all, say explicitly that it is emitted literally and is therefore
diff-neutral.
  </action>
  <acceptance_criteria>
    - `PROCEDURE.md`'s header no longer claims the document is executed unchanged by Phase 162; the sentence names Phases 161 and 163 and points Phase 162 at the chip-sweep section.
    - `## Scope` contains a "Two cell shapes, not one" paragraph naming the chip-sweep position, stating that `P-07` and `P-09` do not apply, pointing its evidence at `CHIP-EVIDENCE.jsonl`, and stating that the standing rules, token table, halt policy, outcome taxonomy, forbidden invocations and recording discipline bind both shapes.
    - `## Arm substitution` lists all eleven chip slugs for `$CHIP` and declares `$CHIP_TOKEN` with the case-sensitivity reason.
    - A `## Chip-sweep step list` H2 exists, placed after `## Step list` and before the next pre-existing H2.
    - The chip section contains exactly nine `### C-NN — <title>` headings with ids `C-01` through `C-09`, strictly ascending; each body carries a fenced command block, a `Performer:` clause and a `Record field(s):` clause.
    - The chip section names all five shared `P` steps by id and all six not-applicable `P` steps by id with a reason for each.
    - No text anywhere in either step-list section contains an arm-annotation marker.
    - `render_steps.py --arm control --section C` and `--arm v133 --section C` both exit 0, are non-empty, and are byte-identical, at 9 lines each.
    - `render_steps.py --arm control` and `--arm v133` (default section) are still byte-identical at **11** lines each.
    - The set of `### P-NN — <title>` heading lines in the working tree is byte-equal to the set in `HEAD`'s copy of the file.
    - Every line inside `## Step list` that differs from `HEAD` lies inside `P-11`'s pinned-baseline block and carries a ten-digit epoch value; the new value equals the live `~/.firestarter/config.json` mtime measured at edit time.
    - `P-11` still asserts all four baseline values — file count and size, content digest, tree digest and mtime — and still carries assertion (2) with the frozen config-dir digest.
    - An `Amendment 4` paragraph exists with all three clause labels, eight numbered items in (a) and eight mirrored reasons in (b).
    - Clause (c) names `A1`, `A2`, `A3/B2`, `B1` and `B3` explicitly and states that no chip-sweep position has run under any text.
    - The closing re-confirmation sentence names the annotation marker as the mechanism **before** any mention of the arm-binary token, and if it mentions the token it states that the token is emitted literally and is diff-neutral.
    - Both sub-repo porcelains are empty.
  </acceptance_criteria>
  <verify>
    <automated>T=/workspaces/.planning/v1.34/tools/render_steps.py
P=/workspaces/.planning/v1.34/PROCEDURE.md
python3 "$T" --arm control --procedure "$P" --section P > /tmp/162-04-pc.txt
RC1=$?
python3 "$T" --arm v133 --procedure "$P" --section P > /tmp/162-04-pv.txt
RC2=$?
python3 "$T" --arm control --procedure "$P" --section C > /tmp/162-04-cc.txt
RC3=$?
python3 "$T" --arm v133 --procedure "$P" --section C > /tmp/162-04-cv.txt
RC4=$?
echo "rcs=$RC1 $RC2 $RC3 $RC4"
test "$RC1" -eq 0
test "$RC2" -eq 0
test "$RC3" -eq 0
test "$RC4" -eq 0
test -s /tmp/162-04-pc.txt
test -s /tmp/162-04-cc.txt
diff -u /tmp/162-04-pc.txt /tmp/162-04-pv.txt
diff -u /tmp/162-04-cc.txt /tmp/162-04-cv.txt
test "$(wc -l < /tmp/162-04-pc.txt)" -eq 11
test "$(wc -l < /tmp/162-04-cc.txt)" -eq 9
echo "PASS: both sections render arm-identically, P=11 lines C=9 lines"</automated>
    <automated>python3 - <<'PY'
import re, subprocess, pathlib
def headings(text):
    return [l for l in text.splitlines() if re.match(r'^### P-\d\d ', l)]
def section(text, name):
    m = re.search(r'^##\s+' + re.escape(name) + r'\s*$', text, re.M)
    assert m, 'section not found: ' + name
    s = m.end()
    n = re.search(r'^##\s+\S.*$', text[s:], re.M)
    return text[s:s + n.start()] if n else text[s:]
old = subprocess.run(['git','-C','/workspaces','show','HEAD:.planning/v1.34/PROCEDURE.md'],
                     capture_output=True, text=True).stdout
new = pathlib.Path('/workspaces/.planning/v1.34/PROCEDURE.md').read_text()
assert headings(old) == headings(new), 'a P-NN heading line changed'
a, b = section(old, 'Step list'), section(new, 'Step list')
import difflib
changed = [l[1:].strip() for l in difflib.unified_diff(a.splitlines(), b.splitlines(), n=0)
           if (l.startswith('+') or l.startswith('-')) and not l.startswith(('+++','---'))]
bad = [l for l in changed if not re.search(r'\b17\d{8}\b', l)]
assert not bad, ('unexpected change inside ## Step list: ' + repr(bad[:5]))
print('PASS: P-NN headings byte-equal; the only Step list changes carry an epoch value')
print('changed lines:', len(changed))
PY</automated>
    <automated>python3 -c "
import re, os
P='/workspaces/.planning/v1.34/PROCEDURE.md'
t=open(P).read()
live=str(int(os.stat(os.path.expanduser('~/.firestarter/config.json')).st_mtime))
assert live in t, 'the re-pinned mtime does not match the live value ' + live
print('PASS: P-11 carries the live mtime', live)
"</automated>
    <automated>python3 -c "
import re
P='/workspaces/.planning/v1.34/PROCEDURE.md'
t=open(P).read()
m=re.search(r'^##\s+Chip-sweep step list\s*\$', t, re.M)
assert m, 'no ## Chip-sweep step list heading'
nxt=re.search(r'^##\s+\S.*\$', t[m.end():], re.M)
sec=t[m.end():m.end()+nxt.start()] if nxt else t[m.end():]
ids=re.findall(r'^### (C-\d\d) ', sec, re.M)
assert ids==['C-0%d'%i for i in range(1,10)], ids
assert sec.count('Performer:')>=9, sec.count('Performer:')
assert sec.count('Record field')>=9, sec.count('Record field')
for pid in ['P-01','P-02','P-04','P-06','P-11','P-03','P-05','P-07','P-08','P-09','P-10']:
    assert pid in sec, 'chip section does not mention ' + pid
assert not re.search(r'\[\s*arm\s*:', sec), 'an arm annotation marker leaked into the chip section'
print('PASS: C-01..C-09 with performer and record clauses, all eleven P steps referenced, no arm marker')
"</automated>
    <automated>python3 -c "
import re
t=open('/workspaces/.planning/v1.34/PROCEDURE.md').read()
i=t.find('Amendment 4')
assert i>0, 'no Amendment 4'
a=t[i:i+12000]
for lbl in ['(a) What changed:','(b) Why:','(c) Which cells ran under which text:']:
    assert lbl in a, lbl
for cell in ['A1','A2','A3/B2','B1','B3']:
    assert cell in a, cell
assert 'render_steps.py' in a
marker=a.find('[arm')
tok=a.find('ARM_BIN')
assert marker>0, 'the amendment does not name the arm annotation marker'
assert tok<0 or marker<tok, 'the arm-binary token is named before the marker'
assert 'CHIP-EVIDENCE.jsonl' in a
assert 'no-image-plan' in a
print('PASS: Amendment 4 clauses, cell roster, marker-first re-confirmation')
"</automated>
    <automated>python3 -c "
t=open('/workspaces/.planning/v1.34/PROCEDURE.md').read()
i=t.find('## Arm substitution'); j=t.find('## Step list')
sec=t[i:j]
for slug in ['w27c512','w27e512','sst27sf512','fm1608','w27e040','sst39sf040','w29c040','w29c020','am27c020','m27c512','2516']:
    assert slug in sec, slug
assert 'CHIP_TOKEN' in sec
print('PASS: arm-substitution table widened to eleven parts and declares the token')
"
git -C /workspaces/firestarter status --porcelain
git -C /workspaces/firestarter_app status --porcelain
echo "submodule_porcelain_checked"</automated>
  </verify>
  <done>`PROCEDURE.md` describes both cell shapes, prescribes `C-01`…`C-09` under its own gated H2, re-pins the `~/.firestarter` mtime so no position books a false `P-H1`, and records all of it in Amendment 4 with the annotation marker named as the mechanism.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the render_steps live gate to both sections and re-measure 14/14, 7/7</name>
  <files>.planning/v1.34/tools/run_gates.sh</files>
  <read_first>
    - `.planning/v1.34/tools/run_gates.sh` — the existing `render_steps.py` live-gate block in full: the two temp files, the two return-code captures, the two line counts, and the three ordered conditions (non-zero exit, empty render, non-empty diff) with their distinct failure messages. Also the header comment's live-gate list as updated in plan 162-03.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md` R6 §"How the gate is invoked, verbatim from `run_gates.sh`" and option (A)'s cost paragraph.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-01-PLAN.md` PD-2.
  </read_first>
  <action>
Extend the **existing** `render_steps.py` live-gate block so it renders and diffs **both** sections
— four renders in total, control and v133 for each of `P` and `C` — inside one gate. Keep the three
ordered conditions and their distinct messages, applied per section, and report the per-section line
counts in the PASS line so a reader sees eleven and nine rather than a bare "diff empty". Extend the
existing failure messages to name which section failed.

**Do not add an eighth gate** (PD-2). The suite target this phase is graded against is **14 tool
selftests and 7 live gates**, and the chip-section diff is a property of the procedure document the
existing gate already owns. Update the header comment's live-gate list to say that the procedure
gate now covers both sections.

Then run the **full** suite and capture the exit code on the very next line — never through a pipe.
Expect **14/14 selftests and 7/7 live gates, exit 0**.

Finally, observe the new half of the gate **firing**, so it is recorded as having fired rather than
as having been configured: temporarily add an `[arm: control]` annotation marker to one `C-NN`
heading in a scratch copy of `PROCEDURE.md`, point the renderer at that copy, and confirm the two
chip-section renders now differ. Restore nothing in the real file — the real file is never mutated
for this check; the scratch copy is the subject. Record the transcript.

Commit `render_steps.py`, `PROCEDURE.md` and `run_gates.sh` as one change.
  </action>
  <acceptance_criteria>
    - `run_gates.sh`'s `render_steps.py` gate block performs four renders and two diffs, and its PASS line reports both section line counts.
    - The suite still reports exactly **7** live gates — no eighth block was added.
    - `bash .planning/v1.34/tools/run_gates.sh` exits **0** with its code captured directly, reporting 14 tool selftests and 7 live gates.
    - `bash .planning/v1.34/tools/run_gates.sh --quick` also exits 0 and its output shows both section line counts.
    - The header comment's live-gate list states that the procedure gate covers both sections.
    - Against a scratch copy of `PROCEDURE.md` carrying an arm-annotation marker on a `C-NN` heading, the two chip-section renders **differ** — the gate's new half is observed firing, and the real `PROCEDURE.md` is byte-unchanged by that check.
    - `EVIDENCE.jsonl`, `EVIDENCE.md`, `CHIP-EVIDENCE.jsonl` and `CHIP-EVIDENCE.md` are all byte-unchanged by this task.
    - Both sub-repo porcelains are empty.
  </acceptance_criteria>
  <verify>
    <automated>bash /workspaces/.planning/v1.34/tools/run_gates.sh > /tmp/162-04-gates.out 2> /tmp/162-04-gates.err
RC=$?
echo "run_gates_rc=$RC"
python3 -c "
t=open('/tmp/162-04-gates.out').read()
n=t.count('selftest PASS'); g=t.count('live gate PASS')
print('selftests',n,'live gates',g)
assert n==14, n
assert g==7, g
assert 'render_steps.py' in t
print('PASS: 14/14 selftests and 7/7 live gates, no eighth gate')
"
test "$RC" -eq 0</automated>
    <automated>bash /workspaces/.planning/v1.34/tools/run_gates.sh --quick > /tmp/162-04-quick.out 2> /tmp/162-04-quick.err
RC=$?
echo "quick_rc=$RC"
test "$RC" -eq 0
grep -q '11' /tmp/162-04-quick.out
grep -q '9' /tmp/162-04-quick.out
echo "PASS: --quick green and reports both section line counts"</automated>
    <automated>T=/workspaces/.planning/v1.34/tools/render_steps.py
P=/workspaces/.planning/v1.34/PROCEDURE.md
S=/tmp/162-04-scratch-procedure.md
python3 - "$P" "$S" <<'PY'
import re, sys
src, dst = sys.argv[1], sys.argv[2]
t = open(src).read()
t2, n = re.subn(r'^(### C-01 [^\n]*)$', r'\1 [arm: control]', t, count=1, flags=re.M)
assert n == 1, 'could not plant the marker on C-01'
open(dst, 'w').write(t2)
print('planted the annotation marker on C-01 in the scratch copy')
PY
python3 "$T" --arm control --procedure "$S" --section C > /tmp/162-04-neg-c.txt
python3 "$T" --arm v133 --procedure "$S" --section C > /tmp/162-04-neg-v.txt
if cmp -s /tmp/162-04-neg-c.txt /tmp/162-04-neg-v.txt; then
  echo "FAIL: the chip-section arm diff did not fire on a planted marker"
  exit 1
fi
echo "NEGATIVE CONTROL FIRED: the chip-section renders differ when an arm marker is present"
git -C /workspaces diff --quiet -- .planning/v1.34/PROCEDURE.md
echo "real PROCEDURE.md untouched by the negative control rc=$?"</automated>
    <automated>git -C /workspaces status --porcelain -- .planning/v1.34/bench/EVIDENCE.jsonl .planning/v1.34/bench/EVIDENCE.md .planning/v1.34/bench/CHIP-EVIDENCE.jsonl .planning/v1.34/bench/CHIP-EVIDENCE.md
echo "record files unchanged by this task (empty output above means unchanged since the last commit)"
git -C /workspaces/firestarter status --porcelain
git -C /workspaces/firestarter_app status --porcelain
echo "submodule_porcelain_checked"</automated>
  </verify>
  <done>The arm-agnosticism gate now covers the chip-sweep section as well as the WRV one, inside a single gate so the suite stays at seven, and the new half has been **observed to fire** on a planted annotation marker before any part is seated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `PROCEDURE.md` prose -> what the executor actually does at the bench | the document is the prescription; an ambiguous or false clause becomes an improvised bench action |
| a new step-list section -> `render_steps.py`'s parser | a malformed id reds the milestone's only arm-agnosticism proof for every remaining phase |
| a stale pinned baseline -> the halt policy | an assertion that is unconditionally red books a false halt at every position and trains the executor to ignore it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-18 | Denial of service | a `C-NN` id inside the WRV step list | high | mitigate | the new list goes under its own H2, which the existing extractor terminates at; a selftest leg drives the refusal explicitly; the P-section render is asserted at 11 lines after the edit |
| T-162-19 | Repudiation | a vacuous re-confirmation over ungated text | high | mitigate | option (A) taken: the renderer learns the section and the existing gate diffs it, and the new half is observed firing on a planted annotation marker |
| T-162-20 | Repudiation | a stale `~/.firestarter` pin booking ten false halts | high | mitigate | the mtime is measured at edit time and re-pinned, the transition is recorded as the fourth recurrence, and all four values stay asserted so the check does not become vacuous |
| T-162-21 | Tampering | an unintended edit to text three executed cells cited | high | mitigate | every `P-NN` heading asserted byte-equal to `HEAD`, and every changed line inside the section asserted to carry an epoch value — scoped to the prescription rather than to whole-file byte identity |
| T-162-22 | Spoofing | a wrong-case chip token writing a report the appender never reads | medium | mitigate | `$CHIP_TOKEN` declared in the token table with its reason, pinned per part in `rig-pins.json`, and cross-checked by the appender against the report's own recorded chip |
| T-162-SC | Tampering | package installs | low | accept | no package manager runs in this plan or anywhere in this phase; no slopsquatting surface exists |
</threat_model>

<verification>
- `bash .planning/v1.34/tools/run_gates.sh` exits 0, read directly, at 14/14 selftests and 7/7 live gates; `--quick` likewise.
- Section P renders arm-identically at 11 lines; section C renders arm-identically at 9 lines.
- Every `### P-NN — <title>` heading is byte-equal to `HEAD`'s, and every changed line inside `## Step list` carries an epoch value.
- `P-11` carries the live `~/.firestarter/config.json` mtime and still asserts all four baseline values.
- The chip section holds `C-01`…`C-09`, each with a fenced command block, a `Performer:` clause and a `Record field(s):` clause, references all eleven `P` steps, and carries no arm-annotation marker.
- Amendment 4 has three clause labels, eight items, the five-cell roster, and a re-confirmation naming the marker first.
- A planted annotation marker on `C-01` in a scratch copy makes the two chip renders differ — the gate's new half fired.
- Both sub-repo porcelains are empty.
</verification>

<success_criteria>
The procedure now describes the shape Phase 162 actually runs, prescribes it as nine gated steps
with literal commands, and stops asserting a baseline that reality had already overtaken. Wave 0 is
discharged: two new tools, one new record shape, one widened renderer, one amended procedure, and a
suite measured at 14/14 selftests and 7/7 live gates with both new gates and the new half of an old
one observed firing. No part has been seated and no evidence row exists.
</success_criteria>

<output>
Create `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-04-SUMMARY.md`
when done. Record: the `--section` argument as built and how the two hardcoded parser facts were
generalised; the two new selftest leg names; the `C-01`…`C-09` titles as landed with their
performer split; the Amendment 4 text verbatim; the measured `~/.firestarter` four values with the
old and new mtime and the recurrence count; the per-section render line counts; the negative-control
transcript for the planted marker; and the measured `run_gates.sh` counts and exit code.
</output>
