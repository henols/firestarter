---
phase: 162-chip-11-part-dev-test-sweep-on-the-reference-rig
plan: 10
type: execute
wave: 10
depends_on: ["162-09"]
files_modified:
  - .planning/v1.34/bench/CHIP-EVIDENCE.jsonl
  - .planning/v1.34/bench/CHIP-EVIDENCE.md
  - .planning/v1.34/bench/cells/CHIP/
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [CHIP-01, CHIP-02, CHIP-03, CHIP-04, CHIP-05]
tags: [bench, close, cell-CHIP, named-absence, reconciliation, teardown, backlog, phase-gate]

must_haves:
  truths:
    - "All eleven inventory parts are accounted for as arithmetic over rows: ten reports plus one named absence equals eleven, evaluated by a script rather than counted by a reader (CHIP-01/SC#1)."
    - "No row in the file carries a null `fw_board_identity`, and every one equals the v1.33 arm's recorded firmware identity from the pre-flight datum (CHIP-02/SC#2)."
    - "One table carries every part's result beside its prior disposition, with the divergence column holding an explicit `same` or `diverges: <how>` in **every** row, no blank cells and no unsourced prior disposition (CHIP-03/SC#3)."
    - "A control-arm re-run exists for **every** part marked `diverges` and for **no** part marked `same`, proven as a three-part equation over rows; the total is stated as `10 + N` with the arithmetic shown and the deviation from the roadmap's `11 + N` named on the same line (CHIP-04/SC#4, D-06)."
    - "All five known-carried parts — the two stuck-bit parts, the locked-boot-block part, the non-deterministic part and the byte-zero-defect part — carry their prior disposition cited inline in their own row and are labelled known-carried, so none of their reds can enter Phase 165's failure set or Phase 166's findings as a v1.34 discovery (CHIP-05/SC#5)."
    - "The WRV record is provably untouched: `EVIDENCE.jsonl`'s expected position count and its non-bring-up row count are byte-identical to the pre-phase snapshot taken in plan 162-01."
    - "Nothing was filed to the issue tracker: an issue count before and after the sweep is captured as **pasted command output**, not as an assertion (D-10, feeding Phase 166's CLOSE-04)."
    - "Everything found and not fixed leaves this phase as a named handoff or a numbered backlog item, never as prose in a closing document."
    - "D-04: no row's baseline is unsourced. Every row's `prior_disposition_source` names a milestone, and the distribution over the ten run parts is counted in `CLOSE.md` and matches the ruling — **2 on v1.34 cell A3/B2 (PD-20), 2 on v1.16, 1 on v1.18, 5 on v1.15, the floor**. A row citing v1.15 for a part a later milestone superseded is a defect, not a choice."
  prohibitions:
    - "No product code. Both sub-repos are byte-unchanged at the end of this phase; both porcelains are empty and both are at their pinned SHAs. **Phase 162 discovers; it does not repair.**"
    - "**No fix for anything this sweep found.** Divergences are classified and fixed in Phase 165, on the v1.33 PR branch, not here and not on v1.34's branch."
    - "**No 2516 run of any kind** — not seated, not read, not written, and no read-only observation. Its absence is an operator-declared hardware fact, and the read-only-observation option is explicitly superseded."
    - "**Do not schedule the 2516 follow-up into Phase 163.** File it as a backlog item and leave it unscheduled — it needs a newer shield **and** further work before it is testable."
    - "**Do not attempt to remove or repair the recurring home-directory configuration change.** Record the recurrence and hand it to Phase 165; the sandbox denies removal and a prior removal attempt is what contaminated the directory."
    - "**No `chip_database.json` edit and no generator edit.** The decode gap found in Wave 0 is filed, not fixed."
    - "**Do not run any GSD roadmap or requirements verb.** Those reformat the whole file and have clobbered a hand-authored dependency table before. Edit the five requirement checkboxes by hand."
    - "No append to `bench/EVIDENCE.jsonl`, and no change to its expected position count."
    - "No `dev test` run of any kind in this plan — the sweep is complete; a further run would break the counting rules."
    - "`run_gates.sh`'s exit code is never read through a pipe."
    - "This plan must not run under `--auto` / `--chain` / any auto-advance mode."
  artifacts:
    - .planning/v1.34/bench/CHIP-EVIDENCE.jsonl
    - .planning/v1.34/bench/CHIP-EVIDENCE.md
    - .planning/v1.34/bench/cells/CHIP/CLOSE.md
    - .planning/v1.34/bench/cells/CHIP/CELL.md
    - .planning/v1.34/bench/cells/CHIP/board_probe_teardown.json
    - .planning/v1.34/bench/cells/CHIP/check_arms_teardown.json
  key_links:
    - "`_schema.close01_counting_rule` and `_schema.chip_sc04_rule` -> the two scripts run here -> Phase 166's CLOSE-01: the reconciliation is an equation this phase evaluates and Phase 166 re-evaluates, never a count either of them performs by eye."
    - "Plan 162-01's `PRE-PHASE.md` snapshot -> this plan's re-run and diff: the cheapest possible proof that the sibling record never leaked into the WRV one."
    - "The issue count before and after -> Phase 166's CLOSE-04, whose criterion explicitly refuses assertions and therefore requires pasted output rather than a code citation."
    - "This phase's diverging positions and their control results -> Phase 165's failure set, **minus** the five known-carried parts, which are handed over as explicitly excluded."
    - "The teardown leave-state -> Phase 163's cell B3: the Rev 2.2 shield goes onto this same Leonardo next, so what is left mounted and seated matters."
---

<objective>
Close the sweep: record the 2516's named absence, prove all five success criteria as arithmetic over
rows, capture the nothing-was-filed evidence as pasted output, hand the divergences and the
non-findings to their downstream owners, tear the cell down, and mark the five requirements.

Purpose: everything this phase produced is only as good as its reconciliation. SC#1's count only
holds if eleven parts are accounted for and the arithmetic is evaluated rather than asserted; SC#4
only holds if the control runs map one-to-one onto the diverging parts and no further; and SC#5 only
holds if the five known-carried reds are labelled here, before Phase 165 inherits a failure set.
Phase 166's CLOSE-01 will re-run these same equations over this same file, so the file has to
survive that unchanged.

Output: the eleventh row, a `CLOSE.md` carrying the four proofs and the handoff lists, the backlog
filings, the teardown records, five requirement checkboxes and a green phase gate.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.34/PROCEDURE.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-01-PLAN.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-CONTEXT.md
@.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-RESEARCH.md
</context>

## Standing bench rule 7 — read before executing

This plan must **not** run under `--auto`, `--chain`, or any auto-advance mode. Its teardown gate is
a real physical handover and the mode is set for the whole phase run, not per plan.

## Shared conventions

`162-05-PLAN.md`'s "Shared conventions for every bench task in this phase" applies here unchanged.
`162-01-PLAN.md`'s planner decisions likewise apply — **PD-10** (the named-absence row's arm field),
**PD-11** (provenance citations belong in this milestone's tooling, never in either sub-repo) and
**PD-12** (counts asserted from the schema, never pinned) bind this plan most directly.

## Requirement completion

**This is the only plan in the phase that may mark a CHIP requirement complete.** Mark all five —
CHIP-01, CHIP-02, CHIP-03, CHIP-04 and CHIP-05 — and only after the four proof scripts in task 2 all
evaluate true. Edit the five checkboxes in `.planning/REQUIREMENTS.md` **by hand**; do not run a GSD
roadmap or requirements verb, because those reformat the whole file and have clobbered a
hand-authored table before. Leave every other requirement untouched.

## Artifacts this phase produces

Created by **this plan**: the eleventh `CHIP-EVIDENCE.jsonl` row (`CHIP__v133__2516`, the named
absence), `bench/cells/CHIP/CLOSE.md`, `bench/cells/CHIP/board_probe_teardown.json`,
`bench/cells/CHIP/check_arms_teardown.json`, and new files under `.planning/todos/` for the backlog
filings. Plus hand edits to the five CHIP checkboxes in `.planning/REQUIREMENTS.md` and the position
line in `.planning/STATE.md`.

<tasks>

<task type="auto">
  <name>Task 1: Record the 2516's named absence and file the three backlog items</name>
  <files>.planning/v1.34/bench/CHIP-EVIDENCE.jsonl, .planning/v1.34/bench/CHIP-EVIDENCE.md, .planning/todos/</files>
  <read_first>
    - `.planning/v1.34/bench/CHIP-EVIDENCE.jsonl` line 1 — `named_absence_convention` and `close01_counting_rule` in full, which together define exactly what this row must look like and how it is counted.
    - `.planning/v1.34/tools/append_chip_evidence.py` — the `--named-absence` branch, its mutual exclusivity with the report arguments, and which fields it fills with the not-measured shape.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-CONTEXT.md` — D-14 in full, including the operator's verbatim declaration and the corroborating context that is recorded but is **not** the reason; and the deferred-ideas entry stating the 2516 is a backlog item and **not** a fold into Phase 163.
    - `.planning/v1.34/bench/cells/CHIP/FM1608-VCC.md` — Wave 0's classification of the generator decode gap as a backlog item, with the two line references it must carry.
    - `.planning/v1.34/bench/cells/CHIP/PREFLIGHT.md` and every position's record — the recurrences of the home-directory configuration change observed during this phase.
    - An existing file under `.planning/todos/pending/` — for the house format of a todo file.
  </read_first>
  <action>
**The eleventh row.** Append the 2516's named absence with `append_chip_evidence.py --named-absence`,
whose reason is the operator's **verbatim** declaration of 2026-08-27 that only shield revision 2.2
and above supports this part and that more work is needed before it can be tested. That is a
**hardware fact of the same class as SC#1's own "adapter absent" example** — not a preference, and
specifically not the weak "we chose not to run it" shape a prior phase rejected. Quote it.

Per the schema's own convention and PD-10, the row's `arm` reads `v133` because that is the arm the
phase was configured under, **not** because a run occurred; its `op` reads that it was not run as a
named absence, and that is the field a reader consults; and every machine-derived field carries the
not-measured shape with its reason on the same line. The row is counted by the close-out rule and is
never counted by the SC#4 rule.

Record the corroborating context **in `anomalies`, explicitly labelled as corroboration and not as
the reason**: the part's small capacity gives it only eight slots, so one masked-slot write would
spend an eighth of the device; its database voltage target is far above anything this shield has
been measured to reach; and its read produced three distinct digests on every recorded attempt
across two prior phases. None of those is why it is absent. The declaration is.

Then re-render with `--check`.

**Three backlog filings**, each a new file under `.planning/todos/pending/` in the house format,
each carrying its own text so a closing document can reference a number instead of restating a
finding:

1. **The 2516 on a newer shield.** It needs shield revision 2.2 or newer **and** further work before
   it is testable. Phase 163 does mount a Rev 2.2 shield on this same board — and the "more work
   first" clause is precisely why this is a backlog item rather than a fold into that phase. **File
   it; do not schedule it.** Record what the further work is understood to be, and cite the
   operator's declaration.
2. **The generator decode gap behind FM1608's declared operating voltage.** A relabel applied to that
   one part changes its recorded type before a later correction tests for the original type, so the
   correction that would substitute the real supply value can no longer reach the one chip the
   relabel touches. Cite both source lines from Wave 0's record. Classify it precisely: it is
   **pre-existing and byte-identical on both arms**, so it is neither a v1.34 finding nor Phase 165's
   work; its only live consequence is a wrong user-facing value in one informational command; and
   the database is **generated**, so any fix belongs in the generator and never in the data file.
3. **The recurring home-directory configuration change.** Record the full recurrence history —
   Phase 161's three cells, the drift measured before this phase began, the re-pin this phase's
   procedure amendment made, and every recurrence observed during this phase's own ten positions —
   with the observation that the content and tree digests have been stable across every recurrence
   and only the modification time moves. State that removal is never attempted and why. Hand it to
   Phase 165 as a standing rig finding, and note that it is a rig fault class rather than a chip
   result.

If the sweep produced evidence bearing on the standing todo about skipping voltage checks when no
programming voltage is used — the sweep's under-voltage warnings exercise exactly that path —
**record the evidence on that existing todo** rather than implementing anything. It remains a
product change and stays out of scope.
  </action>
  <acceptance_criteria>
    - `CHIP-EVIDENCE.jsonl` has exactly one row with `chip == "2516"`, its `named_absence` set to a non-empty reason containing the operator's verbatim declaration, its `arm` equal to `v133` and its `op` stating that it was not run as a named absence.
    - Every machine-derived field on that row is either the not-measured shape with a reason or a legitimately known value; none is blank, null or a bare placeholder — `gate_record.py` accepts the file.
    - The row's `anomalies` records the three corroborating facts and labels them explicitly as corroboration rather than as the reason.
    - The file now holds exactly **eleven** rows whose `arm` is `v133`.
    - Three new files exist under `.planning/todos/pending/`: one for the 2516 on a newer shield, one for the generator decode gap, and one for the recurring home-directory change.
    - The 2516 todo states that it is **not** scheduled into Phase 163 and gives the reason.
    - The generator todo cites both Wave 0 source lines, states that the data file is generated and must not be hand-edited, and classifies the gap as neither a v1.34 finding nor Phase 165 work.
    - The home-directory todo lists every recurrence including this phase's own, states that only the modification time moves, and states that removal is never attempted.
    - `render_chip_evidence.py --check` exits 0.
    - Both sub-repo porcelains are empty.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
J='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl'
lines=open(J).read().splitlines()
sch=json.loads(lines[0])['_schema']
rows=[json.loads(l) for l in lines[1:] if l.strip()]
na=[r for r in rows if r['chip']=='2516']
assert len(na)==1, len(na)
r=na[0]
assert str(r['named_absence']).strip(), 'named_absence is empty'
assert '2.2' in str(r['named_absence']), 'the operator declaration is not quoted'
assert r['arm']=='v133', r['arm']
assert 'not run' in str(r['op']).lower(), r['op']
an=str(r['anomalies']).lower()
assert 'corrobor' in an, 'the corroborating facts are not labelled as corroboration'
prim=[x for x in rows if x['arm']=='v133']
assert len(prim)==11, sorted(x['position_id'] for x in prim)
print('PASS: named absence recorded, 11 primary-arm rows')
print('reason:', str(r['named_absence'])[:180])
"</automated>
    <automated>python3 /workspaces/.planning/v1.34/tools/gate_record.py --jsonl /workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl --pins /workspaces/.planning/v1.34/rig-pins.json
echo "gate_record_rc=$?"
python3 /workspaces/.planning/v1.34/tools/render_chip_evidence.py --jsonl /workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl --target /workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.md --check
echo "render_check_rc=$?"</automated>
    <automated>cd /workspaces
NEW=$(git status --porcelain .planning/todos/ | grep -c '^??' || true)
STAGED=$(git diff --cached --name-only -- .planning/todos/ | wc -l)
echo "untracked_todo_paths=$NEW staged_todo_paths=$STAGED"
ls -1 .planning/todos/pending/ | grep -Ei '2516|build_db|generator|firestarter-config|home-config|mtime' || true
python3 -c "
import glob,os,sys
hits={'2516':[], 'generator':[], 'home':[]}
for p in glob.glob('/workspaces/.planning/todos/pending/*.md'):
    t=open(p).read().lower()
    if '2516' in t and 'rev 2.2' in t: hits['2516'].append(p)
    if 'build_db' in t or 'generator' in t: hits['generator'].append(p)
    if '.firestarter/config.json' in t or 'home-directory configuration' in t: hits['home'].append(p)
for k,v in hits.items():
    assert v, 'no pending todo found for: '+k
    print(k, '->', [os.path.basename(x) for x in v])
print('PASS: three backlog filings present')
"</automated>
    <automated>git -C /workspaces/firestarter status --porcelain
git -C /workspaces/firestarter_app status --porcelain
echo "submodule_porcelain_checked"</automated>
  </verify>
  <done>The eleventh part is accounted for as a named absence with the operator's verbatim hardware reason, its corroborating facts recorded but not mistaken for the reason, and all three unfixed findings are filed as numbered backlog items rather than left as prose.</done>
</task>

<task type="auto">
  <name>Task 2: Prove all five success criteria as arithmetic over rows, and write CLOSE.md</name>
  <files>.planning/v1.34/bench/cells/CHIP/CLOSE.md, .planning/v1.34/bench/CHIP-EVIDENCE.md</files>
  <read_first>
    - `.planning/v1.34/bench/CHIP-EVIDENCE.jsonl` line 1 — `close01_counting_rule`, `chip_sc04_rule`, `control_rerun_exclusion` and `named_absence_convention`, which are the equations this task evaluates.
    - `.planning/v1.34/bench/cells/CHIP/PRE-PHASE.md` — the pre-phase WRV snapshot figures and the issue-count "before" output, both needed for the diffs here.
    - `.planning/ROADMAP.md` §"Phase 162" — Success Criteria 1 through 5 as written, so each proof is stated against its own criterion; and §"Phase 166", so the handoffs name what CLOSE-01 and CLOSE-04 will need.
    - `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-CONTEXT.md` — D-06's arithmetic reading and the deferred list, which fixes what may and may not be handed to Phase 165.
    - `.planning/v1.34/bench/cells/CHIP/CELL.md` — the ten measured totals and the physical-action counts, summarised here.
  </read_first>
  <action>
Write `bench/cells/CHIP/CLOSE.md`, carrying **five proofs, two handoffs and one honesty section**.
Every proof is the **output of a script over the rows**, pasted verbatim — never a claim about the
rows.

**Proof 1 — SC#1.** Evaluate the close-out counting rule: over primary-arm rows only, validated plus
skipped-with-reason equals the expected position count of eleven. State the reconciliation in words
on the same line as the numbers: **ten reports plus one named absence equals eleven parts**.

**Proof 2 — SC#2.** Assert that no row in the file carries a null firmware identity, and that every
non-absence row's identity is **equal to** the value the pre-flight datum recorded in `PREFLIGHT.md`
for the v1.33 arm. Equality is the criterion, not merely non-nullness: SC#2 says the identity must
match the arm's recorded firmware identity. If any control row's identity differs from the primary
rows', that is a finding in its own right — the arms are different firmware builds, so a difference
may be legitimate; state which it is rather than asserting either way.

**Proof 3 — SC#3.** Assert that every row's divergence verdict is exactly `same` or begins with the
diverging prefix followed by a non-empty description; that no row has an empty prior disposition or
an empty prior-disposition source; and that no cell of the rendered divergence table is blank — a
not-measured-with-reason string is a value, a blank is not. Paste the rendered divergence table into
`CLOSE.md` as well, so the criterion's "one table" is visible in one place.

**Proof 4 — SC#4.** Evaluate the SC#4 rule as its three conjoined identities: the control-row count
equals the count of primary rows whose verdict begins with the diverging prefix; every control row's
arbitration target names an existing diverging primary row; and no two control rows name the same
one. Then state the total as **`10 + N`** with N substituted and the arithmetic shown, and **name
the deviation from the roadmap's `11 + N` on the same line**, reading it as `10 + N` per D-06 and
D-14 because the eleventh part is a named absence rather than a run. Expect N to be at least four —
the two ultraviolet parts, the locked-boot-block part and the different-family part were all booked
before the bench.

**Proof 5 — SC#5.** Assert that all five known-carried parts — both stuck-bit parts, the
locked-boot-block part, the non-deterministic part and the byte-zero-defect part — have a
`known_carried` value that is not the literal `no` and a non-empty prior disposition, and that each
one's citation resolves to a real line in the file it names. Then state explicitly, in words, that
their reds are **excluded** from this phase's findings and from the failure set handed to Phase 165.

**Two diffs, both pasted.** Re-run plan 162-01's WRV snapshot script and diff its output against the
pre-phase output recorded in `PRE-PHASE.md`; the expected position count and the non-bring-up row
count must be **identical**, proving the sibling file never leaked into the WRV record. And re-run
the issue-count command against the tracker and paste the **after** output beside the **before**
output from `PRE-PHASE.md`. Phase 166's CLOSE-04 explicitly refuses assertions, and a code citation
*is* an assertion — so this is pasted command output or it is nothing. Alongside it, record what the
sweep actually did on that path: the duplicate-check query ran on every position, its outcome was
recorded per row, and the submission path builds a **prefilled new-issue link** rather than creating
an issue when there is no terminal attached. If the query could not run, that is a recorded
first-class value and the counts still stand as the proof.

**Handoff 1 — to Phase 165.** List every diverging position with its verdict text and its control
result, and state for each whether the control arm agreed or differed. **Exclude the five
known-carried parts explicitly and by name**, with one line each saying why. Also hand over the
recurring home-directory finding as a rig fault, referencing its backlog number. Do **not** classify
anything as caused-or-pre-existing here — that is Phase 165's requirement, and pre-empting it would
be exactly the assumption its own criteria forbid.

**Handoff 2 — to Phase 166.** Name what CLOSE-01 needs from this file: the two counting rules, the
expected position count of eleven, and the one structural warning — that this is the only
milestone-evidence record in the project whose full-device digest column is a **named absence**,
because the command under test retains no read image, so a uniform locked-core assertion must accept
the not-measured shape there. Name what CLOSE-04 needs: the pasted before-and-after issue counts.
And name the three backlog numbers filed in task 1.

**The honesty section.** State plainly what this phase did **not** measure or prove: every voltage
figure in the record is an **idle** firmware sample or an operator meter reading, never a
program-window measurement, so v1.34 makes **no electrical claim** from this sweep; the read bytes of
every `dev test` run are destroyed with the step, so no read digest exists for any position and the
only read-stability evidence is the two-run divergence metric plus any follow-up that fired; the
standing read-instability question is **not** closed and any data points recorded against it are
data points only; the two ultraviolet parts' control re-runs landed on a **different slot** from
their primaries and cannot arbitrate arm against silicon; and the locked-boot-block part's known
failure address is never written by this command on either arm, so nothing here bears on it.

Finally, update `CELL.md` with a one-screen summary: the ten measured totals, the total machine
time against the roughly two-hour budget, the ceiling-fire count, and the actual counts of chip
handlings, JP4 changes and pot moves against the planned nine, two and one.
  </action>
  <acceptance_criteria>
    - `CLOSE.md` exists and contains five clearly labelled proof sections, each carrying **pasted script output**, not prose claims.
    - The close-out counting rule evaluates true over primary-arm rows against the expected count of eleven, and the words "ten reports plus one named absence" appear beside the numbers.
    - No row in the file has a null firmware identity, and every non-absence row's identity equals the pre-flight value for its arm — or, where a control row differs, `CLOSE.md` states whether that difference is legitimate.
    - Every row's divergence verdict is exactly `same` or a non-empty diverging string; no row has an empty prior disposition or prior-disposition source.
    - The rendered divergence table is pasted into `CLOSE.md` and has no blank cell.
    - The SC#4 three-part identity evaluates true, and `CLOSE.md` contains the literal `10 + N` with N substituted and `11 + N` named on the same line.
    - All five known-carried parts have a `known_carried` value other than the literal `no` and a non-empty prior disposition, and `CLOSE.md` names them and states their exclusion from the Phase 165 failure set.
    - `CLOSE.md` contains the pasted WRV snapshot output, and it matches `PRE-PHASE.md`'s figures exactly.
    - `CLOSE.md` contains the pasted issue-count output for **both** before and after, or pasted evidence that the query could not run.
    - `CLOSE.md` contains a Phase 165 handoff naming every diverging position, and a Phase 166 handoff naming the two counting rules, the expected count, the named-absence warning about the digest column, and the three backlog numbers.
    - `CLOSE.md` contains an honesty section carrying all five non-claims listed in the action.
    - `CELL.md` carries the ten measured totals, the machine time against budget, the ceiling-fire count, and the three physical-action counts against plan.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
J='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl'
lines=open(J).read().splitlines()
sch=json.loads(lines[0])['_schema']
rows=[json.loads(l) for l in lines[1:] if l.strip()]
primary=sch['primary_arm']
prim=[r for r in rows if r['arm']==primary]
ctl=[r for r in rows if r['arm']!=primary]
val=sum(1 for r in prim if r['outcome']=='validated')
skip=sum(1 for r in prim if r['outcome']=='skipped-with-reason')
exp=sch['position_count_expected']
print('SC#1: %d validated + %d skipped-with-reason = %d of %d expected' % (val,skip,val+skip,exp))
assert val+skip==exp, (val,skip,exp)
reports=len([r for r in prim if not str(r.get('named_absence','')).strip() or str(r.get('named_absence')).lower().startswith('not applicable')])
absences=len(prim)-reports
print('SC#1 reconciliation: %d reports + %d named absence = %d parts' % (reports,absences,reports+absences))
assert reports==10 and absences==1, (reports,absences)
print('PASS SC#1')
"</automated>
    <automated>python3 -c "
import json,re
J='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl'
rows=[json.loads(l) for l in open(J).read().splitlines()[1:] if l.strip()]
pre=open('/workspaces/.planning/v1.34/bench/cells/CHIP/PREFLIGHT.md').read()
m=re.search(r'fw_board_identity[^\n]*', pre)
print('pre-flight line:', m.group(0) if m else 'MISSING')
bad=[r['position_id'] for r in rows if not str(r.get('named_absence','')).strip().lower().startswith(('','not applicable')) is None]
nulls=[r['position_id'] for r in rows if r['fw_board_identity'] in (None,'','null','None')]
assert not nulls, ('SC#2 violated: null fw_board_identity on', nulls)
ids={}
for r in rows:
    ids.setdefault(r['arm'], set()).add(str(r['fw_board_identity']))
for arm,vals in ids.items():
    print('arm', arm, 'distinct fw_board_identity values:', sorted(vals))
    assert len(vals)==1, ('SC#2: an arm reported more than one firmware identity', arm, sorted(vals))
print('PASS SC#2: no nulls, one identity per arm')
"</automated>
    <automated>python3 -c "
import json
J='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl'
rows=[json.loads(l) for l in open(J).read().splitlines()[1:] if l.strip()]
bad=[]
for r in rows:
    dv=str(r['divergence_verdict'])
    if not (dv=='same' or (dv.startswith('diverges:') and dv[len('diverges:'):].strip())):
        bad.append((r['position_id'],'verdict',dv))
    for k in ('prior_disposition','prior_disposition_source','prior_dispositions_all'):
        if not str(r[k]).strip():
            bad.append((r['position_id'],k,r[k]))
assert not bad, bad
print('SC#3: all', len(rows), 'rows carry an explicit verdict and a sourced prior disposition')
for r in rows:
    print(' ', r['chip'].ljust(12), r['arm'].ljust(8), str(r['divergence_verdict'])[:80])
print('PASS SC#3')
"</automated>
    <automated>python3 -c "
import json
J='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl'
lines=open(J).read().splitlines()
sch=json.loads(lines[0])['_schema']
rows=[json.loads(l) for l in lines[1:] if l.strip()]
primary=sch['primary_arm']
prim=[r for r in rows if r['arm']==primary]
ctl=[r for r in rows if r['arm']!=primary]
div=[r for r in prim if str(r['divergence_verdict']).startswith('diverges')]
same=[r for r in prim if r['divergence_verdict']=='same']
assert len(ctl)==len(div), ('SC#4 count identity', len(ctl), len(div))
targets=[c['control_rerun_for'] for c in ctl]
assert len(set(targets))==len(targets), ('SC#4 double arbitration', targets)
divids={r['position_id'] for r in div}
assert set(targets)<=divids, ('SC#4 orphan arbitration', set(targets)-divids)
sameids={r['position_id'] for r in same}
assert not (set(targets) & sameids), ('SC#4: a same row was arbitrated', set(targets) & sameids)
N=len(ctl)
print('SC#4: 10 + N where N = %d  ->  10 + %d = %d total runs' % (N,N,10+N))
print('SC#4: the roadmap states 11 + N; read as 10 + N per D-06/D-14 because the eleventh part is a named absence, not a run')
print('diverging:', sorted(divids))
print('same     :', sorted(sameids))
print('PASS SC#4')
"</automated>
    <automated>python3 -c "
import json
J='/workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl'
rows=[json.loads(l) for l in open(J).read().splitlines()[1:] if l.strip()]
by={(r['chip'],r['arm']):r for r in rows}
need=['w27e512','w27e040','w29c040','am27c020','fm1608']
bad=[]
for slug in need:
    r=by.get((slug,'v133'))
    assert r is not None, slug
    if str(r['known_carried']).strip().lower()=='no': bad.append((slug,'known_carried is no'))
    if not str(r['prior_disposition']).strip(): bad.append((slug,'empty prior_disposition'))
assert not bad, bad
print('SC#5: all five known-carried parts labelled with a cited prior disposition')
for slug in need:
    print(' ', slug.ljust(12), str(by[(slug,'v133')]['known_carried'])[:100])
print('PASS SC#5')
"</automated>
    <automated>python3 -c "
import json
lines=open('/workspaces/.planning/v1.34/bench/EVIDENCE.jsonl').read().splitlines()
sch=json.loads(lines[0])['_schema']
rows=[json.loads(l) for l in lines[1:] if l.strip()]
nb=[r for r in rows if not str(r.get('cell_id','')).startswith(sch['bringup_cell_id_prefix'])]
print('position_count_expected =', sch['position_count_expected'])
print('non-bringup rows        =', len(nb))
assert sch['position_count_expected']==20
assert len(nb)==12, len(nb)
print('PASS: WRV record identical to the pre-phase snapshot')
"
git -C /workspaces diff --quiet -- .planning/v1.34/bench/EVIDENCE.jsonl .planning/v1.34/bench/EVIDENCE.md
echo "WRV files byte-unchanged rc=$?"</automated>
    <automated>gh issue list --repo henols/firestarter_prom --state all --limit 1000 --json number 2>/tmp/162-10-gh.err | python3 -c "import sys,json;print('issue count:',len(json.load(sys.stdin)))" || cat /tmp/162-10-gh.err
echo "--- paste the line above into CLOSE.md verbatim as the AFTER count ---"</automated>
    <automated>python3 -c "
p='/workspaces/.planning/v1.34/bench/cells/CHIP/CLOSE.md'
t=open(p).read(); low=t.lower()
for frag in ('sc#1','sc#2','sc#3','sc#4','sc#5'):
    assert frag in low, frag
assert '10 + N' in t and '11 + N' in t
assert 'issue count' in low, 'the pasted issue counts are missing'
assert 'position_count_expected' in t, 'the pasted WRV snapshot is missing'
assert 'phase 165' in low and 'phase 166' in low, 'a handoff section is missing'
assert 'no electrical claim' in low, 'the honesty section is missing its electrical non-claim'
assert 'known-carried' in low or 'known_carried' in low
print('PASS: CLOSE.md carries five proofs, both diffs, both handoffs and the honesty section')
"</automated>
  </verify>
  <done>All five success criteria are evaluated as arithmetic over rows with the output pasted rather than claimed, the WRV record is provably untouched, the nothing-was-filed evidence is pasted output, and both downstream phases have a named handoff instead of prose.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Teardown — take the last part out and declare the leave-state for Phase 163</name>
  <what-built>
    The sweep is complete: ten runs, one named absence, all arbitrations done, all five success
    criteria proven as arithmetic over rows, three backlog items filed and both downstream handoffs
    written. Nothing has been fixed and no product code changed.
  </what-built>
  <how-to-verify>
    Final physical step of the phase.

    1. Remove the **ST M27C512** (DIP28) and put it away. Both ultraviolet parts have one small
       region spent each; that is recorded and expected.
    2. Leave the **Leonardo connected** and the **Rev 2.0 shield mounted** for now, with the socket
       **empty**.
    3. **Do not touch the pot.** Its current higher setting and both of its readings are recorded,
       and the next phase will set its own.
    4. Report the state you are leaving the bench in: which board, which shield, whether the socket
       is empty, and the JP4 position.

    What happens next, so the leave-state makes sense: Phase 163 puts a **different shield** on this
    same board, so the Rev 2.0 shield comes off then rather than now, and its removal belongs to that
    phase's own mount step where the revision is declared fresh.
  </how-to-verify>
  <action>
Present the gate. On resume, record the operator's declaration verbatim in `CELL.md` as the
cell-agnostic leave-state: board, port, arm on the board, chip seated or socket empty, pot setting
and shield revision.

Then complete the teardown per the procedure's teardown step as retargeted by Amendment 4: re-run
the board probe to a **distinct** path so the pre-cell probe is not overwritten, re-run the arm check
to its own teardown path, perform the **two-assertion** configuration-directory check in order, and
run the completeness assertion — every position of this cell has a row in `CHIP-EVIDENCE.jsonl` and
the render check is green.

Assertion (1) is the home-directory check against the baseline the Amendment-4 re-pin established.
Compare all four values. A change to any of them is this phase's own recorded finding: record the
recurrence with its numbers and hand it to Phase 165 under the backlog item filed in task 1.
**Never attempt removal.**

Assertion (2) is the frozen configuration directory's digest against its pinned value. This one is
now load-bearing for the copy-out invariant rather than merely confirmatory: a mismatch means a
report was left behind by some position, and that position's recorded digest is wrong.

This gate is **never** auto-approvable.
  </action>
  <resume-signal>Reply with the leave-state: board, shield, socket empty or not, and the JP4 position.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Phase gate — full suite, requirement checkboxes, state, and the commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/v1.34/bench/cells/CHIP/CELL.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the CHIP section, so exactly five checkboxes change and no other line moves.
    - `.planning/STATE.md` — the Current Position block, and the note in the project record that the state readers scrape the **body** rather than the frontmatter and that the position line must stay a single line.
    - `.planning/v1.34/tools/run_gates.sh` — the summary block and its documented exit codes.
    - `.planning/v1.34/bench/cells/CHIP/CLOSE.md` — the five proofs, all of which must have evaluated true before any checkbox is marked.
  </read_first>
  <action>
Run the **full** gate suite and capture the exit code on the very next line — never through a pipe,
because the script sets a strict pipeline mode and a pipe would hand you the last stage's status.
Expect **14 tool selftests and 7 live gates, exit 0**. Both new record gates now have eleven rows
plus arbitrations to check rather than a schema-only file, so this run is the first time the sibling
record is gated at full size.

Confirm the tool count is exactly fourteen `.py` files under the tools directory — fifteen would
mean a scratch file was left behind and would have inflated the selftest count.

Confirm both sub-repos are byte-unchanged and at their pinned SHAs: an empty porcelain in each, and
each repository's head equal to the pinned firmware and application SHAs for the arm that should be
on the board. The v1.33 arm is the one left flashed, because the last arbitration re-flashed back to
it under its own read-back proof.

**Mark the five CHIP requirement checkboxes** in `.planning/REQUIREMENTS.md`, by hand, and only after
confirming that all five proofs in `CLOSE.md` evaluated true. Do not run a GSD roadmap or
requirements verb — those reformat the entire file and have clobbered a hand-authored table before.
Change exactly five characters' worth of content and leave every other requirement untouched.

Update `.planning/STATE.md`'s Current Position to record that Phase 162 is complete, keeping that
line a **single line** because the state reader scrapes the document body rather than the
frontmatter and a wrapped line breaks it.

Finally, snapshot `ROADMAP.md` before any edit and diff it afterwards. If a tool has to touch it,
confirm the diff is confined to this phase's own entry — a known defect in the progress-update path
overwrites a neighbouring phase's plan line positionally, and another has been observed switching
branches from scraped prose. **Check the current branch after any versioned commit helper runs.**

Commit everything this phase produced as one final change, with a message naming the phase, the ten
runs, the named absence and the arbitration count.
  </action>
  <acceptance_criteria>
    - `bash .planning/v1.34/tools/run_gates.sh` exits **0** with its code captured directly, reporting **14** tool selftests and **7** live gates.
    - `ls .planning/v1.34/tools/*.py | wc -l` is `14`.
    - Both sub-repo porcelains are empty, and each repository's head equals the pinned SHA for the arm left on the board.
    - Exactly five checkboxes changed in `.planning/REQUIREMENTS.md` — CHIP-01 through CHIP-05 — and the diff touches no other requirement line.
    - `.planning/STATE.md`'s Current Position records Phase 162 as complete on a single line.
    - `git diff` on `ROADMAP.md` is either empty or confined to this phase's own entry; no other phase's plan line or requirement list moved.
    - `git branch --show-current` is still the v1.34 milestone branch after every commit helper invocation.
    - `bench/cells/CHIP/board_probe_teardown.json` and `check_arms_teardown.json` exist at paths distinct from their pre-cell counterparts, and neither pre-cell file was overwritten.
    - The frozen configuration directory's live digest equals its pinned value and the directory contains no reports entry.
    - `CELL.md` carries the leave-state declaration verbatim and the closing summary figures.
    - `render_chip_evidence.py --check` and `gate_record.py --jsonl CHIP-EVIDENCE.jsonl` both exit 0 against the full eleven-row file.
  </acceptance_criteria>
  <verify>
    <automated>bash /workspaces/.planning/v1.34/tools/run_gates.sh > /tmp/162-10-gates.out 2> /tmp/162-10-gates.err
RC=$?
echo "run_gates_rc=$RC"
python3 -c "
t=open('/tmp/162-10-gates.out').read()
n=t.count('selftest PASS'); g=t.count('live gate PASS')
print('selftests',n,'live gates',g)
assert n==14 and g==7, (n,g)
assert 'append_chip_evidence.py' in t and 'render_chip_evidence.py' in t
print('PASS: 14/14 selftests and 7/7 live gates over the full record')
"
test "$RC" -eq 0
ls /workspaces/.planning/v1.34/tools/*.py | wc -l</automated>
    <automated>python3 -c "
import json,hashlib,pathlib,os
pins=json.load(open('/workspaces/.planning/v1.34/rig-pins.json'))
root=pathlib.Path(pins['config_dir'])
files=sorted(x.relative_to(root).as_posix() for x in root.rglob('*') if x.is_file())
h=hashlib.sha256()
for f in sorted(x for x in root.rglob('*') if x.is_file()):
    h.update(f.relative_to(root).as_posix().encode()); h.update(f.read_bytes())
print('config dir files:', files)
print('config_dir_sha  :', h.hexdigest())
assert not any(f.startswith('reports/') for f in files), 'a dev test report was left inside the frozen config dir'
C='/workspaces/.planning/v1.34/bench/cells/CHIP'
for a,b in (('board_probe.json','board_probe_teardown.json'), ('check_arms_pre_cell.json','check_arms_teardown.json')):
    assert os.path.isfile(C+'/'+a) and os.path.isfile(C+'/'+b), (a,b)
    assert open(C+'/'+a,'rb').read()!=open(C+'/'+b,'rb').read(), 'the teardown probe overwrote its pre-cell counterpart'
print('PASS: frozen dir clean, teardown probes written to distinct paths')
"</automated>
    <automated>python3 /workspaces/.planning/v1.34/tools/render_chip_evidence.py --jsonl /workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl --target /workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.md --check
echo "render_check_rc=$?"
python3 /workspaces/.planning/v1.34/tools/gate_record.py --jsonl /workspaces/.planning/v1.34/bench/CHIP-EVIDENCE.jsonl --pins /workspaces/.planning/v1.34/rig-pins.json
echo "gate_record_rc=$?"</automated>
    <automated>cd /workspaces
python3 -c "
import subprocess,re
old=subprocess.run(['git','show','HEAD:.planning/REQUIREMENTS.md'],capture_output=True,text=True).stdout.splitlines()
new=open('.planning/REQUIREMENTS.md').read().splitlines()
assert len(old)==len(new), ('line count changed -- a formatting verb was run', len(old), len(new))
diff=[(i+1,a,b) for i,(a,b) in enumerate(zip(old,new)) if a!=b]
for n,a,b in diff:
    print('line',n)
    print('  -',a[:120])
    print('  +',b[:120])
assert len(diff)==5, ('expected exactly five changed lines, got', len(diff))
for n,a,b in diff:
    assert re.search(r'\*\*CHIP-0[1-5]\*\*', b), ('a non-CHIP requirement line changed', n, b[:160])
    assert a.replace('[ ]','[x]',1)==b, ('the change is not a bare checkbox flip', n)
print('PASS: exactly five CHIP checkboxes flipped, no other line touched')
"</automated>
    <automated>cd /workspaces
git diff --stat -- .planning/ROADMAP.md
python3 - <<'PY'
import re, subprocess, pathlib
def sections(text):
    out, cur, buf = {}, None, []
    for line in text.splitlines():
        m = re.match(r'^### Phase (\S+?):', line)
        if m:
            if cur is not None:
                out[cur] = '\n'.join(buf)
            cur, buf = m.group(1), [line]
        elif cur is not None:
            buf.append(line)
    if cur is not None:
        out[cur] = '\n'.join(buf)
    return out
old = subprocess.run(['git','-C','/workspaces','show','HEAD:.planning/ROADMAP.md'],
                     capture_output=True, text=True).stdout
new = pathlib.Path('/workspaces/.planning/ROADMAP.md').read_text()
if old == new:
    print('PASS: ROADMAP.md untouched')
else:
    a, b = sections(old), sections(new)
    assert set(a) == set(b), ('a phase entry appeared or disappeared', sorted(set(a) ^ set(b)))
    changed = sorted(k for k in a if a[k] != b[k])
    print('changed phase entries:', changed)
    assert changed == ['162'], ('ROADMAP.md changed outside the Phase 162 entry:', changed)
    print('PASS: ROADMAP.md diff confined to the Phase 162 entry')
PY
echo "current branch: $(git -C /workspaces branch --show-current)"
case "$(git -C /workspaces branch --show-current)" in gsd/v1.34*) echo "PASS: still on the v1.34 milestone branch";; *) echo "FAIL: branch switched"; exit 1;; esac

    </automated>
    <automated>cd /workspaces
git -C /workspaces/firestarter status --porcelain
git -C /workspaces/firestarter_app status --porcelain
python3 -c "
import json,subprocess
pins=json.load(open('/workspaces/.planning/v1.34/rig-pins.json'))
fw=subprocess.run(['git','-C','/workspaces/firestarter','rev-parse','HEAD'],capture_output=True,text=True).stdout.strip()
app=subprocess.run(['git','-C','/workspaces/.v1.34-arms/v133','rev-parse','HEAD'],capture_output=True,text=True).stdout.strip()
print('firmware HEAD :', fw, '  pinned v133 fw_sha :', pins['arms']['v133']['fw_sha'])
print('v133 arm HEAD :', app, '  pinned v133 app_sha:', pins['arms']['v133']['app_sha'])
assert fw==pins['arms']['v133']['fw_sha'], 'the firmware repo is not at the arm left on the board'
print('PASS: the arm left flashed matches the pinned firmware SHA')
"
grep -n 'Phase 162' .planning/STATE.md | head -3</automated>
  </verify>
  <done>The suite is green at 14/14 selftests and 7/7 live gates over the full eleven-row record, both sub-repos are byte-unchanged and at their pinned SHAs, exactly five requirement checkboxes flipped by hand, and the branch is unchanged after every commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a reader's count -> a success criterion | a criterion counted by eye is not a criterion; each one here is an equation over rows |
| this phase's findings -> Phase 165's failure set | five reds are already on record and must be handed over as excluded, not inherited as new |
| a code citation -> a close-out claim | one downstream criterion explicitly refuses assertions, so only pasted command output discharges it |
| a formatting verb -> a hand-authored planning document | a whole-file reformat has clobbered a hand-authored table before |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-51 | Repudiation | a success criterion asserted rather than evaluated | high | mitigate | all five proofs are scripts over the rows whose output is pasted into the close record; the schema's own counting rules are the equations |
| T-162-52 | Repudiation | a known-carried red entering the downstream failure set | high | mitigate | the five parts are asserted labelled and cited before any checkbox is marked, and the handoff excludes them by name with a reason each |
| T-162-53 | Repudiation | the sibling record leaking into the write-read-verify record | high | mitigate | the pre-phase snapshot is re-run and diffed, and both WRV files are asserted byte-unchanged |
| T-162-54 | Information disclosure | an issue accidentally filed to the public tracker | medium | mitigate | counts captured before and after as pasted output; the submission path builds a prefilled link rather than creating an issue with no terminal attached; each position's query outcome is recorded on its own row |
| T-162-55 | Tampering | a formatting verb reflowing a hand-authored planning file | high | mitigate | prohibited outright; the checkbox edit is asserted to change exactly five lines with the line count unchanged; the roadmap diff is asserted confined and the branch is re-checked after every commit |
| T-162-56 | Tampering | a teardown probe overwriting its pre-cell counterpart | medium | mitigate | the two probe outputs are written to distinct paths and asserted to differ, so the before-and-after pair survives |
| T-162-SC | Tampering | package installs | low | accept | no package manager runs anywhere in this phase; the arms are pre-built and pinned |
</threat_model>

<verification>
- The close-out counting rule evaluates true over primary-arm rows against the expected count of eleven, with the reconciliation stated as ten reports plus one named absence.
- No row carries a null firmware identity, and each arm reports exactly one identity across all its rows.
- Every row's divergence verdict is exactly `same` or a non-empty diverging string, and no row has an empty prior disposition or prior-disposition source.
- The three-part SC#4 identity evaluates true, and the close record states the total as `10 + N` with the roadmap's `11 + N` named on the same line.
- All five known-carried parts are labelled and cited, and the close record excludes them from the downstream failure set by name.
- The WRV record's expected count and non-bring-up row count match the pre-phase snapshot exactly, and both WRV files are byte-unchanged.
- The issue counts before and after are present in the close record as pasted command output.
- `bash .planning/v1.34/tools/run_gates.sh` exits 0 at 14/14 selftests and 7/7 live gates, read directly, over the full record.
- The frozen configuration directory is clean and at its pinned digest; the two teardown probes are at distinct paths from their pre-cell counterparts.
- Exactly five requirement checkboxes changed; the roadmap diff is confined or empty; the branch is unchanged; both sub-repos are byte-unchanged and at their pinned SHAs.
</verification>

<success_criteria>
Phase 162 is closed. Eleven parts are accounted for as arithmetic over rows, every report is
firmware-attributable, one table carries every result beside a sourced prior disposition with an
explicit divergence verdict in every row, the control re-runs map one-to-one onto the diverging
parts and onto no others, and all five known-carried reds are labelled and excluded from what
Phase 165 inherits. Nothing was fixed, nothing was filed to the tracker, no product code changed,
and everything found and not fixed left as a numbered backlog item or a named handoff.
</success_criteria>

<output>
Create `.planning/phases/162-chip-11-part-dev-test-sweep-on-the-reference-rig/162-10-SUMMARY.md`
when done. Record: the named-absence row's reason verbatim; all five proof outputs pasted; the final
`10 + N` arithmetic with N substituted and every diverging position named; the five known-carried
parts with their citations; the pasted before-and-after issue counts; the WRV snapshot diff; the
three backlog filings with their file names; both handoff lists as written; the honesty section's
five non-claims; the leave-state declaration verbatim; the ten measured totals against the budget
with the physical-action counts; and the final gate counts and exit code.
</output>
