---
phase: 160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.34/rig-pins.json
  - .planning/v1.34/README.md
  - .planning/v1.34/config/.gitkeep
  - .planning/v1.34/bench/cells/.gitkeep
  - .planning/v1.34/arms-provenance.json
  - .gitignore
autonomous: false
requirements: [RIG-01, RIG-02]
tags: [bench, provenance, avrdude, worktree, venv]

must_haves:
  truths:
    - "The four source SHAs are re-read at execution time from `gh pr view 56/54 --json headRefOid` and `git merge-base`, and match fw control `8695ee5` / fw v133 `5759dc8` / app control `6bfa645` / app v133 `cb189a9` — or execution stops loudly."
    - "D-16: every rig artifact this milestone produces lives under `.planning/v1.34/`, and the directory skeleton (`images/`, `tools/`, `config/`, `bench/cells/`) exists before any tool is authored."
    - "D-06: two git worktrees of `firestarter_app` exist at `/workspaces/.v1.34-arms/{control,v133}`, each with its own `.venv` and its own editable install, so the arm is named by the invoked absolute binary path."
    - "D-08: all three host-arm legs are observed on both arms — `git rev-parse HEAD` names the arm, `git status --porcelain` is empty, and `python -P -c 'import firestarter; print(firestarter.__file__)'` resolves into that worktree."
    - "D-07: one frozen shared `FIRESTARTER_CONFIG_DIR` exists at `.planning/v1.34/config/`, its content SHA is computed and recorded, and both arms are launched with it set at process launch."
    - "D-09: both arms run on one interpreter (devcontainer Python 3.12.14), that version is recorded once, and the fact that it is not the py3.11 app-CI floor is written down as a Phase 166 honesty-ledger line."
    - "Pitfall 8: the two arm venvs' `pip freeze` outputs (excluding `firestarter` itself) are diffed and the diff is empty, so the dependency set is not a second A/B variable."
    - "`rig-pins.json` is the single machine-readable pin record for the milestone: the four SHAs, the arms root, the pinned avrdude binary + conf, the `avr-objcopy` path, the PlatformIO project dir, `UV_CACHE_DIR`, the shared config dir, the interpreter, the five toolchain versions, the three target parameter sets, the two chip fact sets, and the forbidden flag / forbidden binary lists."
  prohibitions:
    - "D-06: bare `firestarter` is never invoked on the bench — a user-site editable install at `/home/vscode/.local/lib/python3.12/site-packages` makes it a third, un-named arm, so every bench command uses an arm venv's absolute binary path."
    - "No arm-identification mechanism may rest on a version string, a firmware handshake, or `fw_board_identity`: both arms self-report fw `3.0.0b22` and app `3.0.0b32`, so all three are provably blind to the arm."
    - "D-16: no file inside `firestarter/` or `firestarter_app/` is created or modified, and this plan creates no sub-repo commit, branch or push — a detached worktree checkout is the only sub-repo mutation permitted."
    - "The stale `tool-avrdude@1.60300.200527` (avrdude 6.3) is never used and is recorded in `rig-pins.json` as a forbidden binary — it predates the `urclock` programmer entirely."
  artifacts:
    - .planning/v1.34/rig-pins.json
    - .planning/v1.34/arms-provenance.json
    - .planning/v1.34/README.md
    - /workspaces/.v1.34-arms/control/.venv/bin/firestarter
    - /workspaces/.v1.34-arms/v133/.venv/bin/firestarter
  key_links:
    - "`rig-pins.json` → every tool authored in plans 03/04/05 reads its paths and per-target parameters from it and fails closed when it is absent."
    - "`FIRESTARTER_CONFIG_DIR` → `firestarter_app/firestarter/config.py:25` resolves it at IMPORT time, so it must be set at process launch, never after."
    - "`python -P` → `/workspaces/firestarter` is a PEP 420 namespace-package shadow of the app package; without `-P` the `__file__` probe yields `None` on every interpreter."
---

<objective>
Create the `.planning/v1.34/` rig skeleton, pin every rig constant in one machine-readable
record, and stand up the two host arms (D-06) with their proof triple observed (D-08), one
frozen shared config dir (D-07), one interpreter (D-09) and an empty dependency diff
(Pitfall 8).

Purpose: nothing else in this phase can be authored deterministically until the paths,
SHAs, avrdude pin and per-target parameters exist in one place, and until the two host arms
are callable by absolute path. This plan is what makes "the arm is in the command line"
literally true.

Output: `.planning/v1.34/{rig-pins.json,README.md,config/,bench/cells/}`,
`.planning/v1.34/arms-provenance.json`, two worktrees + two venvs at
`/workspaces/.v1.34-arms/`, and one `.gitignore` line.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-CONTEXT.md
@.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-RESEARCH.md
</context>

## Requirement completion

- **RIG-01** — this plan closes only the *named source state* half: the four SHAs verified
  and pinned, and the two host arms provably at those SHAs. The six firmware images land in
  plan 02; the on-device read-back confirmation lands in plans 08/09/10.
- **RIG-02** — this plan closes only the *firmware SHA + host app SHA* fields of the
  provenance block. Board signature, `controller:` string and shield revision land in
  plan 04 (tool) and plan 11 (live capture).
- RIG-03 lands in plan 06. RIG-04 lands in plans 03 and 12. RIG-05 lands in plans 04, 07
  and 13.

## Artifacts this phase produces

Created by **this plan** (new symbols — none of these exists in the tree yet):

- Files: `.planning/v1.34/rig-pins.json`, `.planning/v1.34/README.md`,
  `.planning/v1.34/arms-provenance.json`, `.planning/v1.34/config/.gitkeep`,
  `.planning/v1.34/bench/cells/.gitkeep`; directories `.planning/v1.34/images/`,
  `.planning/v1.34/tools/`, `.planning/v1.34/bench/`.
- Out-of-tree paths: `/workspaces/.v1.34-arms/control`, `/workspaces/.v1.34-arms/v133`,
  each with `.venv/bin/firestarter` and `.venv/bin/python`;
  `/workspaces/.v1.34-arms/.uv-cache`.
- `.gitignore` entry: `/.v1.34-arms/`.
- `rig-pins.json` top-level keys: `schema_version`, `milestone`, `phase`, `arms_root`,
  `firmware_repo`, `app_repo`, `arms`, `avrdude`, `avrdude_fallback`,
  `forbidden_binaries`, `objcopy`, `pio_binary`, `pio_project_dir`, `uv_cache_dir`,
  `config_dir`, `interpreter`, `toolchain`, `targets`, `chips`, `forbidden_flags`,
  `forbidden_argv0`, `image_naming`, `tool_conventions`.
- `rig-pins.json` `arms.<control|v133>` keys: `fw_sha`, `app_sha`, `worktree`, `venv_bin`,
  `venv_python`.
- `rig-pins.json` `targets.<uno|uno328pb|leonardo>` keys: `env`, `mcu`, `programmer`,
  `baud`, `flash_size`, `hex_span_expected`, `bootloader`, `judged_span_policy`,
  `vector_exclusions`, `needs_1200_touch`, `chip_out_before_sideload`.
- `rig-pins.json` `chips.<w27c512|w29c020>` keys: `size_bytes`, `pin_count`, `package`,
  `vpp_mv`, `algorithm`, `stamp_width`.
- `arms-provenance.json` keys: `arms`, `interpreter`, `dep_freeze_control`,
  `dep_freeze_v133`, `dep_freeze_sha`, `dep_freeze_diff_empty`, `config_dir`,
  `config_dir_sha`, `uv_cache_dir`, `sha_verification`.

Created by **later plans in this phase** (listed so the source-grounding pass treats them
as new, not as drift): `tools/gen_addr_image.py`, `tools/probe_board.py`,
`tools/capture_provenance.py`, `tools/gate_record.py`, `tools/judge_readback.py`,
`tools/judge_wrv.py`, `tools/render_evidence.py`, `tools/render_steps.py`,
`tools/check_rebuild.py`, `tools/check_arms.py`, `tools/touch_1200.py`,
`tools/run_gates.sh`, `PROCEDURE.md`, `images/firestarter_<env>.<arm>.hex` (6),
`images/SHA256SUMS.txt`, `images/BUILD-MANIFEST.json`, `images/REBUILD-CHECK.json`,
`bench/IMAGE-PLAN.json`, `bench/.gitignore`, `bench/EVIDENCE.jsonl`,
`bench/EVIDENCE.md`, `bench/cells/BRINGUP-uno/`, `bench/cells/BRINGUP-uno328pb/`,
`bench/cells/BRINGUP-leonardo/`, `bench/cells/BRINGUP-wrv/`, PROCEDURE step ids
`P-01`…`P-11`, `P-H1`, `P-H2`.

<tasks>

<task type="auto">
  <name>Task 1: Pin the rig — scaffold `.planning/v1.34/`, re-verify the four SHAs, write `rig-pins.json`</name>
  <files>.planning/v1.34/rig-pins.json, .planning/v1.34/README.md, .planning/v1.34/config/.gitkeep, .planning/v1.34/bench/cells/.gitkeep, .gitignore</files>
  <read_first>
    - `.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-RESEARCH.md` — §"Exact SHAs", §"Standard Stack", §"Recommended layout", §"Runtime State Inventory", Code Example 4 (per-target parameter table), §"Chip facts"
    - `.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-CONTEXT.md` — D-04, D-07, D-09, D-16
    - `/workspaces/.gitignore` — current content, to append one line without disturbing the rest
    - `firestarter/scripts/baseline/size_baseline.json` — the five recorded toolchain pins (read-only reference)
    - `firestarter/platformio.ini` — the three AVR envs and `board_upload.maximum_size = 32768` (read-only reference)
  </read_first>
  <action>
Create the directory skeleton `.planning/v1.34/{images,tools,config,bench/cells}` with
`.gitkeep` files in `config/` and `bench/cells/` so the empty dirs are tracked. Append the
single line `/.v1.34-arms/` to `/workspaces/.gitignore` under a short comment naming it as
the v1.34 host-arm worktree root — use a scoped append, never a whole-file rewrite.

Re-verify all four source SHAs at execution time rather than trusting planning prose (the
standing memory `reference_tag_push_safe_but_local_beta_lags_origin` records that local
refs in this repo set drift): `gh pr view 56 --repo henols/firestarter --json headRefOid`
must equal `5759dc8d644a8a7fb26e9a0ccd11a8bfd53fc463`;
`gh pr view 54 --repo henols/firestarter_app --json headRefOid` must equal
`cb189a9b001e9e34fb7651535de339761301d061`;
`git -C /workspaces/firestarter merge-base origin/beta gsd/v1.33-source-hygiene-firmware-size-reduction`
must equal `8695ee52c27a4bee4387c5c489afd5f3d7275e8a`; and the same merge-base in
`/workspaces/firestarter_app` must equal `6bfa6453d1bac232eb81ab35fa7f14b50b0b291a`. Any
mismatch stops the plan and is reported — do not proceed with a substituted SHA.

Verify that the pinned avrdude accepts `-A` before pinning it: run
`/home/vscode/.platformio/packages/tool-avrdude/avrdude -A -p atmega328p -c arduino -P /dev/null`
and confirm the failure is a port/programmer failure, not `invalid option` (contrast with a
deliberately invalid `-Z`, which must be rejected at option-parse time). Record the
observed avrdude version string. If `-A` is rejected, pin the fallback
`/usr/bin/avrdude` + `/etc/avrdude.conf` instead and record the substitution and both
version numbers as a named divergence in `README.md`.

Write `.planning/v1.34/rig-pins.json` with the exact keys listed in this plan's "Artifacts
this phase produces" section and these values:

`arms_root` = `/workspaces/.v1.34-arms`; `firmware_repo` = `/workspaces/firestarter`;
`app_repo` = `/workspaces/firestarter_app`.
`arms.control` = fw_sha `8695ee52c27a4bee4387c5c489afd5f3d7275e8a`, app_sha
`6bfa6453d1bac232eb81ab35fa7f14b50b0b291a`, worktree `/workspaces/.v1.34-arms/control`,
venv_bin `/workspaces/.v1.34-arms/control/.venv/bin/firestarter`, venv_python
`/workspaces/.v1.34-arms/control/.venv/bin/python`.
`arms.v133` = fw_sha `5759dc8d644a8a7fb26e9a0ccd11a8bfd53fc463`, app_sha
`cb189a9b001e9e34fb7651535de339761301d061`, worktree `/workspaces/.v1.34-arms/v133`, and
the parallel venv paths.
`avrdude` = binary `/home/vscode/.platformio/packages/tool-avrdude/avrdude`, conf
`/home/vscode/.platformio/packages/tool-avrdude/avrdude.conf`, version as measured.
`avrdude_fallback` = `/usr/bin/avrdude` + `/etc/avrdude.conf`, version `7.1`.
`forbidden_binaries` = the stale
`/home/vscode/.platformio/packages/tool-avrdude@1.60300.200527/avrdude` (avrdude 6.3,
predates `urclock`).
`objcopy` = `/home/vscode/.platformio/packages/toolchain-atmelavr/bin/avr-objcopy` (not on
PATH — this is why it is pinned).
`pio_binary` = `/usr/local/bin/pio`; `pio_project_dir` = `/workspaces/firestarter` (every
`pio` invocation runs with this cwd — Pitfall 4: `cwd=/workspaces` crashes on a duplicate
`[platformio]` section in the generated untracked root `platformio.ini`, which must not be
"fixed").
`uv_cache_dir` = `/workspaces/.v1.34-arms/.uv-cache` (Pitfall 9: `/home/vscode/.cache` is
root-owned and `uv` fails without this).
`config_dir` = `/workspaces/.planning/v1.34/config`.
`interpreter` = the measured `python3 --version` string.
`toolchain` = platformio_core `6.1.19`, platform_atmelavr `5.2.0`, toolchain_atmelavr
`1.70300.191015`, avr_gcc `7.3.0`, framework_arduino_avr `5.3.0`,
framework_arduino_avr_minicore `3.1.2`, each measured at execution time from
`~/.platformio/platforms/atmelavr/.piopm` and the packages' `package.json`, with a note
that `platform = atmelavr` is unversioned so nothing enforces these.
`targets.uno` = env `uno`, mcu `atmega328p`, programmer `arduino`, baud `115200`,
flash_size `32768`, hex_span_expected `22952`, bootloader `optiboot 512 B`,
judged_span_policy `hex-extent`, vector_exclusions `[]`, needs_1200_touch `false`,
chip_out_before_sideload `true`.
`targets.uno328pb` = env `uno328pb`, mcu `atmega328pb`, programmer `urclock`, baud
`115200`, flash_size `32768`, hex_span_expected `23000`, bootloader `urboot 384 B`,
judged_span_policy `PENDING-xshowvector`, vector_exclusions `[]`, needs_1200_touch `false`,
chip_out_before_sideload `true`. The `PENDING-xshowvector` value is deliberate: plan 09
replaces it with a policy derived from the recorded `-xshowvector` output, and every tool
must refuse to judge this target while the value is still `PENDING-xshowvector`.
`targets.leonardo` = env `leonardo`, mcu `atmega32u4`, programmer `avr109`, baud `57600`,
flash_size `32768`, hex_span_expected `25098`, bootloader `Caterina 4096 B`,
judged_span_policy `hex-extent`, vector_exclusions `[]`, needs_1200_touch `true`,
chip_out_before_sideload `false` (Leonardo is exempt — flashed with the chip seated).
`chips.w27c512` = size_bytes `65536`, pin_count `28`, package `DIP28`, vpp_mv `12000`,
algorithm `7`, stamp_width `16`.
`chips.w29c020` = size_bytes `262144`, pin_count `32`, package `DIP32`, vpp_mv `12000`,
algorithm `5`, stamp_width `32`.
`forbidden_flags` = `--force`, `-f`, `-b`, `--no-blank-check`, `--skip-erase` (Phase 145
D-17 withdrew the standing permission to use `--force`, permanently).
`forbidden_argv0` = any first token that is not one of the two absolute `venv_bin` paths;
name the bare token that the user-site editable install would resolve to.
`image_naming` = the pattern `firestarter_<env>.<arm>.hex`, with the reason recorded:
`firestarter/name_firmware.py` derives PROGNAME from the `RURP_BOARD_NAME` build flag, so
both arms build to the identical `.pio/build/<env>/firestarter_<env>.hex` and must be
disambiguated at copy time.
`tool_conventions` = the pinned entry-point idiom `sys.exit(main())` for every newly
authored tool under `tools/`, with the single documented exception that
`gen_addr_image.py` is derived from `.planning/phases/145-bench-validation/images/gen_addr_image.py`
and retains that file's `raise SystemExit(main(sys.argv))` form; plus the rule that every
`tools/*.py` supports a `--selftest` mode carrying a positive and a negative fixture, and
that rig tools import stdlib only, with `pyserial` the one permitted non-stdlib import
(`touch_1200.py`).

Write `.planning/v1.34/README.md` as a short orientation page: the milestone's purpose in
two sentences, the directory map, the D-16 boundary statement that nothing here is ever
copied into either sub-repo, the pinned avrdude decision with its rationale (PlatformIO's
own binary + conf is used for the read as well as the write, so the write chain and the
read chain do not introduce a second avrdude version as a variable, while D-01's
independence is preserved because the read is a separate invocation judged by a rig
script rather than by avrdude's own verify pass), and the D-09 non-claim that both arms run
on devcontainer Python 3.12.14 rather than the app-CI py3.11 floor.
  </action>
  <acceptance_criteria>
    - `.planning/v1.34/rig-pins.json` parses as JSON and contains every top-level key named in "Artifacts this phase produces".
    - `rig-pins.json` `arms.control.fw_sha` is exactly `8695ee52c27a4bee4387c5c489afd5f3d7275e8a` and `arms.v133.fw_sha` is exactly `5759dc8d644a8a7fb26e9a0ccd11a8bfd53fc463`.
    - `rig-pins.json` `arms.control.app_sha` is exactly `6bfa6453d1bac232eb81ab35fa7f14b50b0b291a` and `arms.v133.app_sha` is exactly `cb189a9b001e9e34fb7651535de339761301d061`.
    - Each of the four SHAs above was re-read at execution time and the command output is quoted in the plan SUMMARY; a mismatch would have stopped the plan.
    - `rig-pins.json` `targets.uno328pb.judged_span_policy` is the literal `PENDING-xshowvector`.
    - `rig-pins.json` `targets.uno.hex_span_expected` is 22952, `targets.uno328pb.hex_span_expected` is 23000, `targets.leonardo.hex_span_expected` is 25098.
    - `rig-pins.json` `chips.w27c512.size_bytes` is 65536 with `stamp_width` 16; `chips.w29c020.size_bytes` is 262144 with `stamp_width` 32; both carry `vpp_mv` 12000.
    - `rig-pins.json` `avrdude.binary` exists on disk and its measured version string is recorded in the same object.
    - The `-A` acceptance probe was run and its outcome recorded: either `-A` parsed on the pinned binary, or the fallback pin was taken and both version numbers are recorded in `README.md`.
    - `/workspaces/.gitignore` contains a line exactly `/.v1.34-arms/` and `git diff --stat -- .gitignore` shows lines added and none removed.
    - Directories `.planning/v1.34/images`, `.planning/v1.34/tools`, `.planning/v1.34/config`, `.planning/v1.34/bench/cells` all exist.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` are both empty — no sub-repo file was touched.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json,sys,os;p=json.load(open('.planning/v1.34/rig-pins.json'));req=['schema_version','milestone','phase','arms_root','firmware_repo','app_repo','arms','avrdude','avrdude_fallback','forbidden_binaries','objcopy','pio_binary','pio_project_dir','uv_cache_dir','config_dir','interpreter','toolchain','targets','chips','forbidden_flags','forbidden_argv0','image_naming','tool_conventions'];m=[k for k in req if k not in p];assert not m, f'missing keys {m}';assert p['arms']['control']['fw_sha']=='8695ee52c27a4bee4387c5c489afd5f3d7275e8a';assert p['arms']['v133']['fw_sha']=='5759dc8d644a8a7fb26e9a0ccd11a8bfd53fc463';assert p['arms']['control']['app_sha']=='6bfa6453d1bac232eb81ab35fa7f14b50b0b291a';assert p['arms']['v133']['app_sha']=='cb189a9b001e9e34fb7651535de339761301d061';assert p['targets']['uno328pb']['judged_span_policy']=='PENDING-xshowvector';assert p['targets']['uno']['hex_span_expected']==22952 and p['targets']['uno328pb']['hex_span_expected']==23000 and p['targets']['leonardo']['hex_span_expected']==25098;assert p['chips']['w27c512']['size_bytes']==65536 and p['chips']['w27c512']['stamp_width']==16;assert p['chips']['w29c020']['size_bytes']==262144 and p['chips']['w29c020']['stamp_width']==32;assert p['chips']['w27c512']['vpp_mv']==12000 and p['chips']['w29c020']['vpp_mv']==12000;assert os.path.exists(p['avrdude']['binary']), 'pinned avrdude missing';print('OK rig-pins')"</automated>
    <automated>grep -qx '/.v1.34-arms/' /workspaces/.gitignore && test -d .planning/v1.34/images && test -d .planning/v1.34/tools && test -d .planning/v1.34/config && test -d .planning/v1.34/bench/cells && test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain)" && echo "OK scaffold + subrepos clean"</automated>
  </verify>
  <done>`rig-pins.json` exists with every pinned value verified against a live measurement, the skeleton exists, `.gitignore` carries exactly one new line, and both sub-repos are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package-legitimacy gate before the two editable installs</name>
  <what-built>
    Nothing has been installed yet. The next task runs `uv pip install -e .` inside each of
    the two host-arm worktrees. That resolves six transitive Python dependencies, and
    RESEARCH.md's Package Legitimacy Audit returned `[SUS]` for all six.
  </what-built>
  <how-to-verify>
    The six packages and the reason each scored `[SUS]`:

    1. `click` — https://pypi.org/project/click/ — reasons `too-new` + `unknown-downloads`. `too-new` fired because a release landed 2026-08-26, the day research ran.
    2. `pyserial` — https://pypi.org/project/pyserial/ — reason `unknown-downloads`. Latest publish 2020-11-23.
    3. `requests` — https://pypi.org/project/requests/ — reason `unknown-downloads`.
    4. `rich` — https://pypi.org/project/rich/ — reason `unknown-downloads`.
    5. `tqdm` — https://pypi.org/project/tqdm/ — reason `unknown-downloads`.
    6. `packaging` — https://pypi.org/project/packaging/ — reason `unknown-downloads`.

    The single reason set behind every one of the six reduces to `unknown-downloads`: the
    PyPI JSON API exposes no download counter, so the legitimacy seam cannot score it and
    fails to the cautious side.

    What matters for the decision: **none of these six is a choice this phase is making.**
    They are the already-declared floors in `firestarter_app/pyproject.toml:43-50`
    (`click>=8.1`, `pyserial>=3.5`, `requests>=2.20`, `rich>=14.0`, `tqdm>=4.60`,
    `packaging>=21.0`) of the sub-repo that is itself the thing under test. This phase adds
    no new package and no `[SLOP]` verdict was returned.

    Steps to confirm:
    1. Open each of the six PyPI URLs above and confirm the project page names the expected
       upstream source repo (pallets/click, pyserial/pyserial, psf/requests,
       Textualize/rich, tqdm/tqdm, pypa/packaging).
    2. Read `firestarter_app/pyproject.toml` lines 43-50 and confirm all six appear there
       as pre-existing declared dependencies.
    3. Confirm you accept installing the sub-repo's own declared dependency set into two
       isolated venvs under `/workspaces/.v1.34-arms/`.

    The real guard against a dependency problem in this phase is not this gate — it is the
    next task's assertion that the two arm venvs resolve an **identical** dependency set
    (Pitfall 8), because a version drift between the arms would be a genuine second A/B
    variable.
  </how-to-verify>
  <action>
Present the six packages with their registry URLs and their `[SUS]` reason sets exactly as
listed above, then block. Do not install anything before the operator answers. This gate is
never auto-approvable regardless of any auto-advance setting.

Record the operator's verbatim response in the plan SUMMARY, including any package they ask to
be held. If a package is held, stop and report rather than proceeding with a partial install —
the arms must be built from the sub-repo's complete declared dependency set or not at all,
because a partial set would make the two arms non-comparable.
  </action>
  <resume-signal>Type "approved" to permit the two editable installs, or name the package you want held.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Stand up the two host arms — worktrees, venvs, frozen config dir, D-08 triple, dependency-set equality</name>
  <files>.planning/v1.34/arms-provenance.json, .planning/v1.34/config/.gitkeep</files>
  <read_first>
    - `.planning/v1.34/rig-pins.json` — the paths and SHAs written by task 1
    - `.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-RESEARCH.md` — Code Example 7 (the verified worktree + venv sequence), Pitfall 1 (`python -P`), Pitfall 8 (dependency drift), Pitfall 9 (`UV_CACHE_DIR`), §"Runtime State Inventory" (the user-site editable install and the two dead app venvs)
    - `.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-CONTEXT.md` — D-06, D-07, D-08, D-09
    - `firestarter_app/firestarter/config.py` — how `FIRESTARTER_CONFIG_DIR` is resolved at import time (read-only reference)
    - `firestarter_app/pyproject.toml` — the declared dependency floors (read-only reference)
    - `firestarter_app/.gitignore` — confirms `.venv/` is ignored, which is what keeps the D-08 porcelain leg clean
  </read_first>
  <action>
Export `UV_CACHE_DIR` to the pinned `/workspaces/.v1.34-arms/.uv-cache` before any `uv`
call — without it `uv` aborts with `Permission denied` on the root-owned
`/home/vscode/.cache`.

Create both worktrees from the app repo, detached at the pinned SHAs:
`git -C /workspaces/firestarter_app worktree add --detach /workspaces/.v1.34-arms/control 6bfa645`
and the same for `v133` at `cb189a9`. `--detach` is deliberate — no branch can be
accidentally advanced.

Create both venvs and both editable installs in the **same step**, back to back, so the
dependency resolution cannot drift between them (Pitfall 8): for each arm,
`uv venv --python 3.12 .venv` inside the worktree, then
`uv pip install --python .venv/bin/python -e .`. Note the trap this avoids: an editable
install does not follow a worktree, so each venv must be installed against its own tree.
Do not reuse or extend `firestarter_app/.venv` or `firestarter_app/.venv/ci-replica` —
both are dead in this container (their interpreters do not exist).

Seed the frozen shared config dir at the pinned `/workspaces/.planning/v1.34/config`, and
verify that `FIRESTARTER_CONFIG_DIR` pointing there produces the app's expected files
rather than touching `~/.firestarter` (which does not exist — clean slate). Set the
variable at process launch on every invocation; `config.py` computes `HOME_PATH`,
`DATABASE_FILE` and `PIN_MAP_FILE` at import time, so setting it afterwards is too late.
Compute the config dir's content SHA as a sha256 over the sorted file tree (relative path
plus content, in sorted order) and record it.

Observe the D-08 triple on **both** arms and record each of the three outputs verbatim:
`git -C <worktree> rev-parse HEAD` must equal the pinned `app_sha`;
`git -C <worktree> status --porcelain` must be empty; and
`<worktree>/.venv/bin/python -P -c 'import firestarter; print(firestarter.__file__)'` must
print a path under that worktree. The `-P` is load-bearing, not stylistic: `/workspaces`
contains a directory named `firestarter` (the firmware repo), which is discovered as a
PEP 420 namespace-package portion ahead of the setuptools editable finder, so without `-P`
the probe prints `None` on every interpreter including the arm venv's own.

Assert the dependency sets are identical: diff the two `pip freeze` outputs with the
`firestarter` line excluded; the diff must be empty. Record both freeze outputs verbatim
plus a sha256 over the (identical) freeze text.

Confirm each arm's binary reports `Firestarter, version 3.0.0b32` and that
`dev consistency-check --help` is registered on both — `channel.py`'s `BETA_ONLY_DEV_COMMANDS`
gate leaves `dev` registered because `3.0.0b32` parses as a PEP 440 pre-release, and this
must be observed rather than assumed since the judged evidence chain runs through that
command.

Write `.planning/v1.34/arms-provenance.json` with the keys named in "Artifacts this phase
produces", including `sha_verification` carrying the four execution-time SHA readings from
task 1.

Do not neutralize the user-site editable install at
`/home/vscode/.local/lib/python3.12/site-packages` — that is explicitly deferred. Instead
record its existence and the fact that bare `firestarter` on PATH resolves to it, so the
prohibition in PROCEDURE.md and the `gate_record.py` argv check have a documented target.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app worktree list` lists both `/workspaces/.v1.34-arms/control` and `/workspaces/.v1.34-arms/v133`, each `(detached HEAD)`.
    - `git -C /workspaces/.v1.34-arms/control rev-parse HEAD` prints `6bfa6453d1bac232eb81ab35fa7f14b50b0b291a`; the `v133` worktree prints `cb189a9b001e9e34fb7651535de339761301d061`.
    - `git -C <worktree> status --porcelain` produces zero bytes of output for both worktrees.
    - `/workspaces/.v1.34-arms/control/.venv/bin/python -P -c 'import firestarter; print(firestarter.__file__)'` prints a path beginning `/workspaces/.v1.34-arms/control/firestarter/`; the `v133` arm prints the parallel path under its own worktree. Neither prints `None`.
    - The same probe run **without** `-P` from cwd `/workspaces` is recorded as printing `None`, proving the trap is real on this container rather than inherited from research prose.
    - `diff` of the two `pip freeze` outputs with the `firestarter` line excluded is zero bytes.
    - Both `<arm>/.venv/bin/firestarter --version` outputs are the identical string `Firestarter, version 3.0.0b32` — recorded as the measured demonstration that no version string can identify an arm.
    - `<arm>/.venv/bin/firestarter dev consistency-check --help` exits 0 on both arms.
    - `arms-provenance.json` parses, `dep_freeze_diff_empty` is `true`, `config_dir_sha` is a 64-hex-character string, and `interpreter` records `Python 3.12.14`.
    - `.planning/v1.34/config/` exists and `FIRESTARTER_CONFIG_DIR` was exported at process launch for every app invocation in this task; `~/.firestarter` still does not exist.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty in the main checkout, and no commit was created in either sub-repo.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; P=.planning/v1.34/rig-pins.json; C=$(python3 -c "import json;print(json.load(open(\"$P\"))[\"arms\"][\"control\"][\"worktree\"])"); V=$(python3 -c "import json;print(json.load(open(\"$P\"))[\"arms\"][\"v133\"][\"worktree\"])"); test "$(git -C $C rev-parse HEAD)" = "6bfa6453d1bac232eb81ab35fa7f14b50b0b291a"; test "$(git -C $V rev-parse HEAD)" = "cb189a9b001e9e34fb7651535de339761301d061"; test -z "$(git -C $C status --porcelain)"; test -z "$(git -C $V status --porcelain)"; $C/.venv/bin/python -P -c "import firestarter,sys; assert firestarter.__file__ and firestarter.__file__.startswith(\"$C\"), firestarter.__file__; print(firestarter.__file__)"; $V/.venv/bin/python -P -c "import firestarter,sys; assert firestarter.__file__ and firestarter.__file__.startswith(\"$V\"), firestarter.__file__; print(firestarter.__file__)"; diff <($C/.venv/bin/python -m pip freeze | grep -v "^firestarter") <($V/.venv/bin/python -m pip freeze | grep -v "^firestarter"); echo "OK arms + dep-set identical"'</automated>
  </verify>
  <done>Both arms exist as detached worktrees at the pinned SHAs with their own venvs and editable installs; the D-08 triple is observed on both; the dependency-set diff is empty; the frozen config dir is seeded and SHA'd; `arms-provenance.json` records all of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → rig tooling | Operator-supplied strings (paths, SHAs, package approval) enter the rig at this plan |
| meta repo → sub-repos | A worktree checkout reaches into `firestarter_app`; nothing may write there |
| PyPI → arm venvs | `uv pip install -e .` resolves six transitive dependencies from a public registry |
| filesystem | Rig tooling writes under `.planning/v1.34/` and `/workspaces/.v1.34-arms/` only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-01 | Spoofing | arm identity | critical | mitigate | Arm named by absolute venv binary path plus the D-08 triple (SHA + empty porcelain + `__file__`); no version string or handshake is trusted, because both arms report `3.0.0b32` |
| T-160-02 | Tampering | `firestarter_app` working tree | high | mitigate | Worktrees created `--detach` so no branch advances; `git status --porcelain` asserted empty on both worktrees and the main checkout; no commit, branch or push in either sub-repo |
| T-160-03 | Tampering | dependency resolution | medium | mitigate | Both venvs created and installed back to back in one step, then `pip freeze` diff asserted empty (Pitfall 8); both freeze outputs recorded in `arms-provenance.json` |
| T-160-04 | Repudiation | pinned SHAs | high | mitigate | All four SHAs re-read at execution time from `gh pr view … headRefOid` and `git merge-base`; a mismatch stops the plan rather than substituting a value |
| T-160-05 | Tampering | pinned toolchain | medium | accept | `platform = atmelavr` is unversioned and `.piopm` records `requirements: null`, so nothing enforces the five toolchain pins. Recorded as documentation with the non-enforcement stated; SC#1's reproduce-or-record-the-divergence clause is the live control |
| T-160-06 | Elevation of privilege | `UV_CACHE_DIR` | low | mitigate | Cache redirected inside the ignored arms root; the root-owned `/home/vscode/.cache` is never written and never chowned |
| T-160-07 | Information disclosure | committed rig artifacts | low | accept | The config dir is seeded by this phase and carries no credential; `arms-provenance.json` holds SHAs, paths and package versions only. Checked, not assumed |
| T-160-SC | Tampering | pip installs (6 transitive deps) | high | mitigate | RESEARCH.md Package Legitimacy Audit returned `[SUS]` on all six with the reason set reducing to `unknown-downloads`; a blocking-human checkpoint (task 2) precedes the installs and is never auto-approvable. No `[SLOP]` verdict; this phase adds no new package |
</threat_model>

<verification>
- `rig-pins.json` parses and every pinned value matches a live measurement recorded in the SUMMARY.
- `/workspaces/.gitignore` gained exactly one line; nothing was removed.
- Both worktrees are detached at the pinned app SHAs with empty porcelain.
- The `python -P` probe resolves into each worktree; the same probe without `-P` is recorded as `None`.
- The two arm `pip freeze` outputs are identical with `firestarter` excluded.
- Both arms print the identical `3.0.0b32` version string — recorded as the reason no version-based arm check may exist.
- No file under `firestarter/` or `firestarter_app/` was created or modified; no sub-repo commit exists.
</verification>

<success_criteria>
- `.planning/v1.34/` skeleton exists with `rig-pins.json` and `README.md`.
- The four source SHAs are verified at execution time and pinned.
- The pinned avrdude binary + conf is chosen, its `-A` acceptance probed, and any fallback substitution recorded as a named divergence.
- Two host arms are callable by absolute path, each provably at its named commit with a clean tree and a correctly resolving `__file__`.
- One frozen shared `FIRESTARTER_CONFIG_DIR` exists with its content SHA recorded.
- The dependency-set equality assertion is green and both freeze outputs are recorded.
</success_criteria>

<output>
Create `.planning/phases/160-rig-dual-arm-build-flash-provenance-the-shared-cell-procedur/160-01-SUMMARY.md` when done
</output>
