---
phase: 161-board-board-sweep-three-boards-on-rev-2-0
plan: 02
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - .planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/
  - .planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/
  - .planning/v1.34/tools/capture_provenance.py
  - .planning/v1.34/rig-pins.json
autonomous: false
requirements: [BOARD-02, BOARD-03]
tags: [bench, on-device, uno328pb, leonardo, pre-proof, de-risk, no-sweep-position]

must_haves:
  truths:
    - "A v1.33 flash on the ATmega328PB has been read back and judged a match against the **v1.33 arm's own** hex span — the thing no record in this milestone currently holds (D-10)."
    - "`capture_provenance.py` has been observed either completing on a Leonardo, or failing in a named way with a working sequence recorded — so A3/B2's `P-02` is not the first attempt."
    - "Both pre-proofs are recorded as bring-up cells, outside the 12 sweep positions, and neither adds a row to `EVIDENCE.jsonl`."
    - "Any rig-tool change these pre-proofs force is a `P-H1` fix in meta-repo bench tooling, re-gated by the full suite before the sweep starts."
  prohibitions:
    - "No product code. `firestarter/` and `firestarter_app/` stay byte-unchanged. A tool fix belongs in `.planning/v1.34/tools/`; if either sub-repo needs an edit, STOP and report — that is Phase 165's, on the v1.33 PR branch."
    - "No `EVIDENCE.jsonl` row. These are rig proofs, not positions; they hold no `WRV-VERDICT` and cannot be assembled by `append_evidence.py`."
    - "Never assert `sha_actual_judged == sha_expected_judged` on `uno328pb` — 8 vector bytes are excluded, so a correct flash yields unequal raw-span SHAs. The verdict is `judged_match`."
    - "Never use the legacy scalar `hex_span_expected`; it equals the v133 value and would silently pass a wrong-arm judgement on the control side later."
    - "No chip is seated on any board during this plan. No chip write, read, erase, blank or vpp-set runs."
    - "`--wait-new-port` is never passed to `touch_1200.py` — empirically refuted on this Leonardo; the touch returns the same node."
    - "No forbidden flag (`--force`, `-f`, `-b`, `--no-blank-check`, `--skip-erase`) appears in any recorded command."
    - "This plan must not run under `--auto` / `--chain` / any auto-advance mode: it carries operator-physical checkpoints and those modes auto-approve them."
  artifacts:
    - .planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/READBACK-VERDICT.json
    - .planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/PREPROOF.md
    - .planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/PREPROOF.md
  key_links:
    - "`BRINGUP-uno328pb-v133/READBACK-VERDICT.json` → cell A2's second `P-04` (plan 161-04): if the v133 span judges here, A2's v1.33 flash is a known-good operation rather than a live first attempt at cell 2 of 3."
    - "`BRINGUP-leonardo-provenance/PREPROOF.md` → cell A3/B2's `P-02` (plan 161-05): the recorded working sequence is the prescription that plan follows; an A3/B2 `P-02` that is a copy of A1's is the warning sign."
    - "Any `capture_provenance.py` change here → `run_gates.sh` 12/12: the tool's own selftest and the full suite are re-run before the sweep opens."
---

<objective>
Spend ~10 minutes and two board swaps buying two first-contact discoveries **before** the sweep,
instead of during it.

Purpose: Phase 160's strongest method finding is that roughly twenty latent rig defects each had
a passing fixture selftest and failed on first contact with hardware. Two such contacts are
still outstanding and both sit on a cell's critical path. **D-10:** no v1.33 flash on an
ATmega328PB has ever produced a judged match — both recorded 328PB verdicts judge against the
**control** span (26074), and the `vector-exclusion` offsets were derived live on a control-arm
flash. Without this pre-proof, A2's second arm is the first attempt, live, at cell 2 of 3, and
`P-04` classifies a read-back mismatch on a correctly-flashed board as a `P-H1` **halt**.
**Leonardo:** `capture_provenance.py` has run live on exactly one board (the Uno), it shells to
`probe_board.py` first and treats a non-zero as a hard failure, `probe_board.py` performs no
1200-baud touch, and a Leonardo running application firmware was measured to reject the avr109
handshake. A3/B2's `P-02` would hard-refuse — at the milestone's most expensive cell.

Output: `bench/cells/BRINGUP-uno328pb-v133/` with a v133-span read-back verdict, and
`bench/cells/BRINGUP-leonardo-provenance/` with either a completed provenance record or a named
failure plus the working sequence A3/B2 will use.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/v1.34/PROCEDURE.md
@.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-CONTEXT.md
@.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-RESEARCH.md
@.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-PATTERNS.md
</context>

## Standing bench rule 7 — read before executing

This plan must **not** run under `--auto`, `--chain`, or any auto-advance mode. Those modes
auto-approve `human-verify` checkpoints, and `autonomous: false` is **not** self-protecting
against them. Every checkpoint below corresponds to the operator physically touching the rig.

## Requirement completion

This plan de-risks BOARD-02 (A2) and BOARD-03 (A3/B2). It closes **neither** — no sweep
position is produced here. Do not mark any BOARD requirement complete.

## Measured planning facts to carry in

- At planning time (2026-08-27) **two** `/dev/ttyACM*` nodes were present (`ttyACM0`, `ttyACM1`)
  and **zero** `/dev/ttyUSB*`. The port is therefore never inherited and never assumed from a
  prior task — enumerate before and after every swap and confirm identity by signature probe.
- `/workspaces/firestarter` HEAD is `5759dc8d644a8a7fb26e9a0ccd11a8bfd53fc463` (**v133**) with an
  empty porcelain. Both pre-proofs run on the v133 arm, so **no checkout is needed** — assert
  the sha and the clean porcelain rather than re-checking-out.
- uno328pb enumerates through a CH340 bridge (`1a86:7523`) as `/dev/ttyUSB*`; Uno and Leonardo
  both present as `/dev/ttyACM*`, the same name, distinguishable only by node creation time.
- Per-target avrdude resolution is PlatformIO's own and needs no override: `uno` 6.3 (`arduino`),
  `uno328pb` **8.1** (`urclock`), `leonardo` 6.3 (`avr109`). A 6.3 in a uno/leonardo upload log
  is **not** a `forbidden_binaries` violation — that list governs the rig's own direct avrdude
  invocations, which use the pinned 8.1.

## Artifacts this phase produces

Created by **this plan** (excluded from drift checks as newly-created symbols):

- **New cell directories:** `.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/`,
  `.planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/`
- **New files:** `PREPROOF.md` in each, `board_probe.json`, `READBACK-VERDICT.json` +
  `flash_readback.bin` + `expected_span.bin` + `judged_span.bin` + `SHA256SUMS.txt` +
  `avrdude_read.stderr.log` (uno328pb cell), `provenance_BRINGUP-leonardo-provenance.json`
  and `touch.json` (Leonardo cell), `logs/NN_<verb>.std{out,err}.log` in both
- **Possible new tool seam (only if measured necessary):** new optional flags on
  `capture_provenance.py` allowing a pre-obtained probe result to be consumed instead of
  re-probed. Exact flag names are decided by the measurement in Task 4 and recorded there.
- **No new `EVIDENCE.jsonl` row and no new schema key.**

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: P-01 (pre-proof cell) — operator mounts the bare uno328pb and reports its node</name>
  <what-built>
    Wave 0 landed: `append_evidence.py` selftests green, `PROCEDURE.md` carries Amendment 3, and
    `run_gates.sh` is 12/12 selftests + 5/5 live gates at exit 0. Nothing has touched a board.
    The rig is still exactly as Phase 160 left it — Uno + Rev 2.0, W27C512 seated, v1.33 flashed,
    VPP 12.0 V. Before it moves, the enumerated device nodes have been recorded.
  </what-built>
  <how-to-verify>
    This is a physical step with no CLI equivalent. It is the `P-01` mount-and-declare handover
    for a **pre-proof** cell, not a sweep cell.

    1. Disconnect the Uno. Leave its W27C512 seated and its Rev 2.0 shield mounted — nothing in
       this plan touches that board, and A1 (plan 161-03) will ask for it back.
    2. Take the **uno328pb** (ATmega328PB) board. **No shield. No chip.** Bare board only — this
       satisfies the Uno-class chip-out rule trivially, which is exactly why D-10 specifies it.
    3. Connect it over USB and report the device node. It bridges through a CH340, so expect
       `/dev/ttyUSB0` — but report what is actually there. Do not reuse a node from any prior
       task; numbering shuffles across replug.
    4. Confirm the socket is empty and no shield is fitted.

    Expect one firmware flash on this board. Standing bench-board permission covers it.
  </how-to-verify>
  <action>
Before presenting the gate, enumerate `/dev/ttyACM*` and `/dev/ttyUSB*` and record the list with
a timestamp. Present the gate and block.

On resume, re-enumerate and record the post-swap list with its timestamp. Record the operator's
reported node verbatim. If the reported node is absent from the post-swap enumeration, stop and
report — do not proceed on a node that is not there. Carry the confirmed node into Task 2 as
`$PORT`.

This gate is never auto-approvable: connecting a different board has no CLI equivalent.
  </action>
  <resume-signal>Reply with the device node (e.g. `/dev/ttyUSB0`), and confirm "bare board, no shield, no chip" — or describe what went wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: D-10 pre-proof — flash v1.33 to the uno328pb and judge the read-back against the v133 span</name>
  <files>.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/</files>
  <read_first>
    - `.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-RESEARCH.md` — "D-10 — The uno328pb v1.33 Pre-Proof" in full (the two-row table of what is and is not proven, the four commands, the success criterion) and Pitfall 4 (`sha_actual_judged != sha_expected_judged` on a *correct* 328PB flash).
    - `.planning/v1.34/bench/cells/BRINGUP-uno328pb/READBACK-VERDICT.json` and `crossflash/READBACK-VERDICT.json` — the two verdicts that define the gap: both judge against the **control** span 26074, one deliberately mismatching.
    - `.planning/v1.34/rig-pins.json` — `targets.uno328pb` in full (`programmer: urclock`, `judged_span_policy: vector-exclusion`, the two `vector_exclusions` entries and their measured reasons, `hex_span_expected_by_arm`), `arms.v133`, `avrdude`, `forbidden_binaries`, `pio_project_dir`.
    - `.planning/v1.34/tools/judge_readback.py` — the `-A`-explicit avrdude read, the hex normalisation through the pinned `avr-objcopy`, `judge_span_bytes()` versus the raw-span SHA fields, and the six artifacts it writes into `--out-dir`.
    - `.planning/v1.34/PROCEDURE.md` `P-04` — the sanctioned flash path and why the uploader never judges its own upload.
  </read_first>
  <action>
Create `.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/` and a `logs/` subdirectory inside
it. Number logs zero-padded from `00`, one `.stdout.log`/`.stderr.log` pair per invocation, the
suffix naming the verb — the `BRINGUP-wrv` convention. Record the literal argv and the working
directory of every invocation; argv[0] and the script path must be **absolute**, because
`gate_record.py` only recognises a rig-tool invocation whose argv[1] is an absolute path
containing `/.planning/v1.34/tools/`.

Step 0 — identity. Run
`python3 /workspaces/.planning/v1.34/tools/probe_board.py --target uno328pb --port $PORT
--pins /workspaces/.planning/v1.34/rig-pins.json --out $CELLDIR/board_probe.json`.
The expected `connected_part` is `atmega328pb` and the expected `board_signature` is `0x1e9516`
(the constants table's value for this target). A different signature means the wrong board is
attached — stop and report; do not flash it.

Step 1 — firmware source state. Assert, do not change:
`git -C /workspaces/firestarter rev-parse HEAD` equals `rig-pins.json`'s `arms.v133.fw_sha`
(`5759dc8d644a8a7fb26e9a0ccd11a8bfd53fc463`), and
`git -C /workspaces/firestarter status --porcelain` is empty. If HEAD has moved, check out the
v133 sha with the pinned `/usr/local/bin/git` and re-assert. Record both outputs.

Step 2 — flash. `pio run -t upload -e uno328pb` with the working directory
**`/workspaces/firestarter`**, never `/workspaces`: the generated, gitignored
`/workspaces/platformio.ini` carries a duplicate `[platformio]` section that makes
`configparser` abort, so the identical command string succeeds or fails on cwd alone.
PlatformIO resolves avrdude 8.1 and urclock for this env and supplies `-x nometadata`; do not
hand-roll an avrdude write.

Step 3 — the proof. Run
`python3 /workspaces/.planning/v1.34/tools/judge_readback.py --target uno328pb --port $PORT
--flashed-arm v133 --expect-arm v133 --out-dir $CELLDIR
--pins /workspaces/.planning/v1.34/rig-pins.json`. Both arm arguments are `v133` — this is a
same-arm proof, not a cross-flash.

The success criterion is exactly three facts: `judged_match` is `true`; `judged_span_bytes`
equals `rig-pins.json`'s `targets.uno328pb.hex_span_expected_by_arm.v133`, which is **23000**
and is **not** 26074 and **not** read from the legacy scalar; and `vector_exclusions_applied`
carries **both** entries (offset 0 length 4, offset 100 length 4). **Do not compare
`sha_actual_judged` with `sha_expected_judged`** — on this target 8 bytes are excluded from the
byte comparison but the two recorded SHAs are of the *raw* spans, so a correct flash yields
unequal values. Comparing them is the false-RED Pitfall 4 names.

If `judged_match` is `false`: this is the pre-proof doing its job. Do **not** proceed to the
sweep. Record the full verdict, the differing-byte count, and the offsets of the first ten
differences, then determine whether the `vector-exclusion` policy simply fails to transfer from
the control-arm-derived offsets to the v133 image. Re-derive the vector offsets from a fresh
`probe_board.py --show-urclock` interrogation against the **v133** flash and compare them with
the recorded control-derived pair. A policy that needs different offsets per arm is a
`rig-pins.json` finding fixed here, in-phase, with the measured evidence written into
`PREPROOF.md`; it is never carried into A2 as an unexplained result.

Write `PREPROOF.md` recording: what was previously proven and what was not (the two-verdict
table), the commands run with their absolute argv, the three success facts with their measured
values, the explicit statement that the two raw-span SHAs are expected to differ and why, and
whether this pre-proof changed anything in `rig-pins.json`.

State one non-claim in `PREPROOF.md`: this proves the **flash and read-back chain** on the
ATmega328PB for the v133 arm. It proves nothing about the chip-program path, which Backlog
999.2 predicts will brown out — that is A2's job to observe, not to assume.
  </action>
  <acceptance_criteria>
    - `$CELLDIR/board_probe.json` records `connected_part` equal to `rig-pins.json`'s `targets.uno328pb.mcu`, and `board_signature` `0x1e9516`.
    - `$CELLDIR/READBACK-VERDICT.json` exists with `flashed_arm == "v133"`, `expect_arm == "v133"`, `target == "uno328pb"`, `judged_span_policy == "vector-exclusion"`.
    - `judged_match` is `true`, **or** `PREPROOF.md` records a named, measured reason with the re-derived vector offsets and the resulting `rig-pins.json` change.
    - `judged_span_bytes` equals `rig-pins.json`'s `targets.uno328pb.hex_span_expected_by_arm.v133` read at assertion time — not a literal embedded in the check.
    - `vector_exclusions_applied` has exactly 2 entries, at offsets 0 and 100, each length 4.
    - No assertion anywhere in this task's record or verification compares `sha_actual_judged` with `sha_expected_judged`.
    - `flash_readback.bin` is 32768 bytes.
    - The recorded `pio` invocation's working directory is `/workspaces/firestarter`.
    - `git -C /workspaces/firestarter rev-parse HEAD` equals `arms.v133.fw_sha` and its porcelain is empty; `git -C /workspaces/firestarter_app status --porcelain` is empty.
    - `PREPROOF.md` states the non-claim about the chip-program path.
    - `logs/` holds a stdout/stderr pair for every invocation, and every recorded argv[1] is an absolute path.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
C='/workspaces/.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133'
pins=json.load(open('/workspaces/.planning/v1.34/rig-pins.json'))
v=json.load(open(C+'/READBACK-VERDICT.json'))
t=pins['targets'][v['target']]
exp=t['hex_span_expected_by_arm'][v['flashed_arm']]
assert v['target']=='uno328pb', v['target']
assert v['flashed_arm']=='v133' and v['expect_arm']=='v133', (v['flashed_arm'],v['expect_arm'])
assert v['judged_span_bytes']==exp, (v['judged_span_bytes'],exp)
assert v['judged_match'] is True, v
ex=v['vector_exclusions_applied']
assert len(ex)==2 and sorted(e['offset'] for e in ex)==[0,100], ex
assert v['readback_size_bytes']==32768, v['readback_size_bytes']
print('OK v133 span judged on 328pb:',exp)"</automated>
    <automated>python3 -c "
import json
C='/workspaces/.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133'
pins=json.load(open('/workspaces/.planning/v1.34/rig-pins.json'))
p=json.load(open(C+'/board_probe.json'))
assert p['connected_part']==pins['targets']['uno328pb']['mcu'], p['connected_part']
assert p['board_signature']=='0x1e9516', p['board_signature']
print('OK identity', p['connected_part'], p['board_signature'])"</automated>
    <automated>python3 -c "
import os
C='/workspaces/.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133'
assert os.path.getsize(C+'/flash_readback.bin')==32768
assert os.path.isfile(C+'/PREPROOF.md') and os.path.getsize(C+'/PREPROOF.md')>500
print('OK artifacts')"</automated>
    <automated>git -C /workspaces/firestarter status --porcelain; git -C /workspaces/firestarter_app status --porcelain; git -C /workspaces/firestarter rev-parse HEAD</automated>
  </verify>
  <done>The v1.33 arm has been flashed to an ATmega328PB and read back to a judged match against the v133 arm's own 23000-byte span with both vector exclusions applied — the gap D-10 names is closed, or the reason it could not be is measured and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: P-01 (pre-proof cell) — operator mounts the Leonardo with a free shield and declares it</name>
  <what-built>
    The uno328pb v1.33 read-back has been judged (see the verdict just written). The uno328pb
    board is no longer needed until cell A2, and the Rev 2.0 shield is still on the Uno,
    untouched, with its W27C512 seated.
  </what-built>
  <how-to-verify>
    Physical step, `P-01` for the second pre-proof cell.

    1. Disconnect the uno328pb and set it aside — cell A2 (plan 161-04) will ask for it.
    2. Take the **Leonardo** (ATmega32U4). Fit a shield that is **currently free** — the Rev 2.2
       shield is expected to be the free one, since Rev 2.0 is on the Uno. **Leave the socket
       empty; do not seat a chip.**
    3. Read the shield revision off the **silkscreen** and declare it verbatim. It must be one of
       `Rev 2.0`, `Rev 2.2`, `Modified Rev 0` — the tool accepts only those three, and the
       silkscreen is authoritative because the A3 ADC band cannot discriminate them.
    4. Connect over USB and report the device node. The Leonardo presents as `/dev/ttyACM*`,
       the same name class as the Uno, so report exactly what appeared.

    If no shield is free, or you would rather not fit one, say so — the proof degrades cleanly
    to its two most important calls and that is recorded as a partial proof, not faked.
  </how-to-verify>
  <action>
Enumerate the device nodes before presenting the gate and again on resume, with timestamps;
record both lists. Record the operator's declared shield revision **verbatim** — it is written
into the record as `shield_rev_declared` and a declaration is never inferred, corrected or
substituted. If the operator declines to fit a shield, carry that into Task 4 as the
degraded-path branch.

Note when presenting: the shield fitted here is a *carrier for the pre-proof only*. Its ADC
reading is not a Rev 2.0 datum and is not a SHIELD-04 result — that sweep is Phase 163's.

This gate is never auto-approvable: fitting a shield and swapping a board have no CLI
equivalent.
  </action>
  <resume-signal>Reply with the device node, the silkscreen revision you fitted (or "no shield"), and confirm the socket is empty.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Leonardo pre-proof — establish a working P-02 sequence for capture_provenance.py</name>
  <files>.planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/, .planning/v1.34/tools/capture_provenance.py, .planning/v1.34/rig-pins.json</files>
  <read_first>
    - `.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-RESEARCH.md` — Pitfall 2 in full (the internal shell-out order, the hard-failure-never-a-null behaviour, the measured avr109 rejection, the second half of the bind where a post-touch `hw` runs against Caterina) and Open Question 1.
    - `.planning/v1.34/tools/capture_provenance.py` — `probe_board_signature()`, `probe_controller_string()`, the `$ARM_BIN -v -p $PORT hw` shell-out, `read_readback_verdict()`, `RECORD_KEYS`, the `--pending-readback` branch, the `--selftest` pre-parse, and the atomic write.
    - `.planning/v1.34/tools/touch_1200.py` — the bare settle-only mode and why `--wait-new-port` is refuted here.
    - `.planning/v1.34/tools/probe_board.py` — Route 1 / Route 2 and the confirmed absence of any 1200-baud touch.
    - `.planning/v1.34/bench/cells/BRINGUP-leonardo/BOOTLOADER-WINDOW.md` — "Attempt 0" (the measured avr109 rejection against application firmware, rc=1 after ~6.0 s) and the measured window figures.
    - `.planning/v1.34/rig-pins.json` — `targets.leonardo` (`avr109`, baud 57600, `needs_1200_touch: true`, `chip_out_before_sideload: false`) and the touch window numbers (touch→responsive 3.487 s, touch→completed judged read-back 3.878 s, settle 2.0 s).
  </read_first>
  <action>
Create `.planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/` with a `logs/` subdirectory,
same numbering convention, absolute argv recorded for every invocation.

Run A3/B2's `P-02` exactly as plan 161-05 would run it, and observe what happens — do not
pre-empt the failure. In order:

1. `python3 /workspaces/.planning/v1.34/tools/probe_board.py --target leonardo --port $PORT
   --pins /workspaces/.planning/v1.34/rig-pins.json --out $CELLDIR/board_probe.json`.
   Expected part `atmega32u4`, expected signature `0x1e9587`. The measured prediction is that
   this **fails** with rc=1 after ~6 s because the application firmware does not speak avr109;
   record the rc and the elapsed time either way.
2. `FIRESTARTER_CONFIG_DIR=/workspaces/.planning/v1.34/config
   /workspaces/.v1.34-arms/v133/.venv/bin/firestarter -v -p $PORT hw` — the second call
   `capture_provenance.py` makes internally. Set the config dir **inline**, never exported:
   `config.py` computes `HOME_PATH`/`DATABASE_FILE`/`PIN_MAP_FILE` at import time, so a
   session-level export is a partial fix that looks complete.
3. `capture_provenance.py` itself, with `--cell-id BRINGUP-leonardo-provenance
   --position-id BRINGUP-leonardo-provenance --arm v133 --target leonardo --port $PORT
   --chip w27c512 --shield-rev "<the operator's declared value>" --pending-readback
   --out $CELLDIR/provenance_BRINGUP-leonardo-provenance.json`. `--pending-readback` is
   required: no `READBACK-VERDICT.json` exists for this cell and the tool hard-refuses without
   one otherwise. `--arm v133` is correct and needs no checkout — `/workspaces/firestarter` is
   already at the v133 sha, and `fw_sha` is read from that repo, not from the device.
   `--chip w27c512` is a required argument with no bearing on the proof; record that no chip is
   seated and that this field is a shape requirement, not a claim.

If step 1 fails as predicted, establish the working sequence and record it precisely:
`hw` against the application firmware first, then
`python3 /workspaces/.planning/v1.34/tools/touch_1200.py --port $PORT --out $CELLDIR/touch.json`
in **bare settle-only** mode — never `--wait-new-port`, which polls for a node this board never
produces and burned ~5.27 s of the ~8 s Caterina window — then `probe_board.py` inside that
window. Measure and record the elapsed time from touch to a completed probe, against the
recorded 3.487 s / 3.878 s / 2.0 s figures. The touch returns the **same** node; do not wait for
a new one.

Then decide, and state the decision with its evidence: can `capture_provenance.py` consume
those two already-obtained results, or does it need a seam? If it needs one, add the **minimal**
optional flags that let a caller pass a pre-obtained probe JSON and/or skip the internal
re-probe, keeping every existing default behaviour byte-identical so the Uno path is unchanged.
This is meta-repo bench tooling and is a legitimate in-phase `P-H1` fix. Extend the tool's own
`--selftest` with a leg covering the new seam, then re-run the **full** `run_gates.sh` and
require 12/12 selftests, 5/5 live gates, exit 0, measured directly.

If the operator declined to fit a shield, run steps 1 and 2 only — they are the two calls that
actually fail — and record the proof as **partial**: the avr109/touch sequence is established,
`capture_provenance.py`'s end-to-end completion on a Leonardo is not. Say so plainly; do not
pass a shield revision that was not declared.

Write `PREPROOF.md` recording: each call with its rc and elapsed time, the working sequence as
an ordered command list A3/B2 can follow verbatim, the measured touch-window timings, whether a
tool seam was added and exactly what it changed, and the gate result afterwards.

State two non-claims in `PREPROOF.md`: the shield fitted here is a carrier, so its ADC/`hw`
readings are not a Rev 2.0 datum and not a SHIELD-04 result; and the Leonardo is chip-out exempt
by standing bench rule 2, so nothing here relaxes the Uno-class rule for A1 or A2.
  </action>
  <acceptance_criteria>
    - `$CELLDIR/PREPROOF.md` records, for each of the three calls, the literal absolute argv, the return code and the elapsed seconds — including the failures, which are the point.
    - `$CELLDIR/PREPROOF.md` contains an ordered, copy-runnable command list labelled as the sequence A3/B2's `P-02` will follow.
    - Either `provenance_BRINGUP-leonardo-provenance.json` exists with `captured_at_step == 2`, `target_env == "leonardo"`, `arm == "v133"`, a non-null `board_signature`, and `shield_rev_declared` byte-equal to the operator's declared string — **or** `PREPROOF.md` states the proof is partial and names exactly which call blocked it.
    - If `touch_1200.py` ran: `touch.json` exists, the recorded argv contains no `--wait-new-port` token, and the measured touch-to-probe elapsed time is written next to the 3.487 s / 3.878 s reference figures.
    - If `capture_provenance.py` was modified: `git diff` shows only additive optional arguments and their handling, the Uno code path is unchanged, `python3 .planning/v1.34/tools/capture_provenance.py --selftest` exits 0, and the new seam has its own named selftest leg.
    - `bash .planning/v1.34/tools/run_gates.sh` exits 0 with 12/12 selftests and 5/5 live gates, exit code captured directly.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` are both empty.
    - `PREPROOF.md` states both non-claims (the shield is a carrier; the Leonardo chip-out exemption changes nothing for A1/A2).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import os,json
C='/workspaces/.planning/v1.34/bench/cells/BRINGUP-leonardo-provenance'
assert os.path.isfile(C+'/PREPROOF.md'), 'PREPROOF.md missing'
t=open(C+'/PREPROOF.md').read()
assert 'P-02' in t and 'touch' in t.lower(), 'sequence not recorded'
p=C+'/provenance_BRINGUP-leonardo-provenance.json'
if os.path.isfile(p):
    d=json.load(open(p))
    assert d['captured_at_step']==2, d['captured_at_step']
    assert d['target_env']=='leonardo' and d['arm']=='v133', (d['target_env'],d['arm'])
    assert d.get('board_signature'), d
    print('OK full proof', d['board_signature'], d['shield_rev_declared'])
else:
    assert 'partial' in t.lower(), 'no provenance and PREPROOF does not declare a partial proof'
    print('OK partial proof declared')"</automated>
    <automated>python3 -c "
import os,json
C='/workspaces/.planning/v1.34/bench/cells/BRINGUP-leonardo-provenance'
p=C+'/touch.json'
if os.path.isfile(p):
    s=json.dumps(json.load(open(p)))
    assert 'wait-new-port' not in s, 'refuted flag present in touch record'
    print('OK touch record clean')
else:
    print('OK no touch needed')"</automated>
    <automated>bash /workspaces/.planning/v1.34/tools/run_gates.sh > /tmp/161-02-gates.out 2> /tmp/161-02-gates.err; RC=$?; echo "run_gates_rc=$RC"; grep -c 'selftest PASS' /tmp/161-02-gates.out; test "$RC" -eq 0</automated>
    <automated>git -C /workspaces/firestarter status --porcelain; git -C /workspaces/firestarter_app status --porcelain; echo submodules_clean_checked</automated>
  </verify>
  <done>A3/B2's `P-02` has an evidence-backed, copy-runnable command sequence and `capture_provenance.py` is known either to complete on a Leonardo or to need a specific, gated seam that now exists.</done>
</task>

<task type="auto">
  <name>Task 5: Record the pre-proof outcomes and hand the bench to cell A1</name>
  <files>.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/PREPROOF.md, .planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/PREPROOF.md</files>
  <read_first>
    - `.planning/v1.34/bench/EVIDENCE.jsonl` line 1 — `bringup_cell_id_prefix` and `bringup_row_exclusion`, which are why these two cells carry the `BRINGUP-` prefix.
    - `.planning/v1.34/PROCEDURE.md` `P-11` (post-Amendment-3) — the leave-state requirement whose shape this teardown follows even though these are not sweep cells.
    - `.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-CONTEXT.md` — the "A1's opening sequence" paragraph and D-10's locked property (the pre-proof happens **before A1**).
  </read_first>
  <action>
Assert `EVIDENCE.jsonl` is unchanged by this plan: its line count and its sha256 must equal the
values recorded at the end of plan 161-01. These two cells produce **no** evidence row — they
hold no `WRV-VERDICT.json`, `append_evidence.py` would refuse them by construction, and their
`BRINGUP-` prefix keeps them out of the close-out reconciliation. Write that reasoning into both
`PREPROOF.md` files so a later reader does not read the absence as a gap.

Run the `~/.firestarter` baseline check from Amendment 3 clause (4): the directory holds exactly
one file, `config.json`, 30 bytes, sha256
`b323867c1f01b22a705dd9caf003ab7302a249fe46772f5b02e44aaa2760dd79`, tree sha
`423546cd37b5b45d9654e5acd07bd7e2a3c9e1df77e4d5feb79951bf37329951`, mtime `1787817565`. A
**change** is the `P-H1` finding; the directory's mere existence is not. Never attempt deletion.
Then run
`python3 /workspaces/.planning/v1.34/tools/check_arms.py --expect-config-sha
77adfdd26ed8710c4a70882e6dc9ee7bb494286fe225000d581f9e730dd77ad0
--out $CELLDIR/check_arms_teardown.json` and record it.

Record the leave-state in both `PREPROOF.md` files and in the plan SUMMARY, in the `P-11` shape:
board attached, port, arm on it, chip seated (none), pot (untouched), shield. State plainly what
plan 161-03 inherits: the **Uno + Rev 2.0 with W27C512 seated and the v1.33 arm flashed** is
sitting disconnected on the bench, and A1's own `P-01` is where it goes back on — with its chip
coming **out** as part of that same handover, because A1's `P-02` runs an avrdude signature probe
and the Uno-class chip-out rule covers signature probes, not only writes.

Update `.planning/STATE.md`'s SAFETY line to describe the board actually attached at the end of
this plan, following Phase 160's established convention: name the attached board, the seated chip
(none), and what operations are consequently unsafe.

Commit the two cell directories to the meta repo. Do not commit anything under a `reads/`
directory — there is none here.
  </action>
  <acceptance_criteria>
    - `EVIDENCE.jsonl`'s line count is unchanged from the end of plan 161-01 and no row carries a `cell_id` beginning `BRINGUP-uno328pb-v133` or `BRINGUP-leonardo-provenance`.
    - Both `PREPROOF.md` files state why no evidence row exists for these cells, citing the `BRINGUP-` prefix and the absence of a `WRV-VERDICT`.
    - The `~/.firestarter` check is recorded with all four measured baseline values and an explicit `unchanged` / `CHANGED — P-H1` verdict; no deletion was attempted.
    - `check_arms_teardown.json` exists and records the expected config-dir SHA as matched.
    - Both `PREPROOF.md` files carry a leave-state block naming board, port, arm, chip, pot and shield.
    - `.planning/STATE.md`'s SAFETY line names the board attached at the end of this plan and states that no chip is seated.
    - The meta-repo commit touches only paths under `.planning/v1.34/bench/cells/BRINGUP-uno328pb-v133/`, `.planning/v1.34/bench/cells/BRINGUP-leonardo-provenance/`, `.planning/STATE.md`, and — only if Task 4 required it — `.planning/v1.34/tools/capture_provenance.py` and `.planning/v1.34/rig-pins.json`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json,hashlib,pathlib
j=pathlib.Path('/workspaces/.planning/v1.34/bench/EVIDENCE.jsonl')
lines=[l for l in j.read_text().splitlines() if l.strip()]
ids=[json.loads(l).get('cell_id') for l in lines[1:]]
for i in ids:
    assert not str(i).startswith('BRINGUP-uno328pb-v133'), i
    assert not str(i).startswith('BRINGUP-leonardo-provenance'), i
print('OK rows:',len(lines)-1,'cell_ids:',ids)"</automated>
    <automated>python3 -c "
import hashlib,pathlib,os
p=pathlib.Path.home()/'.firestarter'
files=sorted(q.relative_to(p).as_posix() for q in p.rglob('*') if q.is_file())
h=hashlib.sha256()
for rel in files:
    h.update(rel.encode()); h.update((p/rel).read_bytes())
cfg=hashlib.sha256((p/'config.json').read_bytes()).hexdigest()
print('files',files); print('tree_sha',h.hexdigest()); print('config_sha',cfg); print('mtime',int(p.stat().st_mtime))
assert files==['config.json'], files
assert cfg=='b323867c1f01b22a705dd9caf003ab7302a249fe46772f5b02e44aaa2760dd79', cfg
assert h.hexdigest()=='423546cd37b5b45d9654e5acd07bd7e2a3c9e1df77e4d5feb79951bf37329951', h.hexdigest()
print('OK ~/.firestarter unchanged from Amendment 3 baseline')"</automated>
    <automated>python3 -c "
import os
for c in ['BRINGUP-uno328pb-v133','BRINGUP-leonardo-provenance']:
    f='/workspaces/.planning/v1.34/bench/cells/%s/PREPROOF.md'%c
    t=open(f).read()
    for k in ['leave-state','BRINGUP-','WRV-VERDICT']:
        assert k in t, (c,k)
print('OK preproof records')"</automated>
  </verify>
  <done>Both pre-proofs are recorded as bring-up cells with no evidence row, the config-dir baseline is re-asserted, and the bench is handed to cell A1 with an explicit statement of what A1's `P-01` must do first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator declaration → record | the shield revision and the device node enter the record as untested assertions; the signature probe is the only independent check available, and it does not cover the shield |
| host → AVR bootloader | avrdude and PlatformIO drive flash; a wrong-arm or wrong-board flash silently produces a wrong result |
| pinned binary set → invoked binary | a `PATH`-resolved `firestarter`, `avrdude`, `git` or `pio` would be an un-named third arm or an unpinned toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-06 | Spoofing | board identity on a shuffled `ttyACM*`/`ttyUSB*` node | high | mitigate | enumerate nodes before and after every swap with timestamps; `probe_board.py` signature probe compared against `pins.targets[env].mcu`; two ACM nodes were present at planning time so the name is explicitly not a discriminator |
| T-161-07 | Tampering | flashing the wrong arm | high | mitigate | assert `git rev-parse HEAD` equals `pins.arms.v133.fw_sha` with an empty porcelain before upload; prove the result with an independent `judge_readback.py` read, never avrdude's own verify pass |
| T-161-08 | Repudiation | a pre-proof recorded as passing when it did not | high | mitigate | `judged_match` plus a runtime `hex_span_expected_by_arm` lookup is the verdict; the raw-span SHA pair is explicitly excluded from the check so a false RED cannot be "fixed" by comparing the wrong fields |
| T-161-09 | Tampering | a tool fix that silently changes the Uno path | medium | mitigate | any `capture_provenance.py` seam is additive optional arguments only, with a named selftest leg and a full `run_gates.sh` re-run at 12/12 |
| T-161-10 | Elevation of privilege | a forbidden destructive flag reaching a bench command | medium | mitigate | `forbidden_flags` enforced by exact token match in `gate_record.check_commands()`; no chip is seated in this plan, so no chip-destructive path is reachable at all |
| T-161-SC | Tampering | package installs | low | accept | zero packages installed in any ecosystem; the rig is stdlib-only with `pyserial` pre-existing in `touch_1200.py` |
</threat_model>

<verification>
- `BRINGUP-uno328pb-v133/READBACK-VERDICT.json` has `judged_match: true`,
  `flashed_arm == expect_arm == "v133"`, and `judged_span_bytes` equal to a **runtime** lookup of
  `targets.uno328pb.hex_span_expected_by_arm.v133`, with both vector exclusions applied.
- No check anywhere compares `sha_actual_judged` with `sha_expected_judged`.
- `BRINGUP-leonardo-provenance/PREPROOF.md` holds an ordered, copy-runnable `P-02` sequence with
  measured return codes and elapsed times.
- `bash .planning/v1.34/tools/run_gates.sh` exits 0 at 12/12 selftests and 5/5 live gates, exit
  code read directly.
- `EVIDENCE.jsonl` is unchanged: no row was added by this plan.
- `~/.firestarter` matches the Amendment 3 baseline on all four pinned values.
- Both sub-repos' `git status --porcelain` are empty.
</verification>

<success_criteria>
The two first-contact risks that sit on a cell's critical path have been met off the critical
path: a v1.33 flash on the ATmega328PB is read back and judged against the v133 arm's own span,
and A3/B2's `P-02` has an evidence-backed command sequence. Neither pre-proof produced a sweep
position or an evidence row, both sub-repos are byte-unchanged, and the per-cell gate is green
with any tool change in it.
</success_criteria>

<output>
Create `.planning/phases/161-board-board-sweep-three-boards-on-rev-2-0/161-02-SUMMARY.md` when
done. Record: the measured `judged_span_bytes` and `judged_match` for the 328PB pre-proof; the
Leonardo sequence as an ordered command list with rc and elapsed per call; whether
`capture_provenance.py` needed a seam and exactly what changed if so; the `run_gates.sh` counts
and exit code; the `~/.firestarter` verdict; and the leave-state handed to plan 161-03.
</output>
