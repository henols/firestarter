---
phase: 81-2516-db-entry-non-destructive-read-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_database_conversion.py
autonomous: true
requirements: [DB-02, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "FLAG_CAN_ERASE is set on the wire dict for a Flash/EEPROM chip (W29C040, algorithm 0x05)"
    - "FLAG_CAN_ERASE is set on the wire dict for an EEPROM chip (W27C512, algorithm 0x07)"
    - "FLAG_CAN_ERASE is NOT set on the wire dict for a UV-EPROM chip (M27C512)"
    - "The 0xA4 ack_data=False regression guard (test_init_phase_data_frames_not_acked) is green"
    - "The full host suite is green before any bench session"
  artifacts:
    - path: "firestarter_app/tests/test_database_conversion.py"
      provides: "Flash/EEPROM-specific FLAG_CAN_ERASE pinning test (D-05)"
      contains: "def test_convert_w29c040_flash_eeprom_flag_can_erase"
  key_links:
    - from: "firestarter_app/firestarter/database.py::convert_to_programmer"
      to: "FLAG_CAN_ERASE (constants.py 0x02)"
      via: "electrical-type in (EEPROM, Flash/EEPROM) -> simple_flags |= FLAG_CAN_ERASE (line 605)"
      pattern: "electrical-type.*EEPROM.*Flash/EEPROM"
---

<objective>
DB-02: a fresh, from-scratch adversarial re-audit of the `FLAG_CAN_ERASE` decode chain
end-to-end — WITHOUT assuming Phase 77's conclusion holds (per D-04) — pinned by a
Flash/EEPROM-specific test (D-05), with the 0xA4 regression guard and the full host
suite confirmed green (SAFE-02/03).

Purpose: W29C020 / W29C040 are `Flash/EEPROM` (not `EEPROM`) and get bench-proven for
the FIRST time in Phase 82. The Flash/EEPROM erase branch must be independently proven
on this branch, not inherited from Phase 77's EEPROM-only proof. This is a pure-software
pre-bench gate; the bench sweep (Plan 03) and Phase 82 writes depend on it being sound.

Output: a new pinning test `test_convert_w29c040_flash_eeprom_flag_can_erase` in
`tests/test_database_conversion.py`, plus a recorded re-audit trace of the decode chain
in the plan SUMMARY (no production-code change is anticipated — DB-02 is read/test-only).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-CONTEXT.md
@.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-PATTERNS.md
@.planning/research/PITFALLS.md

# The DB-02 decode chain (re-audit these end-to-end per D-04)
@firestarter_app/tools/build_db.py
@firestarter_app/firestarter/database.py
@firestarter_app/firestarter/constants.py
@firestarter_app/tests/test_database_conversion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh adversarial re-audit of the FLAG_CAN_ERASE decode chain (DB-02 / D-04)</name>
  <files>(no file modified — produces a written re-audit trace recorded in the plan SUMMARY)</files>
  <read_first>
    - firestarter_app/tools/build_db.py — Pass-2 `_etype` re-derivation (`flags & 0x10` MP_ERASE_MASK -> EEPROM / UV-EPROM / Flash/EEPROM), the canonical erase-mask ground truth
    - firestarter_app/firestarter/database.py — `_map_data` (~line 418-456: synthetic `info_flags |= 0x10` when `electrical.get("type") in ("EEPROM","Flash/EEPROM")` at line 434, and the `electrical-type` passthrough at line 456) AND `convert_to_programmer` (~lines 593-606: the `FLAG_CAN_ERASE` set site keyed on `full_eprom_data.get("electrical-type","") in ("EEPROM","Flash/EEPROM")` at line 605)
    - firestarter_app/firestarter/constants.py — `FLAG_CAN_ERASE = 0x02` (line 80)
    - firestarter/include/firestarter.h — `#define FLAG_CAN_ERASE 0x02` (line 60); confirm Python/C parity (SAFE-03)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-CONTEXT.md — Phase 77 D-01/D-02 prior art (the conclusion this audit must independently re-derive, NOT trust)
  </read_first>
  <action>
    Re-derive the FLAG_CAN_ERASE path from scratch for a `Flash/EEPROM` chip (W29C040, algorithm 0x05) WITHOUT assuming Phase 77 holds (D-04). Walk: (1) build_db.py decodes `electrical.type` from the minipro erase mask `flags & 0x10`; (2) database.py `_map_data` carries `electrical-type` through verbatim and synthesizes `info-flags |= 0x10` for both `EEPROM` and `Flash/EEPROM`; (3) `convert_to_programmer` at line 605 sets `simple_flags |= FLAG_CAN_ERASE` when `electrical-type in ("EEPROM","Flash/EEPROM")` — NOT via the fragile synthetic `info-flags & 0x10` round-trip. Confirm the firmware consumer: `eprom_write_init` honors FLAG_CAN_ERASE, and `configure_eeprom28c` (0x0D path) ignores it (Phase 77 D-03 carryover). Verify the live behavior empirically: load `EpromDatabase(skip_local_override=True)`, call `convert_to_programmer(get_eprom("W29C040"))`, assert `flags & 0x02 == 0x02`; repeat for W29C020 (also Flash/EEPROM). Record in the SUMMARY whether a real gap exists. EXPECTED finding (from research): the chain is SOUND, the Flash/EEPROM branch is already covered at line 605 — no production change needed. If the audit finds the Flash/EEPROM branch is actually broken, FIX it in convert_to_programmer (extend the membership tuple) and note the lockstep parity check on constants.py <-> firestarter.h (no constant value change expected; FLAG_CAN_ERASE stays 0x02). Confirm `FLAG_CAN_ERASE = 0x02` matches between constants.py and firestarter.h (SAFE-03).
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -c "from firestarter.database import EpromDatabase; db=EpromDatabase(skip_local_override=True); print(all(db.convert_to_programmer(db.get_eprom(c))['flags'] & 0x02 for c in ('W29C040','W29C020','W27C512')) and db.convert_to_programmer(db.get_eprom('M27C512'))['flags'] & 0x02 == 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The one-liner above prints `True` (W29C040, W29C020, W27C512 all carry FLAG_CAN_ERASE; M27C512 UV-EPROM does not).
    - `grep -n 'FLAG_CAN_ERASE' firestarter_app/firestarter/constants.py firestarter/include/firestarter.h` shows `0x02` in both files (parity, SAFE-03).
    - The SUMMARY records the four-stage chain trace and an explicit verdict: "Flash/EEPROM branch SOUND (no change)" or "gap found + fixed".
  </acceptance_criteria>
  <done>The decode chain is independently re-derived for the Flash/EEPROM branch, the live wire-dict behavior is empirically confirmed for W29C040/W29C020, constant parity is verified, and the verdict (sound vs fixed) is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Flash/EEPROM-specific FLAG_CAN_ERASE pinning test + confirm 0xA4 guard + full suite green (D-05 / SAFE-02)</name>
  <files>firestarter_app/tests/test_database_conversion.py</files>
  <read_first>
    - firestarter_app/tests/test_database_conversion.py — the `db` fixture (lines 11-14, `skip_local_override=True`), the `FLAG_CAN_ERASE` import (line 7), and the analog `test_convert_at28c256_flash_eeprom_flag_can_erase` (lines 98-104) plus the `# ---` separator after it
    - .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-PATTERNS.md — the exact new-assertion body for W29C040 (lines 145-156)
    - firestarter_app/tests/test_eprom_operations.py — `test_init_phase_data_frames_not_acked` (line 135), the 0xA4 ack_data=False regression guard (SAFE-02)
  </read_first>
  <behavior>
    - Test: `test_convert_w29c040_flash_eeprom_flag_can_erase` asserts `db.convert_to_programmer(db.get_eprom("W29C040"))["flags"] & FLAG_CAN_ERASE` is truthy (Flash/EEPROM, algorithm 0x05, gets the erase flag).
    - Existing negative control `test_convert_uv_eprom_no_flag_can_erase` (M27C512) still passes (UV-EPROM stays clear).
    - The 0xA4 guard `test_init_phase_data_frames_not_acked` still passes.
  </behavior>
  <action>
    Append `test_convert_w29c040_flash_eeprom_flag_can_erase(db: EpromDatabase) -> None` after `test_convert_at28c256_flash_eeprom_flag_can_erase` (after line 104) and before the `# ---` separator at line 107. Body: fetch `db.get_eprom("W29C040")`, assert it is not None, `out = db.convert_to_programmer(full)`, assert `out["flags"] & FLAG_CAN_ERASE`. Reuse the shared module-scope `db` fixture and the already-present `FLAG_CAN_ERASE` import — add NO new imports. Docstring: cite that this extends the electrical-type derivation lock (ERASE-01 / D-01/D-02) to the flash4 (0x05) Flash/EEPROM family per D-05. Do NOT add a duplicate UV-EPROM negative control — `test_convert_uv_eprom_no_flag_can_erase` already exists. Then confirm the 0xA4 guard and the full suite are green; because the devcontainer runs Python 3.12 which masks CI py3.9/3.11, run `ruff check` and `ruff format --check` on the edited test file against the configured target before declaring green.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -m pytest tests/test_database_conversion.py::test_convert_w29c040_flash_eeprom_flag_can_erase tests/test_eprom_operations.py::test_init_phase_data_frames_not_acked -q && ruff check tests/test_database_conversion.py && ruff format --check tests/test_database_conversion.py && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_database_conversion.py::test_convert_w29c040_flash_eeprom_flag_can_erase -q` exits 0.
    - `pytest tests/test_eprom_operations.py::test_init_phase_data_frames_not_acked -q` exits 0 (0xA4 guard green, SAFE-02).
    - `ruff check tests/test_database_conversion.py` and `ruff format --check tests/test_database_conversion.py` both exit 0 (py3.9/3.11 target, not the 3.12 default).
    - The full suite `pytest -q` exits 0 with coverage >= 70 (no `--cov-fail-under=70` regression).
    - No new import lines added to test_database_conversion.py (the FLAG_CAN_ERASE import at line 7 is reused).
  </acceptance_criteria>
  <done>The Flash/EEPROM pinning test exists and passes, the 0xA4 regression guard is green, ruff is clean against the CI target, and the full host suite is green at >=70% coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB decode -> wire JSON `flags` -> firmware write-init | An incorrect `FLAG_CAN_ERASE` derivation reaches the firmware erase-before-write guard; a wrongly-cleared flag skips auto-erase (Phase 82 write verify fails as OR-of-old-and-new bits), a wrongly-set flag is firmware-inert on the 0x0D path |
| Host test suite -> bench session gate | A removed/red 0xA4 guard lets the empty-input desync regression resurface on every default EEPROM write in Phase 82 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Tampering | `convert_to_programmer` FLAG_CAN_ERASE derivation for Flash/EEPROM | mitigate | Fresh adversarial re-audit (D-04, not Phase-77-trust) + a Flash/EEPROM-specific pinning test (D-05) asserting W29C040 carries the flag and M27C512 (UV) does not |
| T-81-02 | Denial of Service | 0xA4 empty-input write desync guard | mitigate | `test_init_phase_data_frames_not_acked` confirmed green (SAFE-02) before any bench session; flagged in this plan's verify so a removed/red guard blocks progress |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all tooling (pytest/ruff/mypy) pre-installed; EVID-02 forbids new dependencies |
</threat_model>

<verification>
- `cd firestarter_app && python3 -m pytest -q` exits 0, coverage >= 70.
- `ruff check tests/test_database_conversion.py && ruff format --check tests/test_database_conversion.py` exit 0 against the CI target (py3.9/3.11), not the devcontainer 3.12 default.
- `grep -c 'FLAG_CAN_ERASE' firestarter_app/firestarter/constants.py firestarter/include/firestarter.h` shows the constant present in both (parity, SAFE-03).
</verification>

<success_criteria>
- DB-02: the FLAG_CAN_ERASE chain is independently re-audited for both EEPROM and Flash/EEPROM electrical types; the Flash/EEPROM branch is pinned by `test_convert_w29c040_flash_eeprom_flag_can_erase`.
- SAFE-02: the 0xA4 `test_init_phase_data_frames_not_acked` guard is green and the full host suite is green before any bench session.
- SAFE-03: FLAG_CAN_ERASE parity between constants.py and firestarter.h is confirmed (no constant change expected).
</success_criteria>

<output>
Create `.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-01-SUMMARY.md` when done.
Record the four-stage decode-chain re-audit trace and the explicit verdict (Flash/EEPROM branch SOUND, no change / gap found + fixed).
</output>
