---
phase: 81-2516-db-entry-non-destructive-read-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.firestarter/database.json
  - .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md
  - .planning/v1.15/bench/EVIDENCE.md
  - .planning/v1.15/bench/EVIDENCE.json
autonomous: false
requirements: [GRAD-01, GRAD-02, EVID-01, EVID-02]
must_haves:
  truths:
    - "firestarter info 2516 decodes algorithm 0x0B, pinout DIP24_2716, UV-EPROM, vpp_mv 25000, 2048 bytes"
    - "The 2516 is confirmed absent from minipro infoic.xml (the 28 hits are 25160 SPI parts) — recorded as GRAD-01 findings"
    - "The 2516 user-override entry is manually safety-reviewed against the 6 D-02 values"
    - "The operator personally signs off on 81-2516-SAFETY-REVIEW.md before any bench session (human gate)"
    - "The EVIDENCE.{md,json} record exists with the locked column schema, ready for the Plan 03 sweep"
  artifacts:
    - path: "~/.firestarter/database.json"
      provides: "2516 user-override entry (name-keyed add-new path)"
      contains: "\"name\": \"2516\""
    - path: ".planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md"
      provides: "SR-1 safety-review checklist + operator sign-off line (D-01/D-02)"
      contains: "Operator sign-off"
    - path: ".planning/v1.15/bench/EVIDENCE.json"
      provides: "Per-chip evidence record (locked columns), extends v1.13 matrix"
      contains: "cells"
    - path: ".planning/v1.15/bench/EVIDENCE.md"
      provides: "Human-readable per-chip evidence table"
      contains: "Verdict"
  key_links:
    - from: "~/.firestarter/database.json (2516 entry)"
      to: "EpromDatabase._merge_databases add-new-item path"
      via: "name key (not part_number) triggers add at database.py line 244-246"
      pattern: "name.*2516"
    - from: "81-2516-SAFETY-REVIEW.md"
      to: "DIP24_2716 pinout (vpp-pin 21, VCC_PROG 25.0)"
      via: "manual SR-1 pin-map verification (D-02 step 4)"
      pattern: "vpp-pin.*21"
---

<objective>
Author the hand-written `2516` user-override DB entry, record its datasheet-grounded
research (GRAD-01), capture the manual safety review in a dedicated
`81-2516-SAFETY-REVIEW.md` and obtain the operator's personal sign-off (D-01 human gate),
and scaffold the milestone evidence record `.planning/v1.15/bench/EVIDENCE.{md,json}`
with the locked column schema (EVID-01/02).

Purpose: the 2516 is the one genuine graduation candidate, absent from minipro upstream,
so it must be a hand-authored user-override that BYPASSES `check_dispatch.py`/`diff_db.py`
— making the manual safety review the only gate before Phase 83 writes the IRREPLACEABLE
chip. Per D-10 the entry must exist before the Plan 03 sweep can read the 2516 (its 11th
chip). The EVIDENCE scaffold is the structure the Plan 03 sweep populates.

Output: the 2516 entry in `~/.firestarter/database.json`; the signed
`81-2516-SAFETY-REVIEW.md`; and the empty-but-schema-complete EVIDENCE.{md,json}.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-CONTEXT.md
@.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md

# Analogs to copy
@firestarter_app/firestarter/data/chip_database.json
@firestarter_app/firestarter/data/pinouts.json
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md
@firestarter_app/firestarter/database.py
@firestarter_app/firestarter/chip_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research the 2516 (GRAD-01) + author the user-override DB entry (GRAD-02)</name>
  <files>~/.firestarter/database.json</files>
  <read_first>
    - firestarter_app/firestarter/data/chip_database.json — the AM2716 entry (lines 66-86): the exact UV-EPROM / DIP24_2716 / algorithm 11 / 25V structure to copy
    - firestarter_app/firestarter/data/pinouts.json — `DIP24_2716` (line 2): confirm vpp-pin [21], vcc-pin [24], gnd-pin [12], ce-pin [18], oe-pin [20], VCC_PROG 25.0
    - firestarter_app/firestarter/database.py — `_merge_databases` (lines 226-250): the `"name"`-key add-new-item path (lines 244-246) for user-override entries
    - firestarter_app/firestarter/chip_resolver.py — `resolve_chip` (lines 16-63): only `support_status == "supported"` passes the host guard
    - .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-PATTERNS.md — the 2516 entry structure + user-override wrapper (lines 22-79)
    - .planning/research/PITFALLS.md — Pitfall 8 (user-override bypasses safety gates; highest-risk field is vpp_mv 25000 not 12000)
  </read_first>
  <action>
    GRAD-01 research first: confirm the 2516 is ABSENT from minipro `infoic.xml` — grep the snapshot for "2516" and verify all hits are `25160` SPI serial parts (not the parallel UV-EPROM); capture the datasheet-class facts (Intel/TI 2516 = 24-pin NMOS UV-EPROM, 2K x 8 = 2048 bytes, ~25V VPP class, DIP24, 2716 read-compatible). Record these findings in the plan SUMMARY (feeds the D-02 checklist). Then author the entry in `~/.firestarter/database.json` using a manufacturer-like top-level key such as `"INTEL"`. Use a `"name"` key (NOT `"part_number"`) so `_merge_databases` engages the add-new-item path. Set: `electrical.pin_count`=24, `electrical.size_bytes`=2048, `electrical.type`="UV-EPROM", `electrical.vcc`="5V", `electrical.vpp`="25V", `electrical.vpp_mv`=25000; `pinout`="DIP24_2716"; `programming.algorithm`=11 (0x0B), `programming.chip_id_check`=false, `programming.chip_id_value`="0x00000000", `programming.pulse_duration`="500 us"; `support_status`="supported". If `~/.firestarter/database.json` already exists with other entries, MERGE the 2516 in (do not clobber existing user overrides); if absent, create it with the single INTEL->[2516] array. Do NOT run build_db.py — this entry is hand-authored and never flows through the generated DB.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -c "from firestarter.database import EpromDatabase; db=EpromDatabase(skip_local_override=False); e=db.get_eprom('2516'); assert e is not None, '2516 not merged'; o=db.convert_to_programmer(e); print('flags_can_erase', o['flags']&0x02); import json,os; raw=json.load(open(os.path.expanduser('~/.firestarter/database.json'))); print('name-keyed' if any(i.get('name')=='2516' for v in raw.values() for i in v) else 'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter info 2516` output contains `0x0B` (algorithm), `DIP24_2716`, `UV-EPROM`, `25000` (or `25V`), and `2048` bytes (`firestarter info 2516` exits 0 with the override installed).
    - The Python check prints `name-keyed` (the 2516 was merged via the add-new path) and `flags_can_erase 0` (UV-EPROM must NOT carry FLAG_CAN_ERASE — D-02 step 3).
    - GRAD-01 findings (2516 absent from infoic.xml; the 28 hits are 25160 SPI; datasheet class captured) are recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The 2516 user-override entry exists in `~/.firestarter/database.json`, is merged via the name-keyed path, decodes correctly via `firestarter info 2516` (0x0B / DIP24_2716 / UV-EPROM / 25000 / 2048), carries NO FLAG_CAN_ERASE, and the GRAD-01 research is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Author 81-2516-SAFETY-REVIEW.md checklist + scaffold EVIDENCE.{md,json} (D-01/D-02, EVID-01/02)</name>
  <files>.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md, .planning/v1.15/bench/EVIDENCE.md, .planning/v1.15/bench/EVIDENCE.json</files>
  <read_first>
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md — the SR-1 header, 9-item legend, summary table + operator sign-off format to copy
    - firestarter_app/firestarter/data/pinouts.json — `DIP24_2716` pin-map (vpp-pin 21, ce-pin 18, oe-pin 20, vcc-pin 24, gnd-pin 12, VCC_PROG 25.0) for the SR-1 pin verification
    - firestarter_app/val-results/eprom/validation-matrix.json — the v1.13 per-family matrix JSON schema to EXTEND (cells array, verdict, pass_type, evidence_sha, retry_count)
    - firestarter_app/val-results/eprom/validation-matrix.md — the markdown table format to extend
    - .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-PATTERNS.md — the SAFETY-REVIEW header (lines 87-126) and the EVIDENCE schema adaptations (lines 164-203)
  </read_first>
  <action>
    Author `81-2516-SAFETY-REVIEW.md` as an SR-1 checklist (copy the 58-SR-1 header/legend/summary-table/sign-off structure). Note GATE-03 is N/A — user-override entries are not scanned by `tools/check_dispatch.py`, so manual review compensates. The checklist MUST verify the 6 D-02 values against the TMS2516/2516 datasheet: (1) `algorithm = 0x0B` routes to `configure_eprom`; (2) `vpp_mv = 25000` is at-or-below `RURP_VPP_CEILING_MV = 25000` (at the ceiling, not over); (3) `electrical.type = "UV-EPROM"` so FLAG_CAN_ERASE is NOT set; (4) `pinout = DIP24_2716` exists in pinouts.json AND its pin-map routes VPP=pin 21, CE=pin 18, OE=pin 20, VCC=pin 24, GND=pin 12 per datasheet; (5) `support_status = "supported"`; (6) `size_bytes = 2048`. DIP24_2716 HAS a vpp-pin, so item 1 is a positive VPP-presence check (it is a genuine 25V UV-EPROM, unlike the 5V DIP24_2816). Include a `firestarter info 2516` transcript as decode evidence (Claude's-discretion item — include it). End with a `**Operator sign-off:** [ ] Approved — _name / date_` line and an `**Overall SR-1 result: PASS / NEEDS-REVIEW**` line; mark each item PASS/FAIL with the actual value. Then create `.planning/v1.15/bench/` (does not yet exist) and scaffold EVIDENCE.json + EVIDENCE.md with the LOCKED columns (chip, family/algorithm, board+shield, blank-state, op, SHA-or-N/A, verdict, anomalies) plus the EVID-extension fields (read_count for N>=3, blank_check_result, sha256); seed all 11 chip rows (W27C512, W27E512, SST27SF512, W27E040, SST39SF040, W29C020, W29C040, FM1608, ST M27C512, AM27C020, 2516) with verdict="pending" so Plan 03 fills them. Set `"harness_version": "81"`. EXTEND the v1.13 matrix shape (do not invent an incompatible schema).
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "import json; d=json.load(open('.planning/v1.15/bench/EVIDENCE.json')); assert d['harness_version']=='81'; assert len(d['cells'])==11, 'need 11 chip rows'; keys={'chip','family','board','shield','blank_state','op','sha256','verdict','anomalies'}; missing=[c.get('chip') for c in d['cells'] if not keys.issubset(c.keys())]; print('ok' if not missing else ('FAIL '+str(missing)))" && test -f .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md && grep -q "Operator sign-off" .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md && grep -q "vpp-pin" .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md && echo "review-doc-ok"</automated>
  </verify>
  <acceptance_criteria>
    - The Python check prints `ok` (EVIDENCE.json has `harness_version="81"`, exactly 11 chip cells, each carrying all locked columns chip/family/board/shield/blank_state/op/sha256/verdict/anomalies).
    - `81-2516-SAFETY-REVIEW.md` exists and contains an `Operator sign-off` line and a `vpp-pin` verification line (`review-doc-ok` printed).
    - The 6 D-02 values each appear in the checklist with a PASS/FAIL mark and the actual value (algorithm 0x0B, vpp_mv 25000, UV-EPROM, DIP24_2716 with VPP=pin 21, support_status supported, size_bytes 2048).
    - EVIDENCE.md is a human-readable table with a `Verdict` column and all 11 chip rows seeded `pending`.
  </acceptance_criteria>
  <done>The SR-1 safety-review doc verifies all 6 D-02 values against the datasheet and DIP24_2716 pin-map, includes a `firestarter info 2516` transcript and an operator sign-off line; the EVIDENCE.{md,json} record exists with the locked schema and 11 pending chip rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator personally signs off on the 2516 safety review (D-01 human gate, GRAD-02)</name>
  <what-built>
    Task 1 authored the hand-written 2516 user-override entry in `~/.firestarter/database.json`
    (algorithm 0x0B, pinout DIP24_2716, UV-EPROM, vpp_mv 25000, size 2048, support_status supported)
    — an entry that BYPASSES `check_dispatch.py`/`diff_db.py`. Task 2 captured the manual safety
    review in `81-2516-SAFETY-REVIEW.md` verifying the 6 D-02 values against the TMS2516 datasheet
    and the DIP24_2716 pin-map (VPP=pin 21), including a `firestarter info 2516` decode transcript.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-2516-SAFETY-REVIEW.md`
       end to end — confirm each of the 6 D-02 items is marked PASS with the correct value,
       especially `vpp_mv = 25000` (NOT 12000 — Pitfall 8: wrong VPP is the highest-risk error)
       and `electrical.type = UV-EPROM` (so FLAG_CAN_ERASE is not set).
    2. Run `firestarter info 2516` yourself and confirm the decode matches: algorithm 0x0B,
       pinout DIP24_2716, UV-EPROM, 25000 mV (or 25V), 2048 bytes.
    3. Confirm VPP routes to DIP24 pin 21 per the datasheet (the chip is irreplaceable; Phase 83
       writes it based on this decode being correct).
    4. If everything is correct, fill in the `**Operator sign-off:** [x] Approved — <your name> / <date>`
       line in `81-2516-SAFETY-REVIEW.md`. If anything is wrong, describe the issue so it can be fixed.
  </how-to-verify>
  <action>
    Operator-only human gate (D-01) — no Claude automation possible. Present the authored 2516
    user-override entry and the `81-2516-SAFETY-REVIEW.md` checklist to the operator, walk the 6 D-02
    values and the DIP24_2716 VPP=pin-21 routing, and PAUSE for the operator to personally verify and
    sign. This gate is blocking-human and never auto-approvable (the chip is irreplaceable; Phase 83
    writes it based on this decode). On "approved" with the sign-off line filled in, record the sign-off
    status in the SUMMARY; on a discrepancy, fix the entry/checklist and re-present before resuming.
  </action>
  <verify>
    <human-check>Operator confirms the `**Operator sign-off:** [x] Approved` line is filled in 81-2516-SAFETY-REVIEW.md and the 6 D-02 values match `firestarter info 2516`.</human-check>
  </verify>
  <done>The operator has personally reviewed and signed off on the 2516 safety review; the sign-off line is filled in and the SUMMARY records the approval.</done>
  <resume-signal>Type "approved" (and confirm the sign-off line is filled in), or describe the discrepancy to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.firestarter/database.json` (user-override) -> EpromDatabase merge -> chip_resolver -> wire JSON -> firmware | The 2516 override bypasses `check_dispatch.py`/`diff_db.py` — a wrong vpp_mv / algorithm / pinout reaches the hardware with zero automated software-layer protection |
| Manual safety review -> operator sign-off -> bench authorization | The human sign-off is the SOLE compensating control before Phase 83 writes the irreplaceable 2516 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03 | Tampering | 2516 user-override `vpp_mv` field (highest-risk: 12000 vs 25000) | mitigate | SR-1 checklist item 2 verifies `vpp_mv = 25000` against the datasheet; the blocking operator sign-off (D-01) gates any bench use; `firestarter info 2516` transcript pins the rendered decode |
| T-81-04 | Elevation of Privilege | 2516 reaching the hardware without review (bypasses automated gates) | mitigate | `81-2516-SAFETY-REVIEW.md` + operator personal sign-off is the compensating control for the bypassed `check_dispatch.py`/`diff_db.py`; the chip is irreplaceable so the gate is blocking-human and never auto-approvable |
| T-81-05 | Spoofing | wrong algorithm (0x07 12V vs 0x0B 25V NMOS) silently writing at the wrong voltage | mitigate | SR-1 item 1 verifies `algorithm = 0x0B` routes to `configure_eprom`; item 3 confirms UV-EPROM (no FLAG_CAN_ERASE); Plan 03 reads (no VPP applied) before any Phase 83 write |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — doc/data authoring only; EVID-02 forbids new dependencies |
</threat_model>

<verification>
- `firestarter info 2516` exits 0 and shows 0x0B / DIP24_2716 / UV-EPROM / 25000 / 2048 with the override installed.
- `81-2516-SAFETY-REVIEW.md` exists with all 6 D-02 items marked, a `firestarter info 2516` transcript, and a filled-in operator sign-off line.
- `.planning/v1.15/bench/EVIDENCE.json` validates: `harness_version="81"`, 11 chip cells, all locked columns present.
- The operator has personally approved the safety review (Task 3 checkpoint resolved).
</verification>

<success_criteria>
- GRAD-01: the 2516's absence from minipro infoic.xml and its datasheet class are confirmed and recorded.
- GRAD-02: the 2516 user-override entry is authored, decodes correctly via `firestarter info 2516`, manually safety-reviewed, and operator-signed.
- EVID-01/02: the per-chip EVIDENCE.{md,json} record exists with the locked column schema (extending the v1.13 matrix), reusing existing artifacts (no new harness/dependency).
</success_criteria>

<output>
Create `.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-02-SUMMARY.md` when done.
Record the GRAD-01 research findings (2516 absent from infoic.xml; datasheet class) and note the operator sign-off status.
</output>
