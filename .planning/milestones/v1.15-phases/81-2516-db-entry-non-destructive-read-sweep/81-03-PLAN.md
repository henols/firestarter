---
phase: 81-2516-db-entry-non-destructive-read-sweep
plan: 03
type: execute
wave: 2
depends_on: ["81-02"]
files_modified:
  - .planning/v1.15/bench/EVIDENCE.md
  - .planning/v1.15/bench/EVIDENCE.json
autonomous: false
requirements: [SWEEP-01, SWEEP-02, EVID-03, SAFE-01]
must_haves:
  truths:
    - "All 11 chips are read end-to-end and blank-checked on Leonardo + Rev 2.0 with zero chips consumed"
    - "Each chip produces an EVIDENCE row (chip, family/algorithm, board+shield, blank-state, op=read+blank_check, SHA-or-N/A, verdict, anomalies)"
    - "The blank-state of each of the 3 UV-EPROMs (ST M27C512, AM27C020, 2516) is recorded — the Phase 83 gate"
    - "Each PASS verdict is non-vacuous: N>=3 byte-identical reads + a negative control (wrong-file verify exits non-zero)"
    - "Every bench task records its preconditions: board=Leonardo, shield=Rev 2.0, controller: identity, live r1 ~= 270000"
  artifacts:
    - path: ".planning/v1.15/bench/EVIDENCE.json"
      provides: "11 populated per-chip cells with verdicts, SHAs, blank-states"
      contains: "PASS"
    - path: ".planning/v1.15/bench/EVIDENCE.md"
      provides: "Human-readable populated 11-chip evidence table"
      contains: "leonardo"
  key_links:
    - from: "firestarter read / dev write-cycle --runs 3"
      to: ".planning/v1.15/bench/EVIDENCE.json cells"
      via: "per-chip read SHA + blank-check result recorded as a cell"
      pattern: "sha256"
    - from: "2516 user-override (Plan 02)"
      to: "the 2516 read (11th chip in this sweep)"
      via: "DB entry must exist before the 2516 read resolves (D-10)"
      pattern: "2516"
---

<objective>
Read end-to-end and blank-check ALL 11 physical chips on Leonardo + RURP Rev 2.0,
consuming zero chips, and populate `.planning/v1.15/bench/EVIDENCE.{md,json}` with a
full per-chip row — establishing the milestone's evidence baseline and recording the
blank-state of the 3 irreplaceable UV-EPROMs that gates every Phase 83 spend-vs-preserve
decision.

Purpose: this is the non-destructive safety baseline before ANY write anywhere in the
milestone. Reads apply no VPP, so the sweep is risk-free — but it validates the read path
and DB decode for every chip and discovers the UV blank-state. Per D-10 the 2516 (the
11th chip) can only be read because Plan 02 authored its DB entry.

Output: 11 populated EVIDENCE cells (verdict PASS / ANOMALY / SKIP-no-chip), the 3 UV
blank-states recorded, and per-task SAFE-01 precondition records (the SAFE-01/02/03 bench
discipline homed here per D-11, re-applied in Phases 82-84).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/v1.15/bench/EVIDENCE.md
@.planning/v1.15/bench/EVIDENCE.json

# Reused tooling (no new harness — EVID-02)
@firestarter_app/firestarter/eprom_operations.py
@firestarter_app/firestarter/chip_resolver.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Read + blank-check the 8 non-UV chips on Leonardo + Rev 2.0 (SWEEP-01, EVID-03, SAFE-01)</name>
  <what-built>
    Plan 01 confirmed the FLAG_CAN_ERASE chain and the 0xA4 guard green; Plan 02 authored the
    2516 entry and scaffolded EVIDENCE.{md,json} with 11 pending chip rows. This task is the
    operator-executed non-destructive read + blank-check of the 8 non-UV chips — there is no
    Claude automation possible (requires physical chips on the bench). Reads apply NO VPP, so
    this is risk-free. The 8 non-UV chips (D-09: record read + current-state SHA, NOT a pass/fail
    blank gate, since these are never factory-blank): W27C512, W27E512, SST27SF512 (0x07),
    W27E040 (0x08), SST39SF040 (0x06), W29C020, W29C040 (0x05 flash4), FM1608 (0x40 FRAM).
  </what-built>
  <how-to-verify>
    PRECONDITIONS at the start of THIS task (SAFE-01, re-verify per task after any USB event):
    - board = Leonardo (the ONLY authoritative read board; Rev 0 read-fault, uno328pb N/A).
    - shield = Rev 2.0 — ASK yourself which silkscreen rev is mounted (the EEPROM byte cannot
      distinguish revs); record the stated rev.
    - `controller:` port identity confirmed via `firestarter info <any-chip> -p /dev/ttyACMX`.
    - live R1 readback `r1 ~= 270000` (run `firestarter config`); if it reads 1000 (default),
      recalibrate `firestarter config -r1 270000` before any operation.
    - host suite green (SAFE-02, proven in Plan 01).

    For EACH of the 8 non-UV chips:
    1. Visually confirm the chip is seated, pin-1 aligned, ZIF lever fully closed.
    2. Run N>=3 reads — use `firestarter dev write-cycle <chip> --runs 3` (per-run binaries + SHA)
       or N>=3 separate `firestarter read <chip>` and compare SHA-256. All N must be byte-identical.
    3. Run a blank-check (`firestarter blank-check <chip>` or the read+inspect equivalent). For
       non-UV chips record the result as "not blank / mixed data" + the current-contents SHA — this
       is NOT a pass/fail blank gate (D-09).
    4. Negative control (EVID-03): run a `verify` against a deliberately wrong file and confirm it
       exits NON-ZERO at least once across the family — proves the oracle is not silently passing.
    5. On a suspect read (all-0xFF contact fault / jitter / timeout): reseat + retry up to N=2 cycles
       (D-07); if still not clean, record verdict=ANOMALY with the symptom and CONTINUE (D-06) —
       do not halt; flag genuine defects for Phase 84 FIX-01.
    6. Record into `.planning/v1.15/bench/EVIDENCE.{md,json}` for each chip: chip, family/algorithm,
       board+shield, blank-state ("n/a — not factory-blank, current contents recorded"), op=read+blank_check,
       sha256 (the byte-identical read SHA), read_count (N), verdict (PASS/ANOMALY/SKIP-no-chip), anomalies.

    Report which chips PASSED, which were ANOMALY (with symptom), which were SKIP-no-chip, and confirm
    the negative control fired (wrong-file verify exited non-zero).
  </how-to-verify>
  <action>
    Operator-executed bench step (SAFE-01) — no Claude automation possible (requires physical chips on
    the Leonardo bench). Walk the operator through the per-task preconditions (board=Leonardo, shield rev
    stated, controller: identity, live r1~=270000), then the 8 non-UV chips: N>=3 byte-identical reads +
    blank-check (recorded per D-09 as read+current-state SHA, not a pass/fail gate) + one wrong-file
    negative-control verify (EVID-03). On a suspect read, reseat+retry up to N=2 (D-07) then record
    ANOMALY+continue (D-06). PAUSE for the operator to run each chip and report verdicts; record each
    chip's EVIDENCE row.
  </action>
  <verify>
    <human-check>Operator reports per-chip verdicts (PASS/ANOMALY/SKIP-no-chip) for all 8 non-UV chips and confirms the wrong-file negative control exited non-zero; SAFE-01 preconditions recorded.</human-check>
  </verify>
  <done>The 8 non-UV chips are read (N>=3 byte-identical) + blank-checked on Leonardo+Rev 2.0 with verdicts and current-state SHAs recorded in EVIDENCE, the negative control fired, and SAFE-01 preconditions are recorded.</done>
  <resume-signal>Type "recorded" with the per-chip verdicts and the negative-control result, or describe a blocking bench issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Read + blank-check the 3 UV-EPROMs incl. the 2516, record gating blank-state (SWEEP-01/02, EVID-03, SAFE-01, D-10)</name>
  <what-built>
    The non-UV sweep (Task 1) is recorded. This task reads + blank-checks the 3 true UV-EPROMs:
    ST M27C512 (0x07 UV), AM27C020 (0x08 UV), and 2516 (0x0B NMOS UV — the 11th chip, readable only
    because Plan 02 authored its `~/.firestarter/database.json` entry, per D-10). Reads apply NO VPP
    — non-destructive. The blank-state recorded here is the LOAD-BEARING gate for every Phase 83
    spend-vs-preserve decision on these irreplaceable chips (operator has no UV eraser).
  </what-built>
  <how-to-verify>
    Re-verify SAFE-01 preconditions at the start of THIS task (board=Leonardo, shield=Rev 2.0 stated,
    `controller:` identity, live r1 ~= 270000) — port numbers can shuffle between tasks.

    For EACH of the 3 UV-EPROMs (M27C512, AM27C020, 2516):
    1. Confirm chip seated + pin-1 aligned + ZIF closed. The 2516 is 24-pin in a 28-pin socket —
       align pin 1 of the chip to the pin-1 end of the socket (misalignment routes VPP to VCC).
    2. Run N>=3 byte-identical reads (`firestarter read` or `dev write-cycle --runs 3`), compare SHA-256.
    3. Run a blank-check and record the GATING blank-state (D-08): blank (all-0xFF) vs not-blank
       (has data). This is a true pass/fail blank gate for UV chips — it decides Phase 83 spend-vs-preserve.
    4. For the 2516 specifically: confirm `firestarter info 2516` still decodes 0x0B / DIP24_2716 /
       UV-EPROM / 25000 / 2048 (the Plan 02 entry is installed and the operator signed the safety
       review). The 2516 read is non-destructive (no VPP) — do NOT write it this phase.
    5. Negative control (EVID-03): at least one wrong-file `verify` exits non-zero.
    6. Suspect-read handling: reseat + retry up to N=2 (D-07), then record ANOMALY + continue (D-06).
    7. Record each UV chip's EVIDENCE row: chip, family/algorithm, board+shield, blank-state (BLANK /
       NOT-BLANK — the gating value), op=read+blank_check, sha256, read_count, verdict, anomalies.

    Report the 3 UV blank-states explicitly (these gate Phase 83) and confirm the 2516 decode rendered.
  </how-to-verify>
  <action>
    Operator-executed bench step (SAFE-01, D-10) — no Claude automation possible. Re-verify SAFE-01
    preconditions, then read+blank-check the 3 UV-EPROMs (M27C512, AM27C020, 2516). Reads apply NO VPP
    (non-destructive). For each: N>=3 byte-identical reads, record the GATING blank-state (D-08: BLANK /
    NOT-BLANK — this decides Phase 83 spend-vs-preserve), one wrong-file negative-control verify. For the
    2516 confirm `firestarter info 2516` still decodes 0x0B/DIP24_2716/UV-EPROM/25000/2048; do NOT write
    it. Reseat+retry up to N=2 on suspect reads (D-07) then ANOMALY+continue (D-06). PAUSE for the operator
    to run each chip; record each UV EVIDENCE row with its gating blank-state.
  </action>
  <verify>
    <human-check>Operator reports the 3 UV gating blank-states (BLANK/NOT-BLANK) and confirms the 2516 decode rendered; each UV chip has an EVIDENCE row with N>=3 reads recorded.</human-check>
  </verify>
  <done>The 3 UV-EPROMs are read (N>=3 byte-identical) + blank-checked on Leonardo+Rev 2.0, each gating blank-state (BLANK/NOT-BLANK) is recorded in EVIDENCE, the 2516 decode is confirmed, and the negative control fired.</done>
  <resume-signal>Type "recorded" with the 3 UV blank-states (BLANK / NOT-BLANK) and the 2516 decode confirmation, or describe a blocking issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize EVIDENCE.{md,json} — validate all 11 rows populated, no pending verdicts (EVID-01)</name>
  <files>.planning/v1.15/bench/EVIDENCE.md, .planning/v1.15/bench/EVIDENCE.json</files>
  <read_first>
    - .planning/v1.15/bench/EVIDENCE.json — the 11 cells populated by Tasks 1-2 (no remaining `pending`)
    - .planning/v1.15/bench/EVIDENCE.md — the human-readable table to reconcile with the JSON
    - .planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-CONTEXT.md — the locked column set + D-08/D-09 blank-state semantics
  </read_first>
  <action>
    Reconcile EVIDENCE.md against EVIDENCE.json: ensure all 11 chips have a final verdict (PASS /
    ANOMALY / SKIP-no-chip — NO `pending` remains), the 3 UV-EPROMs each carry an explicit gating
    blank-state (BLANK / NOT-BLANK), the 8 non-UV chips carry the "n/a — not factory-blank, current
    contents recorded" blank-state with a current-contents SHA, and every PASS cell has read_count>=3
    and a non-empty sha256. Confirm the schema still EXTENDS the v1.13 matrix (cells array, verdict,
    harness_version="81") and the md table mirrors the json. Add a header note recording that the
    negative control fired (wrong-file verify exited non-zero) at least once. Do NOT alter any verdict
    recorded by the operator in Tasks 1-2 — this task only validates completeness and md/json parity.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "import json; d=json.load(open('.planning/v1.15/bench/EVIDENCE.json')); cells=d['cells']; assert len(cells)==11, 'need 11'; pend=[c['chip'] for c in cells if c['verdict']=='pending']; assert not pend, 'pending: '+str(pend); uv={'M27C512','AM27C020','2516'}; uv_missing=[c['chip'] for c in cells if c['chip'].replace('ST ','').strip() in uv and c['blank_state'] not in ('BLANK','NOT-BLANK')]; print('uv-blank-state-ok' if not uv_missing else 'FAIL '+str(uv_missing)); passes=[c for c in cells if c['verdict']=='PASS']; bad=[c['chip'] for c in passes if c.get('read_count',0)<3 or not c.get('sha256')]; print('pass-rows-ok' if not bad else 'FAIL '+str(bad))"</automated>
  </verify>
  <acceptance_criteria>
    - The Python check prints `uv-blank-state-ok` and `pass-rows-ok` (no assertion failures): 11 cells, zero `pending` verdicts, each UV-EPROM has a BLANK/NOT-BLANK gating blank-state, every PASS row has read_count>=3 and a non-empty sha256.
    - EVIDENCE.md mirrors EVIDENCE.json (same 11 chips, same verdicts) and has a header note that the negative control fired.
    - No operator-recorded verdict from Tasks 1-2 is altered by this task.
  </acceptance_criteria>
  <done>EVIDENCE.{md,json} is complete and self-consistent: 11 chips, no pending verdicts, 3 UV gating blank-states recorded, all PASS rows non-vacuous (N>=3 + SHA), md/json in parity, negative control noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical chip -> read path -> recorded SHA/verdict | A read on a non-Leonardo board or wrong shield produces self-consistent garbage (Bug A jitter / uno328pb 0xFF drift) recorded as a false-PASS |
| port assignment -> bench command target | `/dev/ttyACM*` shuffle after a USB event can target the wrong board (uno328pb brownout / false read) |
| UV blank-state record -> Phase 83 spend decision | A wrong blank-state gates an irreversible UV write decision on an irreplaceable chip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-06 | Spoofing | read oracle (wrong board/shield produces false-PASS) | mitigate | SAFE-01 recorded + verified per task (board=Leonardo, shield=Rev 2.0 stated, `controller:` identity, live r1~=270000); N>=3 byte-identical reads + a negative control (EVID-03) make every PASS non-vacuous |
| T-81-07 | Tampering | stale R1 calibration drives wrong VPP regulator setpoint | mitigate | live `firestarter config` r1 readback as the first per-task step; recalibrate to 270000 if it reads the 1000 default (reads apply no VPP, but the precondition discipline is homed here for Phases 82-84) |
| T-81-08 | Repudiation | port-identity drift targets the wrong board silently | mitigate | `controller:` identity re-verified at the start of EACH task and after any USB event (not session-start) |
| T-81-09 | Information Disclosure | wrong UV blank-state mis-gates a Phase 83 irreversible write | mitigate | D-08 gating blank-state recorded only for the 3 UV-EPROMs from N>=3 byte-identical reads; reseat+retry on suspect reads (D-06/D-07) before recording |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses existing `firestarter` CLI / `dev write-cycle` only (EVID-02, no new harness) |
</threat_model>

<verification>
- All 11 chips have a final EVIDENCE verdict (no `pending`); the Python completeness check prints `uv-blank-state-ok` and `pass-rows-ok`.
- The 3 UV-EPROM gating blank-states (M27C512, AM27C020, 2516) are recorded explicitly.
- Each PASS is non-vacuous: read_count>=3 + a non-empty sha256; the negative control (wrong-file verify exits non-zero) fired at least once.
- Each bench task's SAFE-01 preconditions (board, shield, controller, r1) are recorded in the EVIDENCE header / per-cell notes.
</verification>

<success_criteria>
- SWEEP-01: all 11 chips read end-to-end + blank-checked on Leonardo + Rev 2.0, zero chips consumed, each an EVIDENCE row.
- SWEEP-02: the 3 UV-EPROM blank-states are recorded (the Phase 83 gate).
- EVID-03: every PASS is non-vacuous (N>=3 byte-identical reads + a negative control).
- SAFE-01: every bench task records + verifies board=Leonardo, shield=Rev 2.0, controller: identity, live r1~=270000; no non-Leonardo read is authoritative.
</success_criteria>

<output>
Create `.planning/phases/81-2516-db-entry-non-destructive-read-sweep/81-03-SUMMARY.md` when done.
Record the per-chip verdicts, the 3 UV gating blank-states, any ANOMALY chips flagged for Phase 84 FIX-01, and the SAFE-01 precondition records.
</output>
