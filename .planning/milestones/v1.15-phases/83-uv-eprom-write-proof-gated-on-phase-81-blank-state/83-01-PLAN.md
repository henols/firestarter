---
phase: 83-uv-eprom-write-proof-gated-on-phase-81-blank-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /tmp/firestarter_bench_p83/ST_M27C512_img.bin
  - /tmp/firestarter_bench_p83/AM27C020_zeros.bin
  - .planning/v1.15/bench/EVIDENCE.md
autonomous: true
requirements: [UV-01]
must_haves:
  truths:
    - "Host test suite is green (incl. the 0xA4 ack_data=False guard) before any bench session opens"
    - "The ST M27C512 64KB deterministic image exists with a recorded SHA-256 oracle"
    - "The AM27C020 256KB all-0x00 image exists with a recorded SHA-256 oracle"
    - "EVIDENCE.md carries a Phase 83 section that scopes the work to the 2 read-stable UV chips and records the 2516/GRAD-03 deferral to Phase 84"
  artifacts:
    - path: "/tmp/firestarter_bench_p83/ST_M27C512_img.bin"
      provides: "64KB (0x10000) deterministic pseudo-random write payload for the ST M27C512 BLANK write proof (D-05)"
    - path: "/tmp/firestarter_bench_p83/AM27C020_zeros.bin"
      provides: "256KB (0x40000 = 262144) all-0x00 write payload for the AM27C020 NOT-BLANK 1→0 write proof (D-06)"
    - path: ".planning/v1.15/bench/EVIDENCE.md"
      provides: "Phase 83 UV-EPROM write-proof section header + scope/deferral note"
      contains: "Phase 83"
  key_links:
    - from: "tools/gen_test_image.py"
      to: "/tmp/firestarter_bench_p83/ST_M27C512_img.bin"
      via: "python tools/gen_test_image.py 65536 1 <path> prints the SHA-256 oracle"
      pattern: "gen_test_image"
---

<objective>
Establish the SAFE-02 software preconditions and the deterministic write payloads for the
two read-stable UV-EPROM write proofs, and scaffold the Phase 83 section of the bench
EVIDENCE record. This is the autonomous, no-chip preflight that MUST be green before any
operator bench session (Plans 02/03) opens.

Purpose: UV-01 (non-destructive-first) and the EVID-02 reuse-first / EVID-03 non-vacuous
contract require that the host suite (including the 0xA4 ack_data=False regression guard) is
green and that the exact write images + their SHA-256 oracles exist BEFORE VPP is ever
applied to an irreversible UV part. Generating images on a clean preflight removes any
ambiguity at the bench.
Output: two deterministic image files with recorded SHA-256 oracles, a recorded host-suite /
ruff gate result, and a Phase 83 EVIDENCE.md section header that scopes the phase to the 2
read-stable chips and records the 2516 → Phase 84 deferral.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-uv-eprom-write-proof-gated-on-phase-81-blank-state/83-CONTEXT.md
@.planning/v1.15/bench/EVIDENCE.md
@firestarter_app/tools/gen_test_image.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAFE-02 software gate — host suite + ruff green before bench</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - firestarter_app/CLAUDE.md (tooling gate: ruff check + ruff format --check + pytest --cov-fail-under=70; devcontainer Py3.12 masks CI py3.9/3.11)
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-CONTEXT.md (D-10/D-11 — the SAFE-02 precondition wording, the 0xA4 guard name)
    - .planning/v1.15/bench/EVIDENCE.md §"Phase 82 SAFE-02 Gate" (the recorded-result format to mirror)
  </read_first>
  <action>
    Run the host test suite from firestarter_app/ via pytest, confirming the 0xA4 guard
    `test_init_phase_data_frames_not_acked` PASSes (SAFE-02, per D-10). Run `ruff check` and
    `ruff format --check` over firestarter_app and record PASS/FAIL — note the devcontainer is
    Py3.12 while CI targets py3.9/3.11, so flag any version-sensitive ruff finding rather than
    masking it. This task is read-only verification; it produces NO source change and NO new
    test (the suite already exists from Phase 81/82). If the suite is red, STOP and report —
    do not open a bench session on a red suite.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest -q -k test_init_phase_data_frames_not_acked && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -k test_init_phase_data_frames_not_acked` exits 0 (the 0xA4 ack_data=False guard PASSes)
    - The full host suite (`python -m pytest -q`) exits 0
    - `ruff check .` exits 0 and `ruff format --check .` exits 0 (or any finding is flagged as a py3.12-vs-CI-target discrepancy, not silently passed)
    - The recorded result is appended to EVIDENCE.md in a "Phase 83 SAFE-02 Gate" subsection mirroring the Phase 82 format (test count, 0xA4 guard PASS, ruff PASS, Python version)
  </acceptance_criteria>
  <done>Host suite + 0xA4 guard + ruff gates all green and recorded in EVIDENCE.md; no source/test files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the two UV write images + record SHA oracles; scaffold the Phase 83 EVIDENCE section</name>
  <files>/tmp/firestarter_bench_p83/ST_M27C512_img.bin, /tmp/firestarter_bench_p83/AM27C020_zeros.bin, .planning/v1.15/bench/EVIDENCE.md</files>
  <read_first>
    - firestarter_app/tools/gen_test_image.py (generate_image(size_bytes, seed); CLI prints SHA-256)
    - .planning/phases/83-uv-eprom-write-proof-gated-on-phase-81-blank-state/83-CONTEXT.md (D-05 ST M27C512 = full 64KB deterministic image; D-06 AM27C020 = all-0x00 over 262144 bytes; D-01 the 2516 is OUT)
    - .planning/v1.15/bench/EVIDENCE.md §"UV-EPROM Gating Blank-States" (ST M27C512 BLANK / AM27C020 NOT-BLANK / 2516 read-unstable)
  </read_first>
  <action>
    Generate the ST M27C512 write payload as a full-chip-size deterministic pseudo-random image
    of exactly 65536 bytes (0x10000) using `tools/gen_test_image.py` with size_bytes=65536 and
    seed=1 (Claude's-discretion seed, recorded), writing to /tmp/firestarter_bench_p83/ST_M27C512_img.bin
    and capturing the printed SHA-256 as the image oracle (D-05). Generate the AM27C020 payload
    as exactly 262144 bytes (0x40000) of all-0x00 written to /tmp/firestarter_bench_p83/AM27C020_zeros.bin
    (D-06 — this is a trivial constant buffer, NOT gen_test_image; use a direct byte-write of
    262144 zero bytes), and compute its SHA-256 with sha256sum as the verify oracle. Then append
    a "Phase 83 — UV-EPROM Write Proof" section to EVIDENCE.md with: the scope note (ONLY the 2
    read-stable chips ST M27C512 / AM27C020 per D-01/D-02), the explicit 2516 → Phase 84 deferral
    (no write/preserve/re-read; GRAD-03 + SC#4 + FUT-03 move to Phase 84; the D-08 PASS bar is
    pre-recorded there), an empty write-proof results table with the locked + extension columns
    (chip, family/algorithm, board+shield, blank-state, spend-vs-preserve decision, op, image SHA,
    read-back SHA, read N, verdict, anomalies), and the two image SHA oracles. Do NOT create any
    new Python module or firmware change (D-11 reuse-first).
  </action>
  <verify>
    <automated>test "$(stat -c%s /tmp/firestarter_bench_p83/ST_M27C512_img.bin)" = "65536" && test "$(stat -c%s /tmp/firestarter_bench_p83/AM27C020_zeros.bin)" = "262144" && python3 -c "import sys;d=open('/tmp/firestarter_bench_p83/AM27C020_zeros.bin','rb').read();sys.exit(0 if d==b'\x00'*262144 else 1)" && grep -q "Phase 83" .planning/v1.15/bench/EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/firestarter_bench_p83/ST_M27C512_img.bin is exactly 65536 bytes; re-running `gen_test_image.py 65536 1` reproduces the identical SHA-256 (deterministic oracle)
    - /tmp/firestarter_bench_p83/AM27C020_zeros.bin is exactly 262144 bytes and is all-0x00 (verified byte-for-byte)
    - The AM27C020 all-0x00 SHA-256 equals `sha256sum` of 262144 zero bytes and is recorded in EVIDENCE.md as the verify oracle
    - EVIDENCE.md contains a "Phase 83 — UV-EPROM Write Proof" section that (a) scopes work to ST M27C512 + AM27C020 only, (b) records the 2516/GRAD-03/SC#4/FUT-03 deferral to Phase 84 with the pre-recorded D-08 PASS bar, (c) has an empty results table with the locked + extension columns, (d) records both image SHA oracles
    - No new Python module, firmware file, or third-party dependency was added (EVID-02)
  </acceptance_criteria>
  <done>Both write images exist at the exact chip sizes with recorded reproducible SHA oracles, and the Phase 83 EVIDENCE.md section is scaffolded with the scope + 2516 deferral note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ↔ irreversible UV silicon | Any VPP-applying write to a UV part is permanent; this preflight must NOT apply VPP |
| image file ↔ bench write driver | The image generated here becomes the exact payload written to a chip in Plans 02/03 — a wrong-size or wrong-content image would be written irreversibly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | write image file (wrong size/content) | mitigate | Assert exact byte size (65536 / 262144) and content (all-0x00 for AM27C020; reproducible deterministic SHA for ST M27C512) before any chip is written; oracles recorded in EVIDENCE before the bench session |
| T-83-02 | Denial of Service | bench session opened on a red host suite | mitigate | SAFE-02 gate (Task 1) blocks bench sessions until pytest + 0xA4 guard + ruff are green; STOP-on-red is explicit |
| T-83-03 | Elevation/Destruction | accidental 2516 selection | mitigate | This plan touches NO chip and NO 2516 artifact; the EVIDENCE scope note records the 2516 as OUT (Phase 84) so downstream plans cannot silently include it |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (reuse-first, EVID-02); no new third-party dependency introduced |
</threat_model>

<verification>
- Host suite + 0xA4 guard + ruff: all exit 0 (SAFE-02)
- ST M27C512 image: 65536 bytes, deterministic SHA reproducible from (65536, seed=1)
- AM27C020 image: 262144 bytes, all-0x00, SHA recorded
- EVIDENCE.md has the Phase 83 section with scope + 2516 deferral + empty results table + both oracles
</verification>

<success_criteria>
- SAFE-02 software gate green and recorded (no bench session may open otherwise)
- Both write payloads exist at exact chip sizes with recorded reproducible SHA-256 oracles
- Phase 83 EVIDENCE.md section scaffolded, scoped to the 2 read-stable chips, 2516 deferral documented
- Zero source/firmware/dependency changes (EVID-02 reuse-first honored)
</success_criteria>

<output>
Create `.planning/phases/83-uv-eprom-write-proof-gated-on-phase-81-blank-state/83-01-SUMMARY.md` when done
</output>
