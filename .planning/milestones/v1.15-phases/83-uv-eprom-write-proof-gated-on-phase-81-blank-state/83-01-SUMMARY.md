---
phase: 83-uv-eprom-write-proof-gated-on-phase-81-blank-state
plan: 01
subsystem: bench-evidence / host-tooling-preflight
tags: [uv-eprom, write-proof, safe-02, evidence, preflight, no-hardware]
requires:
  - "Phase 81 UV blank-states (ST M27C512 BLANK / AM27C020 NOT-BLANK / 2516 read-unstable)"
  - "firestarter_app host suite + 0xA4 ack_data=False guard (Phase 81/82)"
  - "tools/gen_test_image.py (Phase 82 deterministic image generator)"
provides:
  - "SAFE-02 software gate result recorded (host suite + 0xA4 guard + CI-scoped ruff green)"
  - "ST_M27C512_img.bin (65536B deterministic) + AM27C020_zeros.bin (262144B all-0x00) with SHA oracles"
  - "Phase 83 EVIDENCE.md section: 2-chip scope, 2516→Phase 84 deferral, empty write-proof table, both oracles"
affects:
  - ".planning/v1.15/bench/EVIDENCE.md"
  - "/tmp/firestarter_bench_p83/ (out-of-repo write payloads for Plans 02/03)"
tech-stack:
  added: []
  patterns:
    - "Reuse-first preflight (EVID-02): no new module/firmware/dependency"
    - "Deterministic SHA oracle via gen_test_image.py (reproducible from size+seed)"
    - "SAFE-02 STOP-on-red gate before any irreversible UV write"
key-files:
  created:
    - "/tmp/firestarter_bench_p83/ST_M27C512_img.bin (out-of-repo, /tmp)"
    - "/tmp/firestarter_bench_p83/AM27C020_zeros.bin (out-of-repo, /tmp)"
  modified:
    - ".planning/v1.15/bench/EVIDENCE.md"
decisions:
  - "CI-authoritative ruff scope is firestarter/ tests/ (ci.yml:60,63); tools/-tree ruff findings flagged out-of-scope, not masked"
  - "ST M27C512 image = gen_test_image 65536 seed=1 (D-05); AM27C020 = direct all-0x00 262144B (D-06)"
metrics:
  duration: "~7 min"
  completed: "2026-06-24"
  tasks: 2
  files_changed: 1
---

# Phase 83 Plan 01: UV-EPROM Write-Proof Software Preflight Summary

**One-liner:** Established the SAFE-02 software gate (663-test host suite + 0xA4 `ack_data=False` guard + CI-scoped ruff all green) and generated the two deterministic UV write payloads (ST M27C512 64KB pseudo-random image + AM27C020 256KB all-0x00) with recorded reproducible SHA-256 oracles, then scaffolded the Phase 83 EVIDENCE.md section scoping the work to the 2 read-stable UV chips and recording the 2516→Phase 84 deferral — all with zero source/firmware/dependency changes (EVID-02 reuse-first).

## What Was Built

### Task 1 — SAFE-02 software gate (verification-only, commit `9fdacca`)
- **0xA4 guard `test_init_phase_data_frames_not_acked`:** PASS (1/1).
- **Full host suite:** 663 tests PASS (unchanged from Phase 82; no source/test files touched), 29 snapshots.
- **CI-authoritative ruff (`ruff check firestarter/ tests/` + `ruff format --check firestarter/ tests/`):** both green (RC=0; `All checks passed!` / 73 files already formatted).
- Result recorded in EVIDENCE.md under a new **"Phase 83 SAFE-02 Gate"** subsection mirroring the Phase 82 format (test count, 0xA4 guard, ruff, Python version, verdict).

### Task 2 — UV write images + EVIDENCE scaffold (commit `d1b42d5`)
- **`/tmp/firestarter_bench_p83/ST_M27C512_img.bin`** — 65536 bytes, generated by `python tools/gen_test_image.py 65536 1`; SHA-256 `604d957094f7cb1f98f50d7408f64e7720e5ae5d4acab8d1047a9a081645d637`. Verified reproducible (re-run produces identical digest).
- **`/tmp/firestarter_bench_p83/AM27C020_zeros.bin`** — 262144 bytes, direct byte-write of all-`0x00` (NOT gen_test_image, per D-06); SHA-256 `8a39d2abd3999ab73c34db2476849cddf303ce389b35826850f9a700589b4a90`, verified byte-for-byte all-zero and equal to `sha256(b"\x00"*262144)`.
- **EVIDENCE.md "Phase 83 — UV-EPROM Write Proof" section:** scopes work to ST M27C512 + AM27C020 only (D-01/D-02); records the 2516/GRAD-03/SC#4/FUT-03 deferral to Phase 84 with the pre-recorded D-08 PASS bar; carries an image-oracle table + an empty write-proof results table with the locked + extension columns (chip, family/algorithm, board+shield, blank-state, spend-vs-preserve decision, op, image SHA, read-back SHA, read N, verdict, anomalies).

## Verification Performed

- Task 1 automated: `pytest -k test_init_phase_data_frames_not_acked` (PASS), full `pytest -q` (663 passed), CI-scoped ruff check + format (RC=0 each).
- Task 2 automated (plan `<verify>`): `stat -c%s` = 65536 / 262144 ✓; AM27C020 all-`0x00` byte check ✓; `grep "Phase 83" EVIDENCE.md` ✓.
- Reproducibility: ST image SHA identical across two independent generator runs.
- EVID-02: confirmed no new Python module, firmware file, or third-party dependency added; `firestarter_app` shows no tracked source modifications.

## Deviations from Plan

**None of Rules 1–4 triggered.** The plan executed as written.

One **flagged discrepancy (not a deviation, per acceptance criteria)**: the plan's `<verify>` block uses broad `ruff check .` / `ruff format --check .`, which surfaces 4 pre-existing findings in `tools/` (`audit_coverage_matrix.py`, `catalog/codegen.py`, `catalog/codegen_vectors.py` — I001/UP031) and 2 format-only files (`.github/scripts/update_version.py`, `tools/check_mypy_watermark.py`). The acceptance criteria explicitly permit flagging an out-of-scope finding rather than masking it. CI gates ruff over `firestarter/ tests/` only (`.github/workflows/ci.yml:60,63`), which is fully green. These findings are **pre-existing** (this plan made zero source changes) and match the Phase 82 EVIDENCE note ("pre-existing I001 errors in unrelated tools unchanged"). This is a path-scope difference, **not** a py3.12-vs-CI version discrepancy. SAFE-02 is satisfied on the CI-authoritative gate. Documented verbatim in the EVIDENCE.md "Phase 83 SAFE-02 Gate" subsection.

## Known Stubs

None. The empty write-proof results table in EVIDENCE.md is **intentional scaffolding** — its rows are filled by the operator bench sessions in Plans 02/03 (this is the no-hardware preflight that precedes them). The 2516 row is intentionally marked DEFERRED → Phase 84 per D-01.

## Threat Surface Notes

The two mitigations this plan owns from the threat register were both applied:
- **T-83-01 (image tampering):** exact byte size (65536 / 262144) asserted and content (all-`0x00` for AM27C020; reproducible deterministic SHA for ST M27C512) verified; both oracles recorded in EVIDENCE before any bench session.
- **T-83-02 (red-suite bench):** SAFE-02 gate (Task 1) recorded green before Plans 02/03; STOP-on-red was honored (suite was green, no stop needed).
- **T-83-03 (2516):** this plan touched no chip and no 2516 artifact; the EVIDENCE scope note records the 2516 as OUT (Phase 84).

No new security-relevant surface introduced (no new endpoints, auth paths, file access, or schema changes).

## Self-Check: PASSED

- Files verified present: `/tmp/firestarter_bench_p83/ST_M27C512_img.bin`, `/tmp/firestarter_bench_p83/AM27C020_zeros.bin`, `.planning/v1.15/bench/EVIDENCE.md`, `83-01-SUMMARY.md`.
- Commits verified in git log: `9fdacca` (Task 1 SAFE-02 gate), `d1b42d5` (Task 2 images + EVIDENCE scaffold).
