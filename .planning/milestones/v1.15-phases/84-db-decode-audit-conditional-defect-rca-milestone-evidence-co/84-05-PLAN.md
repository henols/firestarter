---
phase: 84-db-decode-audit-conditional-defect-rca-milestone-evidence-co
plan: 05
type: execute
wave: 3
depends_on: ["84-01", "84-02", "84-03"]
files_modified:
  - .planning/v1.15/bench/EVIDENCE.md
  - .planning/v1.15/bench/EVIDENCE.json
autonomous: false
requirements: [FIX-01]
must_haves:
  truths:
    - "The Leonardo is re-flashed with the Phase-84 VPP-skip firmware build (84-01 branch) before any re-bench; SAFE-01 preconditions recorded (controller identity, r1≈270000, operator-confirmed silkscreen rev, SAFE-02 host suite + 0xA4 guard green)."
    - "The 2516 (0x0B) is RE-READ ONLY (read + blank-check + decode, N≥3 via `dev consistency-check --runs 3`) after the VPP-skip fix to test whether the directive fix stabilizes the read — it is NEVER written, never preserve-dumped (D-21); GRAD-03 write proof stays DEFERRED regardless (D-22, FUT-03 best-effort)."
    - "AM27C020 0x08 write and W29C040 flash4 256B-page write are re-benched (RCA + confirm), retry budget N=2 (D-54); fixed only if the re-bench reveals a TRIVIAL fix, else a disposition + named future tracker is recorded (0x08→future; W29C040 flash4→reopen Phase-74 Wave-2 / CR-01)."
    - "Genuine stuck-bit FAILs (W27E512, W27E040) are NOT re-benched as defects (D-32, silicon-limited)."
  artifacts:
    - path: ".planning/v1.15/bench/EVIDENCE.md"
      provides: "Phase-84 bench section: re-flash preconditions, 2516 re-read result, 0x08 + flash4 re-bench dispositions"
      contains: "Phase 84"
    - path: ".planning/v1.15/bench/EVIDENCE.json"
      provides: "Machine-readable mirror of the Phase-84 bench rows (Phase-81/82/83 rows preserved)"
      contains: "phase84"
  key_links:
    - from: ".planning/v1.15/bench/EVIDENCE.md"
      to: "dev consistency-check --runs 3"
      via: "N≥3 read oracle for the 2516 re-read"
      pattern: "consistency-check"
---

<objective>
The operator-gated bench session: re-flash the Leonardo with the Phase-84 VPP-skip firmware, then
re-validate the 2516 read (READ ONLY — never written, D-21) and re-bench the two RCA-and-defer
write defects (AM27C020 0x08, W29C040 flash4 256B-page). Confirm root cause on real silicon, fix
ONLY if trivial, else record a disposition + named future tracker. Board LOCKED to Leonardo + RURP
Rev 2.0 (SAFE-01/D-50). All findings appended to EVIDENCE.{md,json}.

Purpose: FIX-01 bench re-verification (SC#2/#3) — proves the in-posture fixes on silicon and
formally dispositions the deferred write-path defects. GRAD-03 (2516 write) stays DEFERRED by design
(D-22) — this plan validates the read only.
Output: a Phase-84 bench section in EVIDENCE.{md,json} with the re-flash preconditions, the 2516
re-read verdict, and the 0x08 + flash4 dispositions.

NOTE: this plan is `autonomous: false` — every task is operator-gated bench work. Do NOT run any
write/erase on the irreplaceable 2516 (D-21). Reseat + retry up to N=2 then record FAIL/ANOMALY and
continue (D-54); the 2516 is exempt (never written).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-CONTEXT.md
@.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-RESEARCH.md
@.planning/v1.15/bench/EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Re-flash the Leonardo with the Phase-84 VPP-skip build + record SAFE-01/02 gate</name>
  <action>
    Operator-gated bench bring-up. Re-flash the Leonardo with the Phase-84 VPP-skip firmware (the
    84-01 v1.15-branch build; board is currently b10 from Phase 82) and record the SAFE-01/02
    preconditions in EVIDENCE.md before any re-bench. Claude drives via USB passthrough; the operator
    confirms the physical board + silkscreen rev. Do NOT proceed to the re-bench tasks until this gate
    is green.
  </action>
  <what-built>
    The Phase-84 VPP-skip firmware build (Plan 84-01, on the firmware `v1.15-bench-validation-of-operator-inventory`
    branch; native suite green; Leonardo flash ≤ ~90% confirmed in 84-01 Task 3) and the SAFE-02 host
    gate (Plan 84-02 host suite + 0xA4 guard + CI-scoped ruff green). The board is currently flashed
    b10 from Phase 82 and must be re-flashed to the Phase-84 build before any re-bench.
  </what-built>
  <how-to-verify>
    Operator + Claude (USB passthrough) bring up the bench and record SAFE-01 preconditions:
    1. Confirm the board: `firestarter fw` — the `controller:` reports leonardo; verify the port
       identity (e.g. /dev/ttyACM0) AFTER any USB event (port numbers shuffle across replug).
    2. ASK the operator which silkscreen shield rev is mounted (the EEPROM hw byte cannot distinguish
       revs); record it (must be Rev 2.0 per D-50).
    3. Live calibration readback: `r1 ≈ 270000`, `r2 = 44000` (NOT the 1000 default → VPP trustworthy).
    4. Leonardo is chip-OUT-sideload-EXEMPT — re-flash with the Phase-84 build:
       `cd firestarter && pio run -t upload -e leonardo` (the v1.15-branch VPP-skip build).
    5. Re-confirm SAFE-02: host suite green incl. `test_init_phase_data_frames_not_acked` (0xA4 guard)
       + `ruff check firestarter/ tests/` green (the 84-02 gate, re-run if any host file changed since).
    Record all of the above as a "Phase 84 SAFE-01/02 Gate" block in EVIDENCE.md.
  </how-to-verify>
  <resume-signal>Type "flashed + preconditions recorded" once the Leonardo carries the Phase-84 VPP-skip build, the silkscreen rev is operator-confirmed Rev 2.0, r1≈270000 is read back, and SAFE-02 is green — or describe any blocker (e.g. flash >90%, wrong rev, port mismatch).</resume-signal>
  <verify>
    <human-check>EVIDENCE.md Phase-84 SAFE-01/02 gate block records controller=leonardo+port, operator-confirmed Rev 2.0, r1≈270000/r2=44000 live readback, SAFE-02 green; Leonardo flashed with the Phase-84 VPP-skip build (not b10).</human-check>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.md has a Phase-84 SAFE-01/02 gate block: controller=leonardo + port, operator-confirmed silkscreen Rev 2.0, r1≈270000/r2=44000 live readback, SAFE-02 host suite + 0xA4 guard + CI-ruff green.
    - The Leonardo is flashed with the Phase-84 VPP-skip build (firmware v1.15 branch), not b10.
  </acceptance_criteria>
  <done>Leonardo carries the Phase-84 VPP-skip firmware; SAFE-01/02 gate block recorded in EVIDENCE.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-read the 2516 ONLY (N≥3) after the VPP-skip fix — never write (D-20/D-21)</name>
  <action>
    Operator-gated. Re-read the irreplaceable 2516 (0x0B) ONLY — read + blank-check + decode, N≥3 via
    `firestarter dev consistency-check 2516 --runs 3` — to test whether the VPP-skip fix alone
    stabilizes its Phase-81-unstable read (D-20). NEVER write it, NEVER preserve-dump it (D-21). Record
    the N=3 SHA stability verdict, the blank-state, and the decode-vs-DB confirmation in
    EVIDENCE.{md,json}. GRAD-03's write proof stays DEFERRED regardless (D-22) — a stabilized read is
    recorded but does NOT authorize a write this phase.
  </action>
  <what-built>
    The VPP-skip firmware fix (84-01): on `read`/`blank-check` the firmware no longer enables the VPP
    regulator or emits the VPP ERROR/WARNING. The 2516 (0x0B Legacy, shared OE/VPP pin) read was
    UNSTABLE in Phase 81 (3 distinct SHAs / N=3 + 2 reseats, VPP pinned 15.3V on the shared pin). D-20
    tests whether the VPP-skip alone stabilizes the read.
  </what-built>
  <how-to-verify>
    RE-READ THE 2516 ONLY — NEVER WRITE IT, NEVER PRESERVE-DUMP IT (D-21; the part is irreplaceable):
    1. Seat the 2516; verify port identity again if any USB event occurred.
    2. Run the N≥3 read oracle: `firestarter dev consistency-check 2516 --runs 3` (reuse, EVID-02 —
       do NOT hand-roll a read loop). Record the per-run SHAs and whether they are now byte-identical
       (1 distinct SHA = stabilized) or still jitter (≥2 distinct = still unstable).
    3. Run `firestarter blank 2516` (blank-check) and `firestarter info 2516` (decode) — record the
       blank-state + that the decode matches the user-override DB entry (algorithm 0x0B, DIP24_2716,
       UV-EPROM, vpp_mv 25000, 2048 bytes).
    4. Capture the firmware VPP behaviour: confirm the chip-1-style 18.8V boot refusal is GONE on read
       (the VPP-skip cleared it) and note the VPP rail reading on the shared pin.
    STOP at read — no write under any circumstance. Whether the read stabilizes or not, GRAD-03's
    write proof stays DEFERRED (D-22) — a still-unstable read is the EXPECTED trigger for the clean
    FUT-03 deferral; a stabilized read is recorded but STILL does not authorize a write this phase.
  </how-to-verify>
  <resume-signal>Type "2516 re-read recorded" with the N=3 SHA stability verdict (stabilized / still-unstable) and the blank-state + decode confirmation — no write performed.</resume-signal>
  <verify>
    <human-check>EVIDENCE.{md,json} record the 2516 N≥3 read SHAs via `dev consistency-check --runs 3`, the stability verdict, blank-check result, decode-vs-DB confirmation, and an explicit "no write / no preserve-dump" note (D-21); GRAD-03/FUT-03 stays DEFERRED (D-22).</human-check>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.{md,json} record the 2516 N≥3 read SHAs (via `dev consistency-check --runs 3`), the stability verdict, the blank-check result, and the decode-vs-DB confirmation.
    - The record explicitly notes NO write / NO preserve-dump occurred (D-21) and that GRAD-03/FUT-03 stays DEFERRED best-effort (D-22) regardless of read stability.
    - The VPP-skip effect on the read is captured (boot refusal cleared yes/no; rail reading noted).
  </acceptance_criteria>
  <done>2516 read re-validated read-only (N≥3) + decode confirmed; no write; GRAD-03/FUT-03 deferred best-effort.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-bench AM27C020 0x08 + W29C040 flash4 (RCA-and-defer, N=2 retry, D-31/D-54)</name>
  <action>
    Operator-gated. Re-bench the two RCA-and-defer write defects on Leonardo + Rev 2.0: (a) AM27C020
    0x08 write (retry after the VPP-skip fix; AM27C020 is already NOT-BLANK/spent so a re-write is
    idempotent) and (c) W29C040 flash4 256B-page write. Retry budget N=2 (D-54). Fix ONLY if the
    re-bench reveals a trivial cause; otherwise record a disposition + named future tracker
    (0x08→future; W29C040 flash4→reopen Phase-74 Wave-2 / CR-01). Do NOT re-bench W27E512/W27E040
    (D-32, genuine silicon wear). Append every result + disposition to EVIDENCE.{md,json}.
  </action>
  <what-built>
    Two RCA-and-defer write defects from Phase 82/83: (a) AM27C020 (0x08, DIP32, P1-as-VPP path) write
    took 0 bits programmed (deterministic, chip intact); (c) W29C040 (0x05 flash4) write timed out at
    the 256B page-0 boundary (`flash4_page_size` already returns 256 for W29C040 — the bench tests
    whether 256B handling works on silicon or the fault is deeper SDP/poll/VPP). Genuine stuck-bit
    FAILs (W27E512, W27E040) are EXCLUDED — silicon-limited per D-32, NOT re-benched.
  </what-built>
  <how-to-verify>
    Re-bench both defects on Leonardo + Rev 2.0 (retry budget N=2 per D-54; AM27C020 is already
    NOT-BLANK/spent so a re-write is idempotent):
    1. AM27C020 0x08 (D-31a): retry the write after the VPP-skip fix (`firestarter write -b` or
       `dev write-cycle` with the deterministic image; reuse `tools/gen_test_image.py`). Record whether
       bits now program. If still 0-bits-programmed after N=2, record the disposition (0x08 32-pin
       Large-EPROM write/VPP path on this bench) + a named future tracker; do NOT fix unless the
       re-bench reveals a trivial cause.
    2. W29C040 flash4 (D-31c): retry `write -b` (256B-page handling, Claude's discretion on
       write -b vs dev write-cycle). Record whether the 256B page-0 boundary now programs. If still
       FAIL after N=2, record the disposition reopening Phase-74 Wave-2 / the CR-01 datasheet-page
       todo; do NOT fix unless trivial.
    3. Do NOT touch W27E512 / W27E040 (D-32 — genuine silicon wear, not FIX-01 material).
    Append every result (PASS / FAIL / ANOMALY + disposition + tracker) to EVIDENCE.{md,json}.
  </how-to-verify>
  <resume-signal>Type "0x08 + flash4 re-bench recorded" with each chip's verdict + disposition (trivial-fix-taken / deferred-with-tracker) — or describe a blocker.</resume-signal>
  <verify>
    <human-check>EVIDENCE.{md,json} record the AM27C020 0x08 + W29C040 flash4 re-bench verdicts + dispositions (trivial-fix-taken / deferred-with-named-tracker); W27E512/W27E040 not re-benched; Phase-81/82/83 rows preserved + md↔json consistent.</human-check>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.{md,json} record the AM27C020 0x08 re-bench (programs / still 0-bits) + disposition + future tracker if deferred.
    - EVIDENCE.{md,json} record the W29C040 flash4 256B-page re-bench + disposition (reopen Phase-74 Wave-2 / CR-01 if deferred).
    - Neither defect was "fixed" unless the re-bench revealed a trivial cause (D-31); W27E512/W27E040 not re-benched (D-32).
    - Phase-81/82/83 EVIDENCE rows are preserved (append-only); EVIDENCE.json stays md↔json consistent.
  </acceptance_criteria>
  <done>0x08 + flash4 re-benched + dispositioned (fix-if-trivial else tracker); stuck-bit FAILs untouched; EVIDENCE append-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (hardware-safety, bench)

| Boundary | Description |
|----------|-------------|
| operator action → irreplaceable 2516 | any write/erase consumes the single irreplaceable part |
| firmware VPP gate (post-fix) → socket pins | the VPP-skip must not have over-broadened to write/erase (verified in 84-01; re-confirmed live here) |
| non-Leonardo read → evidence record | a non-Leonardo read is not authoritative (v1.9 read bug) |

## STRIDE Threat Register (hardware-safety adaptation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-13 | Elevation (irreversible consume) | writing/preserve-dumping the 2516 | mitigate | D-21 hard rule: 2516 is READ ONLY this phase; checkpoint resume-signal explicitly requires "no write performed"; GRAD-03 stays DEFERRED (D-22). |
| T-84-14 | Tampering (over-voltage) | re-bench write on a VPP-driving op after the VPP-skip | accept (verified) | VPP-skip only gates read/blank-check (84-01 negative assertion); write/erase still gate VPP — over-voltage stays blocked; re-bench writes use the normal VPP-gated path. |
| T-84-15 | Spoofing (untrusted oracle) | a non-Leonardo or mis-calibrated read recorded as authoritative | mitigate | SAFE-01 gate (Task 1): controller=leonardo, port identity per task, r1≈270000 readback, operator-confirmed Rev 2.0; no non-Leonardo read authoritative. |
| T-84-16 | Repudiation (lost prior evidence) | overwriting Phase-81/82/83 EVIDENCE rows | mitigate | Append-only; md↔json consistency check; prior rows preserved (Task-3 AC). |
| T-84-SC | Tampering | package installs | n/a | No installs (D-52); reuse `dev consistency-check`/`dev write-cycle`/`gen_test_image.py`. |
</threat_model>

<verification>
- Phase-84 SAFE-01/02 gate block recorded (Leonardo, Rev 2.0, r1≈270000, host suite + 0xA4 + ruff green, Phase-84 fw flashed).
- 2516 re-read (N≥3) recorded READ-ONLY; GRAD-03/FUT-03 DEFERRED.
- 0x08 + flash4 re-bench dispositioned (fixed-if-trivial else tracker); stuck-bit FAILs not re-benched.
- EVIDENCE.{md,json} append-only + consistent.
</verification>

<success_criteria>
- FIX-01 bench re-verification done: in-posture fixes confirmed on silicon; deferred write-path defects formally dispositioned (SC#2/#3).
- 2516 read re-validated without consuming the part (D-21); GRAD-03 deferred best-effort (D-22, FUT-03 open).
</success_criteria>

<output>
Create `.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-05-SUMMARY.md` when done (record the 2516 stability verdict + the two write dispositions + the SAFE-01 preconditions).
</output>
