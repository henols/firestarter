---
phase: 84-db-decode-audit-conditional-defect-rca-milestone-evidence-co
plan: 04
type: execute
wave: 2
depends_on: ["84-01", "84-02", "84-03"]
files_modified:
  - .planning/v1.15/DECODE-AUDIT.md
  - .planning/REQUIREMENTS.md
files_modified_note: "Doc/traceability only (meta-repo). EVIDENCE.{md,json} bench appends are owned by Plan 84-05; this plan only writes the software-side audit doc + REQUIREMENTS traceability."
autonomous: true
requirements: [FIX-01]
must_haves:
  truths:
    - "A new standalone doc `.planning/v1.15/DECODE-AUDIT.md` consolidates all 11 chips' real-silicon-vs-DB findings (pinout, VPP, electrical type, algorithm, size), each row PASS / mismatch-with-disposition, cross-referencing EVIDENCE.{md,json} (SC#1)."
    - "The decode-audit doc records the dispositions for the bench-surfaced defects (VPP-skip fw fix shipped; FM1608 blank-check host fix shipped; the SST39SF040/FM1608 relabel outcome incl. any D-40 STOPped part as an observation; the genuine stuck-bit FAILs as silicon-limited per D-32; the 0x08 + flash4 + 2516 items as RCA-and-defer pending the 84-05 bench)."
    - "REWR-01/02/04 traceability rows are annotated with their silicon FAIL/deferral dispositions; the cosmetic UV-01..04 `[ ]` checkbox drift is corrected (D-41)."
  artifacts:
    - path: ".planning/v1.15/DECODE-AUDIT.md"
      provides: "Consolidated 11-chip decode-correctness audit (SC#1) with per-chip disposition"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "REWR-01/02/04 silicon dispositions + UV-01..04 checkbox drift fix (D-41)"
      contains: "REWR-04"
  key_links:
    - from: ".planning/v1.15/DECODE-AUDIT.md"
      to: ".planning/v1.15/bench/EVIDENCE.md"
      via: "per-chip cross-reference"
      pattern: "EVIDENCE"
---

<objective>
Produce the consolidated decode-correctness audit (SC#1) as a NEW standalone doc
`.planning/v1.15/DECODE-AUDIT.md` (D-42), and clean up the milestone-audit-flagged traceability
tech-debt (D-41). The audit confirms each of the 11 chips' real-silicon behaviour against its DB
claims (pinout, VPP, electrical type, algorithm, size), with a disposition for every mismatch and
every bench-surfaced defect.

Purpose: SC#1 deliverable + the FIX-01 disposition ledger; closes the v1.15 milestone-audit
documentation-accuracy tech-debt so a reader of REQUIREMENTS.md alone is not misled.
Output: DECODE-AUDIT.md + annotated REQUIREMENTS.md rows. (EVIDENCE.{md,json} bench appends are
Plan 84-05's; this plan is the software-side documentation only — it runs after 84-01/02/03 so it
can record their outcomes, and is updated again in Plan 84-06 with the bench results.)
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-CONTEXT.md
@.planning/v1.15/bench/EVIDENCE.md
@.planning/v1.15-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .planning/v1.15/DECODE-AUDIT.md — consolidated 11-chip decode-correctness audit (SC#1)</name>
  <read_first>
    - `.planning/v1.15/bench/EVIDENCE.md` (the full 11-chip sweep table + Phase-82 write rows + Phase-83 UV rows + the DB-01 decode-vs-silicon notes per chip — the raw per-chip log this doc consolidates)
    - `.planning/v1.15/bench/EVIDENCE.json` (machine-readable mirror; cross-check verdicts/SHAs)
    - `.planning/v1.15-MILESTONE-AUDIT.md` (the tech-debt + the FIX-01 inputs the audit must disposition)
    - the 84-01 / 84-02 / 84-03 SUMMARY files (the shipped VPP-skip / FM1608 / relabel outcomes + any D-40 STOPped part to record as an observation)
    - CONTEXT D-42 (new standalone doc), D-32 (genuine stuck-bit FAILs = silicon-limited, NOT FIX-01), D-31 (0x08/flash4 RCA-and-defer), D-43 (best-effort close)
  </read_first>
  <action>
    Create `.planning/v1.15/DECODE-AUDIT.md` as a standalone milestone-close artifact (Claude's
    discretion on layout, D-42). Include a per-chip decode-correctness table for all 11 chips: chip,
    family/algorithm, and for each DB-claimed attribute (pinout, VPP, electrical type, algorithm,
    size) CONFIRMED-vs-silicon or MISMATCH-with-disposition, cross-referencing the EVIDENCE.{md,json}
    row. Add a Dispositions section recording: (i) VPP-skip firmware fix SHIPPED (84-01, FIX-01 fw
    half); (ii) FM1608 blank-check host fix SHIPPED (84-02, FIX-01 host half); (iii) the
    SST39SF040/FM1608 relabel outcome from 84-03 — including any D-40 STOPped part as an explicit
    observation (e.g. "SST39SF040 kept `Flash/EEPROM` to preserve FLAG_CAN_ERASE; `Flash` is the
    intended class — observation only"); (iv) W27E512 + W27E040 genuine stuck-bit erase FAILs as
    silicon-limited dispositions (D-32 — NOT FIX-01 material, identical offset/value every reseat);
    (v) AM27C020 0x08 write + W29C040 flash4 256B-page + 2516 0x0B read as RCA-and-defer items whose
    bench outcome is pending Plan 84-05 (leave a clearly-marked "pending 84-05 bench" placeholder these
    will fill in 84-06). Mark the doc as the SC#1 consolidated audit.
  </action>
  <verify>
    <automated>test -f .planning/v1.15/DECODE-AUDIT.md && grep -c "CONFIRMED\|MISMATCH\|disposition\|EVIDENCE" .planning/v1.15/DECODE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.15/DECODE-AUDIT.md` exists, lists all 11 chips, and gives each DB attribute a CONFIRMED/MISMATCH-with-disposition verdict cross-referencing EVIDENCE.
    - The Dispositions section records all five categories above (VPP-skip shipped; FM1608 shipped; relabel/STOP outcome; stuck-bit silicon-limited per D-32; 0x08/flash4/2516 RCA-and-defer pending 84-05).
    - Any D-40 STOPped relabel part appears as an explicit observation (not silently dropped).
  </acceptance_criteria>
  <done>DECODE-AUDIT.md is the SC#1 consolidated 11-chip audit with full dispositions + bench-pending placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate REWR-01/02/04 silicon dispositions + fix UV-01..04 checkbox drift (D-41)</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` (REWR-01/02/04 rows ~lines 23-26 + traceability table ~90-93; UV-01..04 definition checkboxes ~31-34 vs the traceability rows ~95-98)
    - `.planning/v1.15-MILESTONE-AUDIT.md` §"Tech Debt" (the exact overstatements: REWR-01 W27E512 FAIL, REWR-02 W27E040 FAIL→FUT-05, REWR-04 W29C040 FAIL→Phase-84 FIX-01; UV-01..04 cosmetic drift)
    - `.planning/v1.15/bench/EVIDENCE.md` Phase-82 rows #2/#4/#7 (the genuine FAILs) + #8 (W29C020 PASS)
    - CONTEXT D-41 (annotate REWR rows with silicon dispositions; fix UV checkbox drift)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, annotate the REWR-01, REWR-02, and REWR-04 rows (both the
    requirement definitions and the traceability table) with their silicon dispositions so a reader of
    REQUIREMENTS.md alone is not misled: REWR-01 — W27E512 FAILed on a genuine stuck bit (silicon-limited,
    D-32); only W27C512 + SST27SF512 passed on silicon. REWR-02 — no positive 0x08 write PASS; W27E040
    FAILed on a genuine stuck bit → deferred FUT-05 (needs a functional 0x08 chip). REWR-04 — W29C040
    FAILed on the flash4 256B-page write → handed to Phase-84 FIX-01 (disposition recorded after the
    84-05 re-bench); only W29C020 passed (the Flash/EEPROM auto-erase silicon confirmation). Also fix
    the cosmetic UV-01..04 `[ ]` definition checkbox drift to match their `Complete` traceability rows.
    Do NOT alter the satisfied-by-disposition status semantics — only make the annotations honest.
  </action>
  <verify>
    <automated>grep -n "REWR-01\|REWR-02\|REWR-04\|silicon\|stuck\|FUT-05\|FIX-01" .planning/REQUIREMENTS.md | head -30 && grep -c "^- \[x\] \*\*UV-0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REWR-01/02/04 rows (definition + traceability) carry their silicon FAIL/deferral dispositions (stuck-bit silicon-limited; FUT-05; W29C040→FIX-01).
    - UV-01..04 definition checkboxes are `[x]` consistent with their `Complete` traceability rows (drift fixed).
    - No requirement's satisfied/deferred STATUS is changed — only the annotations are added.
  </acceptance_criteria>
  <done>REWR-01/02/04 traceability is honest about silicon outcomes; UV checkbox drift corrected (D-41).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (documentation integrity)

| Boundary | Description |
|----------|-------------|
| EVIDENCE.{md,json} raw log → DECODE-AUDIT.md / REQUIREMENTS.md summaries | a summary that overstates silicon outcomes misleads the milestone-close reader |

## STRIDE Threat Register (documentation adaptation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-11 | Repudiation (overstated traceability) | REWR-01/02/04 marked Complete without silicon qualification | mitigate | D-41 annotations cross-referenced to EVIDENCE rows; the v1.15 milestone-audit tech-debt is the source of truth. |
| T-84-12 | Repudiation (dropped STOP observation) | a D-40 STOPped relabel part silently omitted from the audit | mitigate | DECODE-AUDIT Dispositions section MUST record any STOPped part as an explicit observation (Task 1 AC). |
| T-84-SC | Tampering | package installs | n/a | No installs (D-52); doc-only plan. |
</threat_model>

<verification>
- DECODE-AUDIT.md exists with the 11-chip audit + full dispositions + bench-pending placeholders.
- REWR-01/02/04 annotated; UV-01..04 checkbox drift fixed.
- No requirement status changed (annotations only).
</verification>

<success_criteria>
- SC#1 consolidated decode audit delivered as a standalone doc (D-42).
- Milestone-audit documentation-accuracy tech-debt closed (D-41).
</success_criteria>

<output>
Create `.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-04-SUMMARY.md` when done.
</output>
