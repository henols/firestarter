---
phase: 84-db-decode-audit-conditional-defect-rca-milestone-evidence-co
plan: 06
type: execute
wave: 4
depends_on: ["84-04", "84-05"]
files_modified:
  - .planning/v1.15/DECODE-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FIX-01]
must_haves:
  truths:
    - "The DECODE-AUDIT.md bench-pending placeholders (2516 0x0B read, AM27C020 0x08 write, W29C040 flash4) are filled with the Plan 84-05 re-bench verdicts + dispositions (SC#1 complete on real silicon)."
    - "FIX-01 is recorded as CLOSED 'fixed where in-posture; deeper write-path defects RCA'd + deferred with rationale' (D-43) — VPP-skip fw fix + FM1608 host fix shipped + re-verified; 0x08 / W29C040 flash4 / 2516-write dispositioned + future-tracked; genuine stuck-bit FAILs silicon-limited (D-32)."
    - "The full software phase gate is green: full host suite, check_dispatch.py, diff_db.py, CI-scoped ruff, and `pio test -e native` (firmware was touched) — and Leonardo flash ≤ ~90% was confirmed (SC#3)."
    - "GRAD-03 / FUT-03 remain documented best-effort DEFERRALS (D-22), NOT mistaken for gaps; milestone close itself is operator-gated (D-12/D-43) and is NOT performed here."
  artifacts:
    - path: ".planning/v1.15/DECODE-AUDIT.md"
      provides: "Finalized SC#1 audit with bench verdicts + the FIX-01 close-statement"
      contains: "FIX-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FIX-01 final status (closed per D-43); GRAD-03/FUT-03 best-effort-deferred disposition"
      contains: "FIX-01"
  key_links:
    - from: ".planning/v1.15/DECODE-AUDIT.md"
      to: ".planning/v1.15/bench/EVIDENCE.md"
      via: "bench verdicts pulled from the Phase-84 bench section"
      pattern: "Phase 84"
---

<objective>
Finalize the milestone evidence: fill the DECODE-AUDIT.md bench-pending placeholders with the Plan
84-05 re-bench verdicts, write the FIX-01 close-statement (D-43), update REQUIREMENTS.md FIX-01 +
GRAD-03/FUT-03 dispositions, and run the full software phase gate (SC#3). This is the consolidation
gate before `/gsd-verify-work`.

Purpose: completes SC#1 (audit reflects real silicon), SC#2 (FIX-01 closed in-posture +
deferrals), and SC#3 (all gates green). The actual `/gsd-complete-milestone` call + firmware
versioning/beta-cut are operator-gated (D-12/D-43) and explicitly NOT done here.
Output: finalized DECODE-AUDIT.md + REQUIREMENTS.md; a recorded green phase gate.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-CONTEXT.md
@.planning/v1.15/bench/EVIDENCE.md
@.planning/v1.15/DECODE-AUDIT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the DECODE-AUDIT bench verdicts + write the FIX-01 close-statement (D-43)</name>
  <read_first>
    - `.planning/v1.15/bench/EVIDENCE.md` Phase-84 bench section (the 84-05 re-bench verdicts: 2516 read stability, AM27C020 0x08 outcome, W29C040 flash4 outcome + dispositions)
    - `.planning/v1.15/DECODE-AUDIT.md` (the bench-pending placeholders authored in 84-04 Task 1)
    - the 84-05 SUMMARY (the 2516 stability verdict + the two write dispositions)
    - CONTEXT D-43 (FIX-01 closes as "fixed where in-posture; deeper write-path defects RCA'd + deferred with rationale" — NOT "all clean"), D-22 (GRAD-03/FUT-03 best-effort deferral), D-32 (stuck-bit silicon-limited)
  </read_first>
  <action>
    Update `.planning/v1.15/DECODE-AUDIT.md`: replace the "pending 84-05 bench" placeholders with the
    actual re-bench verdicts (2516 read stabilized-or-not + decode confirmation; AM27C020 0x08
    programs-or-deferred-with-tracker; W29C040 flash4 256B-page programs-or-reopen-Phase-74-Wave-2).
    Add the FIX-01 close-statement per D-43: VPP-skip firmware fix SHIPPED + bench-confirmed (read
    refusal cleared); FM1608 blank-check host fix SHIPPED + tested; the SST39SF040/FM1608 relabel
    outcome (incl. any D-40 STOP observation); AM27C020 0x08 + W29C040 flash4 RCA'd + deferred with
    named future trackers; W27E512/W27E040 silicon-limited (D-32); GRAD-03/FUT-03 (2516 write)
    documented best-effort DEFERRAL (D-22) — explicitly flagged as an intentional deferral so the
    verifier does NOT treat it as a gap. State that milestone close + firmware beta-cut are
    operator-gated (D-12/D-43) and not performed in this phase.
  </action>
  <verify>
    <automated>grep -ci "pending 84-05\|placeholder\|TBD" .planning/v1.15/DECODE-AUDIT.md; grep -c "FIX-01\|DEFERRED\|FUT-03\|operator-gated" .planning/v1.15/DECODE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - No "pending 84-05" / "placeholder" / "TBD" markers remain in DECODE-AUDIT.md (first grep returns 0).
    - The FIX-01 close-statement is present with all dispositions (VPP-skip + FM1608 shipped; 0x08/flash4 deferred-with-tracker; stuck-bit silicon-limited; GRAD-03/FUT-03 best-effort deferral flagged as intentional).
    - The doc states milestone close + beta-cut are operator-gated and not done here.
  </acceptance_criteria>
  <done>DECODE-AUDIT.md reflects real-silicon bench verdicts + the FIX-01 close-statement; deferrals flagged intentional.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md FIX-01 + GRAD-03/FUT-03 final dispositions</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` (FIX-01 row ~line 49 + traceability ~105; GRAD-03 ~40 + traceability ~101; FUT-03 ~102)
    - the finalized `.planning/v1.15/DECODE-AUDIT.md` (the authoritative close-statement to mirror)
    - CONTEXT D-43 / D-22 / D-12
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md`: set FIX-01's status per D-43 ("fixed where in-posture
    [VPP-skip fw + FM1608 host, bench-re-verified]; deeper write-path defects [AM27C020 0x08, W29C040
    flash4] RCA'd + deferred with rationale + future trackers; stuck-bit FAILs silicon-limited") —
    cross-referencing DECODE-AUDIT.md + the EVIDENCE Phase-84 section. Keep GRAD-03 + FUT-03 as
    documented best-effort DEFERRALS (D-22): 2516 read re-validated (read only, D-21), write proof
    deferred, FUT-03 remains OPEN best-effort. Do NOT mark FIX-01 as a blanket "complete/clean" and do
    NOT close GRAD-03/FUT-03. Note that the milestone close + firmware beta-cut are operator-gated
    (D-12). Make the traceability honest and consistent with DECODE-AUDIT.md.
  </action>
  <verify>
    <automated>grep -n "FIX-01\|GRAD-03\|FUT-03\|in-posture\|deferred\|operator-gated" .planning/REQUIREMENTS.md | head -25</automated>
  </verify>
  <acceptance_criteria>
    - FIX-01 row reflects the D-43 close-statement (in-posture fixes + RCA'd-and-deferred defects), cross-referencing DECODE-AUDIT.md / EVIDENCE.
    - GRAD-03 + FUT-03 remain documented best-effort DEFERRALS (not closed); 2516 recorded as read-revalidated-only (D-21).
    - REQUIREMENTS.md is consistent with DECODE-AUDIT.md; milestone close noted operator-gated.
  </acceptance_criteria>
  <done>REQUIREMENTS.md FIX-01 + GRAD-03/FUT-03 final dispositions recorded, consistent with the audit.</done>
</task>

<task type="auto">
  <name>Task 3: Full software phase gate (SC#3) — host suite + check_dispatch + diff_db + native + flash fit</name>
  <read_first>
    - 84-VALIDATION.md §"Sampling Rate" → "Before `/gsd-verify-work`" (full host suite + `pio test -e native` + Leonardo flash ≤ ~90% if firmware touched)
    - 84-RESEARCH.md §"Pitfall 5" (CI-scoped ruff `firestarter/ tests/`; flag not fix `tools/` findings)
    - CONTEXT D-51 (SAFE-02 incl. 0xA4 guard), D-10 (firmware gate), ROADMAP Phase 84 SC#3
  </read_first>
  <action>
    Run the full phase gate and record the result for `/gsd-verify-work`: full host suite (incl. the
    0xA4 guard `test_init_phase_data_frames_not_acked`), `python tools/check_dispatch.py` (full-DB
    VPP-safety, 0 violations), `python tools/diff_db.py` (relabel explained, exit 0), CI-scoped
    `ruff check firestarter/ tests/` + `ruff format --check firestarter/ tests/`, and — since firmware
    was touched (84-01) — `cd firestarter && pio test -e native` green. Confirm the 84-01-recorded
    Leonardo flash ≤ ~90% (and the 84-05 re-flash succeeded). Flag (not fix) any pre-existing
    `tools/`-tree ruff findings. If any gate is red, STOP and surface — do not mask. Record the green
    gate summary in the SUMMARY.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest 2>&1 | tail -4 && python tools/check_dispatch.py && python tools/diff_db.py && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && cd ../firestarter && pio test -e native 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Full host suite green incl. the 0xA4 guard; `check_dispatch.py` exits 0; `diff_db.py` exits 0.
    - `ruff check firestarter/ tests/` + `ruff format --check firestarter/ tests/` exit 0; out-of-scope `tools/` findings flagged not fixed.
    - `pio test -e native` green (firmware touched); Leonardo flash ≤ ~90% confirmed (from 84-01/84-05).
    - The green gate summary is recorded for `/gsd-verify-work` (SC#3).
  </acceptance_criteria>
  <done>Full software phase gate green (host + dispatch + diff + ruff + native + flash fit); recorded for verification (SC#3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (close integrity)

| Boundary | Description |
|----------|-------------|
| intentional deferrals → verifier | a best-effort deferral (GRAD-03/FUT-03) mistaken for a gap fails the close incorrectly |
| green-gate claim → `/gsd-verify-work` | a masked/red gate claimed green would ship an unsafe DB or firmware |

## STRIDE Threat Register (close adaptation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-17 | Repudiation (deferral-as-gap) | GRAD-03/FUT-03 + 0x08/flash4 deferrals read as failures | mitigate | Explicitly flagged as intentional best-effort deferrals (D-22/D-31/D-43) in DECODE-AUDIT + REQUIREMENTS; verifier reads them as documented dispositions. |
| T-84-18 | Tampering (masked gate) | claiming green while a gate is red / masking `tools/` findings | mitigate | Task-3 runs all gates with hard exit-code asserts; STOP-and-surface on red; out-of-scope findings flagged not masked (D-51). |
| T-84-19 | Elevation (premature close) | performing the milestone close / beta-cut in this phase | accept-by-design (prevented) | Milestone close + firmware versioning/beta-cut are operator-gated (D-12/D-43); this phase only documents readiness. |
| T-84-SC | Tampering | package installs | n/a | No installs (D-52). |
</threat_model>

<verification>
- DECODE-AUDIT.md + REQUIREMENTS.md finalized + consistent; no placeholders left.
- FIX-01 closed per D-43; GRAD-03/FUT-03 best-effort deferred (intentional, not gaps).
- Full software gate green (host + check_dispatch + diff_db + ruff + native + flash fit), recorded.
- Milestone close / beta-cut NOT performed (operator-gated).
</verification>

<success_criteria>
- SC#1 audit reflects real silicon; SC#2 FIX-01 closed in-posture + dispositioned deferrals; SC#3 all gates green.
- Intentional deferrals recorded so verification does not treat them as gaps.
</success_criteria>

<output>
Create `.planning/phases/84-db-decode-audit-conditional-defect-rca-milestone-evidence-co/84-06-SUMMARY.md` when done.
</output>
