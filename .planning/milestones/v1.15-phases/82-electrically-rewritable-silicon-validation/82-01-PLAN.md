---
phase: 82-electrically-rewritable-silicon-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/gen_test_image.py
  - firestarter_app/tests/test_gen_test_image.py
  - .planning/v1.15/bench/EVIDENCE.md
  - .planning/v1.15/bench/EVIDENCE.json
autonomous: true
requirements: [EVID-02, EVID-03, DB-01]
user_setup: []

must_haves:
  truths:
    - "A deterministic per-chip full-size image generator exists and reproduces byte-identical output for a fixed seed"
    - "The host test suite (incl. the 0xA4 ack_data=False guard) and ruff are green before any bench session"
    - "EVIDENCE.{md,json} declares the Phase 82 write-row extension columns without dropping any Phase 81 row"
  artifacts:
    - path: "firestarter_app/tools/gen_test_image.py"
      provides: "Deterministic full-size pseudo-random image generator (size_bytes + seed -> file)"
      contains: "random.Random"
    - path: "firestarter_app/tests/test_gen_test_image.py"
      provides: "Determinism + size pinning test for the generator"
    - path: ".planning/v1.15/bench/EVIDENCE.json"
      provides: "Phase 82 evid_extension_columns declared; Phase 81 cells preserved"
      contains: "write_image_seed_A"
  key_links:
    - from: "firestarter_app/tools/gen_test_image.py"
      to: "chip size_bytes"
      via: "size_bytes CLI arg sourced from chip_database.json electrical.size_bytes"
      pattern: "size_bytes|argv"
---

<objective>
Produce the only new code artifact this phase permits (the deterministic per-chip image
generator, D-03/D-04), prove SAFE-02 green (host suite incl. the 0xA4 guard + ruff against the
CI target), and extend the Phase 81 EVIDENCE.{md,json} scaffold with the Phase 82 write-row
columns — all host-side, no bench, no firmware.

Purpose: Gates every bench task in Plans 02/03. Each A→B write-cycle (D-05) requires two
full-size, deterministic, non-trivial source images per chip; without them the write-cycle SHA
oracle is meaningless. SAFE-02 must be green before any bench session (D-11). EVIDENCE must be
ready to append write rows (D-13) without disturbing the Phase 81 read rows.

Output: `tools/gen_test_image.py` + its pinning test; a recorded SAFE-02 green transcript; the
extended EVIDENCE schema.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-electrically-rewritable-silicon-validation/82-CONTEXT.md
@.planning/phases/82-electrically-rewritable-silicon-validation/82-PATTERNS.md
@/workspaces/firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic per-chip image generator + pinning test</name>
  <files>firestarter_app/tools/gen_test_image.py, firestarter_app/tests/test_gen_test_image.py</files>
  <read_first>
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-PATTERNS.md (the gen_test_image.py analog block — write_test.sh lines 36-68 + the deterministic random.Random(seed) Python equivalent + the suggested /tmp/firestarter_bench_p82/<chip>_img_{A,B}.bin storage convention)
    - firestarter_app/write_test.sh (lines 36-68 — the existing /dev/urandom image-generation block this replaces with a DETERMINISTIC equivalent per D-03)
    - firestarter_app/CLAUDE.md (Tooling gate section — ruff + mypy strict + cov-fail-under=70; the new tool lives under tools/ which is NOT one of the 8 mypy-strict modules, but must still be ruff-clean)
  </read_first>
  <behavior>
    - generate_image(size_bytes, seed) returns exactly size_bytes bytes
    - Same (size_bytes, seed) returns byte-identical output across two calls (determinism via random.Random(seed))
    - Distinct seeds (1 vs 2) over the same size produce DIFFERENT output (so image A != image B → the A→B auto-erase proof is non-vacuous)
    - Output is non-trivial: not uniform 0xFF and not uniform 0x00 (D-03 — an all-0xFF image would not prove a write landed)
    - CLI form `python tools/gen_test_image.py <size_bytes> <seed> <output_path>` writes the image and prints its SHA-256 to stdout (the value EVIDENCE records as sha256_image_A/B)
  </behavior>
  <action>
    Create `tools/gen_test_image.py` implementing a `generate_image(size_bytes: int, seed: int) -> bytes`
    function using `random.Random(seed)` for reproducibility (D-03/D-04), plus a `__main__` CLI taking
    positional `size_bytes seed output_path`, writing the file and printing `hashlib.sha256(data).hexdigest()`.
    Do NOT use `/dev/urandom` (non-deterministic — the whole point per D-03). Storage convention for the
    generated images is `/tmp/firestarter_bench_p82/<chip>_img_A.bin` (seed=1) and `_img_B.bin` (seed=2) at
    each chip's `electrical.size_bytes` (Plans 02/03 invoke this). Add `tests/test_gen_test_image.py` pinning:
    (a) output length == size_bytes for a small size; (b) two calls with seed=1 byte-identical;
    (c) seed=1 output != seed=2 output; (d) output is neither all-0xFF nor all-0x00. Keep the tool ruff-clean.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python -m pytest tests/test_gen_test_image.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_gen_test_image.py` passes (determinism, size, seed-distinctness, non-trivial assertions all green)
    - `python tools/gen_test_image.py 65536 1 /tmp/gt_a.bin` followed by a second run produces the identical printed SHA-256 (determinism observable at the CLI)
    - `ruff check tools/gen_test_image.py` exits 0
  </acceptance_criteria>
  <done>Generator produces deterministic, full-size, non-trivial images; pinning test green; ruff-clean.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-02 green gate + EVIDENCE Phase 82 column extension</name>
  <files>.planning/v1.15/bench/EVIDENCE.md, .planning/v1.15/bench/EVIDENCE.json</files>
  <read_first>
    - .planning/v1.15/bench/EVIDENCE.md (the existing Phase 81 sweep table + header row; Phase 82 EXTENDS it — do NOT replace or rewrite the Phase 81 rows)
    - .planning/v1.15/bench/EVIDENCE.json (existing locked_columns + evid_extension_columns + the 11 Phase 81 cells; Phase 82 ADDS extension columns and will append cells in Plans 02/03)
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-PATTERNS.md (the EVIDENCE.md/json extension column schema: write_image_seed_A, sha256_image_A, write_image_seed_B, sha256_image_B, cr01_risk; the op column values write_A+verify_A / write_B+verify_B; verdict wording PASS / FAIL (CR-01) / FAIL (genuine) / ANOMALY)
    - firestarter_app/CLAUDE.md (Tooling gate: ruff check + ruff format --check + mypy strict 8 modules + pytest --cov-fail-under=70)
    - reference: devcontainer Py3.12 masks CI py3.9/3.11 — validate ruff against the CI target before claiming green (D-11)
  </read_first>
  <action>
    First run the SAFE-02 gate from `/workspaces/firestarter_app`: `pip install -e '.[test]'` if needed, then
    `ruff check .`, `ruff format --check .`, and `python -m pytest -q` — confirming the suite is green INCLUDING
    the 0xA4 guard `test_init_phase_data_frames_not_acked` (run it explicitly: `python -m pytest -k test_init_phase_data_frames_not_acked -q`).
    Record the pass counts + that the 0xA4 guard passed into the EVIDENCE.md SAFE-01/02 preconditions block
    (extend, do not overwrite Phase 81's block — add a "Phase 82 SAFE-02 gate" subsection). Then EXTEND the
    EVIDENCE artifacts per D-13: in EVIDENCE.json add `write_image_seed_A`, `sha256_image_A`,
    `write_image_seed_B`, `sha256_image_B`, `cr01_risk` to `evid_extension_columns` (keep `read_count` +
    `blank_check_result`), preserve all 11 Phase 81 cells unchanged, and add a `phase82` section stub
    documenting the new op values and verdict taxonomy. In EVIDENCE.md add a new "## Phase 82 — Rewritable A→B
    Write Validation" section with the extended header row (Phase 81 columns + the 5 new write columns) and an
    empty body the bench plans append to. Do NOT touch the Phase 81 table rows.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ruff check . && ruff format --check . && python -m pytest -k test_init_phase_data_frames_not_acked -q && python -c "import json; d=json.load(open('/workspaces/.planning/v1.15/bench/EVIDENCE.json')); assert 'write_image_seed_A' in d['evid_extension_columns']; assert len(d['cells'])==11"</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` and `ruff format --check .` both exit 0 from `/workspaces/firestarter_app`
    - The 0xA4 guard `test_init_phase_data_frames_not_acked` PASSES (SAFE-02; D-11)
    - EVIDENCE.json `evid_extension_columns` now contains all 5 Phase 82 write columns AND still contains `read_count`/`blank_check_result`; `cells` length is still 11 (no Phase 81 row dropped)
    - EVIDENCE.md gains a "## Phase 82 — Rewritable A→B Write Validation" section with the extended header row; the Phase 81 sweep table is byte-unchanged
  </acceptance_criteria>
  <done>SAFE-02 green recorded; EVIDENCE schema extended for Phase 82 write rows with zero Phase 81 data loss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → bench source image | A non-deterministic or trivial image would make the A→B SHA oracle vacuous |
| Phase 82 EVIDENCE edit → Phase 81 record | An overwrite would destroy the load-bearing read-sweep + UV blank-state record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Tampering | gen_test_image.py | mitigate | Determinism + non-trivial (not all-0xFF/0x00) pinned by `test_gen_test_image.py`; seed recorded in EVIDENCE so any image is reproducible |
| T-82-02 | Repudiation | EVIDENCE.{md,json} | mitigate | Append-only edit; verify asserts `len(cells)==11` (Phase 81 rows preserved); Phase 81 table left byte-unchanged |
| T-82-03 | Information Disclosure | SAFE-02 gate | accept | No secrets; test/lint output only, low-value target |
| T-82-SC | Tampering | pip install -e '.[test]' | mitigate | No NEW third-party dependency introduced (EVID-02); reinstalls existing pinned extras only — no package-manager additions, so no slopcheck checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_gen_test_image.py` green (Task 1)
- `ruff check .` + `ruff format --check .` exit 0; 0xA4 guard passes (Task 2)
- EVIDENCE.json has 5 new extension columns + 11 preserved cells (Task 2)
</verification>

<success_criteria>
- Deterministic full-size image generator exists, reproduces a fixed SHA per seed, and is pinned by test
- SAFE-02 is green (host suite incl. 0xA4 guard + ruff against CI target) and recorded in EVIDENCE
- EVIDENCE.{md,json} is ready to receive Phase 82 write rows without any loss of Phase 81 content
</success_criteria>

<output>
Create `.planning/phases/82-electrically-rewritable-silicon-validation/82-01-SUMMARY.md` when done
</output>
