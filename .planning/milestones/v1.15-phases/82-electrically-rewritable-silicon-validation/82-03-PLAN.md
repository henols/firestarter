---
phase: 82-electrically-rewritable-silicon-validation
plan: 03
type: execute
wave: 3
depends_on: [82-01, 82-02]
files_modified:
  - .planning/v1.15/bench/EVIDENCE.md
  - .planning/v1.15/bench/EVIDENCE.json
autonomous: false
requirements: [REWR-04, DB-01]
user_setup: []

must_haves:
  truths:
    - "W29C040 (0x05 flash4, Flash/EEPROM) passes full write→read→verify with SHA match AND auto-erase confirmed for the Flash/EEPROM type via A→B"
    - "W29C020 (0x05 flash4) is attempted on the current firmware as-is; a mid-page-poll failure is pre-attributed to CR-01 and handed to Phase 84 (not fixed here)"
    - "The A→B clean-B proof is the first bench confirmation that FLAG_CAN_ERASE fires for the Flash/EEPROM electrical type on silicon"
    - "Each PASS is non-vacuous (N≥3 byte-identical read + wrong-file verify RC=1)"
  artifacts:
    - path: ".planning/v1.15/bench/EVIDENCE.md"
      provides: "W29C040 + W29C020 A→B write-validation rows; W29C020 carries the CR-01 pre-attribution"
      contains: "CR-01"
    - path: ".planning/v1.15/bench/EVIDENCE.json"
      provides: "2 appended flash4 cells; W29C020 cr01_risk set; both DB-01 decode-confirmed"
  key_links:
    - from: "dev write-cycle W29C040 <image_B>"
      to: "Flash/EEPROM auto-erase proof"
      via: "clean B read-back SHA with no explicit erase (the FIRST silicon proof of the FLAG_CAN_ERASE Flash/EEPROM branch)"
      pattern: "write-cycle"
---

<objective>
Bench-prove REWR-04 — the two 0x05 flash4 Winbond chips — via the A→B rewrite on Leonardo + RURP
Rev 2.0: W29C040 (512KB, heuristic-correct under `flash4_page_size`) and W29C020 (256KB, carrying
the KNOWN CR-01 page-size under-sizing risk per D-01/D-02). Confirm auto-erase for the
`Flash/EEPROM` electrical type (REWR-04 SC#3 — the FIRST silicon proof of that branch) and confirm
DB decode vs silicon (DB-01).

Purpose: W29C020/W29C040 are `Flash/EEPROM` (not plain `EEPROM`) and are bench-proven for the first
time here — so the FLAG_CAN_ERASE Flash/EEPROM path that Phase 81's DB-02 re-audited in code gets
its first hardware confirmation. Per D-01 this phase stays host-only / no-firmware-scope: a W29C020
mid-page-poll failure is EXPECTED, pre-attributed to CR-01, recorded as FAIL (CR-01), and handed to
Phase 84 FIX-01 — NOT fixed inline.

Output: 2 appended EVIDENCE.{md,json} rows (W29C040, W29C020), the W29C020 one pre-carrying the
CR-01 root-cause pointer.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-electrically-rewritable-silicon-validation/82-CONTEXT.md
@.planning/phases/82-electrically-rewritable-silicon-validation/82-PATTERNS.md
@.planning/todos/pending/flash4-page-size-datasheet-sourced-cr01.md
@.planning/v1.15/bench/EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: SAFE-01 bench precondition + shield-rev confirmation (flash4 session gate)</name>
  <files>.planning/v1.15/bench/EVIDENCE.md</files>
  <what-built>
    Re-affirm the SAFE-01 hardware preconditions for the flash4 bench session (the `controller:` port can
    shuffle across USB replug between Plan 82-02 and this session, D-10), and re-obtain the operator's
    silkscreen shield-rev confirmation. SAFE-02 (host suite + 0xA4 guard) from Plan 82-01 must still be green.
  </what-built>
  <read_first>
    - .planning/STATE.md (§"Standing bench precondition" — Leonardo + Rev 2.0 ONLY; r1 ≈ 270000; verify controller: port identity per task after any USB event; ASK silkscreen shield rev)
    - .planning/v1.15/bench/EVIDENCE.md (the Phase 82 SAFE-01 block from Plan 82-02 — mirror its format for this session)
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-CONTEXT.md (D-10/D-11)
  </read_first>
  <action>
    Confirm and record (in the EVIDENCE.md Phase 82 SAFE-01 block, this flash4 session) that `controller:` =
    Leonardo with its `/dev/ttyACM*` port (re-verified — it may differ from Plan 82-02's session), live
    `r1 ≈ 270000` (STOP + recalibrate if it reads the 1000 default), operator-stated silkscreen shield rev =
    Rev 2.0 (ASK — do NOT infer from the EEPROM byte), and Plan 82-01's SAFE-02 gate still green. Blocking human
    gate; no flash4 write runs until the operator confirms the mounted rev.
  </action>
  <how-to-verify>
    1. `firestarter hw info` — confirm `controller:` = Leonardo, note the `/dev/ttyACM*` port (re-verify; it may differ from Plan 82-02's session).
    2. Confirm live `r1 ≈ 270000` (R2 ≈ 44000) — STOP and recalibrate if it reads the 1000 default.
    3. ASK the operator which silkscreen shield rev is mounted; record it (must be Rev 2.0). Do NOT infer from the EEPROM byte.
    4. Confirm Plan 82-01 SAFE-02 (host suite + 0xA4 guard) is still green; re-run if the tree changed.
  </how-to-verify>
  <resume-signal>Type "approved Rev 2.0" (or state the actual mounted rev + port) to proceed, or describe the blocker</resume-signal>
  <verify>
    <human-check>Operator confirms Rev 2.0 mounted; controller=Leonardo + port recorded; live r1 ≈ 270000; SAFE-02 green. Preconditions written to EVIDENCE.md flash4-session block.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator-stated shield rev (Rev 2.0) + `controller:`=Leonardo + port recorded for this flash4 session; live `r1 ≈ 270000` confirmed
    - SAFE-02 confirmed green; preconditions written into the EVIDENCE.md Phase 82 SAFE-01 block
  </acceptance_criteria>
  <done>SAFE-01 preconditions verified + operator shield-rev sign-off recorded for the flash4 session; bench cleared.</done>
</task>

<task type="auto">
  <name>Task 2: W29C040 A→B validation (heuristic-correct flash4) — Flash/EEPROM auto-erase proof (REWR-04) + DB-01</name>
  <files>.planning/v1.15/bench/EVIDENCE.md, .planning/v1.15/bench/EVIDENCE.json</files>
  <read_first>
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-PATTERNS.md (the `dev write-cycle` A→B pattern; `dev validate-family flash4 --chip W29C040 --source` as an alternative driver; the EVIDENCE row/cell shapes; consistency-check N≥3 oracle; negative-control verify)
    - firestarter_app/firestarter/data/chip_database.json (W29C040 entry — pinout DIP32_SST39SF040, electrical.type Flash/EEPROM, vpp_mv 12000, size_bytes 524288 — confirm vs silicon for DB-01)
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-CONTEXT.md (D-01 — W29C040 at 512KB is heuristic-CORRECT under flash4_page_size and is the unaffected representative; REWR-04 SC#3 = auto-erase confirmed for the Flash/EEPROM type)
    - .planning/v1.15/bench/EVIDENCE.md (Phase 81 W29C040 read row PASS N=3 — the A→B starting state)
  </read_first>
  <action>
    For W29C040 (size_bytes 524288, algorithm 0x05 flash4, Flash/EEPROM, on Leonardo + Rev 2.0):
    generate image A (seed 1) + image B (seed 2) via `tools/gen_test_image.py` at 524288, recording each SHA.
    Run `firestarter dev write-cycle W29C040 /tmp/firestarter_bench_p82/W29C040_img_A.bin --runs 1` (RC=0 = A verified),
    then `firestarter dev write-cycle W29C040 /tmp/firestarter_bench_p82/W29C040_img_B.bin --runs 1` WITHOUT explicit erase —
    a clean RC=0 on B is the auto-erase proof for the Flash/EEPROM type (REWR-04 SC#3; D-05). Apply the non-vacuous
    bar (D-14): `firestarter dev consistency-check W29C040 --runs 3` (N≥3 byte-identical) + a negative-control
    `firestarter verify W29C040 /tmp/firestarter_bench_p82/W29C040_img_A.bin` after B (must RC=1). On failure, reseat
    + retry N=2 (D-08) then FAIL (genuine)/ANOMALY + CONTINUE. Confirm DB-01 decode (DIP32_SST39SF040 / Flash/EEPROM /
    12V / 524288) vs silicon; record any mismatch for Phase 84 (no inline DB edit). Append one EVIDENCE.md row +
    EVIDENCE.json cell: op `write_A+verify_A → write_B+verify_B`, seeds 1/2, both image SHAs, final B SHA, verdict,
    cr01_risk="none", and an anomalies note that this is the first silicon proof of the FLAG_CAN_ERASE Flash/EEPROM branch.
  </action>
  <verify>
    <human-check>W29C040: write-cycle A RC=0, write-cycle B RC=0 (Flash/EEPROM auto-erase proof), consistency-check --runs 3 RC=0, negative-control verify RC=1; OR FAIL/ANOMALY after N=2 reseat+retry. EVIDENCE updated.</human-check>
    <automated>cd /workspaces && python -c "import json; d=json.load(open('.planning/v1.15/bench/EVIDENCE.json')); c=[x for x in d['cells'] if x['chip']=='W29C040' and x.get('op','').startswith('write')]; assert c, 'no W29C040 write cell'"</automated>
  </verify>
  <acceptance_criteria>
    - W29C040 A→B recorded: PASS (both RC=0 + clean B SHA = Flash/EEPROM auto-erase proven, REWR-04 SC#3) or FAIL/ANOMALY after N=2 reseat+retry
    - PASS is non-vacuous: consistency-check --runs 3 byte-identical + wrong-file verify RC=1
    - EVIDENCE row carries seeds 1/2, both image SHAs, final B SHA, cr01_risk="none", DB-01 decode-vs-silicon confirmation, and the "first Flash/EEPROM branch silicon proof" note
  </acceptance_criteria>
  <done>W29C040 A→B-validated; Flash/EEPROM auto-erase confirmed on silicon (REWR-04 SC#3) or recorded FAIL/ANOMALY; DB-01 evidence appended.</done>
</task>

<task type="auto">
  <name>Task 3: W29C020 A→B attempt-as-is with CR-01 pre-attribution (REWR-04) + DB-01</name>
  <files>.planning/v1.15/bench/EVIDENCE.md, .planning/v1.15/bench/EVIDENCE.json</files>
  <read_first>
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-CONTEXT.md (D-01/D-02 — RUN W29C020 write→verify on the CURRENT firmware AS-IS; flash4_page_size(262144) guesses 128B, real datasheet page = 256B; a mid-page-poll failure is EXPECTED → record FAIL (CR-01) and hand to Phase 84, do NOT fix inline; W29C020 256KB is the affected one, W29C040 512KB is not)
    - .planning/todos/pending/flash4-page-size-datasheet-sourced-cr01.md (the full CR-01 finding + per-family page-size table — the root-cause pointer the EVIDENCE row carries)
    - .planning/phases/82-electrically-rewritable-silicon-validation/82-PATTERNS.md (the W29C020 pre-attributed FAIL cell shape — exact verdict "FAIL (CR-01)" + cr01_risk wording)
    - firestarter_app/firestarter/data/chip_database.json (W29C020 entry — pinout DIP32_SST39SF040, Flash/EEPROM, 262144 — DB-01 confirm)
  </read_first>
  <action>
    Run W29C020 (size_bytes 262144, algorithm 0x05 flash4, Flash/EEPROM) on the CURRENT firmware AS-IS (D-01 —
    NO firmware change in this phase). Generate image A (seed 1) + B (seed 2) at 262144, record SHAs. Run
    `firestarter dev write-cycle W29C020 /tmp/firestarter_bench_p82/W29C020_img_A.bin --runs 1` then the B pass
    (no explicit erase). If it PASSES (clean A then clean B) record PASS with the A→B auto-erase proof. If it
    FAILS mid-page-poll (the EXPECTED CR-01 mode), reseat + retry N=2 (D-08); if still failing record verdict
    "FAIL (CR-01)" with the anomalies note pre-attributing it to `flash4_page_size(262144) → 128B guessed, real
    256B` and the pointer to flash4-page-size-datasheet-sourced-cr01.md + "RCA/fix deferred to Phase 84 FIX-01",
    and cr01_risk="yes (W29C020 — flash4_page_size heuristic under-sizes page for 256KB, defer to Phase 84)".
    Either way CONTINUE — never halt. Apply the non-vacuous bar only if it PASSES (consistency-check --runs 3 +
    negative-control verify RC=1). Confirm DB-01 decode (DIP32_SST39SF040 / Flash/EEPROM / 12V / 262144) vs
    silicon. Append one EVIDENCE.md row + EVIDENCE.json cell. Do NOT modify firmware or hand-edit the DB.
  </action>
  <verify>
    <human-check>W29C020: attempted as-is; either PASS (A→B clean + non-vacuous proof) or FAIL (CR-01) after N=2 reseat+retry with the pre-attributed root-cause note. EVIDENCE updated; no firmware/DB edit.</human-check>
    <automated>cd /workspaces && python -c "import json; d=json.load(open('.planning/v1.15/bench/EVIDENCE.json')); c=[x for x in d['cells'] if x['chip']=='W29C020' and x.get('op','').startswith('write')]; assert c, 'no W29C020 write cell'; v=c[-1]['verdict']; assert v in ('PASS','FAIL (CR-01)','FAIL (genuine)','ANOMALY'), v; assert v=='PASS' or 'cr01' in (c[-1].get('cr01_risk','')+c[-1].get('anomalies','')).lower()"</automated>
  </verify>
  <acceptance_criteria>
    - W29C020 attempted on current firmware as-is; verdict is one of PASS / FAIL (CR-01) / FAIL (genuine) / ANOMALY after N=2 reseat+retry
    - If FAIL (CR-01): the EVIDENCE row pre-attributes it to the flash4_page_size under-sizing (128B guessed vs 256B real) with the todo pointer + "deferred to Phase 84 FIX-01" and cr01_risk set
    - DB-01 decode confirmed vs silicon (DIP32_SST39SF040 / Flash/EEPROM / 262144); no firmware change, no inline DB edit
  </acceptance_criteria>
  <done>W29C020 attempted as-is; PASS recorded or CR-01 pre-attributed FAIL handed to Phase 84; REWR-04 + DB-01 evidence appended; phase stayed host-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI → Leonardo + Rev 2.0 | wrong board/shield corrupts the PASS oracle (read untrustworthy off Leonardo) |
| flash4 page-write loop → W29C020 page | under-sized page (CR-01) corrupts mid-page-poll writes for the 256KB part |
| write VPP path → 12V flash4 socket | a miscalibrated R1 misreads VPP on the write leg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-09 | Spoofing | board/shield identity | mitigate | Task 1 SAFE-01 gate: operator-stated Rev 2.0 + `controller:`=Leonardo + port re-verify (D-10) |
| T-82-10 | Tampering | W29C020 flash4 page write (CR-01) | accept | Known firmware page-size under-sizing; attempted as-is, FAIL pre-attributed to CR-01 + deferred to Phase 84 (D-01/D-02) — NOT fixed inline (keeps phase host-only) |
| T-82-11 | Repudiation | false-PASS oracle | mitigate | Non-vacuous bar on PASS only (D-14): consistency-check --runs 3 + wrong-file verify RC=1 |
| T-82-12 | Elevation | inline firmware/DB edit | mitigate | Phase is host-only / no-firmware-scope (D-01); chip_database.json never hand-edited; any CR-01 fix lives in Phase 84 dual-repo lockstep |
</threat_model>

<verification>
- W29C040: write-cycle A RC=0 + write-cycle B RC=0 (Flash/EEPROM auto-erase proof) + consistency-check --runs 3 RC=0 + negative-control verify RC=1; OR recorded FAIL/ANOMALY after N=2 reseat+retry
- W29C020: attempted as-is; PASS (A→B clean + non-vacuous) OR FAIL (CR-01) pre-attributed + deferred to Phase 84
- EVIDENCE.{md,json} carries 2 new flash4 rows; W29C020 cell carries the CR-01 root-cause pointer when it fails
- No firmware change; no inline DB edit
</verification>

<success_criteria>
- REWR-04: W29C040 PASS A→B with Flash/EEPROM auto-erase confirmed (SC#3) — or recorded FAIL/ANOMALY
- REWR-04: W29C020 attempted as-is; PASS recorded, or CR-01-attributed FAIL handed to Phase 84 (never halted the sweep)
- DB-01: both flash4 chips' decode confirmed vs silicon; mismatches flagged for Phase 84
- Phase stayed host-only / no-firmware-scope (D-01); all PASS non-vacuous (D-14)
</success_criteria>

<output>
Create `.planning/phases/82-electrically-rewritable-silicon-validation/82-03-SUMMARY.md` when done
</output>
