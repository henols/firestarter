---
phase: 198-the-two-voltage-nibbles
plan: 03
type: execute
wave: 3
depends_on: ["198-02"]
files_modified:
  - firestarter_app/tools/DECODE-NOTES.md
  - .planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md
  - .planning/phases/198-the-two-voltage-nibbles/198-BACKLOG-DRAFT.md
  - .planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md
  - .planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md
autonomous: true
requirements: [VOLT-01, VOLT-03]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "VOLT-01 / D-15: `tools/DECODE-NOTES.md` § 9 states the general finding — the two nibbles and the VPP byte select a programmer rail index, not a chip requirement — and states it as a verdict in its opening line, before any evidence, on the § 8 template."
    - "D-15: § 9's evidence is the upstream comment at `database.c` lines 125-126 at the pinned sha, and the arithmetic that neither 5.5 nor 6.5 V lies inside the 5.75–6.25 V band three vendored Fujitsu datasheets state — those being the only two rails the model has nearby."
    - "D-15 correction applied: § 9 cites lines 125-126, not the 123 the phase context carried. The quoted sentence is exact; only the line number was wrong, and a phase about decode provenance does not propagate a citation it measured as off."
    - "D-15: § 9 carries four named limits in the assertive voice § 6 uses — the datasheet corroboration is n = 3 and all three are Fujitsu; no claim is made about the 167 rows at vdd index 0x4 this phase does not reach, which Phase 200 owns; the finding does not assert that any particular row's value is correct; and the low nibble's option-flag reading is this generator's own, not upstream-attested."
    - "D-16: § 9 records that 25 V and 21 V exist in upstream's VPP table at indices 0xF1 and 0xF2, that no filtered row carries either, that all eight NMOS 2716/2732 rows and `MBM27128` saturate at 0xF0 = 18 V which is the model maximum, and that no test can cover the two new entries until upstream ships such a row."
    - "D-03: § 9 names the surviving silent fallback explicitly, because nothing mechanical reports a future unmapped index and the decision was to keep defaulting rather than stop the build."
    - "D-06: § 9 or the disposition record states the `vdd < vcc` predicate, its exact 28-row selection, and that it is documented rather than shipped — no value hangs off it this phase."
    - "D-13: `198-VOLT03-DISPOSITION.md` enumerates all 16 sub-group-1 rows with no elision, classifies each by part class, states which way the evidence cuts for each, and names what would close it."
    - "D-13: the disposition names the direct in-repo contradiction — the pending todo calls these 16 rows genuinely 3.3 V while Phase 148's own decision record and the shipped source comment call the same 16 genuinely 5 V — and sides with the measurement, quoting the todo accurately including its own mention of AMD second-sourced equivalents."
    - "D-13: the honesty limit is that the repository vendors no Microchip and no AMD datasheet at all, so not one of the 16 rows has a vendored datasheet and every disposition is a part-class inference plus an in-repo measurement, never a datasheet reading."
    - "D-14: the archived `148-DB-DIFF.md` is not edited. Its falsified Non-claim is recorded against by pointing at it, and nothing under `.planning/milestones/` is modified."
    - "D-12: the pending todo is closed by a pure rename into the completed directory, with no frontmatter or content edit, and a successor backlog entry is drafted carrying the residue — 28 rows still reporting 5.5 V, 12 held at an unsourced 5000, closable only at one datasheet per row."
  artifacts:
    - firestarter_app/tools/DECODE-NOTES.md
    - .planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md
    - .planning/phases/198-the-two-voltage-nibbles/198-BACKLOG-DRAFT.md
    - .planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md
  key_links:
    - "§ 7 Sources sits before § 8 in `DECODE-NOTES.md`, so § 8 carries its own trailing sources paragraph rather than extending § 7. § 9 must do the same."
    - "§ 9 was opened by plan `198-01` with a decode-provenance subsection and extended by `198-02`. This plan completes it; the verdict goes at the top, above what is already there."
    - "The shipped comment on the value-keyed 4000-rail rewrite in `build_db.py` already says the alternative keys each drag in sixteen genuinely 5 V EEPROMs and set them to 3.3 V. That is the single strongest piece of evidence available for D-13 and it costs nothing to cite, because both sides of the contradiction are already in the tree."
    - "The disposition's consumers are the successor backlog entry and the future VOLT-F1 requirement, not a named next phase — which is why it lands in the phase directory rather than in `.planning/notes/`, where Phase 199's inherited evidence lives."
    - "A `git mv` run against an uncommitted edit to the same file stages the pre-edit blob and silently produces a zero-diff rename. The 197 executor hit exactly this. Commit any content edit before the rename and check `git diff --stat` rather than trusting the commit summary."
  prohibitions:
    - "No comment line is added to any file under `firestarter_app/`. § 9 is markdown next to the code, which is the only legal home for decode rationale."
    - "Nothing under `.planning/milestones/` is edited, read-only included. The archived record's falsified claim is pointed at, never rewritten."
    - "The executor does not edit `.planning/ROADMAP.md`. The backlog entry is drafted to a file in the phase directory and applied by the orchestrator after it confirms the roadmap is byte-identical to its pre-dispatch snapshot."
    - "The todo's frontmatter is not edited. It already carries the resolving phase number, so the close is a pure rename and nothing else."
    - "§ 9 does not present the low nibble's option-flag reading as upstream-attested. No constant in the upstream header names it; the two named powerdown bits sit at positions 12 and 13 and are scoped to PLD parts the generator's filter excludes."
    - "§ 9 does not assert that any particular row's emitted voltage is correct. It says what the field is, not that any instance of it is right."
    - "No `vdd < vcc` build-time assertion is added to `build_db.py`. D-06 documents the predicate; it ships no value this phase."
    - "The disposition record elides no row. All 16 sub-group-1 rows and all 12 sub-group-2 rows appear in full."
    - "No row's emitted value is changed by this plan. It writes documentation and records only."
    - "No commit lands on `beta` or `main` in any repository."
    - "No canon security or compliance prohibition is authored here. This plan writes markdown and renames a file; RESEARCH § Security Domain measured the phase's only genuine hazard as electrical, and no value moves in this plan."
  flagged_assumptions:
    - "VOLT-03 / unclassified (probe returned `unclassified — review manually`, and it stays unresolved by instruction): the deterministic edge probe could not classify VOLT-03 into a known edge category, and it is surfaced here rather than auto-resolved with a backstop. Reviewed manually: the requirement's own text makes leaving the rows unchanged an acceptable outcome, so there is no boundary, ordering, precision or emptiness condition to bound — the risk is evidentiary, not computational. It is discharged by the per-row disposition and its named honesty limit rather than by an acceptance predicate, and a verifier that cannot confirm this should abstain to human review rather than pass it."
    - "The 15 non-LV sub-group-1 rows are 5 V parts by part-number class convention, not by datasheet reading. No Microchip or AMD datasheet is vendored in this repository. The inference is corroborated independently by Phase 148's own measurement and by the shipped source comment, but it remains an inference and the disposition must label it as one."
    - "`28LV64A` is a genuine low-voltage part, which is why its 3300 vdd is plausible where the other 15 rows' is not; it is also the only one of the 16 carrying a different voltage word. Its `vcc_mv` of 5500 stays implausible either way, and the disposition must say so rather than resolving the row in one direction."
---

<objective>
Write down what the phase found, with its limits named, and close the todo that has been waiting for
it since v1.32.

Three artifacts. `DECODE-NOTES.md` § 9 carries the general finding: the voltage word's two nibbles
and its VPP byte select a programmer rail index, not a chip requirement. That is the claim Phases 199
and 200 will build their warning wording on, so its limits matter as much as its verdict —
corroborated by three datasheets, all from one manufacturer, saying nothing about the 167 rows this
phase does not reach, and asserting nothing about whether any given row is right.

`198-VOLT03-DISPOSITION.md` disposes of the 28 rows individually. Its central finding is a
contradiction already sitting in the repository: the pending todo calls sixteen rows genuinely 3.3 V,
while Phase 148's own decision record and a shipped source comment call the same sixteen genuinely
5 V. Both sides are in the tree; the disposition names them and sides with the measurement.

Then the todo closes — genuinely completed, even though no value moved for its rows — and a successor
backlog entry is drafted carrying the residue.

**Edge-probe accounting for this plan: 1 of the phase's 7 probe-surfaced items.** VOLT-03's probe
returned `unclassified`, stays unresolved by instruction, and is surfaced as a flagged assumption
above rather than auto-resolved. Plans `198-01` and `198-02` carry 5 resolved-explicit items between
them; plan `198-04` carries the remaining `unclassified` item.

Purpose: make the finding reusable by the two phases that depend on it, and leave the 28-row question
answered rather than merely deferred again.
Output: a numbered decode-notes section, a per-row disposition record, a closed todo and a drafted
successor.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/198-the-two-voltage-nibbles/198-CONTEXT.md
@.planning/phases/198-the-two-voltage-nibbles/198-RESEARCH.md
@.planning/phases/198-the-two-voltage-nibbles/198-PATTERNS.md
@.planning/phases/198-the-two-voltage-nibbles/198-02-SUMMARY.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DECODE-NOTES § 9 — the finding, and the four things it does not claim</name>
  <files>firestarter_app/tools/DECODE-NOTES.md</files>
  <precondition>Plans `198-01` and `198-02` are committed, so § 9 exists with its decode-provenance subsection and the measured counts this section cites are final.</precondition>
  <read_first>
    - `firestarter_app/tools/DECODE-NOTES.md` § 8 in full (lines 302-367) — the heading form, the bolded-lead-clause body shape, the inline file-and-line provenance tags, and the trailing sources paragraph.
    - `firestarter_app/tools/DECODE-NOTES.md` § 6 "Honest gaps" (from line 257) — the limits voice: a bolded assertive clause naming the thing and its status, then why, then an explicit cross-reference naming what owns it. Not hedges.
    - `firestarter_app/tools/DECODE-NOTES.md` § 9 as plans `198-01` and `198-02` left it.
    - `.planning/phases/198-the-two-voltage-nibbles/198-RESEARCH.md` § F-1 (the transcribed tables, the verbatim upstream comment with its measured line numbers, and the correction to the phase context's citation), § F-2 (why the low-nibble flag reading has no upstream constant behind it), § F-13 (the slot-by-slot mapping of § 8's shape onto § 9's content, and the three mandatory limits in § 6's voice plus the fourth this research earned).
    - `.planning/phases/198-the-two-voltage-nibbles/198-PATTERNS.md` § `DECODE-NOTES.md` — the heading form and body excerpts verbatim.
    - `.planning/phases/198-the-two-voltage-nibbles/198-CONTEXT.md` D-03, D-06, D-15 and D-16.
  </read_first>
  <action>
    Complete `firestarter_app/tools/DECODE-NOTES.md` § 9, restructuring what plans `198-01` and
    `198-02` deposited there so the section reads in the § 8 slot order rather than in the order the
    pieces arrived.

    The heading takes § 8's form: the section number, the backticked subject, the requirement id
    VOLT-01, the phase number, and a verdict clause inside the heading itself.

    Open with the verdict, before any evidence: the two nibbles and the VPP byte select a programmer
    rail index, not a chip requirement. Then, in § 8's slot order:

    What the code does today — the three decode lookups in `build_db.py`, with an inline
    file-and-line provenance tag, and the corrected upstream attribution the earlier plans relocated
    here.

    The positive confirmation — three vendored Fujitsu datasheets all stating 6.0 V ± 0.25 V where the
    index selects 5.5 V. Frame it as a positive match, not merely an absence of contradiction.

    The falsification of the rival reading, that these are chip requirements — model dependence. The
    same index means 12 V on one model and 12.5 V on another; another index means 9.5 V and 21 V. And
    upstream's own comment, quoted verbatim and cited at `database.c` lines 125-126 at the pinned sha.
    **Cite 125-126, not 123.** The phase context carried 123; research measured the quoted sentence at
    125-126, with 123 being a different line of the same block. The quote is exact and the argument is
    unaffected, but a phase whose subject is decode provenance does not propagate a citation it has
    measured as wrong.

    The arithmetic argument, which is the sharpest single piece of evidence and is arithmetic rather
    than appeal: the datasheet band is 5.75–6.25 V, the model's two nearest rails are 5.5 and 6.5, and
    neither is inside it. A field that cannot represent the datasheet value is not recording the
    datasheet value. Add the saturation pattern as a second instance: 28 rows pin at the VPP index whose
    value is 18 V, the model's maximum, including all eight NMOS 2716/2732 rows and `MBM27128`, which a
    separate community report measured as needing 21 V.

    The edge case and dead branch, on § 8's fail-closed-and-provably-dead precedent, per D-16: upstream's
    VPP table carries 25 V and 21 V at two further indices, which are exactly the two figures the six
    `UNSOURCED` NMOS entries hold; no filtered row carries either; the proof is byte-identical
    regeneration; and no test can cover them until upstream ships such a row. Record this deliberately —
    it is a tempting false lead that would otherwise be rediscovered, and the saturation-at-the-maximum
    pattern is direct evidence for the verdict.

    Per D-03, name the surviving silent fallback here, because nothing mechanical reports it: the
    decode still defaults rather than raising on an unmapped index, that was a deliberate decision, and
    after the table completion no row in the filtered population reaches it today.

    Per D-06, state the `vdd < vcc` predicate, its exact 28-row selection against 373 equal and 366
    greater, and that it is documented rather than shipped — no value hangs off it this phase.

    Then the four limits, in § 6's assertive voice — a bolded clause naming the thing and its status,
    then why, then a cross-reference naming what owns it. The datasheet corroboration is n = 3 and all
    three are Fujitsu; no claim is made about the 167 rows at vdd index 0x4 this phase does not reach,
    which Phase 200 owns one datasheet per row; the finding does not assert that any particular row's
    value is correct; and the low nibble's option-flag reading is this generator's own working reading,
    corroborated by the pairing pattern in the census and by nothing upstream — no header constant names
    it, and the two named powerdown bits sit at positions 12 and 13, scoped to PLD parts the filter
    excludes. Note in passing that this is itself a second, independent instance of the section's own
    claim: the same bits mean different things depending on part family.

    Close with a trailing sources paragraph in § 8's form, naming this phase's research document, the
    disposition record, the regeneration diff, the new override entries and the upstream file with its
    line range and sha. § 7 Sources sits before § 8, so § 9 carries its own rather than extending it.

    Add no comment line to any Python file. This section exists precisely because the project's rules
    put decode rationale in markdown beside the code and nowhere else.

    Commit inside `firestarter_app`, citing D-15 and D-16.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import pathlib
t = pathlib.Path('tools/DECODE-NOTES.md').read_text(encoding='utf-8')
s9 = t.split('## 9.')[1] if '## 9.' in t else ''
assert t.count('## 9.') == 1, t.count('## 9.')
assert s9, 'no section 9 body'
for tok in ('VOLT-01', '198', '125-126', 'a8efaedc', '5.75', '6.25', '0xF1', '0xF2', 'Sources'):
    assert tok in s9, tok
print('SECTION9_OK', len(s9.splitlines()))" | /usr/bin/grep -qE '^SECTION9_OK [0-9]+$'</automated>
    <fails_when>the output does not begin `SECTION9_OK` — the AssertionError names the first required token missing from the section 9 body: the requirement id, the phase number, the corrected upstream line citation, the pinned sha, either end of the datasheet band the arithmetic argument turns on, either dead VPP index, or the trailing sources paragraph. A section-9 heading count other than 1 means the section was duplicated rather than completed</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(python3 -c "
import pathlib
t = pathlib.Path('tools/DECODE-NOTES.md').read_text(encoding='utf-8')
print(len(t.split('## 9.')[1].splitlines()))")" -ge 50</automated>
    <fails_when>section 9's body is shorter than 50 lines — § 8 runs to 66 lines and § 9 carries strictly more content (a verdict, five evidence slots, a dead-branch paragraph, four limits and a sources line), so a shorter section means slots were dropped</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import pathlib
t = pathlib.Path('tools/DECODE-NOTES.md').read_text(encoding='utf-8')
s9 = t.split('## 9.')[1]
assert 'n = 3' in s9 or 'n=3' in s9, 'limit 1 (n = 3, all Fujitsu) not stated'
assert '167' in s9, 'limit 2 (the 167 unreached rows) not stated'
assert '200' in s9, 'limit 2 cross-reference to the owning phase not stated'
print('LIMITS_OK')" | /usr/bin/grep -qx 'LIMITS_OK'</automated>
    <fails_when>the output is not `LIMITS_OK` — the AssertionError names which mandatory limit is absent. All four limits are required by D-15; the two with stable numeric tokens are checked mechanically and the other two are checked in review</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff --cached -- '*.py') && OFFENDERS=$(printf '%s\n' "$D" | /usr/bin/grep -E '^\+\s*#' | /usr/bin/grep -v '^\+\s*#!'); printf '%s' "$OFFENDERS"; test -z "$OFFENDERS"</automated>
    <fails_when>non-zero exit, with the offending lines printed immediately above it — a comment line was added to a staged Python file. The pathspec is load-bearing: without it the pattern also matches the markdown this task writes, and reports a file the rule does not govern</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff --quiet -- tools/build_db.py firestarter/data/chip_database.json && python3 -c "print('NO_CODE_DRIFT')" | /usr/bin/grep -qx 'NO_CODE_DRIFT'</automated>
    <fails_when>non-zero exit before the marker prints — the generator source or the generated database moved in a task that writes documentation only</fails_when>
  </verify>
  <acceptance_criteria>
    - `tools/DECODE-NOTES.md` carries exactly one section-9 heading, in § 8's form, naming VOLT-01 and the phase number and carrying a verdict clause.
    - Section 9's body is at least 50 lines and opens with the verdict before any evidence.
    - It cites the upstream comment at lines 125-126 at the pinned sha, not at 123.
    - It carries the 5.75–6.25 arithmetic argument against the two nearest rails.
    - It records the two dead VPP indices, that no filtered row carries either, and that no test can cover them.
    - It names the surviving silent fallback and the documented-not-shipped status of the `vdd < vcc` predicate.
    - It carries all four limits, including the n = 3 Fujitsu-only corroboration and the cross-reference naming the phase that owns the 167 unreached rows.
    - It ends with its own sources paragraph rather than extending § 7.
    - No Python file gained a comment line, and neither `tools/build_db.py` nor the generated database moved.
  </acceptance_criteria>
  <done>
    The finding two later phases will quote is written down next to the code it describes, with a
    verdict a reader can act on and four limits a reader cannot mistake for hedging.
  </done>
  <reversibility rating="costly">D-15 is rated costly by the operator: Phases 199 and 200 will build their warning wording on this framing, and it reframes every voltage in the generated database as a programmer setting rather than a chip requirement. Reverting the section later would not un-inform the phases that consumed it, which is why the limits are mandatory rather than optional.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The 28-row disposition, and the contradiction already in the repository</name>
  <files>.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md</files>
  <read_first>
    - `.planning/notes/197-at28c-guard-evidence-for-phase-199.md` in full — the section order this record copies: reproducible search method, the full row table, per-fact disposition, the honesty limit.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md` — the closest sibling by subject, for its census-figures and per-row-disposition shape.
    - `.planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md` in full — quote its premise accurately, including its own mention of AMD second-sourced equivalents, rather than paraphrasing it narrower than it is.
    - `.planning/phases/198-the-two-voltage-nibbles/198-RESEARCH.md` § F-4 — the 16-row table with voltage words and upstream protocol ids, the per-class evidence direction, the in-repo contradiction with its three citations, the measured datasheet inventory, and the 12-row sub-group-2 table for contrast.
    - `firestarter_app/tools/build_db.py` — the comment block on the value-keyed 4000-rail rewrite, which is the shipped half of the contradiction. Read it; do not edit it.
    - `.planning/phases/198-the-two-voltage-nibbles/198-CONTEXT.md` D-06, D-11, D-13 and D-14.
  </read_first>
  <action>
    Write `.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md` on the shared
    evidence-record shape: a reproducible method first, then the full census with no elision, then a
    per-row disposition each carrying a reason, then a named honesty limit.

    The reproducible method is a runnable command or script, not a description. Give the exact
    filter — the generator's own DIP-parallel predicate — and the exact comparison that selects the
    group, so a reader can re-derive the 28 rows without reconstructing the method from prose.

    State the group's key, per D-06: decoded vdd strictly below decoded vcc selects exactly 28 rows and
    nothing else, against 373 equal and 366 greater across the filtered population, and that partition
    is exact. A program rail below the read rail is internally contradictory, which is why the key is
    value-keyed and part-name-free rather than tuned. Note that Phase 148 measured and rejected the
    inverse relation, which swept 167 UV EPROMs, and that this is its complement and does not inherit
    that defect.

    Enumerate all 16 sub-group-1 rows with no elision: manufacturer, the name as it appears upstream,
    the raw voltage word, the upstream protocol id, the emitted part number and the emitted support
    status. Record that the manufacturer string is literally the one upstream uses, with its space and
    its lowercase second word, because any override key or test literal must match it verbatim. Record
    that fifteen of the sixteen carry one voltage word and only the low-voltage part carries a
    different one — the low bit set, the same bit that distinguishes the paired VPP indices in the
    census. That is a measured signal and it cuts for the low-voltage reading of that one row.

    Then dispose of each row with a reason. Group by class rather than repeating one sentence sixteen
    times: the fifteen 28C-family parts are 5 V by part-number convention, so substituting vdd there
    would set 5 V EEPROMs to 3.3 V — the exact outcome Phase 148 measured and rejected for all three of
    its alternative keys; the single low-voltage part is plausibly a genuine 3.3 V part, and its own
    5.5 V read rail stays implausible either way, which the record must say rather than resolving the
    row in one direction. Label the class reading as an inference, not a datasheet reading.

    Name the contradiction explicitly, because it is the strongest evidence available and both sides
    are already in the tree: the pending todo characterises these rows as genuinely 3.3 V, while Phase
    148's own discussion log, its plan's threat model and the comment shipped on the value-keyed rewrite
    in `build_db.py` all call the same sixteen genuinely 5 V. Cite all three. State that these are
    provably the same sixteen rows, since exactly sixteen rows carry that vcc and vdd pair and they are
    the only rows a type- or algorithm-keyed rule could move.

    Include the 12 sub-group-2 rows in full for contrast, with their voltage words, and record that
    their emitted 5000 was falsified by this phase: it was never a decode, it was an unmapped-index
    fallback, and it is now held by twelve explicit signed entries.

    Record that the archived Phase 148 diff record states the opposite — that those rows already record
    5.0 V — and that this phase falsifies it. Point at the archived text by path and section; do not
    edit it. Archived records are historical-by-intent and rewriting one destroys the evidence of what
    was believed at the time.

    Per D-11, state the outcome plainly: all 28 rows ship unchanged, which the requirement explicitly
    permits, and changing them on an unproven assumption is what it forbids.

    The honesty limit is measured and mandatory: the repository vendors thirteen datasheet PDFs and not
    one of them is a Microchip or an AMD part. Not one of the sixteen rows has a vendored datasheet, so
    every disposition here is a part-class inference plus an in-repo measurement, never a datasheet
    reading. State it in those terms and do not soften it.

    Commit in the meta repository.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md && for h in 'method' 'disposition' 'Honesty limit'; do /usr/bin/grep -qi "$h" .planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md || { echo "MISSING: $h"; exit 1; }; done; echo SECTIONS_OK</automated>
    <fails_when>the output is not `SECTIONS_OK`, or a `MISSING:` line names the absent heading — an evidence record without a reproducible method, a per-row disposition and a named honesty limit does not match the shape every sibling record in this project follows</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md').read_text(encoding='utf-8')
parts = ['AM28C16A','AM28C17A','2804','2816','2817','28C04A','28C04AF','28C16A','28C16AF','28C17A','28C17AF','28C256','28C64A','28C64AF','28C64B','28LV64A']
missing = [p for p in parts if p not in t]
assert not missing, missing
twelve = ['XL2804A','XL2816A','XLE2865A','XLE28C16B','XLE28C256','XLE28C64A','XLE28C64B','M28C64','M28C64-xxW','M28LV64']
m2 = [p for p in twelve if p not in t]
assert not m2, m2
for tok in ('MICROCHIP memory','0x1401','148-DB-DIFF','28','373','366'):
    assert tok in t, tok
print('ROWS_OK')" | /usr/bin/grep -qx 'ROWS_OK'</automated>
    <fails_when>the output is not `ROWS_OK` — the AssertionError lists every sub-group-1 part number absent from the record, then every sub-group-2 part number absent, then the first missing required token: the verbatim upstream manufacturer string, the distinct voltage word carried by the one low-voltage row, the archived record this phase falsifies, or the three predicate bucket counts. Any non-empty list is an elided row, which the no-elision discipline forbids</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md').read_text(encoding='utf-8')
n = sum(1 for l in t.splitlines() if l.lstrip().startswith('|'))
print('TABLE_LINES', n)" | /usr/bin/grep -qE '^TABLE_LINES (3[2-9]|[4-9][0-9]|[0-9]{3,})$'</automated>
    <fails_when>fewer than 32 table lines are present — 16 sub-group-1 rows and 12 sub-group-2 rows plus a header and separator each is 32, so a lower count means a table was summarised instead of enumerated</fails_when>
    <automated>cd /workspaces && S=$(git status --porcelain -- .planning/milestones/) && printf '%s' "$S" && test -z "$S"</automated>
    <fails_when>the command prints any porcelain line before exiting non-zero — a file under the archived milestones directory was modified, which D-14 forbids</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md').read_text(encoding='utf-8').lower()
for tok in ('microchip', 'amd', 'vendor'):
    assert tok in t, tok
assert 'inference' in t or 'infer' in t, 'the class reading is not labelled as an inference'
print('LIMIT_OK')" | /usr/bin/grep -qx 'LIMIT_OK'</automated>
    <fails_when>the output is not `LIMIT_OK` — the AssertionError names the missing element of the mandatory honesty limit: the two manufacturers with no vendored datasheet, the vendoring status itself, or the explicit labelling of the part-class reading as an inference rather than a datasheet reading</fails_when>
  </verify>
  <acceptance_criteria>
    - `198-VOLT03-DISPOSITION.md` exists with a reproducible method, a full census, a per-row disposition and a named honesty limit.
    - All 16 sub-group-1 part numbers and all 12 sub-group-2 part numbers appear; at least 32 table lines are present.
    - The record carries the verbatim upstream manufacturer string, the distinct voltage word on the one low-voltage row, and the three predicate bucket counts.
    - The in-repo contradiction is named with all three citations, and the record states that these are provably the same sixteen rows.
    - The archived Phase 148 record is pointed at by path and section, and nothing under `.planning/milestones/` is modified.
    - The honesty limit states that no Microchip and no AMD datasheet is vendored, that not one of the 16 has one, and that every disposition is an inference plus a measurement.
    - The class reading is explicitly labelled an inference.
  </acceptance_criteria>
  <done>
    Every one of the 28 rows has a written disposition with a reason, the strongest available evidence
    is a contradiction the repository was already carrying, and the record says plainly what it could
    not establish and why.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the todo, draft its successor, and write nothing to the roadmap</name>
  <files>.planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md, .planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md, .planning/phases/198-the-two-voltage-nibbles/198-BACKLOG-DRAFT.md</files>
  <precondition>Tasks 1 and 2 of this plan are committed. A `git mv` run while an edit to the same file is uncommitted stages the pre-edit blob and silently produces a zero-diff rename.</precondition>
  <read_first>
    - `.planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md` — its frontmatter already carries the resolving phase number, so nothing in it needs changing.
    - `.planning/ROADMAP.md` around the existing backlog entry about six `UNSOURCED` override entries — the closest analogue by subject, for its heading suffix form and its four bolded lead-ins.
    - `.planning/phases/198-the-two-voltage-nibbles/198-RESEARCH.md` § F-10 — the pure-rename precedent with its measured similarity index, the uncommitted-edit hazard, and the entry shape with its required content.
    - `.planning/phases/198-the-two-voltage-nibbles/198-PATTERNS.md` § "ROADMAP backlog entry" — the heading suffix form and the writer-discipline note.
    - `.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md` as Task 2 left it — the successor entry's residue figures come from there, not from a re-derivation.
  </read_first>
  <action>
    Close the pending todo with a plain `git mv` from the pending directory to the completed directory,
    changing nothing in the file. Its frontmatter already names this phase as its resolver, and the
    shipped precedent for this close is a pure rename at full similarity with no content change and no
    workflow verb involved. Commit any content edit before running the rename, and check the staged
    diff rather than trusting the commit summary — a rename run over an uncommitted edit stages the
    pre-edit blob and produces a rename that silently carries none of the change.

    The todo asked what the nibbles encode. This phase answers it, so it is genuinely completed even
    though no value moved for its 28 rows, and even though its central premise was falsified twice
    over — its sub-group-2 rows do not already record 5.0 V, they fell back to it, and its sub-group-1
    rows are not genuinely 3.3 V parts by this project's own earlier measurement.

    Draft the successor backlog entry into
    `.planning/phases/198-the-two-voltage-nibbles/198-BACKLOG-DRAFT.md`. Number it as the next free
    backlog phase number after the highest currently in the roadmap. Use the shipped heading form —
    the number, a one-line finding, and the parenthetical naming it as backlog with the filing date,
    the milestone and phase it was filed during, and the artifact it came from — followed by the four
    bolded lead-ins: the goal, the measured entries or facts with their counts, what would close each,
    and the honest consequence of leaving them.

    The entry carries the residue this phase deliberately leaves: 28 rows still reporting 5.5 V; 12 of
    them held at an unsourced 5000 by explicit override entries; each closable only at one datasheet per
    row; and the measured fact that no Microchip and no AMD datasheet is vendored in this repository, so
    16 of the 28 have no route to closure without new PDFs. Take the counts from the disposition record
    rather than re-deriving them.

    **Do not edit `.planning/ROADMAP.md`.** The orchestrator is the single writer for that file and
    applies the entry after confirming the file is byte-identical to its pre-dispatch snapshot. Report,
    in the plan summary, the three verification legs the orchestrator should re-run after applying:
    that the entry is present, that the count of backlog phase headings rose by exactly one with none
    lost, and that every required token from the draft is found in the applied text.

    Commit in the meta repository.
  </action>
  <verify>
    <automated>cd /workspaces && test ! -e .planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md && test -f .planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md</automated>
    <fails_when>non-zero exit — the todo is still present in the pending directory, or absent from the completed directory, meaning the rename did not land or landed to the wrong path</fails_when>
    <automated>cd /workspaces && L=$(git log --diff-filter=R --name-status -5 -- .planning/todos/) && printf '%s\n' "$L" | /usr/bin/grep -q 'vcc-5500-high-margin-verify-rail-group'</automated>
    <fails_when>the grep finds nothing — git did not record the move as a rename in any of the last five rename-touching commits on that path, which means the file was deleted and re-added rather than moved, losing the similarity link the precedent relies on</fails_when>
    <automated>cd /workspaces && S=$(git show --stat HEAD -- .planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md) && printf '%s\n' "$S" | /usr/bin/grep -qE 'vcc-5500-high-margin-verify-rail-group'</automated>
    <fails_when>the grep finds nothing — the most recent commit does not touch the renamed path, so the rename was not committed. Inspect `git diff --stat` rather than trusting the commit message, which is the specific way the 197 executor's zero-diff rename hid itself</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
p = pathlib.Path('.planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md')
t = p.read_text(encoding='utf-8')
assert 'resolves_phase: 198' in t, 'frontmatter resolver line missing or edited'
print('TODO_OK')" | /usr/bin/grep -qx 'TODO_OK'</automated>
    <fails_when>the output is not `TODO_OK` — the resolving-phase line is absent from the moved file, meaning the frontmatter was edited during a close the precedent requires be a pure rename</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-BACKLOG-DRAFT.md').read_text(encoding='utf-8')
assert t.count('### Phase 999.') == 1, t.count('### Phase 999.')
for tok in ('BACKLOG', '198', 'Goal', '28', '12', 'close'):
    assert tok in t, tok
assert 'Microchip' in t or 'MICROCHIP' in t, 'the no-vendored-datasheet residue is not named'
print('DRAFT_OK')" | /usr/bin/grep -qx 'DRAFT_OK'</automated>
    <fails_when>the output is not `DRAFT_OK` — the AssertionError names the failing leg: a backlog heading count other than exactly one, a missing required token from the heading suffix or the four lead-ins, the residue counts, or the manufacturer whose absent datasheets are the reason 16 of the 28 rows have no route to closure</fails_when>
    <automated>cd /workspaces && git diff --quiet HEAD -- .planning/ROADMAP.md && S=$(git status --porcelain -- .planning/ROADMAP.md) && printf '%s' "$S" && test -z "$S"</automated>
    <fails_when>non-zero exit, or a porcelain line is printed — the roadmap was modified by this task. The executor is not its writer; the orchestrator applies the entry after confirming the file is byte-identical to its pre-dispatch snapshot</fails_when>
    <automated>cd /workspaces && S=$(git status --porcelain -- .planning/milestones/) && printf '%s' "$S" && test -z "$S"</automated>
    <fails_when>a porcelain line is printed before the non-zero exit — a file under the archived milestones directory was modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The todo file is absent from the pending directory and present in the completed directory.
    - Git records the move as a rename, and the committing commit's stat output names the renamed path.
    - The moved file still carries its original resolving-phase frontmatter line, unedited.
    - `198-BACKLOG-DRAFT.md` exists and holds exactly one backlog phase heading in the shipped suffix form, with the four bolded lead-ins.
    - The draft names the 28-row and 12-row residue counts and the absent-datasheet limit that blocks 16 of them.
    - `.planning/ROADMAP.md` is byte-unchanged and has no uncommitted modification.
    - Nothing under `.planning/milestones/` is modified.
  </acceptance_criteria>
  <done>
    The todo that has blocked this group since v1.32 is closed as answered rather than deferred, its
    residue is written down where the next reader will find it, and the file only one writer is allowed
    to touch was not touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's measurements → § 9's claims | Phases 199 and 200 will build operator-facing warning wording on what § 9 asserts |
| inference → recorded disposition | A part-class reading with no vendored datasheet is recorded as evidence for 16 rows |
| executor → `.planning/ROADMAP.md` | A single-writer file that workflow verbs reformat wholesale and one verb is known to clobber |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-14 | Repudiation | a false provenance claim on a provenance phase | high | mitigate | § 9 cites the corrected upstream line range measured this phase, and states the low-nibble flag reading as this generator's own rather than upstream-attested; both are asserted by token |
| T-198-15 | Repudiation | an inference presented as a datasheet reading | high | mitigate | The disposition's honesty limit names the two manufacturers with no vendored datasheet and labels the class reading an inference; both are asserted mechanically |
| T-198-16 | Information Disclosure | an evidence record that elides rows | medium | mitigate | Every one of the 28 part numbers is asserted present, and the table-line floor is asserted at 32 |
| T-198-17 | Tampering | an executor writing the single-writer roadmap | high | mitigate | The entry is drafted to a phase-directory file; the roadmap is asserted byte-unchanged with no uncommitted modification |
| T-198-18 | Tampering | a rename that silently carries no change | medium | mitigate | The rename is asserted through git's own rename detection and through the committing commit's stat output, not through the commit message |
| T-198-19 | Tampering | rewriting an archived historical record | medium | mitigate | A porcelain check on the archived milestones directory runs in both record-writing tasks |
| T-198-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package |
</threat_model>

## Artifacts this phase produces (plan 198-03)

**New files**
- `.planning/phases/198-the-two-voltage-nibbles/198-VOLT03-DISPOSITION.md`
- `.planning/phases/198-the-two-voltage-nibbles/198-BACKLOG-DRAFT.md`
- `.planning/todos/completed/vcc-5500-high-margin-verify-rail-group.md` (by rename)

**Completed documentation**
- `firestarter_app/tools/DECODE-NOTES.md` § 9 — verdict, five evidence slots, the dead-branch paragraph, the surviving-fallback note, the documented predicate, four named limits and its own sources paragraph

**Removed files**
- `.planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md` (by rename)

**No new code symbols.** This plan writes markdown and renames one file. It creates no function, constant, dict key, test or override entry.

<verification>
- `tools/DECODE-NOTES.md` carries exactly one section-9 heading, at least 50 body lines, the corrected upstream line citation, the pinned sha, the arithmetic band, both dead VPP indices and its own sources paragraph.
- Section 9 states the n = 3 Fujitsu-only limit and cross-references the phase owning the 167 unreached rows.
- Neither `tools/build_db.py` nor the generated database moves in this plan.
- `198-VOLT03-DISPOSITION.md` names all 28 part numbers, holds at least 32 table lines, carries the three predicate bucket counts, points at the archived record without editing it, and states the no-vendored-datasheet honesty limit with the class reading labelled an inference.
- The todo is absent from pending and present in completed, git records it as a rename, and its frontmatter is unedited.
- `198-BACKLOG-DRAFT.md` holds exactly one backlog heading with the four lead-ins and the residue counts.
- `.planning/ROADMAP.md` is byte-unchanged with no uncommitted modification.
- Nothing under `.planning/milestones/` is modified.
- No Python file gains a comment line.
</verification>

<success_criteria>
VOLT-01 is written down where the code lives, as a verdict with four limits rather than an
observation. VOLT-03 is answered the way the requirement permits: every one of the 28 rows disposed
with a reason, nothing changed on an unproven assumption, and the blocking todo closed as genuinely
completed with its residue filed rather than forgotten.
</success_criteria>

<output>
Create `.planning/phases/198-the-two-voltage-nibbles/198-03-SUMMARY.md` when done. Record § 9's final
body line count, the disposition record's table-line count, the backlog phase number chosen and why it
was the next free one, the git rename similarity index as reported for the todo close, and the three
verification legs the orchestrator should re-run after applying the backlog entry to the roadmap.
</output>
