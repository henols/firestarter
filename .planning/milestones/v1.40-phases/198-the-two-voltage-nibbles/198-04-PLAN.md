---
phase: 198-the-two-voltage-nibbles
plan: 04
type: execute
wave: 2
depends_on: ["198-01"]
files_modified:
  - .planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md
  - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md
autonomous: true
requirements: [VOLT-04]

estimate:
  tokens: 22000
  raw_tokens: 44000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-17: `198-GH66-ANSWER.md` exists as a committed draft with the five-section shape the shipped precedent uses — a header carrying an internal-only warning, a status section, an internal-provenance section, a comment body, and a held-pending deferral section."
    - "D-17: nothing is posted. The issue is not commented on, not labelled, not closed, and no branch is pushed. The status line says so in its opening all-caps verdict."
    - "D-17: the comment body claims corrections only — the two corrected VPP values and the confirmed 6.0 V programming-supply finding — under an explicit heading stating that this does not close the report."
    - "D-17: the comment body says nothing new about causation. The reporter already demonstrated that a correct VPP alone does not fix the failure, and the maintainer's address-pattern hypothesis is already posted and is not this phase's to advance."
    - "D-17: the reporter is credited by name for supplying the datasheet the corrections rest on."
    - "D-17: the comment body carries no project-internal provenance — no plan id, phase number, requirement id or decision id appears in the text intended for posting."
    - "D-17: VOLT-04 is left Pending in `.planning/REQUIREMENTS.md`. No task in this phase flips it, and the deferral record says why."
    - "D-18: `197-GH70-ANSWER.md`'s held-pending deferral section names both issues, so whoever closes the milestone finds one list with the same steps for each rather than two held drafts in two directories to discover independently."
    - "D-18 scope boundary: editing `197-GH70-ANSWER.md` is legitimate. The do-not-edit rule covers archived records under the milestones directory, not the current milestone's live phase directories."
    - "The corrections the draft states match the shipped database exactly — both named rows emit 12500 — so the draft cannot claim a value the artifact does not carry."
  artifacts:
    - .planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md
    - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md
  key_links:
    - "The publication route is already decided as `hold`, carried forward from the operator's ruling on the previous issue without re-asking. The branch has no upstream and the milestone is unpushed, so no version or commit a reader could resolve exists yet — every route other than holding would name something unfindable."
    - "The maintainer's own cross-check on this issue already published 12.5 V and 6.0 V as confirmed mismatches, so this phase's corrections agree with what is already posted and no retraction is needed — unlike the previous issue's pinout finding."
    - "The firmware guard-band consequence is deliberately absent from the public body. It is recorded in the regeneration diff record instead, because the public answer stays strictly to database values that changed."
    - "The four steps in the deferral section are the operational contract for the milestone close. Consolidating both issues into one list is the whole point of the edit; splitting them would reproduce the discovery problem it exists to solve."
  prohibitions:
    - "Nothing is posted to the issue. No `gh issue comment`, no label change, no close, no reaction."
    - "Nothing is pushed. No branch in any repository gains an upstream or is pushed in this plan."
    - "No commit lands on `beta` or `main` in any repository."
    - "No version number, release tag or commit sha is stated in the comment body as though a reader could look it up. The version line stays a placeholder until the beta cut fills it."
    - "No project-internal provenance appears in the comment body — no plan id, phase number, requirement id, decision id or planning-directory path."
    - "The comment body makes no new causation claim and advances no hypothesis. It reports values that changed."
    - "The firmware guard-band consequence is not mentioned in the comment body."
    - "VOLT-04 is not marked complete, and `.planning/REQUIREMENTS.md` is not edited by this plan."
    - "No file under `.planning/milestones/` is edited."
    - "No source file, generated artifact or test under `firestarter_app/` is touched by this plan."
    - "No canon security or compliance prohibition is authored here. The only outward-facing surface this plan creates is a held draft that is asserted not to have been published."
  flagged_assumptions:
    - "VOLT-04 / unclassified (probe returned `unclassified — review manually`, and it stays unresolved by instruction): the deterministic edge probe could not classify VOLT-04 into a known edge category, and it is surfaced here rather than auto-resolved with a backstop. Reviewed manually: the requirement is a publication act, not a computation, so it has no adjacency, boundary, ordering, precision or emptiness condition. Its only edge is temporal — the version a reader can resolve does not exist until the beta cut — and that edge is handled by holding rather than by a predicate. A verifier that cannot confirm the requirement should record it as deliberately Pending rather than pass or fail it."
    - "The draft's factual claims are asserted against the shipped database at the moment it is written. If a later phase in this milestone moves either value again before the beta cut, the draft's numbers go stale silently; the deferral section's re-check step is the only thing that catches it, and it is a human step."
---

<objective>
Write the answer the community report is owed, and do not post it.

The reporter supplied the datasheet this phase's corrections rest on, and the maintainer already
published on that issue that the programming voltage and the programming supply were both mismatched.
This phase corrected both. The honest thing to say is exactly that, plus the explicit statement that
correcting them does not close the report — the reporter has already demonstrated that a correct
programming voltage alone does not fix the failure.

The draft is held, not posted, and the route was settled by the operator one phase ago on a fact that
has not changed: the milestone branch has no upstream in any repository, so there is no version and
no commit a reader could resolve. Every publication route other than holding would name something
unfindable. VOLT-04 therefore stays Pending at phase end, which is the accurate accounting rather than
the tidy one.

The second half of this plan is a filing problem. There are now two held drafts in two phase
directories. Whoever closes this milestone must find both, and will not think to look for the second.
Consolidating them into the one deferral list that already exists is what prevents that.

**Edge-probe accounting for this plan: 1 of the phase's 7 probe-surfaced items.** VOLT-04's probe
returned `unclassified`, stays unresolved by instruction, and is surfaced as a flagged assumption
above rather than auto-resolved. Plans `198-01` and `198-02` carry 5 resolved-explicit items between
them; plan `198-03` carries the other `unclassified` item.

Purpose: have the answer ready and true at the instant it can be posted, and make sure it is found.
Output: a held draft with a comment body that can be posted verbatim, and one consolidated list naming
both issues.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/198-the-two-voltage-nibbles/198-CONTEXT.md
@.planning/phases/198-the-two-voltage-nibbles/198-RESEARCH.md
@.planning/phases/198-the-two-voltage-nibbles/198-PATTERNS.md
@.planning/phases/198-the-two-voltage-nibbles/198-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The held draft — corrections only, credited, and not posted</name>
  <files>.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md</files>
  <precondition>Plan `198-01` is committed, so both corrected values are in the shipped database and the draft's numbers can be asserted against the artifact rather than against an intention.</precondition>
  <precondition>The milestone branch has no upstream in either repository, which is the fact the hold rests on. If that has changed, the route decision must be re-put to the operator rather than assumed.</precondition>
  <read_first>
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md` in full — the five-section structure, the bolded internal-only warning immediately under the title, the all-caps status verdict, the numbered operator decisions, and what the comment body does and does not contain.
    - `.planning/phases/198-the-two-voltage-nibbles/198-RESEARCH.md` § F-11 — the issue's live state, its existing labels, its comment history, and the five-section template with its four numbered release steps.
    - `.planning/phases/198-the-two-voltage-nibbles/198-CONTEXT.md` D-10 and D-17, and the canonical-references entry describing what the maintainer has already published on that issue.
    - `.planning/phases/198-the-two-voltage-nibbles/198-01-SUMMARY.md` — the measured emitted values, so the draft states what shipped rather than what was planned.
    - `firestarter_app/firestarter/data/chip_database.json` — the two corrected rows, read directly.
  </read_first>
  <action>
    Write `.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md` on the shipped five-section
    shape: a title, then immediately a bolded internal-only warning stating that only the comment-body
    section is intended for posting and that the rest must never be pasted onto the issue; then a status
    section, an internal-provenance section explicitly marked as project bookkeeping not for posting, the
    comment body, and a held-pending deferral section also marked internal.

    The status section opens with a single all-caps verdict line recording that the draft is approved,
    held pending the beta cut, and not posted. Then the operator decision, dated, carried forward from
    the ruling on the previous issue without re-asking: the publication route is to hold, because
    nothing in this milestone is pushed. Record the checkable fact the hold rests on — that the
    milestone branch has no upstream and that a remote-branch containment query on its tip resolves
    nothing in either repository — so a later reader can re-verify the reasoning rather than taking it.

    The internal-provenance section carries the project bookkeeping: which plans produced the values,
    which override entries hold them, which datasheet paths back them, and the measured regeneration
    counts. None of this appears in the comment body.

    The comment body is the only text intended for posting, delimited so it can be extracted verbatim.
    It states two things and stops. First, the programming voltage for both the 4 Mbit part the report
    is about and its 1 Mbit sibling now targets 12.5 V rather than 12.0 V, matching the datasheet's
    12.5 V with its stated tolerance. Second, the programming supply figure those parts carry is now
    6.0 V, confirming the datasheet reading rather than the 5.5 V the decode selected. Put both under
    an explicit heading stating that this correction does not close the report. Credit the reporter by
    name for supplying the datasheet, and cite it the way the previous draft's approved amendment does —
    by page and table on the PDF the reporter attached to the issue themselves, not by a path in this
    project's repository.

    Say nothing new about causation. The reporter already retested at a corrected voltage and saw an
    identical failure at the same address, which settles that the voltage alone is not the fault; and
    the maintainer's address-pattern hypothesis is already posted on the issue and is not this phase's
    to advance. Do not mention the firmware guard band — per D-10 that consequence lives in the
    regeneration diff record, and the public answer stays strictly to database values that changed.

    Leave a version placeholder line in the comment body rather than a number. No version, tag or commit
    a reader could look up exists yet; naming one would be the exact over-claim the hold exists to
    prevent. Carry no plan id, phase number, requirement id, decision id or planning-directory path into
    the comment body.

    The held-pending deferral section carries five bolded bullets in the shipped order — what is held,
    why, what releases the hold, exactly what to do at that point as a numbered four-step list, and the
    requirement status. The four steps are: replace the version placeholder with the real published
    version; re-run the no-over-claim and no-attribution checks against the final comment-body text
    only; post only the comment body, verbatim, to the issue by body-file, never pasting the header,
    status, internal-provenance or deferral sections; and record the returned comment URL back into
    this file and mark the requirement complete.

    State in the requirement-status bullet that VOLT-04 is not satisfied by this plan and is carried
    forward to the milestone close. Do not edit `.planning/REQUIREMENTS.md`.

    Post nothing. Push nothing. Label nothing.

    Commit in the meta repository.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md').read_text(encoding='utf-8')
for h in ('## Status', '## Internal provenance', '## Comment Body', '## Held-pending deferral'):
    assert h in t, h
assert t.count('## Comment Body') == 1
print('SECTIONS_OK')" | /usr/bin/grep -qx 'SECTIONS_OK'</automated>
    <fails_when>the output is not `SECTIONS_OK` — the AssertionError names the first missing section heading, or reports a duplicated comment-body heading which would make the extract-verbatim release step ambiguous</fails_when>
    <automated>cd /workspaces && python3 -c "
import re, pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md').read_text(encoding='utf-8')
start = t.index('## Comment Body')
end = t.index('## Held-pending deferral')
body = t[start:end]
bad = []
for pat in (r'\b19[0-9]-[0-9]{2}\b', r'\bVOLT-0[1-4]\b', r'\bD-[0-9]{2}\b', r'\bPhase 19[0-9]\b', r'\.planning/', r'PULSE-0[1-4]'):
    if re.search(pat, body):
        bad.append(pat)
assert not bad, bad
print('BODY_CLEAN')" | /usr/bin/grep -qx 'BODY_CLEAN'</automated>
    <fails_when>the output is not `BODY_CLEAN` — the AssertionError lists every internal-provenance pattern found inside the comment body: a plan id, a requirement id, a decision id, a phase reference, a planning-directory path or a sibling requirement family. Any hit means project bookkeeping would be pasted onto a public issue</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md').read_text(encoding='utf-8')
start = t.index('## Comment Body'); end = t.index('## Held-pending deferral')
body = t[start:end]
for tok in ('12.5', '6.0', 'dim20'):
    assert tok in body, tok
for forbidden in ('11875', '13000', '11400'):
    assert forbidden not in body, forbidden
print('BODY_CONTENT_OK')" | /usr/bin/grep -qx 'BODY_CONTENT_OK'</automated>
    <fails_when>the output is not `BODY_CONTENT_OK` — the AssertionError names either a required element absent from the comment body (the corrected programming voltage, the confirmed programming supply, or the reporter's credit) or a forbidden one present (any of the three firmware settable-band figures, which D-10 keeps out of the public answer)</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -c "
import json
d = json.load(open('firestarter/data/chip_database.json'))
got = {r['part_number']: r['electrical']['vpp_mv'] for r in d['FUJITSU']}
assert got.get('MBM27C4001') == 12500, got.get('MBM27C4001')
assert got.get('MBM27C1001') == 12500, got.get('MBM27C1001')
print('CLAIM_MATCHES_ARTIFACT')" | /usr/bin/grep -qx 'CLAIM_MATCHES_ARTIFACT'</automated>
    <fails_when>the output is not `CLAIM_MATCHES_ARTIFACT` — the AssertionError prints the emitted value that is not 12500, which means the draft would state a number the shipped database does not carry</fails_when>
    <automated>cd /workspaces && XDG_CACHE_HOME=/tmp/fs-xdg gh issue view 66 --repo henols/firestarter --json state,comments --jq '.comments | length' 2>/dev/null | /usr/bin/grep -qx '6'</automated>
    <fails_when>the printed comment count is not 6 — a seventh comment means something was posted to the issue, which this plan forbids absolutely. If the command errors instead of printing a number, re-run it with a writable cache directory before treating the result as a failure</fails_when>
    <automated>cd /workspaces && S=$(git status --porcelain -- .planning/REQUIREMENTS.md .planning/milestones/) && printf '%s' "$S" && test -z "$S" && git diff --quiet HEAD -- .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, or a porcelain line is printed — the requirements file or an archived milestone record was modified. VOLT-04 stays Pending by decision, and flipping it here would record a publication that has not happened</fails_when>
    <automated>cd /workspaces/firestarter_app && S=$(git status --porcelain) && printf '%s' "$S" && test -z "$S"</automated>
    <fails_when>the count is not 0 — the submodule working tree is dirty, meaning this plan touched a source, generated or test file when it should have touched only meta-repository markdown</fails_when>
  </verify>
  <acceptance_criteria>
    - `198-GH66-ANSWER.md` exists with exactly the four named section headings and exactly one comment-body heading, preceded by a bolded internal-only warning.
    - The status section opens with an all-caps verdict recording that the draft is held and not posted, and records the checkable fact the hold rests on.
    - The comment body contains no plan id, requirement id, decision id, phase reference or planning-directory path.
    - The comment body names the corrected programming voltage, the confirmed programming supply, and credits the reporter by name.
    - The comment body contains none of the three firmware settable-band figures.
    - The comment body carries a version placeholder, not a version number.
    - Both named rows emit 12500 in the shipped database, so the draft's claim matches the artifact.
    - The issue still has six comments and is unchanged.
    - `.planning/REQUIREMENTS.md` is byte-unchanged, VOLT-04 stays Pending, and nothing under `.planning/milestones/` is modified.
    - The `firestarter_app` working tree is clean.
  </acceptance_criteria>
  <done>
    The answer is written, every sentence in it is true, and it is not posted — because at this moment
    the one thing a reader would need in order to check it does not exist yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: One consolidated held-pending list, so the second draft is not lost</name>
  <files>.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md</files>
  <read_first>
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md` § "Held-pending deferral" in full — its five bolded bullets, its numbered four-step list, and its requirement-status bullet.
    - `.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md` as Task 1 left it — the second draft's location, its own deferral section and its requirement status.
    - `.planning/phases/198-the-two-voltage-nibbles/198-CONTEXT.md` D-14 and D-18, and `198-RESEARCH.md` § F-11's closing note on the scope boundary between the two.
  </read_first>
  <action>
    Extend the held-pending deferral section in
    `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md` so it names
    both held issues rather than one. The goal is that whoever closes this milestone finds a single
    list, with the same steps for each issue, instead of discovering one draft and never thinking to
    look for a second in a different phase directory.

    Keep the section's existing five-bullet structure and its numbered four-step list. Widen the
    what-is-held bullet to name both issues and the file each draft lives in, by path. Widen the
    requirement-status bullet to name both outstanding requirements. State that the four steps apply
    once per issue, and that each draft's own deferral section carries the same steps so either entry
    point reaches the same instructions.

    Do not rewrite the existing reasoning about why the hold was chosen — that ruling stands and
    applies to both. Do not delete any of the recorded operator decisions.

    **This edit is legitimate and the task says so explicitly so it is not refused.** The project's
    do-not-edit rule covers archived records under the milestones directory, which are
    historical-by-intent. This file is in the current milestone's live phase directory, is still
    operationally load-bearing, and its whole purpose is to be the instruction sheet read at the beta
    cut. Extending it is maintaining a live document, not rewriting history.

    Post nothing and push nothing. Commit in the meta repository.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md').read_text(encoding='utf-8')
s = t[t.index('## Held-pending deferral'):]
for tok in ('70', '66', '198-GH66-ANSWER.md', 'PULSE-04', 'VOLT-04'):
    assert tok in s, tok
for tok in ('What is held', 'Why', 'What releases the hold', 'Exactly what to do', 'Requirement status'):
    assert tok in s, tok
print('DEFERRAL_OK')" | /usr/bin/grep -qx 'DEFERRAL_OK'</automated>
    <fails_when>the output is not `DEFERRAL_OK` — the AssertionError names the first missing element: either issue number, the path to the second draft, either outstanding requirement id, or one of the five bolded bullets that the existing structure must keep</fails_when>
    <automated>cd /workspaces && python3 -c "
import pathlib
t = pathlib.Path('.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md').read_text(encoding='utf-8')
s = t[t.index('## Held-pending deferral'):]
for n in ('1.', '2.', '3.', '4.'):
    assert n in s, n
assert 'Operator decision' in t or 'operator decision' in t, 'recorded operator decision was removed'
assert '## Comment Body' in t, 'the comment body section was disturbed'
print('STRUCTURE_INTACT')" | /usr/bin/grep -qx 'STRUCTURE_INTACT'</automated>
    <fails_when>the output is not `STRUCTURE_INTACT` — the AssertionError names a missing numbered step, the removal of the recorded operator decision, or a disturbed comment-body section. The edit widens one section; it does not restructure the file</fails_when>
    <automated>cd /workspaces && W=$(git diff HEAD --name-only -- .planning/phases/197-the-override-mechanism-and-the-program-pulse/) && C=$(git log -1 --name-only --format= -- .planning/phases/197-the-override-mechanism-and-the-program-pulse/) && printf '%s\n%s\n' "$W" "$C" | /usr/bin/grep -q 'GH70-ANSWER'</automated>
    <fails_when>neither the working-tree stat nor the last commit names the answer file — the edit was not made, or was made to a different path. Only that one file in the previous phase's directory may change</fails_when>
    <automated>cd /workspaces && S=$(git status --porcelain -- .planning/milestones/ .planning/REQUIREMENTS.md) && printf '%s' "$S" && test -z "$S"</automated>
    <fails_when>a porcelain line is printed before the non-zero exit — an archived milestone record or the requirements file was modified. The first is forbidden outright; the second would flip a requirement whose publication has not happened</fails_when>
    <automated>cd /workspaces && XDG_CACHE_HOME=/tmp/fs-xdg gh issue view 70 --repo henols/firestarter --json comments --jq '.comments | length' 2>/dev/null | /usr/bin/grep -qx '3'</automated>
    <fails_when>the printed comment count is not 3 — a fourth comment means the previously-held draft was posted, which nothing in this phase authorises. If the command errors rather than printing a number, re-run it with a writable cache directory before treating the result as a failure</fails_when>
  </verify>
  <acceptance_criteria>
    - The held-pending deferral section names both issue numbers and the path to the second draft file.
    - It names both outstanding requirement ids in its requirement-status bullet.
    - All five bolded bullets and all four numbered steps survive.
    - The recorded operator decision and the comment-body section are undisturbed.
    - Only the answer file changes in the previous phase's directory.
    - Nothing under `.planning/milestones/` and nothing in `.planning/REQUIREMENTS.md` is modified.
    - Neither issue has gained a comment.
  </acceptance_criteria>
  <done>
    There is one list naming every held answer in this milestone, and whoever runs the beta cut reaches
    both drafts from whichever one they find first.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| held draft → public issue | The comment body is written to be pasted verbatim onto a public tracker; anything in the file that is not meant for posting sits one copy-paste away from being posted |
| internal provenance → public text | Plan ids, requirement ids and decision ids are meaningless to a reader and identify internal process on a public issue |
| draft claim → shipped artifact | A number stated in the draft is checkable only against the database that actually shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-20 | Repudiation | naming a version a reader cannot resolve | high | mitigate | The route is to hold; the comment body carries a placeholder rather than a version, and the status section records the checkable fact that no upstream exists |
| T-198-21 | Information Disclosure | internal provenance pasted onto a public issue | high | mitigate | The comment body is scanned by pattern for plan ids, requirement ids, decision ids, phase references and planning-directory paths, and every section other than the body is marked internal in its own heading |
| T-198-22 | Spoofing | a stated value the artifact does not carry | high | mitigate | Both claimed values are asserted directly against the shipped database in the same task that writes them |
| T-198-23 | Tampering | an unauthorised publication | high | mitigate | The comment count on both issues is asserted unchanged, and the submodule working tree is asserted clean |
| T-198-24 | Repudiation | a held draft nobody finds at the beta cut | medium | mitigate | Both issues are consolidated into one deferral list, asserted by issue number and by the path to the second draft |
| T-198-25 | Repudiation | a requirement marked complete before it is | medium | mitigate | The requirements file is asserted byte-unchanged; the deferral record states the requirement is carried forward |
| T-198-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and touches no dependency manifest |
</threat_model>

## Artifacts this phase produces (plan 198-04)

**New files**
- `.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md` — five sections: title with an internal-only warning, `## Status`, `## Internal provenance`, `## Comment Body`, `## Held-pending deferral`

**Changed documentation**
- `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md` — the held-pending deferral section widened to name both issues, both draft paths and both outstanding requirement ids

**No new code symbols.** This plan writes and edits meta-repository markdown only. It creates no function, constant, dict key, test, override entry or generated value, and touches nothing under `firestarter_app/`.

<verification>
- `198-GH66-ANSWER.md` carries the four named section headings, exactly one comment-body heading, and a bolded internal-only warning under the title.
- The comment body matches none of the internal-provenance patterns.
- The comment body names the corrected programming voltage, the confirmed programming supply and the reporter, and contains none of the three firmware settable-band figures.
- Both named rows emit 12500 in the shipped database.
- The `197-GH70-ANSWER.md` deferral section names both issue numbers, the second draft's path and both outstanding requirement ids, with all five bullets and four steps intact.
- Neither issue has gained a comment; nothing is pushed.
- `.planning/REQUIREMENTS.md` is byte-unchanged and VOLT-04 stays Pending.
- Nothing under `.planning/milestones/` is modified, and the `firestarter_app` working tree is clean.
</verification>

<success_criteria>
The answer to the community report exists, is true at the moment it will be posted rather than at the
moment it was written, and claims only what changed. VOLT-04 is honestly left Pending with the reason
recorded, and both held drafts are reachable from one list so the milestone close cannot miss either.
</success_criteria>

<output>
Create `.planning/phases/198-the-two-voltage-nibbles/198-04-SUMMARY.md` when done. Record the comment
counts observed on both issues, the result of the internal-provenance pattern scan over the comment
body, the two emitted values read from the shipped database at the time the draft was written, and an
explicit statement that VOLT-04 remains Pending with the reason.
</output>
