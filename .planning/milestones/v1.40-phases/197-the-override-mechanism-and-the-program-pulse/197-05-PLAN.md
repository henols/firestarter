---
phase: 197-the-override-mechanism-and-the-program-pulse
plan: 05
type: execute
wave: 5
depends_on: ["197-04"]
files_modified:
  - firestarter_app/tools/datasheet_overrides.json
  - firestarter_app/tests/test_datasheet_overrides.py
  - firestarter_app/tests/test_build_db_constant_census.py
  - firestarter_app/tests/golden/build_db_part_specific_constants.json
  - firestarter_app/firestarter/data/chip_database.json
  - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md
autonomous: true
requirements: [OVR-06, PULSE-02, PULSE-03]

estimate:
  tokens: 38000
  raw_tokens: 76000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-19 superseding D-10: the pulse correction is exactly THREE rows, not up to nine. `FUJITSU/MBM27128` goes 200 to 1000 µs, `FUJITSU/MBM27C1000` goes 100 to 500 µs, `FUJITSU/MBM27C1001` goes 100 to 500 µs, and `tools/datasheet_overrides.json` holds exactly 9 entries."
    - "D-19: `FUJITSU/MBM27C4001` gets NO entry. Its own datasheet confirms the decoded 100 µs is already correct, so under D-04 an entry for it would be a no-op that fails the build. The verification is recorded in the plan `197-07` inventory instead."
    - "D-19: `FUJITSU/MBM27128` carries 1000 µs, the Quick Pro initial pulse from Figure 3 of its datasheet, and NOT the conventional 50 ms single-shot from the same datasheet — because the firmware runs a verify-per-pulse loop capped at 25 pulses with `overprogram_factor = 0` on protocols 0x07 and 0x08, which is the fast algorithm's shape. The reasoning is recorded in the entry's own `note`."
    - "D-13 superseded by D-16: the regeneration diff against `70c92ce` is exactly 13 rows and ZERO `support_status` values change — 9 `unsupported_reason` strings, `RAMTRON/FM1608` on two fields, and 3 `programming.pulse_duration_us` values. 746 rows in, 746 rows out."
    - "D-16: the six NMOS override entries and the size-threshold derivation each contribute ZERO rows to the diff, which is what D-06 and D-09 claimed and what the 13-row total confirms."
    - "PULSE-03: `197-REGEN-DIFF.md` accounts for every one of the 13 changed rows, each attributed either to a decode rule that explains it or to an override entry that cites a datasheet for it — the nine reason strings and FM1608's two fields to decode rules, the three pulse values to override entries, two of which cite git-tracked PDFs."
    - "OVR-06: the surviving part-specific-constant list in `tests/golden/build_db_part_specific_constants.json` is frozen with an exact pinned count, and every entry in it carries a non-empty `reason`. Success criterion 2 is satisfied either by an empty list or by a named list with reasons."
    - "OVR-06: the census fails CLOSED. It asserts against a frozen allow-list rather than grepping for a regex that might match nothing after a rename, and a companion test proves the detector reports a planted part-number literal fed to it as synthetic source text."
    - "OVR-06/adjacency: allow-list matching is exact full-string equality, not substring containment. A synthetic literal that contains an allow-listed string as a prefix is still reported as a survivor."
    - "OVR-06/empty: an empty allow-list is still read, still compared and still reported against. The census does not early-return, skip or pass vacuously when the frozen list is empty — that is the state it is expected to be in."
    - "OVR-06/ordering: the census reports survivors in ascending sorted order, so its failure message is stable and diffable across runs."
    - "PULSE-03/adjacency: the diff compares on the `(manufacturer, part_number)` key and asserts the two key sets are equal BEFORE comparing any field, so a row added or removed is reported as a key-set difference rather than silently absorbed into a field comparison."
    - "PULSE-03/empty: a field path present on one side and absent on the other is reported as `<absent>` rather than skipped; the per-row comparison walks the union of both sides' field-path sets."
    - "PULSE-03/ordering: the diff emits rows in `sorted()` key order, so re-running it produces a byte-identical artifact and the recorded evidence is reproducible."
  artifacts:
    - firestarter_app/tools/datasheet_overrides.json
    - firestarter_app/tests/test_build_db_constant_census.py
    - firestarter_app/tests/golden/build_db_part_specific_constants.json
    - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md
  key_links:
    - "`tests/test_chip_database_field_inventory.py::test_27c_protocol_chip_counts_match` pins `{\"7\": 170, \"8\": 127, \"11\": 32}`. An override that moved an algorithm would redden it; the pulse entries touch only `programming.pulse_duration_us`."
    - "`tools/baseline/chip_database.baseline.json` is STALE — 475857 bytes against the live 432461. The diff baseline is `git show 70c92ce:firestarter/data/chip_database.json` and nothing else."
    - "`tools/diff_db.py` does not exist. It is referenced by `tests/golden/chip_database_field_inventory.json`'s `meta.why_not_diff_db`; that stale reference is corrected in plan `197-07`."
  prohibitions:
    - "No override entry is written for `FUJITSU/MBM27C4001`. Its datasheet agrees with the decode, so the entry would be a no-op and the build would refuse."
    - "`FUJITSU/MBM27128` does not carry 50000. The conventional single-shot figure is the wrong reading for a verify-per-pulse firmware loop."
    - "No committed diff tool is added to `tools/`. The 746-row diff is produced by a throwaway script whose text is recorded in the artifact."
    - "`tools/baseline/chip_database.baseline.json` is not used as a diff baseline and is not regenerated."
    - "No new field is added to the emitted database schema to mark a row as datasheet-corrected."
    - "No comment line is added to any `.py` file under `firestarter_app/`."
    - "`firestarter/data/chip_database.json` is never hand-edited."
    - "No commit lands on `beta` or `main` in any repository."
  flagged_assumptions: []
---

<objective>
Finish the corrections, prove no part-number literal survives in the generator, and account for every
row the whole phase moved.

The pulse correction is three rows. Research measured what the three in-repo Fujitsu datasheets
actually cover, and it is not the nine D-10 estimated: two Fujitsu rows need a change, one is
datasheet-confirmed already correct and must therefore get no entry at all, and the remaining nine of
Fujitsu's twelve algorithm 7/8 rows have no datasheet in the repository. Writing an entry for the
confirmed-correct row is the trap D-10's wording sets, and D-04 makes it a build failure rather than
a silent no-op.

OVR-06 asks whether any part-specific constant survives, and success criterion 2 permits survivors
that are named with proof no alternative exists. The honest way to answer is a frozen census rather
than a grep, because a source-scanning gate that greps for a pattern fails OPEN the moment a rename
makes the pattern match nothing — a failure mode this project has already been bitten by.

PULSE-03 asks for the 746-row diff. The expected answer is 13 rows, measured, with every one
attributable to either a decode rule or a datasheet-citing override.

**Known-red window.** The two `tests/test_wire_dict_equivalence.py` tests stay red through this plan
and are closed by plan `197-06`, which measures the real wire delta rather than assuming it.

Purpose: land the last corrections, answer OVR-06 with something that cannot pass vacuously, and
produce the evidence PULSE-03 and the restated OVR-05 both need.
Output: two more override entries, a frozen constant census with a proven-capable-of-failing
detector, and a committed 746-row regeneration diff.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-CONTEXT.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PATTERNS.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-04-SUMMARY.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The two remaining pulse corrections, and the row that must not get one</name>
  <files>firestarter_app/tools/datasheet_overrides.json, firestarter_app/tests/test_datasheet_overrides.py, firestarter_app/firestarter/data/chip_database.json</files>
  <precondition>Network reachability to `gitlab.com` for the pinned `infoic.xml` fetch.</precondition>
  <precondition>`datasheets/MBM27128.pdf`, `datasheets/MBM27C1001.pdf` and `datasheets/MBM27C4001.pdf` are all git-tracked inside `firestarter_app`, established by plan `197-01`. A `datasheet` citation to an untracked file dangles for every other checkout.</precondition>
  <read_first>
    - `firestarter_app/tools/datasheet_overrides.json` as plan `197-03` left it — seven entries, sorted.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-CONTEXT.md` D-10 and D-19.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "PULSE-02 — Which Rows, To What Values" — the twelve-row Fujitsu table, the verbatim datasheet extracts, and the explicit no-op warning; and § "PULSE-01" subsection "What `pulse_duration_us` means on the wire", which is why the Quick Pro figure is the one the field carries.
    - `firestarter_app/firestarter/data/chip_database.json` — the `FUJITSU` rows, to read the current `pulse_duration_us` for each of the three target rows and confirm the `was` values.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "Common Pitfalls" pitfall 2.
  </read_first>
  <behavior>
    - The row `FUJITSU` / `MBM27128` emits `programming.pulse_duration_us` 1000, up from 200.
    - The row `FUJITSU` / `MBM27C1001` emits 500, up from 100.
    - The row `FUJITSU` / `MBM27C1000P,MBM27C1000` still emits 500, unchanged from plan `197-02`.
    - The row `FUJITSU` / `MBM27C4001` still emits 100 and has no entry in the override file.
    - All four rows keep their existing `programming.algorithm` — 7 for `MBM27128`, 8 for the other three — and their existing `pinout` and `support_status`.
    - Adding an entry for `FUJITSU/MBM27C4001` with `was` 100 and `is` 100 aborts the build; this is proven by a test, not by trying it against the shipped file.
  </behavior>
  <action>
    Per D-19, add exactly two entries to `firestarter_app/tools/datasheet_overrides.json`, keeping the
    top-level keys in ascending order.

    `FUJITSU/MBM27128` sets `programming.pulse_duration_us` from 200 to 1000, `datasheet`
    `datasheets/MBM27128.pdf`. Its `note` must carry the reasoning, because the datasheet gives two
    figures and this one is not the obvious one. Record: Figure 3, the Quick Pro flow chart on page 4-20,
    specifies `TPW = 1 ms ± 50 µs` with `VCC = 6V ± 0.25V`, `VPP = 21V ± 0.5V` and an `X = 20` ceiling;
    page 4-19's conventional procedure specifies a 50 ms pulse and is the wrong reading here, because the
    firmware consumes this field as the initial pulse width of a verify-per-pulse loop capped at 25
    pulses with `overprogram_factor = 0` on protocols 0x07 and 0x08, which is the fast algorithm's shape
    and not the single-shot one. Note also that the corrected value still yields an incomplete Quick Pro,
    because the firmware never emits the datasheet's `tOPW` over-program pulse.

    `FUJITSU/MBM27C1001` sets `programming.pulse_duration_us` from 100 to 500, `datasheet`
    `datasheets/MBM27C1001.pdf`, `note` recording page 9-90's AC CHARACTERISTICS table: tPW
    0.475/0.50/0.525 ms, N 1 to 25, tOPW 1.4/1.5/39.4 ms, measured at VCC1 6V ± 0.25V and VPP2
    12.5V ± 0.3V.

    Write NO entry for `FUJITSU/MBM27C4001`. Its datasheet — page 8, AC CHARACTERISTICS — gives tPW
    min 95, typ 100, max 105 µs, and the front page states a 0.1 ms fast-programming pulse. The decode
    already produces 100, so an entry would have `was` equal to `is`, which D-04 makes a build failure.
    That verification belongs in the plan `197-07` inventory, recorded as datasheet-confirmed correct.
    D-10's phrasing invites one entry per datasheet-covered row; that is the trap.

    Add one test to `firestarter_app/tests/test_datasheet_overrides.py` that constructs a synthetic
    no-op entry in memory and asserts the applier raises, so the rule that keeps this row out of the file
    is itself covered rather than merely obeyed. Update the entry-count assertion in that module from 7
    to exactly 9, and leave the `UNSOURCED` count at exactly 6 — both new entries cite tracked PDFs.

    Regenerate and confirm the three pulse values and the fourth unchanged one. Commit inside
    `firestarter_app` on `v1.40-program-parameter-fidelity`, naming D-19 and PULSE-02.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python tools/build_db.py 2>&1 | tail -1 | /usr/bin/grep -q '= 746 total\.'</automated>
    <fails_when>non-zero exit — the generator raised, most likely a no-op or stale-prior `ValueError` from one of the new entries, or its final stdout line does not report 746 total chips</fails_when>
    <automated>cd /workspaces/firestarter_app && python -c "
import json
d = json.load(open('firestarter/data/chip_database.json'))
want = {'MBM27128': (1000, 7), 'MBM27C1000P,MBM27C1000': (500, 8), 'MBM27C1001': (500, 8), 'MBM27C4001': (100, 8)}
got = {r['part_number']: (r['programming']['pulse_duration_us'], r['programming']['algorithm']) for r in d['FUJITSU']}
bad = {k: (v, got.get(k)) for k, v in want.items() if got.get(k) != v}
assert not bad, bad
print('PULSE_OK')" | /usr/bin/grep -qx 'PULSE_OK'</automated>
    <fails_when>the output is not `PULSE_OK` — an `AssertionError` names each row whose pulse width or algorithm is not what D-19 specifies; `MBM27C4001` reading anything but 100 means an entry was written for it, and `MBM27128` reading 50000 means the conventional single-shot figure was taken instead of the Quick Pro one</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(python -c "import json;d=json.load(open('tools/datasheet_overrides.json'));print(len(d))")" -eq 9 && python -c "import json,sys;d=json.load(open('tools/datasheet_overrides.json'));k=list(d);sys.exit(0 if k==sorted(k) else 1)"</automated>
    <fails_when>non-zero exit — the file does not hold exactly 9 entries (10 means an entry was written for the confirmed-correct row), or its keys are not in ascending order</fails_when>
    <automated>cd /workspaces/firestarter_app && python -c "
import json, subprocess
d = json.load(open('tools/datasheet_overrides.json'))
paths = sorted({e['datasheet'] for e in d.values() if e['datasheet'] != 'UNSOURCED'})
tracked = subprocess.run(['git','ls-files','--'] + paths, capture_output=True, text=True).stdout.split()
missing = [p for p in paths if p not in tracked]
assert not missing, missing
assert len(paths) == 3, paths
print('CITATIONS_OK')" | /usr/bin/grep -qx 'CITATIONS_OK'</automated>
    <fails_when>the output is not `CITATIONS_OK` — an `AssertionError` lists a cited datasheet path that `git ls-files` does not report as tracked, which means the citation dangles for every other checkout, or the count of distinct cited PDFs is not 3</fails_when>
    <automated>cd /workspaces/firestarter_app && OUT=$(mktemp) && /tmp/fs-venv311/bin/python -m pytest tests/ -o addopts="" -q -rf > "$OUT" 2>&1; /usr/bin/grep -qE '^[0-9]+ failed, [0-9]+ passed' "$OUT" && test "$(/usr/bin/grep '^FAILED' "$OUT" | /usr/bin/grep -cv test_wire_dict_equivalence)" -eq 0 && /usr/bin/grep -q '^FAILED tests/test_wire_dict_equivalence.py::test_exactly_84_records_change_flags_and_no_other_field_moves' "$OUT"</automated>
    <fails_when>the `N failed, M passed` summary line is absent (so the count legs pass vacuously), or the known-red `test_exactly_84_records_change_flags_and_no_other_field_moves` is not among the failures, or any FAILED line names a module other than `tests/test_wire_dict_equivalence.py` — `tests/test_chip_database_field_inventory.py` appearing means an override moved a protocol count</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff --cached -- '*.py') && OFFENDERS=$(printf '%s\n' "$D" | /usr/bin/grep -E '^\+\s*#' | /usr/bin/grep -v '^\+\s*#!'); printf '%s' "$OFFENDERS"; test -z "$OFFENDERS"</automated>
    <fails_when>non-zero exit, with the offending lines printed immediately above it — a comment line was added to a staged Python file, which the project rule forbids absolutely. The pathspec is load-bearing; without it the pattern also matches markdown</fails_when>
  </verify>
  <acceptance_criteria>
    - `tools/datasheet_overrides.json` holds exactly 9 sorted top-level keys, of which exactly 6 are `UNSOURCED` and exactly 3 cite distinct git-tracked PDF paths.
    - No key naming the Fujitsu 524288-byte 4 Mbit row exists in the file.
    - The four Fujitsu rows emit pulse widths 1000, 500, 500 and 100 with algorithms 7, 8, 8 and 8.
    - Both new entries carry a non-empty `note`; the `MBM27128` note names both the Quick Pro figure and the conventional figure and states why the Quick Pro one is carried.
    - `tests/test_datasheet_overrides.py` pins the entry count at 9 and adds a synthetic no-op test.
    - The full Python 3.11 suite reports exactly 2 failures, both in `tests/test_wire_dict_equivalence.py`.
    - The staged-comment check prints nothing.
  </acceptance_criteria>
  <done>
    All three pulse corrections the in-repo datasheets support are in the override file, the row whose
    datasheet confirms the existing value has no entry, and PULSE-02's 475–525 µs gate is met on the row
    gh#70 reported.
  </done>
  <reversibility rating="reversible">D-19 rates each value reversible: it is one override entry, and deleting the entry restores the decode.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Answer OVR-06 with a frozen census that cannot pass vacuously</name>
  <files>firestarter_app/tests/test_build_db_constant_census.py, firestarter_app/tests/golden/build_db_part_specific_constants.json</files>
  <read_first>
    - `firestarter_app/tools/build_db.py` in whatever state plans `197-01` through `197-04` left it — specifically to see which string literals remain.
    - `firestarter_app/tests/test_chip_database_field_inventory.py` lines 190-265 — the frozen-inventory-plus-AST-walk shape this census copies, including how its golden records both the set and its provenance.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "Part-Specific Constant Census After the Phase (OVR-06)" — the eleven-constant table and its dispositions; and § "Validation Architecture", the note that source-scanning gates fail OPEN on a rename.
    - `.planning/ROADMAP.md` § "Phase 197" success criterion 2.
  </read_first>
  <behavior>
    - The detector, given the real `tools/build_db.py` source and an empty allow-list, returns a sorted list containing no part-number-shaped string literal.
    - The detector, given synthetic source text containing a set literal holding a part-number-shaped string, returns that string. This is the capable-of-failing proof.
    - The detector, given an allow-list holding one string and synthetic source containing a longer string that has the allow-listed one as a prefix, still returns the longer string — matching is exact equality, never containment.
    - The detector, given an empty allow-list and violating synthetic source, still reports; it does not early-return or pass when the allow-list is empty.
    - The detector returns its results in ascending sorted order for any input.
    - Every entry in the frozen allow-list carries a non-empty `reason`, and the allow-list's length is asserted as an exact count.
  </behavior>
  <action>
    Create `firestarter_app/tests/test_build_db_constant_census.py` and its frozen golden
    `firestarter_app/tests/golden/build_db_part_specific_constants.json`.

    Write a module-level detector function taking three arguments: the source text to scan, the frozen
    allow-list as a set, and nothing else it reads from disk. It parses the source with `ast.parse`,
    walks it, collects every `ast.Constant` whose value is a `str`, keeps only those matching a
    part-number shape, subtracts the allow-list by exact string equality, and returns the remainder as
    an ascending sorted list. Take the shape as: at most 16 characters, and fully matched by a pattern of
    up to six ASCII letters, then two to five ASCII digits, then up to eight further ASCII letters or
    digits. That shape admits the literals this phase deleted and excludes the things that are not part
    numbers — a pinout key carries an underscore, a URL carries a colon and slashes, a filename carries
    a dot, and a format specifier carries a hash. Exclusion by shape rather than by a hand-maintained
    list of non-part-numbers is what keeps the allow-list honest.

    Take the arguments as parameters rather than reading the real files inside the detector, because the
    capable-of-failing and adjacency tests must feed it synthetic source and a synthetic allow-list. A
    detector that hardcodes its inputs cannot be proven to fire.

    Run the detector against the real `tools/build_db.py` with an empty allow-list and capture what it
    finds. If it finds nothing, the golden's list is empty and OVR-06's answer is the empty list the
    requirement anticipates. If it finds anything, every survivor goes into the golden WITH a non-empty
    `reason` stating why no alternative exists — success criterion 2 permits exactly that, and a survivor
    without a reason is an undecided hole rather than a named constant. Do not tune the shape pattern to
    make an inconvenient survivor disappear; that converts a real finding into a silent exclusion.

    Give the golden the same two-part structure `tests/golden/chip_database_field_inventory.json` uses:
    the data, and a `meta` block recording what produced it, what it does and does not prove, and how to
    update it. State plainly that the census proves no part-number literal reaches the generator's source
    and does not prove any surviving constant is correct.

    Then write the five tests in the behavior block plus the exact-count assertion on the golden's own
    length. The capable-of-failing test and the adjacency test are what make this gate fail closed; a
    census that has never been seen to redden proves nothing about the source it scans.

    The new module and golden are inside `ruff`'s scope. Express sectioning with class names and
    docstrings. Run `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` before
    committing inside `firestarter_app` on `v1.40-program-parameter-fidelity`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && /tmp/fs-venv311/bin/python -m pytest tests/test_build_db_constant_census.py -o addopts="" -q -rf 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed'</automated>
    <fails_when>the summary line does not match `N passed` — any `FAILED` line or collection error</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(/tmp/fs-venv311/bin/python -m pytest tests/test_build_db_constant_census.py -o addopts="" -q --collect-only 2>/dev/null | /usr/bin/grep -c '::')" -ge 6</automated>
    <fails_when>fewer than 6 test ids are collected — one of the five behaviours plus the exact-count assertion has no test, and in particular the capable-of-failing and adjacency proofs may be missing</fails_when>
    <automated>cd /workspaces/firestarter_app && python -c "
import json
g = json.load(open('tests/golden/build_db_part_specific_constants.json'))
assert 'meta' in g, sorted(g)
entries = g['constants']
assert isinstance(entries, list), type(entries)
bad = [e for e in entries if not e.get('reason')]
assert not bad, bad
print('CENSUS_GOLDEN_OK', len(entries))" | /usr/bin/grep -q '^CENSUS_GOLDEN_OK '</automated>
    <fails_when>the output does not begin `CENSUS_GOLDEN_OK` — an `AssertionError` reports a missing `meta` block, a constants value that is not a list, or an entry with an empty or absent `reason`, which is an undecided hole rather than a named constant</fails_when>
    <automated>cd /workspaces/firestarter_app && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>any diagnostic from `ruff check`, or a `would reformat` line from `ruff format --check`</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff --cached -- '*.py') && OFFENDERS=$(printf '%s\n' "$D" | /usr/bin/grep -E '^\+\s*#' | /usr/bin/grep -v '^\+\s*#!'); printf '%s' "$OFFENDERS"; test -z "$OFFENDERS"</automated>
    <fails_when>non-zero exit, with the offending lines printed immediately above it — a comment line was added to a staged Python file, which the project rule forbids absolutely. The pathspec is load-bearing; without it the pattern also matches markdown</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_build_db_constant_census.py` exists, collects at least 6 tests, and all pass on Python 3.11.
    - The detector takes the source text and the allow-list as parameters; it reads neither from disk itself.
    - One test feeds the detector synthetic source containing a planted part-number literal and asserts it is reported — the census is proven capable of failing, not merely claimed to cover.
    - One test proves allow-list matching is exact equality and not substring containment.
    - One test proves an empty allow-list still reports rather than passing vacuously.
    - One test asserts the detector's output is ascending sorted.
    - `tests/golden/build_db_part_specific_constants.json` has a `meta` block and an exact-length assertion, and every entry carries a non-empty `reason`.
    - `ruff check` and `ruff format --check` are clean; the staged-comment check prints nothing.
  </acceptance_criteria>
  <done>
    OVR-06 is answered with a frozen census that names every surviving part-specific constant with a
    reason, and that has been observed to redden against a planted literal rather than trusted to.
  </done>
  <reversibility rating="reversible">A census golden is re-derivable at any time from the source it scans; widening or narrowing the shape pattern costs one edit and its tests.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Measure and record the 746-row regeneration diff</name>
  <files>.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md</files>
  <precondition>All of plans `197-01` through `197-04` and this plan's Task 1 are committed, so the database being diffed is the phase's final generated state.</precondition>
  <read_first>
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "The Measured Regeneration Diff" — the expected 13-row table with exact before and after values; and § "Regeneration and Diff Mechanics", which supplies the diff script and names the two wrong baselines.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-CONTEXT.md` D-13 and D-16.
    - `.planning/REQUIREMENTS.md` — PULSE-03's exact wording, and OVR-05's original wording that D-13 and D-16 restate.
    - The four preceding plans' SUMMARY files, for the per-plan changed-row counts to reconcile against.
  </read_first>
  <action>
    Produce the evidence PULSE-03 asks for and the restated OVR-05 accepts, and commit it as a phase
    artifact.

    Write a throwaway field-path diff script — not a committed tool in `tools/`. It loads the baseline
    from `git show 70c92ce:firestarter/data/chip_database.json` and the live
    `firestarter/data/chip_database.json`, flattens each to a mapping from `(manufacturer, part_number)`
    to the row, asserts the two key sets are equal before comparing any field so an added or removed row
    surfaces as a key-set difference rather than being absorbed, then for each key in `sorted()` order
    walks the union of both sides' dotted field paths and reports every path whose values differ, using
    a distinguishable absent marker for a path present on only one side. Emit rows in sorted key order so
    re-running produces a byte-identical result.

    Do not diff against `tools/baseline/chip_database.baseline.json`. It is stale — 475857 bytes against
    the live 432461 — and produces a large spurious diff. Do not attempt to use `tools/diff_db.py`; it
    does not exist.

    Write the result to
    `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md` in the meta
    repository. Lead with the reproducible method — the exact baseline ref, the exact command, and the
    script's full text in a fenced block, so any reader can re-derive the table. Then give the row-count
    line: rows in, rows out, rows changed, and how many `support_status` values changed. Then the table,
    one line per changed field, with the manufacturer, the part number, the dotted field path, the before
    value, the after value, and a cause column.

    Every row's cause must be one of two kinds, because that is exactly what PULSE-03 requires: a decode
    rule that explains it, or an override entry that cites a datasheet for it. The nine reason-string rows
    and the two FM1608 fields are decode-rule causes — name the specific rule, which for FM1608's second
    field is the SRAM single-rail rewrite at `build_db.py:700-705` that the deleted label was bypassing.
    The three pulse rows are override causes — name the entry key and its `datasheet` value. A row that
    fits neither kind is a finding, not a formatting problem: stop and report it rather than inventing a
    cause.

    Record the two zero-diff claims explicitly, because they are load-bearing and a table of changes
    cannot show them: the six NMOS override entries reproduce the six values they replaced and contribute
    no changed row, and the size-threshold derivation regenerates byte-identically. Cite the plan and task
    that proved each.

    Reconcile against the expectation and state the result either way: D-16 predicts 13 changed rows and
    zero `support_status` changes. If the measured figure differs, the artifact records the measured
    figure as authoritative and names the discrepancy — the phase's own contradictions section exists
    because a predicted count was wrong before.

    State the honesty limit: the diff proves which values moved and why each was expected to, and does
    not prove any new value is electrically correct. The three pulse values rest on datasheets read
    visually; the six NMOS values rest on an inherited hardcode and carry `UNSOURCED`.

    Commit in the meta repository on `v1.40-program-parameter-fidelity`.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md</automated>
    <fails_when>non-zero exit — the artifact was not written</fails_when>
    <automated>cd /workspaces && for t in '70c92ce' 'unsupported_reason' 'pulse_duration_us' 'electrical.vcc_mv' 'electrical.type' 'UNSOURCED'; do /usr/bin/grep -qF -e "$t" .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md || { echo "MISSING $t"; exit 1; }; done</automated>
    <fails_when>a `MISSING <token>` line is printed — the artifact omits the baseline ref, one of the four changed field paths, or the unsourced-values honesty note</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c '^|' .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md)" -ge 16</automated>
    <fails_when>fewer than 16 table lines — the 14 changed-field lines plus a header and a separator are not all present, so at least one changed field is unaccounted for</fails_when>
    <automated>cd /workspaces/firestarter_app && python -c "
import json, subprocess
b = json.loads(subprocess.run(['git','show','70c92ce:firestarter/data/chip_database.json'],capture_output=True,text=True).stdout)
a = json.load(open('firestarter/data/chip_database.json'))
fb = {(m, r['part_number']): json.dumps(r, sort_keys=True) for m, rs in b.items() for r in rs}
fa = {(m, r['part_number']): json.dumps(r, sort_keys=True) for m, rs in a.items() for r in rs}
assert set(fa) == set(fb), set(fa) ^ set(fb)
changed = sorted(k for k in fb if fb[k] != fa[k])
ss = [k for k in changed if json.loads(fb[k])['support_status'] != json.loads(fa[k])['support_status']]
assert len(changed) == 13, changed
assert not ss, ss
print('DIFF_13_0_OK')" | /usr/bin/grep -qx 'DIFF_13_0_OK'</automated>
    <fails_when>the output is not `DIFF_13_0_OK` — an `AssertionError` lists a changed row count other than 13, or names a row whose `support_status` moved when D-16 says none does, or reports a row added or removed; any of these means the artifact's table must record the measured figure and explain the discrepancy before the plan can close</fails_when>
  </verify>
  <acceptance_criteria>
    - `197-REGEN-DIFF.md` exists and opens with a reproducible method naming the `70c92ce` baseline and carrying the script's full text.
    - It states rows in, rows out, rows changed and `support_status` changes as explicit numbers.
    - Its table carries one line per changed field — 9 reason strings, 2 FM1608 fields, 3 pulse values — each with a cause that is either a named decode rule or a named override entry with its datasheet.
    - It records the two zero-diff claims with the plan and task that proved each.
    - It reconciles the measured count against D-16's predicted 13 and states the result either way.
    - It names its honesty limit.
    - The independent measurement confirms 13 changed rows and 0 `support_status` changes, 746 rows in and out.
  </acceptance_criteria>
  <done>
    Every row the phase moved is accounted for, each attributed to a decode rule or to a
    datasheet-citing override, and the two mechanisms that were claimed to move nothing are recorded as
    having moved nothing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datasheet PDF → override entry | A number read visually from a scanned page becomes a programming pulse width on real silicon |
| `tools/build_db.py` source → OVR-06 census | The census is a source-scanning gate, and this project has measured such gates failing OPEN on a rename |
| generated database → PULSE-03 evidence | The diff is the only artifact that would reveal an unintended row movement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-21 | Tampering | a no-op override entry | high | mitigate | D-04 makes `was == is` a build failure, and the rule is covered by a synthetic test rather than by the shipped file's happen-to-be-correct contents |
| T-197-22 | Tampering | the wrong datasheet figure carried | high | mitigate | The `MBM27128` entry's note records both candidate figures and the firmware-semantics argument for the one carried, so a later reader can audit the choice rather than re-deriving it |
| T-197-23 | Spoofing | a source-scanning gate that fails open | high | mitigate | The census asserts against a frozen allow-list rather than a regex that could match nothing, and is proven to fire against a planted literal fed as synthetic source |
| T-197-24 | Repudiation | an unexplained row movement | high | mitigate | The 746-row diff asserts key-set equality before field comparison and requires every changed field to carry a decode-rule or datasheet-citing-override cause |
| T-197-25 | Tampering | diffing against a stale baseline | medium | mitigate | The baseline is pinned to `git show 70c92ce:...`; `tools/baseline/chip_database.baseline.json` is 43 KB larger than the live file and is explicitly excluded |
| T-197-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package |
</threat_model>

## Artifacts this phase produces (plan 197-05)

**New files**
- `firestarter_app/tests/test_build_db_constant_census.py`
- `firestarter_app/tests/golden/build_db_part_specific_constants.json`
- `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-REGEN-DIFF.md`

**New symbols**
- a part-specific-literal detector in `tests/test_build_db_constant_census.py`, taking the source text and the allow-list as parameters and returning an ascending sorted list of survivors

**New JSON entries in `tools/datasheet_overrides.json`**
- `FUJITSU/MBM27128` — `programming.pulse_duration_us` 200 to 1000, citing `datasheets/MBM27128.pdf`
- `FUJITSU/MBM27C1001` — `programming.pulse_duration_us` 100 to 500, citing `datasheets/MBM27C1001.pdf`

**New golden keys**
- `constants` and `meta` in `tests/golden/build_db_part_specific_constants.json`

**New test names**
- census: real-source-finds-nothing, planted-literal-is-reported, allow-list-matches-exactly-not-by-containment, empty-allow-list-still-reports, output-is-sorted, golden-length-is-exact
- overrides: a synthetic no-op entry raises; the entry-count assertion moves from 7 to 9

**Changed data**
- `firestarter/data/chip_database.json` — two more `programming.pulse_duration_us` values

<verification>
- The four Fujitsu rows read 1000, 500, 500 and 100 µs with algorithms 7, 8, 8, 8.
- The override file holds exactly 9 sorted entries, 6 `UNSOURCED`, 3 citing tracked PDFs.
- The census collects at least 6 tests, all passing, including a proven-capable-of-failing leg.
- The census golden's every entry carries a non-empty `reason`.
- The independent 746-row measurement reports 13 changed rows and 0 `support_status` changes.
- `197-REGEN-DIFF.md` accounts for all 14 changed fields with a cause each.
- The full 3.11 suite has exactly 2 failures, both in `tests/test_wire_dict_equivalence.py`.
- `ruff` is clean; the staged-comment check prints nothing.
</verification>

<success_criteria>
PULSE-02 holds on the reported row and PULSE-03 holds across all 746: every changed row is explained
by a decode rule or by an override citing a datasheet. OVR-06 is answered by a frozen census that has
been seen to fail. The restated OVR-05 has its acceptance evidence — exactly the expected rows and
nothing else.
</success_criteria>

<output>
Create `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-05-SUMMARY.md` when done.
Record the measured changed-row count, whether the census allow-list came out empty and its contents
if not, and the three pulse values as emitted.
</output>
