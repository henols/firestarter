---
phase: 197-the-override-mechanism-and-the-program-pulse
plan: 07
type: execute
wave: 7
depends_on: ["197-06"]
files_modified:
  - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/derive-away-max-27c020-size-hardcode.md
  - .planning/todos/completed/derive-away-max-27c020-size-hardcode.md
  - firestarter_app/tools/DECODE-NOTES.md
  - firestarter_app/tests/golden/chip_database_field_inventory.json
autonomous: true
requirements: [PULSE-01]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-11 CONFIRMED and written down: the generator's `microseconds for all protocols` decode rule survives contact with the datasheets, and the finding is that infoic carries a bulk family default rather than a per-part value. The positive proof is `FUJITSU/MBM27C4001` — raw `pulse_delay` `0x0064` decodes to 100 µs and its datasheet specifies tPW typ 100 µs, in the datasheet's own unit."
    - "D-11: the falsification of `per-part value` is recorded — 462 of 675 algorithm 7/8 `<ic>` entries in the pinned `infoic.xml` carry the identical raw `0x0064`, and two Fujitsu parts sharing that raw value have datasheet pulses of 100 µs and 500 µs respectively."
    - "D-11: the finding records what `pulse_duration_us` means on the wire — the initial pulse width of a firmware verify-per-pulse loop capped at 25 pulses with `overprogram_factor = 0`, so the figure to carry is the fast-algorithm initial tPW and never a conventional single-shot width."
    - "D-12: the inventory records 297 algorithm 7/8 rows, 217 of them at 100 µs before this phase, and the rows this phase does not correct, each disposed with a verdict. The generating command is stated beside the data so the count is auditable."
    - "D-12: `FUJITSU/MBM27C4001` appears in the inventory as datasheet-confirmed correct rather than in the override file, because an entry for it would be a no-op that fails the build under D-04."
    - "PULSE-01/adjacency: the decode-rule boundary is exact protocol-id membership, not a range. `interpret_timing` returns the raw value for `proto_id in (0x07, 0x08, 0x0B)` and 0 for every other id including the adjacent `0x0D`, and it is called with the POST-`classify()` id, so rows promoted into `0x0D` emit `pulse_duration_us: 0`. This is stated in the written finding and holds in the shipped database."
    - "PULSE-01/empty: a missing or unparseable `pulse_delay` raises rather than defaulting to 0. `tests/test_build_db_interpret_timing.py` is unchanged by this phase and still green, and the written finding records that the fatal branch is provably dead against the pinned `infoic.xml` so that module is its only coverage."
    - "PULSE-01/ordering: the finding lands as a new numbered section appended after `tools/DECODE-NOTES.md`'s existing sections 0 through 7, and the existing section numbering is unchanged."
    - "The stale claim in `tools/DECODE-NOTES.md` section 5 — that a cosmetic relabel applies to FM1608 and that there is no decode change — is corrected, because plan `197-04` deleted that relabel and moved two emitted fields."
    - "The stale reference to a non-existent `tools/diff_db.py` in `tests/golden/chip_database_field_inventory.json`'s `meta.why_not_diff_db` is corrected. That file does not exist; `tools/baseline/chip_database.baseline.json` exists but is stale at 475857 bytes against the live 432461."
  artifacts:
    - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md
    - firestarter_app/tools/DECODE-NOTES.md
  key_links:
    - "`tests/test_chip_database_field_inventory.py` does not read the golden's `meta` block, so correcting `why_not_diff_db` cannot redden it — verified by grep before editing."
    - "`.planning/todos/pending/derive-away-max-27c020-size-hardcode.md` has one surviving open point, which plan `197-01`'s derivation closes. Its other two points were closed by v1.32 Phase 182-02."
    - "The measured figure after this phase is 215 rows at 100 µs with no datasheet evidence; CONTEXT.md's deferred note says 216. It is a bookkeeping number, not load-bearing, and the inventory records the measured one."
  prohibitions:
    - "`tools/DECODE-NOTES.md`'s existing sections 0 through 7 are not renumbered."
    - "No override entry is created as a result of this plan. The inventory is the landing place for a datasheet-confirmed-correct row, not the override file."
    - "`.planning/ROADMAP.md` is edited with scoped replacements only. A whole-file write destroys every phase entry outside the diff window."
    - "`phases.clear` and `state.sync` are never run. The first is destructive and the second corrupts STATE.md frontmatter."
    - "No comment line is added to any `.py` file under `firestarter_app/`. This plan touches no Python source."
    - "No commit lands on `beta` or `main` in any repository."
  flagged_assumptions: []
---

<objective>
Write down what the phase learned, inventory what it did not reach, and correct the references it
falsified.

PULSE-01 asks for a finding, not a change: what `pulse_delay` encodes, established per algorithm
family from evidence, including whether the generator's current reading survives. It does survive,
and the proof is positive rather than an absence of contradiction — one Fujitsu part's raw value
decodes to exactly what its datasheet specifies, in the datasheet's own unit. The real finding is
that the upstream data carries a bulk family default: 462 of 675 algorithm 7/8 entries share one raw
value, and two parts carrying it have datasheet pulses five times apart.

D-12 asks for the rows this phase does not correct to be inventoried rather than forgotten. That
inventory is also where the row whose datasheet confirms the existing value belongs — it must not get
an override entry, because a no-op entry fails the build.

Three documents in the repository now assert things this phase made false, and two backlog-worthy
findings surfaced during it that are out of scope here. Both get filed.

Purpose: leave the evidence behind in a form the next phase and the next reader can use, and stop
three stale claims from rotting further.
Output: a measured inventory, a written decode finding, two corrected references, three backlog
entries and one closed todo.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-CONTEXT.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PATTERNS.md
@.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-06-SUMMARY.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The measured 100 µs inventory, with every row disposed</name>
  <files>.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md</files>
  <read_first>
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "D-12 — The Inventory" — the exact counts, the full distribution table, the top-manufacturer breakdown and the runnable command that produces the rows.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "PULSE-02 — Which Rows, To What Values" — the twelve-row Fujitsu table with its per-row action column.
    - `.planning/milestones/v1.36-phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the structural precedent: a stated reproducible method before the data, every row disposed in a bolded verdict vocabulary, and a named honesty limit.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-05-SUMMARY.md` — the pulse values as actually emitted.
  </read_first>
  <action>
    Write
    `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md` in the
    meta repository, per D-12's requirement that the uncorrected rows be recorded as a measured
    inventory in the phase directory.

    Lead with the reproducible method, following the `177-READBACK-INVENTORY.md` precedent: the exact
    command that enumerates every row with `programming.algorithm` in 7 or 8 and
    `programming.pulse_duration_us` equal to 100 from the shipped `firestarter/data/chip_database.json`,
    emitting the manufacturer, the part number, the size and the VPP for each. State the command before
    the data so the count is auditable by anyone.

    Give the census figures as measured: 297 algorithm 7/8 rows in total, 170 on algorithm 7 and 127 on
    algorithm 8, cross-checked against the frozen golden's protocol counts which
    `tests/test_chip_database_field_inventory.py` already pins. Give the full pulse-width distribution
    across those 297 rows. Give the pre-phase count of rows at 100 µs, and the post-phase count, and
    reconcile the difference against the three corrections plus the one datasheet-confirmed row. The
    measured post-phase figure is 215; CONTEXT.md's deferred note says 216. Record the measured one and
    note the discrepancy rather than silently adopting either.

    Dispose every Fujitsu algorithm 7/8 row with a bolded verdict, using three terms: **CORRECTED** for
    the three rows this phase changed, naming the new value and the datasheet; **DATASHEET-CONFIRMED-CORRECT**
    for `FUJITSU/MBM27C4001`, recording that its page-8 AC CHARACTERISTICS table gives tPW min 95, typ
    100, max 105 µs and its front page states a 0.1 ms fast-programming pulse, so the decoded 100 µs is
    already right and an override entry for it would be a no-op that fails the build under D-04; and
    **NO DATASHEET** for the remaining eight.

    For the 215 rows outside the Fujitsu block, a full per-row table is unwieldy. Give a
    per-manufacturer summary table — the measured top manufacturers are ATMEL 16, NSC 15, AMD 14,
    SGS-THOMSON 14, MACRONIX(MXIC) 13, INTEL 11, ST 11, FAIRCHILD 9 — plus the complete list as a fenced
    block, with the generating command adjacent so any reader can regenerate it. All 215 carry the
    verdict **NO DATASHEET**.

    State the honesty limit plainly, as the 177 precedent does: these rows are inventoried, not audited.
    A row's presence in this list is not a claim that its 100 µs is wrong — it is a claim that nobody has
    checked. D-02 makes correcting one cost one datasheet, which is the intended deterrent against a
    bulk sweep.

    Commit in the meta repository on `v1.40-program-parameter-fidelity`.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md</automated>
    <fails_when>non-zero exit — the inventory artifact was not written</fails_when>
    <automated>cd /workspaces && for t in 'CORRECTED' 'DATASHEET-CONFIRMED-CORRECT' 'NO DATASHEET' 'chip_database.json' '297'; do /usr/bin/grep -qF -e "$t" .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md || { echo "MISSING $t"; exit 1; }; done</automated>
    <fails_when>a `MISSING <token>` line is printed — the inventory omits one of the three verdict terms, the source it was generated from, or the algorithm 7/8 total</fails_when>
    <automated>cd /workspaces/firestarter_app && python -c "
import json
db = json.load(open('firestarter/data/chip_database.json'))
rows = [(m, r) for m in sorted(db) for r in db[m] if r['programming']['algorithm'] in (7, 8) and r['programming']['pulse_duration_us'] == 100]
total = sum(1 for m in db for r in db[m] if r['programming']['algorithm'] in (7, 8))
print('TOTALS', total, len(rows))"</automated>
    <fails_when>non-zero exit, or the printed totals disagree with the figures written into the inventory — the artifact records a count it did not measure</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c '^|' .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md)" -ge 20</automated>
    <fails_when>fewer than 20 table lines — the twelve-row Fujitsu disposition table and the per-manufacturer summary table are not both present</fails_when>
  </verify>
  <acceptance_criteria>
    - `197-PULSE-INVENTORY.md` exists and opens with the exact generating command before any data.
    - It records 297 algorithm 7/8 rows split 170 and 127, the full pulse-width distribution, the pre-phase and post-phase counts at 100 µs, and reconciles the difference.
    - It notes the 215-versus-216 discrepancy against CONTEXT.md and states which figure was measured.
    - All twelve Fujitsu algorithm 7/8 rows carry one of the three bolded verdicts, with the three corrected rows naming their new value and datasheet.
    - `FUJITSU/MBM27C4001` is recorded as DATASHEET-CONFIRMED-CORRECT with its tPW figures and the reason it has no override entry.
    - The 215 remaining rows appear as a per-manufacturer summary plus a complete fenced list, with the generating command adjacent.
    - The honesty limit is stated: inventoried, not audited.
    - The printed totals from the independent count match the figures in the artifact.
  </acceptance_criteria>
  <done>
    The rows this phase did not reach are recorded with a reproducible method and a verdict each, and
    the row whose datasheet confirms its existing value is recorded here rather than becoming a build
    failure in the override file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the PULSE-01 finding, and correct the two claims this phase falsified</name>
  <files>firestarter_app/tools/DECODE-NOTES.md, firestarter_app/tests/golden/chip_database_field_inventory.json</files>
  <read_first>
    - `firestarter_app/tools/DECODE-NOTES.md` — the whole file, to see the eight existing numbered sections and the house style of each, and specifically section 5 at `:230-248`, whose heading and closing paragraph both assert a relabel that no longer exists.
    - `firestarter_app/tools/build_db.py:339-365` — `interpret_timing` as the phase left it.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "PULSE-01 — The Falsification Job" in full, including the distribution table of 675 entries, the firmware-semantics subsection and the block quote headed "PULSE-01 finding, as it should be written down".
    - `firestarter_app/tests/golden/chip_database_field_inventory.json` — the `meta.why_not_diff_db` value at `:10`.
    - `firestarter_app/tests/test_chip_database_field_inventory.py` — confirm by grep that nothing reads the golden's `meta` block before editing it.
  </read_first>
  <action>
    Record the finding and repair two stale claims. All three edits are in markdown or JSON, not Python
    source, so the no-comments rule does not restrict the prose here.

    First, append a new numbered section 8 to `firestarter_app/tools/DECODE-NOTES.md`, after the existing
    section 7. Do not renumber anything. The section states the PULSE-01 finding in the shape
    RESEARCH.md's block quote gives: that raw `pulse_delay` is plain microseconds for every protocol that
    consumes it, namely `0x07`, `0x08` and `0x0B`, confirmed positively by `FUJITSU/MBM27C4001` whose raw
    `0x0064` decodes to 100 µs against a datasheet tPW typ of 100 µs; that the value is not per-part,
    because 462 of 675 algorithm 7/8 entries in the pinned `infoic.xml` carry the identical raw `0x0064`
    and two Fujitsu parts sharing it have datasheet pulses of 100 µs and 500 µs; that no uniform
    multiplier is coherent, because the raw ladder spans `0x0001` to `0x2710` and any factor mapping 100
    to 500 also makes the `0x0001` entries a 5 µs program pulse, which no EPROM specifies; and that the
    generator's decode rule is therefore correct while the upstream data carries a bulk family default.

    Include the wire-semantics half, because it is what settles which datasheet number to carry when a
    datasheet gives two: the firmware consumes `pulse_duration_us` as the initial pulse width of a
    verify-per-pulse loop capped at 25 pulses on protocols `0x07` and `0x08`, with `overprogram_factor`
    of 0 on all three rows of its parameter table, so the datasheet's over-program pulse is never emitted
    and the figure to record is the fast-algorithm initial tPW rather than a conventional single-shot
    width. Note the recorded known-divergence that a corrected value still yields an incomplete fast
    algorithm for the same reason.

    Include the boundary and the empty-input behaviour explicitly, because both are load-bearing and
    neither is obvious: the protocol test is exact membership and not a range, so the adjacent `0x0D`
    returns 0; the call passes the post-`classify()` protocol id, so rows promoted into `0x0D` emit 0
    rather than their raw value; and a missing or unparseable raw value raises rather than defaulting to
    0, a branch that is provably dead against the pinned upstream commit and is therefore covered only by
    `tests/test_build_db_interpret_timing.py`.

    Second, correct section 5 of the same file. Its heading claims there is no decode change and that the
    value is already correct in the database, and its closing paragraph states that a cosmetic relabel
    applies after classification. Plan `197-04` deleted that relabel, and two emitted fields moved as a
    result. Rewrite the heading and the falsified clause to state what is now true: the classifier emits
    algorithm 40 for this part by type, the relabel is gone, and the row now reads SRAM with its VCC
    taking the single-rail rewrite. Keep the rest of the section — the ground-truth tuple, the type-4
    argument and the decimal-40 conflation note — which all remain correct.

    Third, correct `tests/golden/chip_database_field_inventory.json`'s `meta.why_not_diff_db`. It reasons
    about a `tools/diff_db.py` that does not exist and a baseline file that is stale. State instead that
    no diff tool exists in `tools/`, that `tools/baseline/chip_database.baseline.json` is stale at 475857
    bytes against the live 432461 and must not be used as a comparison baseline, and that a regeneration
    diff is produced against `git show <ref>:firestarter/data/chip_database.json` by a throwaway script —
    keeping the surviving point, which is that this inventory is deliberately not derived from or
    compared against any regenerable baseline. Confirm by grep first that
    `tests/test_chip_database_field_inventory.py` does not read the `meta` block, so the edit cannot
    redden it.

    Commit inside `firestarter_app` on `v1.40-program-parameter-fidelity`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -q '^## 8\.' tools/DECODE-NOTES.md && for n in 0 1 2 3 4 5 6 7; do /usr/bin/grep -q "^## ${n}\." tools/DECODE-NOTES.md || { echo "MISSING SECTION ${n}"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or a `MISSING SECTION n` line — a section 8 was not appended, or one of the existing sections 0 through 7 lost its number, meaning the existing numbering was disturbed</fails_when>
    <automated>cd /workspaces/firestarter_app && for t in '0x0064' '0x07' '0x0B' '0x0D' '462' '675' 'overprogram_factor' 'interpret_timing'; do /usr/bin/grep -qF -e "$t" tools/DECODE-NOTES.md || { echo "MISSING $t"; exit 1; }; done</automated>
    <fails_when>a `MISSING <token>` line is printed — the finding omits the raw value that proves the unit, the protocol set that consumes it, the adjacent protocol that does not, the two distribution figures, the firmware parameter that settles which datasheet number to carry, or the function whose behaviour is being described</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -qF 'cosmetic `SRAM → FRAM` relabel then applies' tools/DECODE-NOTES.md</automated>
    <fails_when>exit code 1 — the falsified clause in section 5 still asserts that a relabel applies, when plan `197-04` deleted it</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -q 'diff_db' tests/golden/chip_database_field_inventory.json</automated>
    <fails_when>exit code 1 — the golden's meta block still reasons about a tool that does not exist in `tools/`</fails_when>
    <automated>cd /workspaces/firestarter_app && python -c "import json;json.load(open('tests/golden/chip_database_field_inventory.json'));print('JSON_OK')" | /usr/bin/grep -qx 'JSON_OK'</automated>
    <fails_when>the output is not `JSON_OK` — the edit left the golden unparseable, which would make every test reading it error rather than fail</fails_when>
    <automated>cd /workspaces/firestarter_app && /tmp/fs-venv311/bin/python -m pytest tests/test_chip_database_field_inventory.py tests/test_build_db_interpret_timing.py -o addopts="" -q -rf 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed'</automated>
    <fails_when>the summary line does not match `N passed` — editing the golden's meta block reddened the module that reads it, or the decode-rule module regressed</fails_when>
  </verify>
  <acceptance_criteria>
    - `tools/DECODE-NOTES.md` has a new section 8 and its sections 0 through 7 are still numbered 0 through 7.
    - Section 8 states the unit finding with its positive datasheet proof, the bulk-default falsification with both figures, the no-coherent-multiplier argument, the firmware wire semantics, the exact protocol-membership boundary including the adjacent id that returns 0, the post-classify call-site consequence, and the raise-rather-than-default behaviour with its single coverage module.
    - Section 5's heading and its relabel clause state what is true after plan `197-04`, and the rest of the section is unchanged.
    - `tests/golden/chip_database_field_inventory.json` parses, no longer names a non-existent tool, and names the stale baseline with both byte figures.
    - `tests/test_chip_database_field_inventory.py` and `tests/test_build_db_interpret_timing.py` both pass.
  </acceptance_criteria>
  <done>
    PULSE-01's finding is written down where the generator's other decode findings live, and the two
    claims this phase falsified no longer assert the retired behaviour.
  </done>
</task>

<task type="auto">
  <name>Task 3: File the three backlog items and close the todo this phase completes</name>
  <files>.planning/ROADMAP.md, .planning/todos/pending/derive-away-max-27c020-size-hardcode.md, .planning/todos/completed/derive-away-max-27c020-size-hardcode.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "## Backlog" and the entries around `### Phase 999.68`, to match the existing entry shape exactly — heading format, the parenthetical status suffix, and the body fields each entry carries.
    - `.planning/todos/pending/derive-away-max-27c020-size-hardcode.md` in full, to confirm which of its points plan `197-01` closed and which were already closed by v1.32 Phase 182-02.
    - `.planning/todos/pending/pinout-address-width-and-we-pin-corrections.md`, which the pinout finding routes to.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-RESEARCH.md` § "PULSE-02" subsection "A safety-adjacent finding the planner must route: the MBM27C1000 pinout", in full including its `[INFERRED]` causal paragraph and the assumption-log entry A1.
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-03-SUMMARY.md` — the six unsourced entries and their notes.
  </read_first>
  <action>
    File three backlog entries and close one todo, all in the meta repository.

    The highest existing backlog number is 999.68, so the three new entries are 999.69, 999.70 and
    999.71. Add each under the `## Backlog` section of `.planning/ROADMAP.md`, matching the shape of the
    entries already there. Use scoped `Edit` calls that insert the new entries; never rewrite the file.

    999.69 — the algorithm 7/8 rows still carrying 100 µs with no datasheet evidence. Give the measured
    post-phase count from Task 1, point at
    `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md` as the
    inventory, and record the cost model D-02 sets: one datasheet per row, deliberately, as a deterrent
    against a bulk sweep. State that a row's presence is not a claim that its value is wrong.

    999.70 — `FUJITSU/MBM27C1000` is on a pinout that belongs to a different part. This is
    safety-adjacent and must be filed with its evidence and its uncertainty separated. The measured fact:
    the two parts have different pin assignments — the 1000 has `/OE` on pin 2 and `A16` on pin 24, the
    1001 has `A16` on pin 2 and `/OE` on pin 24 — while `firestarter/data/pinouts.json` gives
    `DIP32_27C020` an address-bus list whose index 16 is pin 2 and an `oe-pin` of 24, which is the 1001
    layout, and the database assigns both rows that same pinout key. The inferred consequence, which has
    NOT been bench-tested: under the swap, a write to an address needing A16 set would drive the wrong
    socket pin, and because the real A16 would then follow the OE line's state, the verify would read a
    different address than the program pulse wrote — which matches the reported failure signature of a
    byte failing to program within 25 pulses at the top of the address space. Mark the fact MEASURED and
    the consequence INFERRED, exactly as research did, and note that this contradicts an earlier
    published cross-check on gh#70 that was drawn from the wrong part's datasheet. Route it to the
    existing `pinout-address-width-and-we-pin-corrections.md` todo, and state that it materially changes
    what the gh#70 answer in plan `197-08` may claim.

    999.71 — the six override entries carrying the `UNSOURCED` token. List them, state that their values
    are inherited verbatim from the hardcode plan `197-03` deleted, and state precisely what would close
    each: the Intel 2716 and 2732 datasheets for the two Intel rows, and SGS-THOMSON and ST datasheets
    for the four rows currently carrying an Intel reading. Name the consequence of leaving them: an
    Intel reading is applied to four non-Intel parts, which is exactly the silent cross-application D-02
    exists to make visible, and it is now visible rather than fixed.

    Then close `.planning/todos/pending/derive-away-max-27c020-size-hardcode.md`. Its surviving open
    point — that the constant is derivable and should therefore not exist — is closed by plan `197-01`'s
    derivation. Move the file from `pending/` to `completed/` with `git mv`, appending a closure note
    naming the phase, the plan, and the byte-identical regeneration that proved it. Leave
    `pinout-address-width-and-we-pin-corrections.md` and
    `fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md` in `pending/` — CONTEXT.md folds the first
    as a design constraint only and defers the second entirely.

    Commit in the meta repository on `v1.40-program-parameter-fidelity`.
  </action>
  <verify>
    <automated>cd /workspaces && for n in 69 70 71; do /usr/bin/grep -qE "^### Phase 999\.${n}:" .planning/ROADMAP.md || { echo "MISSING 999.${n}"; exit 1; }; done</automated>
    <fails_when>a `MISSING 999.N` line is printed — that backlog entry is absent, or its heading does not match the existing `### Phase 999.N:` shape and so will not be found by the backlog review</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c '^### Phase ' .planning/ROADMAP.md)" -ge 63</automated>
    <fails_when>the phase-heading count is below the pre-task count plus three — phase entries were lost, which is the signature of a whole-file write on `ROADMAP.md`</fails_when>
    <automated>cd /workspaces && for t in 'MEASURED' 'INFERRED' 'DIP32_27C020' 'UNSOURCED' '197-PULSE-INVENTORY.md'; do /usr/bin/grep -qF -e "$t" .planning/ROADMAP.md || { echo "MISSING $t"; exit 1; }; done</automated>
    <fails_when>a `MISSING <token>` line is printed — a backlog entry omits its evidence-versus-inference marking, the pinout key at issue, the unsourced-entry token, or the pointer to the inventory artifact</fails_when>
    <automated>cd /workspaces && test -f .planning/todos/completed/derive-away-max-27c020-size-hardcode.md && ! test -f .planning/todos/pending/derive-away-max-27c020-size-hardcode.md</automated>
    <fails_when>non-zero exit — the todo was not moved to `completed/`, or a copy was left behind in `pending/` so it will be surfaced again at the next review</fails_when>
    <automated>cd /workspaces && test -f .planning/todos/pending/pinout-address-width-and-we-pin-corrections.md && test -f .planning/todos/pending/fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md</automated>
    <fails_when>non-zero exit — a todo that CONTEXT.md explicitly keeps open was closed as collateral</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` gains exactly three backlog entries numbered 999.69, 999.70 and 999.71, in the existing entry shape, and loses no phase heading.
    - 999.69 names the measured post-phase count and points at the inventory artifact.
    - 999.70 separates the MEASURED pin-assignment fact from the INFERRED failure explanation, names `DIP32_27C020`, routes to the existing pinout todo, and states that it constrains what plan `197-08` may claim on gh#70.
    - 999.71 lists all six `UNSOURCED` entries and names the specific datasheet that would close each.
    - `derive-away-max-27c020-size-hardcode.md` is in `completed/` with a closure note naming the phase, the plan and the byte-identical proof, and is absent from `pending/`.
    - The two todos CONTEXT.md keeps open are still in `pending/`.
  </acceptance_criteria>
  <done>
    The three findings this phase surfaced but does not act on are filed where the backlog review will
    find them, with their evidence and their uncertainty kept apart, and the one todo the phase closes
    is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a written finding → the next phase's planning input | Phases 198, 199 and 200 will plan against what this document says; a wrong claim here propagates |
| a stale in-repo claim → a future reader | Two documents currently assert behaviour this phase removed |
| an inferred hardware fault → a public issue answer | 999.70's causal chain is unbenched and feeds plan `197-08` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-31 | Repudiation | an inferred cause presented as measured | high | mitigate | 999.70 marks the pin-assignment fact MEASURED and the failure explanation INFERRED, separately, and names the earlier published cross-check it contradicts |
| T-197-32 | Repudiation | stale claims rotting in the repository | medium | mitigate | Both falsified claims are corrected in the same phase that falsified them, with negative greps proving the retired wording is gone |
| T-197-33 | Tampering | whole-file rewrite of `ROADMAP.md` | high | mitigate | Scoped `Edit` calls only, with a phase-heading count floor that catches a destructive write |
| T-197-34 | Spoofing | an inventory that asserts a count it did not measure | medium | mitigate | The generating command is stated before the data, and an independent count is run against the shipped database and compared |
| T-197-35 | Information Disclosure | none | low | accept | All three documents are repository-internal planning and decode notes; no secret or credential is involved |
| T-197-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package |
</threat_model>

## Artifacts this phase produces (plan 197-07)

**New files**
- `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-PULSE-INVENTORY.md`

**New sections**
- `firestarter_app/tools/DECODE-NOTES.md` section 8 — the PULSE-01 `pulse_delay` finding

**New ROADMAP backlog entries**
- `### Phase 999.69` — the algorithm 7/8 rows still at 100 µs with no datasheet evidence
- `### Phase 999.70` — `FUJITSU/MBM27C1000` on the `DIP32_27C020` pinout, which is the sibling part's layout
- `### Phase 999.71` — the six `UNSOURCED` override entries and the datasheets that would close them

**Moved files**
- `.planning/todos/pending/derive-away-max-27c020-size-hardcode.md` to `.planning/todos/completed/`

**Corrected in place**
- `firestarter_app/tools/DECODE-NOTES.md` section 5 heading and its relabel clause
- `firestarter_app/tests/golden/chip_database_field_inventory.json` `meta.why_not_diff_db`

<verification>
- The inventory exists with its generating command, the three verdict terms, and figures matching an independent count.
- `tools/DECODE-NOTES.md` has a section 8 and still has sections 0 through 7; the finding carries all eight required tokens.
- The falsified relabel clause and the non-existent-tool reference are both gone.
- The field-inventory golden still parses and its two reader modules pass.
- Three backlog entries exist with no phase heading lost; both kept-open todos remain in `pending/`.
- The closed todo is in `completed/` and not in `pending/`.
</verification>

<success_criteria>
PULSE-01 holds: what `pulse_delay` encodes is established per algorithm family from evidence and
written down, including the confirmation that the generator's current reading survives. D-12 holds:
the rows this phase does not correct are inventoried with a reproducible method and a verdict each.
Nothing this phase falsified is left asserting the retired behaviour.
</success_criteria>

<output>
Create `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-07-SUMMARY.md` when done.
Record the measured post-phase 100 µs row count against CONTEXT.md's 216, and the three backlog
numbers as filed.
</output>
