---
phase: 201-a-partial-write-is-gated-on-its-own-region
plan: 03
type: execute
wave: 3
depends_on: [201-02]
files_modified:
  - firestarter_fw/include/memory_utils.h
  - firestarter_fw/src/proms/memory.cpp
  - firestarter_fw/src/proms/eprom.cpp
  - firestarter_fw/tests/golden/protocol_branch_inventory.json
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/tests/fake_chip.py
  - firestarter_app/tests/test_eprom_operations.py
autonomous: true
requirements: [BLANK-01, BLANK-03]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "BLANK-01 / D-09: `mem_util_blank_check_region(handle, start, end)` holds the scan and `mem_util_blank_check` becomes a one-line whole-device wrapper passing `(0, handle->mem_size)`. One code path, so the nine reference sites cannot drift, and the region form is unit-testable in isolation. This is BLANK-02's `a region-scoped variant or a start/end parameter — not an edit in place`."
    - "BLANK-01 / D-10: only `eprom.cpp:145` changes. `flash_intel.cpp:95` and `flash_nor_unlock.cpp:105` keep calling the whole-device form. Those protocols' parts carry `FLAG_CAN_ERASE`, so the erase immediately above leaves the device blank and the check passes trivially — the same mechanism that hid this defect for its whole life."
    - "RESEARCH Open Question 1, DECIDED: the `0 = absent` fallback and the fail-closed clamp are resolved in exactly ONE place — `mem_util_operation_end(const firestarter_handle_t*)`, declared in `include/memory_utils.h` and defined in `src/proms/memory.cpp`. It returns `handle->mem_size` when `region_end` is 0 or exceeds `mem_size`, else `region_end`. Resolving it in `memory.cpp` adds ZERO rows to the `eprom.cpp` branch-inventory golden; resolving it with an inline ternary inside `eprom.cpp` would add one."
    - "SECURITY V5: `min(region_end, mem_size)` is the fail-closed form and it is enforced inside the single helper, so a host sending `region_end > mem_size` cannot widen a scan past the device. `_process_incoming_data`'s `MSG_ERR_OUT_OF_RANGE` remains as the second gate."
    - "D-09: the region form adds no second cross-call static. `blank_check_saved_address` (`memory.cpp:437`) stays the only one, and its `do not replace with a heap allocation` rationale at `:425-436` still holds. On the write-init path the saved address and the region start are the same value, so the change is `do not reset the cursor to 0` plus `stop at end`."
    - "The four `mem_size` reads inside the scan become `end`-relative in the region form: the completion test (`:456`), the scan upper bound (`:465`), the clamp (`:501`) and the progress denominator (`:510`). The clamp must move with the rest or the final progress frame of a region scan reports past the region."
    - "The single-exit HV wrapper at `eprom.cpp:152-158` still sees every exit: the region call adds no `return` inside `eprom_internal_write_init_body`."
    - "D-05 / C-1: the host emits the key from `_setup_operation` (`eprom_operations.py:456`), beside the read path's existing `command_dict[\"memory-size\"] = addr + read_size` narrowing at `:495`. NOT from `database.py`'s `convert_to_programmer`, which has no access to the file, the address or the command and whose key union is pinned to exactly nine keys."
    - "D-05: the host sends it on every write, not only when `--address` is given. One code path, no branch on address presence. The named consequence is accepted: a 4 KiB file written to a blank-required 64 KiB part stops blank-checking the 60 KiB it will not touch."
    - "D-07: `write_eprom` and `verify_eprom` share one dict-construction path through `_operation_context` to `_setup_operation`, so one change serves both and write and verify agree on where the operation ends."
    - "BLANK-03 / D-16.2 / G-5: `WriteInitPreflightChip._is_blank` learns the region, so the host `write -a` regression leg is no longer vacuous. Today it compares the whole buffer against `b\"\\xff\" * memory_size` (`fake_chip.py:265-268`) and `write_eprom` gates on it at `:282`, modelling the whole-device check exactly."
    - "G-2: the branch-inventory golden is re-derived from the module's own `_extract_predicates` and lands in the SAME commit as the `eprom.cpp` edit, with `meta.blob_shas` taken from `git hash-object` on the working tree before staging and `meta.recorded_at_head` naming the parent commit."
    - "BLANK-03: `test_write_init_accepts_blank_region_on_non_blank_part` turns GREEN at this plan's commit, having been seen RED at plan 201-02's, and its negative control stays GREEN."
  artifacts:
    - firestarter_fw/include/memory_utils.h
    - firestarter_fw/src/proms/memory.cpp
    - firestarter_fw/src/proms/eprom.cpp
    - firestarter_fw/tests/golden/protocol_branch_inventory.json
    - firestarter_app/firestarter/eprom_operations.py
    - firestarter_app/tests/fake_chip.py
    - firestarter_app/tests/test_eprom_operations.py
  key_links:
    - "`test_exactly_one_protocol_keyed_site_at_the_pinned_line` asserts `protocol_lines == [70]`, a hard-coded line number pointing at `switch (handle->protocol)` in `configure_eprom`. ANY line inserted above `eprom.cpp:70`, including a comment, breaks it. The write-init edit at `:144-145` is safely below; do not add a file-header comment."
    - "`_extract_predicates` extracts every ternary `?:` whose text references a handle field (`test_protocol_branch_inventory.py:322-349`, `_is_relevant` at `:266-274`). That is why the fallback is a function call and not an inline ternary: a call in a plain assignment carries no predicate and adds no inventory row."
    - "`_operation_context` forwards to `_setup_operation` POSITIONALLY for its leading six parameters and by keyword only for `fault_inject_outgoing` (`eprom_operations.py:516-540`). The new parameter must be trailing on both signatures and forwarded by name."
    - "`write_eprom` already reads the payload file pre-connect through `require_page_alignment` at `:2010`, four lines above `_operation_context` at `:2014`, and `page_size_gate.py:162-172` does `parse_address` plus `os.path.getsize` there. `verify_eprom` (`:2107-2120`) takes `input_file_path` too but does NOT call `require_page_alignment`, so it must `getsize` itself."
    - "`_apply_write_progress` already DISCARDS the frame's total (`absolute, _total_ignored = map(int, ...)`, `eprom_operations.py:690-721`), so the operator-visible progress bar does not move when the firmware's denominator changes in plan 201-04."
    - "The coupled host test is `tests/test_chip_test_uv_slot_write.py` (6 references to `WriteInitPreflightChip` at `:81, :104, :112, :276, :324, :413/:417`), NOT `tests/test_uv_mask.py`, which imports neither the fake nor `FakeChip`."
  prohibitions:
    - "No commit lands on `beta` or `main` in any of the three repositories. A push to `beta` in either sub-repo PUBLISHES, with no path filter, and `firestarter_app` reaches PyPI where a version can never be reused."
    - "D-08: `handle->mem_size` keeps meaning device size and is narrowed nowhere. Scoping the narrowing per-protocol was considered and rejected separately — it would add a host-side algorithm branch, which is exactly what v1.20 removed. `eeprom28c_check_chip_id` derives `mfr_addr = handle->mem_size - 64` and drives 12 V on A9 at that address (`eeprom_28c.cpp:190`), reached from `eeprom28c_write_init` at `:343`. A narrowed `memory-size` on writes would put 12 V on an arbitrary address on every partial AT28C write."
    - "The write-init blank check is SCOPED, never deleted. `eeprom_28c.cpp:379-385` and `flash_5v_page.cpp:74-78` both deleted theirs outright, and that precedent is the wrong answer for UV parts: a programmed bit cannot be un-programmed, so the precondition is real there and a silently dropped refusal destroys user data irreversibly."
    - "`flash_intel.cpp`, `flash_nor_unlock.cpp`, `flash_5v_page.cpp` and `eeprom_28c.cpp` are not edited. Region-scoping their write-init checks is explicitly deferred (D-10) and needs its own bench budget."
    - "No `return` is added inside `eprom_internal_write_init_body`. The single-exit wrapper's stated property — a `return` added inside the body later cannot bypass the HV clear — is structural and this edit must not break it."
    - "`FakeChip.check_eprom_blank` (`fake_chip.py:205-211`) is not touched. It models the STANDALONE blank-check command, whose behaviour BLANK-02 freezes."
    - "No key is added to `database.py`'s `convert_to_programmer`, and no host-side source-scanning parity gate is created."
    - "`tests/golden/protocol_branch_inventory.json` is never hand-edited to make a surprise disappear. Its `meta.how_to_update` names diffing the extractor's live output as the only sanctioned way to update it."
    - "No line is inserted above `eprom.cpp:70`, including a comment."
  flagged_assumptions:
    - "BLANK-01 edge probe, category `unclassified` — UNRESOLVED. The deterministic probe returned `unclassified — review manually` for BLANK-01 and is therefore not auto-resolved and not auto-dismissed. Reviewed in-prompt: the reviewable reading is whether `the region being written` is half-open or inclusive of its end byte, and what a zero-length payload means. This plan treats the region as half-open `[address, address + payload_length)` with the wire carrying the absolute EXCLUSIVE end. That creates one degenerate collision the requirement does not address: a zero-byte payload at address 0 would compute `region-end == 0`, which D-04 reads as absent and therefore as the WHOLE DEVICE. The plan closes it deliberately by emitting the key only when `region_length` is greater than zero, so the degenerate case falls back to the strictest existing behaviour explicitly rather than by numeric coincidence — but the requirement text does not say that a zero-length write should be whole-device-checked, and that reading is carried forward rather than silently decided."
---

<objective>
One region end travels from the host's command dict, over the wire, through the parser, into a
region-scoped blank check — and a write into a blank region of a non-blank part succeeds.

This is the phase's tracer: the thinnest real path that touches every layer the phase modifies,
wired end to end, carrying the regression test plan 201-02 left RED as its runnable proof. D-02 is
why the end must arrive from the host at all: at the moment write-init calls the blank check, the
only region information the firmware holds is `handle->address`. It is
production-quality, not a prototype. Nothing in it is stubbed, and the `<verify>` that proves it
is the same `pio test` CI runs.

Task 1 is that slice. Task 2 expands it one step: it teaches the host's fake chip the region so
the host-side `write -a` regression leg stops passing vacuously, and pins the emission on both
`write` and `verify`.

The two firmware decisions this plan settles, both left open by CONTEXT.md and both resolved on
evidence: the `0 = absent` fallback and the fail-closed clamp live in ONE helper in `memory.cpp`,
so no new ternary row enters the `eprom.cpp` branch-inventory golden; and the region call replaces
the whole-device call in place, adding no `return` to a body whose single-exit property is
structural.

Purpose: close BLANK-01 in software, on the agent's best early-context tokens, with the
architecture proven end to end before anything expands on it.
Output: `mem_util_blank_check_region`, `mem_util_operation_end`, a host `region_length` parameter,
a region-aware fake, and a GREEN D-16.1.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-CONTEXT.md
@.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-RESEARCH.md
@.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-PATTERNS.md
@.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-02-SUMMARY.md
@CLAUDE.md
@firestarter_fw/CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — a region end from the host dict to a region-scoped blank check, one path only</name>
  <files>firestarter_app/firestarter/eprom_operations.py, firestarter_fw/include/memory_utils.h, firestarter_fw/src/proms/memory.cpp, firestarter_fw/src/proms/eprom.cpp, firestarter_fw/tests/golden/protocol_branch_inventory.json</files>
  <precondition>Both `firestarter_fw` and `firestarter_app` are checked out on branch `v1.40-program-parameter-fidelity` with non-empty working trees, and `git` is on PATH — `test_protocol_branch_inventory.py`'s `_resolve_git` fails closed and never skips if `git` is absent.</precondition>
  <read_first>
    - `firestarter_fw/src/proms/memory.cpp:420-515` in full — the `blank_check_saved_address` static at `:437` with its rationale at `:425-436`, `BLANK_CHECK_CHUNK_SIZE` at `:442`, the whole `mem_util_blank_check` body `:450-513` including the entry/resume branch, the `handle->cmd == CMD_BLANK_CHECK` emit fork at `:480`, the cursor advance at `:493`, the `RAW_DATA_PROGRESS` fork, the clamp at `:501-502` and the emit at `:509-510`.
    - `firestarter_fw/include/memory_utils.h:10-30` — the declaration of `mem_util_blank_check` at `:18` and the block comment above `memory_verify_execute` at `:19-22`, which is the house pattern for a newly-exposed internal: declaration plus a comment saying why it is exposed and where it is defined.
    - `firestarter_fw/src/proms/eprom.cpp:115-160` — the defensive-wrapper comment at `:120-128`, `eprom_internal_write_init_body` at `:129-147` and the single-exit `eprom_write_init` at `:152-158`.
    - `firestarter_fw/tests/test_protocol_branch_inventory.py` — `_extract_predicates`, `_is_relevant` at `:266-274`, the ternary arm at `:322-349`, `test_blob_shas_match_the_recorded_inventory` at `:392-410`, `test_branch_sites_match_the_recorded_inventory` at `:413-441`, `test_exactly_one_protocol_keyed_site_at_the_pinned_line` at `:444-454` and `test_inventory_is_non_vacuous` at `:456-473`.
    - `firestarter_fw/tests/golden/protocol_branch_inventory.json` — `meta.how_to_update`, `meta.recorded_by`, `meta.blob_shas`, and the `counts` block, which currently reads 21/1/20 while `sites` holds 22 entries.
    - `firestarter_app/firestarter/eprom_operations.py:456-500` — `_setup_operation`'s signature and the `COMMAND_READ` narrowing at `:490-495`; `:516-540` — `_operation_context`'s signature and its positional forwarding; `:1968-2040` — `write_eprom` through `_operation_context` at `:2014`; `:2107-2125` — `verify_eprom`.
    - `firestarter_app/firestarter/page_size_gate.py:136-175` — `require_page_alignment`'s pre-connect `parse_address` plus `os.path.getsize`, and the early return at `:157-159` for non-0x05 parts.
    - `.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-RESEARCH.md` sections "The multi-call stability constraint", "Caller census", "G-2" (including the runnable re-derivation script and its four obligations), "The minimal change — no restructure", and "Open Questions" item 1.
    - `.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-PATTERNS.md` sections "`include/memory_utils.h`", "`src/proms/memory.cpp`", "`src/proms/eprom.cpp:145`" and "`firestarter/eprom_operations.py`".
  </read_first>
  <behavior>
    - `test_write_init_accepts_blank_region_on_non_blank_part` (written RED in plan 201-02) turns GREEN: `mem_size = 16384`, `address = 8192`, `region_end = 12288`, one non-blank byte at `0x10`, and `response_code != RESPONSE_CODE_ERROR` after the init loop converges.
    - `test_write_init_still_refuses_when_target_region_is_non_blank` stays GREEN: the same fixture with the non-blank byte at `0x2400` still ends in `RESPONSE_CODE_ERROR`.
    - `test_blank_check_resumes_across_chunks_and_restores_the_cursor` stays GREEN: the wrapper passes `(0, handle->mem_size)` so the standalone command's cursor arithmetic and restore are untouched.
    - `test_erase_end_blank_check_scans_from_zero` stays GREEN: the erase-end function pointer still targets the whole-device wrapper.
    - `mem_util_operation_end` returns `mem_size` for `region_end == 0`, returns `mem_size` for `region_end > mem_size`, and returns `region_end` for `0 < region_end <= mem_size`.
  </behavior>
  <action>
    **Before editing anything**, capture both base commits so every verify leg pins an explicit
    range rather than a relative `HEAD~N` anchor:
    `mkdir -p /tmp/gsd-201 && git -C /workspaces/firestarter_fw rev-parse HEAD > /tmp/gsd-201/03-t1-fw.base && git -C /workspaces/firestarter_app rev-parse HEAD > /tmp/gsd-201/03-t1-app.base`.

    Wire ONE path through every layer. No other call site, no batching, real error handling on the
    single path.

    **Firmware, `include/memory_utils.h`.** Declare two new functions beside
    `mem_util_blank_check`, each with a block comment in the house shape already used above
    `memory_verify_execute` — what it is, why it is exposed, and where it is defined:
      - `uint32_t mem_util_operation_end(const firestarter_handle_t* handle);`
      - `void mem_util_blank_check_region(firestarter_handle_t* handle, uint32_t start, uint32_t end);`
    State in `mem_util_blank_check_region`'s comment that the plain `mem_util_blank_check` is now a
    wrapper over it passing `(0, handle->mem_size)`, so the two cannot drift. State in
    `mem_util_operation_end`'s comment that it is the single resolution point for D-04's
    `0 = absent = whole device` and for the fail-closed clamp, that it is deliberately a function
    rather than an inline ternary because a ternary written inside `eprom.cpp` adds a row to the
    branch-inventory golden, and that `eprom_operations.cpp` and `eprom.cpp` both call it.

    **Firmware, `src/proms/memory.cpp`.** Define `mem_util_operation_end` first:
    return `handle->mem_size` when `handle->region_end` is 0 OR when `handle->region_end` exceeds
    `handle->mem_size`, otherwise return `handle->region_end`. The second clause is the
    fail-closed clamp the security analysis requires — do not rely on the host being correct, and
    do not use a bare ternary that would drop it.

    Then split `mem_util_blank_check`. Rename the existing body to
    `mem_util_blank_check_region(firestarter_handle_t* handle, uint32_t start, uint32_t end)` and
    make exactly five substitutions inside it, leaving everything else byte for byte:
      - `:454` `handle->address = 0;` becomes `handle->address = start;`
      - `:456` `handle->address >= handle->mem_size` becomes `handle->address >= end`
      - `:465` the loop's second bound `i < handle->mem_size` becomes `i < end`
      - `:501-502` the clamp `if (handle->address > handle->mem_size) handle->address = handle->mem_size;` becomes `end`-relative
      - `:510` the progress denominator `handle->mem_size` becomes `end`
    `:453` (`blank_check_saved_address = handle->address;`) stays exactly as it is: on the
    write-init path the saved address and the region start are the same value, which is what makes
    the region form need no new state. The `handle->cmd == CMD_BLANK_CHECK` emit fork at `:480` is
    untouched. Add no second cross-call static and no heap allocation — `memory.cpp:425-436`
    carries the written rationale for the one static that already exists.

    Then define the wrapper, one line plus a comment:
    `void mem_util_blank_check(firestarter_handle_t* handle) { mem_util_blank_check_region(handle, 0, handle->mem_size); }`
    The comment states that this is the whole-device entry point, that six function-pointer
    assignments and two direct calls in other protocol files target it, and that plan 201-05's
    source-contract gate enumerates all of them.

    **Firmware, `src/proms/eprom.cpp`.** At `:145`, inside the existing
    `if (!is_flag_set(FLAG_SKIP_BLANK_CHECK))` guard, replace the plain whole-device call with
    `mem_util_blank_check_region(handle, handle->address, mem_util_operation_end(handle));`
    Add no `return`, no early exit and no new brace level: the single-exit wrapper at `:152-158`
    must still see every exit, and its comment states that property is structural. Passing
    `handle->address` as `start` is correct because the region form reads `start` only inside the
    `!is_operation_in_progress` branch, which runs on entry 1 only — on entries 2..n
    `handle->address` is the scan cursor and `start` is ignored. **Insert no line above
    `eprom.cpp:70`**, including a comment: `test_exactly_one_protocol_keyed_site_at_the_pinned_line`
    hard-codes `[70]`.

    **Firmware, the golden, in this same commit.** Re-derive
    `tests/golden/protocol_branch_inventory.json` using the module's own `_extract_predicates` —
    RESEARCH.md § G-2 carries the runnable script. Honour its four obligations: give every NEW
    site a hand-written non-empty `reason` (the script flags them as `TODO`); keep the site count
    at or above the floor of 21; confirm `protocol_lines` is still `[70]`; take
    `meta.blob_shas["src/proms/eprom.cpp"]` from `git hash-object src/proms/eprom.cpp` on the
    working tree BEFORE staging, and set `meta.recorded_at_head` to the PARENT commit. Also fix
    the `counts` block, which currently reads 21/1/20 while `sites` holds 22 entries — unasserted
    drift, corrected for honesty while you are in the file. Append a paragraph to
    `meta.recorded_by` stating what moved and why. If the re-derivation reports any new site you
    did not expect, stop and investigate rather than writing a placeholder reason: written as
    prescribed, this edit should shift line numbers only and add no predicate.

    **Host, `firestarter/eprom_operations.py`.** Three signatures, one new keyword, no
    control-flow change:
      1. `_setup_operation` gains a TRAILING keyword parameter `region_length: int | None = None`.
         After the `COMMAND_READ` block at `:490-495`, add the emit:
         when `region_length` is not `None` **and greater than zero** and `cmd` is one of
         `COMMAND_WRITE` or `COMMAND_VERIFY`, set `command_dict[JSON_KEY_REGION_END]` to
         `addr + region_length`. Guard the command explicitly; both constants are already imported
         at `:40-41`. The `greater than zero` clause is deliberate: a zero-length payload at
         address 0 would compute an end of 0, which D-04 reads as absent, and emitting nothing is
         the same outcome reached explicitly instead of by numeric coincidence.
      2. `_operation_context` gains the same trailing `region_length: int | None = None` parameter
         and forwards it BY KEYWORD. It currently forwards its leading six parameters positionally
         and only `fault_inject_outgoing` by name (`:516-540`), so the new parameter must not be
         inserted among the positional six.
      3. `write_eprom` (the `_operation_context` call at `:2014`) and `verify_eprom` (at `:2115`)
         each pass `region_length=`. Compute it with a guarded
         `try: os.path.getsize(input_file_path) except OSError: None`. The guard preserves today's
         error ordering exactly: on a non-0x05 write a missing file currently first surfaces at
         `_main_phase_send_data:750`, after connecting, because `require_page_alignment` returns
         early at `:157-159` for those parts. An unguarded `getsize` would move that failure
         earlier for some parts and change behaviour this plan is not authorised to change.

    Commit in `firestarter_fw` and in `firestarter_app` on `v1.40-program-parameter-fidelity` as a
    commit PAIR. The firmware commit MUST carry the golden re-derivation together with the
    `eprom.cpp` edit — `meta.recorded_at_head` is only meaningful if they are one commit. Name
    BLANK-01, D-05, D-09, D-10 and RESEARCH Open Question 1's resolution in the messages.
  </action>
  <verify>
    <automated>git -C /workspaces/firestarter_fw rev-parse --abbrev-ref HEAD && git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD</automated>
    <fails_when>either line prints anything other than `v1.40-program-parameter-fidelity`</fails_when>
    <automated>cd /workspaces/firestarter_fw && OUT=$(pio test -e native_nodevtools -f native/avr/test_val_eprom 2>&1) && printf '%s\n' "$OUT" | tail -25 && printf '%s\n' "$OUT" | /usr/bin/grep -q 'test_write_init_accepts_blank_region_on_non_blank_part.*PASS' && printf '%s\n' "$OUT" | /usr/bin/grep -q 'test_write_init_still_refuses_when_target_region_is_non_blank.*PASS'</automated>
    <fails_when>non-zero exit, with the last 25 lines printed immediately above it. The D-16.1 case not reporting PASS means the region is not reaching the scan; the negative control not reporting PASS means the check was widened or removed rather than scoped, which would silently overwrite programmed bytes on a UV part</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio test -e native 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or the final summary line does not report at least `237 succeeded` with `0 failed` — the whole DEV_TOOLS native suite, which CI runs only on pull requests</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio test -e native_nodevtools 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or the final summary line does not report at least `237 succeeded` with `0 failed`</fails_when>
    <automated>mkdir -p /tmp/no-meta && cd /workspaces/firestarter_fw && FIRESTARTER_META_ROOT=/tmp/no-meta python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or the summary line does not read `269 passed, 32 skipped` or higher with zero failures. A failure in `test_protocol_branch_inventory.py` means the golden was not re-derived in this commit; a failure in `test_write_path_source_contract_v131.py` means the `memory.cpp` split disturbed something its Coverage 6/7/9 legs scan for</fails_when>
    <automated>cd /workspaces/firestarter_fw && python3 -c "
import json,subprocess,sys
inv=json.load(open('tests/golden/protocol_branch_inventory.json'))
live=subprocess.run(['git','hash-object','src/proms/eprom.cpp'],capture_output=True,text=True).stdout.strip()
rec=inv['meta']['blob_shas']['src/proms/eprom.cpp']
n=len(inv['sites']); c=inv['counts']['total_sites']
bad=[s for s in inv['sites'] if not s.get('reason') or str(s['reason']).startswith('TODO')]
print('blob live',live,'recorded',rec,'sites',n,'counts.total',c,'todo_reasons',len(bad))
sys.exit(0 if (live==rec and n>=21 and c==n and not bad) else 1)"</automated>
    <fails_when>non-zero exit, with the five values printed immediately above it. `blob live` differing from `recorded` means the golden was re-derived against a different tree state than the one committed; `counts.total` differing from `sites` means the drift was not corrected; a non-zero `todo_reasons` means a new inventory site was left with the script's placeholder instead of a hand-written reason</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/bin/grep -v '^\s*\*' src/proms/eprom.cpp | /usr/bin/grep -v '^\s*//' | /usr/bin/grep -v '^\s*/\*' | /usr/bin/grep -c 'mem_util_blank_check(handle)' | /usr/bin/grep -qx 0</automated>
    <fails_when>non-zero exit — a whole-device blank-check call survives in code (not comment) in `eprom.cpp`, meaning the write-init call site was added rather than replaced. Comment lines are filtered first so prose about the old form cannot invalidate the count</fails_when>
    <automated>cd /workspaces/firestarter_fw && python3 -c "
import re,sys
s=open('src/proms/eprom.cpp').read()
s=re.sub(r'/\*.*?\*/','',s,flags=re.S); s=re.sub(r'//[^\n]*','',s)
n=s.count('mem_util_blank_check'+'_region(')
print('region_form_calls_in_eprom_cpp =', n)
sys.exit(0 if n==1 else 1)"</automated>
    <fails_when>non-zero exit, with the count printed immediately above it — `eprom.cpp` does not contain exactly one region-form call after comment stripping. Zero means the edit did not land; more than one means the relaxation reached a second site in the same file</fails_when>
    <automated>cd /workspaces/firestarter_fw && BASE=$(cat /tmp/gsd-201/03-t1-fw.base) && test -n "$BASE" && CHANGED=$(git diff --name-only "$BASE"..HEAD -- src/proms/flash_intel.cpp src/proms/flash_nor_unlock.cpp src/proms/flash_5v_page.cpp src/proms/eeprom_28c.cpp) && printf 'out_of_scope_changes=[%s]\n' "$CHANGED" && test -z "$CHANGED" && printf 'OUT_OF_SCOPE_PROTOCOLS_UNTOUCHED\n'</automated>
    <fails_when>the confirmation line is absent, or non-zero exit — one of the four out-of-scope protocol files changed, which widens the relaxation past D-10's boundary onto silicon this phase never benches</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio run 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or fewer than three `SUCCESS` environment lines for uno, uno328pb and leonardo</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio run 2>&1 | /usr/bin/grep -E 'Flash:.*leonardo|^Building in|Flash:' | tail -6</automated>
    <fails_when>no `Flash:` line is printed. This leg is a measurement, not a threshold: leonardo's real Caterina ceiling is 28672 while `platformio.ini:78-82` overrides `board_upload.maximum_size` to 32768, so the linker will NOT fail if the image passes 28672 — it silently overwrites the bootloader. The baseline was 23816 B and the figure must be recorded in the SUMMARY.</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_eprom_operations.py tests/test_wire_dict_equivalence.py tests/test_chip_test_uv_slot_write.py -o addopts="" -q -p no:randomly 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or the summary line does not match `^[0-9]+ passed`. A failure in `test_wire_dict_equivalence.py` means the key reached `convert_to_programmer`; a failure in `test_chip_test_uv_slot_write.py` means the host threading disturbed the already-shipped half (b) of 999.44</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m ruff check firestarter/ tests/ && .venv/ci-replica/bin/python -m ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit — lint output or a reformat-needed listing; both are CI gate steps at `ci.yml:57-61`</fails_when>
    <automated>cd /workspaces/firestarter_app && BASE=$(cat /tmp/gsd-201/03-t1-app.base) && test -n "$BASE" && CHANGED=$(git diff --name-only "$BASE"..HEAD -- firestarter/database.py firestarter/serial_comm.py firestarter/chip_test.py) && printf 'forbidden_changes=[%s]\n' "$CHANGED" && test -z "$CHANGED"</automated>
    <fails_when>exit code 1 — one of the three forbidden host files changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `include/memory_utils.h` declares `uint32_t mem_util_operation_end(const firestarter_handle_t* handle);` and `void mem_util_blank_check_region(firestarter_handle_t* handle, uint32_t start, uint32_t end);`, each with a block comment naming where it is defined and why it is exposed.
    - `mem_util_operation_end` returns `handle->mem_size` both when `handle->region_end` is 0 and when it exceeds `handle->mem_size`; it is defined exactly once, in `src/proms/memory.cpp`.
    - `src/proms/memory.cpp` contains `mem_util_blank_check_region` with a `start`/`end` signature, and `mem_util_blank_check` reduced to a single call passing `0` and `handle->mem_size`.
    - Inside the region form, the completion test, the scan upper bound, the clamp and the progress denominator all read `end`; the line assigning `blank_check_saved_address` is textually unchanged; the `handle->cmd == CMD_BLANK_CHECK` emit fork is textually unchanged.
    - `src/proms/memory.cpp` declares no new file-scope `static` and calls no allocation function.
    - `src/proms/eprom.cpp` contains exactly one `mem_util_blank_check_region(` call and zero comment-stripped occurrences of the plain whole-device call form.
    - `src/proms/eprom.cpp` gains no `return` statement inside `eprom_internal_write_init_body` and no line above line 70.
    - `tests/golden/protocol_branch_inventory.json`: `meta.blob_shas["src/proms/eprom.cpp"]` equals `git hash-object src/proms/eprom.cpp` on the committed tree, `counts.total_sites` equals `len(sites)`, `len(sites)` is at least 21, every site carries a non-empty `reason` with none starting `TODO`, and `meta.recorded_by` gained a paragraph.
    - The golden re-derivation and the `eprom.cpp` edit are in the SAME firmware commit.
    - `pio test -e native` and `pio test -e native_nodevtools` both report at least 237 succeeded, 0 failed, with `test_write_init_accepts_blank_region_on_non_blank_part` PASSing.
    - `FIRESTARTER_META_ROOT=/tmp/no-meta pytest tests/` reports at least 269 passed, 32 skipped, 0 failed.
    - `firestarter/eprom_operations.py`: `_setup_operation` and `_operation_context` each have `region_length` as their last parameter with default `None`; `_operation_context` forwards it by keyword; `write_eprom` and `verify_eprom` each pass it from a guarded `os.path.getsize`.
    - The emit is guarded on `cmd` being `COMMAND_WRITE` or `COMMAND_VERIFY` and on `region_length` being greater than zero.
    - The diff from `/tmp/gsd-201/03-t1-fw.base` to HEAD inside `firestarter_fw` lists no file under `src/proms/` other than `eprom.cpp` and `memory.cpp`.
    - The leonardo flash figure from `pio run` is recorded in the SUMMARY alongside the 23816 B baseline.
  </acceptance_criteria>
  <done>
    A region end travels from `_setup_operation` through the wire into a region-scoped blank check,
    and a write into a blank region of a non-blank part is accepted while a write into a non-blank
    region is still refused. BLANK-01 is closed in software, proven end to end by the test that
    was red one commit ago.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teach the host fake the region so the `write -a` regression leg stops passing vacuously</name>
  <files>firestarter_app/tests/fake_chip.py, firestarter_app/tests/test_eprom_operations.py</files>
  <read_first>
    - `firestarter_app/tests/fake_chip.py:130-165` — `FakeChip.write_eprom`, which already does the region arithmetic: `start = _parse_addr_or_size(address_str) or 0` at `:147`, `end = start + len(incoming)`, the bounds check, and the `self.data[start:end] = incoming` slice at `:153-157`.
    - `firestarter_app/tests/fake_chip.py:200-215` — `FakeChip.check_eprom_blank`, which models the STANDALONE command and must not be touched.
    - `firestarter_app/tests/fake_chip.py:243-290` — `WriteInitPreflightChip`, `_is_blank` at `:265-268`, and the refusal gate at `:282-287` including the `MSG_ERR_NOT_BLANK` code and the exact error string it fabricates.
    - `firestarter_app/tests/test_chip_test_uv_slot_write.py` at `:81, :104, :112, :276, :324, :413, :417` — the six references to `WriteInitPreflightChip`, to confirm none of them asserts on a whole-device refusal that a default-argument widening would change.
    - `firestarter_app/tests/test_eprom_operations.py:260-330` — `_make_captured_setup_operation` at `:263-281`, `test_read_timing_settling_key_constant` at `:283-296` and `test_read_timing_settling_emitted_in_command` at `:314-320`. Note the existing capture records `eprom_data_dict`, the INPUT; this task must assert on the emitted `command_dict`, the OUTPUT.
    - `firestarter_app/firestarter/eprom_operations.py` `_setup_operation` and `_operation_context` as left by Task 1.
    - `.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-RESEARCH.md` sections "G-5" and "G-6".
    - `.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-PATTERNS.md` sections "`tests/fake_chip.py`" and "`tests/test_eprom_operations.py`".
  </read_first>
  <behavior>
    - `WriteInitPreflightChip._is_blank(start=0, end=None)` returns `blank_override` when it is not `None`, otherwise compares `bytes(self.data[start:end])` against `b"\xff" * (end - start)` with `end` defaulting to `self.memory_size`. Every existing no-argument call keeps its current result.
    - `test_write_into_blank_region_of_non_blank_part_succeeds`: a `WriteInitPreflightChip` whose low addresses hold data and whose target region is `0xFF`, written with an `--address` inside the blank region, returns success.
    - `test_write_into_non_blank_region_is_still_refused`: the same fake with data seeded INSIDE the target region returns failure with `MSG_ERR_NOT_BLANK`.
    - `test_region_end_emitted_on_write`: the dict `_setup_operation` returns for `COMMAND_WRITE` contains `"region-end"` equal to `address + payload_length`.
    - `test_region_end_emitted_on_verify`: the same for `COMMAND_VERIFY`.
    - `test_region_end_absent_for_read`: the dict returned for `COMMAND_READ` contains no `"region-end"` key, so the read path's existing `memory-size` narrowing is undisturbed.
  </behavior>
  <action>
    Widen the fake by default argument, additively, so no existing caller changes behaviour.

    In `tests/fake_chip.py`, change `WriteInitPreflightChip._is_blank` to take
    `start: int = 0, end: int | None = None`, keep the `blank_override` early return as the FIRST
    check so every existing caller is unaffected, resolve `end` to `self.memory_size` when it is
    `None`, and compare `bytes(self.data[start:end])` against `b"\xff" * (end - start)`. Then have
    the caller at `:282` compute `start` and `end` the way the base class already does at
    `:147-157` — `start = _parse_addr_or_size(address_str) or 0` and
    `end = start + os.path.getsize(input_file_path)` — and pass them. Leave the
    `MSG_ERR_NOT_BLANK` code and the fabricated error string exactly as they are; the refusal must
    still look identical to a caller, only its scope changes.

    **Do not touch `FakeChip.check_eprom_blank` at `:205-211`.** It models the standalone
    blank-check command and BLANK-02 freezes that behaviour. Its whole-buffer comparison is
    correct and must stay.

    Then write five legs in `tests/test_eprom_operations.py`.

    The two product legs, `test_write_into_blank_region_of_non_blank_part_succeeds` and
    `test_write_into_non_blank_region_is_still_refused`, drive the fake through a `write` with an
    `--address` argument. Construct the fake with a memory size larger than the payload, seed
    non-0xFF bytes at low addresses for the first leg and inside the target region for the second,
    and assert on the returned success flag and — for the refusal — on
    `last_firmware_error_code` being `MSG_ERR_NOT_BLANK`. Put a docstring on the first leg stating
    plainly that before the fake learned the region this leg passed vacuously, because
    `_is_blank` compared the whole buffer and modelled the whole-device check exactly.

    The three emission legs use the `_make_captured_setup_operation` idiom at `:263-281` — the
    `*args, **kwargs` passthrough and the `return None, 0` early-exit shape — but capture the
    OUTPUT rather than the input. Wrap the real `_setup_operation` so the returned `command_dict`
    is recorded, or assert at the serial layer; the existing helper records `eprom_data_dict`,
    which is the input and would prove nothing here. Assert `"region-end"` is present and equals
    `address + payload_length` for `COMMAND_WRITE` and for `COMMAND_VERIFY` (D-05 and D-07), and
    assert it is ABSENT for `COMMAND_READ`, which is the guard that keeps the read path's
    `memory-size` narrowing at `:495` untouched. Use the production constant
    `JSON_KEY_REGION_END` in the assertions, not a bare string, except in
    `test_region_end_key_constant` from plan 201-02 which is the one place the literal is pinned.

    These are data-driven tests that exercise production code. Write no source-scanning or
    AST-introspecting leg: operator ruling `088d2b7` covers both, and it deleted 24 such modules
    including the `json_key_parity` gate D-16.3 literally describes.

    Commit inside `firestarter_app` on `v1.40-program-parameter-fidelity`, naming BLANK-03, D-16.2
    and G-5, and stating that the fake's whole-device model is what made the leg vacuous. Then
    advance the meta-repository gitlink for both submodules and commit in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_eprom_operations.py -o addopts="" -q -p no:randomly 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or the summary line does not match `^[0-9]+ passed`</fails_when>
    <automated>cd /workspaces/firestarter_app && cp tests/fake_chip.py /tmp/fake_chip.bak && sed -i 's/def _is_blank(self, start: int = 0, end: int | None = None)/def _is_blank(self, start: int = 0, end: int | None = 0)/' tests/fake_chip.py && .venv/ci-replica/bin/python -m pytest tests/test_eprom_operations.py -o addopts="" -q -p no:randomly > /tmp/planted.out 2>&1; RC=$?; cp /tmp/fake_chip.bak tests/fake_chip.py; printf 'planted_rc=%s\n' "$RC"; tail -3 /tmp/planted.out; git diff --quiet -- tests/fake_chip.py && printf 'RESTORED\n' && test "$RC" -ne 0</automated>
    <fails_when>the line `RESTORED` is absent (the planted mutation was not reverted and must be reverted by hand before proceeding), or `planted_rc=0` (the suite passed with a deliberately broken default, which means the new legs never exercise the region path and are vacuous)</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_chip_test_uv_slot_write.py tests/test_uv_mask.py tests/test_wire_dict_equivalence.py -o addopts="" -q -p no:randomly 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or the summary line does not match `^[0-9]+ passed`. `test_chip_test_uv_slot_write.py` is the real coupled consumer of the fake — six references — and a default-argument widening should leave all six untouched</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or the summary line does not report at least `2105 passed` with zero failures — the measured pre-phase baseline was 2100 and this phase adds six host legs across plans 201-02 and 201-03</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m ruff check firestarter/ tests/ && .venv/ci-replica/bin/python -m ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit — lint output or a reformat-needed listing</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70 2>&1 | tail -4</automated>
    <fails_when>non-zero exit, or a `FAIL Required test coverage of 70% not reached` line — the coverage gate CI runs verbatim at `ci.yml:63-64`</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import re,sys
s=open('tests/fake_chip.py').read()
i=s.find('def check_eprom_blank')
seg=s[i:i+600]
ok='memory_size' in seg and 'start' not in seg.split('def ')[1][:400]
print(seg.splitlines()[0] if i>0 else 'NOT FOUND')
sys.exit(0 if i>0 else 1)"</automated>
    <fails_when>non-zero exit, or the printed line is `NOT FOUND` — `FakeChip.check_eprom_blank` was removed or renamed. It models the standalone blank-check command whose behaviour BLANK-02 freezes</fails_when>
  </verify>
  <acceptance_criteria>
    - `WriteInitPreflightChip._is_blank` has the signature `(self, start: int = 0, end: int | None = None)` and its `blank_override` early return is still the first statement in the body.
    - `WriteInitPreflightChip.write_eprom` computes `start` and `end` from the address argument and the payload size and passes both to `_is_blank`.
    - `FakeChip.check_eprom_blank` is textually unchanged.
    - `tests/test_eprom_operations.py` contains `test_write_into_blank_region_of_non_blank_part_succeeds`, `test_write_into_non_blank_region_is_still_refused`, `test_region_end_emitted_on_write`, `test_region_end_emitted_on_verify` and `test_region_end_absent_for_read`.
    - The two emission legs assert the value equals `address + payload_length`, not merely that the key exists.
    - `test_region_end_absent_for_read` asserts the key is absent for `COMMAND_READ`.
    - The planted-mutation leg exits non-zero with the mutation applied and the file is restored, proving the new legs are non-vacuous.
    - `tests/test_chip_test_uv_slot_write.py` passes with none of its six `WriteInitPreflightChip` sites edited.
    - The full host suite reports at least 2105 passed with zero failures on `.venv/ci-replica/bin/python`.
    - `ruff check`, `ruff format --check` and the 70% coverage gate all pass.
    - No test in this plan reads a source file or introspects an AST.
  </acceptance_criteria>
  <done>
    The host-side `write -a` regression leg exercises the region rather than the whole buffer, a
    non-blank target region is still refused, and the wire key is pinned as emitted on both
    `write` and `verify` and absent on `read`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → firmware (JSON command frame) | An externally-supplied absolute end address now bounds a scan that gates a high-voltage write |
| firmware → socket | `eprom_generic_init` raises VPP before the blank check; `eeprom28c_check_chip_id` raises 12 V on A9 at `mem_size - 64` on a different protocol's write-init path |
| test double → host product code | `WriteInitPreflightChip` decides whether a host regression leg proves anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-201-03-01 | Tampering | `region_end` larger than the device | high | mitigate | `mem_util_operation_end` clamps to `mem_size` in the single resolution point, so a host or a corrupted frame cannot widen a scan past the device; `_process_incoming_data`'s `MSG_ERR_OUT_OF_RANGE` is the second gate |
| T-201-03-02 | Tampering | 12 V on A9 at an arbitrary address | critical | mitigate | Avoided by construction and by prohibition: `handle->mem_size` is not narrowed anywhere, so `eeprom28c_check_chip_id`'s `mfr_addr = mem_size - 64` derivation (`eeprom_28c.cpp:190`, reached from `eeprom28c_write_init` at `:343`) still targets the real top of the device. A verify leg asserts the four out-of-scope protocol files are untouched |
| T-201-03-03 | Elevation of Privilege | the relaxation escaping to other protocols | high | mitigate | Exactly one region-form call in `eprom.cpp`, asserted by count; the four other protocol files asserted unchanged; plan 201-05's source-contract gate enumerates all nine reference sites permanently |
| T-201-03-04 | Tampering | the refusal being removed rather than scoped | critical | mitigate | The paired negative control `test_write_init_still_refuses_when_target_region_is_non_blank` must PASS, and the host leg `test_write_into_non_blank_region_is_still_refused` asserts `MSG_ERR_NOT_BLANK`. A programmed bit on a UV part cannot be un-programmed, so a silently dropped refusal is irreversible data loss |
| T-201-03-05 | Repudiation | the branch-inventory golden drifting from the source it pins | medium | mitigate | The re-derivation lands in the same commit; a verify leg compares `meta.blob_shas` against `git hash-object` on the committed tree and rejects placeholder reasons |
| T-201-03-06 | Spoofing | a vacuous host regression leg | high | mitigate | A planted mutation on the fake's default argument must drive the suite non-zero, and the file is restored and proved restored in the same command |
| T-201-03-07 | Denial of Service | leonardo flash past the Caterina ceiling | medium | accept | Measured, not gated: `platformio.ini:78-82` overrides `board_upload.maximum_size` to 32768 against a real 28672 ceiling, so the linker will not catch it. Baseline 23816 B leaves 4856 B; the post-change figure is recorded in the SUMMARY rather than enforced, because no size gate exists in CI |
| T-201-03-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package |
</threat_model>

## Artifacts this phase produces (plan 201-03)

**New firmware symbols**
- `mem_util_operation_end(const firestarter_handle_t* handle)` — declared in `include/memory_utils.h`, defined in `src/proms/memory.cpp`; the single resolution point for D-04's absent-semantics and the fail-closed clamp
- `mem_util_blank_check_region(firestarter_handle_t* handle, uint32_t start, uint32_t end)` — declared in `include/memory_utils.h`, defined in `src/proms/memory.cpp`

**Changed firmware symbols**
- `mem_util_blank_check` becomes a one-line whole-device wrapper over the region form
- the call at `src/proms/eprom.cpp:145` becomes the region form

**New host symbols**
- `region_length` — trailing keyword parameter on `_setup_operation` and `_operation_context`
- `test_write_into_blank_region_of_non_blank_part_succeeds`
- `test_write_into_non_blank_region_is_still_refused`
- `test_region_end_emitted_on_write`
- `test_region_end_emitted_on_verify`
- `test_region_end_absent_for_read`

**Changed host symbols**
- `WriteInitPreflightChip._is_blank` gains `start` and `end` default arguments

**Regenerated artifact**
- `firestarter_fw/tests/golden/protocol_branch_inventory.json` — re-derived from `_extract_predicates`, `counts` drift corrected, `meta.recorded_by` appended

**Created by other plans in this phase (listed so drift verification excludes them):** `region-end`, `JSON_KEY_REGION_END`, `key_region_end`, `handle->region_end`, `op_end`, `val_shadow_seed`, `val_shadow_enable`, `val_shadow_reset`, `make_region_handle`, `tests/test_blank_check_region_source_contract.py`, `201-BENCH-RECORD.md`.

<verification>
- `test_write_init_accepts_blank_region_on_non_blank_part` PASSes, having been seen RED at plan 201-02's commit.
- `test_write_init_still_refuses_when_target_region_is_non_blank` still PASSes.
- Both plan 201-01 characterization tests still PASS.
- `pio test -e native` and `-e native_nodevtools` both report at least 237 succeeded, 0 failed.
- `FIRESTARTER_META_ROOT=/tmp/no-meta pytest tests/` is at least 269 passed, 32 skipped.
- The golden's recorded blob sha equals `git hash-object src/proms/eprom.cpp`, `counts.total_sites` equals `len(sites)`, and no `reason` starts with `TODO`.
- `eprom.cpp` holds exactly one region-form call and zero comment-stripped whole-device calls.
- The four out-of-scope protocol files are byte-unchanged.
- The full host suite reports at least 2105 passed; ruff and the 70% coverage gate are clean.
- The planted-mutation leg drives the host suite non-zero and restores the file.
- The leonardo flash figure is recorded against the 23816 B baseline.
</verification>

<success_criteria>
A write into a blank region of a non-blank part succeeds, from the host's command dict through the
wire and the parser into a region-scoped blank check; a write into a non-blank region is still
refused; the standalone blank-check and erase-end callers are untouched; and the host regression
leg exercises the region rather than the whole buffer.
</success_criteria>

<output>
Create `.planning/phases/201-a-partial-write-is-gated-on-its-own-region/201-03-SUMMARY.md` when
done. Record the `pio test` summary lines for both envs, the D-16.1 case flipping from the RED
transcript in `201-02-SUMMARY.md` to PASS, the golden's new blob sha and `recorded_at_head`, the
`pio run` flash figure for leonardo against the 23816 B baseline, the host suite count, the
planted-mutation return code, and all three commit shas.
</output>
