---
phase: 196-adoption-instrument-disposition
plan: 02
type: execute
wave: 2
depends_on: ["196-01"]
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/seeds/SEED-claim-firestarter-slug.md
autonomous: true
requirements: [INSTR-02]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`CLAUDE.md` § Repository Structure no longer claims `tools/` holds two directories, no longer lists the retired instrument as one of them, and carries a retirement sentence on the shape of the `tools/wiki/` paragraph already in that section — naming the act, its date and the durable record's path. That is D-03, and it is not a silent removal."
    - "The retirement sentence in `CLAUDE.md` cites the act by date and by the note's path, never by a commit SHA. A short SHA written into tracked prose does not survive a rebase or a squash, and this branch has already absorbed a merge from `origin/beta`."
    - "`.planning/PROJECT.md` no longer says the instrument still runs and still reports. The two sentences before that remain true as history and are left alone, and the D-1 row further down the file — which is about the slug claim, not the instrument — is not touched, because it carries no reference to sweep."
    - "`.planning/REQUIREMENTS.md` INSTR-01 and its section heading no longer describe a live open choice between re-pointing and removing. That choice closed on 2026-09-17, which is D-10's reason for putting both in scope while leaving the closed v1.38 ROADMAP section alone."
    - "`.planning/ROADMAP.md` Phase 196 success criterion 1 no longer reads as an open either-or, and criterion 3 — whose antecedent, that the instrument is retained, is now false — says so in place rather than leaving a live criterion a verifier must either hunt a consumer for or mark unmet."
    - "The seed keeps `trigger_condition` and § \"Why the trigger is what it is\" verbatim and stays at its canonical path in `.planning/seeds/`, per D-14 and D-15. Exactly one annotation is appended inside the FIRED banner saying the instrument was retired on 2026-09-17 and pointing at the retirement note. The banner's force comes from quoting a live standard and showing 12.8% against it; rewriting the standard to past tense degrades it into a summary of itself."
    - "No live document edited by this plan names the retired artifact by path or by filename. Every one of them describes it instead, because a live document naming a path that no longer exists is itself the residual reference INSTR-02 forbids."
    - "The traceability rows for INSTR-01 and INSTR-02 still read `Pending` when this plan ends. They are flipped in Plan 196-03, after the gate is green and the graph is rebuilt — not before, because a requirement marked complete ahead of its proof is the failure mode this project has seen executors reach for."
    - "The sweep commit's scope assertion resolves an anchor SHA captured before the commit and proves it names a real commit. With no anchor, git rev-list --count $PRE_TASK_SHA..HEAD resolves to HEAD..HEAD, which prints 0 and exits 0 — a five-path scope assertion that has asserted nothing — so the leg aborts with ANCHOR-UNSET and exit 2 instead."
  artifacts:
    - path: "CLAUDE.md"
      provides: "The live repository-structure inventory, corrected: one `tools/` occupant, plus the retirement paragraph pointing at the record"
      contains: "adoption-instrument-retirement.md"
    - path: ".planning/seeds/SEED-claim-firestarter-slug.md"
      provides: "The frozen account of the whole episode, annotated with the instrument's disposition and otherwise unchanged"
      contains: "adoption-instrument-retirement.md"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/notes/adoption-instrument-retirement.md"
      via: "the retirement paragraph's path citation — the durable route from the inventory a reader consults to the reason they need"
      pattern: "\\.planning/notes/adoption-instrument-retirement\\.md"
    - from: ".planning/seeds/SEED-claim-firestarter-slug.md"
      to: ".planning/notes/adoption-instrument-retirement.md"
      via: "the one appended annotation line inside the FIRED banner, alongside the two annotation lines already there"
      pattern: "\\.planning/notes/adoption-instrument-retirement\\.md"
    - from: ".planning/PROJECT.md"
      to: ".planning/notes/adoption-instrument-retirement.md"
      via: "the reworded closing sentence of the third-item paragraph"
      pattern: "\\.planning/notes/adoption-instrument-retirement\\.md"
  prohibitions:
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT edit `.planning/STATE.md`, any file under `.planning/milestones/`, or the seed's `trigger_condition` and its \"Why the trigger is what it is\" section. Editing a frozen record so a later decision appears to have always held destroys the evidence that the decision was later. Those references are excluded from the proof precisely so that nobody is tempted to edit them into agreement."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT use a GSD verb to edit `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`. The requirements and roadmap verbs reformat the whole file, and the plan-progress verb overwrites this repository's phase block positionally — it has destroyed a phase name, its requirement IDs and its dependency line in one call. These are hand edits. The default executor instinct is to reach for the verb, so the prohibition has to be written down rather than assumed."
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT write a commit SHA, a placeholder SHA, or the words `commit TBD` into any tracked prose in this plan. The act this phase performs is cited by date; the durable pointer is the note's path."
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT flip either INSTR traceability row to `Complete` in this plan. The proof those rows assert has not been run yet."
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT weaken, narrow or re-scope the INSTR-02 proof expression to reach green. If a live document genuinely needs a term the expression matches, the document is what changes, not the expression."
    - requirement_id: INSTR-02
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT add any comment to source under `firestarter_fw/` or `firestarter_app/`, and MUST NOT make the existence of a comment an acceptance criterion. This plan touches neither sub-repo, and the rule is not overridable by a plan or a task."
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT add AI or Claude attribution to any commit, in the subject, the body, or a trailer."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT commit to `beta` or to `main` in any repository, and MUST NOT create a GitHub Release in the meta repository. Milestone work stays on the `v1.39-*` branch."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT stage with `git add -A`, `git add .`, or `git commit -a`. Eight tracked files and five untracked paths are dirty before this phase starts; every commit stages explicit paths only."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT touch either sub-repo, advance a gitlink, or enter a submodule. `firestarter_app`'s gitlink is dirty before this phase starts and is left exactly as found."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT move, rename or archive `.planning/seeds/SEED-claim-firestarter-slug.md`. It is the single best account of the whole episode and it must stay findable at its canonical path."
---

<objective>
Bring every live document into agreement with a disposition that is already made: the instrument is
retired, and no document may still describe it as a live open choice or as gating a claim that has
already fired.

Four documents carry a residual reference the proof expression can see, and a fifth — the seed —
carries two that are frozen on purpose and get an annotation instead. This plan edits all five and
nothing else.

It implements D-03 (the `CLAUDE.md` paragraph, mirroring the `tools/wiki/` retirement already in that
section), D-10 (`REQUIREMENTS.md` INSTR-01 and `ROADMAP.md` criterion 1 are in scope; the closed v1.38
ROADMAP section is not), D-14 (the seed is frozen with the dead path annotated) and D-15 (the seed
stays where it is).

Purpose: INSTR-02 — no document describes the instrument as gating a claim that has already fired;
the seed, `CLAUDE.md` and every note pointing at it agree with the chosen disposition.
Output: five edited documents and one commit. The proof that the sweep landed is run in Plan 196-03.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/196-adoption-instrument-disposition/196-CONTEXT.md
@.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md
@.planning/phases/196-adoption-instrument-disposition/196-PATTERNS.md
@.planning/phases/196-adoption-instrument-disposition/196-01-SUMMARY.md
</context>

<environment_notes>
**Three of these files must never be read whole.** `.planning/ROADMAP.md` is 983 KB,
`.planning/PROJECT.md` is 452 KB, and `.planning/STATE.md` is 555 KB with a single line of roughly
52,000 characters. Locate with `/usr/bin/grep -n`, then read a bounded window with `sed -n 'A,Bp'`.
`.planning/STATE.md` is not read and not edited at all in this phase. `.planning/REQUIREMENTS.md` is
7.7 KB and is safe to read whole. `CLAUDE.md` is 101 lines and is safe to read whole.

**`grep` on PATH in this devcontainer is `ugrep`.** It honours `.gitignore` and silently under-scans.
Every command in this plan calls `/usr/bin/grep` by absolute path. `/usr/bin/grep -c` is never a pass
signal — it prints `0` and exits 1 on a non-matching file. A leg expecting zero matches captures the
output, tests it with `[ -z ... ]`, and echoes a positive token. Always pass the pattern with `-e`, so
a future dash-leading pattern cannot exit 2 and be read as "not found".

**Line numbers drift.** Every line number in this plan and in `196-PATTERNS.md` was measured at HEAD
`5d188fc2` and Plan 196-01 has committed since. Locate every edit site by its quoted text, not by its
line number, and treat the numbers as a navigation aid only.

**The proof expression that runs in Plan 196-03 scans every tracked file except six paths:
`.planning/STATE.md`, `.planning/milestones/`, this phase's own directory,
`.planning/seeds/SEED-claim-firestarter-slug.md`, `.planning/graphs/` and `.claude/`.** Four of the
five files edited here are scanned. So a live document that still names the retired artifact by path
or by filename turns that proof red — which is the correct outcome, because a live document naming a
path that no longer exists is a residual reference. Describe the artifact instead: *the PyPI
per-version download-share instrument*. The seed is the one exception; it is excluded, and its
annotation may cite whatever it needs to.

**The working tree is not clean and must not be made clean.** Commit explicit paths only.

**Any `gsd-tools` call — even a read-only one — can silently prune `.planning/config.json`'s
`planning.sub_repos` list.** If a GSD verb is run for any reason, check
`git status --porcelain .planning/config.json` afterwards and `git checkout --` the file if it
changed.

**Branch.** HEAD must be on `v1.39-protocol-0x05-write-correctness`. Never commit to `beta` or `main`.
Commit convention: `docs(196): <subject>`, Conventional Commits, lower case, no trailing period.
</environment_notes>

<artifacts_this_phase_produces>
This plan creates no new file. It produces these named strings in existing tracked prose, and the
tokens its checks emit.

- In `CLAUDE.md`: the substring `holds one directory`, replacing the falsified two-occupant claim; and
  a retirement paragraph containing the literal `2026-09-17` and the literal path
  `.planning/notes/adoption-instrument-retirement.md`. No new heading is introduced.
- In `.planning/ROADMAP.md` Phase 196 success criterion 3: the sentence opener
  `The instrument was not retained`, which records that the criterion's antecedent is false.
- In `.planning/seeds/SEED-claim-firestarter-slug.md`: one appended annotation inside the FIRED
  banner, containing `2026-09-17` and the retirement note's path.
- Check tokens emitted by this plan: `CLAUDE-SWEPT`, `CITED-BY-DATE-NOT-SHA`,
  `CLAUDE-UNTOUCHED-ELSEWHERE`, `PROJECT-SWEPT`, `PROJECT-SCOPE-OK`, `REQUIREMENTS-SWEPT`,
  `TRACEABILITY-STILL-PENDING`, `ROADMAP-SWEPT`, `ROADMAP-SCOPE-OK`, `SEED-APPEND-ONLY`,
  `SEED-AT-CANONICAL-PATH`, `SWEEP-COMMIT-SCOPE-OK`.
- No new anchor file. The commit-scope assertion reuses the executor's own plan ledger,
  `gsd-plan-head-before-196-02` in the git directory, written before this plan's first commit and
  never rewritten. Tasks 1 and 2 make no commit, so that value is this task's pre-task HEAD. Nothing
  under the git directory is tracked, listed by `git status`, or reachable by `git ls-files`, so the
  anchor cannot be staged into this plan's five-path commit and is invisible to the Plan 196-03 proof
  expression.
- The failure tokens `ANCHOR-UNSET` and `ANCHOR-INVALID`, each printed with exit 2 by the
  commit-scope leg when its anchor was never captured, or does not name a commit.
</artifacts_this_phase_produces>

<flagged_assumptions>
Carried forward. Not resolved, not dismissed, and not to be silently dropped.

- **Probe row INSTR-02 — `unclassified`, `unresolved`.** No SPEC exists, so the deterministic edge
  probe ran against the requirement text under the spec-less fallback and returned
  `category: unclassified`, `status: unresolved`, `verification: null`. An `unclassified` row is never
  auto-resolved with a backstop marker, so it is surfaced here. Practical reading: INSTR-02 states a
  semantic property — that no document *describes* the instrument a certain way — and semantics have
  no enumerable data-shape edge. The proof expression in Plan 196-03 is a deliberate proxy for it, and
  its own blind spots are recorded there.
- **Assumption A2 (`196-RESEARCH.md`).** `.planning/REQUIREMENTS.md`'s INSTR section heading is the
  only live phrase-level residue the proof expression cannot see, because broad phrase terms would
  match five archived milestones and make the expression permanently red. This plan rewords the
  heading by hand and gives it its own acceptance criterion. If a later reader finds another
  phrase-level residue, the expression will not catch it either — that is a known limit, not a defect.
- **Assumption A3 (`196-RESEARCH.md`).** Extending D-09's exclusion from the single Phase 193 archive
  directory to all of `.planning/milestones/` is treated as a broadening of D-09's own stated reason
  (archived records are historical-by-intent), not as a new decision. If the operator reads it as a
  new decision, the two hits outside the 193 directory need their own disposition.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: `CLAUDE.md` and `PROJECT.md` — the inventory the deletion falsified, and the paragraph that says the instrument still runs</name>
  <precondition>HEAD is on the milestone branch: `git -C /workspaces rev-parse --abbrev-ref HEAD` prints a name beginning `v1.39-`. If it prints `beta` or `main`, halt and do not commit.</precondition>
  <reversibility rating="reversible">Prose edits to two tracked documents, recoverable from the previous commit with no migration and no broken contract.</reversibility>
  <read_first>
    - /workspaces/CLAUDE.md — the whole file, 101 lines. Read § Repository Structure closely: the `tools/wiki/` retirement paragraph, the count sentence and its two bullets, and the source-comment paragraph three paragraphs below
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-PATTERNS.md — §2 S1 (the exact current text of the count sentence and the bullets), §2 S2 (the exact current text of the `PROJECT.md` paragraph) and §3 in full (both in-file precedents, with their structural moves enumerated in order)
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md — §R4 in full (why the date route wins, with both precedents traced through `git log -L`) and §R1's SWEEP table
    - /workspaces/.planning/PROJECT.md lines 60-80 only — the third-item paragraph in its surrounding context. Do not read this file whole; it is 452 KB
    - /workspaces/.planning/notes/adoption-instrument-retirement.md — the record created by Plan 196-01, so the path cited here is confirmed to exist and to say what the citation claims
  </read_first>
  <files>CLAUDE.md, .planning/PROJECT.md</files>
  <action>
    Two documents, one concern: both describe the instrument as present and running, and both are
    falsified by the deletion Plan 196-01 performed.

    **`CLAUDE.md` § Repository Structure — three moves, one edit.**

    First, the count sentence. It currently claims `tools/` holds two directories. That claim is now
    false. Reword it so it reads that `tools/` holds **one** directory — the substring
    `holds one directory` must be present afterwards. This is the established pattern in this
    repository, which Phase 193 hit in reverse: a phase repairs the sentence its own change falsifies,
    in the same edit that falsifies it.

    Second, the list below it. Delete the second bullet — the one naming the PyPI per-version
    download-share instrument. Leave the first bullet, which names `tools/catalog/` and its
    description as messages codegen and sub-repo sync tooling, completely untouched. Whether one
    surviving occupant reads better as a one-item list or folded into the sentence is yours to decide;
    what is fixed is that the count is correct and `tools/catalog/` keeps its description.

    Third, append a retirement paragraph, on the structural model of the `tools/wiki/` paragraph
    already in that section — the same section, four paragraphs above. Its moves, in order: name the
    thing and what it did in one clause; name the removing act with a date; say where the durable
    record went, by path; and close with a bolded standing consequence in the present tense. Adapt,
    do not transcribe: the wiki paragraph describes two separate acts eleven days apart, and this one
    describes a single act. The paragraph must contain the literal `2026-09-17` and the literal path
    `.planning/notes/adoption-instrument-retirement.md`. Introduce no new heading.

    **Cite by date, never by SHA.** This is settled, and the reason is that a commit cannot cite
    itself. Two precedents in this same file already chose the date route: the wiki paragraph's own
    deleting act is cited as *a later commit deleted it on* a date with no SHA at all, and the
    source-comment paragraph three days old says *removed by operator decision on 2026-09-17*, using
    SHAs only for restore points in other repositories that existed independently. A date is also the
    only citation that survives a rebase or a squash, and this branch has already absorbed a merge
    from `origin/beta`. Write no SHA next to the retirement sentence, write no placeholder, and write
    no `commit TBD`. The deleting commit's SHA belongs in `196-01-SUMMARY.md`, where Plan 196-01
    recorded it.

    **Name the retired artifact by description, never by path or filename.** Say *the PyPI
    per-version download-share instrument*. A live inventory document that still names a directory
    which no longer exists is exactly the residual reference the Plan 196-03 proof measures, and it
    will turn that proof red on this file.

    **Leave the rest of `CLAUDE.md` alone** — the `tools/wiki/` paragraph, the source-comment
    paragraph, § System Overview, § Cross-repo obligations and § Milestone close are all out of scope
    and must be byte-identical afterwards.

    **`.planning/PROJECT.md` — reword the end of the third-item paragraph.** Locate the paragraph that
    begins with the third item being bookkeeping the v1.38 close left behind. Its first sentences
    remain true as history and are left alone: that the instrument was built to measure whether it was
    safe to claim `henols/firestarter`, that the operator claimed it on 2026-09-14 with the trigger
    unmet, and that the seed is `status: fired`. Only the closing claim is falsified — that the
    instrument still runs and still reports, but now answers a question with no consumer. Reword the
    paragraph so it asserts instead: that the instrument was retired on 2026-09-17, and that the
    reason is recorded at `.planning/notes/adoption-instrument-retirement.md`. Drop the path and the
    filename from the opening sentence in the same edit, replacing them with the description. Keep the
    literal `2026-09-14` and the fact that the trigger was unmet — both are still true.

    **Do not touch anything else in `.planning/PROJECT.md`.** In particular, the D-1 row further down
    the file is about the *claim*, not the instrument, carries no reference to sweep, and was listed as
    a site in error — that was measured and confirmed. Every hunk in this file's diff must fall inside
    the third-item paragraph.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md` does not contain the string `holds two directories`.
    - `CLAUDE.md` contains the string `holds one directory`.
    - `CLAUDE.md` still names `tools/catalog/` with its description `messages codegen and sub-repo sync tooling` — asserted in the same command as the two legs above, because repairing the count while dropping the surviving occupant, or dropping the occupant while leaving the count, is the exact trap Phase 193 hit in reverse.
    - Neither the string `tools/adoption` nor the string `pypi_version_share` appears anywhere in `CLAUDE.md`.
    - `CLAUDE.md` contains both `2026-09-17` and `.planning/notes/adoption-instrument-retirement.md`, and that path resolves to a file that exists.
    - No line of `CLAUDE.md` that mentions the retirement note's path also contains a 7-to-40 character lowercase hexadecimal token — i.e. no SHA was written next to the citation.
    - The `tools/wiki/` paragraph in `CLAUDE.md` is byte-identical to its state at the previous commit.
    - `CLAUDE.md` gained no new heading: the count of lines beginning with `#` is unchanged from the previous commit.
    - `.planning/PROJECT.md` does not contain the string `The instrument still runs and still reports`.
    - Neither the string `tools/adoption` nor the string `pypi_version_share` appears anywhere in `.planning/PROJECT.md`.
    - `.planning/PROJECT.md` contains `.planning/notes/adoption-instrument-retirement.md`, and still contains `2026-09-14` and `trigger unmet`.
    - Every hunk in `git diff` for `.planning/PROJECT.md` begins within lines 55 to 85 — proving the D-1 row and the rest of a 452 KB file were not touched.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && C=CLAUDE.md && R="$(/usr/bin/grep -nFe 'holds two directories' -e 'tools/adoption' -e 'pypi_version_share' "$C" || true)" && { [ -z "$R" ] && true || { printf '%s\n' "$R"; false; }; } && /usr/bin/grep -qFe 'holds one directory' "$C" && /usr/bin/grep -qFe 'tools/catalog/' "$C" && /usr/bin/grep -qFe 'messages codegen and sub-repo sync tooling' "$C" && /usr/bin/grep -qFe '2026-09-17' "$C" && /usr/bin/grep -qFe '.planning/notes/adoption-instrument-retirement.md' "$C" && test -s .planning/notes/adoption-instrument-retirement.md && echo CLAUDE-SWEPT</automated>
    <fails_when>non-zero exit, `CLAUDE-SWEPT` absent, or any `<line>:<text>` line printed first — a printed line names which falsified claim or path citation survived. A missing `holds one directory`, a dropped `tools/catalog/` description, a missing date or a missing note path each fail their own `grep -qFe` leg, so the failing leg identifies the omission.</fails_when>
    <automated>cd /workspaces && H="$(/usr/bin/grep -nFe '.planning/notes/adoption-instrument-retirement.md' CLAUDE.md)" && test -n "$H" && S="$(printf '%s\n' "$H" | /usr/bin/grep -nE -e '\b[0-9a-f]{7,40}\b' || true)" && { [ -z "$S" ] && echo CITED-BY-DATE-NOT-SHA || { printf '%s\n' "$S"; false; }; }</automated>
    <fails_when>non-zero exit, `CITED-BY-DATE-NOT-SHA` absent, or a printed line showing a hexadecimal token on the same line as the note's path — that is a SHA citation, which does not survive a rebase. An empty `$H` fails the `-n` test and means the citation itself is missing.</fails_when>
    <automated>cd /workspaces && OLDC="$(git show HEAD:CLAUDE.md)" && test -n "$OLDC" && W="$(printf '%s\n' "$OLDC" | /usr/bin/grep -nFe 'tools/wiki/')" && X="$(/usr/bin/grep -nFe 'tools/wiki/' CLAUDE.md)" && test "$W" = "$X" && OLD="$(printf '%s\n' "$OLDC" | /usr/bin/grep -nE -e '^#' | wc -l)" && NEW="$(/usr/bin/grep -nE -e '^#' CLAUDE.md | wc -l)" && test "$OLD" = "$NEW" && echo CLAUDE-UNTOUCHED-ELSEWHERE</automated>
    <fails_when>non-zero exit, or `CLAUDE-UNTOUCHED-ELSEWHERE` absent. A changed `tools/wiki/` paragraph fails the first equality; a new or removed heading fails the second. Note this leg compares against `HEAD`, so it must run before this plan's commit, not after.</fails_when>
    <automated>cd /workspaces && P=.planning/PROJECT.md && R="$(/usr/bin/grep -nFe 'The instrument still runs and still reports' -e 'tools/adoption' -e 'pypi_version_share' "$P" || true)" && { [ -z "$R" ] && true || { printf '%s\n' "$R"; false; }; } && /usr/bin/grep -qFe '.planning/notes/adoption-instrument-retirement.md' "$P" && /usr/bin/grep -qFe '2026-09-14' "$P" && /usr/bin/grep -qFe 'trigger unmet' "$P" && echo PROJECT-SWEPT</automated>
    <fails_when>non-zero exit, `PROJECT-SWEPT` absent, or a printed `<line>:<text>` line naming the falsified sentence or the path citation that survived. A missing note path, a lost `2026-09-14` or a lost `trigger unmet` each fail their own leg.</fails_when>
    <automated>cd /workspaces && D="$(git diff -U0 -- .planning/PROJECT.md)" && test -n "$D" && B="$(printf '%s\n' "$D" | /usr/bin/grep -e '^@@' | awk '{split($2,a,","); n=substr(a[1],2)+0; if (n < 55 || n > 85) print $0}')" && { [ -z "$B" ] && echo PROJECT-SCOPE-OK || { printf '%s\n' "$B"; false; }; }</automated>
    <fails_when>non-zero exit, `PROJECT-SCOPE-OK` absent, or a printed `@@` hunk header showing an edit outside lines 55 to 85 — that is an out-of-scope edit to a 452 KB file. An empty diff fails the `-n` test, which means the file was not edited at all. This leg compares the working tree against the index, so it must run before this plan's commit.</fails_when>
  </verify>
  <done>`CLAUDE.md` claims one `tools/` directory, keeps `tools/catalog/` with its description, names neither the retired path nor its filename, carries a dated retirement paragraph citing the note by path with no SHA, and is otherwise byte-identical with no new heading. `.planning/PROJECT.md` no longer says the instrument still runs, points at the note, keeps the claim date and the unmet trigger, and every hunk falls inside the third-item paragraph.</done>
</task>

<task type="auto">
  <name>Task 2: `REQUIREMENTS.md` and `ROADMAP.md` — the closed choice that four live lines still describe as open</name>
  <reversibility rating="reversible">Prose edits to two tracked documents. Recoverable from the previous commit; no migration, no broken contract.</reversibility>
  <read_first>
    - /workspaces/.planning/REQUIREMENTS.md — the whole file, 7.7 KB. Read the INSTR section heading and both requirement bullets, and the Traceability table with its already-`Complete` sibling rows, which are the format analog for the flip Plan 196-03 performs
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-PATTERNS.md — §2 S3 and §2 S4, which quote the exact current text of all five edit sites and name which lines do not change
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md — §R5.3 in full (the verbatim current text of each target, the two documented verb hazards, and the structural reason the positional hazard is real here) and §R1's note on the section heading being invisible to the proof expression
    - /workspaces/.planning/ROADMAP.md lines 310-335 only — the Phase 196 block: goal, requirements line, three success criteria, depends-on and the plans line. Do not read this file whole; it is 983 KB
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Four live lines across two documents still describe a choice that closed on 2026-09-17. That is
    D-10's scope call: these are in, and the completed checkbox line inside the closed v1.38 ROADMAP
    section is not, because it is a historical record of a plan that ran.

    **Hand-edit both files. Do not run a GSD verb on either.** The requirements and roadmap verbs
    reformat the whole file, and the plan-progress verb overwrites this repository's phase block
    positionally — the v1.39 milestone section carries no table for it to anchor on, which is exactly
    the shape in which a positional writer overwrites adjacent prose, and it has already destroyed a
    phase name, its requirement IDs and its dependency line in one call. An executor's default success
    criteria point at the verb, so this prohibition is stated rather than assumed.

    **`.planning/REQUIREMENTS.md`, edit 1 — the INSTR section heading.** It currently frames the
    section as the instrument answering a live question *or* being retired. Reword it so it states the
    settled disposition: the instrument is retired, with its reason recorded. Keep the leading
    `### INSTR — ` and keep the `(v1.38 carry-over)` qualifier. This heading is the only live
    phrase-level residue the Plan 196-03 proof expression cannot see — broad phrase terms would match
    five archived milestones and make the expression permanently red — so it needs its own criterion
    and it gets one below.

    **`.planning/REQUIREMENTS.md`, edit 2 — the INSTR-01 requirement text.** It currently reads as an
    either-or: the instrument either measures a question with a named consumer, or is removed, and
    either way the disposition is recorded with its reason. Reword it so it asserts the settled state:
    the instrument is removed, and the disposition is recorded with its reason. Drop the path and the
    filename in the same edit and replace them with the description. Keep the `- [ ] **INSTR-01**: `
    prefix exactly as it is — the checkbox is flipped by the requirements bookkeeping, not by this
    reword.

    **Leave INSTR-02's text completely alone.** It already states the property correctly and carries
    no reference to sweep. **Leave both Traceability rows reading `Pending`.** They are flipped in Plan
    196-03, after the proof is green and the graph is rebuilt. A requirement marked complete ahead of
    its proof is a failure mode this project has watched executors reach for, and the criterion below
    exists to catch it.

    **`.planning/ROADMAP.md`, edit 1 — Phase 196 success criterion 1.** It currently reads as an open
    either-or: the instrument is either re-pointed at a question with a named consumer, or removed, and
    the disposition and its reason are recorded where a reader meets the decision rather than only in a
    commit message. Reword it so it asserts the settled state: the instrument is removed, and the
    disposition and its reason are recorded at `.planning/notes/adoption-instrument-retirement.md` —
    where a reader meets the decision, not only in a commit message. Drop the path and the filename and
    replace them with the description. Keep the leading `1. ` and the criterion's position in the list.

    **Criterion 2 is unchanged.** It already states INSTR-02's property correctly.

    **`.planning/ROADMAP.md`, edit 2 — criterion 3, which is now vacuously satisfied.** Its antecedent
    — that the instrument is retained — is false. Keep both of its existing sentences and append one
    more, beginning with the literal `The instrument was not retained`, stating that the criterion is
    therefore vacuously satisfied and no consumer is named. This is a planner decision made
    deliberately rather than left open: a live success criterion whose antecedent is false invites a
    verifier either to hunt for a named consumer that by construction cannot exist, or to mark the
    criterion unmet. One appended clause removes that ambiguity and costs nothing.

    **Do not touch the `**Plans:**` line.** It was set at planning time and the orchestrator owns it;
    the phase-completion bookkeeping flips it to its completed form. Editing it here would put two
    writers on one line.

    **Every edit in `.planning/ROADMAP.md` stays inside the Phase 196 block.** Nothing above line 310
    or below line 340 may change, and in particular nothing at or after the `## v1.38 ` section header
    — that whole section is a closed historical record.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` does not contain the string `answers a live question, or is retired`.
    - `.planning/REQUIREMENTS.md` still has exactly one line beginning `### INSTR — `, and that line still contains `(v1.38 carry-over)`.
    - `.planning/REQUIREMENTS.md` does not contain the string `either measures a question with a named`.
    - `.planning/REQUIREMENTS.md` still has exactly one line matching `- \[[ x]\] \*\*INSTR-01\*\*: ` and exactly one matching `- \[[ x]\] \*\*INSTR-02\*\*: `.
    - INSTR-02's requirement text is byte-identical to its state at the previous commit.
    - Neither the string `tools/adoption` nor the string `pypi_version_share` appears anywhere in `.planning/REQUIREMENTS.md`.
    - Both traceability rows still read `Pending`: the file contains `| INSTR-01 | Phase 196 | Pending |` and `| INSTR-02 | Phase 196 | Pending |`.
    - `.planning/ROADMAP.md` does not contain the string `is either re-pointed at a question with a named consumer` anywhere.
    - `.planning/ROADMAP.md` contains the string `The instrument was not retained`, and it falls inside the Phase 196 block — above the line matching `^## v1\.38 `.
    - `.planning/ROADMAP.md` contains `.planning/notes/adoption-instrument-retirement.md` above the `^## v1\.38 ` boundary.
    - Neither `tools/adoption` nor `pypi_version_share` appears in `.planning/ROADMAP.md` above the `^## v1\.38 ` boundary. Below it they may and do — that section is the closed v1.38 record D-10 leaves alone.
    - `.planning/ROADMAP.md` still contains `**Plans:** 3 plans` — the line was not edited by this task.
    - Every hunk in `git diff` for `.planning/ROADMAP.md` begins within lines 310 to 340.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && Q=.planning/REQUIREMENTS.md && R="$(/usr/bin/grep -nFe 'answers a live question, or is retired' -e 'either measures a question with a named' -e 'tools/adoption' -e 'pypi_version_share' "$Q" || true)" && { [ -z "$R" ] && true || { printf '%s\n' "$R"; false; }; } && test "$(/usr/bin/grep -nFe '### INSTR — ' "$Q" | wc -l)" = 1 && /usr/bin/grep -qFe '(v1.38 carry-over)' "$Q" && test "$(/usr/bin/grep -nE -e '^- \[[ x]\] \*\*INSTR-01\*\*: ' "$Q" | wc -l)" = 1 && test "$(/usr/bin/grep -nE -e '^- \[[ x]\] \*\*INSTR-02\*\*: ' "$Q" | wc -l)" = 1 && echo REQUIREMENTS-SWEPT</automated>
    <fails_when>non-zero exit, `REQUIREMENTS-SWEPT` absent, or a printed `<line>:<text>` line naming the open-choice wording or the path citation that survived. A heading count other than 1, a lost `(v1.38 carry-over)` qualifier, or a mangled requirement bullet prefix each fail their own leg.</fails_when>
    <automated>cd /workspaces && Q=.planning/REQUIREMENTS.md && OLDQ="$(git show HEAD:"$Q")" && test -n "$OLDQ" && OLD="$(printf '%s\n' "$OLDQ" | /usr/bin/grep -A1 -e '^- \[[ x]\] \*\*INSTR-02\*\*: ')" && NEW="$(/usr/bin/grep -A1 -e '^- \[[ x]\] \*\*INSTR-02\*\*: ' "$Q")" && test -n "$OLD" && test "$OLD" = "$NEW" && P1="$(/usr/bin/grep -nFe '| INSTR-01 | Phase 196 | Pending |' "$Q")" && P2="$(/usr/bin/grep -nFe '| INSTR-02 | Phase 196 | Pending |' "$Q")" && test -n "$P1" && test -n "$P2" && echo TRACEABILITY-STILL-PENDING</automated>
    <fails_when>non-zero exit, or `TRACEABILITY-STILL-PENDING` absent. A reworded INSTR-02 fails the `"$OLD" = "$NEW"` equality; a row flipped to `Complete` ahead of its proof empties `$P1` or `$P2` and fails a `-n` test. This leg compares against `HEAD`, so it must run before this plan's commit.</fails_when>
    <automated>cd /workspaces && RM=.planning/ROADMAP.md && V="$(/usr/bin/grep -nE -e '^## v1\.38 ' "$RM" | head -1 | cut -d: -f1)" && test -n "$V" && HEAD_PART="$(sed -n "1,$((V-1))p" "$RM")" && test -n "$HEAD_PART" && R="$(printf '%s\n' "$HEAD_PART" | /usr/bin/grep -nFe 'is either re-pointed at a question with a named consumer' -e 'tools/adoption' -e 'pypi_version_share' || true)" && { [ -z "$R" ] && true || { printf '%s\n' "$R"; false; }; } && printf '%s\n' "$HEAD_PART" | /usr/bin/grep -qFe 'The instrument was not retained' && printf '%s\n' "$HEAD_PART" | /usr/bin/grep -qFe '.planning/notes/adoption-instrument-retirement.md' && printf '%s\n' "$HEAD_PART" | /usr/bin/grep -qFe '**Plans:** 3 plans' && echo ROADMAP-SWEPT</automated>
    <fails_when>non-zero exit, `ROADMAP-SWEPT` absent, or a printed `<line>:<text>` line naming a residual reference above the v1.38 boundary. An empty `$V` fails the `-n` test and means the `^## v1\.38 ` section header was renamed, in which case the boundary is unknown and the leg refuses to guess rather than silently scanning the whole file. A missing `The instrument was not retained`, a missing note path, or an edited plans line each fail their own `grep -qFe` leg.</fails_when>
    <automated>cd /workspaces && D="$(git diff -U0 -- .planning/ROADMAP.md)" && test -n "$D" && B="$(printf '%s\n' "$D" | /usr/bin/grep -e '^@@' | awk '{split($2,a,","); n=substr(a[1],2)+0; if (n < 310 || n > 340) print $0}')" && { [ -z "$B" ] && echo ROADMAP-SCOPE-OK || { printf '%s\n' "$B"; false; }; }</automated>
    <fails_when>non-zero exit, `ROADMAP-SCOPE-OK` absent, or a printed `@@` hunk header showing an edit outside lines 310 to 340 — that is an out-of-scope edit to a 983 KB file and the most likely symptom of a positional GSD verb having been run. An empty diff fails the `-n` test and means neither criterion was edited. Runs before this plan's commit.</fails_when>
  </verify>
  <done>The INSTR section heading and INSTR-01's text state the settled disposition with no path citation; INSTR-02's text and both `Pending` traceability rows are untouched; ROADMAP criterion 1 states the settled disposition and points at the note; criterion 3 records that its antecedent is false; the plans line is unedited; and every ROADMAP hunk falls inside the Phase 196 block.</done>
</task>

<task type="auto">
  <name>Task 3: The seed — one appended annotation, nothing rewritten, and the sweep commit</name>
  <reversibility rating="reversible">An appended annotation in a frozen record. The freeze itself (D-14) is the reversible choice: the trigger text can always be rewritten later if a reader ever wants it in past tense, and nothing depends on its tense.</reversibility>
  <read_first>
    - /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md lines 1-60 — the frontmatter including `trigger_condition`, the whole FIRED banner, and the two annotation lines the banner already ends with. Those two lines are the placement analog: they point outward, then explain why the text below is retained unchanged
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-PATTERNS.md — §2 S5, which quotes both frozen sites and the banner's existing annotation lines verbatim, and names the banner's register
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-CONTEXT.md — D-14 and D-15, and the `<specifics>` note on the banner's register
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md — §R5.1, the per-item disposition table for the dirty working tree, so nothing that does not belong to this phase is staged
  </read_first>
  <files>.planning/seeds/SEED-claim-firestarter-slug.md</files>
  <action>
    Append one annotation to the seed's FIRED banner and commit the whole sweep.

    **Establish the commit-scope anchor first, before the annotation and before the commit, and use
    the executor's own plan ledger for it.** The executor protocol writes HEAD to
    `gsd-plan-head-before-196-02` inside the git directory before this plan's first commit and never
    rewrites it, precisely because each Bash call is a fresh shell and an exported variable would be
    gone by the time a later leg reads it. Do not invent a second anchor file beside it. As the first
    step of this task: read that ledger, export the value as `PRE_TASK_SHA` for this task's
    commit-scope leg, and record it in the summary. If the file does not exist yet, write it yourself
    with `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-plan-head-before-196-02"` before the
    annotation. Tasks 1 and 2 make no commit, so that value is this task's pre-task HEAD and the right
    anchor for the five-path assertion. Nothing under the git directory is tracked, listed by
    `git status`, or reachable by `git ls-files`, so the anchor cannot be staged among this plan's
    five paths and is invisible to the Plan 196-03 proof expression. If the ledger survives from an
    abandoned earlier attempt, the count assertion fails loudly rather than passing — resolve the
    extra commits, and never delete the ledger to make a number come out.

    **The anchor is load-bearing, and the leg refuses to run without it.** With `$PRE_TASK_SHA`
    empty, `git rev-list --count $PRE_TASK_SHA..HEAD` becomes `git rev-list --count ..HEAD`, which
    git resolves to `HEAD..HEAD`: it prints `0` and exits 0, so a leg built on it has asserted
    nothing about which paths this phase committed. The leg below therefore resolves the anchor,
    proves it names a commit here, and aborts with `ANCHOR-UNSET` or `ANCHOR-INVALID: <value>` and
    exit 2 when it does not. If it aborts that way, capture the anchor — do not edit the leg, and do
    not substitute `HEAD~1`, which makes the count 1 by construction.

    **What changes: one annotation, appended.** It says the instrument was retired on 2026-09-17 and
    points at `.planning/notes/adoption-instrument-retirement.md`. Place it inside the FIRED banner
    block, alongside the two annotation lines the banner already ends with — the one naming where the
    evidence lives and the one explaining that the trigger text below is retained unchanged. Match
    their voice: flat, outward-pointing, one fact per line. This file is excluded from the Plan 196-03
    proof expression, so the annotation may cite the note by path freely.

    **What does not change: everything else, byte for byte.** The `trigger_condition` frontmatter keeps
    its text exactly as written, including the sentence naming the instrument that produces the
    evidence. The section explaining why the trigger is what it is keeps its text exactly as written,
    including the same naming. Neither is reworded, neither is moved to past tense, and neither is
    deleted. The reason is not sentiment: the trigger text is the standard the operator's act was
    judged against, and the banner's force comes from quoting a *live* standard and showing 12.8%
    against it. Rewriting the standard into past tense degrades the banner into a summary of itself.
    INSTR-02 is already satisfied in this file — it says in bold at the top that the claim fired and
    the trigger was not met. What was missing is only that the instrument itself is now gone.

    **The file stays at its canonical path in `.planning/seeds/`** (D-15). Do not move it, do not
    rename it, do not archive it. It is the single best account of the whole episode and it must stay
    findable.

    **Commit.** One commit, staging exactly the five paths this plan edits: `CLAUDE.md`,
    `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and
    `.planning/seeds/SEED-claim-firestarter-slug.md`. Never `git add -A`, never `git add .`, never
    `git commit -a` — eight tracked files and five untracked paths were dirty before this phase started
    and none of them belongs to it. Subject on the repository convention: `docs(196): <subject>`, lower
    case, no trailing period. Add no AI or Claude attribution of any kind. Confirm
    `git status --porcelain .planning/config.json` prints nothing before committing, and
    `git checkout --` that one path if a GSD verb pruned it.

    **Run every `<verify>` leg of Tasks 1 and 2 before this commit, not after.** Four of them compare
    the working tree against `HEAD` or against the index, and committing first makes those comparisons
    vacuous.
  </action>
  <acceptance_criteria>
    - `git diff` for `.planning/seeds/SEED-claim-firestarter-slug.md` contains exactly one hunk.
    - That diff removes zero lines — every changed line is an addition. This is what proves `trigger_condition` and the "Why the trigger is what it is" section are untouched, without needing to name either.
    - That diff adds between 1 and 3 lines, which is one annotation and its wrapping, not a rewrite.
    - The hunk begins within lines 15 to 50 — inside the FIRED banner, not in the frontmatter and not in the body below.
    - The added lines contain both `2026-09-17` and `.planning/notes/adoption-instrument-retirement.md`.
    - The file is still tracked at `.planning/seeds/SEED-claim-firestarter-slug.md`, and `git status --porcelain` reports it as modified, never as renamed or deleted.
    - The commit-scope anchor exists before the commit: `gsd-plan-head-before-196-02` in the git directory is non-empty and its contents name a commit in this repository. It is untracked by construction — nothing under the git directory is listed by `git status` or reachable by `git ls-files` — so it appears in no commit and in no proof reading.
    - Exactly one new commit exists on top of that captured anchor, and its diff touches exactly the five paths this plan edits — no more, no fewer. With no anchor captured the assertion is refused outright, with `ANCHOR-UNSET` and exit 2, rather than answered with a vacuous zero.
    - The commit message contains no AI or Claude attribution.
    - HEAD is still on a branch whose name begins `v1.39-`, and `.planning/config.json` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && S=.planning/seeds/SEED-claim-firestarter-slug.md && D="$(git diff -U0 -- "$S")" && test -n "$D" && test "$(printf '%s\n' "$D" | /usr/bin/grep -ne '^@@' | wc -l)" = 1 && test "$(printf '%s\n' "$D" | /usr/bin/grep -nE -e '^-[^-]' | wc -l)" = 0 && A="$(printf '%s\n' "$D" | /usr/bin/grep -E -e '^\+[^+]')" && test -n "$A" && test "$(printf '%s\n' "$A" | wc -l)" -ge 1 && test "$(printf '%s\n' "$A" | wc -l)" -le 3 && printf '%s\n' "$A" | /usr/bin/grep -qFe '2026-09-17' && printf '%s\n' "$A" | /usr/bin/grep -qFe '.planning/notes/adoption-instrument-retirement.md' && echo SEED-APPEND-ONLY</automated>
    <fails_when>non-zero exit, or `SEED-APPEND-ONLY` absent. A removed-line count above 0 fails the `= 0` test and is the D-14 freeze violation this leg exists to catch; more than one hunk fails the `= 1` test; more than three added lines fails the `-le 3` test and means a rewrite rather than an annotation; a missing date or note path fails its own `grep -qFe`. An empty diff fails the `-n` test and means no annotation was added at all.</fails_when>
    <automated>cd /workspaces && S=.planning/seeds/SEED-claim-firestarter-slug.md && test -n "$(git ls-files -- "$S")" && ST="$(git status --porcelain -- "$S")" && test -n "$ST" && case "$ST" in R*|*D*) printf '%s\n' "$ST"; false;; *) true;; esac && DF="$(git diff -U0 -- "$S")" && test -n "$DF" && H="$(printf '%s\n' "$DF" | /usr/bin/grep -e '^@@' | awk '{split($3,a,","); n=substr(a[1],2)+0; if (n >= 15 && n <= 50) print "IN"}')" && test "$H" = IN && echo SEED-AT-CANONICAL-PATH</automated>
    <fails_when>non-zero exit, `SEED-AT-CANONICAL-PATH` absent, or a printed porcelain line beginning `R` or containing `D` — a rename or a deletion, either of which breaks D-15. A hunk starting outside lines 15 to 50 leaves `$H` empty and fails the `= IN` test, meaning the annotation landed in the frontmatter or in the body rather than in the banner.</fails_when>
    <automated>cd /workspaces && GD="$(git rev-parse --git-dir)" && ANC="${PRE_TASK_SHA:-$(cat "$GD/gsd-plan-head-before-196-02" 2>/dev/null || true)}" && { [ -n "$ANC" ] || { echo 'ANCHOR-UNSET: no pre-task SHA was captured - commit-scope assertion refused'; exit 2; }; } && { git rev-parse --verify --quiet "$ANC^{commit}" >/dev/null || { echo "ANCHOR-INVALID: $ANC"; exit 2; }; } && CNT="$(git rev-list --count "$ANC"..HEAD)" && NAMES="$(git diff --name-only "$ANC"..HEAD)" && MSG="$(git log -1 --format='%B')" && BR="$(git rev-parse --abbrev-ref HEAD)" && CFG="$(git status --porcelain .planning/config.json)" && test "$CNT" = 1 && test "$(printf '%s\n' "$NAMES" | wc -l)" = 5 && for p in CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/seeds/SEED-claim-firestarter-slug.md; do printf '%s\n' "$NAMES" | /usr/bin/grep -qFe "$p" || { echo "MISSING-FROM-COMMIT: $p"; exit 1; }; done && A="$(printf '%s\n' "$MSG" | /usr/bin/grep -niE -e 'co-authored-by.*(claude|anthropic|ai)|generated (with|by).*(claude|ai)|claude\.ai|ai-assisted' || true)" && { [ -z "$A" ] && true || { printf '%s\n' "$A"; false; }; } && printf '%s\n' "$BR" | /usr/bin/grep -qe '^v1\.39-' && test -z "$CFG" && echo SWEEP-COMMIT-SCOPE-OK</automated>
    <fails_when>non-zero exit, `SWEEP-COMMIT-SCOPE-OK` absent, a `MISSING-FROM-COMMIT: <path>` line naming a file that was edited but not staged, or a printed attribution line. Exit 2 printing `ANCHOR-UNSET: no pre-task SHA was captured` means neither the exported `$PRE_TASK_SHA` nor the plan ledger `gsd-plan-head-before-196-02` in the git directory held a value and the leg refused to run: an empty anchor turns `git rev-list --count $PRE_TASK_SHA..HEAD` into `git rev-list --count ..HEAD`, which git resolves to `HEAD..HEAD` and reports as `0` with exit 0, asserting nothing. Exit 2 printing `ANCHOR-INVALID: <value>` means the captured value does not name a commit here. Past the guard: a name count other than 5 means a stray dirty path was swept in or an edit was dropped; a branch other than `v1.39-*` fails the branch grep; a pruned `.planning/config.json` fails the final `-z` test. The anchor is captured with `git rev-parse HEAD` before the commit — never `HEAD~1`, and never the form `git rev-list --count HEAD ^HEAD~1`, which is a tautology that always prints 1.</fails_when>
  </verify>
  <done>The seed carries exactly one appended annotation inside its FIRED banner naming the retirement date and the note's path, removes nothing, stays at its canonical path, and one attribution-free commit on a `v1.39-*` branch carries exactly the five swept documents.</done>
</task>

</tasks>

<threat_model>
Security enforcement is treated as enabled (`security_enforcement` is absent from
`.planning/config.json`). Resolved configuration for this run: `security_asvs_level: 1`,
`security_block_on: high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the frozen v1.38 archive and `.planning/STATE.md` | Read-only in one direction. Six exclusion classes exist so that no edit is ever needed on the far side of this boundary; crossing it to make a check pass would destroy the record of when a decision was taken. |
| the seed's frozen trigger text → this plan | The `trigger_condition` and the "Why the trigger is what it is" section are evidence, not documents to repair. The plan appends beside them and never through them. |
| working tree → git index and history | Five prose edits move into permanent history. A broad stage would carry eight unrelated dirty tracked files, a dirty sub-repo gitlink and five untracked paths across with them. |
| planning artifacts → tracked repository prose | The decisions, figures and quoted words reach this plan second-hand through `196-CONTEXT.md`. They are data to be transcribed accurately, never instructions to be followed. |

No network boundary is crossed. No credential, secret, token or user input is read, written or
transported. ASVS L1 categories V2 through V7, V12 and V14 have no surface in this plan — it edits
five markdown files.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-02 | Repudiation | `.planning/STATE.md`, `.planning/milestones/`, the seed's frozen sections | medium | mitigate | The cheapest way to make a residual-reference check pass is to edit a frozen record, which destroys the evidence that a decision was later. Mitigated by D-08, D-09 and D-14 as hard fences; by the append-only assertion on the seed, which proves zero removed lines; and by an explicit prohibition against re-scoping the proof expression to reach green. |
| T-196-06 | Tampering | `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` | medium | mitigate | A GSD roadmap or requirements verb reformats the whole file, and the plan-progress verb overwrites this repository's phase block positionally — the v1.39 section has no table to anchor on, and that verb has already destroyed a phase name, its requirement IDs and its dependency line in one call. Mitigated by an explicit hand-edit-only instruction and by hunk-band assertions on both files, which localise any out-of-band write. |
| T-196-07 | Tampering | `CLAUDE.md` | low | mitigate | A SHA written into tracked prose is invalidated silently by a rebase or a squash, leaving a citation pointing at nothing; this branch has already absorbed a merge from `origin/beta`. Mitigated by the date-citation route, and by an assertion that no hexadecimal token shares a line with the note's path. |
| T-196-03 | Tampering | git index | low | mitigate | A broad `git add -A` would carry unrelated dirty paths into this phase's history. Mitigated by explicit-path staging and a five-path commit-scope assertion. |
| T-196-04 | Tampering | `beta` and `main` branches; the meta-repository release surface | medium | mitigate | A commit on `beta` in either sub-repo publishes a pre-release and can reach PyPI; a GitHub Release in the meta repository makes every stranded CLI report firmware up to date. Mitigated by a branch precondition, a branch assertion in the commit legs, and a prohibition against creating a release. This plan touches no sub-repo and creates no tag. |
| T-196-05 | Tampering | `.planning/config.json` | low | mitigate | Any `gsd-tools` invocation, including a read-only one, can silently prune the `planning.sub_repos` list. Mitigated by a porcelain check on that one path before the commit and a `git checkout --` recovery instruction. |
| T-196-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this phase and no dependency is added, upgraded or resolved, so `196-RESEARCH.md` carries no Package Legitimacy Audit — correctly, because there is no package to audit. Accepted with that as the stated rationale. |

No threat is rated `high` or `critical`, so `security_block_on: high` is satisfied with zero blocking
threats and no security checkpoint is inserted.
</threat_model>

<verification>
Run every `<automated>` leg of Tasks 1 and 2 **before** Task 3's commit. Four of them compare the
working tree against `HEAD` or against the index, and committing first makes those comparisons
vacuous — a leg that is vacuously true is indistinguishable from a leg that passed.

All nine positive tokens must appear: `CLAUDE-SWEPT`, `CITED-BY-DATE-NOT-SHA`,
`CLAUDE-UNTOUCHED-ELSEWHERE`, `PROJECT-SWEPT`, `PROJECT-SCOPE-OK`, `REQUIREMENTS-SWEPT`,
`TRACEABILITY-STILL-PENDING`, `ROADMAP-SWEPT`, `ROADMAP-SCOPE-OK`. Then Task 3's three:
`SEED-APPEND-ONLY`, `SEED-AT-CANONICAL-PATH`, `SWEEP-COMMIT-SCOPE-OK`.

The INSTR-02 proof expression is **not** run in this plan. It runs in Plan 196-03, after the graph
rebuild, and running it here would only report a number this plan cannot act on.

Two properties no automated leg in this plan can reach, both of which need a read:

1. **Semantics.** INSTR-02's real property is that no document *describes* the instrument as gating a
   claim that has already fired. The checks here match identifiers. A sentence that says the
   instrument is still running, without naming it, passes every leg. Read each of the five edited
   passages once, as a reader who knows nothing about this phase, and confirm none of them reads that
   way.
2. **Register.** The seed's annotation has to sit in the banner's voice — flat, outward-pointing, one
   fact per line. A leg can prove it is one appended line containing a date and a path; it cannot
   prove it belongs there.
</verification>

<success_criteria>
- `CLAUDE.md` claims one `tools/` directory, keeps `tools/catalog/` and its description, names the retired artifact by description only, and carries a dated retirement paragraph citing the record by path with no SHA — with the `tools/wiki/` paragraph and every other section byte-identical and no new heading.
- `.planning/PROJECT.md` no longer claims the instrument still runs, points at the record, keeps the claim date and the unmet trigger, and every hunk falls inside the third-item paragraph.
- `.planning/REQUIREMENTS.md`'s INSTR heading and INSTR-01 state the settled disposition with no path citation; INSTR-02's text is untouched; both traceability rows still read `Pending`.
- `.planning/ROADMAP.md` criterion 1 states the settled disposition and points at the record; criterion 3 records that its antecedent is false; the plans line is unedited; every hunk falls inside the Phase 196 block; nothing at or after the `^## v1\.38 ` header changed.
- The seed gained exactly one appended annotation inside its FIRED banner, removed nothing, and stayed at its canonical path.
- One attribution-free commit on a `v1.39-*` branch carries exactly those five paths.
- The commit-scope assertion ran against an anchor captured before the commit and proven to name a commit; with no anchor it aborts with `ANCHOR-UNSET` and exit 2 rather than reporting a vacuous zero.
</success_criteria>

<output>
Create `.planning/phases/196-adoption-instrument-disposition/196-02-SUMMARY.md` when done.

Record in it: the final wording of the `CLAUDE.md` retirement paragraph and of the seed annotation,
both verbatim, so a reviewer can judge register without re-reading the files; the reworded INSTR
section heading and INSTR-01 text, verbatim; the sentence appended to ROADMAP criterion 3; the
pre-task SHA used for the commit-scope assertion, which is the same value written to
the plan ledger `gsd-plan-head-before-196-02`; and any place where the description-not-path rule
forced a phrasing that reads awkwardly, since that is the one cost this plan knowingly accepts.
</output>
