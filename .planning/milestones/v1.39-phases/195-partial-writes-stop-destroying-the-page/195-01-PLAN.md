---
phase: 195-partial-writes-stop-destroying-the-page
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: [firestarter_fw, firestarter_app]
files_modified:
  - tools/catalog/messages.toml
  - firestarter_fw/include/messages.h
  - firestarter_app/firestarter/messages.py
  - firestarter_fw/src/proms/flash_5v_page.cpp
  - firestarter_fw/test/native/avr/test_val_5v_page/test_val_5v_page.cpp
  - firestarter_app/firestarter/page_size_gate.py
  - firestarter_app/firestarter/exceptions.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/tests/test_page_size_alignment_refusal.py
  - .planning/todos/pending/error-message-band-a0-bf-exhausted.md
autonomous: true
requirements: [WRITE-01, WRITE-02]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-01: a protocol 0x05 write whose start address is not a whole multiple of the part's page size, or whose payload length is not a whole multiple of the page size, is refused by the host before any serial byte is emitted, and the refusal message names the chip, the page size, the start address and the length."
    - "D-04: the firmware independently refuses any protocol 0x05 write chunk whose address or data_size is not a whole multiple of the resolved page size, and performs zero register writes when it does."
    - "WRITE-01 adjacency edge: a write that starts exactly on a page boundary and covers exactly a whole number of pages is accepted; a write that merely touches a page -- starting or ending one byte inside it -- is refused."
    - "WRITE-02 adjacency edge: a refused write emits no success line and exits non-zero; an accepted page-exact write still emits the success line, so the guard is not a blanket refusal."
    - "WRITE-02 ordering edge: for a CLI-driven write the host refusal is the only message the operator sees, because it fires before the port opens; the firmware refusal is reachable only when the host predicate is bypassed, and it is proven by the native suite rather than through the CLI."
    - "D-02: the new ERROR-severity message id is 0xC0, extending the ERROR band past the exhausted 0xA0-0xBF range, and the convention is recorded in the catalog header rather than left to be re-derived."
    - "D-09: the || is_first_byte and || is_last_byte clauses stay in place, because the silicon commits the page 200 microseconds after the last load regardless of what the firmware does -- deleting them would remove the SDP unlock and the verify poll, not the commit."
  artifacts:
    - path: tools/catalog/messages.toml
      provides: "MSG_ERR_FL4_PAGE_ALIGN at 0xC0, severity ERROR, a u24 address param and a u16 size param, plus the recorded ERROR-band-extension convention"
      contains: "MSG_ERR_FL4_PAGE_ALIGN"
    - path: firestarter_fw/src/proms/flash_5v_page.cpp
      provides: "the per-chunk alignment guard, placed before the byte loop so a refused chunk performs zero register writes"
      contains: "MSG_ERR_FL4_PAGE_ALIGN"
    - path: firestarter_fw/test/native/avr/test_val_5v_page/test_val_5v_page.cpp
      provides: "the unaligned-start refusal case and the page-exact positive control, both driven through the dispatched pointer"
      contains: "refuses_unaligned_start"
    - path: firestarter_app/firestarter/page_size_gate.py
      provides: "require_page_alignment -- the fail-closed, pre-connect alignment raiser for protocol 0x05 writes"
      contains: "require_page_alignment"
    - path: firestarter_app/firestarter/exceptions.py
      provides: "PageAlignmentError, a subclass of EpromOperationError"
      contains: "PageAlignmentError"
    - path: firestarter_app/firestarter/cli_handlers.py
      provides: "the CLI pre-flight alignment call and a dedicated except arm above the generic EpromOperationError arm"
      contains: "PageAlignmentError"
    - path: firestarter_app/firestarter/eprom_operations.py
      provides: "the operator-layer alignment call, placed before the operation context opens the port"
      contains: "require_page_alignment"
    - path: firestarter_app/tests/test_page_size_alignment_refusal.py
      provides: "the tracer legs -- refusal before the port opens, page-exact pass-through, and verbatim rendering with no generic prefix"
      contains: "PageAlignmentError"
  key_links:
    - from: "tools/catalog/messages.toml"
      to: "firestarter_fw/include/messages.h and firestarter_app/firestarter/messages.py"
      via: "bash tools/catalog/sync_to_subrepos.sh, run from the meta repo with ruff reachable, which rewrites exactly those two generated files"
    - from: "firestarter_app/firestarter/page_size_gate.py require_page_alignment"
      to: "firestarter_app/firestarter/eprom_operations.py"
      via: "called immediately after require_page_size and before the operation context opens the serial port -- the layer that also covers dev test"
    - from: "firestarter_app/firestarter/exceptions.py PageAlignmentError"
      to: "firestarter_app/firestarter/cli_handlers.py map_typed_errors"
      via: "a dedicated except arm above the generic EpromOperationError arm, rendering the message verbatim"
    - from: "firestarter_fw/src/proms/flash_5v_page.cpp page_mask"
      to: "the new alignment guard"
      via: "the mask flash_5v_page_mask already resolved at the top of write_execute, reused rather than recomputed with a variable-divisor modulo"
  prohibitions:
    - "Must NOT perform any register write before refusing. A page load cannot be aborted once bytes are loaded, so refusing without also performing no write is not a fix -- it is the same corruption with an error frame attached."
    - "Must NOT claim the firmware guard leaves the device unchanged. The firmware never learns the total payload length, so a firmware-layer refusal can fire on a later chunk after earlier chunks were already programmed. Its honest claim is narrower: no page was partially loaded by the refused chunk."
    - "Must NOT report success, progress, or a zero exit code for a write this guard refused."
    - "Must NOT widen the refusal past protocol 0x05. Key the host predicate on the algorithm value through the existing single-sourced protocol-id constant, never on a capability flag."
    - "Must NOT allocate a firmware page-staging buffer. A 512-byte buffer was measured to leave 142 bytes of RAM on uno for the entire call stack, which is a stack overflow rather than a tight fit."
    - "Must NOT hand-edit firestarter_fw/include/messages.h or firestarter_app/firestarter/messages.py. Both are generated by the meta repo's catalog sync and a hand edit is silently overwritten on the next run."
    - "Must NOT write, reword or replace any comment in firestarter_fw or firestarter_app source, including under test/ and tests/. Where an existing comment clause is falsified by this plan, delete the falsified clause and write no replacement."
    - "Must NOT name anything from the planning directory in any scanned source file -- no phase number, no requirement id, no decision id, no file path under .planning. Python docstrings are never scanned; C and C++ have no such exemption."
    - "Must NOT introduce a non-literal delayMicroseconds argument anywhere under firestarter_fw src, include, lib or platform. A source-contract scanner requires every such argument to be a literal or a clamped value."
    - "Must NOT run the app test suite under the devcontainer default Python 3.12. The app CI floor is 3.11 and a green 3.12 result masks a real CI break."
    - "Must NOT mark WRITE-01, WRITE-02 or WRITE-03 Complete in the requirements file. Plan 04 flips the two software requirements once all software evidence exists, and WRITE-03 waits for silicon."
    - "Must NOT push any branch."
---

<objective>
Refuse one unaligned protocol `0x05` write end to end -- catalog id, firmware guard, host predicate,
operator-visible message -- and prove the page-exact write still works.

Purpose: this is the tracer. It wires the single thinnest path through every layer this phase
touches: the meta-repo message catalog, the firmware write handler, the host pre-connect predicate,
the typed-exception rendering arm, and both test trees. Everything else in the phase is expansion
out from this proven slice. If this slice cannot be made to work, no amount of additional test
coverage would have saved the phase.

The capability delivered: a `firestarter write W29C020 probe64.bin -a 0x40` no longer reaches the
serial port at all. It stops with a message naming the chip, the page size, the start address and
the length, and exits non-zero -- and the firmware independently refuses the same chunk with zero
register writes, so a third-party or downgraded host cannot reach the silicon either.

Output: `MSG_ERR_FL4_PAGE_ALIGN` at `0xC0` in the canonical catalog with both generated artefacts
synced, the firmware alignment guard with one native refusal case and one positive control, the
host `require_page_alignment` raiser with its typed exception wired into both call sites and the
rendering arm, and a host test module carrying the refusal, the pass-through and the rendering legs.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md
@.planning/phases/195-partial-writes-stop-destroying-the-page/195-PATTERNS.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-05-PLAN.md
</context>

## Phase decisions closed by this plan set

There is no `195-CONTEXT.md`. The operator chose to plan without one and delegated the fix-shape
decision to the planner, on the research evidence. These are the closures, each one line, repeated
verbatim in every plan of this phase.

| ID | Decision | Reversibility |
|---|---|---|
| D-01 | The fix shape is a two-layer **refusal**: a host pre-flight predicate `S % P == 0 && L % P == 0` that runs before the port opens, plus a firmware per-chunk guard. Read-modify-write is not built. | costly |
| D-02 | The new ERROR-severity message id is `0xC0`, extending the ERROR band past the exhausted `0xA0-0xBF` range; the convention is recorded in the catalog header and the pending band todo is closed in the same commit. | costly |
| D-03 | No override flag ships in this phase. An override that proceeds and then prints the plain success line violates WRITE-02 literally, and its output contract is a separable usability decision. Filed as a todo by plan 04. | reversible |
| D-04 | The firmware guard **refuses**; it never warns and proceeds. With a passing host predicate no legitimate chunk is ever misaligned, so the guard has zero false positives and is the only defence against an old, third-party or downgraded host. | reversible |
| D-05 | The pending `host-page-size-gate-accepts-invalid-values` todo is folded in: `require_page_size` gains the same power-of-two and 512-ceiling test the firmware applies, so the two layers agree on which page sizes are valid. Plan 03 implements it. | reversible |
| D-06 | The third loss direction -- interior loss at a chunk boundary, derived from source and never observed -- is modelled in the native suite by plan 02 and attempted on the bench by plan 05, and its result is recorded either way, including "not attempted, with the reason". | reversible |
| D-07 | Scope correction: `SST39SF020` is `algorithm: 6` (PROTO_FLASH_NOR_UNLOCK), not protocol `0x05`. It never traverses `flash_5v_page.cpp` and cannot regress from this phase. Success criterion 4 therefore reduces to one bench part plus database and native checks, and the correction is recorded rather than covered by a vacuous bench pass. | reversible |
| D-08 | The host predicate extends `page_size_gate.py` with a second public function rather than opening a sibling module, so `_WRITE_OPERATIONS` and `FLASH4_PROTOCOL_ID` stay single-sourced. The module docstring's purity claim is narrowed in the same edit from "no I/O" to "no transport I/O", because the raiser performs one size probe of the payload path and fails closed on it. | reversible |
| D-09 | The `|| is_first_byte` and `|| is_last_byte` clauses stay in `flash_5v_page_write_execute`. The silicon commits the page by itself 200 microseconds after the last load, so deleting them removes the SDP unlock and the verify poll -- not the commit -- turning a loud corruption into a silent one. Under the guard they are provably redundant, not wrong. | reversible |
| D-10 | The new message carries two params so the operator can tell which clause fired: a `u24` address rendered as a hex address and a `u16` size rendered as decimal, emitted with the file's existing five-byte big-endian pack pattern. | reversible |
| D-11 | Bench evidence runs on `W29C020` on the Leonardo rig, and the loss reproduction leg runs on a firmware build made by reverting exactly one source file to its pre-195 blob -- because once the fix lands, the loss cannot be demonstrated at all. | reversible |
| D-12 | `dev test`'s 256-byte write region at address 0 is **not** changed. It stays aligned for `P` in {64, 128, 256}, which is 25 of 27 parts including all three protocol `0x05` validated parts, and becomes a refused partial page on the two `P = 512` parts, neither of which has ever been tested on silicon. Plan 03 asserts the consequence so it is visible rather than discovered, and plan 04 files the region-rounding proposal. | costly |

**Why D-01 is `costly` and not `one-way`:** the research states that host-side alignment (read the
head and tail page, splice, issue an aligned write) restores the lost capability without touching the
RAM ceiling and can land after the refusal without reopening WRITE-01 or WRITE-02. The undo is an
addition, not a migration. **Why D-02 is `costly` and not `one-way`:** nothing ships in this phase,
so the undo before release is one catalog edit plus one lockstep regeneration -- but the convention
is inherited by every future message across both sub-repos, which puts it above `reversible`.
No decision in this phase is `one-way`, so no `checkpoint:decision` is inserted.

## Spec-less edge-coverage fallback -- no-silent-drop arithmetic

There is no SPEC for this phase, so both the edge and the prohibition sections fall back to the
probe protocol. The edge probe surfaced **7** items across WRITE-01, WRITE-02 and WRITE-03.

**7 surfaced == 6 authored into `must_haves.truths` + 1 surfaced as a flagged assumption.**

| # | Requirement | Category | Domain reading | Status | Verification | Where authored |
|---|---|---|---|---|---|---|
| 1 | WRITE-01 | adjacency | a write that exactly touches a page boundary versus one that lands one byte inside a page | resolved | explicit | truths, plan 01 |
| 2 | WRITE-01 | empty | a zero-length payload and a single-byte payload | resolved | explicit | truths, plans 02 and 03 |
| 3 | WRITE-01 | ordering | chunk ordering across a page boundary -- the interior loss direction | resolved | explicit | truths, plan 02 |
| 4 | WRITE-02 | adjacency | the success line beside the refusal, at the exact boundary case | resolved | explicit | truths, plan 01 |
| 5 | WRITE-02 | empty | whether a zero-length write may still print the success line | resolved | explicit | truths, plan 03 |
| 6 | WRITE-02 | ordering | which layer's message the operator sees when both would refuse | resolved | explicit | truths, plan 01 |
| 7 | WRITE-03 | unclassified | review manually | **unresolved** | -- | flagged assumption below |

**Flagged planner assumption (item 7, WRITE-03, unclassified -- never auto-dismissed).** I assume
WRITE-03's "both loss directions demonstrated on real silicon" can only be satisfied by a bench
session that flashes a **pre-fix** firmware first, because after the fix lands neither direction can
be produced at all. The risk if wrong is that a reviewer expects the demonstration to come from the
post-fix firmware, which would be impossible by construction. Mitigation: plan 05 runs the
reproduction on a build made by reverting exactly one source file, records both builds' size lines,
and states the sequencing in the transcript's own section 3, which is recorded twice for this reason.

**Prohibition probe** -- the kept values and safety items are authored descriptor-less in
`must_haves.prohibitions` above, per plan. **Canon-referral breadcrumb:** untrusted-input validation
of the wire dict, the CLI address argument and the payload path is canon ASVS V5 and V12 -- covered
by the `<threat_model>` block below and by `/gsd-secure-phase`, not minted as a prohibition.

## Artifacts this phase produces

Repeated verbatim in every plan of this phase so a source-grounding pass can exclude newly-created
symbols from drift verification and not flag deleted ones.

**CREATED:**

| Artifact | Repo | Plan |
|---|---|---|
| `MSG_ERR_FL4_PAGE_ALIGN` = `0xC0`, severity ERROR, params `u24` address + `u16` size, `wire_format` `id_frame` | meta catalog, generated into fw + app | 01 |
| the ERROR-band-extension convention line in the `tools/catalog/messages.toml` header | meta | 01 |
| the per-chunk alignment guard in `flash_5v_page_write_execute`, placed before the byte loop | fw | 01 |
| `test_5v_page_write_execute_refuses_unaligned_start` | fw | 01 |
| `test_5v_page_write_execute_accepts_page_exact_write` -- the positive control | fw | 01 |
| `PageAlignmentError` (subclass of `EpromOperationError`) | app | 01 |
| `page_size_gate.require_page_alignment(chip_name, programmer_data, operation, address_str, input_file_path)` | app | 01 |
| `tests/test_page_size_alignment_refusal.py` | app | 01 created, 03 extended |
| `test_5v_page_write_execute_refuses_partial_length`, `..._refuses_unaligned_start_and_partial_length`, `..._refuses_unaligned_second_chunk`, `..._accepts_aligned_two_chunk_p256`, `..._accepts_aligned_two_chunk_p512`, `..._accepts_zero_length_chunk` | fw | 02 |
| `_ACCEPTED_PAGE_SIZES` and the power-of-two plus 512-ceiling test inside `require_page_size` | app | 03 |
| `tests/test_validated_parts_regression_surface.py` | app | 03 |
| `.planning/v1.39/195-partial-write-refusal-record.md` | meta | 04 |
| three pending todos -- host-side alignment splicing, the override flag, and the `dev test` write-region rounding | meta | 04 |
| a `## Notes` bullet in `VALIDATED-EPROMS.md` recording the `SST39SF020` protocol correction | meta | 04 |
| `.planning/v1.39/195-w29c020-partial-write-bench-transcript.md` | meta | 05 |

**DELETED:**

| Artifact | Repo | Plan |
|---|---|---|
| the falsified four-byte clauses on `make_write_handle_with_data` and its leading comment | fw | 01 |
| `.planning/todos/pending/error-message-band-a0-bf-exhausted.md`, moved to `.planning/todos/completed/` | meta | 01 |
| `.planning/todos/pending/host-page-size-gate-accepts-invalid-values.md`, moved to `.planning/todos/completed/` | meta | 03 |

**REGENERATED, never hand-edited:** `firestarter_fw/include/messages.h`,
`firestarter_app/firestarter/messages.py`, `/workspaces/VALIDATED-EPROMS.md`.

**Deliberately unchanged:** `firestarter_app/firestarter/data/chip_database.json`,
`firestarter_app/firestarter/chip_test.py`, `firestarter_fw/src/eprom_operations.cpp`,
`firestarter_fw/src/json_parser.c`, and the `|| is_first_byte` / `|| is_last_byte` clauses in
`flash_5v_page_write_execute` (D-09).

## The measured insertion points

All five were read at planning time against firmware `c2b8baa`, app `3140172`, meta `42b7afd7`.

| Layer | Site | What is there now |
|---|---|---|
| canonical catalog | `tools/catalog/messages.toml`, the `0xBF` stanza at `:700-706` | `MSG_ERR_FL4_PAGE_SIZE`. The `DATA (0xE0..0xEF)` banner follows at `:708-710`. The new stanza goes between them |
| firmware guard | `firestarter_fw/src/proms/flash_5v_page.cpp`, the mask block at `:80-86` | `flash_5v_page_mask` resolves `page_mask` and refuses with `MSG_ERR_FL4_PAGE_SIZE`. The alignment guard goes immediately after this block, still before the `for` loop |
| firmware pack pattern | same file, `:126-131` | a five-byte big-endian pack feeding `LOG_ERROR_ID_BYTES(MSG_ERR_FL4_VERIFY_TIMEOUT, _b, 5)`. The new emit reuses this shape exactly |
| host operator layer | `firestarter_app/firestarter/eprom_operations.py`, `require_page_size(...)` at `:2009` | the last guard before `with self._operation_context(...)` at `:2011`, which is where the port opens. This layer also covers `dev test` |
| host CLI pre-flight | `firestarter_app/firestarter/cli_handlers.py`, `page_size_gate.require_page_size(...)` at `:764` | immediately before `app.eprom_operator.write_eprom(...)` at `:766`. `eprom_data`, `input_file` and `address` are all in scope |
| rendering arm | `firestarter_app/firestarter/cli_handlers.py` `map_typed_errors` | `except PageSizeUnavailableError` renders `str(e)` verbatim; the generic `except EpromOperationError` arm below it prefixes with a programmer-error label. A new subclass needs its own arm above the generic one |

## The one existing native test the guard breaks, and why

`make_write_handle_with_data()` at `test_val_5v_page.cpp:186-201` builds `page_size = 256`,
`address = 0`, `data_size = 4`. `4 & 255 != 0`, so the new guard refuses it, and **two** existing
green cases that share the helper -- `test_5v_page_write_execute_emits_sdp` and
`test_5v_page_write_execute_no_vpp` -- would turn red. This is expected, not a defect: a 4-byte
write to a 256-byte-page part is exactly the partial page this phase exists to refuse.

Raise the helper's `data_size` to `256` -- one whole page at `W29C040`'s real page size. Both
oracles are preserved: exactly one SDP signature (the single page start at address 0 fires it), and
no VPP bit appears in any control-register write. Recording density is three records per written
byte plus eleven for the SDP prefix, so 256 bytes records 779 entries against the 4096 cap and
`bus_recording_saturated()` stays false.

Every other existing geometry is already page-exact and is unaffected: the two `write_init` handles
carry `data_size = 0` (which passes the guard and runs no loop), the seven boundary cases drive
`(64,128)`, `(128,256)`, `(256,512)`, `(512,512)` and the two-chunk `512 @ 512`, and the four
page-size rejection cases refuse at the mask validator before the alignment guard is reached.

<tasks>

<task type="tracer">
  <name>Task 1: Mint the alignment refusal id at 0xC0, record the band convention, sync both sub-repos</name>
  <precondition>`firestarter_app/.venv311/bin/python -m ruff --version` prints a ruff version. `tools/catalog/sync_to_subrepos.sh` writes `messages.py` unnormalised with only a WARNING when `ruff` is not reachable, and `ruff` is not on this devcontainer's PATH -- it lives inside the app's test virtualenv.</precondition>
  <files>tools/catalog/messages.toml, firestarter_fw/include/messages.h, firestarter_app/firestarter/messages.py, .planning/todos/pending/error-message-band-a0-bf-exhausted.md</files>
  <read_first>
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 4 in full -- the band exhaustion measurement, why `0xC0` is mechanically free, which existing guard judges the choice, and the three standing sync traps
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-PATTERNS.md` the `tools/catalog/messages.toml` section -- the `0xBF` stanza to copy, the two-param `MSG_ERR_ENERGY_CAP` stanza for the list form, and the exact insertion position
    - `.planning/todos/pending/error-message-band-a0-bf-exhausted.md` -- the whole file, including the two facts that make the decision tractable and the sentence naming what would close it
    - `tools/catalog/messages.toml` -- the header block at `:1-11`, the `0xBE` and `0xBF` stanzas at `:686-706`, and the DATA banner at `:708-710`
    - `tools/catalog/sync_to_subrepos.sh` -- the two roots it rewrites and the `command -v ruff` branch
    - `firestarter_app/tests/test_protection_status_catalog.py` -- `test_error_band_fully_spent_0xa0_through_0xbf`, the guard that must stay green
  </read_first>
  <action>
Per D-02 and D-10. One catalog edit, one sync, one todo disposition.

STEP 1 -- edit `tools/catalog/messages.toml` and no other catalog copy. Insert a new `[[messages]]`
stanza after the `0xBF` `MSG_ERR_FL4_PAGE_SIZE` stanza and before the `DATA (0xE0..0xEF)` banner,
preserving source order (the file header forbids reordering; codegen sorts by id ascending anyway).

The stanza, field by field:
`id = 0xC0`; `name = "MSG_ERR_FL4_PAGE_ALIGN"`; `severity = "ERROR"`; `wire_format = "id_frame"`.
`format` states the refusal and both offending values, with exactly two printf specifiers so the
codegen format-versus-param-count rule is satisfied -- an address specifier in the `%06lx` shape the
neighbouring `MSG_ERR_FL4_VERIFY_TIMEOUT` already uses, and a decimal specifier for the size. Word it
so the operator learns the write was refused and that the offending property is alignment, not the
page size (the `0xBF` message already owns "page size rejected" and reusing it here would be untrue).
`params` is the two-element list form: `{ type = "u24", render = "hex_addr" }` followed by
`{ type = "u16", render = "dec" }`. `MSG_ERR_ENERGY_CAP` at `:690-698` is the list-form analog.

STEP 2 -- record the convention in the catalog header comment block at `:1-11`, per D-02. Add one
line stating that the `ERROR` severity band `0xA0-0xBF` is fully allocated and that `ERROR`-severity
ids now extend into `0xC0-0xDF`, with severity taken from the explicit `severity` field rather than
inferred from the numeric range. This is the project convention the pending todo asked the next
phase to make and record. Keep it to the header's existing comment style; this is a TOML config file
in the meta repo, not product source, so the no-comments rule does not reach it.

STEP 3 -- validate, then regenerate and sync in one command, with ruff reachable. Run the codegen
check first, then prepend the app virtualenv's `bin` directory to `PATH` for the sync invocation so
`messages.py` is written normalised rather than with a WARNING. Nothing in either sub-repo's CI
catches an unnormalised or a forgotten sync, so confirm by hand that exactly two generated files
moved: `firestarter_fw/include/messages.h` and `firestarter_app/firestarter/messages.py`.

STEP 4 -- prove idempotence. Run the sync a second time and confirm neither generated file is dirty
afterwards. A second run that moves a file means the first run wrote something the generator would
not reproduce.

STEP 5 -- close the band todo, per D-02 and the research's pitfall 3. Move
`.planning/todos/pending/error-message-band-a0-bf-exhausted.md` to `.planning/todos/completed/` with
`git mv`, and append a short closing section naming the decision taken (`0xC0` minted as ERROR,
band extended into `0xC0-0xDF`, convention recorded in the catalog header) and the phase that took
it. Do this in the same commit as the mint, so a future reader cannot find the spent id without
finding the decision.

STEP 6 -- commit. One commit in the meta repo covering the catalog and the todo move. One commit
inside `firestarter_fw` for the regenerated `include/messages.h`. One commit inside
`firestarter_app` for the regenerated `firestarter/messages.py`. Both sub-repo commits land on the
`v1.39-protocol-0x05-write-correctness` branch. Use a plain `git commit`; do not use the GSD commit
verb here, because it scrapes roadmap prose and can switch branches.
  </action>
  <acceptance_criteria>
    - `tools/catalog/messages.toml` contains a stanza with `name = "MSG_ERR_FL4_PAGE_ALIGN"`, `id = 0xC0`, `severity = "ERROR"` and `wire_format = "id_frame"`.
    - That stanza's `params` list has exactly two entries, the first `type = "u24"` with `render = "hex_addr"` and the second `type = "u16"`.
    - The stanza's `format` string contains exactly two printf specifiers, and the string does not contain the words `page size` in the `0xBF` message's "page size %u rejected" sense -- the message names alignment, not page-size validity.
    - The catalog header comment block states that `ERROR` ids extend into `0xC0-0xDF` and that severity comes from the explicit field, not the id range.
    - `python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` exits 0.
    - `firestarter_fw/include/messages.h` defines `MSG_ERR_FL4_PAGE_ALIGN` as `0xC0`.
    - `firestarter_app/firestarter/messages.py` defines `MSG_ERR_FL4_PAGE_ALIGN`.
    - A second consecutive run of `tools/catalog/sync_to_subrepos.sh` leaves both generated files clean in `git status --porcelain`.
    - The sync run did not print `WARNING: ruff not found`.
    - `firestarter_app/tests/test_protection_status_catalog.py::test_error_band_fully_spent_0xa0_through_0xbf` still passes.
    - `.planning/todos/pending/error-message-band-a0-bf-exhausted.md` no longer exists and a file of the same name exists under `.planning/todos/completed/`, carrying a closing section that names `0xC0` and the recorded convention.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check && echo CATALOG-VALID</automated>
    <fails_when>`CATALOG-VALID` is absent from the output, or the codegen prints a `CatalogError` naming a duplicate id, an invalid severity, an invalid param type or a format-versus-param-count mismatch</fails_when>
    <automated>cd /workspaces && PATH="$PWD/firestarter_app/.venv311/bin:$PATH" bash tools/catalog/sync_to_subrepos.sh 2>&1 | tee /tmp/claude-1000/sync1.log | tail -5; ! /usr/bin/grep -q 'ruff not found' /tmp/claude-1000/sync1.log && echo SYNC-NORMALIZED</automated>
    <fails_when>`SYNC-NORMALIZED` is absent from the output, which means the sync ran without ruff on PATH and wrote `messages.py` with pure-formatting drift that no CI gate will catch</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -q 'MSG_ERR_FL4_PAGE_ALIGN' tools/catalog/messages.toml && /usr/bin/grep -Eq '#define[[:space:]]+MSG_ERR_FL4_PAGE_ALIGN[[:space:]]+0xC0' firestarter_fw/include/messages.h && /usr/bin/grep -q 'MSG_ERR_FL4_PAGE_ALIGN' firestarter_app/firestarter/messages.py && echo SYNC-PRESENT</automated>
    <fails_when>`SYNC-PRESENT` is absent from the output. Either the catalog was not edited, or one of the two generated artefacts was not regenerated, or the firmware id was not minted at `0xC0`</fails_when>
    <automated>cd /workspaces && PATH="$PWD/firestarter_app/.venv311/bin:$PATH" bash tools/catalog/sync_to_subrepos.sh >/dev/null 2>&1 && FWP=$(git -C firestarter_fw status --porcelain -- include/messages.h) && APPP=$(git -C firestarter_app status --porcelain -- firestarter/messages.py) && test -z "$FWP$APPP" && echo SYNC-IDEMPOTENT</automated>
    <fails_when>`SYNC-IDEMPOTENT` is absent from the output, which means a second sync run moved a generated file and the committed artefacts are not what the generator produces</fails_when>
    <automated>cd /workspaces && test ! -e .planning/todos/pending/error-message-band-a0-bf-exhausted.md && test -e .planning/todos/completed/error-message-band-a0-bf-exhausted.md && /usr/bin/grep -q '0xC0' .planning/todos/completed/error-message-band-a0-bf-exhausted.md && echo TODO-CLOSED</automated>
    <fails_when>`TODO-CLOSED` is absent from the output. Either the todo is still in `pending/`, or the moved copy does not record the id the decision spent</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_protection_status_catalog.py -o addopts="" -q 2>&1 | tail -3</automated>
    <fails_when>the summary line reports any failures, or no summary line appears -- the band guard asserts `0xA0` through `0xBF` are all present and all ERROR, and minting `0xC0` must leave it green</fails_when>
  </verify>
  <done>Per D-02 and D-10 the alignment refusal has a named, parameterised ERROR id at `0xC0`; the band-extension convention is recorded where the next reader of the catalog will meet it; both generated artefacts are regenerated by the one script that owns them, normalised, and proven idempotent; and the pending band todo is closed in the same change rather than deferred a second time.</done>
</task>

<task type="auto">
  <name>Task 2: The firmware alignment guard, before the first register write, with one refusal case and one positive control</name>
  <precondition>`firestarter_fw/include/messages.h` defines `MSG_ERR_FL4_PAGE_ALIGN`, which task 1 generated. The firmware will not compile against a stale header.</precondition>
  <files>firestarter_fw/src/proms/flash_5v_page.cpp, firestarter_fw/test/native/avr/test_val_5v_page/test_val_5v_page.cpp</files>
  <read_first>
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 1 -- the defect site verbatim, the datasheet quotations on the implicit whole-page erase and the 200-microsecond load window, and the three loss directions
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 3b -- the smallest correct predicate, the mask form, and the arithmetic proving every legitimate chunk of a page-aligned host write is itself page-aligned
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 5 -- the suite to extend, the SDP-signature oracle, the saturation guard and the five restated traps
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-PATTERNS.md` the `flash_5v_page.cpp` and `test_val_5v_page.cpp` sections -- the refusal block to mirror, the macro-selection analog, and the note that the parameterised driver hard-codes address zero
    - `firestarter_fw/src/proms/flash_5v_page.cpp` -- the whole file, in particular the mask block at `:80-86`, the byte loop at `:88-113`, and the five-byte pack at `:126-131`
    - `firestarter_fw/test/native/avr/test_val_5v_page/test_val_5v_page.cpp` -- `make_write_handle_with_data` at `:186-201`, `test_5v_page_write_execute_refuses_with_no_page_size` at `:342-361`, `drive_page_boundary_case` at `:363-377`, and the `RUN_TEST` block at `:750-790`
    - `firestarter_fw/test/native/avr/_shared/host_stubs_common.inc` -- `bus_recording_count`, `recorded_reg`, `clear_bus_recording` and `bus_recording_saturated` at `:168-190`
    - `/workspaces/CLAUDE.md` -- the hard no-comments rule and where rationale goes instead
  </read_first>
  <action>
Per D-04, D-09 and D-10. One guard, one helper repair, two native cases.

STEP 1 -- the guard, in `flash_5v_page_write_execute`. Place it **immediately after** the existing
`flash_5v_page_mask` refusal block and **before** the `for` loop, reusing the `page_mask` that block
just resolved. Position is the whole point: a page load cannot be aborted once bytes are loaded, so
the refusal must happen before the first `firestarter_set_data` of the chunk. Do not recompute the
mask and do not use a modulo by a variable divisor -- that links the software division routine and
costs hot-loop cycles.

The condition refuses when either `(handle->address & page_mask) != 0` or
`(handle->data_size & page_mask) != 0`. The body does three things in this order: pack five bytes
big-endian -- `handle->address` as three bytes most-significant first into indices 0, 1 and 2, then
the low sixteen bits of `handle->data_size` as two bytes most-significant first into indices 3 and 4
-- then `LOG_ERROR_ID_BYTES(MSG_ERR_FL4_PAGE_ALIGN, _b, 5)`, then
`handle->response_code = RESPONSE_CODE_ERROR;` and `return;`. The block at `:126-131` is the
in-file pack precedent and the block at `:80-86` is the in-file refusal precedent; match both.
Use a brace-scoped local array exactly as `:125-133` does, so the array does not live across the
whole function.

STEP 2 -- per D-09, leave `bool is_first_byte` and `bool is_last_byte` and their two `||` clauses
exactly as they are. Under the guard they are provably redundant: with an address and a size that
are both whole multiples of the page, `is_page_start` is already true at `i == 0` and
`reached_page_end` is already true at the final byte. Deleting them would not stop the silicon from
committing the page -- the device commits by itself 200 microseconds after the last load -- it would
only remove the SDP unlock and the verify poll, converting a loud corruption into a silent one. Do
not add a comment saying any of this; it goes in the summary and the commit message.

STEP 3 -- repair `make_write_handle_with_data` in the native suite. Its `data_size = 4` against
`page_size = 256` is a partial page and the new guard refuses it, which would turn
`test_5v_page_write_execute_emits_sdp` and `test_5v_page_write_execute_no_vpp` red. Raise
`data_size` to `256`, one whole page at the part's real page size. Both oracles survive unchanged:
one SDP signature from the single page start, and no VPP bit in any control-register write. Delete
the clauses the change falsifies -- the helper's leading comment says "4-byte zero data buffer" and
the field's trailing comment says "small: 4 zero bytes at page 0". Delete the falsified clauses and
write no replacement; `/workspaces/CLAUDE.md` forbids writing any comment into this tree and says a
plan cannot override it.

STEP 4 -- two new native cases, both driven through `h.firestarter_operation_main(&h)` so the
dispatch is exercised rather than the static function.

`test_5v_page_write_execute_refuses_unaligned_start`: protocol `0x05`, `CMD_WRITE`,
`response_code` starting `RESPONSE_CODE_OK`, `chip_id` zero, `mem_size` 262144, `page_size` 128,
`address` 64, `data_size` 128. Assert `RESPONSE_CODE_ERROR` and `bus_recording_count() == 0`, with
messages in the same voice as the existing refusal case. The zero-writes assertion is the
load-bearing half -- refusing while having already driven the bus is not WRITE-01.

`test_5v_page_write_execute_accepts_page_exact_write`: the same geometry with `address` 128 and
`data_size` 128. Assert `bus_recording_saturated()` is false, `RESPONSE_CODE_OK`, and exactly one
SDP signature from `count_sdp_signatures`. This is the non-vacuity control that distinguishes a
working guard from a blanket refusal, and it also proves a non-zero start address is accepted when
it is page-aligned.

Do not mutate `drive_page_boundary_case` -- it hard-codes `h.address = 0` and seven existing cases
depend on it. Build these two handles inline, following the existing refusal case's shape.

STEP 5 -- register both with `RUN_TEST(...)` in the existing block, grouped beside
`test_5v_page_write_execute_refuses_with_no_page_size`.

STEP 6 -- build and measure. Run both native environments, then both AVR builds, capturing the
`bootloader-guard` line and the flash figures for each. The comparison baselines measured on this
branch are `uno` 21616 of 32256 bytes and `leonardo` 23734 of 28672 bytes; record the deltas. There
is no size-baseline watermark gate on this branch -- `bootloader_guard.py` is the only size
enforcement and it runs only on an ELF relink, so the figures must be captured from a build that
actually relinked. Then run the firmware's own guard scripts and the planning-citation gate.

STEP 7 -- commit inside `firestarter_fw` on the v1.39 branch, with a plain `git commit`.
  </action>
  <acceptance_criteria>
    - `firestarter_fw/src/proms/flash_5v_page.cpp` contains `MSG_ERR_FL4_PAGE_ALIGN` and the string `page_mask` appears in the guard condition rather than a `%` operator.
    - The line number of the `MSG_ERR_FL4_PAGE_ALIGN` emit is lower than the line number of the `for (uint32_t i = 0;` loop header, proving the guard precedes the first register write.
    - `flash_5v_page.cpp` still contains both `is_first_byte` and `is_last_byte`.
    - `make_write_handle_with_data` sets `data_size` to `256`, and neither the helper's leading comment nor the field's trailing comment still claims four bytes.
    - `test_val_5v_page.cpp` defines `test_5v_page_write_execute_refuses_unaligned_start` and `test_5v_page_write_execute_accepts_page_exact_write`, and both appear in a `RUN_TEST` line.
    - The refusal case asserts both `RESPONSE_CODE_ERROR` and `bus_recording_count()` equal to zero.
    - The positive control asserts `bus_recording_saturated()` is false, `RESPONSE_CODE_OK`, and exactly one SDP signature.
    - `pio test -e native` and `pio test -e native_nodevtools` each report every case succeeded, with a total at least two higher than the pre-change count of 208.
    - `pio run -e uno` and `pio run -e leonardo` both succeed and both print a `bootloader-guard` line; the flash figures and their deltas against 21616 and 23734 are recorded in the summary.
    - `scripts/check_cmake_manifest.py` and `scripts/check_erase_no_vpp.py` both exit 0.
    - The firmware planning-citation gate reports `OK:` with a file count and names no scanned file.
    - No new `delayMicroseconds` call with a non-literal argument exists anywhere under `src`, `include`, `lib` or `platform`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_fw && G=$(/usr/bin/grep -n 'MSG_ERR_FL4_PAGE_ALIGN' src/proms/flash_5v_page.cpp | head -1 | cut -d: -f1) && L=$(/usr/bin/grep -n 'for (uint32_t i = 0;' src/proms/flash_5v_page.cpp | head -1 | cut -d: -f1) && test -n "$G" && test -n "$L" && test "$G" -lt "$L" && echo GUARD-BEFORE-LOOP</automated>
    <fails_when>`GUARD-BEFORE-LOOP` is absent from the output. Either the emit is missing, or the loop header was renamed, or the guard sits inside or after the loop -- in which case bytes have already been driven onto the bus when it refuses</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/bin/grep -q 'is_first_byte' src/proms/flash_5v_page.cpp && /usr/bin/grep -q 'is_last_byte' src/proms/flash_5v_page.cpp && ! /usr/bin/grep -Eq 'handle->(address|data_size)[[:space:]]*%' src/proms/flash_5v_page.cpp && echo SHAPE-OK</automated>
    <fails_when>`SHAPE-OK` is absent from the output. Either one of the two clauses was deleted against D-09, or the guard used a modulo by a variable divisor instead of the resolved page mask</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/bin/grep -q 'h.data_size  = 256' test/native/avr/test_val_5v_page/test_val_5v_page.cpp && ! /usr/bin/grep -q '4-byte zero data buffer' test/native/avr/test_val_5v_page/test_val_5v_page.cpp && ! /usr/bin/grep -q '4 zero bytes at page 0' test/native/avr/test_val_5v_page/test_val_5v_page.cpp && echo HELPER-REPAIRED</automated>
    <fails_when>`HELPER-REPAIRED` is absent from the output. Either the shared helper still drives a partial page, or a comment clause the change falsified is still present</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/bin/grep -c 'RUN_TEST(test_5v_page_write_execute_refuses_unaligned_start)\|RUN_TEST(test_5v_page_write_execute_accepts_page_exact_write)' test/native/avr/test_val_5v_page/test_val_5v_page.cpp</automated>
    <fails_when>the printed count is not `2` -- a defined but unregistered case never runs, and its assertions prove nothing</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio test -e native -f "*test_val_5v_page*" 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or any line matching `FAILED` or `:FAIL:` in the tail, or no line reporting a succeeded-case count</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio test -e native 2>&1 | tail -4 && pio test -e native_nodevtools 2>&1 | tail -4</automated>
    <fails_when>either environment's tail reports a non-zero failed count, or reports fewer than 210 total cases, or no summary line appears for one of the two environments</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio run -e uno 2>&1 | /usr/bin/grep -E 'bootloader-guard|Flash:|RAM:|FAILED' | tail -4 && pio run -e leonardo 2>&1 | /usr/bin/grep -E 'bootloader-guard|Flash:|RAM:|FAILED' | tail -4</automated>
    <fails_when>either build prints `FAILED`, or no `bootloader-guard` line appears for an environment, which means the guard did not run because nothing relinked and the recorded flash figure is stale</fails_when>
    <automated>cd /workspaces/firestarter_fw && python3 scripts/check_cmake_manifest.py && python3 scripts/check_erase_no_vpp.py && python3 tools/planning_citation_gate.py src include test tests scripts platform tools name_firmware.py zero_bootloader_reserve.py bootloader_guard.py</automated>
    <fails_when>non-zero exit, or the citation gate names any scanned file instead of reporting `OK:` with a file count</fails_when>
  </verify>
  <done>Per D-04 the firmware refuses any protocol `0x05` chunk whose address or size is not a whole multiple of the resolved page size, and it refuses before a single register write, proven by an explicit zero-recording assertion rather than by the absence of an expected id. Per D-09 the two redundant clauses stay, because deleting them removes the unlock and the poll rather than the commit. The shared helper that drove a partial page is repaired to a whole page, so the two pre-existing oracles keep their meaning instead of being silently weakened.</done>
</task>

<task type="auto">
  <name>Task 3: The host pre-connect alignment refusal, wired into both layers and rendered verbatim</name>
  <precondition>`firestarter_app/.venv311/bin/python --version` reports a 3.11 version. The app CI floor is Python 3.11 and the devcontainer default is 3.12; if the virtualenv is missing or its interpreter is gone, recreate it with `UV_CACHE_DIR` pointed at a writable directory before running anything, because `uv` fails hard on an unwritable cache.</precondition>
  <files>firestarter_app/firestarter/page_size_gate.py, firestarter_app/firestarter/exceptions.py, firestarter_app/firestarter/eprom_operations.py, firestarter_app/firestarter/cli_handlers.py, firestarter_app/tests/test_page_size_alignment_refusal.py</files>
  <read_first>
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 2 -- the six-step chain from the firmware poll to the success line, and the two levers that satisfy WRITE-02
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 3b -- why only a host pre-connect refusal can claim the device is unchanged, the two insertion points, and the measured table of what refusing breaks
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-PATTERNS.md` the `page_size_gate.py`, `exceptions.py`, `cli_handlers.py`, `eprom_operations.py` and test-module sections -- every analog is verbatim there
    - `firestarter_app/firestarter/page_size_gate.py` -- the whole module: the docstring's stated purity and fail-closed polarity, `_WRITE_OPERATIONS`, `_REFUSAL_FORMAT`, `requires_page_size` and `require_page_size`
    - `firestarter_app/firestarter/exceptions.py` -- `EpromOperationError`, `ChipNotImplementedError` and `PageSizeUnavailableError` at `:92-102`
    - `firestarter_app/firestarter/cli_handlers.py` -- `map_typed_errors` at `:188-221` noting the arm ordering, and the write pre-flight at `:762-766`
    - `firestarter_app/firestarter/eprom_operations.py` -- `write_eprom`'s signature at `:1968-1977`, the guard calls at `:2003-2009`, the operation context at `:2011`, and `parse_address` use at `:481-486`
    - `firestarter_app/tests/test_page_size_write_refusal.py` -- the whole module, which is the drop-in template for all four properties including the CliRunner leg
  </read_first>
  <action>
Per D-01, D-03 and D-08. One new exception, one new public function, two call sites, one rendering
arm, one new test module.

STEP 1 -- `firestarter/exceptions.py`. Add `PageAlignmentError` as a subclass of
`EpromOperationError`, beside `PageSizeUnavailableError` and in the same style. Its docstring states
the property: the guard fires before any wire dict reaches the transport and before any serial byte
is emitted, because a protocol `0x05` page commit erases every byte of the touched page that was not
loaded, and that erase cannot be undone once the load has started. Subclass `EpromOperationError` so
the CLI's typed-error mapper already handles the family; step 4 gives it its own arm.

STEP 2 -- `firestarter/page_size_gate.py`, per D-08: extend the existing module, do not open a
sibling. Add one public function
`require_page_alignment(chip_name, programmer_data, operation, address_str, input_file_path)`
returning `None`, with the same two early returns the existing raiser has -- return immediately when
`operation` is not in `_WRITE_OPERATIONS`, and again when `requires_page_size(programmer_data)` is
false. Neither early return may touch the filesystem, so a non-write or non-`0x05` call stays free.

After the early returns it resolves three values and fails closed on each:
the page size from the wire dict under `JSON_KEY_PAGE_SIZE` (absent or falsy raises -- this is
already `require_page_size`'s job at the same call sites, and duplicating the check here keeps the
function correct when called alone);
the start address as `parse_address(address_str) or 0`, importing `parse_address` from
`firestarter.address_parser`, and raising `PageAlignmentError` on `ValueError` rather than logging
and returning as `_setup_operation` does at `:484-486`;
the payload length as `os.path.getsize(input_file_path)`, raising `PageAlignmentError` on `OSError`.

Then the predicate: raise `PageAlignmentError` unless both `start % page_size == 0` and
`length % page_size == 0`. Note that a zero length satisfies the second clause and must **not** be
refused -- an empty payload drives no page cycle at all, and refusing it would regress the existing
empty-input behaviour.

Add a module-level refusal format constant beside `_REFUSAL_FORMAT`, rendered with
`chip_name.upper()`, and naming all four numbers the operator needs: the page size, the start
address, the payload length, and which clause failed is inferable from them. State plainly that a
partial page write erases every byte of the touched page that was not sent. One format constant, one
home for the wording.

Narrow the module docstring in the same edit, per D-08. The current text claims "no I/O"; this
function performs one size probe of a path the operator named. Change the claim to "no transport
I/O" and state that the single filesystem touch is that size probe and that it fails closed. Do not
weaken the fail-closed polarity paragraph. Docstrings are string expressions, not comments, and the
citation gate never scans them -- this is the legal home for the rationale.

STEP 3 -- both call sites. In `firestarter/eprom_operations.py`, add the alignment call immediately
after the existing `require_page_size(...)` line and before `with self._operation_context(...)`,
passing `eprom_name`, `eprom_data_dict`, `"write"`, `address_str` and `input_file_path`. This is the
layer that matters, because the port opens inside that context and this layer also covers `dev test`
and every non-CLI caller. In `firestarter/cli_handlers.py`, add the sibling call immediately after
`page_size_gate.require_page_size(eprom, eprom_data, "write")`, passing `eprom`, `eprom_data`,
`"write"`, `address` and `input_file`. Let the exception propagate to the typed-error mapper; do not
call `sys.exit` directly. Match each layer's existing call style -- bare names at the operator layer,
module-prefixed at the CLI layer.

STEP 4 -- the rendering arm in `map_typed_errors`. Add `except PageAlignmentError` raising a Click
exception carrying `str(e)` verbatim, placed **above** the generic `except EpromOperationError` arm.
Below it the arm is unreachable and the operator would receive the chip-naming message behind a
generic programmer-error prefix. The existing `PageSizeUnavailableError` arm is the positional model.

STEP 5 -- per D-03, ship no override flag. Do not add an option to the `write` command, and do not
add a bypass parameter to the guard. An override that proceeds and then prints the plain success
line violates WRITE-02 literally; plan 04 files it as a separable decision with its own output
contract.

STEP 6 -- `tests/test_page_size_alignment_refusal.py`, a new module built on
`tests/test_page_size_write_refusal.py`'s structure. The tracer needs three legs now; plan 03
extends the module to the full matrix.

Leg 1, refusal before the port opens: build a real wire dict for `W29C020` from the live database
with the no-local-override convention, write a 64-byte file into `tmp_path`, patch
`EpromOperator._operation_context`, call `write_eprom` with `address_str="0x40"`, assert
`PageAlignmentError` is raised, assert the context mock was never called, and assert the message
contains `W29C020`.

Leg 2, page-exact pass-through: the same wire dict, a 128-byte file, no address, with the context
mock's enter and exit configured as the template does. Assert no exception and that the context mock
was called once. This is what distinguishes a working guard from a blanket refusal.

Leg 3, verbatim rendering: assert through `map_typed_errors` that a `PageAlignmentError` surfaces as
a Click exception whose message equals the raised text with no generic prefix, and assert through
`CliRunner` on the real `write` command that the exit code is non-zero, the message appears in the
output, and the string `Programmer error` does not.

The module docstring carries the property list, exactly as the template's does. It may name its
origin phase, because docstrings are never scanned by the citation gate -- but nothing outside a
docstring may.

STEP 7 -- run the new module, then the whole app suite on Python 3.11 against a committed tree, then
mypy at the watermark and both ruff gates over `firestarter/` and `tests/`, then the app
planning-citation gate. Commit inside `firestarter_app` on the v1.39 branch with a plain
`git commit`.
  </action>
  <acceptance_criteria>
    - `firestarter_app/firestarter/exceptions.py` defines `PageAlignmentError` and it is a subclass of `EpromOperationError`.
    - `firestarter_app/firestarter/page_size_gate.py` defines `require_page_alignment` and imports `parse_address` from `firestarter.address_parser`.
    - `require_page_alignment` raises `PageAlignmentError` for a write against a protocol `0x05` wire dict with page size 128 when the start address is `0x40`, and again when the payload length is 64.
    - `require_page_alignment` returns without raising for a start of 0 and a length of 128, for a start of 128 and a length of 256, and for a length of 0.
    - `require_page_alignment` returns without raising for any operation that is not a write, and for any wire dict whose algorithm is not the flash4 protocol id, without touching the filesystem on those paths.
    - `require_page_alignment` raises `PageAlignmentError` rather than returning when the payload path does not exist, and rather than logging when the address string cannot be parsed.
    - The raised message contains the chip name upper-cased, the page size, the start address and the payload length.
    - `page_size_gate.py` contains no import of a serial, usb, transport, probe or firmware-version symbol.
    - The module docstring no longer claims "no I/O" and states the single filesystem touch and its fail-closed polarity.
    - `firestarter_app/firestarter/eprom_operations.py` calls `require_page_alignment` on a line whose number is lower than the line of `with self._operation_context(`.
    - `firestarter_app/firestarter/cli_handlers.py` calls the alignment guard in the write pre-flight, and its `except PageAlignmentError` arm has a lower line number than its generic `except EpromOperationError` arm.
    - `firestarter_app/firestarter/cli_handlers.py` gained no new `click.option` on the `write` command.
    - `tests/test_page_size_alignment_refusal.py` reports at least 4 tests, all passing.
    - The whole `firestarter_app` suite reports 0 failed on Python 3.11, run against a committed tree, with the coverage gate satisfied.
    - mypy at the watermark, `ruff check` and `ruff format --check` all pass over `firestarter/` and `tests/`, and the app planning-citation gate reports `OK:` with a file count.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "
import os, tempfile
from firestarter import page_size_gate as g
from firestarter.exceptions import PageAlignmentError, EpromOperationError
from firestarter.flash4_erase_gate import FLASH4_PROTOCOL_ID
assert issubclass(PageAlignmentError, EpromOperationError)
d = {'algorithm': FLASH4_PROTOCOL_ID, 'page-size': 128}
other = {'algorithm': 13, 'page-size': 128}
tmp = tempfile.mkdtemp()
def mk(n):
    p = os.path.join(tmp, 'p%d.bin' % n)
    open(p, 'wb').write(b'\x55' * n)
    return p
p128, p64, p256, p0 = mk(128), mk(64), mk(256), mk(0)
g.require_page_alignment('w29c020', d, 'write', None, p128)
g.require_page_alignment('w29c020', d, 'write', '0x80', p256)
g.require_page_alignment('w29c020', d, 'write', None, p0)
g.require_page_alignment('w29c020', d, 'read', '0x40', p64)
g.require_page_alignment('at28c256', other, 'write', '0x40', p64)
bad = [('0x40', p128), (None, p64), ('0x40', p64)]
for a, f in bad:
    try:
        g.require_page_alignment('w29c020', d, 'write', a, f)
    except PageAlignmentError as e:
        s = str(e)
        assert 'W29C020' in s, s
        assert '128' in s, s
    else:
        raise SystemExit('FAIL: the alignment guard did not refuse %r' % ((a, f),))
for a, f in [('zzz', p128), (None, os.path.join(tmp, 'missing.bin'))]:
    try:
        g.require_page_alignment('w29c020', d, 'write', a, f)
    except PageAlignmentError:
        pass
    else:
        raise SystemExit('FAIL: the alignment guard failed OPEN on an unresolvable input')
print('ALIGN-OK fail-closed on unaligned start, partial length, both, bad address and missing file')
"</automated>
    <fails_when>`ALIGN-OK` is absent from the output, or the output contains `FAIL:` which means the guard either accepted a partial page or failed open on an unresolvable input, or a raised message omits the upper-cased chip name or the page size</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -Eq 'import (serial|usb)|serial_comm|_probe_port|firmware_version' firestarter/page_size_gate.py && ! /usr/bin/grep -q 'no I/O' firestarter/page_size_gate.py && echo PURE-OK</automated>
    <fails_when>`PURE-OK` is absent from the output. Either the gate module reaches for transport, probing or firmware-version state, or the docstring still carries the now-false "no I/O" claim</fails_when>
    <automated>cd /workspaces/firestarter_app && A=$(/usr/bin/grep -n 'require_page_alignment' firestarter/eprom_operations.py | head -1 | cut -d: -f1) && C=$(/usr/bin/grep -n 'with self._operation_context(' firestarter/eprom_operations.py | head -1 | cut -d: -f1) && test -n "$A" && test -n "$C" && test "$A" -lt "$C" && ARM=$(/usr/bin/grep -n 'except PageAlignmentError' firestarter/cli_handlers.py | head -1 | cut -d: -f1) && GEN=$(/usr/bin/grep -n 'except EpromOperationError' firestarter/cli_handlers.py | head -1 | cut -d: -f1) && test -n "$ARM" && test "$ARM" -lt "$GEN" && /usr/bin/grep -q 'require_page_alignment' firestarter/cli_handlers.py && echo WIRING-OK</automated>
    <fails_when>`WIRING-OK` is absent from the output. Either the operator-layer call is missing or sits after the port opens, or the guard is wired into only one layer leaving `dev test` unguarded, or the new except arm is at or below the generic arm and therefore unreachable</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_page_size_alignment_refusal.py -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>the summary line reports any failures, or reports fewer than 4 tests, or no summary line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && PORC=$(git status --porcelain) && test -z "$PORC" && .venv311/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>non-zero exit from the porcelain emptiness test, which means an uncommitted path exists and the suite was run against a tree that does not match the commit. Also fails when the pytest summary line reports any failures, or when no summary line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python tools/check_mypy_watermark.py && .venv311/bin/python -m ruff check firestarter/ tests/ && .venv311/bin/python -m ruff format --check firestarter/ tests/ && python3 tools/planning_citation_gate.py firestarter tests tools</automated>
    <fails_when>non-zero exit, or the watermark script reports a count above its recorded watermark, or ruff names a file it would reformat, or the citation gate names any scanned file instead of reporting `OK:` with a file count</fails_when>
  </verify>
  <done>Per D-01 the host refuses an unaligned or partial-page protocol `0x05` write before the port opens, from both the CLI pre-flight and the operator layer that also covers `dev test`, with a message naming the chip and both offending numbers. Per D-08 the predicate lives in the existing gate module with its purity claim narrowed honestly rather than left false. Per D-03 no override ships. The refusal reaches the operator verbatim, proven by a rendering test and a real CLI invocation rather than by reading the arm order.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; threats rated `high` or above block the phase.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments to the host predicate | The `-a` address string and the payload path are operator-supplied and reach the predicate as control inputs, not merely as data |
| resolved wire dict to the host predicate | The page size may originate in a user override file that never passed the database generator's validator |
| host to serial to the firmware JSON parser | `address` and `page_size` cross the wire; the firmware never receives the total payload length, so it cannot reason about the write as a whole |
| firmware guard to the silicon | A page load cannot be aborted once bytes are loaded; everything after the first byte load is unrecoverable |
| typed exception to the operator's terminal | An arm placed below the generic family arm is unreachable, so the operator would see a generic prefix instead of the chip-naming refusal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-01 | Tampering | a page cycle driven over a partially loaded page | critical | mitigate | The host predicate refuses before the port opens and the firmware guard refuses before the first register write. Both are proven by explicit negative assertions -- the context mock is never called, and the bus recording count is zero |
| T-195-02 | Repudiation | a write reporting success over bytes it erased | high | mitigate | The refusal raises rather than returning a status, so the success line at the end of `write_eprom` is unreachable and the CLI maps the exception to a non-zero exit through the typed-error mapper. A `CliRunner` leg asserts the exit code and the absence of the success line |
| T-195-03 | Tampering | a guard that fails open on an unresolvable input | high | mitigate | An unparseable address and an unreadable payload path both raise rather than proceed, diverging deliberately from `_setup_operation`'s log-and-return. A verify leg drives both and requires a raise |
| T-195-04 | Elevation of privilege | a refusal wired into the CLI layer only | high | mitigate | The guard is called at the operator layer immediately before the operation context opens the port, which covers `dev test` and every library caller. A verify leg asserts the call exists in both files and precedes the context |
| T-195-05 | Tampering | a page size from a user override file that the database generator never validated | medium | mitigate | Plan 03 folds in the power-of-two and 512-ceiling test so the host rejects the same set the firmware rejects. Until then the firmware still refuses and performs zero register writes, so the cost is the quality of the failure rather than its safety |
| T-195-06 | Repudiation | an unreachable rendering arm | medium | mitigate | A verify leg compares the new arm's line number against the generic family arm's, and a rendering test asserts the message carries no generic prefix |
| T-195-07 | Tampering | a hand edit to a generated artefact | medium | mitigate | Both generated files are written only by the meta repo's sync script, the sync is run twice to prove idempotence, and the run is required to have had `ruff` reachable |
| T-195-08 | Denial of service | a firmware page-staging buffer exhausting RAM | high | accept | No staging buffer is allocated. The measured cost of a 512-byte buffer is 142 bytes of remaining RAM on `uno` for the entire call stack, which is why D-01 chose refusal over firmware read-modify-write |
| T-195-09 | Information disclosure | a new host against old firmware still corrupting silently | high | accept | Deliberately out of scope, already filed as a pending skew todo by Phase 194. The host does not gate on firmware version, because the version probe truncates the prerelease suffix |
| T-195-10 | Tampering | npm, pip or cargo installs | high | accept | This plan installs zero packages and changes no dependency manifest. The research records no package to audit, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` exits 0.
- `MSG_ERR_FL4_PAGE_ALIGN` is `0xC0` in `firestarter_fw/include/messages.h` and present in `firestarter_app/firestarter/messages.py`.
- A second `sync_to_subrepos.sh` run leaves both generated files clean, and the first run did not warn about ruff.
- The band todo is under `.planning/todos/completed/` and names `0xC0`.
- The firmware alignment emit precedes the byte loop; both `is_first_byte` and `is_last_byte` survive.
- `pio test -e native` and `pio test -e native_nodevtools` each report every case succeeded, at least 210 cases.
- Both AVR builds succeed and print a `bootloader-guard` line; flash deltas are recorded.
- `tests/test_page_size_alignment_refusal.py` reports at least 4 tests, all passing.
- The whole app suite reports 0 failed on Python 3.11 against a committed tree.
- Both planning-citation gates report `OK:` with a file count.
- `git status --porcelain` is empty in both sub-repos.
</verification>

<success_criteria>
One unaligned protocol `0x05` write is refused end to end. The host refuses before the port opens,
with a message naming the chip, the page size, the start address and the payload length, rendered
verbatim and exiting non-zero. The firmware independently refuses the same chunk with zero register
writes. A page-exact write at a non-zero page-aligned start still succeeds and still emits exactly
one SDP signature, so the guard is demonstrably not a blanket refusal. The refusal id is minted at
`0xC0` with its band convention recorded and the pending todo closed, and both generated artefacts
are regenerated by the one script that owns them.
</success_criteria>

<output>
Create `.planning/phases/195-partial-writes-stop-destroying-the-page/195-01-SUMMARY.md` when done.
Record the flash deltas for both AVR environments against 21616 and 23734, the decision to keep the
two redundant clauses (D-09) with its datasheet grounds, the repair to the shared native helper and
why two pre-existing cases would otherwise have turned red, and the exact wording chosen for both the
firmware and host refusal messages.
</output>
