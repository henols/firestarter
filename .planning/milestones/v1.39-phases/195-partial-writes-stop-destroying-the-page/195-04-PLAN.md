---
phase: 195-partial-writes-stop-destroying-the-page
plan: 04
type: execute
wave: 3
depends_on: [195-02, 195-03]
files_modified:
  - .planning/v1.39/195-partial-write-refusal-record.md
  - .planning/todos/pending/host-side-page-alignment-splicing.md
  - .planning/todos/pending/protocol-0x05-write-override-flag-output-contract.md
  - .planning/todos/pending/dev-test-write-region-rounds-to-page-size.md
  - VALIDATED-EPROMS.md
  - firestarter_fw
  - firestarter_app
autonomous: true
requirements: [WRITE-01, WRITE-02]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-01: the record states the fix shape chosen, the two measured facts that decided it -- a 512-byte firmware staging buffer leaves 142 bytes of RAM on uno for the whole call stack, and the firmware never learns the total payload length -- and the capability the refusal costs, so a reader meets the reasoning rather than only the outcome."
    - "D-07: the scope correction that SST39SF020 is not a protocol 0x05 part is recorded where a reader of the validated-parts ledger will meet it, not only in a phase document."
    - "D-03 and the deferred host-side alignment strand are filed as pending todos with their measured grounds, so the capability the refusal costs is actionable rather than lost."
    - "D-12: the dev test region consequence is filed with the proposal that would resolve it, rather than folded into a safety fix."
    - "The meta repository's two submodule pointers are advanced to the commits this phase produced, so the phase is reproducible from the meta repo alone."
  artifacts:
    - path: .planning/v1.39/195-partial-write-refusal-record.md
      provides: "the phase's decision record -- the fix shape with its measured grounds, the scope correction, criterion 4's reduced shape, and the capability cost"
      contains: "SST39SF020"
    - path: VALIDATED-EPROMS.md
      provides: "a Notes bullet recording that SST39SF020 is a different protocol and is outside this phase's blast radius"
      contains: "SST39SF020"
    - path: .planning/todos/pending/host-side-page-alignment-splicing.md
      provides: "the deferred strand that would restore unaligned writes by reading the head and tail page host-side and issuing an aligned write"
  key_links:
    - from: ".planning/v1.39/195-partial-write-refusal-record.md"
      to: "VALIDATED-EPROMS.md Notes"
      via: "the scope correction recorded in both places, because the ledger is where a reader meets the four validated parts and the phase record is where they meet the reasoning"
    - from: "the three filed todos"
      to: "the record's capability-cost section"
      via: "each todo names the measured grounds the record carries, so neither document has to be read to act on the other"
  prohibitions:
    - "Must NOT hand-edit the validated-parts ledger's generated table. Only its Notes section is preserved verbatim across a rewrite; everything else is rendered from the ledger's own records, and a hand edit fails the ledger's own check."
    - "Must NOT mark WRITE-01, WRITE-02 or WRITE-03 Complete. The milestone requires bench validation on real silicon and a green native test is explicitly not sufficient, so the requirement flips belong to the bench plan that produces the silicon evidence."
    - "Must NOT claim the phase preserves untouched bytes. It refuses; the preserving shape was measured out of firmware RAM and deliberately deferred to a filed strand."
    - "Must NOT claim the firmware guard alone leaves the device unchanged. Only the host pre-connect refusal can make that claim, because the firmware never learns the total payload length."
    - "Must NOT present the derived interior-loss direction as an observed silicon result. It is derived from source plus a datasheet quotation and has never been run."
    - "Must NOT run any GSD state-sync verb against the project state file. Several such verbs corrupt its frontmatter, and any note this phase writes is a hand edit."
    - "Must NOT use the GSD commit verb. It scrapes roadmap prose and can switch branches; use a plain git commit."
    - "Must NOT push any branch."
---

<objective>
Record what was decided, why, and what it cost -- then file the three strands this phase deliberately
did not build, and advance the meta repository's submodule pointers.

Purpose: the software evidence is complete after plans 01 through 03, but the reasoning that
produced it lives only in research and plan text. Three things must survive this phase as
first-class project records: the fix-shape decision with the two measurements that forced it, the
scope correction that one of the four named validated parts is not a protocol `0x05` part at all,
and the capability the refusal costs together with the strand that would restore it. Without the
third, a real usability regression becomes invisible the moment the phase seals.

Output: a committed phase record, three pending todos, a ledger Notes bullet, a reply on the
originating issue carrying the scope correction, and both gitlinks advanced.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md
@.planning/phases/195-partial-writes-stop-destroying-the-page/195-01-SUMMARY.md
@.planning/phases/195-partial-writes-stop-destroying-the-page/195-02-SUMMARY.md
@.planning/phases/195-partial-writes-stop-destroying-the-page/195-03-SUMMARY.md
@.planning/v1.39/194-page-size-27-row-record.md
</context>

## Phase decisions closed by this plan set

There is no `195-CONTEXT.md`. The operator chose to plan without one and delegated the fix-shape
decision to the planner, on the research evidence. These are the closures, each one line, repeated
verbatim in every plan of this phase.

| ID | Decision | Reversibility |
|---|---|---|
| D-01 | The fix shape is a two-layer **refusal**: a host pre-flight predicate `S % P == 0 && L % P == 0` that runs before the port opens, plus a firmware per-chunk guard. Read-modify-write is not built. | costly |
| D-02 | The new ERROR-severity message id is `0xC0`, extending the ERROR band past the exhausted `0xA0-0xBF` range; the convention is recorded in the catalog header and the pending band todo is closed in the same commit. | costly |
| D-03 | No override flag ships in this phase. An override that proceeds and then prints the plain success line violates WRITE-02 literally, and its output contract is a separable usability decision. Filed as a todo by plan 04. | reversible |
| D-04 | The firmware guard **refuses**; it never warns and proceeds. With a passing host predicate no legitimate chunk is ever misaligned, so the guard has zero false positives and is the only defence against an old, third-party or downgraded host. | reversible |
| D-05 | The pending `host-page-size-gate-accepts-invalid-values` todo is folded in: `require_page_size` gains the same power-of-two and 512-ceiling test the firmware applies, so the two layers agree on which page sizes are valid. Plan 03 implements it. | reversible |
| D-06 | The third loss direction -- interior loss at a chunk boundary, derived from source and never observed -- is modelled in the native suite by plan 02 and attempted on the bench by plan 05, and its result is recorded either way, including "not attempted, with the reason". | reversible |
| D-07 | Scope correction: `SST39SF020` is `algorithm: 6` (PROTO_FLASH_NOR_UNLOCK), not protocol `0x05`. It never traverses `flash_5v_page.cpp` and cannot regress from this phase. Success criterion 4 therefore reduces to one bench part plus database and native checks, and the correction is recorded rather than covered by a vacuous bench pass. | reversible |
| D-08 | The host predicate extends `page_size_gate.py` with a second public function rather than opening a sibling module, so `_WRITE_OPERATIONS` and `FLASH4_PROTOCOL_ID` stay single-sourced. The module docstring's purity claim is narrowed in the same edit from "no I/O" to "no transport I/O", because the raiser performs one size probe of the payload path and fails closed on it. | reversible |
| D-09 | The `|| is_first_byte` and `|| is_last_byte` clauses stay in `flash_5v_page_write_execute`. The silicon commits the page by itself 200 microseconds after the last load, so deleting them removes the SDP unlock and the verify poll -- not the commit -- turning a loud corruption into a silent one. Under the guard they are provably redundant, not wrong. | reversible |
| D-10 | The new message carries two params so the operator can tell which clause fired: a `u24` address rendered as a hex address and a `u16` size rendered as decimal, emitted with the file's existing five-byte big-endian pack pattern. | reversible |
| D-11 | Bench evidence runs on `W29C020` on the Leonardo rig, and the loss reproduction leg runs on a firmware build made by reverting exactly one source file to its pre-195 blob -- because once the fix lands, the loss cannot be demonstrated at all. | reversible |
| D-12 | `dev test`'s 256-byte write region at address 0 is **not** changed. It stays aligned for `P` in {64, 128, 256}, which is 25 of 27 parts including all three protocol `0x05` validated parts, and becomes a refused partial page on the two `P = 512` parts, neither of which has ever been tested on silicon. Plan 03 asserts the consequence so it is visible rather than discovered, and plan 04 files the region-rounding proposal. | costly |

## Spec-less edge-coverage fallback -- no-silent-drop arithmetic

The edge probe surfaced **7** items across WRITE-01, WRITE-02 and WRITE-03.
**7 surfaced == 6 authored into `must_haves.truths` + 1 surfaced as a flagged assumption.**
The full table and the flagged assumption are recorded once in `195-01-PLAN.md`. This plan authors
no new edge predicate; it records the reasoning and the costs behind the ones plans 01 through 03
authored, and it is the plan that keeps the flagged WRITE-03 assumption visible by writing the
sequencing constraint into the phase record rather than leaving it in plan text.

**Prohibition probe** -- the kept values and safety items are authored descriptor-less in
`must_haves.prohibitions` above. **Canon-referral breadcrumb:** no untrusted-input surface is touched
by this plan; it writes records and moves file pointers.

## Artifacts this phase produces

Repeated verbatim in every plan of this phase so a source-grounding pass can exclude newly-created
symbols from drift verification and not flag deleted ones.

**CREATED:**

| Artifact | Repo | Plan |
|---|---|---|
| `MSG_ERR_FL4_PAGE_ALIGN` = `0xC0`, severity ERROR, params `u24` address + `u16` size, `wire_format` `id_frame` | meta catalog, generated into fw + app | 01 |
| the ERROR-band-extension convention line in the `tools/catalog/messages.toml` header | meta | 01 |
| the per-chunk alignment guard in `flash_5v_page_write_execute`, placed before the byte loop | fw | 01 |
| `test_5v_page_write_execute_refuses_unaligned_start` | fw | 01 |
| `test_5v_page_write_execute_accepts_page_exact_write` -- the positive control | fw | 01 |
| `PageAlignmentError` (subclass of `EpromOperationError`) | app | 01 |
| `page_size_gate.require_page_alignment(chip_name, programmer_data, operation, address_str, input_file_path)` | app | 01 |
| `tests/test_page_size_alignment_refusal.py` | app | 01 created, 03 extended |
| `test_5v_page_write_execute_refuses_partial_length`, `..._refuses_unaligned_start_and_partial_length`, `..._refuses_unaligned_second_chunk`, `..._accepts_aligned_two_chunk_p256`, `..._accepts_aligned_two_chunk_p512`, `..._accepts_zero_length_chunk` | fw | 02 |
| `_ACCEPTED_PAGE_SIZES` and the power-of-two plus 512-ceiling test inside `require_page_size` | app | 03 |
| `tests/test_validated_parts_regression_surface.py` | app | 03 |
| `.planning/v1.39/195-partial-write-refusal-record.md` | meta | 04 |
| three pending todos -- host-side alignment splicing, the override flag, and the `dev test` write-region rounding | meta | 04 |
| a `## Notes` bullet in `VALIDATED-EPROMS.md` recording the `SST39SF020` protocol correction | meta | 04 |
| `.planning/v1.39/195-w29c020-partial-write-bench-transcript.md` | meta | 05 |

**DELETED:**

| Artifact | Repo | Plan |
|---|---|---|
| the falsified four-byte clauses on `make_write_handle_with_data` and its leading comment | fw | 01 |
| `.planning/todos/pending/error-message-band-a0-bf-exhausted.md`, moved to `.planning/todos/completed/` | meta | 01 |
| `.planning/todos/pending/host-page-size-gate-accepts-invalid-values.md`, moved to `.planning/todos/completed/` | meta | 03 |

**REGENERATED, never hand-edited:** `firestarter_fw/include/messages.h`,
`firestarter_app/firestarter/messages.py`, `/workspaces/VALIDATED-EPROMS.md`.

**Deliberately unchanged:** `firestarter_app/firestarter/data/chip_database.json`,
`firestarter_app/firestarter/chip_test.py`, `firestarter_fw/src/eprom_operations.cpp`,
`firestarter_fw/src/json_parser.c`, and the `|| is_first_byte` / `|| is_last_byte` clauses in
`flash_5v_page_write_execute` (D-09).

<tasks>

<task type="auto">
  <name>Task 1: The phase record -- the decision, its measured grounds, and what it costs</name>
  <files>.planning/v1.39/195-partial-write-refusal-record.md</files>
  <read_first>
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` sections 1, 3 and 7 -- the defect mechanism with the datasheet quotations, the full fix-shape comparison with its measured RAM table, and the regression surface with the protocol correction
    - `.planning/v1.39/194-page-size-27-row-record.md` -- the sibling phase record, for the section shape this project uses and for its evidence-class discipline
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-01-SUMMARY.md`, `195-02-SUMMARY.md` and `195-03-SUMMARY.md` -- the measured figures, case names and partition counts this record cites
    - `.planning/REQUIREMENTS.md` -- D-1 through D-5 and WRITE-01 through WRITE-03 verbatim, so the record quotes rather than paraphrases the contract it answers
  </read_first>
  <action>
Per D-01, D-06, D-07 and D-12. One document, in this project's phase-record voice: every figure
carries the command or the source that produced it, and nothing is carried forward from research
without a citation.

Frontmatter in the shape `194-page-size-27-row-record.md` uses: a title, the phase slug, the
measurement date, a status line stating exactly what the document is authoritative for and what it
is not, a `pairs_with` pointer to the bench transcript plan 05 will produce, and the requirement ids
it speaks to.

Section 1 -- **the defect, and why the two clauses exist.** The mechanism from the part's own
datasheet: the page erase is implicit, whole-page and unconditional, so any byte not loaded is
erased when the page commits. Quote the datasheet sentence verbatim with its source. Name the two
source clauses that turn a partial chunk into a complete page cycle, and state per D-09 that they
were deliberately kept, with the reason: the device commits by itself 200 microseconds after the
last load, so deleting the clauses removes the unlock and the poll rather than the commit.

Section 2 -- **the three loss directions**, with the predicate for each. Mark the first two as
reported with silicon evidence on the originating issue, and mark the third as **derived from source
and a datasheet quotation and never observed**. Per D-06 that classification is the point; do not
let the third read as an observed result.

Section 3 -- **the fix shape, and the two measurements that decided it.** State the shape chosen
per D-01. Then the two facts, both with their measurement: a 512-byte firmware staging buffer was
measured to leave 142 bytes of RAM on `uno` and 213 on `leonardo` for the entire call stack, which
is a stack overflow rather than a tight fit; and the firmware never receives the total payload
length, so a firmware-only refusal fires on the last chunk after earlier chunks were already
programmed, which is why only a host pre-connect refusal can honestly claim the device is unchanged.
State plainly that the milestone's own decision D-2 permits refusal and states the preference order.

Section 4 -- **what it cost, and what would restore it.** The measured blast radius, caller by
caller: a full-chip write of a power-of-two-sized image is unaffected; an odd-sized image is refused;
an unaligned patch write is refused, and that is the originating issue's headline use case. Name the
deferred strand -- reading the head and tail page host-side, splicing, and issuing an aligned write --
and state that it needs no firmware RAM and no firmware change and can land later without reopening
WRITE-01 or WRITE-02. Point at the todo task 2 files.

Section 5 -- **criterion 4, as measured.** The four-row table with each part's algorithm, page size
and chip id, the protocol correction for `SST39SF020` per D-07, the two-name silicon identity for
`AE29F2008`, and the route each part actually needs. Then the `dev test` statement per D-12 with the
exact partition counts plan 03 measured: 25 of 27 rows unaffected, 2 refused, both recording a page
size of 512, and no validated part among them.

Section 6 -- **what this record proves and what it does not.** It proves the software behaviour on
both layers with the native and host suites named. It does not prove anything on silicon -- that is
the bench transcript's job, and per the milestone's decision D-4 a green native test is explicitly
not sufficient for a defect found on a bench. State that the loss reproduction can only be produced
by a pre-fix firmware build, which is why the bench plan flashes twice.

Commit in the meta repo with a plain `git commit`.
  </action>
  <acceptance_criteria>
    - `.planning/v1.39/195-partial-write-refusal-record.md` exists with frontmatter carrying a title, the phase slug, a measured date, a status line, a `pairs_with` pointer and requirement ids.
    - The record contains all six sections named above, each with a heading.
    - Section 2 labels the third loss direction as derived and never observed, using those words or a direct equivalent, and does not label it as reproduced or confirmed.
    - Section 3 states the 142-byte and 213-byte remaining-RAM figures and states that the firmware never learns the payload length.
    - Section 4 names the deferred host-side alignment strand and points at a todo path under `.planning/todos/pending/`.
    - Section 5 states that `SST39SF020` is algorithm 6 and cannot regress from this phase, and carries the 25 and 2 partition counts.
    - Section 6 states that the record proves no silicon behaviour and that a green native test is not sufficient.
    - The record does not contain the words "preserves every byte" as a claim about what this phase shipped.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && R=.planning/v1.39/195-partial-write-refusal-record.md && test -f "$R" && head -1 "$R" | /usr/bin/grep -qx -- '---' && /usr/bin/grep -q 'pairs_with' "$R" && for TOK in SST39SF020 AE29F2008 W29C040 142 213 'algorithm 6'; do /usr/bin/grep -qFe "$TOK" "$R" || { echo "MISSING-TOKEN $TOK"; exit 1; }; done && echo RECORD-COMPLETE</automated>
    <fails_when>`RECORD-COMPLETE` is absent, or the output names a `MISSING-TOKEN`, or the file has no frontmatter opening line -- each missing token is a section whose measured content was replaced by prose</fails_when>
    <automated>cd /workspaces && R=.planning/v1.39/195-partial-write-refusal-record.md && S=$(/usr/bin/grep -c '^## ' "$R") && test "$S" -ge 6 && ! /usr/bin/grep -qFe 'preserves every byte' "$R" && echo RECORD-HONEST</automated>
    <fails_when>`RECORD-HONEST` is absent from the output. Either fewer than six top-level sections exist, or the record claims the phase preserves every byte, which is the shape this phase measured out of firmware RAM and deliberately did not build</fails_when>
  </verify>
  <done>Per D-01 the reasoning that produced the fix shape survives the phase as a project record rather than only as plan text, with both deciding measurements stated. Per D-06 the derived third loss direction is labelled as derived. Per D-07 the protocol correction is recorded with its measurement. Per D-12 the `dev test` consequence carries its exact partition counts. The record states plainly what it cannot prove.</done>
</task>

<task type="auto">
  <name>Task 2: File the three deferred strands and record the scope correction in the ledger</name>
  <precondition>`python3 .claude/skills/devtest-triage/scripts/eprom_ledger.py check` exits 0 before any edit. The ledger is generated and its check exits 1 when the file differs from a fresh render, so a pre-existing difference must be seen before this task adds to it.</precondition>
  <files>.planning/todos/pending/host-side-page-alignment-splicing.md, .planning/todos/pending/protocol-0x05-write-override-flag-output-contract.md, .planning/todos/pending/dev-test-write-region-rounds-to-page-size.md, VALIDATED-EPROMS.md</files>
  <read_first>
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 3c hybrid c1 -- the read-splice-write shape, the region-read primitive it would reuse, and the honest caveat that a read failure must abort rather than proceed
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 2 -- the override trap, stated as the single easiest way to fail this phase's own requirement
    - `.planning/todos/completed/error-message-band-a0-bf-exhausted.md` -- the todo shape this project uses: frontmatter with created, title, area, `resolves_phase` and files, then the problem, the facts, and what would close it
    - `.claude/skills/devtest-triage/SKILL.md` -- the ledger's ownership rules and the three commands
    - `.claude/skills/devtest-triage/scripts/eprom_ledger.py` -- the module docstring, which states that a Notes section is preserved verbatim across a rewrite and is the home for anything a table cannot carry
    - `/workspaces/VALIDATED-EPROMS.md` -- the existing Notes bullet, which is the analog for recording a by-identity finding
  </read_first>
  <action>
Per D-03, D-07 and D-12. Three todos, one ledger Notes bullet.

STEP 1 -- `host-side-page-alignment-splicing.md`. The deferred strand that restores the capability
the refusal costs. State the problem in the caller's terms: patching part of a ROM image with an
unaligned start address is a normal operation that now refuses. State the shape: read the head page
and the tail page through the existing region-read primitive, splice the payload over them, and issue
a page-aligned write; the firmware guard stays as the backstop. State the three facts that make it
tractable -- it needs no firmware RAM, it works at every page size including 512, and the region-read
primitive already exists and is used this way by the chip-test engine. State the two honest caveats:
a read failure must abort rather than proceed, and the region-read accuracy should be verified
explicitly before anything is built on it. Set `area` to the host app and leave `resolves_phase`
unassigned.

STEP 2 -- `protocol-0x05-write-override-flag-output-contract.md`. Per D-03. State the request as it
was made on the originating issue: refuse behind an explicit override flag. State the trap that made
this phase decline it: an override that proceeds and then prints the plain success line violates
WRITE-02 literally, because that requirement is unconditional. State what would close it: a decided
output contract for the override path -- what the outcome line says, what the exit code is, and what
the operator must acknowledge -- not the flag itself. Leave `resolves_phase` unassigned.

STEP 3 -- `dev-test-write-region-rounds-to-page-size.md`. Per D-12. State the measured consequence:
the chip-test engine writes 256 bytes at address 0 for every non-UV part, which is a whole number of
pages for 25 of the 27 protocol `0x05` rows and a refused partial page on the two whose page size is
512. State why this phase did not fix it: changing the region changes what every community validation
run measures, which is a ledger-affecting decision that must not ride along with a safety fix. State
the proposal: round the region length up to the part's page size where one is recorded. Name the two
affected parts and note that neither has silicon evidence. Leave `resolves_phase` unassigned.

STEP 4 -- the ledger Notes bullet. Add one bullet to the `## Notes` section of `VALIDATED-EPROMS.md`
recording that `SST39SF020` is `algorithm: 6`, a sector-erase NOR part handled by a different
firmware file, and is therefore outside the protocol `0x05` write path's blast radius entirely --
so a finding about that path does not apply to it. The Notes section is the one part of the file
preserved verbatim across a rewrite, and it is where the existing two-name silicon finding already
lives. Do not touch the generated table, the aliases, the families or the variation sections.

STEP 5 -- prove the edit round-trips. Run the ledger's `write` command to regenerate the file from
its own records, then its `check` command. If `check` exits non-zero after `write`, the Notes edit
was placed outside the preserved section; move it and repeat. Both must be clean before committing.

STEP 6 -- commit in the meta repo with a plain `git commit`, covering the three todos and the ledger.
  </action>
  <acceptance_criteria>
    - All three todo files exist under `.planning/todos/pending/` with frontmatter carrying at least `created`, `title`, `area` and `resolves_phase`.
    - The host-side alignment todo names the region-read primitive and states that a read failure must abort rather than proceed.
    - The override todo states that an override printing the plain success line violates the requirement, and states that the output contract rather than the flag is what would close it.
    - The `dev test` region todo names both affected parts, the 25 and 2 partition counts, and the proposal to round the region to the page size.
    - `VALIDATED-EPROMS.md` `## Notes` gains a bullet naming `SST39SF020` and its algorithm, and the file's validated-chips table is byte-identical to its pre-task content.
    - `eprom_ledger.py write` followed by `eprom_ledger.py check` both exit 0.
    - No row of the validated-chips table was added, removed or reworded.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && for T in host-side-page-alignment-splicing protocol-0x05-write-override-flag-output-contract dev-test-write-region-rounds-to-page-size; do F=".planning/todos/pending/$T.md"; test -f "$F" || { echo "MISSING-TODO $T"; exit 1; }; /usr/bin/grep -q 'resolves_phase' "$F" || { echo "NO-FRONTMATTER $T"; exit 1; }; done && echo TODOS-FILED</automated>
    <fails_when>`TODOS-FILED` is absent, or the output names a `MISSING-TODO` or `NO-FRONTMATTER` -- a deferred strand with no filed todo is a capability loss that disappears when the phase seals</fails_when>
    <automated>cd /workspaces && python3 .claude/skills/devtest-triage/scripts/eprom_ledger.py write && python3 .claude/skills/devtest-triage/scripts/eprom_ledger.py check && /usr/bin/grep -A 40 '^## Notes' VALIDATED-EPROMS.md | /usr/bin/grep -q 'SST39SF020' && echo LEDGER-ROUNDTRIP-OK</automated>
    <fails_when>`LEDGER-ROUNDTRIP-OK` is absent from the output, or the check exits non-zero, which means the Notes edit was placed outside the section the ledger preserves verbatim and a regeneration would silently discard it</fails_when>
    <automated>cd /workspaces && DIFFSTAT=$(git diff --stat -- VALIDATED-EPROMS.md) && printf '%s\n' "$DIFFSTAT" && ROWS=$(/usr/bin/grep -c '^| [A-Z0-9]' VALIDATED-EPROMS.md) && echo "TABLE-ROWS=$ROWS"</automated>
    <fails_when>the diff stat shows more than a handful of changed lines, or `TABLE-ROWS` differs from the count before this task, which means the generated table was altered rather than only the Notes section</fails_when>
  </verify>
  <done>Per D-03 and the deferred alignment strand, the capability this phase's refusal costs is actionable rather than lost -- the shape, the primitive it would reuse and the caveat that a read failure must abort are all recorded. Per D-12 the `dev test` consequence carries its proposal. Per D-07 the scope correction lives where a reader of the validated-parts ledger will meet it, in the one section the generator preserves verbatim, proven by a write-then-check round trip.</done>
</task>

<task type="auto">
  <name>Task 3: Carry the scope correction back to the originating issue, and advance both gitlinks</name>
  <precondition>`gh auth status` reports an authenticated account for the issue host. If it does not, skip the issue comment, record the drafted text in the phase record's section 5, file a pending todo naming the comment that still needs posting, and continue -- the gitlink advance does not depend on it.</precondition>
  <files>firestarter_fw, firestarter_app, .planning/v1.39/195-partial-write-refusal-record.md</files>
  <read_first>
    - `.planning/phases/195-partial-writes-stop-destroying-the-page/195-RESEARCH.md` section 7 -- the correction and the note that the originating issue's claim about the NOR part is incorrect for the same reason
    - `.planning/v1.39/195-partial-write-refusal-record.md` -- section 5 as task 1 wrote it, which is the source of the comment's content
    - `.claude/skills/devtest-triage/SKILL.md` -- the project's convention for a datasheet-grounded findings comment, so the reply matches the voice the issue tracker already carries
    - `/workspaces/CLAUDE.md` -- the gitlink convention, which has advanced per phase since v1.36
  </read_first>
  <action>
Per D-07. One issue comment and one meta-repo commit.

STEP 1 -- the comment on the originating issue about partial and unaligned protocol `0x05` writes.
Scope it to the measured scope correction and nothing more. State that of the four validated parts
the issue names, one is `algorithm: 6` -- a sector-erase NOR part handled by a different firmware
file -- so it never traverses the page-write path and is outside the defect's blast radius; give the
measurement that establishes it, which is the part's own row in the shipped database. State that two
of the other three are the same silicon under two names, with the shared chip id. Do **not** claim
the defect is fixed or released: nothing has shipped, and this phase's work is on a milestone branch.
Do not attach a label and do not close anything.

If the issue host is not authenticated, do not retry and do not improvise a different channel:
record the drafted comment verbatim in the phase record and file one pending todo naming it.

STEP 2 -- advance both gitlinks in the meta repository. The submodule pointers must name the commits
plans 01 through 03 produced inside each sub-repo, so the phase is reproducible from the meta repo
alone. Confirm before staging that each sub-repo is on the `v1.39-protocol-0x05-write-correctness`
branch and that each working tree is clean, then stage both submodule paths and commit. A gitlink
advanced from a dirty tree records a commit that does not contain the work.

STEP 3 -- confirm afterwards that the meta repository has no unstaged submodule change left and that
both recorded gitlinks resolve to commits reachable from each sub-repo's current branch head.

STEP 4 -- do not mark any requirement Complete here. The milestone requires silicon evidence and a
green native test is explicitly not sufficient, so all three WRITE requirements are flipped by the
bench plan that produces that evidence. Do not run any state-sync verb.
  </action>
  <acceptance_criteria>
    - Either a comment exists on the originating issue carrying the protocol correction and the two-name silicon identity, or the drafted text is recorded in the phase record and a pending todo names the unposted comment.
    - The posted or drafted comment does not claim the defect is fixed or released and does not close the issue.
    - Both sub-repos are on the `v1.39-protocol-0x05-write-correctness` branch with clean working trees.
    - The meta repository's recorded gitlink for each sub-repo equals that sub-repo's current branch head.
    - `git status --porcelain` in the meta repository shows no unstaged submodule change.
    - `.planning/REQUIREMENTS.md` is unchanged by this plan -- no WRITE requirement is marked Complete.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && for S in firestarter_fw firestarter_app; do B=$(git -C "$S" branch --show-current); test "$B" = "v1.39-protocol-0x05-write-correctness" || { echo "WRONG-BRANCH $S=$B"; exit 1; }; test -z "$(git -C "$S" status --porcelain)" || { echo "DIRTY $S"; exit 1; }; L=$(git rev-parse "HEAD:$S"); H=$(git -C "$S" rev-parse HEAD); test "$L" = "$H" || { echo "GITLINK-STALE $S link=$L head=$H"; exit 1; }; done && echo GITLINKS-ADVANCED</automated>
    <fails_when>`GITLINKS-ADVANCED` is absent, or the output names `WRONG-BRANCH`, `DIRTY` or `GITLINK-STALE` -- a stale gitlink means the meta repository points at a commit that does not contain this phase's work</fails_when>
    <automated>cd /workspaces && PORC=$(git status --porcelain -- firestarter_fw firestarter_app) && test -z "$PORC" && echo SUBMODULE-PORCELAIN-CLEAN</automated>
    <fails_when>`SUBMODULE-PORCELAIN-CLEAN` is absent from the output, which means a submodule pointer change is still unstaged or uncommitted in the meta repository</fails_when>
    <automated>cd /workspaces && test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md)" && echo REQS-UNTOUCHED</automated>
    <fails_when>`REQS-UNTOUCHED` is absent from the output, which means a requirement was flipped before the silicon evidence the milestone's own decision requires exists</fails_when>
  </verify>
  <done>Per D-07 the scope correction reaches the issue the defect was reported on, so the project does not leave a false claim standing about a part the change cannot reach, and the comment claims nothing about a fix that has not shipped. Both gitlinks name the commits this phase produced, from clean trees on the milestone branch. No requirement is flipped ahead of its evidence.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; threats rated `high` or above block the phase.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase record to a future reader | A record that overstates what was proven is a repudiation surface: it lets a later decision rest on evidence that never existed |
| the generated ledger to a hand edit | Only the Notes section survives a regeneration; anything written elsewhere is silently discarded |
| the project to a public issue tracker | A comment is visible to users and cannot be unsent; claiming a fix that has not shipped would misinform them |
| the meta repository to its submodules | A gitlink advanced from a dirty tree records a commit that does not contain the work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-28 | Repudiation | a record that presents the derived third loss direction as observed | high | mitigate | The record labels it derived and never observed, and a verify leg requires the file to carry that classification rather than a confirmation |
| T-195-29 | Repudiation | a record claiming the phase preserves untouched bytes | high | mitigate | A verify leg greps for the preserving claim and fails the plan if it appears; the record states the shape chosen and the measurements that ruled the other one out |
| T-195-30 | Tampering | a hand edit to the generated ledger silently discarded | medium | mitigate | The edit goes in the one section preserved verbatim, and a write-then-check round trip proves it survives regeneration |
| T-195-31 | Repudiation | a capability loss that disappears when the phase seals | high | mitigate | Three pending todos are filed with their measured grounds, and a verify leg requires all three to exist with frontmatter |
| T-195-32 | Repudiation | a public claim that a defect is fixed when nothing has shipped | medium | mitigate | The comment is scoped to the measured scope correction, claims no fix and no release, attaches no label and closes nothing |
| T-195-33 | Tampering | a gitlink advanced from a dirty or wrong-branch tree | high | mitigate | A verify leg checks the branch, the porcelain emptiness and the gitlink-equals-head condition for both sub-repos before accepting the advance |
| T-195-34 | Repudiation | a requirement marked Complete ahead of its evidence | high | mitigate | This plan flips nothing; a verify leg requires the requirements file to be untouched, and the bench plan that produces the silicon evidence owns the flips |
| T-195-35 | Tampering | npm, pip or cargo installs | high | accept | This plan installs zero packages and changes no dependency manifest |
</threat_model>

<verification>
- `.planning/v1.39/195-partial-write-refusal-record.md` exists with six sections and carries every measured token.
- The record labels the third loss direction derived and never observed, and makes no preserving claim.
- All three pending todos exist with frontmatter.
- The ledger's `write` then `check` both exit 0, and the Notes section names the corrected part.
- The validated-chips table row count is unchanged.
- The originating issue carries the scope correction, or the drafted text is recorded with a todo naming it.
- Both gitlinks equal their sub-repo branch heads, from clean trees on the milestone branch.
- `.planning/REQUIREMENTS.md` is unchanged by this plan.
</verification>

<success_criteria>
The fix-shape decision, the two measurements that forced it, the scope correction and the capability
cost all exist as project records rather than only as plan text. The three strands this phase
deliberately did not build are filed with their grounds, so each is actionable. The ledger carries
the protocol correction in the section a regeneration preserves. The originating issue no longer
leaves a false claim standing about a part outside the defect's blast radius. Both submodule
pointers name the commits this phase produced, and no requirement is flipped ahead of its evidence.
</success_criteria>

<output>
Create `.planning/phases/195-partial-writes-stop-destroying-the-page/195-04-SUMMARY.md` when done.
Record the record's section list, the three todo filenames, whether the issue comment was posted or
drafted and why, and the two gitlink hashes before and after the advance.
</output>
