---
phase: 194-real-page-size-reaches-the-firmware
plan: 03
type: execute
wave: 2
depends_on: [194-01]
commits_land_in: [firestarter_app]
files_modified:
  - firestarter_app/tests/test_page_size_invariants.py
  - firestarter_app/tests/test_vcc_margin_rail.py
  - firestarter_app/tests/golden/chip_database_field_inventory.json
autonomous: true
requirements: [PAGE-02]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-08 host half: for every one of the 27 protocol 0x05 parts the emitted page_size equals the part's real page, and for exactly the 18 whose old derivation was already right it also equals what that derivation produced."
    - "D-02: the page_size carrier count across all 746 rows is exactly 45, made of 18 upstream-native 0x0D rows and 27 upstream-native 0x05 rows, with no curated carrier remaining."
    - "D-01: build_db.py carries no part-number-keyed page-size table, asserted by absence rather than by a count, so a future agent cannot quietly reintroduce one."
    - "D-04: infoic_page_size_raw's carrier count is unchanged at 744, and no test asserts anything new about it."
    - "Every count in the field-inventory golden is re-derived by an independent traversal of the regenerated database, never hand-edited to make a change disappear."
    - statement: "The generated database and the 27-row record are deterministic -- a second generator run against the same pinned upstream input produces a byte-identical database, and the 27-row table has one stable order that both the host test and the record artefact share."
      verification: backstop
  artifacts:
    - path: firestarter_app/tests/test_page_size_invariants.py
      provides: "the widened 45-carrier count leg, the 45-identity provenance allow-list, and the new 27-row two-halves table leg"
      contains: "45"
    - path: firestarter_app/tests/test_vcc_margin_rail.py
      provides: "an absence assertion replacing the two-entry count assertion on the deleted curated table"
      contains: "hasattr"
    - path: firestarter_app/tests/golden/chip_database_field_inventory.json
      provides: "the re-derived field inventory, with programming.page_size at 45"
  key_links:
    - from: "firestarter_app/firestarter/data/chip_database.json"
      to: "firestarter_app/tests/test_page_size_invariants.py"
      via: "the 27-row table's oracle is the row's own infoic_page_size_raw, joined per part identity"
    - from: "firestarter_app/tests/test_lock_status_class_partition.py's _ALGORITHM_0X05_KEYS"
      to: "the 27 identities in the widened allow-list"
      via: "import or duplicate, never relocate -- that module's own leg asserts the live population against it"
    - from: "the deleted flash_5v_page_page_size derivation"
      to: "the derivation function written out inside the new host test"
      via: "the no-regression half of success criterion 2 needs the old rule to compare against, and after the firmware deletion the test is the only place it survives"
  prohibitions:
    - "Must NOT loosen any count assertion into a floor or a range. An exact count is what detects an unintended carrier, and a floor would let the 66 promoted rows gain a page size silently. The existing 149-layer comment states the rule for its own count, and the same rule applies here."
    - "Must NOT hand-edit tests/golden/chip_database_field_inventory.json. Its own how_to_update demands an independent re-derivation of every number, and names the commit-message content required. A hand edit is how a real surprise disappears."
    - "Must NOT delete the leg in test_vcc_margin_rail.py that guarded against a part-number-keyed page-size table. Deleting the leg loses the guarantee. Replace the count assertion with an absence assertion, which is the machine-checked form of the instruction that the database reflects upstream ground truth."
    - "Must NOT relocate _ALGORITHM_0X05_KEYS out of tests/test_lock_status_class_partition.py. That module's own leg asserts the live database population against it, so moving it breaks a guarantee that has nothing to do with page size."
    - "Must NOT delete or weaken the two synthetic non-vacuity legs in test_page_size_invariants.py. They are what stops the widened allow-list from passing vacuously."
    - "Must NOT change tests/test_b15_page_size_corroboration.py, tests/test_erase_flag_invariants.py or tests/test_lock_status_class_partition.py. All three are green, orthogonal, and must stay green."
    - "Must NOT hand-edit firestarter_app/firestarter/data/chip_database.json. It is a build output."
    - "Must NOT write, reword or replace any comment in firestarter_app source or test files. Where an existing docstring states a count this phase changes, reword that docstring rather than adding new explanatory prose beside it -- a docstring is a string expression, not a comment."
    - "Must NOT mark PAGE-01, PAGE-02 or PAGE-03 Complete in .planning/REQUIREMENTS.md. Plan 194-06 flips PAGE-01 and PAGE-02 in one hand edit after all software evidence exists, and PAGE-03's hardware leg stays OPEN per D-11."
    - "Must NOT run the app test suite under the devcontainer's default Python 3.12. The app's CI floor is 3.11, and a green 3.12 result can mask a real CI break."
    - "Must NOT push any branch."
---

<objective>
Move the three host data gates that pin the old page-size shape, by re-derivation rather than by
hand, and add the 27-row measurement that makes PAGE-02's no-regression half a measured fact rather
than an assertion.

Purpose: plan 01 regenerated the database and left these three gates RED by design. Each one exists
to detect exactly the kind of change plan 01 made, so each must move deliberately and keep its
detecting power. The new 27-row leg is the host half of D-08. It asserts both halves of success
criterion 2 at once: that all 27 parts now carry their real page, and that the 18 whose old
derivation was already correct did not move.

Output: a widened carrier count of 45 with a 45-identity provenance allow-list, a 27-row
two-halves table leg, an absence assertion replacing the count assertion on the deleted curated
table, and a field-inventory golden re-derived by independent traversal.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-PATTERNS.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-01-SUMMARY.md
</context>

## Spec-less edge-coverage fallback -- no-silent-drop arithmetic

The edge probe surfaced **5** items over PAGE-01/02/03.
**5 surfaced == 3 authored into `must_haves.truths` + 2 surfaced as flagged assumptions.**
The full table and both flagged assumptions are recorded once in `194-01-PLAN.md`. This plan carries
the **backstop** edge -- the `ordering` probe -- as a structured
`{ statement, verification: backstop }` marker in its own `must_haves.truths`. The honest reading of
"is output order specified and stable" for this phase is the determinism of the generated database
and of the 27-row record. It is authored as a backstop rather than as an explicit criterion because
proving byte-identical regeneration depends on an external pinned input this plan does not control,
so the verifier is expected to abstain rather than to pass it silently if it cannot confirm it.

**Prohibition probe** -- the kept values and safety items are authored descriptor-less in
`must_haves.prohibitions` above. **Canon-referral breadcrumb:** untrusted-wire-field input
validation is canon ASVS V5 -- covered by `<threat_model>` and `/gsd-secure-phase`, not minted as a
prohibition.

## Artifacts this phase produces

Repeated verbatim in every plan of this phase so a source-grounding pass can exclude newly-created
symbols from drift verification and not flag deleted ones.

**CREATED:**

| Artifact | Repo | Plan |
|---|---|---|
| `MSG_ERR_FL4_PAGE_SIZE` = `0xBF`, severity ERROR, one `u16` param, `wire_format` `id_frame` | meta catalog, generated into fw + app | 01 |
| `FLASH_5V_PAGE_SIZE_MAX` (512) -- a `#define` local to `src/proms/flash_5v_page.cpp` | fw | 01 |
| `flash_5v_page_mask(uint16_t requested, uint32_t* out_mask)` -> `bool` -- the validator, out-param plus success return | fw | 01 |
| `bus_recording_saturated()` and an `#ifndef`-guarded `HOST_STUBS_MAX_RECORDING` (default 4096) | fw | 01 |
| `test_5v_page_write_execute_page_starts_at_real_page_not_derived` -- the tracer boundary case, (65536, 128) | fw | 01 |
| the 7-distinct-pair boundary cases, the deliberately-wrong-`mem_size` case, and the four refusal mismatch cases plus their positive control | fw | 02 |
| the 27-row `algorithm: 5` host table leg, both halves (real page for all 27, the old derivation's value for the 18) | app | 03 |
| `tests/golden/wire_dict_expected_deltas_194.json` -- 25 entries, indices generated programmatically | app | 04 |
| `PageSizeUnavailableError` (subclass of `EpromOperationError`) | app | 05 |
| `firestarter/page_size_gate.py` -- `requires_page_size()` predicate and `require_page_size()` raiser | app | 05 |
| `tests/test_page_size_write_refusal.py` | app | 05 |
| `.planning/v1.39/194-page-size-27-row-record.md` | meta | 06 |
| two pending todos -- the new-host/old-firmware skew (D-12) and the ERROR band exhaustion (U2) | meta | 06 |
| `.planning/v1.39/194-w29c020-bench-transcript.md` | meta | 07 |

**DELETED:**

| Artifact | Repo | Plan |
|---|---|---|
| `flash_5v_page_page_size()` and its block comment | fw | 01 |
| `_PAGE_SIZE_BY_PART`, its two entries, its leading comment block and its inline citations | app | 01 |
| the two-arm emit comment's falsified clauses and the curated-dict-naming clause beside `raw_page_size` | app | 01 |
| the falsified "algorithm 13 / 0x0D only" clauses in `constants.py`, `database.py` (two blocks), `json_parser.c` and `include/firestarter.h` | app + fw | 06 |

**REGENERATED, never hand-edited:** `firestarter_app/firestarter/data/chip_database.json`,
`firestarter_fw/include/messages.h`, `firestarter_app/firestarter/messages.py`.

**Deliberately unchanged:** `firestarter_fw/src/proms/eeprom_28c.cpp` (reference only),
`firestarter_app/firestarter/database.py` and `constants.py` logic, `firestarter_fw/src/json_parser.c`
logic, `tests/golden/wire_dict_baseline.json`, `tests/golden/wire_dict_expected_deltas_149.json`,
`tests/test_lock_status_class_partition.py`, `tests/test_b15_page_size_corroboration.py`,
`.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md`.

## The three gates and what moves in each

| Gate | What it asserts today | What it must assert |
|---|---|---|
| `tests/test_page_size_invariants.py` leg 4 | exactly 20 carriers across 746 rows, 18 native plus 2 curated | exactly **45**, 18 upstream-native `0x0D` plus 27 upstream-native `0x05`. The function name encodes the number and must be renamed |
| `tests/test_page_size_invariants.py` leg 6 | a 20-identity provenance allow-list built from a curated set and two native sets | a 45-identity allow-list. The curated set is subsumed, because both of its entries are `0x05`-native and arrive in the new 27-entry set |
| `tests/test_vcc_margin_rail.py` third assertion | the curated dict has exactly 2 entries | the curated dict does **not exist**. Assert absence, and keep the other two assertions in the same function untouched |
| `tests/golden/chip_database_field_inventory.json` | `programming.page_size` is 20, at the machine-read position | `programming.page_size` is **45**, plus the two prose mentions that also carry the number |

Nine of the eleven legs in `test_page_size_invariants.py` do **not** move. Leg 5 (power of two in
`[1, 512]`) was verified to still hold for all 45. Legs 10 and 11 are the synthetic non-vacuity
legs, and both must survive.

<tasks>

<task type="auto">
  <name>Task 1: Widen the carrier count to 45 and the provenance allow-list to 45 identities</name>
  <precondition>`firestarter_app/.venv311/bin/python --version` reports a 3.11 version. The app's CI floor is Python 3.11 and the devcontainer default is 3.12, so a green 3.12 result can mask a real CI break.</precondition>
  <files>firestarter_app/tests/test_page_size_invariants.py</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R4 gate 1 -- which two of the eleven legs move, the verbatim leg 4 assertion, the identity frozenset layout at `:69-108`, and the list of nine legs that do not move with the reason for each
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R1 -- the 27-row join table, the independently re-derived carrier projection of 45 as 18 plus 27, and corroboration 2 naming the in-repo frozenset
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-PATTERNS.md` §"Host data gates" -- the self-analog for leg 4, the frozenset composition, and the instruction to import or duplicate rather than relocate
    - `firestarter_app/tests/test_page_size_invariants.py` -- the whole file: the module docstring, the identity frozensets at `:69-108`, leg 4 at `:237-247`, leg 6 at `:265-272`, and the two synthetic non-vacuity legs
    - `firestarter_app/tests/test_lock_status_class_partition.py` -- `_ALGORITHM_0X05_KEYS` at `:296-327` with its module-level count assertion, and the leg that asserts the live population against it
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-01-SUMMARY.md` -- the regenerated database's measured shape
    - `/workspaces/firestarter_app/CLAUDE.md`
  </read_first>
  <action>
Run every host command with `firestarter_app/.venv311/bin/python`, never the devcontainer default.

STEP 1 -- leg 4, the count. Change the carrier count assertion from 20 to **45** and rename the
function so its name carries the new number, because the current name encodes 20. Update the failure
message's parenthetical to describe the new composition as 18 upstream-native `0x0D` plus 27
upstream-native `0x05`, and drop the word "curated" from it, because no curated carrier remains.
Keep the total-row assertion at 746. Keep the assertion an exact equality. A floor would let a
promoted row gain a page size without detection.

The 45 is a re-derivation, not a hand edit. It was produced by joining every row's own upstream
protocol id against the pinned upstream input, and plan 01 re-asserted it against the regenerated
database. State that derivation in the commit message.

STEP 2 -- leg 6, the provenance allow-list. Replace the two-set union with the union of the two
existing 18-identity native `0x0D` sets and a new 27-identity native `0x05` set. Delete the
2-entry curated set: both of its members are `0x05`-native, so they arrive in the new set, and the
"curated" concept disappears with the table plan 01 deleted.

Source the 27 identities from `tests/test_lock_status_class_partition.py`'s `_ALGORITHM_0X05_KEYS`
rather than re-authoring the list. Import it, or duplicate it with a small adapter for the separator
difference -- that module uses a manufacturer-slash-part string and this one uses a tuple. Do not
move or re-home it: that module's own leg asserts the live database population against it, and
relocating it breaks a guarantee unrelated to page size. If you duplicate, add an assertion that the
duplicated set has exactly 27 members, so a partial copy fails loudly.

STEP 3 -- leave the other nine legs alone. In particular keep both synthetic non-vacuity legs
exactly as they are. They are what stops a widened allow-list from passing vacuously. Re-run them
and confirm they still fail on their synthetic offenders and pass on the real database.

STEP 4 -- reword the module docstring where it states the old count. A docstring is a string
expression and is not scanned by the citation gate, and rewording an existing docstring is not
writing a comment. Do not add new explanatory prose beside it.

STEP 5 -- run the module on Python 3.11 and record the leg count and the summary line. Then commit
inside `firestarter_app` on the v1.39 branch.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/test_page_size_invariants.py` asserts a carrier count of exactly 45 and asserts a total row count of exactly 746, and the function's own name contains 45 rather than 20.
    - The provenance allow-list is the union of the two native `0x0D` identity sets and a 27-member native `0x05` set, and no set named for curated identities remains in the module.
    - The 27 `0x05` identities are sourced from `_ALGORITHM_0X05_KEYS`, and `tests/test_lock_status_class_partition.py` is byte-unchanged.
    - Both synthetic non-vacuity legs are still present and still pass.
    - `.venv311/bin/python -m pytest tests/test_page_size_invariants.py -o addopts="" -q` reports 0 failed and reports at least 12 tests, which is the eleven surviving legs plus the new one added in task 2.
    - `.venv311/bin/python -m pytest tests/test_lock_status_class_partition.py -o addopts="" -q` reports 0 failed.
    - The commit message states that 45 was re-derived by an upstream provenance join, and names the 18 plus 27 composition.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python --version && .venv311/bin/python -m pytest tests/test_page_size_invariants.py tests/test_lock_status_class_partition.py -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>the reported Python version does not begin with 3.11, or the pytest summary line reports any failures, or no summary line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -q '45' tests/test_page_size_invariants.py && ! /usr/bin/grep -q '_CURATED_PAGE_SIZE_IDENTITIES' tests/test_page_size_invariants.py && sha256sum -c - <<'SUMS'
bf0d93e34b4d668da12aed285cec419cd66f035d94bff0863749b592845dcc75  tests/test_lock_status_class_partition.py
SUMS</automated>
    <fails_when>non-zero exit from either grep leg, or the `sha256sum -c` output reports `FAILED` for the lock-status module. The new count is missing, or the curated identity set survives, or that module was modified. The hash was measured at plan time on the unmodified file</fails_when>
  </verify>
  <done>Per D-02, the carrier count leg asserts exactly 45 across 746 rows, composed of 18 upstream-native `0x0D` rows and 27 upstream-native `0x05` rows. The provenance allow-list holds all 45 identities, sourced from the in-repo frozenset rather than re-authored. Both non-vacuity legs survive, so the widened allow-list cannot pass on nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 27-row two-halves table -- the host half of D-08</name>
  <precondition>`firestarter_app/.venv311/bin/python --version` reports a 3.11 version.</precondition>
  <files>firestarter_app/tests/test_page_size_invariants.py</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-08 verbatim -- both halves of the host requirement, and the reason the firmware half exists beside it
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R4 gate 1's final paragraph -- what the new leg must assert, and why the old derivation function must be written out inside the test
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R1 -- the 27-row table with each part's size, raw page, old derived page and verdict, and the named list of the 9 under-sized parts
    - `firestarter_app/tests/test_page_size_invariants.py` -- the module's existing loader helper, its carrier selector, and the style of its existing per-row legs
    - `firestarter_app/firestarter/data/chip_database.json` -- the regenerated `algorithm: 5` rows
  </read_first>
  <action>
Add one leg to `tests/test_page_size_invariants.py`. It asserts both halves of success criterion 2
in one place, over all 27 protocol `0x05` parts.

Field paths, measured this session so no executor has to rediscover them: the algorithm is
`programming.algorithm`, the capacity is `electrical.size_bytes`, and both page fields are
`programming.page_size` and `programming.infoic_page_size_raw`. There is no top-level `algorithm`
key and no top-level `size_bytes` key.

STEP 1 -- half (a), all 27. For every row whose `programming.algorithm` is 5, assert that
`programming.page_size` is present. Assert that it equals that row's own
`programming.infoic_page_size_raw`. That is the part's real page, taken from the row's own upstream
provenance rather than from a transcribed table. Assert the count of rows the leg iterates as
exactly 27, so a row dropping out of that population fails the leg rather than shrinking it
silently.

STEP 2 -- half (b), the 18 no-regression rows. Write the old derivation rule out inside the test as
a small local function over `electrical.size_bytes`: 64 for a capacity up to and including 65536,
128 for a capacity up to and including 262144, and 256 above that. Then assert that exactly **18**
of the 27 rows have an emitted `page_size` equal to what that rule produces, and exactly **9**
differ. Assert both counts as exact equalities. Assert that each of the 9 differs by a factor of
exactly two, with the emitted value the larger of the pair. That is the measured shape of the
defect. Name the 9 parts in the failure message, so a regression says which part moved.

Writing the derivation out inside the test is deliberate and is the only place it survives, because
D-06 deleted it from the firmware. Without it there is nothing left to compare against, and the
no-regression half of success criterion 2 would become an assertion rather than a measurement.

STEP 3 -- keep the leg's oracle inside the database. Do not hard-code 27 part numbers or 27 page
values into the assertion body. The identity list belongs to the allow-list leg from task 1, and
this leg's job is the numeric relationship between two fields of the same row.

STEP 4 -- run the module on Python 3.11. Then run the whole app suite, on a committed tree, to
confirm nothing else in the module moved. Commit inside `firestarter_app`.
  </action>
  <acceptance_criteria>
    - A new leg in `tests/test_page_size_invariants.py` asserts, for exactly 27 rows with `algorithm` 5, that `programming.page_size` equals that row's `programming.infoic_page_size_raw`.
    - The same leg asserts exactly 18 rows where the emitted value equals the old capacity-bracket derivation, and exactly 9 where it does not.
    - The leg asserts that each of the 9 differs by a factor of exactly two, with the emitted value the larger of the pair.
    - The old derivation rule is written out inside the test as a local function returning 64, 128 or 256 by capacity bracket.
    - The leg's failure message names the differing parts.
    - `.venv311/bin/python -m pytest tests/test_page_size_invariants.py -o addopts="" -q` reports 0 failed, and the new leg has been seen to pass against the regenerated database.
    - The leg does not hard-code a list of 27 part numbers or 27 page values.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_page_size_invariants.py -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>the summary line reports any failures, or reports fewer than 12 tests, or no summary line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "
import json, pathlib
db = json.loads(pathlib.Path('firestarter/data/chip_database.json').read_text(encoding='utf-8'))
rows = [c for chips in db.values() for c in chips if c.get('programming', {}).get('algorithm') == 5]
def old(n):
    return 64 if n <= 65536 else (128 if n <= 262144 else 256)
assert len(rows) == 27, len(rows)
assert all(c['programming'].get('page_size') for c in rows), 'a row carries no page_size'
assert all(c['programming']['page_size'] == c['programming']['infoic_page_size_raw'] for c in rows)
same = [c for c in rows if c['programming']['page_size'] == old(c['electrical']['size_bytes'])]
diff = [c for c in rows if c['programming']['page_size'] != old(c['electrical']['size_bytes'])]
assert len(same) == 18, len(same)
assert len(diff) == 9, len(diff)
assert all(c['programming']['page_size'] == 2 * old(c['electrical']['size_bytes']) for c in diff)
print('TABLE-OK 27 rows, 18 unchanged, 9 corrected by exactly 2x')
"</automated>
    <fails_when>`TABLE-OK` is absent from the output. A raised assertion prints the offending count or the message naming the row instead of the marker</fails_when>
  </verify>
  <done>Per D-08, the host half is measured across all 27 protocol `0x05` parts, not asserted for the 9 alone. Half (a) proves every part now carries its real page. Half (b) proves the 18 whose old derivation was already correct did not move, and that the 9 moved by exactly a factor of two. The old derivation rule survives inside the test, which is the only place it can, because the firmware no longer holds it.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the curated table's absence, and re-derive the field-inventory golden</name>
  <precondition>`firestarter_app/.venv311/bin/python --version` reports a 3.11 version.</precondition>
  <files>firestarter_app/tests/test_vcc_margin_rail.py, firestarter_app/tests/golden/chip_database_field_inventory.json</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R4 gate 2 -- the verbatim function with its three assertions, why the leg is replaced rather than deleted, and why `hasattr` is the right predicate
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R4 gate 3 -- the three places in the golden that carry the number, the corrected line positions, the verbatim `how_to_update` text, and the re-derivation command
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-PATTERNS.md` §"Host data gates" -- the absence-assertion shape, and the golden re-derivation pattern
    - `firestarter_app/tests/test_vcc_margin_rail.py` -- the module docstring that also states the count, and the target function with its three assertions
    - `firestarter_app/tests/golden/chip_database_field_inventory.json` -- the whole file: `meta.how_to_update`, `meta.why_counts_not_names`, `meta.phase_149_update`, and the machine-read counts block
    - `firestarter_app/tests/test_chip_database_field_inventory.py` -- the traversal the gate performs, and its sixth leg which ast-walks the generator's entry construction
  </read_first>
  <action>
Two separate changes in one task, because both are single-assertion moves against the same
regenerated database.

STEP 1 -- `tests/test_vcc_margin_rail.py`. The target function carries three assertions. Leave the
first two exactly as they are: the decode table still decodes to 4000, and the margin rail is still
single-sourced from it. Replace only the third. It currently asserts that the curated page-size
table has exactly two entries. Plan 01 deleted that table, so the assertion would now raise an
attribute error rather than fail readably.

Replace it with an assertion that the generator module has **no** attribute of that name, with a
message stating that the page size is generated from upstream provenance and never curated. Do not
delete the leg. Its stated purpose is that no new part-number-keyed sibling table was introduced,
and D-01 makes that purpose stronger rather than moot. An absence assertion is the machine-checked
form of the instruction that the generator reflects upstream ground truth, and it is the only thing
that stops a future agent quietly reintroducing the table.

Reword the module docstring where it states the old two-entry count. A docstring is a string
expression, not a comment, so rewording it is permitted. Do not add new prose beside it.

STEP 2 -- `tests/golden/chip_database_field_inventory.json`. Follow the file's own `how_to_update`
literally, and quote it in the commit message. It requires re-deriving **every** number in the file
by an independent traversal of the live database, two levels deep, and it explicitly forbids
hand-editing a count to make a surprise disappear. It also requires the commit message to state
which key changed, on which level, and why.

Run the independent traversal: count manufacturers, count chips, and build separate counters over
top-level keys, `programming` keys and `electrical` keys. Transcribe every number the traversal
prints, not only the one you expect to move. The expected outcome is that `programming.page_size`
moves from 20 to **45**, that `programming.infoic_page_size_raw` stays at **744**, and that nothing
else moves. Treat that as a prediction to be checked, not as a shortcut.

Three places in the file carry the number and all three must move: the machine-read count inside the
`programming` level, and two prose mentions in the `meta` block. The name-list entry elsewhere in
the file does **not** move, because it records that the key exists rather than how many rows carry
it.

STEP 3 -- confirm the ast leg still exercises the path it was written for. The sixth leg of
`tests/test_chip_database_field_inventory.py` walks the generator's entry construction and handles
conditional-expression branches specifically. Plan 01 kept the emit arm as a conditional expression
inside the same dictionary unpacking for exactly this reason. Run that module and confirm the leg
passes. If it fails, the arm was restructured against plan 01's constraint, and the fix belongs in
the generator rather than in this gate.

STEP 4 -- run the five affected modules on Python 3.11, then the whole app suite on a committed
tree, and record both summary lines. Commit inside `firestarter_app`.
  </action>
  <acceptance_criteria>
    - `tests/test_vcc_margin_rail.py` asserts the absence of the curated page-size attribute on the generator module, using `hasattr`, and its other two assertions in the same function are unchanged.
    - The leg still exists as a named test and was not deleted.
    - `tests/golden/chip_database_field_inventory.json` records `programming.page_size` as 45 at the machine-read position, records `programming.infoic_page_size_raw` as 744, and its two prose mentions of the old count are updated.
    - Every other number in the golden is unchanged, and the change was produced by running an independent traversal rather than by editing the one expected number.
    - The commit message quotes the golden's `how_to_update` requirement and states which key changed, on which level, and why.
    - `.venv311/bin/python -m pytest tests/test_vcc_margin_rail.py tests/test_chip_database_field_inventory.py -o addopts="" -q` reports 0 failed, including the ast leg.
    - The whole `firestarter_app` suite reports 0 failed on Python 3.11, run on a committed tree, with the coverage gate satisfied.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_vcc_margin_rail.py tests/test_chip_database_field_inventory.py tests/test_page_size_invariants.py -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>the summary line reports any failures, or no summary line appears, or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -q 'hasattr' tests/test_vcc_margin_rail.py && .venv311/bin/python -c "
import json, pathlib
g = json.loads(pathlib.Path('tests/golden/chip_database_field_inventory.json').read_text(encoding='utf-8'))
prog = g['levels']['programming']
assert prog['page_size'] == 45, prog['page_size']
assert prog['infoic_page_size_raw'] == 744, prog['infoic_page_size_raw']
print('GOLDEN-OK page_size=45 infoic_page_size_raw=744')
"</automated>
    <fails_when>`GOLDEN-OK` is absent from the output. The absence assertion is missing from the margin-rail module, or a raised assertion prints the stale count instead of the marker</fails_when>
    <automated>cd /workspaces/firestarter_app && PORC=$(git status --porcelain) && test -z "$PORC" && .venv311/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>non-zero exit from the porcelain capture or the emptiness test, which means an uncommitted path exists and the guard fired before pytest ran. Also fails when the pytest summary line reports any failures, or when no summary line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 tools/planning_citation_gate.py firestarter tests tools</automated>
    <fails_when>non-zero exit, or the output names any scanned file instead of reporting `OK:` with a file count</fails_when>
  </verify>
  <done>Per D-01, the generator's freedom from a curated page-size table is asserted by absence rather than by a count, so the guarantee survives the table's deletion. The field-inventory golden records 45 carriers, produced by the independent re-derivation its own instructions demand, with the commit message naming the changed key and its level. The ast leg still exercises the conditional-expression path plan 01 preserved for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated database to the host data gates | Each gate exists to detect an unintended change in the emitted database. A loosened gate is a detector that no longer detects |
| golden file to the change it records | A hand-edited count makes a real surprise disappear without anyone deciding it was acceptable |
| generator source to the ast leg | One gate reads the generator's source shape rather than its output, so a restructure changes which path that gate exercises |
| Python 3.12 result to the 3.11 CI floor | A green result on the devcontainer default can mask a real CI break |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-16 | Repudiation | a count assertion loosened into a floor | high | mitigate | Every count in this plan stays an exact equality. A floor would let one of the 66 promoted `0x0D` rows gain a page size without detection, and 31 of those carry a raw page size of literally 1 |
| T-194-17 | Repudiation | a hand-edited golden | high | mitigate | The golden's own `how_to_update` requires an independent traversal of every number and forbids editing a count to make a surprise disappear. The commit message must name the changed key, its level and the reason |
| T-194-18 | Repudiation | a deleted guard leg | high | mitigate | The curated-table guard is replaced by an absence assertion rather than deleted, so the guarantee that no part-number-keyed page-size table exists becomes stronger, not absent |
| T-194-19 | Repudiation | a widened allow-list passing vacuously | high | mitigate | Both synthetic non-vacuity legs survive unchanged, and the 27 identities are sourced from an in-repo frozenset that carries its own count assertion |
| T-194-20 | Tampering | a generator restructure silently changing which path the ast leg walks | medium | mitigate | The emit arm stays a conditional expression inside the same dictionary unpacking, and this plan runs the ast leg to confirm it still passes |
| T-194-21 | Repudiation | a green 3.12 run read as CI parity | medium | mitigate | Every host command runs through the 3.11 interpreter, which is the app's CI floor, and the interpreter version is asserted in the first verify leg |
| T-194-09 | Tampering | npm, pip or cargo installs | high | accept | This plan installs zero packages and changes no dependency manifest. RESEARCH records no package to audit, so no legitimacy checkpoint is required |
| T-194-22 | Information disclosure | the 27-row leg names parts in a failure message | low | accept | Part numbers are public catalogue data already published in the shipped database. Naming them is what makes a regression diagnosable |
</threat_model>

<verification>
- `.venv311/bin/python --version` reports 3.11, and every host leg in this plan ran through it.
- `.venv311/bin/python -m pytest tests/ -o addopts="" -q` reports 0 failed on a committed tree.
- `tests/test_page_size_invariants.py` reports at least 12 tests, all passing.
- `tests/golden/chip_database_field_inventory.json` records `programming.page_size` as 45.
- `tests/test_lock_status_class_partition.py`, `tests/test_b15_page_size_corroboration.py` and `tests/test_erase_flag_invariants.py` are byte-unchanged and green.
- The app planning-citation gate reports `OK:` with a file count.
- `git -C firestarter_app status --porcelain` is empty, and the gitlink is advanced in the meta repo.
</verification>

<success_criteria>
The carrier count gate asserts exactly 45 across 746 rows with a 45-identity provenance allow-list,
and both non-vacuity legs survive. The 27-row leg measures both halves of success criterion 2: every
protocol `0x05` part now carries its real page, the 18 already-correct parts did not move, and the 9
moved by exactly a factor of two. The curated table's absence is asserted rather than its count. The
field-inventory golden records 45, produced by the independent re-derivation its own instructions
require. The whole app suite is green on the 3.11 CI floor.
</success_criteria>

<output>
Create `.planning/phases/194-real-page-size-reaches-the-firmware/194-03-SUMMARY.md` when done.
Record the re-derivation method behind 45, the full traversal output for the golden, and the
docstring rewordings made.
</output>
