---
phase: 194-real-page-size-reaches-the-firmware
plan: 06
type: execute
wave: 3
depends_on: [194-01, 194-02, 194-03, 194-04, 194-05]
commits_land_in: [firestarter_fw, firestarter_app]
files_modified:
  - firestarter_app/firestarter/constants.py
  - firestarter_app/firestarter/database.py
  - firestarter_fw/src/json_parser.c
  - firestarter_fw/include/firestarter.h
  - .planning/v1.39/194-page-size-27-row-record.md
  - .planning/todos/pending/new-host-old-firmware-0x05-page-size-skew.md
  - .planning/todos/pending/error-message-band-a0-bf-exhausted.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "No comment anywhere in either sub-repo still claims that the per-chip page size is consumed by one algorithm only, or that other handlers never consume the wire key -- the four inherited comments this phase falsified carry only their surviving true clauses."
    - "D-02 stays legible without a comment: the provenance rule is pinned by the widened carrier count, the 45-identity allow-list and the curated-table absence assertion, so a later agent cannot flatten it undetected."
    - "The 27-row measurement record exists as a committed artifact, and every number in it carries the command that produced it."
    - "D-11: the record states which of the 9 previously under-sized parts were exercised on real silicon and which rest on the database comparison alone, and the two are not conflated."
    - "D-09: the pending firmware INFO log todo is unmodified, with its resolves_phase tag intact, and the drifted catalog path it names is recorded as drift rather than repaired in place."
    - "D-12: the new-host against old-firmware skew exists as a pending backlog item, including the caveat that the host's version probe truncates the prerelease suffix."
    - "U2: the exhaustion of the ERROR message band exists as a pending backlog item, naming 0xBF as spent and the unallocated range as the open question."
    - "PAGE-01 and PAGE-02 are marked Complete exactly once, by hand, after every piece of software evidence exists. PAGE-03 is not marked Complete."
  artifacts:
    - path: .planning/v1.39/194-page-size-27-row-record.md
      provides: "the 27-row measurement record with the evidence-class split and per-number commands"
      contains: "0 of 9 on hardware"
    - path: .planning/todos/pending/new-host-old-firmware-0x05-page-size-skew.md
      provides: "the filed D-12 deferral, with the prerelease-suffix caveat"
    - path: .planning/todos/pending/error-message-band-a0-bf-exhausted.md
      provides: "the filed U2 band-exhaustion question"
    - path: firestarter_app/firestarter/constants.py
      provides: "the page-size wire-key comment with its falsified clauses deleted and its true clauses intact"
    - path: firestarter_app/firestarter/database.py
      provides: "both page-size comment blocks with their falsified clauses deleted"
    - path: firestarter_fw/src/json_parser.c
      provides: "the page-size table row comment with its falsified validation clause deleted and its saturation clause intact"
    - path: firestarter_fw/include/firestarter.h
      provides: "the page_size member comment with its falsified fallback clause deleted and both true clauses intact"
  key_links:
    - from: "the deleted comment clauses"
      to: "the widened host gates in plan 194-03"
      via: "the machine-checked assertions are what now carry the provenance rule the prose used to state"
    - from: ".planning/v1.39/194-page-size-27-row-record.md"
      to: "Phase 195 and the gh#67 reply"
      via: "both need to cite the 27-row measurement, and the record survives the milestone archive"
    - from: "the record's evidence-class split"
      to: "PAGE-03's status in .planning/REQUIREMENTS.md"
      via: "the split is what justifies leaving PAGE-03 unmarked rather than claiming it on database evidence"
  prohibitions:
    - "Must NOT reword, replace or add any comment in either sub-repo. Deletion of a falsified clause is the only permitted comment operation, and it is permitted because it writes nothing. This rule is not overridable by a task, a skill or a later instruction."
    - "Must NOT delete a clause that is still true. The reset-per-command cross-reference on the firmware handle member and the saturation clause on the parser table row are both still accurate, and deleting either would destroy a correct statement to tidy an incorrect neighbour."
    - "Must NOT conflate hardware evidence with database-comparison evidence in the record. The two evidence classes have different strength, and a reader who cannot tell them apart cannot judge what was actually proven on silicon."
    - "Must NOT mark PAGE-03 Complete. Its hardware leg stays OPEN until the ordered part arrives, per D-11, and no amount of database agreement closes a requirement that asks for a read-back on real silicon."
    - "Must NOT edit .planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md. D-09 keeps it pending and unmodified with its tag intact. Record its two drifted claims in the SUMMARY instead."
    - "Must NOT edit .planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md. It concerns the 0x0D rule, which this phase deliberately leaves untouched."
    - "Must NOT widen the ERROR message band into the unallocated range. U2 spends 0xBF deliberately and files the band question rather than answering it, because the answer is a project convention decision outside this phase's scope."
    - "Must NOT change any logic in constants.py, database.py, json_parser.c or firestarter.h. This plan deletes comment clauses and touches no executable line."
    - "Must NOT run any gsd state-sync verb against STATE.md. Four such verbs corrupt its frontmatter, and the REQUIREMENTS edit in this plan is a hand edit."
    - "Must NOT push any branch."
---

<objective>
Retire the four inherited comments this phase falsified, publish the 27-row measurement as a
citable record with its evidence classes kept apart, and file the two questions this phase
deliberately does not answer.

Purpose: three separate honesty obligations land here. First, four comments in shipped source assert
that the per-chip page size belongs to one algorithm and that other handlers never consume the wire
key, and both claims are now false -- the surgical deletion of those clauses is also the record of
the identity promotion this phase performed. Second, Phase 195 and the reply on the reported issue
both need to cite the 27-row measurement, so it must exist as a committed artifact rather than
inside a plan summary. Third, D-12 and the band-exhaustion consequence of U2 are real follow-ups
whose only correct disposition in this phase is to be filed with their reasons.

Output: five surgical comment-clause deletions across four files, a 27-row record under the
milestone directory, two pending backlog items, and one hand edit marking PAGE-01 and PAGE-02
Complete while leaving PAGE-03 open.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-PATTERNS.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-01-SUMMARY.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-02-SUMMARY.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-03-SUMMARY.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-04-SUMMARY.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-05-SUMMARY.md
@.planning/v1.33/sweep-outcome-record.md
</context>

## Spec-less edge-coverage fallback -- no-silent-drop arithmetic

The edge probe surfaced **5** items over PAGE-01/02/03.
**5 surfaced == 3 authored into `must_haves.truths` + 2 surfaced as flagged assumptions.**
The full table and both flagged assumptions are recorded once in `194-01-PLAN.md`. This plan carries
no probe-derived edge of its own. The record artifact it produces is where the second flagged
assumption -- that PAGE-03 cannot be closed in this phase -- is discharged in writing.

**Prohibition probe** -- the kept values and safety items are authored descriptor-less in
`must_haves.prohibitions` above. **Canon-referral breadcrumb:** untrusted-wire-field input
validation is canon ASVS V5 -- covered by `<threat_model>` and `/gsd-secure-phase`, not minted as a
prohibition.

## Artifacts this phase produces

Repeated verbatim in every plan of this phase so a source-grounding pass can exclude newly-created
symbols from drift verification and not flag deleted ones.

**CREATED:**

| Artifact | Repo | Plan |
|---|---|---|
| `MSG_ERR_FL4_PAGE_SIZE` = `0xBF`, severity ERROR, one `u16` param, `wire_format` `id_frame` | meta catalog, generated into fw + app | 01 |
| `FLASH_5V_PAGE_SIZE_MAX` (512) -- a `#define` local to `src/proms/flash_5v_page.cpp` | fw | 01 |
| `flash_5v_page_mask(uint16_t requested, uint32_t* out_mask)` -> `bool` -- the validator, out-param plus success return | fw | 01 |
| `bus_recording_saturated()` and an `#ifndef`-guarded `HOST_STUBS_MAX_RECORDING` (default 4096) | fw | 01 |
| `test_5v_page_write_execute_page_starts_at_real_page_not_derived` -- the tracer boundary case, (65536, 128) | fw | 01 |
| the 7-distinct-pair boundary cases, the deliberately-wrong-`mem_size` case, and the four refusal mismatch cases plus their positive control | fw | 02 |
| the 27-row `algorithm: 5` host table leg, both halves (real page for all 27, the old derivation's value for the 18) | app | 03 |
| `tests/golden/wire_dict_expected_deltas_194.json` -- 25 entries, indices generated programmatically | app | 04 |
| `PageSizeUnavailableError` (subclass of `EpromOperationError`) | app | 05 |
| `firestarter/page_size_gate.py` -- `requires_page_size()` predicate and `require_page_size()` raiser | app | 05 |
| `tests/test_page_size_write_refusal.py` | app | 05 |
| `.planning/v1.39/194-page-size-27-row-record.md` | meta | 06 |
| two pending todos -- the new-host/old-firmware skew (D-12) and the ERROR band exhaustion (U2) | meta | 06 |
| `.planning/v1.39/194-w29c020-bench-transcript.md` | meta | 07 |

**DELETED:**

| Artifact | Repo | Plan |
|---|---|---|
| `flash_5v_page_page_size()` and its block comment | fw | 01 |
| `_PAGE_SIZE_BY_PART`, its two entries, its leading comment block and its inline citations | app | 01 |
| the two-arm emit comment's falsified clauses and the curated-dict-naming clause beside `raw_page_size` | app | 01 |
| the falsified single-algorithm clauses in `constants.py`, `database.py` (two blocks), `json_parser.c` and `include/firestarter.h` | app + fw | 06 |

**REGENERATED, never hand-edited:** `firestarter_app/firestarter/data/chip_database.json`,
`firestarter_fw/include/messages.h`, `firestarter_app/firestarter/messages.py`.

**Deliberately unchanged:** `firestarter_fw/src/proms/eeprom_28c.cpp` (reference only),
`firestarter_app/firestarter/database.py` and `constants.py` logic, `firestarter_fw/src/json_parser.c`
logic, `tests/golden/wire_dict_baseline.json`, `tests/golden/wire_dict_expected_deltas_149.json`,
`tests/test_lock_status_class_partition.py`, `tests/test_b15_page_size_corroboration.py`,
`.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md`.

## The requirements convention for this phase

No plan in this phase may let the executor's automatic requirement-marking step flip a PAGE
requirement. Plans 01 through 05 all carry a prohibition against it. **This plan performs the single
hand edit**, after every piece of software evidence exists, and it marks **PAGE-01 and PAGE-02 only**.
PAGE-03 stays unmarked, because its hardware leg is held OPEN per D-11 and plan 07 records the
silicon run that does not close it.

<tasks>

<task type="auto">
  <name>Task 1: Delete the falsified clauses from the four inherited comments, surgically</name>
  <files>firestarter_app/firestarter/constants.py, firestarter_app/firestarter/database.py, firestarter_fw/src/json_parser.c, firestarter_fw/include/firestarter.h</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-PATTERNS.md` §"Comment-Deletion Boundary" -- the authoritative per-file table of which clauses go and which must survive, with the line ranges verified in the same session
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §"Unresolved" item U1 -- the three routes considered and why deletion is the one taken, plus the note that one firmware site carries a live cross-reference that must survive
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §"Project Constraints" -- the no-comments hard rule and the exact citation-gate patterns, including that Python docstrings are string expressions and are never scanned while C and C++ comments have no such exemption
    - `firestarter_app/firestarter/constants.py` -- the page-size wire-key comment block immediately above `JSON_KEY_PAGE_SIZE`
    - `firestarter_app/firestarter/database.py` -- the page-size carry comment above `page_size_val`, and the wire-emit comment above the `page-size` assignment
    - `firestarter_fw/src/json_parser.c` -- the comment on the `page-size` table row, immediately above the `FIELD(key_page_size, ...)` line
    - `firestarter_fw/include/firestarter.h` -- the trailing comment on the `uint16_t page_size` member
    - `/workspaces/CLAUDE.md` §"Source code comments -- hard rule"
  </read_first>
  <action>
Deletion only. Write nothing. Reword nothing. Replace nothing. The hard rule in `CLAUDE.md` forbids
writing a comment into either sub-repo and is not overridable by this plan, and deleting a clause
that has become false writes nothing at all. Use the pattern map's Comment-Deletion Boundary table
as the authority for each file. It names both halves per site: the clauses that go and the clauses
that must survive.

STEP 1 -- `firestarter_app/firestarter/constants.py`, one comment block above the page-size wire-key
constant. Two clauses go. The first is the parenthetical qualifying which chips supply the value,
which names the curated source that plan 01 deleted and restricts the provenance keying to one
algorithm. The second is the trailing parenthetical restricting which handler consumes the key.
What must survive: the firmware-sync pointer naming the parser symbol, and the commit reference
describing the dispatch of the stored string. Both are still accurate.

STEP 2 -- `firestarter_app/firestarter/database.py`, first block, above the internal carry. Three
clauses go: the clause offering a curated chip as one of the two sources, the parenthetical naming a
single algorithm beside the upstream provenance condition, and the trailing parenthetical about
which handlers consume the key. What must survive: the whole explanation that the guard is a
truthiness test rather than a presence test, and the note on the deliberately distinct underscore
and hyphen spellings. Both are still accurate and both are load-bearing for a reader.

STEP 3 -- `firestarter_app/firestarter/database.py`, second block, above the wire emit. Two clauses
go: the parenthetical qualifying the supplying source, and the clause attributing the fallback to a
single algorithm. What must survive: the truthiness-test explanation.

STEP 4 -- `firestarter_fw/src/json_parser.c`, the comment on the page-size table row. One clause
goes: the sentence stating where validation and the fallback live, which named a single handler and
is now incomplete because a second handler validates independently and refuses rather than falling
back. What must survive: the sentence about the table saturating an out-of-range value before the
handler sees it, and the statement that the earlier non-clamp justification no longer applies. Both
are still true, and the saturation clause is exactly what makes the firmware's rejection of 65535
comprehensible.

STEP 5 -- `firestarter_fw/include/firestarter.h`, the trailing comment on the page-size handle
member. One clause goes: the clause attributing the fallback behaviour to a single handler. Two
things must survive and the deletion must be surgical around them: the statement that a value of
zero means the field is absent, and the cross-reference stating that the field is reset per command
in the parser, exactly like the neighbouring identifier field. That cross-reference is still true
and is the most useful sentence in the comment.

STEP 6 -- confirm the edits are comment-only. `git diff` for all four files must show no change to
any executable line. Then run the full test and build set for both repos, because a comment deletion
inside a firmware header changes line numbers and can move a build: both AVR envs, both native
envs, the firmware python suite, the whole app suite on Python 3.11, and both planning-citation
gates.

STEP 7 -- commit, one commit per repository, and itemise every deleted clause in the commit body and
in the plan SUMMARY. The SUMMARY is where the rationale belongs. Also record in the SUMMARY the two
drifted claims found in the pending firmware INFO log todo: it names a catalog path under a sub-repo
that does not exist, and it argues a cost that the measurement showed an id-only message does not
carry. Do not edit that todo. D-09 keeps it pending and unmodified.
  </action>
  <acceptance_criteria>
    - `firestarter_app/firestarter/constants.py` no longer contains the phrase `0x0D only`, and still contains `json_parser.c (key_page_size)`.
    - `firestarter_app/firestarter/database.py` no longer contains the phrase `never consume this` and no longer contains `EEPROM_POLL only`, and still contains `TRUTHINESS` in both blocks.
    - `firestarter_fw/src/json_parser.c` no longer contains `eeprom28c_page_mask`, and still contains `saturates an out-of-range value`.
    - `firestarter_fw/include/firestarter.h` still contains `Reset per command in json_parse` and still contains `0 = absent`, and no longer contains `applies its own named fallback floor`.
    - `git diff` for all four files touches no executable line: the diff's added and removed lines are all comment lines or blank.
    - No file in either sub-repo gains a comment line in this task.
    - `pio run -e uno`, `pio run -e leonardo`, `pio test -e native`, `pio test -e native_nodevtools` and the firmware python suite all pass.
    - The whole `firestarter_app` suite passes on Python 3.11, and mypy at the watermark plus both ruff gates pass.
    - Both planning-citation gates report `OK:` with a file count.
    - `.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md` is byte-unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && ! /usr/bin/grep -q '0x0D only' firestarter_app/firestarter/constants.py && /usr/bin/grep -q 'json_parser.c (key_page_size)' firestarter_app/firestarter/constants.py && ! /usr/bin/grep -q 'never consume this' firestarter_app/firestarter/database.py && ! /usr/bin/grep -q 'EEPROM_POLL only' firestarter_app/firestarter/database.py && /usr/bin/grep -c 'TRUTHINESS' firestarter_app/firestarter/database.py</automated>
    <fails_when>non-zero exit from any leg, or the final printed count is fewer than 2, which means a surviving true clause was deleted along with a falsified neighbour</fails_when>
    <automated>cd /workspaces && ! /usr/bin/grep -q 'eeprom28c_page_mask' firestarter_fw/src/json_parser.c && /usr/bin/grep -q 'saturates an out-of-range value' firestarter_fw/src/json_parser.c && /usr/bin/grep -q 'Reset per command in json_parse' firestarter_fw/include/firestarter.h && /usr/bin/grep -q '0 = absent' firestarter_fw/include/firestarter.h && ! /usr/bin/grep -q 'applies its own named fallback floor' firestarter_fw/include/firestarter.h && echo FW-CLAUSES-OK</automated>
    <fails_when>`FW-CLAUSES-OK` is absent from the output. A falsified clause survives, or one of the three clauses that had to survive was deleted</fails_when>
    <automated>cd /workspaces && SHA=$(git -C firestarter_app log --format=%H -1 -- firestarter/constants.py firestarter/database.py) && echo "app clause-deletion commit: $SHA" && DIFF=$(git -C firestarter_app diff --unified=0 "$SHA^..$SHA" -- firestarter/constants.py firestarter/database.py) && CODE=$(printf '%s\n' "$DIFF" | /usr/bin/grep -E '^[+-]' | /usr/bin/grep -v '^[+-][+-]' | /usr/bin/grep -vE '^[+-][[:space:]]*(#|$)' || true) && test -z "$CODE" && echo APP-COMMENT-ONLY</automated>
    <fails_when>`APP-COMMENT-ONLY` is absent from the output, which means the named commit's host diff touched a line that is neither a comment nor blank, so this task changed executable code. Also fails when the printed sha is not this task's own commit</fails_when>
    <automated>cd /workspaces && SHA=$(git -C firestarter_fw log --format=%H -1 -- src/json_parser.c include/firestarter.h) && echo "fw clause-deletion commit: $SHA" && DIFF=$(git -C firestarter_fw diff --unified=0 "$SHA^..$SHA" -- src/json_parser.c include/firestarter.h) && CODE=$(printf '%s\n' "$DIFF" | /usr/bin/grep -E '^[+-]' | /usr/bin/grep -v '^[+-][+-]' | /usr/bin/grep -vE '^[+-][[:space:]]*(\*|/\*|\*/|//|$)' || true) && test -z "$CODE" && echo FW-COMMENT-ONLY</automated>
    <fails_when>`FW-COMMENT-ONLY` is absent from the output, which means the named commit's firmware diff touched a line that is neither a comment continuation nor blank, so this task changed executable code. Also fails when the printed sha is not this task's own commit</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio run -e uno 2>&1 | /usr/bin/grep -E 'bootloader-guard|FAILED' | tail -2 && pio run -e leonardo 2>&1 | /usr/bin/grep -E 'bootloader-guard|FAILED' | tail -2 && pio test -e native 2>&1 | tail -3 && pio test -e native_nodevtools 2>&1 | tail -3</automated>
    <fails_when>the output contains `FAILED`, or either pio test summary line reports any number other than 0 failed, or a `bootloader-guard` line reports a negative margin</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -3 && python3 tools/planning_citation_gate.py firestarter tests tools && cd /workspaces/firestarter_fw && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -3 && python3 tools/planning_citation_gate.py src include test tests scripts platform tools name_firmware.py zero_bootloader_reserve.py bootloader_guard.py</automated>
    <fails_when>either pytest summary line reports any failures, or either citation gate names a scanned file instead of reporting `OK:` with a file count, or non-zero exit</fails_when>
    <automated>cd /workspaces && sha256sum -c - <<'SUMS'
c523d21f09efafad359d08d0847fe5696ca7b73e25f355302331c7e8678749bf  .planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md
7769fd12a091d156e464e6faad4e9b242872777b2a15a8e2392d5f4161e33b9f  .planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md
SUMS</automated>
    <fails_when>the output reports `FAILED` for either path, which means one of the two reviewed-and-not-folded todos was modified. The two hashes were measured at plan time on the unmodified files</fails_when>
  </verify>
  <done>All five falsified clauses are deleted and every clause that stayed true survives, including the firmware cross-reference about the per-command reset. The diffs touch no executable line, and no comment was written, reworded or replaced. Both repositories build and test green. Per D-09 the pending INFO log todo is byte-unchanged, and its two drifted claims are recorded in the SUMMARY rather than repaired in place.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the 27-row measurement record with its evidence classes kept apart</name>
  <precondition>The SUMMARY files for plans 194-01 through 194-05 exist, because every number this record states is taken from a measurement one of them performed.</precondition>
  <files>.planning/v1.39/194-page-size-27-row-record.md</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` §"Claude's Discretion" -- the settled decision that the record is a committed artifact under the milestone directory, the column list, and the reason a self-regenerating golden was rejected
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-11 -- the hardware-gated-deferral shape and the exact split wording
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R1 -- the full 27-row join table with every part's manufacturer, capacity, raw page, previous emitted value, old derived value, verdict and upstream protocol, plus the named list of the 9 and the corroborations
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R5's final note -- the 27-to-7 geometry collapse, which belongs in the record rather than in firmware source
    - `.planning/v1.33/sweep-outcome-record.md` -- the precedent: its frontmatter keys, its status line, its `pairs_with` key, and its body convention that every number carries the command that produced it
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-PATTERNS.md` §`.planning/v1.39/` -- the analog and the note that the directory does not exist yet
    - all five plan SUMMARY files for this phase -- every measured number the record states
  </read_first>
  <action>
Create `.planning/v1.39/`, which does not exist yet, and write one record into it. Follow the
milestone-record precedent exactly: frontmatter with a title, the phase, the plan, a measured date,
a status line naming what the record is authoritative for, and the requirement ids it serves.

STEP 1 -- the 27-row table. One row per protocol `0x05` part, with these columns: manufacturer, part
number, capacity in bytes, the real page from the row's own upstream raw attribute, the value the
old capacity-bracket derivation produced, the verdict, and the evidence class. Sort it stably and
state the sort key, so this table and the host test agree on order and a future reader can diff
them.

STEP 2 -- the evidence-class split, which is the reason this record exists. Two columns of evidence,
never merged into one. Name explicitly which of the 9 previously under-sized parts were exercised on
real silicon, and which rest on the database comparison alone. Until the ordered part arrives that
split reads **0 of 9 on hardware, 9 of 9 on the database comparison**. Write that sentence in the
record. The record must make it impossible for a reader to mistake database agreement for a
read-back on silicon.

STEP 3 -- every number carries its command. Copy the precedent's convention literally. For each
figure state the verbatim command that produced it and which plan ran it: the carrier count from the
host gate, the 27-row two-halves result, the 25-entry wire delta layer, the native case counts and
the 7-geometry collapse, the flash deltas for both AVR envs against 21598 and 23716, and the
`W29C512` end-to-end path from the tracer. Do not restate a figure from the research without
attributing it, and do not restate one that a plan re-measured without using the plan's number.

STEP 4 -- the corroborations worth carrying. Record the independent datasheet agreement for the
sector size of one Atmel part, which reaches the same value without going through the upstream file
at all. Record that the `0x0D` side of the provenance rule was independently re-proven at 18 native
plus 66 promoted, so the extension to `0x05` changed nothing there. Record the 27-to-7 geometry
collapse and the reason the capacity column is decorative to the firmware oracle after this phase.

STEP 5 -- state what the record does NOT prove. It does not prove any of the 9 reads back
byte-identical on silicon. It does not prove the partial-write defect is fixed, which is the next
phase's subject, and a correct page size does not by itself stop a partial write erasing the rest of
a page. It does not prove a new host is safe against old firmware, which is filed in task 3.

STEP 6 -- commit in the meta repo. The record must be citable by Phase 195 and by the reply on the
reported issue, and it must survive the milestone archive.
  </action>
  <acceptance_criteria>
    - `.planning/v1.39/194-page-size-27-row-record.md` exists and is tracked by git.
    - It contains a table with exactly 27 data rows, one per protocol `0x05` part, carrying manufacturer, part number, capacity, real page, old derived page, verdict and evidence class.
    - It contains the sentence `0 of 9 on hardware, 9 of 9 on the database comparison`, and the hardware and database evidence appear as separate columns or separate sections rather than merged.
    - It names all 9 previously under-sized parts explicitly.
    - Every figure in the record is accompanied by the verbatim command that produced it and the plan that ran it.
    - It records the 27-to-7 geometry collapse and the measured flash deltas for both AVR envs.
    - It contains a section stating what the record does not prove, naming the silicon read-back, the partial-write defect and the firmware-skew question.
    - Its frontmatter carries a title, the phase, the plan, a measured date, a status line and the requirement ids.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.39/194-page-size-27-row-record.md && git ls-files --error-unmatch .planning/v1.39/194-page-size-27-row-record.md >/dev/null && /usr/bin/grep -q '0 of 9 on hardware, 9 of 9 on the database comparison' .planning/v1.39/194-page-size-27-row-record.md && echo RECORD-OK</automated>
    <fails_when>`RECORD-OK` is absent from the output. The record is missing, or untracked, or the evidence-split sentence is absent or worded differently</fails_when>
    <automated>cd /workspaces && for P in 'AT29BV020' 'AT29BV040' 'AT29C020' 'AT29C040' 'AT29C512' 'AT29LV512' 'SST29EE512' 'SST29LE512' 'W29C512'; do /usr/bin/grep -q "$P" .planning/v1.39/194-page-size-27-row-record.md || { echo "MISSING-PART $P"; exit 1; }; done && echo NINE-NAMED</automated>
    <fails_when>the output contains `MISSING-PART` followed by a part number, or `NINE-NAMED` is absent, which means the record does not name all 9 previously under-sized parts</fails_when>
    <automated>cd /workspaces && ROWS=$(/usr/bin/grep -cE '^\| *[A-Z]+ *\|' .planning/v1.39/194-page-size-27-row-record.md) && echo "data rows: $ROWS" && test "$ROWS" -ge 27</automated>
    <fails_when>the printed row count is fewer than 27, or non-zero exit, which means the 27-row table is incomplete or its manufacturer column does not lead each row</fails_when>
  </verify>
  <done>Per D-11 the 27-row measurement is a committed, citable artifact, and its evidence classes are kept apart. The split reads 0 of 9 on hardware and 9 of 9 on the database comparison, so no reader can mistake database agreement for a silicon read-back. Every figure carries the command that produced it and the plan that ran it. A section states what the record does not prove.</done>
</task>

<task type="auto">
  <name>Task 3: File the two deferred questions, then mark PAGE-01 and PAGE-02 Complete by hand</name>
  <files>.planning/todos/pending/new-host-old-firmware-0x05-page-size-skew.md, .planning/todos/pending/error-message-band-a0-bf-exhausted.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-12 verbatim, and §"Deferred Ideas" -- the skew's mechanism and the version-probe caveat
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md` §R3 "Is there an existing id to reuse" -- the full ERROR band inventory, the single free id, the unallocated range, and the finding that severity is an explicit catalog field rather than an id-range derivation
    - `.planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md` -- the frontmatter shape for a filed follow-up: created, title, area, resolves_phase, source and files
    - `.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md` -- read for the frontmatter convention only. Do not edit it
    - `.planning/REQUIREMENTS.md` -- the PAGE-01, PAGE-02 and PAGE-03 entries and the coverage table, so the hand edit matches the file's existing form
    - `.planning/v1.39/194-page-size-27-row-record.md` -- the record task 2 produced, which the PAGE-03 note cites
  </read_first>
  <action>
Two new pending todos and one hand edit.

STEP 1 -- the skew todo, `new-host-old-firmware-0x05-page-size-skew.md`. State the mechanism
plainly. A host carrying this phase's database sends a page size. Firmware older than this phase
ignores it and still derives. So the 9 previously under-sized parts corrupt silently, and the
operator still sees a successful result. State that the host deliberately does not gate on firmware
version for protocol `0x05` writes in this phase, per D-12, and that this is a filed deferral rather
than an oversight.

Carry the caveat for whoever picks it up: the host's port probe truncates the prerelease suffix, so
the host cannot distinguish one prerelease build from the next. Any version boundary must therefore
sit where the numeric part alone is decisive. Set `resolves_phase` to `unassigned`, set `area` to
`both`, and list the files a future fix would touch.

STEP 2 -- the band todo, `error-message-band-a0-bf-exhausted.md`. State the measured position. The
ERROR band in the canonical catalog ran from `0xA0` through `0xBF`, held 31 messages, had exactly one
free id, and this phase spent it on the page-size refusal. The next error message anywhere in the
project therefore forces a decision. Record the two facts that make the decision tractable: the
following range is entirely unallocated, and severity is carried as an explicit catalog field rather
than being derived from the id range, so extending the band is mechanically possible. Record that
this phase deliberately did not make that decision, because it is a project convention with
consequences outside one phase. Set `resolves_phase` to `unassigned` and `area` to `both`, because a
band change regenerates artifacts in both sub-repos.

STEP 3 -- the hand edit to `.planning/REQUIREMENTS.md`. Mark **PAGE-01 and PAGE-02** Complete, and
update their coverage cells to match the file's existing form. This is the single flip for the whole
phase. Every other plan carries a prohibition against flipping a PAGE requirement, precisely so this
edit happens once, after all software evidence exists.

Leave **PAGE-03 unmarked**. Add a short status note beside it stating that the software half landed,
that the hardware leg is held open per D-11 until the ordered part arrives, and citing the record
task 2 produced for the evidence-class split. Do not write a note that could be read as a partial
completion claim. PAGE-03 asks for a read-back on real silicon, and nothing in this phase provides
one for the 9.

Make the edit by hand. Do not run any state-sync verb: several of them corrupt the project state
file's frontmatter, and the requirements verbs reformat the whole file.

STEP 4 -- snapshot the roadmap before and after any tooling touches it, and diff the two, because a
phase-completion verb in this project has been observed to overwrite an unrelated phase's plan line.
If a diff shows a change outside this phase's own section, restore it.

STEP 5 -- commit in the meta repo, one commit covering both todos and the requirements edit.
  </action>
  <acceptance_criteria>
    - `.planning/todos/pending/new-host-old-firmware-0x05-page-size-skew.md` exists, is tracked, carries frontmatter with created, title, area, resolves_phase and files, and its body states both the mechanism and the prerelease-suffix caveat.
    - `.planning/todos/pending/error-message-band-a0-bf-exhausted.md` exists, is tracked, and names `0xBF` as the spent id and the unallocated range as the open question.
    - `.planning/REQUIREMENTS.md` marks PAGE-01 and PAGE-02 Complete, and their coverage cells match the file's existing form.
    - `.planning/REQUIREMENTS.md` does NOT mark PAGE-03 Complete, and carries a status note beside PAGE-03 that cites the 27-row record and states the hardware leg is held open.
    - `.planning/ROADMAP.md` is unchanged outside this phase's own section.
    - `.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md` and `.planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md` are both byte-unchanged.
    - No INSTR or WRITE requirement's state changed.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && git ls-files --error-unmatch .planning/todos/pending/new-host-old-firmware-0x05-page-size-skew.md .planning/todos/pending/error-message-band-a0-bf-exhausted.md >/dev/null && /usr/bin/grep -q 'resolves_phase' .planning/todos/pending/new-host-old-firmware-0x05-page-size-skew.md && /usr/bin/grep -q '0xBF' .planning/todos/pending/error-message-band-a0-bf-exhausted.md && echo TODOS-FILED</automated>
    <fails_when>`TODOS-FILED` is absent from the output. Either todo is missing or untracked, or the skew todo has no frontmatter key for its phase, or the band todo does not name the spent id</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -A2 'PAGE-03' .planning/REQUIREMENTS.md | /usr/bin/grep -q '\[ \]' && /usr/bin/grep -q '\[x\] \*\*PAGE-01' .planning/REQUIREMENTS.md && /usr/bin/grep -q '\[x\] \*\*PAGE-02' .planning/REQUIREMENTS.md && echo REQ-STATE-OK</automated>
    <fails_when>`REQ-STATE-OK` is absent from the output. PAGE-03 was marked complete, or PAGE-01 or PAGE-02 was left unmarked after all software evidence existed</fails_when>
    <automated>cd /workspaces && sha256sum -c - <<'SUMS'
c523d21f09efafad359d08d0847fe5696ca7b73e25f355302331c7e8678749bf  .planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md
7769fd12a091d156e464e6faad4e9b242872777b2a15a8e2392d5f4161e33b9f  .planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md
SUMS</automated>
    <fails_when>the output reports `FAILED` for either path, which means one of the two reviewed-and-not-folded todos was modified. The two hashes were measured at plan time on the unmodified files</fails_when>
    <automated>cd /workspaces && SHA=$(git log --format=%H -1) && echo "meta commit: $SHA" && NAMES=$(git show --name-only --format= "$SHA") && printf '%s\n' "$NAMES" && ! printf '%s\n' "$NAMES" | /usr/bin/grep -qx '.planning/ROADMAP.md' && echo ROADMAP-UNTOUCHED</automated>
    <fails_when>`ROADMAP-UNTOUCHED` is absent from the output, which means this task's own commit names the roadmap among its changed paths, and this task does not edit the roadmap</fails_when>
  </verify>
  <done>Per D-12 the new-host against old-firmware skew is filed with its mechanism and its version-probe caveat. Per U2 the band exhaustion is filed with the two facts that make the decision tractable. PAGE-01 and PAGE-02 are marked Complete once, by hand, after all software evidence exists. PAGE-03 is left unmarked with a note citing the evidence-class split, because nothing in this phase provides a silicon read-back for the 9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inherited comment to a future reader | A comment that outlived its truth actively misleads. Deleting a false clause is the cheapest correction that writes nothing |
| record artifact to the next phase and the issue reply | Both cite this record. A conflated evidence class becomes a claim about silicon that nobody ever made |
| requirement checkbox to a milestone close | A requirement marked Complete on the wrong evidence closes a question that is still open |
| planning tooling to unrelated roadmap sections | A completion verb in this project has been observed to overwrite an unrelated phase's plan line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-36 | Repudiation | PAGE-03 claimed on database evidence | high | mitigate | PAGE-03 is left unmarked, and the record states the split as 0 of 9 on hardware and 9 of 9 on the database comparison. A verify leg asserts the checkbox is still open |
| T-194-37 | Repudiation | conflated evidence classes in the record | high | mitigate | Hardware and database evidence appear as separate columns or sections, the split sentence is asserted verbatim, and a section states what the record does not prove |
| T-194-38 | Repudiation | a figure restated without its command | medium | mitigate | The record copies the milestone-record precedent that every number carries the verbatim command that produced it and the plan that ran it |
| T-194-39 | Tampering | a comment deletion that removes a true clause | high | mitigate | The pattern map's deletion-boundary table names both halves per site, and verify legs assert each surviving clause is still present as well as each falsified clause being gone |
| T-194-40 | Tampering | a comment deletion that changes executable code | high | mitigate | Two verify legs filter each repository's diff down to non-comment, non-blank lines and require the result to be empty |
| T-194-41 | Tampering | a planning verb reformatting or clobbering unrelated content | medium | mitigate | The requirements edit is performed by hand, state-sync verbs are prohibited, and a verify leg asserts the roadmap is unchanged by this task |
| T-194-42 | Repudiation | a deferred question lost instead of filed | medium | mitigate | Both deferrals become tracked pending todos with frontmatter, and a verify leg asserts both exist and are tracked |
| T-194-09 | Tampering | npm, pip or cargo installs | high | accept | This plan installs zero packages and changes no dependency manifest. RESEARCH records no package to audit, so no legitimacy checkpoint is required |
| T-194-35 | Information disclosure | a new host against old firmware still corrupts silently | high | accept | Deliberately out of scope per D-12, and filed here with its mechanism and its version-probe caveat rather than silently dropped |
</threat_model>

<verification>
- All five falsified comment clauses are gone, and every clause that stayed true is still present.
- Both sub-repo diffs for this plan contain no non-comment, non-blank line.
- Both AVR envs build inside their ceilings, both native envs are green, and both python suites are green.
- Both planning-citation gates report `OK:` with a file count.
- `.planning/v1.39/194-page-size-27-row-record.md` is tracked, holds 27 data rows, names all 9 parts and carries the evidence-split sentence.
- Both new pending todos are tracked and carry frontmatter.
- `.planning/REQUIREMENTS.md` marks PAGE-01 and PAGE-02 Complete and leaves PAGE-03 open.
- `.planning/ROADMAP.md` is unchanged by this plan.
- The two reviewed-and-not-folded todos are byte-unchanged.
</verification>

<success_criteria>
No comment in either sub-repo still restricts the per-chip page size to one algorithm, and no clause
that remained true was deleted with it. The 27-row measurement exists as a committed record whose
hardware and database evidence are kept apart, reading 0 of 9 on hardware and 9 of 9 on the database
comparison, with every figure attributed to the command that produced it. The skew question and the
band question are both filed as pending items with their reasons. PAGE-01 and PAGE-02 are Complete.
PAGE-03 is open, with a note that cites the record.
</success_criteria>

<output>
Create `.planning/phases/194-real-page-size-reaches-the-firmware/194-06-SUMMARY.md` when done.
Itemise every deleted clause per file, record the two drifted claims found in the pending INFO log
todo, and state the requirement states set and deliberately not set.
</output>
