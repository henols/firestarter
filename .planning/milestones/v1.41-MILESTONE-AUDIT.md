---
milestone: v1.41
name: Verification Moves to the Host
audited: 2026-09-23
audit_trigger: close-time — all 6 phases complete (`init.milestone-op` → phase_count 6, completed_phases 6)
supersedes: "the mid-flight audit of 2026-09-21 at 2/6 phases (commit b5fbc201), which read gaps_found 14/34 only because phases 204-207 did not yet exist"
status: tech_debt
scores:
  requirements: 34/34
  phases: 6/6
  integration: 9/9 seams WIRED
  flows: 7/7 complete
live_tree:
  firestarter_app: "64da8f6 (v1.41-verification-to-host)"
  firestarter_fw: "a55f2d8 (v1.41-verification-to-host)"
  wiki_origin_master: "13571d2"
gates_run_at_audit:
  app_pytest_py311: "2365 passed, 0 failed, 36 snapshots"
  app_ruff: "check clean; format --check clean over CI scope (firestarter/ tests/, 153 files)"
  fw_native: "244/244"
  fw_native_nodevtools: "244/244"
  fw_pytest_tests: "316 passed, 0 failed"
  messages_codegen: "catalog valid (79 messages); regenerated messages.h/messages.py byte-identical to synced copies"
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 202-one-comparison-engine-on-the-host
    items:
      - "WR-01: READ_ABORT_ACCEPTANCE_WINDOW_S = 3.0 s (eprom_operations.py:111) is reasoned from firmware timing, not measured under host scheduling/USB jitter. A beyond-the-window test exists (tests/test_eprom_operations.py:837); a jitter-loaded boundary test does not. Worst case: a deliberate abort misreported as exit 2."
      - "IN-01: dead `if not offs:` branch in CompareAccumulator.feed(). Deliberate defensive redundancy."
      - "CMP-F2: classify_streamed's `transport` bucket is still unreachable in production. Phase 206 D-09 kept it dead on purpose (`_run_cycle_block` calls `_run_step` with runs=1)."
      - "NO SECURITY.md — `workflow.security_enforcement` is true and the security step hook is active at verify:post."
      - "WR-02 CLOSED by Phase 206 (verdict 2 now maps to VERDICT_SKIPPED + STATUS_ERROR). Listed only to record that the mid-flight audit's open item is resolved."
  - phase: 203-the-write-guard-moves-up-a-layer
    items:
      - "WR-01: a --verify run whose write landed but whose --skip-sdp-unlock ack check fails prints 'did not complete -- nothing was verified'. Exit code 1 is correct; only the wording is wrong for that one state. Pinned by a test."
      - "WR-02: a guard refusal reached through an incomplete-but-not-mismatched compare prints a fabricated address/value (0x<region_start>/0x00). The refusal itself is fail-closed; only the diagnostic can mislead."
      - "IN-01: negative, page-aligned 0x05 address hits require_page_alignment before require_non_negative_address. No safety impact."
      - "Process: plan 203-03 did not follow RED/GREEN order. Documented in its own SUMMARY."
  - phase: 204-the-command-surfaces-leave-the-firmware
    items:
      - "WINDOWS.md entry 3 OPEN (kind unmet-truth): the bench part read Chip ID 0x1818 against the database's 0xda08 for W27C512/W27E512. Every 204 claim is identity-independent by construction, but the part's identity still needs operator sign-off. Does not block /gsd-ship (`workflow.windows_enforce` is false)."
      - "Bench coverage is Leonardo-only; no Uno-class board was attached. Stated plainly in both bench records."
      - "NO SECURITY.md (security_enforcement true)."
      - "204-VERIFICATION.md can never be re-fingerprinted as-is: one covered file (firestarter_fw/tests/test_blank_check_region_source_contract.py) was deleted by 205-03 commit 1cf1b22."
  - phase: 205-the-pre-flights-leave-the-firmware
    items:
      - "Bench coverage limits, self-declared in 205-BENCH-MATRIX.md: no true UV part (W27C512 proxy), zero validated Intel-flash or NOR-unlock parts to bench, no Uno-class board."
      - "firestarter_fw/CLAUDE.md says tests/ 'measured at 320 collected tests'; the live tree collects 316 (all pass)."
      - "NO SECURITY.md (security_enforcement true)."
      - "Accepted risk, stated at activation: with the device-side refusal gone, the host guard is the only safety net against half-programming a non-blank UV part. There is no second line of defence."
  - phase: 206-dev-test-keeps-its-fidelity-on-one-session
    items:
      - "Open operator decision from 206-REVIEW-FIX.md: `_run_step_untimed`'s SerialError/HardwareOperationError branch hard-sets run_count=1, so a default (runs=2/3) step that RAISES gets the `runs=1` repeat-policy tag and is re-keyed into the --fast dedup group. Pre-existing; changing it would re-key already-filed reports."
      - "206-REVIEW IN-01/IN-02 out of fix scope, not addressed."
      - "The lease does not reach HardwareManager (read_programmer_identity, sampler connects). Documented and accepted as D-05."
      - "206-VERIFICATION.md reads fresh only because its covered_files lists no source files. The three post-verification review fixes (f51e17b, ab6c2e4, 64da8f6) postdate it by ~7 h and are not covered by it. They ARE covered by this audit's green 2365-test run."
  - phase: 207-the-version-and-the-record
    items:
      - "Record drift: 207-REVIEW-FIX.md says wiki commit 13571d2 is 'committed locally only, not pushed'. It was pushed at 2026-09-23T20:25:36Z and is origin/master. STATE.md's Phase 207 decision line still describes WR-01 as not fixed."
  - phase: cross-phase
    items:
      - "Meta gitlink for firestarter_app is uncommitted: HEAD records 2a08c7d, the submodule is at 64da8f6 (the three 206 review-fix commits). Commit the gitlink advance before close."
      - "Both sub-repo milestone branches have no upstream. Expected — GSD pushes at ship time — but nothing is on GitHub yet."
      - "Verification freshness: 202, 203, 204, 205 read `stale` from verification.status. Every drift traced to later phases' own commits on shared files (REQUIREMENTS.md, ROADMAP.md, eprom_operations.py, cli_handlers.py, chip_test.py, write_blank_guard.py, firmware sources) plus the 205-03 deletion above. None traces to an unrecorded change. The full gate set re-ran green on the live tree in this audit."
      - "Untracked sub-repo clutter in firestarter_app: connect-cost-2026-09-23-110029/, datasheets/LST62832I.pdf. Not committed; remove or ignore before a clean sdist."
      - "`ruff format --check .` over the whole app tree flags .github/scripts/update_version.py. Outside CI's gated scope."
nyquist:
  skipped: true
  reason: "`workflow.nyquist_validation` is false and no validate-phase step hook is active at verify:post. VALIDATION.md files exist for 203-206 but are informational only."
security:
  hook_active: true
  verified_phases: [203, 206, 207]   # each status verified, threats_open 0, ASVS L1
  missing_phases: [202, 204, 205]
---

# Milestone v1.41 Audit — Verification Moves to the Host

**Audited:** 2026-09-23 (close-time)
**Status:** `tech_debt` — 34 of 34 requirements satisfied, all six phases verified `passed`, every
cross-phase seam wired, every end-to-end flow complete. No blockers. Accumulated debt is listed
below for a close-or-clean-up decision.

This audit supersedes the mid-flight audit of 2026-09-21 (commit `b5fbc201`). That audit read
`gaps_found` only because phases 204–207 did not yet exist.

## Scope

| Phase | Name | Plans | Verification | Security |
|---|---|---|---|---|
| 202 | One comparison engine, on the host | 5 | passed 8/8 (stale digest) | **missing** |
| 203 | The write guard moves up a layer | 4 | passed 5/5 (stale digest) | verified, 0 open |
| 204 | The command surfaces leave the firmware | 5 | passed 6/6 + 8/8 reqs (stale digest) | **missing** |
| 205 | The pre-flights leave the firmware | 8 | passed 9/9, re-verification after CR-01 (stale digest) | **missing** |
| 206 | `dev test` keeps its fidelity, on one session | 4 | passed 10/10 (fresh, but covers no source) | verified, 0 open |
| 207 | The version and the record | 3 | passed 4/4 (fresh) | verified, 0 open |

## Requirements Coverage (3-source cross-reference)

Sources: each phase's VERIFICATION.md requirements table; each SUMMARY.md's `requirements-completed`
frontmatter; the REQUIREMENTS.md traceability table and checkboxes.

| Group | IDs | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|---|---|---|---|---|---|---|
| CMP | 01–08 | 202 | all SATISFIED | union of 202-01..05 covers all 8 | `[x]` Complete | **satisfied** |
| WRITE | 01–06 | 203 | all SATISFIED | union of 203-01..04 covers all 6 | `[x]` Complete | **satisfied** |
| FWCMD | 01–06 | 204 | all SATISFIED | union of 204-01/02/04/05 covers all 6 | `[x]` Complete | **satisfied** |
| REL | 02, 03 | 204 | SATISFIED (bench) | 204-01, 204-05 | `[x]` Complete | **satisfied** |
| FWBLANK | 01–05 | 205 | all SATISFIED | union of 205-01/03/04/05/06/07/08 covers all 5 | `[x]` Complete | **satisfied** |
| DEVTEST | 01–03 | 206 | all SATISFIED | 206-01, 206-02 | `[x]` Complete | **satisfied** |
| SESS | 01, 02 | 206 | all SATISFIED | 206-03, 206-04 | `[x]` Complete | **satisfied** |
| REL | 01, 04 | 207 | SATISFIED | 207-01, 207-03 | `[x]` Complete | **satisfied** |

**Orphans:** none. Every ID in the traceability table appears in exactly the phase VERIFICATION.md
its row names.

**Three SUMMARYs list `requirements-completed: []`** — 204-03, 205-02 and 207-02. Each is deliberate
under the shared-ID convention: the plan contributed to IDs that a sibling plan closes, and says so
in its own frontmatter (207-02 in a comment). No ID depends on one of these three alone.

## Integration (gsd-integration-checker, against the live tree)

| # | Seam | Verdict | Evidence |
|---|---|---|---|
| 1 | Ordering safety: a host check exists wherever 205 removed a firmware pre-flight | WIRED | `GUARDED_PROTOCOL_IDS = {0x06, 0x07, 0x08, 0x0B, 0x10}` (`write_blank_guard.py:58`); `write_eprom` is the only composer of `COMMAND_WRITE` and calls the guard first (`eprom_operations.py:2500-2513`); `dev test` writes go through `write_eprom` (`chip_test.py:3417-3429`); `erase -b` runs the host check (`cli_handlers.py:1352-1360`); `erase -s -b` refused pre-wire; 0x06 at non-zero address withdraws the exemption (`write_blank_guard.py:171-215`) |
| 2 | No host path composes ordinal 4 or 6, or sets flag 0x08 | WIRED | Only historical comments remain; every `0x08` hit is a protocol id or debug-message id, not the control flag; `firmware.py` composes no memory-op ordinals |
| 3 | Constants/flag parity, `constants.py` ↔ `firestarter.h` | WIRED | Ordinal ladder and flag ladder identical; gaps 4, 6 and 0x08 carry matching never-reuse notes on both sides |
| 4 | `messages.toml` ↔ synced `messages.h` / `messages.py` | WIRED | `codegen.py --check` OK (79 messages); regenerated output byte-identical to both synced copies; orphaned debug ids annotated and never emitted |
| 5 | One comparison engine for verify, blank, write --verify, erase -b and dev test | WIRED | `_drive_region_compare` (`eprom_operations.py:2681`) is the shared drive; `chip_test.classify_fingerprint` delegates to `compare.classify_streamed`; no second implementation |
| 6 | 206 lease honoured by every method used inside a plan | WIRED | `lease()` (`eprom_operations.py:769`), teardown skipped only when leased (`:756-760`), single call site `cli_handlers.py:2950`; the guard's own read honours it. `HardwareManager` is outside the lease by decision D-05 |
| 7 | Version handling at 3.1.0b1 | WIRED | `firmware.py:290` uses PEP 440 `Version`; `_validate_firmware_version` gates only `major < 3`; no hard-coded 3.0.x assumption |
| 8 | Wiki claims vs code | WIRED | Refusal format matches `write_blank_guard._REFUSAL_FORMAT`; `Unknown command: %d` matches `messages.toml` and the 204 bench transcript; published `Testing-Chips.md` now says "three times" |
| 9 | Full gates green on the live tree, including 206's post-verification fixes | WIRED | See `gates_run_at_audit` in frontmatter |

**E2E flows — all COMPLETE:**

| Flow | Path |
|---|---|
| A. `verify` (match, mismatch, `--full`, region) | `cli_handlers.py:1146` → `verify_eprom` → `_drive_region_compare` → `COMMAND_READ` only → exit 0/1/2 |
| B. `blank` | same drive, `check_eprom_blank` |
| C. `write` on a UV part (guard, `-b`, `--verify`) | guard read → same-port write → optional read-back; distinct verdict per arm (`cli_handlers.py:889-963`); no arm prints "successful" on a non-match |
| D. `write` on an erasable part, incl. 0x06 at non-zero address | exemption withdrawn for 0x06 only; region check runs |
| E. `erase -b` / `erase -s -b` | host blank check after erase / refused before the wire, exit 2 |
| F. `dev test` with lease, verdict 2, dedup fingerprint | verdict 2 → `VERDICT_SKIPPED` + `STATUS_ERROR`; `cmp=host` tag only when non-empty; `captured_compare[-1]`; `run_count` stays nominal |
| G. Published 3.0.0b49 host vs 3.1.0b1 firmware | ordinal 4/6 → `is_memory_cmd` false → `configure_memory` never reached → `default:` arm emits `MSG_ERR_UNKNOWN_CMD` (`firestarter.cpp:74-92, 332-338`). Source-traced here; hardware-proven in `204-BENCH-MATRIX.md` |

**Not checked in this audit:** no hardware was driven. REL-02, REL-03 and flow G rest on the phase
bench records, which the current source is consistent with.

## Verification Integrity

`verification.status` reads `stale` for 202, 203, 204 and 205, and `passed` for 206 and 207.

- **All stale readings are benign.** Every covered file that moved after its verdict was moved by a
  later milestone phase's own commit: REQUIREMENTS.md and ROADMAP.md by 206/207 bookkeeping; the
  shared app sources by 203–206 feature commits; firmware sources by 205. One covered file for 204
  was deleted by 205-03 (`1cf1b22`), so 204's digest can never be recomputed as-is. No drift traces
  to an unrecorded change.
- **206's `passed` is weaker than it looks.** Its `covered_files` lists only `.planning/` files, so
  the digest cannot see the three source commits that landed after the verdict. Those commits are
  covered by this audit's full-suite run, not by 206-VERIFICATION.md.
- **Net:** the verdicts describe the tree as it was when each phase closed. The live tree is
  covered by this audit's gate run: 2365 app tests, 244 + 244 firmware native tests, 316 firmware
  pytest tests, ruff and codegen, all green.

## Tech Debt by Phase

The frontmatter `tech_debt` block is the complete list. The items that need an operator call, as
opposed to a record fix:

1. **WINDOWS #3** (204): chip ID `0x1818` vs `0xda08`. Operator sign-off on the bench part's identity.
2. **run_count=1 on a raised step** (206): changing it re-keys filed reports. Keep or change?
3. **Three missing SECURITY.md files** (202, 204, 205): `security_enforcement` is on. Run
   `/gsd-secure-phase` for each, or accept the gap at close.
4. **202 WR-01**: accept the reasoned 3.0 s abort window, or measure it under host load.

Housekeeping before close (no decision needed):

- Commit the `firestarter_app` gitlink advance, 2a08c7d → 64da8f6.
- Fix the 207 record drift: 207-REVIEW-FIX.md and STATE.md say the wiki fix is unpushed or unfixed,
  but it is live at `13571d2`.
- Update `firestarter_fw/CLAUDE.md`'s "320 collected tests" to 316.
- Remove or ignore the untracked `firestarter_app/connect-cost-2026-09-23-110029/` and
  `datasheets/LST62832I.pdf`.

**Total:** 26 open items across 6 phases plus cross-phase, and 1 closed item kept for the record
(202 WR-02). 4 of the open items need an operator decision.

## Nyquist

Skipped. `workflow.nyquist_validation` is false and no validate-phase hook is active.
