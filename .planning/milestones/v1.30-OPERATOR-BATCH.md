# v1.30 — Operator Batch

Everything that needs the operator, accumulated across phases 136 → 136.1 → 137 and presented
**once**, immediately before `/gsd-complete-milestone`. Per the operator's standing instruction
(2026-08-04, reaffirmed 2026-08-05): **drive all phases in order; do not stop mid-flight to ask.**

Anything Claude could decide, Claude decided and recorded in the owning phase's CONTEXT/RECORD.
This file holds only what genuinely needs a human — plus questions raised and answered along the way,
kept so the close can see what was assumed.

---

## A. Blocking — must be done by the operator before the milestone closes

| # | Item | Why it cannot be delegated | Raised by |
|---|---|---|---|
| A-1 | ~~Approve the gh#12 follow-up reply wording, then post it~~ — **RESOLVED 2026-08-05.** Wording **APPROVED** by the operator in real time at 137-05 Task 2, with one named correction made under that approval (already committed `3596604d`): the required silicon caveat ("No AT28C silicon was tested during this milestone") was woven into the "where I need help" paragraph, framed as the *reason help is needed* rather than as a disclaimer — this simultaneously resolved A-4. Frozen at `137-GH12-COMMENT.md` blob `3a628c56de4d45dfe2be0c645fced0e25d5ebceb` (2646 bytes). **Posting is HELD, not authorized** — the operator did not say "post now", and it would be moot regardless: 137-05 Task 3's fresh, independent shipped-check (re-run 2026-08-05, not reused from Task 1) confirms the removal has **not** shipped — `git -C firestarter_app merge-base --is-ancestor 259a0f0 origin/beta` still exits 1, and PyPI's highest published prerelease is still `3.0.0b15` (no new version beyond the pre-v1.30 baseline; `info.version` itself reads `2.0.7`, the latest **stable** release, which is a different field from "highest prerelease" — noted so a future reader does not mistake the two). **POST ONLY after the beta push (A-2) confirms the removal is live** — re-run 137-05 Task 1's live-check, then run exactly: `gh issue comment 12 --repo henols/firestarter_prom --body-file .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md`. | Phase 137 / CLOSE-06 |
| A-2 | **Push the branch and open the PR to `beta`** — **STAGED, ready to run.** | Outward-facing, so left for the operator. **`/gsd-pr-branch` turned out to be a no-op and no filtered branch was created** — measured, not assumed: `firestarter_app`'s `gsd/v1.30-sdp-surface-retirement` already has **zero** `.planning/` files in its diff against `origin/beta` (48 files: `tests/` 28, `firestarter/` 10, `tools/` 9, `pyproject.toml` 1). Executors commit code to the submodule and planning to meta, so the separation the filter exists to create is already structural. **That branch IS the PR head.** Body written at `.planning/milestones/v1.30-PR-BODY.md`. Run: `git -C firestarter_app push -u origin gsd/v1.30-sdp-surface-retirement` then `gh pr create --repo henols/firestarter_app --base beta --head gsd/v1.30-sdp-surface-retirement --title "v1.30 — SDP Surface Retirement & Behavioral Lock Proof" --body-file .planning/milestones/v1.30-PR-BODY.md`. **Merging pushes `beta` and auto-fires CI** — that cut a spurious beta twice at the v1.21 close; the PR body says so. **Do not PR the meta repo to `beta`:** meta forks off `main` per this project's own branching rule, and is 758 commits ahead of `origin/beta` — a meta PR to beta would be meaningless. | Standing close instruction |
| A-3 | ~~Decide how CLOSE-06 is ticked~~ — **RESOLVED 2026-08-05, operator chose option (b).** CLOSE-06 stays **`[ ]` open** — the more literal reading of "the gh#12 follow-up reply **is posted**", and it is not, per A-1's fresh shipped-check above. v1.30 will close at **55/56** with CLOSE-06 openly outstanding, consistent with this project's established pattern of recording an honest partial state (six consecutive `override_closeout` milestone closes) rather than a false-complete one. The single action that closes it is the follow-up command named in A-1's row, run after A-2 ships. | Phase 137 / CLOSE-06 |

| A-4 | ~~Resolve a direct conflict between your wording instruction and this phase's own claim gate~~ — **RESOLVED 2026-08-05, operator chose option (ii)**: reword so the checker's required caveat is satisfied without reading as a disclaimer, by weaving "No AT28C silicon was tested during this milestone" into the "where I need help" paragraph as the stated reason help is needed. Committed `3596604d`. `check_permitted_claims.py` now `PASS`es on `137-GH12-COMMENT.md` alone, and the gate is ARMED and green across all four closing artifacts for the first time this phase (verified: `PASS: scanned 137-LEDGER.md, 137-DECISION.md, 137-RELEASE-NOTES-app.md, 137-GH12-COMMENT.md`). Neither the gate mechanism nor any other wording was changed. This unblocks 137-06's CLOSE-01. | Phase 137 / 137-05 Task 1 |

---

## ▶ RUN STATUS — where the autonomous run stopped

**RESUMED and 137-05 now COMPLETE (2026-08-05).** The operator answered A-1/A-3/A-4 in real time at
Task 2's `checkpoint:human-action` blocking gate; Task 3 froze the reviewed text and, per the operator's
"hold" instruction plus a fresh shipped-check independently confirming NOT YET SHIPPED, took the
freeze-only branch (no `gh issue comment` call was made).

- **Requirements: 54 ticked / 2 open.** Open: **CLOSE-01** (137-06, blocked behind 137-05, now
  unblocked) and **CLOSE-06** (deliberately held open — see A-1/A-3).
- **Phases 131, 132, 133, 134, 136, 136.1 are complete.** Phase 137 is 5/6 plans done.
- **Nothing has been posted to GitHub. Nothing has been pushed. No release exists.**
- Fresh shipped-check re-run 2026-08-05 (137-05 Task 3, independent of Task 1's): RETIRE-01's deletion
  commit `259a0f0` is **still not** an ancestor of `origin/beta`, and PyPI's highest published
  prerelease is still `3.0.0b15`. So the removal has **not** shipped — consistent with A-3's disposition.

**To resume:** 137-06 → `/gsd-complete-milestone` → the staged beta PR (A-2) → then A-1's named
follow-up command to discharge CLOSE-06.

---

## ▶ PHASE 137 COMPLETE (2026-08-05) — this file is now ready for the operator, as ONE list

All six of Phase 137's plans are done. The claim gate is armed and green against all four real
closing artifacts (`PASS: scanned 137-LEDGER.md, 137-DECISION.md, 137-RELEASE-NOTES-app.md,
137-GH12-COMMENT.md`, re-confirmed live at 137-06 Task 1); CLOSE-01 is ticked. Project-wide
requirement state: **55 ticked / 1 open** (`CLOSE-06`, deliberately held open, see A-1/A-3 above —
this is v1.30 closing at **55/56 by design**, not an oversight). This is the "everything that needs
the operator, accumulated across phases 136 → 136.1 → 137" list, presented once, per this file's own
standing header instruction, immediately before `/gsd-complete-milestone`.

**Two items remain for the operator, in order:**
1. **A-2** — push the branch and open the PR to `beta` (opening is safe; merging fires CI, say so in
   the PR body).
2. **A-1's named follow-up command** — after A-2's push confirms the removal is live (re-run 137-05
   Task 1's live-check), post the frozen, already-approved `137-GH12-COMMENT.md` via:
   `gh issue comment 12 --repo henols/firestarter_prom --body-file
   .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md` — this is
   the single action that discharges CLOSE-06.

Nothing else in this file needs operator action before `/gsd-complete-milestone` can run. Sections B
(decisions Claude made), C (known-open findings), D (questions answered), and E (deferred policy
calls) are visible-not-blocking, as their own headers state.

---

## B. Decisions Claude made that the operator may want to reverse

*(Recorded so they are visible, not to block. Each names where the full reasoning lives.)*

| # | Decision | Where recorded |
|---|---|---|
| B-1 | Phase 134's verifier finding F-01 (LEG-02 cited "41 REFUSE chips"; the test actually covers 703 — every non-ALLOW entry) was **corrected in place** rather than filed as a defect, because Phase 137's honesty ledger consumes that citation. | `134-RECORD.md` correction 8 |
| B-2 | `134-VALIDATION.md` was approved by Claude before dispatch, with the plan-checker caveat later corrected when the concurrent planning session committed its `VERIFICATION PASSED` verdict. Original text kept visible rather than deleted. | `134-VALIDATION.md` |

---

## C. Known-open findings deliberately NOT fixed in v1.30

| # | Finding | Disposition |
|---|---|---|
| C-1 | `build_db_diff`'s `ladder_state` no longer reaches `community-reported` for a genuinely-passing ALLOW chip. Measured by 134-03, confirmed-and-deferred by 134-06 rather than absorbed. | **Disposition: defer-with-owner**, filed as `.planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md`, Owner: henols — Phase 137, plan 137-04 |
| C-2 | gh#20's underlying AT28C256 write-path failure (host 3.0.0b14, Rev 2.3, VPP 11.8V / VPE 13.7V). | Filed as backlog **999.29**, `Owner: henols` |
| C-3 | The **Evidence Ceiling**: "the lock inhibited the write" is not provable this milestone — no fixture simulates real inhibition; real silicon is missing with no fallback. | Permanent residual. Must be restated, never softened, in the close |

---

## D. Questions raised and answered mid-run

| # | Question | Answer given |
|---|---|---|
| D-1 | "41 REFUSE chips — why are any refused?" | REFUSE scopes **one test leg**, not chip support. The 41 are `infoic.xml`'s own answer (b15=0 — no SDP command decoder). On those parts the SDP sequence is not inert; its bytes land as data at the bus-truncated magic addresses. Claude's initial "AT28C64 is a curation gap" claim was **wrong** and is a twice-repeated documented error → now requirement PROV-05. |
| D-2 | "No ICs refused — generate it from infoic.xml" | The list already **is** infoic-derived (Phase 120). Re-proven 2026-08-05: live fetch of minipro @ `a8efaed` re-derived a **byte-identical** partition. The real gap was provenance, not coverage → Phase 136.1 (PROV-01…06), which changes provenance and **never** verdicts. |

---

## E. Operator decisions deferred — recorded as policy calls, not implementer guesses

Both come from the gh#8 / 999.15 channel-split design (2026-07-28), which explicitly left them to the
operator. **Neither blocks any of CHAN-01…07**, so Phase 136 shipped without them.

| # | Decision | Why it is yours, not Claude's |
|---|---|---|
| E-1 | **Is welding beta → dev-tools acceptable at all?** | It protects stable users while leaving the beta community fully exposed to the same tools. That is a policy call about who deserves protection, not a technical one. |
| E-2 | **Should `dev read` / `dev test` graduate out of the `dev` namespace?** | Deferred by gh#8 §5, forced by v1.21, deferred again 2026-07-28, and now a third time. Three deferrals suggests it wants deciding rather than re-deferring. |

---

*Appended to as phases 136, 136.1 and 137 run.*
