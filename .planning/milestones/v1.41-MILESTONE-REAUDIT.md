---
milestone: v1.41
name: Verification Moves to the Host
audited: 2026-09-24
audit_trigger: "post-207.1 re-audit — 207.1 is verified (22/22), secured and validated; both gitlinks were advanced at 02afa6be"
supersedes: "the second re-audit in this file (commit 3f7a4b78, 2026-09-24). Its findings are carried forward below; the deltas are listed under `changed_since_previous_reaudit`."
relation_to_close_audit: "does NOT supersede `.planning/milestones/v1.41-MILESTONE-AUDIT.md` (2026-09-23, commit 4bab20c9). That file stays unchanged: it is the source list of `207.1-LEDGER.md`, and `207.1-VERIFICATION.md` lists it in `covered_files`, so an edit would move that digest."
status: tech_debt
scores:
  requirements: 34/34
  phases: 7/7 verified
  integration: "9/9 seams WIRED; 1 warning (SESS-01 failure-recovery edge, 207.1 WR-02)"
  flows: 7/7 complete
live_tree:
  firestarter_app: "db8d491 (v1.41-verification-to-host)"
  firestarter_fw: "dbdf9c8 (v1.41-verification-to-host)"
  meta_gitlinks_committed: "app db8d491, fw dbdf9c8 (02afa6be). Both gitlinks equal the live heads."
  sub_repo_worktrees: "clean in both"
  on_any_remote: "nothing from v1.41 (207.1-VERIFICATION truth 22; unchanged since)"
changed_since_previous_reaudit:
  - "205-SECURITY.md written (db82425e): verified, threats_open 0. The operator chose 'Accept all open' for T-205-10 and T-205-16 (both high) and AR-205-01..05."
  - "207.1-06 and 207.1-07 finished: gitlinks advanced once (3f46a284), closing ledger written (466f84fd, 27 rows)."
  - "207.1 verified (22/22 passed, recorded at 3effc710), code-reviewed (231dca1c, 0 critical / 3 warning / 6 info), secured (aab7a790) and validated (be681b85)."
  - "Both gitlinks advanced again (02afa6be) over post-phase commits: app b8b920c (CLAUDE.md rewrite) and db8d491 (`dev reg --firestarter` help text now names the real CTRL_* constants); fw dbdf9c8 (CLAUDE.md rewrite)."
  - "STATE.md `## Session` now reads 'Phase 207.1 complete — all phases complete'. The stale-position item is closed."
  - "ROADMAP.md ticks all seven 207.1 plans, so `init.milestone-op` completed_phases 7/7 is now true. The overcount item is closed."
gates_run_at_audit:
  app_ruff_py311: "ruff 0.16.8: check clean over firestarter/ tests/; format --check clean over the whole tree (175 files)"
  app_pytest_py311: "2373 passed, 0 failed, 36 snapshots, coverage 86.40 % (fail-under 70), 412 s"
  fw_native: "244/244"
  fw_native_nodevtools: "244/244"
  fw_pytest_tests: "316 passed"
  messages_codegen: "catalog valid (79 messages, version 1); regenerated messages.h and messages.py byte-identical to the synced copies"
gaps:
  requirements: []
  phases: []
  integration: []
  flows: []
tech_debt:
  - phase: 207.1-address-v1-41-tech-debt
    items:
      - "OPEN — 207.1-REVIEW WR-02 (pre-existing since 206): `_setup_operation` calls `consume_remaining_input()` at eprom_operations.py:696, before the `try` whose `except SerialError` calls `_disconnect_programmer()` (:697-708). A drain failure leaves the dead link leased, so later leased steps re-drain it instead of cold-connecting. Contradicts lease()'s D-06 docstring and 206-SECURITY T-206-12. The integration checker reproduced it on the live tree. No test covers it (test_session_lease.py `_FakeComm.consume_remaining_input` never raises). Affects SESS-01 and the dev test flow on a mid-run USB unplug only. Fix is a two-line move plus one test leg."
      - "OPEN — 207.1-REVIEW WR-01: the D-03 comment at chip_test.py:2618-2641 names one cycle-block route to `runs=1` (the `hardware_refused` break) and omits the second (a raised cycle becomes SKIPPED and drops out of `ran`). It also contradicts itself: 'would re-key reports already filed' at :2626 vs 're-key none of them' at :2640. Comment only; the D-03 decision stands."
      - "OPEN — 207.1-REVIEW WR-03 (pre-existing): `update_version.py --set-version` does not enter beta mode; on a non-beta ref it silently writes a stable patch bump. Publish path, no test, outside every CI gate. Needs an operator disposition (fix or backlog)."
      - "OPEN — 207.1-REVIEW IN-01..IN-06: info-level style, duplication and hygiene findings."
  - phase: cross-phase
    items:
      - "OPEN — the Nyquist hook is active only through an UNCOMMITTED edit: working-tree .planning/config.json sets workflow.nyquist_validation true, HEAD sets false. The 202, 204 and 207.1 validate-phase runs happened under that edit. Commit it or revert it."
      - "ACCEPTED (D-19, extended) — every phase digest 202-207.1 reads `stale`. 207 and 207.1 are new since the ledger: db8d491 changed cli_handlers.py (help text only) and dbdf9c8 rewrote fw CLAUDE.md, both covered files of 207.1. The green gate run above is the mitigation, as in D-19."
      - "INFO — the meta working tree carries unrelated uncommitted edits (.devcontainer/, .vscode/, .planning/estimation-calibration.json, .planning/graphs/). None is milestone work; settle them before /gsd-ship so the ship commit does not sweep them in."
  - phase: closed-by-207.1
    items:
      - "All 26 close-time audit items plus 202 WR-02 are dispositioned in 207.1-LEDGER.md: 14 fixed, 11 accepted, 2 closed on evidence. Verified 22/22 by 207.1-VERIFICATION.md."
nyquist:
  hook_active: true   # through the uncommitted config edit above
  compliant_phases: [202, 204, 207.1]
  partial_phases: [203]
  not_validated_phases: [205, 206]
  missing_phases: [207]
  overall: partial
security:
  hook_active: true
  verified_phases: [202, 203, 204, 205, 206, 207, 207.1]
  missing_phases: []
---

# Milestone v1.41 Re-Audit — Verification Moves to the Host

**Audited:** 2026-09-24, after Phase 207.1 closed. This is the third re-audit, and the first with
every milestone phase verified.

**Status:** `tech_debt`. There is no blocker:

- All 34 requirements are satisfied, and the three sources agree.
- All 7 phases are verified, and all 7 have a verified SECURITY.md.
- All 9 seams are wired and all 7 flows are complete.
- Every gate is green on the live tree, and both gitlinks equal the live sub-repo heads.

What remains is the three warnings from `207.1-REVIEW.md`, of which WR-02 is a real (edge-case)
defect, plus the uncommitted Nyquist config edit.

The close-time audit (`v1.41-MILESTONE-AUDIT.md`, 2026-09-23) stays as written.

## What changed since the second re-audit (3f7a4b78)

| Change | Commit | Effect |
|---|---|---|
| 205-SECURITY.md: verified, 0 open (operator accepted T-205-10, T-205-16) | db82425e | Last missing SECURITY.md closed |
| Gitlinks advanced once; closing ledger (27 rows) | 3f46a284, 466f84fd | D-13 closed; all 26 items dispositioned |
| 207.1 verified 22/22, reviewed, secured, validated | 3effc710, 231dca1c, aab7a790, be681b85 | The "unverified phase" blocker is gone |
| Gitlinks advanced over post-phase commits | 02afa6be | app `db8d491`, fw `dbdf9c8`: meta HEAD now ships the live trees |
| STATE.md session line and ROADMAP 207.1 checkboxes current | — | Two items the earlier re-audits raised are closed |

## Scope

| Phase | Plans | Verification | Security | Nyquist |
|---|---|---|---|---|
| 202 | 5 | passed 8/8 | verified, 0 open | COMPLIANT |
| 203 | 4 | passed 5/5 | verified, 0 open | PARTIAL (`nyquist_compliant: false`) |
| 204 | 5 | passed 6/6 + 8/8 reqs | verified, 0 open | COMPLIANT |
| 205 | 8 | passed 9/9 | verified, 0 open (**new**) | NOT-VALIDATED (draft) |
| 206 | 4 | passed 10/10 | verified, 0 open | NOT-VALIDATED (draft) |
| 207 | 3 | passed 4/4 | verified, 0 open | MISSING |
| 207.1 | 7 | passed 22/22 (**new**) | verified, 0 open (**new**) | COMPLIANT (**new**) |

## Requirements Coverage (3-source cross-reference)

| Source | Result |
|---|---|
| REQUIREMENTS.md traceability table | 34 rows, all `Complete`; file unchanged since the close-time audit |
| REQUIREMENTS.md checkboxes | 34 ticked `[x]`, 0 unticked |
| Phase VERIFICATION.md files | All 7 read `passed`. 202-207 together name all 34 IDs; 207.1 carries none |
| SUMMARY `requirements-completed` fields | Together they cover all 34 IDs. 204-03, 205-02, 207-02 and the seven 207.1 SUMMARYs list none |

| Group | Phase | Final |
|---|---|---|
| CMP-01…08 | 202 | satisfied |
| WRITE-01…06 | 203 | satisfied |
| FWCMD-01…06, REL-02, REL-03 | 204 | satisfied |
| FWBLANK-01…05 | 205 | satisfied |
| DEVTEST-01…03, SESS-02 | 206 | satisfied |
| SESS-01 | 206 | satisfied, with the WR-02 warning below |
| REL-01, REL-04 | 207 | satisfied |

**Orphans:** none.

SESS-01 stays `satisfied`. Its verified contract (one link reused across the dev test's steps) holds
on every path the checker traced. WR-02 breaks only the documented fallback after a transport
failure during the pre-setup drain. That is tech debt against SESS-01, not a failed requirement.

## Integration (gsd-integration-checker, live tree at app db8d491 / fw dbdf9c8)

The checker was told the earlier result and was asked to re-derive it. It did not run the full
suites. This audit ran them (see the frontmatter).

| # | Seam | Status | Anchor |
|---|---|---|---|
| 1 | Compare engine ↔ verify / blank / write guard | WIRED | `eprom_operations.py:2363`, `:3005`, `:3253` → `_drive_region_compare` |
| 2 | Write guard, negative-address gate before page alignment | WIRED | `eprom_operations.py:2481-2482`, `cli_handlers.py:876-877` |
| 3 | Ordinals 4 and 6 retired in both ladders and in dispatch | WIRED | 0 hits in `constants.py` and fw `include/`, `src/`; `firestarter.cpp:333` → `MSG_ERR_UNKNOWN_CMD` |
| 4 | Host pre-flight blank check replaces the firmware one | WIRED | fw blank-check machinery 0 hits; `cli_handlers.py:1262`, `:1386`, `:906` |
| 5 | Dev test step ↔ compare evidence | WIRED | `chip_test.py:2726-2740`, `diagnostic_report.py:335` |
| 6 | `setup_command` `is_connected()` assert on every caller | WIRED | `serial_comm.py:858`; callers `_probe_port` `:964`, leased `_setup_operation` `eprom_operations.py:690` |
| 7 | `lease()` | WIRED | `eprom_operations.py:802`; sole call site `cli_handlers.py:2978` |
| 8 | Messages catalog ↔ synced artifacts | WIRED | byte-identical regeneration; no messages file in either post-phase diff |
| 9 | Version strings | WIRED | both `3.1.0b1` |

**Flows:** read, verify, write with guard (including `--verify`), blank check, `erase -b`, dev test
and `fw`. All 7 are COMPLETE.

**Delta since the second re-audit.** App `5302f63..db8d491` touches `CLAUDE.md` and 8 lines of
`cli_handlers.py`. The help text of `dev reg --firestarter` now names the CTRL_* constants. The
checker confirmed all nine named constants and values at `constants.py:194-202`, and that no
snapshot pins the string. Fw `4b14111..dbdf9c8` touches `CLAUDE.md` only. No regression and no
requirement is affected.

**WARNING — 207.1 WR-02 confirmed and reproduced.** At `eprom_operations.py:696` the leased branch
drains input outside the `try/except SerialError` at `:697-708`. The checker simulated a
`SerialError` from the drain: `_disconnect_programmer()` was not called, `comm` stayed set, and the
lease stayed held. pyserial keeps `is_open` true after a read exception, so the next leased step takes
the same branch again. `test_session_lease.py` never makes the drain raise. The checker said the
gap was "never flagged" in the milestone trail. That is wrong: `207.1-REVIEW.md` WR-02 flagged it.
It is not in the ledger because the ledger scope was the 26 close-time items.

The bench-only evidence (FWCMD-05 on target, the 204/205 bench matrices, FWBLANK-05 flash and RAM
figures, SESS-02 timing, REL-04's wiki) was not re-run. It is accepted on the phase records, as in
the earlier audits.

## Tech Debt

### Closed by 207.1

All 26 close-time items plus 202 WR-02 are dispositioned in `207.1-LEDGER.md`: 14 fixed, 11
accepted, 2 closed on evidence. `207.1-VERIFICATION.md` verified the ledger row by row.

### Open

| # | Item | Source | Weight |
|---|---|---|---|
| 1 | Lease drain outside the `SerialError` handler; dead link stays leased after a mid-drain unplug | 207.1-REVIEW WR-02 (from 206) | Real defect, edge case. Two-line fix plus one test leg |
| 2 | D-03 comment omits the second cycle-block route and contradicts itself (`:2626` vs `:2640`) | 207.1-REVIEW WR-01 | Comment only |
| 3 | `update_version.py --set-version` silently takes the stable path on a non-beta ref | 207.1-REVIEW WR-03 (pre-existing) | Publish path; needs an operator disposition |
| 4 | IN-01..IN-06 | 207.1-REVIEW | Info |
| 5 | Nyquist hook active only through the uncommitted `workflow.nyquist_validation: true` | config.json working tree | Record hygiene |
| 6 | All phase digests `stale`; 207 and 207.1 newly so after post-phase commits | `verification.status` | Accepted under D-19; the gate run is the mitigation |
| 7 | Unrelated uncommitted meta edits (`.devcontainer/`, `.vscode/`, `estimation-calibration.json`, `graphs/`) | `git status` | Settle before `/gsd-ship` |

## Nyquist

The hook is active only through the uncommitted config edit.

| Phase | VALIDATION.md | Classification | Action |
|---|---|---|---|
| 202, 204, 207.1 | exists | COMPLIANT | none |
| 203 | exists | PARTIAL: validated, `nyquist_compliant: false` | `/gsd-validate-phase 203` |
| 205, 206 | exists | NOT-VALIDATED: `status: draft` | `/gsd-validate-phase 205`, `206` |
| 207 | missing | MISSING | `/gsd-validate-phase 207`, or accept |

This section is discovery only. It auto-ran nothing.

## Recommendation

None of the open items needs a new phase:

1. WR-01 and WR-02 are small app changes: `/gsd-code-review 207.1 --fix` (or a `/gsd-quick`) closes
   both. WR-02 is the one worth fixing before a beta push, because it is a real behavior gap under a
   documented contract.
2. WR-03: fix it, or file it to the backlog. It touches the publish path, so it is the operator's call.
3. Commit or revert the Nyquist config edit. If kept, validate 203, 205, 206 and 207, or accept them
   at close.
4. Then `/gsd-complete-milestone v1.41`.
