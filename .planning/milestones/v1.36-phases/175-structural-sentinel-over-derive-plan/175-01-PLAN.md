---
phase: 175-structural-sentinel-over-derive-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/plan_corpus.py
  - firestarter_app/tests/test_derive_plan_structural_sentinel.py
  - .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-tracer-end-to-end.txt
  - .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-anti-vacuity-red-green.txt
autonomous: true
requirements: [PRUNE-06]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One structural claim travels the whole spine end to end: firestarter_app/tests/plan_corpus.py builds 1,354 real Plan objects from the shipped database through EpromDatabase(skip_local_override=True) per D-07 at write_scope full and partial per D-08, firestarter_app/tests/test_derive_plan_structural_sentinel.py runs a write-to-verify predicate over every one of them, the predicate calls the production cycle_block_bounds helper per D-02 rather than re-deriving the block rule, a hand-built counter-plan carrying a write and no verify is reported as a violation per D-09, and the whole pass is transcribed into evidence/175-01-tracer-end-to-end.txt."
    - "The op vocabulary is totally partitioned and fails closed per D-01 and D-06: the 13 OP_* string constants discovered at runtime from vars(chip_test) split into exactly 2 requires-verify members and 11 exempt members each carrying a non-empty prose reason, the union equals the discovered set, the intersection is empty, and injecting a fourteenth OP_* constant into the module namespace at runtime makes the partition assertion raise rather than pass."
    - "The requires-verify bucket is declared as a literal frozenset of OP_WRITE and OP_WRITE_PARTIAL and is never computed by subtraction from any registry the product module owns, per D-01's explicit rejection of derivation-by-subtraction."
    - "A verify only counts as an oracle when it is supported per D-03 and matches its write on write_region, region_policy and cycle_payload per D-04: removing the verify step, flipping the verify to supported=False, and skewing the verify's write_region start by one each flag 1,354 of 1,354 write-bearing plans."
    - "Sweeping the predicate across all 1,354 shipped full-scope and partial-scope plans reports zero write-without-verify violations, and the sweep asserts its own corpus size as the absolute number 1354 before asserting zero violations, so a sweep that visited no rows cannot read green."
    - "The corpus census is pinned absolutely: 746 database rows, 677 distinct part numbers, 1,354 plans, 16,248 steps, 9,304 unsupported steps."
    - "A write whose index falls outside the half-open range cycle_block_bounds returns is reported as a VIOLATION and never skipped: the hand-built shape write, verify, sdp-lock, write, verify returns bounds (0, 2) and the second write at index 3 is flagged, because cycle_block_bounds finds only the first block and the shipped corpus never exercises this arm."
    - "A plan with no write step is vacuously clean and says so: an empty step list, a hand-built id/read/blank-check shape, and a real write_scope='none' plan each return bounds of None and zero violations, and the write_scope='none' leg asserts as a fixture-setup premise that the plan really carries no write step."
    - "A plan whose only write carries supported=False produces zero violations by an explicit, documented decision rather than by accident, and the sweep asserts that the shipped corpus contains exactly zero unsupported write steps, so the arm is recorded as currently unreachable rather than silently untested."
    - "firestarter_app/firestarter/ is byte-unchanged and the firmware repo is byte-unchanged: this phase reads product code and modifies none of it."
    - "Every Phase 174 frozen hash is unmoved: tests/test_blast_radius_invariance.py and tests/test_rekey_ledger.py still report 114 passed, and no row is added to .planning/MILESTONES.md by this phase."
    - "Neither new file carries a comment line, neither carries a pytest skip marker of any kind, and the mypy watermark still reports 35 errors at watermark 35."
  artifacts:
    - path: firestarter_app/tests/plan_corpus.py
      provides: "Shared REAL_DB singleton, all_rows selector, PART_NUMBERS, SWEEP_SCOPES, plan_corpus() cached corpus builder, mock_operator, plan_with_steps, step"
      contains: "skip_local_override=True"
    - path: firestarter_app/tests/test_derive_plan_structural_sentinel.py
      provides: "REQUIRES_VERIFY, EXEMPT_REASONS, assert_total_partition, write_verify_violations, the whole-database sweep and the anti-vacuity legs"
      contains: "cycle_block_bounds"
    - path: .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-tracer-end-to-end.txt
      provides: "Transcript of the census, the partition, the whole-database sweep, the one-chip run_plan alignment and the module run"
    - path: .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-anti-vacuity-red-green.txt
      provides: "Observed RED for each of the four predicate weakenings, then the restored GREEN"
  key_links:
    - from: firestarter_app/tests/test_derive_plan_structural_sentinel.py
      to: firestarter_app/firestarter/chip_test.py
      via: "the predicate calls the production cycle_block_bounds helper and discovers the op vocabulary from vars(chip_test); it re-implements neither"
      pattern: "cycle_block_bounds"
    - from: firestarter_app/tests/test_derive_plan_structural_sentinel.py
      to: firestarter_app/tests/plan_corpus.py
      via: "the sentinel imports the corpus and the hand-built-plan helpers rather than building a second database instance"
      pattern: "from tests.plan_corpus import"
    - from: firestarter_app/tests/plan_corpus.py
      to: firestarter_app/firestarter/database.py
      via: "one module-level EpromDatabase(skip_local_override=True), so no developer override can change a sweep result"
      pattern: "skip_local_override=True"
  prohibitions:
    - statement: "No production file is modified. This phase reads firestarter_app/firestarter/ and the firmware repo and writes to neither. If a locked decision appears to require a production change, the plan says so loudly rather than authorising the change."
      status: required
      verification: judgment
    - statement: "No comment line is written into any source file, at all. Docstrings are the house mechanism for explanation and are the only place an explanation may go. This is a standing operator hard rule that a plan cannot override."
      status: required
      verification: judgment
    - statement: "The requires-verify bucket is never derived by subtraction from a registry the product module owns. Both candidate registries happen to equal the correct answer today and both silently re-scope when widened for an unrelated purpose."
      status: required
      verification: judgment
    - statement: "The cycle-block rule is never re-implemented. The production helper is called. Its own docstring family list omits the NA erase step, so a rule built from the docstring is wrong on 373 plans."
      status: required
      verification: judgment
    - statement: "A bare grep is never used to produce gate evidence. The devcontainer grep is ugrep and honors .gitignore, so it silently under-scans."
      status: required
      verification: judgment
    - statement: "No pytest skip marker of any kind is added to a new module. A gate that can skip itself is the fail-open shape this phase exists to prevent."
      status: required
      verification: judgment
    - statement: "A Step is never mutated in place and a Plan is never copied with copy.copy. The corpus is shared and module-scoped, so an in-place edit silently corrupts every later test in the module. Every mutation goes through dataclasses.replace."
      status: required
      verification: judgment
    - statement: "EpromDatabase is never constructed without skip_local_override=True. One shipped module omits it and is a counter-example never to copy."
      status: required
      verification: judgment
---

<objective>
Carry ONE structural claim — "a plan never emits a write with no verify behind it" — through every
layer of the sentinel, end to end, and watch each layer go RED before it goes green.

The layers, in order: a shared corpus module that turns the shipped database into 1,354 real `Plan`
objects; a total, fail-closed partition of the module's own 13 `OP_*` constants into requires-verify
and exempt-with-a-named-reason (D-01, D-06); a relational predicate anchored to the production
`cycle_block_bounds` helper (D-02) that demands a *supported* (D-03), field-matching (D-04) verify at
a higher index inside the same cycle block; a sweep across all 1,354 plans at `full` and `partial`
(D-07, D-08); a one-chip `run_plan` alignment smoke that proves the shared operator double is wired
for plan 175-04; and anti-vacuity in both shapes D-09 requires — a hand-built counter-plan and a
mutated-corpus sweep.

Purpose: this is the thinnest complete path through the licence Phase 177 will cite. If the
architecture is wrong — if the shared corpus module cannot be imported by sibling test modules, if
the predicate cannot be anchored to the production helper, if a new annotated helper trips the
zero-headroom mypy watermark — it is wrong after one commit, not after five. Plans 175-02, 175-03 and
175-04 are expansion across a spine this plan proves.

Output: two new source files in `firestarter_app` and two committed evidence transcripts in the meta
repo.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-CONTEXT.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-RESEARCH.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-PATTERNS.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<environment_preamble>
Every command in this plan runs from `/workspaces` unless it says otherwise, and every Python
invocation goes through the py3.11 CI-replica interpreter, never the devcontainer's default 3.12.

The replica venv already exists and was re-verified this planning session:
`firestarter_app/.venv311/bin/python` is Python 3.11.16 with pytest 9.1.1, and the editable install
resolves to `/workspaces/firestarter_app/firestarter/__init__.py`. Use `./.venv311/bin/python` from
inside `firestarter_app`.

Five measured environment facts bind every command below:

- `grep` in this devcontainer is ugrep 7.8.4 and honors `.gitignore`, so it silently under-scans. Use
  `/usr/bin/grep` for anything asserted as gate evidence.
- pytest `addopts` is `-ra -q`. Clear it with `-o addopts=""` and pass exactly one `-q` so the
  `N passed` summary line lands at the start of a line. Every evidence transcript is captured that way.
- The app suite is GREEN at the branch base: `pytest tests/ -o addopts="" -q` measured
  **302.20 s, 2108 passed, 1 warning, 32 snapshots passed**. There is no pre-existing RED to carve out.
  `test_flash_path_record_sync` is **not** in `firestarter_app` — it is the firmware repo's — so the
  app suite has no whole-repo-porcelain gate and no commit-before-running requirement.
- The mypy watermark has **zero headroom**: `tools/check_mypy_watermark.py` reports
  `checked 165 source files` / `mypy errors: 35 (watermark: 35)`. `tests/*.py` is inside mypy's scope
  and inside `ruff check firestarter/ tests/`; `tests/fixtures/` and `tools/` are inside neither. With
  `check_untyped_defs = false`, an **annotated** def's body IS checked and an unannotated one's is not,
  so annotated helpers are the entire risk surface.
- CI also runs `ruff format --check firestarter/ tests/`. Run `ruff format` on both new files before
  committing.

Commits: both new files are committed INSIDE the `firestarter_app` submodule on branch
`gsd/v1.36-dev-test-fidelity`. Both repos are already on that branch. The submodule gitlink and the two
evidence transcripts are committed in the meta repo. Do not create, rename or switch a branch.

Zero comments. Not one `#` line in either file this plan writes, and no HTML comment in any Markdown
it writes. All reasoning goes in docstrings. The code excerpts in `175-RESEARCH.md` and
`175-PATTERNS.md` carry `# Source:` lines that are quotations of surrounding prose — they must not be
copied through. Docstrings are not comments and are required.

Do NOT run the meta repo's `tools/wiki/selftest.sh`; it mutates Phase 168 evidence.
</environment_preamble>

<tasks>

<task type="tracer">
  <name>Task 1: The predicate spine — shared corpus, fail-closed op partition, write-to-verify predicate, whole-database sweep, one-chip run_plan alignment</name>
  <reversibility rating="reversible">Both files are new test-side modules that nothing in production imports; the op-partition reason strings and the predicate's field set can be edited in place at any later point without a migration.</reversibility>
  <files>firestarter_app/tests/plan_corpus.py, firestarter_app/tests/test_derive_plan_structural_sentinel.py, .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-tracer-end-to-end.txt</files>
  <read_first>
    - `firestarter_app/firestarter/chip_test.py` lines 314 to 344 (the 13 `OP_*` string constants D-01 partitions, in three groups), 406 to 445 (the `Step` and `Plan` dataclasses — note `Step.reason` is a required positional and both classes are plain, non-frozen dataclasses), 486 to 520 (`derive_plan`'s signature and docstring), 1226 to 1262 (`_CYCLE_BLOCK_OPS`, `_CYCLE_BLOCK_START_OPS` and `cycle_block_bounds` in full — read the body, not only the docstring, because the docstring's family list omits the NA erase step), 1582 to 1642 (`run_plan`'s signature, its `runs: int = 2` default and the `StepResult(op="__plan__")` guard that fires when `runs < 2` without `allow_single_run=True`), 1025 to 1075 (`StepResult` and `_skip_result`)
    - `firestarter_app/tests/test_op_registration_parity.py` lines 126 to 195 (`_ALL_OPS` built from `vars(chip_test_mod)` and the import-time `assert len(_ALL_OPS) == 13` census pin), lines 440 to 470 (`_OP_REGISTRY_EXEMPTIONS` — the `dict` keyed to a prose reason *value*, which is the house mechanism that satisfies the no-comments rule), lines 810 to 936 (the empty-reason guard, the stale-row guard and the `try/except AssertionError/else raise` non-vacuity shape). Copy the STRUCTURES and the docstrings; this module carries large pre-2026-08-29 `#` comment blocks at `:126-128`, `:143-152`, `:172-186` and `:815-817` that must NOT be copied through
    - `firestarter_app/tests/test_erase_flag_invariants.py` lines 1 to 68 (the module-docstring anti-vacuity rubric to reproduce), line 98 (`_REAL_DB = EpromDatabase(skip_local_override=True)` — copy THIS line), lines 113 to 132 (`_all_rows`, the two-level descent; it is an annotated def that is currently error-free at watermark 35, so a verbatim copy is safe), lines 264 to 303 (the element-wise op-order pin and its drift-naming message)
    - `firestarter_app/tests/test_chip_test.py` lines 985 to 1100 (the fuller `_mock_operator` variant — the one that sets `sdp_lock.return_value` and `sdp_unlock.return_value` — plus `_OPERATOR_METHODS` and `_plan_with_steps`; these defs are unannotated and must stay that way), lines 1453 to 1476 (`test_cycle_block_bounds_matches_each_family_plan_shape` — four chips; cite it, do not restate it), lines 2601 to 2612 (`assert len(results) == len(plan.steps)` for M8720 at `full` — the one-chip instance of the alignment claim; cite it, leave it alone)
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` lines 215 to 268 (`_REAL_DB`, `_OPERATOR_METHODS`, the near-copy `_mock_operator`, `_plan_with_steps`, `_result`) and lines 827 to 897 (`test_shipped_ops_never_reach_sdp_arm` — the `vars(chip_test)` discovery idiom). Structures yes, `#` comment blocks at `:210-228`, `:264-275` and `:887-888` no
    - `firestarter_app/tests/fw_presence.py` and `firestarter_app/tests/scan_paths.py` (the placement convention: a shared helper never lives in a `test_*`-prefixed module, and `from tests.X import Y` is the established import idiom because `tests/__init__.py` exists)
    - `firestarter_app/pyproject.toml` lines 105 to 132 (pytest `addopts`, ruff scope and `extend-exclude`) and lines 155 to 180 (mypy `check_untyped_defs`, `disallow_untyped_defs` and the `tests/fixtures/` exclude)
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-RESEARCH.md` sections B, C, D, E and the Code Examples section
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-PATTERNS.md` in full
  </read_first>
  <behavior>
    - Importing `tests.plan_corpus` and calling `plan_corpus()` returns a mapping with exactly 1,354 entries keyed `(part_number, scope)` for 677 part numbers at scopes `full` and `partial`.
    - `all_rows(REAL_DB)` returns exactly 746 `(manufacturer, chip_record)` pairs, and `PART_NUMBERS` has exactly 677 members.
    - Summing `len(plan.steps)` over the corpus gives exactly 16,248, of which exactly 9,304 steps have `supported=False`.
    - The op vocabulary discovered from `vars(chip_test)` has exactly 13 members, all `str`.
    - `REQUIRES_VERIFY | set(EXEMPT_REASONS)` equals that discovered set, `REQUIRES_VERIFY & set(EXEMPT_REASONS)` is empty, and every value in `EXEMPT_REASONS` is non-empty after `strip()`.
    - Adding a fourteenth `OP_*` attribute to the `chip_test` module namespace at runtime makes `assert_total_partition` raise `AssertionError`.
    - `write_verify_violations` returns an empty list for every one of the 1,354 shipped plans.
    - `write_verify_violations` returns a non-empty list for a hand-built plan whose steps are `id`, `read`, `write`, `erase` with no verify.
    - `run_plan` on the `AT28C256` plan at `write_scope="full"` with the shared `mock_operator()` and the default `runs=2` returns exactly as many `StepResult` objects as the plan has steps.
  </behavior>
  <action>
Build the spine. Each layer is the thinnest thing that is production-quality, not a stub — every file
this task creates is the file plans 175-02, 175-03 and 175-04 expand.

**Layer 1, `firestarter_app/tests/plan_corpus.py`.** A shared, non-`test_`-prefixed helper module at
the top level of `tests/`, NOT under `tests/fixtures/` — that directory is excluded from both ruff
(`pyproject.toml:121`) and mypy (`:174`), and a shared helper should be linted. Do not import anything
from `tests/test_chip_test.py`; that would be the first import from a `test_*` module in this tree and
would drag its module-level database construction into every importer.

Export, with these exact names:

- `REAL_DB` — one module-level `EpromDatabase(skip_local_override=True)`, copying
  `test_erase_flag_invariants.py:98` verbatim. Do NOT copy `test_chip_test_cycle.py:24`, which omits
  the flag and makes results machine-dependent. The module docstring states that D-07 fixes the sweep
  domain to the shipped database only and that the sentinel makes no claim about user-override entries
  in `~/.firestarter/database.json`.
- `all_rows(db)` — the two-level descent over `db.proms`, copied verbatim from
  `test_erase_flag_invariants.py:113-132` including its `-> list[tuple[str, dict]]` annotation, which is
  measured error-free at watermark 35. The docstring repeats the trap: `db.proms` maps a manufacturer
  key to a *list* of chip records, so a one-level `for row in db.proms:` iterates manufacturer strings
  and every downstream assertion built on it passes vacuously.
- `PART_NUMBERS` — a sorted tuple of the distinct `part_number` values across `all_rows(REAL_DB)`,
  measured 677. The docstring records that 746 rows collapse to 677 names because 65 part numbers
  appear on more than one row, and that `derive_plan(name, db)` resolves them all to the same plan, so
  the corpus is keyed by name and not by row.
- `SWEEP_SCOPES` — the tuple `("full", "partial")`, per D-08. The docstring records that
  `write_scope="none"` emits no write step so the write-to-verify rule is vacuously true across it, and
  that `dev test` itself only ever produces `full` or `partial`, making these two a strict superset of
  the reachable domain.
- `plan_corpus()` — returns a `dict` keyed `(part_number, scope)` to a `Plan`, built by calling
  `derive_plan(name, REAL_DB, write_scope=scope)` for every name in `PART_NUMBERS` and every scope in
  `SWEEP_SCOPES`. Cache the built mapping in a module-level private variable and return the cached
  object on every later call, so the 1.26 s build is paid once per pytest process no matter how many
  sibling modules import it. Leave this def unannotated.
- `OPERATOR_METHODS` — the eight-name list `check_eprom_id`, `read_eprom`, `check_eprom_blank`,
  `write_eprom`, `verify_eprom`, `erase_eprom`, `sdp_lock`, `sdp_unlock`.
- `mock_operator(**returns)` — the fuller variant from `test_chip_test.py:1009`, the one that sets
  `sdp_lock.return_value` and `sdp_unlock.return_value` rather than letting them fall through to a
  truthy auto-Mock. `Mock(spec=OPERATOR_METHODS)` is load-bearing: without `spec=`, an out-of-spec
  attribute access silently returns a truthy Mock instead of raising. `check_eprom_id` returns the
  fixed `(True, 0x1234)`. Leave this def unannotated. The docstring must state two things plainly:
  that this is the FOURTH copy of this helper in the tree (the others are at
  `tests/test_chip_test.py:1009`, `tests/test_chip_test_sdp_leg.py:239` and
  `tests/test_chip_test_cycle.py:26`) so the drift is visible rather than discovered later; and that
  the fixed `(True, 0x1234)` mismatches most real chip IDs, which closes the destructive gate and makes
  2,545 of the 6,944 supported steps come back SKIPPED, so a sweep built on this double proves step and
  result alignment and NA-on-unsupported, and does NOT exercise the write path on most chips.
- `plan_with_steps(*steps, name="M8720", is_uv=False)` — builds a `Plan` from bare `Step`s, following
  `test_chip_test.py:1093`. Unannotated.
- `step(op, *, supported=True, reason="", **fields)` — a thin `Step` builder, because `Step.reason` is
  a required positional and a hand-built counter-plan reads badly without it. Unannotated.

Keep every new def in this module unannotated except the verbatim `all_rows` copy. Run
`tools/check_mypy_watermark.py` before committing; the watermark has zero headroom and this module is
inside mypy's scope.

**Layer 2, `firestarter_app/tests/test_derive_plan_structural_sentinel.py`.** Import from the corpus
module with `from tests.plan_corpus import ...`, the idiom `tests/scan_paths.py:80` already uses. Do
not construct a second `EpromDatabase` anywhere in this file.

Write a module docstring carrying four blocks:

1. **What this module adds that is not already covered.** `tests/test_op_registration_parity.py` is
   already a total, fail-closed `(op, registry)` gate over the same 13-op vocabulary, with an
   import-time `assert len(_ALL_OPS) == 13` that makes a fourteenth op fail *collection* of that
   module. This module's new axis is the **verify disposition** — for each op, whether it needs an
   oracle and, when it does not, the named reason why. Say that explicitly, or a later reader deletes
   this closure as a duplicate. Also cite `tests/test_chip_test.py:1453-1476`, which pins
   `cycle_block_bounds` block contents for four named chips, and state that this module generalizes
   the same helper across the whole database rather than restating those four.
2. **The anti-vacuity rule, stated up front**, reproducing the rubric of
   `tests/test_erase_flag_invariants.py:1-22` and naming the specific way a lazy assertion passes
   vacuously here: a sweep whose selector visits zero rows reports zero violations, which is why every
   sweep in this module asserts its corpus size as an absolute number before asserting a zero.
3. **Why the exemption bucket is the deliverable.** The predicate is three lines; the reason each of
   the eleven non-write ops does not need a verify is the actual work. Name the load-bearing case:
   `write-inhibited`'s read-back IS its oracle and it expects INEQUALITY —
   `_dispatch_sdp_leg` sets `source_payload, expected_readback = pattern_b, pattern_a` at
   `chip_test.py:3291-3296` — so an `OP_VERIFY` behind it would assert the opposite of the step's
   intent. A naive rule does not merely over-fire on the 40 chips with a live SDP leg; it demands a
   step that inverts the test.
4. **Why the block rule is not re-implemented.** D-02 requires the production helper. The helper's own
   docstring family list shows the UV block as two steps wide, but `derive_plan` emits the erase step
   anyway as `supported=False` and the helper does not filter on `supported`, so the real UV block is
   three steps wide on 373 plans. A rule built from the docstring would be wrong.

Then the module-level declarations:

- `REQUIRES_VERIFY: frozenset[str]` — a literal `frozenset` containing exactly `chip_test.OP_WRITE`
  and `chip_test.OP_WRITE_PARTIAL`. Declare it literally. It happens to equal the module's own
  cycle-block start registry by value today, and it happens to be the write-shaped subset of the
  module's destructive registry; D-01 rejects deriving it from either, because both silently re-scope
  when widened for an unrelated ordering or gating purpose.
- `EXEMPT_REASONS: dict[str, str]` — eleven entries keyed by the `OP_*` constant, each value a prose
  sentence. Reasons are values, not comments; this is the house mechanism and it satisfies the
  no-comments rule. Use the eleven reasons tabulated in `175-RESEARCH.md` section B: `OP_ID` read-only
  identity compare, `OP_READ` read-only and its own output is its result, `OP_BLANK_CHECK` read-only
  and itself an oracle so an oracle does not need an oracle, `OP_VERIFY` it IS the oracle,
  `OP_ERASE` clears rather than staging bytes so a verify has no expected buffer, with its oracle being
  the blank-check `derive_plan` relocates behind it and its own separately-named leg landing in plan
  175-02 per D-05, `OP_SDP_LOCK` and `OP_SDP_UNLOCK` change protection state and not contents with the
  following read-back as oracle, and the four SDP-leg write-shaped ops `OP_WRITE_BASELINE_B`,
  `OP_WRITE_BASELINE_A`, `OP_WRITE_INHIBITED` and `OP_WRITE_RESTORED` each carrying
  `_dispatch_sdp_leg`'s inline read-back as their oracle, with `OP_WRITE_INHIBITED`'s reason naming the
  inequality expectation and the `chip_test.py:3291-3296` line range.
- `module_op_constants()` — the three-line `vars(chip_test)` discovery comprehension from
  `test_chip_test_sdp_leg.py:840-846`: every value whose name starts with `OP_` and which is a `str`.
  Unannotated.
- `assert_total_partition(discovered)` — asserts the union of the two buckets equals `discovered` with
  a message naming the symmetric difference, asserts the intersection is empty, and asserts every
  reason is non-empty after `strip()`. Keep the census assertion OUT of this function so a fourteenth
  op fails the partition rather than the count. Unannotated.
- `write_verify_violations(plan)` — the predicate. Call `chip_test.cycle_block_bounds(plan.steps)`
  once. For each enumerated step: skip it unless its op is in `REQUIRES_VERIFY` and it is
  `supported`; if the bounds are `None`, or the index is not inside the half-open range, append a
  violation whose third element is the string `write sits outside the cycle block` and continue; then
  look for any candidate in `plan.steps[index + 1 : stop]` whose op is `OP_VERIFY`, whose `supported`
  is `True` (D-03), and whose `write_region`, `region_policy` and `cycle_payload` all equal the write
  step's (D-04); if none is found, append a violation whose third element is
  `no supported, field-matching verify in the block`. Return a list of
  `(index, op, reason)` tuples. Unannotated.

  The out-of-block branch is fail-closed and load-bearing: `cycle_block_bounds` returns only the FIRST
  maximal run, so `[write, verify, sdp-lock, write, verify]` returns `(0, 2)` and the second write at
  index 3 is entirely outside it. Treating that as a skip would leave a second write unguarded. The
  shipped corpus has exactly one write per plan, so this arm is unreachable today and is covered by a
  hand-built leg in Task 2. Do not write `if bounds is None: continue`, and do not write a lower-bound
  check without the matching upper-bound check.

  An unsupported write is skipped by explicit decision, stated in the function's docstring: a step that
  never calls the operator cannot be a write with no oracle. The shipped corpus contains exactly zero
  unsupported write steps, so this arm is currently unreachable, and Task 2 records that as a measured
  fact rather than leaving an unexplained branch.

Then the tests in this task:

- `test_op_census_is_thirteen` — `len(module_op_constants()) == 13`, with a message telling the reader
  the census baked into the docstring and into `EXEMPT_REASONS` needs re-measuring. Mirrors
  `test_op_registration_parity.py:186-190`.
- `test_op_vocabulary_is_totally_partitioned` — calls `assert_total_partition(module_op_constants())`
  and additionally asserts `len(REQUIRES_VERIFY) == 2` and `len(EXEMPT_REASONS) == 11`.
- `test_a_fourteenth_op_fails_the_partition_closed` — uses pytest's `monkeypatch` fixture to
  `setattr(chip_test, "OP_FUTURE_WRITE", "future-write", raising=False)`, then wraps
  `assert_total_partition(module_op_constants())` in the `try/except AssertionError: pass /
  else: raise AssertionError(...)` shape from `test_op_registration_parity.py:903-928`, whose `else`
  message names the exact defect: a future operation type omitted from the closure list escaped the
  check, so the partition is not fail-closed. This is roadmap success criterion 3 read literally.
- `test_corpus_census_is_pinned` — `len(all_rows(REAL_DB)) == 746`, `len(PART_NUMBERS) == 677`,
  `len(plan_corpus()) == 1354`, the summed step count `== 16248` and the unsupported step count
  `== 9304`, each with a drift-naming message. This is the anti-empty floor every later sweep rests on.
- `test_no_shipped_plan_emits_a_write_without_a_verify` — asserts `len(corpus) == 1354` FIRST, then
  collects `(key, violations)` for every plan and asserts the collected list is empty, with a message
  showing the first ten offenders. Roadmap success criterion 2.
- `test_no_shipped_plan_carries_an_unsupported_write` — counts steps whose op is in `REQUIRES_VERIFY`
  and whose `supported` is `False` across the corpus and asserts the count is exactly 0, so the
  predicate's skip-an-NA-write arm is recorded as measured-unreachable rather than untested.
- `test_one_chip_run_plan_alignment_smoke` — derives `AT28C256` at `write_scope="full"`, runs it
  through `chip_test.run_plan(plan, mock_operator(), REAL_DB)` at the default `runs=2`, and asserts
  `len(results) == len(plan.steps)`. Its docstring states that this exists to prove the shared
  `mock_operator` double is wired before plan 175-04 builds a 1,354-plan sweep on it, cites
  `tests/test_chip_test.py:2612` as the shipped M8720 instance of the same claim, and records that
  `run_plan` with `runs < 2` and without `allow_single_run=True` returns a single
  `StepResult(op="__plan__")`, so the default `runs=2` is deliberate.

Finally, run `ruff check firestarter/ tests/`, `ruff format firestarter/ tests/` and
`tools/check_mypy_watermark.py`, then write the transcript. Emit each measurement as its own
`key=value` line into
`.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-tracer-end-to-end.txt`,
following the `== LABEL` / `rc=N` shape of
`.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-tracer-end-to-end.txt`.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/plan_corpus.py` exists and defines `REAL_DB`, `all_rows`, `PART_NUMBERS`, `SWEEP_SCOPES`, `plan_corpus`, `OPERATOR_METHODS`, `mock_operator`, `plan_with_steps` and `step`.
    - `firestarter_app/tests/test_derive_plan_structural_sentinel.py` exists and defines `REQUIRES_VERIFY`, `EXEMPT_REASONS`, `module_op_constants`, `assert_total_partition` and `write_verify_violations`.
    - `len(REQUIRES_VERIFY) == 2` and `len(EXEMPT_REASONS) == 11`, and their union equals the 13-member set discovered from `vars(chip_test)`.
    - The transcript records `rows=746`, `names=677`, `plans=1354`, `steps=16248`, `unsupported=9304`, `ops=13`, `requires_verify=2`, `exempt=11`, `violations=0`, `unsupported_writes=0` and `align=OK`.
    - `write_verify_violations` on a hand-built `id, read, write, erase` plan returns a non-empty list.
    - The module docstring of the sentinel names `test_op_registration_parity` and states the verify-disposition axis it adds.
    - The sentinel module calls `cycle_block_bounds(` and constructs no `EpromDatabase(`.
    - `REQUIRES_VERIFY` is assigned from a `frozenset(` literal and not from any private module registry.
    - Neither new file contains a line matching `^\s*#`, and neither contains `pytest.mark.skip`, `skipif` or `pytest.skip`.
    - `ruff check firestarter/ tests/` exits 0, `ruff format --check firestarter/ tests/` exits 0, and `tools/check_mypy_watermark.py` prints `mypy errors: 35 (watermark: 35)`.
    - `git -C firestarter_app status --porcelain firestarter/` prints nothing and `git -C firestarter status --porcelain` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/175-structural-sentinel-over-derive-plan/evidence && mkdir -p "$E" && EV=$E/175-01-tracer-end-to-end.txt && { echo "== 1 corpus census"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.plan_corpus import REAL_DB, all_rows, PART_NUMBERS, plan_corpus; c=plan_corpus(); print('rows='+str(len(all_rows(REAL_DB)))); print('names='+str(len(PART_NUMBERS))); print('plans='+str(len(c))); print('steps='+str(sum(len(p.steps) for p in c.values()))); print('unsupported='+str(sum(1 for p in c.values() for s in p.steps if not s.supported)))"); echo "rc=$?"; echo "== 2 op vocabulary partition"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.test_derive_plan_structural_sentinel import REQUIRES_VERIFY, EXEMPT_REASONS, module_op_constants, assert_total_partition; d=module_op_constants(); print('ops='+str(len(d))); print('requires_verify='+str(len(REQUIRES_VERIFY))); print('exempt='+str(len(EXEMPT_REASONS))); assert_total_partition(d); print('partition=OK')"); echo "rc=$?"; echo "== 3 predicate over the whole database"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.plan_corpus import plan_corpus; from tests.test_derive_plan_structural_sentinel import write_verify_violations, REQUIRES_VERIFY; c=plan_corpus(); print('violations='+str(sum(len(write_verify_violations(p)) for p in c.values()))); print('unsupported_writes='+str(sum(1 for p in c.values() for s in p.steps if s.op in REQUIRES_VERIFY and not s.supported)))"); echo "rc=$?"; echo "== 4 one-chip run_plan alignment"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); import firestarter.chip_test as ct; from tests.plan_corpus import REAL_DB, mock_operator; p=ct.derive_plan('AT28C256', REAL_DB, write_scope='full'); r=ct.run_plan(p, mock_operator(), REAL_DB); print('align='+('OK' if len(r)==len(p.steps) else 'MISALIGNED')); print('align_steps='+str(len(p.steps)))"); echo "rc=$?"; echo "== 5 the new module"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_derive_plan_structural_sentinel.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 5 && /usr/bin/grep -qx 'rows=746' "$EV" && /usr/bin/grep -qx 'names=677' "$EV" && /usr/bin/grep -qx 'plans=1354' "$EV" && /usr/bin/grep -qx 'steps=16248' "$EV" && /usr/bin/grep -qx 'unsupported=9304' "$EV" && /usr/bin/grep -qx 'ops=13' "$EV" && /usr/bin/grep -qx 'requires_verify=2' "$EV" && /usr/bin/grep -qx 'exempt=11' "$EV" && /usr/bin/grep -qx 'partition=OK' "$EV" && /usr/bin/grep -qx 'violations=0' "$EV" && /usr/bin/grep -qx 'unsupported_writes=0' "$EV" && /usr/bin/grep -qx 'align=OK' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV" && ! /usr/bin/grep -qE '[0-9]+ skipped' "$EV"</automated>
    <fails_when>Fewer or more than exactly five `rc=0` lines appear in the transcript; or any one of the exact lines `rows=746`, `names=677`, `plans=1354`, `steps=16248`, `unsupported=9304`, `ops=13`, `requires_verify=2`, `exempt=11`, `partition=OK`, `violations=0`, `unsupported_writes=0`, `align=OK` is absent (a changed count means the corpus, the vocabulary or the predicate moved and the pinned number must be re-measured, not edited to match); or the pytest leg emits `no tests ran`, any `N failed` or `N error` count, any `N skipped` count, or no line starting with a passed count. The app suite is green at the branch base, so none of these signals can come from a pre-existing failure.</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/plan_corpus.py firestarter_app/tests/test_derive_plan_structural_sentinel.py | /usr/bin/grep -vE ':0$' | wc -l)" -eq 0 && ! /usr/bin/grep -qE 'pytest\.mark\.skip|skipif|pytest\.skip' firestarter_app/tests/plan_corpus.py firestarter_app/tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -q 'skip_local_override=True' firestarter_app/tests/plan_corpus.py && /usr/bin/grep -q 'cycle_block_bounds(' firestarter_app/tests/test_derive_plan_structural_sentinel.py && ! /usr/bin/grep -qE '^[[:space:]]*[A-Za-z_][A-Za-z0-9_]*[[:space:]]*=[[:space:]]*EpromDatabase\(' firestarter_app/tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -qE '^REQUIRES_VERIFY[^=]*=[^=]*frozenset\(' firestarter_app/tests/test_derive_plan_structural_sentinel.py && ! /usr/bin/grep -qE '^REQUIRES_VERIFY[^=]*=[^=]*_[A-Z_]+_OPS' firestarter_app/tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -q 'test_op_registration_parity' firestarter_app/tests/test_derive_plan_structural_sentinel.py && (cd firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/ && ./.venv311/bin/python -m ruff format --check firestarter/ tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)') && test -z "$(git -C firestarter_app status --porcelain firestarter/)" && test -z "$(git -C firestarter status --porcelain)"</automated>
    <fails_when>Either new file reports a non-zero comment-line count; either contains a skip marker; the corpus module lacks the deterministic-database flag; the sentinel module does not call the production block helper, or constructs its own database, or assigns its requires-verify set from something other than a `frozenset(` literal, or assigns it from a private registry name ending in `_OPS`, or its docstring does not name the parity module it must distinguish itself from; `ruff check` or `ruff format --check` exits non-zero; the watermark line is anything other than `mypy errors: 35 (watermark: 35)`; or either `git status --porcelain` invocation prints a path, meaning a product module or the firmware repo was modified in a test-only phase.</fails_when>
  </verify>
  <done>The corpus, the fail-closed partition, the predicate, the whole-database sweep and the one-chip alignment smoke all run green with every pinned count matching, the two new files are comment-free and lint-clean at the mypy watermark, no product file moved, and the transcript is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Anti-vacuity — nine hand-built counter-plans, three mutated-corpus legs, and four predicate weakenings each SEEN red</name>
  <files>firestarter_app/tests/test_derive_plan_structural_sentinel.py, .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-anti-vacuity-red-green.txt</files>
  <read_first>
    - `firestarter_app/tests/test_derive_plan_structural_sentinel.py` and `firestarter_app/tests/plan_corpus.py` as Task 1 left them
    - `firestarter_app/tests/test_op_registration_parity.py` lines 903 to 936 (the `try/except AssertionError/else raise` non-vacuity shape, whose `else` branch names the exact defect — stronger than `pytest.raises` because the message says what went wrong) and lines 821 to 858 (the empty-reason and stale-row guards, each of which asserts the CLEAN case first and only then plants)
    - `firestarter_app/tests/test_erase_flag_invariants.py` lines 1 to 68 (the Reachability block that transcribes the observed RED per leg and cites the artifact that recorded it — reproduce that block in this module)
    - `firestarter_app/firestarter/chip_test.py` lines 406 to 445 again, this time for the mutation rules: `Plan` and `Step` are plain, MUTABLE, non-frozen dataclasses, `Plan.steps` is a `list`, and `Step.write_region` is an optional `tuple[int, int]`
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-RESEARCH.md` section C (the measured `cycle_block_bounds` result for every edge shape) and section D (the measured mutation sensitivity table)
    - `.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt` (the `== LABEL` / `rc_<name>=N` transcript idiom this task's transcript follows)
  </read_first>
  <behavior>
    - A hand-built `id, read, write, erase` plan with no verify is flagged with the reason `no supported, field-matching verify in the block`.
    - A hand-built `write, verify, sdp-lock, write, verify` plan returns `cycle_block_bounds` of `(0, 2)` and flags exactly one violation, at index 3, with the reason `write sits outside the cycle block`.
    - A hand-built `verify, write` plan is flagged; a hand-built `write, sdp-lock, verify` plan is flagged; a hand-built `write` alone is flagged.
    - A hand-built `write, verify` pair whose verify is `supported=False` is flagged.
    - A hand-built `write, verify` pair whose verify's `write_region` differs from the write's is flagged; likewise for `region_policy` and for `cycle_payload`.
    - An empty step list, a hand-built `id, read, blank-check` plan and a real `write_scope="none"` plan each return `cycle_block_bounds` of `None` and zero violations.
    - A hand-built `id, read, write(NA), verify, erase` plan returns bounds `(2, 5)` and zero violations.
    - Removing every `OP_VERIFY` step from a copy of each write-bearing shipped plan flags 1,354 of 1,354.
    - Flipping every `OP_VERIFY` step's `supported` to `False` on a copy flags 1,354 of 1,354.
    - Skewing every `OP_VERIFY` step's `write_region` start by one on a copy flags 1,354 of 1,354.
  </behavior>
  <action>
Prove the predicate is not tautological, in both shapes D-09 requires, and then prove the proof itself
is reachable by watching it fail.

**Part A — the hand-built counter-plans.** Build each shape with `plan_with_steps` and `step` from the
corpus module. Nine tests, one named function each. Every one asserts the CLEAN case first where a
clean case exists, the discipline `test_op_registration_parity.py:821` establishes.

- `test_planted_counter_plan_with_no_verify_is_flagged` — `id, read, write, erase`. Roadmap success
  criterion 1, stated in the docstring as such. Assert the violation's reason string is
  `no supported, field-matching verify in the block`.
- `test_a_second_write_outside_the_block_is_a_violation_not_a_skip` — `write, verify, sdp-lock, write,
  verify`. Assert `cycle_block_bounds` returns `(0, 2)` as a fixture-setup premise, using the
  `fixture setup error: ...` message prefix from `test_chip_test_blank_check_order.py:148` so a broken
  premise reads differently from a broken invariant; then assert exactly one violation, at index 3,
  with the reason `write sits outside the cycle block`. The docstring records that this arm is
  unreachable on the shipped corpus, where every plan carries exactly one write, and that it is coded
  fail-closed for that reason.
- `test_a_verify_at_a_lower_index_is_not_an_oracle` — `verify, write`.
- `test_a_verify_outside_the_block_is_not_an_oracle` — `write, sdp-lock, verify`. Assert bounds are
  `(0, 1)` as a premise, which is what makes D-02's same-cycle-block reading stronger than
  anywhere-later-in-the-list.
- `test_a_lone_write_is_flagged` — `write` alone, bounds `(0, 1)`.
- `test_an_unsupported_verify_is_not_an_oracle` — `write, verify(NA)`. D-03.
- `test_a_field_skewed_verify_is_not_an_oracle` — three sub-cases in one function, each a `write,
  verify` pair identical except for one of `write_region`, `region_policy` and `cycle_payload`. All
  three are flagged. D-04.
- `test_plans_with_no_write_are_vacuously_clean` — an empty step list, a hand-built `id, read,
  blank-check` plan, and a real `derive_plan(name, REAL_DB, write_scope="none")` plan for a name taken
  from `PART_NUMBERS`. Each returns bounds `None` and zero violations. For the real plan, assert as a
  fixture-setup premise that it carries no step whose op is in `REQUIRES_VERIFY`, so the leg cannot
  pass because the premise silently changed.
- `test_an_unsupported_write_is_skipped_by_decision` — `id, read, write(NA), verify, erase`, bounds
  `(2, 5)`, zero violations. The docstring states this is a decision and not an omission, and cites the
  measured fact that the shipped corpus carries zero unsupported write steps, so the arm is currently
  unreachable.

**Part B — the mutated-corpus legs.** Three tests. Each one first filters the corpus to plans carrying
a supported write, asserts that count is exactly `1354` with a message saying a sweep that visits zero
rows must not pass, then mutates a COPY of every one of them and asserts the unflagged list is empty,
showing the first ten names on failure.

Copy rules, which are Phase 174's own CR-01 defect wearing new clothes and are not negotiable: use
`dataclasses.replace(plan, steps=[...])`, which yields a fresh list while sharing the `Step` objects.
`copy.copy(plan)` SHARES `plan.steps` and is forbidden. `copy.deepcopy` is unnecessary and two orders
slower over 1,354 plans. For a mutation that changes a FIELD rather than list membership, build a new
`Step` with `dataclasses.replace(step, ...)`; never assign to a `Step` attribute in place, because the
corpus is module-cached and those `Step` objects are shared with every other test in the process.

- `test_removing_the_verify_flags_every_write_bearing_plan` — steps filtered to drop `OP_VERIFY`.
- `test_an_unsupported_verify_flags_every_write_bearing_plan` — each `OP_VERIFY` step replaced with a
  copy whose `supported` is `False`. D-03.
- `test_a_region_skewed_verify_flags_every_write_bearing_plan` — each `OP_VERIFY` step replaced with a
  copy whose `write_region` start is incremented by one. D-04.

Add a Reachability block to the module docstring, following
`test_erase_flag_invariants.py:64-68`, which names each anti-vacuity leg and cites
`evidence/175-01-anti-vacuity-red-green.txt` as the artifact carrying its observed RED.

**Part C — see the RED.** A gate authored before the content it guards can be unreachable and prove
nothing, so each weakening below must be observed, not assumed.

Commit the module with Parts A and B first. Then, one at a time, apply each weakening to
`write_verify_violations`, run
`./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_derive_plan_structural_sentinel.py`,
append the output to the transcript, and restore the file with
`git -C firestarter_app checkout -- tests/test_derive_plan_structural_sentinel.py` before applying the
next one. The four weakenings, each mapping to a decision:

- **W1** — make the function return an empty list unconditionally. Every Part A and Part B leg must go
  RED.
- **W2** — drop the `supported` conjunct from the verify-candidate test. The D-03 legs must go RED.
- **W3** — drop the three field-equality conjuncts. The D-04 legs must go RED.
- **W4** — replace the outside-the-block violation with a `continue`, which is the warning sign
  `175-RESEARCH.md` Pitfall 2 names. The second-write leg must go RED.

After W4 is restored, run the module once more clean. The transcript's shape follows
`174-01-anti-vacuity-red-green.txt`: a `== LABEL` line stating the weakening and that RED is expected,
the raw pytest output, then an `rc_w<N>=1` line; and finally a `== RESTORED` section with the clean run
and an `rc_clean=0` line. Capture every run with `-o addopts=""` or the `-ra -q` default suppresses the
count line and the transcript cannot show a failed count at all.

Leave no residue: after the last restore, `git -C firestarter_app status --porcelain
tests/test_derive_plan_structural_sentinel.py` must print nothing.
  </action>
  <acceptance_criteria>
    - The sentinel module defines at least nine hand-built counter-plan tests and exactly three mutated-corpus tests.
    - Each mutated-corpus test asserts the write-bearing plan count is exactly `1354` before it asserts anything about flags.
    - The transcript contains four sections labelled for W1 through W4, each followed by an `rc_w<N>=1` line and each containing a line matching `[0-9]+ failed`.
    - The transcript contains a `== RESTORED` section followed by `rc_clean=0` and a line matching `^[0-9]+ passed`, with no failed, error or skipped count in that section.
    - The module docstring contains a Reachability block naming `175-01-anti-vacuity-red-green.txt`.
    - The module uses `dataclasses.replace` and never imports the standard-library shallow-and-deep-copy module, which is the only route to the shared-steps-list trap.
    - `git -C firestarter_app status --porcelain tests/test_derive_plan_structural_sentinel.py` prints nothing after the last restore.
    - `git -C firestarter_app status --porcelain firestarter/` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-anti-vacuity-red-green.txt && test -s "$EV" && test "$(/usr/bin/grep -cE '^rc_w[1-4]=1$' "$EV")" -eq 4 && /usr/bin/grep -qx 'rc_clean=0' "$EV" && test "$(/usr/bin/grep -cE '^[0-9]+ failed' "$EV")" -ge 4 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && test "$(/usr/bin/grep -cE '^== ' "$EV")" -ge 5 && ! /usr/bin/grep -q 'no tests ran' "$EV"</automated>
    <fails_when>The transcript is empty or missing; it does not carry exactly four `rc_w1=1` through `rc_w4=1` lines (a weakening that produced `rc_w<N>=0` means that leg is vacuous — it passed while the predicate was broken); it lacks the `rc_clean=0` restored line; fewer than four sections carry a `N failed` count; no line starts with a passed count; fewer than five `== ` section labels are present; or any run reported `no tests ran`, which would mean the RED came from a collection error rather than from the weakening.</fails_when>
    <automated>cd /workspaces && (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_derive_plan_structural_sentinel.py) > /tmp/175-01-antivac.txt 2>&1; tail -5 /tmp/175-01-antivac.txt; /usr/bin/grep -qE '^[0-9]+ passed' /tmp/175-01-antivac.txt && ! /usr/bin/grep -qE '[0-9]+ (failed|error|skipped)' /tmp/175-01-antivac.txt && test "$(/usr/bin/grep -cE '^def test_' firestarter_app/tests/test_derive_plan_structural_sentinel.py)" -ge 18 && /usr/bin/grep -q 'dataclasses.replace' firestarter_app/tests/test_derive_plan_structural_sentinel.py && ! /usr/bin/grep -qE '^[[:space:]]*(import copy|from copy import)' firestarter_app/tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -q '175-01-anti-vacuity-red-green.txt' firestarter_app/tests/test_derive_plan_structural_sentinel.py && test -z "$(git -C firestarter_app status --porcelain tests/test_derive_plan_structural_sentinel.py)" && test -z "$(git -C firestarter_app status --porcelain firestarter/)"</automated>
    <fails_when>The restored module does not print a passed count, or prints any failed, error or skipped count; fewer than eighteen `def test_` functions are present (Task 1 contributes seven and Task 2 at least twelve); the module does not use `dataclasses.replace`; it imports the shallow-and-deep-copy standard library module at all, which is the only route to the shared-steps-list trap; its Reachability block does not name the evidence transcript; the module file is left dirty in the working tree after the last weakening was restored; or a product module was modified.</fails_when>
  </verify>
  <done>Nine hand-built counter-plans and three mutated-corpus legs prove the predicate is sensitive on all 1,354 shipped plans and on every edge shape the shipped corpus cannot reach, four deliberate weakenings each turned the module RED and the RED was transcribed, and the module is restored and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate to tree | The sentinel's own verdict crosses here. A predicate that cannot fail, or a sweep that visits zero rows, asserts nothing while reading green. In a test-only phase this is the only threat class that matters. |
| generated database to sweep | `firestarter/data/chip_database.json` is machine-generated. Every absolute count in this plan is a measurement of that artifact, and a regeneration moves them. |
| developer home to sweep | `~/.firestarter/database.json` can override the shipped database and change a sweep result on one machine only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-01 | Repudiation | `write_verify_violations` in `tests/test_derive_plan_structural_sentinel.py` | high | mitigate | Task 2's four weakenings, each observed RED and transcribed into `evidence/175-01-anti-vacuity-red-green.txt`, plus nine hand-built counter-plans and three mutated-corpus legs each flagging an absolute, non-zero, exactly-equal count. |
| T-175-02 | Repudiation | the whole-database sweep's selector | high | mitigate | `all_rows` is the two-level descent copied verbatim from the shipped analog, and every sweep asserts its corpus size as the absolute `1354` before asserting a zero. This repo carries a live example of the failure mode: a parity test guarding a firmware `#define` that does not exist has always passed vacuously. |
| T-175-03 | Tampering | `EpromDatabase` construction in `tests/plan_corpus.py` | high | mitigate | One module-level `EpromDatabase(skip_local_override=True)`, asserted present by `/usr/bin/grep`, so no developer's local override can move a measured count. D-07 fixes the domain to the shipped database and makes no claim about override entries. |
| T-175-04 | Tampering | the `OP_*` vocabulary closure | high | mitigate | `assert_total_partition` fails closed on a fourteenth op, proven by a runtime `monkeypatch.setattr` leg that uses the `try/except/else raise` shape rather than a bare `pytest.raises`, so the failure message names the defect. |
| T-175-05 | Tampering | the shared `mock_operator` double | medium | mitigate | `Mock(spec=OPERATOR_METHODS)` — without `spec=`, an out-of-spec attribute access silently returns a truthy Mock and a dispatch bug reads as a pass. |
| T-175-06 | Information disclosure | `run_plan`'s transient `tempfile` use during the alignment smoke | low | accept | Audited clean in the CI replica: after a full 1,354-plan sweep there were zero new entries in `$HOME`, `~/.firestarter`, the CWD and the system temp directory. `TemporaryDirectory` and the paired `unlink` calls are unconditional. Accepting is correct because the data is a synthetic pattern buffer, not a secret. |
| T-175-SC | Tampering | package-manager installs | medium | accept | No package is installed. `175-RESEARCH.md`'s Package Legitimacy Audit records zero packages and zero `[ASSUMED]`, `[SUS]` or `[SLOP]` rows; HYG-02 forbids a new runtime dependency milestone-wide and the `.venv311` replica already exists at the branch base, so no install runs at all. |
</threat_model>

<verification>
Run from `/workspaces`, in this order:

1. `cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_derive_plan_structural_sentinel.py` — exits 0, prints a passed count, prints no failed, error or skipped count.
2. `cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py` — exits 0 and prints `114 passed`. This is the no-move check: no Phase 174 frozen hash may move in this phase, and if one does that is a defect in this phase, not a re-key to declare.
3. `cd firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/` and `... ruff format --check firestarter/ tests/` — both exit 0.
4. `cd firestarter_app && ./.venv311/bin/python tools/check_mypy_watermark.py` — prints `mypy errors: 35 (watermark: 35)`.
5. `test -z "$(git -C firestarter_app status --porcelain firestarter/)"` and `test -z "$(git -C firestarter status --porcelain)"` — no product module and no firmware file was touched.
6. Both evidence transcripts exist, are non-empty, and their `rc` tallies match the verify blocks above.

The whole app suite is not a criterion for this plan; plan 175-05 owns that gate.
</verification>

<success_criteria>
- The 13 `OP_*` constants are totally partitioned into 2 requires-verify and 11 exempt-with-a-reason, the partition fails closed on a fourteenth op, and the requires-verify bucket is a literal and not a subtraction.
- The write-to-verify predicate calls the production `cycle_block_bounds` helper, demands a supported and field-matching verify, treats a write outside the block as a violation, and reports zero violations across all 1,354 shipped plans.
- Every sweep asserts its corpus size absolutely before asserting a zero, so an empty sweep cannot read green.
- Anti-vacuity exists in both shapes D-09 requires and every leg has been SEEN red, with the RED transcribed.
- No product file changed, no Phase 174 frozen hash moved, no comment line was written, and the mypy watermark is unmoved.
</success_criteria>

## Planner Assumptions — unresolved edge probes

No SPEC.md exists for this phase, so the deterministic edge probe was run over PRUNE-05 and PRUNE-06 by
the orchestrator and returned four items. Three had a defensible, measured acceptance criterion and were
authored into `must_haves.truths` as plain strings; one came back **unclassified**, so the probe could
not say what the edge even is, and it is recorded here as an explicitly flagged planner assumption
rather than converted into a criterion. An assumption dressed as a criterion is the failure this phase
exists to prevent.

Authored as truths:

- **PRUNE-06, adjacency** — the cycle-block boundary. Authored into this plan's `must_haves.truths` as
  the out-of-block violation truth. Measured: `[write, verify, sdp-lock, write, verify]` returns bounds
  `(0, 2)` and the second write at index 3 is outside it, so the boundary case is "the write is not
  inside the returned half-open range", and the resolution is VIOLATION, never skip.
- **PRUNE-06, empty** — authored into this plan's `must_haves.truths` as the no-write and
  unsupported-write truths. Measured: an empty step list, a writeless shape and all 677
  `write_scope="none"` plans each return bounds `None` and zero violations; a plan whose only write is
  `supported=False` returns zero violations by explicit decision, and the shipped corpus carries exactly
  zero such steps.
- **PRUNE-06, ordering** — carried in plan 175-03, where the frozen artifact's family ordering and chip
  iteration order are the thing being made stable.

Flagged, not authored:

- **PRUNE-05, unclassified.** The probe could not classify the edge for "unsupported steps keep their
  `StepResult` with an NA verdict; only the work is skipped." Assumption carried by plans 175-03 and
  175-04: the requirement's two halves are exactly (a) a committed pin of the plan shapes the whole
  database produces, at the `(op, supported)` grain, and (b) a `run_plan` sweep asserting
  `len(results) == len(plan.steps)` and an `NA` verdict on every unsupported step. If PRUNE-05 has a
  third surface — for example a claim about what a *downstream consumer* of a `StepResult` does with an
  NA row — this phase does not cover it. Recorded rather than resolved.

Four probe items, three authored into `must_haves`, one flagged here. Nothing was dropped and nothing
was auto-dismissed.

## Artifacts this phase produces

New in `firestarter_app` (paths relative to the submodule root), from this plan:

- `tests/plan_corpus.py` — module constants `REAL_DB`, `PART_NUMBERS`, `SWEEP_SCOPES`, `OPERATOR_METHODS`; functions `all_rows(db)`, `plan_corpus()`, `mock_operator(**returns)`, `plan_with_steps(*steps, name="M8720", is_uv=False)`, `step(op, *, supported=True, reason="", **fields)`.
- `tests/test_derive_plan_structural_sentinel.py` — module constants `REQUIRES_VERIFY` (frozenset of 2), `EXEMPT_REASONS` (dict of 11); functions `module_op_constants()`, `assert_total_partition(discovered)`, `write_verify_violations(plan)`; the two violation reason strings `write sits outside the cycle block` and `no supported, field-matching verify in the block`.

New in the meta repo (paths relative to `/workspaces`), from this plan:

- `.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-tracer-end-to-end.txt`
- `.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-01-anti-vacuity-red-green.txt`

Later plans in this phase add: `tests/test_derive_plan_no_drop_sweep.py`, `tests/test_plan_shapes_drift.py`, `tests/fixtures/plan_shapes.json`, `tools/measure_plan_shapes.py`, three more evidence transcripts, `COVERAGE.md` and one pending-todo file. Each is listed in its own plan's section.

<output>
Create `.planning/phases/175-structural-sentinel-over-derive-plan/175-01-SUMMARY.md` when done. It must
transcribe the observed RED output for each of the four predicate weakenings verbatim, not merely state
that the weakenings were applied.
</output>
