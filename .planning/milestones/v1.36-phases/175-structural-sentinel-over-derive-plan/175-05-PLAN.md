---
phase: 175-structural-sentinel-over-derive-plan
plan: 05
type: execute
wave: 3
depends_on: [175-02, 175-03, 175-04]
files_modified:
  - .planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md
  - .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt
autonomous: true
requirements: [PRUNE-05, PRUNE-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The phase's central claim is sealed by measurement, not by assertion: firestarter_app/firestarter/ is byte-unchanged, the firmware repository is byte-unchanged, and firestarter/data/chip_database.json is byte-unchanged, all three proven by git status --porcelain over the whole phase's work."
    - "No re-key happened: tests/test_blast_radius_invariance.py and tests/test_rekey_ledger.py still report 114 passed against the same frozen literals, the meta-side ledger checker still exits 0, and no row was added to .planning/MILESTONES.md by this phase. Phase 174's D-11 binds Phases 175 through 181 and is satisfied vacuously here, which is the only correct outcome for a test-only phase."
    - "Every new Python file this phase created carries zero comment lines except the generator's single shebang, and no new or modified test module carries a pytest skip marker of any kind."
    - "The mypy watermark is unmoved at 35 errors against watermark 35, ruff check passes over firestarter/ and tests/, and ruff format --check passes over the same scope."
    - "The whole app suite is green with all of this phase's work present, and the count is at least the 2,108 measured at the branch base, so no shipped test was broken as collateral."
    - "The deferred stale-comment defect is FILED and not fixed: a pending todo names the exact range firestarter_app/firestarter/cli_handlers.py:2295-2303, quotes the offending sentence, records the three independent confirmations that it describes the reverted 260821-wna design, and states that this phase deliberately did not touch it because it is a product file."
    - "COVERAGE.md carries the reasoned no-API declaration in place of a coverage matrix, so the verify:pre seal-time gate has something deterministic to accept."
    - "PRUNE-05 and PRUNE-06 are both marked Complete in .planning/REQUIREMENTS.md, in the checkbox list and in the traceability table, and neither was marked complete by an earlier plan while its work was still partly unlanded."
    - "The phase's evidence directory holds at least five committed transcripts, one per plan, and every anti-vacuity leg this phase authored has its observed RED in one of them."
  artifacts:
    - path: .planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md
      provides: "The reasoned no-external-API declaration for the seal-time gate"
      contains: "No external API integration"
    - path: .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md
      provides: "The filed defect for the stale UV-prompt design-history comment at cli_handlers.py:2295-2303"
      contains: "2295"
    - path: .planning/REQUIREMENTS.md
      provides: "PRUNE-05 and PRUNE-06 marked Complete in both the checkbox list and the traceability table"
    - path: .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt
      provides: "The whole-phase gate transcript: production diff, no-move check, watermark, ruff, comment and skip census, all four new modules, and the full app suite"
  key_links:
    - from: .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt
      to: firestarter_app/tests/test_blast_radius_invariance.py
      via: "the no-move check runs the Phase 174 oracle verbatim and records its 114-passed count as this phase's proof that nothing re-keyed"
      pattern: "114 passed"
    - from: .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md
      to: firestarter_app/firestarter/cli_handlers.py
      via: "the todo names the exact line range and quotes the offending sentence; it does not modify the file"
      pattern: "cli_handlers.py"
  prohibitions:
    - statement: "The stale comment at cli_handlers.py:2295-2303 is FILED, never fixed. This phase touches no product file, and fixing it would invalidate the zero-production-diff claim the whole phase rests on."
      status: required
      verification: judgment
    - statement: "No row is added to .planning/MILESTONES.md by this phase. A re-key row appearing from Phase 175 would itself be the defect."
      status: required
      verification: judgment
    - statement: "The GSD requirements and roadmap CLI verbs are not used to update REQUIREMENTS.md. They reformat the whole file; a hand edit scoped to the two rows is the correct instrument."
      status: required
      verification: judgment
    - statement: "The meta repository's tools/wiki/selftest.sh is never run. It mutates Phase 168 evidence."
      status: required
      verification: judgment
    - statement: "The whole app suite being green is a criterion for this plan and must be measured, not asserted. It was green at the branch base at 2,108 passed, so there is no pre-existing RED to excuse a failure with."
      status: required
      verification: judgment
    - statement: "A bare grep is never used to produce gate evidence; the devcontainer grep honors .gitignore and silently under-scans."
      status: required
      verification: judgment
---

<objective>
Seal the phase against its own central claim.

Everything this phase built asserts something about `derive_plan`; this plan asserts something about the
phase. It measures, in one transcript, that the phase is test-only (zero diff in
`firestarter_app/firestarter/`, zero diff in the firmware repository, zero diff in the generated chip
database), that it re-keyed nothing (the Phase 174 oracle still reports 114 passed and
`.planning/MILESTONES.md` gained no row, satisfying 174's D-11 vacuously, which is the only correct
outcome here), that it left the tree clean (zero comments, zero skip markers, ruff and the zero-headroom
mypy watermark unmoved), and that it broke nothing (the whole app suite green at or above its
branch-base count of 2,108).

It also discharges the two paperwork obligations the phase carries: the deferred stale-comment defect at
`cli_handlers.py:2295-2303` is FILED as a pending todo — CONTEXT.md is explicit that it is a defect to
file and not to fix, because this phase touches no product file — and the reasoned no-external-API
declaration in `COVERAGE.md` is confirmed present for the seal-time gate.

Output: one filed todo, one confirmed declaration, two requirement rows marked Complete, and one
committed phase-seal transcript.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-CONTEXT.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-RESEARCH.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-01-SUMMARY.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-02-SUMMARY.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-03-SUMMARY.md
@.planning/phases/175-structural-sentinel-over-derive-plan/175-04-SUMMARY.md
@CLAUDE.md
</context>

<environment_preamble>
Every command runs from `/workspaces` unless stated, and every Python invocation goes through
`firestarter_app/.venv311/bin/python`, the py3.11.16 CI replica.

`grep` here is ugrep and honors `.gitignore`; use `/usr/bin/grep` for gate evidence. pytest `addopts`
is `-ra -q`, so pass `-o addopts=""` whenever a count line is needed.

**Timing.** The full app suite measured **302.20 s / 2108 passed** at the branch base. This phase adds
roughly 42 seconds of test time twice over — once in the suite itself and once inside the child suite
`tests/test_skip_census.py` spawns — so budget about **390 seconds**. Run the suite step with an
explicit command timeout of at least 540 seconds; the default two-minute Bash timeout will kill it
mid-run and the failure will look like a defect.

Do NOT run the meta repository's `tools/wiki/selftest.sh`; it mutates Phase 168 evidence.

Commits: everything this plan touches is in the meta repository. No `firestarter_app` file is modified
by this plan at all — that is the point of it. Do not create, rename or switch a branch.

Zero comments, and no HTML comment in any Markdown this plan writes.
</environment_preamble>

<tasks>

<task type="auto">
  <name>Task 1: The whole-phase gate sweep — production diff, no-move, hygiene, and the full app suite</name>
  <files>.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt</files>
  <read_first>
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-01-SUMMARY.md`, `175-02-SUMMARY.md`, `175-03-SUMMARY.md` and `175-04-SUMMARY.md` — every file the four prior plans created or modified, so the census below covers all of them and nothing else
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-RESEARCH.md` section I in full (the verbatim no-move command, its measured `114 passed in 1.57s`, the two additional byte-level checks worth making verify legs, and the correction that `test_flash_path_record_sync` lives in the firmware repository and not in `firestarter_app`)
    - `.planning/MILESTONES.md`, the `### v1.36 Re-Key Ledger` section only — to confirm its row count is unchanged from what Phase 174 left
    - `/workspaces/tools/rekey/check_rekey_ledger.py` (the meta-side cross-tree checker Phase 174 built; this plan runs it, does not modify it)
    - `.planning/phases/174-blast-radius-invariance-harness/174-05-PLAN.md`, its phase-closeout task, for the shape of a whole-phase gate sweep in this project
  </read_first>
  <behavior>
    - `git -C firestarter_app status --porcelain firestarter/` prints nothing.
    - `git -C firestarter status --porcelain` prints nothing.
    - `git -C firestarter_app status --porcelain firestarter/data/chip_database.json` prints nothing.
    - `pytest tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py -o addopts="" -q` prints `114 passed` and exits 0.
    - `python3 tools/rekey/check_rekey_ledger.py` exits 0 and prints one line beginning `OK:`.
    - The `### v1.36 Re-Key Ledger` table in `.planning/MILESTONES.md` has the same number of rows it had before this phase began.
    - Every new Python file this phase created reports zero comment lines, except `tools/measure_plan_shapes.py`, which reports exactly one.
    - No new or modified test module contains a pytest skip marker.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0, and `tools/check_mypy_watermark.py` prints `mypy errors: 35 (watermark: 35)`.
    - The whole app suite exits 0 with a passed count of at least 2,108 and no failed, error or skipped count.
    - The phase evidence directory holds at least five `.txt` transcripts.
  </behavior>
  <action>
Run the phase's own gates in one pass and transcribe them into
`.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt`, using the
`== LABEL` / `rc_<name>=N` shape the four prior transcripts use.

**Gate 1, the test-only claim.** Three `git status --porcelain` invocations: `firestarter/` inside the
app submodule, the whole firmware repository, and the generated chip database. All three must print
nothing. Emit each as a `key=value` line so an empty result is a positive record rather than an absence.
This is the claim the entire phase rests on; if it fails, the phase's own audit is void and the failure
must be reported rather than worked around.

**Gate 2, the no-move check.** Run RESEARCH section I's command verbatim:
`cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py -o addopts="" -q`.
It measured `114 passed in 1.57s` at the branch base and must still print a `114 passed` line. Add the
two byte-level checks RESEARCH recommends alongside it, which are stronger than "the assertions still
pass": `git -C firestarter_app diff --quiet -- tests/fixtures/rekey_ledger.py` and the same for
`tests/fixtures/reports/`. Then run the meta-side checker `python3 tools/rekey/check_rekey_ledger.py`,
which must exit 0 and print an `OK:` line, and count the `RK-174-` rows in `.planning/MILESTONES.md` so
the transcript records that the ledger gained nothing. A re-key row appearing from Phase 175 would
itself be the defect; D-11 is satisfied here vacuously, and vacuously is the only correct outcome for a
test-only phase.

**Gate 3, the comment and skip census.** Over the five Python files this phase created —
`firestarter_app/tests/plan_corpus.py`,
`firestarter_app/tests/test_derive_plan_structural_sentinel.py`,
`firestarter_app/tests/test_derive_plan_no_drop_sweep.py`,
`firestarter_app/tests/test_plan_shapes_drift.py` and `firestarter_app/tools/measure_plan_shapes.py` —
count comment lines with `/usr/bin/grep -cE '^\s*#'`. The first four must report zero; the generator must
report exactly one, its shebang. Then assert that none of the four test modules, nor
`firestarter_app/tests/test_skip_census.py`, contains a pytest skip marker. Emit the per-file counts as
`key=value` lines so the transcript shows what was measured rather than only that a check passed.

**Gate 4, the tooling gates CI runs.** `ruff check firestarter/ tests/`,
`ruff format --check firestarter/ tests/` and `tools/check_mypy_watermark.py`, each with its own `rc_`
line, and the watermark's output line captured verbatim so the transcript carries
`mypy errors: 35 (watermark: 35)` and not merely an exit code.

**Gate 5, the four new modules together.** One pytest invocation over
`tests/test_derive_plan_structural_sentinel.py`, `tests/test_derive_plan_no_drop_sweep.py`,
`tests/test_plan_shapes_drift.py` and `tests/test_skip_census.py`, with `-o addopts=""`, recording the
passed count and proving no skip.

**Gate 6, the whole app suite.** `pytest tests/ -o addopts="" -q` from inside `firestarter_app`. It was
green at 2,108 passed at the branch base, so a failure here is this phase's and there is no pre-existing
RED to excuse it. Record the passed count. Budget roughly 390 seconds and set a command timeout of at
least 540 seconds.

**Gate 7, the evidence inventory.** Count the `.txt` files in the phase evidence directory; there must
be at least five, one per plan. Emit the count and the file list.

Write every gate into the one transcript in order, and commit it in the meta repository.
  </action>
  <acceptance_criteria>
    - The transcript records `app_product_diff=clean`, `firmware_diff=clean` and `chip_database_diff=clean`.
    - The transcript contains a line matching `^114 passed` and an `rc_nomove=0` line.
    - The transcript records `rekey_ledger_rows=` with the same count Phase 174 left, and an `rc_ledger_checker=0` line.
    - The transcript records `comments_plan_corpus=0`, `comments_sentinel=0`, `comments_no_drop=0`, `comments_drift=0` and `comments_generator=1`.
    - The transcript records `skip_markers=0`.
    - The transcript contains the verbatim line `mypy errors: 35 (watermark: 35)` and `rc_ruff=0`, `rc_ruff_format=0` and `rc_watermark=0`.
    - The transcript contains an `rc_new_modules=0` line and a passed count for the four-module run.
    - The transcript contains an `rc_full_suite=0` line and a `full_suite_passed=` line whose value is at least 2108.
    - The transcript records `evidence_files=` with a value of at least 5.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/175-structural-sentinel-over-derive-plan/evidence && mkdir -p "$E" && EV=$E/175-05-phase-seal.txt && { echo "== 1 the test-only claim"; echo "app_product_diff=$([ -z "$(git -C firestarter_app status --porcelain firestarter/)" ] && echo clean || echo DIRTY)"; echo "firmware_diff=$([ -z "$(git -C firestarter status --porcelain)" ] && echo clean || echo DIRTY)"; echo "chip_database_diff=$([ -z "$(git -C firestarter_app status --porcelain firestarter/data/chip_database.json)" ] && echo clean || echo DIRTY)"; echo "== 2 the no-move check"; (cd firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py -o addopts="" -q); echo "rc_nomove=$?"; git -C firestarter_app diff --quiet -- tests/fixtures/rekey_ledger.py; echo "rc_ledger_bytes=$?"; git -C firestarter_app diff --quiet -- tests/fixtures/reports/; echo "rc_reports_bytes=$?"; python3 tools/rekey/check_rekey_ledger.py; echo "rc_ledger_checker=$?"; echo "rekey_ledger_rows=$(/usr/bin/grep -c 'RK-174-' .planning/MILESTONES.md)"; echo "== 3 comment and skip census"; echo "comments_plan_corpus=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/plan_corpus.py)"; echo "comments_sentinel=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_derive_plan_structural_sentinel.py)"; echo "comments_no_drop=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_derive_plan_no_drop_sweep.py)"; echo "comments_drift=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_plan_shapes_drift.py)"; echo "comments_generator=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tools/measure_plan_shapes.py)"; echo "skip_markers=$(/usr/bin/grep -clE 'pytest\.mark\.skip|skipif|pytest\.skip' firestarter_app/tests/plan_corpus.py firestarter_app/tests/test_derive_plan_structural_sentinel.py firestarter_app/tests/test_derive_plan_no_drop_sweep.py firestarter_app/tests/test_plan_shapes_drift.py | wc -l)"; echo "== 4 the CI tooling gates"; (cd firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/); echo "rc_ruff=$?"; (cd firestarter_app && ./.venv311/bin/python -m ruff format --check firestarter/ tests/); echo "rc_ruff_format=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_mypy_watermark.py); echo "rc_watermark=$?"; echo "== 5 the four new modules together"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_derive_plan_structural_sentinel.py tests/test_derive_plan_no_drop_sweep.py tests/test_plan_shapes_drift.py tests/test_skip_census.py); echo "rc_new_modules=$?"; echo "== 6 evidence inventory"; echo "evidence_files=$(ls "$E"/*.txt | wc -l)"; ls "$E"/*.txt; } > "$EV" 2>&1; /usr/bin/grep -qx 'app_product_diff=clean' "$EV" && /usr/bin/grep -qx 'firmware_diff=clean' "$EV" && /usr/bin/grep -qx 'chip_database_diff=clean' "$EV" && /usr/bin/grep -qE '^114 passed' "$EV" && /usr/bin/grep -qx 'rc_nomove=0' "$EV" && /usr/bin/grep -qx 'rc_ledger_bytes=0' "$EV" && /usr/bin/grep -qx 'rc_reports_bytes=0' "$EV" && /usr/bin/grep -qx 'rc_ledger_checker=0' "$EV" && /usr/bin/grep -qx 'comments_plan_corpus=0' "$EV" && /usr/bin/grep -qx 'comments_sentinel=0' "$EV" && /usr/bin/grep -qx 'comments_no_drop=0' "$EV" && /usr/bin/grep -qx 'comments_drift=0' "$EV" && /usr/bin/grep -qx 'comments_generator=1' "$EV" && /usr/bin/grep -qx 'skip_markers=0' "$EV" && /usr/bin/grep -qx 'rc_ruff=0' "$EV" && /usr/bin/grep -qx 'rc_ruff_format=0' "$EV" && /usr/bin/grep -qx 'rc_watermark=0' "$EV" && /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' "$EV" && /usr/bin/grep -qx 'rc_new_modules=0' "$EV" && test "$(/usr/bin/grep -oE '^evidence_files=[0-9]+$' "$EV" | cut -d= -f2)" -ge 5</automated>
    <fails_when>Any of `app_product_diff`, `firmware_diff` or `chip_database_diff` reads `DIRTY`, which voids the phase's test-only claim outright; the no-move run does not print a line starting `114 passed`, or `rc_nomove` is non-zero, meaning a Phase 174 frozen hash moved and this phase re-keyed something it declared it would not; either byte-level ledger or report check is non-zero; the meta-side ledger checker exits non-zero; any of the four test-module comment counts is above zero or the generator's is not exactly 1; `skip_markers` is above zero; `rc_ruff`, `rc_ruff_format` or `rc_watermark` is non-zero, or the watermark line is anything other than `mypy errors: 35 (watermark: 35)`; the four-module run exits non-zero; or fewer than five evidence transcripts exist. This command takes roughly 90 seconds; run it with a command timeout of at least 300 seconds.</fails_when>
    <automated>cd /workspaces && EV=.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt && { echo "== 7 the whole app suite"; (cd firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q > /tmp/175-05-suite.txt 2>&1); echo "rc_full_suite=$?"; tail -6 /tmp/175-05-suite.txt; echo "full_suite_passed=$(/usr/bin/grep -oE '^[0-9]+ passed' /tmp/175-05-suite.txt | head -1 | cut -d' ' -f1)"; } >> "$EV" 2>&1; /usr/bin/grep -qx 'rc_full_suite=0' "$EV" && test "$(/usr/bin/grep -oE '^full_suite_passed=[0-9]+$' "$EV" | cut -d= -f2)" -ge 2108 && ! /usr/bin/grep -qE '^[0-9]+ (failed|error)' /tmp/175-05-suite.txt && ! /usr/bin/grep -q 'no tests ran' /tmp/175-05-suite.txt</automated>
    <fails_when>The full app suite exits non-zero, or its recorded passed count is below the 2,108 measured at the branch base, or its summary line carries a failed or error count, or it reports `no tests ran`. The suite was green at the branch base, so any failure here is this phase's and must be fixed rather than excused; a passed count below 2,108 means tests were removed or deselected rather than added. This command takes roughly 390 seconds; run it with a command timeout of at least 540 seconds.</fails_when>
  </verify>
  <done>One transcript records, by measurement rather than assertion, that the phase changed no product file, moved no frozen hash, added no ledger row, wrote no comment and no skip marker, left ruff and the zero-headroom mypy watermark unmoved, and kept the whole app suite green at or above its branch-base count.</done>
</task>

<task type="auto">
  <name>Task 2: File the deferred defect, confirm the coverage declaration, and mark PRUNE-05 and PRUNE-06 Complete</name>
  <files>.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md, .planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md, .planning/REQUIREMENTS.md, .planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt</files>
  <read_first>
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-RESEARCH.md` section J in full — it carries the exact range `firestarter_app/firestarter/cli_handlers.py:2295-2303`, the offending sentence quoted verbatim, and the three independent confirmations that the block describes the reverted `260821-wna` design
    - `.planning/phases/175-structural-sentinel-over-derive-plan/175-CONTEXT.md`, the `<deferred>` block, which says to file this alongside the pending comment-stripping sweep rather than merged into it
    - `.planning/todos/pending/write-sdp-relock-deferred.md` first 10 lines, for the todo frontmatter convention in this project: `title`, `date`, `priority`, `blocked_by`, `resolves_phase`
    - `.planning/todos/pending/` listing, to confirm no todo already covers this exact range
    - `.planning/REQUIREMENTS.md` lines 47 to 60 (the No-Information Operations checkbox list, where PRUNE-05 is line 57 and PRUNE-06 is line 58) and lines 150 to 160 (the traceability table rows for both)
    - `.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md`, which the planner already authored — this task confirms it and commits it, it does not rewrite it
  </read_first>
  <behavior>
    - A new file exists at `.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md` with the project's five frontmatter keys.
    - The todo names the exact line range `2295-2303`, names `cli_handlers.py`, and quotes at least one clause of the offending sentence.
    - The todo states explicitly that Phase 175 deliberately did not fix it because the phase is test-only and the file is product code.
    - `firestarter_app/firestarter/cli_handlers.py` is unmodified.
    - `.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md` exists and contains the sentence `No external API integration: test-only sentinel over an in-repo Python module.`
    - `.planning/REQUIREMENTS.md` shows PRUNE-05 and PRUNE-06 with a checked box, and both traceability rows read Complete.
  </behavior>
  <action>
Discharge the phase's two paperwork obligations and close out the requirement rows.

**File the defect, do not fix it.** Write
`.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md` with the project's todo
frontmatter — `title`, `date` set to the execution date, `priority` medium, `blocked_by` set to
"nothing technical; deferred by Phase 175's test-only boundary", and `resolves_phase` set to `none`.

The body records, in this order:

1. The exact range: `firestarter_app/firestarter/cli_handlers.py:2295-2303`. RESEARCH corrected
   CONTEXT.md's cited `2295-2305`: the start is right, but lines 2304 to 2308 describe the non-UV
   full-device write and are still accurate, so the strictly-stale block ends at 2303.
2. The offending sentence, quoted: the comment says a UV-erasable EPROM is asked first, that quick task
   `260821-wna` makes "yes" permit the whole device to be written if the chip reads blank, and that off
   a TTY the ask is treated as a declined prompt so a single 256-byte slot is written anyway.
3. Why it is stale, with the three independent confirmations RESEARCH recorded: `_resolve_write_scope`'s
   own docstring 59 lines above says the opposite in the present tense — UV parts get partial,
   everything else full, and there is no prompt on any path; the function body is a two-branch `if` with
   `del interactive` and no prompt at all; and `TestUVWriteHasNoPrompt` pins the absence of `Confirm` and
   `_default_uv_write_confirm` structurally. Quick task `260822-aq6`, commit `2b42dac`, is the reversal
   that removed the prompt.
4. That Phase 175 deliberately did NOT fix it: the phase is test-only, `cli_handlers.py` is product code,
   and touching it would have invalidated the zero-production-diff claim the phase's own audit rests on.
5. That it also falls under the standing no-comments-in-source rule and under the pending
   `2026-08-27-strip-gsd-provenance-comments-from-source.md` sweep, and that CONTEXT.md says to file it
   ALONGSIDE that sweep rather than merged into it.
6. That Phase 175 pinned the behaviour the comment misdescribes, so the correct fix is to delete the
   block rather than to update it — `_resolve_write_scope` returning `"partial"` for all 270 UV rows is
   now asserted over the whole database.

Do not open `cli_handlers.py` for editing. Reading it is fine; writing to it is not.

**Confirm the coverage declaration.** `.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md`
was authored at plan time and carries the reasoned no-external-API declaration the `verify:pre`
seal-time gate accepts in place of a matrix. Confirm it is present, unmodified and committed. Do not
rewrite it; if it is missing, recreate it with the sentence
`No external API integration: test-only sentinel over an in-repo Python module.` and the reasoning that
this phase adds Python tests over an in-repo module with no network call, no SDK, no package install, no
serial port, no credentials and no writes outside the repository.

**Mark the requirements Complete.** Edit `.planning/REQUIREMENTS.md` BY HAND, scoped to four lines: the
PRUNE-05 and PRUNE-06 checkboxes in the No-Information Operations list, and their two rows in the
traceability table. Do not use the GSD requirements or roadmap CLI verbs; they reformat the whole file,
and this project has a recorded case of a positional overwrite destroying a dependency table. Both
requirements are complete only now, at the end of the phase: PRUNE-06 spans plans 175-01 and 175-02 and
PRUNE-05 spans 175-03 and 175-04, so neither could honestly have been marked complete by an earlier
plan.

Append a short confirmation section to the phase-seal transcript recording the todo path, the
COVERAGE.md presence check, and the two requirement rows as they now read.
  </action>
  <acceptance_criteria>
    - `.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md` exists and contains the strings `cli_handlers.py`, `2295`, `2303`, `260821-wna`, `260822-aq6` and `test-only`.
    - The todo's frontmatter contains `title:`, `date:`, `priority:`, `blocked_by:` and `resolves_phase:`.
    - `git -C firestarter_app status --porcelain firestarter/cli_handlers.py` prints nothing.
    - `.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md` exists and contains `No external API integration: test-only sentinel over an in-repo Python module.`
    - `.planning/REQUIREMENTS.md` contains a line matching `- \[x\] \*\*PRUNE-05\*\*` and one matching `- \[x\] \*\*PRUNE-06\*\*`.
    - `.planning/REQUIREMENTS.md`'s traceability rows for PRUNE-05 and PRUNE-06 both read `Complete`.
    - The phase-seal transcript records `todo_filed=yes`, `coverage_declared=yes` and `requirements_complete=2`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && T=.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md && C=.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md && test -s "$T" && /usr/bin/grep -q 'cli_handlers.py' "$T" && /usr/bin/grep -q '2295' "$T" && /usr/bin/grep -q '2303' "$T" && /usr/bin/grep -q '260821-wna' "$T" && /usr/bin/grep -q '260822-aq6' "$T" && /usr/bin/grep -q 'test-only' "$T" && /usr/bin/grep -q '^title:' "$T" && /usr/bin/grep -q '^date:' "$T" && /usr/bin/grep -q '^priority:' "$T" && /usr/bin/grep -q '^blocked_by:' "$T" && /usr/bin/grep -q '^resolves_phase:' "$T" && test -s "$C" && /usr/bin/grep -qF 'No external API integration: test-only sentinel over an in-repo Python module.' "$C" && test -z "$(git -C firestarter_app status --porcelain firestarter/cli_handlers.py)"</automated>
    <fails_when>The todo file is missing or empty; it omits the file name, either boundary of the exact line range, either quick-task identifier, or the statement that the phase was test-only; any of the five frontmatter keys is absent; COVERAGE.md is missing or does not carry the exact declaration sentence the seal-time gate looks for; or `cli_handlers.py` shows as modified, meaning the defect was fixed instead of filed and the phase's zero-production-diff claim is void.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -qE '^- \[x\] \*\*PRUNE-05\*\*' .planning/REQUIREMENTS.md && /usr/bin/grep -qE '^- \[x\] \*\*PRUNE-06\*\*' .planning/REQUIREMENTS.md && /usr/bin/grep -qE '^\| PRUNE-05 \|.*\| Complete \|' .planning/REQUIREMENTS.md && /usr/bin/grep -qE '^\| PRUNE-06 \|.*\| Complete \|' .planning/REQUIREMENTS.md && test "$(/usr/bin/grep -cE '^- \[ \] \*\*PRUNE-0[56]\*\*' .planning/REQUIREMENTS.md)" -eq 0 && EV=.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt && { echo "== 8 paperwork"; echo "todo_filed=yes"; echo "coverage_declared=yes"; echo "requirements_complete=$(/usr/bin/grep -cE '^- \[x\] \*\*PRUNE-0[56]\*\*' .planning/REQUIREMENTS.md)"; } >> "$EV" && /usr/bin/grep -qx 'requirements_complete=2' "$EV" && test -z "$(git -C firestarter_app status --porcelain firestarter/)" && test -z "$(git -C firestarter status --porcelain)"</automated>
    <fails_when>Either PRUNE checkbox is still unchecked, or either traceability row still reads anything other than Complete, or an unchecked PRUNE-05 or PRUNE-06 line survives anywhere in the file (which would mean the checkbox list was duplicated rather than edited); the transcript's `requirements_complete` count is not exactly 2; or either repository shows a modified product file at the point the phase is sealed.</fails_when>
  </verify>
  <done>The stale-comment defect is filed with its exact range, its quoted sentence and its three confirmations, without touching the product file; the reasoned no-API declaration is present for the seal-time gate; and both PRUNE requirements are marked Complete in both places, by hand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase to tree | The phase's own claim to be test-only crosses here. A claim asserted in prose and never measured is exactly the shape of gate this phase exists to forbid. |
| this phase to Phase 174's oracle | Phase 174's frozen hashes are the milestone's blast-radius oracle. This phase must leave them untouched, and "the assertions still pass" is weaker than "the bytes did not move". |
| deferred defect to product code | The stale comment is inside a product file. Filing it is in scope; fixing it is not, and fixing it would void the phase's audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-22 | Repudiation | the phase's own test-only claim | high | mitigate | Three `git status --porcelain` measurements recorded as positive `key=value` lines in the seal transcript rather than as silent absences, covering the app product tree, the whole firmware repository and the generated chip database. |
| T-175-23 | Tampering | Phase 174's frozen hashes | high | mitigate | The verbatim no-move command with its `114 passed` line, plus two byte-level `git diff --quiet` checks on the ledger module and the report corpus, plus the meta-side cross-tree checker, plus a row count over the `MILESTONES.md` re-key ledger to prove it gained nothing. |
| T-175-24 | Repudiation | the whole app suite as a criterion | high | mitigate | The suite is measured, its passed count recorded, and the count gated at or above the branch-base 2,108 so a silently-deselected test cannot read as green. The suite was green at the base, so there is no pre-existing RED available as an excuse. |
| T-175-25 | Tampering | the deferred defect | medium | mitigate | The todo is written in the meta repository only, and a negative gate asserts `cli_handlers.py` is unmodified, so filing cannot silently become fixing. |
| T-175-26 | Repudiation | requirement completion | medium | mitigate | Both PRUNE rows are marked Complete only in the final plan of the phase, by hand, and a gate rejects any surviving unchecked PRUNE-05 or PRUNE-06 line, which is this project's recorded failure mode of an executor marking a multi-plan requirement complete early. |
| T-175-SC | Tampering | package-manager installs | medium | accept | No package is installed by this phase at all; `175-RESEARCH.md`'s Package Legitimacy Audit records zero packages and zero `[ASSUMED]`, `[SUS]` or `[SLOP]` rows. |
</threat_model>

<verification>
Run from `/workspaces`, in this order:

1. The seal transcript exists, is non-empty, and carries every `key=value` and `rc_` line the acceptance criteria name.
2. `cd firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q` — exits 0 with a passed count of at least 2,108. Allow 540 seconds.
3. `python3 tools/rekey/check_rekey_ledger.py` — exits 0 with an `OK:` line.
4. `git -C firestarter_app status --porcelain firestarter/` and `git -C firestarter status --porcelain` both print nothing.
5. `.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md` and `.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md` both exist and are committed.
6. PRUNE-05 and PRUNE-06 read Complete in both places in `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- The phase's test-only claim, its no-re-key claim and its suite-green claim are each recorded as a measurement in one transcript, not asserted in prose.
- Zero comments across five new Python files except one shebang, zero skip markers, ruff clean, and the mypy watermark unmoved at 35 of 35.
- The deferred stale-comment defect is filed with its exact range and its evidence, and the product file it names is untouched.
- The reasoned no-external-API declaration is in place for the seal-time gate.
- PRUNE-05 and PRUNE-06 are marked Complete at the end of the phase and not before.
</success_criteria>

## Planner Assumptions — unresolved edge probes

The full four-item accounting lives in `175-01-PLAN.md`. This plan carries none of its own; it seals the
phase rather than adding a predicate. The one flagged item, PRUNE-05's unclassified probe row, is
recorded in `175-01-PLAN.md` and carried by plans 175-03 and 175-04, and it remains flagged at seal
time rather than being quietly resolved by the phase closing.

## Artifacts this phase produces

New in the meta repository from this plan:

- `.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md` — frontmatter keys `title`, `date`, `priority`, `blocked_by`, `resolves_phase`.
- `.planning/phases/175-structural-sentinel-over-derive-plan/evidence/175-05-phase-seal.txt`

Confirmed and committed by this plan:

- `.planning/phases/175-structural-sentinel-over-derive-plan/COVERAGE.md` — the reasoned no-external-API declaration, authored at plan time.

Modified by this plan:

- `.planning/REQUIREMENTS.md` — PRUNE-05 and PRUNE-06 checkboxes and traceability rows marked Complete.

Nothing in `firestarter_app` and nothing in the firmware repository is created or modified by this plan.

<output>
Create `.planning/phases/175-structural-sentinel-over-derive-plan/175-05-SUMMARY.md` when done. It must
quote the seal transcript's `app_product_diff`, `firmware_diff`, `chip_database_diff`, the `114 passed`
line, the watermark line and the `full_suite_passed` count verbatim, and it must state that this phase
declared no re-key and added no `MILESTONES.md` row.
</output>
