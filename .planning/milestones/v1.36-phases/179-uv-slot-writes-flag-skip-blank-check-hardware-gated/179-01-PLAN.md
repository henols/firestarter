---
phase: 179-uv-slot-writes-flag-skip-blank-check-hardware-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/tests/fake_chip.py
  - firestarter_app/tests/test_uv_mask.py
  - firestarter_app/tests/test_chip_test_cycle.py
  - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/
autonomous: true
requirements: [UV-01, UV-02, UV-03]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UV-01: a UV plan's write step passes `FLAG_SKIP_BLANK_CHECK` (`0x08`) as the FOURTH POSITIONAL argument to `operator.write_eprom`, so a firmware that refuses a non-blank write at `firestarter/src/proms/eprom.cpp:143-145` performs the masked slot write instead of refusing it."
    - "UV-01: the flag is passed POSITIONALLY, never as `operation_flags=`. MEASURED: two of the three write-capable doubles name that parameter `flags` (`tests/test_chip_test.py:1067`, `tests/fixtures/report_shapes.py:432`); a keyword call lands in `**_kw` on both and silently leaves the flag at 0."
    - "UV-02: a two-cycle `dev test` run on a UV part holding data outside the target slot reaches `submit.overall_verdict(results) == \"PASS\"` with the write step's and the verify step's `run_count == 2`. MEASURED this session end-to-end against a firmware-faithful double: `blank-check SKIPPED / write OK run_count=2 / verify OK run_count=2 / flags 0x8,0x8 / overall PASS`."
    - "UV-02: BOTH defects are closed, not one. The flag alone buys `run_count == 2` and leaves `overall_verdict` at `FAIL`, because `submit.overall_verdict` (`submit.py:164-171`) is FAIL-dominant on any `BAD` and the standalone `blank-check` step still returns `VERDICT_BAD` (`chip_test.py:2626-2641`)."
    - "UV-02 / Q1 Option B: the UV blank-check verdict is adjudicated at EXECUTION time in `_dispatch_step`, not by flipping `Step.supported` in `derive_plan`. MEASURED: `tests/fixtures/plan_shapes.json` reads only `(op, supported)` (`tools/measure_plan_shapes.py:63-114`), so execution-time adjudication leaves that 677-chip pin BYTE-UNCHANGED, while an Option-A `supported` flip would re-key all 301 UV chips and redden `tests/test_chip_test_blank_check_order.py::test_am27512_uv_blank_check_position_is_unchanged`."
    - "UV-02 / Q1a: the adjudicated verdict is `VERDICT_SKIPPED`, never `VERDICT_NA`. MEASURED: `submit._reason_text` (`submit.py:222-243`) suppresses the Reason cell to `-` for `VERDICT_NA` ONLY, so `NA` would destroy the very finding the step exists to surface; `SKIPPED` renders it verbatim. `VERDICT_OK` is structurally unavailable — it collides with the frozen `m27c512-full-all-ok` hash `6d3afbc52315`."
    - "UV-02 / Q1b: `_dispatch_step` learns the plan is UV from an additive `Step.uv_prewrite: bool = False`, set ONCE by `derive_plan` on the `OP_BLANK_CHECK` step of a UV plan. `_dispatch_step` receives `step`, never `plan` (`chip_test.py:2536-2546`), so a `Step` field is the only derive-once/read-many route; it follows `Step.region_policy`'s established discipline exactly."
    - "UV-02 / Q7: the `MSG_ERR_NOT_BLANK` diagnostic SURVIVES the verdict change — `reason` and `error_code` stay populated on the adjudicated step. MEASURED: a UV plan's `cycle_block_bounds` is `(3, 6)` for `m27c512`, `am27c020` and `tms27c512`, so the pre-write blank-check sits OUTSIDE the cycle block and its non-`None` `error_code` structurally cannot trip `hardware_refused` (`chip_test.py:1648-1652`); `run_plan`'s per-step path has no such mechanism at all."
    - "UV-03: the flag is derived from a STRUCTURAL monotonicity witness — `target.masked and target.current and target.current_is_probe_read` — evaluated by `_is_monotonic_masked_target`, and NEVER from `step.region_policy`. `_resolve_write_target` (`chip_test.py:2881-2883`) keeps its single policy-keyed branch; this plan adds no second one."
    - "UV-03: the witness is LIVE on every real cycle run. MEASURED: the staged tranche targets that actually reach `write_eprom` carry `current_source == \"probe read (tranche 1/2)\"` (`chip_test.py:1518`), so `.planning/research/SUMMARY.md:89`'s prescribed `current_source == \"probe read\"` equality NEVER matches and would ship green and inert. A structural boolean is used instead of any string compare."
    - "UV-03: `WriteTarget.current_is_probe_read` defaults to `False` (fail-closed) and is set `True` at EXACTLY ONE site — `_resolve_write_target`'s UV probe arm, beside `current_source=\"probe read\"` — and carried through unchanged by `_uv_cycle_targets`. `_alternating_cycle_targets`' complement does NOT set it, so the default does the work."
    - "UV-03 edge (probe row `UV-03 / empty`): an empty, single-element or absent target is fail-closed. `_is_monotonic_masked_target(None)` is `False`; a target with `current == b\"\"` is `False`; a target with `masked=False` is `False`. In every one of those cases `write_flags` is `0` and the firmware pre-flight runs exactly as it does today."
    - "Q3: the flag is COMPOSED inline at the write call site as a bare local `int`, following the SDP leg's precedent (`chip_test.py:3405-3414`, `:3437-3443`). Only the WITNESS is a named predicate, so UV-03's \"the witness wins\" is independently testable. `build_flags` (`eprom_operations.py:269-308`) stays the CLI path's wire-flag home and is NOT touched — recorded because `.planning/research/PITFALLS.md:449` recommends the opposite."
    - "`operator.check_eprom_blank(name, eprom_data)` keeps taking NO `operation_flags`: firmware's `CMD_BLANK_CHECK` structurally does not read the bit (`firestarter/src/proms/eprom.cpp:56-58`)."
    - "`run_count=1` on the blank-check step stays hard-coded and correct — `OP_BLANK_CHECK` is not in `_REPEAT_POLICY_OPS` (`chip_test.py:1139`). It is not \"fixed\"."
    - "The firmware-faithful double models the REAL host contract: `write_eprom` RETURNS `False` and records `last_firmware_error_code` / `last_firmware_error_message` on itself. MEASURED: `eprom_operations._run_state_machine` (`:597-609`) catches `EpromOperationError` and returns `(False, str(e))`, so `write_eprom` (`:2025-2027`) never raises on a firmware refusal and `_firmware_error` (`chip_test.py:3017-3019`) is what recovers the code. A double that RAISES — as `179-RESEARCH.md`'s Don't-hand-roll row prescribes — is NOT firmware-faithful and would exercise a code path real hardware never reaches."
    - "This is a HOST-ONLY change. `git -C /workspaces/firestarter status --porcelain` is empty after every commit; `FLAG_SKIP_BLANK_CHECK` is already `0x08` on both sides (`firestarter_app/firestarter/constants.py:122`, `firestarter/include/firestarter.h:139`), unchanged."
    - "`tests/fixtures/plan_shapes.json` is byte-unchanged by this plan, proven by `git diff --quiet` on the commit."
    - "EXPECTED-RED, by protocol: after the production commit exactly ONE of the eighteen `FROZEN_HASHES` entries — `m27c512-full-blank-check-bad` — no longer reproduces, its `LADDER_PINS` pair no longer matches, and `tools/snapshot_report_shapes.py --check` reports exactly one `DRIFT:` line. That is `.planning/MILESTONES.md`'s D-11 two-commit re-key protocol working, not a defect. Plan `179-02` is the separate re-baseline and declaration."
    - "Environment: every Python command runs through `firestarter_app/.venv311/bin/python` (py3.11, matching CI), every evidence-grade scan uses `/usr/bin/grep`, and every count-bearing pytest run passes `-o addopts=\"\"`."
  prohibitions:
    - "No `#` comment is written into any source or test file this phase touches. Not one. Rationale goes in a docstring, or in the bare-string-literal-above-the-constant carrier already shipped at `tests/test_blast_radius_invariance.py:88-186`. This is a standing operator rule and no plan can override it."
    - "The blank-check skip must never be granted to a target whose `current` was ASSUMED rather than read. The retired D-C branch (\"chip reported blank, mask taken as all-`0xFF`, device never read again\", removal recorded at `chip_test.py:2905-2917`) is precisely the case the witness exists to refuse, and a `masked and bool(current)` witness would readmit it."
    - "The flag must never be keyed on `step.region_policy`, `Plan.is_uv`, a `current_source` string compare, or a `current_source` prefix match. UV-03 forbids the first; the second is not threaded to the write site; the third is measured never to match; the fourth is brittle provenance-prose matching in exactly the family UV-03 warns about."
    - "`operation_flags` must never be passed as a keyword argument to `operator.write_eprom` from `chip_test.py`."
    - "No dict literal whose keys intersect the wire vocabulary (`flags` included) may be constructed in `chip_test.py` — `tools/check_devtest_orchestrator.py` bans it. A bare positional int is compliant."
    - "`derive_plan`'s `Step(op=OP_BLANK_CHECK, supported=True, reason=\"\")` for UV stays `supported=True` at its existing position, and the four-case ladder's shape is not otherwise altered."
    - "`firestarter_app/firestarter/data/chip_database.json` is never hand-edited, and is asserted porcelain-clean on every commit."
    - "No file in the `firestarter/` firmware submodule is modified."
    - "`firestarter/eprom_operations.py`, `firestarter/diagnostic_report.py`, `firestarter/submit.py` and `firestarter/cli_handlers.py` are not touched by this plan."
    - "No new exported report key is added. `_step_dict`'s five write-target keys (`diagnostic_report.py:838-843`) are unchanged, so `_STEPS_ELEMENT_0_KEYS` and `_TO_DICT_KEYS` in `tests/test_blast_radius_invariance.py` stay byte-unchanged."
    - "No sixth `verdict` value is introduced and no existing verdict literal is respelled."
    - "`firestarter_app/build/lib/firestarter/` is a stale build artifact — never edited, never cited."
    - "No snapshot under `tests/fixtures/reports/` is hand-edited or regenerated by this plan; the drift it causes is deliberately left for plan `179-02`."
  artifacts:
    - path: "firestarter_app/firestarter/chip_test.py"
      provides: "`WriteTarget.current_is_probe_read`, `Step.uv_prewrite`, `_is_monotonic_masked_target`, the inline positional `write_flags` pass at the write call site, and the execution-time UV blank-check verdict adjudication"
      contains: "current_is_probe_read"
      exports: ["WriteTarget", "Step", "_is_monotonic_masked_target"]
    - path: "firestarter_app/tests/fake_chip.py"
      provides: "`WriteInitPreflightChip` — a `FakeChip` subclass modelling the firmware write-init blank-check pre-flight (`eprom.cpp:143-145`) with the REAL host contract: return `False`, record `last_firmware_error_code`"
      contains: "class WriteInitPreflightChip"
      exports: ["WriteInitPreflightChip"]
    - path: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/179-01-tracer-end-to-end.txt"
      provides: "The measured end-to-end record of one UV slot write reaching PASS with run_count 2, plus both witness-disagreement directions and the single expected frozen-hash move"
      contains: "overall=PASS"
    - path: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/179-01-carry-through.txt"
      provides: "The measured proof that both additive fields default fail-closed and that the probe witness survives the tranche staging that is the only path onto the report"
      contains: "tranche_witness=True,True"
  key_links:
    - from: "firestarter_app/firestarter/chip_test.py::_resolve_write_target"
      to: "firestarter_app/firestarter/chip_test.py::_uv_cycle_targets"
      via: "current_is_probe_read=True is set at the UV probe arm and carried through onto every staged tranche, because the tranches are the ONLY targets that ever reach write_eprom"
      pattern: "current_is_probe_read=target\\.current_is_probe_read"
    - from: "firestarter_app/firestarter/chip_test.py::_is_monotonic_masked_target"
      to: "firestarter_app/firestarter/chip_test.py::_dispatch_multi_run"
      via: "write_flags = FLAG_SKIP_BLANK_CHECK if _is_monotonic_masked_target(resolved_target) else 0, passed as the FOURTH POSITIONAL argument to operator.write_eprom"
      pattern: "_is_monotonic_masked_target\\(resolved_target\\)"
    - from: "firestarter_app/firestarter/chip_test.py::derive_plan"
      to: "firestarter_app/firestarter/chip_test.py::_dispatch_step"
      via: "Step.uv_prewrite is decided once on the UV plan's OP_BLANK_CHECK step and read at dispatch to adjudicate BAD -> SKIPPED; _dispatch_step never receives the Plan"
      pattern: "step\\.uv_prewrite"
    - from: "firestarter_app/firestarter/chip_test.py::_dispatch_step"
      to: "firestarter_app/firestarter/submit.py::overall_verdict"
      via: "the adjudicated SKIPPED verdict clears the FAIL-dominant fold at submit.py:168-169 while the reason and error_code survive to the report"
      pattern: "VERDICT_SKIPPED"
---

<objective>
Prove the whole UV slot write end to end on ONE path: a single two-cycle `dev test` run on a
`m27c512` holding data outside the top slot, carried through every layer this phase changes — the
structural witness field, its carry-through onto the staged tranche, the witness predicate, the
inline positional flag at the write call site, the additive `Step` field, and the execution-time
blank-check adjudication — until `submit.overall_verdict(results)` reads `PASS` with the write and
verify steps at `run_count == 2`.

This is the tracer. It is production-quality, not a prototype: it carries the same `<verify>` bar
every later task carries and it is the skeleton `179-02`, `179-03` and `179-04` build out from. If
the witness form (UV-03) or the adjudicated verdict (Q1/Q1a) is wrong, it is wrong after ONE commit
— before a frozen shape has been published against it (plan `179-02`) and before a UV slot has been
spent on the bench (plan `179-04`).

Purpose: `chip_test.py:3186-3194` calls `operator.write_eprom(...)` with no `operation_flags`, so
`FLAG_SKIP_BLANK_CHECK` is never on the wire and the firmware refuses the write on a non-blank UV
part; and the plan's own standalone `blank-check` step returns `VERDICT_BAD`, so even with the flag
the run still titles `FAIL`. Both are closed here, in one commit.

Output: a UV part holding data outside the target slot accepts the slot write, both cycles run, the
"not blank" finding survives as a reason plus a firmware `error_code` on a non-`BAD` step, and
exactly one frozen hash moves — the one `RK-174-04` has been waiting for since Phase 174.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-RESEARCH.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-PATTERNS.md
@firestarter_app/CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by phase 179 and does not exist at `firestarter_app @ 835baba`. A
drift check that cannot resolve one of these against the pre-phase tree is reading a NEW symbol,
not a stale citation.

| Artifact | Kind | Home | Plan |
|---|---|---|---|
| `WriteTarget.current_is_probe_read` | dataclass field, `bool = False` | `firestarter/chip_test.py` | 179-01 |
| `Step.uv_prewrite` | dataclass field, `bool = False` | `firestarter/chip_test.py` | 179-01 |
| `_is_monotonic_masked_target` | module function `-> bool` | `firestarter/chip_test.py` | 179-01 |
| `WriteInitPreflightChip` | test double class | `tests/fake_chip.py` | 179-01 |
| `uv-slot-write-pass` | frozen `shape_id` (currently reserved) | `tests/fixtures/report_shapes.py` | 179-02 |
| `_build_uv_slot_write_pass` | builder function | `tests/fixtures/report_shapes.py` | 179-02 |
| `tests/fixtures/reports/uv-slot-write-pass.json` | new file (generated) | `tests/fixtures/reports/` | 179-02 |
| `RK-174-04-p179-uv-blank-check-abort` `after_hash` | declared ledger value | `tests/fixtures/rekey_ledger.py` | 179-02 |
| `tests/test_chip_test_uv_slot_write.py` | new test module | `firestarter_app/tests/` | 179-03 |
| `179-DECISIONS.md` | record (`D-179-1`, `D-179-2`) | `.planning/phases/179-…/` | 179-02 |
| `179-MEASUREMENT.md` | record (bench artifact) | `.planning/phases/179-…/` | 179-04 |

New test functions created by THIS plan: `test_write_target_current_is_probe_read_defaults_to_false`,
`test_step_uv_prewrite_defaults_to_false`,
`test_uv_cycle_tranches_carry_the_probe_read_witness`,
`test_alternating_cycle_complement_carries_no_probe_read_witness`,
`test_uv_plan_blank_check_sits_outside_the_cycle_block`.

New evidence files: `179-01-tracer-end-to-end.txt`, `179-01-carry-through.txt`, both under
`.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/`.

## Spec-less probe arithmetic (no silent drops)

This phase has no SPEC, so the deterministic edge probe supplied the edge predicates. **4 items
surfaced == 1 authored into `must_haves` + 3 surfaced as flagged assumptions.** Every one of the
four came back `unresolved`; none is auto-resolved with a backstop.

- **1 authored here** — `UV-03`, category `empty`: the fail-closed truth for an absent / empty /
  unmasked target is asserted in `must_haves.truths` above and proved by this plan's Leg 3.
- **3 flagged below** — `UV-01` (`unclassified`), `UV-02` (`unclassified`), `UV-03` (`encoding`).
  An `unclassified` row is never auto-resolved, and the `encoding` row is flagged with its reason
  rather than dropped.

## Flagged assumptions (unresolved probe rows and open questions — never silently dropped)

- **UV-01, probe category `unclassified`.** The probe could not assign an edge category to "a UV
  part holding data outside the target slot accepts a slot write". Assumption taken: "accepts"
  means the FIRMWARE performs the write — `write_eprom` returns `True` and the step reports
  `VERDICT_OK` — and nothing weaker (a host-side skip of the pre-flight, a warning, a retry). If the
  operator reads "accepts" as also requiring `firestarter write -a 0x…` to stop being refused on the
  same part, that is a firmware+host change in a different phase (RESEARCH assumption A7) and this
  plan under-delivers. Surfaced, not resolved.
- **UV-02, probe category `unclassified`.** The probe could not assign an edge category to
  "`overall_verdict == \"PASS\"` with `run_count == 2`". Assumption taken: `run_count == 2` is the
  WRITE step's and the VERIFY step's, not the blank-check step's — `run_count` is per-step
  (`chip_test.py:1346-1396`), and the blank-check's hard-coded `run_count=1` is correct because
  `OP_BLANK_CHECK` is not in `_REPEAT_POLICY_OPS`. If the operator reads UV-02 as requiring the
  blank-check step to run twice, that is a `_REPEAT_POLICY_OPS` change nobody has asked for.
  Surfaced, not resolved.
- **UV-03, probe category `encoding`** ("Whose definition of length/equality applies — bytes, code
  points, grapheme clusters, or normalized form?"). **This is very likely a classification miss for
  this domain, and it is recorded as a flagged assumption rather than dropped for exactly that
  reason.** There is no text in the witness: `WriteTarget.current` and `WriteTarget.pattern` are
  `bytes`, `mask_write_pattern` is a per-byte `&`, and `WriteTarget.__post_init__` compares
  `len(self.pattern)` against the region length in BYTES. Assumption taken: the byte domain is the
  only domain in play and no normalization question exists. The one place a string could have crept
  in — a `current_source` compare — is precisely what this plan structurally refuses. Surfaced, not
  resolved.
- **Q5 / RESEARCH assumption A5 — no report disclosure key.** After this phase `dev test m27c512`
  passes on a non-blank part while `firestarter write -a 0x…` on the identical part is still refused
  by the identical firmware, and this plan adds NO key saying so. Reasoning: `write_current_source`
  is already exported (`diagnostic_report.py:842`) and already reads `"probe read (tranche 1/2)"` on
  a filed report; `RPT-A4` (`plan.is_uv`) is explicitly Phase 181's; and an unrequested exported key
  grows the frozen-key surface this milestone is holding still. `.planning/research/PITFALLS.md:236-247`
  argues the other way and its own verdict is "Both, or neither" — this plan takes "neither".
  Surfaced, not resolved; recorded again in `179-04`'s close as a follow-up.
- **Ladder consequence, MEASURED and accepted.** After the verdict adjudication, a UV run whose
  slots are ALL exhausted reads `blank-check SKIPPED / write SKIPPED / verify SKIPPED` and
  `build_db_diff`'s fourth arm (`"OK" in verdicts and verdicts <= {"OK","NA","SKIPPED"}`,
  `diagnostic_report.py:403-405`) proposes `candidate for community-reported` where today the `BAD`
  blank-check landed it on `community-fail`. MEASURED this session on the
  `m27c512-full-blank-check-bad` shape, and identical for `NA` and `SKIPPED` — so the flip is FORCED
  by UV-02 (any verdict that clears the FAIL fold also clears the ladder's BAD arm), not chosen. The
  underlying hole — a SKIPPED write still proposing graduation — is PRE-EXISTING and reachable today
  on any non-UV refused write; `build_db_diff` is deliberately NOT touched here. `179-04` files it
  as a todo.

<tasks>

<task type="tracer">
  <name>Task 1: One UV slot write, through every layer, in one commit</name>
  <precondition>`/workspaces/firestarter_app/.venv311/bin/python --version` prints `Python 3.11.x`, and `cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import firestarter; print(firestarter.__file__)"` prints a path under `/workspaces/firestarter_app/firestarter/` — the editable install resolves to this worktree, not to a site-packages copy or to `build/lib/`.</precondition>
  <reversibility rating="costly">The adjudicated blank-check verdict moves the frozen `dedup_fingerprint` of `m27c512-full-blank-check-bad`, and plan `179-02` then DECLARES that move in `tests/fixtures/rekey_ledger.py` and `.planning/MILESTONES.md`. Reverting after `179-02` is not a code revert: it needs a second declared re-key row, a matching MILESTONES row, and a green `tools/rekey/check_rekey_ledger.py` in the meta repository. The two additive dataclass fields are themselves cheaply reversible (both default fail-closed and neither is exported).</reversibility>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/fake_chip.py</files>
  <read_first>
    - firestarter_app/firestarter/chip_test.py lines 447-480 (`Step` and its derive-once/read-only docstring contract), 560-600 (`derive_plan`'s head, `is_uv = is_uv_eprom(full)`, the region/policy decision), 684-730 (the four-case blank-check ladder and the `if not erase_is_executable:` append), 1286-1345 (`_CYCLE_BLOCK_OPS`, `_CYCLE_BLOCK_START_OPS`, `cycle_block_bounds`, `_cycle_target`), 1346-1400 (`_aggregate_cycle_results` and `run_count=len(ran)`), 1460-1540 (`_alternating_cycle_targets` and `_uv_cycle_targets`, the two carry-through sites), 1590-1660 (`_run_cycle_block`'s `hardware_refused` break), 1197-1235 (`WriteContext`), 2327-2413 (`WriteTarget`, its additive-field block and `__post_init__`), 2589-2650 (`_dispatch_step`'s head and its `OP_BLANK_CHECK` arm), 2870-2985 (`_resolve_write_target`: the policy branch at `:2881-2883` and the UV probe arm's `WriteTarget(...)` construction), 2995-3020 (`_firmware_error`), 3090-3200 (`_dispatch_multi_run`'s head, `resolved_target`, and the write call site at `:3186-3194`), 3400-3450 (the SDP leg's inline flag composition and its POSITIONAL `write_eprom` call — the pattern to copy)
    - firestarter_app/firestarter/constants.py line 122 (`FLAG_SKIP_BLANK_CHECK = 0x08`)
    - firestarter_app/firestarter/eprom_operations.py lines 269-310 (`build_flags` — read to confirm it is NOT the home used here), 560-612 (`_run_state_machine`'s `except EpromOperationError` returning `(False, str(e))` and stamping `last_firmware_error_*` — the contract the double must model), 1967-2030 (`write_eprom`'s signature and its `_run_state_machine` call), 2256-2292 (`check_eprom_blank` returning a bare bool)
    - firestarter_app/firestarter/submit.py lines 149-190 (`overall_verdict`'s status guard and FAIL-dominant fold, `build_title`), 222-243 (`_reason_text` and its `VERDICT_NA`-only suppression — the Q1a evidence)
    - firestarter_app/tests/fake_chip.py lines 1-60 (module docstring and `_parse_addr_or_size`), 60-130 (the constructor classmethods), 130-235 (`write_eprom`, `verify_eprom`, `read_eprom`, `check_eprom_blank`)
    - firestarter/src/proms/eprom.cpp lines 40-60 and 138-150 (READ-ONLY reference: the write-init pre-flight this phase disables, and `CMD_BLANK_CHECK`'s structural inability to read the bit)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-RESEARCH.md (all of it — the two falsified prior claims, the measured baselines, Q1-Q8)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-PATTERNS.md (patterns 1, 2, 3 and 5, and the Shared Patterns block)
  </read_first>
  <action>
Land the whole host change in ONE commit inside `firestarter_app` on `gsd/v1.36-dev-test-fidelity`.
Splitting it leaves the tree transiently false-green: passing the flag without adjudicating the
blank-check verdict produces `run_count == 2` and an `overall_verdict` still reading `FAIL`, which
is exactly the half-fix UV-02 exists to forbid.

**The comment carrier, decided.** This commit writes ZERO `#` comments. Every excerpt quoted in
`179-PATTERNS.md` is quoted WITH the surrounding comments because that is what the tree contains —
copy the code shape, never the comments. New field and function rationale goes in a real docstring:
`WriteTarget`'s class docstring, `Step`'s class docstring, and `_is_monotonic_masked_target`'s own
docstring. Do NOT touch any pre-existing comment block in `chip_test.py`; they are not this
phase's to sweep.

**1. `WriteTarget.current_is_probe_read` — the structural witness (UV-03).** Add
`current_is_probe_read: bool = False` as the LAST field of the frozen `WriteTarget` dataclass,
after `region_policy`. Default `False` is fail-closed: every direct `WriteTarget(...)` construction
already in the suite keeps working AND keeps the flag off. Extend the class docstring — which
already documents `current_source` as "provenance for the report" — with the sibling sentence:
`current_is_probe_read` is `True` only when `current` was read off the DEVICE for the EXACT region
being written, it is the monotonicity witness the `FLAG_SKIP_BLANK_CHECK` pass is derived from, and
it exists as a boolean rather than a `current_source` compare because the staged tranche targets
that actually reach `write_eprom` carry `"probe read (tranche 1/2)"`, so a string equality against
`"probe read"` never matches on a real run. Do NOT export it: `_step_dict`
(`diagnostic_report.py:838-843`) keeps exactly its five write-target keys.

**2. The set site — `_resolve_write_target`'s UV probe arm.** In the `WriteTarget(...)`
construction inside the slot loop (the one carrying `current_source="probe read"`,
`current=current`, `slots_remaining=...`), add `current_is_probe_read=True`. This is the ONLY
assignment of `True` anywhere in the tree. The unmasked arm above it
(`current_source="address-derived pattern (unmasked)"`) is left alone — its default `False` is the
correct value.

**3. The carry sites.** In `_uv_cycle_targets`' staged `WriteTarget(...)`, add
`current_is_probe_read=target.current_is_probe_read`, beside the existing
`slots_remaining`/`slots_total`/`region_policy` carry-throughs. This is load-bearing: the staged
tranches are the ONLY targets that ever reach `write_eprom` or the report, so a field not carried
here is invisible on exactly the family it exists for. In `_alternating_cycle_targets`' complement,
add NOTHING — the default `False` is correct, because that recipe builds an unmasked
address-derived complement for SRAM/FRAM and never a probe-read image.

**4. `_is_monotonic_masked_target` — the witness predicate (UV-03).** Add a module-level
`def _is_monotonic_masked_target(target: WriteTarget | None) -> bool:` immediately above
`_dispatch_multi_run`, returning `True` only when ALL THREE of `target is not None`,
`target.masked`, `target.current` (non-empty) and `target.current_is_probe_read` hold — the exact
three conjuncts ROADMAP criterion 3 names ("target masked, current value known, sourced from a
probe read"). Its docstring states: the safety argument is monotonicity — `mask_write_pattern`
computes `current & desired` per byte, which can only clear `1 -> 0`, and that property holds ONLY
because `current` is a real probe read of the exact target region; `region_policy == "uv-slot"` and
"the mask came from a probe read" are currently coextensive but are NOT the same predicate, and
were provably not coextensive one design iteration ago (the retired D-C branch whose removal is
recorded at `chip_test.py:2905-2917`); and the empty/absent cases (`None`, `current == b""`,
`masked=False`) all return `False`, so the firmware pre-flight runs exactly as it does today.

**5. The write call site — the inline positional flag (UV-01, Q3).** At `chip_test.py:3186-3194`,
immediately before the `for _ in range(runs):` loop or immediately inside the `if op in (OP_WRITE,
OP_WRITE_PARTIAL):` arm, assign a bare local int:
`write_flags = FLAG_SKIP_BLANK_CHECK if _is_monotonic_masked_target(resolved_target) else 0`.
Then pass `write_flags` as the FOURTH POSITIONAL argument to `operator.write_eprom(...)`, exactly
as the SDP leg does at `chip_test.py:3437-3443` — `name, eprom_data, tmp_source_path, write_flags,
address_str=_address_arg(region_start)`. Import `FLAG_SKIP_BLANK_CHECK` from
`firestarter.constants` alongside the existing `FLAG_SKIP_SDP_UNLOCK` import. NEVER a keyword call:
`tests/test_chip_test.py:1067` and `tests/fixtures/report_shapes.py:432` both name that parameter
`flags`, so `operation_flags=` lands in `**_kw` on two of the three write-capable doubles and
silently leaves the flag at 0 — a no-op that passes every assertion about the RESULT. NEVER a dict:
`tools/check_devtest_orchestrator.py` bans a dict literal in this file whose keys intersect the wire
vocabulary, `flags` included. `resolved_target` is already in scope here (assigned at `:3116-3135`),
so no new plumbing is needed. The verify arm is NOT touched — firmware's verify runs no write-init
pre-flight.

**6. `Step.uv_prewrite` — the additive plan signal (Q1b).** Add `uv_prewrite: bool = False` as the
LAST field of the `Step` dataclass, after `full_device_permitted`. Extend `Step`'s class docstring:
`uv_prewrite` is set ONCE by `derive_plan`, ONLY on the `OP_BLANK_CHECK` step of a UV plan, and it
means "a `not blank` result here is an expected, operator-actionable FINDING about a used UV part,
not a chip fault" — the write is irrecoverable and only UV light erases, so the step keeps its
pre-write position and its diagnostic while ceasing to dominate the verdict fold. It follows the
same derive-once/read-many discipline `region_policy` and `write_region` already carry;
`_dispatch_step` receives `step`, never `plan`, which is why the signal rides here rather than on
`Plan.is_uv`.

**7. `derive_plan` — the one set site for `uv_prewrite`.** In the four-case ladder's `else:` arm
(`chip_test.py:720`), construct
`Step(op=OP_BLANK_CHECK, supported=True, reason="", uv_prewrite=is_uv)`. `supported` stays `True`
and the append position is unchanged, so `tests/fixtures/plan_shapes.json` (which reads only
`(op, supported)`) and `tests/test_chip_test_blank_check_order.py` both stay green and byte-
unchanged. Do NOT set `uv_prewrite` on the two NA arms above it, and do NOT set it on any other
step.

**8. `_dispatch_step` — the execution-time adjudication (UV-02, Q1/Q1a/Q7).** In the
`if step.op == OP_BLANK_CHECK:` arm, keep `is_ok = operator.check_eprom_blank(name, eprom_data)`
exactly as it is — no `operation_flags`, ever, because firmware's `CMD_BLANK_CHECK` does not read
the bit. Keep `code, message = (None, "") if is_ok else _firmware_error(operator)` byte-unchanged
and keep BOTH `reason=message` and `error_code=code` on the returned `StepResult`: the firmware's
address+value for a failing blank-check is the single most useful datum in a `dev test` failure and
it survives this change (Q7 answered YES). Change ONLY the verdict expression, to:
`VERDICT_OK` when `is_ok`; `VERDICT_SKIPPED` when not `is_ok` and `step.uv_prewrite`; `VERDICT_BAD`
otherwise. Keep `run_count=1` hard-coded. Extend the surrounding rationale in `_dispatch_step`'s
docstring (never a `#` comment) with the three measured facts: `VERDICT_OK` is structurally
unavailable for this case because it collides with the frozen `m27c512-full-all-ok` hash
`6d3afbc52315`; `VERDICT_NA` is rejected because `submit._reason_text` suppresses the Reason cell to
`-` for `NA` only, which would destroy the finding; and the non-`None` `error_code` on a non-`BAD`
step is safe because a UV plan's `cycle_block_bounds` is `(3, 6)`, putting the pre-write blank-check
OUTSIDE the cycle block where `hardware_refused` lives.

**9. `tests/fake_chip.py` — `WriteInitPreflightChip`, the firmware-faithful double.** Add a
`FakeChip` subclass modelling `firestarter/src/proms/eprom.cpp:143-145`. It must reproduce the REAL
host contract, which `179-RESEARCH.md`'s "Don't hand-roll" row gets wrong: `EpromOperator.write_eprom`
does NOT raise on a firmware refusal — `_run_state_machine` catches the `EpromOperationError` and
returns `(False, str(e))` (`eprom_operations.py:597-609`), stamping `last_firmware_error_code` and
`last_firmware_error_message` on the operator, and `chip_test._firmware_error` is what reads them
back. So the subclass:
  - declares `last_firmware_error_code: int | None` and `last_firmware_error_message: str | None`,
    both `None` initially, and CLEARS both at the top of every operator-shaped method it defines —
    mirroring `_run_state_machine`'s clear-on-entry, without which one step's failure leaks into the
    next step's `error_code`;
  - records every `operation_flags` value it is handed in a `write_flags_seen: list[int]`, which is
    what the UV-03 legs assert on;
  - in `write_eprom`, when `not (operation_flags & FLAG_SKIP_BLANK_CHECK)` and the device region is
    not blank, stamps `last_firmware_error_code = MSG_ERR_NOT_BLANK` (`0xB0`, `firestarter/include/messages.h:93`)
    with a message of the firmware's shape and RETURNS `False` — no exception;
  - otherwise delegates to `super().write_eprom(...)`, keeping `FakeChip`'s UV AND-physics
    (`existing & incoming`) and absolute-offset addressing intact — subclass, never re-implement;
  - overrides `check_eprom_blank` to stamp the same pair when the device is not blank, because the
    standalone blank-check on real hardware also returns `False` with the code recorded;
  - sets `self.check_eprom_id = Mock(return_value=(True, id_value))` in `__init__`, so
    `tests/fixtures/report_shapes.py::_build_real_path_report`'s existing
    `operator.check_eprom_id.return_value = ...` stamping line works against this double UNCHANGED
    in plan `179-02`. Without that shim the shared helper raises on a plain bound method.
The subclass lives in `tests/fake_chip.py` because BOTH `tests/test_chip_test_uv_slot_write.py`
(plan `179-03`) and `tests/fixtures/report_shapes.py` (plan `179-02`) need it, and `fake_chip.py` is
already the tree's one home for a state-modelling chip double; it is not a test module and pytest
does not collect it. Its docstring names `eprom.cpp:143-145` so a reader can find the firmware
source it mirrors.

**EXPECTED RED after this commit, deliberately.** `tests/test_blast_radius_invariance.py`'s
parametrized `FROZEN_HASHES` leg and its `LADDER_PINS` leg both go red for
`m27c512-full-blank-check-bad`, `tools/snapshot_report_shapes.py --check` reports one `DRIFT:` line,
and `tests/test_rekey_ledger.py::test_every_ledger_row_shape_id_resolves_and_recomputes` goes red
for `RK-174-04`. That is `.planning/MILESTONES.md`'s D-11 two-commit protocol working as designed —
a behaviour change lands on its own and turns the gate red; a SEPARATE commit declares the re-key.
Do NOT repair any of them here. Do NOT regenerate any snapshot here. Plan `179-02` owns the repair.
Say so in the commit message body, exactly as `3f01714` (Phase 177) did.
  </action>
  <acceptance_criteria>
    - `./.venv311/bin/python -c "from firestarter.chip_test import WriteTarget, Step, _is_monotonic_masked_target; print(WriteTarget.__dataclass_fields__['current_is_probe_read'].default, Step.__dataclass_fields__['uv_prewrite'].default)"` prints `False False`.
    - `_is_monotonic_masked_target(None)` is `False`; it is `False` for a target with `masked=False`; it is `False` for a target with `masked=True, current_is_probe_read=True, current=b""`; it is `True` only for a target with `masked=True`, non-empty `current` and `current_is_probe_read=True`.
    - `derive_plan("m27c512", db, write_scope="full")` returns a plan whose `OP_BLANK_CHECK` step has `supported is True`, sits at index 2, and has `uv_prewrite is True`; the same plan for the non-UV `at28c256` has `uv_prewrite is False` on its blank-check step.
    - `cycle_block_bounds(plan.steps)` for that `m27c512` plan is `(3, 6)` — unchanged.
    - A two-cycle `run_plan` on `m27c512` against a `WriteInitPreflightChip` seeded with content at `0x0000-0x00FF` reports: `blank-check` `verdict == "SKIPPED"` with a non-empty `reason` and `error_code == 176`; `write` `verdict == "OK"` with `run_count == 2`; `verify` `verdict == "OK"` with `run_count == 2`.
    - `chip.write_flags_seen == [8, 8]` for that run — the flag was on the wire on BOTH cycles.
    - `submit.overall_verdict(results) == "PASS"` and `submit.build_title(report, "m27c512")` ends in `— PASS (<12 hex chars>)`.
    - The write step's `write_target.current_source` starts with `probe read (tranche` AND its `current_is_probe_read` is `True` — the witness survived the tranche staging.
    - UV-03 disagreement, direction A: `_dispatch_multi_run(OP_WRITE, …, step=<region_policy="uv-slot">, write_context=WriteContext(cycle_targets=[<masked=False target>]))` records `write_flags_seen == [0]` and makes ZERO `read_eprom` calls — the policy says `uv-slot` and the witness is absent, so the flag is NOT set.
    - UV-03 disagreement, direction B: the same seam with `step.region_policy == "fixed"` and a `masked=True, current=<256 bytes>, current_is_probe_read=True` target records `write_flags_seen == [8]` — the policy says `fixed` and the witness is present, so the flag IS set.
    - Exactly ONE of the eighteen `FROZEN_HASHES` entries fails to reproduce, and it is `m27c512-full-blank-check-bad`; its freshly computed value is a 12-hex string different from `077a32d1a5c4`.
    - `git diff HEAD~1 --quiet -- tests/fixtures/plan_shapes.json` succeeds — the 677-chip plan-shape pin is byte-unchanged.
    - `git diff HEAD~1 --name-only -- tests/fixtures/reports/` is empty — no snapshot was regenerated in this commit.
    - `tests/test_chip_test_blank_check_order.py`, `tests/test_plan_shapes_drift.py`, `tests/test_uv_mask.py`, `tests/test_chip_test.py`, `tests/test_chip_test_cycle.py` and `tests/test_chip_test_sdp_leg.py` all pass.
    - `git diff` for this commit adds zero lines matching `^\+[[:space:]]*#` across `firestarter/` and `tests/`.
    - `ruff check`, `ruff format --check` and `tools/check_mypy_watermark.py` are green, the watermark still reading exactly 35; `tools/check_devtest_orchestrator.py` and `tools/check_diagnostic_report_claims.py` both exit 0.
    - `git -C /workspaces/firestarter status --porcelain` is empty and `git status --porcelain firestarter/data/chip_database.json` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence && mkdir -p "$E" && EV=$E/179-01-tracer-end-to-end.txt && { echo "== 1 the UV slot write across every layer"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from unittest.mock import Mock
from firestarter import chip_test as ct
from firestarter import submit as sub
from firestarter.constants import FLAG_SKIP_BLANK_CHECK
from firestarter.database import EpromDatabase
from firestarter.diagnostic_report import AutoCapture, DiagnosticReport, TransportHealth, dedup_fingerprint
from tests.fake_chip import WriteInitPreflightChip
db = EpromDatabase(skip_local_override=True)
full = db.get_eprom('m27c512')
mem = int(full['memory-size'])
plan = ct.derive_plan('m27c512', db, write_scope='full')
bc = next(s for s in plan.steps if s.op == ct.OP_BLANK_CHECK)
print('plan_is_uv=' + str(plan.is_uv))
print('bc_index=' + str([s.op for s in plan.steps].index(ct.OP_BLANK_CHECK)))
print('bc_supported=' + str(bc.supported))
print('bc_uv_prewrite=' + str(bc.uv_prewrite))
nonuv = ct.derive_plan('at28c256', db, write_scope='full')
print('nonuv_bc_uv_prewrite=' + str(next(s for s in nonuv.steps if s.op == ct.OP_BLANK_CHECK).uv_prewrite))
print('cycle_block=' + str(ct.cycle_block_bounds(plan.steps)))
chip = WriteInitPreflightChip(memory_size=mem, uv=True)
chip.data[0x0000:0x0100] = bytes(range(256))
results = ct.run_plan(plan, chip, db, runs=2)
r = {x.op: x for x in results}
print('bc_verdict=' + r['blank-check'].verdict)
print('bc_reason_nonempty=' + str(bool(r['blank-check'].reason)))
print('bc_error_code=' + str(r['blank-check'].error_code))
print('write_verdict=' + r['write'].verdict)
print('write_run_count=' + str(r['write'].run_count))
print('verify_verdict=' + r['verify'].verdict)
print('verify_run_count=' + str(r['verify'].run_count))
print('flags_seen=' + ','.join(hex(f) for f in chip.write_flags_seen))
wt = r['write'].write_target
print('tranche_source_prefix=' + str(wt.current_source.startswith('probe read (tranche')))
print('tranche_witness=' + str(wt.current_is_probe_read))
print('overall=' + sub.overall_verdict(results))
prog = db.convert_to_programmer(full)
rep = DiagnosticReport(auto_capture=AutoCapture(host_version='0.0.0', chip='m27c512', protocol=str(prog.get('algorithm'))), transport=TransportHealth(), plan=plan, results=results)
title = sub.build_title(rep, 'm27c512')
print('title_pass=' + str(sub.overall_verdict(results) == 'PASS' and '— PASS (' in title))
print('reason_cell_survives=' + str(sub._reason_text(r['blank-check'].verdict, r['blank-check'].reason) != '-'))
"); echo "rc=$?"; echo "== 2 the witness, both disagreement directions and every empty case"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter import chip_test as ct
from firestarter.database import EpromDatabase
from tests.fake_chip import WriteInitPreflightChip
db = EpromDatabase(skip_local_override=True)
ed = ct.resolve_chip('m27c512', db=db)
print('witness_none=' + str(ct._is_monotonic_masked_target(None)))
cur = b'\xff' * 256
des = ct.generate_pattern(0x1000, 256)
t_ok = ct.WriteTarget(region=(0x1000, 256), pattern=ct.mask_write_pattern(cur, des), masked=True, bits_cleared=ct.bits_cleared_by(cur, des), bits_retained=ct.bits_retained_by(cur, des), current_source='probe read', current=cur, current_is_probe_read=True, region_policy=ct.REGION_POLICY_FIXED)
t_nocur = ct.WriteTarget(region=(0x1000, 256), pattern=ct.mask_write_pattern(cur, des), masked=True, bits_cleared=ct.bits_cleared_by(cur, des), bits_retained=ct.bits_retained_by(cur, des), current_source='probe read', current=b'', current_is_probe_read=True, region_policy=ct.REGION_POLICY_FIXED)
t_unmasked = ct.WriteTarget(region=(0xFF00, 256), pattern=ct.generate_pattern(0xFF00, 256), masked=False, bits_cleared=0, bits_retained=0, current_source='address-derived pattern (unmasked)', region_policy=ct.REGION_POLICY_UV_SLOT)
print('witness_ok=' + str(ct._is_monotonic_masked_target(t_ok)))
print('witness_empty_current=' + str(ct._is_monotonic_masked_target(t_nocur)))
print('witness_unmasked=' + str(ct._is_monotonic_masked_target(t_unmasked)))
step_a = ct.Step(op=ct.OP_WRITE, supported=True, reason='', destructive=True, write_region=(0xFF00, 256), region_policy=ct.REGION_POLICY_UV_SLOT, full_device_permitted=False, cycle_payload=ct.CYCLE_PAYLOAD_UV_TRANCHE)
wc = ct.WriteContext(); wc.cycle_targets = [t_unmasked]; wc.cycle_index = 0
ca = WriteInitPreflightChip(memory_size=65536, uv=True)
ct._dispatch_multi_run(ct.OP_WRITE, 'm27c512', ed, ca, runs=1, step=step_a, write_context=wc)
print('disagree_A_policy_uvslot_witness_absent=' + ','.join(hex(f) for f in ca.write_flags_seen))
print('disagree_A_reads=' + str(sum(1 for c in ca.calls if c[0] == 'read_eprom')))
step_b = ct.Step(op=ct.OP_WRITE, supported=True, reason='', destructive=True, write_region=(0x1000, 256), region_policy=ct.REGION_POLICY_FIXED, full_device_permitted=False)
wc2 = ct.WriteContext(); wc2.cycle_targets = [t_ok]; wc2.cycle_index = 0
cb = WriteInitPreflightChip(memory_size=65536, uv=True)
ct._dispatch_multi_run(ct.OP_WRITE, 'm27c512', ed, cb, runs=1, step=step_b, write_context=wc2)
print('disagree_B_policy_fixed_witness_present=' + ','.join(hex(f) for f in cb.write_flags_seen))
print('disagree_B_reads=' + str(sum(1 for c in cb.calls if c[0] == 'read_eprom')))
"); echo "rc=$?"; echo "== 3 exactly one frozen hash moved, and it is the pre-seeded one"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter.diagnostic_report import dedup_fingerprint
from tests.fixtures.report_shapes import FROZEN_HASHES, SHAPE_IDS, build_shape
moved = {s: dedup_fingerprint(build_shape(s)) for s in SHAPE_IDS if dedup_fingerprint(build_shape(s)) != FROZEN_HASHES[s]}
print('shape_count=' + str(len(SHAPE_IDS)))
print('moved_shapes=' + ','.join(sorted(moved)))
print('moved_count=' + str(len(moved)))
for s, h in sorted(moved.items()):
    print('new_hash_' + s + '=' + h)
"); echo "rc=$?"; echo "== 4 the modules that must stay green"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_chip_test_blank_check_order.py tests/test_plan_shapes_drift.py tests/test_uv_mask.py tests/test_chip_test.py tests/test_chip_test_cycle.py tests/test_chip_test_sdp_leg.py tests/test_devtest_firmware_error_propagation.py); echo "rc=$?"; echo "== 5 the two AST gates"; (cd firestarter_app && ./.venv311/bin/python tools/check_devtest_orchestrator.py); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_diagnostic_report_claims.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 6 && /usr/bin/grep -qx 'plan_is_uv=True' "$EV" && /usr/bin/grep -qx 'bc_index=2' "$EV" && /usr/bin/grep -qx 'bc_supported=True' "$EV" && /usr/bin/grep -qx 'bc_uv_prewrite=True' "$EV" && /usr/bin/grep -qx 'nonuv_bc_uv_prewrite=False' "$EV" && /usr/bin/grep -qx 'cycle_block=(3, 6)' "$EV" && /usr/bin/grep -qx 'bc_verdict=SKIPPED' "$EV" && /usr/bin/grep -qx 'bc_reason_nonempty=True' "$EV" && /usr/bin/grep -qx 'bc_error_code=176' "$EV" && /usr/bin/grep -qx 'write_verdict=OK' "$EV" && /usr/bin/grep -qx 'write_run_count=2' "$EV" && /usr/bin/grep -qx 'verify_verdict=OK' "$EV" && /usr/bin/grep -qx 'verify_run_count=2' "$EV" && /usr/bin/grep -qx 'flags_seen=0x8,0x8' "$EV" && /usr/bin/grep -qx 'tranche_source_prefix=True' "$EV" && /usr/bin/grep -qx 'tranche_witness=True' "$EV" && /usr/bin/grep -qx 'overall=PASS' "$EV" && /usr/bin/grep -qx 'title_pass=True' "$EV" && /usr/bin/grep -qx 'reason_cell_survives=True' "$EV" && /usr/bin/grep -qx 'witness_none=False' "$EV" && /usr/bin/grep -qx 'witness_ok=True' "$EV" && /usr/bin/grep -qx 'witness_empty_current=False' "$EV" && /usr/bin/grep -qx 'witness_unmasked=False' "$EV" && /usr/bin/grep -qx 'disagree_A_policy_uvslot_witness_absent=0x0' "$EV" && /usr/bin/grep -qx 'disagree_A_reads=0' "$EV" && /usr/bin/grep -qx 'disagree_B_policy_fixed_witness_present=0x8' "$EV" && /usr/bin/grep -qx 'disagree_B_reads=0' "$EV" && /usr/bin/grep -qx 'shape_count=18' "$EV" && /usr/bin/grep -qx 'moved_count=1' "$EV" && /usr/bin/grep -qx 'moved_shapes=m27c512-full-blank-check-bad' "$EV" && /usr/bin/grep -qE '^new_hash_m27c512-full-blank-check-bad=[0-9a-f]{12}$' "$EV" && ! /usr/bin/grep -qx 'new_hash_m27c512-full-blank-check-bad=077a32d1a5c4' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than six `rc=0` lines in the evidence file; or any pinned `key=value` line absent — notably `flags_seen=0x8,0x8` (the flag never reached the wire, or reached it on only one cycle), `bc_verdict=SKIPPED`, `write_run_count=2`, `overall=PASS`, `tranche_witness=True` (the witness died in the tranche staging), `disagree_A_policy_uvslot_witness_absent=0x0` (the flag was set from the policy string, not the witness), `disagree_B_policy_fixed_witness_present=0x8` (the witness is inert), `moved_count=1`, or `moved_shapes=m27c512-full-blank-check-bad`; or `new_hash_m27c512-full-blank-check-bad` still reads `077a32d1a5c4` (the verdict never changed); or the evidence file contains `no tests ran` or a line matching `[0-9]+ (failed|error)`; or non-zero exit from the whole conjunction.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- firestarter/ tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*["'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- tests/fixtures/plan_shapes.json && git diff HEAD~1 --quiet -- tests/fixtures/rekey_ledger.py && git diff HEAD~1 --quiet -- tests/fixtures/report_shapes.py && git diff HEAD~1 --quiet -- tests/fixtures/shape_ids.json && git diff HEAD~1 --quiet -- pyproject.toml && test "$(git diff HEAD~1 --name-only -- tests/fixtures/reports/ | wc -l)" -eq 0 && ./.venv311/bin/python -m ruff check firestarter/ tests/ && ./.venv311/bin/python -m ruff format --check firestarter/ tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' && test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git status --porcelain firestarter/data/chip_database.json)"</automated>
    <fails_when>non-zero exit — specifically: the added-comment count is not 0; `plan_shapes.json`, `rekey_ledger.py`, `report_shapes.py`, `shape_ids.json` or `pyproject.toml` shows a diff (this commit is production + the double only); any file under `tests/fixtures/reports/` was regenerated; ruff prints a violation; the mypy line does not read exactly `mypy errors: 35 (watermark: 35)`; or either porcelain check prints a path.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check > /tmp/179-01-snap.txt 2>&1; test "$(/usr/bin/grep -c '^DRIFT: ' /tmp/179-01-snap.txt)" -eq 1 && /usr/bin/grep -q 'm27c512-full-blank-check-bad.json' /tmp/179-01-snap.txt</automated>
    <fails_when>the snapshot checker reports zero `DRIFT:` lines (the verdict change never reached `to_dict()`), or more than one (this commit disturbed a shape it had no business touching), or the single drifting file is not `m27c512-full-blank-check-bad.json`.</fails_when>
  </verify>
  <done>A two-cycle `dev test` run on a UV part holding data outside the target slot writes the slot, reads `overall_verdict == "PASS"` with `run_count == 2` on both the write and the verify step, keeps the "not blank" finding as a reason plus firmware `error_code 176` on a non-`BAD` step, sets the flag from the witness and not from the policy string in BOTH disagreement directions, leaves `plan_shapes.json` byte-unchanged, adds zero source comments, and moves exactly one frozen hash — `m27c512-full-blank-check-bad`, the shape `RK-174-04` was pre-seeded for.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The carry-through and fail-closed pins the additive fields live or die by</name>
  <files>firestarter_app/tests/test_uv_mask.py, firestarter_app/tests/test_chip_test_cycle.py</files>
  <read_first>
    - firestarter_app/tests/test_uv_mask.py lines 1-60 (module docstring and imports), 170-300 (`_valid_target`, `test_write_target_region_policy_defaults_to_fixed` — the additive-field precedent to copy verbatim in shape — and the five `__post_init__` refusal tests with their non-vacuity first lines)
    - firestarter_app/tests/test_chip_test_cycle.py (the whole module's helper block and at least two existing `_plan_cycle_targets` / `_uv_cycle_targets` tests, to reuse their construction idiom rather than inventing one)
    - firestarter_app/firestarter/chip_test.py lines 1404-1540 (`_plan_cycle_targets`, `_alternating_cycle_targets`, `_uv_cycle_targets`), 1286-1330 (`_CYCLE_BLOCK_OPS`, `_CYCLE_BLOCK_START_OPS`, `cycle_block_bounds`), 2327-2413 (`WriteTarget`), 447-480 (`Step`)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-PATTERNS.md (pattern 2 — the additive-field / set-site / carry-through triple, and the named-leg collection proof)
  </read_first>
  <behavior>
    - A `WriteTarget` constructed by any pre-existing helper — one that names no `current_is_probe_read` at all — reports `current_is_probe_read is False`. The default is fail-closed, exactly as `region_policy` defaults to `REGION_POLICY_FIXED`.
    - A `Step` constructed by any pre-existing helper reports `uv_prewrite is False`.
    - `_uv_cycle_targets(<a probe-read masked target>, cycles=2)` returns two targets and BOTH report `current_is_probe_read is True` — the witness survives the only staging path that reaches `write_eprom`.
    - `_uv_cycle_targets(<the same target with current_is_probe_read=False>, cycles=2)` returns two targets both reporting `current_is_probe_read is False` — the carry is a carry, not a hard-coded `True`.
    - `_alternating_cycle_targets(<an unmasked target>, cycles=2)` returns a complement reporting `current_is_probe_read is False`.
    - For every UV chip in a small named set (`m27c512`, `am27c020`, `tms27c512`) at `write_scope="full"`, `cycle_block_bounds(plan.steps)` starts strictly AFTER the `OP_BLANK_CHECK` index — so the pre-write blank-check's non-`None` `error_code` can never reach `_run_cycle_block`'s `hardware_refused` break.
  </behavior>
  <action>
Author these as behaviour tests, one function per behaviour, in a SECOND commit that touches
`tests/` only. No `#` comments anywhere; rationale goes in each test's docstring, copying the
wording style of `test_write_target_region_policy_defaults_to_fixed`
(`tests/test_uv_mask.py:194-200`), which is the exact precedent this pair of additive fields
follows.

In `tests/test_uv_mask.py`, beside the existing `region_policy` default test, add
`test_write_target_current_is_probe_read_defaults_to_false` — call the module's own `_valid_target()`
helper, which names no such field, and assert the default. Its docstring states that the default is
FAIL-CLOSED in a stronger sense than `region_policy`'s: every direct `WriteTarget(...)` in the suite
keeps working AND keeps `FLAG_SKIP_BLANK_CHECK` off. Add
`test_step_uv_prewrite_defaults_to_false` in the same file (or in
`tests/test_chip_test_cycle.py` if `Step` is not already imported there — pick the module that
already imports it and say which in the docstring).

In `tests/test_chip_test_cycle.py`, add three carry-through tests. Build ONE probe-shaped masked
target — `current` a 256-byte all-`0xFF` block, `pattern` = `mask_write_pattern(current, generate_pattern(start, 256))`,
`masked=True`, `bits_cleared`/`bits_retained` from `bits_cleared_by`/`bits_retained_by`,
`current_source="probe read"`, `current_is_probe_read=True`, `region_policy=REGION_POLICY_UV_SLOT` —
and assert `_uv_cycle_targets(target, 2)` yields two staged targets that BOTH carry the witness.
Then repeat with `current_is_probe_read=False` on the source target and assert both staged targets
carry `False`: this is the anti-vacuity sibling, and without it the first test passes just as well
against a hard-coded `current_is_probe_read=True` in `_uv_cycle_targets`. Then assert
`_alternating_cycle_targets(<unmasked target>, 2)`'s complement carries `False`.

Add `test_uv_plan_blank_check_sits_outside_the_cycle_block`, parametrized over `m27c512`,
`am27c020` and `tms27c512` at `write_scope="full"`: derive the plan, locate the `OP_BLANK_CHECK`
index, call `cycle_block_bounds(plan.steps)`, and assert `blank_check_index < block_start`. Its
docstring states what the pin protects: `_dispatch_step` now returns a non-`None` `error_code` on a
step whose verdict is `SKIPPED`, and that is only safe while the step is outside the block where
`_run_cycle_block` breaks on `result.error_code is not None` (`chip_test.py:1648-1652`). A future
phase that moves the UV blank-check inside the block would silently reintroduce the cycle-2 abort
this phase exists to remove; this test goes red instead.

Commit once, tests only, inside `firestarter_app`.
  </action>
  <acceptance_criteria>
    - `pytest --collect-only` lists all five new test node ids, and none of the five names produces a `leg_<name>=0` line in the collection proof.
    - `_valid_target().current_is_probe_read is False` and `Step(op="blank-check", supported=True, reason="").uv_prewrite is False`.
    - `_uv_cycle_targets(<witness-bearing target>, 2)` returns a list of length 2 whose members all report `current_is_probe_read is True`; the same call on a witness-less source returns two members all reporting `False`.
    - `_alternating_cycle_targets(<unmasked target>, 2)` yields a complement whose `current_is_probe_read` is `False`.
    - `cycle_block_bounds` starts strictly after the `OP_BLANK_CHECK` index for all three of `m27c512`, `am27c020`, `tms27c512` at `write_scope="full"`.
    - `git diff HEAD~1 --quiet -- firestarter/` succeeds — this is a tests-only commit.
    - `git diff HEAD~1 --quiet -- tests/fixtures/` succeeds — no fixture was touched.
    - The commit adds zero lines matching `^\+[[:space:]]*#`, and `ruff check tests/` and `ruff format --check tests/` are green.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence && mkdir -p "$E" && EV=$E/179-01-carry-through.txt && { echo "== 1 the named legs exist"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q --collect-only tests/test_uv_mask.py tests/test_chip_test_cycle.py) > /tmp/179-01-collect.txt 2>&1; echo "rc=$?"; for t in test_write_target_current_is_probe_read_defaults_to_false test_step_uv_prewrite_defaults_to_false test_uv_cycle_tranches_carry_the_probe_read_witness test_alternating_cycle_complement_carries_no_probe_read_witness test_uv_plan_blank_check_sits_outside_the_cycle_block; do echo "leg_${t}=$(/usr/bin/grep -c "$t" /tmp/179-01-collect.txt)"; done; echo "== 2 the carry-through, measured directly"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter import chip_test as ct
from firestarter.database import EpromDatabase
cur = b'\xff' * 256
des = ct.generate_pattern(0xFF00, 256)
def mk(w):
    return ct.WriteTarget(region=(0xFF00, 256), pattern=ct.mask_write_pattern(cur, des), masked=True, bits_cleared=ct.bits_cleared_by(cur, des), bits_retained=ct.bits_retained_by(cur, des), current_source='probe read', current=cur, current_is_probe_read=w, region_policy=ct.REGION_POLICY_UV_SLOT)
print('tranche_witness=' + ','.join(str(t.current_is_probe_read) for t in ct._uv_cycle_targets(mk(True), 2)))
print('tranche_no_witness=' + ','.join(str(t.current_is_probe_read) for t in ct._uv_cycle_targets(mk(False), 2)))
un = ct.WriteTarget(region=(0x1000, 256), pattern=ct.generate_pattern(0x1000, 256), masked=False, bits_cleared=0, bits_retained=0, current_source='address-derived pattern (unmasked)')
print('complement_witness=' + ','.join(str(t.current_is_probe_read) for t in ct._alternating_cycle_targets(un, 2)))
db = EpromDatabase(skip_local_override=True)
for name in ('m27c512', 'am27c020', 'tms27c512'):
    p = ct.derive_plan(name, db, write_scope='full')
    ops = [s.op for s in p.steps]
    print('outside_block_' + name + '=' + str(ops.index(ct.OP_BLANK_CHECK) < ct.cycle_block_bounds(p.steps)[0]))
"); echo "rc=$?"; echo "== 3 the two modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_uv_mask.py tests/test_chip_test_cycle.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 3 && test "$(/usr/bin/grep -cE '^leg_[a-z_]+=0$' "$EV")" -eq 0 && /usr/bin/grep -qx 'tranche_witness=True,True' "$EV" && /usr/bin/grep -qx 'tranche_no_witness=False,False' "$EV" && /usr/bin/grep -qx 'complement_witness=False,False' "$EV" && /usr/bin/grep -qx 'outside_block_m27c512=True' "$EV" && /usr/bin/grep -qx 'outside_block_am27c020=True' "$EV" && /usr/bin/grep -qx 'outside_block_tms27c512=True' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than three `rc=0` lines; or any `leg_<name>=0` line present (that test function was never collected); or `tranche_witness` is not `True,True` (the witness died in the staging that is the only path onto the report); or `tranche_no_witness` is not `False,False` (the carry is a hard-coded `True` and the first leg is vacuous); or any `outside_block_<chip>` line is `False` (the blank-check moved inside the cycle block and its `error_code` can now abort cycle 2); or the evidence contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*["'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- firestarter/ && git diff HEAD~1 --quiet -- tests/fixtures/ && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/ && test -z "$(git -C /workspaces/firestarter status --porcelain)"</automated>
    <fails_when>non-zero exit — the added-comment count is not 0, `firestarter/` or `tests/fixtures/` shows a diff in a tests-only commit, ruff prints a violation, or the firmware submodule is not porcelain-clean.</fails_when>
  </verify>
  <done>Both additive fields are pinned fail-closed by a test that constructs them through a helper naming neither; the probe witness is proved to survive the tranche staging AND proved not to be hard-coded there; the complement is proved to carry no witness; and the UV blank-check's position outside the cycle block — the structural fact that makes a non-`None` `error_code` on a `SKIPPED` step safe — is pinned for three UV chips.</done>
</task>

</tasks>

<threat_model>
ASVS level 1, blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host engine to physical, irreversible device write | This plan DELIBERATELY disables a firmware safety pre-flight. A wrong witness programs bits into a UV part the operator has no eraser for. |
| serial link to host engine | The firmware's `MSG_ERR_NOT_BLANK` text becomes `StepResult.reason` — untrusted device/driver-originated string data, carried through to a public issue body. |
| host process to public GitHub issue | A UV run now reaches `PASS`, which is what `submit.build_title` puts in a public tracker title and what `build_db_diff` proposes for `community-reported` graduation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-179-01 | Tampering | `chip_test._dispatch_multi_run`'s write call site | **critical** | mitigate | The flag is derived from `_is_monotonic_masked_target` — three structural conjuncts (`masked`, non-empty `current`, `current_is_probe_read`) — and never from `region_policy`, `Plan.is_uv` or a `current_source` string. `current_is_probe_read` defaults `False` and is set `True` at exactly one site: the probe arm that actually read the device. Both disagreement directions are asserted in this task's Leg 2, and the empty/absent cases are asserted in the same leg. The failure mode if the witness is wrong is a physically unrecoverable part. |
| T-179-02 | Spoofing | `WriteTarget` construction by a future caller | high | mitigate | The witness is a boolean a caller could set `True` while supplying an assumed `current` — the retired D-C branch's exact shape. Mitigated by making the field's ONLY `True` assignment the probe arm, by the carry-through-not-re-derive discipline asserted in Task 2, and by `WriteTarget.__post_init__`'s pre-existing `_UV_MIN_CLEARED_BITS` / `_UV_MIN_RETAINED_BITS` floors, which structurally refuse a degenerate masked target regardless of provenance. |
| T-179-03 | Repudiation | `_dispatch_step`'s `OP_BLANK_CHECK` arm | high | mitigate | The verdict changes; the FINDING does not. `reason` and `error_code` both survive on the adjudicated step (Q7), `submit._reason_text` renders a `SKIPPED` reason verbatim (`NA` would suppress it to `-`, which is why `NA` was rejected), and the exported `steps[].error_code` still carries the firmware's `MSG_ERR_NOT_BLANK`. A used UV part remains visibly a used UV part in a filed report. |
| T-179-04 | Information disclosure | `StepResult.reason` from the firmware | medium | mitigate | The firmware's address+value text is pre-existing payload through `submit.sanitize_dict`, which deep-scrubs `to_dict()` (home dirs, `/dev/tty*`, `COM\d+`, `/tmp/*`, the username). This plan adds no new exported key, so the scrubbed surface is unchanged. |
| T-179-05 | Elevation of privilege | `build_db_diff`'s ladder | medium | accept | MEASURED: an all-slots-exhausted UV run now proposes `candidate for community-reported` where the `BAD` blank-check previously landed it on `community-fail`. The flip is FORCED by UV-02 — any verdict clearing the FAIL fold also clears the ladder's BAD arm, identically for `NA` and `SKIPPED`. The underlying hole (a `SKIPPED` write still proposing graduation) is PRE-EXISTING and reachable today on any non-UV refused write. `build_db_diff` is deliberately not touched; the disposition is `accept` with a follow-up todo filed in `179-04`. The ladder is advisory and human-triaged by construction (`diagnostic_report.py:366-375`). |
| T-179-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. `179-RESEARCH.md`'s Environment Availability table records zero missing dependencies with no fallback needed, and `pyproject.toml` is asserted byte-unchanged by this task's second verify leg. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_chip_test_blank_check_order.py tests/test_plan_shapes_drift.py tests/test_uv_mask.py tests/test_chip_test.py tests/test_chip_test_cycle.py tests/test_chip_test_sdp_leg.py tests/test_devtest_firmware_error_propagation.py -o addopts="" -q -p no:randomly` — all green.
2. `./.venv311/bin/python -m ruff check firestarter/ tests/`, `ruff format --check`, `tools/check_mypy_watermark.py` (35/35), `tools/check_devtest_orchestrator.py`, `tools/check_diagnostic_report_claims.py` — all exit 0.
3. `git diff --quiet -- tests/fixtures/plan_shapes.json` across both commits — the 677-chip pin is byte-unchanged.
4. EXPECTED RED, do not repair here: `tests/test_blast_radius_invariance.py` (`FROZEN_HASHES` and `LADDER_PINS` for `m27c512-full-blank-check-bad`), `tests/test_rekey_ledger.py::test_every_ledger_row_shape_id_resolves_and_recomputes`, and `tools/snapshot_report_shapes.py --check` (exactly one `DRIFT:` line). Plan `179-02` is the separate re-baseline and declaration, per `.planning/MILESTONES.md`'s D-11 protocol.
5. Zero `#` comment lines added across both commits; `/workspaces/firestarter` porcelain empty; `chip_database.json` porcelain empty.
</verification>

<success_criteria>
- A UV part holding data outside the target slot accepts a write to that slot without being refused, proved end to end against a double that models the firmware pre-flight with the REAL host contract (ROADMAP criterion 1, host half — the hardware half is plan `179-04`).
- That same run's `overall_verdict` reads `PASS` and the write and verify steps' `run_count` reads 2 (ROADMAP criterion 2, host half).
- The blank-check skip is derived from the monotonicity witness and not from the `region_policy` string, proved by a measurement in which the two signals disagree in BOTH directions and the witness wins (ROADMAP criterion 3 — the committed test form is plan `179-03`).
- The frozen-hash blast radius is exactly one shape, and it is the shape `RK-174-04` was pre-seeded for (plan `179-02` declares it).
</success_criteria>

<output>
Create `.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-01-SUMMARY.md` when done.
</output>
