---
phase: 179-uv-slot-writes-flag-skip-blank-check-hardware-gated
plan: 02
type: execute
wave: 2
depends_on: ["179-01"]
files_modified:
  - firestarter_app/tests/fixtures/report_shapes.py
  - firestarter_app/tests/fixtures/shape_ids.json
  - firestarter_app/tests/fixtures/reports/
  - firestarter_app/tests/fixtures/rekey_ledger.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - .planning/MILESTONES.md
  - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md
  - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/
autonomous: false
requirements: [UV-01, UV-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "UV-02: the frozen corpus gains `uv-slot-write-pass` — the FIRST shape in the corpus that exercises a real UV masked slot write. MEASURED: `_build_m27c512_full_all_ok` is a misnomer; its write and verify steps are `SKIPPED` with `run_count=0`, because `_fixed_return_operator.read_eprom` returns `True` while writing no file, so `_read_region` gets `b\"\"`, every UV slot is unevaluable and `_resolve_write_target` refuses."
    - "UV-02: `uv-slot-write-pass` is registered across ALL EIGHT gate-enforced sites in ONE commit — `_BUILDERS`, `FROZEN_HASHES`, removal from `RESERVED_SHAPE_IDS`, `LADDER_PINS`, `_PINNED_SHAPE_ID_SET`, `tests/fixtures/shape_ids.json`, the generated `tests/fixtures/reports/uv-slot-write-pass.json`, plus the prose repairs. Any partial state is red by construction because `SHAPE_IDS = tuple(sorted(_BUILDERS))`."
    - "Ordering inside that commit is load-bearing: `tools/snapshot_report_shapes.py` rejects a `--shape` value absent from `SHAPE_IDS`, so the builder and its `_BUILDERS` entry must be on disk before the hash can be MEASURED and before the snapshot can be GENERATED at all."
    - "Every frozen hash and every snapshot is MEASURED, never predicted. `179-RESEARCH.md`'s projections are explicitly MEDIUM confidence (mutation over an existing report object, not a build through modified production code) and this plan's own pre-measurement of `927571e5110f` came from a monkeypatched simulation, not from the shipped builder. Re-measure; freeze the measured value."
    - "UV-02 / `RK-174-04`: the pre-seeded ledger row `RK-174-04-p179-uv-blank-check-abort` (`tests/fixtures/rekey_ledger.py:149-154`) is DECLARED — its `after_hash` is filled with the measured post-change `m27c512-full-blank-check-bad` hash, and `.planning/MILESTONES.md`'s bound row is updated in the SAME commit. `tools/rekey/check_rekey_ledger.py` binds them; either alone fails."
    - "`RK-174-04`'s recorded mechanism is the accurate one — the `blank-check` verdict triple moving. `.planning/research/PITFALLS.md:186-188` step 2 (the standalone blank-check's `error_code` tripping `hardware_refused` and aborting cycle 2) is FALSIFIED: MEASURED, a UV plan's `cycle_block_bounds` is `(3, 6)` and the pre-write blank-check sits OUTSIDE it. The abort came from the WRITE step's own firmware refusal."
    - "`before_hash` `077a32d1a5c4` is never overwritten, and no OTHER ledger row's `after_hash` is filled. Declaring a non-move is a bookkeeping error (Phase 178 D-09); `tests/test_rekey_ledger.py` enforces both directions at `:332` and `:358`."
    - "The declaration is a SEPARATE commit from the re-baseline, per `.planning/MILESTONES.md`'s D-11 protocol, so the re-key is a reviewable unit and an executor cannot reflexively repair the test inside the same commit that changed the behaviour."
    - "MEASURED and PINNED: `LADDER_PINS['m27c512-full-blank-check-bad']` moves from `(_DISPOSITION_COMMUNITY_FAIL_LITERAL, \"community-fail\")` to `(_DISPOSITION_CANDIDATE_LITERAL, \"community-reported\")`, and `uv-slot-write-pass` pins to the same CANDIDATE pair. `test_ladder_pins_cover_all_four_build_db_diff_arms` still finds exactly four distinct pairs: gh20/gh23/gh28 keep COMMUNITY_FAIL populated, attr01/gh47/readback-gated keep INCONCLUSIVE populated, and the two `synthetic-arm4-*` shapes keep NO-CHANGE populated."
    - "`RESERVED_SHAPE_IDS` becomes EMPTY, and every read site is repaired so none becomes VACUOUS. `test_build_shape_raises_for_every_reserved_shape_id` iterates the set — over an empty set it passes over zero iterations and asserts nothing. After this plan the module still asserts (a) the reservation namespace is fully drawn down and (b) `build_shape` still raises `KeyError` for a name outside `SHAPE_IDS`, probed with an explicit unregistered sentinel."
    - "Counts move 18 -> 19 in five places that are separately enforced: `_BUILDERS`, `FROZEN_HASHES`, `LADDER_PINS`, `_PINNED_SHAPE_ID_SET` (in SORTED position) and `tests/fixtures/shape_ids.json`. Phase 178's verify line `reserved_left=uv-slot-write-pass` INVERTS here to `reserved_left=` (empty)."
    - "`tests/fixtures/plan_shapes.json` stays byte-unchanged by this plan — it is derived from `derive_plan` alone and `179-01` left `supported` untouched."
    - "The tree is GREEN again at the end of this plan: the full suite floor is `>= 2242` (the measured pre-phase baseline was `2241 passed, 32 snapshots passed`), `tools/snapshot_report_shapes.py --check` reports 19 matching snapshots, and `tools/rekey/check_rekey_ledger.py` prints `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`."
    - "Environment: every Python command runs through `firestarter_app/.venv311/bin/python`, `tools/rekey/check_rekey_ledger.py` runs with `python3` FROM `/workspaces` (running it from the sub-repo gives `Errno 2`), every evidence-grade scan uses `/usr/bin/grep`, and every count-bearing pytest run passes `-o addopts=\"\"`."
  prohibitions:
    - "No `#` comment is written into any source or test file this phase touches. `report_shapes.py` carries its rationale in the module docstring and in each builder's own docstring; `test_blast_radius_invariance.py` carries its rationale in the bare-string-literal-above-the-constant idiom already shipped at `:88-186` and `:216-226`."
    - "No snapshot under `tests/fixtures/reports/` is hand-written or hand-edited. Every one is produced by `tools/snapshot_report_shapes.py`; the `--check` gate compares against a fresh regeneration, so a hand-written file is red by construction."
    - "No hash is transcribed from a planning document, from `179-RESEARCH.md`'s projection table, or from this plan's own pre-measured `927571e5110f`. Every frozen value is measured in `.venv311` off the committed builder."
    - "`before_hash` cells are never edited. `RK-174-04`'s `077a32d1a5c4` stays exactly as it is — it is the evidence that the change was exactly what it declared and nothing more."
    - "No ledger row other than `RK-174-04` is touched, and no new ledger row is appended by this plan."
    - "`tests/fixtures/rekey_ledger.py` stays free of executable logic — plain four-tuples only, no dataclass, no enum, no computed expression. The meta-side checker parses it with `ast.parse` + `ast.literal_eval` rather than importing it."
    - "No file under `firestarter_app/firestarter/` is modified by this plan. It is fixture-and-record work only, and `git diff --quiet -- firestarter/` is asserted on both commits."
    - "No file in the `firestarter/` firmware submodule is modified."
    - "`firestarter_app/firestarter/data/chip_database.json` is never hand-edited."
    - "`tests/fixtures/devtest_issue_corpus.json` is historical evidence and is never re-hashed or edited."
    - "The re-baseline and the declaration are never squashed into one commit."
  artifacts:
    - path: "firestarter_app/tests/fixtures/report_shapes.py"
      provides: "`_build_uv_slot_write_pass`, its `_BUILDERS` entry, its measured `FROZEN_HASHES` entry, the re-baselined `m27c512-full-blank-check-bad` hash, and an emptied `RESERVED_SHAPE_IDS`"
      contains: "uv-slot-write-pass"
      exports: ["_build_uv_slot_write_pass"]
    - path: "firestarter_app/tests/fixtures/reports/uv-slot-write-pass.json"
      provides: "The generated byte-compared `to_dict()` snapshot for the first UV masked-slot-write shape in the corpus"
      contains: "uv-slot"
    - path: "firestarter_app/tests/fixtures/shape_ids.json"
      provides: "The committed sorted anchor, 18 -> 19 entries, with `uv-slot-write-pass` in sorted position"
      contains: "uv-slot-write-pass"
    - path: "firestarter_app/tests/fixtures/rekey_ledger.py"
      provides: "`RK-174-04-p179-uv-blank-check-abort` declared — `after_hash` filled with the measured value, `before_hash` untouched"
      contains: "RK-174-04-p179-uv-blank-check-abort"
    - path: "firestarter_app/tests/test_blast_radius_invariance.py"
      provides: "`LADDER_PINS` and `_PINNED_SHAPE_ID_SET` at 19 entries, the re-pinned `m27c512-full-blank-check-bad` ladder pair, and the de-vacuumed reserved-name tests"
      contains: "uv-slot-write-pass"
    - path: ".planning/MILESTONES.md"
      provides: "The `RK-174-04-p179-uv-blank-check-abort` row's `after` cell and `declared` date, bound to the ledger by `tools/rekey/check_rekey_ledger.py`"
      contains: "RK-174-04-p179-uv-blank-check-abort"
    - path: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md"
      provides: "`D-179-1` (how ROADMAP criterion 4 is satisfied) and `D-179-2` (how `uv-slot-write-pass` is constructed), each a single-line label naming the option taken and a one-line reason"
      contains: "D-179-1"
    - path: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/179-02-shape-registration.txt"
      provides: "The measured registration seal — 19 shapes, 19 snapshots, an empty reservation set, and the single measured re-key"
      contains: "reserved_left="
  key_links:
    - from: "firestarter_app/tests/fake_chip.py::WriteInitPreflightChip"
      to: "firestarter_app/tests/fixtures/report_shapes.py::_build_uv_slot_write_pass"
      via: "the shared firmware-faithful double is imported into the fixture registry so the frozen shape exercises the ACTUAL write-init pre-flight, not a Mock that answers True"
      pattern: "WriteInitPreflightChip"
    - from: "firestarter_app/tests/fixtures/report_shapes.py::FROZEN_HASHES"
      to: "firestarter_app/tests/fixtures/rekey_ledger.py::LEDGER"
      via: "the re-baselined m27c512-full-blank-check-bad value and RK-174-04's after_hash are the SAME measured 12-hex string, in two separate commits"
      pattern: "RK-174-04-p179-uv-blank-check-abort"
    - from: "firestarter_app/tests/fixtures/rekey_ledger.py::LEDGER"
      to: ".planning/MILESTONES.md"
      via: "tools/rekey/check_rekey_ledger.py binds every ledger row to a MILESTONES.md row in both directions; either side alone fails"
      pattern: "8 ledger row\\(s\\), 8 MILESTONES.md row\\(s\\) bound"
---

<objective>
Restore the tree to green by paying the re-key `179-01` deliberately incurred, and publish the
first frozen shape in this corpus that exercises a real UV masked slot write.

Two things happen here and they are deliberately not one commit. First the frozen corpus is
re-baselined and `uv-slot-write-pass` — the LAST reserved shape id, claimed for Phase 179 back in
Phase 174 — is registered across all eight gate-enforced sites, drawing the reservation namespace
down to empty. Then, separately, `RK-174-04-p179-uv-blank-check-abort` is DECLARED: its `after_hash`
filled with the measured value and `.planning/MILESTONES.md`'s bound row updated in the same commit,
so the re-key is a reviewable unit rather than a test repaired inside the commit that broke it.

Purpose: `.planning/MILESTONES.md`'s D-11 protocol exists so "exactly the change it declared and
nothing more" is a machine check instead of a sentence. `179-01` turned one gate red on purpose;
this plan is the only thing entitled to turn it green.

Output: 19 frozen shapes, 19 matching snapshots, an empty `RESERVED_SHAPE_IDS` whose emptiness is
asserted rather than silently vacuous, one declared re-key bound across two repositories, and a
green full suite.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-RESEARCH.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-PATTERNS.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-01-SUMMARY.md
@firestarter_app/CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by phase 179 and does not exist at `firestarter_app @ 835baba`.

| Artifact | Kind | Home | Plan |
|---|---|---|---|
| `WriteTarget.current_is_probe_read` | dataclass field, `bool = False` | `firestarter/chip_test.py` | 179-01 |
| `Step.uv_prewrite` | dataclass field, `bool = False` | `firestarter/chip_test.py` | 179-01 |
| `_is_monotonic_masked_target` | module function `-> bool` | `firestarter/chip_test.py` | 179-01 |
| `WriteInitPreflightChip` | test double class | `tests/fake_chip.py` | 179-01 |
| `uv-slot-write-pass` | frozen `shape_id` | `tests/fixtures/report_shapes.py` | 179-02 |
| `_build_uv_slot_write_pass` | builder function | `tests/fixtures/report_shapes.py` | 179-02 |
| `tests/fixtures/reports/uv-slot-write-pass.json` | new file (generated) | `tests/fixtures/reports/` | 179-02 |
| `RK-174-04-p179-uv-blank-check-abort` `after_hash` | declared ledger value | `tests/fixtures/rekey_ledger.py` | 179-02 |
| `D-179-1`, `D-179-2` | recorded decisions | `.planning/phases/179-…/179-DECISIONS.md` | 179-02 |
| `tests/test_chip_test_uv_slot_write.py` | new test module | `firestarter_app/tests/` | 179-03 |
| `179-MEASUREMENT.md` | record (bench artifact) | `.planning/phases/179-…/` | 179-04 |

New test functions created by THIS plan: none — this plan REPAIRS
`test_build_shape_raises_for_every_reserved_shape_id` and
`test_shape_id_set_is_pinned_and_disjoint_from_reserved` so neither goes vacuous over an emptied
`RESERVED_SHAPE_IDS`, and adds one sentinel probe inside the former rather than a new function.

New evidence files: `179-02-shape-registration.txt`, `179-02-rekey-declaration.txt`, both under
`.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/`.

## Spec-less probe arithmetic (no silent drops)

The four probe-surfaced edge rows are accounted for in `179-01-PLAN.md`: **4 surfaced == 1 authored
into `must_haves` + 3 surfaced as flagged assumptions.** This plan authors none of the four
directly and re-flags none of them; they are neither duplicated nor dropped here.

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: The two calls no CONTEXT.md makes — criterion 4's route, and the shape's construction</name>
  <files>.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md</files>
  <reversibility rating="one-way">`D-179-2` publishes a 12-hex `dedup_fingerprint` and a byte-compared `to_dict()` snapshot as frozen identity, and draws the reservation namespace down to empty. Undoing either afterwards is not a code revert: it needs a NEW declared re-key row in `tests/fixtures/rekey_ledger.py`, a matching row in `.planning/MILESTONES.md`, and a green `tools/rekey/check_rekey_ledger.py` in the meta repository. `D-179-1`'s Option B is one-way in a different sense — a fifth `ALLOWED_SKIP_REASONS` entry is a standing policy precedent for what this suite is willing to skip, and there is no precedent to remove it against.</reversibility>
  <decision>
There is NO `179-CONTEXT.md`. The operator chose to plan without one, so every gray area is a named,
visible assumption rather than a silent pick — and two of them are one-way doors that must not be
walked through by an executor.

**`D-179-1` — how is ROADMAP criterion 4 satisfied?** The criterion reads: *"The regression test
proving criteria 1 and 2 is committed to the suite and passes against real UV hardware, closing the
gap that no such test exists today."* **[VERIFIED: `tests/test_skip_census.py:114-140`, measured]**
there is NO hardware-gated pytest test anywhere in this repository. `ALLOWED_SKIP_REASONS` has
exactly four entries — sibling firmware repo absent, `firestarter` entry point not on PATH,
meta-repo ledger not available, `EVIDENCE.json` not found — and not one of them is board- or
chip-presence. There is no mechanism to copy and no precedent to follow. `179-PATTERNS.md` records
this under **No Analog Found**.

**`D-179-2` — how is `uv-slot-write-pass` constructed?** `179-01` proved the engine reaches the
target state; this decides what gets FROZEN as its published identity. **[VERIFIED: measured]** no
existing corpus shape exercises a UV masked write at all — `_build_m27c512_full_all_ok` is a
misnomer whose write and verify are `SKIPPED` with `run_count=0` — so the default
`_fixed_return_operator` cannot be used and a choice must be made.
  </decision>
  <options>
    - **`D-179-1` Option A (RECOMMENDED) — the split, per `179-RESEARCH.md` route R1.** Commit a firmware-faithful-double regression test (`tests/test_chip_test_uv_slot_write.py`, plan `179-03`) that runs everywhere in CI with no skip marker and asserts `submit.overall_verdict(...) == "PASS"` with write `run_count == 2`; then satisfy "passes against real UV hardware" with a `blocking-human` bench wave on a real ST M27C512 producing a committed `179-MEASUREMENT.md`, on the Phase 176-05 precedent. **Honest cost, stated plainly: the committed test proves the HOST logic and the bench artifact proves the HARDWARE claim. Neither alone is criterion 4 — the phase closes only with both.** This is RESEARCH assumption A2 and it needs the operator's agreement, because it reads criterion 4's two clauses as satisfiable by two different artifacts.
    - **`D-179-1` Option B — route R2.** Add a genuinely-skipping hardware test plus a FIFTH `ALLOWED_SKIP_REASONS` entry for board/chip presence. This makes the test literally "committed to the suite", but it is skipped in CI forever and is therefore a permanently-dormant assertion — the "fixture-passing selftests ≠ working tooling" failure this project has already recorded. It also sets a standing policy precedent for board-gated skips with nothing to model it on.
    - **`D-179-1` Option C — route R3.** A shell script beside `firestarter_test.sh` / `write_test.sh`. Honest about being a bench script, but not "committed to the suite" in the pytest sense the criterion uses, and it inherits none of the suite's collection or CI guarantees.
    - **`D-179-2` Option A (RECOMMENDED) — real-path.** Build it with `_build_real_path_report(chip="m27c512", write_scope="full", operator=<a `WriteInitPreflightChip` seeded with content OUTSIDE the top slot>, runs=2)`, so the frozen shape is a hash of what the engine really produces: the actual witness, the actual positional flag, the actual firmware-pre-flight refusal it now clears, and the actual adjudicated blank-check verdict. **Cost, stated:** like every other real-path shape in the corpus, its hash is coupled to `derive_plan` and therefore to `chip_database.json` regeneration.
    - **`D-179-2` Option B — hand-specified.** Build it with `build_shape_from_step_specs`, pinning the step vector by hand, as `prune03-synthesized-fingerprint-match` and `sst27sf512-six-step` are. Immune to `chip_database.json` regeneration. **Cost, stated:** it freezes a `StepResult` combination the engine might never actually produce, and the shape then proves nothing about the flag, the witness or the adjudication — which is the entire reason UV-01/UV-02 wanted this shape.
  </options>
  <resume-signal>`.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md` exists and contains a single-line `D-179-1` label and a single-line `D-179-2` label, each naming `Option A`, `Option B` or `Option C` plus a one-line reason, with no value reading `TBD` / `TODO` / `undecided`. Resume at Task 2.</resume-signal>
  <read_first>
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-RESEARCH.md (Q4 in full, including the R1/R2/R3 table and assumption A2; the Blast radius section; assumption A6 on the projected hashes)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-PATTERNS.md (section 6's eight-site table and its ordering constraint; the **No Analog Found** table)
    - firestarter_app/tests/test_skip_census.py lines 100-145 (`ALLOWED_SKIP_REASONS`, all four entries and the in-source note about a programmer board changing this suite's behaviour)
    - firestarter_app/tests/fixtures/report_shapes.py lines 1-75 (the module docstring, including the reserved-name rule and the hand-specified-vs-real-path split), 112-182 (`build_shape_from_step_specs` and its post-construction seams), 400-500 (`_fixed_return_operator`, `_sdp_aware_operator`, `_build_real_path_report`), 530-560 (`_build_m27c512_full_blank_check_bad` and its RESEARCH-correction docstring), 640-720 (the neighbouring builders, `_BUILDERS`, `SHAPE_IDS`, `FROZEN_HASHES`, `RESERVED_SHAPE_IDS` and the disjointness assertion)
    - .planning/MILESTONES.md lines 30-53 (the corrections table, the re-key ledger table, and the declared-re-key protocol for Phases 175-181)
    - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-DECISIONS.md (the single-line label format `D-178-1` is written in, which the decision-coverage gate reads line by line)
  </read_first>
  <action>
Present `D-179-1` and `D-179-2` to the operator, recommended option first, with the measured
tradeoff behind each, then write both answers to `179-DECISIONS.md` as SINGLE-LINE labels naming the
option taken plus a one-line reason. Do not proceed to Task 2 until both are recorded — a
single-line label is required because the decision-coverage gate reads these labels line by line and
fails closed on a wrapped one.

State plainly what is and is not open. `179-01` has already LANDED the witness form, the adjudicated
verdict and the flag composition; those are not being re-asked and the operator does not need to
re-walk them. What is open is (1) whether a committed-double test plus a bench artifact together
satisfy a criterion whose text asks for one test that does both, and (2) which construction route a
one-way published hash is taken through.

Say the physical cost out loud while asking, because it bears on `D-179-1`: a UV write is
irreversible, the operator has no UV eraser, and every bench run permanently consumes one 256-byte
slot of the part's ~256. A refused write costs nothing (the firmware refuses before programming any
byte), so only the post-fix confirmation spends a slot.

Task 2 implements `D-179-2` Option A by default and names Option B's alternate edits inline, so a
recorded Option B does not need a replan. Plan `179-03` implements `D-179-1` Option A by default and
names Option B's and Option C's alternate edits inline, for the same reason.
  </action>
  <acceptance_criteria>
    - `.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md` exists and is non-empty.
    - It contains a line matching `D-179-1` that names `Option A`, `Option B` or `Option C` and carries a reason.
    - It contains a line matching `D-179-2` that names `Option A` or `Option B` and carries a reason.
    - Each label is on a SINGLE line — no wrapped label, because the decision-coverage gate fails closed on one.
    - No line in the file is exactly `TBD`, `TODO` or `undecided` (case-insensitive).
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md && test -s "$D" && /usr/bin/grep -qE '^\s*[-*#]*\s*\**D-179-1\**' "$D" && /usr/bin/grep -qE 'D-179-1.*(Option A|Option B|Option C)' "$D" && /usr/bin/grep -qE '^\s*[-*#]*\s*\**D-179-2\**' "$D" && /usr/bin/grep -qE 'D-179-2.*(Option A|Option B)' "$D" && ! /usr/bin/grep -qiE '^\s*(TBD|TODO|undecided)\s*$' "$D"</automated>
    <fails_when>non-zero exit — the decisions file is missing or empty, no line carries a `D-179-1` or a `D-179-2` label, either label line names no option, or a line reads exactly TBD / TODO / undecided.</fails_when>
  </verify>
  <done>`D-179-1` and `D-179-2` are recorded as single-line labels naming the route taken and the reason, and Tasks 2 and 3 — and plan `179-03` — know which set of edits to make.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the one moved shape and register the last reserved id — eight sites, one commit</name>
  <reversibility rating="one-way">This commit publishes `uv-slot-write-pass`'s frozen hash and its byte-compared snapshot, and empties `RESERVED_SHAPE_IDS`. `D-179-2` is the gate that precedes it.</reversibility>
  <files>firestarter_app/tests/fixtures/report_shapes.py, firestarter_app/tests/fixtures/shape_ids.json, firestarter_app/tests/fixtures/reports/uv-slot-write-pass.json, firestarter_app/tests/fixtures/reports/m27c512-full-blank-check-bad.json, firestarter_app/tests/test_blast_radius_invariance.py</files>
  <read_first>
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md (the recorded `D-179-2`, which selects the edit set below)
    - firestarter_app/tests/fixtures/report_shapes.py lines 1-75 (module docstring and the reserved-name rule — the prose that must be repaired when the set empties), 112-182 (`build_shape_from_step_specs`), 400-500 (`_fixed_return_operator`, `_build_real_path_report` and its `operator.check_eprom_id.return_value` stamping line), 530-560 (`_build_m27c512_full_blank_check_bad`), 640-720 (`_build_attr01_status_axis_transport_fault` — the Phase-178 builder to copy in shape — `_BUILDERS`, `SHAPE_IDS`, `FROZEN_HASHES`, `RESERVED_SHAPE_IDS` and the module-level disjointness assertion at `:715-718`)
    - firestarter_app/tests/fake_chip.py (the whole file, including `179-01`'s new `WriteInitPreflightChip` and its `check_eprom_id` Mock shim)
    - firestarter_app/tests/test_blast_radius_invariance.py lines 196-260 (the `_DISPOSITION_*_LITERAL` block, `LADDER_PINS`' bare-string-literal header with its stale "seventeen frozen shapes" sentence, and the table itself), 350-410 (the parametrized ladder test and `test_ladder_pins_cover_all_four_build_db_diff_arms`), 545-575 (`_PINNED_SHAPE_ID_SET` and `test_shape_id_set_is_pinned_and_disjoint_from_reserved`), 760-780 (`test_build_shape_raises_for_every_reserved_shape_id` and its count sentence)
    - firestarter_app/tools/snapshot_report_shapes.py lines 140-180 (the `--shape` validation against `SHAPE_IDS`, the `--check` DRIFT output and the derived count line)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-PATTERNS.md (section 6: the eight-site table, the ordering constraint, and the builder to copy)
  </read_first>
  <action>
Land the re-baseline AND the registration in ONE commit, inside `firestarter_app` on
`gsd/v1.36-dev-test-fidelity`, following Phase 177's `693c0c3` precedent exactly. Any partial state
is red by construction: `SHAPE_IDS = tuple(sorted(_BUILDERS))`, so the moment the builder is
registered the five-way closure, the pinned id set, the committed anchor and the snapshot stems all
disagree until every site lands. No `#` comments anywhere.

**Ordering inside the commit is load-bearing.** `tools/snapshot_report_shapes.py` rejects a
`--shape` value absent from `SHAPE_IDS`, so sites 1 and 2 must be on disk before the hash can be
MEASURED (site 3) and before the snapshot can be GENERATED at all (site 8).

**1. `_build_uv_slot_write_pass()` (site 1).** A module-level builder returning a `DiagnosticReport`,
with all its rationale in a docstring — never a `#` comment. Import the double as
`from ..fake_chip import WriteInitPreflightChip` (the relative form matching the tree's
`from .fake_chip import FakeChip` idiom; `tests/fixtures` resolves as a namespace package and
`tools/snapshot_report_shapes.py` imports this module package-qualified as
`tests.fixtures.report_shapes`, so the relative import resolves — if any consumer breaks, fall back
to the absolute `from tests.fake_chip import WriteInitPreflightChip` and say so in the docstring).
Construct the double at the `m27c512` memory size read off `_REAL_DB` — never a hard-coded 65536 —
with `uv=True`, seed `chip.data[0x0000:0x0100] = bytes(range(256))` so the part holds data OUTSIDE
the top slot (that is UV-01's precondition, and it is the same shape as Phase 83's bench-proven
16-byte write at `0x0000`), and hand it to `_build_real_path_report(chip="m27c512",
write_scope="full", operator=chip, runs=2)`. The docstring states: this is the FIRST shape in the
corpus that exercises a real UV masked slot write; the default `_fixed_return_operator` cannot be
used because its `read_eprom` returns `True` while writing no file, so `_read_region` gets `b""`,
every UV slot is unevaluable and `_resolve_write_target` refuses — which is why
`_build_m27c512_full_all_ok`'s write and verify are `SKIPPED` with `run_count=0` despite its name;
and the double is firmware-faithful in the sense that matters, returning `False` and recording
`last_firmware_error_code` exactly as `eprom_operations._run_state_machine` does, rather than
raising. Place it beside the other real-path builders. *(If `D-179-2` recorded Option B: build it
with `build_shape_from_step_specs` instead, pinning the step vector
`id=OK / read=OK / blank-check=SKIPPED / write=OK / verify=OK` with `run_counts` giving write and
verify 2, using a chip/protocol pair no other registered shape uses — and say in the docstring what
it therefore does NOT prove.)*

**2. `_BUILDERS` (site 2).** Add `"uv-slot-write-pass": _build_uv_slot_write_pass,` — 18 entries to
19.

**3. `FROZEN_HASHES` (site 3).** Add the MEASURED 12-hex hash for `uv-slot-write-pass`, and
RE-BASELINE `"m27c512-full-blank-check-bad"` to its MEASURED post-`179-01` value. Measure both by
calling `dedup_fingerprint(build_shape(<id>))` in `.venv311` off the committed builder. Do NOT
transcribe `179-RESEARCH.md`'s projections (`e42f1567967a`, `559bb7d81a92`) or this plan's
pre-measured `927571e5110f` — those came from mutation over an existing report object and from a
monkeypatched simulation respectively, and RESEARCH marks them MEDIUM confidence for exactly this
reason. The other 17 committed literals stay byte-for-byte.

**4. `RESERVED_SHAPE_IDS` (site 4) and its prose.** Remove `"uv-slot-write-pass"`; the frozenset
becomes EMPTY. Keep the module-level `assert not (set(SHAPE_IDS) & RESERVED_SHAPE_IDS)` — it costs
nothing and re-arms automatically the day a future phase reserves a name — but repair the module
docstring at `:22-33`, which currently reads "`uv-slot-write-pass` (Phase 179) is now the only one
left", to state that all three reserved names are now registered and the reservation namespace is
fully drawn down, and that the assertion below therefore guards an empty set until a future phase
re-populates it.

**5. `LADDER_PINS` (site 5) — two edits, both measured.** Add
`"uv-slot-write-pass": (_DISPOSITION_CANDIDATE_LITERAL, "community-reported")` in sorted position,
and MOVE `"m27c512-full-blank-check-bad"` from
`(_DISPOSITION_COMMUNITY_FAIL_LITERAL, "community-fail")` to
`(_DISPOSITION_CANDIDATE_LITERAL, "community-reported")`. **[VERIFIED: measured this session]** the
second is FORCED by UV-02, not chosen: any verdict that clears `submit.overall_verdict`'s
FAIL-dominant fold also clears `build_db_diff`'s `"BAD" in verdicts` arm, identically for `NA` and
`SKIPPED`. Verify both against a live `build_db_diff` rather than reasoning about them. Repair the
block's bare-string-literal header: its stale "seventeen frozen shapes" reads nineteen after this
commit, and it should record that the blank-check-bad row moved arms because the UV pre-write
blank-check stopped spending a chip's `BAD`. `test_ladder_pins_cover_all_four_build_db_diff_arms`
must still find exactly four distinct pairs — gh20/gh23/gh28 keep COMMUNITY-FAIL populated,
attr01/gh47/readback-gated keep INCONCLUSIVE populated, and the two `synthetic-arm4-*` shapes keep
NO-CHANGE populated.

**6. `_PINNED_SHAPE_ID_SET` (site 6).** Add `"uv-slot-write-pass"` in SORTED position — 18 to 19.
The list is compared element-wise against `sorted(SHAPE_IDS)`, so an out-of-order insertion is red.

**7. `tests/fixtures/shape_ids.json` (site 7).** Add `"uv-slot-write-pass"` to the committed sorted
anchor — 18 to 19.

**8. The snapshots (site 8).** GENERATE, never hand-write:
`./.venv311/bin/python tools/snapshot_report_shapes.py --shape uv-slot-write-pass` for the new file,
and `./.venv311/bin/python tools/snapshot_report_shapes.py --shape m27c512-full-blank-check-bad` to
re-baseline the one `179-01` drifted. Then run `--check` with no `--shape` and confirm all 19 match.
Exactly TWO files under `tests/fixtures/reports/` change in this commit.

**9. De-vacuum the two reserved-name tests.** `test_build_shape_raises_for_every_reserved_shape_id`
iterates `RESERVED_SHAPE_IDS`; over an empty set it passes over zero iterations and asserts
NOTHING — a pre-authored gate leg that has become unreachable. Repair it so the module still
asserts both halves of what it was protecting: (a) that the reservation namespace is now fully drawn
down, by an explicit `RESERVED_SHAPE_IDS == frozenset()` assertion carrying a message naming Phases
177, 178 and 179 as the three that drew it down; and (b) that `build_shape` still raises `KeyError`
for a name outside `SHAPE_IDS`, by probing an explicit unregistered sentinel string alongside the
(now empty) loop. Repair its docstring's count sentence in the same edit. Then census EVERY other
`RESERVED_SHAPE_IDS` read site — `/usr/bin/grep -rn 'RESERVED_SHAPE_IDS' tests/ tools/
firestarter/` finds them, and `test_shape_id_set_is_pinned_and_disjoint_from_reserved`'s
`assert not (set(SHAPE_IDS) & RESERVED_SHAPE_IDS)` at `:572` is in the same family — and make sure
none of them silently becomes a no-op statement without a reader being told so.

**10. Count-bearing prose.** Sweep for stale counts with
`/usr/bin/grep -rniE 'seventeen|eighteen|nineteen|1[789] (shape|snapshot|frozen)' tests/ tools/` and
repair every hit this commit invalidates. Known: `LADDER_PINS`' header (`:216`) and
`test_build_shape_raises_for_every_reserved_shape_id`'s docstring.

Commit once. The tree is green again at the END of Task 3, not here — the ledger row is still
undeclared, so `tests/test_rekey_ledger.py::test_every_ledger_row_shape_id_resolves_and_recomputes`
is EXPECTED RED after this commit and Task 3 repairs it.
  </action>
  <acceptance_criteria>
    - `len(SHAPE_IDS) == 19` and `"uv-slot-write-pass" in SHAPE_IDS`.
    - `RESERVED_SHAPE_IDS == frozenset()`.
    - `dedup_fingerprint(build_shape("uv-slot-write-pass"))` equals `FROZEN_HASHES["uv-slot-write-pass"]`, and that value is a 12-hex string.
    - `build_shape("uv-slot-write-pass")` has a `blank-check` step with `verdict == "SKIPPED"`, a `write` step with `verdict == "OK"` and `run_count == 2`, and a `verify` step with `verdict == "OK"` and `run_count == 2`; and `submit.overall_verdict(<that report>.results) == "PASS"`.
    - That report's `write` step carries a `write_target` whose `masked` is `True` and whose `current_source` starts with `probe read (tranche` — the frozen shape really is a masked slot write, not a fixed-region one.
    - `FROZEN_HASHES["m27c512-full-blank-check-bad"]` is a 12-hex string that is NOT `077a32d1a5c4`, and it reproduces from a fresh `build_shape` call.
    - All 17 other `FROZEN_HASHES` entries reproduce byte-unmoved from a fresh `build_shape` call.
    - `sorted(SHAPE_IDS) == _PINNED_SHAPE_ID_SET` and `json.load(open("tests/fixtures/shape_ids.json"))["shape_ids"] == sorted(SHAPE_IDS)`.
    - `len(LADDER_PINS) == 19`, `len(set(LADDER_PINS.values())) == 4`, and `build_db_diff` reproduces the pinned pair for every one of the 19 shapes — including the moved `m27c512-full-blank-check-bad` and the new `uv-slot-write-pass`, both landing on `("suggests: candidate for community-reported (advisory)", "community-reported")`.
    - `tools/snapshot_report_shapes.py --check` exits 0 and reports 19 matching snapshots.
    - Exactly two files under `tests/fixtures/reports/` are changed by this commit, and one of them is the new `uv-slot-write-pass.json`.
    - `git diff HEAD~1 --quiet -- firestarter/` and `git diff HEAD~1 --quiet -- tests/fixtures/plan_shapes.json` and `git diff HEAD~1 --quiet -- tests/fixtures/rekey_ledger.py` and `git diff HEAD~1 --quiet -- tests/fixtures/devtest_issue_corpus.json` all succeed.
    - `tests/test_blast_radius_invariance.py` passes in full, and `/usr/bin/grep -rn 'RESERVED_SHAPE_IDS' tests/` shows every read site either non-vacuous or explicitly documented as guarding a drawn-down namespace.
    - The commit adds zero lines matching `^\+[[:space:]]*#`; `ruff check tests/` and `ruff format --check tests/` are green; `tools/check_mypy_watermark.py` still reads exactly 35.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence && mkdir -p "$E" && EV=$E/179-02-shape-registration.txt && { echo "== 1 the registry, the new shape and the re-baseline"; (cd firestarter_app && ./.venv311/bin/python -c "
import json, sys
sys.path.insert(0, '.')
from firestarter import submit as sub
from firestarter.database import EpromDatabase
from firestarter.diagnostic_report import build_db_diff, dedup_fingerprint
from tests.fixtures.report_shapes import FROZEN_HASHES, RESERVED_SHAPE_IDS, SHAPE_IDS, build_shape
from tests.test_blast_radius_invariance import LADDER_PINS, _PINNED_SHAPE_ID_SET
print('shape_count=' + str(len(SHAPE_IDS)))
print('registered=' + str('uv-slot-write-pass' in SHAPE_IDS))
print('reserved_left=' + ','.join(sorted(RESERVED_SHAPE_IDS)))
print('pinned_set_len=' + str(len(_PINNED_SHAPE_ID_SET)))
print('pinned_set_matches=' + str(sorted(SHAPE_IDS) == _PINNED_SHAPE_ID_SET))
anchor = json.load(open('tests/fixtures/shape_ids.json'))['shape_ids']
print('anchor_matches=' + str(anchor == sorted(SHAPE_IDS)))
print('ladder_len=' + str(len(LADDER_PINS)))
print('ladder_distinct=' + str(len(set(LADDER_PINS.values()))))
rep = build_shape('uv-slot-write-pass')
r = {x.op: x for x in rep.results}
print('new_bc_verdict=' + r['blank-check'].verdict)
print('new_write_verdict=' + r['write'].verdict)
print('new_write_run_count=' + str(r['write'].run_count))
print('new_verify_verdict=' + r['verify'].verdict)
print('new_verify_run_count=' + str(r['verify'].run_count))
print('new_overall=' + sub.overall_verdict(rep.results))
wt = r['write'].write_target
print('new_target_masked=' + str(bool(wt and wt.masked)))
print('new_target_probe=' + str(bool(wt and wt.current_source.startswith('probe read (tranche'))))
h_new = dedup_fingerprint(rep)
print('new_hash=' + h_new)
print('new_hash_frozen_matches=' + str(h_new == FROZEN_HASHES['uv-slot-write-pass']))
h_reb = dedup_fingerprint(build_shape('m27c512-full-blank-check-bad'))
print('rebaselined_hash=' + h_reb)
print('rebaselined_frozen_matches=' + str(h_reb == FROZEN_HASHES['m27c512-full-blank-check-bad']))
print('rebaselined_moved_off_before=' + str(h_reb != '077a32d1a5c4'))
inherited = {'sst27sf512-six-step': '7fb88e0b07d6', 'sst27sf512-six-step-readback-gated': 'ff974e416dca', 'gh47-sst27sf512-pass': 'f9dbc31dcd27', 'gh28-m27c512-fail': '31547956e56b', 'gh20-at28c256-fail': '00e121446ceb', 'gh23-w27e257-fail': '7a89fcea856a', 'synthetic-arm4-no-ok': 'f90dfe1a44f7', 'synthetic-arm4-empty-results': '8d6208d00be7', 'm27c512-full-all-ok': '6d3afbc52315', 'm27c512-full-canonical-name': '776846bf2dc8', 'm27c512-full-comma-joined-name': '37ad34d39a19', 'm27c512-full-runs-1': 'e4838f7bb1d3', 'at28c256-full-all-ok-sdp': '050ad3830704', 'sst27sf512-full-all-ok': '14d306256076', 'w27e257-full-all-ok': '3a9f95aba65e', 'prune03-synthesized-fingerprint-match': '3b83a55efb3a', 'attr01-status-axis-transport-fault': '93cef8030c40'}
print('inherited_count=' + str(len(inherited)))
print('inherited_drifted=' + ','.join(sorted(s for s, h in inherited.items() if dedup_fingerprint(build_shape(s)) != h or FROZEN_HASHES[s] != h)))
db = EpromDatabase(skip_local_override=True)
bad = []
for sid, (disp, ladder) in sorted(LADDER_PINS.items()):
    rp = build_shape(sid)
    d = build_db_diff(rp.auto_capture.chip, db, rp.results)
    if (d.proposed_disposition, d.ladder_state) != (disp, ladder):
        bad.append(sid)
print('ladder_mismatches=' + ','.join(bad))
print('ladder_uvslot=' + str(LADDER_PINS['uv-slot-write-pass'][1]))
print('ladder_blankcheckbad=' + str(LADDER_PINS['m27c512-full-blank-check-bad'][1]))
"); echo "rc=$?"; echo "== 2 snapshots regenerate clean at nineteen"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; echo "== 3 the invariance harness"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py); echo "rc=$?"; echo "== 4 no reserved read site went silently vacuous"; (cd firestarter_app && /usr/bin/grep -rn 'RESERVED_SHAPE_IDS' tests/ tools/ firestarter/ 2>/dev/null | /usr/bin/grep -v Binary); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 4 && /usr/bin/grep -qx 'shape_count=19' "$EV" && /usr/bin/grep -qx 'registered=True' "$EV" && /usr/bin/grep -qx 'reserved_left=' "$EV" && /usr/bin/grep -qx 'pinned_set_len=19' "$EV" && /usr/bin/grep -qx 'pinned_set_matches=True' "$EV" && /usr/bin/grep -qx 'anchor_matches=True' "$EV" && /usr/bin/grep -qx 'ladder_len=19' "$EV" && /usr/bin/grep -qx 'ladder_distinct=4' "$EV" && /usr/bin/grep -qx 'new_bc_verdict=SKIPPED' "$EV" && /usr/bin/grep -qx 'new_write_verdict=OK' "$EV" && /usr/bin/grep -qx 'new_write_run_count=2' "$EV" && /usr/bin/grep -qx 'new_verify_verdict=OK' "$EV" && /usr/bin/grep -qx 'new_verify_run_count=2' "$EV" && /usr/bin/grep -qx 'new_overall=PASS' "$EV" && /usr/bin/grep -qx 'new_target_masked=True' "$EV" && /usr/bin/grep -qx 'new_target_probe=True' "$EV" && /usr/bin/grep -qE '^new_hash=[0-9a-f]{12}$' "$EV" && /usr/bin/grep -qx 'new_hash_frozen_matches=True' "$EV" && /usr/bin/grep -qE '^rebaselined_hash=[0-9a-f]{12}$' "$EV" && /usr/bin/grep -qx 'rebaselined_frozen_matches=True' "$EV" && /usr/bin/grep -qx 'rebaselined_moved_off_before=True' "$EV" && /usr/bin/grep -qx 'inherited_count=17' "$EV" && /usr/bin/grep -qx 'inherited_drifted=' "$EV" && /usr/bin/grep -qx 'ladder_mismatches=' "$EV" && /usr/bin/grep -qx 'ladder_uvslot=community-reported' "$EV" && /usr/bin/grep -qx 'ladder_blankcheckbad=community-reported' "$EV" && /usr/bin/grep -qE 'OK: 19 snapshot\(s\)' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than four `rc=0` lines; or any pinned `key=value` line absent — notably `reserved_left=` with an EMPTY value (Phase 178's line read `reserved_left=uv-slot-write-pass`; this phase inverts it), `shape_count=19`, `new_overall=PASS`, `new_write_run_count=2`, `new_target_masked=True` (the frozen shape is not actually a masked slot write), `inherited_drifted=` with an empty value (a shape this commit had no business touching moved), `ladder_mismatches=` with an empty value, or `OK: 19 snapshot(s)`; or the evidence contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*["'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- firestarter/ && git diff HEAD~1 --quiet -- tests/fixtures/plan_shapes.json && git diff HEAD~1 --quiet -- tests/fixtures/rekey_ledger.py && git diff HEAD~1 --quiet -- tests/fixtures/devtest_issue_corpus.json && test "$(git diff HEAD~1 --name-only -- tests/fixtures/reports/ | wc -l)" -eq 2 && git diff HEAD~1 --name-only -- tests/fixtures/reports/ | /usr/bin/grep -q 'uv-slot-write-pass.json' && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' && test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git status --porcelain firestarter/data/chip_database.json)"</automated>
    <fails_when>non-zero exit — the added-comment count is not 0; `firestarter/`, `plan_shapes.json`, `rekey_ledger.py` or `devtest_issue_corpus.json` shows a diff (this commit is fixtures-and-pins only, and the ledger belongs to Task 3); the changed-snapshot count is not exactly 2 or `uv-slot-write-pass.json` is not among them; ruff prints a violation; the mypy line does not read exactly `mypy errors: 35 (watermark: 35)`; or either porcelain check prints a path.</fails_when>
  </verify>
  <done>The corpus carries 19 frozen shapes and 19 matching generated snapshots; `uv-slot-write-pass` is registered across all eight gate-enforced sites and is measured to be a real UV masked slot write reaching `PASS` with `run_count == 2`; `m27c512-full-blank-check-bad` is re-baselined to its measured value with all 17 other hashes byte-unmoved; the ladder pins are re-measured with all four arms still populated; and `RESERVED_SHAPE_IDS` is empty with every read site repaired so none is silently vacuous.</done>
</task>

<task type="auto">
  <name>Task 3: Declare RK-174-04 — the ledger and its MILESTONES row, one separate commit</name>
  <reversibility rating="one-way">Filling `after_hash` publishes the re-key across two repositories. `before_hash` is never overwritten precisely so the original value stays as the evidence that the change was exactly what it declared; undoing the declaration means appending a NEW row, not editing this one.</reversibility>
  <files>firestarter_app/tests/fixtures/rekey_ledger.py, .planning/MILESTONES.md</files>
  <read_first>
    - firestarter_app/tests/fixtures/rekey_ledger.py lines 1-30 (the module contract: plain four-tuples, no dataclass, no computed expression, parsed with `ast.parse` + `ast.literal_eval` by the meta-side checker), 56-70 (`RK-174-04`'s provenance note, which already carries the corrected mechanism and names Phase 179 as owner), 140-165 (the `RK-174-03` / `RK-174-04` / `RK-174-05` rows and the shape of a DECLARED row's third slot)
    - firestarter_app/tests/test_rekey_ledger.py lines 50-90 (`test_every_ledger_row_shape_id_resolves_and_recomputes` and the `after_hash if after_hash is not None else before_hash` rule), 280-400 (the row-count pin, the ascending-id pin, `test_no_declared_row_has_after_hash_equal_to_before_hash` at `:332`, `test_undeclared_after_hash_routes_to_before_hash_and_never_abstains` at `:358`)
    - .planning/MILESTONES.md lines 30-53 (the corrections table, the ledger table with its `RK-174-04-p179-uv-blank-check-abort` row, and the declared-re-key protocol paragraph)
    - /workspaces/tools/rekey/check_rekey_ledger.py (the whole file — it is short; it is the binding gate and it runs from `/workspaces`, never from the sub-repo)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/179-02-shape-registration.txt (the measured `rebaselined_hash` value Task 2 produced — the ONLY source for the value written here)
  </read_first>
  <action>
A SEPARATE commit, touching only the ledger row and its bound `.planning/MILESTONES.md` row. This
separation is the point of `.planning/MILESTONES.md`'s D-11 protocol: the re-key becomes a
reviewable unit, and an executor cannot reflexively repair a red test inside the commit that changed
the behaviour. Note the commit lands in TWO repositories — `tests/fixtures/rekey_ledger.py` inside
`firestarter_app`, and `.planning/MILESTONES.md` in the meta repo — so this is two commits in
mechanical terms and one declaration in reviewable terms; make both, back to back, and do not let
either stand alone.

**1. `tests/fixtures/rekey_ledger.py`.** In the `RK-174-04-p179-uv-blank-check-abort` row, replace
the third slot's `None` with the MEASURED post-change hash — the `rebaselined_hash` value Task 2's
evidence file recorded, which is the same string now sitting in
`FROZEN_HASHES["m27c512-full-blank-check-bad"]`. Do NOT touch the second slot: `077a32d1a5c4` stays
exactly as it is. Do NOT touch any other row, do NOT append a row, and do NOT introduce any
executable expression — the third slot must be a plain string literal reachable by
`ast.literal_eval`.

**2. `RK-174-04`'s provenance note** (`rekey_ledger.py:56-65`). It already states the corrected
mechanism ("the collapse rides the `blank-check` verdict triple moving from OK to BAD") and names
Phase 179 as owner. Extend it — in the module docstring, never a `#` comment — with what Phase 179
actually measured: the triple moves `BAD -> SKIPPED`, not `OK -> BAD`, because the pre-write UV
blank-check now reports a finding rather than a chip fault; and `.planning/research/PITFALLS.md:186-188`
step 2's claim that this step's `error_code` trips `hardware_refused` and aborts cycle 2 is
FALSIFIED — a UV plan's `cycle_block_bounds` is `(3, 6)` and the step sits outside the block, so the
abort came from the WRITE step's own firmware refusal. Record the falsification rather than deleting
the superseded claim, exactly as `RK-174-01`'s note keeps its own falsified `60a031573aab`.

**3. `.planning/MILESTONES.md`.** Update the `RK-174-04-p179-uv-blank-check-abort` row: the `after`
cell takes the same measured hash (replacing `(undeclared)`) and the `declared` cell takes today's
date (replacing `(pending)`). The `before` cell is untouched. Also repair the row's `change` text so
it reads the mechanism Phase 179 measured — the `blank-check` verdict triple moving `BAD -> SKIPPED`
under an execution-time UV adjudication — rather than the `OK -> BAD` direction the row was seeded
with. Edit ONLY those cells and that row; `.planning/MILESTONES.md` is hand-authored and a
whole-file rewrite would destroy the surrounding record.

**4. Do not declare a non-move.** No other row gains an `after_hash` in this commit. Declaring a
non-move is a bookkeeping error (Phase 178 D-09), and `tests/test_rekey_ledger.py` enforces it in
both directions. In particular `RK-174-02` (rejected) and `RK-174-03` (Phase 181's) stay
`(undeclared)`, and `RK-174-09`'s assertion of NO further re-key on `sst27sf512-full-all-ok` must
still hold — this phase moved exactly one shape.

**5. The gate runs from `/workspaces`.** `python3 tools/rekey/check_rekey_ledger.py`, from the meta
repo root. Running it from `firestarter_app/` gives `Errno 2` — it is not under
`firestarter_app/tools/`.
  </action>
  <acceptance_criteria>
    - The `RK-174-04-p179-uv-blank-check-abort` row's third slot is a 12-hex string equal to `FROZEN_HASHES["m27c512-full-blank-check-bad"]`, and its second slot is still exactly `077a32d1a5c4`.
    - `len(LEDGER) == 8`; exactly one row's `after_hash` changed from `None` to a string in this commit; no other row's tuple differs.
    - `tests/test_rekey_ledger.py` passes in full, including `test_every_ledger_row_shape_id_resolves_and_recomputes`, `test_no_declared_row_has_after_hash_equal_to_before_hash` and `test_undeclared_after_hash_routes_to_before_hash_and_never_abstains`.
    - `cd /workspaces && python3 tools/rekey/check_rekey_ledger.py` exits 0 and prints `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`.
    - `.planning/MILESTONES.md`'s `RK-174-04-p179-uv-blank-check-abort` row carries the same 12-hex value in its `after` cell, a real date in its `declared` cell, and its `before` cell still reads `077a32d1a5c4`.
    - `git diff HEAD~1 --name-only` inside `firestarter_app` lists exactly `tests/fixtures/rekey_ledger.py`.
    - The full suite passes with at least 2242 tests (the measured pre-phase baseline was `2241 passed, 32 snapshots passed`) and reports 32 syrupy snapshots passed (the `tests/fixtures/reports/` JSON files are NOT syrupy snapshots — that count is unchanged by this phase).
    - The commit adds zero lines matching `^\+[[:space:]]*#`; ruff check and format are green; the mypy watermark still reads exactly 35.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence && mkdir -p "$E" && EV=$E/179-02-rekey-declaration.txt && { echo "== 1 the declared row, measured against the frozen value"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from tests.fixtures.rekey_ledger import LEDGER
from tests.fixtures.report_shapes import FROZEN_HASHES
row = next(r for r in LEDGER if r[3] == 'RK-174-04-p179-uv-blank-check-abort')
print('ledger_rows=' + str(len(LEDGER)))
print('rk04_shape=' + row[0])
print('rk04_before=' + row[1])
print('rk04_after=' + str(row[2]))
print('rk04_after_matches_frozen=' + str(row[2] == FROZEN_HASHES['m27c512-full-blank-check-bad']))
print('rk04_before_untouched=' + str(row[1] == '077a32d1a5c4'))
print('rk04_after_differs_from_before=' + str(row[2] != row[1]))
print('other_declared=' + ','.join(sorted(r[3] for r in LEDGER if r[2] is not None and r[3] != 'RK-174-04-p179-uv-blank-check-abort')))
"); echo "rc=$?"; echo "== 2 the cross-tree binding gate"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; echo "== 3 the ledger tests"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_rekey_ledger.py tests/test_blast_radius_invariance.py); echo "rc=$?"; echo "== 4 the MILESTONES row"; /usr/bin/grep -n 'RK-174-04-p179-uv-blank-check-abort' .planning/MILESTONES.md | head -5; echo "rc=$?"; echo "== 5 the whole suite, green again"; (cd firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 5 && /usr/bin/grep -qx 'ledger_rows=8' "$EV" && /usr/bin/grep -qx 'rk04_shape=m27c512-full-blank-check-bad' "$EV" && /usr/bin/grep -qx 'rk04_before=077a32d1a5c4' "$EV" && /usr/bin/grep -qE '^rk04_after=[0-9a-f]{12}$' "$EV" && /usr/bin/grep -qx 'rk04_after_matches_frozen=True' "$EV" && /usr/bin/grep -qx 'rk04_before_untouched=True' "$EV" && /usr/bin/grep -qx 'rk04_after_differs_from_before=True' "$EV" && /usr/bin/grep -qx 'other_declared=RK-174-01-p177-readback-gating,RK-174-05-p177-match-bucket-d4d6,RK-174-06-p178-status-axis-must-not-rekey,RK-174-07-p177-w27e257-all-ok-synthesized-match' "$EV" && /usr/bin/grep -qF 'OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound' "$EV" && /usr/bin/grep -qE '^32 snapshots passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV" && test "$(/usr/bin/grep -oE '^[0-9]+ passed' "$EV" | tail -1 | cut -d' ' -f1)" -ge 2242</automated>
    <fails_when>fewer than five `rc=0` lines; or `rk04_after` is not a 12-hex string (the row is still `None`, i.e. undeclared); or `rk04_after_matches_frozen` is not `True` (the ledger and `FROZEN_HASHES` disagree, which is the exact drift the two-commit protocol exists to catch); or `rk04_before` is not `077a32d1a5c4` (a `before` cell was overwritten); or `other_declared` names a row this phase should not have touched; or the binding line `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound` is absent; or the suite's `N passed` count is below 2242 or `32 snapshots passed` is absent; or the evidence contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 --name-only | tr -d '[:space:]')" = "tests/fixtures/rekey_ledger.py" && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*["'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' && test -z "$(git -C /workspaces/firestarter status --porcelain)"</automated>
    <fails_when>non-zero exit — the declaration commit inside `firestarter_app` touched any file other than `tests/fixtures/rekey_ledger.py` (the D-11 protocol requires it to stand alone), the added-comment count is not 0, ruff prints a violation, the mypy line does not read exactly `mypy errors: 35 (watermark: 35)`, or the firmware submodule is not porcelain-clean.</fails_when>
  </verify>
  <done>`RK-174-04-p179-uv-blank-check-abort` is declared in both repositories with the measured value, its `before_hash` untouched, no other row declared, the cross-tree checker printing `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`, and the full suite green again at 2242 or more tests with 32 snapshots.</done>
</task>

</tasks>

<threat_model>
ASVS level 1, blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository to the public dedup namespace | A `dedup_fingerprint` frozen here becomes the 12-hex identity embedded in filed GitHub issue titles, which `tools/parse_devtest_issue.py` READS and never re-computes. |
| firestarter_app submodule to the meta repository | The re-key declaration must be true on both sides simultaneously; `tools/rekey/check_rekey_ledger.py` is the only thing that checks it. |
| the frozen corpus to every future phase | 19 hashes and 19 byte-compared snapshots become the blast-radius oracle Phases 180 and 181 are measured against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-179-06 | Tampering | `FROZEN_HASHES` / `tests/fixtures/reports/` | high | mitigate | Every frozen value is MEASURED in `.venv311` off the committed builder and cross-checked against a fresh `build_shape` in the same evidence run; no hash is transcribed from a planning document, from `179-RESEARCH.md`'s MEDIUM-confidence projection table, or from this plan's own pre-measured `927571e5110f`. Snapshots are GENERATED by `tools/snapshot_report_shapes.py`, whose `--check` gate compares against a fresh regeneration, so a hand-written file is red by construction. |
| T-179-07 | Repudiation | `tests/fixtures/rekey_ledger.py` | high | mitigate | `before_hash` is never overwritten, so the original value remains the evidence that the change was exactly what it declared and nothing more. The declaration is a SEPARATE commit touching only the row and its `MILESTONES.md` counterpart, so an executor cannot repair a red gate inside the commit that reddened it. `tools/rekey/check_rekey_ledger.py` binds both sides in both directions and is asserted green. |
| T-179-08 | Denial of information | `RESERVED_SHAPE_IDS` emptying | high | mitigate | `test_build_shape_raises_for_every_reserved_shape_id` iterates the set and would pass over zero iterations — a pre-authored gate leg that has become UNREACHABLE, a failure family this project has recorded before. Task 2 repairs it to assert the drawn-down namespace explicitly and to keep the `KeyError` property alive with an unregistered sentinel probe, and censuses every other read site. |
| T-179-09 | Tampering | `.planning/MILESTONES.md` | medium | mitigate | Only the `RK-174-04` row's `change`, `after` and `declared` cells are edited, by scoped edit and never by a whole-file rewrite — the file is hand-authored and carries the entire milestone record. The binding checker reads both sides and fails on either an orphan row or an undeclared one. |
| T-179-10 | Information disclosure | `tests/fixtures/reports/uv-slot-write-pass.json` | low | accept | The generated snapshot is a `to_dict()` of a synthetic report built from a database chip name and an in-memory double; it contains no host path, no serial port and no username. `submit.sanitize_dict` is the scrubbing boundary for anything actually filed, and this file is never filed. |
| T-179-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. `179-RESEARCH.md`'s Environment Availability table records zero missing dependencies with no fallback needed, and `pyproject.toml` is asserted byte-unchanged across the phase. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q` — `>= 2242 passed`, `32 snapshots passed`, no failures.
2. `./.venv311/bin/python tools/snapshot_report_shapes.py --check` — `OK: 19 snapshot(s) … match a fresh regeneration`.
3. `cd /workspaces && python3 tools/rekey/check_rekey_ledger.py` — exit 0, `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`, exactly one row newly declared.
4. `./.venv311/bin/python -m ruff check firestarter/ tests/`, `ruff format --check`, `tools/check_mypy_watermark.py` (35/35), `tools/check_devtest_orchestrator.py`, `tools/check_diagnostic_report_claims.py` — all exit 0.
5. `git diff --quiet -- firestarter/` across both of this plan's `firestarter_app` commits — no production file was touched.
6. Zero `#` comment lines added; `/workspaces/firestarter` porcelain empty; `chip_database.json` porcelain empty.
</verification>

<success_criteria>
- The corpus carries a frozen shape that exercises a real UV masked slot write reaching `PASS` with `run_count == 2` — the machine-checked form of ROADMAP criteria 1 and 2 for the host path.
- The blast radius this phase incurred is declared, bound across two repositories, and measured to be exactly one shape.
- `RESERVED_SHAPE_IDS` is drawn down to empty without a single gate leg silently going vacuous.
- The tree is green again, at a suite count strictly above the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-02-SUMMARY.md` when done.
</output>
