---
phase: 179-uv-slot-writes-flag-skip-blank-check-hardware-gated
plan: 04
type: execute
wave: 4
depends_on: ["179-03"]
files_modified:
  - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/
  - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/
autonomous: false
requirements: [UV-01, UV-02, UV-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ROADMAP criterion 1, hardware half: a PHYSICAL UV part holding data outside the target slot accepts a write to that slot without being refused, recorded verbatim in `179-MEASUREMENT.md` from a real `firestarter dev test m27c512` run against a real board."
    - "ROADMAP criterion 2, hardware half: that same physical run's issue title reads `[dev test] m27c512 — PASS (<12 hex>)` and its write and verify steps read `run_count == 2`."
    - "Chip seating and removal are OPERATOR-ONLY. Claude may drive the serial port, run the CLI and read the output; it may not handle a chip. The bench step is a `blocking-human` checkpoint and NEVER an autonomous task."
    - "This gate is NEVER auto-approvable, and it is defended THREE ways rather than one. (a) It is `type=\"checkpoint:human-action\"`, not `checkpoint:human-verify`: the bench step is not post-hoc verification of work Claude did, it is a step Claude LITERALLY CANNOT DO, and `human-action` is the one checkpoint type that still stops in auto mode. (b) It carries `gate=\"blocking-human\"`, which is never bypassed in any mode. (c) It is defended by MACHINE — `179-MEASUREMENT.md` must carry a measured 12-hex hash, a measured firmware/`controller:` line, a measured slot address and a measured exit code, none of which an auto-approval can fabricate. The type matters for a second reason: `workflow.human_verify_mode` resolves to `end-of-phase` on this project, which SUPPRESSES planner-emitted `checkpoint:human-verify` tasks into an end-of-phase UAT batch — and this checkpoint's result is what Task 2's seal BRANCHES on, so deferring it past the seal would invert the phase. `checkpoint:human-action` is unaffected by that mode."
    - "A UV write is PHYSICALLY IRREVERSIBLE and the operator has NO UV eraser. Slot budget is stated up front and bounded: a successful `dev test` run permanently consumes exactly ONE 256-byte slot of the part's 256, and a REFUSED write consumes zero (the firmware refuses at write-init before programming any byte). The bench wave budgets at most TWO slots — one confirmation run and one spare."
    - "The bench part is `m27c512` — the CLI token, not the human label \"ST M27C512\", which does not resolve. `am27c020` is explicitly NOT a fallback: its 0x08 write path is recorded marginal on this bench (`project_v118_phase99_bench_defer`, `project_phase83_shipped`) and a failure there would be uninterpretable between chip, bench and this phase's change."
    - "An electrically-erasable proxy CANNOT stand in. The `uv-slot` policy is a host/database decision keyed on `electrical-type == \"UV-EPROM\"` (`chip_test.py:519`); a W27C512 or W27E257 is `EEPROM`, takes `REGION_POLICY_FIXED`, never resolves a masked target and never produces a witness. A genuine UV part is mandatory for criterion 1."
    - "`--fast` is FORBIDDEN on the bench run. It runs one write/verify cycle instead of two, and UV-02's whole claim is `run_count == 2`."
    - "The artifact carries exactly one sentinel line, `BENCH RESULT: PASS` or `BENCH RESULT: UNAVAILABLE`, and the phase seal BRANCHES on it. A `PASS` sentinel marks UV-01, UV-02 and UV-03 Complete. An `UNAVAILABLE` sentinel marks UV-03 Complete (it is fully proved by `179-03`'s committed disagreement legs and needs no hardware) and leaves UV-01 and UV-02 as `Blocked (hardware)` with their checkboxes unticked and a one-line note naming the artifact. Sealing green on an unmet hardware criterion is forbidden."
    - "Serial port identity is verified PER TASK, not once: `ttyACM*` numbers shuffle across replug, so the artifact records the `Current firmware version: …, for controller: …, on port …` line verbatim from `firestarter -p <port> fw` immediately before the run, and the same port is the one `dev test` is given."
    - "`firestarter fw --install` is NOT run. This phase requires no firmware change — `firestarter/src/proms/eprom.cpp:143-145` already honours `FLAG_SKIP_BLANK_CHECK` — and `--install` is recorded to flash the ATTACHED board regardless of `--board`."
    - "Phase close: every gate is green in both repositories — the full suite at 2254 or above with 32 syrupy snapshots, `snapshot_report_shapes.py --check` at 19, `check_devtest_orchestrator.py`, `check_diagnostic_report_claims.py`, `check_rekey_ledger.py` (8 rows bound, run from `/workspaces`), ruff check, ruff format, the mypy watermark at exactly 35 — plus `firmware_diff=clean`, `chip_database_diff=clean`, `app_tree=clean` and `phase_added_comments=0` across every commit this phase made."
    - "Two prior planning claims this phase FALSIFIED are recorded rather than quietly corrected: `.planning/research/PITFALLS.md:186-188` step 2 (the standalone blank-check's `error_code` tripping `hardware_refused`) and `.planning/research/SUMMARY.md:89` (the `current_source == \"probe read\"` witness form). Both are repaired in place with the measurement that falsified them, following Phase 178's `835baba` precedent."
    - "Three residuals are FILED as todos rather than left implicit: the `build_db_diff` ladder flip on an all-slots-exhausted UV run (T-179-05, measured and accepted), the Q5 disclosure key deferred to Phase 181 (RESEARCH assumption A5), and the Q8 stale-comment blocker."
    - "Q8 DECIDED and recorded, not silently dropped: the stale UV-prompt design-history block at `firestarter_app/firestarter/cli_handlers.py` (the paragraph beginning `# ALWAYS WRITES:`) is NOT edited by this phase. MEASURED: the stale sentences start mid-line and end mid-line, so removing them requires REWRITING two retained comment lines — and the standing operator rule is that no comment is written into source, ever, by any plan. The only zero-comment-added alternative is deleting the whole paragraph, which would also delete still-accurate prose about the AT28C family, the full-device write and report persistence. The finding is appended to the todo's `blocked_by` field so the todo stops reading as merely un-actioned."
    - "Environment: every Python command runs through `firestarter_app/.venv311/bin/python`, `tools/rekey/check_rekey_ledger.py` runs with `python3` FROM `/workspaces`, every evidence-grade scan uses `/usr/bin/grep`, and every count-bearing pytest run passes `-o addopts=\"\"`."
  prohibitions:
    - "No autonomous bench step. Chip insertion, removal and any physical handling are OPERATOR-ONLY, without exception."
    - "The bench checkpoint is never auto-approved, and no `--auto` / `--chain` invocation may satisfy it. If the checkpoint's machine defence cannot be met from real measured output, the correct outcome is `BENCH RESULT: UNAVAILABLE`, not a fabricated record."
    - "`--fast` is never used on the bench run — a one-cycle run cannot demonstrate `run_count == 2` and its report is explicitly excluded from community agreement."
    - "`firestarter fw --install` is never run in this phase, and no firmware file is modified."
    - "`am27c020` is never substituted for `m27c512` as the bench part, and no electrically-erasable part is offered as a proxy."
    - "No more than TWO UV slots are spent. If the first run does not produce a `PASS`, STOP and record the failure rather than retrying into the part's remaining life."
    - "No `#` comment is written into any source file. `firestarter_app/firestarter/cli_handlers.py` is NOT modified by this phase — see the Q8 truth above."
    - "No source or test file is modified by this plan at all. It is bench-record and `.planning/` work only."
    - "`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are edited by SCOPED edit only, never by a whole-file rewrite or by a `gsd` verb that reformats the whole file — both are hand-authored, `roadmap.update-plan-progress` is recorded to clobber the dependency table, and the requirements/roadmap verbs are recorded to reformat the whole file."
    - "A requirement is never marked Complete on the strength of a host-only proof when its criterion names hardware. The sentinel branch is the mechanism, and it fails closed toward `Blocked (hardware)`."
    - "`.planning/MILESTONES.md` is not re-touched by this plan — `179-02` owns the `RK-174-04` row and its declaration stands."
  artifacts:
    - path: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md"
      provides: "The committed bench record: the port identity and firmware version, the exact command, the full step table, the title line, the exit code, the slot address and slots-remaining, the slot budget spent, and the single `BENCH RESULT:` sentinel"
      contains: "BENCH RESULT:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UV-01, UV-02 and UV-03 marked per the bench sentinel — Complete on `PASS`, or UV-03 Complete with UV-01/UV-02 `Blocked (hardware)` on `UNAVAILABLE`"
      contains: "UV-03"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 179's four plan checkboxes ticked and its `**Plans:**` line finalised"
      contains: "179-04-PLAN.md"
    - path: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/179-04-phase-seal.txt"
      provides: "The measured phase seal — every gate in both repositories, the comment count across every commit this phase made, and both submodules porcelain"
      contains: "phase_added_comments=0"
  key_links:
    - from: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the single BENCH RESULT sentinel decides whether UV-01 and UV-02 are marked Complete or Blocked (hardware) -- a machine-checked branch, never a judgement call at seal time"
      pattern: "BENCH RESULT: (PASS|UNAVAILABLE)"
    - from: "firestarter_app/tests/test_chip_test_uv_slot_write.py"
      to: ".planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md"
      via: "D-179-1 Option A's two halves -- the committed test proves the host claim, the bench artifact proves the hardware claim, and criterion 4 needs both"
      pattern: "test_uv_slot_write_on_a_non_blank_part_reaches_pass_with_run_count_two"
---

<objective>
Close the phase honestly: put a real UV EPROM in a real socket, run the real command, and record
what actually happened — then seal only what was actually proved.

The bench wave is a `blocking-human` checkpoint because a chip cannot be seated by an agent, and
because a UV write is physically irreversible on a part the operator has no eraser for. It is
defended by machine as well as by policy: the artifact must carry a measured hash, a measured
firmware version, a measured controller identity and a measured slot address, so an auto-approving
`--auto` run cannot satisfy it by asserting success.

The seal then branches on one sentinel line. `BENCH RESULT: PASS` marks UV-01, UV-02 and UV-03
Complete. `BENCH RESULT: UNAVAILABLE` marks UV-03 Complete — it is fully proved by `179-03`'s
committed disagreement legs and needs no hardware — and leaves UV-01 and UV-02 `Blocked (hardware)`
with their boxes unticked. A phase that could not reach the bench says so.

Purpose: `D-179-1` Option A splits criterion 4 across two artifacts and its honest cost is that
neither alone is the criterion. This plan is the half that cannot be automated, plus the seal that
refuses to let the automatable half stand in for it.

Output: a committed bench measurement, three requirements marked exactly as far as the evidence
reaches, three residuals filed as todos, two falsified prior claims repaired in place, and every
gate green in both repositories.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-RESEARCH.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-01-SUMMARY.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-02-SUMMARY.md
@.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-03-SUMMARY.md
@firestarter_app/CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by phase 179 and does not exist at `firestarter_app @ 835baba`.

| Artifact | Kind | Home | Plan |
|---|---|---|---|
| `WriteTarget.current_is_probe_read` | dataclass field, `bool = False` | `firestarter/chip_test.py` | 179-01 |
| `Step.uv_prewrite` | dataclass field, `bool = False` | `firestarter/chip_test.py` | 179-01 |
| `_is_monotonic_masked_target` | module function `-> bool` | `firestarter/chip_test.py` | 179-01 |
| `WriteInitPreflightChip` | test double class | `tests/fake_chip.py` | 179-01 |
| `uv-slot-write-pass` | frozen `shape_id` | `tests/fixtures/report_shapes.py` | 179-02 |
| `_build_uv_slot_write_pass` | builder function | `tests/fixtures/report_shapes.py` | 179-02 |
| `tests/fixtures/reports/uv-slot-write-pass.json` | new file (generated) | `tests/fixtures/reports/` | 179-02 |
| `RK-174-04-p179-uv-blank-check-abort` `after_hash` | declared ledger value | `tests/fixtures/rekey_ledger.py` | 179-02 |
| `tests/test_chip_test_uv_slot_write.py` | new test module (12 legs) | `firestarter_app/tests/` | 179-03 |
| `179-DECISIONS.md` (`D-179-1`, `D-179-2`) | record | `.planning/phases/179-…/` | 179-02 |
| `179-MEASUREMENT.md` | record (bench artifact) | `.planning/phases/179-…/` | 179-04 |
| `BENCH RESULT:` sentinel | machine-read record line | `179-MEASUREMENT.md` | 179-04 |

New test functions created by THIS plan: none — this plan writes no source and no test.

New evidence files: `179-04-phase-seal.txt`, under
`.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence/`.

## Spec-less probe arithmetic (no silent drops)

The four probe-surfaced edge rows are accounted for in `179-01-PLAN.md`: **4 surfaced == 1 authored
into `must_haves` + 3 surfaced as flagged assumptions.** This plan authors none and re-flags none;
Task 2 carries all three flagged assumptions forward into the phase record so they leave the phase
visible rather than expiring with the plan set.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: The bench wave — one real UV part, one real slot, one committed record</name>
  <files>.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md</files>
  <action>Seat a real ST M27C512 UV EPROM that holds data OUTSIDE the top 256-byte slot, confirm the shield revision and the serial port out loud, and remove the chip when the run is finished. Claude drives the port, runs `firestarter -p <port> fw` and `firestarter -p <port> dev test m27c512`, reads the output and writes `179-MEASUREMENT.md`; the ONE thing requiring a human is the physical handling of the chip, which no CLI can do. A UV write is physically irreversible and the operator has NO UV eraser — every successful run permanently consumes one of the part's ~256 256-byte slots, the budget is two, and `D-179-1` (recorded in `179-02`) is the decision that authorised spending them.</action>
  <read_first>
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-RESEARCH.md (the `## Bench hardware` section in full: the four candidate parts with their protocol / memory / slot / VPP / chip-id, why `m27c512` is the recommended part, why `am27c020` is not, why an erasable proxy cannot stand in, and assumption A1)
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-DECISIONS.md (`D-179-1` — this task is Option A's hardware half)
    - .planning/phases/176-*/176-MEASUREMENT.md (the structural precedent for a committed bench measurement artifact — copy its shape, not its content)
    - firestarter_app/firestarter/cli_handlers.py lines 380-392 (the top-level `-p` / `--port` option this run must pass explicitly) and the `dev test` command's own option list around line 2365 (to confirm `--fast` is the one-cycle weakening this run must NOT use)
    - firestarter_app/firestarter/firmware.py line 223 (the `Current firmware version: …, for controller: … on port …` line whose verbatim text is the port-identity record)
  </read_first>
  <instructions>
**BEFORE ANYTHING IS PLUGGED IN — three questions the operator must answer, because RESEARCH could
only bound them (assumption A1) and getting them wrong spends part of an irreplaceable chip:**

1. **Is the ST M27C512 on hand, and is it in a usable state?** It must hold data OUTSIDE the top
   256-byte slot at `0xFF00-0xFFFF`, and it must have at least one slot that is not saturated. Phase
   83 recorded a bench PASS on this part with a 16-byte write at `0x0000` — that is data outside the
   top slot, and it is exactly UV-01's precondition. **The CLI token is `m27c512`; the human label
   "ST M27C512" does not resolve.**
2. **Which shield revision is in the rig, and which serial port is the board on?** `hw_revision`
   cannot distinguish Rev 2.2 from Rev 2.0 from the modified Rev 0, so this must be asked and never
   inferred, and `ttyACM*` numbers shuffle across replug so the port must be confirmed for THIS
   session.
3. **Is spending up to two 256-byte slots acceptable?** A successful `dev test` run permanently
   consumes exactly ONE slot of the part's 256, and there is NO UV eraser on this bench. A REFUSED
   write consumes zero, because the firmware refuses at write-init before programming any byte. The
   budget is one confirmation run plus one spare; if the first run does not produce a `PASS`, STOP
   and record the failure rather than retrying into the part's remaining life.

**If the answer to (1) is no — the part is missing, saturated, or its state is unknown — STOP.** Do
not substitute `am27c020`: its 0x08 write path is recorded marginal on this bench and a failure
there would be uninterpretable between chip, bench and this phase's change. Do not substitute an
electrically-erasable part: a W27C512 or W27E257 is `EEPROM`, takes `REGION_POLICY_FIXED`, never
resolves a masked target and never produces a witness, so the host path under test is unreachable
from it. Write `179-MEASUREMENT.md` with `BENCH RESULT: UNAVAILABLE` and the operator's stated
reason, and let Task 2's seal branch mark UV-01 and UV-02 `Blocked (hardware)`.

**OPERATOR ACTIONS (Claude cannot do these):**
- Seat the M27C512 in the ZIF socket, pin 1 oriented correctly, with the board unpowered.
- Confirm the shield revision out loud.
- Remove the chip when the run is finished.

**CLAUDE ACTIONS (permitted — the serial port and the CLI are Claude's to drive):**
1. Confirm port identity FIRST: `firestarter -p <port> fw` and record the
   `Current firmware version: …, for controller: … on port …` line VERBATIM. Do NOT run
   `fw --install` — this phase requires no firmware change, and `--install` is recorded to flash the
   attached board regardless of `--board`.
2. Read the part before writing anything, so the artifact records the pre-run state and confirms the
   part is not blank: a `firestarter -p <port> read m27c512` and the resulting file's non-`0xFF`
   content outside `0xFF00`.
3. Run the test, exactly once, with two cycles: `firestarter -p <port> dev test m27c512`.
   **`--fast` is FORBIDDEN** — it runs one write/verify cycle and UV-02's whole claim is
   `run_count == 2`. Decline the filing prompt; the artifact is the record, not a GitHub issue.
4. Record the exit code with `echo "exit=$?"` immediately after.

**WHAT `179-MEASUREMENT.md` MUST CONTAIN** — every line measured, none transcribed from this plan:
- The port-identity line verbatim, including `controller:` and the firmware version.
- The shield revision as stated by the operator.
- The exact command line run, on its OWN line prefixed `Command: ` — the gate reads `--fast` only
  from lines starting with that prefix, so prose elsewhere in the file cannot invalidate it — and the
  exit code.
- The pre-run evidence that the part was NOT blank outside the target slot.
- The full step table as printed: `id`, `read`, `blank-check`, `write`, `verify` with each verdict,
  each `run_count`, and the `blank-check` step's reason and `error_code`.
- The title line, in the form `[dev test] m27c512 — PASS (<12 hex>)`.
- The write-coverage line naming the slot address written and `slots_remaining` / `slots_total`.
- The slot budget: how many slots were spent, and how many the part has left.
- **Exactly one sentinel line: `BENCH RESULT: PASS` or `BENCH RESULT: UNAVAILABLE`.**
- If `UNAVAILABLE`, the operator's stated reason on the following line.

**WHAT PASSES:** `BENCH RESULT: PASS` with a title reading `— PASS (`, the write and verify steps at
`run_count` 2, the blank-check step at `SKIPPED` with its reason and `error_code` preserved, and
exit code 0.

**WHAT FAILS:** any run whose write step is refused (`Not blank`), whose `run_count` is 1, or whose
title reads `FAIL` or `INCONCLUSIVE`. Record it exactly as it happened with
`BENCH RESULT: UNAVAILABLE` and the observed failure as the reason — do NOT retry into the part's
remaining slots, and do NOT edit the record to look better than the run was.

**THIS GATE IS NEVER AUTO-APPROVABLE.** `--auto` and `--chain` are recorded to auto-approve
`checkpoint:human-verify` gates, and `autonomous: false` is not self-protecting. The verify below is
the second line of defence: it requires a measured 12-hex hash, a measured firmware/controller line,
a measured slot address and a measured exit code, none of which an auto-approval can produce.
  </instructions>
  <verification>Claude can check, afterward and without touching the chip: that `firestarter -p <port> fw` still reports the same `controller:` and port it reported before the run (the board did not shuffle mid-session); that `179-MEASUREMENT.md` carries exactly one `BENCH RESULT:` sentinel with every measured line the acceptance criteria name; that the recorded command contains no `--fast`; that at most two slots are recorded as spent; and that `git -C /workspaces/firestarter status --porcelain` is empty, proving no firmware was flashed.</verification>
  <resume-signal>`.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md` exists and carries exactly one `BENCH RESULT: PASS` or `BENCH RESULT: UNAVAILABLE` sentinel, backed by the measured lines the acceptance criteria name. Type "done" once the chip is out of the socket. Resume at Task 2.</resume-signal>
  <acceptance_criteria>
    - `179-MEASUREMENT.md` exists, is non-empty, and contains exactly ONE line matching `^BENCH RESULT: (PASS|UNAVAILABLE)$`.
    - No line in the file is exactly `TBD`, `TODO`, `N/A` or `undecided`.
    - The file contains a line matching `Current firmware version: .*controller: .*port` — the verbatim port-identity record.
    - The file names the shield revision, and carries the exact command on a line matching `^Command: .*dev test m27c512`; no `^Command: ` line contains `--fast`.
    - If the sentinel is `PASS`: the file contains a line matching `\[dev test\] m27c512 — PASS \([0-9a-f]{12}\)`, a recorded `run_count` of 2 for both the write and the verify step, a `blank-check` step recorded as `SKIPPED` with a non-empty reason, a slot address matching `0x[0-9a-fA-F]{4,6}`, a `slots_remaining` figure, and a recorded exit code of 0.
    - If the sentinel is `UNAVAILABLE`: the file contains a `Reason:` line with a non-empty operator-stated reason.
    - The file records the slot budget spent, and the number spent is at most 2.
    - No firmware file was flashed and no firmware file was modified: `git -C /workspaces/firestarter status --porcelain` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && M=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md && test -s "$M" && test "$(/usr/bin/grep -cE '^BENCH RESULT: (PASS|UNAVAILABLE)$' "$M")" -eq 1 && ! /usr/bin/grep -qiE '^\s*(TBD|TODO|N/A|undecided)\s*$' "$M" && /usr/bin/grep -qE 'Current firmware version: .*controller: .*port' "$M" && /usr/bin/grep -qE '^Command: .*dev test m27c512' "$M" && ! /usr/bin/grep -E '^Command: ' "$M" | /usr/bin/grep -q -- '--fast' && { if /usr/bin/grep -qx 'BENCH RESULT: PASS' "$M"; then /usr/bin/grep -qE '\[dev test\] m27c512 — PASS \([0-9a-f]{12}\)' "$M" && /usr/bin/grep -qE '0x[0-9a-fA-F]{4,6}' "$M" && /usr/bin/grep -qiE 'slots_remaining' "$M" && /usr/bin/grep -qiE 'blank-check.*SKIPPED' "$M" && test "$(/usr/bin/grep -ciE '^\|?\s*(write|verify)\b.*\b2\b' "$M")" -ge 2 && /usr/bin/grep -qiE 'exit(_code)?[ =:]+0' "$M"; else /usr/bin/grep -qE '^Reason: .+' "$M"; fi; } && test -z "$(git -C /workspaces/firestarter status --porcelain)"</automated>
    <fails_when>non-zero exit — the artifact is missing or empty; it carries zero or more than one `BENCH RESULT:` sentinel; a line reads exactly TBD / TODO / N/A / undecided; the verbatim `Current firmware version: … controller: … port` line is absent (the port identity was never confirmed for this session, or the record was fabricated); no `^Command: ` line records the `dev test m27c512` invocation, or a `^Command: ` line contains `--fast`; a `PASS` sentinel is not backed by a `— PASS (<12 hex>)` title line, a slot address, a `slots_remaining` figure, a `SKIPPED` blank-check row, two rows recording `run_count` 2, and a recorded exit code 0; an `UNAVAILABLE` sentinel carries no non-empty `Reason:` line; or the firmware submodule is not porcelain-clean.</fails_when>
  </verify>
  <done>A committed bench record exists that states exactly what happened on real hardware — either a measured `PASS` with `run_count == 2` on a physical UV part holding data outside the target slot, or a measured `UNAVAILABLE` with the operator's reason — with at most two of the part's slots spent and nothing in the record that was not observed.</done>
</task>

<task type="auto">
  <name>Task 2: Seal the phase — the sentinel branch, the residuals, the falsified claims, and every gate in both repos</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/todos/pending/, .planning/research/PITFALLS.md, .planning/research/SUMMARY.md</files>
  <read_first>
    - .planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md (the `BENCH RESULT:` sentinel that decides the requirement marking)
    - .planning/REQUIREMENTS.md lines 68-82 (the `### UV Slot Writes` section with UV-01/02/03 verbatim) and lines 165-180 (the requirement/phase/status table, including the three `| UV-0N | Phase 179 | Pending |` rows)
    - .planning/ROADMAP.md lines 401-416 (Phase 179's Goal, Depends on, Requirements, Hardware-gated note, four Success Criteria and its `**Plans**: TBD` line — and Phase 178's block immediately above at lines 372-400 for the exact `**Plans:** N plans` / `Plans:` / `**Wave N**` / `- [x] …` format to match)
    - .planning/research/PITFALLS.md lines 164-247 (Pitfalls 5, 6 and 7 — Pitfall 5 step 2 is the falsified claim, Pitfall 7 is Q5's disclosure argument)
    - .planning/research/SUMMARY.md lines 85-95 and 130-140 (the falsified witness form at `:89`, and the milestone recommendation Q1 Option B follows)
    - .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md (the Q8 todo whose `blocked_by` field this task extends)
    - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-04-SUMMARY.md (the close shape to copy — what a sealed phase's record states and how the falsified-claim repairs were worded at `835baba`)
  </read_first>
  <action>
Seal the phase. Every edit below is a SCOPED edit to a hand-authored `.planning/` file — never a
whole-file rewrite, and never a `gsd` verb that reformats the file. `roadmap.update-plan-progress`
is recorded to clobber the dependency table and the requirements/roadmap verbs are recorded to
reformat whole files; hand-edit instead.

**1. Read the sentinel and BRANCH.** Read the single `BENCH RESULT:` line from `179-MEASUREMENT.md`.

  - **`BENCH RESULT: PASS`** — in `.planning/REQUIREMENTS.md`, tick UV-01, UV-02 and UV-03 to
    `- [x]` and set all three status-table rows to `Complete`.
  - **`BENCH RESULT: UNAVAILABLE`** — tick UV-03 only, and set its status row to `Complete`: it is
    fully proved by `179-03`'s committed disagreement legs and needs no hardware. Leave UV-01 and
    UV-02 as `- [ ]`, set their status rows to `Blocked (hardware)`, and append to each row a
    one-line note naming `179-MEASUREMENT.md` and the operator's stated reason. Do NOT mark them
    Complete on the strength of the host-only proof; criterion 1 and criterion 2 both name a
    physical part, and `D-179-1` Option A's honest cost is exactly that neither half stands in for
    the other.

**2. `.planning/ROADMAP.md`, Phase 179.** Replace `**Plans**: TBD` with `**Plans:** 4 plans`, then
add the `Plans:` block in the wave format Phase 178's section uses immediately above — `**Wave 1**`
through `**Wave 4**`, one `- [x] 179-NN-PLAN.md — <one-line objective>` per plan. Note this phase is
deliberately SERIAL: `179-01` leaves one frozen gate red by protocol and `179-02` is the only thing
entitled to turn it green, so no two plans could safely share a wave. Do NOT touch any other phase's
section — `phase.complete` is recorded to clobber an unrelated phase's `**Plans:**` line, so
snapshot the file and diff it after editing.

**3. Repair the two falsified prior claims IN PLACE**, following Phase 178's `835baba` precedent —
record the falsification and the measurement beside the superseded claim rather than deleting it.
  - `.planning/research/PITFALLS.md:186-188` (Pitfall 5, step 2): the claim that the standalone
    blank-check's `error_code` trips `hardware_refused` and aborts cycle 2 is FALSIFIED. Measured:
    for a UV plan the blank-check sits at index 2, OUTSIDE a cycle block that is `(3, 6)` for
    `m27c512`, `am27c020` and `tms27c512`, and `run_plan`'s per-step path has no `hardware_refused`
    mechanism at all. The abort came from the WRITE step's own firmware refusal. The consequence for
    the plan was unchanged — both defects still had to be fixed — but the mechanism named was wrong.
  - `.planning/research/SUMMARY.md:89`: the prescribed witness form
    `target.current_source == "probe read"` is FALSIFIED. Measured: the staged tranche targets that
    actually reach `write_eprom` carry `"probe read (tranche 1/2)"` (`chip_test.py:1518`), so the
    equality never matches on a real run and a witness written that way would have shipped green and
    inert. Phase 179 uses a structural boolean (`WriteTarget.current_is_probe_read`) instead, and
    `tests/test_chip_test_uv_slot_write.py::test_the_prescribed_probe_read_string_equality_would_never_match`
    pins the falsification so a return to the string form goes red.

**4. File three residuals as todos in `.planning/todos/pending/`**, each with the standard
front-matter (`title`, `date`, `priority`, `blocked_by`, `resolves_phase`). Two are NEW files and
their slugs are fixed, because the seal gate greps for them by name — use exactly
`<YYYY-MM-DD>-uv-ladder-flip-on-exhausted-slots.md` and
`<YYYY-MM-DD>-uv-write-shortcut-disclosure-key.md`; the third is an edit to an existing todo:
  - **The ladder flip (T-179-05).** MEASURED: after this phase, a UV run whose slots are all
    exhausted reads `blank-check SKIPPED / write SKIPPED / verify SKIPPED` and `build_db_diff`'s
    fourth arm proposes `candidate for community-reported` where the `BAD` blank-check previously
    landed it on `community-fail`. The flip is FORCED by UV-02 — any verdict clearing the FAIL fold
    also clears the ladder's BAD arm, identically for `NA` and `SKIPPED`. The underlying hole (a
    `SKIPPED` write still proposing graduation) is PRE-EXISTING and reachable today on any non-UV
    refused write. Disposition accepted for this phase; the todo names `build_db_diff`
    (`diagnostic_report.py:403-405`) as the site and states that the honest fix is a "no write
    actually ran" guard, not a UV special case.
  - **Q5 / RESEARCH assumption A5 — the disclosure key, deferred to Phase 181.** After this phase
    `dev test m27c512` passes on a non-blank part while `firestarter write -a 0x…` on the identical
    part is still refused by the identical firmware, and no exported key says so.
    `.planning/research/PITFALLS.md:236-247` argues for one and its own verdict is "Both, or
    neither"; this phase took "neither" because `write_current_source` already discloses the probe,
    `RPT-A4` (`plan.is_uv`) is explicitly Phase 181's, and an unrequested exported key would grow the
    frozen-key surface this milestone is holding still.
  - **Q8 — the stale UV-prompt comment, BLOCKED.** Do not edit
    `firestarter_app/firestarter/cli_handlers.py`. Instead append to the EXISTING todo
    `.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md` the measured
    blocker: the stale sentences begin mid-line (`# ALWAYS WRITES: every run writes to the chip,
    unconditionally. A`) and end mid-line (`# single 256-byte slot is written anyway. Every OTHER
    family --`), so removing them requires REWRITING two retained comment lines — and the standing
    operator rule forbids writing any comment into source, ever, with no plan able to override it.
    The only zero-comment-added alternative is deleting the whole `ALWAYS WRITES` paragraph, which
    would also delete still-accurate prose about the AT28C family, the full-device write and report
    persistence. State that this needs the operator's call on which cost to pay, and that Phase 179
    deliberately paid neither.

**5. Carry the three flagged assumptions into the phase record**, so they leave the phase visible
rather than expiring with the plan set: UV-01's `unclassified` probe row (what "accepts" means),
UV-02's `unclassified` probe row (whose `run_count` is 2), and UV-03's `encoding` probe row (very
likely a classification miss — the witness is entirely in the byte domain, `WriteTarget.current` and
`.pattern` are `bytes`, `mask_write_pattern` is a per-byte `&`, and `__post_init__` compares byte
lengths; there is no text and therefore no normalization question). Record them in the phase SUMMARY
with the same wording they carry in `179-01-PLAN.md`.

**6. Run every gate in both repositories and capture the seal evidence.** The eight `rc=0` legs are
ruff check, ruff format, the mypy watermark, `snapshot_report_shapes.py --check`,
`check_devtest_orchestrator.py`, `check_diagnostic_report_claims.py`, `check_rekey_ledger.py` (from
`/workspaces`), and the full suite. Plus `firmware_diff=clean`, `chip_database_diff=clean`,
`app_tree=clean`, `phase_added_comments=0` measured across EVERY commit this phase made — anchor the
comment scan at the pre-phase sha `835baba`, never at `HEAD~1`, because a `HEAD~1` scan on the last
commit measures one commit and calls it a phase. Commit before running the full suite:
`test_flash_path_record_sync` asserts whole-repo porcelain.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s UV-03 line reads `- [x] **UV-03**` and its status row reads `Complete`.
    - If the sentinel is `PASS`: UV-01 and UV-02 read `- [x]` with status `Complete`. If the sentinel is `UNAVAILABLE`: UV-01 and UV-02 read `- [ ]` with status `Blocked (hardware)` and each row names `179-MEASUREMENT.md`.
    - `.planning/ROADMAP.md`'s Phase 179 section reads `**Plans:** 4 plans` and lists four ticked `- [x] 179-NN-PLAN.md — …` entries under `**Wave 1**` … `**Wave 4**`; every OTHER phase section in the file is byte-unchanged.
    - `.planning/research/PITFALLS.md` and `.planning/research/SUMMARY.md` each carry the falsification note beside the superseded claim, and neither superseded claim is deleted.
    - `.planning/todos/pending/` gained exactly the two new todos `<date>-uv-ladder-flip-on-exhausted-slots.md` and `<date>-uv-write-shortcut-disclosure-key.md`, and the existing Q8 todo's `blocked_by` field names the mid-line-boundary finding.
    - `firestarter_app/firestarter/cli_handlers.py` is byte-unchanged across the whole phase.
    - The full suite passes at 2254 or more with `32 snapshots passed`; `snapshot_report_shapes.py --check` reports 19; `check_rekey_ledger.py` prints `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`; ruff check, ruff format, `check_devtest_orchestrator.py` and `check_diagnostic_report_claims.py` all exit 0; the mypy watermark reads exactly 35.
    - Across every commit this phase made — measured from the pre-phase sha `835baba`, not from `HEAD~1` — the added-`#`-comment count over `firestarter/` and `tests/` is 0.
    - `git -C /workspaces/firestarter status --porcelain` is empty, `git -C /workspaces/firestarter_app status --porcelain` is empty, and `chip_database.json` shows no diff.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/evidence && mkdir -p "$E" && EV=$E/179-04-phase-seal.txt && M=.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-MEASUREMENT.md && { echo "== 0 the sentinel and the branch it forces"; echo "sentinel=$(/usr/bin/grep -oE '^BENCH RESULT: (PASS|UNAVAILABLE)$' "$M" | cut -d' ' -f3)"; echo "uv01_ticked=$(/usr/bin/grep -cE '^- \[x\] \*\*UV-01\*\*' .planning/REQUIREMENTS.md)"; echo "uv02_ticked=$(/usr/bin/grep -cE '^- \[x\] \*\*UV-02\*\*' .planning/REQUIREMENTS.md)"; echo "uv03_ticked=$(/usr/bin/grep -cE '^- \[x\] \*\*UV-03\*\*' .planning/REQUIREMENTS.md)"; echo "uv_status_rows=$(/usr/bin/grep -cE '^\| UV-0[123] \| Phase 179 \| (Complete|Blocked \(hardware\))' .planning/REQUIREMENTS.md)"; echo "uv_pending_rows=$(/usr/bin/grep -cE '^\| UV-0[123] \| Phase 179 \| Pending' .planning/REQUIREMENTS.md)"; echo "roadmap_plans=$(/usr/bin/grep -cE '^\*\*Plans:\*\* 4 plans' .planning/ROADMAP.md)"; echo "roadmap_ticked=$(/usr/bin/grep -cE '^- \[x\] 179-0[1234]-PLAN\.md' .planning/ROADMAP.md)"; echo "roadmap_tbd_179=$(sed -n '/^### Phase 179:/,/^### Phase 180:/p' .planning/ROADMAP.md | /usr/bin/grep -c 'TBD')"; echo "pitfalls_falsified=$(/usr/bin/grep -ciE 'falsifi' .planning/research/PITFALLS.md)"; echo "summary_falsified=$(/usr/bin/grep -ciE 'falsifi' .planning/research/SUMMARY.md)"; echo "q8_blocker=$(/usr/bin/grep -ciE 'mid-line|rewrit' .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md)"; echo "new_todos=$(ls .planning/todos/pending/ | /usr/bin/grep -cE 'uv-ladder-flip-on-exhausted-slots|uv-write-shortcut-disclosure-key')"; echo "== 1 the eight gates"; (cd firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python -m ruff format --check firestarter/ tests/); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_mypy_watermark.py); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_devtest_orchestrator.py); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_diagnostic_report_claims.py); echo "rc=$?"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q); echo "rc=$?"; echo "== 2 the phase-wide hygiene, anchored at the pre-phase sha"; echo "phase_added_comments=$(cd firestarter_app && git diff 835baba..HEAD -- firestarter/ tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*[\"'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')"; echo "cli_handlers_diff=$(cd firestarter_app && git diff 835baba..HEAD --name-only -- firestarter/cli_handlers.py | wc -l)"; echo "plan_shapes_diff=$(cd firestarter_app && git diff 835baba..HEAD --name-only -- tests/fixtures/plan_shapes.json | wc -l)"; echo "firmware_diff=$(test -z "$(git -C /workspaces/firestarter status --porcelain)" && echo clean || echo dirty)"; echo "app_tree=$(test -z "$(git -C /workspaces/firestarter_app status --porcelain)" && echo clean || echo dirty)"; echo "chip_database_diff=$(cd firestarter_app && test -z "$(git status --porcelain firestarter/data/chip_database.json)" && echo clean || echo dirty)"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 8 && /usr/bin/grep -qx 'phase_added_comments=0' "$EV" && /usr/bin/grep -qx 'cli_handlers_diff=0' "$EV" && /usr/bin/grep -qx 'plan_shapes_diff=0' "$EV" && /usr/bin/grep -qx 'firmware_diff=clean' "$EV" && /usr/bin/grep -qx 'app_tree=clean' "$EV" && /usr/bin/grep -qx 'chip_database_diff=clean' "$EV" && /usr/bin/grep -qx 'uv03_ticked=1' "$EV" && /usr/bin/grep -qx 'uv_status_rows=3' "$EV" && /usr/bin/grep -qx 'uv_pending_rows=0' "$EV" && /usr/bin/grep -qx 'roadmap_plans=1' "$EV" && /usr/bin/grep -qx 'roadmap_ticked=4' "$EV" && /usr/bin/grep -qx 'roadmap_tbd_179=0' "$EV" && test "$(/usr/bin/grep -oE '^pitfalls_falsified=[0-9]+' "$EV" | cut -d= -f2)" -ge 1 && test "$(/usr/bin/grep -oE '^summary_falsified=[0-9]+' "$EV" | cut -d= -f2)" -ge 1 && test "$(/usr/bin/grep -oE '^q8_blocker=[0-9]+' "$EV" | cut -d= -f2)" -ge 1 && test "$(/usr/bin/grep -oE '^new_todos=[0-9]+' "$EV" | cut -d= -f2)" -ge 2 && { if /usr/bin/grep -qx 'sentinel=PASS' "$EV"; then /usr/bin/grep -qx 'uv01_ticked=1' "$EV" && /usr/bin/grep -qx 'uv02_ticked=1' "$EV"; else /usr/bin/grep -qx 'uv01_ticked=0' "$EV" && /usr/bin/grep -qx 'uv02_ticked=0' "$EV" && /usr/bin/grep -qE '^\| UV-01 \| Phase 179 \| Blocked \(hardware\)' .planning/REQUIREMENTS.md; fi; } && /usr/bin/grep -qE 'OK: 19 snapshot\(s\)' "$EV" && /usr/bin/grep -qF 'OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound' "$EV" && /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' "$EV" && /usr/bin/grep -qE '^32 snapshots passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV" && test "$(/usr/bin/grep -oE '^[0-9]+ passed' "$EV" | tail -1 | cut -d' ' -f1)" -ge 2254</automated>
    <fails_when>fewer than eight `rc=0` lines (one of the eight gates is red); or `phase_added_comments` is not 0 (a `#` comment entered source somewhere in this phase — measured from the pre-phase sha `835baba`, so a `HEAD~1`-scoped scan cannot hide it); or `cli_handlers_diff` is not 0 (Q8 was edited despite being decided BLOCKED); or `plan_shapes_diff` is not 0 (the 677-chip pin moved); or `firmware_diff` / `app_tree` / `chip_database_diff` is not `clean`; or `uv03_ticked` is not 1, or `uv_pending_rows` is not 0, or `uv_status_rows` is not 3 (a requirement was left in its Pending state); or the sentinel branch is violated — `PASS` without UV-01/UV-02 ticked, or `UNAVAILABLE` with either ticked or without a `Blocked (hardware)` row; or `roadmap_plans`/`roadmap_ticked`/`roadmap_tbd_179` do not read 1/4/0; or either research file carries no falsification note; or the Q8 todo carries no blocker note; or fewer than two todo filenames match `uv-ladder-flip-on-exhausted-slots` / `uv-write-shortcut-disclosure-key` (the residuals were not filed, or were filed under names the gate cannot find); or `OK: 19 snapshot(s)`, the ledger binding line, `mypy errors: 35 (watermark: 35)` or `32 snapshots passed` is absent; or the suite's `N passed` count is below 2254; or the evidence contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -cE '^### Phase (17[0-9]|18[01]):' .planning/ROADMAP.md)" -ge 6 && sed -n '/^### Phase 178:/,/^### Phase 179:/p' .planning/ROADMAP.md | /usr/bin/grep -qxF '**Plans:** 4 plans' && sed -n '/^### Phase 180:/,/^### Phase 181:/p' .planning/ROADMAP.md | /usr/bin/grep -qxF '**Plans**: TBD' && sed -n '/^### Phase 181:/,$p' .planning/ROADMAP.md | /usr/bin/grep -qxF '**Plans**: TBD' && test "$(sed -n '/^### Phase 178:/,/^### Phase 179:/p' .planning/ROADMAP.md | /usr/bin/grep -cE '^- \[x\] 178-0[1234]-PLAN\.md')" -eq 4</automated>
    <fails_when>non-zero exit — fewer than six `### Phase 17x/18x:` headings survive (a section was destroyed by a whole-file rewrite); Phase 178's `**Plans:** 4 plans` line or its four ticked plan entries are gone (`phase.complete` is recorded to clobber an unrelated phase's `**Plans:**` line); or Phase 180's or Phase 181's `**Plans**: TBD` line was overwritten by an edit that should have been scoped to Phase 179.</fails_when>
  </verify>
  <done>The phase is sealed exactly as far as the evidence reaches: UV-03 Complete on the committed disagreement legs, UV-01 and UV-02 Complete on a measured bench `PASS` or `Blocked (hardware)` on a measured `UNAVAILABLE` — never on the host proof alone. Both falsified prior claims are repaired in place with the measurement that falsified them, three residuals are filed as todos rather than left implicit, `cli_handlers.py` is byte-unchanged with Q8's blocker recorded, every gate is green in both repositories, and the added-source-comment count across the WHOLE phase is zero.</done>
</task>

</tasks>

<threat_model>
ASVS level 1, blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent to physical, irreplaceable hardware | A UV write is irreversible and the operator has no eraser. Every bench run permanently consumes one of ~256 slots on a part that cannot be restored. |
| the checkpoint to the automation that can auto-approve it | `--auto` / `--chain` are recorded to auto-approve `checkpoint:human-verify` gates; `autonomous: false` is NOT self-protecting. |
| the phase record to every downstream reader | A requirement marked Complete is read by `/gsd-verify-work`, by the milestone audit and by the next phase's planner as a settled fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-179-15 | Tampering | the physical UV part | **critical** | mitigate | Chip handling is OPERATOR-ONLY and stated as such in the checkpoint. The slot budget is bounded at two and stated before anything is plugged in. `--fast` is forbidden. A failed first run STOPS rather than retrying into the part's remaining life. `am27c020` and every erasable proxy are refused by name, with the measured reason for each. |
| T-179-16 | Spoofing | the bench checkpoint under `--auto` | **critical** | mitigate | The gate is `blocking-human` and the plan states it is never auto-approvable, but policy alone is not the defence: the verify requires a measured 12-hex hash, the verbatim `Current firmware version: … controller: … port` line, a measured slot address, a measured `slots_remaining` and a measured exit code. An auto-approval that never touched a board cannot produce them, and a fabricated record fails the sentinel-branch check in Task 2. |
| T-179-17 | Repudiation | the requirement marking | high | mitigate | The `BENCH RESULT:` sentinel is a machine-read branch, not a judgement call: `UNAVAILABLE` forces UV-01 and UV-02 to `Blocked (hardware)` with unticked boxes, and Task 2's verify asserts the branch was honoured in both directions. A phase that could not reach the bench cannot seal as though it had. |
| T-179-18 | Tampering | `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` | high | mitigate | Both are hand-authored and both have recorded whole-file-rewrite hazards (`roadmap.update-plan-progress` clobbers the dependency table; `phase.complete` clobbers an unrelated phase's `**Plans:**` line; the requirements/roadmap verbs reformat whole files). Only scoped edits are permitted, and the final verify leg asserts Phase 178's block and Phases 180/181's `**Plans**: TBD` lines survive untouched. |
| T-179-19 | Information disclosure | the committed bench artifact | medium | mitigate | `179-MEASUREMENT.md` records a serial port path and a board identity, which are local-environment facts and not secrets; it records no home directory, no username and no token. It is a `.planning/` record and is never sent through `submit_report`. The filing prompt is declined, so no bench output reaches a public tracker. |
| T-179-20 | Elevation of privilege | `firestarter fw --install` | high | mitigate | Not run. This phase requires no firmware change (`eprom.cpp:143-145` already honours the flag), and `--install` is recorded to flash the ATTACHED board regardless of `--board`. The checkpoint says so explicitly and the verify asserts the firmware submodule is porcelain-clean. |
| T-179-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. `179-RESEARCH.md`'s Environment Availability table records zero missing dependencies with no fallback needed. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q` — `>= 2254 passed`, `32 snapshots passed`, no failures.
2. `./.venv311/bin/python tools/snapshot_report_shapes.py --check` — `OK: 19 snapshot(s) … match a fresh regeneration`.
3. `cd /workspaces && python3 tools/rekey/check_rekey_ledger.py` — exit 0, `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`.
4. `./.venv311/bin/python -m ruff check firestarter/ tests/`, `ruff format --check`, `tools/check_mypy_watermark.py` (35/35), `tools/check_devtest_orchestrator.py`, `tools/check_diagnostic_report_claims.py` — all exit 0.
5. `git diff 835baba..HEAD -- firestarter/ tests/` adds zero `#` comment lines across the WHOLE phase, and `firestarter/cli_handlers.py` and `tests/fixtures/plan_shapes.json` are byte-unchanged across the whole phase.
6. `179-MEASUREMENT.md` carries exactly one `BENCH RESULT:` sentinel, and `.planning/REQUIREMENTS.md`'s three UV rows match the branch that sentinel forces.
7. `/workspaces/firestarter` and `/workspaces/firestarter_app` both porcelain-clean; `chip_database.json` unchanged.
</verification>

<success_criteria>
- A physical UV part holding data outside the target slot accepted a write to that slot without being refused, recorded from a real run (ROADMAP criterion 1) — or the phase records, in machine-readable form, that the bench could not be reached and marks UV-01/UV-02 `Blocked (hardware)` rather than sealing on the host proof.
- That same physical run's `overall_verdict` read `PASS` and its write and verify steps read `run_count == 2` (ROADMAP criterion 2), or the same `UNAVAILABLE` branch applies.
- Criterion 4 is satisfied as `D-179-1` records it, with both halves present and neither standing in for the other: the committed, collected, unskipped regression from `179-03`, and this plan's committed bench artifact.
- Every gate is green in both repositories, no `#` comment entered source anywhere in the phase, and every residual — the ladder flip, the Q5 disclosure deferral and the Q8 blocker — is filed rather than implicit.
</success_criteria>

<output>
Create `.planning/phases/179-uv-slot-writes-flag-skip-blank-check-hardware-gated/179-04-SUMMARY.md` when done.
</output>
