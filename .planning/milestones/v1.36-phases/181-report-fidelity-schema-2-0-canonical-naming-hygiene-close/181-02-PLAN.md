---
phase: 181-report-fidelity-schema-2-0-canonical-naming-hygiene-close
plan: 02
type: execute
wave: 2
depends_on: [181-01]
files_modified:
  - firestarter_app/tests/plan_corpus.py
  - firestarter_app/tests/test_chip_test.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
  - firestarter_app/tests/test_chip_test_blank_check_order.py
  - firestarter_app/tests/test_chip_test_timing.py
  - firestarter_app/tests/test_erase_flag_invariants.py
  - firestarter_app/tests/test_derive_plan_structural_sentinel.py
  - firestarter_app/tests/test_derive_plan_no_drop_sweep.py
  - firestarter_app/tests/test_plan_shapes_drift.py
  - firestarter_app/tests/fixtures/plan_shapes.json
  - firestarter_app/tests/test_diagnostic_report.py
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-none-surface-retirement.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-corpus-reanchor.txt
autonomous: true
requirements: [RPT-B2]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-09's equivalence claim is MEASURED, not asserted: for every one of the 677 distinct part numbers, `derive_plan(name, db, write_scope='partial' if is_uv else 'full')` is byte-identical under canonical serialization to what the parameterless form will produce, recorded as a digest a later plan re-computes and compares"
    - "`write_scope='none'` is proven unreachable from `dev test`: the only production call site resolves through a function whose first branch returns `full` for a non-UV chip and `partial` otherwise, so `none` has no production caller"
    - "`Plan.locked_destructive` is proven EMPTY for all 677 names at the reachable scope, so `count_applicable`'s `+ len(plan.locked_destructive)` term and `sdp_oracle_applicable`'s second arm are dead on every reachable path — the measurement that de-risks D-08"
    - "No test in the tree exercises the `write_scope='none'` shape any more: the 78 explicit `write_scope=\"none\"` occurrences and the 15 bare `derive_plan(name, db)` calls that silently meant `none` are all either re-pointed to the reachable scope or deleted with the reason recorded (D-08)"
    - "`tests/plan_corpus.py` derives one plan per part number at the single reachable scope, and every count pin that read 1354 now reads the re-measured value, taken from a measurement and never from arithmetic on the old number"
    - "`tests/fixtures/plan_shapes.json` is regenerated by `tools/measure_plan_shapes.py` and `_EXPECTED_AGGREGATE` in `tests/test_plan_shapes_drift.py` is re-anchored to the regenerated values, with the before and after aggregates both recorded"
    - "`derive_plan`'s signature, `_WRITE_SCOPE_*`, `_WRITE_SCOPES`, `Plan.locked_destructive` and every product-source line are BYTE-UNCHANGED by this plan — the deletion is plan 181-04's, and this plan's job is to make it a small mechanical edit rather than a 117-site one"
    - "All 19 `FROZEN_HASHES` literals remain byte-identical to app base `04fd982`"
    - "No `#` comment was added to any file this plan touches, measured by a non-increase `tokenize` COMMENT-token census against the app-base baselines"
    - "`git -C /workspaces/firestarter status --porcelain` prints nothing and `git -C /workspaces/firestarter_app status --porcelain firestarter/` prints nothing"
  prohibitions:
    - "chip_database.json must never be hand-edited -- it is generated"
    - "A gate must not read green while the rule it guards is violated"
    - "No comment may be written into product source, and firestarter_app/tests/ counts as product source"
    - "A re-anchored count pin must come from a measurement, never from arithmetic on the number it replaces"
  artifacts:
    - path: "firestarter_app/tests/plan_corpus.py"
      provides: "the corpus re-keyed to one plan per part number at the single reachable scope"
      contains: "PART_NUMBERS"
    - path: "firestarter_app/tests/fixtures/plan_shapes.json"
      provides: "the regenerated plan-shape aggregate the drift gate pins absolutely"
      contains: "_generated_by"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt"
      provides: "the D-09 equivalence digest over 677 names, plus the empty-locked_destructive measurement that de-risks D-08"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-none-surface-retirement.txt"
      provides: "the per-file before and after census of the 78 explicit and 15 bare `none` sites, with a disposition for each"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-corpus-reanchor.txt"
      provides: "the before and after corpus and plan-shape aggregate numbers, each labelled as measured"
  key_links:
    - from: "firestarter_app/tests/plan_corpus.py"
      to: "firestarter_app/tests/test_derive_plan_structural_sentinel.py"
      via: "the sentinel module imports `plan_corpus()` and pins its size, so the corpus re-key and its pins move together"
      pattern: "plan_corpus"
    - from: "firestarter_app/tests/fixtures/plan_shapes.json"
      to: "firestarter_app/tools/measure_plan_shapes.py"
      via: "the committed artifact carries a `_generated_by` banner naming its generator and is regenerated, never hand-edited"
      pattern: "measure_plan_shapes"
---

<objective>
Make plan `181-04`'s demolition a small, reviewable product edit instead of a 117-site one — and
measure D-09's equivalence claim before anything is deleted.

Two things happen here and neither touches product source. First, D-09's stated equivalence
(`write_scope == "partial"` iff `is_uv`, so the parameter carries no information `derive_plan` does
not already hold) becomes a **measured digest** over all 677 distinct part numbers, alongside the two
measurements that de-risk D-08: that `write_scope="none"` has no production caller, and that
`Plan.locked_destructive` is empty for every name at the reachable scope, which is what makes
`count_applicable`'s extra term and `sdp_oracle_applicable`'s second arm provably dead rather than
merely believed dead. Second, the entire `write_scope="none"` **test** surface is retired: every one
of the 78 explicit occurrences and the 15 bare `derive_plan(name, db)` calls that silently meant
`"none"` is re-pointed to the reachable scope or deleted with its reason recorded, and the corpus is
re-keyed to one plan per name with every count pin re-measured.

Purpose: this is the split CONTEXT.md's risk warning implies but does not name. The demolition is
measured at **117 `derive_plan` call sites** — 102 with an explicit `write_scope` and 15 bare — plus
73 `locked_destructive`/`locked_steps` sites, a 1354-entry corpus, and a committed
`plan_shapes.json` golden with absolute aggregates. Landing all of that in one plan exceeds a single
agent's working budget; landing the test migration first, while the parameter still exists and every
commit stays green, makes the actual deletion a uniform kwarg-drop.
Output: a retired `"none"` test surface, a re-keyed corpus, a regenerated plan-shape golden, three
evidence transcripts, and **zero product-source lines changed**.

**This plan deletes no product symbol.** `derive_plan`'s signature, `_WRITE_SCOPE_NONE`,
`_WRITE_SCOPE_PARTIAL`, `_WRITE_SCOPE_FULL`, `_WRITE_SCOPES`, the `ValueError` ladder,
`write_execute`, `Plan.locked_destructive`, `BannerCounts.locked_steps` and every line in
`cli_handlers.py` are byte-unchanged when this plan ends. They all die in `181-04`.
</objective>

<orchestrator_dispositions>
Measurements the orchestrator took against the live tree at app `04fd982`. These **OVERRIDE**
`181-CONTEXT.md` and `181-PATTERNS.md` where they conflict, and three of them are larger than either
document states.

- **`write_scope="none"` is 78 occurrences across 9 files**, not CONTEXT.md's "~69 across 7":
  `firestarter/chip_test.py` 14, `firestarter/diagnostic_report.py` 1, `tests/test_chip_test.py` 32,
  `tests/test_chip_test_sdp_leg.py` 16, `tests/test_erase_flag_invariants.py` 5,
  `tests/test_chip_test_blank_check_order.py` 4, `tests/test_chip_test_timing.py` 4,
  `tests/plan_corpus.py` 1, `tests/test_derive_plan_structural_sentinel.py` 1.
- **NEW, found by neither document: 15 bare `derive_plan(name, db)` calls today mean
  `write_scope="none"`,** because that is the parameter's default. They are at
  `tests/test_chip_test.py` lines 373, 397, 417, 430, 445, 475, 483, 496, 508, 539, 546, 558;
  `tests/test_chip_test_sdp_leg.py` 301 and 1589; and `tests/test_diagnostic_report.py` 129. After
  D-09 a bare call means a write-executing plan, so **these 15 sites change meaning silently** — they
  are the highest-risk sites in the whole demolition and each needs an explicit disposition.
  Total `derive_plan` call sites: **117** (102 with an explicit `write_scope`, 15 bare).
- **NEW: `tests/plan_corpus.py` sweeps `PART_NUMBERS` x `SWEEP_SCOPES` = 677 x 2 = 1354 plans**, keyed
  `(name, scope)`, and is consumed by `tests/test_derive_plan_structural_sentinel.py` (about 13 call
  sites) and `tests/test_derive_plan_no_drop_sweep.py` (about 5). Absolute `1354` pins live at
  `test_derive_plan_no_drop_sweep.py:154-155`, `test_derive_plan_structural_sentinel.py:301`, `:313`,
  `:545`, `:567`, `:593`, `:694`, and in `test_plan_shapes_drift.py:41`.
- **NEW: `tests/fixtures/plan_shapes.json` is a committed generated artifact** with a `_generated_by`
  banner, produced by `tools/measure_plan_shapes.py`, and `test_plan_shapes_drift.py:37-44` pins its
  aggregate ABSOLUTELY: `rows 746`, `distinct_part_numbers 677`, `plans 1354`,
  `distinct_shape_families 8`, `total_steps 16248`, `unsupported_steps 9304`. Four of those six move.
- **`181-PATTERNS.md` is WRONG about the SDP append.** It says the `OP_WRITE_INHIBITED`
  `locked_destructive` append at `chip_test.py:894` is "not a `write_scope="none"` append". It IS one:
  it sits in the `elif sdp_allowed:` arm whose controlling `if` is `write_execute`, which is False
  only at `write_scope="none"`. That is why `sdp_oracle_applicable`'s second arm is dead on every
  reachable path, and it is the single most important de-risking fact for D-08. **Measure it in Task
  1 rather than trusting either document.**
- **CONTEXT.md's two de-risking measurements re-verified TRUE:** every `report_shapes.py` builder
  passes `write_scope="full"` (10 call sites, all `"full"`), and `tests/plan_corpus.py` explicitly
  excludes `"none"` from `SWEEP_SCOPES` with its vacuity reason written out.
- **`tests/test_derive_plan_structural_sentinel.py:84` does a MODULE-LEVEL
  `from firestarter.cli_handlers import _resolve_write_scope`.** That breaks at COLLECTION time when
  `181-04` deletes the function, reddening the whole 31-test module rather than one test. It is
  removed here, in this plan, so `181-04` cannot trip it.
- **The re-key ledger is RETIRED** (`tools/rekey/check_rekey_ledger.py` does not exist), the green-tree
  battery is **seven** legs, and the suite floor is **2239**, not 2242.
</orchestrator_dispositions>

<flagged_assumptions>
The spec-less probe fallback is ACTIVE. The authoritative report is `coverage.json`: **30 applicable
edges over 18 requirements**, all arriving `unresolved`. Category tally re-counted from the file:
`unclassified` **12** (the orchestrator summary said 11 — the file is authoritative), `empty` 6,
`adjacency` 5, `ordering` 5, `encoding` 1, `concurrency` 1.

**This plan's share is ZERO rows.** RPT-B2's single `unclassified` row is authored in plan `181-04`,
where `banner.locked_steps` is actually deleted, so it is not double-counted here. No edge row is
dropped: the phase-wide distribution is 6 rows in `181-01`, 0 here, 7 in `181-03`, 2 in `181-04`, 5 in
`181-05`, 5 in `181-06`, 2 in `181-07` and 3 in `181-08` — 30 in total, of which 16 are authored
explicit, 2 backstop and 12 flagged.

**Prohibition recall (§B, run in-prompt).** Four of the phase's ten survivors land here, authored
descriptor-less in `must_haves.prohibitions`, never under `truths`. The fourth is specific to this
plan and worth naming: a re-anchored count pin must come from a **measurement**, never from arithmetic
on the number it replaces — `total_steps` and `unsupported_steps` look like they should simply halve,
and asserting a halved number without generating the artifact would be a fabricated measurement.
**Canon-referral breadcrumb:** no OWASP, injection, path-traversal, prototype-pollution, GDPR or
generic-fairness candidate survived Stage 2 for this plan — it changes no data path, only tests.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md
</context>

<environment>
Three git repositories; **branch names differ between meta and the submodules**.

- Meta `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`. Owns `.planning/`, `.claude/`.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, **phase base app
  HEAD `04fd982`**. **Commit inside the submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — **not touched**. Must stay porcelain-clean.

`git -C /workspaces/firestarter_app status --porcelain firestarter/` names the app's own **package
directory**, not the firmware submodule.

Python is `/workspaces/firestarter_app/.venv311/bin/python` — 3.11, matching app CI. The devcontainer
default is 3.12 and that divergence has already broken beta CI once.

`pytest` `addopts` is `-ra -q`; a count line needs `-o addopts=""`.

Shell `grep` here is **ugrep and silently honours `.gitignore`** — every evidence-grade scan uses
`/usr/bin/grep`. `-qF` with a dash-leading pattern exits 2 and fails OPEN; use `-qFe`.

`tests/test_skip_census.py`'s three failures are **pre-existing** and unrelated. No leg here runs the
full suite; the battery runs once, in `181-08`.

**No push, no PR, no tag, no branch cut** in any task (D-22).
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and CONTEXT.md D-26 are
**non-overridable**, and `firestarter_app/tests/` counts as product source. Write **no `#` comment**.
Every rationale — why a `"none"` test was deleted rather than re-pointed, why a count pin moved — goes
into a **docstring** or into the plan `SUMMARY.md`.

The files here carry heavy pre-existing comment loads. Those are **pre-existing — do not "fix" them**
and **do not copy their style**. Baselines measured at app `04fd982` by `tokenize` COMMENT-token
census; the gate is **non-increase**, so a deletion cannot redden it:
`tests/test_chip_test.py` 621, `tests/test_chip_test_sdp_leg.py` 227,
`tests/test_erase_flag_invariants.py` 27, `tests/test_chip_test_blank_check_order.py` 21,
`tests/test_chip_test_timing.py` 4, `tests/test_diagnostic_report.py` 183, and **exactly 0** for
`tests/plan_corpus.py` and `tests/test_derive_plan_structural_sentinel.py`, which have never carried
one.
</comment_discipline>

<tasks>

<task type="auto">
  <name>Task 1: Measure D-09's equivalence and D-08's two de-risking facts, before a single line is deleted</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` with plan `181-01` committed and a clean working tree, and `git -C /workspaces/firestarter status --porcelain` prints nothing.</precondition>
  <reversibility rating="costly">D-08 and D-09 are rated costly by CONTEXT.md and this task is their gate. Restoring the deleted surface later means re-adding a dataclass field, a keyword parameter, four `derive_plan` branches, `count_applicable`'s extra term, `sdp_oracle_applicable`'s second arm, and 117 call sites plus 73 assertions. This task exists so the decision is taken against a measurement rather than a belief; it changes nothing itself and is therefore freely reversible on its own.</reversibility>
  <files>
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/chip_test.py:534-600` — `_WRITE_SCOPE_NONE`/`_PARTIAL`/`_FULL` at `:534-536`, `_WRITE_SCOPES` at `:537`, `derive_plan`'s signature at `:540` with its `write_scope: str = "none"` DEFAULT, the three-value docstring at `:541-576`, the `ValueError` ladder at `:576-580`, `is_uv = is_uv_eprom(full)` at `:591` and `write_execute` at `:592`.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:600-700` — the region computation: `full_device_permitted = write_scope == _WRITE_SCOPE_FULL` at `:609`, the `if write_scope == _WRITE_SCOPE_NONE` arm at `:622`, `elif is_uv` at `:624`, `elif write_scope == _WRITE_SCOPE_FULL` at `:633`, the trailing `else` at `:650`, `leg_region` at `:663-668`, and `erase_is_executable` at `:685`.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:740-912` — every `write_execute` guard and all four `locked_destructive.append` sites (`:763`, `:789`, `:807`, `:894`). Read `:886-900` carefully: the SDP append at `:894` sits in `elif sdp_allowed:`, whose controlling `if` is `write_execute`, so it is reachable ONLY at `write_scope="none"`. `181-PATTERNS.md` says otherwise and is wrong.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:2012-2031` — `sdp_oracle_applicable`. Its FIRST arm walks `plan.steps` for a supported `OP_WRITE_INHIBITED`; its SECOND arm walks `plan.locked_destructive`. This is the exit-code path Phase 178 just changed and CONTEXT.md flags as the risk concentration.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:3690-3731` — `BannerCounts` with its `locked_steps` field, and `count_applicable`, whose `M` is `sum(1 for s in plan.steps if s.supported) + len(plan.locked_destructive)`.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2257-2312` — `_is_uv_eprom` and `_resolve_write_scope`, whose whole body after the `del interactive` line is `if not _is_uv_eprom(app, chip): return "full"` then `return "partial"`. This is D-09's equivalence in two lines.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2395-2405` — the ONLY production `derive_plan` call site, and the `interactive = _is_interactive()` local that feeds nothing else.
    - `/workspaces/firestarter_app/tests/plan_corpus.py` in full (146 lines) — `all_rows`, `PART_NUMBERS` (677), `SWEEP_SCOPES`, `plan_corpus()` and its `(name, scope)` key. Note the module docstring's own statement that `full` plus `partial` is already a strict superset of the reachable domain.
  </read_first>
  <behavior>
    - For all 677 distinct part numbers, the canonical serialization of `derive_plan(name, db, write_scope="partial" if is_uv_eprom(db.get_eprom(name)) else "full")` produces a stable digest.
    - `Plan.locked_destructive` is the empty list for all 677 names at that reachable scope.
    - `sdp_oracle_applicable` returns the same boolean for every one of those 677 plans whether or not its second arm is consulted.
    - `count_applicable`'s `M` is unchanged for every one of those 677 plans whether or not its `+ len(plan.locked_destructive)` term is included.
    - `_resolve_write_scope` returns `"partial"` for exactly the UV rows and `"full"` for every other row, matching `Plan.is_uv` one-for-one.
  </behavior>
  <action>
Change no file under `firestarter_app/` in this task. Produce one evidence transcript.

Write and run a throwaway measurement script under
`/tmp/claude-1000/-workspaces/e56f1ef2-5e2e-4d66-b7e8-ad89ac5bfac2/scratchpad/` — not in the repo, so
nothing needs deleting afterwards — using `./.venv311/bin/python` from `/workspaces/firestarter_app`
and importing `tests.plan_corpus`'s `REAL_DB` and `PART_NUMBERS` so the domain is the same 677 names
every sentinel already sweeps, and `EpromDatabase(skip_local_override=True)` rather than
`EpromDatabase()`, which would silently read a developer override and make every number
machine-dependent.

Define ONE canonical serializer for a `Plan` and use it for every measurement in this phase, so plan
`181-04` can recompute the identical digest: the plan `name`, `reason`, `is_uv`, then for each step in
order its `op`, `supported`, `reason`, `destructive`, `write_region`, `region_policy` and
`full_device_permitted`, then `locked_destructive` verbatim. Join with a separator that cannot occur
in any field, concatenate over `sorted(PART_NUMBERS)`, and take a `sha256` hexdigest. Record the
serializer's field order in the transcript so `181-04` reproduces it exactly rather than inventing a
second one.

Measure and record, as flat `key=value` scalars in
`.planning/phases/181-.../evidence/181-02-derive-plan-equivalence.txt`:

- `part_numbers=677` (assert it, and if the measured value differs, record the discrepancy and use the
  measured number)
- `reachable_scope_digest=<sha256>` — over the reachable scope, `"partial"` when `is_uv_eprom(full)`
  else `"full"`, which is exactly what `_resolve_write_scope` returns
- `serializer_field_order=<the comma-joined field list>`
- `rows_with_nonempty_locked_destructive_at_reachable_scope=0` — the D-08 de-risking measurement. If
  it is anything other than 0, **stop and report**: the deletion is not behaviour-identical and this
  plan must not proceed.
- `sdp_oracle_applicable_second_arm_changes_result_for=0` — computed by evaluating the first arm alone
  against the full function for all 677 plans
- `count_applicable_m_changes_without_locked_term_for=0` — same shape, over `M`
- `resolve_write_scope_matches_plan_is_uv_for=677` and `resolve_write_scope_mismatches=0` — the
  one-for-one check that `"partial"` iff `is_uv`, using `_resolve_write_scope` with a minimal
  app-context double carrying only `db`
- `write_scope_none_production_callers=0` — measured by an AST census over `firestarter/` for
  `derive_plan` calls whose `write_scope` keyword is the literal `"none"` or absent
- `bare_derive_plan_calls_in_tests=15` and `explicit_write_scope_none_occurrences=78` — the two
  numbers `181-PATTERNS.md` and CONTEXT.md under-counted, re-measured here
- a numbered section stating, in prose a reviewer can check, that the `locked_destructive` append at
  `chip_test.py:894` sits in the `elif sdp_allowed:` arm under `if write_execute:` and is therefore a
  `write_scope="none"`-only append, correcting `181-PATTERNS.md`

Also record the SDP-capability split as measured (`sdp_capability` is count-pinned at 43 ALLOW / 41
REFUSE / 84 total by its own gate) so a reviewer can see the second arm was exercised over both
classes rather than only over rows where it could not fire.

Commit only the transcript, in `/workspaces` on `gsd/v1.36-dev-test-fidelity-planning`. The app repo
must be byte-unchanged at the end of this task.
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt; test -s "$E" && /usr/bin/grep -qF 'part_numbers=677' "$E" && /usr/bin/grep -qE '^reachable_scope_digest=[0-9a-f]{64}$' "$E" && /usr/bin/grep -qF 'serializer_field_order=' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing or empty, the domain size was not asserted, the digest line is absent or is not a 64-character lowercase hex string, or the serializer field order was not recorded, which would leave plan 181-04 unable to reproduce the digest.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt; /usr/bin/grep -qF 'rows_with_nonempty_locked_destructive_at_reachable_scope=0' "$E" && /usr/bin/grep -qF 'sdp_oracle_applicable_second_arm_changes_result_for=0' "$E" && /usr/bin/grep -qF 'count_applicable_m_changes_without_locked_term_for=0' "$E" && /usr/bin/grep -qF 'resolve_write_scope_mismatches=0' "$E" && /usr/bin/grep -qF 'write_scope_none_production_callers=0' "$E"</automated>
    <fails_when>non-zero exit — any of the five de-risking measurements is absent or recorded as a non-zero value. A non-zero `rows_with_nonempty_locked_destructive_at_reachable_scope` means D-08 is not behaviour-identical and the phase must halt for the operator rather than proceed.</fails_when>
    <automated>cd /workspaces/firestarter_app && S=$(git status --porcelain) && test -z "$S" && D=$(git diff 04fd982 --stat) && test -z "$D"</automated>
    <fails_when>non-zero exit — the app repo is dirty or its tree differs from app base `04fd982` in any file, which means this measurement task changed code it was forbidden to touch. Note this leg runs only in Task 1; from Task 2 onward the tree legitimately differs from base.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/181-02-derive-plan-equivalence.txt` exists and records `part_numbers=677`, a 64-hex `reachable_scope_digest=`, and `serializer_field_order=` naming the exact field list.
    - It records all five zero-valued de-risking measurements: `rows_with_nonempty_locked_destructive_at_reachable_scope=0`, `sdp_oracle_applicable_second_arm_changes_result_for=0`, `count_applicable_m_changes_without_locked_term_for=0`, `resolve_write_scope_mismatches=0`, `write_scope_none_production_callers=0`.
    - It records `resolve_write_scope_matches_plan_is_uv_for=677`, `bare_derive_plan_calls_in_tests=15` and `explicit_write_scope_none_occurrences=78`.
    - It carries a numbered section stating that the `chip_test.py:894` append is reachable only at `write_scope="none"`, correcting `181-PATTERNS.md`.
    - It records the SDP-capability ALLOW/REFUSE split so the second-arm measurement is visibly non-vacuous.
    - `git -C /workspaces/firestarter_app status --porcelain` prints nothing and `git diff 04fd982 --stat` prints nothing.
  </acceptance_criteria>
  <done>D-08 and D-09 are no longer taken on the strength of two sentences in a decision record: the equivalence is a digest, the two dead code paths are measured dead over all 677 names and both SDP classes, and the one document claim that contradicted the code has been corrected in writing.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the `write_scope="none"` test surface — 78 explicit sites and 15 bare calls, each re-pointed or deleted with its reason</name>
  <files>
    firestarter_app/tests/test_chip_test.py,
    firestarter_app/tests/test_chip_test_sdp_leg.py,
    firestarter_app/tests/test_chip_test_blank_check_order.py,
    firestarter_app/tests/test_chip_test_timing.py,
    firestarter_app/tests/test_erase_flag_invariants.py,
    firestarter_app/tests/test_derive_plan_structural_sentinel.py,
    firestarter_app/tests/test_diagnostic_report.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-none-surface-retirement.txt
  </files>
  <read_first>
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt` — Task 1's transcript. The reachable-scope rule it records (`"partial"` when `is_uv_eprom(full)` else `"full"`) is the rule every re-pointed site adopts. Do not invent a second rule.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:360-600` — the densest `"none"` region and where 12 of the 15 bare calls live. Read the surrounding test names and docstrings: several assert precisely that write/verify/erase are OMITTED, which is the shape that ceases to exist.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:900-970` — `test_derive_plan_write_scope_none_unchanged_by_region_policy` at `:903` and `test_derive_plan_write_scope_rejects_unknown_value` at `:960`. Both are tests OF the parameter, not of a behaviour that survives it.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:2640-2730` — the `locked_destructive`-versus-`steps` comparison tests, which pair a `"none"` plan against a `"full"` one.
    - `/workspaces/firestarter_app/tests/test_chip_test_sdp_leg.py:290-320` and `:1580-1600` — the two bare calls, and `:2095-2110`, whose comment already records that the reversal left `_resolve_write_scope` returning only `"full"`/`"partial"`.
    - `/workspaces/firestarter_app/tests/test_derive_plan_structural_sentinel.py:80-95` — the import block. Line 84 is the MODULE-LEVEL `from firestarter.cli_handlers import _resolve_write_scope` that must go, and `:86-92` is the `plan_corpus` import.
    - `/workspaces/firestarter_app/tests/test_derive_plan_structural_sentinel.py:1010-1070` — `test_resolve_write_scope_returns_partial_for_every_uv_row`, the one test whose subject is the function `181-04` deletes.
    - `/workspaces/firestarter_app/tests/test_diagnostic_report.py:120-140` — the bare call at `:129`, and `:1955-1975` for the explicit `"full"` sibling at `:1963`, so the two can be made consistent.
    - `/workspaces/firestarter_app/tests/test_erase_flag_invariants.py`, `/workspaces/firestarter_app/tests/test_chip_test_blank_check_order.py`, `/workspaces/firestarter_app/tests/test_chip_test_timing.py` — the remaining 13 explicit sites; each file is small enough to read whole.
  </read_first>
  <behavior>
    - After this task, an AST census over `firestarter_app/tests/` finds ZERO `derive_plan` calls whose `write_scope` keyword is the literal `"none"` and ZERO `derive_plan` calls with no `write_scope` keyword at all.
    - Every surviving `derive_plan` call in `tests/` passes an explicit `"full"` or `"partial"`.
    - No test asserts that write, verify or erase is structurally omitted from `Plan.steps`, and no test asserts `derive_plan` raises `ValueError` for an unrecognised scope.
    - Every module edited here still collects and passes at its own count, with the deltas recorded.
  </behavior>
  <action>
Work file by file, committing once at the end so the tree is green at every commit boundary. For each
of the 93 sites (78 explicit plus 15 bare) choose exactly one of two dispositions and record which:

**Re-point** when the test asserts a property that survives at the reachable scope. Replace the site
with an explicit `write_scope="full"` for a non-UV chip or `write_scope="partial"` for a UV one,
matching Task 1's recorded rule. Keep the explicit keyword — do NOT convert to a bare two-argument
call, because a bare call still means `"none"` until `181-04` lands and would silently invert the
test's meaning for the duration of this wave. The bare-to-explicit conversion is the whole point of
this task for those 15 sites.

**Delete** when the test's subject is the omitted-write shape itself or the parameter's own
validation. Named candidates, to be confirmed against the code rather than trusted from this list:
`test_derive_plan_write_scope_none_unchanged_by_region_policy`,
`test_derive_plan_write_scope_rejects_unknown_value`, and every assertion pairing a `"none"` plan's
`locked_destructive` against a `"full"` plan's `steps`. For a test that carries BOTH a surviving claim
and a `"none"`-only claim, keep the surviving half at the reachable scope and drop the `"none"` half —
never delete a surviving assertion to simplify the edit, and never keep a `"none"` assertion by
inventing a replacement shape for it.

For every deletion, record in the `SUMMARY.md`: the test name, the claim it made, and why that claim
has no subject after D-08/D-09. That record is the deletion's justification and it is the only place
it exists — D-26 forbids putting it in the source.

Two specific edits that are not call sites:

Remove the MODULE-LEVEL `from firestarter.cli_handlers import _resolve_write_scope` at
`tests/test_derive_plan_structural_sentinel.py:84` and delete
`test_resolve_write_scope_returns_partial_for_every_uv_row`. Its claim — that `_resolve_write_scope`
returns `"partial"` for every UV row — is exactly D-09's equivalence, and Task 1 has already measured
it over all 677 names with the result in `evidence/181-02-derive-plan-equivalence.txt`. Cite that
transcript in the `SUMMARY.md` as the claim's new home. Removing the module-level import here, in this
plan, is what stops `181-04` reddening all 31 tests in that module at COLLECTION time.

In `tests/test_chip_test.py`, leave the four `is_uv_eprom` tests at `:315-350` alone: their subject is
`chip_test.is_uv_eprom`, the engine predicate, which survives untouched. Only
`cli_handlers._is_uv_eprom`, the handler wrapper, dies in `181-04`.

Then write `evidence/181-02-none-surface-retirement.txt`: a per-file table with one row per file
giving the before count, the after count (which must be 0 for both the explicit and the bare census),
the number of sites re-pointed and the number deleted; then a flat list of every deleted test name.
Record `explicit_none_after=0` and `bare_derive_plan_after=0` as scalars, plus the per-module pytest
counts before and after.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`. Commit BEFORE
running any leg that asserts whole-repo porcelain.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import ast,pathlib,subprocess;fs=[f for f in subprocess.run(["git","ls-files","tests/*.py","tests/fixtures/*.py"],capture_output=True,text=True).stdout.split()];bare=[];none=[]
for f in fs:
    try: t=ast.parse(pathlib.Path(f).read_text(encoding="utf-8"))
    except Exception: continue
    for n in ast.walk(t):
        if isinstance(n,ast.Call):
            fn=n.func; nm=fn.attr if isinstance(fn,ast.Attribute) else (fn.id if isinstance(fn,ast.Name) else None)
            if nm=="derive_plan":
                kws={k.arg:k.value for k in n.keywords}
                if "write_scope" not in kws: bare.append(f+":"+str(n.lineno))
                elif isinstance(kws["write_scope"],ast.Constant) and kws["write_scope"].value=="none": none.append(f+":"+str(n.lineno))
print("bare_derive_plan_after="+str(len(bare)));print("explicit_none_after="+str(len(none)))
assert not bare, bare
assert not none, none'</automated>
    <fails_when>non-zero exit — the printed `bare_derive_plan_after=` or `explicit_none_after=` value is greater than 0 and the following assert lists the surviving sites by file and line. A bare call that survives will silently flip meaning when 181-04 removes the parameter default.</fails_when>
    <automated>cd /workspaces/firestarter_app && C=$(/usr/bin/grep -rc 'write_scope="none"' --include='*.py' tests | /usr/bin/grep -v ':0$' | wc -l) && test "$C" -eq 0</automated>
    <fails_when>non-zero exit — at least one file under `tests/` still contains the literal `write_scope="none"`, including in a docstring or a name; the count of non-zero files must be exactly 0.</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -qF 'from firestarter.cli_handlers import _resolve_write_scope' tests/test_derive_plan_structural_sentinel.py && ! /usr/bin/grep -qF 'def test_resolve_write_scope_returns_partial_for_every_uv_row' tests/test_derive_plan_structural_sentinel.py</automated>
    <fails_when>non-zero exit — the module-level import or the test whose subject is the doomed function survives, which would redden all 31 tests in that module at collection time when 181-04 deletes `_resolve_write_scope`.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_erase_flag_invariants.py tests/test_chip_test_blank_check_order.py tests/test_chip_test_timing.py tests/test_diagnostic_report.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary line carries a `failed`, `error` or `skipped` word rather than a bare `N passed` — the six modules must be wholly green after the migration. Baseline collected counts at app base are 162, 83, 6, 5, 6 and 77; record the post-migration counts and the delta in the SUMMARY.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-none-surface-retirement.txt; test -s "$E" && /usr/bin/grep -qF 'explicit_none_after=0' "$E" && /usr/bin/grep -qF 'bare_derive_plan_after=0' "$E" && test "$(/usr/bin/grep -cE '^(repoint|delete)' "$E")" -ge 1</automated>
    <fails_when>non-zero exit — the transcript is missing or empty, either after-count scalar is absent, or no per-site disposition row was written, which would leave the 93 dispositions unrecorded.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;B={"tests/test_chip_test.py":621,"tests/test_chip_test_sdp_leg.py":227,"tests/test_erase_flag_invariants.py":27,"tests/test_chip_test_blank_check_order.py":21,"tests/test_chip_test_timing.py":4,"tests/test_diagnostic_report.py":183,"tests/test_derive_plan_structural_sentinel.py":0};bad=[p+":"+str(n) for p,e in B.items() for n in [sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT)] if n>e];print("phase_added_comments="+("0" if not bad else ",".join(bad)));raise SystemExit(1 if bad else 0)'</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file and its measured count show where a `#` comment was added above the app-base baseline, including a trailing one a line-anchored regex cannot see.</fails_when>
  </verify>
  <acceptance_criteria>
    - An AST census over `tests/` finds zero `derive_plan` calls with a `write_scope="none"` literal and zero with no `write_scope` keyword.
    - Zero files under `tests/` contain the literal `write_scope="none"`.
    - `tests/test_derive_plan_structural_sentinel.py` contains neither the module-level `_resolve_write_scope` import nor `test_resolve_write_scope_returns_partial_for_every_uv_row`.
    - Every surviving `derive_plan` call in `tests/` passes an explicit `"full"` or `"partial"`, never a bare two-argument form.
    - The six named modules report a bare `N passed` with no `failed`, `error` or `skipped` word.
    - `evidence/181-02-none-surface-retirement.txt` records `explicit_none_after=0`, `bare_derive_plan_after=0`, a per-file before/after/re-pointed/deleted table, and a flat list of every deleted test name.
    - Every deleted test is recorded in the `SUMMARY.md` with its name, its claim, and why that claim has no subject after D-08/D-09.
    - The four `is_uv_eprom` tests at `tests/test_chip_test.py:315-350` are unchanged — their subject is the engine predicate, which survives.
    - `tokenize` COMMENT-token counts are at or below the seven app-base baselines.
  </acceptance_criteria>
  <done>No test in the tree exercises a shape that is about to stop existing, and no test depends on `derive_plan`'s parameter default. The 15 silent bare calls are explicit, the omitted-write tests are gone with their reasons written down, and the module-level import that would have reddened 31 tests at collection time is removed a wave before it could fire.</done>
</task>

<task type="auto">
  <name>Task 3: Re-key the plan corpus to one plan per name and re-measure every count pin it feeds</name>
  <files>
    firestarter_app/tests/plan_corpus.py,
    firestarter_app/tests/test_derive_plan_structural_sentinel.py,
    firestarter_app/tests/test_derive_plan_no_drop_sweep.py,
    firestarter_app/tests/test_plan_shapes_drift.py,
    firestarter_app/tests/fixtures/plan_shapes.json,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-corpus-reanchor.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/plan_corpus.py` in full — especially the `SWEEP_SCOPES and D-08` docstring paragraph, which already states that `full` plus `partial` is a strict superset of the reachable domain and that sweeping `"none"` would inflate a zero-violations count with rows that cannot violate anything. That paragraph is rewritten, not deleted: the same argument now says the single reachable scope is the whole domain.
    - `/workspaces/firestarter_app/tests/test_derive_plan_structural_sentinel.py:290-340` — the three corpus-size pins at `:300`, `:301`, `:313`, and the `(name, scope)` key destructuring pattern used throughout.
    - `/workspaces/firestarter_app/tests/test_derive_plan_structural_sentinel.py:530-600` and `:685-780` and `:925-1000` — the remaining `1354` pins at `:545`, `:567`, `:593`, `:694`, and every `corpus = plan_corpus()` consumer whose loop unpacks a two-element key.
    - `/workspaces/firestarter_app/tests/test_derive_plan_no_drop_sweep.py:60-200` — the import at `:67`, the sweep at `:115-160` with its `plan_count == 1354` pin at `:154-155`, the single-key lookup `plan_corpus()[("M8720", "full")]` at `:193`, and the pinned 40-member key slice at `:224-250`.
    - `/workspaces/firestarter_app/tests/test_plan_shapes_drift.py:25-110` — `_COMMITTED_ARTIFACT` at `:35`, `_GEN_SCRIPT` at `:36`, `_EXPECTED_AGGREGATE` at `:37-44` with its six absolute numbers, the `_generated_by` banner test, and the stale-artifact regeneration check at `:82-105`.
    - `/workspaces/firestarter_app/tools/measure_plan_shapes.py` — the generator. It must sweep the same single reachable scope the corpus does, or the committed artifact and the corpus will describe different populations.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt` — the reachable-scope rule, reused verbatim.
  </read_first>
  <behavior>
    - `plan_corpus()` returns a mapping keyed by part number alone, with one plan per name, sized 677.
    - `plan_corpus()["M8720"]` is a `Plan` whose steps carry a real write region — the reachable-scope shape, not the omitted-write one.
    - `tools/measure_plan_shapes.py` regenerates `tests/fixtures/plan_shapes.json` and the stale-artifact check passes against the committed copy.
    - `_EXPECTED_AGGREGATE`'s six numbers all match the regenerated artifact, and `rows` and `distinct_part_numbers` are unchanged at 746 and 677.
  </behavior>
  <action>
Re-key the corpus and re-anchor every pin it feeds, in ONE commit inside
`/workspaces/firestarter_app`, so the tree is green at the commit boundary.

In `tests/plan_corpus.py`: delete `SWEEP_SCOPES` and change `plan_corpus()` to build
`{name: derive_plan(name, REAL_DB, write_scope="partial" if <name is UV> else "full")}` over
`PART_NUMBERS`, using Task 1's recorded reachable-scope rule and `chip_test.is_uv_eprom` on the full DB
dict — never a protocol proxy, per `Plan.is_uv`'s own docstring. Keep the explicit `write_scope`
keyword: the parameter still exists in this wave and a bare call would mean `"none"`. Rewrite the
`SWEEP_SCOPES and D-08` docstring paragraph so it makes the same vacuity argument about the new single
scope, and add one sentence recording that `181-04` drops the keyword itself. `PART_NUMBERS`, `REAL_DB`,
`all_rows`, `mock_operator`, `plan_with_steps` and `step` are unchanged.

In `tests/test_derive_plan_structural_sentinel.py` and `tests/test_derive_plan_no_drop_sweep.py`:
change every `corpus[(name, scope)]` lookup and every two-element key unpacking to the single-key form,
and re-anchor every `1354` pin to **677**. `len(PART_NUMBERS) == 677` at `:300` does NOT move. Re-point
`plan_corpus()[("M8720", "full")]` to `plan_corpus()["M8720"]`. The pinned 40-member key slice becomes
a 40-member name slice; keep it a pinned literal list, not a derived one, since its whole purpose is to
fail when the key set drifts.

**The four moving aggregate numbers must be MEASURED, not computed.** Run
`tools/measure_plan_shapes.py` to regenerate `tests/fixtures/plan_shapes.json`, read the regenerated
values, and set `_EXPECTED_AGGREGATE` from them. `plans`, `total_steps`, `unsupported_steps` and
possibly `distinct_shape_families` all move; `rows` stays 746 and `distinct_part_numbers` stays 677.
Do not assume `total_steps` and `unsupported_steps` simply halve — they might, and asserting a halved
number without generating the artifact would be a fabricated measurement, which this plan's
prohibitions forbid. If `distinct_shape_families` changes, say so in the `SUMMARY.md` and name which
family disappeared: a shape family that existed only under the omitted-write scope is a real finding
about what the sweep was covering.

Update the generator only if it hard-codes a scope list of its own; it must sweep the same single
reachable scope the corpus does, or the artifact and the corpus describe different populations and the
drift gate is measuring the wrong thing.

Then write `evidence/181-02-corpus-reanchor.txt` recording, each labelled `measured`:
`corpus_size_before=1354`, `corpus_size_after=677`, `part_numbers=677` (unchanged),
`plan_shapes_rows_before=746` / `_after=746`, `plan_shapes_plans_before=1354` / `_after=<measured>`,
`plan_shapes_total_steps_before=16248` / `_after=<measured>`,
`plan_shapes_unsupported_steps_before=9304` / `_after=<measured>`,
`plan_shapes_distinct_shape_families_before=8` / `_after=<measured>`, and
`aggregate_numbers_source=tools/measure_plan_shapes.py regeneration`.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`. Then run the
cleanliness legs.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import sys;sys.path.insert(0,".");from tests.plan_corpus import plan_corpus,PART_NUMBERS;c=plan_corpus();print("corpus_size="+str(len(c)));print("part_numbers="+str(len(PART_NUMBERS)));assert len(PART_NUMBERS)==677,len(PART_NUMBERS);assert len(c)==677,len(c);k=next(iter(c));assert isinstance(k,str),type(k);p=c["M8720"];assert any(s.write_region is not None for s in p.steps),"reachable-scope plan carries no write region"'</automated>
    <fails_when>non-zero exit — the printed `corpus_size=` is not 677, the key type is a tuple rather than a string, or the M8720 plan carries no step with a write region, which would mean the corpus is still deriving the omitted-write shape.</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -qF 'SWEEP_SCOPES' tests/plan_corpus.py tests/test_derive_plan_structural_sentinel.py tests/test_derive_plan_no_drop_sweep.py && ! /usr/bin/grep -rqF '1354' tests/</automated>
    <fails_when>non-zero exit — `SWEEP_SCOPES` survives in one of the three named modules, or the literal `1354` survives anywhere under `tests/`, which means a count pin still describes the two-scope corpus.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_plan_shapes_drift.py tests/test_derive_plan_no_drop_sweep.py tests/test_derive_plan_structural_sentinel.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. Baseline collected counts at app base are 6, 6 and 31; the sentinel module loses one test in Task 2, so record the measured post-migration counts in the SUMMARY.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import json,pathlib;a=json.loads(pathlib.Path("tests/fixtures/plan_shapes.json").read_text(encoding="utf-8"));assert a.get("_generated_by"),"missing _generated_by banner";import re,ast;src=pathlib.Path("tests/test_plan_shapes_drift.py").read_text(encoding="utf-8");t=ast.parse(src);exp=[n for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,"id","")=="_EXPECTED_AGGREGATE" for x in n.targets)][0];d={k.value:v.value for k,v in zip(exp.value.keys,exp.value.values)};print("expected="+str(d));assert d["rows"]==746,d;assert d["distinct_part_numbers"]==677,d;assert d["plans"]==677,d'</automated>
    <fails_when>non-zero exit — the regenerated artifact lost its `_generated_by` banner, or `_EXPECTED_AGGREGATE` does not carry `rows 746`, `distinct_part_numbers 677` and `plans 677`. The printed `expected=` mapping shows the actual six values for the SUMMARY.</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff 04fd982 --name-only -- firestarter/) && test -z "$D"</automated>
    <fails_when>non-zero exit — a file under `firestarter/` differs from app base `04fd982`, which means this plan changed product source it was forbidden to touch. Only `181-01`'s two lines in `diagnostic_report.py` are permitted, so if that file appears here, confirm the diff is `181-01`'s and no more before proceeding.</fails_when>
    <automated>A=$(git -C /workspaces/firestarter status --porcelain) && B=$(git -C /workspaces/firestarter_app status --porcelain) && test -z "$A" && test -z "$B"</automated>
    <fails_when>non-zero exit — the firmware submodule is dirty or the app repo has uncommitted work. This leg must run AFTER the commit or it reddens on this task's own edits.</fails_when>
  </verify>
  <acceptance_criteria>
    - `plan_corpus()` returns 677 entries keyed by a plain string, and `plan_corpus()["M8720"]` carries a step with a non-`None` write region.
    - `SWEEP_SCOPES` is absent from `tests/plan_corpus.py` and from both consumer modules.
    - The literal `1354` appears nowhere under `tests/`.
    - `_EXPECTED_AGGREGATE` carries `rows 746`, `distinct_part_numbers 677`, `plans 677`, and its remaining three numbers match the regenerated `tests/fixtures/plan_shapes.json`.
    - `tests/fixtures/plan_shapes.json` still carries its `_generated_by` banner and was produced by running `tools/measure_plan_shapes.py`, never hand-edited.
    - `tests/test_plan_shapes_drift.py`, `tests/test_derive_plan_no_drop_sweep.py` and `tests/test_derive_plan_structural_sentinel.py` all report a bare `N passed`.
    - `evidence/181-02-corpus-reanchor.txt` records every before and after number with `measured` labelling and names the generator as the source.
    - `git diff 04fd982 --name-only -- firestarter/` lists at most `firestarter/diagnostic_report.py`, and only with plan `181-01`'s two-line change.
    - Both porcelain legs print nothing, run after this task's commits.
  </acceptance_criteria>
  <done>The corpus and every count pin it feeds describe the domain `dev test` can actually reach, the four moving aggregate numbers come from a regenerated artifact rather than from arithmetic, and plan `181-04` now faces a uniform kwarg-drop across 102 identical call sites instead of a 117-site migration entangled with a product deletion.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Manifest for plan `181-02`.

**CREATED:** three `evidence/*.txt` transcripts
(`181-02-derive-plan-equivalence.txt`, `181-02-none-surface-retirement.txt`,
`181-02-corpus-reanchor.txt`).

**DELETED by this plan:** `tests/plan_corpus.SWEEP_SCOPES`; `tests/plan_corpus.plan_corpus()`'s
`(name, scope)` key shape; the module-level
`from firestarter.cli_handlers import _resolve_write_scope` in
`tests/test_derive_plan_structural_sentinel.py`;
`test_resolve_write_scope_returns_partial_for_every_uv_row`;
`test_derive_plan_write_scope_none_unchanged_by_region_policy`;
`test_derive_plan_write_scope_rejects_unknown_value`; and every other test whose subject is the
omitted-write shape — the full list is written into the `SUMMARY.md` and into
`evidence/181-02-none-surface-retirement.txt`, because the drift verifier needs to know a missing test
was intended.

**CHANGED IN VALUE:** the corpus size pin (1354 to 677) at all eight sites; `_EXPECTED_AGGREGATE`'s
`plans`, `total_steps`, `unsupported_steps` and possibly `distinct_shape_families`; the regenerated
`tests/fixtures/plan_shapes.json`.

**NOT deleted by this plan, deliberately:** every product symbol. `derive_plan`'s `write_scope`
parameter, `_WRITE_SCOPE_NONE`, `_WRITE_SCOPE_PARTIAL`, `_WRITE_SCOPE_FULL`, `_WRITE_SCOPES`, the
`ValueError` ladder, `write_execute`, `Plan.locked_destructive`, `BannerCounts.locked_steps`,
`count_applicable`'s extra term, `sdp_oracle_applicable`'s second arm, `_banner_dict`'s
`locked_steps`, `cli_handlers._resolve_write_scope` and `cli_handlers._is_uv_eprom` are all
byte-unchanged. They die in `181-04`.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated proportionately: this plan changes only tests and one committed
test fixture. It opens no network or serial path, adds no dependency, reads no credential, and does
not change a single byte that reaches a filed GitHub issue. The real hazard is epistemic — a
re-anchored number that was reasoned about rather than measured.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped database to the sweep domain | `EpromDatabase(skip_local_override=True)` is load-bearing: the plain constructor reads a developer's `~/.firestarter/database.json` and would make every count in this plan machine-dependent. |
| generator to committed artifact | `tests/fixtures/plan_shapes.json` is generated and carries a `_generated_by` banner. A hand-edited artifact would make the drift gate assert its own hand-typed numbers. |
| measurement to record | Task 1's digest is the only evidence `181-04`'s deletion is behaviour-identical. If it were transcribed rather than computed, the deletion would proceed on a belief. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-181-06 | Tampering | `_EXPECTED_AGGREGATE` and `tests/fixtures/plan_shapes.json` | high | mitigate | A hand-typed aggregate turns an absolute drift gate into a tautology. Mitigated by regenerating through `tools/measure_plan_shapes.py`, by the `_generated_by` banner assertion, by the module's own stale-artifact check, and by the `aggregate_numbers_source=` scalar in the transcript. |
| T-181-07 | Tampering | the 15 bare `derive_plan` calls | high | mitigate | Left bare, each silently inverts meaning the moment `181-04` removes the parameter default — a test that asserted an omitted write would start asserting it about a write-executing plan and could pass for the wrong reason. Mitigated by the AST census leg, which fails on a count above zero and names every survivor by file and line. |
| T-181-08 | Repudiation | the 93 site dispositions | medium | mitigate | Every re-point and every deletion is recorded per-file in the transcript and per-test in the `SUMMARY.md`, so a reviewer can see what stopped being tested rather than inferring it from a diff. |
| T-181-09 | Tampering | the generated `chip_database.json` and the firmware submodule | low | mitigate | Both asserted byte-unchanged by the porcelain legs. `chip_database.json` is generated and is never hand-edited. |
| T-181-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package; every module used is Python standard library plus the already-installed test extra. No Package Legitimacy Audit is required and no install checkpoint is inserted. |

Two threats are rated `high` and both are dispositioned **mitigate** with runnable gates, so nothing
here blocks.
</threat_model>

<verification>
1. `evidence/181-02-derive-plan-equivalence.txt` carries the 64-hex reachable-scope digest, the
   recorded serializer field order, and all five zero-valued de-risking measurements.
2. An AST census over `tests/` returns zero `write_scope="none"` calls and zero bare `derive_plan`
   calls.
3. `tests/test_derive_plan_structural_sentinel.py` no longer imports `_resolve_write_scope` at module
   scope.
4. `plan_corpus()` is 677 entries keyed by name; the literal `1354` is gone from `tests/`.
5. `tests/fixtures/plan_shapes.json` is regenerated, keeps its `_generated_by` banner, and
   `_EXPECTED_AGGREGATE` matches it.
6. All nine touched test modules report a bare `N passed`.
7. `git diff 04fd982 --name-only -- firestarter/` lists nothing beyond plan `181-01`'s change.
8. All 19 `FROZEN_HASHES` literals still byte-identical to app base.
9. `tokenize` COMMENT-token counts at or below every app-base baseline.

**Do not** run the seven-leg battery here, do not invoke `check_rekey_ledger.py`, and do not assert a
suite floor of 2241 or 2242. The battery runs once, in `181-08`.
</verification>

<success_criteria>
- D-09's equivalence and D-08's two dead-path claims are measured over all 677 names, with the digest
  and the serializer field order recorded so `181-04` can reproduce them.
- The `write_scope="none"` test surface is retired to zero, including the 15 bare calls neither source
  document found, with every disposition recorded.
- The corpus and its eight count pins describe the reachable domain, and the four moving aggregates
  come from a regenerated artifact.
- Zero product-source lines changed by this plan; zero `#` comments added; firmware untouched.
- RPT-B2's deletion is now a small mechanical edit rather than a 117-site migration.
</success_criteria>

<output>
Create `.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-02-SUMMARY.md`
when done. Record in it: the reachable-scope digest and serializer field order; every deleted test with
its claim and why that claim has no subject after D-08/D-09; the measured before and after aggregate
numbers with `distinct_shape_families` called out by name if it moved; the post-migration pytest count
for each of the nine touched modules; any line number that had moved since this plan was written; and
every rationale that would otherwise have been a source comment.
</output>

