---
phase: 181-report-fidelity-schema-2-0-canonical-naming-hygiene-close
plan: 09
type: execute
wave: 8
depends_on: [181-08]
files_modified:
  - firestarter_app/firestarter/diagnostic_report.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_diagnostic_report.py
  - firestarter_app/tests/test_dev_test_cmd.py
  - firestarter_app/tests/test_voltage_field_census.py
  - .claude/skills/devtest-triage/SKILL.md
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-voltage-census.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-skill-same-commit.txt
autonomous: true
requirements: [RPT-B1, RPT-F2]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The two standalone rail slots are deleted from the `DiagnosticReport` dataclass, from `_voltage_dict()` and therefore from the schema, and the class docstring sentence that named them dies with them (RPT-B1)"
    - "`_voltage_dict()` emits exactly the four destructive before/after keys, and `_VOLTAGE_KEYS` was shrunk in the SAME commit with its counted-keys docstring moved from six to four (RPT-B1, D-14)"
    - "This is the FIRST time a Phase-174 key-list pin has ever been SHRUNK — `git log -S'_VOLTAGE_KEYS'` returns only the commit that created it, so the additive precedent is an additive precedent only, and the shrink's non-vacuity is its own verification leg rather than a copy of one"
    - "RPT-B1's premise is proven by TEST, not asserted: an attribute-scoped AST census over `firestarter/` finds zero assignments to either name on a report object and zero emit sites, and the one test that assigned them by hand is re-pointed with its reason recorded (D-12)"
    - "The census is attribute-scoped rather than textual, because the same two names are DATABASE fields on an unrelated code path: a textual scan returns about twenty false positives across the layout module and three test modules, which are a different field with the same name (D-12's measured trap)"
    - "The census has been observed RED against a planted in-memory assignment to a report object, with anchor uniqueness asserted before the mutation and no fixture file written"
    - "The parse-input fixtures that carry the deleted keys are DELIBERATELY preserved: a pre-2.0 body carrying them must keep parsing, which is RPT-E2, so removing them would delete the evidence that the deletion is forward-only"
    - "The two frozen `devtest-triage` fixtures still parse under schema 2.0 after the deletion, re-proven post-deletion rather than only before it, and neither fixture file is modified (RPT-E2)"
    - "`.claude/skills/devtest-triage/SKILL.md` names the four surviving rail fields and states plainly that they are REGULATOR RAIL readings, never socket readings — a rig with the socket unhooked still reads a healthy rail — and that a body carrying the two deleted keys is a pre-2.0 report (RPT-F2)"
    - "The skill's datasheet-cross-check example row is left byte-unchanged, with the measurement recorded: its value is the DATABASE's programming voltage compared against the datasheet via the `firestarter info` view, not the report's rail reading, so rewriting it as a rail field would turn a correct row into a false one"
    - "CONTEXT.md's claim that the skill's example row is the report field is amended in place, carrying a Phase 181 amendment marker, so the falsified premise cannot be re-derived from the record"
    - "D-5's same-commit rule is discharged as far as it can be and the residue is stated: the deletion is in the app submodule and the skill is in the meta repo, so ONE commit is impossible; both land inside this task, the skill first so no window exists in which the report has dropped a key the skill still presents as live, each commit message names the other side, and both shas are recorded"
    - "All 19 `FROZEN_HASHES` literals are byte-identical to app base `04fd982` — the deleted keys were never in `dedup_fingerprint`'s pre-image (D-16)"
    - "No `#` comment was added to product source, measured by a non-increase `tokenize` COMMENT-token census, and exactly 0 for the new census module"
    - "`git -C /workspaces/firestarter status --porcelain` prints nothing and the generated `chip_database.json` is byte-unchanged"
  prohibitions:
    - "The two frozen devtest-triage fixtures must never be regenerated from a live to_dict() output -- their own headers forbid it and they are RPT-E2's forward-only parse targets"
    - "The parse-input fixtures carrying the deleted keys must never be scrubbed of them -- they are the evidence that the deletion is forward-only"
    - "The skill's datasheet-cross-check example row must not be rewritten as if its value were the report's rail reading -- it is the database's programming voltage, a different field with the same name"
    - "A name census over these two fields must never be textual -- the same names are database fields on an unrelated path and a textual scan fails open by drowning in false positives"
    - "No change may re-key any of the 19 frozen dedup hashes -- a filed issue body carries its hash and count_agreeing never re-hashes, so a re-key is permanent for the historical corpus"
    - "A gate must not read green while the rule it guards is violated"
    - "No comment may be written into product source, and firestarter_app/tests/ counts as product source"
  artifacts:
    - path: "firestarter_app/firestarter/diagnostic_report.py"
      provides: "the four-key `_voltage_dict` and the dataclass with the two standalone slots gone"
      contains: "_voltage_dict"
    - path: "firestarter_app/tests/test_voltage_field_census.py"
      provides: "D-12's attribute-scoped AST census, its planted-RED leg, its vacuity leg and the measured false-positive count a textual scan would return"
      contains: "ast"
    - path: ".claude/skills/devtest-triage/SKILL.md"
      provides: "the rail-not-socket statement over the four surviving fields and the pre-2.0 note (RPT-F2)"
      contains: "vpp_after_mv"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md"
      provides: "the in-place amendment of the falsified skill-row claim, carrying a Phase 181 amendment marker"
      contains: "Status: Phase 181 amendment"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-voltage-census.txt"
      provides: "the census result, the planted RED transcript, the measured textual-false-positive count and the shrink's non-vacuity proof"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-skill-same-commit.txt"
      provides: "the two-repo same-commit adjudication with both shas, the ordering and its reason, and the untouched example row recorded with its measurement"
  key_links:
    - from: "firestarter_app/tests/test_voltage_field_census.py"
      to: "firestarter_app/firestarter/diagnostic_report.py"
      via: "the census parses the module's own source and walks assignment targets, matching an attribute on a report object rather than a text pattern"
      pattern: "diagnostic_report"
    - from: ".claude/skills/devtest-triage/SKILL.md"
      to: ".claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md"
      via: "the skill's pre-2.0 note explains the deleted keys a triager still sees in the frozen fixtures and in every already-filed body"
      pattern: "schema_version"
---

<objective>
Delete two report fields no code path has ever assigned, and prove that rather than assert it.

`voltage.vpp_mv` and `voltage.vpe_mv` are the standalone non-destructive rail slots. `render()`
already dropped their rows; only `to_dict()` still emits them, and the only assignments to either
name on a report object anywhere in the tree are two lines in a test that builds the shape by hand
(D-12, verified). RPT-B1 deletes the dataclass fields, the two emits and the schema keys, and
requires the no-path-assigns-them claim to be a **test**. RPT-F2 lands the `devtest-triage` skill's
update alongside it (D-5), naming the four surviving rail fields and stating plainly what they do not
prove.

Purpose: this is the phase's only deletion through `_VOLTAGE_KEYS`, and Phase 174 pinned those keys
saying in as many words that Phase 181's deletion would have to argue with a gate that predates it.
The argument is D-12's census. It also has to be a HONEST census: the same two names are database
fields on an unrelated code path, so a textual scan drowns in false positives and would fail open.
Output: two deleted fields, a shrunk key-list pin, a new attribute-scoped census module with its
planted RED, a skill update, a CONTEXT.md amendment and two evidence transcripts.

**One premise in the record is falsified and is corrected rather than followed.** CONTEXT.md and
`181-PATTERNS.md` both identify the skill's `vpp_mv: 13500` example row as the report field and ask
for it to be replaced by the before/after fields. Measured: that row sits inside the
datasheet-versus-database cross-check table, whose firestarter-side column is the `firestarter info`
view — it is the DATABASE's programming voltage for the part, a different field with the same name.
Rewriting it as a rail reading would turn a correct row into a false one.
</objective>

<orchestrator_dispositions>
Measurements taken against the live tree. These **OVERRIDE** `181-CONTEXT.md` and `181-PATTERNS.md`
where they conflict, and Task 3 amends CONTEXT.md so the conflict does not survive in the record.

- **The skill's `:375` occurrence is the DATABASE field, measured.** `.claude/skills/devtest-triage/SKILL.md:375`
  reads a table row comparing a datasheet's programming voltage against `vpp_mv: 13500`, inside the
  section whose header row is "Check | Datasheet source | firestarter source | Failure means" and
  whose VPP row's firestarter-side column is the `firestarter info` view's `VPP:` line. The report's
  own rail fields are named separately at `:330`
  (`voltage.vpp_before_mv` / `vpe_before_mv`). `181-PATTERNS.md`'s claim that `:375` is the sole
  occurrence is TRUE as a text count and WRONG as an identification. **Do not rewrite that row.**
- **The skill carries no `locked_steps` occurrence at all**, measured, so RPT-B2 needed no skill edit
  and plan `181-04` correctly did not make one.
- **The two report-object assignments are `tests/test_diagnostic_report.py:982-983`**, and the two
  emit sites are the `_voltage_dict` lines. `_make_sampler` (`cli_handlers.py:2230-2254`) assigns
  ONLY the four before/after fields. That is D-12's premise and it re-verified TRUE this session.
- **A textual scan is dishonest here.** `/usr/bin/grep -rn "vpp_mv"` over `firestarter/`, `tests/`
  and `tools/` returns about twenty hits that are all the DATABASE field — `ic_layout.py`,
  `test_check_dispatch_invariants.py`, `test_chip_resolver.py`, `test_eprom_database.py`,
  `test_extra_chips_supplement.py`, `test_diff_db_gate.py`, `test_sdp_capability.py`,
  `test_wire_dict_equivalence.py`, `test_variant_decode_evidence_stability.py` — plus the wire-dict
  key set in `tools/check_devtest_orchestrator.py`. The census MUST match an attribute on a report
  object, not a substring.
- **Two parse-INPUT fixtures deliberately carry the deleted keys**, measured at
  `tests/test_parse_devtest_issue.py:536` and `:674` (`"vpp_mv": "not measured"`). They are inputs
  representing an older body, not report outputs. **Leave them.** Removing them would delete the
  evidence that the deletion is forward-only, which is RPT-E2.
- **One live assertion on the deleted keys is in the end-to-end module**, measured at
  `tests/test_dev_test_cmd.py:839-840`, asserting both keys render as the not-measured sentinel on a
  real run. It becomes an absence assertion.
- **`render()`'s docstring already names the deleted fields** in the sentence recording that the
  six-value voltage row became one row per rail, "dropping the `vpp_mv`/`vpe_mv` standalone slots
  that no code path assigns". That sentence stays true in substance but names fields that no longer
  exist; it is a required edit. The `DiagnosticReport` class docstring's clause naming them is the
  second.
- **`_write_step_index`'s docstring mentions the omitted-write scope**, which plan `181-04` already
  handled. Do not re-edit it here.
- **`_VOLTAGE_KEYS` has no companion length pin** (only `_TRANSPORT_HEALTH_KEYS` has one), but its
  counted-keys docstring says six and explicitly names the two deletes as RPT-B1's.
- **All three `plan:pre` detectors resolved to their skip branch, re-run this session.**
  `api-coverage` `{"detected":false}` — no `COVERAGE.md`. `assumption-delta scan 181`
  `{"detected":false,"signals":[]}` — no `<assumption_delta_decision>` block. No ORM-relevant file —
  no `[BLOCKING]` schema-push task.
- **The re-key ledger is RETIRED**, the green-tree battery is **seven** legs, and the suite floor is
  **2239**. The battery runs once, in plan `181-10`.
</orchestrator_dispositions>

<flagged_assumptions>
The spec-less probe fallback is ACTIVE (no SPEC.md). The report handed to this session carries **28
applicable edges over 18 requirements**, all arriving `unresolved`; plans `181-01`..`181-04` were
authored against a 30-row file whose two extra rows are `HYG-02`'s and `HYG-03`'s. Recorded, not
reconciled silently.

**This plan's share is 2 rows, both `unclassified`, both surfaced as flagged planner assumptions.
Never auto-resolved with a backstop, never dismissed — the same disposition plans `181-01` and
`181-04` gave their `unclassified` rows.**

- **FA-181-09 (`RPT-B1`, unclassified).** The probe returned `unclassified — review manually`.
  RPT-B1 is a deletion, so it has no input domain: the verifiable properties are the fields' absence
  from the dataclass, from the emitted mapping and from the key-list pin, plus the census proving no
  path assigns them. All four are authored as truths above. **If the operator disagrees, the edge to
  author is what `voltage` should serialize for a report on which the sampler never ran at all** —
  today the four survivors each render the not-measured sentinel, which is the existing honest
  fallback and is unchanged by this plan.
- **FA-181-10 (`RPT-F2`, unclassified).** Also `unclassified — review manually`. RPT-F2 is a
  documentation edit whose only checkable properties are textual presence and absence, authored as
  truths above. **If the operator disagrees, the edge to author is what the skill should tell a
  triager about a body whose `voltage` block is absent entirely** — no such body exists, because
  `to_dict()` always emits the block, but a hand-edited issue body could omit it and the skill says
  nothing about that case today.

**No-silent-drop equality:** 2 rows in scope == 0 authored as row resolutions + 2 flagged. Holds.

**Prohibition recall (§B, run in-prompt).** Stage 1 over-produced about a dozen raw must-NOTs;
Stage 2 kept the integrity items authored above, descriptor-less, never under `truths`. Four are
specific to this plan: **the frozen skill fixtures must never be regenerated**, **the parse-input
fixtures must keep their deleted keys**, **the skill's datasheet row must not be rewritten as a rail
field**, and **the census must never be textual**. The last is the sharpest: a textual census here
would return about twenty matches on an unrelated database field, and the obvious "fix" — filtering
them out by filename — is precisely how a gate learns to fail open. **Canon-referral breadcrumb:**
no OWASP, injection, path-traversal, prototype-pollution, GDPR or generic-fairness candidate survived
Stage 2 here. The outward-facing artifacts are the filed issue body, which loses two keys, and the
skill document, which gains a disclosure sentence; both are carried in `<threat_model>`.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md
</context>

<environment>
Three git repositories; **branch names differ between meta and the submodules**, and this plan is the
only one in the phase that commits a PRODUCT change and a META change as a paired unit.

- Meta `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`. Owns `.planning/` and
  `.claude/`, including the skill and its fixtures.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, **phase base app
  HEAD `04fd982`**. **Commit inside the submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — **not touched**. Must stay porcelain-clean.

`git -C /workspaces/firestarter_app status --porcelain firestarter/` names the app's own **package
directory**, not the firmware submodule.

Python is `/workspaces/firestarter_app/.venv311/bin/python` — 3.11, matching app CI. The devcontainer
default is 3.12 and that divergence has already broken beta CI once.

`pytest` `addopts` is `-ra -q`; a count line needs `-o addopts=""`.

Shell `grep` here is **ugrep and silently honours `.gitignore`** — every evidence-grade scan uses
`/usr/bin/grep`. `-qF` with a dash-leading pattern exits 2 and fails OPEN; use `-qFe`.

**Every line number in this plan is measured against app `04fd982` plus plans `181-01`, `181-04`
through `181-08`, and shifts as this plan's own edits land.** Anchor every edit by content.

`tests/test_skip_census.py`'s three failures are **pre-existing** and unrelated.

**No push, no PR, no tag, no branch cut** in any task (D-22).
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and CONTEXT.md D-26 are
**non-overridable**, and `firestarter_app/tests/` counts as product source. Write **no `#` comment**.
Every rationale — why the census is attribute-scoped, why the parse-input fixtures keep the deleted
keys, why the skill's example row is untouched — goes into a **docstring**, into the skill's own prose
(markdown, not source) or into the plan `SUMMARY.md`.

`.claude/skills/devtest-triage/SKILL.md` is a markdown document, not product source; its prose is the
deliverable and the no-comments rule does not reach it.

The new census module carries **exactly 0** comment tokens. Re-measure the counts for
`firestarter/diagnostic_report.py`, `tests/test_diagnostic_report.py`, `tests/test_dev_test_cmd.py`
at the head of Task 1; the gate is **non-increase against those measured values**, and
`tests/test_blast_radius_invariance.py` is **exactly 0**. `diagnostic_report.py`'s count may FALL as
docstring-adjacent narrative naming the deleted fields is removed, which the non-increase direction
permits.
</comment_discipline>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the two standalone rail slots, shrink the key-list pin in the same commit, and land the skill update as its paired half</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` with plans `181-01` through `181-08` committed and a clean working tree; `git -C /workspaces/firestarter status --porcelain` prints nothing; and both frozen `devtest-triage` fixtures are unmodified.</precondition>
  <reversibility rating="costly">A schema deletion is not reversible in the historical corpus: every already-filed body still carries the two keys and no migration is possible. What makes it safe to take is that the deletion is FORWARD-only — both consumers accept `schema_version` by presence and neither requires the keys — and that no code path has ever assigned them, so no measurement is lost. Restoring them later would mean re-adding two dataclass fields, two emits, two schema keys and a pin entry, and would put two keys back that no run populates.</reversibility>
  <files>
    firestarter_app/firestarter/diagnostic_report.py,
    firestarter_app/tests/test_blast_radius_invariance.py,
    firestarter_app/tests/test_diagnostic_report.py,
    firestarter_app/tests/test_dev_test_cmd.py,
    .claude/skills/devtest-triage/SKILL.md,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-skill-same-commit.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:630-700` — the `DiagnosticReport` dataclass: the docstring clause naming the standalone slots ("plus standalone non-destructive readings"), the four before/after fields and the two standalone fields beneath them.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:733-756` — `_voltage_dict` in full: its docstring's statement that it is the one place in the module that knows the sentinel for a voltage reading, the four before/after emits and the two standalone emits.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:909-945` — `render()`'s docstring, including the sentence that names the standalone slots as already dropped from the console. It stays true in substance and must stop naming fields that no longer exist.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2230-2254` — `_make_sampler`, quoted as D-12's proof rather than as an edit target: it assigns ONLY the four before/after fields.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:110-124` — `_VOLTAGE_KEYS`, six entries, and its counted-keys docstring, which names the two deletes as RPT-B1's and states that Phase 181's deletion has to argue with a gate that predates it.
    - `/workspaces/firestarter_app/tests/test_diagnostic_report.py:960-995` — the destructive-versus-standalone voltage test: the four before/after assertions, the two hand assignments at `:982-983` and the two standalone assertions that follow. This is the one test that assigns the fields.
    - `/workspaces/firestarter_app/tests/test_dev_test_cmd.py:820-845` — the end-to-end voltage test whose last two assertions name the deleted keys.
    - `/workspaces/firestarter_app/tests/test_parse_devtest_issue.py:530-540` and `:668-680` — the two parse-INPUT fixtures carrying the deleted keys. **Read them so you do not touch them:** they represent an older body and they are RPT-E2's evidence.
    - `/workspaces/.claude/skills/devtest-triage/SKILL.md:310-345` — the datasheet cross-check table, its header row, its VPP row (whose firestarter-side column is the `firestarter info` view) and the separate report-voltage sentence at `:330`. **Identify for yourself which occurrence is the report field before editing anything.**
    - `/workspaces/.claude/skills/devtest-triage/SKILL.md:370-380` — the example mismatch row at `:375`. Confirm from its column structure that its value is the database's programming voltage. **It is not edited.**
    - `/workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md` — the frozen schema-1.2 fixture, its FROZEN FIXTURE header forbidding regeneration, and the deleted keys it deliberately still carries.
  </read_first>
  <behavior>
    - `dataclasses.fields(DiagnosticReport)` contains the four before/after rail names and neither standalone name.
    - `to_dict()["voltage"]` has exactly four keys.
    - Constructing a `DiagnosticReport` with a keyword named for either deleted field raises `TypeError`.
    - `_VOLTAGE_KEYS` has four entries and the voltage key-list pin passes.
    - The skill document names all four surviving rail fields and states the rail-not-socket property.
    - The skill's datasheet cross-check example row is byte-identical to base.
    - Both frozen skill fixtures are byte-identical to base.
    - `dedup_fingerprint` is unchanged for all 19 registered shapes.
  </behavior>
  <action>
Two commits, in this order, both inside this task.

**First, the meta-repo half (RPT-F2).** Edit `.claude/skills/devtest-triage/SKILL.md`. Extend the
report-voltage sentence in the datasheet-cross-check section so it: names all four surviving rail
fields, both before and after, for VPP and VPE; states plainly that these are REGULATOR RAIL readings
and never socket readings, so a rig whose socket is unhooked still reads a healthy rail and a healthy
rail proves nothing about contact — the report says so itself in its own disclosure field, which the
sentence names so a triager can find it in a body; and states that as of schema 2.0 the report no
longer carries the two standalone rail keys, so a body carrying them is a pre-2.0 report, which the
two frozen fixtures under this skill's own `fixtures/` directory deliberately still are and which
must never be regenerated.

**Do not touch the datasheet cross-check example row.** Its value is the database's programming
voltage for the part, compared against the datasheet through the `firestarter info` view — a
different field that happens to share a name with the report's deleted key. Record that
identification in the transcript with the column structure that establishes it.

Commit that edit in `/workspaces` on `gsd/v1.36-dev-test-fidelity-planning`, with a message naming
RPT-F2, D-5 and the app-side deletion it pairs with. The skill goes FIRST deliberately: its new
sentence is already true before the deletion lands (the four rail fields are the ones a triager
should read today), so there is no window in which the report has dropped a key the skill still
presents as live. State that ordering and its reason in the transcript.

**Second, the app half (RPT-B1).** In `firestarter/diagnostic_report.py`: delete the two standalone
rail fields from the `DiagnosticReport` dataclass, delete their two emits from `_voltage_dict()`, and
delete the class-docstring clause that named them. Rewrite `render()`'s docstring sentence so it
records the console change without naming fields that no longer exist — the substance ("the single
six-value voltage row became one row per rail") stays, the dead names go. Leave `_voltage_dict`'s
own docstring statement about being the one place that knows the sentinel exactly as it is; it is
still true of the four survivors. Keep every string literal clear of the 14 forbidden claim phrases
`tools/check_diagnostic_report_claims.py` scans for.

In `tests/test_blast_radius_invariance.py`, same commit (D-14): remove both names from
`_VOLTAGE_KEYS`, leaving four entries, and move its counted-keys docstring from six to four naming
Phase 181 plan 09 and RPT-B1. Record in that docstring the fact `181-PATTERNS.md` measured and no
precedent covers: `git log -S'_VOLTAGE_KEYS'` returns only the commit that created these pins, so
this is the FIRST shrink of a Phase-174 key-list pin, and the additive precedent from Phase 178 is an
additive precedent only. Say it plainly rather than citing a precedent that does not exist.

In `tests/test_diagnostic_report.py`: re-point the destructive-versus-standalone voltage test. Delete
the two hand assignments and the two standalone assertions, keep the four before/after assertions,
and add an assertion that the emitted voltage mapping has exactly four keys and neither deleted name.
Its docstring records what changed and why: the standalone half of its claim had no subject after
RPT-B1, because no code path ever assigned those fields, and the surviving half is the destructive
before/after pairing, which is untouched.

In `tests/test_dev_test_cmd.py`: turn the two end-to-end assertions on the deleted keys into an
assertion that the real run's voltage mapping carries exactly the four surviving keys.

**Leave `tests/test_parse_devtest_issue.py` alone.** Its two fixtures carry the deleted keys as
INPUT, representing an older body, and they are the evidence that the deletion is forward-only.

Regenerate the syrupy snapshots, re-run `tools/snapshot_report_shapes.py --check` and the snapshot
suite without the update flag, and confirm every changed snapshot differs only by the two removed
lines.

Commit inside `/workspaces/firestarter_app`, with a message naming RPT-B1, D-12, D-14 and the
meta-side skill commit's sha.

Then write `evidence/181-09-skill-same-commit.txt` recording: `meta_commit_sha=`,
`app_commit_sha=`, `commit_order=skill_first`, and the reason in one sentence;
`skill_names_four_rail_fields=true`, `skill_states_rail_not_socket=true`,
`skill_states_pre_2_0_note=true`, `skill_datasheet_row_changed=false`,
`skill_fixtures_changed=0`, `parse_input_fixtures_changed=0`, `voltage_key_count_before=6`,
`voltage_key_count_after=4`. Close with a numbered section stating the D-5 residue plainly: one
commit across two repositories is impossible, so the criterion is discharged as two commits landed
inside one task with cross-referencing messages, and the record says so rather than claiming a
literal same-commit that did not happen.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import dataclasses,sys;sys.path.insert(0,".");from firestarter.diagnostic_report import DiagnosticReport
names={f.name for f in dataclasses.fields(DiagnosticReport)}
print("rail_fields="+",".join(sorted(n for n in names if "vp" in n)))
assert {"vpp_before_mv","vpp_after_mv","vpe_before_mv","vpe_after_mv"} <= names,sorted(names)
assert "vpp_mv" not in names,sorted(names)
assert "vpe_mv" not in names,sorted(names)
from tests.fixtures.report_shapes import build_shape
d=build_shape("sst27sf512-six-step").to_dict();v=sorted(d["voltage"])
print("voltage_keys="+",".join(v));assert len(v)==4,v
print("deletion_ok=true")'</automated>
    <fails_when>non-zero exit, or the printed `deletion_ok=true` line is absent. The printed `rail_fields=` and `voltage_keys=` lists show exactly what survived: a five- or six-key voltage mapping means an emit was left behind, and a surviving dataclass field means the model still carries a slot nothing assigns.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import sys;sys.path.insert(0,".");from firestarter.diagnostic_report import DiagnosticReport,AutoCapture,TransportHealth
from firestarter.chip_test import Plan
ok=False
try:
    DiagnosticReport(auto_capture=AutoCapture(host_version="x"),transport=TransportHealth(),plan=Plan(name="x",reason="",steps=[]),vpp_mv=1)
except TypeError as e:
    ok=True;print("construction_rejected="+str(e)[:80])
assert ok,"a keyword named for a deleted field was still accepted"'</automated>
    <fails_when>non-zero exit — constructing a report with a keyword named for a deleted field did NOT raise `TypeError`, which means the field survives on the dataclass under some other guise. The printed `construction_rejected=` line is the expected outcome.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import ast,pathlib;src=pathlib.Path("tests/test_blast_radius_invariance.py").read_text(encoding="utf-8");t=ast.parse(src);a=[n for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,"id","")=="_VOLTAGE_KEYS" for x in n.targets)][0];keys=[e.value for e in a.value.elts];print("voltage_pin="+",".join(keys));assert len(keys)==4,keys;assert "vpp_mv" not in keys and "vpe_mv" not in keys,keys'</automated>
    <fails_when>non-zero exit — the printed `voltage_pin=` list does not have exactly four entries, or still carries a deleted name. The pin and the keys must move in the same commit or the tree is broken at a bisect point.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py tests/test_diagnostic_report.py tests/test_dev_test_cmd.py tests/test_parse_devtest_issue.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. A failure in `test_parse_devtest_issue.py` means a parse-INPUT fixture was scrubbed of the deleted keys, which would delete the evidence that the deletion is forward-only.</fails_when>
    <automated>cd /workspaces && test -z "$(git status --porcelain .claude/skills/devtest-triage/fixtures/)" && /usr/bin/grep -qF 'vpp_after_mv' .claude/skills/devtest-triage/SKILL.md && /usr/bin/grep -qF 'vpe_after_mv' .claude/skills/devtest-triage/SKILL.md</automated>
    <fails_when>non-zero exit — a byte of either frozen fixture changed (their own headers forbid regeneration and they are RPT-E2's parse targets), or the skill does not name both after-side rail fields, which the ROADMAP's own success criterion requires.</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c 'vpp_mv: 13500' .claude/skills/devtest-triage/SKILL.md)" -eq 1 && D=$(git diff 2facbc8f -- .claude/skills/devtest-triage/SKILL.md) && test -z "$(printf '%s\n' "$D" | /usr/bin/grep -E '^[-+].*13500')"</automated>
    <fails_when>non-zero exit — the datasheet cross-check example row is no longer present exactly once, or it was added or removed relative to the phase's meta base `2facbc8f` (the sha plan `181-01` recorded). That row is a correct database-versus-datasheet comparison and rewriting it as a rail field would make it false. If the measured meta base differs from `2facbc8f`, record the discrepancy in the SUMMARY and re-run against the measured sha rather than dropping the leg.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check && ./.venv311/bin/python tools/check_diagnostic_report_claims.py && D=$(git diff 04fd982 -- tests/fixtures/report_shapes.py) && test -z "$(printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][[:space:]]*"[a-z0-9-]+": "[0-9a-f]{12}",')"</automated>
    <fails_when>non-zero exit — the snapshots were not regenerated after the two removed keys, the claim scanner matched a forbidden phrase in the rewritten docstrings, or a twelve-hex frozen-hash literal line moved since app base `04fd982`.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-skill-same-commit.txt; test -s "$E" && /usr/bin/grep -qF 'commit_order=skill_first' "$E" && /usr/bin/grep -qF 'skill_datasheet_row_changed=false' "$E" && /usr/bin/grep -qF 'skill_fixtures_changed=0' "$E" && /usr/bin/grep -qF 'voltage_key_count_after=4' "$E" && /usr/bin/grep -qE '^(meta|app)_commit_sha=[0-9a-f]{7,}' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing, empty, or does not record the ordering, the untouched datasheet row, the untouched fixtures, the new key count and at least one real commit sha. Without the shas the two-repo adjudication is a claim rather than a record.</fails_when>
  </verify>
  <acceptance_criteria>
    - `DiagnosticReport` carries the four before/after rail fields and neither standalone field, and a keyword named for either raises `TypeError`.
    - `to_dict()["voltage"]` has exactly four keys.
    - `_VOLTAGE_KEYS` has four entries, its docstring says four, and it records that this is the first shrink of a Phase-174 pin with the additive precedent named as additive only.
    - The class docstring clause and the `render()` docstring sentence no longer name the deleted fields, and `render()`'s substance is preserved.
    - The re-pointed voltage test keeps its four before/after assertions and asserts the four-key mapping; its docstring records why the standalone half has no subject.
    - The end-to-end test asserts the four surviving keys on a real run.
    - `tests/test_parse_devtest_issue.py` is unmodified and green.
    - The skill names all four rail fields, states the rail-not-socket property, states the pre-2.0 note, and its datasheet cross-check example row is byte-unchanged.
    - Both frozen skill fixtures are byte-unchanged.
    - `tools/snapshot_report_shapes.py --check`, `tools/check_diagnostic_report_claims.py` exit 0, and zero frozen-hash literal lines moved.
    - `evidence/181-09-skill-same-commit.txt` records both shas, the ordering with its reason, the seven boolean scalars and the D-5 residue statement.
  </acceptance_criteria>
  <done>Two keys that never carried a measurement are out of the schema, the pin that predates the deletion has been argued with rather than worked around, and the triage skill now tells a reader what the surviving rail readings do not prove — with the one row in it that looks like the same field, and is not, left correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-12's census — prove by test that no code path assigns the deleted names, attribute-scoped and observed RED</name>
  <files>
    firestarter_app/tests/test_voltage_field_census.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-voltage-census.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py:57-148` — the exact census idiom to copy: attribution of each site to its innermost enclosing function by LINE-NUMBER containment (correct regardless of walk order), the `assert len(source) > 1000` file-actually-loaded guard, the `assert source.count(anchor) == 1` uniqueness check before an in-memory `source.replace(...)`, and a separate explicitly-named vacuity test. **In-memory mutation only — write no fixture file.**
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py` — the module the census parses, after Task 1's deletion.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2230-2254` — `_make_sampler`, the only place a report's rail fields are assigned at all. The census must find its four assignments and confirm none of them names a deleted field.
    - `/workspaces/firestarter_app/firestarter/ic_layout.py` — one of the modules carrying the DATABASE field of the same name. Read one hit to see for yourself that it is a dict key on a chip record, not an attribute on a report object; that is the false-positive class the census must not count.
    - `/workspaces/firestarter_app/tools/check_devtest_orchestrator.py:190-205` — the wire-dict key set, which also carries the database name. Another member of the same false-positive class.
  </read_first>
  <behavior>
    - The census over `firestarter/` finds zero assignments whose target is an attribute named for either deleted field.
    - The census finds the four before/after assignments in the sampler, attributed to their enclosing function, so it is visibly finding real sites.
    - The census finds zero emit sites: no dict-literal key in the serializer module is named for either deleted field.
    - A planted in-memory assignment to a report object's deleted-field attribute makes the census count non-zero and the claim raise `AssertionError`.
    - An empty expected-site set fails rather than passing vacuously.
    - A textual count of the same names over the same directories is measured and recorded as materially larger than the census's, so the difference between the two methods is a number rather than an argument.
  </behavior>
  <action>
Create `firestarter_app/tests/test_voltage_field_census.py`, a new module with **exactly 0 comment
tokens** — every rationale in docstrings.

Its module docstring states D-12's claim and the trap: the same two names are DATABASE fields on an
unrelated code path, so a textual scan returns roughly twenty false positives across the layout
module, the orchestrator's wire-dict key set and several database test modules; the census therefore
matches an ATTRIBUTE STORE on a report object, never a substring, and the module records the textual
count alongside its own so a reader can see the difference rather than take it on trust.

A private helper walks a parsed module and returns the set of assignment sites whose target is an
`ast.Attribute` whose attribute name is one of the two deleted names, plus the four surviving rail
names, attributing each site to its innermost enclosing function by line-number containment. Cover
both plain assignments and annotated ones. Its docstring states why containment rather than walk
order, citing the readback-inventory census as the source of the idiom.

A second private helper loads a module's source from its own `__file__` and asserts the source
length exceeds 1000 characters before returning it — the file-actually-loaded guard that stops a
mis-resolved path passing vacuously. This project has a recorded checker whose directory resolution
found nothing and exited 0; the guard exists because of it.

Four test functions:

`test_no_code_path_assigns_the_deleted_rail_fields` — the positive claim. Census
`firestarter/diagnostic_report.py` and `firestarter/cli_handlers.py`; assert zero sites for either
deleted name; and assert the four before/after names ARE found, attributed to the sampler's enclosing
function, so the census is visibly non-vacuous. Assert the emitted-key half too: no dict-literal key
in the serializer module is named for either deleted field.

`test_a_planted_assignment_reddens_the_census` — the anti-vacuity leg. Assert the planted anchor
occurs exactly once in the loaded source BEFORE mutating, build the mutant with a single in-memory
`replace`, re-run the census over the mutant, assert the count is non-zero, and assert the positive
claim raises `AssertionError` against it. Write no file.

`test_an_empty_expected_site_set_fails_rather_than_passing_vacuously` — the separate, explicitly
named vacuity leg.

`test_a_textual_scan_would_return_false_positives_a_census_does_not` — the measurement that justifies
the method. Count textual occurrences of the two names across the package and its tests, count the
census's own sites, assert the textual count is materially larger, and assert that at least one
textual hit lies in a module the census correctly ignores. Its docstring names the modules by name
and states that the honest census expects zero assignments and zero emit sites while a textual scan
cannot distinguish a report attribute from a database key.

Then write `evidence/181-09-voltage-census.txt` in the house transcript form recording:
`census_assignment_sites=0`, `census_emit_sites=0`,
`census_surviving_rail_sites=<measured>`, `census_enclosing_functions=<measured>`,
`textual_hits_package_and_tests=<measured>`, `false_positive_modules=<the list>`,
`census_at_head=GREEN`, `census_at_planted_assignment=RED`, `empty_expected_set=RED`,
`fixture_files_written=0`, and the pytest tail for the new module.

Commit the module inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_voltage_field_census.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^4 passed'</automated>
    <fails_when>non-zero exit, or any count other than `4 passed` — a lower count means a test was not collected, and any `failed` count means either the census found an assignment that should not exist or a planted mutation failed to redden it, which would make the census vacuous.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import ast,pathlib,sys;src=pathlib.Path("tests/test_voltage_field_census.py").read_text(encoding="utf-8");assert len(src)>1200,len(src);t=ast.parse(src);fns=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)];print("functions="+",".join(fns))
for want in ("test_no_code_path_assigns_the_deleted_rail_fields","test_a_planted_assignment_reddens_the_census","test_an_empty_expected_site_set_fails_rather_than_passing_vacuously","test_a_textual_scan_would_return_false_positives_a_census_does_not"):
    assert want in fns,(want,fns)
mods=set()
for n in ast.walk(t):
    if isinstance(n,ast.Import):
        for a in n.names: mods.add(a.name.split(".")[0])
    elif isinstance(n,ast.ImportFrom) and n.module: mods.add(n.module.split(".")[0])
std=set(sys.stdlib_module_names);extra=sorted(m for m in mods if m not in std and m not in ("pytest","firestarter","tests"))
print("non_stdlib_imports="+(",".join(extra) if extra else "none"));assert not extra,extra
assert "ast" in mods,mods'</automated>
    <fails_when>non-zero exit — one of the four named tests is absent, the module is under 1200 characters (so it cannot contain a real census), it imports something outside the standard library and the project, or it never imports the AST module, which would mean the census is textual after all.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;n=sum(1 for t in tokenize.generate_tokens(io.StringIO(open("tests/test_voltage_field_census.py",encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT);print("comment_tokens="+str(n));raise SystemExit(1 if n else 0)'</automated>
    <fails_when>non-zero exit, or the printed `comment_tokens=` value is greater than 0 — a `#` comment reached a new test module, which `/workspaces/CLAUDE.md` forbids without exception for `firestarter_app/tests/`.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-voltage-census.txt; test -s "$E" && /usr/bin/grep -qF 'census_assignment_sites=0' "$E" && /usr/bin/grep -qF 'census_emit_sites=0' "$E" && /usr/bin/grep -qF 'census_at_planted_assignment=RED' "$E" && /usr/bin/grep -qF 'empty_expected_set=RED' "$E" && /usr/bin/grep -qF 'fixture_files_written=0' "$E" && /usr/bin/grep -qF 'false_positive_modules=' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing, empty, or does not record the zero census counts, both RED observations, the zero-fixture scalar and the named false-positive modules. The last one is what makes the attribute-scoping decision auditable rather than a preference.</fails_when>
    <automated>cd /workspaces/firestarter_app && test -z "$(git status --porcelain tests/fixtures/)"</automated>
    <fails_when>non-zero exit — a file under `tests/fixtures/` changed. The census mutates parsed source in memory only and must write no fixture.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_voltage_field_census.py` exists, carries exactly 0 comment tokens, imports nothing outside the standard library and the project, and contains all four named tests.
    - The census matches attribute stores by name, attributes each site to its innermost enclosing function by line-number containment, and guards its source load with a length assertion.
    - It reports zero assignment sites and zero emit sites for the deleted names, and finds the four surviving rail assignments in the sampler's enclosing function.
    - The planted assignment reddens the claim, with anchor uniqueness asserted before the mutation and no file written.
    - The empty-expected-set leg is a separate, explicitly named test.
    - The textual-versus-census measurement is recorded, with at least one false-positive module named.
    - `evidence/181-09-voltage-census.txt` records all ten named scalars.
    - Nothing under `tests/fixtures/` was modified.
  </acceptance_criteria>
  <done>RPT-B1's "proven by test, not asserted" clause is discharged by a census that cannot be fooled by the twenty database hits that share the deleted fields' names, and the census itself has been seen red against the one thing that would falsify it — a real assignment, planted in memory.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the falsified skill-row claim in CONTEXT.md, re-prove the forward-only parse after the deletion, and re-prove the invariance</name>
  <files>
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md,
    firestarter_app/tests/test_blast_radius_invariance.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-voltage-census.txt
  </files>
  <read_first>
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` §"Canonical References", the `### Product source this phase edits` bullet for the skill — the sentence claiming the example row at `:375` is the report field and asking for it to be replaced by the before/after fields. That is the passage to amend.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-04-PLAN.md` §Task 3 — the in-place amendment shape this task copies: a status marker, the falsified sentence REMOVED rather than annotated, the measurement in its place, and a leg proving both the new text's presence and the old claim's absence. Plan `181-04` has already added one such marker to this file; a second is expected and each amendment identifies its own subject.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py` — the forward-only parse test plan `181-01` added (`test_frozen_pre_2_0_fixtures_still_parse_forward_only`). This task re-runs it POST-deletion, which is the run that actually matters, and extends its docstring to say so.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the state-the-verdict closing form the transcript's final section mirrors.
  </read_first>
  <behavior>
    - CONTEXT.md carries a Phase 181 amendment marker for the skill-row claim, and the falsified sentence is gone rather than annotated.
    - The amended passage names the measured column structure that establishes the row as the database field.
    - The forward-only parse test passes AFTER the deletion, and both frozen fixtures are still unmodified.
    - All 19 registered shapes reproduce their frozen hashes.
  </behavior>
  <action>
One commit in `/workspaces`, plus one small app commit if the forward-only test's docstring is
extended.

**The amendment.** Edit
`.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` in
place, in the §"Canonical References" bullet describing the skill. Use a scoped edit — never rewrite
the whole file.

Add a `## Status: Phase 181 amendment` marker line at the head of the amended passage, labelled the
way plan `181-04`'s amendment is labelled, carrying the date and the words `measured correction` so
the two amendments in this file are distinguishable by subject.

**Remove the falsified claim outright**, do not merely annotate it: the sentence asserting that the
skill's example row at `:375` is the report's rail field and that RPT-F2 replaces it with the
before/after fields. An annotated claim still reads as the original instruction with a caveat, and a
future reader could act on it. In its place record the measurement: the row sits in the
datasheet-versus-database cross-check table whose firestarter-side column is the `firestarter info`
view, so its value is the DATABASE's programming voltage for the part and rewriting it as a rail
reading would turn a correct row into a false one; the report's own rail fields are named separately
in the same document; and RPT-F2 is therefore discharged by extending THAT sentence and adding the
pre-2.0 note, with the example row left byte-unchanged. Name the leg that proves it: the skill-diff
leg in Task 1 fails if that row is added or removed.

While there, state the D-5 residue in the same passage in one sentence — one commit across two
repositories is impossible, the criterion is discharged as two commits landed inside one task with
cross-referencing messages, skill first — so the record does not claim a literal same-commit that
did not happen.

**The forward-only re-proof.** Re-run plan `181-01`'s forward-only parse test now that the deletion
has landed; that is the run that actually establishes RPT-E2's property, since before the deletion
there was nothing forward about it. Extend that test's docstring with one sentence recording that it
now runs against a tree in which the three keys it asserts survive in a parsed pre-2.0 body have been
deleted from the current schema — which is exactly the claim. Assert both fixture files are still
unmodified.

**The invariance re-proof.** Extend `evidence/181-09-voltage-census.txt` with a numbered section
recording `frozen_hash_literal_count=19`,
`frozen_hash_lines_added_or_removed_since_04fd982=0`, `shapes_reproducing=19/19`,
`forward_only_parse_after_deletion=GREEN`, `skill_fixtures_changed=0`,
`context_amendment_marker=present`, `falsified_claim_present=false`. Close in the state-the-verdict
form: name the verdict, name what is excluded and why, and name the gate that would redden if the
situation changed.

Then run the cleanliness legs.
  </action>
  <verify>
    <automated>cd /workspaces && C=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md; test -s "$C" && /usr/bin/grep -qF '## Status: Phase 181 amendment' "$C" && /usr/bin/grep -qF 'measured correction' "$C" && /usr/bin/grep -qF 'firestarter info' "$C"</automated>
    <fails_when>non-zero exit — CONTEXT.md is missing or empty, the amendment marker is absent, the amendment does not identify itself as a measured correction, or it does not name the view that establishes the row as the database field. The leading existence test is what stops the greps passing vacuously on a missing file.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md; test -s "$C" && ! /usr/bin/grep -qF 'RPT-F2/D-5 replaces it with' "$C"</automated>
    <fails_when>non-zero exit — the falsified instruction survives verbatim in CONTEXT.md, which would let a future reader rewrite a correct database-versus-datasheet row as a rail field. Annotating it is not sufficient; the sentence must be gone.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q -k "forward_only" 2>&1 | /usr/bin/grep -qE '^1 passed'</automated>
    <fails_when>non-zero exit, or any count other than `1 passed` — `0 passed` or a `no tests ran` line means the forward-only test was not selected by the filter (so RPT-E2's post-deletion run never happened), and a `failed` count means a pre-2.0 body carrying the deleted keys no longer parses, which would falsify RPT-E2 itself.</fails_when>
    <automated>cd /workspaces && test -z "$(git status --porcelain .claude/skills/devtest-triage/fixtures/)" && D=$(git diff 2facbc8f --name-only -- .claude/skills/devtest-triage/fixtures/) && test -z "$D"</automated>
    <fails_when>non-zero exit — either frozen fixture is dirty, or one differs from the phase's meta base `2facbc8f` (the sha plan `181-01` recorded). Their headers forbid regeneration and they are RPT-E2's only forward-only parse targets. If the measured meta base differs, record the discrepancy in the SUMMARY and re-run against the measured sha rather than dropping the leg.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. A failure in `test_dedup_fingerprint_is_frozen` means the deletion moved a frozen hash, which cannot happen — the deleted keys were never in the pre-image — and therefore indicates something else moved.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-09-voltage-census.txt; test -s "$E" && /usr/bin/grep -qF 'forward_only_parse_after_deletion=GREEN' "$E" && /usr/bin/grep -qF 'shapes_reproducing=19/19' "$E" && /usr/bin/grep -qF 'falsified_claim_present=false' "$E"</automated>
    <fails_when>non-zero exit — the transcript does not record the post-deletion forward-only result, the 19-of-19 reproduction or the amendment's completion.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize,pathlib
for p in ["firestarter/diagnostic_report.py","tests/test_diagnostic_report.py","tests/test_dev_test_cmd.py"]:
    n=sum(1 for t in tokenize.generate_tokens(io.StringIO(pathlib.Path(p).read_text(encoding="utf-8")).readline) if t.type==tokenize.COMMENT)
    print("comment_tokens "+p+"="+str(n))
z=sum(1 for t in tokenize.generate_tokens(io.StringIO(pathlib.Path("tests/test_blast_radius_invariance.py").read_text(encoding="utf-8")).readline) if t.type==tokenize.COMMENT)
print("pin_module="+str(z));raise SystemExit(1 if z else 0)'</automated>
    <fails_when>non-zero exit — a `#` comment reached `tests/test_blast_radius_invariance.py`. The per-file `comment_tokens <path>=` lines are the measured values: any file whose count ROSE above the number measured at the head of Task 1 is where a comment was added; a FALL in the serializer module is expected from the deletion and is permitted.</fails_when>
    <automated>A=$(git -C /workspaces/firestarter status --porcelain) && B=$(git -C /workspaces/firestarter_app status --porcelain firestarter/) && C=$(git -C /workspaces/firestarter_app status --porcelain) && test -z "$A" && test -z "$B" && test -z "$C"</automated>
    <fails_when>non-zero exit — the firmware submodule is dirty, the app package directory is dirty, or the app repo has uncommitted work. The third leg must run AFTER the commits or it reddens on this task's own edits.</fails_when>
  </verify>
  <acceptance_criteria>
    - `181-CONTEXT.md` carries a `## Status: Phase 181 amendment` marker for the skill-row claim, identified as a measured correction, naming the `firestarter info` view.
    - The falsified instruction to replace the example row does not survive verbatim.
    - The amended passage states the D-5 two-repo residue in one sentence.
    - The forward-only parse test passes post-deletion, selected explicitly, and its docstring records that this is the run that establishes the property.
    - Both frozen skill fixtures are unmodified in the working tree and in this plan's commits.
    - `tests/test_blast_radius_invariance.py` reports a bare `N passed`, with 19 of 19 shapes reproducing and zero frozen-hash literal lines moved.
    - `evidence/181-09-voltage-census.txt` carries the re-proof section with its seven scalars and closes in the state-the-verdict form.
    - No file's comment count rose above the value measured at the head of Task 1; the pin module is exactly 0.
    - All three porcelain legs print nothing, run after this task's commits.
  </acceptance_criteria>
  <done>The record no longer instructs a future reader to rewrite a correct row into a false one, RPT-E2's forward-only claim has been tested on the only tree where it means anything — the one where the keys are gone — and the deletion is shown to have moved no frozen hash.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Manifest for plan `181-09`, so a newly-created symbol is not flagged as drift.

**CREATED**
- `firestarter_app/tests/test_voltage_field_census.py` (new module) with
  `test_no_code_path_assigns_the_deleted_rail_fields`,
  `test_a_planted_assignment_reddens_the_census`,
  `test_an_empty_expected_site_set_fails_rather_than_passing_vacuously`,
  `test_a_textual_scan_would_return_false_positives_a_census_does_not`, plus its two private helpers
- the rail-not-socket and pre-2.0 prose in `.claude/skills/devtest-triage/SKILL.md`
- a second `## Status: Phase 181 amendment` passage in `181-CONTEXT.md`, for the skill-row claim
- `.planning/phases/181-.../evidence/181-09-voltage-census.txt`
- `.planning/phases/181-.../evidence/181-09-skill-same-commit.txt`

**DELETED**
- `DiagnosticReport.vpp_mv` and `DiagnosticReport.vpe_mv` (dataclass fields)
- their two emits in `_voltage_dict()`, and therefore `to_dict()["voltage"]["vpp_mv"]` and
  `["vpe_mv"]`
- the two `_VOLTAGE_KEYS` entries
- the class-docstring clause naming the standalone slots
- the two hand assignments and two standalone assertions in the destructive-versus-standalone
  voltage test
- the two end-to-end assertions naming the deleted keys

**CHANGED IN VALUE**
- `_VOLTAGE_KEYS`' counted-keys docstring: six becomes four
- `render()`'s docstring sentence: keeps its substance, stops naming deleted fields
- the syrupy Amber snapshots for every registered shape
- the forward-only parse test's docstring, recording the post-deletion run

**DELIBERATELY NOT CHANGED**
- the four before/after rail fields and every one of their assignments in `_make_sampler`
- `_voltage_dict`'s sentinel-substitution docstring statement
- the two parse-INPUT fixtures in `tests/test_parse_devtest_issue.py` carrying the deleted keys
- both frozen `devtest-triage` fixtures
- the skill's datasheet cross-check example row
- `dedup_fingerprint`, `is_submittable`, all 19 `FROZEN_HASHES` literals
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated proportionately: this plan removes two never-assigned keys from
a locally-written artifact and adds prose to a skill document. No network listener, no
authentication, no credential, no new input path. Two outward-facing artifacts are touched: the
filed issue body loses two keys, and the triage skill gains a disclosure sentence that a human acts
on.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| already-filed issue bodies to the parser and the skill | Every one of the 26 filed bodies still carries the deleted keys. Both consumers accept `schema_version` by presence only, so the deletion is forward-only — and that is re-proven after the deletion rather than only before it. |
| the rail reading to a human triager's conclusion | The measured fact that the rail monitor asserts no socket-routing bit means a healthy reading proves nothing about contact. The skill's new sentence is the only place a triager learns that from the document rather than from the report's own disclosure field. |
| the database field to the report field, sharing a name | Two unrelated values are spelled the same way. A textual census cannot tell them apart, and a document edit that confuses them turns a correct row false. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-181-31 | Tampering | the D-12 census's method | high | mitigate | A textual census here matches roughly twenty database-field hits, and the obvious repair — filtering by filename — is how a gate learns to fail open on the very sites it should scan. Mitigated by attribute-scoped AST matching, by the planted-assignment RED leg, by the separate vacuity leg, and by measuring and recording the textual count beside the census's own. |
| T-181-32 | Tampering | the frozen `devtest-triage` fixtures and the parse-input fixtures | high | mitigate | Regenerating either frozen fixture, or scrubbing the parse-input fixtures of the deleted keys, would destroy the only evidence that the deletion is forward-only — and it would do so while making the suite green. Mitigated by the porcelain leg over the fixtures directory, by the commit-diff leg, by leaving the parse module unmodified, and by re-running the forward-only test post-deletion. |
| T-181-33 | Repudiation | the falsified skill-row claim in the record | medium | mitigate | An unamended CONTEXT.md instructs a future reader to rewrite a correct database-versus-datasheet row as a rail field. Mitigated by removing the sentence outright rather than annotating it, by the amendment marker, by the negative leg that fails if the instruction survives, and by the skill-diff leg that fails if the row itself moves. |
| T-181-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package. Every module used — `ast`, `io`, `tokenize`, `pathlib`, `dataclasses` — is Python standard library, so no Package Legitimacy Audit is required and no install checkpoint is inserted. |

Two threats are rated `high` and both are dispositioned **mitigate** with runnable gates, so nothing
here blocks.
</threat_model>

<verification>
1. Both standalone rail fields are gone from the dataclass, from `_voltage_dict()` and from
   `_VOLTAGE_KEYS`, whose docstring says four and records the first-ever shrink.
2. A keyword named for either deleted field raises `TypeError`; `to_dict()["voltage"]` has four keys.
3. The attribute-scoped census reports zero assignment sites and zero emit sites, finds the four
   surviving assignments, and has been observed RED against a planted assignment.
4. The textual-versus-census difference is measured and recorded with named false-positive modules.
5. The skill names all four rail fields, states rail-not-socket, states the pre-2.0 note, and its
   datasheet example row is byte-unchanged.
6. Both frozen skill fixtures and both parse-input fixtures are unmodified.
7. CONTEXT.md carries the amendment marker and the falsified instruction is gone.
8. The forward-only parse test passes POST-deletion; 19 of 19 shapes reproduce; zero frozen-hash
   literal lines moved.
9. No comment count rose; the census module is exactly 0; all three porcelain legs print nothing.

**Do not** run the seven-leg green-tree battery here, do not invoke `check_rekey_ledger.py`, and do
not assert a suite floor of 2241 or 2242. The battery runs once, in plan `181-10`, with floor 2239.
</verification>

<success_criteria>
- RPT-B1 is discharged: the two standalone rail slots are gone from the dataclass, the serializer and
  the schema, and the no-path-assigns-them claim is a test rather than a sentence.
- RPT-F2 is discharged: the skill names the four surviving rail fields, states plainly that they are
  rail and not socket readings, and carries the pre-2.0 note — landed as the paired half of the
  deletion, with the two-repo residue stated rather than glossed.
- D-12 is honoured including its measured trap; D-5 is discharged as far as two repositories allow.
- D-16 holds: 19 frozen hashes, zero moved literal lines, and RPT-E2 re-proven after the deletion.
- Zero firmware lines, zero `#` comments added, no push.
</success_criteria>

<output>
Create `.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-09-SUMMARY.md`
when done. Record in it: both commit shas and the ordering with its reason; the census's counts beside
the textual count, with the false-positive modules named; the two RED observations; the correction
that the skill's example row is the database field, with the column structure that establishes it; the
re-pointed test's old and new claims; the measured pytest count for every touched module; any anchor
that had moved since this plan was written; and every rationale that would otherwise have been a
source comment.
</output>
