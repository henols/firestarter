---
phase: 181-report-fidelity-schema-2-0-canonical-naming-hygiene-close
plan: 03
type: execute
wave: 2
depends_on: [181-01]
files_modified:
  - firestarter_app/pyproject.toml
  - firestarter_app/tests/test_runtime_dependencies.py
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-03-dependency-pins.txt
autonomous: true
requirements: [HYG-01, HYG-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`pyproject.toml`'s `test` extra bounds syrupy as `syrupy>=5.0,<7`, carrying its upper-bound justification in the same style as the `py32` extra's `pyusb>=1.3.1,<2` pin (HYG-01, D-17)"
    - "The bound admits the installed 6.0.0 and refuses 7.0.0: a specifier check accepts `6.0.0` and rejects `7.0.0`, so the boundary is exercised on both sides rather than asserted"
    - "The runtime `dependencies` list is pinned BY TEST to exactly the six names `pyserial`, `requests`, `tqdm`, `click`, `rich`, `packaging` — a test, not a claim (HYG-02, D-17)"
    - "The dependency pin is exact SET equality, order-insensitive, so adding a seventh name and removing one of the six both redden it, and a reordering of the list changes nothing"
    - "An empty `dependencies` list reddens the pin — it asserts the exact six-name set, never a subset"
    - "The pin needs no new dependency to make its own point: it reads `pyproject.toml` with stdlib `tomllib`, available on the py3.11 floor app CI runs, which is itself HYG-02's claim"
    - "The dependency pin has been observed RED against an in-memory planted mutation in both directions — a seventh name added and one of the six removed — with the parsed mapping mutated in process and no fixture file written"
    - { statement: "If the pin runs while pyproject.toml is being edited concurrently it reads whatever is on disk at that instant and holds no lock; a torn read is not defended against and would surface as a parse error rather than a false pass", verification: backstop }
    - "The snapshot suite and `tools/snapshot_report_shapes.py --check` are green under the bounded syrupy, measured against the installed 6.0.0"
    - "No `#` comment was added to product source. `pyproject.toml` is NOT product source and its comments are correct there; the new test module carries exactly 0 comment tokens and its rationale lives in docstrings"
    - "`git -C /workspaces/firestarter status --porcelain` prints nothing and `git -C /workspaces/firestarter_app status --porcelain firestarter/` prints nothing"
  prohibitions:
    - "No new runtime dependency may be added, and no new dependency may be introduced in order to test that claim"
    - "A gate must not read green while the rule it guards is violated"
    - "No comment may be written into product source, and firestarter_app/tests/ counts as product source"
  artifacts:
    - path: "firestarter_app/pyproject.toml"
      provides: "the bounded syrupy specifier and the unchanged six-name runtime dependency list"
      contains: "syrupy>=5.0,<7"
    - path: "firestarter_app/tests/test_runtime_dependencies.py"
      provides: "HYG-02 as a test -- exact set equality over the six runtime names, with both planted-RED directions and an empty-list control"
      contains: "tomllib"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-03-dependency-pins.txt"
      provides: "the measured specifier boundary on both sides, the installed syrupy version, and the RED observations"
  key_links:
    - from: "firestarter_app/tests/test_runtime_dependencies.py"
      to: "firestarter_app/pyproject.toml"
      via: "stdlib tomllib parse of the project table, resolved from the test file's own parent directory"
      pattern: "pyproject.toml"
---

<objective>
Close the milestone's two dependency-hygiene requirements, and close them the way the requirement text
demands: HYG-02 says the claim is asserted **by test, not by a sentence**.

`syrupy` is bounded `>=5.0,<7` (HYG-01, D-17) because PyPI now serves 6.0.0, whose headline change is
native Amber serialization of stdlib dataclasses — and `Plan`, `Step`, `Fingerprint` and the report
classes are all dataclasses, so a major bump is exactly where the 19 committed report snapshots would
silently re-render. The runtime `dependencies` list is pinned to exactly six names, by a test that
uses only the standard library, which is itself the claim it makes.

Purpose: this is the smallest plan in the phase and it is deliberately independent — it touches
`pyproject.toml` and one new test module, and shares no file with the D-08/D-09 demolition running in
the same and the next wave. It can fail without entangling anything.
Output: one bounded specifier, one new test module with its planted-RED legs, one evidence transcript.
</objective>

<orchestrator_dispositions>
Measurements taken against the live tree at app `04fd982`. These OVERRIDE the source documents where
they conflict.

- **`syrupy` 6.0.0 is what is installed** in `/workspaces/firestarter_app/.venv311`, measured via
  `importlib.metadata.version`. So `>=5.0,<7` changes nothing about the current environment: it refuses
  a future 7.x. Say that plainly rather than implying the bound fixes a present problem.
- **`pyproject.toml` line numbers confirmed exactly** as CONTEXT.md states: `dependencies` at `:46-53`
  (six entries — `pyserial>=3.5`, `requests>=2.20`, `tqdm>=4.60`, `click>=8.1`, `rich>=14.0`,
  `packaging>=21.0`), `syrupy>=5.0` at `:73`.
- **The analog is the `py32` extra at `:62-70`** — the only bounded dependency in the file, and it
  carries its reasoning in comments directly above the pin. The `mypy>=2.1.0,<3` pin at `:84` is a
  second example of the same style in the very extra HYG-01 edits. `pyproject.toml` is not product
  source; comments there are correct and both existing pins are the model.
- **`181-PATTERNS.md` records "No Analog Found" for a pyproject-parsing test** — no existing test reads
  `pyproject.toml`. `tomllib` is stdlib on py3.11, so the new module needs no dependency, which is
  HYG-02's own point. Do not add `toml` or `tomli`.
- **All three `plan:pre` detectors resolved to their skip branch** — no `COVERAGE.md`, no
  `<assumption_delta_decision>`, no schema-push task.
- **The green-tree battery is seven legs and the suite floor is 2239.** It runs once, in `181-08`.
</orchestrator_dispositions>

<flagged_assumptions>
The spec-less probe fallback is ACTIVE. `coverage.json` carries **30 applicable edges over 18
requirements**, all arriving `unresolved`; the category tally re-counted from the file is
`unclassified` **12** (the orchestrator summary said 11 — the file is authoritative), `empty` 6,
`adjacency` 5, `ordering` 5, `encoding` 1, `concurrency` 1.

**This plan's share is 7 rows — the largest of any plan, and every one is resolvable.**

*Resolved (explicit) → plain `must_haves.truths` strings — 6:*
- `HYG-01/adjacency` — the specifier's own boundary: `<7` admits every 6.x and refuses `7.0.0`
  exactly, exercised on both sides.
- `HYG-01/empty` — the `test` extra is non-empty and `syrupy` appears in it exactly once.
- `HYG-01/ordering` — the pin is over the specifier string, not over position; reordering the extra
  changes nothing.
- `HYG-02/adjacency` — exact set equality: a seventh name and a missing sixth both redden.
- `HYG-02/empty` — an empty `dependencies` list reddens, because the pin is equality and not a subset.
- `HYG-02/ordering` — the comparison is set-based and order-insensitive by construction.

*Resolved (backstop) → a structured flat-scalar marker in `must_haves.truths` — 1:*
- `HYG-02/concurrency` — "If interrupted or run in parallel, what is guaranteed?" The pin reads
  `pyproject.toml` from disk and holds no lock. Nothing in this project defends a config read against
  a concurrent edit, and inventing a lock would be scope invention, so the honest disposition is a
  backstop marker: a torn read surfaces as a `tomllib` parse error, not as a false pass. At verify
  time a backstop truth the verifier cannot confirm with explicit evidence abstains to
  `human_needed` with reason `insufficient_spec` rather than passing silently.

*Unresolved:* none in this plan.

**No-silent-drop equality:** 7 rows in scope == 6 explicit truths + 1 backstop marker + 0 flagged.
Holds.

**Prohibition recall (§B, run in-prompt).** Three of the phase's ten survivors land here. The first is
specific and sharp: **no new dependency may be introduced in order to test that no new dependency was
introduced.** Reaching for `toml` or `tomli` to parse `pyproject.toml` would falsify HYG-02 in the act
of asserting it. **Canon-referral breadcrumb:** a supply-chain candidate did surface — an unbounded
specifier is a real integrity risk — but it is already the substance of HYG-01 itself and is carried in
`<threat_model>` as `T-181-SC`, so it is not double-minted as a prohibition.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md
</context>

<environment>
Three git repositories; **branch names differ between meta and the submodules**.

- Meta `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`. Owns `.planning/`.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, phase base app HEAD
  `04fd982`. **Commit inside the submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — **not touched**. Must stay porcelain-clean.

`git -C /workspaces/firestarter_app status --porcelain firestarter/` names the app's own **package
directory**, not the firmware submodule.

Python is `/workspaces/firestarter_app/.venv311/bin/python` — **3.11.16**, matching app CI. The
devcontainer default is 3.12 and that divergence has already broken beta CI once. `tomllib` entered
the standard library in 3.11, so it is available on the CI floor; do NOT reach for `toml` or `tomli`.

`pytest` `addopts` is `-ra -q`; a count line needs `-o addopts=""`.

Shell `grep` here is **ugrep and silently honours `.gitignore`** — every evidence-grade scan uses
`/usr/bin/grep`. `-qF` with a dash-leading pattern exits 2 and fails OPEN; use `-qFe`. That matters in
this plan: `<7` and `>=5.0,<7` are fine, but any pattern you write beginning with `-` needs `-qFe`.

**Do not re-install or upgrade anything.** No `pip install`, no `pip install -e`, no lockfile
regeneration. The bound is a source edit; the installed 6.0.0 already satisfies it.

**No push, no PR, no tag, no branch cut** in any task (D-22).
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and CONTEXT.md D-26 cover everything under
`firestarter/` and `firestarter_app/`, **including `firestarter_app/tests/`**. The new test module must
carry **exactly 0** comment tokens; every rationale goes in a docstring.

`pyproject.toml` is **not product source** and the rule does not reach it. Its two existing bounded
pins (`pyusb>=1.3.1,<2` at `:62-70` and `mypy>=2.1.0,<3` at `:78-84`) both carry their upper-bound
reasoning in comments directly above the specifier, and HYG-01 follows that established style. That is
a deliberate distinction, not an exception being carved out: the rule is about the firmware and the
pip package's Python source, which a reader of the shipped artifact sees.
</comment_discipline>

<tasks>

<task type="auto">
  <name>Task 1: Bound syrupy `>=5.0,<7`, with the upper bound justified in the file's own established style</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` with plan `181-01` committed and a clean working tree; `./.venv311/bin/python` reports 3.11 and `importlib.metadata.version("syrupy")` reports a 6.x release.</precondition>
  <reversibility rating="reversible">A specifier string in one line of `pyproject.toml`. Nothing is installed, no lockfile moves, and the currently-installed release already satisfies both the old and the new specifier.</reversibility>
  <files>
    firestarter_app/pyproject.toml,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-03-dependency-pins.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/pyproject.toml:44-90` — the whole dependency region: `dependencies` at `:46-53`, the `dev` extra, the `py32` extra at `:62-70` with its four-line upper-bound justification above `pyusb>=1.3.1,<2`, the `test` extra opening at `:71` with `syrupy>=5.0` at `:73`, and the `mypy>=2.1.0,<3` pin at `:78-84` with its seven-line justification. Both existing pins are the style to copy.
    - `/workspaces/firestarter_app/tools/snapshot_report_shapes.py` — the `--check` mode that is green-tree leg 4, so you know what the syrupy bound is protecting.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` D-17 and the HYG-01 requirement text in `.planning/REQUIREMENTS.md` §Hygiene — the reason named there is Amber's native dataclass serialization, and `Plan`, `Step`, `Fingerprint` and the report classes are all dataclasses.
  </read_first>
  <behavior>
    - The `test` extra's syrupy entry is the exact string `syrupy>=5.0,<7`.
    - A specifier check over that string accepts the installed 6.x version and rejects `7.0.0`.
    - The snapshot suite and `tools/snapshot_report_shapes.py --check` are green with nothing reinstalled.
    - `dependencies` is byte-unchanged by this task.
  </behavior>
  <action>
Change one specifier in `firestarter_app/pyproject.toml`: `"syrupy>=5.0",` becomes
`"syrupy>=5.0,<7",` in the `test` extra.

Above it, add an upper-bound justification comment in the same voice and shape as the `py32` extra's
`pyusb` pin and the `mypy>=2.1.0,<3` pin in this same extra — a short paragraph naming the concrete
failure mode, not a generic caution. The substance, from HYG-01's own requirement text and D-17: PyPI
now serves 6.0.0, whose headline change is native Amber serialization of stdlib dataclasses;
`Plan`, `Step`, `Fingerprint` and the report classes are all dataclasses; the 19 committed report
snapshots and `tools/snapshot_report_shapes.py --check` (green-tree leg 4) are exactly what a
serialization change would silently re-render. State the raise protocol the sibling pins state: raise
the bound deliberately and re-verify the snapshots in the same commit. Record that the currently
installed release is already inside the bound, so this refuses a future major rather than fixing a
present break.

Comments are correct in `pyproject.toml` — it is not product source. Do not put this reasoning in a
Python source comment anywhere.

**Do not run any installer.** No `pip install`, no `pip install -e .[test]`, no lockfile regeneration.
The bound is a source edit and the installed release already satisfies it; a reinstall in this
devcontainer is how the py3.12 divergence gets in.

Then measure the boundary on both sides rather than asserting it, using `packaging` (already a runtime
dependency, so nothing new is introduced): build a specifier from the string you just wrote, and record
that the installed version satisfies it and that `7.0.0` does not.

Then write `.planning/phases/181-.../evidence/181-03-dependency-pins.txt` in the house transcript form,
recording: `syrupy_specifier=>=5.0,<7`; `syrupy_installed=<measured version>`;
`specifier_accepts_installed=true`; `specifier_accepts_7_0_0=false`; `runtime_dependencies_changed=0`;
and the verbatim tails plus explicit `rc=` lines for the snapshot suite and for
`tools/snapshot_report_shapes.py --check`.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qF '"syrupy>=5.0,<7",' pyproject.toml && ! /usr/bin/grep -qF '"syrupy>=5.0",' pyproject.toml</automated>
    <fails_when>non-zero exit — the bounded specifier is absent, or the old unbounded `"syrupy>=5.0",` entry survives alongside it, which would leave two syrupy requirements in the same extra.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import tomllib,pathlib;from importlib.metadata import version;from packaging.specifiers import SpecifierSet;d=tomllib.loads(pathlib.Path("pyproject.toml").read_text(encoding="utf-8"));ex=d["project"]["optional-dependencies"]["test"];hits=[e for e in ex if e.split(">")[0].split("<")[0].split("=")[0].strip()=="syrupy"];assert len(hits)==1,hits;spec=SpecifierSet(hits[0].split("syrupy",1)[1]);inst=version("syrupy");print("syrupy_installed="+inst);print("specifier="+str(spec));assert inst in spec,(inst,str(spec));assert "7.0.0" not in spec,str(spec);print("boundary_ok=true")'</automated>
    <fails_when>non-zero exit — the printed `boundary_ok=true` line is absent. The chain fails when syrupy appears zero or twice in the `test` extra, when the installed release falls outside the specifier, or when `7.0.0` satisfies it, which would mean the upper bound is missing or wrong.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check</automated>
    <fails_when>non-zero exit, or the tool prints a drift line naming a shape whose committed snapshot no longer matches a fresh regeneration under the installed syrupy.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import tomllib,pathlib;d=tomllib.loads(pathlib.Path("pyproject.toml").read_text(encoding="utf-8"));names=sorted(e.split(">")[0].split("<")[0].split("=")[0].strip() for e in d["project"]["dependencies"]);print("runtime="+",".join(names));assert names==["click","packaging","pyserial","requests","rich","tqdm"],names'</automated>
    <fails_when>non-zero exit — the printed `runtime=` list is not exactly the six expected names, meaning this task changed the runtime dependency list it was forbidden to touch.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-03-dependency-pins.txt; test -s "$E" && /usr/bin/grep -qF 'specifier_accepts_installed=true' "$E" && /usr/bin/grep -qF 'specifier_accepts_7_0_0=false' "$E" && /usr/bin/grep -qF 'runtime_dependencies_changed=0' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing, empty, or does not record the boundary measured on both sides, which would leave the adjacency edge asserted rather than exercised.</fails_when>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains the exact entry `"syrupy>=5.0,<7",` and no `"syrupy>=5.0",` entry.
    - `syrupy` appears exactly once in the `test` extra.
    - The installed syrupy release satisfies the specifier and `7.0.0` does not, proven by the `packaging` leg.
    - An upper-bound justification comment sits directly above the pin, naming Amber's dataclass serialization, the affected dataclasses, and the raise-and-re-verify protocol.
    - `dependencies` parses to exactly `click, packaging, pyserial, requests, rich, tqdm`.
    - `tools/snapshot_report_shapes.py --check` exits 0 with nothing reinstalled.
    - `evidence/181-03-dependency-pins.txt` records the specifier, the installed version, both boundary results, and `runtime_dependencies_changed=0`.
    - No installer was run: the `SUMMARY.md` states that no `pip install` was invoked.
  </acceptance_criteria>
  <done>HYG-01 is discharged with its upper bound justified in the file's own established voice, and the boundary is measured on both sides — the installed release inside it, a future major outside it — rather than asserted by the specifier's shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HYG-02 as a test — exact set equality over the six runtime names, using only the standard library, with both planted-RED directions</name>
  <files>
    firestarter_app/tests/test_runtime_dependencies.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-03-dependency-pins.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/pyproject.toml:44-90` — the `dependencies` table the new test parses, and the extras it must NOT pin.
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py:57-148` — the anti-vacuity triad this project treats as a house standard, and the three details that matter regardless of subject: assert the subject is real before asserting anything about it (a length or size guard); assert the planted anchor is unique BEFORE mutating; and carry the vacuity check as a separate, explicitly-named test rather than folded into the positive one. **Use in-memory mutation — write no fixture file.**
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:523-575` — the in-memory MAPPING mutation form, which is the closer analog here than the source-string form, since `tomllib` hands you a mapping.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md` §"No Analog Found" — the row recording that no existing test parses `pyproject.toml`, so this module's shape is an original decision and `tomllib` being stdlib is itself HYG-02's claim.
  </read_first>
  <behavior>
    - Parsing `pyproject.toml` with `tomllib` yields a `project.dependencies` list whose bare names, as a set, equal exactly `{click, packaging, pyserial, requests, rich, tqdm}`.
    - Adding a seventh name to the parsed list in memory makes the set comparison raise `AssertionError`.
    - Removing one of the six from the parsed list in memory makes it raise `AssertionError`.
    - Comparing against an empty expected set raises `AssertionError` rather than passing vacuously.
    - Reordering the parsed list changes nothing — the comparison is over sets.
    - No import in the module is outside the standard library except `pytest` itself.
  </behavior>
  <action>
Create `firestarter_app/tests/test_runtime_dependencies.py`, a new module with **exactly 0 comment
tokens** — every rationale in docstrings.

Its module docstring states HYG-02's claim and the two things that make the module honest: that it uses
stdlib `tomllib` (3.11+, matching the CI floor) precisely because reaching for `toml` or `tomli` would
add a dependency in order to assert that no dependency was added; and that `181-PATTERNS.md` found no
precedent for a pyproject-parsing test in this tree, so the shape here is an original decision rather
than a copied one.

A private helper resolves `pyproject.toml` from the test file's own parent's parent — never a
directory-relative path, because this project has a recorded checker (`check_permitted_claims.py`)
whose `_HERE` resolved to the wrong directory, scanned nothing, and exited 0. Guard the resolution:
assert the file exists and that the parsed `project.dependencies` list is non-empty before asserting
anything about its contents.

A second private helper reduces a requirement string to its bare distribution name by splitting on the
first of `>`, `<`, `=`, `!`, `~`, `[` and `;` and stripping whitespace, so `pyserial>=3.5` and a future
`pyserial>=3.5 ; python_version<"3.12"` both reduce to `pyserial`. State in its docstring that it is
deliberately a name reducer and not a specifier parser: HYG-02 pins WHICH distributions ship, not which
versions.

Module-level constant `_EXPECTED_RUNTIME_DISTRIBUTIONS`, a `frozenset` of exactly `click`,
`packaging`, `pyserial`, `requests`, `rich`, `tqdm`. Its docstring records that this is the shipped set
named verbatim in HYG-02 and that the pin is EQUALITY, never a subset, so both a seventh name and a
missing sixth redden.

Four test functions:

`test_runtime_dependency_set_is_exactly_the_six_shipped_distributions` — the positive pin. Assert set
equality, and assert the parsed list's length is 6 as a second, independent check so a duplicate entry
reducing to an existing name cannot hide. Its docstring states that extras are deliberately NOT pinned:
`dev`, `py32` and `test` may grow freely, because HYG-02 is about what a `pip install firestarter`
puts on a user's machine.

`test_a_planted_seventh_dependency_reddens_the_pin` — assert the planted name is absent from the
parsed list first (the anchor-uniqueness step), then append it to an in-memory copy and assert the set
comparison raises `AssertionError`.

`test_a_planted_removal_reddens_the_pin` — assert one of the six is present first, then remove it from
an in-memory copy and assert the comparison raises `AssertionError`. Both directions are needed: a
subset assertion would survive the removal and an equality assertion is what this proves.

`test_an_empty_expected_set_fails_rather_than_passing_vacuously` — the separate vacuity leg, comparing
the real parsed set against an empty expected set inside `pytest.raises(AssertionError)`.

Mutate parsed copies only. **Write no fixture file and never modify `pyproject.toml` from a test.**

Then extend `evidence/181-03-dependency-pins.txt` with a numbered section recording
`runtime_distribution_count=6`, `runtime_distributions=click,packaging,pyserial,requests,rich,tqdm`,
`pin_at_head=GREEN`, `pin_at_planted_seventh=RED`, `pin_at_planted_removal=RED`,
`pin_at_empty_expected=RED`, `fixture_files_written=0`,
`non_stdlib_imports_in_module=pytest`, and the pytest tail showing `4 passed`.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`. Then run the
cleanliness legs.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_runtime_dependencies.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^4 passed'</automated>
    <fails_when>non-zero exit, or any count other than `4 passed` — a lower count means a test was not collected, and any `failed` count means either the positive pin does not hold or a planted mutation failed to redden it, which would make the pin vacuous.</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qF 'import tomllib' tests/test_runtime_dependencies.py && ! /usr/bin/grep -qE '^import (toml|tomli)$' tests/test_runtime_dependencies.py && ! /usr/bin/grep -qE '^from (toml|tomli) ' tests/test_runtime_dependencies.py</automated>
    <fails_when>non-zero exit — the module does not use stdlib `tomllib`, or it imports `toml` or `tomli`, which would add a dependency in the act of asserting that none was added.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import ast,pathlib,sys;src=pathlib.Path("tests/test_runtime_dependencies.py").read_text(encoding="utf-8");assert len(src)>800,len(src);t=ast.parse(src);mods=set()
for n in ast.walk(t):
    if isinstance(n,ast.Import):
        for a in n.names: mods.add(a.name.split(".")[0])
    elif isinstance(n,ast.ImportFrom) and n.module: mods.add(n.module.split(".")[0])
std=set(sys.stdlib_module_names);extra=sorted(m for m in mods if m not in std and m!="pytest");print("non_stdlib_imports="+(",".join(extra) if extra else "none"));assert not extra,extra'</automated>
    <fails_when>non-zero exit — the printed `non_stdlib_imports=` line names at least one module outside the standard library and outside `pytest`, or the module source is under 800 characters, which would mean the guard resolved a stub rather than the real file.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;n=sum(1 for t in tokenize.generate_tokens(io.StringIO(open("tests/test_runtime_dependencies.py",encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT);print("comment_tokens="+str(n));raise SystemExit(1 if n else 0)'</automated>
    <fails_when>non-zero exit, or the printed `comment_tokens=` value is greater than 0 — a `#` comment reached a new test module, which `/workspaces/CLAUDE.md` forbids without exception for `firestarter_app/tests/`.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-03-dependency-pins.txt; test -s "$E" && /usr/bin/grep -qF 'pin_at_planted_seventh=RED' "$E" && /usr/bin/grep -qF 'pin_at_planted_removal=RED' "$E" && /usr/bin/grep -qF 'pin_at_empty_expected=RED' "$E" && /usr/bin/grep -qF 'fixture_files_written=0' "$E"</automated>
    <fails_when>non-zero exit — the transcript does not record all three RED observations plus the zero-fixture scalar, which means the pin was never seen red against a planted counter-example.</fails_when>
    <automated>A=$(git -C /workspaces/firestarter status --porcelain) && B=$(git -C /workspaces/firestarter_app status --porcelain firestarter/) && C=$(git -C /workspaces/firestarter_app status --porcelain) && test -z "$A" && test -z "$B" && test -z "$C"</automated>
    <fails_when>non-zero exit — the firmware submodule is dirty, the app package directory is dirty, or the app repo has uncommitted work. This leg must run AFTER the commit or it reddens on this task's own edits.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_runtime_dependencies.py` exists and contains all four named test functions.
    - It imports stdlib `tomllib` and imports nothing outside the standard library except `pytest`, proven by the AST leg.
    - `_EXPECTED_RUNTIME_DISTRIBUTIONS` is a `frozenset` of exactly the six names, and the positive test asserts set EQUALITY plus a parsed-length check of 6.
    - Both planted directions redden the pin, each asserting the anchor's presence or absence before mutating, and the empty-expected-set leg is a separate named test.
    - No fixture file is written and `pyproject.toml` is not modified by any test.
    - The module carries exactly 0 comment tokens.
    - `./.venv311/bin/python -m pytest tests/test_runtime_dependencies.py -o addopts="" -q` prints `4 passed`.
    - `evidence/181-03-dependency-pins.txt` records the six distribution names, all three RED observations, `fixture_files_written=0` and `non_stdlib_imports_in_module=pytest`.
    - All three porcelain legs print nothing, run after this task's commits.
  </acceptance_criteria>
  <done>HYG-02 is a gate rather than a sentence: the six shipped distributions are pinned by exact set equality, the pin has been seen red in both drift directions and against a vacuous expectation, and it makes its point using only the standard library — which is the claim.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Manifest for plan `181-03`.

**CREATED**
- `firestarter_app/tests/test_runtime_dependencies.py` (new module)
- `_EXPECTED_RUNTIME_DISTRIBUTIONS` (module-level `frozenset`)
- `test_runtime_dependency_set_is_exactly_the_six_shipped_distributions`
- `test_a_planted_seventh_dependency_reddens_the_pin`
- `test_a_planted_removal_reddens_the_pin`
- `test_an_empty_expected_set_fails_rather_than_passing_vacuously`
- `.planning/phases/181-.../evidence/181-03-dependency-pins.txt`

**CHANGED IN VALUE**
- the `test` extra's syrupy entry: `syrupy>=5.0` becomes `syrupy>=5.0,<7`

**DELETED by this plan:** nothing.

**Explicitly NOT changed:** `project.dependencies` (byte-identical, six entries), the `dev` and `py32`
extras, every `firestarter/` source file, and the installed environment — no installer is run.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated proportionately: this plan edits one specifier and adds one test
that reads a file from the repository. No network path, no serial path, no authentication, no
credential, no user input beyond `argv`. Nothing here reaches the filed GitHub issue body.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI to the build environment | The only genuinely external trust crossing in this plan. An unbounded specifier lets a future major arrive unreviewed; that is what HYG-01 closes. |
| `pyproject.toml` on disk to the pin | The test reads the file at whatever state it is in. It holds no lock; a concurrent edit surfaces as a `tomllib` parse error rather than a false pass. That is the backstop-marked edge above. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-181-SC | Tampering | the `syrupy` specifier and the six runtime distributions | high | mitigate | An unbounded specifier admits an unreviewed major from a third-party index into the gate that guards 19 committed report snapshots. Mitigated by the `<7` bound with a stated raise protocol, and by the HYG-02 set-equality pin, which reddens the moment a seventh distribution appears in the shipped set. No package is installed by this plan, so the Package Legitimacy Gate's install-time checkpoint does not apply and no `[ASSUMED]`/`[SUS]` verification is required. |
| T-181-10 | Spoofing | `pyproject.toml` path resolution in the new test | medium | mitigate | A directory-relative resolution that finds nothing and passes is a recorded defect in this project (`check_permitted_claims.py`'s `_HERE`). Mitigated by resolving from the test file's own parents, by an existence assertion, by a non-empty-list guard, and by the 800-character source guard in the AST leg. |
| T-181-11 | Tampering | the HYG-02 pin itself | medium | mitigate | A subset assertion would survive a removal and a vacuous expected set would survive anything. Mitigated by set EQUALITY, by both planted directions, and by the separate empty-expected-set leg. |
| T-181-12 | Repudiation | `evidence/181-03-dependency-pins.txt` | low | mitigate | Every claim is a flat `key=value` scalar plus a verbatim command tail and an explicit `rc=`, including the boundary measured on both sides. |

One threat is rated `high` (T-181-SC) and it is dispositioned **mitigate** with two runnable gates, so
nothing here blocks.
</threat_model>

<verification>
1. `pyproject.toml` carries `"syrupy>=5.0,<7",` exactly once and no unbounded syrupy entry.
2. The specifier accepts the installed release and rejects `7.0.0`, measured with `packaging`.
3. `tools/snapshot_report_shapes.py --check` exits 0 with nothing reinstalled.
4. `project.dependencies` parses to exactly the six expected names.
5. `tests/test_runtime_dependencies.py` reports `4 passed`, imports nothing outside the standard
   library except `pytest`, and carries 0 comment tokens.
6. Both planted directions and the empty-expected-set leg were observed RED, transcripts on disk.
7. All three porcelain legs print nothing.

**Do not** run the seven-leg battery here, do not invoke `check_rekey_ledger.py`, do not assert a
suite floor of 2241 or 2242, and **do not run any installer**. The battery runs once, in `181-08`.
</verification>

<success_criteria>
- HYG-01 is discharged: syrupy is bounded `>=5.0,<7` with a justification in the file's own style, and
  the boundary is exercised on both sides.
- HYG-02 is discharged as a **test**, not a claim: exact set equality over the six shipped
  distributions, seen red in both drift directions, using only the standard library.
- Zero new dependencies, zero installer invocations, zero product-source lines changed, zero `#`
  comments in the new module, firmware untouched.
</success_criteria>

<output>
Create `.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-03-SUMMARY.md`
when done. Record in it: the measured installed syrupy version, both boundary results, the three RED
observations, an explicit statement that no `pip install` was invoked, any line number that had moved
since this plan was written, and every rationale that would otherwise have been a source comment.
</output>
