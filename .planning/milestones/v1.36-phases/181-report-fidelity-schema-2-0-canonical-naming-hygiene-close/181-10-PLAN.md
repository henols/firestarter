---
phase: 181-report-fidelity-schema-2-0-canonical-naming-hygiene-close
plan: 10
type: execute
wave: 9
depends_on: [181-09]
files_modified:
  - .planning/MILESTONES.md
  - .planning/REQUIREMENTS.md
  - firestarter_app/tests/test_blast_radius_invariance.py
  - .planning/todos/pending/2026-09-08-uv-ladder-flip-on-exhausted-slots.md
  - .planning/todos/pending/2026-09-08-slots-remaining-off-by-one-at-target-resolution.md
  - .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md
  - .planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md
  - .planning/todos/pending/2026-08-31-dev-test-chip-name-must-match-database.md
  - .planning/todos/pending/delete-banner-locked-steps-dead-field.md
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CLOSURE.md
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-hyg03-record.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt
autonomous: true
requirements: [HYG-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`MILESTONES.md`'s v1.36 section records, in the same voice as the existing corrections, that `dedup_fingerprint` must NEVER be refactored to hash `to_dict()` or to reflect over dataclass fields (HYG-03, D-18)"
    - "The record states WHY in the terms this milestone earned: every additive field would then re-key every historical report, `count_agreeing` reads the hash embedded in an already-filed body and never re-hashes, and this phase alone added seven keys that would each have moved every hash"
    - "The record names the mechanism that makes the exclusion work — an explicit five-entry allow-list with no reflection, so absence from the list IS the exclusion — and names the gate that would redden if someone tried the refactor anyway"
    - "HYG-03 is more than prose by the end of this plan: an AST pin asserts `dedup_fingerprint`'s body calls no serializer and reflects over no dataclass fields, and it has been observed RED against a planted `to_dict()`-hashing body"
    - "All 18 of this phase's requirement checkboxes and all 18 of its `REQUIREMENTS.md` traceability rows are flipped, and NO other row in that file changed — asserted by counting the changed lines and matching every one against the 18 ids (D-22)"
    - "`.planning/ROADMAP.md` is NOT edited by this plan: the orchestrator owns roadmap writes, and the generated roadmap verbs reformat the whole file and have been measured clobbering an unrelated phase's dependency table"
    - "The six disposed todos are moved to `.planning/todos/completed/` with the house frontmatter — a `resolved:` line naming the date and the fixing phase, `status: resolved`, and a `## RESOLUTION` section — and none is deleted"
    - "The already-fixed ladder todo is closed with ZERO implementation, naming Phase 177's match bucket as the fixing phase, exactly as CONTEXT.md's Folded Todos section adjudicates it"
    - "The RPT-F1 naming todo is closed as resolved-by-this-phase rather than implemented-as-written: its proposed solution (putting the canonical name into `ac.chip`) is superseded by the additive decision, its two open sub-questions are answered, and its test-sweep ask was refused with a measurement"
    - "`181-CLOSURE.md` LEADS with the zero-re-key claim rather than with a field list, in the state-the-verdict / name-what-is-excluded / name-the-gate form the Phase 177 inventory established (D-16)"
    - "The seven-leg green-tree battery runs ONCE, here, and is recorded leg by leg with an explicit `rc=` per leg: ruff check, ruff format check, the mypy watermark, the snapshot check, the orchestrator gate, the claim scanner, and the full suite at a floor of 2239"
    - "The full suite is run as a BACKGROUND command, because it is measured at about 741 seconds against a 600-second per-call foreground timeout, and a timeout would report a failure indistinguishable from a real one"
    - "The full-suite verdict is RE-DERIVED at verify time from pytest's own output file, never read from the transcript's hand-written `suite_passed=` scalar: the count is parsed from pytest's summary line, reconciled against pytest's own `collected` total, required newer than the app repo's HEAD commit, and required to agree with the recorded scalars"
    - "That re-derivation has been observed RED against three plants — a transcript whose `suite_passed=` was hand-edited, a stale raw output backdated before the phase's commits, and a raw output whose own pass count was falsified — so a green reading cannot come from a mis-transcribed, subset or stale run"
    - "The three `tests/test_skip_census.py` failures are recorded as PRE-EXISTING with their cause, so a reader can tell them from a regression this phase caused"
    - "The phase STOPS at the record: no beta cut, no tag, no pull request, no push, no branch deletion, and no ROADMAP phase-completion write (D-22)"
    - "All 19 `FROZEN_HASHES` literals are byte-identical to app base `04fd982`, measured one final time after every plan in the phase has landed (D-16)"
    - "No `#` comment was added to product source, and `tests/test_blast_radius_invariance.py` still carries exactly 0"
    - "`git -C /workspaces/firestarter status --porcelain` prints nothing and the generated `chip_database.json` is byte-unchanged"
  prohibitions:
    - "dedup_fingerprint must never be refactored to hash to_dict() or to reflect over dataclass fields -- every additive field would then re-key every historical report"
    - "No push, no pull request, no tag, no branch cut and no beta lockstep in this phase -- the close is left to the milestone commands, keeping a verifier pass between the last code change and any outward-facing push"
    - "A requirement checkbox must not be flipped for a requirement whose plan did not land -- an executor marking a multi-plan requirement complete early is a recorded failure in this project"
    - "The generated requirements and roadmap writer verbs must not be used -- they reformat the whole file and have been measured clobbering an unrelated phase's table"
    - "A disposed todo must be moved with its resolution recorded, never deleted"
    - "A gate must not read green while the rule it guards is violated"
    - "No comment may be written into product source, and firestarter_app/tests/ counts as product source"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "HYG-03's decision recorded in the v1.36 section (D-18)"
      contains: "to_dict"
    - path: "firestarter_app/tests/test_blast_radius_invariance.py"
      provides: "the AST pin that makes HYG-03 enforceable, plus its planted-RED leg"
      contains: "dedup_fingerprint"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the 18 checkbox flips and the 18 traceability rows, with nothing else touched"
      contains: "RPT-F2"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CLOSURE.md"
      provides: "the closing document, leading with the zero-re-key claim"
      contains: "FROZEN_HASHES"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-hyg03-record.txt"
      provides: "the HYG-03 record's placement and the AST pin's GREEN/RED transcript"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt"
      provides: "the seven legs with an explicit rc per leg, the measured suite count against the 2239 floor, the three pre-existing failures named, and `suite_raw_output=` pointing at pytest's own output file so the verdict can be re-derived rather than trusted"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: "firestarter_app/firestarter/diagnostic_report.py"
      via: "the recorded decision names `dedup_fingerprint`'s explicit five-entry allow-list as the mechanism the record protects"
      pattern: "dedup_fingerprint"
    - from: "firestarter_app/tests/test_blast_radius_invariance.py"
      to: ".planning/MILESTONES.md"
      via: "the AST pin is the gate the recorded decision names, so the record and the enforcement point at each other"
      pattern: "dedup_fingerprint"
---

<objective>
Close the phase at the record, and leave behind the one decision that protects everything the
milestone built.

HYG-03 is the tempting cleanup: `dedup_fingerprint` hand-lists five components, and a future reader
will see a hand-written list beside a `to_dict()` that already produces the whole mapping and
"simplify" it. Doing that would make every additive field re-key every historical report — and this
phase alone added seven keys. D-18 requires the decision recorded in `MILESTONES.md`; this plan
records it there and then makes it enforceable with an AST pin, so the record has a gate pointing
back at it.

Beside that: the 18 requirement checkbox and traceability flips, the six todo dispositions, the
closing document that leads with the zero-re-key claim rather than with a field list, and the
seven-leg green-tree battery run ONCE for the whole phase.

Purpose: D-22 draws the line here deliberately. The beta cut, the `v1.36` tag and the three
`.github`-only pull requests to protected `main` branches belong to the milestone commands, so a
verifier pass sits between the last code change and any outward-facing push — the same shape v1.35
closed with, and the gate that caught real gaps in phases 174, 179 and 180.
Output: one recorded decision, one AST pin with its planted RED, 36 flipped lines, six moved todos,
one closing document and two evidence transcripts.

**Nothing in this plan pushes anything.** No beta cut, no tag, no pull request, no branch deletion,
no remote write of any kind.
</objective>

<orchestrator_dispositions>
Measurements taken against the live tree. These **OVERRIDE** `181-CONTEXT.md` and `181-PATTERNS.md`
where they conflict.

- **The full suite cannot be run in the foreground.** It is measured at about 741 seconds
  (`MILESTONES.md` §v1.36 records 740.92 s) against a 600-second per-call foreground timeout. A
  timed-out run returns a non-zero status that reads exactly like a real failure, and this project has
  already recorded one gate misdiagnosed that way. Run it as a BACKGROUND command, poll for
  completion, and paste the verbatim summary tail into the transcript.
- **The suite has three PRE-EXISTING failures**, in `tests/test_skip_census.py`, caused by a 180-second
  child-run timeout cap against the ~741-second suite. They are unrelated to anything this milestone
  touched and were re-confirmed unchanged at Phase 174's close. Record them as pre-existing with their
  cause; do NOT attempt to fix them and do NOT let them read as a regression.
- **The suite floor is 2239**, not the 2242 recorded in Phase 179's plans. Deleting
  `tests/test_rekey_ledger.py` removed exactly 26 test instances and took the measured count from
  2265 to 2239. A plan asserting 2242 would read RED against a healthy tree. This phase ADDS tests, so
  the measured count will exceed 2239 — record the measured number and compare against the floor
  rather than pinning an equality.
- **`tools/rekey/check_rekey_ledger.py` does not exist.** The battery is SEVEN legs, not the eight
  recorded in Phase 179's reusable ritual. A plan copying the eight-leg version forward invokes a
  deleted script.
- **The mypy watermark is 35** and the gate is `tools/check_mypy_watermark.py`, scoped to
  `firestarter/ tests/` under py3.11. Note that mypy prints OK when the target is MISSING, which is
  why the watermark tool exists rather than a bare invocation.
- **`test_flash_path_record_sync` asserts whole-repo porcelain**, so every commit in this plan must
  land BEFORE the full-suite leg runs, or the suite reddens on this plan's own uncommitted work.
- **`REQUIREMENTS.md` carries BOTH surfaces**: the 18 v1 checkboxes in §"v1 Requirements"
  (`- [ ] **RPT-A1**:` and its siblings) and 18 rows in its own §Traceability table
  (`| RPT-A1 | Phase 181 | Pending |`). Both flip. That is 36 changed lines and no more.
- **`ROADMAP.md` also carries a v1.36 coverage table with the same 18 ids.** It is NOT this plan's to
  edit: the orchestrator owns roadmap writes, and `roadmap.update-plan-progress` has been measured
  overwriting an unrelated phase's dependency table by position. Leave `ROADMAP.md` alone entirely.
- **Six todos are disposed here, not four.** CONTEXT.md's Folded Todos section names four; two more
  are discharged by this phase's own work and would otherwise sit open against landed code: the
  dead-banner-field todo (closed by plan `181-04`'s RPT-B2 deletion) and the chip-name-must-match
  todo (closed by plan `181-05`'s RPT-F1, and explicitly named in CONTEXT.md's Reviewed Todos as
  resolved-by-this-phase rather than implemented-as-written).
- **The completed-todo convention** is a `resolved:` frontmatter line naming the date and how, a
  `status: resolved` line, and a `## RESOLUTION (<date>)` section — measured from
  `.planning/todos/completed/w27c512-eeprom-misclassification.md`.
- **All three `plan:pre` detectors resolved to their skip branch, re-run this session.**
  `api-coverage` `{"detected":false}` — no `COVERAGE.md`. `assumption-delta scan 181`
  `{"detected":false,"signals":[]}` — no `<assumption_delta_decision>` block. No ORM-relevant file —
  no `[BLOCKING]` schema-push task.
</orchestrator_dispositions>

<flagged_assumptions>
The spec-less probe fallback is ACTIVE (no SPEC.md). The report handed to this session carries **28
applicable edges over 18 requirements**, all arriving `unresolved`.

**This plan's share is ZERO rows, and that is itself worth recording rather than passing over.**
`HYG-03` has no row in the authoritative report handed to this session. The earlier planning session
worked from a 30-row file whose two extra rows were `HYG-02`'s and `HYG-03`'s; `HYG-02`'s are
discharged in plan `181-03`. So the accounting closes as: 28 rows in the current report, 30 in the
earlier one, delta 2, both accounted for — one discharged elsewhere, one absent here.

**Why zero is the right answer for HYG-03 rather than a gap.** HYG-03's deliverable is a RECORD:
"a decision is recorded that `dedup_fingerprint` must not be refactored to hash `to_dict()`". A
record has no input domain, no boundary, no ordering and no encoding edge — the probe's taxonomy has
nothing to bite on, and inventing a row for it would be fabricating an edge to satisfy a count.
**The flagged assumption, stated explicitly rather than dropped:**

- **FA-181-11 (`HYG-03`, no row in the report).** The requirement is discharged by prose in
  `MILESTONES.md`, and this plan strengthens it with an AST pin beyond D-18's letter. **If the
  operator disagrees, the edge to author is what should happen if a future phase legitimately NEEDS a
  new hash component** — the answer today is the empty-default direction every existing discriminator
  follows (return an empty string for the default case and skip the append, so already-filed
  fingerprints stay byte-identical), which the record names but which no test pins for a
  hypothetical future component.

**Phase-wide edge accounting, closed here.** Across the ten plans: 6 rows in `181-01`, 0 in
`181-02`, 7 in `181-03` (against the earlier 30-row file), 2 in `181-04`, 4 in `181-05`, 6 in
`181-06`, 2 in `181-07`, 3 in `181-08`, 2 in `181-09`, 0 here. No row in either report version is
silently dropped: every one is either authored as an explicit truth, marked backstop, or surfaced as
a numbered flagged assumption.

**Prohibition recall (§B, run in-prompt).** Stage 1 over-produced about a dozen raw must-NOTs;
Stage 2 kept the integrity items authored above, descriptor-less, never under `truths`. Three are
specific to this plan: **the never-hash-`to_dict()` rule itself**, which is the requirement;
**no push, no PR, no tag** (D-22's fence, and this project's `main` branches are protected in all
three repositories with no bypass, so an attempted push would fail loudly rather than silently —
which is not a reason to try); and **no checkbox flipped for a requirement whose plan did not land**,
which is a recorded failure mode in this project rather than a hypothetical. **Canon-referral
breadcrumb:** no OWASP, injection, path-traversal, prototype-pollution, GDPR or generic-fairness
candidate survived Stage 2 here — this plan writes records and runs gates.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md
</context>

<environment>
Three git repositories; **branch names differ between meta and the submodules**.

- Meta `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`. Owns `.planning/`, `.claude/`.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, **phase base app
  HEAD `04fd982`**. **Commit inside the submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — **not touched**. Must stay porcelain-clean.

Python is `/workspaces/firestarter_app/.venv311/bin/python` — 3.11, matching app CI. The devcontainer
default is 3.12 and that divergence has already broken beta CI once.

`pytest` `addopts` is `-ra -q`; a count line needs `-o addopts=""`.

Shell `grep` here is **ugrep and silently honours `.gitignore`** — every evidence-grade scan uses
`/usr/bin/grep`. `-qF` with a dash-leading pattern exits 2 and fails OPEN; use `-qFe`.

**The full suite runs for about twelve minutes.** Run it as a background command and poll; do not run
it in the foreground.

**No push, no PR, no tag, no branch cut, no beta lockstep** in any task (D-22). `main` is protected in
all three repositories with no bypass; this phase does not go near it.
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and CONTEXT.md D-26 are
**non-overridable**, and `firestarter_app/tests/` counts as product source. Write **no `#` comment**.
The only product-source edit in this plan is the AST pin's two test functions, and
`tests/test_blast_radius_invariance.py` must stay at **exactly 0** comment tokens.

`.planning/*.md` and `.planning/todos/*` are records, not source; their prose is the deliverable.
</comment_discipline>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record HYG-03's decision in `MILESTONES.md`, and make it enforceable with an AST pin observed RED</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` with plans `181-01` through `181-09` committed and a clean working tree, and `git -C /workspaces/firestarter status --porcelain` prints nothing.</precondition>
  <reversibility rating="reversible">A paragraph in a record plus two test functions. Nothing in the shipped package changes behaviour, and the pin asserts a property that already holds.</reversibility>
  <files>
    .planning/MILESTONES.md,
    firestarter_app/tests/test_blast_radius_invariance.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-hyg03-record.txt
  </files>
  <read_first>
    - `/workspaces/.planning/MILESTONES.md:1-20` — the v1.36 section: the retirement paragraph's voice, the corrections tables, the "What was done about it" paragraph and the Phase 178 paragraph. The HYG-03 entry joins these in the same voice — a stated decision with the measurement behind it.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:263-320` — `dedup_fingerprint` in full: the explicit `parts` list, the two conditional appends whose tags return an empty string for the default case, the join and the truncated digest. **This is the mechanism the record protects**, and the pin's subject.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:676-683` — the `run_status` docstring, the house model for STATING a dedup exclusion.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:268-320` — `test_dedup_fingerprint_is_frozen`, its 19-way parametrization and the two-commit review rule in its failure message. The new pin joins this module because that is where the hash's invariants already live.
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py:57-148` — the anti-vacuity triad: assert the subject is real, assert the anchor is unique before mutating, and carry the vacuity leg separately. **In-memory mutation only — write no fixture file.**
    - `/workspaces/.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-CONTEXT.md` D-08 — the record that `dedup_fingerprint` builds from an explicit five-entry allow-list with no reflection over dataclass fields, which is the sentence HYG-03's record makes permanent.
  </read_first>
  <behavior>
    - The AST pin passes at HEAD: `dedup_fingerprint`'s body contains no call to the serializer, no dataclass-reflection call and no comprehension over a report's own attribute names.
    - The pin reddens against an in-memory mutant whose body hashes the serialized mapping.
    - The pin reddens against an in-memory mutant whose body reflects over dataclass fields.
    - An empty forbidden-call set fails rather than passing vacuously.
    - `MILESTONES.md`'s v1.36 section contains the HYG-03 decision paragraph.
  </behavior>
  <action>
Two commits: the record in `/workspaces`, the pin inside `/workspaces/firestarter_app`.

**The record (HYG-03, D-18).** Add one paragraph to `.planning/MILESTONES.md`'s v1.36 section, in the
voice of the existing corrections — a stated decision with its measurement, not an exhortation. It
must say, in substance: `dedup_fingerprint` must NEVER be refactored to hash `to_dict()` or to
reflect over dataclass fields; the reason is that the function's pre-image is an explicit five-entry
allow-list and a field's ABSENCE from that list is the entire exclusion mechanism, so a reflective
rewrite would fold every additive field into the hash; `count_agreeing` reads the hash embedded in an
already-filed issue body and never re-hashes, so any such re-key is permanent for the historical
corpus and no migration of a filed issue is possible; and this phase alone is the measurement — it
added seven exported keys, each of which would have moved every hash under a reflective
implementation, and instead moved none. Name the temptation explicitly, because that is what makes
the record useful: a hand-written five-entry list sitting beside a `to_dict()` that already produces
the whole mapping looks like duplication, and it is not. Name the gate: the AST pin added below, plus
the 19-way frozen parametrization and the two-commit review rule carried in its failure message.

Use a scoped edit. Do not rewrite `MILESTONES.md`, do not reflow the v1.36 section's existing
paragraphs, and do not touch the archived milestone sections below it — the milestone close is
hand-archived in this project and the file is long.

**The pin (a planner strengthening beyond D-18's letter, recorded as such).** Add two module-level
test functions to `firestarter_app/tests/test_blast_radius_invariance.py`, plus one private helper.

The helper parses `firestarter/diagnostic_report.py` from its own module file, asserts the source
length exceeds 1000 characters (the file-actually-loaded guard), locates the `dedup_fingerprint`
function definition, and returns the set of called names and attribute names reachable from its body.

`test_dedup_fingerprint_hashes_an_explicit_allow_list_and_never_the_serialized_mapping` — assert the
returned set contains the join and digest calls it legitimately makes, and contains NONE of the
serializer entry point, the dataclass-reflection helpers or an attribute-name reflection. Its
docstring carries HYG-03's reason in two sentences and points at the `MILESTONES.md` paragraph as the
decision's home, so the record and the enforcement name each other.

`test_a_planted_reflective_body_reddens_the_allow_list_pin` — assert the anchor text occurs exactly
once in the loaded source, build two in-memory mutants (one hashing the serialized mapping, one
reflecting over dataclass fields), and assert the pin's claim raises `AssertionError` against each.
Add the vacuity leg in the same test or as a third function: an empty forbidden-name set must fail
rather than pass. Mutate strings in memory only; write no fixture file.

Then write `evidence/181-10-hyg03-record.txt` recording `milestones_paragraph_present=true`,
`milestones_section=v1.36`, `record_names_allow_list=true`, `record_names_count_agreeing=true`,
`record_names_gate=true`, `pin_at_head=GREEN`, `pin_at_planted_to_dict_body=RED`,
`pin_at_planted_reflection=RED`, `empty_forbidden_set=RED`, `fixture_files_written=0`, and the
pytest tail.

Commit the record in `/workspaces`; commit the pin inside `/workspaces/firestarter_app`.
  </action>
  <verify>
    <automated>cd /workspaces && M=.planning/MILESTONES.md; test -s "$M" && /usr/bin/grep -qF 'to_dict' "$M" && /usr/bin/grep -qF 'count_agreeing' "$M" && test "$(/usr/bin/grep -c 'dedup_fingerprint' "$M")" -ge 3</automated>
    <fails_when>non-zero exit — `MILESTONES.md` is missing or empty, or the HYG-03 paragraph is absent: the file must now name the serializer entry point, name the consumer that reads a filed hash without re-hashing, and mention the hash function at least three times across the section. The leading existence test stops the greps passing vacuously on a missing file.</fails_when>
    <automated>cd /workspaces && M=.planning/MILESTONES.md; N=$(/usr/bin/grep -c '' "$M") && test "$N" -ge 20 && D=$(git diff --numstat -- "$M") && printf '%s\n' "$D" | /usr/bin/grep -qE '^[0-9]+	[0-2]	'</automated>
    <fails_when>non-zero exit — the record shrank below twenty lines (a whole-file rewrite rather than a scoped edit), or the diff removed more than two lines, which for an added paragraph means existing prose was reflowed or deleted. The archived milestone sections below v1.36 must be untouched.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. A failure in the new pin means either the hash function already reflects (which would falsify the milestone's whole invariance claim) or a planted mutant failed to redden it, making the pin vacuous.</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qF 'def test_dedup_fingerprint_hashes_an_explicit_allow_list_and_never_the_serialized_mapping(' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'def test_a_planted_reflective_body_reddens_the_allow_list_pin(' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'MILESTONES.md' tests/test_blast_radius_invariance.py</automated>
    <fails_when>non-zero exit — one of the two named tests is absent, or the pin's docstring does not point at the record's home, leaving the decision and its gate unlinked.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;n=sum(1 for t in tokenize.generate_tokens(io.StringIO(open("tests/test_blast_radius_invariance.py",encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT);print("comment_tokens="+str(n));raise SystemExit(1 if n else 0)'</automated>
    <fails_when>non-zero exit, or the printed `comment_tokens=` value is greater than 0 — a `#` comment reached a module that has never carried one.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-hyg03-record.txt; test -s "$E" && /usr/bin/grep -qF 'pin_at_head=GREEN' "$E" && /usr/bin/grep -qF 'pin_at_planted_to_dict_body=RED' "$E" && /usr/bin/grep -qF 'pin_at_planted_reflection=RED' "$E" && /usr/bin/grep -qF 'record_names_gate=true' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing, empty, or does not record both planted RED observations and the record's naming of its own gate.</fails_when>
  </verify>
  <acceptance_criteria>
    - `MILESTONES.md`'s v1.36 section carries a paragraph stating the never-hash-`to_dict()` decision, naming the five-entry allow-list mechanism, `count_agreeing`'s read-without-re-hashing behaviour, this phase's seven added keys, and the gate that enforces it.
    - The edit is scoped: no archived milestone section changed and at most two lines were removed.
    - Both named test functions exist, the pin asserts the absence of the serializer call and of dataclass reflection, and its docstring names the record's home.
    - Both planted mutants and the empty-forbidden-set leg each raise `AssertionError`, with anchor uniqueness asserted first and no fixture file written.
    - `tests/test_blast_radius_invariance.py` reports a bare `N passed` and carries exactly 0 comment tokens.
    - `evidence/181-10-hyg03-record.txt` records all ten named scalars.
  </acceptance_criteria>
  <done>The cleanup that would have destroyed the milestone's own oracle is written down as a decision with its measurement, in the record where this project keeps such decisions, and a gate now reddens if someone tries it anyway.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the 18 requirement checkboxes and traceability rows with nothing else touched, and dispose of the six todos</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/todos/pending/2026-09-08-uv-ladder-flip-on-exhausted-slots.md,
    .planning/todos/pending/2026-09-08-slots-remaining-off-by-one-at-target-resolution.md,
    .planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md,
    .planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md,
    .planning/todos/pending/2026-08-31-dev-test-chip-name-must-match-database.md,
    .planning/todos/pending/delete-banner-locked-steps-dead-field.md
  </files>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` §"v1 Requirements" — the 18 phase-181 checkboxes in the Report Fidelity and Hygiene sections, and §Traceability's 18 `| <id> | Phase 181 | Pending |` rows. Count them yourself before editing; the flip is 36 lines and no more.
    - `/workspaces/.planning/todos/completed/w27c512-eeprom-misclassification.md:1-20` — the completed-todo frontmatter convention: `resolved:` naming the date and how, `status: resolved`, `## RESOLUTION (<date>)`.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` `### Folded Todos` and `### Reviewed Todos (not folded)` — the four folded dispositions verbatim, including that the ladder-state todo takes ZERO implementation and names Phase 177, and that the chip-name todo is marked resolved by this phase rather than implemented as written, with its proposed solution superseded and its test-sweep ask refused.
    - Each of the six todo files' own frontmatter, so the added keys match each file's existing shape rather than a template.
  </read_first>
  <behavior>
    - Every one of the 18 checkboxes reads as checked and every one of the 18 traceability rows reads Complete.
    - No line in `REQUIREMENTS.md` outside those 36 changed.
    - `ROADMAP.md` is unmodified.
    - All six todos are present under `completed/` and absent from `pending/`, each carrying a `resolved:` line, `status: resolved` and a `## RESOLUTION` section.
  </behavior>
  <action>
One commit in `/workspaces`. No app-repo change in this task.

**The flips.** Edit `.planning/REQUIREMENTS.md` by hand, with scoped edits. Flip the 18 phase-181
checkboxes in §"v1 Requirements" and the 18 matching rows in §Traceability from Pending to Complete.
**Touch no other row**: the file carries requirements from every prior phase of this milestone and
their state is not this phase's to change.

Do NOT use a generated requirements writer verb. This project has measured those verbs reformatting
the whole file, and a sibling roadmap verb overwriting an unrelated phase's dependency table by
position. Hand edits only, and diff the file before committing.

Before flipping any single id, confirm the plan that owns it has landed and its `SUMMARY.md` exists.
An executor marking a multi-plan requirement complete before its last plan ran is a recorded failure
in this project; the check is cheap and it is the one thing that makes these 36 lines trustworthy.

**`ROADMAP.md` is not edited by this plan.** Its v1.36 coverage table carries the same 18 ids and the
orchestrator owns roadmap writes. Say so in the SUMMARY so the omission reads as deliberate.

**The six todo dispositions.** Move each file from `.planning/todos/pending/` to
`.planning/todos/completed/` with `git mv`, then add the house frontmatter keys and a
`## RESOLUTION (<date>)` section to each, naming the fixing phase and plan:

- the UV-ladder flip todo, fixed by plan `181-08`'s shared write-refusal predicate;
- the `slots_remaining` off-by-one todo, fixed by the same predicate at the disclosure point in plan
  `181-08`, with the note that the resolver's own resolve-time count was deliberately left unchanged;
- the stale UV-prompt comment todo, discharged by plan `181-04`'s deletion of the whole narrative
  block, recording that the two prior blockers dissolved under the hard no-comments rule;
- the `ladder_state` community-reported regression todo, closed with **ZERO implementation**, naming
  Phase 177's match bucket as the fixing phase and recording that it was verified in code and simply
  never closed;
- the dead banner-field todo, discharged by plan `181-04`'s RPT-B2 deletion;
- the chip-name-must-match-database todo, discharged by plan `181-05`'s RPT-F1 — recording that it is
  resolved BY this phase rather than implemented AS WRITTEN: its proposed solution of putting the
  canonical name into the raw-token field is superseded by the additive decision, its two open
  sub-questions are answered by the alias-match and verbatim-parenthetical rules, and its test-sweep
  ask was refused with a measurement.

Delete none of them. A deleted todo destroys the record that the problem existed.
  </action>
  <verify>
    <automated>cd /workspaces && R=.planning/REQUIREMENTS.md; test -s "$R" && for id in RPT-A1 RPT-A2 RPT-A3 RPT-A4 RPT-A5 RPT-B1 RPT-B2 RPT-D1 RPT-D2 RPT-E1 RPT-E2 RPT-E3 RPT-F1 RPT-F2 HYG-01 HYG-02 HYG-03 HYG-04; do /usr/bin/grep -qE "^- \[x\] \*\*$id\*\*" "$R" || { echo "unflipped_checkbox=$id"; exit 1; }; /usr/bin/grep -qE "^\| $id \| Phase 181 \| Complete \|" "$R" || { echo "unflipped_row=$id"; exit 1; }; done; echo "flips_ok=18"</automated>
    <fails_when>non-zero exit — the loop prints `unflipped_checkbox=<id>` or `unflipped_row=<id>` naming the first id whose checkbox or traceability row was not flipped. The `flips_ok=18` line is the only successful output.</fails_when>
    <automated>cd /workspaces && S=$(git diff --numstat -- .planning/REQUIREMENTS.md) && A=$(printf '%s\n' "$S" | awk 'NR==1{print $1}') && B=$(printf '%s\n' "$S" | awk 'NR==1{print $2}') && echo "added=$A removed=$B" && test "$A" = "36" && test "$B" = "36"</automated>
    <fails_when>non-zero exit, or the printed `added=`/`removed=` pair is not `36`/`36` — more than the 18 checkboxes and 18 traceability rows changed, which means a requirement belonging to another phase was touched. If the diff is against a already-committed state, re-run against the pre-task revision rather than adjusting the expected number.</fails_when>
    <automated>cd /workspaces && test -z "$(git status --porcelain .planning/ROADMAP.md)" && F=$(git show --name-only --format= HEAD) && test -z "$(printf '%s\n' "$F" | /usr/bin/grep -F '.planning/ROADMAP.md')"</automated>
    <fails_when>non-zero exit — `ROADMAP.md` is dirty or was changed by this plan's commit. The orchestrator owns roadmap writes and the generated writer verb has been measured clobbering an unrelated phase's dependency table.</fails_when>
    <automated>cd /workspaces && for f in 2026-09-08-uv-ladder-flip-on-exhausted-slots 2026-09-08-slots-remaining-off-by-one-at-target-resolution 2026-09-04-stale-uv-prompt-comment-in-cli-handlers build-db-diff-ladder-state-community-reported-regression 2026-08-31-dev-test-chip-name-must-match-database delete-banner-locked-steps-dead-field; do test -f ".planning/todos/completed/$f.md" || { echo "not_completed=$f"; exit 1; }; test ! -f ".planning/todos/pending/$f.md" || { echo "still_pending=$f"; exit 1; }; /usr/bin/grep -qF 'status: resolved' ".planning/todos/completed/$f.md" || { echo "no_status=$f"; exit 1; }; /usr/bin/grep -qF '## RESOLUTION' ".planning/todos/completed/$f.md" || { echo "no_resolution_section=$f"; exit 1; }; done; echo "todos_disposed=6"</automated>
    <fails_when>non-zero exit — the loop prints `not_completed=`, `still_pending=`, `no_status=` or `no_resolution_section=` naming the first todo that was not moved, or was moved without the house frontmatter and resolution section. The `todos_disposed=6` line is the only successful output.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -qF 'Phase 177' .planning/todos/completed/build-db-diff-ladder-state-community-reported-regression.md && /usr/bin/grep -qiF 'superseded' .planning/todos/completed/2026-08-31-dev-test-chip-name-must-match-database.md</automated>
    <fails_when>non-zero exit — the already-fixed ladder todo does not name Phase 177 as its fixing phase (which would credit this phase with work it did not do), or the chip-name todo does not record that its proposed solution was superseded, leaving a future reader to implement the rejected design.</fails_when>
    <automated>cd /workspaces && S=$(git status --porcelain .planning/todos/) && test -z "$(printf '%s\n' "$S" | /usr/bin/grep -E '^( D|D )')"</automated>
    <fails_when>non-zero exit — a file under `.planning/todos/` shows as deleted rather than renamed. A deleted todo destroys the record that the problem existed; `git mv` preserves it.</fails_when>
  </verify>
  <acceptance_criteria>
    - All 18 phase-181 checkboxes read as checked and all 18 traceability rows read Complete.
    - Exactly 36 lines were added and 36 removed in `REQUIREMENTS.md`; no other requirement's state changed.
    - No generated requirements or roadmap writer verb was invoked; the SUMMARY says so.
    - `ROADMAP.md` is unmodified by this plan.
    - Each of the six todos exists under `completed/`, is absent from `pending/`, and carries `status: resolved`, a `resolved:` line and a `## RESOLUTION` section.
    - The ladder-state todo names Phase 177 and records zero implementation; the chip-name todo records its proposed solution as superseded.
    - No file under `.planning/todos/` was deleted.
  </acceptance_criteria>
  <done>The requirement ledger says what landed, in 36 lines and no others; six todos that were fixed — two of them by an earlier phase that never closed them — say which phase fixed them and how, and none of them was quietly dropped.</done>
</task>

<task type="auto">
  <name>Task 3: Write the closing document leading with the zero-re-key claim, run the seven-leg battery once, and stop at the record</name>
  <files>
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CLOSURE.md,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt
  </files>
  <read_first>
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the closing form to mirror: state the verdict, name what is excluded and WHY, give the reason, and say which gate would redden if the situation changed. `181-CLOSURE.md` is this form applied to the whole phase.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` `<specifics>` — the instruction to LEAD the closing document with the zero-re-key claim rather than with a field list, because that is the claim the whole milestone was scoped around, and the instruction to present the duration comparison as what the removed row EXCLUDED rather than as a second count.
    - `/workspaces/.planning/MILESTONES.md:1-20` — the seven-leg battery's own list, the 2239 floor, the ~741-second suite measurement and the three pre-existing `test_skip_census.py` failures with their cause.
    - Every `evidence/181-*.txt` transcript this phase produced — the closing document cites them rather than restating their numbers, so a reader can re-run.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` D-22 — the scope fence in full, including the note that local `beta` must be recreated from the remote before any later ship, which is information for the milestone command and NOT an action for this plan.
  </read_first>
  <behavior>
    - `181-CLOSURE.md` exists, its first substantive claim is the zero-re-key result, and it carries the four-part closing form.
    - All seven battery legs are recorded with an explicit status per leg.
    - The recorded suite count is at or above the 2239 floor and the three pre-existing failures are named with their cause.
    - The recorded count is re-derivable from pytest's own output file: its summary counts sum to its own `collected` total, the file is newer than the app repo's HEAD commit, and it agrees with the recorded scalars.
    - The re-derivation reddens against a hand-edited transcript scalar, against a backdated raw output, and against a raw output whose pass count was falsified.
    - No commit in this plan pushes, tags, opens a pull request or cuts a branch.
    - All 19 frozen hashes reproduce, measured after every plan in the phase has landed.
  </behavior>
  <action>
One commit in `/workspaces`. Commit every other artifact in the phase FIRST — the full-suite leg
includes a test that asserts whole-repo porcelain and would redden on uncommitted work.

**The closing document.** Write
`.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CLOSURE.md`.

Its opening sentence is the milestone's headline: eighteen requirements landed and all nineteen
frozen dedup hashes are byte-identical to the phase base. Lead with that, not with a list of added
keys — the enumeration is the body, not the verdict.

Then, in the Phase 177 inventory's four-part form:
1. **The verdict.** Zero re-keys declared, asserted positively across all 19 registered shapes, with
   the measured literal-line count and the base sha.
2. **What is excluded, and why.** Every change this phase made falls outside `dedup_fingerprint`'s
   five-entry allow-list: the canonical name is additive and the hashed chip token keeps the
   operator's raw spelling; every deletion was of an unhashed key; the duration mean, the elapsed
   field, the divergence export, the fingerprint siblings, the UV boolean and the structured chip id
   are all unhashed; the schema string is never read by the hash. Name each and say why.
3. **The reason it matters.** `count_agreeing` reads the hash embedded in an already-filed issue body
   and never re-hashes, so a re-key is permanent for the historical corpus and no migration of a
   filed issue is possible.
4. **Which gate would redden.** The 19-way frozen parametrization, the literal-line diff against the
   app base, the write-op selector pin from plan `181-04`, and the new allow-list AST pin.

Then a short section per requirement cluster, each citing its evidence transcript by path rather
than restating its numbers. Include the duration comparison D-07 asks for, presented as **what the
removed row excluded** — the connects, the database load, the identity read and plan derivation —
never as a second count, per the milestone's operation-counts-not-seconds house rule.

Then a section naming what this phase deliberately did NOT do, with reasons: no canonical-spelling
sweep of the test suite, no second derived duration number, no console two-sided chip-id row, no
firmware change, no rewrite of the skill's database-versus-datasheet example row, no re-key. Each
with the decision id that settled it.

Close with D-22's fence: the beta cut, the `v1.36` tag and the three `.github`-only pull requests to
protected `main` branches are left to the milestone commands, and local `beta` must be recreated from
the remote before any of that — recorded here as information for whoever runs it, not as an action
taken here.

**The battery.** Run all seven legs, in order, and record each with an explicit status:

1. `ruff check` over the package and the tests.
2. `ruff format --check` over the same.
3. The mypy watermark tool, whose watermark is 35.
4. The snapshot-shapes check.
5. The `dev test` orchestrator gate.
6. The diagnostic-report claim scanner.
7. The full suite, at a floor of 2239.

**Run leg 7 as a BACKGROUND command and poll for completion.** It is measured at about 741 seconds
against a 600-second foreground per-call timeout, and a timeout returns a status indistinguishable
from a real failure. Write its output to a file OUTSIDE both repositories — the scratchpad, not the
work tree, so no stray log dirties porcelain — then paste the verbatim summary tail into the
transcript.

The command is `./.venv311/bin/python -m pytest tests/ -o addopts=""` from
`/workspaces/firestarter_app`, redirected (stdout and stderr both) into that scratchpad file. **Clear
`addopts` and add no `-q` of your own.** `addopts` is `-ra -q`, and a second `-q` suppresses the count
line; clearing it restores BOTH lines the verify leg needs — the `collected N items` header and the
final `N passed` summary — and the leg reconciles one against the other, so losing either reddens it.

**That scratchpad file is the gate's input, not the transcript.** Leave it in place until the verify
legs have run, and do not hand-edit it: the verify leg parses pytest's own summary line, sums
`passed`/`failed`/`skipped`/`xfailed`/`xpassed`/`error` against pytest's own `collected` total,
requires the file to carry `rootdir: /workspaces/firestarter_app` and to be newer than the app repo's
HEAD commit, and only then compares the result against the scalars you record. Record its absolute
path as `suite_raw_output=` and pytest's collected total as `suite_collected=`. The `suite_passed=`
scalar you write is checked AGAINST that re-derivation rather than believed — a transcript is written
by the party the gate constrains, and this project has already been bitten by gates whose input was
authored by their own subject.

Then write `evidence/181-10-green-tree-battery.txt` recording one section per leg with the command,
the verbatim tail and an explicit `rc=` line; then the scalars `leg_count=7`,
`ruff_check_rc=`, `ruff_format_rc=`, `mypy_watermark_rc=`, `snapshot_check_rc=`,
`orchestrator_gate_rc=`, `claim_scanner_rc=`, `suite_passed=<measured>`, `suite_floor=2239`,
`suite_failed=<measured>`, `suite_collected=<measured>`,
`suite_raw_output=<absolute path of pytest's own output file, outside both repositories>`,
`preexisting_failures=3`,
`preexisting_failure_module=tests/test_skip_census.py`,
`preexisting_failure_cause=<the 180-second child-run timeout cap against the ~741-second suite>`,
`frozen_hash_literal_count=19`, `frozen_hash_lines_added_or_removed_since_04fd982=0`,
`shapes_reproducing=19/19`, `pushed=false`, `tagged=false`, `pr_opened=false`,
`branch_cut=false`.

If `suite_failed` exceeds 3, or if any failure is outside `tests/test_skip_census.py`, **stop and
report** — that is a regression this phase caused and it is not for this plan to absorb.
  </action>
  <verify>
    <automated>cd /workspaces && C=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CLOSURE.md; test -s "$C" && test "$(/usr/bin/grep -cE '^## ' "$C")" -ge 5 && /usr/bin/grep -qF 'FROZEN_HASHES' "$C" && /usr/bin/grep -qF 'count_agreeing' "$C" && /usr/bin/grep -qF '04fd982' "$C"</automated>
    <fails_when>non-zero exit — the closing document is missing or empty, has fewer than five section headers, or does not name the frozen-hash table, the consumer that reads a filed hash without re-hashing, or the app base sha the invariance is measured against. The leading existence test stops the greps passing vacuously on a missing file.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CLOSURE.md; H=$(/usr/bin/grep -nE 'byte-identical|zero re-key|no re-key' "$C" | head -1 | cut -d: -f1) && test -n "$H" && test "$H" -le 12</automated>
    <fails_when>non-zero exit — the zero-re-key claim does not appear within the document's first twelve lines, which means the document leads with something else. CONTEXT.md's specifics require it to lead, because it is the claim the milestone was scoped around.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt; test -s "$E" && /usr/bin/grep -qF 'leg_count=7' "$E" && test "$(/usr/bin/grep -cE '^rc=' "$E")" -ge 7</automated>
    <fails_when>non-zero exit — the battery transcript is missing, empty, does not declare seven legs, or carries fewer than seven explicit `rc=` lines. Fewer than seven means a leg was skipped, and the eight-leg version of this ritual would invoke a deleted checker.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt; P=$(/usr/bin/grep -oE '^suite_passed=[0-9]+' "$E" | cut -d= -f2) && test -n "$P" && test "$P" -ge 2239 && /usr/bin/grep -qF 'suite_floor=2239' "$E" && /usr/bin/grep -qF 'preexisting_failures=3' "$E"</automated>
    <fails_when>non-zero exit — the recorded suite count is absent or below the 2239 floor, the floor itself is not recorded, or the three pre-existing failures are not named. A count below the floor means tests were lost; the floor must never be asserted as 2241 or 2242, which are superseded numbers from Phase 179's plans. This leg checks the transcript is COMPLETE; it does not establish that the suite passed, because its input is hand-written. The verdict itself is re-derived from pytest's own output in the next leg, which is the authoritative one.</fails_when>
    <automated>E=/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt; n(){ printf '%s' "$2" | /usr/bin/grep -oE "[0-9]+ $1" | /usr/bin/grep -oE '^[0-9]+' | tail -1; }; ck(){ test -s "$1" && test -s "$2" && /usr/bin/grep -qF 'rootdir: /workspaces/firestarter_app' "$2" && test "$(stat -c %Y "$2")" -gt "$3" && C=$(/usr/bin/grep -oE '^collected [0-9]+ item' "$2" | /usr/bin/grep -oE '[0-9]+' | tail -1) && L=$(/usr/bin/grep -E '^=+ .*[0-9]+ passed' "$2" | tail -1) && P=$(n passed "$L") && F=$(n failed "$L") && K=$(n skipped "$L") && X=$(n xfailed "$L") && Y=$(n xpassed "$L") && G=$(n error "$L") && test -n "$C" && test -n "$P" && test "$((P+${F:-0}+${K:-0}+${X:-0}+${Y:-0}+${G:-0}))" -eq "$C" && test "$P" -ge 2239 && test "$P" = "$(/usr/bin/grep -oE '^suite_passed=[0-9]+' "$1" | cut -d= -f2)" && test "${F:-0}" = "$(/usr/bin/grep -oE '^suite_failed=[0-9]+' "$1" | cut -d= -f2)"; }; RAW=$(/usr/bin/grep -oE '^suite_raw_output=/[^ ]+' "$E" | cut -d= -f2-) && printf '%s' "$RAW" | /usr/bin/grep -qvE '^/workspaces/' && H=$(git -C /workspaces/firestarter_app log -1 --format=%ct) && ck "$E" "$RAW" "$H" && echo "rederived_passed_at_or_above_floor=GREEN" && T2=$(mktemp) && sed -E 's/^suite_passed=[0-9]+/suite_passed=99999/' "$E" > "$T2" && ! ck "$T2" "$RAW" "$H" && echo "planted_hand_edited_scalar=RED" && R2=$(mktemp) && cp "$RAW" "$R2" && touch -d 2020-01-01 "$R2" && ! ck "$E" "$R2" "$H" && echo "planted_stale_raw_output=RED" && R3=$(mktemp) && sed -E 's/([0-9]+) passed/99999 passed/' "$RAW" > "$R3" && touch -r "$RAW" "$R3" && ! ck "$E" "$R3" "$H" && echo "planted_falsified_raw_count=RED" && rm -f "$T2" "$R2" "$R3"</automated>
    <fails_when>non-zero exit, or any of the four markers is missing from the output. `rederived_passed_at_or_above_floor=GREEN` absent means the verdict did not re-derive: `suite_raw_output=` names no absolute path outside both repositories, pytest's own output file is gone or empty, it does not carry `rootdir: /workspaces/firestarter_app` (so it is not this suite's output), its mtime is not newer than the app repo's HEAD commit (so it is stale — a transcript quoting a run that predates this phase's own commits), its summary counts do not sum to its own `collected` total (so a number was edited), the re-derived pass count is below the 2239 floor, or the transcript's `suite_passed=`/`suite_failed=` scalars disagree with the re-derived numbers. A missing `planted_hand_edited_scalar=RED`, `planted_stale_raw_output=RED` or `planted_falsified_raw_count=RED` marker is worse: the check ran green against a transcript whose count was rewritten to 99999, against a raw output backdated to 2020, or against a raw output whose own pass count was rewritten — meaning the gate cannot tell a real 2239-test pass from a fabricated one and must not be trusted. All three plants are in-memory copies under `mktemp`; no file in either repository is touched.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-10-green-tree-battery.txt; /usr/bin/grep -qF 'pushed=false' "$E" && /usr/bin/grep -qF 'tagged=false' "$E" && /usr/bin/grep -qF 'pr_opened=false' "$E" && /usr/bin/grep -qF 'branch_cut=false' "$E" && ! /usr/bin/grep -qF 'check_rekey_ledger' "$E"</automated>
    <fails_when>non-zero exit — the transcript does not record D-22's fence as held on all four counts, or it references the deleted re-key ledger checker. The four scalars are the record that the phase stopped where it was told to stop.</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff 04fd982 -- tests/fixtures/report_shapes.py) && test -z "$(printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][[:space:]]*"[a-z0-9-]+": "[0-9a-f]{12}",')" && test -z "$(git status --porcelain)"</automated>
    <fails_when>non-zero exit — a twelve-hex frozen-hash literal line moved since app base `04fd982`, or the app repo has uncommitted work. This is the phase's final measurement of its headline claim, taken after every plan has landed.</fails_when>
    <automated>cd /workspaces && L=$(git log --oneline -20) && ! printf '%s\n' "$L" | /usr/bin/grep -qiE 'tag v1\.36|open pr|beta cut' && test -z "$(git status --porcelain firestarter firestarter_app 2>/dev/null)"</automated>
    <fails_when>non-zero exit — a recent commit subject claims a tag, a pull request or a beta cut, or a submodule gitlink is left uncommitted. D-22 leaves all three to the milestone commands; a gitlink bump belongs to whoever performs the close.</fails_when>
    <automated>A=$(git -C /workspaces/firestarter status --porcelain) && B=$(git -C /workspaces/firestarter_app status --porcelain firestarter/) && C=$(git -C /workspaces status --porcelain .planning/ .claude/) && test -z "$A" && test -z "$B" && test -z "$C"</automated>
    <fails_when>non-zero exit — the firmware submodule is dirty, the app package directory is dirty, or the meta repo has uncommitted planning or skill work. This leg must run AFTER every commit in the phase or it reddens on this plan's own artifacts.</fails_when>
  </verify>
  <acceptance_criteria>
    - `181-CLOSURE.md` exists, leads with the zero-re-key claim within its first twelve lines, carries at least five sections, and names the frozen-hash table, `count_agreeing` and the app base sha.
    - It carries the four-part closing form, a section per requirement cluster citing evidence transcripts by path, the duration comparison stated as what the removed row excluded, and a deliberately-not-done section with the decision id behind each item.
    - It closes with D-22's fence, recorded as information for the milestone command rather than as an action taken.
    - `evidence/181-10-green-tree-battery.txt` records seven legs with at least seven explicit `rc=` lines, the measured suite count at or above 2239, the floor itself, and the three pre-existing failures with their module and cause.
    - The full-suite verdict is re-derived at verify time from pytest's own output file rather than read from the recorded scalar: the summary counts reconcile against pytest's own `collected` total, the file carries this suite's `rootdir` and is newer than the app repo's HEAD commit, and the recorded `suite_passed=`/`suite_failed=` agree with the re-derived numbers.
    - That re-derivation is observed RED against three plants in the same leg — a hand-edited `suite_passed=`, a raw output backdated to 2020, and a falsified raw pass count — each on an in-memory `mktemp` copy, with no file in either repository touched.
    - The transcript records `pushed=false`, `tagged=false`, `pr_opened=false` and `branch_cut=false`, and references no deleted checker.
    - Zero twelve-hex frozen-hash literal lines moved since app base `04fd982`, measured after every plan landed, with 19 of 19 shapes reproducing.
    - No commit subject claims a tag, a pull request or a beta cut; no submodule gitlink is left uncommitted.
    - All three porcelain legs print nothing, run after every commit in the phase.
  </acceptance_criteria>
  <done>The phase has a closing document that opens with the claim it was scoped around rather than with an inventory of keys, the seven-leg battery is on the record with a status per leg and the suite measured against the right floor, and the phase stopped exactly where D-22 drew the line.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Manifest for plan `181-10`, so a newly-created symbol is not flagged as drift.

**CREATED**
- the HYG-03 decision paragraph in `.planning/MILESTONES.md` §v1.36
- `test_dedup_fingerprint_hashes_an_explicit_allow_list_and_never_the_serialized_mapping` and
  `test_a_planted_reflective_body_reddens_the_allow_list_pin`, plus their private source-loading
  helper, in `firestarter_app/tests/test_blast_radius_invariance.py`
- `.planning/phases/181-.../181-CLOSURE.md`
- `.planning/phases/181-.../evidence/181-10-hyg03-record.txt`
- `.planning/phases/181-.../evidence/181-10-green-tree-battery.txt`
- six files newly present under `.planning/todos/completed/`, each with a `resolved:` line,
  `status: resolved` and a `## RESOLUTION` section

**CHANGED IN VALUE**
- 18 requirement checkboxes and 18 traceability rows in `.planning/REQUIREMENTS.md`

**DELETED:** nothing. The six todos are MOVED, not deleted.

**DELIBERATELY NOT CHANGED**
- `.planning/ROADMAP.md`, including its v1.36 coverage table — the orchestrator owns roadmap writes
- every `firestarter/` source file: this plan's only product-source edit is two test functions
- the archived milestone sections in `MILESTONES.md`
- all 19 `FROZEN_HASHES` literals

**DELIBERATELY NOT DONE (D-22)**
- no beta cut, no `v1.36` tag, no pull request, no push, no branch deletion, no submodule gitlink
  bump for the close, and no ROADMAP phase-completion write
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated proportionately: this plan writes records, flips checkboxes,
moves todo files and runs gates. Its only product-source edit is two test functions. The largest
hazard in it is not technical — it is a record that overstates what happened, or a push that D-22
forbids.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's work to the milestone close | The close reads this phase's records. An overstated closing document or a prematurely flipped checkbox propagates into a shipped milestone summary. |
| the recorded decision to a future refactor | `MILESTONES.md` is where this project's non-negotiable decisions live. A decision recorded without its measurement is an exhortation a future reader can talk themselves out of. |
| local branches to the protected remotes | `main` is protected in all three repositories with no bypass. D-22 keeps this phase away from the remote entirely, so the verifier runs before anything outward-facing. |
| the suite runner to the 600-second call timeout | A foreground full-suite run times out at 600 seconds against a ~741-second suite and returns a status indistinguishable from a real failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-181-34 | Repudiation | the closing document and the battery transcript | high | mitigate | A closing document that asserts a green battery it did not run, or a suite count it did not measure, is the failure the whole evidence convention exists to prevent. Mitigated by the per-leg `rc=` requirement; by re-deriving the suite verdict at verify time from pytest's own output file rather than from the recorded scalar — reconciled against pytest's own `collected` total, required to carry this suite's `rootdir`, required newer than the app repo's HEAD commit, and observed RED against a hand-edited scalar, a backdated raw output and a falsified raw count; by the pre-existing-failure disclosure; and by the leading-claim position check on the document itself. |
| T-181-35 | Tampering | `REQUIREMENTS.md`'s other phases' rows | high | mitigate | A generated writer verb reformats the whole file, and its roadmap sibling has been measured overwriting an unrelated phase's table by position. Mitigated by hand edits only, by the exact 36-added / 36-removed line count, and by the per-id loop that names the first unflipped id rather than failing opaquely. |
| T-181-36 | Elevation of Privilege | the protected remote branches | medium | accept | D-22 forbids any push, tag, pull request or branch cut here, and the remotes refuse a direct push regardless. Accepted rather than mitigated because the control is the remote's own ruleset plus the fence; the transcript records all four as false so the fence is auditable. |
| T-181-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package and runs no installer. The battery invokes only tools already in the pinned test extra, so no Package Legitimacy Audit is required and no install checkpoint is inserted. |

Two threats are rated `high` and both are dispositioned **mitigate** with runnable gates, so nothing
here blocks.
</threat_model>

<verification>
1. `MILESTONES.md` carries the HYG-03 decision with its mechanism, its consumer, its measurement and
   its gate, added by a scoped edit.
2. The allow-list AST pin passes at HEAD and reddens against both planted reflective bodies and an
   empty forbidden set.
3. All 18 checkboxes and all 18 traceability rows are flipped, in exactly 36 added and 36 removed
   lines, with no other row touched and `ROADMAP.md` unmodified.
4. All six todos are under `completed/` with the house frontmatter and a resolution section; none was
   deleted; the ladder todo names Phase 177 and the chip-name todo records the supersession.
5. `181-CLOSURE.md` leads with the zero-re-key claim and carries the four-part closing form.
6. The seven-leg battery is recorded with a status per leg; the suite count is at or above 2239; the
   three pre-existing failures are named with their cause. The suite verdict is re-derived from
   pytest's own output file — reconciled against its own `collected` total and required newer than the
   app repo's HEAD commit — and that re-derivation is observed RED against a hand-edited scalar, a
   stale raw output and a falsified raw count.
7. Zero frozen-hash literal lines moved since app base `04fd982`, measured after every plan landed.
8. `pushed`, `tagged`, `pr_opened` and `branch_cut` are all recorded false, and no commit subject
   claims otherwise.
9. `tests/test_blast_radius_invariance.py` carries exactly 0 comment tokens; all porcelain legs print
   nothing.

**This is the only plan in the phase that runs the battery.** It is seven legs, the floor is 2239,
and `tools/rekey/check_rekey_ledger.py` does not exist.
</verification>

<success_criteria>
- HYG-03 is discharged: the never-hash-`to_dict()` decision is recorded in `MILESTONES.md` with its
  measurement, and it now has a gate that reddens if someone tries the refactor.
- D-18 and D-22 are both honoured: the record lands, and the close does not.
- The 18 requirement flips are complete and scoped to exactly those 36 lines.
- Six todos are disposed with their fixing phase named, including two an earlier phase fixed and never
  closed.
- The closing document leads with the claim the milestone was scoped around.
- The seven-leg battery is green and on the record, with the suite measured against the 2239 floor.
- Zero firmware lines, zero `#` comments added, and nothing pushed, tagged, opened or cut.
</success_criteria>

<output>
Create `.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-10-SUMMARY.md`
when done. Record in it: where the HYG-03 paragraph landed and its two planted RED observations; the
36-line requirement diff with the confirmation that each id's owning plan had landed first; the six
todo dispositions with their fixing phases; the seven battery legs with their statuses and the measured
suite count against the floor; the three pre-existing failures named as pre-existing; an explicit
statement that `ROADMAP.md` was deliberately not edited and why; an explicit statement that nothing was
pushed, tagged, opened or cut; any anchor that had moved since this plan was written; and every
rationale that would otherwise have been a source comment.
</output>