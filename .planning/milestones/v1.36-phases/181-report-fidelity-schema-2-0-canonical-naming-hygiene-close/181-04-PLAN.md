---
phase: 181-report-fidelity-schema-2-0-canonical-naming-hygiene-close
plan: 04
type: execute
wave: 3
depends_on: [181-02]
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/diagnostic_report.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_chip_test.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
  - firestarter_app/tests/test_erase_flag_invariants.py
  - firestarter_app/tests/test_chip_test_blank_check_order.py
  - firestarter_app/tests/test_diagnostic_report.py
  - firestarter_app/tests/test_parse_devtest_issue.py
  - firestarter_app/tests/test_provenance.py
  - firestarter_app/tests/test_dev_test_cmd.py
  - firestarter_app/tests/test_derive_plan_structural_sentinel.py
  - firestarter_app/tests/test_check_devtest_orchestrator.py
  - firestarter_app/tools/check_devtest_orchestrator.py
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-scope-narrowing.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-write-op-pin-red.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-frozen-hash-reproof.txt
autonomous: true
requirements: [RPT-B2, HYG-04]

estimate:
  tokens: 160000
  raw_tokens: 160000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`write_scope` is narrowed to exactly `full` and `partial` with NO DEFAULT, so a two-argument `derive_plan(name, db)` raises `TypeError` rather than silently selecting a scope — the shape that makes the 15 previously-bare call sites safe (D-09 as narrowed, operator adjudication 2026-09-09)"
    - "`write_op = OP_WRITE_PARTIAL if write_scope == _WRITE_SCOPE_PARTIAL else OP_WRITE` still reads off `write_scope` and NOT off `is_uv`, pinned by an AST test observed RED against the `is_uv` form — the single line whose change re-keyed 7 of the 19 frozen hashes"
    - "`full_device_permitted = write_scope == _WRITE_SCOPE_FULL` is byte-unchanged, for the same reason"
    - "All 19 `FROZEN_HASHES` literals are byte-identical to app base `04fd982` after the deletion — zero twelve-hex literal lines added or removed (D-16, unamended)"
    - "`_WRITE_SCOPE_NONE`, `write_execute`, the `none` region arm, the `none`-only `locked_destructive` appends, `_SDP_LOCKED_REASON` and the trailing `none`-branch narrative all no longer exist, and `_WRITE_SCOPES` has exactly two members with the fail-closed `ValueError` ladder retained (D-08)"
    - "`Plan.locked_destructive` no longer exists — field, local, all four appends, the `Plan(...)` keyword and its docstring paragraph — and `sdp_oracle_applicable` now derives solely from `plan.steps`, its second arm removed as provably dead: the `chip_test.py:894` append it read sits in `elif sdp_allowed:` under `if write_execute:` and was reachable only at `write_scope=none` (this corrects `181-PATTERNS.md`)"
    - "`count_applicable`'s `M` is `sum(1 for s in plan.steps if s.supported)` with no `+ len(...)` term, and `BannerCounts.locked_steps` no longer exists"
    - "`banner.locked_steps` is deleted from BOTH `_banner_dict` branches, including the `None`-banner early return, and `_BANNER_KEYS` has two entries with its counted-keys docstring moved in the same commit (RPT-B2, D-14)"
    - "`cli_handlers._resolve_write_scope` no longer exists; its two-line rule is inlined at the single production call site as `partial` when `_is_uv_eprom(app, chip)` else `full`, so `_is_uv_eprom` keeps a real caller and `_dev_test_exit_code`'s `sdp_oracle_not_run` candidate is reached by an unchanged path"
    - "The stale `ALWAYS WRITES` and `REVERSAL` narrative block in `cli_handlers.py` is gone in full, from the `Design history` opener through the `Exit code` paragraph, because it describes a prompt that does not exist (folded todo 3, D-26)"
    - "HYG-04 is discharged as a FOUR-way edit, not the two-way D-19 describes: `_resolve_write_scope` leaves `_HANDLER_FUNCTION_NAMES`, leaves the positive membership assertion, leaves `_EXPECTED_DEV_TEST_REFERENCED_HELPERS`, and the `len(derived) >= 6` floor moves to the re-measured value"
    - "`tests/test_derive_plan_structural_sentinel.py` still collects and passes as a module — the module-level import that would have reddened it at collection time was removed a wave earlier in 181-02"
    - "CONTEXT.md's D-09 is amended in place so the rejected design cannot be regenerated from it: the falsified `carries no information` and `unmoved by construction` claims are gone, replaced by the measurement, and a `## Status: Phase 181 amendment` marker is present"
    - "No `#` comment was added to product source, measured by a non-increase `tokenize` COMMENT-token census; the deliberate deletions make several counts fall, which the non-increase direction permits"
    - "`git -C /workspaces/firestarter status --porcelain` prints nothing and `git -C /workspaces/firestarter_app status --porcelain firestarter/` prints nothing"
  prohibitions:
    - "No change may re-key any of the 19 frozen dedup hashes -- a filed issue body carries its hash and count_agreeing never re-hashes, so a re-key is permanent for the historical corpus"
    - "The write-op selector must never be re-pointed at is_uv -- that is the measured cause of the 7 re-keys this adjudication exists to avoid"
    - "chip_database.json must never be hand-edited -- it is generated"
    - "A gate must not read green while the rule it guards is violated"
    - "No comment may be written into product source, and firestarter_app/tests/ counts as product source"
  artifacts:
    - path: "firestarter_app/firestarter/chip_test.py"
      provides: "the narrowed two-value no-default `write_scope`, and the engine with `locked_destructive` and `write_execute` removed"
      contains: "_WRITE_SCOPES"
    - path: "firestarter_app/firestarter/cli_handlers.py"
      provides: "the inlined scope rule, `_resolve_write_scope` deleted, and the stale narrative block removed"
      contains: "_is_uv_eprom"
    - path: "firestarter_app/tools/check_devtest_orchestrator.py"
      provides: "HYG-04's allow-list with `_resolve_write_scope` removed"
      contains: "_HANDLER_FUNCTION_NAMES"
    - path: "firestarter_app/tests/test_derive_plan_structural_sentinel.py"
      provides: "the post-deletion absence pins, the no-default TypeError pin, and the write-op selector pin with its planted-RED leg"
      contains: "write_scope"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md"
      provides: "D-09 amended in place with the measurement, carrying a Phase 181 amendment marker"
      contains: "Status: Phase 181 amendment"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-write-op-pin-red.txt"
      provides: "GREEN-at-HEAD / RED-at-planted-is_uv-form transcript for the write-op selector pin"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-frozen-hash-reproof.txt"
      provides: "the D-16 re-proof after the deletion -- 19 literals, zero moved, plus the seven would-have-moved pairs recorded as the avoided outcome"
  key_links:
    - from: "firestarter_app/firestarter/cli_handlers.py"
      to: "firestarter_app/firestarter/chip_test.py"
      via: "the single production `derive_plan` call passes an explicit scope inlined from `_is_uv_eprom`, replacing the deleted `_resolve_write_scope`"
      pattern: "derive_plan"
    - from: "firestarter_app/tools/check_devtest_orchestrator.py"
      to: "firestarter_app/tests/test_check_devtest_orchestrator.py"
      via: "the allow-list and its four consumer assertions move in the same commit, or the bidirectional gate reddens"
      pattern: "_HANDLER_FUNCTION_NAMES"
    - from: "firestarter_app/tests/test_derive_plan_structural_sentinel.py"
      to: "firestarter_app/tests/fixtures/report_shapes.py"
      via: "the write-op selector pin protects the seven UV-chip-at-full shapes whose hashes the `is_uv` form would move"
      pattern: "write_op"
---

<objective>
Perform D-08's deletion at the depth the operator adjudicated on 2026-09-09, and leave the milestone's
headline claim intact: **zero re-keys, all 19 frozen hashes byte-identical.**

Everything D-08 names dies: `_WRITE_SCOPE_NONE`, `write_execute` and every guard it controlled, the
`"none"` region arm, `Plan.locked_destructive` in all seven of its appearances,
`BannerCounts.locked_steps`, `count_applicable`'s extra term, `sdp_oracle_applicable`'s second arm,
`banner.locked_steps` in both `_banner_dict` branches (RPT-B2), `cli_handlers._resolve_write_scope`,
and the stale `ALWAYS WRITES` narrative block. HYG-04's allow-list edit lands in the same commit as
the function it describes, as a **four-way** edit rather than the two-way D-19 anticipated.

One clause of D-09 is reversed, and only on a measurement: `write_scope` is **narrowed to two values
with no default** rather than dropped entirely. Dropping it would have moved the write-op selector onto
`is_uv`, and that re-keys **7 of the 19 frozen hashes** — including the two shapes Phase 174 planted
as D-02's rejected alternatives, which this phase's own RPT-F1 needs as a tripwire.

Purpose: this is the phase's risk concentration and it gets its own plan and its own wave, as
CONTEXT.md's `<specifics>` asks. Plan `181-02` already retired the `"none"` test surface and re-keyed
the corpus, so what remains here is a bounded product deletion plus its surviving assertion fallout —
not a 117-site migration entangled with a behaviour change.
Output: the deletion, the four-way HYG-04 edit, three post-deletion pins with a planted-RED
transcript, the D-16 re-proof, and CONTEXT.md's D-09 amended in place.
</objective>

<orchestrator_dispositions>
The adjudication and the measurements behind it. These **OVERRIDE** `181-CONTEXT.md` and
`181-PATTERNS.md` where they conflict, and Task 3 amends CONTEXT.md so the conflict does not survive
in the record.

- **THE ADJUDICATION (operator, 2026-09-09).** D-09's clause *"dropped entirely, not merely narrowed
  to two values"* is **reversed**. `derive_plan` keeps `write_scope`, narrowed to `"full"`/`"partial"`
  with **no default**. `_resolve_write_scope` is still deleted, its rule inlined at the single call
  site. Everything else D-08 names still dies. **D-16 stands unamended.**
- **THE MEASUREMENT that forced it.** 7 of the 19 registered shapes build a UV chip (`m27c512` /
  `M27C512` / `M27C512,M27V512`) at `write_scope="full"` — a combination `dev test` can never produce.
  Today the write op is `write`; selected by `is_uv` it becomes `write-partial`, and
  `dedup_fingerprint` hashes `f"{op}={verdict}:{cls}"`. Measured before and after pairs:
  `attr01-status-axis-transport-fault` `93cef8030c40` to `d40335a45702`;
  `m27c512-full-all-ok` `6d3afbc52315` to `71869ccc23b4`;
  `m27c512-full-blank-check-bad` `e42f1567967a` to `8aba8efea9c6`;
  `m27c512-full-canonical-name` `776846bf2dc8` to `c4ab2e895c1a`;
  `m27c512-full-comma-joined-name` `37ad34d39a19` to `34ea6230b5cb`;
  `m27c512-full-runs-1` `e4838f7bb1d3` to `d9d4724e0c2a`;
  `uv-slot-write-pass` `927571e5110f` to `dea6e2474d30`. All 19 reproduce byte-exactly at HEAD first,
  so this is the deletion's effect and not pre-existing builder drift. Two of the seven —
  `m27c512-full-canonical-name` and `m27c512-full-comma-joined-name` — are the shapes Phase 174 froze
  expressly as D-02's rejected alternatives.
- **Consequently `full_device_permitted` and `write_op` are BYTE-UNCHANGED.** CONTEXT.md D-09's
  "Consequences the planner must carry" list says both become tests on `is_uv`. Under the adjudication
  they do not. **Do not apply that half of D-09.**
- **The `ValueError` ladder SURVIVES.** CONTEXT.md D-09 says it goes with `_WRITE_SCOPES`. With two
  values left there is still something to validate fail-closed, so `_WRITE_SCOPES` becomes a
  two-member frozenset and the ladder stays.
- **`181-PATTERNS.md` is WRONG about the SDP append.** It claims the `OP_WRITE_INHIBITED`
  `locked_destructive` append at `chip_test.py:894` is "not a `write_scope="none"` append" and that its
  removal must be adjudicated separately. It IS a `"none"`-only append: it sits in the
  `elif sdp_allowed:` arm whose controlling `if` is `write_execute`, false only at `"none"`. That is
  what makes `sdp_oracle_applicable`'s second arm provably dead, and plan `181-02` measured it dead
  over all 677 names across both SDP-capability classes. It is the strongest de-risking result in the
  phase — keep it prominent in the SUMMARY.
- **HYG-04 is a FOUR-way edit, not two-way.** Beyond `_HANDLER_FUNCTION_NAMES`:
  `tests/test_check_devtest_orchestrator.py:525` carries a POSITIVE
  `assert "_resolve_write_scope" in ..._HANDLER_FUNCTION_NAMES`; `:544-551`'s
  `_EXPECTED_DEV_TEST_REFERENCED_HELPERS` asserts **exact equality on six names**; `:602-606` asserts
  `len(derived) >= 6`; and `:576-577`'s docstring justifies subset-not-equality by saying `_is_uv_eprom`
  is listed but not referenced from `dev_test`'s body. Under this plan `_is_uv_eprom` BECOMES
  referenced and `_is_interactive` stops being, so that justification inverts.
- **`interactive = _is_interactive()` becomes a ruff-F841 dead local** once `_resolve_write_scope`
  goes; it is the assignment's only consumer. `ruff check` is green-tree leg 1, so this is not
  optional.
- **`_is_interactive` is KEPT** as a function and as an allow-list entry. It resolves to a real
  callable, so test 9 stays green, and `tests/test_dev_test_cmd.py` patches it at three sites for
  prompt suppression. D-19 mandates removing only `_resolve_write_scope`; deleting a general TTY
  predicate would widen the diff for no requirement. Record the adjudication in the SUMMARY: it is now
  the allow-list's listed-but-unreferenced entry, taking over the role `_is_uv_eprom` used to hold.
- **All three `plan:pre` detectors resolved to their skip branch.** No `COVERAGE.md`, no
  `<assumption_delta_decision>`, no schema-push task.
- **The re-key ledger is RETIRED** (`tools/rekey/check_rekey_ledger.py` does not exist), the green-tree
  battery is **seven** legs, and the suite floor is **2239**, not 2242. The battery runs once, in
  `181-08`.
</orchestrator_dispositions>

<flagged_assumptions>
The spec-less probe fallback is ACTIVE. `coverage.json` carries **30 applicable edges over 18
requirements**, all arriving `unresolved`; the tally re-counted from the file is `unclassified` **12**
(the orchestrator summary said 11 — the file is authoritative), `empty` 6, `adjacency` 5, `ordering` 5,
`encoding` 1, `concurrency` 1. Phase-wide distribution: 6 rows in `181-01`, 0 in `181-02`, 7 in
`181-03`, **2 here**, 5 in `181-05`, 5 in `181-06`, 2 in `181-07`, 3 in `181-08` — 30 total, of which
16 are authored explicit, 2 backstop and 12 flagged.

**This plan's 2 rows, both `unclassified`, both surfaced as flagged assumptions. Never auto-resolved
with a backstop, never dismissed:**

- **FA-181-04 (`RPT-B2`, unclassified).** The probe returned `unclassified — review manually`. RPT-B2
  is a deletion, so it has no input domain: the verifiable property is the key's absence from both
  `_banner_dict` branches and from `_BANNER_KEYS`, which is authored as a truth above. **If the
  operator disagrees, the edge to author is what `banner` should serialize for a report whose `banner`
  is `None`** — the early-return branch, which this plan explicitly requires be edited too, because a
  deletion touching only the populated branch leaves the key on a bannerless report and `_BANNER_KEYS`
  cannot catch it (that pin is taken off a shape that has a banner).
- **FA-181-05 (`HYG-04`, unclassified).** HYG-04 is a registration invariant over a frozenset, not a
  data transformation, so the probe's shape taxonomy has nothing to bite on. The verifiable property
  is bidirectional membership — every listed name resolves to a real callable, and every helper
  `dev_test`'s body references is listed — both of which are existing tests this plan re-anchors.

**No-silent-drop equality:** 2 rows in scope == 0 authored into `must_haves.truths` + 2 flagged. Holds.

**Prohibition recall (§B, run in-prompt).** Five of the phase's ten survivors land here, authored
descriptor-less in `must_haves.prohibitions`. One is specific to this plan and is the sharpest
must-NOT in it: **the write-op selector must never be re-pointed at `is_uv`.** That is not a style
preference — it is the measured cause of seven permanent re-keys, and a future refactor that "tidies"
`write_scope == _WRITE_SCOPE_PARTIAL` into `is_uv` would look like an obvious simplification while
silently renaming seven filed dedup groups. The AST pin in Task 3 is that prohibition's gate.
**Canon-referral breadcrumb:** no OWASP, injection, path-traversal, prototype-pollution, GDPR or
generic-fairness candidate survived Stage 2 here — the deletion narrows the code's own decision
surface and touches no external input path.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt
</context>

<environment>
Three git repositories; **branch names differ between meta and the submodules**.

- Meta `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`. Owns `.planning/`, `.claude/`.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, **phase base app
  HEAD `04fd982`**. **Commit inside the submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — **not touched**. Must stay porcelain-clean.

`git -C /workspaces/firestarter_app status --porcelain firestarter/` names the app's own **package
directory**, not the firmware submodule.

Python is `/workspaces/firestarter_app/.venv311/bin/python` — 3.11, matching app CI. The devcontainer
default is 3.12 and that divergence has already broken beta CI once.

`pytest` `addopts` is `-ra -q`; a count line needs `-o addopts=""`.

Shell `grep` here is **ugrep and silently honours `.gitignore`** — every evidence-grade scan uses
`/usr/bin/grep`. `-qF` with a dash-leading pattern exits 2 and fails OPEN; use `-qFe`.

**Every line number in this plan is measured at app base `04fd982` and shifts as this plan's own
edits land.** Anchor every edit by content, never by line number, and record any anchor that had moved
in the SUMMARY.

`tests/test_skip_census.py`'s three failures are **pre-existing** and unrelated.

**No push, no PR, no tag, no branch cut** in any task (D-22).
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and CONTEXT.md D-26 are
**non-overridable**, and `firestarter_app/tests/` counts as product source. Write **no `#` comment**.

This plan is unusual: it **deletes** comments, in two places, and both deletions are because the
comment is a **false statement about current behaviour**, not because it is a comment. Those two are
the `ALWAYS WRITES` / `REVERSAL` narrative block in `cli_handlers.py` and the `write_scope="none"`
narrative in `chip_test.py`'s SDP arm and `count_applicable` docstring region. Everything else
pre-existing stays: the repo-wide provenance-comment sweep is a separate task, explicitly declined in
CONTEXT.md's Reviewed Todos, and this phase is not a down payment on it.

`tools/check_devtest_orchestrator.py` is **not product source** — its comment block above
`_HANDLER_FUNCTION_NAMES` is correct there and must be updated, not deleted, when the name leaves.

Baselines at app `04fd982` by `tokenize` COMMENT-token census. The gate is **non-increase**, which is
what lets the deliberate deletions land: `firestarter/chip_test.py` 924,
`firestarter/cli_handlers.py` 408, `firestarter/diagnostic_report.py` 214,
`tests/test_chip_test.py` 621, `tests/test_chip_test_sdp_leg.py` 227,
`tests/test_diagnostic_report.py` 183, `tests/test_check_devtest_orchestrator.py` 98,
`tests/test_dev_test_cmd.py` 216, `tests/test_erase_flag_invariants.py` 27,
`tests/test_chip_test_blank_check_order.py` 21, `tests/test_parse_devtest_issue.py` 71,
`tests/test_provenance.py` 12, and **exactly 0** for `tests/test_blast_radius_invariance.py` and
`tests/test_derive_plan_structural_sentinel.py`.
</comment_discipline>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the scope to two values with no default, and delete `locked_destructive` and `write_execute` from the engine and the serializer</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` with plans `181-01` and `181-02` committed and a clean working tree; `.planning/phases/181-.../evidence/181-02-derive-plan-equivalence.txt` exists and records all five zero-valued de-risking measurements.</precondition>
  <reversibility rating="costly">CONTEXT.md rates D-08 costly and it is right: restoring this surface means re-adding a dataclass field, four `derive_plan` branches, `count_applicable`'s extra term, `sdp_oracle_applicable`'s second arm and the assertions that read them. The narrowing is the cheaper half — the parameter survives, so no call site changes and no frozen hash moves. Proceed only if `181-02`'s transcript records `rows_with_nonempty_locked_destructive_at_reachable_scope=0`; if it does not, halt and report.</reversibility>
  <files>
    firestarter_app/firestarter/chip_test.py,
    firestarter_app/firestarter/diagnostic_report.py,
    firestarter_app/tests/test_blast_radius_invariance.py,
    firestarter_app/tests/test_chip_test.py,
    firestarter_app/tests/test_chip_test_sdp_leg.py,
    firestarter_app/tests/test_erase_flag_invariants.py,
    firestarter_app/tests/test_chip_test_blank_check_order.py,
    firestarter_app/tests/test_diagnostic_report.py,
    firestarter_app/tests/test_parse_devtest_issue.py,
    firestarter_app/tests/test_provenance.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-scope-narrowing.txt
  </files>
  <read_first>
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-02-derive-plan-equivalence.txt` — the five zero-valued de-risking measurements and the canonical serializer field order. **Read it before touching anything**; if `rows_with_nonempty_locked_destructive_at_reachable_scope` is not 0, halt.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:405-415` — `_SDP_LOCKED_REASON` and the `write_scope="none"` advisory-prose comment above it. Both go: the constant has no remaining consumer once the `"none"` append does.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:495-520` — `Plan`, its `locked_destructive` docstring paragraph at `:498-505`, the field at `:514`, and the `is_uv` paragraph at `:507-513` which stays.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:534-600` — the three scope constants, `_WRITE_SCOPES`, `derive_plan`'s signature with its `= "none"` default, the three-value docstring, the `ValueError` ladder, and `write_execute` at `:592`.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:600-700` — the region chain. `full_device_permitted` at `:609` **does not change**. The `if write_scope == _WRITE_SCOPE_NONE` arm at `:622-624` goes, so the chain opens with `if is_uv:`. `elif write_scope == _WRITE_SCOPE_FULL:` at `:633` and the trailing `else:` at `:650` both **survive** — a non-UV chip at `"partial"` still reaches that `else` and the test suite still exercises it. `leg_region` at `:663-668` **does not change**. `erase_is_executable` at `:685` loses its `and write_execute` term.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:700-912` — every `if write_execute:` guard (`:719` as `elif write_execute and protocol in ...`, `:748`, `:776`, `:797`, `:852`), the three scope-driven `locked_destructive.append` calls in their `else:` arms (`:763`, `:789`, `:807`), the SDP `if write_execute:` at `:852` with its `elif sdp_allowed:` at `:886-894` and the trailing `# else:` narrative at `:895-900`, and the `Plan(...)` construction at `:903-909`. **`write_op` at `:749` does not change.**
    - `/workspaces/firestarter_app/firestarter/chip_test.py:2012-2031` — `sdp_oracle_applicable`, whose second arm at `:2031` goes and whose docstring clause naming the `write_scope="none"` ALLOW-chip shape goes with it.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:3688-3731` — `BannerCounts` with its `locked_steps` field and docstring clause, and `count_applicable` with its `+ len(...)` term, its `locked_steps=` keyword, and the docstring paragraph at `:3716-3721` that explains the counting in terms of `write_scope="none"` and becomes a false statement the moment this task lands.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:846-855` — `_banner_dict`. RPT-B2 deletes `locked_steps` from **both** branches, including the `None`-banner early return `{"n_ran": None, "m_applicable": None, "locked_steps": []}`.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:120-131` — `_BANNER_KEYS`, three entries, with a counted-keys docstring that says three and names `locked_steps` as RPT-B2's delete. Its assertion site is near `:453`.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md` §"`chip_test.py`" — the measured deletion-site list. Read it for the anchors, but note the SDP-append claim in it is wrong; see `<orchestrator_dispositions>`.
  </read_first>
  <behavior>
    - `derive_plan(name, db)` with no `write_scope` raises `TypeError`.
    - `derive_plan(name, db, write_scope="none")` raises `ValueError` naming the two accepted values.
    - `derive_plan("m27c512", db, write_scope="full")` yields a first write step whose `op` is `write` and whose `full_device_permitted` is `True` — unchanged from base, which is what keeps the seven UV-chip-at-full hashes still.
    - `derive_plan("m27c512", db, write_scope="partial")` yields `write-partial` with `full_device_permitted` `False` — also unchanged.
    - `Plan` has no `locked_destructive` attribute; `BannerCounts` has no `locked_steps` attribute.
    - `to_dict()["banner"]` has exactly two keys for a report with a banner AND for a report whose banner is `None`.
    - `sdp_oracle_applicable` returns the same boolean for every plan the corpus produces as it did at base.
  </behavior>
  <action>
One commit inside `/workspaces/firestarter_app`, leaving the tree green.

**Halt check first.** Read `evidence/181-02-derive-plan-equivalence.txt`. If
`rows_with_nonempty_locked_destructive_at_reachable_scope` is not `0`, or
`sdp_oracle_applicable_second_arm_changes_result_for` is not `0`, or
`count_applicable_m_changes_without_locked_term_for` is not `0`, **stop and report** — the deletion is
not behaviour-identical and the operator must be asked again.

**In `firestarter/chip_test.py`:**

Delete `_WRITE_SCOPE_NONE` and its narrative comment. Make `_WRITE_SCOPES` a two-member frozenset of
`_WRITE_SCOPE_PARTIAL` and `_WRITE_SCOPE_FULL`. **Keep the `ValueError` ladder** — with two values
there is still something to validate fail-closed; only its message's value list narrows. Change
`derive_plan`'s signature so `write_scope` is keyword-only with **no default**: a two-argument call must
raise `TypeError`, not silently pick a scope. That no-default shape is the whole safety argument for
this adjudication — plan `181-02` found 15 call sites that were bare and therefore silently meant
`"none"`, and a `TypeError` is what guarantees no fourth such site can appear.

Rewrite `derive_plan`'s docstring: two accepted values, not three; delete the `"none"` bullet entirely;
keep the `"full"` and `"partial"` bullets as written. Add one paragraph recording, in the module's own
voice, that the parameter is deliberately **retained** rather than derived from `is_uv`, and why:
seven registered report shapes build a UV chip at `"full"`, a combination `dev test` never produces, and
selecting the write op from `is_uv` would rename their write op and re-key their dedup fingerprints —
including the two shapes Phase 174 froze as D-02's rejected alternatives. Name the pin that guards it
(Task 3's write-op selector pin). This is a docstring, never a comment.

Delete `write_execute` and unconditionalize everything it guarded: the `if write_execute:` blocks
become straight-line code and their `else:` arms — the three `locked_destructive.append` calls — go
with them; `elif write_execute and protocol in _AUTO_ERASE_ON_WRITE_PROTOCOLS:` loses the first term;
`erase_is_executable` loses `and write_execute`; the SDP arm's `if write_execute:` goes, its
`elif sdp_allowed:` branch and the whole trailing `# else:` narrative about REFUSE chips at
`write_scope="none"` go, leaving the `if sdp_allowed:` / `else:` pair that emits real steps or NA steps.

Delete the `if write_scope == _WRITE_SCOPE_NONE:` region arm so the chain opens `if is_uv:`. **Do not
touch `full_device_permitted`, `write_op` or `leg_region`** — CONTEXT.md D-09's "Consequences" list says
to re-point the first two onto `is_uv`; the adjudication reverses that and the measurement is why.

Delete `Plan.locked_destructive` — the docstring paragraph, the field, the local at `:672` and the
`Plan(...)` keyword. Delete `_SDP_LOCKED_REASON`. In `sdp_oracle_applicable`, delete the second arm and
the docstring clause describing it, and add one docstring sentence recording that the arm was removed
as **provably dead**: the append it read was reachable only at `write_scope="none"`, measured over all
677 part numbers and both SDP-capability classes in `181-02`. Delete `BannerCounts.locked_steps` and
its docstring clause; delete `count_applicable`'s `+ len(...)` term and its `locked_steps=` keyword, and
rewrite the docstring paragraph that explains `M` in terms of `write_scope="none"` so it describes what
the function now does.

**In `firestarter/diagnostic_report.py`:** delete `locked_steps` from **both** `_banner_dict` branches
(RPT-B2). The `None`-banner early return is the easy one to miss and `_BANNER_KEYS` cannot catch it,
because that pin is taken off a shape that has a banner. Also remove the one `write_scope="none"`
occurrence in this file if it is a narrative claim about the omitted-write shape.

**In `tests/test_blast_radius_invariance.py`, same commit (D-14):** remove `"locked_steps"` from
`_BANNER_KEYS`, leaving two entries, and update its counted-keys docstring from three to two, naming
Phase 181 plan 04 and RPT-B2. Record in that docstring what `181-PATTERNS.md` measured and no
precedent covers: `git log -S'_VOLTAGE_KEYS'` returns only the commit that created these pins, so this
is the **first time a Phase-174 key-list pin has ever been shrunk**, and `ec1db5c` is an additive
precedent only.

**Then the surviving test fallout.** Plan `181-02` already deleted the `"none"`-shape tests, so the
`locked_destructive` / `locked_steps` assertion surface is much smaller than the 73 occurrences
measured at app base. **Re-measure it** rather than planning against 73: run a `/usr/bin/grep -rc` for
both names across `firestarter/`, `tests/` and `tools/`, fix every surviving site, and record the
before and after counts. Expect residue in `tests/test_chip_test.py`, `tests/test_chip_test_sdp_leg.py`,
`tests/test_erase_flag_invariants.py`, `tests/test_chip_test_blank_check_order.py`,
`tests/test_diagnostic_report.py`, `tests/test_parse_devtest_issue.py` and `tests/test_provenance.py`.
For each, re-point if the claim survives and delete if its subject is gone, recording every deletion in
the SUMMARY with its claim and why the claim has no subject.

Then write `evidence/181-04-scope-narrowing.txt` recording: `write_scopes_members=2`;
`derive_plan_write_scope_has_default=false`; `bare_call_raises=TypeError`;
`none_value_raises=ValueError`; `locked_destructive_occurrences_in_firestarter=0`;
`locked_steps_occurrences_in_firestarter=0`; `banner_keys_count=2`;
`locked_name_occurrences_before=<measured>` and `_after=0`; `full_device_permitted_line_changed=false`;
`write_op_line_changed=false`; and the per-module pytest tails.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import inspect,pytest,sys;sys.path.insert(0,".");from firestarter import chip_test as ct;from tests.plan_corpus import REAL_DB;p=inspect.signature(ct.derive_plan).parameters;ws=p["write_scope"];print("kind="+str(ws.kind));print("default="+repr(ws.default));assert ws.default is inspect.Parameter.empty,ws.default;assert ws.kind is inspect.Parameter.KEYWORD_ONLY,ws.kind;assert len(ct._WRITE_SCOPES)==2,sorted(ct._WRITE_SCOPES);assert not hasattr(ct,"_WRITE_SCOPE_NONE");assert not hasattr(ct,"_SDP_LOCKED_REASON")
try: ct.derive_plan("m27c512",REAL_DB); raise SystemExit("bare call did not raise")
except TypeError: pass
try: ct.derive_plan("m27c512",REAL_DB,write_scope="none"); raise SystemExit("none did not raise")
except ValueError: pass
print("narrowing_ok=true")'</automated>
    <fails_when>non-zero exit, or the printed `narrowing_ok=true` line is absent. The chain fails when `write_scope` still carries a default, is not keyword-only, when `_WRITE_SCOPES` has other than two members, when `_WRITE_SCOPE_NONE` or `_SDP_LOCKED_REASON` still exist, when a two-argument call does not raise `TypeError`, or when the literal `"none"` does not raise `ValueError`.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import sys;sys.path.insert(0,".");from firestarter.chip_test import derive_plan,Plan,BannerCounts,OP_WRITE,OP_WRITE_PARTIAL;from tests.plan_corpus import REAL_DB;import dataclasses
assert "locked_destructive" not in {f.name for f in dataclasses.fields(Plan)},dataclasses.fields(Plan)
assert "locked_steps" not in {f.name for f in dataclasses.fields(BannerCounts)},dataclasses.fields(BannerCounts)
a=derive_plan("m27c512",REAL_DB,write_scope="full");b=derive_plan("m27c512",REAL_DB,write_scope="partial")
wa=[s for s in a.steps if s.op in (OP_WRITE,OP_WRITE_PARTIAL)][0];wb=[s for s in b.steps if s.op in (OP_WRITE,OP_WRITE_PARTIAL)][0]
print("uv_full_op="+wa.op+" fdp="+str(wa.full_device_permitted));print("uv_partial_op="+wb.op+" fdp="+str(wb.full_device_permitted))
assert wa.op==OP_WRITE and wa.full_device_permitted is True,(wa.op,wa.full_device_permitted)
assert wb.op==OP_WRITE_PARTIAL and wb.full_device_permitted is False,(wb.op,wb.full_device_permitted)
print("selector_unmoved=true")'</automated>
    <fails_when>non-zero exit, or `selector_unmoved=true` is absent. The printed `uv_full_op=` line shows the actual op: if it reads `write-partial` for a UV chip at `"full"`, the write-op selector was re-pointed at `is_uv` and seven frozen hashes have just moved — that is the exact defect this adjudication exists to prevent. The dataclass asserts fail when `locked_destructive` or `locked_steps` survives as a field.</fails_when>
    <automated>cd /workspaces/firestarter_app && C=$(/usr/bin/grep -rc 'locked_destructive\|locked_steps\|write_execute\|_WRITE_SCOPE_NONE' --include='*.py' firestarter | /usr/bin/grep -v ':0$' | wc -l) && test "$C" -eq 0</automated>
    <fails_when>non-zero exit — at least one file under `firestarter/` still mentions `locked_destructive`, `locked_steps`, `write_execute` or `_WRITE_SCOPE_NONE`, in code, docstring or comment. The count of non-zero files must be exactly 0.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import sys;sys.path.insert(0,".");from tests.fixtures.report_shapes import build_shape;from firestarter.chip_test import BannerCounts
r=build_shape("sst27sf512-six-step");d=r.to_dict();print("banner_keys_with_banner="+",".join(sorted(d["banner"])));assert sorted(d["banner"])==["m_applicable","n_ran"],sorted(d["banner"])
r.banner=None;d2=r.to_dict();print("banner_keys_none_branch="+",".join(sorted(d2["banner"])));assert sorted(d2["banner"])==["m_applicable","n_ran"],sorted(d2["banner"])'</automated>
    <fails_when>non-zero exit — either printed key list contains `locked_steps`. The second assertion is the one that matters most: a deletion touching only the populated branch leaves the key on a bannerless report, and `_BANNER_KEYS` cannot catch it because that pin is taken off a shape with a banner.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_erase_flag_invariants.py tests/test_chip_test_blank_check_order.py tests/test_diagnostic_report.py tests/test_parse_devtest_issue.py tests/test_provenance.py tests/test_derive_plan_structural_sentinel.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. A collection `error` naming `test_derive_plan_structural_sentinel.py` would mean `181-02` did not remove its module-level import.</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff 04fd982 -- tests/fixtures/report_shapes.py) && test -z "$(printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][[:space:]]*"[a-z0-9-]+": "[0-9a-f]{12}",')"</automated>
    <fails_when>non-zero exit — at least one twelve-hex frozen-hash literal line was added or removed since app base `04fd982`, which is a re-key and is exactly what this adjudication exists to avoid.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;B={"firestarter/chip_test.py":924,"firestarter/cli_handlers.py":408,"firestarter/diagnostic_report.py":214,"tests/test_chip_test.py":621,"tests/test_chip_test_sdp_leg.py":227,"tests/test_diagnostic_report.py":183,"tests/test_erase_flag_invariants.py":27,"tests/test_chip_test_blank_check_order.py":21,"tests/test_parse_devtest_issue.py":71,"tests/test_provenance.py":12,"tests/test_blast_radius_invariance.py":0,"tests/test_derive_plan_structural_sentinel.py":0};bad=[p+":"+str(n)+">"+str(e) for p,e in B.items() for n in [sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT)] if n>e];print("phase_added_comments="+("0" if not bad else ",".join(bad)));raise SystemExit(1 if bad else 0)'</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file and its measured count show where a `#` comment count rose above the app-base baseline. Counts that FALL are expected and permitted; only a rise reddens.</fails_when>
  </verify>
  <acceptance_criteria>
    - `derive_plan`'s `write_scope` is keyword-only with no default; a two-argument call raises `TypeError` and `write_scope="none"` raises `ValueError`.
    - `_WRITE_SCOPES` has exactly two members; `_WRITE_SCOPE_NONE` and `_SDP_LOCKED_REASON` do not exist.
    - `derive_plan("m27c512", db, write_scope="full")` still yields write op `write` with `full_device_permitted` `True`, and at `"partial"` still yields `write-partial` with `False`.
    - `Plan` has no `locked_destructive` field and `BannerCounts` has no `locked_steps` field.
    - No file under `firestarter/` mentions `locked_destructive`, `locked_steps`, `write_execute` or `_WRITE_SCOPE_NONE`.
    - `to_dict()["banner"]` has exactly `m_applicable` and `n_ran` both for a banner-bearing report and for one whose `banner` is `None`.
    - `_BANNER_KEYS` has two entries, its docstring says two, and it records that this is the first shrink of a Phase-174 key-list pin with `ec1db5c` named as an additive precedent only.
    - `sdp_oracle_applicable` derives solely from `plan.steps` and its docstring records the second arm's removal as provably dead, citing `181-02`'s measurement over 677 names and both SDP classes.
    - All nine named test modules report a bare `N passed`.
    - Zero twelve-hex frozen-hash literal lines added or removed since app base `04fd982`.
    - `evidence/181-04-scope-narrowing.txt` records `write_op_line_changed=false` and `full_device_permitted_line_changed=false` alongside the eleven other scalars.
    - `tokenize` COMMENT-token counts are at or below all twelve baselines.
  </acceptance_criteria>
  <done>D-08's deletion has landed at the adjudicated depth: the omitted-write scope, its advisory list, its banner field, its counting term and its oracle arm are all gone, the two lines whose movement would have re-keyed seven filed dedup groups are provably unmoved, and a bare `derive_plan` call is now a `TypeError` rather than a silent scope.</done>
</task>

<task type="auto">
  <name>Task 2: Delete `_resolve_write_scope` and the stale narrative block, inline the scope rule, and land HYG-04's four-way allow-list edit in the same commit</name>
  <files>
    firestarter_app/firestarter/cli_handlers.py,
    firestarter_app/tools/check_devtest_orchestrator.py,
    firestarter_app/tests/test_check_devtest_orchestrator.py,
    firestarter_app/tests/test_dev_test_cmd.py,
    firestarter_app/tests/test_chip_test.py,
    firestarter_app/tests/test_chip_test_sdp_leg.py
  </files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2218-2230` — `_is_interactive` and its docstring, which explains why tests patch it rather than `sys.stdin.isatty`. It is KEPT.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2255-2312` — `_is_uv_eprom`, which KEEPS a caller under this plan, and `_resolve_write_scope`, whose whole body after `del interactive` is `if not _is_uv_eprom(app, chip): return "full"` then `return "partial"`. That two-line rule is what gets inlined.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2305-2366` — the narrative block to delete. It opens at the `Design history for dev_test` comment (measured `:2315`), runs through the `ALWAYS WRITES` paragraph (`:2331-2349`), the `REVERSAL (operator-specified 2026-07-29)` paragraph (`:2351-2360`) and the `Exit code:` paragraph, and ends immediately before `@dev.command(name="test")` (measured `:2366`). `_ALWAYS_WRITES_PASS_COUNT = 6` at `:2312` and its own comment block **stay** — that constant backs a live sweep invariant.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2395-2405` — the hard-fail, the UV-scope comment, `interactive = _is_interactive()` at `:2401` and the single `derive_plan` call at `:2403`.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py:2415-2425` — the module's stated local-import convention, with the reason written out. If any new import is needed, follow it.
    - `/workspaces/firestarter_app/tools/check_devtest_orchestrator.py:140-166` — the fail-open warning comment at `:145-151`, which names `_resolve_write_scope` in prose, and `_HANDLER_FUNCTION_NAMES` at `:152-165` (ten names). This file is NOT product source; its comments are correct and must be updated rather than deleted.
    - `/workspaces/firestarter_app/tests/test_check_devtest_orchestrator.py:488-620` — all four HYG-04 consumer sites: `test_handler_function_names_all_resolve_to_real_callables` at `:493`; `test_handler_function_names_contains_the_new_uv_scope_helpers` at `:518-525` with its POSITIVE `assert "_resolve_write_scope" in ...`; `_EXPECTED_DEV_TEST_REFERENCED_HELPERS` at `:544-551`, six names, asserted by **exact equality** at `:607`; the `len(derived) >= 6` floor at `:602-606`; and the `:576-577` docstring justifying subset-not-equality on the grounds that `_is_uv_eprom` is listed but not referenced from `dev_test`'s body.
    - `/workspaces/firestarter_app/tests/test_dev_test_cmd.py:785-800` — the test whose body imports `_resolve_write_scope` and inspects its signature. Also `:520`, `:754` and `:782`, where `_is_interactive` is patched for prompt suppression; those patches stay.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:3020-3026` and `/workspaces/firestarter_app/tests/test_chip_test_sdp_leg.py:2098-2104` — two pre-existing comments asserting that `_resolve_write_scope` returns only `"full"`/`"partial"`. They become statements about a function that does not exist.
  </read_first>
  <behavior>
    - `cli_handlers` has no `_resolve_write_scope` attribute and still has `_is_uv_eprom` and `_is_interactive`.
    - The single production `derive_plan` call passes `write_scope="partial"` when `_is_uv_eprom(app, chip)` is true and `"full"` otherwise, and reaches `derive_plan` with an explicit keyword.
    - `dev test` on a UV chip and on a non-UV chip both still produce the same plan they did at app base, and `_dev_test_exit_code`'s `sdp_oracle_not_run` candidate is reached by the same path.
    - `_HANDLER_FUNCTION_NAMES` contains no name that fails to resolve to a callable in `cli_handlers`.
    - The set of `_`-prefixed helpers `dev_test`'s body references equals `_EXPECTED_DEV_TEST_REFERENCED_HELPERS`, and that set now contains `_is_uv_eprom` and no longer contains `_resolve_write_scope` or `_is_interactive`.
    - `ruff check` reports no F841 unused-local finding in `cli_handlers.py`.
  </behavior>
  <action>
One commit inside `/workspaces/firestarter_app`. The allow-list edit and the function deletion MUST be
in this same commit — D-19 says so, and `test_handler_function_names_all_resolve_to_real_callables`
turns RED the instant a listed name stops resolving.

**In `firestarter/cli_handlers.py`:**

Delete `_resolve_write_scope` entirely, docstring included. Delete the `interactive = _is_interactive()`
assignment — `_resolve_write_scope` was its only consumer and leaving it is a ruff F841 finding, and
`ruff check` is green-tree leg 1. **Keep the function `_is_interactive` itself**: it resolves to a real
callable so the allow-list stays honest, `tests/test_dev_test_cmd.py` patches it at three sites for
prompt suppression, and D-19 mandates removing only `_resolve_write_scope`. Record in the SUMMARY that
`_is_interactive` is now the allow-list's listed-but-unreferenced entry, taking over the role
`_is_uv_eprom` used to hold.

Replace the `derive_plan` call so the scope is inlined from the UV predicate — `"partial"` when
`_is_uv_eprom(app, chip)` else `"full"` — passed as an explicit `write_scope` keyword, since the
parameter now has no default. Keep the existing comment above it that explains why the UV-scope
resolution happens after the absent-chip hard-fail; that statement is still true. `_is_uv_eprom` thereby
keeps a real caller and becomes a helper `dev_test`'s body references, which is what makes the HYG-04
edit below a four-way one.

Delete the whole stale narrative block: from the `Design history for dev_test` opener through the
`Exit code:` paragraph, ending immediately before the `@dev.command(name="test")` decorator. Anchor by
those two literals, not by line number. This discharges folded todo 3
(`.planning/todos/pending/2026-09-04-stale-uv-prompt-comment-in-cli-handlers.md`) at the depth
CONTEXT.md settles it: the `ALWAYS WRITES` paragraph describes a UV prompt that does not exist, the
`REVERSAL` paragraph describes "that same UV ask" in the present tense, and the hard no-comments rule
makes deleting the whole block the only zero-comment-added fix. Anything in it still true — the six
write passes, the exit-code ladder, the report-persistence behaviour — is derivable from the code and
from `_ALWAYS_WRITES_PASS_COUNT`, which stays. `181-PATTERNS.md` measured that the narrow `:2331-2345`
cut CONTEXT.md names would leave the `REVERSAL` paragraph's false statement standing; take the whole
block.

**In `firestarter_app/tools/check_devtest_orchestrator.py`, same commit:** remove
`"_resolve_write_scope"` from `_HANDLER_FUNCTION_NAMES`, leaving nine names, and update the fail-open
warning comment above it so it no longer names a function that does not exist. Comments are correct in
this file — it is tooling, not product source — so edit the prose rather than deleting it, and keep the
substance: an unlisted helper makes this gate silently under-cover exactly that new code, which
RESEARCH C-4 proved passes with `EXIT=0`.

**In `firestarter_app/tests/test_check_devtest_orchestrator.py`, same commit — this is the four-way
part:** drop the `_resolve_write_scope` membership assertion from
`test_handler_function_names_contains_the_new_uv_scope_helpers`, keeping the `_is_uv_eprom` one, and
rewrite its name and docstring so they describe the surviving claim rather than "this plan's two new
handler helpers". Remove `_resolve_write_scope` and `_is_interactive` from
`_EXPECTED_DEV_TEST_REFERENCED_HELPERS` and add `_is_uv_eprom`, then **re-measure** the derived set and
move the `len(derived) >= 6` floor to the measured value — do not compute it from this plan's prose;
run the derivation and read the number. Rewrite the `:576-577` docstring paragraph so its
subset-not-equality justification names `_is_interactive` as the listed-but-unreferenced entry, since
`_is_uv_eprom` is now referenced. Keep the two legs' deliberate asymmetry intact: one proves every
listed name is real, the other proves every referenced helper is listed.

**In `firestarter_app/tests/test_dev_test_cmd.py`:** delete the test whose body imports
`_resolve_write_scope` and inspects its signature. Its subject no longer exists; record the deletion
and its claim in the SUMMARY. Leave the three `_is_interactive` patches alone.

**In `tests/test_chip_test.py` and `tests/test_chip_test_sdp_leg.py`:** delete the two pre-existing
comment lines that assert `_resolve_write_scope` returns only `"full"`/`"partial"`. They are deleted on
the same ground as the `ALWAYS WRITES` block — they are false statements about current behaviour in
files this phase edits — not as a down payment on the repo-wide provenance sweep, which CONTEXT.md's
Reviewed Todos explicitly declines.

Commit inside `/workspaces/firestarter_app`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'from firestarter import cli_handlers as ch;assert not hasattr(ch,"_resolve_write_scope"),"_resolve_write_scope survives";assert callable(ch._is_uv_eprom);assert callable(ch._is_interactive);print("handler_surface_ok=true")'</automated>
    <fails_when>non-zero exit, or `handler_surface_ok=true` is absent — `_resolve_write_scope` still exists on the module, or `_is_uv_eprom` or `_is_interactive` stopped being callable, which would redden the allow-list resolution gate.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import ast,pathlib;src=pathlib.Path("firestarter/cli_handlers.py").read_text(encoding="utf-8");assert len(src)>10000,len(src);t=ast.parse(src);fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=="dev_test"][0];calls=[n for n in ast.walk(fn) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name) and n.func.id=="derive_plan"];assert len(calls)==1,len(calls);kw={k.arg for k in calls[0].keywords};print("derive_plan_kwargs="+",".join(sorted(kw)));assert kw=={"write_scope"},kw;names={n.id for n in ast.walk(calls[0]) if isinstance(n,ast.Name)};print("scope_expr_names="+",".join(sorted(names)));assert "_is_uv_eprom" in names,names'</automated>
    <fails_when>non-zero exit — `dev_test`'s body does not contain exactly one `derive_plan` call, the call does not pass `write_scope` as its only keyword, or the scope expression does not reference `_is_uv_eprom`. The printed `scope_expr_names=` list shows what it actually references.</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -qF 'ALWAYS WRITES' firestarter/cli_handlers.py && ! /usr/bin/grep -qF 'REVERSAL (operator-specified' firestarter/cli_handlers.py && ! /usr/bin/grep -qF 'Design history for `dev_test`' firestarter/cli_handlers.py && /usr/bin/grep -qF '_ALWAYS_WRITES_PASS_COUNT = 6' firestarter/cli_handlers.py</automated>
    <fails_when>non-zero exit — one of the three stale narrative anchors survives in `cli_handlers.py`, or `_ALWAYS_WRITES_PASS_COUNT = 6` was deleted along with the block. That constant backs a live sweep invariant and must stay.</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -rqF '_resolve_write_scope' firestarter/ tests/ tools/</automated>
    <fails_when>non-zero exit — the name `_resolve_write_scope` survives somewhere under `firestarter/`, `tests/` or `tools/`, in code, docstring or comment. Every one of the four HYG-04 sites plus the two stale test comments must be clear.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_check_devtest_orchestrator.py tests/test_dev_test_cmd.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. A failure in `test_every_helper_referenced_by_dev_test_is_listed` means the derived set and `_EXPECTED_DEV_TEST_REFERENCED_HELPERS` disagree, or the `len(derived)` floor was not re-measured. Baseline collected counts are 26 and 62; both fall by one and the SUMMARY records the measured values.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python tools/check_devtest_orchestrator.py</automated>
    <fails_when>non-zero exit, or the checker prints a violation line naming a `dev test` helper it scanned. This is green-tree leg 5 and it is the gate HYG-04 exists to keep honest.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/</automated>
    <fails_when>non-zero exit, or ruff prints an `F841` finding for `cli_handlers.py` — the `interactive` local was left assigned with no consumer after `_resolve_write_scope` was deleted. This is green-tree leg 1.</fails_when>
  </verify>
  <acceptance_criteria>
    - `cli_handlers` has no `_resolve_write_scope`; `_is_uv_eprom` and `_is_interactive` are both still callable.
    - `dev_test`'s body contains exactly one `derive_plan` call, passing `write_scope` as its only keyword, and its scope expression references `_is_uv_eprom`.
    - `interactive = _is_interactive()` is gone and `ruff check firestarter/ tests/` reports no F841.
    - The literals `ALWAYS WRITES`, `REVERSAL (operator-specified` and the `Design history for dev_test` opener are all absent from `cli_handlers.py`, while `_ALWAYS_WRITES_PASS_COUNT = 6` survives.
    - The name `_resolve_write_scope` appears nowhere under `firestarter/`, `tests/` or `tools/`.
    - `_HANDLER_FUNCTION_NAMES` has nine entries and the fail-open warning comment above it no longer names a nonexistent function.
    - `_EXPECTED_DEV_TEST_REFERENCED_HELPERS` contains `_is_uv_eprom`, does not contain `_resolve_write_scope` or `_is_interactive`, and the `len(derived)` floor equals the re-measured derived-set size.
    - The `:576-577` docstring names `_is_interactive` as the listed-but-unreferenced entry.
    - `tests/test_check_devtest_orchestrator.py` and `tests/test_dev_test_cmd.py` both report a bare `N passed`, and `tools/check_devtest_orchestrator.py` exits 0.
    - The deleted `test_dev_test_cmd.py` signature test is recorded in the SUMMARY with its claim.
  </acceptance_criteria>
  <done>HYG-04 is discharged at the depth D-19 demands and one step further than it anticipated: the deleted function is out of the allow-list, out of its positive membership assertion, out of the derived-set expectation and out of the floor that guards it, the scope rule now lives at its single call site, and the block of prose describing a prompt that does not exist is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin what must never come back, re-prove D-16 after the deletion, and amend CONTEXT.md's D-09 so the rejected design cannot be regenerated from it</name>
  <files>
    firestarter_app/tests/test_derive_plan_structural_sentinel.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-write-op-pin-red.txt,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-frozen-hash-reproof.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_derive_plan_structural_sentinel.py:80-95` and `:290-340` — the import block after `181-02`'s removal, and the corpus-size pins, so the new pins are added in the module that already owns structural claims about `derive_plan`. It carries **0** comment tokens today; keep it that way.
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py:57-148` — the anti-vacuity triad to copy: innermost-`FunctionDef` attribution by line-number containment (correct regardless of `ast.walk` order), `assert len(source) > 1000` as the file-actually-loaded guard, `assert source.count(_PLANTED_ANCHOR) == 1` before an in-memory `source.replace(...)`, and a separate explicitly-named vacuity test. **In-memory string mutation only — write no fixture file.**
    - `/workspaces/firestarter_app/firestarter/chip_test.py` — the post-Task-1 `write_op` assignment and the `full_device_permitted` assignment, to take their exact current text as the planted anchors.
    - `/workspaces/firestarter_app/tests/fixtures/report_shapes.py:705-764` — `SHAPE_IDS`, the 19 `FROZEN_HASHES` literals, and the `m27c512-full-canonical-name` / `m27c512-full-comma-joined-name` rows Phase 174 froze as D-02's rejected alternatives.
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` §"How deep the deletions cut", D-09 in full — the paragraph to amend, including its "Verified equivalent before locking" sentence, its "carries no information the function does not already hold" claim, its "`op=write` vs `op=write-partial` component is unmoved because the selecting condition is the same either way" claim, and its "Consequences the planner must carry" list.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-02-SUMMARY.md` — the in-place amendment shape this task copies: amend so the rejected design cannot be regenerated, add a status marker, and prove both the new text's presence and the old claim's absence.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the "which gate would redden" sentence form, reused here in miniature and again in `181-08`'s closing document.
  </read_first>
  <behavior>
    - The write-op selector pin, at HEAD: the `write_op` assignment's value expression references `write_scope` and does NOT reference `is_uv`.
    - Against the planted mutant: replacing that expression with the `is_uv` form makes the pin's assertion raise `AssertionError`.
    - Anchor uniqueness: the anchor text occurs exactly once in `chip_test.py`, asserted before mutating.
    - The vacuity leg: a pin that only checked `write_scope` is *present* would stay green on the mutant, so the pin asserts absence of `is_uv` too, and an empty forbidden-name set fails rather than passing.
    - `full_device_permitted`'s value expression likewise references `write_scope` and not `is_uv`.
    - Every one of the 19 shape ids still produces its committed `FROZEN_HASHES` literal.
  </behavior>
  <action>
Two commits: the pins inside `/workspaces/firestarter_app`, the amendment and transcripts in
`/workspaces`.

**The pins.** Add three module-level test functions to
`firestarter_app/tests/test_derive_plan_structural_sentinel.py`, plus one private helper and two
module-level anchor constants. That module already imports `ast` and the corpus; add no new dependency
and write no `#` comment.

Helper `_scope_selector_names(source, target)`: parse with `ast.parse`, locate the `ast.FunctionDef`
named `derive_plan`, find the single `ast.Assign` whose target name is `target`, and return the sorted
set of `ast.Name` ids reachable from its value expression. Its docstring states that extraction is
unambiguous because each name is assigned exactly once inside `derive_plan`.

`test_the_write_op_selector_reads_write_scope_and_never_is_uv` — the pin that matters most in this
phase. Assert the extracted name set for `write_op` CONTAINS `write_scope` and DOES NOT CONTAIN
`is_uv`. Its docstring carries the measurement, because that is the only place a future reader will
find it: dropping the parameter and selecting the op from `is_uv` re-keys **7 of the 19** frozen dedup
hashes, since seven registered shapes build a UV chip at `write_scope="full"` — a combination
`dev test` never produces — and `dedup_fingerprint` hashes the op. Name the two shapes Phase 174 froze
as D-02's rejected alternatives, `m27c512-full-canonical-name` and `m27c512-full-comma-joined-name`,
and state that this phase's own RPT-F1 depends on them as a tripwire. State the ceiling: this is a
static claim about the shape of one expression.

`test_full_device_permitted_reads_write_scope_and_never_is_uv` — the same shape for the second line
CONTEXT.md D-09's "Consequences" list would have re-pointed.

`test_a_planted_is_uv_selector_reddens_the_write_op_pin` — assert the anchor occurs exactly once,
build the mutant with a single in-memory `source.replace(...)` putting the `is_uv` form in place, assert
the extracted set now contains `is_uv`, and assert the pin's own claim raises `AssertionError` on the
mutant. Add the vacuity leg in the same test or as a fourth: an empty forbidden-name set must fail
rather than pass, because a pin checking only for `write_scope`'s presence stays green on the mutant.

**The D-16 re-proof.** Write `evidence/181-04-frozen-hash-reproof.txt` recording, after the deletion:
`frozen_hash_literal_count=19`; `frozen_hash_lines_added_or_removed_since_04fd982=0`;
`shapes_reproducing=19/19`; the `tests/test_blast_radius_invariance.py` pytest tail; and a numbered
section listing the **seven before/after pairs that were avoided**, so the record shows what the
adjudication bought: `attr01-status-axis-transport-fault` `93cef8030c40` would have become
`d40335a45702`; `m27c512-full-all-ok` `6d3afbc52315` to `71869ccc23b4`; `m27c512-full-blank-check-bad`
`e42f1567967a` to `8aba8efea9c6`; `m27c512-full-canonical-name` `776846bf2dc8` to `c4ab2e895c1a`;
`m27c512-full-comma-joined-name` `37ad34d39a19` to `34ea6230b5cb`; `m27c512-full-runs-1`
`e4838f7bb1d3` to `d9d4724e0c2a`; `uv-slot-write-pass` `927571e5110f` to `dea6e2474d30`. State that
`baseline_mismatches=0` at HEAD before the measurement, so these are the deletion's effect and not
pre-existing drift.

Write `evidence/181-04-write-op-pin-red.txt` recording `anchor_count=1`, `pin_at_head=GREEN`,
`pin_at_planted_is_uv=RED`, `empty_forbidden_set=RED`, `fixture_files_written=0`, and the pytest tail.

**The CONTEXT.md amendment.** Edit
`.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md` in
place, in D-09's paragraph under §"How deep the deletions cut". Use a scoped edit — never rewrite the
whole file.

Add a `## Status: Phase 181 amendment` marker line at the head of the amended passage, labelled
consistently with how the 177 and 180 amendments are marked, carrying the date `2026-09-09` and the
words `operator adjudication`.

**Remove the two now-falsified claims outright**, do not merely annotate them: the sentence asserting
the parameter *carries no information the function does not already hold*, and the sentence asserting
the `op=write` versus `op=write-partial` component is *unmoved because the selecting condition is the
same either way*. A future reader must not be able to regenerate the rejected design from this
paragraph, which is exactly why annotation is not enough — an annotated claim still reads as the
original decision with a caveat.

Replace them with the measurement and the adjudication: the equivalence holds on every path `dev test`
can reach and fails on the frozen corpus, because 7 of the 19 registered shapes build a UV chip at
`write_scope="full"`, a combination production never produces; selecting the op from `is_uv` renames
their write op and re-keys all seven, two of which are the D-02 tripwires Phase 174 planted for this
phase's own RPT-F1; therefore `write_scope` is **narrowed to two values with no default** rather than
dropped, `_resolve_write_scope` is still deleted with its rule inlined, and everything else D-08 names
still dies. State that D-16 stands unamended. Delete or correct the two entries in D-09's "Consequences
the planner must carry" list that direct `full_device_permitted` and `write_op` onto `is_uv` — they are
now instructions to cause the defect. Name the gate:
`test_the_write_op_selector_reads_write_scope_and_never_is_uv` in
`tests/test_derive_plan_structural_sentinel.py` is what would redden if a later change tried it, and it
is the gate that caught this one.

Commit the pins inside `/workspaces/firestarter_app`; commit the amendment and both transcripts in
`/workspaces`. Then run the cleanliness legs.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_derive_plan_structural_sentinel.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^[0-9]+ passed$'</automated>
    <fails_when>non-zero exit, or the summary carries a `failed`, `error` or `skipped` word rather than a bare `N passed`. The base count is 31, minus one test deleted in `181-02`, plus the three or four added here; record the measured count in the SUMMARY rather than trusting arithmetic.</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qF 'def test_the_write_op_selector_reads_write_scope_and_never_is_uv(' tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -qF 'def test_full_device_permitted_reads_write_scope_and_never_is_uv(' tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -qF 'def test_a_planted_is_uv_selector_reddens_the_write_op_pin(' tests/test_derive_plan_structural_sentinel.py && /usr/bin/grep -qF '776846bf2dc8' tests/test_derive_plan_structural_sentinel.py</automated>
    <fails_when>non-zero exit — one of the three named tests is absent, or the pin's docstring does not carry the `m27c512-full-canonical-name` literal that names the tripwire it protects.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^103 passed'</automated>
    <fails_when>non-zero exit, or any count other than `103 passed` — that is `181-01`'s post-Task-3 count, so a `failed` here means the deletion moved a frozen hash or a key-list pin after all, and a different count means a test was added or lost outside this plan.</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff 04fd982 -- tests/fixtures/report_shapes.py) && test -z "$(printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][[:space:]]*"[a-z0-9-]+": "[0-9a-f]{12}",')"</automated>
    <fails_when>non-zero exit — at least one twelve-hex frozen-hash literal line was added or removed since app base `04fd982`. This is the D-16 re-proof and the whole point of the adjudication.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md; test -s "$C" && /usr/bin/grep -qF '## Status: Phase 181 amendment' "$C" && /usr/bin/grep -qF 'operator adjudication' "$C" && /usr/bin/grep -qF '776846bf2dc8' "$C" && /usr/bin/grep -qF 'test_the_write_op_selector_reads_write_scope_and_never_is_uv' "$C"</automated>
    <fails_when>non-zero exit — CONTEXT.md is missing or empty, the amendment marker is absent, the amendment does not name the adjudication, it does not cite the tripwire hash literal, or it does not name the gate that would redden. The leading existence test is what stops the greps passing vacuously on a missing file.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md; test -s "$C" && ! /usr/bin/grep -qF 'carries no information the function does not already hold' "$C" && ! /usr/bin/grep -qF 'unmoved because the selecting condition is the same either way' "$C"</automated>
    <fails_when>non-zero exit — one of the two falsified claims survives verbatim in CONTEXT.md, which would let a future reader regenerate the rejected design from the record. Annotating them is not sufficient; the sentences must be gone.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-frozen-hash-reproof.txt; test -s "$E" && /usr/bin/grep -qF 'frozen_hash_lines_added_or_removed_since_04fd982=0' "$E" && /usr/bin/grep -qF 'shapes_reproducing=19/19' "$E" && /usr/bin/grep -qF 'd40335a45702' "$E" && /usr/bin/grep -qF 'dea6e2474d30' "$E"</automated>
    <fails_when>non-zero exit — the re-proof transcript is missing, does not record zero moved literal lines, does not record all 19 reproducing, or does not list the avoided seven (the first and last of the seven post-values are the markers).</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-04-write-op-pin-red.txt; test -s "$E" && /usr/bin/grep -qF 'anchor_count=1' "$E" && /usr/bin/grep -qF 'pin_at_head=GREEN' "$E" && /usr/bin/grep -qF 'pin_at_planted_is_uv=RED' "$E" && /usr/bin/grep -qF 'fixture_files_written=0' "$E"</automated>
    <fails_when>non-zero exit — the RED-proof transcript is missing or does not record all four scalars, which means the phase's sharpest pin was never observed red against the mutation it exists to catch.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;n=sum(1 for t in tokenize.generate_tokens(io.StringIO(open("tests/test_derive_plan_structural_sentinel.py",encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT);print("comment_tokens="+str(n));raise SystemExit(1 if n else 0)'</automated>
    <fails_when>non-zero exit, or the printed `comment_tokens=` value is greater than 0 — a `#` comment reached a module that has never carried one.</fails_when>
    <automated>A=$(git -C /workspaces/firestarter status --porcelain) && B=$(git -C /workspaces/firestarter_app status --porcelain firestarter/) && C=$(git -C /workspaces/firestarter_app status --porcelain) && test -z "$A" && test -z "$B" && test -z "$C"</automated>
    <fails_when>non-zero exit — the firmware submodule is dirty, the app package directory is dirty, or the app repo has uncommitted work. This leg must run AFTER the commits or it reddens on this task's own edits.</fails_when>
  </verify>
  <acceptance_criteria>
    - All three named pin tests exist in `tests/test_derive_plan_structural_sentinel.py`, and the write-op pin's docstring cites `776846bf2dc8`.
    - The write-op pin asserts `write_scope` is present AND `is_uv` is absent, not merely the former.
    - The planted `is_uv` mutant reddens the pin, anchor uniqueness is asserted before mutating, and no fixture file is written.
    - An empty forbidden-name set fails rather than passing vacuously.
    - `tests/test_blast_radius_invariance.py` reports `103 passed` and zero frozen-hash literal lines moved since app base `04fd982`.
    - `181-CONTEXT.md` carries `## Status: Phase 181 amendment`, the words `operator adjudication`, the literal `776846bf2dc8`, and the gate's name.
    - Neither falsified claim survives verbatim in `181-CONTEXT.md`.
    - D-09's "Consequences the planner must carry" list no longer directs `full_device_permitted` or `write_op` onto `is_uv`.
    - `evidence/181-04-frozen-hash-reproof.txt` records zero moved literals, 19 of 19 reproducing, and all seven avoided pairs.
    - `evidence/181-04-write-op-pin-red.txt` records the four named scalars.
    - `tests/test_derive_plan_structural_sentinel.py` carries exactly 0 comment tokens.
    - All three porcelain legs print nothing, run after this task's commits.
  </acceptance_criteria>
  <done>The mutation that would have re-keyed seven filed dedup groups is now a gate that has been seen red, D-16's claim is re-proven after the deletion rather than before it, and the decision record no longer contains a falsified justification a future reader could act on.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Manifest for plan `181-04`.

**CREATED**
- `test_the_write_op_selector_reads_write_scope_and_never_is_uv`
- `test_full_device_permitted_reads_write_scope_and_never_is_uv`
- `test_a_planted_is_uv_selector_reddens_the_write_op_pin`
- the `_scope_selector_names` helper and its two anchor constants
- `.planning/phases/181-.../evidence/181-04-scope-narrowing.txt`
- `.planning/phases/181-.../evidence/181-04-write-op-pin-red.txt`
- `.planning/phases/181-.../evidence/181-04-frozen-hash-reproof.txt`
- a `## Status: Phase 181 amendment` passage in `181-CONTEXT.md`

**DELETED**
- `chip_test._WRITE_SCOPE_NONE`, `chip_test._SDP_LOCKED_REASON`, `chip_test.write_execute` (local)
- `derive_plan`'s `write_scope` DEFAULT (the parameter itself survives, keyword-only, no default)
- `derive_plan`'s `"none"` region arm, its three `else:`-arm `locked_destructive.append` calls, its
  `elif sdp_allowed:` locked branch and the trailing `"none"`-branch narrative
- `Plan.locked_destructive` (docstring paragraph, field, local, `Plan(...)` keyword)
- `BannerCounts.locked_steps` (field and docstring clause)
- `count_applicable`'s `+ len(plan.locked_destructive)` term and its `locked_steps=` keyword
- `sdp_oracle_applicable`'s second arm
- `_banner_dict`'s `locked_steps` key, in BOTH branches (RPT-B2)
- `_BANNER_KEYS`' `"locked_steps"` entry
- `cli_handlers._resolve_write_scope` and the `interactive = _is_interactive()` local
- the `Design history for dev_test` / `ALWAYS WRITES` / `REVERSAL` / `Exit code` narrative block in
  `cli_handlers.py` (folded todo 3)
- `"_resolve_write_scope"` from `tools/check_devtest_orchestrator._HANDLER_FUNCTION_NAMES` and from
  `_EXPECTED_DEV_TEST_REFERENCED_HELPERS`
- the `test_dev_test_cmd.py` test inspecting `_resolve_write_scope`'s signature
- two stale comment lines naming `_resolve_write_scope` in `tests/test_chip_test.py` and
  `tests/test_chip_test_sdp_leg.py`
- the surviving `locked_destructive` / `locked_steps` assertions across seven test modules; the full
  list is written into the `SUMMARY.md`, because the drift verifier needs to know a missing assertion
  was intended

**DELIBERATELY NOT CHANGED** — the adjudication's whole content
- `write_op = OP_WRITE_PARTIAL if write_scope == _WRITE_SCOPE_PARTIAL else OP_WRITE`
- `full_device_permitted = write_scope == _WRITE_SCOPE_FULL`
- `leg_region`, `_WRITE_SCOPE_PARTIAL`, `_WRITE_SCOPE_FULL`, the `ValueError` ladder,
  `cli_handlers._is_uv_eprom`, `cli_handlers._is_interactive`, `_ALWAYS_WRITES_PASS_COUNT`
- all 19 `FROZEN_HASHES` literals
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated proportionately: this is a local-only host CLI change. No
network surface, no authentication, no session, no credential, no user input beyond `argv` and a serial
device the operator physically owns. The plan removes a code path rather than adding one. The single
outward-facing artifact in the system is the filed GitHub issue body, and this plan changes exactly one
thing that reaches it — `banner.locked_steps` stops being emitted.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report object to the filed GitHub issue body | The one outward crossing. `sanitize_dict` is untouched; the only change is one fewer key. |
| filed issue body to `count_agreeing` | `count_agreeing` reads the `dedup_fingerprint` embedded in an already-filed body and never re-hashes, so a re-key silently splits or merges dedup groups with no migration path. This is the boundary the whole adjudication protects. |
| `plan` object to `_dev_test_exit_code` | `sdp_oracle_applicable` feeds the `sdp_oracle_not_run` code-2 candidate, a path Phase 178 just changed. Removing its second arm changes the process exit code if the arm was ever live. |
| decision record to a future planner | An unamended D-09 is an instruction to cause seven permanent re-keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-181-13 | Tampering | the write-op selector and the 19 frozen hashes | high | mitigate | Re-pointing the selector at `is_uv` re-keys seven filed dedup groups permanently. Mitigated by the narrowing itself, by Task 3's AST pin observed RED against the exact mutation, by the git-diff literal-absence leg against app base `04fd982`, and by the 19-way parametrized frozen test. |
| T-181-14 | Tampering | `sdp_oracle_applicable` and `_dev_test_exit_code`'s code-2 candidate | high | mitigate | A silent exit-code change is the failure CONTEXT.md flags as the risk concentration. Mitigated by `181-02`'s measurement that the removed arm changes the result for zero of 677 plans across both SDP-capability classes, by the halt check at the head of Task 1, and by the full `tests/test_chip_test_sdp_leg.py` and `tests/test_dev_test_cmd.py` modules staying green. |
| T-181-15 | Tampering | `_HANDLER_FUNCTION_NAMES` | medium | mitigate | The allow-list is a fail-open gate: RESEARCH C-4 proved a violating helper in an unlisted function passes with `EXIT=0`. Mitigated by landing the allow-list edit in the same commit as the deletion and by the two deliberately asymmetric bidirectional tests plus the re-measured floor. |
| T-181-16 | Information Disclosure | the deleted `banner.locked_steps` key | low | accept | The key has always serialized as an empty list on every reachable run; removing it discloses strictly less. Consumers accept `schema_version` by presence only, and RPT-E2's forward-only parse leg proves a pre-2.0 body carrying the key still parses. |
| T-181-17 | Repudiation | the amended D-09 passage | medium | mitigate | Annotation would leave the rejected design regenerable. Mitigated by removing both falsified sentences outright, by the status marker, and by the negative-grep leg that fails if either sentence survives verbatim. |
| T-181-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package; every module used is Python standard library. No Package Legitimacy Audit is required and no install checkpoint is inserted. |

Two threats are rated `high` and both are dispositioned **mitigate** with multiple runnable gates, so
nothing here blocks.
</threat_model>

<verification>
1. `write_scope` is keyword-only with two accepted values and no default; a bare call raises
   `TypeError` and `"none"` raises `ValueError`.
2. `write_op` and `full_device_permitted` still read off `write_scope`, pinned by AST and observed RED
   against the `is_uv` form.
3. Zero twelve-hex frozen-hash literal lines moved since app base `04fd982`;
   `tests/test_blast_radius_invariance.py` reports `103 passed`.
4. No file under `firestarter/` mentions `locked_destructive`, `locked_steps`, `write_execute`,
   `_WRITE_SCOPE_NONE` or `_resolve_write_scope`.
5. `to_dict()["banner"]` has two keys on both the populated and the `None`-banner branch.
6. `tools/check_devtest_orchestrator.py` exits 0; `ruff check firestarter/ tests/` reports no F841.
7. `181-CONTEXT.md` carries the amendment marker and neither falsified claim.
8. `tokenize` COMMENT-token counts at or below every app-base baseline; counts that fall are expected.
9. All three porcelain legs print nothing.

**Do not** run the seven-leg battery here, do not invoke `check_rekey_ledger.py`, and do not assert a
suite floor of 2241 or 2242. The battery runs once, in `181-08`, with floor **2239**.
</verification>

<success_criteria>
- RPT-B2 is discharged whole: `banner.locked_steps` is gone from both `_banner_dict` branches and from
  `_BANNER_KEYS`, and `Plan.locked_destructive` and `BannerCounts.locked_steps` are gone with it.
- HYG-04 is discharged as a four-way edit, in the same commit as the function it describes.
- D-08's deletion lands at the adjudicated depth, with the two dead paths removed on a measurement
  rather than a belief.
- D-16 stands unamended and is re-proven after the deletion: 19 frozen hashes, zero moved.
- The mutation that would have broken it is a gate that has been seen red.
- The decision record no longer contains a justification the measurement falsified.
- Zero firmware lines, zero `#` comments added, no push, no PR, no tag.
</success_criteria>

<output>
Create `.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-04-SUMMARY.md`
when done. Record in it: the halt-check values read from `181-02`'s transcript; the re-measured
`locked_*` occurrence count before and after; every deleted test and assertion with its claim and why
that claim has no subject; the re-measured `len(derived)` floor and the derived helper set; the
adjudication that `_is_interactive` is kept and is now the allow-list's listed-but-unreferenced entry;
the correction that `chip_test.py:894` was a `"none"`-only append, contradicting `181-PATTERNS.md`; the
measured pytest count for every touched module; any anchor that had moved since this plan was written;
and every rationale that would otherwise have been a source comment.
</output>

