---
phase: 181-report-fidelity-schema-2-0-canonical-naming-hygiene-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/diagnostic_report.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_diagnostic_report.py
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-frozen-hash-invariance.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-key-pin-planted-red.txt
  - .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-forward-only-parse.txt
autonomous: true
requirements: [RPT-A4, RPT-E1, RPT-E2, RPT-E3]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`to_dict()` carries a top-level boolean `is_uv` whose value expression is the attribute chain `self.plan.is_uv` — the single `derive_plan` decision — and nothing in the report re-derives UV-ness from a proxy (RPT-A4)"
    - "`SCHEMA_VERSION` reads `2.0`, and the literal assertions that named `1.8` name `2.0` in the SAME commit as the key change (RPT-E1, D-13, D-14)"
    - "All 19 `FROZEN_HASHES` literals in `tests/fixtures/report_shapes.py` are byte-identical to app base `04fd982` — zero twelve-hex literal lines added or removed — proving an additive top-level key cannot re-key a filed report (D-16)"
    - "`_TO_DICT_KEYS` gained exactly `is_uv`, its counted-keys docstring says fourteen, and both moved in the same commit as the key (D-14)"
    - "The two frozen `devtest-triage` fixtures, carrying `schema_version` `1.2` and `1.4` plus `locked_steps`, `vpp_mv` and `vpe_mv`, still parse to a report object under schema 2.0, asserted by test, with neither fixture file modified (RPT-E2)"
    - "RPT-E3 re-anchors, in writing, from the retired GATE-06 `RK-174-` ledger to the 19 absolute `FROZEN_HASHES` literals plus the two-commit review rule carried in `test_dedup_fingerprint_is_frozen`'s own failure message (D-15)"
    - "RPT-E3's exception clause discharges as EMPTY: no declared re-key, no ledger row, and no invocation of the deleted `tools/rekey/check_rekey_ledger.py` (D-15, D-16)"
    - "Two shapes whose canonical pre-image is equal MERGE into one dedup group and may legitimately share a frozen literal under two shape ids — the gh#39/gh#40 precedent — so an equal-hash pair is not a collision defect"
    - "`synthetic-arm4-empty-results` is the registered empty-input shape and its literal `8d6208d00be7` is unmoved by this plan"
    - "Frozen-hash assertion order is deterministic by construction — `SHAPE_IDS` is `tuple(sorted(_BUILDERS))` and the parametrization is `sorted(FROZEN_HASHES.items())` — so equal-comparing entries cannot reorder between runs"
    - "The key-list pin has been observed RED against an in-memory planted mutation of a real `to_dict()` mapping, anchor uniqueness asserted first, no fixture file written, transcript on disk under `evidence/`"
    - "No `#` comment was added: `tokenize` COMMENT-token counts stay at or below 214 for `firestarter/diagnostic_report.py` and 183 for `tests/test_diagnostic_report.py`, and exactly 0 for `tests/test_blast_radius_invariance.py`"
    - "`git -C /workspaces/firestarter status --porcelain` prints nothing and `git -C /workspaces/firestarter_app status --porcelain firestarter/` prints nothing"
  prohibitions:
    - "No change may re-key any of the 19 frozen dedup hashes -- a filed issue body carries its hash and count_agreeing never re-hashes, so a re-key is permanent for the historical corpus"
    - "dedup_fingerprint must never be refactored to hash to_dict() or to reflect over dataclass fields"
    - "The two frozen devtest-triage fixtures must never be regenerated from a live to_dict() output"
    - "A gate must not read green while the rule it guards is violated"
    - "No comment may be written into product source, and firestarter_app/tests/ counts as product source"
  artifacts:
    - path: "firestarter_app/firestarter/diagnostic_report.py"
      provides: "the `is_uv` top-level key and `SCHEMA_VERSION` at 2.0"
      contains: "is_uv"
    - path: "firestarter_app/tests/test_blast_radius_invariance.py"
      provides: "the moved `_TO_DICT_KEYS` pin, the 2.0 triple-equality, the forward-only parse leg and the planted-RED key-pin leg"
      contains: "_TO_DICT_KEYS"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-frozen-hash-invariance.txt"
      provides: "the D-16 headline transcript -- 19 literals, zero moved, measured against app base 04fd982"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-key-pin-planted-red.txt"
      provides: "GREEN-at-HEAD / RED-at-planted-mutant transcript for the key-list pin"
    - path: ".planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-forward-only-parse.txt"
      provides: "the RPT-E2 transcript -- both 1.2/1.4 fixtures parsed under 2.0, both files byte-unchanged"
  key_links:
    - from: "firestarter_app/firestarter/diagnostic_report.py"
      to: "firestarter_app/firestarter/chip_test.py"
      via: "`to_dict()` reads `self.plan.is_uv`, the value `derive_plan` decided once"
      pattern: "is_uv"
    - from: "firestarter_app/tests/test_blast_radius_invariance.py"
      to: "firestarter_app/tests/fixtures/report_shapes.py"
      via: "`test_dedup_fingerprint_is_frozen` parametrizes over the 19 absolute FROZEN_HASHES literals"
      pattern: "FROZEN_HASHES"
    - from: "firestarter_app/tests/test_blast_radius_invariance.py"
      to: ".claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md"
      via: "the RPT-E2 forward-only parse leg reads the frozen schema-1.2 fixture body"
      pattern: "devtest-triage"
---

<objective>
Land the phase's thinnest complete vertical slice and prove, on the first commit, the mechanism every
later plan depends on: **an additive report key cannot re-key a filed report.**

One key travels the whole stack — `derive_plan`'s single `Plan.is_uv` decision (RPT-A4) reaches
`to_dict()` as a top-level boolean, the Phase-174 `_TO_DICT_KEYS` pin moves with it in the same commit
(D-14), `SCHEMA_VERSION` goes to `2.0` with its literal assertions (RPT-E1, D-13), the syrupy
snapshots regenerate, and **all 19 `FROZEN_HASHES` literals stay byte-identical** (D-16). RPT-E2's
forward-only parse and RPT-E3's re-anchoring onto the surviving mechanism (D-15) close out beside it.

Purpose: if the additive-key/frozen-hash relationship does not hold, every remaining plan in this
phase is built on sand and the milestone's headline closing sentence is false. Proving it end to end
on one key, before any deletion lands, is what makes the D-08/D-09 demolition in plan `181-02` a
bounded risk rather than an entangled one.
Output: one new top-level report key, a schema bump, a moved key-list pin, regenerated snapshots, a
forward-only parse test, an RPT-E3 re-anchoring statement, three evidence transcripts.

**This plan deletes nothing** — no field, no key, no parameter, no comment block. Every deletion in
this phase happens in `181-02` or later, deliberately after this slice is green.
</objective>

<orchestrator_dispositions>
Findings the orchestrator verified against the live tree. These OVERRIDE `181-CONTEXT.md` and
`181-PATTERNS.md` where they conflict.

- **All three `plan:pre` detectors resolved to their skip branch.** `api-coverage`
  `{"detected":false}` — **do not create `COVERAGE.md`**. `assumption-delta scan 181`
  `{"detected":false}` — no `<assumption_delta_decision>` block required. `schema-gate` found no
  ORM-relevant file — no `[BLOCKING]` schema-push task.
- **The re-key ledger protocol is RETIRED.** `tools/rekey/check_rekey_ledger.py` does not exist. The
  green-tree battery is **seven** legs, not eight, and the app suite floor is **2239**, not 2242. Do
  not invoke the deleted checker; do not assert 2242.
- **`_TO_DICT_KEYS` is thirteen entries today, not twelve.** `181-PATTERNS.md` inherited the
  pre-`rail_reading_disclosure` count; the pin's own docstring already says thirteen. This plan takes
  it to **fourteen**.
- **`test_schema_version_is_pinned`'s docstring already names Phase 181 by number** and its failure
  message already says `RPT-E1 moves this to '2.0' in Phase 181`. Both sentences become past tense.
- **`tests/test_blast_radius_invariance.py` collects 100 tests at app base `04fd982`** (measured).
  Task 1 adds none, Task 2 adds one, Task 3 adds two.
</orchestrator_dispositions>

<flagged_assumptions>
The spec-less probe fallback is ACTIVE (no SPEC.md). The authoritative report is `coverage.json`:
**30 applicable edges over 18 requirements, all arriving `unresolved`.** Category tally re-counted from
the file itself: `unclassified` **12** (the orchestrator summary said 11 — the file is authoritative),
`empty` 6, `adjacency` 5, `ordering` 5, `encoding` 1, `concurrency` 1.

**This plan's six rows, resolved under the §A `--auto` rules.**

*Resolved (explicit) → plain `must_haves.truths` strings — 3:* `RPT-E3/adjacency` (equal pre-images
merge into one dedup group; an equal literal under two shape ids is the gh#39/gh#40 precedent, not a
collision defect), `RPT-E3/empty` (`synthetic-arm4-empty-results` is the registered empty-input shape,
literal `8d6208d00be7` unmoved), `RPT-E3/ordering` (`SHAPE_IDS` is `tuple(sorted(_BUILDERS))` and the
parametrization is `sorted(FROZEN_HASHES.items())`, so order is deterministic).

*Unresolved (`unclassified`) → flagged assumptions. Never auto-resolved with a backstop, never
dismissed — 3:*

- **FA-181-01 (`RPT-A4`).** RPT-A4 names a carry-through of an already-decided boolean, so it has no
  input domain the probe's taxonomy reaches. It is verified by the `_TO_DICT_KEYS` pin and the
  read-off-`self.plan` structural criterion below. **If the operator disagrees, the edge to author is
  what `is_uv` should read for a `Plan` carrying no steps** — the `not found in database` shape, which
  `dev test` refuses before a report exists.
- **FA-181-02 (`RPT-E1`).** A version-string bump has no boundary, adjacency, encoding or ordering
  edge: both consumers accept `schema_version` by PRESENCE only (a live fixture carries `9.9-future`)
  and the dedup hash never reads it, so there is no comparison semantic to pin.
- **FA-181-03 (`RPT-E2`).** Forward-only parsing is a set-membership claim over two committed fixture
  files, not an input-domain edge. The verified property — both parse, neither file changes — is
  authored as a truth above.

**No-silent-drop equality:** 6 rows in scope == 3 authored into `must_haves.truths` + 3 flagged. Holds.

**Prohibition recall (§B, run in-prompt — there is no compiled engine).** Stage 1 over-produced
roughly eighteen raw must-NOTs across the phase; Stage 2 dropped the routine-engineering ones (must
not break the suite, must not widen the diff, must not leave a stale line number, must not leave dead
code) and kept **10** integrity/transparency items, distributed across the phase's seven plans. Five
land here, authored descriptor-less in `must_haves.prohibitions`, never under `truths`.
**Canon-referral breadcrumb:** no OWASP, injection, path-traversal, prototype-pollution, GDPR or
generic-fairness candidate survived Stage 2 here — the one outward-facing artifact is the GitHub issue
body, whose `sanitize_dict` path is untouched by this plan and is covered in `<threat_model>` rather
than minted as a prohibition.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-CONTEXT.md
@/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md
</context>

<environment>
Three git repositories; **branch names differ between meta and the submodules** — do not assume they
match.

- Meta `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`, HEAD `2facbc8f`. Owns
  `.planning/` and `.claude/`.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, **phase base app
  HEAD `04fd982`**. Owns `firestarter/`, `tests/`, `tools/`, `pyproject.toml`. **Commit inside the
  submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — branch `gsd/v1.36-dev-test-fidelity`, HEAD
  `c14f191`. **Not touched by this phase.** Must stay porcelain-clean.

The trap: `git -C /workspaces/firestarter_app status --porcelain firestarter/` names the app's own
**package directory**, not the firmware submodule. Both legs appear below and they check different
things.

Python is `/workspaces/firestarter_app/.venv311/bin/python`. The devcontainer default is 3.12; app CI
runs 3.11 only, and that divergence has already broken beta CI once.

`pytest` `addopts` is `-ra -q`; a count line needs `-o addopts=""`. Doubling `-q` hides it.

Shell `grep` here is **ugrep and silently honours `.gitignore`** — every evidence-grade scan uses
`/usr/bin/grep`. `/usr/bin/grep -qF` with a dash-leading pattern exits 2 and the gate then fails OPEN;
use `-qFe` for any pattern starting with `-`.

`tests/test_skip_census.py`'s three failures are **pre-existing** and unrelated. No leg in this plan
runs the full suite.

**No push, no PR, no tag, no branch cut** in any task (D-22).
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and CONTEXT.md D-26 are
**non-overridable by this plan**, and `firestarter_app/tests/` counts as product source. Write **no
`#` comment at all**. Every rationale — the `is_uv` carry-through argument, the schema-2.0
justification, the RPT-E3 re-anchoring — goes into a **docstring**, which is how these modules already
carry their reasoning.

The files edited here are dense with pre-existing narrative comments (`diagnostic_report.py` carries
214 comment tokens). Those are **pre-existing — do not "fix" them** and **do not copy their style**.

Baselines measured at app `04fd982` by a `tokenize` COMMENT-token census. The gate is **non-increase**
for the two product-adjacent files so a deliberate deletion later in the phase cannot redden it, and
**exact equality at 0** for the pin module, which has never carried a comment:
`firestarter/diagnostic_report.py` <= 214, `tests/test_diagnostic_report.py` <= 183,
`tests/test_blast_radius_invariance.py` == 0.
</comment_discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — `plan.is_uv` reaches `to_dict()`, schema goes to 2.0, the key pin moves in the same commit, and all 19 frozen hashes stay byte-identical</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` at HEAD `04fd982` with a clean working tree, and `git -C /workspaces/firestarter status --porcelain` prints nothing.</precondition>
  <reversibility rating="reversible">An additive top-level key plus a version-string bump; both consumers accept `schema_version` by presence only and the dedup hash never reads it, so reverting is a two-line change with no filed-artifact consequence.</reversibility>
  <files>
    firestarter_app/firestarter/diagnostic_report.py,
    firestarter_app/tests/test_blast_radius_invariance.py,
    firestarter_app/tests/test_diagnostic_report.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-frozen-hash-invariance.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:45-56` — `SCHEMA_VERSION = "1.8"` at `:50`, and the `NOT_MEASURED` / `NOT_REPORTED` distinction directly below it. Do not invent a third sentinel.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:631-686` — the `DiagnosticReport` dataclass. `plan: Plan` is a REQUIRED field, never `None`, so `self.plan.is_uv` needs no guard. Read the `run_status` docstring at `:676-683` verbatim: it is the model for how to STATE a dedup exclusion in a docstring.
    - `/workspaces/firestarter_app/firestarter/diagnostic_report.py:886-908` — `to_dict()`. Thirteen keys today, in the written order. Hand-written, never `dataclasses.asdict()` (Pitfall 3); it calls `self._utc_now()` on every invocation, which is why plan `181-06`'s `elapsed` must be a stored field.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:495-515` — `Plan`, and specifically the `is_uv` docstring paragraph: decided EXACTLY ONCE by `derive_plan`, downstream may only READ it, nothing may re-derive UV-ness from the `algorithm == 0x0B` proxy (32 of 301 UV parts).
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:88-200` — all seven key-list pins with their counted-keys docstrings. `_TO_DICT_KEYS` at `:94`, alphabetically sorted. `_TRANSPORT_HEALTH_KEYS` carries a companion LENGTH pin at `:482` — check whether the list you move carries one too.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:268-320` — `test_dedup_fingerprint_is_frozen` and the two-commit review rule in its failure message at `:276-280`. That message is the surviving mechanism D-15 re-anchors RPT-E3 onto.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:417-521` — the seven pin assertion sites and `test_schema_version_is_pinned` at `:506-519`.
    - `/workspaces/firestarter_app/tests/fixtures/report_shapes.py:705-764` — `_BUILDERS` (19), `SHAPE_IDS` at `:727`, the 19 `FROZEN_HASHES` literals at `:729-750`, `RESERVED_SHAPE_IDS = frozenset()` at `:751`. **Read-only for this plan.**
    - `/workspaces/.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-PATTERNS.md` §"`diagnostic_report.py` — additive keys" — commit `ec1db5c` (`feat(178-01)`) is the master analog: one commit, one additive top-level key, one additive steps key, a schema bump, both pins moved, all 17 snapshots regenerated. Read the whole section, then read the commit.
  </read_first>
  <behavior>
    - `to_dict()["is_uv"]` is `True` for shape `m27c512-full-all-ok` (M27C512 is a UV-EPROM) and `False` for `at28c256-full-all-ok-sdp`.
    - `to_dict()["is_uv"]` equals `report.plan.is_uv` — read off the object, never recomputed from `electrical-type`, `algorithm` or any other proxy.
    - `to_dict()["schema_version"]` is the string `2.0`, sourced from the module constant, not a second literal.
    - `to_dict()` returns exactly fourteen top-level keys.
    - Each of the 19 shape ids still produces its existing `FROZEN_HASHES` literal after the key lands.
  </behavior>
  <action>
Change three product lines and their pins, in ONE commit inside `/workspaces/firestarter_app`.

In `firestarter/diagnostic_report.py`: set `SCHEMA_VERSION` to the string `2.0` (RPT-E1, D-13, and
REQUIREMENTS §"Decisions taken at definition" D-3). Leave the pre-existing trailing narrative on that
line alone.

Add one entry to `to_dict()`'s returned mapping: key `is_uv`, value `self.plan.is_uv`. Place it
immediately after `run_status` and before `rail_reading_disclosure`, so the diff is one inserted line.
Do NOT compute it, do NOT call `is_uv_eprom`, do NOT read `electrical-type` — RPT-A4's whole point is
that the value is read off the single `derive_plan` decision. `plan` is a required dataclass field, so
no `None` guard is needed.

Extend `to_dict()`'s docstring with one sentence naming `is_uv` as the carry-through of `Plan.is_uv`
(RPT-A4) and stating, in the voice of the `run_status` docstring, that it sits deliberately outside
`dedup_fingerprint`'s explicit five-entry allow-list, which is why it cannot re-key a filed report
(D-16). A docstring, never a comment — D-26.

In `tests/test_blast_radius_invariance.py`, same commit (D-14): add `"is_uv"` to `_TO_DICT_KEYS`,
keeping the list alphabetically sorted, and update that pin's counted-keys docstring from thirteen to
**fourteen**, naming Phase 181 plan 01 and RPT-A4 as the addition exactly as `ec1db5c` did for
`run_status`. If the list carries a companion length assertion, move that number too — a key edit that
misses its length twin leaves a self-contradicting pin.

Change `test_schema_version_is_pinned`'s triple-equality so both the constant and the baked value read
`2.0`, and rewrite its failure-message tail so it no longer promises a future move: it records that
Phase 181 plan 01 took it there and states the two facts that made the bump mechanically free (both
parsers accept `schema_version` by presence only; the dedup hash never reads it). Move its docstring
from the forward-looking sentence to the past-tense one.

In `tests/test_diagnostic_report.py`, same commit: change the `schema_version` literal assertion near
`:1489` from `1.8` to `2.0`. Locate it by content, not by line number — earlier edits shift the file.

Then regenerate the syrupy snapshots. `ec1db5c`'s commit message records that it regenerated all 17
frozen report snapshots for exactly this reason: an additive key moves the Amber snapshots even though
it cannot move the hashes. Run the snapshot suite with `--snapshot-update`, then re-run
`tools/snapshot_report_shapes.py --check` and the snapshot suite WITHOUT the update flag and confirm
both green. Review the snapshot diff before committing: **every changed snapshot must differ only by
the added `is_uv` line and the `schema_version` value.** A snapshot differing anywhere else means
something other than this task changed the shape — stop and report rather than commit.

Then write `.planning/phases/181-.../evidence/181-01-frozen-hash-invariance.txt` in the house
transcript form (numbered section headers, flat `key=value` scalars, verbatim pytest tail, an explicit
`rc=` line after each command). It must record `frozen_hash_literal_count=19`;
`frozen_hash_lines_added_or_removed_since_04fd982=0`; the `tests/test_blast_radius_invariance.py`
pytest tail; `schema_version_before=1.8`, `schema_version_after=2.0`; `to_dict_key_count_before=13`,
`to_dict_key_count_after=14`; and the number of snapshot files whose diff is confined to the `is_uv`
and `schema_version` lines. In its own numbered section, state explicitly that **RPT-E3's exception
clause discharges as empty** and that no ledger row was written because
`tools/rekey/check_rekey_ledger.py` no longer exists (D-15).

If the measured base count for `tests/test_blast_radius_invariance.py` differs from 100, record the
discrepancy in the SUMMARY and use the measured number rather than silently accepting a mismatch.

Commit inside `/workspaces/firestarter_app` on `gsd/v1.36-dev-test-fidelity`; commit the transcript in
`/workspaces` on `gsd/v1.36-dev-test-fidelity-planning`. Commit BEFORE running any leg asserting
whole-repo porcelain — `test_flash_path_record_sync` asserts it and would redden on your own
uncommitted work.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^100 passed'</automated>
    <fails_when>non-zero exit, or the summary reports any count other than exactly `100 passed` — a `failed` count means a key-list pin or the schema triple-equality did not move with the key; a collection `error` line with no count means the module did not import.</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(git diff 04fd982 -- tests/fixtures/report_shapes.py) && test -z "$(printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][[:space:]]*"[a-z0-9-]+": "[0-9a-f]{12}",')"</automated>
    <fails_when>non-zero exit — at least one twelve-hex frozen-hash literal line was added or removed in `tests/fixtures/report_shapes.py` since app base `04fd982`, which is a re-key and is exactly what D-16 forbids.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import ast,pathlib;src=pathlib.Path("firestarter/diagnostic_report.py").read_text(encoding="utf-8");assert len(src)>1000;t=ast.parse(src);fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=="to_dict"];assert len(fn)==1;d=[n for n in ast.walk(fn[0]) if isinstance(n,ast.Dict)][0];keys=[k.value for k in d.keys];print("to_dict_keys="+str(len(keys)));assert len(keys)==14,keys;assert "is_uv" in keys;v=d.values[keys.index("is_uv")];assert isinstance(v,ast.Attribute) and v.attr=="is_uv",ast.dump(v);assert isinstance(v.value,ast.Attribute) and v.value.attr=="plan",ast.dump(v);print("is_uv_source=self.plan.is_uv")'</automated>
    <fails_when>non-zero exit, or the printed `to_dict_keys=` value is not 14 — the chain fails when `to_dict()` does not carry exactly fourteen keys, when `is_uv` is absent, or when its value expression is anything other than the attribute chain `self.plan.is_uv` (a recomputation such as `is_uv_eprom(...)` or an `electrical-type` read trips the last two asserts).</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qE '^SCHEMA_VERSION = "2\.0"' firestarter/diagnostic_report.py && ! /usr/bin/grep -qF '"1.8"' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF '"2.0"' tests/test_blast_radius_invariance.py</automated>
    <fails_when>non-zero exit — the module constant is not the string `2.0`, or a `"1.8"` literal survives in the pin module, or no `"2.0"` literal reached it.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check</automated>
    <fails_when>non-zero exit, or the tool prints a drift line naming a shape whose committed snapshot no longer matches a fresh regeneration — the snapshots were not regenerated after the additive key landed.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c 'import io,tokenize;B={"firestarter/diagnostic_report.py":214,"tests/test_diagnostic_report.py":183};bad=[p for p,e in B.items() if sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT)>e];n0=sum(1 for t in tokenize.generate_tokens(io.StringIO(open("tests/test_blast_radius_invariance.py",encoding="utf-8").read()).readline) if t.type==tokenize.COMMENT);bad+=[] if n0==0 else ["tests/test_blast_radius_invariance.py:"+str(n0)];print("phase_added_comments="+("0" if not bad else ",".join(bad)));raise SystemExit(1 if bad else 0)'</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file is where a `#` comment was added, including a trailing one a line-anchored regex cannot see.</fails_when>
  </verify>
  <acceptance_criteria>
    - `firestarter/diagnostic_report.py` contains the line `SCHEMA_VERSION = "2.0"` at module scope.
    - `to_dict()` returns exactly fourteen keys and `is_uv` is one of them, proven by the AST leg above.
    - The value expression for `is_uv` is the attribute chain `self.plan.is_uv`; no call node appears in it.
    - `_TO_DICT_KEYS` in `tests/test_blast_radius_invariance.py` contains `"is_uv"` and its docstring contains the word `fourteen`.
    - `tests/test_blast_radius_invariance.py` contains no `"1.8"` literal and does contain `"2.0"`.
    - `./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q` prints `100 passed`.
    - `git diff 04fd982 -- tests/fixtures/report_shapes.py` contains zero added or removed twelve-hex frozen-hash literal lines.
    - `./.venv311/bin/python tools/snapshot_report_shapes.py --check` exits 0.
    - `evidence/181-01-frozen-hash-invariance.txt` contains `frozen_hash_literal_count=19`, `frozen_hash_lines_added_or_removed_since_04fd982=0`, `to_dict_key_count_after=14` and a section stating RPT-E3's exception clause discharges as empty.
    - `tokenize` COMMENT-token counts: `firestarter/diagnostic_report.py` at most 214, `tests/test_diagnostic_report.py` at most 183, `tests/test_blast_radius_invariance.py` exactly 0.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing, and `git -C /workspaces/firestarter_app status --porcelain firestarter/` prints nothing.
  </acceptance_criteria>
  <done>The phase's headline claim is a running gate rather than a projection: an additive top-level key and a major schema bump landed end to end, from the single `derive_plan` decision through the serializer to the moved key-list pin, and all 19 frozen dedup hashes are provably byte-identical to the phase base.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RPT-E2 — the frozen schema-1.2 and 1.4 fixtures still parse under 2.0, asserted by test, with neither fixture file touched</name>
  <files>
    firestarter_app/tests/test_blast_radius_invariance.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-forward-only-parse.txt
  </files>
  <read_first>
    - `/workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md` — the frozen schema-1.2 fixture. Its first line is a FROZEN FIXTURE header that forbids regeneration; `schema_version` is at `:14`, `locked_steps` at `:80`, `vpp_mv` at `:87`, `vpe_mv` at `:88`.
    - `/workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md` — the frozen schema-1.4 sibling, same key positions.
    - `/workspaces/firestarter_app/tools/parse_devtest_issue.py:59-120` — `_DEV_TEST_MARKER`, `_FENCE`, `_MAX_BODY_BYTES` and `_extract_fenced_report`. The crucial property: `schema_version` is accepted by PRESENCE only (`if "schema_version" not in obj`), never by an ordering or range comparison, and a live fixture carries `9.9-future`.
    - `/workspaces/firestarter_app/tests/test_parse_devtest_issue.py` — the existing parse-test conventions and how a fixture body is loaded (29 tests today). Reuse its loading idiom rather than writing a second one.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:1-90` — the module docstring and imports, so the new test is added without a new dependency.
  </read_first>
  <behavior>
    - `_extract_fenced_report` over the schema-1.2 fixture body returns a dict whose `schema_version` is `1.2` and which still carries `banner.locked_steps`, `voltage.vpp_mv` and `voltage.vpe_mv`.
    - The same holds for the schema-1.4 fixture with `schema_version` `1.4`.
    - Neither call raises, and neither depends on the current value of `SCHEMA_VERSION`.
    - A body whose fenced JSON omits `schema_version` entirely returns `None` — the presence-only rule is a real gate, not a no-op.
  </behavior>
  <action>
Add exactly one module-level test function to `firestarter_app/tests/test_blast_radius_invariance.py`,
named `test_frozen_pre_2_0_fixtures_still_parse_forward_only`, placed immediately after
`test_schema_version_is_pinned` so the schema story reads in one place.

It resolves both frozen fixture bodies from the meta tree at
`/workspaces/.claude/skills/devtest-triage/fixtures/`, guarding the resolution: assert each body's
length exceeds 1000 characters before parsing, so a mis-resolved path cannot pass vacuously. Because
the app suite must not hard-depend on the meta tree — that exact reach-out with no skip guard is what
sent app CI to `13 failed, 13 passed` on a bare checkout and condemned the retired ledger — wrap the
resolution in a `pytest.skip` when the directory is absent, and state that reason in the test's
docstring.

For each fixture it asserts, using `tools.parse_devtest_issue._extract_fenced_report`: the returned
object is a dict; its `schema_version` is the fixture's own historical value (`1.2` and `1.4`
respectively) and NOT the current `SCHEMA_VERSION`; and the three keys this phase deletes downstream
are still present in the parsed object (`banner.locked_steps`, `voltage.vpp_mv`, `voltage.vpe_mv`) —
that presence IS the forward-only property RPT-E2 asks for, so assert it explicitly rather than
implying it.

Add a third assertion in the same test as the non-vacuity control: a synthesized body whose fenced
JSON omits `schema_version` returns `None`. Without it, a parser that returned a dict for anything at
all would satisfy the two positive legs.

The docstring states RPT-E2's claim in one sentence — deletions are forward-only: a report written
under an older schema keeps parsing after the deletion, because both parsers accept `schema_version`
by presence only — and states the ceiling: this pins the PARSE, not the rendering, and it does not
claim the triage skill's prose still reads correctly for a pre-2.0 body.

**Do not modify either fixture file.** Their own headers forbid regeneration and they are RPT-E2's
parse targets; a regenerated fixture would silently delete the very keys the test exists to prove
survive.

Then write `evidence/181-01-forward-only-parse.txt` recording, as flat scalars:
`fixture_1_2_schema_version=1.2`, `fixture_1_4_schema_version=1.4`,
`locked_steps_present_in_parsed_1_2=true`, `vpp_mv_present_in_parsed_1_2=true`,
`vpe_mv_present_in_parsed_1_2=true`, `missing_schema_version_returns_none=true`,
`fixture_files_modified=0`, plus the pytest tail showing `101 passed`.

Commit the test inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^101 passed'</automated>
    <fails_when>non-zero exit, or any count other than `101 passed` — `100 passed` means the new test was not collected, and a `skipped` count means the meta-tree fixture directory did not resolve, which is a real gap rather than a pass.</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qF 'def test_frozen_pre_2_0_fixtures_still_parse_forward_only(' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'locked_steps' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'vpp_mv' tests/test_blast_radius_invariance.py</automated>
    <fails_when>non-zero exit — the named test function is absent, or it does not assert on the two deleted-downstream keys by name, which would leave the forward-only property implied rather than proven.</fails_when>
    <automated>cd /workspaces && S=$(git status --porcelain .claude/skills/devtest-triage/fixtures/) && test -z "$S"</automated>
    <fails_when>non-zero exit — a byte of either frozen fixture changed, which their own FROZEN FIXTURE headers forbid and which would delete the keys this test exists to prove survive.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-forward-only-parse.txt; test -s "$E" && /usr/bin/grep -qF 'fixture_1_2_schema_version=1.2' "$E" && /usr/bin/grep -qF 'fixture_1_4_schema_version=1.4' "$E" && /usr/bin/grep -qF 'missing_schema_version_returns_none=true' "$E" && /usr/bin/grep -qF 'fixture_files_modified=0' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing, empty, or does not record all four scalars, so the forward-only result and its non-vacuity control were never written down.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_blast_radius_invariance.py` contains `def test_frozen_pre_2_0_fixtures_still_parse_forward_only(`.
    - That test asserts the parsed `schema_version` equals `1.2` for one fixture and `1.4` for the other, and asserts `locked_steps`, `vpp_mv` and `vpe_mv` are present in the parsed object.
    - That test contains a control asserting a body with no `schema_version` returns `None`.
    - That test guards fixture resolution with a length assertion above 1000 characters and a `pytest.skip` when the meta-tree directory is absent.
    - `./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q` prints `101 passed`.
    - `git status --porcelain .claude/skills/devtest-triage/fixtures/` prints nothing.
    - `evidence/181-01-forward-only-parse.txt` records the four named scalars.
  </acceptance_criteria>
  <done>RPT-E2 is proven rather than asserted: two reports written under schema 1.2 and 1.4 — carrying all three keys this phase deletes — still parse under 2.0, and the presence-only rule that makes that true is shown to be a real gate by its own negative control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Anti-vacuity — the key-list pin observed RED against a planted mutation, and RPT-E3's re-anchoring written into the oracle it now points at</name>
  <files>
    firestarter_app/tests/test_blast_radius_invariance.py,
    .planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-key-pin-planted-red.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:523-575` — `test_to_dict_key_list_pins_are_sensitive_to_added_and_removed_keys`, which mutates a real `to_dict()` MAPPING in process, never production. This is the form to extend; the `test_readback_inventory.py` source-string form belongs to plan `181-05`'s census instead.
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py:57-148` — the three load-bearing details of the anti-vacuity idiom regardless of which form is used: assert the anchor occurs exactly once BEFORE mutating; assert the resolved subject is real (a length guard); and carry a separate, explicitly-named vacuity test rather than folding it into the positive one.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:268-283` — `test_dedup_fingerprint_is_frozen` and its failure message, the exact prose D-15 re-anchors RPT-E3 onto. Quote it, do not paraphrase it.
    - `/workspaces/firestarter_app/tests/test_blast_radius_invariance.py:743-772` — `test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree`, the pre-existing completeness pin proving `set(SHAPE_IDS) == set(FROZEN_HASHES)`. **Do not duplicate it** — cite it from the new docstrings instead.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the closing form and the "which gate would redden" sentence pattern, which the RPT-E3 statement mirrors in miniature.
  </read_first>
  <behavior>
    - Adding one key to a real `to_dict()` mapping moves it away from `_TO_DICT_KEYS`, and the pin's own equality claim raises `AssertionError`.
    - Removing one key from that mapping does the same, in the opposite direction.
    - An empty expected key list fails rather than passing vacuously.
    - The pin asserts sorted-list EQUALITY, never subset or membership.
  </behavior>
  <action>
Add exactly two module-level test functions to `firestarter_app/tests/test_blast_radius_invariance.py`.

`test_the_to_dict_key_pin_reddens_on_a_planted_added_and_removed_key`: build a real report via
`build_shape("sst27sf512-six-step")`, take `to_dict()`, assert the mapping carries exactly fourteen
keys and that `is_uv` occurs exactly once in the key list (the anchor-uniqueness step, done before any
mutation). Then, on two in-memory copies, add a synthetic key and delete `is_uv`, and assert in each
case that `sorted(mutated)` differs from `_TO_DICT_KEYS` and that the pin's own equality assertion
raises `AssertionError`. Mutate the mapping only — never production, never a file on disk.

`test_the_to_dict_key_pin_is_not_vacuous_against_an_empty_expected_list`: assert that comparing a real
`to_dict()` key list against an empty expected list raises `AssertionError`, as the separate,
explicitly-named vacuity leg. Do not fold this into the test above.

Both docstrings must state the claim and the ceiling, and must record the fact `181-PATTERNS.md`
measured and no precedent covers: **`_VOLTAGE_KEYS` and `_BANNER_KEYS` have never been SHRUNK.**
`git log -S'_VOLTAGE_KEYS'` returns only the commit that created them, so plan `181-05` and plan
`181-02` perform the first deletions ever to pass through these pins, and `ec1db5c` is an additive
precedent only. Say so plainly rather than citing a precedent that does not exist.

Then extend the module docstring of `tests/test_blast_radius_invariance.py` with a short paragraph
carrying **RPT-E3's re-anchoring (D-15)**, stated as: the GATE-06 `RK-174-` ledger table, its app-side
fixture, its checker and its CI workflow were retired on 2026-09-08, so RPT-E3's phrase "the re-keys
declared under GATE-06" has no table to point at; RPT-E3's anchor is now the 19 absolute
`FROZEN_HASHES` literals in `tests/fixtures/report_shapes.py` plus the two-commit review rule carried
verbatim in `test_dedup_fingerprint_is_frozen`'s failure message; and Phase 181 declares **zero**
re-keys, so RPT-E3's exception clause discharges as empty and that rule never fires in this phase.
Quote the failure message's own words for the two-commit rule. This is a docstring, not a comment.

Then write `evidence/181-01-key-pin-planted-red.txt` recording `anchor_count=1`,
`pin_at_head=GREEN`, `pin_at_planted_added_key=RED`, `pin_at_planted_removed_key=RED`,
`empty_expected_list=RED`, `fixture_files_written=0`, and the pytest tail showing `103 passed`.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`. Then run the
final cleanliness legs.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^103 passed'</automated>
    <fails_when>non-zero exit, or any count other than `103 passed` — `101 passed` means neither new test was collected, `102 passed` means one is missing, and any `failed` count means a planted mutation did not redden the pin, which would mean the pin is vacuous.</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qF 'def test_the_to_dict_key_pin_reddens_on_a_planted_added_and_removed_key(' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'def test_the_to_dict_key_pin_is_not_vacuous_against_an_empty_expected_list(' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'FROZEN_HASHES' tests/test_blast_radius_invariance.py && /usr/bin/grep -qF 'retired' tests/test_blast_radius_invariance.py</automated>
    <fails_when>non-zero exit — one of the two named tests is absent, or the module docstring does not carry the RPT-E3 re-anchoring paragraph (the `retired` literal is its marker).</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -rqF 'check_rekey_ledger' tests/ firestarter/ tools/ && ! /usr/bin/grep -rqF '2242' tests/test_blast_radius_invariance.py</automated>
    <fails_when>non-zero exit — a reference to the deleted `check_rekey_ledger` checker or to the superseded `2242` suite floor was introduced; the checker no longer exists and the measured floor is 2239.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/evidence/181-01-key-pin-planted-red.txt; test -s "$E" && /usr/bin/grep -qF 'anchor_count=1' "$E" && /usr/bin/grep -qF 'pin_at_head=GREEN' "$E" && /usr/bin/grep -qF 'pin_at_planted_added_key=RED' "$E" && /usr/bin/grep -qF 'pin_at_planted_removed_key=RED' "$E" && /usr/bin/grep -qF 'fixture_files_written=0' "$E"</automated>
    <fails_when>non-zero exit — the RED-proof transcript is missing, empty, or does not record all five scalars, which means the pin was never observed red against a planted counter-example.</fails_when>
    <automated>A=$(git -C /workspaces/firestarter status --porcelain) && B=$(git -C /workspaces/firestarter_app status --porcelain firestarter/) && C=$(git -C /workspaces/firestarter_app status --porcelain) && test -z "$A" && test -z "$B" && test -z "$C"</automated>
    <fails_when>non-zero exit — the firmware submodule is dirty, or the app package directory is dirty, or the app repo has uncommitted work; the third leg must run AFTER the commit or it reddens on this task's own edits.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both named test functions exist in `tests/test_blast_radius_invariance.py`.
    - The added-key and removed-key mutations each produce an `AssertionError` from the pin's own equality claim, and the empty-expected-list leg does too.
    - Anchor uniqueness is asserted before any mutation, and no fixture file is written by either test.
    - The module docstring states that the GATE-06 ledger is retired, names the 19 `FROZEN_HASHES` literals plus the two-commit review rule as RPT-E3's new anchor, and states Phase 181 declares zero re-keys.
    - The module docstring records that `_VOLTAGE_KEYS` and `_BANNER_KEYS` have never been shrunk before and that `ec1db5c` is an additive precedent only.
    - No file under `tests/`, `firestarter/` or `tools/` references `check_rekey_ledger`.
    - `./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q` prints `103 passed`.
    - `evidence/181-01-key-pin-planted-red.txt` records the five named scalars.
    - All three porcelain legs print nothing, run after this task's commits.
  </acceptance_criteria>
  <done>The pin that will govern every deletion in this phase has been seen red against both directions of key drift and against a vacuous expected list, and RPT-E3 now points at a mechanism that exists — the 19 absolute literals and the two-commit review rule — rather than at a retired ledger table.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This section is the drift verifier's manifest for plan `181-01`. Symbols this plan CREATES and
DELETES; the phase-wide manifest is repeated in each plan for its own scope.

**CREATED by this plan**
- `to_dict()` top-level key `is_uv` (boolean, `firestarter/diagnostic_report.py`)
- `_TO_DICT_KEYS` entry `"is_uv"` (`tests/test_blast_radius_invariance.py`)
- `test_frozen_pre_2_0_fixtures_still_parse_forward_only`
- `test_the_to_dict_key_pin_reddens_on_a_planted_added_and_removed_key`
- `test_the_to_dict_key_pin_is_not_vacuous_against_an_empty_expected_list`
- `.planning/phases/181-.../evidence/181-01-frozen-hash-invariance.txt`
- `.planning/phases/181-.../evidence/181-01-forward-only-parse.txt`
- `.planning/phases/181-.../evidence/181-01-key-pin-planted-red.txt`

**CHANGED IN VALUE (not created, not deleted)**
- `SCHEMA_VERSION`: `"1.8"` becomes `"2.0"`
- the `schema_version` literals in `tests/test_blast_radius_invariance.py` and
  `tests/test_diagnostic_report.py`
- the syrupy Amber snapshots for all registered shapes

**DELETED by this plan:** nothing. Every deletion in this phase belongs to `181-02` or later.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated proportionately: this is a local-only host CLI change with no
network surface, no authentication, no session, no credential and no user input beyond `argv` and a
serial device the operator physically owns. This plan adds one boolean report key and a version string.
The one genuinely outward-facing artifact in the system is the **GitHub issue body** the submit path
files, and this plan does not change what reaches it beyond adding `is_uv` and moving
`schema_version`; `sanitize_dict` is untouched.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator `argv` and the chip in the socket to the report object | The only untrusted content reaching a report is the operator's own chip and their own typed token. |
| report object to the filed GitHub issue body | The one outward crossing. `sanitize_dict` scrubs it; this plan neither widens nor bypasses that path. |
| meta repo `.planning/`/`.claude/` to the `firestarter_app` submodule | Task 2's parse leg reads two meta-tree fixture files from an app-side test. Guarded by a `pytest.skip` — an unguarded reach-out is the measured defect that sent app CI to `13 failed, 13 passed` and condemned the retired ledger. |
| first-party source to `ast.parse` in a test | Test-time only; the parsed text is never executed and no file is written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-181-01 | Tampering | the 19 `FROZEN_HASHES` literals | high | mitigate | A silent re-key is permanent: `count_agreeing` reads the hash embedded in a filed issue body and never re-hashes. Mitigated by the git-diff literal-absence leg against app base `04fd982` plus the 19-way parametrized frozen test, both run in Task 1 and re-run at the phase seal. |
| T-181-02 | Tampering | the two frozen `devtest-triage` fixtures | medium | mitigate | Task 2's porcelain leg over `.claude/skills/devtest-triage/fixtures/` asserts zero bytes changed; their own headers forbid regeneration. |
| T-181-03 | Repudiation | the three `evidence/*.txt` transcripts | low | mitigate | Every claim is a flat `key=value` scalar plus a verbatim command tail and an explicit `rc=`, so a reader re-runs and diffs rather than trusting. |
| T-181-04 | Tampering | the firmware submodule and the generated `chip_database.json` | low | mitigate | Task 3's cleanliness legs assert both byte-unchanged; the same legs re-run at the phase seal. `chip_database.json` is generated and is never hand-edited. |
| T-181-05 | Information Disclosure | `is_uv` in the filed issue body | low | accept | `is_uv` is a property of the chip TYPE, already derivable from the part number any filed issue names in its title. It discloses nothing about the operator or the host. |
| T-181-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package. Every module used — `ast`, `io`, `tokenize`, `pathlib`, `json` — is Python standard library, so no Package Legitimacy Audit is required and no install checkpoint is inserted. |

One threat is rated `high` (T-181-01) and it is dispositioned **mitigate** with two independent
runnable gates, so nothing here blocks.
</threat_model>

<verification>
1. `tests/test_blast_radius_invariance.py` reports `103 passed` after Task 3 (100 at base, plus one in
   Task 2 and two in Task 3).
2. `git diff 04fd982 -- tests/fixtures/report_shapes.py` shows zero added or removed twelve-hex
   frozen-hash literal lines — the D-16 headline leg.
3. `to_dict()` carries exactly fourteen keys; `is_uv`'s value expression is the attribute chain
   `self.plan.is_uv`, proven by AST.
4. `SCHEMA_VERSION` is `"2.0"`; no `"1.8"` literal survives in the pin module.
5. `tools/snapshot_report_shapes.py --check` exits 0.
6. Both frozen `devtest-triage` fixtures parse under 2.0 and neither file changed.
7. The key-list pin was observed RED in both drift directions and against an empty expected list.
8. `tokenize` COMMENT-token counts at or below 214 / 183, and exactly 0 for the pin module.
9. All three porcelain legs print nothing.

**Do not** run the seven-leg green-tree battery here, do not invoke `check_rekey_ledger.py`, and do
not assert a suite floor of 2241 or 2242 anywhere. The checker was deleted, the battery is seven legs,
and the measured floor is 2239. The battery runs once, in plan `181-07`.
</verification>

<success_criteria>
- RPT-A4 is discharged: `plan.is_uv` reaches `to_dict()` as a top-level boolean read off the single
  `derive_plan` decision, with the key-list pin moved in the same commit.
- RPT-E1 is discharged: `schema_version` reads `2.0` and every literal that named `1.8` moved with it.
- RPT-E2 is discharged: both frozen pre-2.0 fixtures still parse, proven by test, with a non-vacuity
  control and neither fixture modified.
- RPT-E3 is discharged: the requirement is re-anchored in writing onto the 19 absolute literals and
  the surviving two-commit review rule, and its exception clause is stated to discharge as empty.
- D-16's headline claim holds and is measurable: 19 frozen hashes, zero moved literal lines.
- Zero deletions, zero firmware lines, zero `#` comments added.
</success_criteria>

<output>
Create `.planning/phases/181-report-fidelity-schema-2-0-canonical-naming-hygiene-close/181-01-SUMMARY.md`
when done. Record in it: the measured pytest counts, the measured base count for
`tests/test_blast_radius_invariance.py` if it differed from 100, the three RED observations, the count
of regenerated snapshots and confirmation their diffs were confined to `is_uv` and `schema_version`,
any line number that had moved since this plan was written, and — per `/workspaces/CLAUDE.md` — every
rationale that would otherwise have been a source comment.
</output>
