---
phase: 176-transport-instrumentation-connect-cost-measurement-partially
plan: 03
type: execute
wave: 3
depends_on: ["176-02"]
files_modified:
  - firestarter_app/firestarter/transport_counters.py
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/firestarter/diagnostic_report.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/test_serial_comm.py
  - firestarter_app/tests/test_transport_counters.py
  - firestarter_app/tests/test_diagnostic_report.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/fixtures/reports/
autonomous: false
requirements: [RPT-C1, RPT-C2]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A magic preamble arriving with no length bytes behind it raises `resync_length_missing` by exactly one and moves no other counter; a declared frame length longer than the bytes available raises `resync_body_truncated` by exactly one and moves no other counter."
    - "A single `get_response` call whose stream first re-syncs on a truncated frame and then runs out of time raises BOTH `resync_body_truncated` and `timeouts` by exactly one each -- two distinct events on one call are neither merged into one counter nor allowed to suppress each other."
    - "`test_read_and_parse_lines_ringfence_unchanged` is GREEN after the change, against a NEW pinned digest, and its docstring records the Phase 176 re-pin in the same shape the Phase 65-01 re-pin is recorded in: what changed, and the argument that it is not a transport-path change."
    - "The re-pin argument is recorded and checkable: a counter increment placed beside an existing `logger.warning` in a re-sync branch adds no read, no branch on wire bytes and no `start_time` write, so it changes none of the four things the fence header names -- the byte-by-byte read loop, the magic-preamble dispatch, the frame-length read, or the timeout reset semantics."
    - "All four instrumentation sites RPT-C1 names now increment a real counter reachable by the report, and `transport_health` carries nine keys."
    - "The Phase 174 oracle is green before and after at `114 passed`, with `_TRANSPORT_HEALTH_KEYS` at nine keys and all 16 snapshots regenerated in the SAME commit as the production change."
  artifacts:
    - path: "firestarter_app/tests/test_serial_comm.py"
      provides: "The re-pinned ring-fence digest with the Phase 176 justification recorded in the test docstring"
      contains: "_PINNED_SHA256"
    - path: "firestarter_app/tests/test_transport_counters.py"
      provides: "The two re-sync legs and the two-counters-on-one-call adjacency leg"
      contains: "resync_body_truncated"
  key_links:
    - from: "firestarter_app/firestarter/serial_comm.py"
      to: "firestarter_app/firestarter/transport_counters.py"
      via: "both re-sync branches increment beside their existing logger.warning, before the continue"
      pattern: "record_resync_(length_missing|body_truncated)\\("
    - from: "firestarter_app/tests/test_serial_comm.py"
      to: "firestarter_app/firestarter/serial_comm.py"
      via: "the SHA-256 of inspect.getsource(_read_and_parse_lines) against the re-pinned digest"
      pattern: "_PINNED_SHA256"
---

<objective>
Instrument the two re-sync events that sit INSIDE the v1.9 ring fence, and re-pin the fence's
SHA-256 deliberately, in the same commit, with the reasoning recorded in the test docstring exactly
the way the Phase 65-01 re-pin already is.

This is the phase's one real structural obstacle. Both re-sync sites live inside
`_read_and_parse_lines`, which carries a `DO NOT MODIFY -- v1.9 RCA territory` header AND a live
SHA-256 source pin at `tests/test_serial_comm.py:430-460`. Any character added to that function
turns that test red.

The operator has ruled: RESEARCH section 7 Option 1 -- instrument the sites and re-pin the digest
deliberately, gated behind a blocking human checkpoint. Option 2 (counting via a `logging.Handler`
keyed on the two warning strings) is the recorded FALLBACK if the checkpoint is declined; it is
honest but it makes two prose strings load-bearing and trades a checkable pin for an uncheckable
prose dependency. Option 3 (dropping Sites A and B and closing the phase with two of RPT-C1's four
named sites unmet) was rejected.

Purpose: close RPT-C1 completely, without spending an operator-era safety marker silently.

Output: all four RPT-C1 sites wired, a re-pinned and green ring-fence test whose docstring carries
the argument, and `transport_health` at its final nine keys.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-RESEARCH.md
@.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-PATTERNS.md
@.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-02-SUMMARY.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
</context>

## Artifacts this phase produces

Identical to the list in `176-01-PLAN.md` under the same heading. The symbols this plan is the first
to create are `record_resync_length_missing()`, `record_resync_body_truncated()`, and the
`resync_length_missing` and `resync_body_truncated` counters and `TransportHealth` fields.

## Flagged planner assumptions

- **PA-9 (the fence's scope, RESEARCH assumption A4).** That a counter increment beside a
  `logger.warning` in a re-sync branch does not change "the byte-by-byte read loop, the
  magic-preamble dispatch, the frame-length read, or the timeout reset semantics" is a reading of
  the fence's own wording, not an operator-era ruling. It is the phase's key judgement call, which
  is exactly why the checkpoint below exists and why it carries `gate="blocking-human"`.
- **PA-10 (fence-header prose untouched).** `inspect.getsource` covers the `def` line, the docstring
  and the body but NOT the block comment above the `def`, so the fence header is not part of the
  digest. This plan does not edit that header -- it is a comment, and the standing project rule
  forbids writing comments into source. The re-pin's reasoning goes in the TEST docstring, which is
  where the Phase 65-01 precedent put it.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: approve the deliberate v1.9 ring-fence re-pin before it lands</name>
  <read_first>
    - `firestarter_app/firestarter/serial_comm.py` lines 407-420 -- the fence header and the four
      invariants it names, which are what the argument turns on
    - `firestarter_app/tests/test_serial_comm.py` lines 428-462 -- the pin, the Phase 65-01 re-pin
      paragraph already recorded there, and the `GATE-1.8d VIOLATION` message
    - `176-RESEARCH.md` section 7 in full -- the three options, the deferral-target status, and the
      recommendation
    - `.planning/ROADMAP.md` line 15 -- v1.9's paused status, one of the two facts weighing against
  </read_first>
  <what-built>
    Nothing has been changed yet. This checkpoint runs BEFORE the ring-fence edit, because the edit
    is the thing being approved.

    Plans `176-01` and `176-02` have already landed three of RPT-C1's four instrumentation sites --
    `decode_failures`, `timeouts` and `probe_timeouts` -- all at seams OUTSIDE the fence. The two
    remaining sites are the re-sync branches at `serial_comm.py:485-490` and `:500-505`, both inside
    `_read_and_parse_lines`.

    What the next task will do if you approve:

    1. Add one counter increment to each re-sync branch, beside the existing `logger.warning` and
       before the existing `continue`. No read, no branch on wire bytes, no `start_time` write.
    2. Recompute the SHA-256 of `inspect.getsource(SerialCommunicator._read_and_parse_lines)` and
       replace `_PINNED_SHA256` in `tests/test_serial_comm.py` with the new value, in the SAME
       commit.
    3. Record the reasoning in that test's docstring, in the same shape the Phase 65-01 re-pin is
       already recorded in there: the previous digest, the phase, what changed, and why it is not a
       transport-path change.
  </what-built>
  <how-to-verify>
    This is a judgement call about a deliberate safety marker, not a visual check. What to weigh:

    THE ARGUMENT FOR. The fence header names four things a change must not touch: the byte-by-byte
    read loop, the magic-preamble dispatch, the frame-length read, and the timeout reset semantics.
    A counter increment in a re-sync branch touches none of them -- it adds no `read()` call, no
    branch on a wire byte, and no write to `start_time`. A re-pin precedent already exists and is
    recorded in that same docstring: Phase 65-01 re-pinned it for adding `id` plumbing to the
    `Response` construction, on the reasoning that it was "not a transport-path change".

    THE ARGUMENT FOR CAUTION. v1.9 Read-Bug RCA is still formally PAUSED (since 2026-06-01, at
    Phase 44) and the Phase 26 baseline binaries the fence protects still exist on disk. Against
    that: v1.10 already exonerated the transport and re-baselined against different chips -- neither
    of which was the original GATE-1.8d baseline.

    IF YOU DECLINE. The recorded fallback is RESEARCH section 7 Option 2: attach a counting
    `logging.Handler` to the `SerialComm` logger and key it on the two warning messages. It needs no
    body change and no re-pin, but Site A's message is a constant string while Site B's is an
    f-string, so only a prefix match works; it silently mis-counts if anyone rewords a warning, and
    it couples the counters to logging configuration. Say "fallback" and plan `176-03` will be
    re-planned onto that route rather than proceeding.

    This checkpoint carries `gate="blocking-human"`, so `--auto` and `--chain` cannot approve it.
    That is deliberate and is stated here so a future autonomous run cannot claim it was not warned:
    auto-mode auto-approves ordinary human-verify gates, and `autonomous: false` alone is not
    self-protecting.
  </how-to-verify>
  <resume-signal>Type "approved" to re-pin deliberately, "fallback" to take the logging-handler route instead, or describe a different constraint</resume-signal>
  <acceptance_criteria>
    - A human has answered `approved`, `fallback`, or stated a different constraint, and the answer
      is transcribed verbatim into `176-03-SUMMARY.md`.
    - On `approved`, Task 1 proceeds and the SUMMARY records both the previous and the new digest.
    - On `fallback`, `_read_and_parse_lines` is NOT edited, no digest is re-pinned, and execution
      STOPS with a request to re-plan `176-03` onto the logging-handler route.
    - No auto-approval occurred: the gate is `blocking-human`, so an `--auto` or `--chain` run that
      reached this task must have halted rather than answered it.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Instrument both re-sync branches and re-pin the ring-fence digest in the same commit</name>
  <precondition>The ring-fence checkpoint above was answered "approved" by a human; if it was answered "fallback", stop and request re-planning rather than editing `_read_and_parse_lines`.</precondition>
  <reversibility rating="costly">Re-pinning a deliberate operator-era safety marker; the undo is a one-commit revert of the two increments and the digest together, but the PRECEDENT that this class of change is in scope for the fence propagates to later phases, which is why the argument is recorded in the docstring rather than only in a SUMMARY.</reversibility>
  <read_first>
    - `firestarter_app/firestarter/serial_comm.py` lines 407-420 -- the fence header and its four
      named invariants, read in full before touching anything below it
    - `firestarter_app/firestarter/serial_comm.py` lines 480-510 -- both re-sync branches verbatim
    - `firestarter_app/tests/test_serial_comm.py` lines 428-462 -- the ring-fence test in full: the
      Phase 65-01 docstring paragraph that is the literal template for the Phase 176 paragraph, the
      `_PINNED_SHA256` assignment, and the `GATE-1.8d VIOLATION` assertion message to leave intact
    - `firestarter_app/firestarter/transport_counters.py` -- the sink as plans `176-01` and `176-02`
      left it
    - `176-RESEARCH.md` section 7 in full, and Pitfall 1; `176-PATTERNS.md` section
      `tests/test_serial_comm.py` Analog A
  </read_first>
  <behavior>
    - Test 1: feeding only a magic preamble and nothing more, then driving
      `list(comm._read_and_parse_lines(0.05))` to exhaustion, raises `resync_length_missing` by
      exactly one and leaves every other counter unchanged.
    - Test 2: feeding a magic preamble plus a declared length of 8 followed by only two body bytes,
      then driving the generator to exhaustion, raises `resync_body_truncated` by exactly one and
      leaves every other counter unchanged.
    - Test 3 (the adjacency case): the Test 2 byte sequence driven through
      `comm.get_response(timeout=0.05)` instead raises `SerialTimeoutError` AND raises BOTH
      `resync_body_truncated` and `timeouts` by exactly one each. Two distinct events on one call
      are neither merged nor suppressed.
    - Test 4: the ring-fence test is green against the new digest, and recomputing the digest from
      `inspect.getsource` matches the value written into `_PINNED_SHA256`.
  </behavior>
  <files>firestarter_app/firestarter/transport_counters.py, firestarter_app/firestarter/serial_comm.py, firestarter_app/firestarter/diagnostic_report.py, firestarter_app/firestarter/cli_handlers.py, firestarter_app/tests/test_serial_comm.py, firestarter_app/tests/test_transport_counters.py, firestarter_app/tests/test_diagnostic_report.py, firestarter_app/tests/test_blast_radius_invariance.py, firestarter_app/tests/fixtures/reports/</files>
  <action>
    In `transport_counters.py` add two counters, `resync_length_missing` and
    `resync_body_truncated`, with `record_resync_length_missing() -> None` and
    `record_resync_body_truncated() -> None`, and extend `reset()` and `snapshot()` to cover all
    five wired counters. These are unconditional increments -- they are not routed by
    `probe_scope()`, because a re-sync is a wire-level event whose meaning does not depend on
    whether a probe is in progress. State that distinction in the module docstring.

    In `serial_comm.py`, add exactly one line to each re-sync branch inside
    `_read_and_parse_lines`: `transport_counters.record_resync_length_missing()` in the branch that
    fires when fewer than two length bytes arrived, and
    `transport_counters.record_resync_body_truncated()` in the branch that fires when the body
    length does not equal the declared frame length. Each goes between the existing
    `logger.warning(...)` call and the existing `continue`. Change nothing else in the generator:
    no new `read()`, no new branch on a wire byte, no assignment to `start_time`, no reordering.
    Those four are precisely what the fence header protects, and the re-pin argument depends on all
    four still being true afterwards.

    Then re-pin the fence. Run the ring-fence test once and let it fail; take the ACTUAL digest it
    reports and write that into `_PINNED_SHA256` in `tests/test_serial_comm.py`. In the same commit,
    extend that test's docstring with a Phase 176 paragraph in the same shape as the Phase 65-01
    paragraph already there: the date, the digest it was updated FROM, the phase, what changed, and
    the argument -- two counter increments beside existing warnings in re-sync branches, adding no
    read, no branch on wire bytes and no `start_time` write, so none of the four invariants the
    fence header names is affected. Keep the Phase 65-01 paragraph; the docstring accumulates
    re-pins, it does not replace them. Leave the `GATE-1.8d VIOLATION` assertion message byte-
    identical. Do not edit the fence header comment in `serial_comm.py` -- it is a comment, the
    standing project rule forbids writing comments into source, and `inspect.getsource` does not
    cover it anyway.

    In `diagnostic_report.py` add `resync_body_truncated: int | None = None` and
    `resync_length_missing: int | None = None` to `TransportHealth` in alphabetical position among
    the counters, add both `NOT_MEASURED` substitutions to `_transport_dict()`, and add both names
    to `_SUSPECT_SCANNED_FIELDS` -- a re-sync is genuinely abnormal on a healthy link, so it belongs
    in the suspicion domain. The domain-closure test from plan `176-02` will confirm both landed in
    one set or the other rather than escaping silently. In `cli_handlers.py` assign both from the
    existing snapshot local, in the same block as the three earlier assignments.

    Update `_TRANSPORT_HEALTH_KEYS` to the final nine-element sorted list, drop the next-key guard
    assertion added earlier (no further key is planned in this phase) and replace it with an
    assertion that the list has exactly nine entries, keeping the update-deliberately message.
    Extend `test_transport_not_measured`'s key tuple with both new names. Re-baseline the 16
    snapshots with `tools/snapshot_report_shapes.py` then `--check`, and confirm the diff is 16
    files with exactly two added lines and zero deleted lines each. All of this lands in THIS task's
    commit alongside the production change.

    Add the four behaviors above to `tests/test_transport_counters.py`, using `MAGIC_PREAMBLE_REF`
    and `struct.pack` from `tests/conftest.py` and the tiny-real-clock 20-50 ms technique. Drive
    `_read_and_parse_lines` directly for Tests 1 and 2 so the timeout site is not also tripped, and
    use `get_response` only for Test 3, where tripping both is the point.
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/evidence && mkdir -p "$E" && EV=$E/176-03-ringfence-and-resync.txt && { echo "== 1 the two re-sync legs and the adjacency case"; (cd firestarter_app && ./.venv311/bin/python -c "
import struct, sys
sys.path.insert(0, '.')
sys.path.insert(0, 'tests')
from firestarter import transport_counters as tc
from firestarter.exceptions import SerialTimeoutError
from tests.conftest import MAGIC_PREAMBLE_REF, _FakeSerial
from firestarter.serial_comm import SerialCommunicator


def comm():
    fs = _FakeSerial()
    c = SerialCommunicator.__new__(SerialCommunicator)
    c.connection = fs
    c.port_name = '/dev/null'
    c.baud_rate = 250000
    c.timeout = 0.1
    c.seen_message_ids = set()
    return c, fs


tc.reset()
c, fs = comm()
fs.feed(MAGIC_PREAMBLE_REF)
list(c._read_and_parse_lines(0.05))
s = tc.snapshot()
print('length_missing=' + str(s['resync_length_missing']))
print('length_missing_others=' + str(sum(v for k, v in s.items() if k != 'resync_length_missing')))
tc.reset()
c, fs = comm()
fs.feed(MAGIC_PREAMBLE_REF + struct.pack('>H', 8) + b'\x01\x02')
list(c._read_and_parse_lines(0.05))
s = tc.snapshot()
print('body_truncated=' + str(s['resync_body_truncated']))
print('body_truncated_others=' + str(sum(v for k, v in s.items() if k != 'resync_body_truncated')))
tc.reset()
c, fs = comm()
fs.feed(MAGIC_PREAMBLE_REF + struct.pack('>H', 8) + b'\x01\x02')
try:
    c.get_response(timeout=0.05)
    print('adjacency_raised=False')
except SerialTimeoutError:
    print('adjacency_raised=True')
s = tc.snapshot()
print('adjacency_resync=' + str(s['resync_body_truncated']))
print('adjacency_timeouts=' + str(s['timeouts']))
print('adjacency_probe=' + str(s['probe_timeouts']))
print('keys=' + ','.join(sorted(tc.snapshot())))
"); echo "rc=$?"; echo "== 2 the ring-fence pin, recomputed"; (cd firestarter_app && ./.venv311/bin/python -c "
import hashlib, inspect, pathlib, re, sys
sys.path.insert(0, '.')
from firestarter.serial_comm import SerialCommunicator
actual = hashlib.sha256(inspect.getsource(SerialCommunicator._read_and_parse_lines).encode('utf-8')).hexdigest()
src = pathlib.Path('tests/test_serial_comm.py').read_text(encoding='utf-8')
pinned = re.search(r'_PINNED_SHA256 = \"([0-9a-f]{64})\"', src).group(1)
print('digest_matches_pin=' + str(actual == pinned))
print('digest_changed=' + str(pinned != '6d9e4fe4b67b78c110418305113b275174f16b2ecc9e0f55fbf5d9a623398184'))
print('records_phase_176=' + str('Phase 176' in src))
print('keeps_65_01_paragraph=' + str('Phase 65-01' in src))
print('keeps_violation_message=' + str('GATE-1.8d VIOLATION' in src))
"); echo "rc=$?"; echo "== 3 the ring-fence test itself"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_serial_comm.py tests/test_serial_characterization.py tests/test_transport_counters.py tests/test_diagnostic_report.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 3 && /usr/bin/grep -qx 'length_missing=1' "$EV" && /usr/bin/grep -qx 'length_missing_others=0' "$EV" && /usr/bin/grep -qx 'body_truncated=1' "$EV" && /usr/bin/grep -qx 'body_truncated_others=0' "$EV" && /usr/bin/grep -qx 'adjacency_raised=True' "$EV" && /usr/bin/grep -qx 'adjacency_resync=1' "$EV" && /usr/bin/grep -qx 'adjacency_timeouts=1' "$EV" && /usr/bin/grep -qx 'adjacency_probe=0' "$EV" && /usr/bin/grep -qx 'keys=decode_failures,probe_timeouts,resync_body_truncated,resync_length_missing,timeouts' "$EV" && /usr/bin/grep -qx 'digest_matches_pin=True' "$EV" && /usr/bin/grep -qx 'digest_changed=True' "$EV" && /usr/bin/grep -qx 'records_phase_176=True' "$EV" && /usr/bin/grep -qx 'keeps_65_01_paragraph=True' "$EV" && /usr/bin/grep -qx 'keeps_violation_message=True' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV" && ! /usr/bin/grep -qE '[0-9]+ skipped' "$EV"</automated>
    <fails_when>non-zero exit; fewer than three `rc=0` lines; `length_missing_others` or `body_truncated_others` not `0`, meaning a re-sync leg moved a counter it should not have; `adjacency_resync` or `adjacency_timeouts` not `1`, meaning the two distinct events were merged or one suppressed the other; `digest_matches_pin=False`, meaning the pin was not updated to the real digest; `digest_changed=False`, meaning the pin was never actually moved and the increments therefore never landed in the generator; `records_phase_176=False` or `keeps_65_01_paragraph=False`, meaning the re-pin was not recorded or the prior precedent was overwritten; `keeps_violation_message=False`; "no tests ran"; any "N failed", "N error" or "N skipped" line</fails_when>
    <automated>cd /workspaces && git -C firestarter_app diff --numstat -- tests/fixtures/reports/ > /tmp/176-03-snap.txt; test "$(wc -l < /tmp/176-03-snap.txt)" -eq 16 && test "$(awk '$1 != 2' /tmp/176-03-snap.txt | wc -l)" -eq 0 && test "$(awk '$2 != 0' /tmp/176-03-snap.txt | wc -l)" -eq 0 && (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check) && (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py 2>&1 | tail -2 | /usr/bin/grep -qE '^114 passed') && (cd firestarter_app && ./.venv311/bin/python -c "
import pathlib, re
src = pathlib.Path('tests/test_blast_radius_invariance.py').read_text(encoding='utf-8')
keys = re.findall(r'\"([a-z_]+)\"', re.search(r'_TRANSPORT_HEALTH_KEYS = \[(.*?)\]', src, re.S).group(1))
print('pin=' + ','.join(keys))
print('count=' + str(len(keys)))
") > /tmp/176-03-pin.txt && /usr/bin/grep -qx 'pin=cobs_errors,crc_failures,decode_failures,probe_timeouts,resync_body_truncated,resync_length_missing,retries,timeouts,transport_suspect' /tmp/176-03-pin.txt && /usr/bin/grep -qx 'count=9' /tmp/176-03-pin.txt && test "$(git -C firestarter_app diff -- firestarter/serial_comm.py | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0</automated>
    <fails_when>non-zero exit; the snapshot diff not being exactly 16 rows of two insertions and zero deletions; `snapshot_report_shapes.py --check` exiting non-zero; the oracle not reading `114 passed`; `pin=` not the exact nine-key sorted list or `count` not `9`; any added line in the `serial_comm.py` diff beginning with a hash, which would mean a comment was written into ring-fenced source</fails_when>
  </verify>
  <acceptance_criteria>
    - `transport_counters.snapshot()` keys are exactly
      `decode_failures,probe_timeouts,resync_body_truncated,resync_length_missing,timeouts`.
    - Each re-sync trigger moves exactly its own counter and nothing else, proven by the two
      `*_others=0` lines.
    - The adjacency case moves `resync_body_truncated` and `timeouts` by one each and leaves
      `probe_timeouts` at zero.
    - `_PINNED_SHA256` equals the freshly recomputed digest AND differs from the pre-phase value.
    - `tests/test_serial_comm.py` records a Phase 176 re-pin paragraph, still contains the Phase
      65-01 paragraph, and still contains the `GATE-1.8d VIOLATION` assertion message.
    - `_TRANSPORT_HEALTH_KEYS` is the nine-element sorted list; the oracle reads `114 passed`; the
      snapshot diff is 16 files with two added lines each.
    - Zero comment lines added to `serial_comm.py`.
  </acceptance_criteria>
  <done>All four RPT-C1 sites increment a real report-reachable counter, the ring fence is green against a deliberately re-pinned digest, and the argument for that re-pin is recorded in the test docstring where the next reader will find it.</done>
</task>

<task type="auto">
  <name>Task 2: Anti-vacuity for the fenced sites, and prove the fence still bites</name>
  <read_first>
    - `firestarter_app/tests/test_serial_comm.py` lines 428-462 -- the re-pinned test as Task 1 left
      it
    - `.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md` section on anti-vacuity
    - the two anti-vacuity transcripts this phase has already produced, under
      `.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/evidence/`
  </read_first>
  <files>firestarter_app/tests/test_transport_counters.py</files>
  <action>
    Produce the observed-RED evidence for the two fenced sites, appended to
    `evidence/176-03-anti-vacuity-red-green.txt` with an `rc_wN=1` marker after each weakening and a
    final `rc_clean=0` after restoring: (w1) delete the increment from the length-missing branch;
    (w2) delete the increment from the body-truncated branch; (w3) swap the two increments so each
    branch records the other's counter, which must break both plus-one legs while a naive
    total-count assertion would still pass -- this is the weakening that proves the legs are
    per-counter and not merely a sum; (w4) delete the `timeouts` increment, which must break the
    adjacency leg specifically while leaving the two direct re-sync legs green.

    Then prove the fence still bites after the re-pin, so the re-pin did not neuter it: append a
    fifth transcript in which one byte of the `_read_and_parse_lines` body is altered -- change the
    `resync_length_missing` call to the `resync_body_truncated` call, or add a whitespace-only line
    -- and `tests/test_serial_comm.py::test_read_and_parse_lines_ringfence_unchanged` is SEEN red
    with the `GATE-1.8d VIOLATION` message, marked `rc_fence=1`. Restore and re-run to green. A pin
    updated to a value nobody proved still discriminates is a pin in name only.

    Restore every weakening and re-run the full set green before committing. Confirm with
    `git status --porcelain` that no weakening survived into the working tree.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/evidence/176-03-anti-vacuity-red-green.txt && test -s "$EV" && test "$(/usr/bin/grep -cE '^rc_w[1-4]=1$' "$EV")" -eq 4 && /usr/bin/grep -qx 'rc_fence=1' "$EV" && /usr/bin/grep -q 'GATE-1.8d VIOLATION' "$EV" && /usr/bin/grep -qx 'rc_clean=0' "$EV" && test "$(/usr/bin/grep -cE '^[0-9]+ failed' "$EV")" -ge 5 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV"</automated>
    <fails_when>non-zero exit; the transcript missing or empty; fewer than four `rc_wN=1` lines, meaning a counter weakening was never SEEN red; `rc_fence=1` absent or the `GATE-1.8d VIOLATION` string absent, meaning the re-pinned fence was never proven to still discriminate; `rc_clean=0` absent, meaning nothing was restored; fewer than five "N failed" lines; "no tests ran"</fails_when>
    <automated>cd /workspaces && (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_serial_comm.py tests/test_transport_counters.py tests/test_diagnostic_report.py tests/test_serial_characterization.py) > /tmp/176-03-final.txt 2>&1; tail -3 /tmp/176-03-final.txt; /usr/bin/grep -qE '^[0-9]+ passed' /tmp/176-03-final.txt && ! /usr/bin/grep -qE '[0-9]+ (failed|error|skipped)' /tmp/176-03-final.txt && /usr/bin/grep -q 'record_resync_length_missing' firestarter_app/firestarter/serial_comm.py && /usr/bin/grep -q 'record_resync_body_truncated' firestarter_app/firestarter/serial_comm.py && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_transport_counters.py)" -eq 0 && (cd firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/ && ./.venv311/bin/python -m ruff format --check firestarter/ tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)')</automated>
    <fails_when>non-zero exit; no "N passed" line; any "N failed", "N error" or "N skipped" line, which would mean a weakening survived into the working tree; either `record_resync_*` call absent from `serial_comm.py`; a non-zero comment-line count in the counter test module; a ruff or watermark failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/176-03-anti-vacuity-red-green.txt` carries four `rc_wN=1` lines, an `rc_fence=1`
      line, the literal `GATE-1.8d VIOLATION` text from the failing fence run, and a final
      `rc_clean=0`.
    - The swap weakening (w3) is present, proving the legs are per-counter rather than a total.
    - The final green run reports "N passed" with no failed, error or skipped lines.
    - Both `record_resync_length_missing` and `record_resync_body_truncated` are present in
      `serial_comm.py` after restoration.
    - `ruff check`, `ruff format --check` and the watermark at `mypy errors: 35 (watermark: 35)` all
      pass.
  </acceptance_criteria>
  <done>Every counter leg in this plan has been SEEN red under a weakening, and the re-pinned ring fence has been SEEN red under a one-line body change -- so both the counters and the fence are proven to discriminate rather than merely asserted to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board to host serial stream | A hostile or malfunctioning board can drive both re-sync branches arbitrarily often by sending a magic preamble with no valid frame behind it. |
| the v1.9 ring fence | A process boundary rather than a data one: the SHA pin is the only mechanical guard on a body a paused RCA still nominally depends on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-176-10 | Denial of Service | the two re-sync counters, driven from the wire | low | accept | A hostile board can drive both counts arbitrarily high at O(log n) memory for a Python int. Nothing sized from frame content is stored -- both incrementers take no arguments -- and the branches already existed, so the attack surface is unchanged. |
| T-176-11 | Tampering | the ring-fence SHA pin | high | mitigate | A pin updated to a value nobody proved still discriminates is a pin in name only. Mitigated by Task 2's `rc_fence=1` leg, which alters one line of the generator body and SEES the pin go red with its `GATE-1.8d VIOLATION` message before restoring. |
| T-176-12 | Repudiation | the re-pin itself | high | mitigate | A silent re-pin would leave no record of who decided the fence was spent for this class of change. Mitigated by the blocking-human checkpoint and by the Phase 176 paragraph in the test docstring, which is asserted present by `records_phase_176=True` and keeps the Phase 65-01 precedent alongside it. |
| T-176-13 | Spoofing | a re-sync flooding `transport_suspect` | medium | accept | Both re-sync counters ARE in the suspicion domain deliberately: a re-sync is genuinely abnormal on a healthy link, unlike a port-discovery timeout. A hostile board can therefore raise `transport_suspect` about itself, which is the correct outcome, not an attack. |
| T-176-SC | Tampering | package-manager installs | high | accept | This plan installs nothing; HYG-02 forbids a new runtime dependency. The Package Legitimacy Gate has no package to audit. |
</threat_model>

<verification>
- `tests/test_serial_comm.py` green, including the re-pinned ring-fence test.
- `tests/test_transport_counters.py`, `tests/test_diagnostic_report.py` and
  `tests/test_serial_characterization.py` green.
- `tests/test_blast_radius_invariance.py` plus `tests/test_rekey_ledger.py` at `114 passed` with the
  nine-key pin.
- `tools/snapshot_report_shapes.py --check` exits `0`; the snapshot diff is 16 files, two added
  lines each.
- `evidence/176-03-anti-vacuity-red-green.txt` carries four counter weakenings plus the fence
  weakening, all SEEN red.
- `ruff check`, `ruff format --check`, mypy watermark at `35 (watermark: 35)`.
</verification>

<success_criteria>
- All four RPT-C1 sites -- both re-sync events, the decode failure and the response timeout -- each
  increment their own dedicated, report-reachable counter.
- `transport_health` carries nine keys, five of them real counts and three of them `not measured`
  with a recorded reason (RPT-C2).
- The ring fence is green against a deliberately re-pinned digest, with the argument recorded in the
  test docstring and the re-pin approved by a human at a gate `--auto` cannot bypass.
</success_criteria>

<output>
Create `.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-03-SUMMARY.md` when done.

Record in the SUMMARY: the previous digest, the new digest, and the one-paragraph argument, so the
re-pin is discoverable from the phase record as well as from the test docstring.

Do not mark any requirement Complete in this plan. Commit with plain `git commit` from inside
`firestarter_app`, then a second plain `git commit` in `/workspaces` for the gitlink and the
`.planning` evidence. Do NOT run `gsd-tools query commit`. Do not create, switch, rename or delete
any branch.
</output>
