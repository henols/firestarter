---
phase: 177-evidence-gated-read-back
plan: 03
type: execute
wave: 3
depends_on: [177-02]
files_modified:
  - firestarter_app/tests/test_readback_inventory.py
  - .planning/seeds/dev-test-adaptive-sequencing.md
  - .planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md
  - .planning/phases/177-evidence-gated-read-back/COVERAGE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PRUNE-04, PRUNE-07]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `dev test` engine's whole population of device-read call sites is enumerated by a machine, not asserted in prose: `firestarter/chip_test.py` contains exactly two `operator.read_eprom` call sites, and a test names the enclosing function of each."
    - "Both engine read sites are named-and-excluded with their reason recorded: the fingerprint read-back is excluded by D-1 because a verify returns a bool and one mismatch address while `classify_fingerprint` needs the whole mismatch distribution; the SDP leg's read-back IS the verdict."
    - "The one genuine read-whole-device-to-compare-a-held-buffer site in the package -- `eprom_operations.write_cycle_eprom` -- is named, located and excluded with its reason, so PRUNE-04 closes as measured rather than as unexamined."
    - "The census gate is seen to go RED: a planted third `operator.read_eprom` call site in the engine fails it, so the count is a gate and not a claim."
    - "A planner reading ONLY `.planning/seeds/dev-test-adaptive-sequencing.md` cannot regenerate the destructive interpretation: R1's sweeping sentence is REPLACED in place, not merely appended past, and the replacement states affirmatively that the fingerprint read-back and the SDP leg must stay real reads, with the reason."
    - "R2 no longer mis-states its own predicate: it records that the gate must consult the step's outcomes across all cycles rather than the final cycle's one-element `outcomes` list, and that a passing step still reports a synthesized `match` fingerprint."
    - "The seed's `status` frontmatter field reflects the fired trigger explicitly rather than being left stale, and its four-field frontmatter schema is otherwise unchanged."
    - "The whole app suite, `ruff check`, `ruff format --check`, the mypy watermark at 35/35, the snapshot drift check, the orchestrator checker and the cross-tree re-key checker all pass at phase close."
    - "No file in the `firestarter/` firmware submodule and no line of `firestarter_app/firestarter/data/chip_database.json` changed across the whole phase."
    - "PRUNE-01, PRUNE-02, PRUNE-03, PRUNE-04 and PRUNE-07 are each marked Complete in `.planning/REQUIREMENTS.md`'s traceability table, and no requirement owned by another phase is touched."
  prohibitions:
    - "No `#` comment is written into any source or test file this phase touches. Rationale goes in a docstring."
    - "`eprom_operations.write_cycle_eprom`'s read-back block is not converted to a verify and not refactored into a parallel read implementation. Its own docstring forbids the second and D-1's reasoning forbids the first -- it is a read-repeatability diagnostic, the uno328pb read-bug oracle."
    - "The SDP leg's `_read_region` read-back is never converted to a verify. It IS the verdict, and its length gate and degeneracy gate depend on getting real bytes back."
    - "`_read_region` is not duplicated. It is the one place the region slice lives; a second region read plus a slice from offset 0 silently reads hole padding."
    - "The seed's destructive sentence is not merely deleted. Deleting it without an affirmative replacement leaves the general rule in the preceding sentence sweeping the fingerprint read-back back in by default, and criterion 5 fails."
    - "The seed's frontmatter schema is not widened. The analog amendment changed one field's value inside an unchanged four-field schema."
    - "No requirement owned by another phase is marked Complete. Only PRUNE-01, PRUNE-02, PRUNE-03, PRUNE-04 and PRUNE-07 change status in this phase."
    - "No new runtime dependency is added (HYG-02), and no GitHub issue is edited or closed."
  artifacts:
    - path: "firestarter_app/tests/test_readback_inventory.py"
      provides: "The machine census of the engine's device-read call sites, its named-and-excluded dispositions, and the planted-third-site anti-vacuity leg"
      contains: "def test_"
    - path: ".planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md"
      provides: "The eight-row read-back call-site inventory with each site's disposition and reason, and PRUNE-04's closure statement"
      contains: "write_cycle_eprom"
    - path: ".planning/seeds/dev-test-adaptive-sequencing.md"
      provides: "R1 amended in place so the fingerprint read-back is affirmatively excluded, and R2 corrected to state the cross-cycle predicate and the synthesized match"
      contains: "must stay real reads"
    - path: ".planning/phases/177-evidence-gated-read-back/COVERAGE.md"
      provides: "The reasoned no-external-API declaration the seal-time coverage gate accepts in place of a matrix"
      contains: "No external API integration"
  key_links:
    - from: "firestarter_app/tests/test_readback_inventory.py"
      to: "firestarter_app/firestarter/chip_test.py"
      via: "an ast walk over the module source counting operator.read_eprom call sites and naming their enclosing functions"
      pattern: "read_eprom"
    - from: ".planning/seeds/dev-test-adaptive-sequencing.md"
      to: ".planning/notes/dev-test-sequence-cost-model.md"
      via: "the amended R1 states the seam the note already carries -- verify decides, a read-back diagnoses"
      pattern: "read-back diagnoses"
---

<objective>
Close the two requirements that are not hash work: PRUNE-04, whose in-engine population this phase
measured as EMPTY once D-1's exclusion and the SDP carve-out apply, and PRUNE-07, the seed sentence
that told a planner to destroy the diagnostic the next rule preserves.

Then seal the phase by measurement.

Purpose: PRUNE-04 must close as *measured*, with the inventory committed and every excluded site
named with its reason -- the same standing this milestone grants PRUNE-08's *measured, not worth
doing*. And PRUNE-07 must be an in-place replacement, because an appended correction leaves the
destructive sentence readable and a planner reading only the seed regenerates it.

Output: a machine census that goes red if a third engine read site appears, a seed that can no
longer be misread, and a phase whose every gate is green.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-evidence-gated-read-back/177-RESEARCH.md
@.planning/phases/177-evidence-gated-read-back/177-PATTERNS.md
@.planning/phases/177-evidence-gated-read-back/177-DECISIONS.md
@.planning/seeds/dev-test-adaptive-sequencing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRUNE-04 closed by census -- enumerate every engine read site, name each exclusion, and see the gate go red</name>
  <files>firestarter_app/tests/test_readback_inventory.py, .planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md</files>
  <read_first>
    - firestarter_app/firestarter/chip_test.py (lines 2603-2672 for `_dispatch_read` and its read-vs-read divergence metric; 2710-2745 for `_read_region` and its "the ONE place this slice lives" docstring; 2746-2870 for `_resolve_write_target` and the uv-slot-only probe; 3090-3125 for the fingerprint read-back; 3300-3400 for `_dispatch_sdp_leg` and its "this read-back is NOT best-effort decoration" docstring)
    - firestarter_app/firestarter/eprom_operations.py (lines 1146-1260 for `write_cycle_eprom`, its `source_sha` at :1171, its read block and the SHA compare at :1246-1250, and its docstring at :1166-1168 forbidding a parallel read implementation; 2094-2135 for `verify_eprom`'s exact signature)
    - .planning/phases/177-evidence-gated-read-back/177-RESEARCH.md (the "PRUNE-04 call-site inventory" section, including the four search commands that make its coverage judgeable)
    - .planning/phases/177-evidence-gated-read-back/177-DECISIONS.md (D-177-1)
    - firestarter_app/tools/check_devtest_orchestrator.py (lines 140-175 -- `_HANDLER_FUNCTION_NAMES` is handler-side only, so a new `chip_test.py` helper needs no HYG-04 registration; confirm rather than assume)
  </read_first>
  <reversibility rating="reversible">A new test module and a meta-repo inventory document. Both are plain reverts.</reversibility>
  <behavior>
    - An `ast` walk over `firestarter/chip_test.py` finds exactly 2 call nodes of the form `operator.read_eprom(...)`.
    - The enclosing function of those two sites is `_dispatch_read` and `_read_region`, and no other.
    - `_read_region` is the only helper in the module that slices a region read-back -- asserted by finding exactly one function whose body opens the read-back output file and slices it.
    - `EpromOperator.verify_eprom` exists with parameters `eprom_name`, `eprom_data_dict`, `input_file_path`, `operation_flags`, `address_str`, so the "use the on-device verify instead" instruction has a real target.
    - The census fails when a third `operator.read_eprom(...)` call is planted into a copy of the module source -- the gate is seen to go red before it is trusted.
    - The census fails when the enclosing-function allow-list is emptied -- it cannot pass vacuously on a module it did not parse.
  </behavior>
  <action>
Implement decision `D-177-1` Option A: close PRUNE-04 as **measured-empty within the `dev test`
engine**, with the inventory committed and every excluded site named with its reason. If
`177-DECISIONS.md` recorded Option B instead, do the alternate work stated at the end of this action
and skip the census-only closure.

**New test module `firestarter_app/tests/test_readback_inventory.py`.** Write it first, see it red
against a deliberately wrong expectation, then make it green. Write no `#` comment; the module
docstring carries the rationale. Use `ast`, not `grep`: a grep census fails open on a rename and
this project has a recorded checker that resolved its own directory wrongly, scanned nothing and
exited 0. Parse `firestarter/chip_test.py`'s source with `ast.parse`, walk for `ast.Call` nodes whose
`func` is an `ast.Attribute` with `attr == "read_eprom"` whose value is `ast.Name(id="operator")`,
and assert the count is exactly 2. Map each call node back to its enclosing `FunctionDef` by walking
the tree once and recording the innermost function per node, and assert the enclosing-function set
is exactly `{"_dispatch_read", "_read_region"}`. Resolve the module path from
`firestarter.chip_test.__file__`, never from the test file's own directory, and assert the parsed
source is non-empty so the census cannot pass on an unread file.

Add the two anti-vacuity legs. Leg one: take the module source, splice in a third
`operator.read_eprom(...)` call inside an existing function body, re-run the same census function
over the mutated source and assert it now reports 3 -- the gate is SEEN to go red. Leg two: run the
census with an empty expected enclosing-function set and assert it fails, so an accidentally-empty
allow-list cannot make the test vacuous. Both legs operate on in-memory strings; write no file.

Add the disposition legs, each asserting the reason is recorded where a reader will find it, not
only in a planning document: assert `_read_region`'s docstring still states it is the one place the
region slice lives; assert `_dispatch_sdp_leg`'s docstring still states its read-back is the verdict
rather than decoration; and assert `EpromOperator.verify_eprom`'s signature carries the five
parameter names above, so the rule's replacement primitive is pinned by name.

**New meta-repo artifact `.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md`.**
Reproduce RESEARCH.md's eight-row inventory as a markdown table with columns site, what it reads,
what it compares against, and disposition-with-reason, and state the search method that makes its
coverage judgeable rather than asserted. The eight rows and their dispositions are: the fingerprint
read-back at `chip_test.py:3108` (region-scoped since quick task 260821-wna, compared against the
in-memory pattern) EXCLUDED by D-1; the SDP leg's read-back at `chip_test.py:3338` EXCLUDED by the
seed's own carve-out because it is the verdict; the uv-slot probe at `chip_test.py:2843` NOT
APPLICABLE because no held buffer exists and it is unreachable on a non-UV policy; `_dispatch_read`
at `chip_test.py:2642` NOT APPLICABLE because it measures read repeatability, which a verify cannot
measure, and which is PRUNE-08 and Phase 180's territory; `consistency_check_eprom` at
`eprom_operations.py:1079` NOT APPLICABLE for the same reason; `write_cycle_eprom` at
`eprom_operations.py:1195-1250` -- THE ONLY GENUINE MATCH IN THE PACKAGE, outside the `dev test`
engine, whose read-back is itself a read-repeatability diagnostic and the uno328pb read-bug oracle,
EXCLUDED with that reason; the plain `read` command and `dev_read_eprom` NOT APPLICABLE because they
compare against nothing; and the DFU read-backs OUT OF SCOPE because they are the DFU bus, not the
EPROM bus. Close with the requirement statement: PRUNE-04's in-engine population is measured EMPTY
once D-1's exclusion and the SDP carve-out apply, so the requirement closes as *measured-empty, with
the named-and-excluded reason recorded* -- the same standing PRUNE-08 is granted for closing as
*measured, not worth doing*. Name the two call sites the census pins and state that the census gate
reddens if a third appears.

**If D-177-1 recorded Option B instead:** convert `write_cycle_eprom`'s read block
(`eprom_operations.py:1195-1250`) to `self.verify_eprom(...)`, delete the `readback_sha` comparison
against `source_sha`, keep the inventory artifact as the record of what was and was not converted,
and add a test asserting `write_cycle_eprom` makes zero `_main_phase_read_data` calls. Record in the
SUMMARY that the uno328pb read-repeatability oracle was deliberately given up, and which decision
authorised it.

Commit the test module to the `firestarter_app` sub-repo (`test(177-03): census the engine's device
read sites and close PRUNE-04 as measured-empty`) and the inventory artifact to the meta repo
(`docs(177-03): the read-back call-site inventory behind PRUNE-04's closure`).
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/177-evidence-gated-read-back/evidence && mkdir -p "$E" && EV=$E/177-03-readback-census.txt && { echo "== 1 the census itself"; (cd firestarter_app && ./.venv311/bin/python -c "
import ast, inspect, pathlib, sys
sys.path.insert(0, '.')
from firestarter import chip_test as ct
from firestarter.eprom_operations import EpromOperator
src = pathlib.Path(ct.__file__).read_text(encoding='utf-8')
print('source_nonempty=' + str(len(src) > 1000))
tree = ast.parse(src)
owner = {}
for fn in [n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]:
    for child in ast.walk(fn):
        owner.setdefault(id(child), fn.name)
sites = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr == 'read_eprom' and isinstance(n.func.value, ast.Name) and n.func.value.id == 'operator']
print('engine_read_sites=' + str(len(sites)))
print('enclosing=' + ','.join(sorted({owner.get(id(n), '?') for n in sites})))
print('verify_params=' + ','.join(list(inspect.signature(EpromOperator.verify_eprom).parameters)[1:]))
print('read_region_doc_pins_one_place=' + str('ONE place' in (ct._read_region.__doc__ or '')))
print('sdp_doc_pins_verdict=' + str('verdict' in (ct._dispatch_sdp_leg.__doc__ or '').lower()))
"); echo "rc=$?"; echo "== 2 the census module"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_readback_inventory.py); echo "rc=$?"; echo "comments_test_readback_inventory=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_readback_inventory.py)"; echo "== 3 the inventory artifact"; I=.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md; echo "inventory_present=$([ -s "$I" ] && echo yes || echo no)"; echo "inventory_rows=$(/usr/bin/grep -cE '^\|' "$I" 2>/dev/null || echo 0)"; echo "names_write_cycle=$(/usr/bin/grep -c 'write_cycle_eprom' "$I" 2>/dev/null || echo 0)"; echo "names_sdp_leg=$(/usr/bin/grep -c '_dispatch_sdp_leg\|SDP leg' "$I" 2>/dev/null || echo 0)"; echo "names_dispatch_read=$(/usr/bin/grep -c '_dispatch_read' "$I" 2>/dev/null || echo 0)"; echo "states_closure=$(/usr/bin/grep -ci 'measured-empty\|measured empty' "$I" 2>/dev/null || echo 0)"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 2 && /usr/bin/grep -qx 'source_nonempty=True' "$EV" && /usr/bin/grep -qx 'engine_read_sites=2' "$EV" && /usr/bin/grep -qx 'enclosing=_dispatch_read,_read_region' "$EV" && /usr/bin/grep -qx 'verify_params=eprom_name,eprom_data_dict,input_file_path,operation_flags,address_str' "$EV" && /usr/bin/grep -qx 'read_region_doc_pins_one_place=True' "$EV" && /usr/bin/grep -qx 'sdp_doc_pins_verdict=True' "$EV" && /usr/bin/grep -qx 'comments_test_readback_inventory=0' "$EV" && /usr/bin/grep -qx 'inventory_present=yes' "$EV" && test "$(/usr/bin/grep -oE '^inventory_rows=[0-9]+' "$EV" | cut -d= -f2)" -ge 10 && ! /usr/bin/grep -qx 'names_write_cycle=0' "$EV" && ! /usr/bin/grep -qx 'names_sdp_leg=0' "$EV" && ! /usr/bin/grep -qx 'names_dispatch_read=0' "$EV" && ! /usr/bin/grep -qx 'states_closure=0' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>non-zero exit; or `engine_read_sites=` reports anything other than `2`; or `enclosing=` is anything other than `_dispatch_read,_read_region`; or `source_nonempty=False` (the census parsed an empty file); or `verify_params=` does not list the five expected parameter names in order; or `comments_test_readback_inventory=` is non-zero; or the inventory file is missing, has fewer than 10 table lines, or one of the `names_*` / `states_closure` counts is `0`; or `no tests ran` / an `N failed` / `N error` line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q --collect-only tests/test_readback_inventory.py > /tmp/177-03-collect.txt 2>&1 && test "$(/usr/bin/grep -cE '^tests/test_readback_inventory\.py::' /tmp/177-03-collect.txt)" -ge 5 && ./.venv311/bin/python -m ruff check tests/test_readback_inventory.py && ./.venv311/bin/python -m ruff format --check tests/test_readback_inventory.py && git diff HEAD~1 --quiet -- firestarter/</automated>
    <fails_when>non-zero exit; or fewer than five test node ids are collected from the new module, which would mean the anti-vacuity legs were not written; or `ruff check` / `ruff format --check` reports a finding; or the commit touched a path under `firestarter/` (Option A adds no production code)</fails_when>
  </verify>
  <acceptance_criteria>
    - An `ast` census over `firestarter/chip_test.py` reports exactly 2 `operator.read_eprom` call sites.
    - The enclosing-function set of those sites is exactly `{_dispatch_read, _read_region}`.
    - `tests/test_readback_inventory.py` collects at least five test node ids, including a leg that plants a third call site into the module source and asserts the census reports 3, and a leg that asserts an empty expected allow-list fails.
    - `inspect.signature(EpromOperator.verify_eprom)` lists `eprom_name, eprom_data_dict, input_file_path, operation_flags, address_str` after `self`.
    - `_read_region.__doc__` still contains `ONE place`; `_dispatch_sdp_leg.__doc__` still contains the word `verdict`.
    - `tests/test_readback_inventory.py` contains zero lines whose first non-blank character opens a comment.
    - `.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` exists with at least 10 table lines and names `write_cycle_eprom`, the SDP leg and `_dispatch_read`, each with a disposition and a reason.
    - The inventory states PRUNE-04's closure as measured-empty within the engine.
    - Under Option A the commit adds no file under `firestarter_app/firestarter/`.
  </acceptance_criteria>
  <done>PRUNE-04 is closed by a machine census that names both engine read sites and their exclusions, the inventory behind the closure is committed, and a planted third site is proven to redden the gate.</done>
</task>

<task type="auto">
  <name>Task 2: PRUNE-07 -- replace the sentence that told a planner to delete the diagnostic</name>
  <files>.planning/seeds/dev-test-adaptive-sequencing.md</files>
  <read_first>
    - .planning/seeds/dev-test-adaptive-sequencing.md (the whole file: the four-field frontmatter, R1's final paragraph, R2's gate statement and its two bullets)
    - .planning/phases/177-evidence-gated-read-back/177-RESEARCH.md (the "The seed's self-contradiction" section, which quotes R1 and R2 verbatim and supplies candidate replacement wording for both)
    - .planning/phases/177-evidence-gated-read-back/177-PATTERNS.md (section 8 -- the `380210ed` amendment precedent and the two departures criterion 5 forces)
    - .planning/notes/dev-test-sequence-cost-model.md (lines 68-90 -- the project's already-correct formulation of the seam)
    - .planning/seeds/py32f071-no-external-tool-fw-install.md (the analog's amended frontmatter and its dated headed block)
  </read_first>
  <precondition>D-1 in `.planning/REQUIREMENTS.md` settles the contradiction in R2's favour, and `177-RESEARCH.md`'s "The seed's self-contradiction" section supplies the candidate wording for both halves. Both must be read before the paragraph is replaced.</precondition>
  <reversibility rating="costly">A seed is the artifact a future planner reads instead of this phase's reasoning, so a wrong amendment is expensive -- but the file is meta-repo tracked and a revert restores it exactly. Nothing here is published outside the repository, which is what would make it one-way.</reversibility>
  <action>
One commit in the META repo (`/workspaces`). Follow the amendment precedent `380210ed` -- which
amended `.planning/seeds/py32f071-no-external-tool-fw-install.md` with one frontmatter field changed
inside an unchanged four-field schema plus a dated headed block appended -- with the two departures
criterion 5 forces.

**Departure one: replace R1's final paragraph IN PLACE.** The paragraph ends with a sentence that
sweeps the fingerprint read-back into R1's general rule. Deleting that sentence is not enough: the
general rule in the sentence before it would still sweep the fingerprint read-back in by default,
and criterion 5 says a planner reading only the seed must be unable to regenerate the destructive
interpretation. Replace the paragraph with an affirmative exclusion naming BOTH excluded read-backs
and the reason for each: the fingerprint read-back, which R2 governs -- a verify returns a bool and
one mismatch address (`memory_verify_execute` returns at the first mismatching byte), while
`classify_fingerprint` needs the whole mismatch distribution, `ff_ratio` across the buffer for
bucket one and per-bit clustering across every diff offset for bucket two, so converting it deletes
the diagnostic R2 exists to preserve -- and the SDP leg, whose `_read_region` read-back is the
verdict. Close with the seam the project already has in
`.planning/notes/dev-test-sequence-cost-model.md:75-76`: verify decides, a read-back diagnoses, and
the read-back only needs to run when verify says something is wrong. Use the phrase "must stay real
reads" so the exclusion is affirmative rather than an absence.

**Departure two: extend R2 so its anchor stops mis-stating its own predicate.** R2 currently names a
predicate that is insufficient under the cycle block. Append to R2 that the predicate must consult
the step's outcomes across all cycles: under `_run_cycle_block` each cycle calls
`_dispatch_multi_run` with `runs=1`, so the final cycle's outcome list has one element and describes
that cycle only, and a cycle-1-fail / cycle-2-pass run must keep its fingerprint. Add that a passing
step still REPORTS a fingerprint -- synthesized from `bad=0`, `total=region_length`,
`ff_ratio: None` and classified `match`. Close that addition with the sentence "The read-back is
what costs; the classification is free." -- and note that the acceptance gate greps the seed for
the exact substring `The read-back is what costs`, so that substring must land unbroken on a single
line of the seed rather than wrapped across two. Keep R2's two existing bullets and its `ff_ratio` false-PASS paragraph
intact; the structural-dependency sentence in that paragraph is now satisfied by Phase 175's
sentinel and should say so.

**The frontmatter `status` field.** The seed's `trigger_condition` is the next milestone that
touches `dev test` / `chip_test.py`; v1.36 is that milestone, and the field currently reads as
untriggered. Change that one field's VALUE, on a SINGLE line (a wrapped continuation line beginning with a lowercase word and a colon would be counted as a fifth frontmatter key by the gate), to state what has fired and what has not -- R1 and R2 are
realised by Phase 177, R3 remains for Phase 180 (PRUNE-08) and R4 is deferred to Future
Requirements -- exactly as the analog did. Change no other field and add no field: the four-field
schema stays as it is.

**The dated block.** Append a `**Phase 177, YYYY-MM-DD.**` headed block in the analog's voice
recording what changed and why: that R1's sweeping sentence contradicted R2's promise of
byte-identical fidelity on failing runs, that D-1 settled the contradiction in R2's favour, and that
R1 was amended in place rather than merely annotated so the destructive reading is not merely
outvoted but absent. State explicitly what was NOT touched: R3, R4, the projected-effect table, the
per-class characteristics section and the out-of-scope section.

Commit message `docs(177-03): amend the seed so R1 no longer instructs a planner to destroy the
diagnostic R2 preserves`, body naming D-1 and PRUNE-07 and stating what was not touched.
  </action>
  <verify>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md && EV=.planning/phases/177-evidence-gated-read-back/evidence/177-03-seed-amendment.txt && { echo "destructive_sentence_present=$(/usr/bin/grep -c 'applies to the fingerprint read-backs' "$S")"; echo "affirmative_exclusion=$(/usr/bin/grep -c 'must stay real reads' "$S")"; echo "names_both_exclusions=$(/usr/bin/grep -ci 'SDP leg' "$S")"; echo "states_the_seam=$(/usr/bin/grep -c 'read-back diagnoses' "$S")"; echo "r2_cross_cycle=$(/usr/bin/grep -ci 'across all cycles' "$S")"; echo "r2_synthesized=$(/usr/bin/grep -c 'The read-back is what costs' "$S")"; echo "dated_block=$(/usr/bin/grep -cE '^\*\*Phase 177, 20[0-9]{2}-[0-9]{2}-[0-9]{2}\.\*\*' "$S")"; echo "frontmatter_fields=$(sed -n '2,/^---$/p' "$S" | /usr/bin/grep -cE '^[a-z_]+:')"; echo "status_still_dormant=$(sed -n '2,/^---$/p' "$S" | /usr/bin/grep -cE '^status:[[:space:]]*dormant[[:space:]]*$')"; echo "r3_intact=$(/usr/bin/grep -c 'Sample for a rate, sweep for a map' "$S")"; echo "r4_intact=$(/usr/bin/grep -c 'One session per plan' "$S")"; echo "projection_table_intact=$(/usr/bin/grep -c 'am27c020' "$S")"; } > "$EV" 2>&1; /usr/bin/grep -qx 'destructive_sentence_present=0' "$EV" && ! /usr/bin/grep -qx 'affirmative_exclusion=0' "$EV" && ! /usr/bin/grep -qx 'names_both_exclusions=0' "$EV" && ! /usr/bin/grep -qx 'states_the_seam=0' "$EV" && ! /usr/bin/grep -qx 'r2_cross_cycle=0' "$EV" && ! /usr/bin/grep -qx 'r2_synthesized=0' "$EV" && /usr/bin/grep -qx 'dated_block=1' "$EV" && /usr/bin/grep -qx 'frontmatter_fields=4' "$EV" && /usr/bin/grep -qx 'status_still_dormant=0' "$EV" && /usr/bin/grep -qx 'r3_intact=1' "$EV" && /usr/bin/grep -qx 'r4_intact=1' "$EV" && ! /usr/bin/grep -qx 'projection_table_intact=0' "$EV"</automated>
    <fails_when>non-zero exit; or `destructive_sentence_present=` is anything other than `0` (the swept-in sentence survives, so a planner reading only the seed can still regenerate it); or any of `affirmative_exclusion` / `names_both_exclusions` / `states_the_seam` / `r2_cross_cycle` / `r2_synthesized` is `0`; or `dated_block=` is not exactly `1`; or `frontmatter_fields=` is not exactly `4` (a field was added or removed); or `status_still_dormant=1` (the fired trigger was left unrecorded); or `r3_intact` / `r4_intact` is not `1` or `projection_table_intact` is `0` (out-of-scope content was collaterally edited)</fails_when>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'applies to the fingerprint read-backs' .planning/seeds/dev-test-adaptive-sequencing.md` returns `0`.
    - The file contains the phrase `must stay real reads`, names the SDP leg as the second exclusion, and contains `read-back diagnoses`.
    - R2 contains `across all cycles` and the synthesized-`match` sentence.
    - Exactly one line matches `^\*\*Phase 177, 20\d\d-\d\d-\d\d\.\*\*`.
    - The frontmatter still carries exactly four top-level keys, and `status:` no longer reads `dormant`.
    - R3's heading, R4's heading and the projected-effect table are unchanged -- each still present exactly once.
    - The commit lands in the meta repo and touches no path under `firestarter_app/`.
  </acceptance_criteria>
  <done>The seed no longer contains the sentence that instructed a planner to convert the fingerprint read-back to a verify, states affirmatively why both excluded read-backs must stay real reads, corrects R2's predicate, and records the fired trigger in its own frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Seal the phase by measurement -- every gate, both repos, and the requirement ledger</name>
  <files>.planning/phases/177-evidence-gated-read-back/COVERAGE.md, .planning/REQUIREMENTS.md, .planning/phases/177-evidence-gated-read-back/evidence/177-03-phase-seal.txt</files>
  <read_first>
    - firestarter_app/CLAUDE.md (the tooling gate list)
    - firestarter_app/tools/ci_parity.sh (lines 1-60 -- what it mirrors and what it deliberately does not)
    - .planning/REQUIREMENTS.md (the traceability table -- the five rows this phase owns)
    - .planning/phases/177-evidence-gated-read-back/evidence/ (every evidence file this phase produced)
    - .planning/MILESTONES.md (lines 38-45 -- the D-11 protocol, to confirm the commit topology this phase produced satisfies it)
  </read_first>
  <precondition>The `firestarter_app` working tree is committed and clean before the full suite runs. `tests/test_flash_path_record_sync` asserts whole-repo porcelain, so an uncommitted file makes it fail for a reason unrelated to this phase.</precondition>
  <reversibility rating="reversible">Documentation, a coverage declaration and requirement status flips. All plain reverts.</reversibility>
  <action>
Seal the phase by running every gate and writing down what each one measured. Change no behaviour in
this task; if a gate is red, fix the cause and record it, never weaken the gate.

**Write `.planning/phases/177-evidence-gated-read-back/COVERAGE.md`.** The deterministic API-coverage
detector was run over this phase's ROADMAP section at plan time and returned no integration signal,
so the seal-time gate accepts a reasoned declaration in place of a matrix. Write exactly one line in
the sanctioned form: `No external API integration: ` followed by a one-line reason naming what the
phase touches instead -- the `dev test` host engine's fingerprint read-back gate, the frozen-hash
fixture registry and one planning seed. The reason is required, exactly like an opt-out reason.

**Run the gate battery** from `/workspaces`, capturing everything into one evidence file:
scope -- `git -C firestarter status --porcelain` empty, and
`git -C firestarter_app status --porcelain firestarter/data/chip_database.json` empty;
hygiene -- zero comment lines in every file this phase CREATED, and zero comment-opening lines added
by this phase's diff over every file it MODIFIED, measured across the whole phase range rather than
one commit; tooling -- `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/`
and `tools/check_mypy_watermark.py` printing exactly `mypy errors: 35 (watermark: 35)`;
oracle -- `tools/snapshot_report_shapes.py --check`, then
`pytest tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_devtest_issue_corpus.py`;
orchestrator -- `tools/check_devtest_orchestrator.py`;
cross-tree -- `python3 tools/rekey/check_rekey_ledger.py` from `/workspaces`;
suite -- the whole `pytest tests/ -o addopts="" -q`, recording the passed count. The measured
full-suite baseline is 740.92 s, so allow for it. If `tests/test_skip_census.py` is the only failing
module, re-run it at this phase's base commit before recording anything: its three failures are
recorded as pre-existing (a 180 s child-run timeout cap against the 740.92 s suite) and if they
reproduce at the base commit they are recorded as pre-existing rather than treated as this phase's.

**Confirm the commit topology D-11 requires.** Record, from `git log`, that the behaviour commit
from `177-01` contains no `tests/fixtures/` path and that the declaration commits from `177-02` are
separate commits. This is the property `tools/rekey/check_rekey_ledger.py` cannot check -- it checks
consistency, not topology -- so it is checked here by inspection and written down.

**Mark the requirements.** In `.planning/REQUIREMENTS.md`, flip PRUNE-01, PRUNE-02, PRUNE-03,
PRUNE-04 and PRUNE-07 from `Pending` to `Complete` in the traceability table, and tick their v1
checkboxes in the "No-Information Operations" section. Touch no other requirement row: PRUNE-08
stays Pending and belongs to Phase 180, and every ATTR / UV / RPT / HYG row belongs to a later
phase. Edit the file by hand -- the GSD requirements verbs reformat the whole file and this one is
hand-authored. Record PRUNE-04's closure wording in the same edit: closed as measured-empty within
the engine, with `write_cycle_eprom` named and excluded, evidenced by
`177-READBACK-INVENTORY.md`.

**Note the consumer impact in the SUMMARY, without editing the consumer.**
`.claude/skills/devtest-triage/SKILL.md` reads `dedup_fingerprint` as a dedup key only and reads
neither the classification string nor `ladder_state`, so no skill edit is forced; but the re-key does
orphan the historical dedup groups it relies on, and an all-OK run is now ladder-promotable where it
previously was not. One sentence, in the SUMMARY.

Commit to the meta repo, message `docs(177-03): seal phase 177 -- gates measured, requirements
marked`.
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/177-evidence-gated-read-back/evidence && mkdir -p "$E" && EV=$E/177-03-phase-seal.txt && C=.planning/phases/177-evidence-gated-read-back/COVERAGE.md && { echo "== 1 scope"; echo "firmware_diff=$([ -z "$(git -C firestarter status --porcelain)" ] && echo clean || echo DIRTY)"; echo "chip_database_diff=$([ -z "$(git -C firestarter_app status --porcelain firestarter/data/chip_database.json)" ] && echo clean || echo DIRTY)"; echo "app_tree=$([ -z "$(git -C firestarter_app status --porcelain)" ] && echo clean || echo DIRTY)"; echo "== 2 comment census on created files"; echo "comments_test_readback_inventory=$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_readback_inventory.py)"; echo "== 3 tooling"; (cd firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python -m ruff format --check firestarter/ tests/); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_mypy_watermark.py); echo "rc=$?"; echo "== 4 oracle"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_devtest_issue_corpus.py); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_devtest_orchestrator.py); echo "rc=$?"; echo "== 5 cross-tree"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; echo "== 6 the whole suite"; (cd firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q > /tmp/177-03-suite.txt 2>&1); echo "rc=$?"; tail -4 /tmp/177-03-suite.txt; echo "full_suite_passed=$(/usr/bin/grep -oE '^[0-9]+ passed' /tmp/177-03-suite.txt | head -1 | cut -d' ' -f1)"; echo "== 7 declarations"; echo "coverage_present=$([ -s "$C" ] && echo yes || echo no)"; echo "reqs_complete=$(/usr/bin/grep -cE '^\| (PRUNE-01|PRUNE-02|PRUNE-03|PRUNE-04|PRUNE-07) \| Phase 177 \| Complete \|' .planning/REQUIREMENTS.md)"; echo "prune08_untouched=$(/usr/bin/grep -cE '^\| PRUNE-08 \| Phase 180 \| Pending \|' .planning/REQUIREMENTS.md)"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 8 && /usr/bin/grep -qx 'firmware_diff=clean' "$EV" && /usr/bin/grep -qx 'chip_database_diff=clean' "$EV" && /usr/bin/grep -qx 'app_tree=clean' "$EV" && /usr/bin/grep -qx 'comments_test_readback_inventory=0' "$EV" && /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' "$EV" && /usr/bin/grep -q '^PASS: scanned ' "$EV" && test "$(/usr/bin/grep -oE '^full_suite_passed=[0-9]+$' "$EV" | cut -d= -f2)" -ge 2151 && /usr/bin/grep -qx 'coverage_present=yes' "$EV" && /usr/bin/grep -qx 'reqs_complete=5' "$EV" && /usr/bin/grep -qx 'prune08_untouched=1' "$EV" && /usr/bin/grep -qF 'No external API integration' "$C" && ! /usr/bin/grep -q 'no tests ran' /tmp/177-03-suite.txt && ! /usr/bin/grep -qE '^[0-9]+ (failed|error)' /tmp/177-03-suite.txt</automated>
    <fails_when>non-zero exit; or fewer than eight `rc=0` lines (one gate in the battery is red); or `firmware_diff` / `chip_database_diff` / `app_tree` reports `DIRTY`; or `comments_test_readback_inventory=` is non-zero; or the watermark line is not exactly `mypy errors: 35 (watermark: 35)`; or `full_suite_passed=` is below 2151; or `reqs_complete=` is not exactly `5`; or `prune08_untouched=` is not `1`; or `COVERAGE.md` lacks the `No external API integration` declaration; or `no tests ran` / an `N failed` / `N error` line appears in the suite output</fails_when>
    <automated>cd /workspaces && BASE=$(git -C firestarter_app merge-base HEAD "$(git -C firestarter_app rev-parse HEAD)~6" 2>/dev/null || git -C firestarter_app rev-parse HEAD~6) && test "$(git -C firestarter_app diff "$BASE"..HEAD -- firestarter/ tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && test "$(git -C firestarter_app log --oneline "$BASE"..HEAD | wc -l)" -ge 3 && test -z "$(git -C firestarter_app log --format=%H "$BASE"..HEAD -- tests/fixtures/ | head -1 | xargs -I{} git -C firestarter_app show --name-only --format= {} | /usr/bin/grep -x 'firestarter/chip_test.py')"</automated>
    <fails_when>non-zero exit; or the phase's whole diff over `firestarter/` and `tests/` adds a line whose first non-blank character opens a comment; or fewer than three commits exist in the phase range, which would mean the behaviour and declaration commits were collapsed; or the most recent commit touching `tests/fixtures/` also touched `firestarter/chip_test.py`, which is the D-11 violation this leg exists to catch</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter status --porcelain` is empty and `git -C firestarter_app status --porcelain` is empty.
    - `firestarter_app/firestarter/data/chip_database.json` is unmodified.
    - `tests/test_readback_inventory.py` contains zero comment lines, and the phase's whole `firestarter/` + `tests/` diff adds zero comment-opening lines.
    - `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/` pass and `tools/check_mypy_watermark.py` prints exactly `mypy errors: 35 (watermark: 35)`.
    - `tools/snapshot_report_shapes.py --check` exits 0.
    - `tests/test_blast_radius_invariance.py`, `tests/test_rekey_ledger.py` and `tests/test_devtest_issue_corpus.py` all pass.
    - `tools/check_devtest_orchestrator.py` prints a `PASS: scanned ` line and exits 0.
    - `python3 tools/rekey/check_rekey_ledger.py` run from `/workspaces` exits 0.
    - `pytest tests/ -o addopts="" -q` exits 0 with at least 2151 passed and no `no tests ran` line.
    - `.planning/phases/177-evidence-gated-read-back/COVERAGE.md` exists and begins with the `No external API integration: ` declaration followed by a reason.
    - `.planning/REQUIREMENTS.md`'s traceability table shows exactly five `Phase 177 | Complete` rows -- PRUNE-01, PRUNE-02, PRUNE-03, PRUNE-04, PRUNE-07 -- and `PRUNE-08 | Phase 180 | Pending` is unchanged.
    - The phase range carries at least three commits in `firestarter_app`, and no commit touches both `tests/fixtures/` and `firestarter/chip_test.py`.
  </acceptance_criteria>
  <done>Every gate is measured and written down, the requirement ledger reflects exactly the five requirements this phase owns, the D-11 commit topology is confirmed by inspection, and the no-external-API declaration the seal-time gate reads is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact to future planner | `.planning/seeds/dev-test-adaptive-sequencing.md` is read INSTEAD of this phase's reasoning by whoever plans Phase 180. A sentence in it is an instruction that will be executed. |
| device to host EPROM data path | The read sites this task censuses are where untrusted device bytes enter the diagnostic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-177-13 | Tampering | the seed's R1 rule as an instruction to a future planner | high | mitigate | The recorded failure mode is real: R1's sweeping sentence instructs a planner to convert the fingerprint read-back to a verify, which deletes the four-bucket diagnostic R2's own gate exists to preserve for failing runs. Mitigated by IN-PLACE replacement with an affirmative exclusion plus the reason, asserted by a gate that counts the old sentence at zero and the new phrase at non-zero. |
| T-177-14 | Repudiation | PRUNE-04 closing with no record | high | mitigate | A requirement closed as "nothing to do" with no inventory is indistinguishable from one never examined. Mitigated by the committed eight-row inventory naming every site and its reason, plus an `ast` census that is SEEN to go red against a planted third site. |
| T-177-15 | Tampering | a `grep`-based census failing open | medium | mitigate | The devcontainer `grep` is ugrep and honors `.gitignore`, and this project has a recorded checker whose directory-relative path resolution made it scan nothing and exit 0. Mitigated by parsing with `ast` from `chip_test.__file__`, asserting the parsed source is non-empty, and adding an empty-allow-list leg that must fail. |
| T-177-16 | Denial of information | the `write_cycle_eprom` read-repeatability oracle | high | mitigate | Converting it to a verify would delete the uno328pb read-bug oracle. Mitigated by naming it as excluded with the reason in both the inventory and the requirement closure, so a later reader cannot mistake the exclusion for an oversight. |
| T-177-17 | Elevation of privilege | none applicable | low | accept | This phase adds no authentication, authorization, session or multi-user surface. ASVS V2/V3/V4 do not apply to a local CLI with no auth boundary. |
| T-177-SC | Tampering | package-manager installs | high | accept | This plan installs nothing. HYG-02 forbids a new runtime dependency and RESEARCH.md's Package Legitimacy Audit records zero packages to audit, so no `[ASSUMED]` / `[SUS]` verdict exists and no legitimacy checkpoint is required. |
</threat_model>

<verification>
Run from `/workspaces` unless a leg says otherwise. Every Python invocation uses
`firestarter_app/.venv311` (CI is py3.11 only). Every grep treated as evidence uses `/usr/bin/grep`.
The `firestarter_app` tree must be committed before the full suite runs, because
`tests/test_flash_path_record_sync` asserts whole-repo porcelain.

1. `engine_read_sites=2` and `enclosing=_dispatch_read,_read_region`.
2. `tests/test_readback_inventory.py` collects at least five node ids and passes, including the
   planted-third-site leg and the empty-allow-list leg.
3. `.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` names `write_cycle_eprom`,
   the SDP leg and `_dispatch_read`, each with a disposition and a reason.
4. The seed's swept-in sentence counts zero; `must stay real reads`, `read-back diagnoses`,
   `across all cycles` and the synthesized-`match` sentence all count non-zero; exactly one dated
   `**Phase 177, ...**` block; the frontmatter still has four keys and `status` is no longer stale.
5. The whole gate battery is green: eight `rc=0` lines, the watermark at 35/35, a `PASS: scanned `
   line, the cross-tree checker at 0, and the full suite at 2151+ passed.
6. `.planning/REQUIREMENTS.md` shows exactly five `Phase 177 | Complete` rows.
7. `git -C /workspaces/firestarter status --porcelain` is empty across the whole phase.
</verification>

<success_criteria>
- PRUNE-04 closes as measured, with a machine census that reddens on a planted site and an inventory
  that names every excluded call site and its reason.
- PRUNE-07 closes with the destructive sentence gone from the seed, replaced by an affirmative
  exclusion that states why, and R2 corrected so its own anchor stops mis-stating its predicate.
- Every gate in the battery is green, the phase's whole diff adds zero source comments, no firmware
  and no database file changed, and the five requirements this phase owns -- and only those five --
  are marked Complete.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan and absent at the phase's base commit -- exclude from source-grounding drift
verification:

- `firestarter_app/tests/test_readback_inventory.py` -- new module and every test function in it
- `.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md`
- `.planning/phases/177-evidence-gated-read-back/COVERAGE.md`
- `.planning/phases/177-evidence-gated-read-back/evidence/177-03-readback-census.txt`
- `.planning/phases/177-evidence-gated-read-back/evidence/177-03-seed-amendment.txt`
- `.planning/phases/177-evidence-gated-read-back/evidence/177-03-phase-seal.txt`

The full phase-wide new-symbol list is in `177-01-PLAN.md`'s
`<artifacts_this_phase_produces>` section.
</artifacts_this_phase_produces>

<flagged_assumptions>
The spec-less probe fallback's 7 edge items are enumerated once, in `177-01-PLAN.md`'s
`<flagged_assumptions>` section (3 authored into `must_haves.truths`, 4 surfaced as flagged
assumptions; 3 + 4 = 7, no drops). The assumptions specific to this plan are:

- **PA-15 (assumption A6, RESEARCH.md).** Roadmap criterion 4 as written presupposes at least one
  in-engine call site to convert, and the measurement shows there is none once D-1's exclusion and
  the SDP carve-out apply. This plan does NOT quietly satisfy the criterion by converting an
  unrelated site; it closes the requirement as measured-empty with the inventory as the evidence,
  under decision `D-177-1`. If the operator chose Option B, the alternate action is stated inline in
  Task 1 rather than left to the executor's judgement.
- **PA-16 (HYG-04 does not fire).** `tools/check_devtest_orchestrator.py`'s `_HANDLER_FUNCTION_NAMES`
  allow-list is handler-side only, so a new helper inside `chip_test.py` needs no registration.
  Verified against the checker's source rather than assumed, and the checker is a seal-battery leg
  either way.
- **PA-17 (the seed's `status` flip).** RESEARCH.md flags this as a judgement the planner must make
  explicitly rather than leave. It is made here: the field's VALUE changes to record what fired and
  what did not, inside an unchanged four-field schema, exactly as the `380210ed` precedent did. The
  gate asserts the schema width AND that the stale value is gone.
- **PA-18 (`test_skip_census.py`).** Its three failures are recorded as pre-existing (a 180 s
  child-run timeout cap against a 740.92 s suite). Phase 176 nevertheless measured the full suite at
  `rc=0`, so this plan gates strictly on `rc=0` and instructs the executor to re-run at the phase
  base commit before recording anything if that module alone is red.
- **PA-19 (consumer impact, RESEARCH open question 5 -- already answered).**
  `.claude/skills/devtest-triage/SKILL.md` reads `dedup_fingerprint` as a dedup key only, at five
  sites, and reads neither the classification string nor `ladder_state`. No skill edit is forced and
  none is planned. The re-key does orphan the historical dedup groups the skill relies on, and an
  all-OK run becomes ladder-promotable; that is recorded in the SUMMARY, not fixed here.
- **PA-20 (the phase commit range).** The topology leg resolves the phase base as `HEAD~6`. If the
  executor produced a different number of commits, resolve the base from the last commit before this
  phase's first instead and record which base was used; the property being checked is that no single
  commit touches both `tests/fixtures/` and `firestarter/chip_test.py`.
</flagged_assumptions>

<output>
Create `.planning/phases/177-evidence-gated-read-back/177-03-SUMMARY.md` when done, recording: the
measured engine read-site count and their enclosing functions; which D-177-1 option was implemented;
the seed's before/after for R1's replaced paragraph and R2's addition; every gate's measured result
including the full-suite passed count; whether `test_skip_census.py` was red and whether it
reproduced at the base commit; the five requirement rows flipped; and the one-sentence
`devtest-triage` consumer-impact note.
</output>
