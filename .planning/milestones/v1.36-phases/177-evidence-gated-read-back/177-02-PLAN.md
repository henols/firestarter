---
phase: 177-evidence-gated-read-back
plan: 02
type: execute
wave: 2
depends_on: [177-01]
files_modified:
  - firestarter_app/tests/fixtures/report_shapes.py
  - firestarter_app/tests/fixtures/shape_ids.json
  - firestarter_app/tests/fixtures/rekey_ledger.py
  - firestarter_app/tests/fixtures/reports/
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_rekey_ledger.py
  - .planning/MILESTONES.md
  - .planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md
  - .planning/phases/177-evidence-gated-read-back/177-DECISIONS.md
autonomous: false
requirements: [PRUNE-03]

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Every frozen shape whose `dedup_fingerprint` moved under plan `177-01` is re-baselined to a value produced by a run in `firestarter_app/.venv311`, never to a value transcribed from a planning document."
    - "Every declared ledger row's `after_hash` equals a freshly computed `dedup_fingerprint(build_shape(shape_id))` and differs from that row's `before_hash`; no row's `before_hash` is overwritten."
    - "`gh23-w27e257-fail` is still frozen at exactly `7a89fcea856a`, and the nine other unmoved shapes keep their committed values byte-for-byte -- so the re-key is provably exactly the change it declared and nothing more."
    - "`LADDER_PINS` still covers exactly four distinct `(proposed_disposition, ladder_state)` pairs after the D-4/D-6 flip: the INCONCLUSIVE arm is re-populated rather than left empty."
    - "`prune03-synthesized-fingerprint-match` is registered in all four places at once -- `_BUILDERS`, `FROZEN_HASHES`, `LADDER_PINS` and `tests/fixtures/shape_ids.json` -- and removed from `RESERVED_SHAPE_IDS`, so the collision assert does not fire."
    - "`tests/fixtures/reports/*.json` are regenerated by `tools/snapshot_report_shapes.py`, never hand-edited, and `--check` reports no drift."
    - "The app-side ledger and `.planning/MILESTONES.md` agree in BOTH directions: `python3 tools/rekey/check_rekey_ledger.py` run from `/workspaces` exits 0."
    - "The declaration lands in commits SEPARATE from `177-01`'s behaviour commit, so the re-key is a reviewable unit and no executor repaired the gate inside the commit that moved it."
    - "The measured filed-corpus re-key count is recorded with its per-issue before/after mapping, and any disagreement with the inherited projection of 18 rows is written into `MILESTONES.md`'s corrections table rather than silently absorbed."
    - "The falsified projection for `sst27sf512-six-step` is corrected in prose in both `MILESTONES.md` and `rekey_ledger.py`'s provenance docstring; the `before` cell of every pre-existing row is untouched."
  prohibitions:
    - "No `#` comment is written into any source or test file this phase touches. Rationale goes in a docstring or in the fixture module's provenance prose."
    - "A ledger row's `before_hash` is never overwritten. The ledger is append-only; a superseded anchor is replaced by a NEW row, never by an edit."
    - "`tests/fixtures/reports/*.json` are never hand-edited. They carry a `_generated_by` do-not-edit banner and a `--check` drift mode."
    - "No hash literal is transcribed from `177-RESEARCH.md`, `MILESTONES.md` prose or this plan into `FROZEN_HASHES` or `rekey_ledger.py`. Every value is produced by a run."
    - "`tests/fixtures/devtest_issue_corpus.json`'s filed rows are never mutated. They are the historical evidence; the re-key mapping is a NEW artifact beside them."
    - "No GitHub issue is edited, closed or commented on. This milestone builds the fixes; it does not work the tracker."
    - "`.claude/skills/devtest-triage/SKILL.md` is not edited in this phase. It reads `dedup_fingerprint` as a dedup key only and reads neither the classification string nor `ladder_state`; D-5/RPT-F2 bind it to Phase 181."
    - "`rekey_ledger.py` stays logic-free. It is parsed by `ast.literal_eval` from the meta side and must contain no evaluable expression."
    - "No file in the `firestarter/` firmware submodule is modified, and no file under `firestarter_app/firestarter/` is modified by this plan."
  artifacts:
    - path: "firestarter_app/tests/fixtures/report_shapes.py"
      provides: "The re-baselined `FROZEN_HASHES`, the re-pointed sst27sf512 pair, and the newly registered `prune03-synthesized-fingerprint-match` builder"
      contains: "prune03-synthesized-fingerprint-match"
      exports: ["_build_prune03_synthesized_fingerprint_match"]
    - path: "firestarter_app/tests/fixtures/shape_ids.json"
      provides: "The committed sorted anchor, updated in the SAME commit that adds the builder"
      contains: "prune03-synthesized-fingerprint-match"
    - path: "firestarter_app/tests/fixtures/rekey_ledger.py"
      provides: "Declared `after_hash` values for the rows this phase moved, plus appended rows for the shapes Phase 174 owned no row for and a re-anchor row for Phase 178"
      contains: "RK-174-07-p177"
    - path: "firestarter_app/tests/fixtures/reports/"
      provides: "16+1 regenerated report snapshots carrying the new fingerprint classification strings"
      contains: "_generated_by"
    - path: ".planning/MILESTONES.md"
      provides: "The GATE-06 declaration rows with before/after and a date, the falsified-projection correction, and the recorded filed-corpus re-key count"
      contains: "RK-174-07-p177"
    - path: ".planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md"
      provides: "The published old-to-new `dedup_fingerprint` mapping for every filed community report this change re-keys"
      contains: "before"
  key_links:
    - from: "firestarter_app/tests/fixtures/rekey_ledger.py"
      to: ".planning/MILESTONES.md"
      via: "tools/rekey/check_rekey_ledger.py binds every declared row to its MILESTONES.md row in both directions"
      pattern: "RK-174-0[1-9]-p17[78]"
    - from: "firestarter_app/tests/fixtures/report_shapes.py"
      to: "firestarter_app/tests/fixtures/shape_ids.json"
      via: "SHAPE_IDS = tuple(sorted(_BUILDERS)) is pinned element-wise against the committed anchor"
      pattern: "prune03-synthesized-fingerprint-match"
    - from: "firestarter_app/tests/fixtures/report_shapes.py"
      to: "firestarter_app/tests/fixtures/reports/"
      via: "tools/snapshot_report_shapes.py regenerates one snapshot per registered shape id"
      pattern: "_generated_by"
---

<objective>
Declare the re-key. Plan `177-01` moved report identities and left the Phase 174 gate red on
purpose; this plan re-baselines every moved value to a MEASURED number, registers the shape Phase
174 reserved for this change, records the declaration in the append-only ledger and in
`MILESTONES.md`, and publishes the mapping for the community reports the change re-keys.

Purpose: `count_agreeing` reads the `dedup_fingerprint` EMBEDDED in a filed issue and never
re-hashes, so this re-key is permanent for the historical corpus. The only thing that separates a
declared re-key from data loss is this record.

Output: a green Phase 174 oracle, a ledger and a `MILESTONES.md` table that agree in both
directions, and a published old-to-new mapping.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-evidence-gated-read-back/177-RESEARCH.md
@.planning/phases/177-evidence-gated-read-back/177-PATTERNS.md
@.planning/phases/177-evidence-gated-read-back/177-DECISIONS.md
@.planning/phases/177-evidence-gated-read-back/177-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Confirm the measured hashes before they become the published record</name>
  <files>.planning/phases/177-evidence-gated-read-back/177-DECISIONS.md</files>
  <decision>
`177-01` produced the measured `after_hash` values; this is the last moment before they are frozen,
declared in an append-only ledger and published as a mapping for reports already filed on a public
tracker. `count_agreeing` reads the `dedup_fingerprint` embedded in a filed issue body and never
re-hashes, so once declared these values cannot be withdrawn -- only superseded by another declared
row. `177-01`'s checkpoint decided the SHAPE of the change; this one confirms the NUMBERS it
actually produced, which did not exist when that decision was taken.

Present, from `.planning/phases/177-evidence-gated-read-back/evidence/177-01-red-capture.txt`: the
`moved=` set with each shape's before and measured-after value; the `unmoved_count=`; whether each
measured value agreed with the projection recorded in `177-RESEARCH.md`
(`at28c256-full-all-ok-sdp` `050ad3830704`, `sst27sf512-full-all-ok` `14d306256076`,
`w27e257-full-all-ok` `3a9f95aba65e`, `gh47-sst27sf512-pass` `1f812aae49ca`,
`sst27sf512-six-step` `7fb88e0b07d6`); the measured `corpus_rekeyed` count against the inherited
projection of 18; and the measured `distinct_arms` count. Name every disagreement explicitly -- a
disagreement is a finding to record in `MILESTONES.md`'s corrections table, never something to
absorb.
  </decision>
  <options>
    - **A (RECOMMENDED): proceed with the measured values as declared.** The measurement is
      authoritative; every projection in `177-RESEARCH.md` and `MILESTONES.md` is a projection of an
      unimplemented change and any disagreement is recorded as a correction.
    - **B: halt and investigate.** Take this if the moved set contains a shape not on the expected
      list, if `gh23-w27e257-fail` moved, if `distinct_arms` cannot be restored to four, or if the
      measured `corpus_rekeyed` count differs from 18 by an amount that suggests the rule was
      applied wrongly rather than that the projection was imprecise.
  </options>
  <resume-signal>`177-DECISIONS.md` carries a `D-177-5` line recording that the measured values were reviewed, the option taken, and every disagreement with a projection named. Resume at Task 2.</resume-signal>
  <read_first>
    - .planning/phases/177-evidence-gated-read-back/evidence/177-01-red-capture.txt
    - .planning/phases/177-evidence-gated-read-back/177-01-SUMMARY.md
    - .planning/phases/177-evidence-gated-read-back/177-RESEARCH.md (the "Measured blast radius" and "Filed-corpus impact" tables, as the expectations to compare against)
  </read_first>
  <reversibility rating="reversible">This task writes one decision line. It changes no code and freezes no value.</reversibility>
  <action>
Read the measured values out of `evidence/177-01-red-capture.txt`, lay them beside the projections
recorded in `177-RESEARCH.md`, and present both columns to the operator with every disagreement
named. Record the answer as a single-line `D-177-5` entry in
`.planning/phases/177-evidence-gated-read-back/177-DECISIONS.md`, listing the option taken, the
moved set as measured, and each disagreement. Do not proceed to Task 2 until it is recorded.
  </action>
  <verify>
    <automated>cd /workspaces && D=.planning/phases/177-evidence-gated-read-back/177-DECISIONS.md && test -s "$D" && /usr/bin/grep -q 'D-177-5' "$D" && ! /usr/bin/grep -qiE '^\s*D-177-5.*(TBD|TODO|undecided)\s*$' "$D"</automated>
    <fails_when>non-zero exit, or `177-DECISIONS.md` carries no `D-177-5` line, or that line's value is a bare TBD/TODO/undecided placeholder</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/177-evidence-gated-read-back/177-DECISIONS.md` carries a single-line `D-177-5` entry naming `Option A` or `Option B`.
    - The entry lists the measured moved set and, for each moved shape, whether the measured value agreed with the projection recorded in `177-RESEARCH.md`.
    - The entry states the measured `corpus_rekeyed` count and whether it equalled 18.
    - Any disagreement is named explicitly rather than described as "as expected".
  </acceptance_criteria>
  <done>The operator has seen the numbers that are about to become a permanent public record, and the review is written down with every disagreement named.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the moved shapes and register the reserved one -- the four-way closure, in one commit</name>
  <files>firestarter_app/tests/fixtures/report_shapes.py, firestarter_app/tests/fixtures/shape_ids.json, firestarter_app/tests/fixtures/reports/, firestarter_app/tests/test_blast_radius_invariance.py</files>
  <read_first>
    - firestarter_app/tests/fixtures/report_shapes.py (lines 108-160 for `build_shape_from_step_specs`'s `(op, verdict, fingerprint_classification, reason)` grammar and its `run_counts`/`coverage_policy` seams; 178-220 for the two hand-specified sst27sf512 builders and their docstrings; 520-596 for the real-path builders; 599-655 for `_BUILDERS`, `SHAPE_IDS`, `FROZEN_HASHES`, `RESERVED_SHAPE_IDS`, the collision assert and `build_shape`)
    - firestarter_app/tests/fixtures/shape_ids.json (the `_note` stating the same-commit rule)
    - firestarter_app/tests/test_blast_radius_invariance.py (lines 190-250 for `_TRACER_SHAPE_ID` and `LADDER_PINS`; 322-390 for `test_build_db_diff_ladder_pin_for_tracer_shape`, the parametrized ladder pin and the four-arm sentinel; 699-760 for the anchor test, the four-way agreement test and the reserved-id test)
    - firestarter_app/tools/snapshot_report_shapes.py (lines 1-40 for the `--check` drift mode and the `_generated_by` banner)
    - .planning/phases/177-evidence-gated-read-back/evidence/177-01-red-capture.txt (the measured moved set, the measured after-values and the measured `distinct_arms` count)
    - .planning/phases/177-evidence-gated-read-back/177-DECISIONS.md (D-177-2 and D-177-3)
  </read_first>
  <precondition>`177-DECISIONS.md` records D-177-2 and D-177-3, and Task 1's confirmation is recorded. The shape set to re-point depends on both.</precondition>
  <reversibility rating="costly">A frozen-hash literal is a test fixture and reverts cleanly; the report identity it records already moved in `177-01`'s behaviour commit. The cost of a revert is a full re-measurement, not a lost record.</reversibility>
  <action>
One commit in the `firestarter_app` sub-repo on `gsd/v1.36-dev-test-fidelity`, separate from
`177-01`'s behaviour commit. Write no `#` comment anywhere; the fixture module records rationale in
builder docstrings and in its module docstring, and that is the home for every "why" below.

**Re-baseline `FROZEN_HASHES` from the run, not from a document.** For each shape id listed on the
`moved=` line of `evidence/177-01-red-capture.txt`, replace its 12-hex string literal with the value
produced by `dedup_fingerprint(build_shape(shape_id))` in `firestarter_app/.venv311`. The four
shapes the behaviour commit moved on the real path are `at28c256-full-all-ok-sdp` (currently
`52fb759dc48c`), `sst27sf512-full-all-ok` (`4b3e52cab987`), `w27e257-full-all-ok` (`22908e2954c3`)
and `gh47-sst27sf512-pass` (`f9dbc31dcd27`). `177-RESEARCH.md` projects `050ad3830704`,
`14d306256076`, `3a9f95aba65e` and `1f812aae49ca` for them -- those are EXPECTATIONS to compare
against and to flag if they disagree, never the values to write. Leave every other entry byte-identical,
in particular `gh23-w27e257-fail` at `7a89fcea856a`, `gh20-at28c256-fail` at `00e121446ceb`,
`gh28-m27c512-fail` at `31547956e56b`, `synthetic-arm4-no-ok` at `f90dfe1a44f7`,
`synthetic-arm4-empty-results` at `8d6208d00be7`, `m27c512-full-all-ok` at `6d3afbc52315`,
`m27c512-full-blank-check-bad` at `077a32d1a5c4`, `m27c512-full-canonical-name` at `776846bf2dc8`,
`m27c512-full-comma-joined-name` at `37ad34d39a19` and `m27c512-full-runs-1` at `e4838f7bb1d3`.

**Re-point the two hand-specified sst27sf512 builders (D-177-3 Option A).** In
`_build_sst27sf512_six_step` (`:178-193`), change the `write` and `verify` `step_specs` slot-3 value
from `indeterminate` to `match`; a passing write can no longer produce `indeterminate` after
PRUNE-03, so the shape as a model of a passing run is otherwise falsified. That edit IS `RK-174-01`'s
declared re-key. In `_build_sst27sf512_six_step_readback_gated` (`:197-219`), re-point the shape to
the gate's OTHER branch: a step that FAILED, kept its real read-back and classified `indeterminate`.
Use verdict `marginal` for the `write` and `verify` specs -- never `BAD`, which routes
`build_db_diff` to the community-fail arm -- with slot-3 `indeterminate`, so the shape lands on the
INCONCLUSIVE arm and re-populates it. Rewrite BOTH docstrings: the gated one currently describes
itself as the projected after-shape of `RK-174-01` under a reading PRUNE-03 forbids, and that prose
must be replaced by what the shape now pins. If `177-DECISIONS.md` recorded Option B or C for
D-177-3, follow the option recorded there instead and say so in the SUMMARY.

**Register `prune03-synthesized-fingerprint-match` in all four places at once.** Add
`_build_prune03_synthesized_fingerprint_match`, a hand-specified `build_shape_from_step_specs`
builder distinct from every existing shape -- use a chip and protocol no other builder uses so its
hash cannot collide -- whose `write` and `verify` specs carry verdict `OK` and slot-3 `match`. Its
docstring states what it pins: the exact lowercase `match` literal inside `dedup_fingerprint`'s
pre-image grammar, independent of the engine, so a later phase that respells the bucket reddens here
first. Then, in the same commit: add it to `_BUILDERS`; add its measured hash to `FROZEN_HASHES`;
add its measured `(proposed_disposition, ladder_state)` pair to `LADDER_PINS`; add its id to
`tests/fixtures/shape_ids.json`'s sorted `shape_ids` array; and REMOVE it from `RESERVED_SHAPE_IDS`,
or the `assert not (set(SHAPE_IDS) & RESERVED_SHAPE_IDS)` at `:647-650` fires by design.
`test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree` enforces all four at once, so a
partial edit fails loudly.

**Update the two ladder assertions the D-4/D-6 flip moves.** `LADDER_PINS`'s INCONCLUSIVE arm holds
exactly three members today (`at28c256-full-all-ok-sdp`, `gh47-sst27sf512-pass`,
`sst27sf512-six-step`) and all three flip to the candidate/`community-reported` pair, which drops the
distinct-pair count to three and reddens `test_ladder_pins_cover_all_four_build_db_diff_arms`.
Update each moved shape's `LADDER_PINS` entry to its measured pair, and confirm the re-pointed
`sst27sf512-six-step-readback-gated` restores the arm to four distinct pairs. Update
`test_build_db_diff_ladder_pin_for_tracer_shape` (`:322-333`), whose two assertions name the
inconclusive disposition constant and its literal text, to the tracer shape's measured post-177 arm;
keep the triple-equality idiom (computed == constant == literal) intact.

**Regenerate the snapshots, never hand-edit them.** Run
`./.venv311/bin/python tools/snapshot_report_shapes.py` then
`./.venv311/bin/python tools/snapshot_report_shapes.py --check`. There will be 17 snapshots after
this task: the 16 existing plus the newly registered shape.

Commit message `test(177-02): re-baseline the frozen shapes PRUNE-03 moved and register the reserved
match shape`, body naming PRUNE-03, D-4/D-6 and D-177-3, stating the measured before/after for every
moved shape and stating that the ledger declaration follows in the next commit per D-11.
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/177-evidence-gated-read-back/evidence && mkdir -p "$E" && EV=$E/177-02-rebaseline.txt && { echo "== 1 every frozen hash is a fresh build"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter.diagnostic_report import dedup_fingerprint
from tests.fixtures.report_shapes import FROZEN_HASHES, RESERVED_SHAPE_IDS, SHAPE_IDS, build_shape
bad = [s for s in SHAPE_IDS if dedup_fingerprint(build_shape(s)) != FROZEN_HASHES[s]]
print('stale_frozen=' + ','.join(sorted(bad)))
print('shape_count=' + str(len(SHAPE_IDS)))
print('hashes_pairwise_distinct=' + str(len(set(FROZEN_HASHES.values())) == len(FROZEN_HASHES)))
print('new_shape_registered=' + str('prune03-synthesized-fingerprint-match' in SHAPE_IDS))
print('new_shape_unreserved=' + str('prune03-synthesized-fingerprint-match' not in RESERVED_SHAPE_IDS))
print('gh23_unmoved=' + str(FROZEN_HASHES['gh23-w27e257-fail'] == '7a89fcea856a'))
unmoved = {'gh20-at28c256-fail': '00e121446ceb', 'gh28-m27c512-fail': '31547956e56b', 'synthetic-arm4-no-ok': 'f90dfe1a44f7', 'synthetic-arm4-empty-results': '8d6208d00be7', 'm27c512-full-all-ok': '6d3afbc52315', 'm27c512-full-blank-check-bad': '077a32d1a5c4', 'm27c512-full-canonical-name': '776846bf2dc8', 'm27c512-full-comma-joined-name': '37ad34d39a19', 'm27c512-full-runs-1': 'e4838f7bb1d3'}
print('unmoved_drifted=' + ','.join(sorted(k for k, v in unmoved.items() if FROZEN_HASHES[k] != v)))
print('gated_off_stale_projection=' + str(FROZEN_HASHES['sst27sf512-six-step-readback-gated'] != '60a031573aab'))
print('gated_distinct_from_tracer=' + str(FROZEN_HASHES['sst27sf512-six-step-readback-gated'] != FROZEN_HASHES['sst27sf512-six-step']))
"); echo "rc=$?"; echo "== 2 the four-arm sentinel and the anchor"; (cd firestarter_app && ./.venv311/bin/python -c "
import json, pathlib, sys
sys.path.insert(0, '.')
from tests.fixtures.report_shapes import SHAPE_IDS
from tests.test_blast_radius_invariance import LADDER_PINS
print('distinct_arms=' + str(len(set(LADDER_PINS.values()))))
print('ladder_keys_match_shapes=' + str(set(LADDER_PINS) == set(SHAPE_IDS)))
anchor = json.loads(pathlib.Path('tests/fixtures/shape_ids.json').read_text(encoding='utf-8'))['shape_ids']
print('anchor_matches=' + str(anchor == sorted(SHAPE_IDS)))
"); echo "rc=$?"; echo "== 3 snapshots regenerate clean"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; echo "snapshot_count=$(ls firestarter_app/tests/fixtures/reports/*.json | wc -l)"; echo "== 4 the 174 oracle"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q tests/test_blast_radius_invariance.py tests/test_devtest_issue_corpus.py tests/test_diagnostic_report.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 4 && /usr/bin/grep -qx 'stale_frozen=' "$EV" && /usr/bin/grep -qx 'shape_count=17' "$EV" && /usr/bin/grep -qx 'hashes_pairwise_distinct=True' "$EV" && /usr/bin/grep -qx 'new_shape_registered=True' "$EV" && /usr/bin/grep -qx 'new_shape_unreserved=True' "$EV" && /usr/bin/grep -qx 'gh23_unmoved=True' "$EV" && /usr/bin/grep -qx 'unmoved_drifted=' "$EV" && /usr/bin/grep -qx 'gated_off_stale_projection=True' "$EV" && /usr/bin/grep -qx 'gated_distinct_from_tracer=True' "$EV" && /usr/bin/grep -qx 'distinct_arms=4' "$EV" && /usr/bin/grep -qx 'ladder_keys_match_shapes=True' "$EV" && /usr/bin/grep -qx 'anchor_matches=True' "$EV" && /usr/bin/grep -qx 'snapshot_count=17' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>non-zero exit; or fewer than four `rc=0` lines; or `stale_frozen=` lists any shape id (a frozen value that a fresh build does not reproduce); or `unmoved_drifted=` lists any shape id (a value moved that must not); or `gh23_unmoved=False`; or `distinct_arms=` reports anything other than `4`; or `shape_count=` / `snapshot_count=` reports anything other than `17`; or `anchor_matches=False`; or `no tests ran` / an `N failed` / `N error` line appears</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && test -z "$(git diff HEAD~1 --name-only -- firestarter/)" && git diff HEAD~1 --quiet -- tests/fixtures/devtest_issue_corpus.json && test "$(git diff HEAD~1 --name-only -- tests/fixtures/reports/ | wc -l)" -ge 5 && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/</automated>
    <fails_when>non-zero exit; or the commit's diff over `tests/` adds a line whose first non-blank character opens a comment; or any path under `firestarter/` appears in this commit; or `tests/fixtures/devtest_issue_corpus.json` is modified; or fewer than five snapshot files changed, which would mean the regenerator did not run; or `ruff check` / `ruff format --check` reports a finding</fails_when>
  </verify>
  <acceptance_criteria>
    - For every shape id in `SHAPE_IDS`, `dedup_fingerprint(build_shape(shape_id)) == FROZEN_HASHES[shape_id]` -- the `stale_frozen=` line is empty.
    - `len(SHAPE_IDS) == 17` and `ls tests/fixtures/reports/*.json | wc -l` is 17.
    - `FROZEN_HASHES` values are pairwise distinct.
    - `FROZEN_HASHES['gh23-w27e257-fail'] == '7a89fcea856a'`, and each of the nine other named unmoved shapes keeps its committed literal -- the `unmoved_drifted=` line is empty.
    - `'prune03-synthesized-fingerprint-match' in SHAPE_IDS` and it is absent from `RESERVED_SHAPE_IDS`.
    - `json.load(shape_ids.json)['shape_ids'] == sorted(SHAPE_IDS)`.
    - `set(LADDER_PINS) == set(SHAPE_IDS)` and `len(set(LADDER_PINS.values())) == 4`.
    - `FROZEN_HASHES['sst27sf512-six-step-readback-gated']` is neither `60a031573aab` nor equal to `FROZEN_HASHES['sst27sf512-six-step']`.
    - `tools/snapshot_report_shapes.py --check` exits 0.
    - `tests/test_blast_radius_invariance.py`, `tests/test_devtest_issue_corpus.py` and `tests/test_diagnostic_report.py` all pass.
    - The commit touches no path under `firestarter_app/firestarter/` and does not modify `tests/fixtures/devtest_issue_corpus.json`.
    - The commit's `tests/` diff adds zero comment-opening lines.
  </acceptance_criteria>
  <done>Every moved frozen hash is a measured value, the reserved shape is registered across all four registries, the four ladder arms are all still populated, the 17 snapshots regenerate clean, and the ledger has not been touched yet.</done>
</task>

<task type="auto">
  <name>Task 3: Declare it in the ledger -- fill the rows this phase moved, append the ones Phase 174 owned none for, re-anchor Phase 178</name>
  <files>firestarter_app/tests/fixtures/rekey_ledger.py, firestarter_app/tests/test_rekey_ledger.py</files>
  <read_first>
    - firestarter_app/tests/fixtures/rekey_ledger.py (the whole file: the module docstring's per-row provenance paragraphs, the append-only rule, and the six-row `LEDGER` tuple with its `(shape_id, before_hash, after_hash, ledger_id)` grammar)
    - firestarter_app/tests/test_rekey_ledger.py (lines 54-90 for the recompute sweep and the ledger-id grammar; 295-380 for `test_ledger_has_exactly_six_pre_seeded_rows`, `test_ledger_id_values_are_unique`, `test_shape_id_ledger_id_pairs_are_unique`, `test_no_declared_row_has_after_hash_equal_to_before_hash`, `test_undeclared_after_hash_routes_to_before_hash_and_never_abstains` and `test_ledger_id_order_is_ascending`)
    - /workspaces/tools/rekey/check_rekey_ledger.py (lines 1-60 for the both-directions binding, the `(undeclared)` literal rule and the duplicate-row rejection)
    - .planning/MILESTONES.md (lines 14-45)
    - .planning/phases/177-evidence-gated-read-back/evidence/177-02-rebaseline.txt (the measured values Task 2 froze)
  </read_first>
  <precondition>Task 1's confirmation of the measured `after_hash` values is recorded in `177-DECISIONS.md`. The ledger is append-only (D-09) and a declared row cannot be withdrawn.</precondition>
  <reversibility rating="one-way">A declared ledger row is the permanent record of a hash that already moved: D-09 forbids overwriting a `before` cell and Phase 178 measures from the new anchor, so the row cannot be withdrawn, only superseded by another row. Gated by Task 1.</reversibility>
  <action>
One commit in the `firestarter_app` sub-repo, separate from Task 2's. Write no `#` comment; the
ledger's own home for rationale is its module docstring, one paragraph per row, and it must stay
logic-free because the meta-side checker parses it with `ast.literal_eval` and never imports it.

**Fill `after_hash` for the three rows whose shape this phase moved.** Take each value from the now
re-baselined `FROZEN_HASHES`, never from a document:
`RK-174-01-p177-readback-gating` (`sst27sf512-six-step`, before `4dc282a5d596`);
`RK-174-05-p177-match-bucket-d4d6` (`at28c256-full-all-ok-sdp`, before `52fb759dc48c`); and
`RK-174-06-p178-status-axis-must-not-rekey` (`sst27sf512-full-all-ok`, before `4b3e52cab987`).
The third is not optional and is the non-obvious one: `test_every_ledger_row_shape_id_resolves_and_recomputes`
asserts an undeclared row's `before_hash` still reproduces from a fresh build, so leaving
`RK-174-06` undeclared after its shape moved makes that test permanently red. Never touch any
`before` cell. If `177-DECISIONS.md` recorded D-177-2 Option B, `at28c256-full-all-ok-sdp` did not
move and `RK-174-05` stays undeclared; if it recorded D-177-3 Option B, `sst27sf512-six-step` did not
move and `RK-174-01` stays undeclared. Declare exactly the rows whose shapes the measurement shows
moved, and state in the SUMMARY which rows those were and why.

**Append the rows Phase 174 owned none for.** In ascending `ledger_id` order, following the
`RK-174-<NN>-<owner>-<slug>` grammar:
`("w27e257-full-all-ok", "22908e2954c3", <measured>, "RK-174-07-p177-w27e257-all-ok-synthesized-match")`
and
`("gh47-sst27sf512-pass", "f9dbc31dcd27", <measured>, "RK-174-08-p177-gh47-filed-hash-synthesized-match")`.
The second is a filed community hash and the reason the mapping in Task 4 must be published.

**Append the re-anchor row for Phase 178.**
`("sst27sf512-full-all-ok", <RK-174-06's newly measured after value>, None, "RK-174-09-p178-status-axis-must-not-rekey-reanchored")`.
`RK-174-06`'s entire purpose is to give ATTR-04 something to confirm against, and Phase 177 moved
the baseline it was anchored to. Appending re-anchors that assertion at the new value instead of
violating it; `test_shape_id_ledger_id_pairs_are_unique`'s own docstring states that two rows MAY
legitimately name the same `shape_id` and that only the `ledger_id` must be unique.

**Add one provenance paragraph per new row and correct one existing paragraph.** Match the voice of
the existing paragraphs at `:27-34`: shape id, owner, mechanism, and what the row asserts. Then
correct `RK-174-01`'s paragraph: the value it names as this phase's projected `after_hash` models
the fingerprint being DROPPED on a pass, which PRUNE-03 forbids, and the measurement in
`evidence/177-01-red-capture.txt` falsifies it. Say so in prose, name the measured value, and keep
the falsified number visible as the superseded claim rather than deleting it.

**Update the three ledger tests whose literals this declaration invalidates** -- all three tell you
so in their own text. `test_ledger_has_exactly_six_pre_seeded_rows` asserts `len(LEDGER) == 6`;
change the expected count to the new row count and rewrite its message so it still names what a
silent deletion or an unreviewed addition would change.
`test_undeclared_after_hash_routes_to_before_hash_and_never_abstains` asserts every row is
undeclared and its docstring says "update it when a row is first declared"; narrow its sweep to the
rows whose `after_hash` is `None`, keep its never-abstains property, and add a guard so it fails
rather than passing vacuously if that subset is empty. `test_ledger_id_order_is_ascending` needs no
change if the appended ids are in order -- confirm rather than assume.

Commit message `test(177-02): declare the PRUNE-03 re-key in the ledger and re-anchor Phase 178`,
body naming GATE-06, D-4/D-6, D-09 and D-11, listing every row filled or appended with its
before/after, and stating that the meta-repo half lands next.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/177-evidence-gated-read-back/evidence/177-02-ledger.txt && { echo "== 1 the ledger's own arithmetic"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter.diagnostic_report import dedup_fingerprint
from tests.fixtures.rekey_ledger import LEDGER
from tests.fixtures.report_shapes import FROZEN_HASHES, build_shape
print('rows=' + str(len(LEDGER)))
ids = [r[3] for r in LEDGER]
print('ids_unique=' + str(len(set(ids)) == len(ids)))
print('ids_ascending=' + str(ids == sorted(ids)))
print('pairs_unique=' + str(len({(r[0], r[3]) for r in LEDGER}) == len(LEDGER)))
print('declared=' + ','.join(sorted(r[3] for r in LEDGER if r[2] is not None)))
print('undeclared=' + ','.join(sorted(r[3] for r in LEDGER if r[2] is None)))
bad = []
for shape_id, before, after, lid in LEDGER:
    expected = after if after is not None else before
    if dedup_fingerprint(build_shape(shape_id)) != expected:
        bad.append(lid)
    if after is not None and after == before:
        bad.append(lid + ':no-move')
    if after is not None and after != FROZEN_HASHES[shape_id]:
        bad.append(lid + ':not-frozen-value')
print('inconsistent=' + ','.join(sorted(bad)))
print('logic_free=' + str(all(isinstance(x, (str, type(None))) for r in LEDGER for x in r))) 
"); echo "rc=$?"; echo "== 2 no before cell was overwritten"; git -C firestarter_app show HEAD~2:tests/fixtures/rekey_ledger.py > /tmp/177-02-ledger-before.py 2>/dev/null; (cd firestarter_app && ./.venv311/bin/python -c "
import ast, pathlib
def load(p):
    tree = ast.parse(pathlib.Path(p).read_text(encoding='utf-8'))
    for node in tree.body:
        if isinstance(node, ast.Assign) and getattr(node.targets[0], 'id', '') == 'LEDGER':
            return ast.literal_eval(node.value)
    raise SystemExit('no LEDGER')
old = {r[3]: (r[0], r[1]) for r in load('/tmp/177-02-ledger-before.py')}
new = {r[3]: (r[0], r[1]) for r in load('tests/fixtures/rekey_ledger.py')}
print('before_cells_overwritten=' + ','.join(sorted(k for k in old if k in new and old[k] != new[k])))
print('rows_removed=' + ','.join(sorted(set(old) - set(new))))
"); echo "rc=$?"; echo "== 3 the ledger test module"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q tests/test_rekey_ledger.py tests/test_blast_radius_invariance.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 3 && /usr/bin/grep -qx 'inconsistent=' "$EV" && /usr/bin/grep -qx 'ids_unique=True' "$EV" && /usr/bin/grep -qx 'ids_ascending=True' "$EV" && /usr/bin/grep -qx 'pairs_unique=True' "$EV" && /usr/bin/grep -qx 'logic_free=True' "$EV" && /usr/bin/grep -qx 'before_cells_overwritten=' "$EV" && /usr/bin/grep -qx 'rows_removed=' "$EV" && /usr/bin/grep -qE '^rows=[89]$' "$EV" && /usr/bin/grep -qE '^declared=.*RK-174-0' "$EV" && /usr/bin/grep -q 'RK-174-09-p178-status-axis-must-not-rekey-reanchored' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>non-zero exit; or fewer than three `rc=0` lines; or `inconsistent=` lists any ledger id (a row whose expected value does not reproduce from a fresh build, or whose `after_hash` equals its `before_hash`, or whose `after_hash` disagrees with `FROZEN_HASHES`); or `before_cells_overwritten=` or `rows_removed=` lists anything; or `ids_ascending=False` / `pairs_unique=False` / `logic_free=False`; or the row count is outside 8-9; or the re-anchor ledger id is absent; or `no tests ran` / an `N failed` / `N error` line appears</fails_when>
  </verify>
  <acceptance_criteria>
    - `len(LEDGER)` is 9 (or 8 if D-177-2/D-177-3 Option B removed one declaration), every `ledger_id` is unique, matches the `RK-174-<NN>-<owner>-<slug>` grammar and the list is in ascending order.
    - For every row, `dedup_fingerprint(build_shape(shape_id))` equals `after_hash` when declared and `before_hash` when not.
    - No declared row has `after_hash == before_hash`, and every declared `after_hash` equals the corresponding `FROZEN_HASHES` value.
    - `RK-174-07-p177-w27e257-all-ok-synthesized-match` and `RK-174-08-p177-gh47-filed-hash-synthesized-match` exist with `before_hash` `22908e2954c3` and `f9dbc31dcd27` respectively.
    - `RK-174-09-p178-status-axis-must-not-rekey-reanchored` exists, names `sst27sf512-full-all-ok`, has `after_hash` `None` and a `before_hash` equal to `RK-174-06`'s newly declared `after_hash`.
    - Comparing the parsed `LEDGER` against the pre-plan revision shows zero rows removed and zero `(shape_id, before_hash)` pairs changed for a pre-existing `ledger_id`.
    - `LEDGER` contains only `str` and `None` values -- no evaluable expression.
    - The `RK-174-01` provenance paragraph names the measured value AND keeps the superseded projection visible as a falsified claim.
    - `tests/test_rekey_ledger.py` and `tests/test_blast_radius_invariance.py` both pass.
  </acceptance_criteria>
  <done>Every hash this phase moved is declared in the append-only ledger against a measured value, the shapes Phase 174 owned no row for have rows, Phase 178's must-not-re-key assertion is re-anchored at the new baseline, and no `before` cell was overwritten.</done>
</task>

<task type="auto">
  <name>Task 4: The meta half -- MILESTONES.md, the falsified projection, and the published mapping</name>
  <files>.planning/MILESTONES.md, .planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md</files>
  <read_first>
    - .planning/MILESTONES.md (lines 14-45: the corrections table, the six-row ledger table with its `ledger_id | shape_id | change | owner | before | after | declared` columns, and the D-11 protocol paragraph)
    - /workspaces/tools/rekey/check_rekey_ledger.py (the row regex, the `(undeclared)` exact-literal rule, the duplicate-`ledger_id` rejection and the zero-rows rejection)
    - firestarter_app/tests/fixtures/rekey_ledger.py (as committed by Task 3 -- the authoritative side)
    - .planning/phases/177-evidence-gated-read-back/evidence/177-01-red-capture.txt (the measured `corpus_rekeyed` count and the `corpus_map=` lines)
    - .planning/phases/177-evidence-gated-read-back/177-DECISIONS.md (D-177-4)
    - .planning/REQUIREMENTS.md (GATE-06, D-4 + D-6)
  </read_first>
  <precondition>D-177-4 is recorded in `177-DECISIONS.md` and Task 1's confirmation is recorded. Publishing a mapping for externally filed reports is not withdrawable by a revert.</precondition>
  <reversibility rating="one-way">Publishing an old-to-new mapping for reports already filed on a public tracker is a statement about external data that cannot be withdrawn by a revert. Gated by Task 1.</reversibility>
  <action>
One commit in the META repo (`/workspaces`), landing together with Task 3's sub-repo commit. D-11's
"same commit" is per tree -- the ledger and `MILESTONES.md` are in different git repositories, and
the cross-tree binding is enforced by running the checker, not by a single commit.

**Declare each row in `MILESTONES.md`'s ledger table.** For every row Task 3 declared, replace that
row's `(undeclared)` cell with the measured hash and its `(pending)` cell with today's date. The
checker compares `(shape_id, before, after)` exactly and rejects an off-by-one-character truncation
or an uppercase hash, so transcribe from the committed ledger, not from prose. Add one new table row
for each row Task 3 appended, with the same seven columns. For `RK-174-06`, amend only its `change`
cell prose to record that the shape was re-keyed by Phase 177's fingerprint synthesis rather than by
Phase 178's status axis, and that ATTR-04's assertion is re-anchored at
`RK-174-09-p178-status-axis-must-not-rekey-reanchored`; leave its `shape_id` and `before` cells
untouched.

**Correct the falsified projection.** The paragraph after the table names a projected `after_hash`
for `sst27sf512-six-step` measured by emptying the write/verify classification -- the reading
PRUNE-03 forbids. Rewrite it to state the measured value, name the superseded projection as
falsified, and add a row to the "Corrections measured by plan 174-03" table in the same voice as its
existing rows (Claim / As inherited / As measured). This project has a standing rule against
silently absorbing a measured disagreement and that table is its home. Record in the same place
whether `evidence/177-01-red-capture.txt`'s measured `corpus_rekeyed` count agreed with the
inherited projection of 18 rows; if it did not, that disagreement is a correction row of its own.

**Record the filed-corpus re-key (GATE-06).** State the measured count and the ascending list of
issue numbers in `MILESTONES.md`, and state plainly what it means: `count_agreeing` reads the
`dedup_fingerprint` embedded in the filed issue body and never re-hashes, so the re-key is permanent
for the historical corpus and no migration is possible. Per D-177-4 Option A, write the full
old-to-new mapping to `.planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md` as a
markdown table with one row per re-keyed issue carrying issue number, chip, before hash and after
hash, sourced from the `corpus_map=` lines of `evidence/177-01-red-capture.txt`. State in that file
that `tests/fixtures/devtest_issue_corpus.json` is unchanged and remains the historical evidence,
and that gh#39 and gh#40 shared a hash before and must be checked as still sharing one after -- a
dedup group that survives intact, merely re-keyed. Edit no GitHub issue. If D-177-4 recorded Option
B, write the count and the issue list into `MILESTONES.md` only and omit the mapping file, saying so
in the SUMMARY.

**Run the binding checker as the acceptance evidence:**
`python3 tools/rekey/check_rekey_ledger.py` from `/workspaces` against the populated
`firestarter_app` submodule. It fails closed on a missing path, an unparsable ledger, a duplicated
row, an orphan on either side and a `MILESTONES.md` carrying zero `RK-174-` rows.

Commit message `docs(177-02): declare the PRUNE-03 re-key and publish the filed-corpus mapping`,
body naming GATE-06, D-4/D-6, D-11 and D-177-4.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/177-evidence-gated-read-back/evidence/177-02-cross-tree.txt && { echo "== 1 the cross-tree binding"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; echo "== 2 the table's own shape"; echo "milestones_rk_rows=$(/usr/bin/grep -cE '^\| RK-174-[^|]+\|' .planning/MILESTONES.md)"; echo "still_undeclared=$(/usr/bin/grep -cE '^\| RK-174-[^|]+\|.*\(undeclared\)' .planning/MILESTONES.md)"; echo "still_pending=$(/usr/bin/grep -cE '^\| RK-174-[^|]+\|.*\(pending\)' .planning/MILESTONES.md)"; echo "reanchor_row=$(/usr/bin/grep -c 'RK-174-09-p178-status-axis-must-not-rekey-reanchored' .planning/MILESTONES.md)"; echo "corrections_rows=$(sed -n '/^| Claim | As inherited | As measured |/,/^$/p' .planning/MILESTONES.md | /usr/bin/grep -cE '^\|')"; echo "== 3 the published mapping"; M=.planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md; echo "mapping_present=$([ -s "$M" ] && echo yes || echo no)"; echo "mapping_rows=$(/usr/bin/grep -cE '^\|[[:space:]]*[0-9]+[[:space:]]*\|' "$M" 2>/dev/null || echo 0)"; echo "corpus_untouched=$(git -C firestarter_app diff --quiet HEAD -- tests/fixtures/devtest_issue_corpus.json && echo yes || echo no)"; echo "== 4 the app-side oracle is green"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q tests/test_rekey_ledger.py tests/test_blast_radius_invariance.py tests/test_devtest_issue_corpus.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 2 && test "$(/usr/bin/grep -oE '^milestones_rk_rows=[0-9]+' "$EV" | cut -d= -f2)" -ge 8 && /usr/bin/grep -qx 'reanchor_row=1' "$EV" && test "$(/usr/bin/grep -oE '^still_undeclared=[0-9]+' "$EV" | cut -d= -f2)" -le 4 && /usr/bin/grep -qx 'mapping_present=yes' "$EV" && test "$(/usr/bin/grep -oE '^mapping_rows=[0-9]+' "$EV" | cut -d= -f2)" -ge 15 && /usr/bin/grep -qx 'corpus_untouched=yes' "$EV" && test "$(/usr/bin/grep -oE '^corrections_rows=[0-9]+' "$EV" | cut -d= -f2)" -ge 7 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>non-zero exit; or the checker printed an `ERROR:` line and exited non-zero; or fewer than eight `RK-174-` rows in `MILESTONES.md`; or the re-anchor row is absent; or more than four rows still carry `(undeclared)`; or the mapping file is missing/empty or carries fewer than 15 issue rows; or `corpus_untouched=no`; or the corrections table did not gain at least one row beyond its committed six; or `no tests ran` / an `N failed` / `N error` line appears</fails_when>
  </verify>
  <acceptance_criteria>
    - `python3 tools/rekey/check_rekey_ledger.py` run from `/workspaces` exits 0 and prints no `ERROR:` line.
    - `MILESTONES.md` carries at least 8 `RK-174-` table rows, one per ledger row, with matching `shape_id` and `before` cells.
    - Every row Task 3 declared shows a 12-lowercase-hex `after` cell and a real date in its `declared` cell -- neither is the literal `(undeclared)` or `(pending)`.
    - `RK-174-06`'s `shape_id` and `before` cells are byte-unchanged; only its `change` prose gained the re-anchor note.
    - A row for `RK-174-09-p178-status-axis-must-not-rekey-reanchored` exists in `MILESTONES.md`.
    - The "Corrections measured by plan 174-03" table gained at least one row recording the falsified `sst27sf512-six-step` projection, and records whether the measured filed-corpus count agreed with 18.
    - `.planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md` exists with one table row per re-keyed filed issue, each carrying issue number, chip, before hash and after hash.
    - The mapping file states explicitly that `devtest_issue_corpus.json` is unchanged and that gh#39 and gh#40's shared dedup group survives intact.
    - `git -C firestarter_app diff --quiet HEAD -- tests/fixtures/devtest_issue_corpus.json` succeeds.
    - `tests/test_rekey_ledger.py`, `tests/test_blast_radius_invariance.py` and `tests/test_devtest_issue_corpus.py` all pass.
  </acceptance_criteria>
  <done>The declaration exists on both sides of the repo boundary, the checker binds them, the falsified projection is corrected in the record rather than absorbed, and the community reports this change re-keys have a published old-to-new mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host to public issue tracker | The `dedup_fingerprint` values this plan re-baselines are already embedded in filed issue bodies on `henols/firestarter_prom`. `count_agreeing` reads the embedded value and never re-hashes, so the tracker's copy and this repo's copy diverge permanently the moment a hash moves. |
| meta repo to app sub-repo | `.planning/MILESTONES.md` and `firestarter_app/tests/fixtures/rekey_ledger.py` are in different git repositories and can only be checked against each other from the meta side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-177-07 | Repudiation | the declared `after_hash` values | high | mitigate | A row declared on one side and never on the other lets a re-key exist with no record. Mitigated by `tools/rekey/check_rekey_ledger.py`, run as this plan's acceptance evidence, which binds both trees in both directions and fails closed on a missing path, an unparsable ledger, a duplicated row, an orphan on either side and a zero-row `MILESTONES.md`. |
| T-177-08 | Tampering | a transcribed rather than measured hash | high | mitigate | Every acceptance criterion here compares `FROZEN_HASHES` and the ledger's `after_hash` against a freshly computed `dedup_fingerprint(build_shape(...))`; the projections in `177-RESEARCH.md` are named as expectations to flag, never as values to write. |
| T-177-09 | Denial of information | the historical dedup corpus | high | mitigate | 18-ish filed reports lose their key. No migration is possible -- the issues are external. Mitigated by publishing the old-to-new mapping as a committed artifact, and by asserting `devtest_issue_corpus.json` is unchanged so the historical evidence survives beside the mapping. |
| T-177-10 | Tampering | the append-only ledger's `before` cells | high | mitigate | Overwriting a `before` cell destroys the evidence that the change was exactly what it declared. Asserted structurally by parsing the pre-plan revision of `rekey_ledger.py` with `ast.literal_eval` and diffing the `(shape_id, before_hash)` pairs per `ledger_id`. |
| T-177-11 | Tampering | `rekey_ledger.py` as a parser input | medium | mitigate | The meta-side checker parses this file from another tree. Keeping it logic-free -- plain tuples of `str`/`None`, asserted by test -- is what makes `ast.literal_eval` safe and is why it is never imported. |
| T-177-12 | Information Disclosure | the published mapping file | low | accept | It carries only issue numbers, chip names and non-secret 12-hex dedup ids, all of which are already public in the filed issues themselves. |
| T-177-SC | Tampering | package-manager installs | high | accept | This plan installs nothing. HYG-02 forbids a new runtime dependency and RESEARCH.md's Package Legitimacy Audit records zero packages to audit, so no `[ASSUMED]` / `[SUS]` verdict exists and no legitimacy checkpoint is required. |
</threat_model>

<verification>
Run from `/workspaces` unless a leg says otherwise. Every Python invocation uses
`firestarter_app/.venv311` (CI is py3.11 only; the devcontainer default 3.12 masks app CI). Every
grep treated as evidence uses `/usr/bin/grep`, because the `grep` on PATH is ugrep and honors
`.gitignore`.

1. `stale_frozen=` is empty -- every frozen hash reproduces from a fresh build.
2. `unmoved_drifted=` is empty and `gh23_unmoved=True` -- the re-key is exactly what it declared.
3. `shape_count=17`, `snapshot_count=17`, `anchor_matches=True`, `distinct_arms=4`.
4. `inconsistent=`, `before_cells_overwritten=` and `rows_removed=` are all empty.
5. `python3 tools/rekey/check_rekey_ledger.py` exits 0 from `/workspaces`.
6. `cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q
   tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_devtest_issue_corpus.py
   tests/test_diagnostic_report.py` passes.
7. `.planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md` exists with one row per
   re-keyed filed issue.
8. Three separate commits landed: two in `firestarter_app`, one in the meta repo. None of them is
   the behaviour commit from `177-01`.
</verification>

<success_criteria>
- Every hash PRUNE-03 moved is re-baselined to a measured value and declared in both trees.
- Nothing that should not have moved, moved -- provable by the ten named unmoved literals.
- The reserved shape id Phase 174 set aside for this change is registered across all four registries
  and the four `build_db_diff` arms are all still exercised.
- The community reports this change re-keys have a published old-to-new mapping, and the historical
  corpus fixture is untouched.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan and absent at the phase's base commit -- exclude from source-grounding drift
verification:

- `_build_prune03_synthesized_fingerprint_match` -- new builder in
  `firestarter_app/tests/fixtures/report_shapes.py`
- `prune03-synthesized-fingerprint-match` -- new shape id in `_BUILDERS`, `FROZEN_HASHES`,
  `LADDER_PINS` and `tests/fixtures/shape_ids.json`, removed from `RESERVED_SHAPE_IDS`
- `RK-174-07-p177-w27e257-all-ok-synthesized-match` -- new ledger id
- `RK-174-08-p177-gh47-filed-hash-synthesized-match` -- new ledger id
- `RK-174-09-p178-status-axis-must-not-rekey-reanchored` -- new ledger id
- `firestarter_app/tests/fixtures/reports/prune03-synthesized-fingerprint-match.json` -- new
  generated snapshot (exact filename produced by `tools/snapshot_report_shapes.py`)
- `.planning/phases/177-evidence-gated-read-back/177-REKEY-MAPPING.md`
- `.planning/phases/177-evidence-gated-read-back/evidence/177-02-rebaseline.txt`
- `.planning/phases/177-evidence-gated-read-back/evidence/177-02-ledger.txt`
- `.planning/phases/177-evidence-gated-read-back/evidence/177-02-cross-tree.txt`

The full phase-wide new-symbol list is in `177-01-PLAN.md`'s
`<artifacts_this_phase_produces>` section.
</artifacts_this_phase_produces>

<flagged_assumptions>
The spec-less probe fallback's 7 edge items are enumerated once, in `177-01-PLAN.md`'s
`<flagged_assumptions>` section (3 authored into `must_haves.truths`, 4 surfaced as flagged
assumptions; 3 + 4 = 7, no drops). The assumptions specific to this plan are:

- **PA-10 (assumption A7, RESEARCH.md).** Two ledger rows may name the same `shape_id`. Verified
  against the source rather than assumed: `test_shape_id_ledger_id_pairs_are_unique`'s docstring
  states it explicitly ("a shape can be re-keyed twice by two different phases"), and
  `check_rekey_ledger.py` keys its comparison on `ledger_id`. The re-anchor row depends on this.
- **PA-11 (`RK-174-06` must be declared by Phase 177).** Not an option but a consequence:
  `test_every_ledger_row_shape_id_resolves_and_recomputes` asserts an undeclared row's `before_hash`
  still reproduces, so a row whose shape moved cannot remain undeclared. This planner measured that
  consequence from the test source; RESEARCH.md's Pitfall 3 recommended appending a new row instead,
  which alone would leave `RK-174-06` permanently red.
- **PA-12 (the INCONCLUSIVE ladder arm empties).** All three current members of that arm flip under
  D-4/D-6, which reddens the four-arm sentinel. This planner measured the membership from
  `LADDER_PINS` and `build_db_diff`'s four branches. The re-pointed
  `sst27sf512-six-step-readback-gated` re-populates it; if D-177-3 chose otherwise, another shape
  must, and the `distinct_arms=4` criterion is what forces the executor to notice.
- **PA-13 (the 26-row corpus gate does not redden by itself).** `devtest_issue_corpus.json` records
  each row's filed classification strings as DATA and the test rebuilds from that data, so the
  18-row figure is a projection over the filed corpus, not a test that goes red. It must be measured
  by re-applying the rule to the recorded rows -- which is why the mapping is an artifact rather
  than a test fixture edit.
- **PA-14 (the new shape's snapshot filename).** `tools/snapshot_report_shapes.py` owns the naming;
  the acceptance criterion counts snapshots rather than naming the file, so a different convention
  in the generator does not create a false failure.
</flagged_assumptions>

<output>
Create `.planning/phases/177-evidence-gated-read-back/177-02-SUMMARY.md` when done, recording: every
shape id that moved with its measured before and after; which ledger rows were declared and which
were appended; the final `len(LEDGER)`; the measured `corpus_rekeyed` count and whether it agreed
with the inherited projection of 18; the `distinct_arms` count after the re-point; and the checker's
exit status.
</output>
