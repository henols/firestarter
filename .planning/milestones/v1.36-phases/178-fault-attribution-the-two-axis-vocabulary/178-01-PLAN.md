---
phase: 178-fault-attribution-the-two-axis-vocabulary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/firestarter/diagnostic_report.py
  - firestarter_app/firestarter/submit.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/test_dev_test_cmd.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/fixtures/reports/
  - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/
autonomous: true
requirements: [ATTR-01, ATTR-02, ATTR-03, ATTR-04]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: a `dev test` run whose `check_eprom_id` raises `SerialError` reports the `id` step with `verdict == \"SKIPPED\"` and `status == \"ERROR\"` -- never `BAD`. `SKIPPED` is chosen because it is the only legal result value that keeps `_id_step_closes_gate` closed AND preserves the fault reason in both the filed markdown table and the exported JSON."
    - "D-01: `_id_step_closes_gate` (`chip_test.py:1930-1941`) is left byte-unchanged and still returns True on that step, and `tests/test_dev_test_cmd.py`'s `operator.sdp_lock.assert_not_called()` and `hold_state == SDP_HOLD_NOT_RUN` assertions stay byte-unchanged as the proof. That non-change is an asserted truth, not an assumption."
    - "D-02: `StepResult.status` exists with values `\"COMPLETE\"` / `\"ERROR\"` / `\"SKIP\"` defaulting to `STATUS_COMPLETE`, and `DiagnosticReport.run_status` carries the derived top-level fold. The `support_status` collision (`diagnostic_report.py:364`) is disambiguated in the docstring, never in the field name."
    - "D-03: the OCP `TestStatus` vocabulary is adopted by name only. `pip show ocptv` finds nothing, `pyproject.toml`'s dependency list is byte-unchanged, and no new runtime dependency of any kind is added."
    - "D-04: exactly three consumers consult the status axis ahead of the verdict fold and only these three -- `submit.overall_verdict` (the title), `build_db_diff`'s ladder (an ERROR guard arm ahead of the existing four arms), and `_dev_test_exit_code` (the exit code)."
    - "D-05: the issue title for a transport fault reads `INCONCLUSIVE (harness)`, reusing the existing `INCONCLUSIVE` token so `devtest_issues.py`'s `TITLE_RE` still captures `INCONCLUSIVE` and its `SOFT` set still classifies it, with zero triage-tooling change. No new bare title token is introduced."
    - "D-06: status `ERROR` reaches the process exit code as a code-2 CANDIDATE added into the same `codes` set the verdict codes go into, decided by the single unchanged `_EXIT_CODE_PRECEDENCE` walk -- never a numeric `max`. A run that is both BAD and ERROR therefore exits 1, while its title still reads `INCONCLUSIVE (harness)`; that asymmetry is deliberate."
    - "D-07: `SCHEMA_VERSION` reads `\"1.8\"`. Both parsers match `schema_version` by presence only and the dedup hash never reads it, so the bump is mechanically free; Phase 181 still takes it to 2.0."
    - "D-08: `dedup_fingerprint` (`diagnostic_report.py:248-300`) gains NO `parts.append` line for the status axis. Exclusion is by construction against an explicit five-entry allow-list, not by a filter that a later phase could invert."
    - "D-12: the trigger for status ERROR is the `(SerialError, HardwareOperationError)` exception arm and nothing else. `transport_suspect` is reported and never folded into status -- three of its seven scanned counters are permanently `None`, so deriving a status from it would fabricate confidence."
    - "D-16: no code path in this plan reads, revives or tests `FP_TRANSPORT`. It is dead code on this branch (`_run_cycle_block` hard-codes `runs=1` at `chip_test.py:1598`) and building the status classification on it would ship a branch that never fires."
    - "ATTR-04, ordering edge: `dedup_fingerprint`'s `parts` list is built in `report.results` order and the status axis appends nothing, so the pre-image order is unchanged; `_TRACER_CANONICAL` still reproduces byte-for-byte at `tests/test_blast_radius_invariance.py:191-194`."
    - "ATTR-03, concurrency edge: the title fold and the exit-code fold are pure functions of the in-memory `results` list plus `report.run_status`. Neither reads process-global mutable state, so two concurrent `dev test` processes cannot cross-contaminate each other's title or exit code."
    - "The three test sites that pin `VERDICT_BAD` on the transport path are repaired FORWARD, never relaxed: `tests/test_dev_test_cmd.py:2014-2037`, `tests/test_chip_test_sdp_leg.py::test_serial_timeout_degrades_one_step`, and `::test_hardware_error_degrades_one_step`, plus the three `_EXPECTED_PRECEDENCE_MATRIX` rows at `tests/test_chip_test_sdp_leg.py:444-446`."
    - "`_PRE_EDIT_PRECEDENCE_MATRIX` (the Phase-133 before-image) and `_INTENDED_PRECEDENCE_DELTA` are byte-unchanged, so `test_precedence_matrix_delta_is_exactly_intended` stays green without being widened -- the delta set is still exactly those same three exception classes."
    - "All 17 pre-existing `FROZEN_HASHES` entries are byte-unchanged after this plan's commit, and `tools/rekey/check_rekey_ledger.py` exits 0 with 8 rows bound. This phase declares no re-key."
    - "Environment: every Python command runs through `firestarter_app/.venv311/bin/python` (py3.11, matching CI), every evidence-grade scan uses `/usr/bin/grep`, and every count-bearing pytest run passes `-o addopts=\"\"`."
  prohibitions:
    - "No `#` comment is written into any source or test file this phase touches. Rationale goes in a docstring or in the bare-string-literal-above-the-constant carrier already shipped at `tests/test_blast_radius_invariance.py:88-186`. This is a standing operator rule and this plan cannot override it."
    - "The status axis must never become a silent suppressor. A transport-faulted run is filed exactly as often, and shown exactly as prominently, as a chip-faulted one -- suppression converts a visible wrong verdict into an invisible missing one."
    - "A transport fault must never silently open the destructive-write gate. Re-attributing the fault may not make the tool write to a chip whose identity was never confirmed."
    - "The status axis must never be used to grade confidence -- no softer FAIL, no percentage, no hedged verdict. Where no honest signal exists the report says so rather than guessing."
    - "The transport-fault arm constructs its `StepResult` directly and NEVER through `_skip_result`, which stamps `STATUS_SKIP` and would erase the ERROR discrimination this phase exists to create."
    - "`is_submittable` (`diagnostic_report.py:212-226`) is not touched by this plan. Not one byte."
    - "`_id_step_closes_gate`, `_baseline_closes_sdp_gate`, `_reason_text` and `_step_dict`'s NA reason suppression are not touched. Their non-change is D-01's whole basis."
    - "No sixth `verdict` value is introduced and no existing verdict literal is respelled -- four are UPPERCASE and `marginal` is lowercase, and all five sit inside seventeen frozen hashes."
    - "No file in the `firestarter/` firmware submodule is modified. This phase is host-only."
    - "`firestarter_app/firestarter/data/chip_database.json` is never hand-edited."
    - "No snapshot under `tests/fixtures/reports/` is hand-edited. Every one is regenerated by `tools/snapshot_report_shapes.py`."
    - "`_overall_exit_code` (`cli_handlers.py:2114-2127`) is left alone -- it has zero production call sites and wiring the status axis into the dead helper would ship an unreachable branch."
  artifacts:
    - path: "firestarter_app/firestarter/chip_test.py"
      provides: "The status vocabulary `STATUS_COMPLETE`/`STATUS_ERROR`/`STATUS_SKIP`, the additive `StepResult.status` field, the re-pointed `(SerialError, HardwareOperationError)` arm, and the `run_status(results)` fold"
      contains: "STATUS_ERROR = \"ERROR\""
      exports: ["STATUS_COMPLETE", "STATUS_ERROR", "STATUS_SKIP", "run_status"]
    - path: "firestarter_app/firestarter/diagnostic_report.py"
      provides: "`SCHEMA_VERSION = \"1.8\"`, the carried `DiagnosticReport.run_status` field, the `status` per-step export key, the `run_status` top-level export key, and `build_db_diff`'s ERROR guard arm"
      contains: "\"run_status\": self.run_status"
    - path: "firestarter_app/firestarter/submit.py"
      provides: "`_TITLE_VERDICT_HARNESS` and the status-axis-first guard inside `overall_verdict`"
      contains: "INCONCLUSIVE (harness)"
    - path: "firestarter_app/firestarter/cli_handlers.py"
      provides: "The `report.run_status = run_status(results)` assignment seam and `_dev_test_exit_code`'s second non-verdict precedence candidate"
      contains: "run_status_error"
    - path: ".planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/178-01-tracer-end-to-end.txt"
      provides: "The measured end-to-end record of one transport-faulted run across every layer this phase changes"
      contains: "hash_unmoved_across_status=True"
  key_links:
    - from: "firestarter_app/firestarter/chip_test.py::_run_step_untimed"
      to: "firestarter_app/firestarter/chip_test.py::StepResult"
      via: "the (SerialError, HardwareOperationError) arm constructs StepResult(verdict=VERDICT_SKIPPED, status=STATUS_ERROR) directly, never through _skip_result"
      pattern: "status=STATUS_ERROR"
    - from: "firestarter_app/firestarter/chip_test.py::run_status"
      to: "firestarter_app/firestarter/cli_handlers.py::dev_test"
      via: "derive in the engine, assign in the handler -- report.run_status = run_status(results) beside the sdp_hold_state assignment"
      pattern: "report\\.run_status = run_status\\("
    - from: "firestarter_app/firestarter/diagnostic_report.py::DiagnosticReport.run_status"
      to: "firestarter_app/firestarter/cli_handlers.py::_dev_test_exit_code"
      via: "run_status_error keyword-only bool computed at the call site, added as codes.add(2) into the same set the verdict codes enter"
      pattern: "run_status_error="
    - from: "firestarter_app/firestarter/chip_test.py::StepResult.status"
      to: "firestarter_app/firestarter/diagnostic_report.py::dedup_fingerprint"
      via: "the DELIBERATE absence of a parts.append line -- exclusion by construction against an explicit five-entry allow-list"
      pattern: "parts\\.append"
---

<objective>
Prove the entire two-axis vocabulary end to end on ONE path: a single `dev test` run whose id check
raises `SerialError`, carried through every layer this phase changes -- the vocabulary, the
dataclass field, the exception arm, the run-level fold, the JSON export, the ladder, the issue
title and the process exit code -- and whose `dedup_fingerprint` is measured unmoved.

This is the tracer. It is production-quality, not a prototype: the same `<verify>` bar every later
task carries, and the skeleton every expansion task builds out from. If the result-axis value
(D-01) or the exclusion mechanism (D-08) is wrong, it is wrong after ONE commit -- before a frozen
shape has been registered against it (plan `178-03`) and before the ATTR-06 disclosure has been
plumbed through the same `to_dict()` (plan `178-04`).

Purpose: `chip_test.py:2524-2541` currently spends `VERDICT_BAD` -- a chip verdict -- on a
half-seated cable, and its own comment says so. That is the gh#23 gap. After this plan the tool
never spends a chip's `BAD` on a fault that was never the chip's.

Output: a run that failed to execute validly reports `status: ERROR` beside `verdict: SKIPPED`,
titles `INCONCLUSIVE (harness)`, exits 2, lands the ladder on inconclusive, still offers the filing
prompt, and hashes identically to the same run with the status axis at its default.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-CONTEXT.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-RESEARCH.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-PATTERNS.md
@firestarter_app/CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by phase 178 and does not exist at `firestarter_app @ 0a29d8b`. A
drift check that cannot resolve one of these against the pre-phase tree is reading a new symbol,
not a stale citation.

| Artifact | Kind | Home | Plan |
|---|---|---|---|
| `STATUS_COMPLETE` | module constant `"COMPLETE"` | `firestarter/chip_test.py` | 178-01 |
| `STATUS_ERROR` | module constant `"ERROR"` | `firestarter/chip_test.py` | 178-01 |
| `STATUS_SKIP` | module constant `"SKIP"` | `firestarter/chip_test.py` | 178-01 |
| `StepResult.status` | dataclass field, `str = STATUS_COMPLETE` | `firestarter/chip_test.py` | 178-01 |
| `run_status(results)` | module function `-> str` | `firestarter/chip_test.py` | 178-01 |
| `DiagnosticReport.run_status` | dataclass field, `str = ""` | `firestarter/diagnostic_report.py` | 178-01 |
| `"run_status"` | top-level `to_dict()` key | `firestarter/diagnostic_report.py` | 178-01 |
| `"status"` | `steps[]` element key | `firestarter/diagnostic_report.py` | 178-01 |
| `SCHEMA_VERSION == "1.8"` | changed constant value | `firestarter/diagnostic_report.py` | 178-01 |
| `_TITLE_VERDICT_HARNESS` | module constant `"INCONCLUSIVE (harness)"` | `firestarter/submit.py` | 178-01 |
| `run_status_error` | keyword-only param on `_dev_test_exit_code` | `firestarter/cli_handlers.py` | 178-01 |
| `_RAIL_READING_DISCLOSURE` | module constant | `firestarter/diagnostic_report.py` | 178-04 |
| `"rail_reading_disclosure"` | top-level `to_dict()` key | `firestarter/diagnostic_report.py` | 178-04 |
| `attr01-status-axis-transport-fault` | frozen `shape_id` | `tests/fixtures/report_shapes.py` | 178-03 |
| `_build_attr01_status_axis_transport_fault` | builder function | `tests/fixtures/report_shapes.py` | 178-03 |
| `statuses=` | new kwarg on `build_shape_from_step_specs` | `tests/fixtures/report_shapes.py` | 178-03 |
| `tests/fixtures/reports/attr01-status-axis-transport-fault.json` | new file | `tests/fixtures/reports/` | 178-03 |

New test functions created by this phase: `test_transport_fault_carries_skipped_verdict_and_error_status`,
`test_run_status_folds_error_when_any_step_errored`, `test_run_status_is_complete_when_no_step_errored`,
`test_the_transport_arm_does_not_route_through_skip_result`,
`test_id_step_closes_gate_predicate_is_unchanged_by_the_status_axis`,
`test_overall_verdict_error_status_is_inconclusive_harness`,
`test_overall_verdict_defaults_to_complete_for_a_status_less_result`,
`test_build_title_for_a_transport_fault_is_not_fail`,
`test_a_transport_fault_reason_survives_the_markdown_reason_cell`,
`test_error_run_status_routes_the_ladder_to_inconclusive`,
`test_transport_fault_exits_two_and_titles_inconclusive_harness`,
`test_status_axis_does_not_perturb_dedup_fingerprint`,
`test_a_verdict_change_still_perturbs_dedup_fingerprint`,
`test_status_axis_does_not_perturb_an_empty_results_fingerprint`,
`test_status_axis_does_not_reorder_the_fingerprint_pre_image`,
`test_is_submittable_is_unchanged_by_the_status_axis`,
`test_a_transport_faulted_report_still_reaches_the_confirm_prompt`,
`test_the_transport_precedence_rows_carry_the_error_status`,
`test_rail_reading_disclosure_is_exported_and_rendered`,
`test_rail_reading_disclosure_renders_when_no_rail_was_measured`.

New evidence files: `178-01-tracer-end-to-end.txt`, `178-02-confirmation.txt`,
`178-03-shape-registration.txt`, `178-03-attr04-seal.txt`, `178-04-attr06.txt`,
`178-04-phase-seal.txt`, all under
`.planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/`.

## Spec-less probe arithmetic (no silent drops)

The phase has no SPEC, so the deterministic edge probe supplied the edge predicates. **12 items
surfaced == 10 authored into `must_haves` + 2 surfaced as flagged assumptions.**

- 2 authored here (ATTR-03 concurrency; ATTR-04 ordering).
- 5 authored in plan `178-02` (ATTR-04 adjacency, empty, ordering; ATTR-05 idempotency, ATTR-05
  concurrency). ATTR-04's ordering edge is deliberately asserted in BOTH this plan and `178-02` as a
  cross-cutting constraint, so the two plans hold 6 DISTINCT edges between them, not 7.
- 3 authored in plan `178-04` (ATTR-06 empty, ordering, concurrency).
- 1 authored in plan `178-04` as a structured `verification: backstop` marker (ATTR-06 adjacency).
- 2 remain `unresolved` because the probe classified them `unclassified`, and an `unclassified` row
  is never auto-resolved with a backstop. They are the flagged assumptions below.

## Flagged assumptions (unresolved probe rows -- never silently dropped)

- **ATTR-01, category `unclassified`.** The probe could not assign an edge category to "a run
  carries a status axis separate from the result axis". Assumption taken: the axis is
  *per-step-plus-a-derived-run-level-fold* (D-02), not a single run-level flag. If the operator
  reads ATTR-01 as run-level only, `StepResult.status` is unnecessary and the frozen snapshot in
  plan `178-03` pins a key that should not exist. Surfaced, not resolved.
- **ATTR-02, category `unclassified`.** The probe could not assign an edge category to "a step that
  failed for a tool or transport reason is not reported as a chip verdict". Assumption taken: "tool
  or transport reason" means exactly the `(SerialError, HardwareOperationError)` exception arm
  (D-12), and NOT `EpromOperationError` -- which carries a firmware `error_code` and is a genuine
  chip/firmware finding. If the operator reads "tool reason" more widely, the second arm at
  `chip_test.py:2542-2549` is in scope too and this plan under-delivers. Surfaced, not resolved.

<tasks>

<task type="tracer">
  <name>Task 1: One transport-faulted run, through every layer, in one commit</name>
  <precondition>`firestarter_app/.venv311/bin/python --version` prints `Python 3.11.x` and `./.venv311/bin/python -c "import firestarter; print(firestarter.__file__)"` prints a path under `/workspaces/firestarter_app/firestarter/` -- the editable install resolves to this worktree, not to a site-packages copy.</precondition>
  <reversibility rating="costly">D-08 excludes the status axis from `dedup_fingerprint`. Reversing that later re-keys every filed community group whose run carried a non-default status, and the 12-hex hash is embedded in filed GitHub issue titles that `tools/parse_devtest_issue.py` reads and never re-computes. The undo is a declared re-key across two repositories, not a code revert.</reversibility>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/firestarter/diagnostic_report.py, firestarter_app/firestarter/submit.py, firestarter_app/firestarter/cli_handlers.py, firestarter_app/tests/test_dev_test_cmd.py, firestarter_app/tests/test_chip_test_sdp_leg.py, firestarter_app/tests/test_blast_radius_invariance.py, firestarter_app/tests/fixtures/reports/</files>
  <read_first>
    - firestarter_app/firestarter/chip_test.py lines 930-945 (the `VERDICT_*` block), 1060-1120 (`StepResult` and `_skip_result`), 1925-1960 (`_id_step_closes_gate`, `_baseline_closes_sdp_gate`), 1955-2045 (`sdp_hold_state`, the fold's neighbours), 2500-2560 (the exception-arm chain), 3095-3135 (the two bare `verdict=VERDICT_SKIPPED` constructions)
    - firestarter_app/firestarter/diagnostic_report.py lines 44-56 (`SCHEMA_VERSION`, `NOT_MEASURED`, `NOT_REPORTED`), 160-200 (`_is_transport_suspect` and its honesty docstring), 248-300 (`dedup_fingerprint`), 349-384 (`build_db_diff`), 620-655 (`DiagnosticReport` fields and `sdp_hold_state`'s carriage-only shape), 768-812 (`_step_dict`), 845-870 (`to_dict`)
    - firestarter_app/firestarter/submit.py lines 40-55 (imports), 140-170 (`overall_verdict`, `build_title`), 200-225 (`_reason_text` and its SKIPPED carve-out)
    - firestarter_app/firestarter/cli_handlers.py lines 30-60 (imports), 2086-2160 (`_VERDICT_EXIT_CODES`, `_verdict_code`, `_EXIT_CODE_PRECEDENCE`, `_overall_exit_code`, `_dev_test_exit_code`), 2436-2455 (the derive-in-engine / assign-in-handler seam), 2510-2532 (the unconditional filing ask and the exit-code call site)
    - firestarter_app/tests/test_blast_radius_invariance.py lines 85-200 (all seven pinned key lists and `_TRACER_CANONICAL`)
    - firestarter_app/tests/test_dev_test_cmd.py lines 2000-2040 (the R2b test and the id-mismatch test directly above it, which must NOT move)
    - firestarter_app/tests/test_chip_test_sdp_leg.py lines 330-470 (`_PRECEDENCE_EXCEPTION_CLASSES`, `_derive_precedence_row`, both precedence matrices, `_INTENDED_PRECEDENCE_DELTA`), 560-615 (the two LEG-11 degrade tests)
    - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-PATTERNS.md (Moves 1, 3, 4 and Shared Patterns A, B, C, D)
  </read_first>
  <action>
Land the whole two-axis vocabulary and its three consumer widenings in ONE commit. Splitting it
leaves the suite red or, worse, transiently false-green: the transport step's exported verdict
changes the instant the arm is re-pointed, so the three tests that pin `BAD` on that path and the
two pinned key lists move in the same commit, and every committed snapshot regenerates with them.

**The comment carrier, decided.** This phase writes ZERO `#` comments. New module constants use the
bare-string-literal-above-the-constant idiom already shipped at
`tests/test_blast_radius_invariance.py:88-186` -- a `"""..."""` expression statement immediately
above the assignments. New field and function rationale goes in a real docstring. Both in-tree
analogs for a constants block (`VERDICT_*` at `chip_test.py:933-939`, `SDP_HOLD_*` at
`:1962-1970`) state their rationale in `#` comments; copy the WORDING, relocate the mechanism.
Do not touch either pre-existing comment block -- they are not this phase's to sweep.

**1. `firestarter/chip_test.py` -- the vocabulary.** Immediately after `VERDICT_MARGINAL` at
`:939`, add a bare triple-quoted expression statement then `STATUS_COMPLETE = "COMPLETE"`,
`STATUS_ERROR = "ERROR"`, `STATUS_SKIP = "SKIP"`. The literal carries three things: the OCP
`TestStatus` provenance and that the vocabulary is adopted by name only with no `ocptv` package and
no new runtime dependency (D-03); the disambiguation against the database's `support_status`, whose
one colliding read site is `diagnostic_report.py:364` (D-02); and the `SDP_HOLD_*` warning verbatim
in substance -- these are report VALUES, not op strings, they carry no `OP_` prefix, and they must
never join `_ALL_OPS` or `_MULTIWORD_OP_VALUES` in `tests/test_op_registration_parity.py`.

**2. `StepResult` -- the additive field.** Add `status: str = STATUS_COMPLETE` as the LAST field of
the dataclass. Extend the class docstring at `:1066-1079`, which already carries the `verdict`
contract sentence, with the sibling sentence: `status` is one of COMPLETE/ERROR/SKIP, it is the
run-validity axis held separately from the chip-verdict axis, and it is deliberately NOT part of
`dedup_fingerprint` (D-08). Leave `duration_s`'s existing comment block untouched.

**3. `_skip_result` and the two bare SKIPPED constructions -- the SKIP cell.** Stamp
`status=STATUS_SKIP` inside `_skip_result` (`:1105-1106`) and on the two direct constructions at
`:3107` and `:3127`. This is the OCP `SKIP` x `NOT_APPLICABLE` cell: `SKIPPED` and `NA` on the
result axis both mean the step did not run. Leaving `STATUS_SKIP` with zero assignment sites would
reproduce exactly the dead-vocabulary failure mode D-16 records for `FP_TRANSPORT`.

**4. The transport arm -- ATTR-02's target.** At `:2524-2541` change the returned `StepResult` to
`verdict=VERDICT_SKIPPED, status=STATUS_ERROR`, keeping `reason=str(exc)` and `run_count=1` and
still omitting `error_code` (neither exception class carries it). Construct it DIRECTLY -- never
route it through `_skip_result`, which stamps `STATUS_SKIP`. Do not move the clause: the
`ProgrammerNotFoundError` re-raise above at `:2504-2523` and the `EpromOperationError` arm below at
`:2542-2549` both stay exactly where and what they are, and the second stays a chip verdict.

**5. `run_status(results)` -- the fold.** Add a module-level `run_status(results: list[StepResult])
-> str` beside `sdp_hold_state`, returning `STATUS_ERROR` if any result's `status` is
`STATUS_ERROR`, else `STATUS_COMPLETE`. Its docstring states the fold is two-valued at the run
level by design -- `STATUS_SKIP` never propagates upward, because a plan carrying an unsupported
step is not an invalid run -- and mirrors the honesty rule `_is_transport_suspect`'s docstring
states: a status is read only where it is PRESENT, never fabricated from an absent counter. Name
D-12 there: `transport_suspect` is reported and never folded, because three of its seven scanned
counters are permanently `None`.

**6. `firestarter/diagnostic_report.py` -- schema, carriage and export.** Set `SCHEMA_VERSION` to
`"1.8"` (D-07). Add `run_status: str = ""` as the last `DiagnosticReport` field, following
`sdp_hold_state`'s carriage-only shape at `:634-648`: a plain `str`, declared and serialised here,
never derived here. Add `"status": result.status` to `_step_dict` (`:768-808`) and
`"run_status": self.run_status` to `to_dict` (`:849-869`). Write NO `parts.append` line in
`dedup_fingerprint` (`:248-300`) -- D-08 is satisfied by the absence.

**7. `build_db_diff` -- the ladder guard (D-04).** Insert one arm AHEAD of the existing
`if "BAD" in verdicts:` at `:371`: when any result's status reads `STATUS_ERROR`, set
`proposed = _DISPOSITION_INCONCLUSIVE` and `ladder_state = _LADDER_NONE`. Read the status as
`getattr(r, "status", STATUS_COMPLETE)` so a duck-typed result object without the field folds to
COMPLETE. Without this arm a SKIPPED transport step on an otherwise clean run falls through the
third arm's `verdicts <= {"OK", "NA", "SKIPPED"}` test and a run that did not execute validly gets
proposed for `community-reported` graduation. Import `STATUS_COMPLETE, STATUS_ERROR` from
`firestarter.chip_test` alongside the existing import block at `:32`.

**8. `firestarter/submit.py` -- the title (D-05).** Add a module constant
`_TITLE_VERDICT_HARNESS = "INCONCLUSIVE (harness)"`. Inside `overall_verdict` (`:140-152`) insert
the status guard AHEAD of the `if "BAD" in verdicts:` test, returning `_TITLE_VERDICT_HARNESS` when
any result's `getattr(r, "status", STATUS_COMPLETE)` reads `STATUS_ERROR`. Import
`STATUS_COMPLETE, STATUS_ERROR` beside the existing `VERDICT_NA` import at `:48`. Repair the
docstring: name the fourth return value, state that the qualifier rides in the title body so
`devtest_issues.py`'s `TITLE_RE` still captures the bare `INCONCLUSIVE` token and its `SOFT` set
still classifies it with zero triage-tooling change, and state that this status-axis-first ordering
is deliberately NOT the exit code's -- a run that is both BAD and ERROR titles harness-inconclusive
but exits 1. Leave `build_title`'s f-string untouched.

**9. `firestarter/cli_handlers.py` -- assignment and exit code (D-06).** Beside
`report.sdp_hold_state = sdp_hold_state(plan, results)` at `:2450`, add
`report.run_status = run_status(results)` -- an assignment only, never an inline fold. Import
`run_status` and `STATUS_ERROR` in the existing `from firestarter.chip_test import (...)` block.
Widen `_dev_test_exit_code` (`:2130-2157`) with a second keyword-only parameter
`run_status_error: bool`, fully annotated because this helper is inside the mypy strict island, and
add `codes.add(2)` for it into the SAME `codes` set the verdict codes entered, before the single
unchanged `for code in _EXIT_CODE_PRECEDENCE` walk. No `max()`, no second branch, no new return
path. Extend the docstring's "Cost, stated:" paragraph -- the exit code now gains two non-verdict
terms, not "exactly this one". Widen the call site at `:2526-2529` with
`run_status_error=(report.run_status == STATUS_ERROR)`, computed at the call site exactly as the
existing ALLOW-only `sdp_oracle_not_run` term is. Leave `_overall_exit_code` at `:2114-2127`
completely alone.

**10. Repair the three BAD-pinning test sites FORWARD.** RESEARCH.md's red-list named only the
first; the census run at plan time found all three.
  - `tests/test_dev_test_cmd.py::test_r2_transport_error_during_id_check_closes_gate_and_renders_notrun`
    (`:2014-2037`): change the first assertion to `steps["id"]["verdict"] == "SKIPPED"` and ADD
    `assert steps["id"]["status"] == "ERROR"`. The remaining four assertions
    (`operator.sdp_lock.assert_not_called()`, `hold_state == SDP_HOLD_NOT_RUN`, and the rendered
    `sdp_hold_state NOT-RUN` line) stay byte-unchanged -- they are the safety-gate proof and
    relaxing any of them is how this repair goes wrong. Repair the docstring sentence that says the
    step degrades to BAD. Do NOT touch the id-mismatch test directly above it at `:1970-1988`,
    whose `BAD` is a genuine chip verdict.
  - `tests/test_chip_test_sdp_leg.py::test_serial_timeout_degrades_one_step` and
    `::test_hardware_error_degrades_one_step` (`:564-611`): assert
    `read_result.verdict == VERDICT_SKIPPED` and add `read_result.status == STATUS_ERROR`. Keep
    `blank_check_result.verdict == VERDICT_OK`, `operator.check_eprom_blank.assert_called()` and
    `read_result.error_code is None` unchanged -- those prove one step degraded without aborting
    the run.
  - `tests/test_chip_test_sdp_leg.py` `_EXPECTED_PRECEDENCE_MATRIX` (`:444-446`): move the
    `SerialError`, `SerialTimeoutError` and `HardwareOperationError` rows from `(None, "BAD", None)`
    to `(None, "SKIPPED", None)`. Do NOT touch `_PRE_EDIT_PRECEDENCE_MATRIX` -- it is a Phase-133
    before-image and rewriting it falsifies history. Do NOT widen `_INTENDED_PRECEDENCE_DELTA`: it
    already names exactly those three classes, so the computed delta is unchanged and
    `test_precedence_matrix_delta_is_exactly_intended` stays green untouched. Do NOT widen
    `_derive_precedence_row`'s three-tuple; the status proof for those rows is authored separately
    in plan `178-02`.

**11. The two pinned key lists.** In `tests/test_blast_radius_invariance.py`, add `"run_status"` to
`_TO_DICT_KEYS` (`:92-104`, 11 to 12) and `"status"` to `_STEPS_ELEMENT_0_KEYS` (`:175-189`, 13 to
14), each in its sorted position, and update each list's bare-string-literal header to state the
new count. Do not touch `_VOLTAGE_KEYS`, `_BANNER_KEYS`, `_AUTO_CAPTURE_KEYS`,
`_TRANSPORT_HEALTH_KEYS` or `_DB_DIFF_KEYS`.

**12. Regenerate every snapshot.** Run
`./.venv311/bin/python tools/snapshot_report_shapes.py` with no `--shape` argument, so all 17
committed files under `tests/fixtures/reports/` regenerate. Never hand-edit one.

Commit once, inside `firestarter_app`, on the milestone branch.
  </action>
  <acceptance_criteria>
    - `./.venv311/bin/python -c "from firestarter.chip_test import STATUS_COMPLETE, STATUS_ERROR, STATUS_SKIP; print(STATUS_COMPLETE, STATUS_ERROR, STATUS_SKIP)"` prints `COMPLETE ERROR SKIP`.
    - A `run_plan` on `sst27sf512` whose `check_eprom_id` raises `SerialError` returns an `id` `StepResult` whose `verdict` is `SKIPPED`, whose `status` is `ERROR`, and whose `reason` is a non-empty string.
    - `chip_test._id_step_closes_gate` returns `True` for that same `StepResult`, and `operator.sdp_lock` was never called during the run.
    - `DiagnosticReport.to_dict()` for that run contains a top-level key `run_status` reading `ERROR`, and every element of `steps[]` contains a key `status`.
    - `to_dict()["schema_version"]` reads `1.8`.
    - `submit.build_title(report, "sst27sf512")` contains the substring `INCONCLUSIVE (harness)` and does not contain the substring `— FAIL`.
    - `_dev_test_exit_code(results, sdp_oracle_not_run=False, run_status_error=True)` returns `2` for that run, and returns `1` when the same call is made on a result list that also contains a `BAD` step.
    - `build_db_diff("sst27sf512", db, results).ladder_state` is the empty string and `.proposed_disposition` is the inconclusive disposition, not the community-reported candidate one.
    - `dedup_fingerprint(report)` equals `dedup_fingerprint(report_with_every_status_reset_to_COMPLETE)` for the same results.
    - All 17 entries of `FROZEN_HASHES` reproduce from a fresh `build_shape` call, and `tools/snapshot_report_shapes.py --check` reports 17 matching snapshots.
    - `tests/test_dev_test_cmd.py`, `tests/test_chip_test_sdp_leg.py`, `tests/test_blast_radius_invariance.py` and `tests/test_rekey_ledger.py` all pass.
    - `git diff` for this commit adds zero lines matching `^\+[[:space:]]*#` across `firestarter/` and `tests/`.
    - `ruff check`, `ruff format --check` and `tools/check_mypy_watermark.py` are all green, the watermark still reading 35.
    - `tools/check_devtest_orchestrator.py` and `tools/check_diagnostic_report_claims.py` both exit 0.
    - `git -C /workspaces/firestarter status --porcelain` is empty -- no firmware file was touched.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence && mkdir -p "$E" && EV=$E/178-01-tracer-end-to-end.txt && { echo "== 1 the faulted path across every layer"; (cd firestarter_app && ./.venv311/bin/python -c "
import copy, sys
sys.path.insert(0, '.')
from unittest.mock import Mock
from firestarter import chip_test as ct
from firestarter import submit as sub
from firestarter.cli_handlers import _dev_test_exit_code
from firestarter.database import EpromDatabase
from firestarter.diagnostic_report import AutoCapture, DiagnosticReport, TransportHealth, SCHEMA_VERSION, build_db_diff, dedup_fingerprint, _DISPOSITION_INCONCLUSIVE
from firestarter.exceptions import SerialError
print('const_complete=' + ct.STATUS_COMPLETE)
print('const_error=' + ct.STATUS_ERROR)
print('const_skip=' + ct.STATUS_SKIP)
print('schema=' + SCHEMA_VERSION)
db = EpromDatabase(skip_local_override=True)
names = ['check_eprom_id','read_eprom','check_eprom_blank','write_eprom','verify_eprom','erase_eprom','sdp_lock','sdp_unlock']
op = Mock(spec=names)
op.check_eprom_id.side_effect = SerialError('half-seated cable')
for n in names[1:]:
    getattr(op, n).return_value = True
plan = ct.derive_plan('sst27sf512', db, write_scope='full')
results = ct.run_plan(plan, op, db, runs=2)
idr = next(r for r in results if r.op == ct.OP_ID)
print('id_verdict=' + idr.verdict)
print('id_status=' + idr.status)
print('id_reason_nonempty=' + str(bool(idr.reason)))
print('gate_closes=' + str(ct._id_step_closes_gate(idr)))
print('sdp_lock_calls=' + str(op.sdp_lock.call_count))
print('fold=' + ct.run_status(results))
rep = DiagnosticReport(auto_capture=AutoCapture(host_version='0.0.0', chip='sst27sf512', protocol='7'), transport=TransportHealth(), plan=plan, results=results)
rep.run_status = ct.run_status(results)
rep.db_diff = build_db_diff('sst27sf512', db, results)
d = rep.to_dict()
print('top_key_run_status=' + str('run_status' in d))
print('exported_run_status=' + str(d['run_status']))
print('step_key_status=' + str(all('status' in s for s in d['steps'])))
print('exported_schema=' + d['schema_version'])
title = sub.build_title(rep, 'sst27sf512')
print('title_has_harness=' + str('INCONCLUSIVE (harness)' in title))
print('title_is_fail=' + str(sub.overall_verdict(results) == 'FAIL'))
print('exit_error_only=' + str(_dev_test_exit_code(results, sdp_oracle_not_run=False, run_status_error=True)))
bad = copy.deepcopy(results); bad[-1].verdict = ct.VERDICT_BAD
print('exit_bad_and_error=' + str(_dev_test_exit_code(bad, sdp_oracle_not_run=False, run_status_error=True)))
print('ladder_state_empty=' + str(rep.db_diff.ladder_state == ''))
print('ladder_inconclusive=' + str(rep.db_diff.proposed_disposition == _DISPOSITION_INCONCLUSIVE))
print('still_submittable=' + str(d['is_submittable']))
before = dedup_fingerprint(rep)
neutral = copy.deepcopy(results)
for r in neutral:
    r.status = ct.STATUS_COMPLETE
rep2 = DiagnosticReport(auto_capture=rep.auto_capture, transport=TransportHealth(), plan=plan, results=neutral)
print('hash_unmoved_across_status=' + str(before == dedup_fingerprint(rep2)))
"); echo "rc=$?"; echo "== 2 no frozen hash moved and no parts.append was written"; (cd firestarter_app && ./.venv311/bin/python -c "
import inspect, sys
sys.path.insert(0, '.')
from firestarter.diagnostic_report import dedup_fingerprint
from tests.fixtures.report_shapes import FROZEN_HASHES, SHAPE_IDS, build_shape
bad = [s for s in SHAPE_IDS if dedup_fingerprint(build_shape(s)) != FROZEN_HASHES[s]]
print('stale_frozen=' + ','.join(sorted(bad)))
print('shape_count=' + str(len(SHAPE_IDS)))
src = inspect.getsource(dedup_fingerprint)
print('hash_reads_status=' + str('status' in src))
print('parts_append_count=' + str(src.count('parts.append')))
"); echo "rc=$?"; echo "== 3 the repaired and pinned modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts=\"\" -q -p no:randomly tests/test_dev_test_cmd.py tests/test_chip_test_sdp_leg.py tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py); echo "rc=$?"; echo "== 4 snapshots regenerate clean"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; echo "== 5 the two AST gates"; (cd firestarter_app && ./.venv311/bin/python tools/check_devtest_orchestrator.py); echo "rc=$?"; (cd firestarter_app && ./.venv311/bin/python tools/check_diagnostic_report_claims.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 6 && /usr/bin/grep -qx 'const_complete=COMPLETE' "$EV" && /usr/bin/grep -qx 'const_error=ERROR' "$EV" && /usr/bin/grep -qx 'const_skip=SKIP' "$EV" && /usr/bin/grep -qx 'schema=1.8' "$EV" && /usr/bin/grep -qx 'exported_schema=1.8' "$EV" && /usr/bin/grep -qx 'id_verdict=SKIPPED' "$EV" && /usr/bin/grep -qx 'id_status=ERROR' "$EV" && /usr/bin/grep -qx 'id_reason_nonempty=True' "$EV" && /usr/bin/grep -qx 'gate_closes=True' "$EV" && /usr/bin/grep -qx 'sdp_lock_calls=0' "$EV" && /usr/bin/grep -qx 'fold=ERROR' "$EV" && /usr/bin/grep -qx 'top_key_run_status=True' "$EV" && /usr/bin/grep -qx 'exported_run_status=ERROR' "$EV" && /usr/bin/grep -qx 'step_key_status=True' "$EV" && /usr/bin/grep -qx 'title_has_harness=True' "$EV" && /usr/bin/grep -qx 'title_is_fail=False' "$EV" && /usr/bin/grep -qx 'exit_error_only=2' "$EV" && /usr/bin/grep -qx 'exit_bad_and_error=1' "$EV" && /usr/bin/grep -qx 'ladder_state_empty=True' "$EV" && /usr/bin/grep -qx 'ladder_inconclusive=True' "$EV" && /usr/bin/grep -qx 'still_submittable=True' "$EV" && /usr/bin/grep -qx 'hash_unmoved_across_status=True' "$EV" && /usr/bin/grep -qx 'stale_frozen=' "$EV" && /usr/bin/grep -qx 'shape_count=17' "$EV" && /usr/bin/grep -qx 'hash_reads_status=False' "$EV" && /usr/bin/grep -qx 'parts_append_count=3' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than six `rc=0` lines in the evidence file; or any of the pinned `key=value` lines is absent (notably `id_status=ERROR`, `hash_unmoved_across_status=True`, `stale_frozen=` with an empty value, `hash_reads_status=False`); or the evidence file contains `no tests ran` or a line matching `[0-9]+ (failed|error)`; or non-zero exit from the whole conjunction.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- firestarter/ tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*[\"'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && test "$(git diff HEAD~1 --name-only -- tests/fixtures/reports/ | wc -l)" -eq 17 && git diff HEAD~1 --quiet -- tests/fixtures/devtest_issue_corpus.json && git diff HEAD~1 --quiet -- tests/fixtures/rekey_ledger.py && git diff HEAD~1 --quiet -- pyproject.toml && ./.venv311/bin/python -m ruff check firestarter/ tests/ && ./.venv311/bin/python -m ruff format --check firestarter/ tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' && test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git status --porcelain firestarter/data/chip_database.json)"</automated>
    <fails_when>non-zero exit -- specifically: the added-comment count is not 0, the regenerated-snapshot file count is not exactly 17, `devtest_issue_corpus.json` / `rekey_ledger.py` / `pyproject.toml` show a diff, ruff prints a violation, the mypy line does not read exactly `mypy errors: 35 (watermark: 35)`, or either porcelain check prints a path.</fails_when>
    <automated>cd /workspaces && python3 tools/rekey/check_rekey_ledger.py</automated>
    <fails_when>non-zero exit, or stdout does not contain `OK: 8 ledger row(s), 8 MILESTONES.md row(s) bound`.</fails_when>
  </verify>
  <done>A transport-faulted run reports `verdict: SKIPPED` with `status: ERROR`, folds to `run_status: ERROR`, titles `INCONCLUSIVE (harness)`, exits 2, lands the ladder on inconclusive, still offers the filing prompt, and hashes identically to the same run with the status axis at its default. All 17 frozen hashes are unmoved, the destructive gate still closes, and the commit adds zero source comments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The three consumer widenings, proved one at a time</name>
  <files>firestarter_app/tests/test_submit.py, firestarter_app/tests/test_dev_test_cmd.py, firestarter_app/tests/test_diagnostic_report.py</files>
  <read_first>
    - firestarter_app/tests/test_submit.py lines 148-185 (the `_step` `SimpleNamespace` helper -- it has NO `status` attribute today, which is exactly what the duck-type default must tolerate) and the `submit_report` test block around lines 640-760
    - firestarter_app/tests/test_diagnostic_report.py lines 200-330 (`_minimal_report`) and 1690-1730 (the `duration_s` exclusion idiom)
    - firestarter_app/tests/test_dev_test_cmd.py lines 1960-2040 (the two R2 gate tests and their operator/app fixtures)
    - firestarter_app/firestarter/submit.py lines 140-170 and 200-225
    - firestarter_app/firestarter/diagnostic_report.py lines 349-384
    - firestarter_app/firestarter/cli_handlers.py lines 2130-2160 and 2510-2532
  </read_first>
  <behavior>
    - `overall_verdict` on a result list containing one `status == STATUS_ERROR` entry returns `INCONCLUSIVE (harness)`, even when another entry carries `BAD` -- the status guard is ahead of the verdict fold.
    - `overall_verdict` on the existing `SimpleNamespace(op=, verdict=, fingerprint=)` doubles, which carry no `status` attribute at all, still returns `PASS` / `FAIL` / `INCONCLUSIVE` exactly as before -- the duck-type default is `COMPLETE`.
    - `build_title` on a transport-faulted report yields a title whose verdict token, parsed by the same `^\[dev test\]\s+(?P<chip>\S+)\s+[—-]\s+(?P<verdict>[A-Za-z]+)` regex `devtest_issues.py` uses, is the bare word `INCONCLUSIVE`.
    - `_reason_text(VERDICT_SKIPPED, "half-seated cable")` returns the reason verbatim, not `-` -- the D-01 evidence that `SKIPPED` preserves the fault reason where `NA` would suppress it.
    - `build_db_diff` with one `STATUS_ERROR` result among otherwise-`OK` results returns `ladder_state == ""` and the inconclusive disposition, never `community-reported`.
    - `_dev_test_exit_code(..., run_status_error=True)` returns 2 on an otherwise-clean run and 1 when a `BAD` verdict is also present -- the precedence candidate, never a max.
    - An end-to-end `dev test` invocation whose id check raises `SerialError` exits 2 and writes a report whose `run_status` is `ERROR`.
  </behavior>
  <action>
Author the behaviour tests for D-04's three widened consumers, one function per behaviour, and see
each one red against a stashed pre-Task-1 tree before it is green. No `#` comments.

In `tests/test_submit.py`, extend the module's `_step` helper with an OPTIONAL `status` keyword that
defaults to omitting the attribute entirely, so the four existing `overall_verdict` tests keep
passing status-less doubles and continue to prove the `getattr(..., STATUS_COMPLETE)` default. Add
`test_overall_verdict_error_status_is_inconclusive_harness` (a BAD step plus an ERROR-status step
still titles harness-inconclusive, proving the guard is ahead of the verdict fold),
`test_overall_verdict_defaults_to_complete_for_a_status_less_result` (assert on the four existing
status-less shapes by calling `overall_verdict` directly), `test_build_title_for_a_transport_fault_is_not_fail`
(apply `devtest_issues.py`'s own regex shape to the produced title and assert the captured verdict
group is exactly `INCONCLUSIVE`), and `test_a_transport_fault_reason_survives_the_markdown_reason_cell`
(call `submit._reason_text` with `VERDICT_SKIPPED` and a non-empty reason and assert the reason
comes back verbatim).

In `tests/test_diagnostic_report.py`, add `test_error_run_status_routes_the_ladder_to_inconclusive`:
build a `_minimal_report`-shaped result list of `OK` steps, set one result's `status` to
`STATUS_ERROR`, call `build_db_diff`, and assert `ladder_state == ""` and
`proposed_disposition == _DISPOSITION_INCONCLUSIVE`. Add the anti-vacuity sibling in the same
function or immediately beside it: with every status at `STATUS_COMPLETE` the same result list
lands on `_DISPOSITION_CANDIDATE` / `community-reported`, so the guard is demonstrably doing the
work rather than the ladder already landing there.

In `tests/test_dev_test_cmd.py`, add
`test_transport_fault_exits_two_and_titles_inconclusive_harness`: reuse the R2b test's operator and
app fixtures, invoke `dev test` off-TTY with `check_eprom_id.side_effect = SerialError(...)`, and
assert the CliRunner result's `exit_code` is 2 and the written report's `run_status` is `ERROR`.
Do not duplicate the R2b test's gate assertions -- that test already owns them.
  </action>
  <acceptance_criteria>
    - `pytest --collect-only` lists all five new test node ids under their respective modules.
    - `submit.overall_verdict([_step("id", "BAD"), _step("read", "OK", status="ERROR")])` returns the string `INCONCLUSIVE (harness)`.
    - The four pre-existing `overall_verdict` tests (`..._all_ok_is_pass`, `..._marginal_is_inconclusive`, `..._bad_dominates_marginal`, `..._bad_alone_is_fail`) pass with their assertions byte-unchanged.
    - `re.match(r"^\[dev test\]\s+(?P<chip>\S+)\s+[—-]\s+(?P<verdict>[A-Za-z]+)", title).group("verdict")` equals `INCONCLUSIVE` for a transport-faulted report's title.
    - `submit._reason_text("SKIPPED", "half-seated cable")` returns `half-seated cable`.
    - `build_db_diff` returns `("", _DISPOSITION_INCONCLUSIVE)` with a status-ERROR result present and `("community-reported", _DISPOSITION_CANDIDATE)` with the identical result list at default status.
    - The CliRunner `dev test` invocation with a `SerialError` id check exits with code 2.
    - `ruff check` and `ruff format --check` pass on the three touched test files, and the commit adds zero `#` comment lines.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence && mkdir -p "$E" && EV=$E/178-01-consumers.txt && { echo "== 1 the named legs exist"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q --collect-only tests/test_submit.py tests/test_diagnostic_report.py tests/test_dev_test_cmd.py) > /tmp/178-01-collect.txt 2>&1; echo "rc=$?"; for t in test_overall_verdict_error_status_is_inconclusive_harness test_overall_verdict_defaults_to_complete_for_a_status_less_result test_build_title_for_a_transport_fault_is_not_fail test_a_transport_fault_reason_survives_the_markdown_reason_cell test_error_run_status_routes_the_ladder_to_inconclusive test_transport_fault_exits_two_and_titles_inconclusive_harness; do echo "leg_${t}=$(/usr/bin/grep -c "$t" /tmp/178-01-collect.txt)"; done; echo "== 2 the four inherited overall_verdict legs are untouched"; echo "inherited_diff=$(cd firestarter_app && git diff HEAD~1 -- tests/test_submit.py | /usr/bin/grep -cE '^-.*overall_verdict\(results\) ==')"; echo "== 3 the three modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_submit.py tests/test_diagnostic_report.py tests/test_dev_test_cmd.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 2 && test "$(/usr/bin/grep -cE '^leg_[a-z_]+=0$' "$EV")" -eq 0 && /usr/bin/grep -qx 'inherited_diff=0' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than two `rc=0` lines; or any `leg_<name>=0` line present (that test function was never collected); or `inherited_diff` is not 0 (an existing `overall_verdict` assertion was edited rather than left alone); or the evidence file contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -vE '^\+[[:space:]]*[\"'"'"']' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- firestarter/ && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/</automated>
    <fails_when>non-zero exit -- the added-comment count is not 0, `firestarter/` shows a diff in a tests-only commit, or ruff prints a violation.</fails_when>
  </verify>
  <done>Each of D-04's three widened consumers has its own named passing test; the four inherited `overall_verdict` assertions are byte-unchanged, proving the duck-typed status-less default holds; and an end-to-end `dev test` on a transport fault exits 2.</done>
</task>

</tasks>

<threat_model>
ASVS level 1, blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host process to public GitHub issue | `submit_report` sends a sanitized `to_dict()` payload and an argv-passed title to a public tracker; every new string key this plan exports crosses here |
| serial link to host engine | `SerialError` / `HardwareOperationError` text becomes `StepResult.reason`, which is untrusted device/driver-originated string data |
| host engine to destructive write | `_id_step_closes_gate` decides whether a write proceeds against a chip whose identity was not confirmed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-178-01 | Information disclosure | `submit.sanitize_dict` / the new `status` and `run_status` export keys | medium | mitigate | Both keys are added to `to_dict()`, which is the ONLY input `sanitize_dict` deep-scrubs, so `_SCRUBS` (home dirs, `/dev/tty*`, `COM\d+`, `/tmp/*`, the username) sees them. Nothing is rendered-only. The transport `reason` text is pre-existing payload through the same sanitizer and its tests are re-run against a schema-1.8 body. |
| T-178-02 | Tampering | `chip_test._id_step_closes_gate` | high | mitigate | D-01 selects `VERDICT_SKIPPED` precisely because the gate predicate keys on `(VERDICT_BAD, VERDICT_SKIPPED)`; the predicate is left byte-unchanged and `tests/test_dev_test_cmd.py`'s `operator.sdp_lock.assert_not_called()` plus `hold_state == SDP_HOLD_NOT_RUN` assertions are kept byte-unchanged as the standing proof. `marginal` and `NA` were both rejected on this ground. |
| T-178-03 | Repudiation | `submit.overall_verdict` / `build_title` | medium | mitigate | This plan's purpose: a transport fault stops filing a public `FAIL` against a manufacturer's part. The title reads `INCONCLUSIVE (harness)` and the ladder refuses to propose graduation. |
| T-178-04 | Denial of information | `submit_report`'s filing ask | high | mitigate | `is_submittable` is left byte-unchanged (D-11) and no status-axis term enters any of the three existing early-return paths; plan `178-02` carries the asserted non-suppression test. Suppressing on suspicion is the Firefox flaky-dismissal failure mode and is forbidden by ATTR-05. |
| T-178-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. D-03 adopts the OCP vocabulary by name only and forbids a new runtime dependency; `pyproject.toml` is asserted byte-unchanged by this task's second verify leg. RESEARCH.md's Environment Availability table records zero missing dependencies with no fallback needed. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_chip_test_sdp_leg.py tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_submit.py tests/test_diagnostic_report.py -o addopts="" -q -p no:randomly` -- all green.
2. `./.venv311/bin/python tools/snapshot_report_shapes.py --check` -- 17 snapshots match a fresh regeneration.
3. `cd /workspaces && python3 tools/rekey/check_rekey_ledger.py` -- exit 0, 8 rows bound, no declared re-key added.
4. `./.venv311/bin/python -m ruff check firestarter/ tests/`, `ruff format --check`, `tools/check_mypy_watermark.py` (35/35), `tools/check_devtest_orchestrator.py`, `tools/check_diagnostic_report_claims.py` -- all exit 0.
5. Zero `#` comment lines added across both commits; `/workspaces/firestarter` porcelain empty.
</verification>

<success_criteria>
- A run whose transport failed reports a status-axis value distinct from the chip-verdict axis and that step does not carry a `BAD` chip verdict (ROADMAP criterion 1).
- The overall verdict and the auto-generated issue title for that run reflect the status-axis outcome (ROADMAP criterion 2).
- The status-axis value is additive and excluded from the `dedup_fingerprint` input, with no sixth `verdict` value introduced (ROADMAP criterion 3, first half -- the non-vacuous confirmation is plan `178-02`, the frozen-shape confirmation is plan `178-03`).
- The run still offers the submit prompt (ROADMAP criterion 4, mechanism half -- the asserted proof is plan `178-02`).
</success_criteria>

<output>
Create `.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-01-SUMMARY.md` when done.
</output>
