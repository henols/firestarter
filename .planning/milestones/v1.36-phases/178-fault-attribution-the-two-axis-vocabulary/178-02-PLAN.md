---
phase: 178-fault-attribution-the-two-axis-vocabulary
plan: 02
type: execute
wave: 2
depends_on: [178-01]
files_modified:
  - firestarter_app/tests/test_diagnostic_report.py
  - firestarter_app/tests/test_submit.py
  - firestarter_app/tests/test_chip_test.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
  - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/178-02-confirmation.txt
  - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/178-02-attr05.txt
  - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/178-02-nonchange.txt
autonomous: true
requirements: [ATTR-01, ATTR-02, ATTR-04, ATTR-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-10: the ATTR-04 confirmation carries a SECOND, POSITIVE leg -- two reports built identically except for their status-axis values produce the SAME `dedup_fingerprint`. A green Phase 174 oracle alone proves only that nothing already-frozen moved; it cannot prove the new field is out of the hash, because no frozen shape carried one before plan 178-03."
    - "D-10: Leg B varies the status axis ONLY -- both reports hold the identical `verdict` on every step, exactly as the shipped `test_durations_do_not_perturb_dedup_fingerprint` idiom does. Varying the verdict alongside the status makes the equality assertion false and the test proves nothing."
    - "D-08: the anti-vacuity sibling proves the equality is not trivially true -- two reports differing in a step VERDICT produce DIFFERENT `dedup_fingerprint` values, so Leg B is a measured exclusion rather than a hash that reads nothing."
    - "ATTR-04, adjacency edge: two reports whose step verdicts are exactly equal and whose status axes differ MERGE into one dedup group; two reports differing in a step verdict SEPARATE. The status axis never splits a group and never merges two the verdict axis separated."
    - "ATTR-04, empty edge: a report with zero `results` hashes identically with and without the status axis exercised, because the per-step loop never runs; the frozen `synthetic-arm4-empty-results` shape stays at `8d6208d00be7`. A single-element `results` list behaves the same way."
    - "ATTR-04, ordering edge: `dedup_fingerprint` builds `parts` in `report.results` order and the status axis appends nothing, so permuting statuses across a fixed verdict sequence changes neither the hash nor the pre-image length."
    - "D-11: `is_submittable` (`diagnostic_report.py:212-226`) is left byte-unchanged. The milestone research's T3 (`.planning/research/SUMMARY.md:132`, `FEATURES.md:348`) proposed a run-validity term on it; ATTR-05 and ROADMAP criterion 4 forbid it, ATTR-05 wins, T3 is VOID, and the non-change is asserted by test rather than assumed."
    - "D-11, ATTR-05: a transport-faulted report still reaches `confirm_fn`. Auto-classification changes the title and the disposition only; the offer to file always stands."
    - "ATTR-05, idempotency edge: calling `submit_report` twice on the same transport-faulted report reaches `confirm_fn` both times -- nothing in the status axis is consumed, cached, or one-shot."
    - "ATTR-05, concurrency edge: the three existing paths that return without asking -- the `is_submittable` refuse gate, the off-TTY print, and the prior-URL comment offer -- are unchanged and none gains a status-axis term, so no interleaving of runs can silence the offer."
    - "D-01: `_id_step_closes_gate` is byte-identical to its pre-phase form and still returns True for a transport-faulted step; `_baseline_closes_sdp_gate` is likewise untouched. The non-change is the asserted truth."
    - "D-01: the transport-fault arm constructs its `StepResult` DIRECTLY, never through `_skip_result` -- proved by AST, so a later reader cannot tidy it into the helper and silently stamp `STATUS_SKIP` over the ERROR discrimination."
    - "D-02: the three `STATUS_*` constants appear in neither `_ALL_OPS` nor `_MULTIWORD_OP_VALUES`, so `tests/test_op_registration_parity.py`'s AST inversion guard stays correct and no later reader registers a report value as an op string."
    - "D-16: `FP_TRANSPORT` is untouched and unreferenced by any code or test this phase adds. No test hand-constructs a `Fingerprint` with classification `transport` and calls it end-to-end coverage."
  prohibitions:
    - "No `#` comment is written into any test file this plan touches. Rationale goes in a docstring."
    - "The status axis must never become a silent suppressor. A transport-faulted run is filed exactly as often as a chip-faulted one -- suppression converts a visible wrong verdict into an invisible missing one."
    - "The status axis must never be used to grade confidence -- no softer FAIL, no percentage, no hedged verdict. Where no honest signal exists the report says so rather than guessing."
    - "No second submittability gate is introduced. A parallel run-validity predicate beside `is_submittable` would let the two disagree; ATTR-05 forbids gating at all."
    - "No `firestarter/` source file is modified by this plan. It is tests-only; a source change here would mean plan 178-01 shipped incomplete."
    - "Leg B never varies the verdict alongside the status. Holding the verdict constant is what makes the equality load-bearing."
    - "No file under `tests/fixtures/` is modified -- plan 178-03 owns the fixture registry and a concurrent edit here would collide with its frozen-shape work."
    - "No file in the `firestarter/` firmware submodule is modified."
  artifacts:
    - path: "firestarter_app/tests/test_diagnostic_report.py"
      provides: "Leg B (D-10), its anti-vacuity verdict sibling, and the ATTR-04 adjacency / empty / ordering edge legs"
      contains: "def test_status_axis_does_not_perturb_dedup_fingerprint"
    - path: "firestarter_app/tests/test_submit.py"
      provides: "The ATTR-05 non-suppression proofs -- `is_submittable` byte-unchanged and a transport-faulted report reaching `confirm_fn`, twice"
      contains: "def test_a_transport_faulted_report_still_reaches_the_confirm_prompt"
    - path: "firestarter_app/tests/test_chip_test.py"
      provides: "The D-01 safety-gate non-change assertion, the AST proof that the transport arm bypasses `_skip_result`, and the `run_status` fold's two legs"
      contains: "def test_id_step_closes_gate_predicate_is_unchanged_by_the_status_axis"
    - path: "firestarter_app/tests/test_chip_test_sdp_leg.py"
      provides: "The status proof for the three re-pointed precedence rows, added beside the three-tuple matrix rather than by widening it"
      contains: "def test_the_transport_precedence_rows_carry_the_error_status"
    - path: ".planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/178-02-confirmation.txt"
      provides: "The measured record of Leg B, its anti-vacuity sibling and the three ATTR-04 edges"
      contains: "legB_equal=True"
    - path: ".planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence/178-02-attr05.txt"
      provides: "The measured record that `is_submittable` carries no run-validity term and the prompt still fires twice"
      contains: "mentions_run_status=False"
  key_links:
    - from: "firestarter_app/tests/test_diagnostic_report.py::test_status_axis_does_not_perturb_dedup_fingerprint"
      to: "firestarter_app/firestarter/diagnostic_report.py::dedup_fingerprint"
      via: "two report clones differing only in StepResult.status, asserted equal -- the positive half D-10 requires"
      pattern: "dedup_fingerprint\\("
    - from: "firestarter_app/tests/test_submit.py::test_a_transport_faulted_report_still_reaches_the_confirm_prompt"
      to: "firestarter_app/firestarter/submit.py::submit_report"
      via: "confirm_fn injected as a Mock and asserted called on a report whose run_status is ERROR"
      pattern: "confirm_fn"
    - from: "firestarter_app/tests/test_chip_test.py::test_the_transport_arm_does_not_route_through_skip_result"
      to: "firestarter_app/firestarter/chip_test.py"
      via: "an ast walk over the (SerialError, HardwareOperationError) handler body asserting a direct StepResult call and no _skip_result call"
      pattern: "_skip_result"
---

<objective>
Make ATTR-04's confirmation non-vacuous and ATTR-05's non-suppression asserted rather than assumed.

Plan `178-01` wired the axis and left every frozen hash green. That greenness proves only that
nothing already frozen moved -- it cannot prove the status field is out of the hash, because no
frozen shape carries a status yet (D-10). This plan supplies the positive half: two reports that
differ ONLY in the status axis and hash identically, with an anti-vacuity sibling showing a verdict
change still moves the hash.

It also nails down the three things this phase must be measured NOT to have changed: the
destructive-write gate predicate (D-01), `is_submittable` (D-11, which voids the milestone
research's T3), and `FP_TRANSPORT` (D-16).

Purpose: a phase that closes on "the gate stayed green" without either leg has proved nothing about
the field it shipped, and a phase that quietly narrows the filing offer has converted a visible
wrong verdict into an invisible missing one.

Output: twelve named tests and two evidence files that make every one of those claims measurable.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-CONTEXT.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-RESEARCH.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-PATTERNS.md
@.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-01-SUMMARY.md
</context>

## Artifacts this phase produces

The full phase-178 symbol table lives in `178-01-PLAN.md` under this same heading. Symbols this
plan CREATES: `test_status_axis_does_not_perturb_dedup_fingerprint`,
`test_a_verdict_change_still_perturbs_dedup_fingerprint`,
`test_status_axis_does_not_perturb_an_empty_results_fingerprint`,
`test_status_axis_does_not_reorder_the_fingerprint_pre_image`,
`test_is_submittable_is_unchanged_by_the_status_axis`,
`test_a_transport_faulted_report_still_reaches_the_confirm_prompt`,
`test_transport_fault_carries_skipped_verdict_and_error_status`,
`test_run_status_folds_error_when_any_step_errored`,
`test_run_status_is_complete_when_no_step_errored`,
`test_the_transport_arm_does_not_route_through_skip_result`,
`test_id_step_closes_gate_predicate_is_unchanged_by_the_status_axis`,
`test_the_transport_precedence_rows_carry_the_error_status`, plus the evidence files
`178-02-confirmation.txt` and `178-02-attr05.txt`. Everything they import (`STATUS_*`,
`StepResult.status`, `run_status`, `DiagnosticReport.run_status`) is created by plan `178-01` and
does not exist at `firestarter_app @ 0a29d8b`.

## Spec-less probe arithmetic (no silent drops)

12 edge items surfaced == 10 authored into `must_haves` + 2 surfaced as flagged assumptions. This
plan authors 5 of the 10: ATTR-04 adjacency, ATTR-04 empty, ATTR-04 ordering, ATTR-05 idempotency,
ATTR-05 concurrency. The two `unclassified` rows (ATTR-01, ATTR-02) stay `unresolved` and are
carried as flagged assumptions in `178-01-PLAN.md`; they are not re-flagged here and not dropped.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Leg B -- prove the status axis is out of the hash, and prove the proof is not vacuous</name>
  <files>firestarter_app/tests/test_diagnostic_report.py</files>
  <read_first>
    - firestarter_app/tests/test_diagnostic_report.py lines 200-330 (`_minimal_report` and its `step_specs` contract) and lines 1690-1730 (`test_durations_do_not_perturb_dedup_fingerprint`, the exact idiom to clone -- note both of its reports hold `VERDICT_OK`)
    - firestarter_app/firestarter/diagnostic_report.py lines 248-300 (`dedup_fingerprint`, the five-entry allow-list and its own exclusion docstring)
    - firestarter_app/tests/test_blast_radius_invariance.py lines 188-200 (`_TRACER_SHAPE_ID`, `_TRACER_CANONICAL`) and lines 279-300 (the plain-slice pre-image test)
    - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-PATTERNS.md (Move 1, Hop 4 and Hop 5, including the structural note that the clone must hold the verdict constant)
  </read_first>
  <behavior>
    - Two reports built from identical `step_specs`, differing only in `results[0].status` (COMPLETE vs ERROR), produce equal `dedup_fingerprint` values.
    - Two reports built from identical `step_specs` but differing in `results[0].verdict` produce DIFFERENT `dedup_fingerprint` values -- the anti-vacuity half.
    - A report with `results == []` hashes identically whether or not a status is set, and a single-element `results` list behaves the same.
    - Permuting which step carries the ERROR status, across a fixed verdict sequence, leaves the hash invariant, and `dedup_fingerprint`'s source still holds exactly three `parts.append` sites and no reference to the status field.
  </behavior>
  <action>
Clone the shipped `duration_s` exclusion idiom at `tests/test_diagnostic_report.py:1698-1716` four
times, each varying exactly one dimension. No `#` comments; every rationale goes in the docstring.

`test_status_axis_does_not_perturb_dedup_fingerprint` (D-10, Leg B): build two reports from the SAME
`step_specs`, set one report's `results[0].status` to `STATUS_ERROR` and leave the other at the
`STATUS_COMPLETE` default, assert the two `dedup_fingerprint` values are equal. The docstring states
the constraint the shipped idiom carries: both reports hold the identical `verdict` on every step,
and varying the verdict alongside the status would make the assertion false and the test empty.

`test_a_verdict_change_still_perturbs_dedup_fingerprint`: the same two reports with statuses held
equal and one report's `results[0].verdict` moved from `VERDICT_OK` to `VERDICT_BAD`, asserted NOT
equal. Without this leg the equality above is satisfiable by a hash that reads nothing.

`test_status_axis_does_not_perturb_an_empty_results_fingerprint`: a report with `results == []` and
a report with a single-element `results` list, each hashed at the default status and at
`STATUS_ERROR`, all four asserted stable. In the same function cross-check the frozen shape --
`dedup_fingerprint(build_shape("synthetic-arm4-empty-results"))` still equals
`FROZEN_HASHES["synthetic-arm4-empty-results"]`, read from the registry rather than transcribed.

`test_status_axis_does_not_reorder_the_fingerprint_pre_image`: build a three-step report, hash it,
then assign `STATUS_ERROR` to each step in turn while holding every verdict fixed, and assert the
hash is invariant across all three permutations. In the same function assert that
`inspect.getsource(dedup_fingerprint)` still contains exactly three `parts.append` call sites and
no occurrence of the substring `status` -- the structural half of D-08, which catches a future
append for the status axis even where some shape happens to collide.
  </action>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_diagnostic_report.py` lists all four new node ids.
    - Two reports differing only in `results[0].status` yield equal `dedup_fingerprint` values.
    - Two reports differing only in `results[0].verdict` yield different `dedup_fingerprint` values.
    - A zero-`results` report's `dedup_fingerprint` is invariant under any status assignment, and `FROZEN_HASHES["synthetic-arm4-empty-results"]` reads `8d6208d00be7` and reproduces from `build_shape`.
    - `inspect.getsource(dedup_fingerprint)` contains exactly three `parts.append` occurrences and zero occurrences of the substring `status`.
    - `tests/test_diagnostic_report.py` passes in full, and the commit adds zero `#` comment lines.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence && mkdir -p "$E" && EV=$E/178-02-confirmation.txt && { echo "== 1 the two legs, measured"; (cd firestarter_app && ./.venv311/bin/python -c "
import inspect, sys
sys.path.insert(0, '.')
from firestarter.chip_test import STATUS_COMPLETE, STATUS_ERROR, VERDICT_BAD, VERDICT_OK, Plan, StepResult
from firestarter.diagnostic_report import AutoCapture, DiagnosticReport, TransportHealth, dedup_fingerprint
def rep(specs):
    return DiagnosticReport(auto_capture=AutoCapture(host_version='0.0.0', chip='X', protocol='1'), transport=TransportHealth(), plan=Plan(name='X'), results=[StepResult(op=o, verdict=v) for o, v in specs])
specs = [('id', VERDICT_OK), ('read', VERDICT_OK), ('write', VERDICT_OK)]
a = rep(specs); b = rep(specs); b.results[0].status = STATUS_ERROR
print('legB_equal=' + str(dedup_fingerprint(a) == dedup_fingerprint(b)))
c = rep(specs); c.results[0].verdict = VERDICT_BAD
print('antivacuity_differs=' + str(dedup_fingerprint(a) != dedup_fingerprint(c)))
e0 = rep([]); e1 = rep([])
print('empty_equal=' + str(dedup_fingerprint(e0) == dedup_fingerprint(e1)))
s0 = rep([('id', VERDICT_OK)]); s1 = rep([('id', VERDICT_OK)]); s1.results[0].status = STATUS_ERROR
print('single_equal=' + str(dedup_fingerprint(s0) == dedup_fingerprint(s1)))
base = dedup_fingerprint(a); perms = []
for i in range(3):
    p = rep(specs)
    for j, r in enumerate(p.results):
        r.status = STATUS_ERROR if j == i else STATUS_COMPLETE
    perms.append(dedup_fingerprint(p))
print('ordering_invariant=' + str(all(h == base for h in perms)))
src = inspect.getsource(dedup_fingerprint)
print('parts_append_count=' + str(src.count('parts.append')))
print('hash_reads_status=' + str('status' in src))
"); echo "rc=$?"; echo "== 2 the frozen empty-results shape"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter.diagnostic_report import dedup_fingerprint
from tests.fixtures.report_shapes import FROZEN_HASHES, build_shape
print('empty_shape_frozen=' + str(dedup_fingerprint(build_shape('synthetic-arm4-empty-results')) == FROZEN_HASHES['synthetic-arm4-empty-results']))
print('empty_shape_value=' + FROZEN_HASHES['synthetic-arm4-empty-results'])
"); echo "rc=$?"; echo "== 3 the named legs"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q --collect-only tests/test_diagnostic_report.py) > /tmp/178-02-collect.txt 2>&1; echo "rc=$?"; for t in test_status_axis_does_not_perturb_dedup_fingerprint test_a_verdict_change_still_perturbs_dedup_fingerprint test_status_axis_does_not_perturb_an_empty_results_fingerprint test_status_axis_does_not_reorder_the_fingerprint_pre_image; do echo "leg_${t}=$(/usr/bin/grep -c "$t" /tmp/178-02-collect.txt)"; done; echo "== 4 the module"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_diagnostic_report.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 4 && /usr/bin/grep -qx 'legB_equal=True' "$EV" && /usr/bin/grep -qx 'antivacuity_differs=True' "$EV" && /usr/bin/grep -qx 'empty_equal=True' "$EV" && /usr/bin/grep -qx 'single_equal=True' "$EV" && /usr/bin/grep -qx 'ordering_invariant=True' "$EV" && /usr/bin/grep -qx 'parts_append_count=3' "$EV" && /usr/bin/grep -qx 'hash_reads_status=False' "$EV" && /usr/bin/grep -qx 'empty_shape_frozen=True' "$EV" && /usr/bin/grep -qx 'empty_shape_value=8d6208d00be7' "$EV" && test "$(/usr/bin/grep -cE '^leg_[a-z_]+=0$' "$EV")" -eq 0 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than four `rc=0` lines in the evidence file; or `legB_equal=True` absent (the status axis reached the hash); or `antivacuity_differs=True` absent (the equality is vacuous -- the hash ignores the verdict too); or `hash_reads_status=False` absent; or `empty_shape_value` reads anything but `8d6208d00be7`; or any `leg_<name>=0` line present; or the evidence file contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- firestarter/ && git diff HEAD~1 --quiet -- tests/fixtures/ && ./.venv311/bin/python -m ruff check tests/test_diagnostic_report.py && ./.venv311/bin/python -m ruff format --check tests/test_diagnostic_report.py</automated>
    <fails_when>non-zero exit -- the added-comment count is not 0, `firestarter/` or `tests/fixtures/` shows a diff in a tests-only commit, or ruff prints a violation.</fails_when>
  </verify>
  <done>Leg B passes, its anti-vacuity sibling passes, the empty and ordering edges are asserted, and `dedup_fingerprint`'s source is structurally proved to carry no status append.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ATTR-05 -- the offer to file always stands, and T3 is recorded void</name>
  <files>firestarter_app/tests/test_submit.py</files>
  <read_first>
    - firestarter_app/firestarter/submit.py lines 590-640 (`submit_report`'s signature and documented step ordering) and lines 700-745 (the prior-URL branch, the dedup-failure note, the `confirm_fn` call site)
    - firestarter_app/firestarter/diagnostic_report.py lines 205-230 (`is_submittable`)
    - firestarter_app/tests/test_submit.py lines 620-700 (`_make_report` and the seam-injection idiom, including the `find_prior_report_fn` and `isatty_fn` mocks)
    - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-RESEARCH.md (section 5, Finding F-1 and the S-1/S-2/S-3 suppressor census)
  </read_first>
  <behavior>
    - `is_submittable`'s source mentions none of `status`, `run_status`, `STATUS_ERROR`, `run_valid`, and its truth table is unchanged: True only when `chip`, `protocol` and `host_version` are all truthy, irrespective of any status-axis value on the report.
    - `submit_report` on a report whose `run_status` is ERROR, with a TTY and no prior URL, calls `confirm_fn` exactly once.
    - A second `submit_report` call on the same report calls a fresh `confirm_fn` mock once again -- the offer is not one-shot.
    - The title that run would file contains `INCONCLUSIVE (harness)` while the prompt still fired: auto-classification changed the title and disposition and changed nothing about whether the offer was made.
  </behavior>
  <action>
Author the ATTR-05 non-suppression proofs. This is the leg that stops the milestone research's T3
from creeping back in: `.planning/research/SUMMARY.md:132` and `FEATURES.md:348` list a run-validity
term on `is_submittable` as a Phase 178 deliverable, ATTR-05 and ROADMAP criterion 4 forbid it,
ATTR-05 wins, T3 is void (D-11), and the non-change is asserted rather than assumed. No `#` comments.

`test_is_submittable_is_unchanged_by_the_status_axis`: read `is_submittable`'s source with
`inspect.getsource` and assert it contains none of the four run-validity tokens named above; then
assert its truth table directly -- True for a fully-populated `AutoCapture`, and False when any one
of `chip` / `protocol` / `host_version` is falsy -- with the report's `run_status` set to
`STATUS_ERROR` in every case. The docstring records T3 as void and names ATTR-05 as the requirement
that wins.

`test_a_transport_faulted_report_still_reaches_the_confirm_prompt`: reuse the module's existing
`_make_report()` plus seam-injection idiom. Set `report.run_status = STATUS_ERROR` and give one
result `status = STATUS_ERROR` with `verdict = VERDICT_SKIPPED`, then call `submit_report` with
`confirm_fn=Mock(return_value=False)`, `isatty_fn=Mock(return_value=True)` and
`find_prior_report_fn=Mock(return_value=(None, True))`, and assert `confirm_fn.assert_called_once()`.
In the SAME test call `submit_report` a second time with a fresh `confirm_fn` mock and assert it is
called once again -- the ATTR-05 idempotency edge. Add a third assertion block asserting the title
`submit.build_title` produces for that report contains `INCONCLUSIVE (harness)`, so the two halves
of ATTR-05 are proved together.
  </action>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_submit.py` lists both new node ids.
    - `inspect.getsource(is_submittable)` contains none of the substrings `status`, `run_status`, `STATUS_ERROR`, `run_valid`.
    - `submit_report` with a status-ERROR report, a TTY and no prior URL calls `confirm_fn` exactly once, and calls a fresh mock once again on a second invocation.
    - `submit.build_title` for that report contains the substring `INCONCLUSIVE (harness)`.
    - `tests/test_submit.py` passes in full and the commit adds zero `#` comment lines.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence && mkdir -p "$E" && EV=$E/178-02-attr05.txt && { echo "== 1 is_submittable carries no run-validity term"; (cd firestarter_app && ./.venv311/bin/python -c "
import inspect, sys
sys.path.insert(0, '.')
from firestarter.diagnostic_report import AutoCapture, is_submittable
src = inspect.getsource(is_submittable)
for tok in ('status', 'run_status', 'STATUS_ERROR', 'run_valid'):
    print('mentions_' + tok + '=' + str(tok in src))
full = AutoCapture(host_version='1', chip='x', protocol='7')
print('full_true=' + str(is_submittable(full)))
print('no_chip_false=' + str(is_submittable(AutoCapture(host_version='1', chip='', protocol='7')) is False))
print('no_proto_false=' + str(is_submittable(AutoCapture(host_version='1', chip='x', protocol='')) is False))
print('no_host_false=' + str(is_submittable(AutoCapture(host_version='', chip='x', protocol='7')) is False))
"); echo "rc=$?"; echo "== 2 the named legs"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q --collect-only tests/test_submit.py) > /tmp/178-02-submit-collect.txt 2>&1; echo "rc=$?"; for t in test_is_submittable_is_unchanged_by_the_status_axis test_a_transport_faulted_report_still_reaches_the_confirm_prompt; do echo "leg_${t}=$(/usr/bin/grep -c "$t" /tmp/178-02-submit-collect.txt)"; done; echo "== 3 the module"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_submit.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 3 && /usr/bin/grep -qx 'mentions_status=False' "$EV" && /usr/bin/grep -qx 'mentions_run_status=False' "$EV" && /usr/bin/grep -qx 'mentions_STATUS_ERROR=False' "$EV" && /usr/bin/grep -qx 'mentions_run_valid=False' "$EV" && /usr/bin/grep -qx 'full_true=True' "$EV" && /usr/bin/grep -qx 'no_chip_false=True' "$EV" && /usr/bin/grep -qx 'no_proto_false=True' "$EV" && /usr/bin/grep -qx 'no_host_false=True' "$EV" && test "$(/usr/bin/grep -cE '^leg_[a-z_]+=0$' "$EV")" -eq 0 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than three `rc=0` lines; or any `mentions_<token>=True` line present (a run-validity term reached `is_submittable` -- T3 resurrected); or any of the four truth-table lines missing; or either `leg_<name>=0` line present; or the evidence file contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- firestarter/ && git diff HEAD~1 --quiet -- tests/fixtures/ && ./.venv311/bin/python -m ruff check tests/test_submit.py && ./.venv311/bin/python -m ruff format --check tests/test_submit.py</automated>
    <fails_when>non-zero exit -- the added-comment count is not 0, `firestarter/` or `tests/fixtures/` shows a diff in a tests-only commit, or ruff prints a violation.</fails_when>
  </verify>
  <done>`is_submittable` is measured byte-unchanged with T3 recorded void, and a transport-faulted report is proved to reach `confirm_fn` on both a first and a second invocation while its title reads `INCONCLUSIVE (harness)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The measured non-changes -- the safety gate, the helper bypass, and the dead bucket</name>
  <files>firestarter_app/tests/test_chip_test.py, firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - firestarter_app/firestarter/chip_test.py lines 1925-1960 (`_id_step_closes_gate`, `_baseline_closes_sdp_gate`), lines 1100-1115 (`_skip_result`), lines 2500-2560 (the exception-arm chain)
    - firestarter_app/tests/test_chip_test_sdp_leg.py lines 330-470 (`_PRECEDENCE_EXCEPTION_CLASSES`, `_derive_precedence_row`, both matrices, `_INTENDED_PRECEDENCE_DELTA`, and the delta-gate tests that must stay green untouched)
    - firestarter_app/tests/test_op_registration_parity.py lines 60-120 and 320-375 (`_ALL_OPS`, `_MULTIWORD_OP_VALUES`, the declared non-registries and the AST inversion guard)
    - .planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-RESEARCH.md (section 7, Finding F-2 and Pitfall P-5; section 11, consumer C-1)
  </read_first>
  <behavior>
    - `_id_step_closes_gate` returns True for a `StepResult` carrying `verdict=VERDICT_SKIPPED, status=STATUS_ERROR`, and its source text contains the two-element verdict tuple exactly as before -- no status term was added to the predicate.
    - An AST walk over `chip_test.py` finds the `(SerialError, HardwareOperationError)` handler body constructing `StepResult` directly, with zero `_skip_result` calls inside that handler.
    - `run_status` returns ERROR when any result carries `STATUS_ERROR` and COMPLETE otherwise, including for an empty results list.
    - None of `STATUS_COMPLETE`, `STATUS_ERROR`, `STATUS_SKIP` appears in `_ALL_OPS` or `_MULTIWORD_OP_VALUES`.
    - The three re-pointed precedence rows carry `status == STATUS_ERROR` on the derived `StepResult`, asserted beside the three-tuple matrix rather than by widening it.
  </behavior>
  <action>
Author the non-change proofs. Every one of these asserts something this phase deliberately did NOT
do, which is why each needs a test: an unasserted non-change is an assumption. No `#` comments.

In `tests/test_chip_test.py`:

`test_transport_fault_carries_skipped_verdict_and_error_status` -- run `run_plan` on a plan whose
first operator call raises `SerialError`, and assert the produced `StepResult` carries
`verdict == VERDICT_SKIPPED`, `status == STATUS_ERROR`, a non-empty `reason`, and `error_code is
None`.

`test_id_step_closes_gate_predicate_is_unchanged_by_the_status_axis` (D-01) -- assert
`_id_step_closes_gate` returns True for that `StepResult`, and assert via `inspect.getsource` that
the predicate's body still reads the two-element `(VERDICT_BAD, VERDICT_SKIPPED)` tuple and mentions
neither `status` nor `STATUS_`. The docstring states why: `marginal` and `NA` both leave this gate
OPEN, which would admit a destructive write against a chip whose identity was never confirmed, and
`SKIPPED` was chosen precisely so the predicate needs no edit.

`test_the_transport_arm_does_not_route_through_skip_result` -- parse `chip_test.py` with `ast`,
locate the `ExceptHandler` whose type tuple names `SerialError` and `HardwareOperationError`, and
assert its body contains at least one direct `StepResult(...)` call and zero calls named
`_skip_result`. `_skip_result` stamps `STATUS_SKIP`; routing the transport arm through it would
silently erase the ERROR discrimination this phase exists to create.

`test_run_status_folds_error_when_any_step_errored` and
`test_run_status_is_complete_when_no_step_errored` -- the fold's two legs, the second including the
empty-list case. Add an assertion in the second that none of the three `STATUS_*` values is a member
of `_ALL_OPS` or `_MULTIWORD_OP_VALUES`, mirroring the `SDP_HOLD_*` precedent (D-02).

In `tests/test_chip_test_sdp_leg.py`, add `test_the_transport_precedence_rows_carry_the_error_status`
beside the existing precedence tests: for each of `SerialError`, `SerialTimeoutError` and
`HardwareOperationError`, inject the exception exactly as `_derive_precedence_row` does and assert
the resulting blank-check `StepResult` carries `status == STATUS_ERROR`, while
`EpromOperationError`'s result carries `status == STATUS_COMPLETE` -- the second arm is a genuine
chip/firmware finding and must not have acquired an error status. Do NOT widen
`_derive_precedence_row`'s three-tuple and do NOT touch `_PRE_EDIT_PRECEDENCE_MATRIX` or
`_INTENDED_PRECEDENCE_DELTA`; widening the tuple would force a rewrite of the Phase-133 before-image.

Finally, assert D-16 mechanically in `tests/test_chip_test.py`: a `/usr/bin/grep`-equivalent scan
performed in Python over the files this phase touched finds zero occurrences of `FP_TRANSPORT`
outside `chip_test.py`'s own untouched constant block and its classifier, and no test in this phase
constructs a `Fingerprint` whose classification is `transport`.
  </action>
  <acceptance_criteria>
    - `pytest --collect-only` lists all six new node ids across the two modules.
    - `_id_step_closes_gate(StepResult(op="id", verdict="SKIPPED", status="ERROR"))` returns True.
    - `inspect.getsource(_id_step_closes_gate)` contains `VERDICT_BAD, VERDICT_SKIPPED` and contains neither `status` nor `STATUS_`.
    - The AST walk finds exactly one `ExceptHandler` naming both `SerialError` and `HardwareOperationError`, with at least one `StepResult` call and zero `_skip_result` calls in its body.
    - `run_status([])` returns `COMPLETE`; `run_status` over a list containing one ERROR-status result returns `ERROR`.
    - No `STATUS_*` value is a member of `_ALL_OPS` or `_MULTIWORD_OP_VALUES`.
    - The three transport exception classes each derive a blank-check `StepResult` with `status == "ERROR"`, and `EpromOperationError`'s derives `status == "COMPLETE"`.
    - `test_exception_precedence_matrix`, `test_precedence_matrix_delta_is_exactly_intended` and `test_precedence_matrix_deriver_is_non_vacuous` all still pass, and `_PRE_EDIT_PRECEDENCE_MATRIX` and `_INTENDED_PRECEDENCE_DELTA` show zero diff for this commit.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/178-fault-attribution-the-two-axis-vocabulary/evidence && mkdir -p "$E" && EV=$E/178-02-nonchange.txt && { echo "== 1 the gate, the bypass and the fold"; (cd firestarter_app && ./.venv311/bin/python -c "
import ast, inspect, pathlib, sys
sys.path.insert(0, '.')
from firestarter import chip_test as ct
r = ct.StepResult(op='id', verdict=ct.VERDICT_SKIPPED, status=ct.STATUS_ERROR, reason='half-seated cable')
print('gate_closes=' + str(ct._id_step_closes_gate(r)))
g = inspect.getsource(ct._id_step_closes_gate)
print('gate_tuple_intact=' + str('VERDICT_BAD, VERDICT_SKIPPED' in g))
print('gate_mentions_status=' + str('status' in g or 'STATUS_' in g))
tree = ast.parse(pathlib.Path(ct.__file__).read_text(encoding='utf-8'))
hits = []
for node in ast.walk(tree):
    if isinstance(node, ast.ExceptHandler) and isinstance(node.type, ast.Tuple):
        names = {n.id for n in node.type.elts if isinstance(n, ast.Name)}
        if {'SerialError', 'HardwareOperationError'} <= names:
            calls = [c.func.id for c in ast.walk(node) if isinstance(c, ast.Call) and isinstance(c.func, ast.Name)]
            hits.append((calls.count('StepResult'), calls.count('_skip_result')))
print('transport_handlers=' + str(len(hits)))
print('handler_stepresult_calls=' + str(hits[0][0] if hits else -1))
print('handler_skipresult_calls=' + str(hits[0][1] if hits else -1))
print('fold_empty=' + ct.run_status([]))
print('fold_error=' + ct.run_status([ct.StepResult(op='id', verdict=ct.VERDICT_OK), r]))
print('fold_complete=' + ct.run_status([ct.StepResult(op='id', verdict=ct.VERDICT_OK)]))
"); echo "rc=$?"; echo "== 2 the status values are not op strings"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from firestarter.chip_test import STATUS_COMPLETE, STATUS_ERROR, STATUS_SKIP
from tests.test_op_registration_parity import _ALL_OPS, _MULTIWORD_OP_VALUES
vals = {STATUS_COMPLETE, STATUS_ERROR, STATUS_SKIP}
print('status_in_all_ops=' + str(bool(vals & set(_ALL_OPS))))
print('status_in_multiword=' + str(bool(vals & set(_MULTIWORD_OP_VALUES))))
"); echo "rc=$?"; echo "== 3 the named legs"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q --collect-only tests/test_chip_test.py tests/test_chip_test_sdp_leg.py) > /tmp/178-02-nc-collect.txt 2>&1; echo "rc=$?"; for t in test_transport_fault_carries_skipped_verdict_and_error_status test_id_step_closes_gate_predicate_is_unchanged_by_the_status_axis test_the_transport_arm_does_not_route_through_skip_result test_run_status_folds_error_when_any_step_errored test_run_status_is_complete_when_no_step_errored test_the_transport_precedence_rows_carry_the_error_status; do echo "leg_${t}=$(/usr/bin/grep -c "$t" /tmp/178-02-nc-collect.txt)"; done; echo "== 4 the modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_op_registration_parity.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 4 && /usr/bin/grep -qx 'gate_closes=True' "$EV" && /usr/bin/grep -qx 'gate_tuple_intact=True' "$EV" && /usr/bin/grep -qx 'gate_mentions_status=False' "$EV" && /usr/bin/grep -qx 'transport_handlers=1' "$EV" && /usr/bin/grep -qx 'handler_skipresult_calls=0' "$EV" && /usr/bin/grep -qx 'handler_stepresult_calls=1' "$EV" && /usr/bin/grep -qx 'fold_empty=COMPLETE' "$EV" && /usr/bin/grep -qx 'fold_error=ERROR' "$EV" && /usr/bin/grep -qx 'fold_complete=COMPLETE' "$EV" && /usr/bin/grep -qx 'status_in_all_ops=False' "$EV" && /usr/bin/grep -qx 'status_in_multiword=False' "$EV" && test "$(/usr/bin/grep -cE '^leg_[a-z_]+=0$' "$EV")" -eq 0 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV"</automated>
    <fails_when>fewer than four `rc=0` lines; or `gate_closes=True` / `gate_tuple_intact=True` / `gate_mentions_status=False` missing (the safety predicate moved); or `handler_skipresult_calls=0` missing (the transport arm was routed through the SKIP-stamping helper); or `transport_handlers=1` missing (the handler was split or duplicated); or a `status_in_*=True` line present; or any `leg_<name>=0` line present; or the evidence file contains `no tests ran` or a line matching `[0-9]+ (failed|error)`.</fails_when>
    <automated>cd /workspaces/firestarter_app && test "$(git diff HEAD~1 -- tests/ | /usr/bin/grep -E '^\+' | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && git diff HEAD~1 --quiet -- firestarter/ && git diff HEAD~1 --quiet -- tests/fixtures/ && test "$(git diff HEAD~1 -- tests/test_chip_test_sdp_leg.py | /usr/bin/grep -cE '^[-+].*(_PRE_EDIT_PRECEDENCE_MATRIX|_INTENDED_PRECEDENCE_DELTA)')" -eq 0 && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/</automated>
    <fails_when>non-zero exit -- the added-comment count is not 0, `firestarter/` or `tests/fixtures/` shows a diff, the Phase-133 before-image or the intended-delta set was edited (a non-zero count of diff lines naming either), or ruff prints a violation.</fails_when>
  </verify>
  <done>The destructive-gate predicate, the `_skip_result` bypass, the `run_status` fold, the op-registry disjointness and the three re-pointed precedence rows are each asserted by a named test, and the Phase-133 before-image is proved untouched.</done>
</task>

</tasks>

<threat_model>
ASVS level 1, blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host process to public GitHub issue | `submit_report` sends a sanitized `to_dict()` payload and an argv-passed title to a public tracker |
| host engine to destructive write | `_id_step_closes_gate` decides whether a write proceeds against a chip whose identity was not confirmed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-178-02 | Tampering | `chip_test._id_step_closes_gate` | high | mitigate | Task 3 asserts the predicate returns True for the transport-faulted `StepResult` AND that its source still reads the two-element `(VERDICT_BAD, VERDICT_SKIPPED)` tuple with no status term -- so a future "tidy-up" that widens the predicate to consult the status axis, or narrows it, reddens here. |
| T-178-04 | Denial of information | `submit_report`'s filing ask | high | mitigate | Task 2 asserts `is_submittable` carries none of the four run-validity tokens and that `confirm_fn` fires on a status-ERROR report, twice. T3 is recorded void in the test docstring, so a later reader cannot resurrect it by citing the milestone research. |
| T-178-05 | Tampering | `chip_test._skip_result` | medium | mitigate | Task 3's AST leg asserts zero `_skip_result` calls inside the transport handler. Routing the arm through the helper would stamp `STATUS_SKIP` and silently downgrade an invalid run to a merely-skipped one. |
| T-178-01 | Information disclosure | `submit.sanitize_dict` | medium | mitigate | Both new export keys ride `to_dict()`, the only input the sanitizer deep-scrubs; `tests/test_submit.py` passes in full against a schema-1.8 body, which re-runs every `_SCRUBS` test against the new payload. |
| T-178-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. D-03 adopts the OCP vocabulary by name only and forbids a new runtime dependency; `pyproject.toml` is asserted unchanged by plan 178-01. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_diagnostic_report.py tests/test_submit.py tests/test_chip_test.py tests/test_chip_test_sdp_leg.py tests/test_op_registration_parity.py -o addopts="" -q -p no:randomly` -- all green.
2. `./.venv311/bin/python tools/snapshot_report_shapes.py --check` -- still 17 matching snapshots; this plan touched no fixture.
3. `cd /workspaces && python3 tools/rekey/check_rekey_ledger.py` -- exit 0, 8 rows bound.
4. `ruff check tests/`, `ruff format --check tests/` -- green. Zero `#` comment lines added across all three commits.
5. `git diff` across this plan's commits shows zero changed lines under `firestarter/` and `tests/fixtures/`.
</verification>

<success_criteria>
- The status-axis exclusion from `dedup_fingerprint` is confirmed by a POSITIVE leg, not only by a green invariance oracle (ROADMAP criterion 3).
- A run with a genuine chip fault and a run with a transport-only fault both still offer the submit prompt; auto-classification changes the title and disposition only (ROADMAP criterion 4).
- The destructive-write gate is measured still closed on the transport path, so ROADMAP criterion 1 is met without opening a safety hole.
</success_criteria>

<output>
Create `.planning/phases/178-fault-attribution-the-two-axis-vocabulary/178-02-SUMMARY.md` when done.
</output>
