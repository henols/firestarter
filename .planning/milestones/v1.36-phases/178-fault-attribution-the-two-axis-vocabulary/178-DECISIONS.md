# Phase 178 Plan 03 — Decisions

D-178-1: Option A (real-path) -- build `attr01-status-axis-transport-fault` via `_build_real_path_report(chip=..., write_scope="full", operator=<a `_fixed_return_operator()` whose `check_eprom_id.side_effect` is a `SerialError`>, runs=2)`, because it exercises the ACTUAL re-pointed exception arm, the actual destructive-gate closure and the actual downstream `_skip_result` cascade rather than freezing a hand-specified `StepResult` combination the engine might never produce; the accepted cost is that, like every other real-path shape in the corpus, its hash stays coupled to `derive_plan` and therefore to future `chip_database.json` regeneration. Option B (a new `statuses=` seam on `build_shape_from_step_specs`) was rejected -- an unused fixture parameter would be exactly the dead-vocabulary failure mode D-16 records for `FP_TRANSPORT`.
