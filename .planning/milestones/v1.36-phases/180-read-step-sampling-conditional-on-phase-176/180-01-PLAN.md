---
phase: 180-read-step-sampling-conditional-on-phase-176
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_readback_inventory.py
  - firestarter_app/tests/test_chip_test.py
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-verdict-pin-red.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-behavioural-legs.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-one-connect-pin.txt
autonomous: true
requirements: [PRUNE-08]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`_dispatch_read`'s `verdict=` expression is pinned by an `ast` test asserting the exact name set `[VERDICT_BAD, VERDICT_OK, last_ok]`, so a verdict that also reads the read-vs-read comparison reddens it (D-06 leg 3, roadmap criterion 3)"
    - "A last full read returning False yields VERDICT_BAD; a first read returning False with a passing last read yields VERDICT_OK — both proven through the real `run_plan`, not by calling `_dispatch_read` directly (D-06 legs 1 and 2)"
    - "One `operator.read_eprom` call is pinned to exactly one `_operation_context`, which connects through `_setup_operation`/`find_and_connect` and calls `_disconnect_programmer` inside a non-empty `finalbody` (Ruling 1; D-02's structural half)"
    - "Both new structural pins have been observed RED against a planted in-memory string mutation, each anchor's uniqueness asserted first, no fixture file written, transcripts on disk under `evidence/` (D-07)"
    - "`180-PRUNE-08-CLOSURE.md` exists and states the close's load-bearing argument in connect counts a reader can recompute — the enumerated 64 KiB block list and N = log2(size) - 6 (D-01, D-03)"
    - "No read-rate or wire-time figure from `.planning/notes/dev-test-sequence-cost-model.md` appears in the closing document, so the published argument rests on no modelled number (D-04, Ruling 3)"
    - "Nothing under `firestarter_app/firestarter/`, nothing in the `firestarter` firmware submodule, and no byte of `chip_database.json` changed"
    - "`tokenize` COMMENT-token counts stay at 621 for `tests/test_chip_test.py` and 0 for `tests/test_readback_inventory.py` — no `#` comment was added to either module"
  prohibitions:
    - "A gate must not read green while the rule it guards is violated"
    - "The closing document must not present a modelled figure as a measured one"
    - "The closing document must not state a per-connect cost that blends the two board classes"
  artifacts:
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      provides: "PRUNE-08's verdict and the recomputable connect arithmetic"
      contains: "## The connect arithmetic"
    - path: "firestarter_app/tests/test_readback_inventory.py"
      provides: "D-06 leg 3 (verdict-source pin) and the one-connect premise pin, each with a planted-mutation RED leg"
      contains: "_dispatch_read"
    - path: "firestarter_app/tests/test_chip_test.py"
      provides: "D-06 legs 1 and 2 — the positive half of roadmap criterion 3"
      contains: "run_plan"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-verdict-pin-red.txt"
      provides: "GREEN-at-HEAD / RED-at-planted-mutant transcript for the verdict pin"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-behavioural-legs.txt"
      provides: "the leg1 / leg2 / control matrix through the real run_plan"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-one-connect-pin.txt"
      provides: "GREEN-at-HEAD / RED-at-planted-mutant transcript for the one-connect pin"
  key_links:
    - from: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      to: ".planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-MEASUREMENT.md"
      via: "cites 4a/4b as the per-board-class per-connect cost and 7 as the licence to spend it"
      pattern: "176-MEASUREMENT"
    - from: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      to: "firestarter_app/tests/test_readback_inventory.py"
      via: "names the one-connect pin as the structural evidence behind the arithmetic"
      pattern: "test_readback_inventory"
    - from: "firestarter_app/tests/test_readback_inventory.py"
      to: "firestarter_app/firestarter/eprom_operations.py"
      via: "ast parse of the module source resolved from the imported module's __file__"
      pattern: "eprom_operations"
---

<objective>
Pin the two facts the whole PRUNE-08 close rests on, and open the closing document on the argument
they carry.

Fact one is roadmap criterion 3's: `_dispatch_read`'s verdict comes from the **last full read** and
from nothing derived from the read-vs-read comparison (D-06). Fact two is D-02's structural half: one
`operator.read_eprom` call pays exactly **one** full connect, because `_operation_context` connects on
entry and disconnects in its `finally` block (Ruling 1). Each is pinned by test, each pin is observed
RED against a planted counter-example (D-07), and the closing document is opened with the verdict and
the connect arithmetic those pins make honest (D-01, D-03).

Purpose: the close's load-bearing premises stop being prose assertions and become gates. Phase 177's
`177-READBACK-INVENTORY.md` closed PRUNE-04 the same way and named the gate that would redden.
Output: two additive tests in `tests/test_readback_inventory.py`, two in `tests/test_chip_test.py`,
three evidence transcripts, and `180-PRUNE-08-CLOSURE.md` with its verdict and arithmetic sections.

**This phase ships zero lines of sampling code and zero product-source edits.** No bit-structured
sampler, no block list in code, no bit-shift boundary computation, no escalate-on-divergence path, no
change to `_dispatch_read`'s behaviour. It is NOT hardware-gated (D-02) — no bench wave.
</objective>

<orchestrator_dispositions>
Three questions the researcher raised were ruled on by the orchestrator before planning. Each is
recorded here as a one-line disposition the operator can override at review time.

- **Ruling 1 (RESEARCH.md C-7, line 302) — the one-connect premise gets a structural pin.**
  CONTEXT.md's `<code_context>` instructs it directly (*"pin it by test or AST rather than asserting
  it in prose"*); the `<domain>` out-of-scope list does not forbid it. It is not a fifth deliverable —
  it is how deliverable 1's load-bearing premise is stated honestly. Implemented as Task 3, same
  module, no new import. **Its docstring must state that it is a static pin.**
- **Ruling 3 (RESEARCH.md C-6, line 287) — the closing document leads with the model-free
  connect-count form.** The factor-of-two-and-a-half illustration reproduces only by supplying the
  full read's wire time from a **modelled** figure that D-04 bounds. It is **omitted entirely** here;
  the published argument is 10 connects versus 1, which needs no wire time at all. Task 1's verify
  makes "the load-bearing form requires no modelled figure" checkable.
- **Ruling 2 (seed R4's now-false sentence) — implemented in plan `180-02`, Task 2.** Recorded here so
  the three dispositions read together.

Two measured corrections are folded in: **C-1** (drop the blanket "all supported" — 10 of 746 rows are
not; state the supported counts per bucket) lands in plan `180-02`; **C-4** (state the block list by
enumeration, not by the seed's loose range notation) lands in Task 1 below.
</orchestrator_dispositions>

<assumption_delta_decision>
- **noun:** the read step's two full sweeps (unchanged)
- **decision:** `no-change`
- **rationale:** the `pluralization` signal is a false positive on the descriptive word "second" in the
  ROADMAP goal's phrase *"the read step's second full sweep"*, which names an existing, unchanged step
  rather than a singular-to-plural transition; this phase ships zero product-code change, so no
  identity model moves. No promote, no add-alongside, no invariant test.
</assumption_delta_decision>

<flagged_assumptions>
The deterministic edge probe over PRUNE-08's requirement text returned **1 item**, `category:
unclassified`, `status: unresolved`, `probe: "unclassified — review manually"`. Per the spec-less
probe fallback an `unclassified` row stays `unresolved` — never auto-resolved with a backstop, never
auto-dismissed. It is surfaced here rather than silently dropped:

- **FA-01 (unresolved, unclassified) — PRUNE-08 has no data/behaviour-shape edge to classify.** The
  requirement's text is a conditional go/no-go over a *measurement*, not over an input domain: it
  names no boundary, adjacency, encoding or ordering the probe's shape taxonomy can reach. The close
  is therefore verified by the goal-backward `must_haves` above and by the D-06 / Ruling-1 pins, not
  by an edge-coverage criterion. **If the operator disagrees, the edge to author is the one this plan
  deliberately does not carry: what the verdict should be at a device size above the crossover** —
  which D-05 declines to re-file as a requirement.

**No-silent-drop equality:** 1 probe-surfaced edge == 0 authored into `must_haves.truths` + 1 surfaced
as a flagged assumption. Holds.

**Prohibition recall (run in-prompt — there is no compiled engine).** Stage 1 over-produced roughly
ten raw must-NOTs; Stage 2 dropped the routine-engineering ones (must not break the suite, must not
widen the diff, must not leave a stale line number) and kept **5** values/transparency items,
distributed across this phase's three plans: *a gate must not read green while the rule it guards is
violated*; *the closing document must not present a modelled figure as a measured one*; *the closing
document must not state a per-connect cost that blends the two board classes* (this plan); *the seed
must not be left in a state from which the rejected sampling design can be regenerated*; *the close
must not overstate its evidence* (plan 02); *PRUNE-08 must not be marked Complete before its closing
evidence exists* (plan 03). **Canon-referral:** none — no OWASP, prototype-pollution, path-traversal,
injection, GDPR or generic-fairness candidate surfaced, so nothing was minted or breadcrumbed. All
five are authored descriptor-less in `must_haves.prohibitions`, never in `truths`.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-CONTEXT.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md
@/workspaces/.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-MEASUREMENT.md
@/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md
</context>

<environment>
Two git repositories are in play and **their branch names differ** — do not assume they match.

- Meta repo `/workspaces` — branch `gsd/v1.36-dev-test-fidelity-planning`. Owns `.planning/`.
- Submodule `/workspaces/firestarter_app` — branch `gsd/v1.36-dev-test-fidelity`, app HEAD `ffb0060`.
  Owns `tests/`. **Commit inside the submodule**, on that branch.
- Submodule `/workspaces/firestarter` (firmware) — **not touched**. Must stay porcelain-clean.

Always `./.venv311/bin/python` from `/workspaces/firestarter_app` — the devcontainer default is 3.12
and app CI runs 3.11 only, a divergence that has already broken beta CI once.

Shell `grep` in this devcontainer is **ugrep and honours `.gitignore`**, so it silently under-scans.
Every evidence-grade scan in this plan uses `/usr/bin/grep`.

`pytest` `addopts` is `-ra -q`; a count line requires `-o addopts=""`. Doubling `-q` hides it.
</environment>

<comment_discipline>
`/workspaces/CLAUDE.md` §"Source code comments — hard rule" and `firestarter_app/CLAUDE.md:5-21` are
**non-overridable by this plan**, and `firestarter_app/tests/` counts as product source. Write **no
`#` comments at all** into either test module. Every rationale — including each new pin's claim and
its stated ceiling — goes into a **docstring**, which is how these modules already carry their
reasoning (`test_readback_inventory.py`: 0 comments, 4 docstrings, several paragraphs of rationale in
the module docstring alone).

`test_read_step_disagreement_is_divergence_metric_not_marginal`, the sibling Task 2 sits beside,
carries four `#` comments, two of them trailing and two citing a GSD decision identifier. Those are
**pre-existing — do not "fix" them** (out of scope, widens the diff) and **do not copy their style**.

The inherited `phase_added_comments` regex leg matches only lines whose first non-space character is
`#`, so a trailing comment ships green under it. Every comment assertion in this plan uses the
`tokenize` COMMENT-token delta gate instead, baselines `tests/test_chip_test.py = 621` and
`tests/test_readback_inventory.py = 0`, both re-measured at app HEAD `ffb0060`.
</comment_discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — pin the read verdict to the last full read, prove the pin RED, and open the closing document on the connect arithmetic</name>
  <precondition>`/workspaces/firestarter_app` is on branch `gsd/v1.36-dev-test-fidelity` with a clean working tree, and `/workspaces/firestarter` is porcelain-clean.</precondition>
  <files>
    firestarter_app/tests/test_readback_inventory.py,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-verdict-pin-red.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py` — the module being modified, in full (139 lines). Reuse `_engine_source()` at :95 verbatim; reuse the planted-mutation idiom at :109-115; match the whole-set assertion style at :101-106.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:2767-2812` — `_dispatch_read` as it stands: `last_ok` initialised at 2778, rebound at 2783 inside the run loop, the verdict at 2808, `divergence` computed at 2789-2803 and never feeding the verdict.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md` section 3 — the AST extraction skeleton and the three load-bearing details of the planted-mutation idiom.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-RESEARCH.md` section "The Arithmetic" (line 455) — the enumerated block list, the N derivation, and the model-free statement to write.
    - `/workspaces/.planning/phases/176-transport-instrumentation-connect-cost-measurement-partially/176-MEASUREMENT.md` sections 4a, 4b, 6 and 7 — the measured figures, the never-blend rule, the validation ceiling, and the hand-off naming PRUNE-08 as the consumer.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the closing form to mirror, and its "Search method" section, which states how a population was enumerated so a reader can re-run it.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/evidence/177-03-readback-census.txt` — the evidence transcript form: numbered section headers, flat key=value scalars, verbatim pytest tail, explicit rc after each command.
  </read_first>
  <behavior>
    - The pin, at HEAD: the `verdict=` keyword of the single `StepResult(...)` call inside `_dispatch_read` resolves to an `ast.IfExp` whose sorted `ast.Name` id set equals exactly `["VERDICT_BAD", "VERDICT_OK", "last_ok"]`.
    - The pin, against the planted mutant: the same extraction over a source string whose verdict expression also consults the divergence term yields a four-element name set, and the pin's equality claim raises `AssertionError`.
    - Anchor uniqueness: the anchor occurs exactly once in `chip_test.py`, asserted before the mutation so the replacement cannot silently no-op.
    - Vacuity: the pin asserts set **equality**, never membership — a pin checking only that `last_ok` is present stays green on the mutant, which is the exact failure roadmap criterion 3 names.
  </behavior>
  <action>
Add exactly two module-level test functions to `firestarter_app/tests/test_readback_inventory.py`, plus one module-level anchor constant and one private helper. No new import is needed — `ast`, `pathlib`, `pytest` and the `chip_test` module alias are already present at :33-40.

Helper `_verdict_expression_names(source)`: parse `source` with `ast.parse`; find the `ast.FunctionDef` named `_dispatch_read`; find the single `ast.Call` inside it whose `func` is an `ast.Name` with id `StepResult`; take the keyword whose `arg` is `verdict`; return the sorted set of `ast.Name` ids reachable by `ast.walk` from that keyword's value. Its docstring states that extraction is unambiguous because `_dispatch_read` contains exactly one `StepResult(...)` call.

Module-level constant `_VERDICT_ANCHOR`, the verdict line verbatim with its eight-space indent and trailing newline: `        verdict=VERDICT_OK if last_ok else VERDICT_BAD,` — measured to occur exactly once in the whole of `chip_test.py`.

Test `test_read_verdict_expression_reads_only_the_last_full_read_result`: call `_engine_source()` (reuse it verbatim; write no second resolver — this project has a recorded checker that resolved directory-relative, scanned nothing and exited 0) and assert `_verdict_expression_names(source)` equals the exact sorted list `["VERDICT_BAD", "VERDICT_OK", "last_ok"]`. Assert the whole list, never membership. Its docstring states the claim (D-06 leg 3, roadmap criterion 3: the read step's verdict source is the last full read, never anything derived from the read-vs-read comparison), cites the two existing behavioural siblings in `tests/test_chip_test.py` at :2141 and :2166 that already cover criterion 3's negative half rather than restating them, and states the pin's ceiling: it is a static claim about the shape of the verdict expression.

Test `test_a_planted_divergence_term_in_the_verdict_reddens_the_pin`: assert `source.count(_VERDICT_ANCHOR) == 1` first; build the mutant by replacing the anchor once, rewriting its condition so the verdict also consults the divergence name; assert `_verdict_expression_names(mutant)` is the four-element set that additionally contains `divergence`; and assert the pin's own equality claim raises `AssertionError` on the mutant, using `pytest.raises` in the style of the module's existing vacuity leg at :118-120. Strings in memory only — write no fixture file. Its docstring states that the mutant is the literal failure mode roadmap criterion 3 names: a verdict that silently picks up the comparison.

Then create `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` with a title line and exactly these two sections, in this order.

`## The verdict` — state, in the requirement's own vocabulary and bolded mid-paragraph as `177-READBACK-INVENTORY.md` does, that **PRUNE-08 closes as measured, not worth doing** (D-01), taking roadmap success criterion 2, and that criterion 1 is not taken because the measurement makes board class a non-discriminator. Cite `176-MEASUREMENT.md` section 4a (Uno-class median 2.518 s, remainder 0.018 s), section 4b (Leonardo-class median 2.607 s, remainder 0.107 s), the shared 2.500 s structural floor, and the 0.089 s gap between the classes. Quote section 4a/4b's own never-blend rule and honour it: state each figure per board class and never average them. Cite section 6 as the reason board class is a non-discriminator while recording that section 6 falsified the Uno-dominance hypothesis and attributes no mechanism, so this document does not claim to know why. Cite section 7 as the licence to spend the measurement, and note that section 7's "small relative to the floor" remark is about the *remainders*, not the connect cost — a sampler pays the whole 2.518 s / 2.607 s, floor included, once per block.

`## The connect arithmetic` — the recomputable core, written so a reader re-derives rather than believes (this is the analogue of 177's "Search method" section). It must contain, in order: (a) the enumerated 64 KiB block list on one line, exactly `0x0000 0x0100 0x0200 0x0400 0x0800 0x1000 0x2000 0x4000 0x8000 0xFF00`, labelled as block 0, the eight power-of-two boundaries strictly inside the device, and the top block; (b) the derivation `N = log2(size) - 6`, equal to 10 at the 64 KiB reference size, with the note that reading the seed's range notation literally yields 11 and contradicts the seed's own stated answer, so the enumeration is authoritative (C-4); (c) the size table for 2 KiB / 32 KiB / 64 KiB / 128 KiB / 256 KiB / 512 KiB giving N and sample bytes; (d) the three reference parts this milestone tests — SST27SF512, W27C512, M27C512 — each 65536 B, each N of 10, each `supported`; (e) the structural premise, that `EpromOperator._operation_context` connects on entry through `_setup_operation` and `find_and_connect` and calls `_disconnect_programmer` in its `finally` block, so each `read_eprom` call pays exactly one full connect, naming `firestarter_app/tests/test_readback_inventory.py`'s one-connect pin as the test that holds it; and (f) the load-bearing sentence, stating that replacing the second full sweep substitutes 10 whole `read_eprom` calls for 1, so the sample costs `10 connects where the sweep it replaces costs 1` — a net plus-nine connects per read step — before a single byte of its 2560 B moves. Then state the measured unit per class and never blended: ten connects is 25.18 s on the measured Uno-class board and 26.07 s on the measured Leonardo-class board.

**Publish no figure from `.planning/notes/dev-test-sequence-cost-model.md`** (D-04, Ruling 3): no read rate, no wire time, no per-operation overhead, and omit the factor-of-two-and-a-half illustration entirely. The argument above is complete without one, which is exactly the robustness CONTEXT.md's `<specifics>` says to lead with — even a badly wrong rate leaves ten connects dearer than one. Cite `chip_test.py:2767` for `_dispatch_read` and `:2851` for `_read_region`; do not inherit 177's stale `:2642` / `:2843`.

Finally write `evidence/180-01-verdict-pin-red.txt` in the 177 transcript form: numbered section headers, flat key=value scalars, the verbatim pytest tail and an explicit rc line after each command. It must record `anchor_count=1`, the HEAD name list, the planted name list, `pin_at_head=GREEN`, `pin_at_planted=RED`, and the `tests/test_readback_inventory.py` pytest tail showing 8 passed.

Commit the test change inside `/workspaces/firestarter_app` on `gsd/v1.36-dev-test-fidelity`; commit the document and the transcript in `/workspaces` on `gsd/v1.36-dev-test-fidelity-planning`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^8 passed'</automated>
    <fails_when>non-zero exit, or the pytest summary line reports any count other than exactly `8 passed` (6 pre-existing plus the 2 added here) — a count of 6 means neither new test was collected, 7 means one is missing.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md; test -s "$C" && /usr/bin/grep -qF '## The verdict' "$C" && /usr/bin/grep -qF '## The connect arithmetic' "$C" && /usr/bin/grep -qF '0x0000 0x0100 0x0200 0x0400 0x0800 0x1000 0x2000 0x4000 0x8000 0xFF00' "$C" && /usr/bin/grep -qF '10 connects where the sweep it replaces costs 1' "$C" && /usr/bin/grep -qF '2.518' "$C" && /usr/bin/grep -qF '2.607' "$C" && /usr/bin/grep -qF '176-MEASUREMENT' "$C"</automated>
    <fails_when>non-zero exit; the closing document is absent or empty, or one of the two required headings, the enumerated block list, the model-free connect sentence, either per-board-class median, or the citation of the measurement document is missing.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md; test -s "$C" && ! /usr/bin/grep -qF 'KB/s' "$C" && ! /usr/bin/grep -qF '2.56' "$C" && ! /usr/bin/grep -qiE '^[[:space:]]*(TBD|TODO|undecided)[[:space:]]*$' "$C"</automated>
    <fails_when>non-zero exit — the closing document is missing or empty (the leading existence guard is what stops the three negative greps passing vacuously on an absent file), or a modelled read-rate figure, a blended per-connect number, or a bare placeholder line is present in it.</fails_when>
    <automated>cd /workspaces && test -s .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-verdict-pin-red.txt && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-verdict-pin-red.txt; /usr/bin/grep -qF 'anchor_count=1' "$E" && /usr/bin/grep -qF 'pin_at_head=GREEN' "$E" && /usr/bin/grep -qF 'pin_at_planted=RED' "$E"</automated>
    <fails_when>non-zero exit — the RED-proof transcript is missing, empty, or does not record all three scalars, which means the pin was never observed red against the planted mutant.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_readback_inventory.py` contains `def test_read_verdict_expression_reads_only_the_last_full_read_result(` and `def test_a_planted_divergence_term_in_the_verdict_reddens_the_pin(`.
    - The pin asserts equality against the full sorted list, not membership: `tests/test_readback_inventory.py` contains `["VERDICT_BAD", "VERDICT_OK", "last_ok"]`.
    - `./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q` prints `8 passed`.
    - `180-PRUNE-08-CLOSURE.md` contains the headings `## The verdict` and `## The connect arithmetic`.
    - `180-PRUNE-08-CLOSURE.md` contains the literal block list `0x0000 0x0100 0x0200 0x0400 0x0800 0x1000 0x2000 0x4000 0x8000 0xFF00` and the literal sentence fragment `10 connects where the sweep it replaces costs 1`.
    - `180-PRUNE-08-CLOSURE.md` contains both `2.518` and `2.607` and does not contain `2.56`.
    - `180-PRUNE-08-CLOSURE.md` does not contain the literal `KB/s`.
    - `evidence/180-01-verdict-pin-red.txt` contains `anchor_count=1`, `pin_at_head=GREEN` and `pin_at_planted=RED`.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing, and `git -C /workspaces/firestarter_app status --porcelain firestarter/` prints nothing.
  </acceptance_criteria>
  <done>The read step's verdict source is a gate rather than a docstring claim, that gate has been seen red against the exact mutation roadmap criterion 3 names, and the closing document states the 10-versus-1 connect argument in a form a reader can recompute without any modelled figure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two behavioural legs — the verdict is the LAST full read's return value, proven through the real run_plan</name>
  <files>
    firestarter_app/tests/test_chip_test.py,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-behavioural-legs.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_chip_test.py:2135-2180` — the two existing siblings `test_read_step_disagreement_is_divergence_metric_not_marginal` (:2141, ends :2164) and `test_read_step_agreement_no_divergence_recorded` (:2166, ends :2172), and the following test at :2177 that fixes the insertion point.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:1019-1035` — `_mock_operator()`, a `Mock(spec=_OPERATOR_METHODS)` whose `read_eprom.return_value` is preset to `True`.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:1103-1112` — `_plan_with_steps(*steps)` and `_result(results, op)`; and `:307` for `_REAL_DB`.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:1428-1442` — `_writes_bytes_to_output_file(data)`, which hard-codes a `True` return and therefore cannot serve these legs as-is.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md` section 2 — the comment-stripped analog and the one adaptation these legs require.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:2767-2812` — why `last_ok` holds the last run's return value and why `divergence` never reaches the verdict.
  </read_first>
  <behavior>
    - Leg 1: `read_eprom` returns `True` then `False` across two runs; the read step's verdict is `VERDICT_BAD`.
    - Leg 2: `read_eprom` returns `False` then `True`; the read step's verdict is `VERDICT_OK`.
    - Both legs assert `run_count == 2`, so a leg that silently collapsed to one run cannot pass.
    - Both legs go through the real `run_plan`; `_dispatch_read` is never called directly.
  </behavior>
  <action>
Add exactly two module-level test functions to `firestarter_app/tests/test_chip_test.py`, placed immediately after `test_read_step_agreement_no_divergence_recorded` (which ends at :2172) and before `test_write_step_attaches_fingerprint_with_region_start_addr_base` (:2177). Flat module-level functions, no class, no fixture, no parametrize, matching the file's naming convention of long names that assert the claim.

Name them `test_read_step_last_run_failure_yields_bad` and `test_read_step_first_run_failure_with_passing_last_run_yields_ok`.

Both build their operator with `_mock_operator()` — never a hand-rolled double, which drifts from `_OPERATOR_METHODS` and silently stops constraining the call signature. Neither `read_eprom.return_value` nor `_writes_bytes_to_output_file` can vary a return per call, so each leg installs an inline side-effect closure on `operator.read_eprom.side_effect`, adapting the disagreement test's `call_count` counter-dict idiom at :2145-2153 but making the **return value** the per-call variable: a `call_returns` list read modulo its length, indexed by the counter dict, with the counter incremented on each call. The side effect's signature must be `(_name, _eprom_data, output_file=None, **_kwargs)` and it must write bytes to `output_file` whenever `output_file` is truthy — a leg that writes no file leaves `run_bytes` empty and silently changes what is under test.

Leg 1 uses `call_returns` of `[True, False]` and asserts the read step's `verdict` equals `VERDICT_BAD`. Leg 2 uses `[False, True]` and asserts the verdict equals `VERDICT_OK`. Both build the plan with `_plan_with_steps(Step(op=OP_READ, supported=True, reason=""))`, call `run_plan(plan, operator, _REAL_DB, runs=2)`, extract the step with `_result(results, OP_READ)`, and additionally assert `run_count == 2`.

Each test's docstring states its claim and why the pair matters together: leg 1 alone proves a failing last read produces a bad verdict, and leg 2 is what discriminates last-read from first-read or from any fold across runs — together they prove the verdict is the last full read's return value. Each docstring also cites, rather than restates, the two existing siblings that already cover roadmap criterion 3's negative half (two diverging reads yield `VERDICT_OK`, never `VERDICT_MARGINAL`, with a positive divergence count recorded). Do not duplicate those tests.

These legs go through the real `run_plan` deliberately: its dispatcher returns `_dispatch_read(...)` directly for the read op with no post-processing, and Phase 178's transport arm keys on exceptions, not on a `False` return, so nothing intercepts the failing read. Do not call `_dispatch_read` directly.

Then write `evidence/180-01-behavioural-legs.txt` in the 177 transcript form: numbered section headers, flat key=value scalars, verbatim pytest tail, explicit rc line. It must record a four-row matrix — leg1, leg2 and the two controls (both reads passing, both reads failing) — each row giving the return sequence, the resulting verdict, the expected verdict and the observed `run_count`, plus the pytest tail for the read-step selection showing 4 passed.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_chip_test.py -o addopts="" -q -k "read_step" 2>&1 | /usr/bin/grep -qE '^4 passed'</automated>
    <fails_when>non-zero exit, or the summary line reports any count other than `4 passed` — `2 passed` means neither new leg was collected, and any `failed` count means a leg's verdict assertion did not hold through the real run_plan.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import io,tokenize;B={'tests/test_chip_test.py':621,'tests/test_readback_inventory.py':0};bad=[p for p,e in B.items() if sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)!=e];print('phase_added_comments='+('0' if not bad else ','.join(bad)));raise SystemExit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file is where a `#` comment was added, including a trailing one the inherited regex leg cannot see.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-behavioural-legs.txt; test -s "$E" && test "$(/usr/bin/grep -cE '^(leg1|leg2|ctl)' "$E")" -ge 4</automated>
    <fails_when>non-zero exit — the transcript is missing or does not carry all four matrix rows, so the control arms were never recorded alongside the two legs.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_chip_test.py` contains `def test_read_step_last_run_failure_yields_bad(` and `def test_read_step_first_run_failure_with_passing_last_run_yields_ok(`.
    - Both new functions appear between line 2172 and the definition of `test_write_step_attaches_fingerprint_with_region_start_addr_base`.
    - Both new functions call `run_plan(` and neither calls `_dispatch_read(` directly.
    - Both new functions assert `run_count == 2`.
    - `./.venv311/bin/python -m pytest tests/test_chip_test.py -o addopts="" -q -k "read_step"` prints `4 passed`.
    - The `tokenize` COMMENT-token count for `tests/test_chip_test.py` is still exactly 621.
    - This task's own commit inside `/workspaces/firestarter_app` touches `tests/test_chip_test.py` and no file under `firestarter/`, checked with an explicit pinned range `<sha>^..<sha>` for that commit (record the sha in the SUMMARY; never anchor on a relative `HEAD~N`, which in a repository with parallel sessions can name another session's commit).
    - `evidence/180-01-behavioural-legs.txt` carries four matrix rows, each naming a return sequence, a resulting verdict, an expected verdict and a run count.
  </acceptance_criteria>
  <done>Roadmap criterion 3's positive half is proven: a failing last read gives a bad verdict, a failing first read with a passing last read gives an OK verdict, and the pair together shows the verdict is the last full read's return value and nothing folded across runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the one-connect-per-read premise the whole arithmetic rests on (Ruling 1), and prove that pin RED</name>
  <files>
    firestarter_app/tests/test_readback_inventory.py,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-one-connect-pin.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py` — as left by Task 1. Note the module docstring at :1-31, the `_engine_source()` resolver at :95-98, the planted-mutation idiom at :109-115, the emptied-expectation vacuity leg at :118-120, and `test_verify_eprom_signature_names_the_replacement_primitive` at :123, the precedent that this module legitimately reaches into `eprom_operations`.
    - `/workspaces/firestarter_app/firestarter/eprom_operations.py:454-560` — `_setup_operation` (defined :454) calling `SerialCommunicator.find_and_connect` at :499; `_operation_context` (:515) calling `_setup_operation` at :531, its `try:` at :545, its `yield` at :547, its `finally:` at :548 and `self._disconnect_programmer()` at :550; `_disconnect_programmer` defined :552.
    - `/workspaces/firestarter_app/firestarter/eprom_operations.py:896-912` — `read_eprom` at :896, whose entire body is a single context opened at :905, with no loop, no second context and no re-entry; and note that the near-identical block at :1837 differs only in its size argument, which is why the planted anchor must be chosen for uniqueness.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-RESEARCH.md` section "Structural Premise" (line 394) — the three clauses an honest pin must assert and the ceiling its docstring must state.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md` section 3 — the pin (b) design and the source excerpt with comments stripped.
  </read_first>
  <behavior>
    - Clause 1, the clause that carries the arithmetic: `read_eprom`'s `FunctionDef` contains exactly one `ast.With` item whose `context_expr` is a `Call` on an `Attribute` named `_operation_context`.
    - Clause 2: `_operation_context`'s body calls `_setup_operation`, and `_setup_operation`'s body calls `find_and_connect`.
    - Clause 3: `_operation_context` has an `ast.Try` node with a non-empty `finalbody`, and `_disconnect_programmer` is called from within that `finalbody`, not merely somewhere in the function.
    - Against the planted mutant, in which `read_eprom`'s single `with` header gains a second `_operation_context` item, clause 1 measures 2 and the pin raises `AssertionError`.
  </behavior>
  <action>
Add exactly two module-level test functions to `firestarter_app/tests/test_readback_inventory.py`, plus one sibling source resolver, one helper and one anchor constant. No new import is needed: `EpromOperator` is already imported at :40, so `eprom_operations` is reachable through its `__module__`, or import the module alias alongside the existing `chip_test` alias if that reads more plainly.

Sibling resolver `_operations_source()`: read the `eprom_operations` module source with `pathlib.Path(...).read_text(encoding="utf-8")` resolved from that module's own `__file__`, never from this test file's directory, and assert the text is longer than 1000 characters so the pin cannot pass on a file it never read. Do **not** reuse `_engine_source()` for a different module — write the sibling.

Helper `_read_eprom_connect_shape(source)`: parse with `ast.parse` and return the three clause results as a small tuple or dict — the count of `_operation_context` context expressions inside `read_eprom`'s `FunctionDef`; whether `_operation_context` calls `_setup_operation` and `_setup_operation` calls `find_and_connect`; and whether `_operation_context` has a `Try` with a non-empty `finalbody` whose nodes contain a call to `_disconnect_programmer`. Clause 3 must scan the `finalbody` nodes specifically, not the whole function — asserting the call site is inside the `finalbody` is the difference between a real pin and a decorative one.

Module-level constant `_CONTEXT_ANCHOR`, the two-line tail of `read_eprom`'s context header verbatim, comprising the twelve-space `size_str,` argument line followed by the eight-space `) as (cmd_data, _, op_name):` line, each with its trailing newline. This exact two-line form occurs exactly once in `eprom_operations.py`; the near-identical block at :1837 differs in its size argument and does not match.

Test `test_one_read_eprom_call_costs_exactly_one_connect`: call `_operations_source()` and assert all three clauses — the context count equals 1, the connect chain holds, and the disconnect call is inside the `finalbody`. Its docstring states the claim in the closing document's own terms (one `read_eprom` call pays exactly one full connect, so an N-block sample pays N connects, which is the premise the connect arithmetic in `180-PRUNE-08-CLOSURE.md` rests on) and states the ceiling honestly and explicitly: **this is a static pin.** It proves the code is shaped so one call opens one context that connects and disconnects; it does not prove at runtime that exactly one serial open occurred, and it says nothing about the retry-across-ports walk inside `find_and_connect`. That ceiling is sufficient because the closing argument needs a statement about shape, not about a runtime trace.

Test `test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin`: assert `source.count(_CONTEXT_ANCHOR) == 1` first so the mutation cannot silently no-op; build the mutant with a single bounded `replace(..., 1)` that extends the `with` header with a second `_operation_context` item bound to a throwaway name; assert the mutated clause-1 count is 2; and assert the pin's own clause-1 claim raises `AssertionError` on the mutant, using `pytest.raises` in the style of the module's existing vacuity leg. Strings in memory only — write no fixture file. Its docstring states that the mutant is the literal counter-example the arithmetic cares about: a read that pays two connects instead of one.

Finally, update the module docstring so it is not left stale. Its leg-count sentence is now wrong — there are four anti-vacuity legs in the module after this phase (the census plant, the emptied allow-list, and the two new planted mutants) — and the docstring must also name the two new pins and, for the one-connect pin, restate its static-pin ceiling. Rewrite that sentence; do not append a contradicting one.

Then write `evidence/180-01-one-connect-pin.txt` in the 177 transcript form. It must record `anchor_count=1`, `head_contexts=1`, `planted_contexts=2`, `connect_chain=True`, `disconnect_in_finalbody=True`, `pin_at_head=GREEN`, `pin_at_planted=RED`, and the `tests/test_readback_inventory.py` pytest tail showing 10 passed.

Commit inside `/workspaces/firestarter_app`; commit the transcript in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q 2>&1 | /usr/bin/grep -qE '^10 passed'</automated>
    <fails_when>non-zero exit, or the summary line reports any count other than `10 passed` — `8 passed` means neither new function was collected, and any `failed` count means a clause did not hold against the real module source.</fails_when>
    <automated>cd /workspaces/firestarter_app && ! /usr/bin/grep -qF 'Two anti-vacuity legs' tests/test_readback_inventory.py && /usr/bin/grep -qF 'static pin' tests/test_readback_inventory.py && /usr/bin/grep -qF 'finalbody' tests/test_readback_inventory.py</automated>
    <fails_when>non-zero exit — the module docstring still carries its stale leg count, the one-connect pin's ceiling is not stated, or clause 3 does not reach the `finalbody` and is therefore decorative.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import io,tokenize;B={'tests/test_chip_test.py':621,'tests/test_readback_inventory.py':0};bad=[p for p,e in B.items() if sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)!=e];print('phase_added_comments='+('0' if not bad else ','.join(bad)));raise SystemExit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file is where a `#` comment was added, including a trailing one the inherited regex leg cannot see.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-one-connect-pin.txt; test -s "$E" && /usr/bin/grep -qF 'head_contexts=1' "$E" && /usr/bin/grep -qF 'planted_contexts=2' "$E" && /usr/bin/grep -qF 'disconnect_in_finalbody=True' "$E" && /usr/bin/grep -qF 'pin_at_planted=RED' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing, or one of the four scalars is absent, which means the pin's discrimination between head and mutant was not observed.</fails_when>
    <automated>test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain firestarter/)"</automated>
    <fails_when>non-zero exit — either command substitution is non-empty, meaning the firmware submodule or the app's product-source tree (including the generated `chip_database.json`) was modified by this phase.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_readback_inventory.py` contains `def test_one_read_eprom_call_costs_exactly_one_connect(` and `def test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin(`.
    - `tests/test_readback_inventory.py` contains `_operations_source` and the string `finalbody`.
    - `tests/test_readback_inventory.py` does not contain the literal `Two anti-vacuity legs`, and does contain the literal `static pin`.
    - `./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q` prints `10 passed`.
    - The `tokenize` COMMENT-token count for `tests/test_readback_inventory.py` is still exactly 0.
    - `evidence/180-01-one-connect-pin.txt` contains `head_contexts=1`, `planted_contexts=2`, `disconnect_in_finalbody=True` and `pin_at_planted=RED`.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>The premise the entire closing argument rests on — one read costs one connect — is held by a structural test with three real clauses, that test has been seen red against a read that pays two connects, and its docstring says plainly what a static pin can and cannot prove.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths this phase **creates** (they do not exist at app HEAD `ffb0060` or meta HEAD
`f031be81`, so a drift check must not flag them as unresolved references):

**New test functions** — `test_read_verdict_expression_reads_only_the_last_full_read_result`,
`test_a_planted_divergence_term_in_the_verdict_reddens_the_pin`,
`test_read_step_last_run_failure_yields_bad`,
`test_read_step_first_run_failure_with_passing_last_run_yields_ok`,
`test_one_read_eprom_call_costs_exactly_one_connect`,
`test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin`.

**New test helpers and constants** — `_verdict_expression_names`, `_VERDICT_ANCHOR`,
`_operations_source`, `_read_eprom_connect_shape`, `_CONTEXT_ANCHOR`.

**New documents** —
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md`.

**New evidence files** (the `evidence/` directory itself does not exist yet) —
`180-01-verdict-pin-red.txt`, `180-01-behavioural-legs.txt`, `180-01-one-connect-pin.txt`,
`180-02-closure-claims.txt`, `180-02-seed-amendment.txt`, `180-03-requirement-marking.txt`,
`180-03-phase-seal.txt`, all under
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/`.

**New seed section** — `## Status: Phase 180 amendment` in
`.planning/seeds/dev-test-adaptive-sequencing.md` (plan `180-02`).
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. Stated honestly: this phase ships **zero product-source code**, adds no
dependency, opens no network or serial path, parses no untrusted input, and touches no credential,
secret or user data. Its only executable artifacts are six additive pytest functions that `ast`-parse
first-party source strings in memory. The real attack surface is documentary — a planning record that
a future planner will read as instruction.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta repo `.planning/` ↔ `firestarter_app` submodule | The only cross-repo coupling: a test in the app repo is cited by a document in the meta repo as the gate that would redden. Nothing crosses at runtime; the coupling is a citation that can go stale. |
| planning record → a future planner | The closing document and the seed are read as instructions by the next milestone. A false statement left standing here is the only thing in this phase that can cause harm downstream. |
| first-party source file → `ast.parse` in a test | Test-time only, no execution of the parsed text, no file written, no I/O beyond reading a module already imported into the same process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-01 | Tampering | the inherited `phase_added_comments` regex gate | medium | mitigate | Replace the `^\+[[:space:]]*#` leg with the `tokenize` COMMENT-token delta gate, baselines 621 / 0 at app HEAD `ffb0060`, so a trailing comment cannot ship green past a non-overridable project rule. Used in Task 2 and Task 3. |
| T-180-02 | Repudiation | `evidence/180-*.txt` transcripts | low | mitigate | Every claim is a flat key=value scalar plus the verbatim command tail and an explicit rc, so a reader re-runs and diffs rather than trusting. Both structural pins additionally record their planted-mutant RED observation. |
| T-180-03 | Tampering | `firestarter_app/firestarter/`, the `firestarter` firmware submodule, the generated `chip_database.json` | low | mitigate | Task 3's cleanliness leg asserts all three byte-unchanged; the same leg runs again in the plan `180-03` phase seal. |
| T-180-04 | Information Disclosure | the six new pytest functions | low | accept | They parse a first-party module already imported into the test process; no file is written, no socket or serial port is opened, no secret is read. |
| T-180-SC | Tampering | npm / pip / cargo installs | low | accept | This phase installs no package. Every module used — `ast`, `tokenize`, `io`, `pathlib` — is Python standard library, so no Package Legitimacy Audit is required and no install checkpoint is inserted. |

No threat is rated `high`, so nothing here blocks.
</threat_model>

<verification>
1. `tests/test_readback_inventory.py` reports `10 passed`; `tests/test_chip_test.py -k read_step` reports `4 passed`.
2. Both new structural pins were observed RED against their planted mutant, transcripts on disk.
3. `180-PRUNE-08-CLOSURE.md` carries `## The verdict` and `## The connect arithmetic`, states the enumerated block list and the 10-versus-1 sentence, quotes both per-board-class medians, blends neither, and publishes no modelled read-rate figure.
4. `tokenize` COMMENT-token counts unchanged at 621 / 0.
5. `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain firestarter/` both print nothing.

**Do not** run the eight-leg gate battery, invoke `check_rekey_ledger.py`, or assert a suite floor of
2241 or 2242 anywhere in this phase. The re-key checker was deleted, the battery is **seven** legs, and
the measured floor is **2239** before this phase's six additive tests. The full battery runs once, in
plan `180-03`.
</verification>

<success_criteria>
- Roadmap criterion 3 is discharged for this plan's share: the read step's verdict source is pinned by three tests (two behavioural, one structural) and cannot silently become a sample's verdict.
- The one-connect premise is pinned structurally and its ceiling is stated (Ruling 1).
- Every new gate has an on-disk RED transcript (D-07).
- The closing document exists with a verdict and an arithmetic a reader can recompute (D-01, D-03), carrying no modelled figure (D-04, Ruling 3).
- Zero product-source and zero firmware lines changed; zero `#` comments added.
</success_criteria>

<output>
Create `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-01-SUMMARY.md` when done.
Record in it: the measured pytest counts, the two RED observations, any line number that had moved
since this plan was written, and — per `/workspaces/CLAUDE.md` — any rationale that would otherwise
have been a source comment.
</output>
