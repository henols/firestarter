---
phase: 180-read-step-sampling-conditional-on-phase-176
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .planning/seeds/dev-test-adaptive-sequencing.md
  - firestarter_app/tests/test_readback_inventory.py
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-seed-regeneration-closed.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-one-connect-pin-hardened.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-verdict-pin-hardened.txt
autonomous: true
requirements: [PRUNE-08]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A planner reading only `.planning/seeds/dev-test-adaptive-sequencing.md` can no longer regenerate the rejected sampling design (D-09, the one `partial` must-have in `180-VERIFICATION.md`): the seed carries zero occurrences of each of the ten named fragments of the rejected sampler's escalation trigger, stated-cost argument and block enumeration, measured by `/usr/bin/grep -cF` per fragment"
    - "The connect-count objection D-09 requires preserved survives the removal: the seed still carries `Measured and rejected (Phase 180, PRUNE-08)`, `10 connects`, `_operation_context` and the `180-PRUNE-08-CLOSURE.md` citation"
    - "Nothing in the seed outside R3's body and the Phase 180 amendment section moved: the frontmatter's four fields, R1 through R3's heading, R4 through the out-of-scope section, and the whole Phase 177 amendment section are each byte-identical to the pinned pre-edit blob `7a47a1fd9236bb4068bc72be6c8f8ed35f86e300`, proven by `cmp` on sed-extracted regions rather than by inspection"
    - "The Phase 180 amendment section records the removal as a dated follow-through opening `**Follow-through, gap closure.**`, with its own restated not-touched list, so the seed's own account of which paragraphs moved is not left false"
    - "`test_one_read_eprom_call_costs_exactly_one_connect` now fails on a stray connect route anywhere in `read_eprom`'s body, not only on a second `_operation_context` item in its `with` header (WR-01): `_read_eprom_connect_shape` returns `connect_route_calls` and the pin asserts it equals 1"
    - "`test_read_verdict_expression_reads_only_the_last_full_read_result` now fails on a `last_ok` reassignment the `verdict=` expression cannot see (WR-02): `_last_ok_assignment_shape` returns `tags == ['const_true', 'read_eprom_call']` at HEAD and the pin asserts that equality, plus `read_assign_in_for_loop is True` and `for_loop_count == 1`"
    - "Each hardening was SEEN RED against a planted counter-example the PRE-hardening pin passes: for WR-01 the mutant leaves `context_count == 1` while `connect_route_calls` reads 2; for WR-02 the mutant leaves `_verdict_expression_names` at exactly `['VERDICT_BAD', 'VERDICT_OK', 'last_ok']` while `tags` gains a third `other` entry — both discriminations asserted inside the new anti-vacuity tests and both recorded with rc values in committed transcripts"
    - "Neither pin's docstring claims more than its check proves: the verdict pin's docstring no longer contains `the structural half that catches` and does contain `assigned exactly twice`; the one-connect pin's docstring names `connect_route_calls`"
    - "The stale sibling line-number citation is gone (IN-01): `tests/test_readback_inventory.py` matches the pattern for a parenthesised line citation zero times and still names `test_read_step_last_run_failure_yields_bad`"
    - "`tests/test_readback_inventory.py` reports 12 passed, up from the measured 10 at app HEAD `93a1672`"
    - "Zero lines under `firestarter_app/firestarter/` changed, zero lines in the `firestarter` firmware submodule changed, and `firestarter/data/chip_database.json` byte-unchanged"
    - "No `#` comment was added: `tokenize` COMMENT counts stay exactly 621 for `tests/test_chip_test.py` and 0 for `tests/test_readback_inventory.py`"
  prohibitions:
    - statement: "The seed must not be left in a state from which the rejected sampling design can be regenerated. This is the prohibition `180-VERIFICATION.md` recorded as `disposition: unresolved`; it is closed here by removal, not by annotation."
      status: required
      verification: judgment
    - statement: "The rejection verdict must not be softened into an annotation. A retained paragraph fronted by a historical banner is the exact route D-09 rejects as insufficient — outvoted but not absent — and must not be planned or written. Nor may the connect-count objection be weakened, shortened or moved out of R3 while the paragraphs around it are removed."
      status: required
      verification: judgment
    - statement: "No test docstring may claim more than its own check proves. WR-02 is a docstring overclaim, not only a coverage gap, and a hardened check whose docstring still oversells it has closed half the finding."
      status: required
      verification: judgment
    - statement: "No hardened gate is trusted on a green run. Each must be seen RED against a planted counter-example that the PRE-hardening pin passes; a mutant both versions catch re-proves plan 180-01's leg rather than the hardening, and a leg only ever seen green proves nothing."
      status: required
      verification: judgment
    - statement: "No file under `firestarter_app/firestarter/` is modified, no file in the `firestarter` firmware submodule is modified, and `firestarter/data/chip_database.json` is not touched. The database is generated and must never be hand-edited."
      status: required
      verification: judgment
    - statement: "No `#` comment line and no trailing `#` comment is written into any file this plan touches. Docstrings carry every rationale. This rule is `/workspaces/CLAUDE.md`'s hard rule and a plan cannot override it; the four pre-existing comments in `test_chip_test.py` at the sibling read-step legs are out of scope and must be neither copied nor removed."
      status: required
      verification: judgment
    - statement: "`180-PRUNE-08-CLOSURE.md` and plans 180-01, 180-02 and 180-03 with their SUMMARYs are not re-opened, rewritten or renumbered. Twenty-three of twenty-four must-haves verified against those artifacts as they stand; editing them would invalidate evidence already accepted."
      status: required
      verification: judgment
  artifacts:
    - path: ".planning/seeds/dev-test-adaptive-sequencing.md"
      provides: "R3 with the rejected design's operative text absent rather than annotated, the connect-count objection preserved, and a dated follow-through in the Phase 180 amendment section"
      contains: "Measured and rejected (Phase 180, PRUNE-08)"
    - path: "firestarter_app/tests/test_readback_inventory.py"
      provides: "the one-connect pin extended to every connect route in `read_eprom`'s body, the verdict pin extended to `last_ok`'s assignment shape, both docstrings brought down to what they prove, and two new discriminating anti-vacuity legs"
      contains: "connect_route_calls"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-seed-regeneration-closed.txt"
      provides: "one scalar per removed fragment, one per preserved fragment, and four region `cmp` results against the pinned pre-edit blob"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-one-connect-pin-hardened.txt"
      provides: "WR-01's discrimination — the planted stray connect route seen invisible to `context_count` and RED against `connect_route_calls`"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-verdict-pin-hardened.txt"
      provides: "WR-02's discrimination — the planted `last_ok` reassignment seen invisible to `_verdict_expression_names` and RED against `_last_ok_assignment_shape`, plus the two docstring scalars"
  key_links:
    - from: ".planning/seeds/dev-test-adaptive-sequencing.md"
      to: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      via: "R3's surviving paragraph points at the closing document for the rejection argument and is the only route from the seed to the rejected design's detail"
      pattern: "180-PRUNE-08-CLOSURE"
    - from: "firestarter_app/tests/test_readback_inventory.py"
      to: "firestarter_app/firestarter/eprom_operations.py"
      via: "`_read_eprom_connect_shape` walks `read_eprom`'s whole body for `_operation_context`, `_setup_operation` and `find_and_connect` calls, resolved from `eo.__file__`"
      pattern: "connect_route_calls"
    - from: "firestarter_app/tests/test_readback_inventory.py"
      to: "firestarter_app/firestarter/chip_test.py"
      via: "`_last_ok_assignment_shape` walks `_dispatch_read`'s whole body for assignments targeting `last_ok`, resolved from `ct.__file__`"
      pattern: "_last_ok_assignment_shape"
---

<objective>
Close the one gap that made Phase 180 `gaps_found`, and make the two structural pins the phase rests on
actually hold their premises.

Plan `180-02` replaced R3's live imperative sentence with a "Measured and rejected" verdict but was told,
by its own `<action>` text, to leave the two paragraphs immediately below it alone. Those two paragraphs
are an escalation policy and a stride rationale written in the present tense, and together with the block
enumeration the verdict paragraph itself still carries they leave the rejected sampler's full behavioural
spec readable end to end — the design D-09 wanted *absent* is present, merely preceded by a paragraph
saying it was rejected. This plan removes it. Not annotates it: D-09 already names annotation as the
insufficient route, quoting Phase 177's R1 precedent, so a "for the historical record" banner is
explicitly not on the table.

Then the two Warning-level findings in `180-REVIEW.md`. The operator has answered the routed
human-verification item: harden both pins now, in this phase. WR-01 has no runtime backstop anywhere in
the phase's diff, and the one-connect premise is what the entire PRUNE-08 closure argument rests on — a
silently-false premise there invalidates the close, not merely a test.

Purpose: the seed a future planner reads first stops carrying a buildable spec for a design this phase
measured as dearer than what it would replace, and the two gates that guard the close stop being narrower
than their own docstrings claim.
Output: an amended seed, a hardened `test_readback_inventory.py` at 12 passing tests, and three evidence
transcripts, two of which carry a RED the pre-hardening pin would have missed.
</objective>

<gap_record>
From `180-VERIFICATION.md` (`status: gaps_found`, 23/24 must-haves):

- **`gaps[0]`, status `partial`** — truth: *"A planner reading only
  `.planning/seeds/dev-test-adaptive-sequencing.md` can no longer regenerate the rejected sampling design
  (D-09, Ruling 2, plan 180-02 must_have)"*. `missing`: rewrite or remove the escalation and stated-cost
  paragraphs so nothing after the rejection verdict reads as a live instruction. **Closed by Task 1 —
  by removal.** The `missing` item offers a re-framing route as its first option; D-09 forecloses it, and
  the disposition is not re-opened here.
- **`prohibitions_flagged[0]`, disposition `unresolved`** — *"The seed must not be left in a state from
  which the rejected sampling design can be regenerated"*, same root cause. **Closed by Task 1**, and
  authored into `must_haves.prohibitions` above, descriptor-less.
- **`human_verification[1]`** — the judgment call about whether the retained paragraphs are harmless
  historical colour. **It disappears with the paragraphs.** Once no paragraph after the verdict describes
  the sampler's operation, there is nothing left to read either way, and the ten absent-fragment scalars
  in Task 1's transcript settle mechanically what the verifier could not settle by grep last time.
- **`human_verification[2]`** — WR-01/WR-02 hardening, routed to the operator. **Answered: harden both,
  plus IN-01 and IN-02.** WR-01, WR-02 and IN-01 land in Task 2 of this plan; IN-02 lands in plan
  `180-05` Task 1.

Plans `180-01`, `180-02` and `180-03` are executed and immutable. This plan adds to them and rewrites
none of them.
</gap_record>

<assumption_delta_decision>
The assumption-delta detector fired on Phase 180 (`kind: pluralization`, term `second`, from the goal's
"the read step's **second** full sweep"). Orchestrator disposition: **`no-change`**.

- **Primary noun:** unchanged — the full read remains the sole verdict source.
- **Decision:** `no-change`.
- **Rationale:** the signal is a false positive — "second full sweep" names an existing behaviour of the
  read step, not a newly-introduced second case, and this phase's whole verdict (PRUNE-08 closed as
  measured-not-worth-doing) is that the second sweep stays exactly as it is. Nothing generalizes; no noun
  gains or loses a monopoly; no second representation is introduced.

Advisory; never blocks.
</assumption_delta_decision>

<flagged_assumptions>
The spec-less probe fallback is live for this phase — no SPEC.md exists, so neither `## Edge Coverage`
nor `## Prohibitions` was supplied (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`).

**Edge probe, surfaced `unresolved` (1 item, not dropped, not auto-resolved):**

- **PRUNE-08 — `category: unclassified`, `status: unresolved`, `probe: "unclassified — review manually"`.**
  The deterministic edge probe could not classify PRUNE-08 into an edge category, so per the fallback
  protocol it stays `unresolved` and is surfaced here as an explicit flagged assumption rather than being
  auto-resolved with a backstop. The reason it resists classification is legible: PRUNE-08 is a
  *conditional documentary* requirement whose two branches are ship-or-close, and the branch taken ships
  no behaviour at all — there is no input domain to enumerate edges over. **The assumption this plan
  therefore makes explicit:** PRUNE-08's remaining acceptance surface is prose fidelity plus gate
  strength, both of which are covered by named-literal and AST assertions in this plan's tasks rather
  than by an edge table. No boundary, empty, precision or ordering edge is claimed to exist for it.

**No-silent-drop equality, edges:** 1 probe-surfaced item == 0 authored into `must_haves.truths` + 1
surfaced as a flagged assumption. No resolved-`backstop` edge exists, so no
`{ statement, verification: backstop }` marker is authored anywhere in this plan set.

**No-silent-drop equality, prohibitions:** §B recall over-produced adversarially, then dropped
routine-engineering candidates and kept the values items. **9 survived Stage 2 across the plan set == 9
authored into `must_haves.prohibitions`**: seven in this plan, and two more in `180-05` that bind only
its tasks — the premature-flip prohibition and the roadmap-scope prohibition. `180-05` additionally
re-states five of this plan's seven because they bind its own tasks; that is a re-statement for the
executor, not a tenth item. All nine are descriptor-less: no `check_kind`, `check_target`, `check_rule`,
`check_violation_fixture` or `check_clean_fixture` scalar appears on any of them, which is what keeps each
fail-closed. Two candidates were dropped with breadcrumbs: *"the seal must not assert a transcribed suite
count"* (routine engineering — already a `<verify>` leg in `180-05` Task 3) and *"no dependency may be
installed"* (canon supply-chain; carried in the threat register as `T-180-SC`, not minted as a
prohibition).
</flagged_assumptions>

<source_audit>
| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Phase 180 goal: the second sweep is replaced only where 176 proves it cheaper; closing without shipping code is a legitimate outcome | 180-01…03 | COVERED | Verified 3/4 roadmap criteria as a set; this gap-closure does not re-open the branch decision |
| GOAL | SC-1 | Ship the sample if cheaper on ≥1 board class | 180-01…03 | COVERED | Correctly not taken (N/A) — verified |
| GOAL | SC-2 | Else close PRUNE-08 as measured-not-worth-doing, citing the measurement | 180-01, 180-02 | COVERED | Verified; re-sealed in `180-05` |
| GOAL | SC-3 | The verdict source stays pinned to the full read by test | 180-01, **180-04 T2** | COVERED | 180-01 built the pins; this plan closes WR-01/WR-02 so the pins hold their stated premises |
| GOAL | SC-4 | If sampling ships, block-wise comparison proven by a hole-padded fixture | 180-02 | COVERED | Explicit N/A with the quoted false precondition (D-10) — verified |
| REQ | PRUNE-08 | Read step's second sweep replaced by a sample only if cheaper; else close as measured-not-worth-doing | 180-04, 180-05 | COVERED | Both plans carry `requirements: [PRUNE-08]`; re-flip to Complete in `180-05` T2 |
| RESEARCH | — | §"What an honest, non-vacuous pin would have to assert" — three clauses the one-connect pin must carry | 180-01, **180-04 T2** | COVERED | The three clauses shipped in 180-01; WR-01 adds the fourth the section's own ceiling paragraph implies |
| RESEARCH | — | Pitfall 3 — an AST pin that only checks `last_ok` is present | 180-01, **180-04 T2** | COVERED | 180-01 shipped set equality; WR-02 extends it to `last_ok`'s assignment shape |
| RESEARCH | — | C-3 — the inherited comment gate cannot see trailing comments; the sibling leg carries four comments, two of them trailing | 180-04, 180-05 | COVERED | `tokenize` gate at 621/0 in both plans; the four pre-existing comments explicitly out of scope |
| RESEARCH | — | §Gate Battery — seven legs, floor 2239 | 180-05 T3 | COVERED | Re-run with the floor moved to the measured 2247 |
| RESEARCH | — | Pitfall 5 — running the suite with a dirty tree | 180-05 T3 | COVERED | `<precondition>` on Task 3 |
| RESEARCH | — | Pitfall 6 — marking PRUNE-08 Complete before its evidence exists | 180-05 T2 | COVERED | `<precondition>` gates the flip on the gap fixes being committed |
| RESEARCH | — | §Evidence transcript form (`== N label`, flat scalars, explicit rc) | 180-04, 180-05 | COVERED | Every transcript in both plans |
| CONTEXT | D-01 | PRUNE-08 closes as measured-not-worth-doing | 180-01, 180-02 | COVERED | Discharged; not re-opened |
| CONTEXT | D-02 | Evidence standard is MEAS-01 plus code structure only | 180-01, **180-04 T2** | COVERED | The structural half is the one-connect pin, hardened here |
| CONTEXT | D-03 | The argument is stated in connect counts, never seconds | 180-01, **180-04 T1** | COVERED | The surviving R3 paragraph keeps the 10-against-1 connect form |
| CONTEXT | D-04 | The modelled figures are not published as findings | 180-02 | COVERED | Discharged; Task 1 adds no figure |
| CONTEXT | D-05 | The size-gated variant is not re-filed as a requirement | 180-02 | COVERED | Discharged; Task 1 files nothing |
| CONTEXT | D-06 | Three tests pinning the verdict source | 180-01, **180-04 T2** | COVERED | Leg 3's pin is what WR-02 hardens |
| CONTEXT | D-07 | Every new gate gets a planted-mutation RED proof, string mutations only | **180-04 T2** | COVERED | Two new discriminating legs plus two transcripts |
| CONTEXT | D-08 | Conditional close naming R4-01 | 180-02 | COVERED | Discharged |
| CONTEXT | D-09 | Amend seed R3 in place, following Phase 177's R1 precedent; preserve the connect-count objection | 180-02, **180-04 T1** | COVERED | Partly met by 180-02; completed here — the governing decision for the blocking gap |
| CONTEXT | D-10 | Criterion 4 is Not Applicable, recorded with its reason | 180-02 | COVERED | Discharged |
| CONTEXT | D-11 | `_read_region` recorded as the forward-looking primitive, not a licence | 180-02 | COVERED | Discharged |

All rows COVERED. Excluded as not-gaps: CONTEXT.md `## Deferred Ideas` (size-gated sampling ≥512 KiB;
R4-01 session leasing) and the 41 reviewed-and-not-folded todos.
</source_audit>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-CONTEXT.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-VERIFICATION.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-REVIEW.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-02-SUMMARY.md
</context>

<environment>
Two repositories, two branches, separate commits.

- Meta repo `/workspaces`, branch `gsd/v1.36-dev-test-fidelity-planning`, owns `.planning/`. Task 1 and
  all three evidence transcripts commit here.
- Submodule `/workspaces/firestarter_app`, branch `gsd/v1.36-dev-test-fidelity`, app HEAD `93a1672`.
  Task 2's change to `tests/test_readback_inventory.py` commits **inside** the submodule.
- Submodule `/workspaces/firestarter` (firmware) is not touched at all and must stay porcelain-clean.

Python is always `./.venv311/bin/python` from `/workspaces/firestarter_app`. The devcontainer default is
3.12; app CI runs 3.11 only, and that divergence has already broken beta CI once. Never a bare `python`
or `pytest`.

Shell `grep` here is **ugrep and honours `.gitignore`**, so it silently under-scans. Every
evidence-grade scan below uses `/usr/bin/grep`. Every command that needs to read a pytest count line
passes `-o addopts=""`, because the project's own addopts is `-ra -q` and doubling the quiet flag hides
the count.

Edit `.planning/` files **by hand**. Never the `gsd-tools` requirements or roadmap verbs — they reformat
the whole file, and `roadmap.update-plan-progress` additionally clobbers the dependency table by
positional overwrite. Neither `REQUIREMENTS.md` nor `ROADMAP.md` is touched by this plan at all; both
belong to plan `180-05`.

**One pinned blob, load-bearing.** The pre-edit seed is meta blob
`7a47a1fd9236bb4068bc72be6c8f8ed35f86e300` (materialize with `git cat-file blob
7a47a1fd9236bb4068bc72be6c8f8ed35f86e300`). Every byte-unchanged region check in Task 1 compares against
that blob rather than against `HEAD`, so the pins stay reproducible after the edit commits instead of
becoming a one-shot claim. Anchoring such a check to `HEAD` is vacuous the moment the change lands, and
this project has a recorded case of exactly that: `git rev-list --count HEAD ^HEAD~1` is always 1.

Measured this planning session against the real files, so nothing below is assumed:
`/usr/bin/grep -cF` on the seed returns 1 for each of `Escalate to the full second read`,
`only when the sample diverges`, `first_offset`, `**Cost, stated:**`, `divergence metric becomes an`,
`uno328pb`, `A fault confined entirely to`, `the bit-structured stride is`, `address-line-aligned`,
`256 B block at each` and `block 0 and the top block`, and 1 for `Measured and rejected (Phase 180,
PRUNE-08)`, `10 connects` and `_operation_context`; the three sed region ranges used below extract 45,
72 and 18 lines respectively from the pinned blob; `tests/test_readback_inventory.py` reports
`10 passed`; `/usr/bin/grep -cE '\(:[0-9]+'` on it returns 1 and `/usr/bin/grep -cF 'the structural half
that catches'` returns 1; `tokenize` COMMENT counts are 621 and 0; `read_eprom`'s body contains no direct
`_setup_operation` or `find_and_connect` call, so `connect_route_calls` reads 1 at HEAD; and
`_dispatch_read` assigns `last_ok` exactly twice — `last_ok = True` at the top and
`last_ok = operator.read_eprom(...)` inside its single `for` loop.

**Both discriminations were measured against the real source this planning session, so neither RED below
is assumed reachable.** Each anchor occurs exactly once; both mutants parse as valid Python. With a
`self._setup_operation(...)` line planted after the output-filename line inside `read_eprom`, the
PRE-hardening one-connect pin stays fully green — `context_count` 1, `connect_chain` True,
`disconnect_in_finalbody` True — while the hardened `connect_route_calls` reads **2** against **1** at
HEAD. With a `last_ok` rebinding planted after `_dispatch_read`'s `reason` assignment,
`_verdict_expression_names` stays at exactly `VERDICT_BAD,VERDICT_OK,last_ok` — the whole pre-hardening
claim, unmoved — while the hardened `tags` read `const_true,read_eprom_call,other` against
`const_true,read_eprom_call` at HEAD, with `for_loop_count` 1 and `read_assign_in_for_loop` True. Those
are the values the verify block asserts.
</environment>

<tasks>

<task type="tracer">
  <name>Task 1: The rejected sampler's operative text is absent from the seed, not annotated — end to end, with the untouched regions pinned byte-for-byte</name>
  <precondition>`git cat-file -e 7a47a1fd9236bb4068bc72be6c8f8ed35f86e300` succeeds in `/workspaces`. Every byte-unchanged region check in this task is measured against that pinned pre-edit blob; without it the four `cmp` legs cannot run and the task must halt rather than fall back to a `HEAD`-relative comparison that goes vacuous once the edit commits.</precondition>
  <reversibility rating="costly">The seed is the artifact a future planner reads first. D-09 already rates this amendment `costly`: if it is skipped or later reverted, a subsequent milestone can regenerate the rejected sampling design from the seed alone with no trace of Phase 176's measurement — which is precisely what `180-VERIFICATION.md` found had happened by halves. Flagged, not gated: removing prose is a one-commit revert, and the removed text survives verbatim in git history and in this plan's SUMMARY.</reversibility>
  <files>
    .planning/seeds/dev-test-adaptive-sequencing.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-seed-regeneration-closed.txt
  </files>
  <read_first>
    - `/workspaces/.planning/seeds/dev-test-adaptive-sequencing.md` — the whole file (211 lines). Read R3 at :70-99 line by line: the heading at :70, the two factual sentences at :72-73, the `**Measured and rejected (Phase 180, PRUNE-08).**` paragraph at :75-87, the escalation paragraph at :89-91 and the bolded stated-cost paragraph at :93-99. Then R4 at :101-116, and the two amendment sections at :172-187 and :189-211.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-VERIFICATION.md` — the `gaps:` block at :26-49, the `prohibitions_flagged:` block at :50-53, and `human_verification` item 1 at :55-57. The `reason:` field names the two paragraphs and the verdict-contradiction mechanism exactly; it is this task's acceptance surface.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-CONTEXT.md:124-141` — D-09 in full, including the quoted Phase 177 sentence about replacing in place so the destructive reading is absent rather than outvoted, and the instruction to preserve the connect-count objection inside the amended text.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-02-PLAN.md:196-207` — Task 2's `<action>`, whose closing sentence told the executor to leave the escalation and stated-cost paragraphs as they are. That instruction is what this task overturns; read it so the SUMMARY can record the planning defect accurately.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` — the whole file. Confirm before writing any pointer sentence what it does and does not contain: measured this planning session, it carries the block enumeration but **no** escalation-trigger and **no** stride-rationale text, so a sentence claiming it records those would be false.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md:382-440` — the Phase 177 amendment quoted verbatim and the six moves to copy.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-seed-amendment.txt` — the key set this transcript extends.
  </read_first>
  <action>
Make two edits to `.planning/seeds/dev-test-adaptive-sequencing.md`, by hand. Nothing else in the file moves.

**Edit 1 — R3's body.** Leave the heading at :70 and the two factual sentences at :72-73 byte-unchanged: a statistical property established by full-device sweeps is an observation, not an instruction, and R1's heading survived Phase 177's amendment unchanged, so the precedent settles the heading. Then reduce everything from `**Measured and rejected (Phase 180, PRUNE-08).**` to the end of the rule to a single paragraph, and delete the two paragraphs below it outright.

What that single paragraph must still say, because D-09 requires the connect-count objection preserved inside the amended text: keep the bolded `**Measured and rejected (Phase 180, PRUNE-08).**` lead-in; keep that the sample was priced against `EpromOperator._operation_context` at `firestarter_app/firestarter/eprom_operations.py:515-550`, which connects on entry and disconnects inside its own `finally` block, so every `read_eprom` call, sampled or full, pays one full connect; keep that at the 64 KiB reference size the sample was 10 separate `read_eprom` calls — the literal `10 connects` must survive — against the 1 the full sweep it would replace already costs; keep that ten connects exceed one connect on both measured board classes (MEAS-01) at every reference size this milestone tests, so the sample is dearer than the sweep it would replace, not cheaper; and keep the pointer to `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` for the full argument, the per-board-class measured figures and the size-axis finding.

What must be gone from that paragraph and from the rule entirely:

  1. The clause inside the verdict paragraph that enumerates the sample's block structure — the
     per-boundary block, together with block zero and the topmost block. A block *count* is part of the
     objection and stays; the rule for choosing which blocks is a buildable spec and goes. Do not
     substitute the `1 << k` notation or any other stride formulation for it.
  2. The whole paragraph directing escalation to the full second read on sample divergence, including its
     list of the four divergence-count keys. It is an escalation policy in the present tense and there is
     nothing left for it to escalate.
  3. The whole bolded stated-cost paragraph, from the estimate-over-a-sampled-subset sentence through the
     stride-selection rationale that closes it.

Close the paragraph with one sentence stating that nothing in this rule is a live instruction to build
the sample, and that the design's operative detail — how its blocks were chosen and when it would have
escalated — is deliberately not restated here. Do **not** write that the closing document records the
escalation trigger or the stride rationale: it does not, and a false pointer is worse than none. Do
**not** front any retained sentence with a historical-record banner: an annotated retention is the route
D-09 rejects by name, and the paragraphs are removed, not framed.

One judgment this task records rather than defers, because the gap record asks for it explicitly. The
stated-cost paragraph contains one sentence that reads like a finding — that a scattered transport fault,
the `uno328pb` signature, is caught with high probability by any sample of that size because scatter is
what makes it detectable. **It goes with the rest**, and the reason belongs in the SUMMARY: no phase
measured a detection probability, so it is an unmeasured probabilistic argument rather than a
measurement; it is the "and this would still be safe" half of the rejected design's case, which is
exactly what a planner needs to re-propose it; and it refers to a sample size that no longer exists
anywhere in the seed, so it would dangle. The genuinely measured facts — MEAS-01's per-board-class
medians and the connect arithmetic — are preserved, in R4's corrected sentence and in the surviving R3
paragraph respectively.

**Edit 2 — a dated follow-through inside the `## Status: Phase 180 amendment` section.** Append it at the
end of that section, at the end of the file. Do not touch the `## Status: Phase 177 amendment` section,
and do not restate any of the removed operative text. Open it with the exact bold lead-in `**Follow-through, gap closure.**` followed by the date in the
precedent's form, and record four things: that Phase 180's verification found R3's escalate-on-divergence paragraph and its
stated-cost paragraph still standing below the rejection verdict, still readable as live design
description; that they are now removed outright rather than annotated, and that R3 no longer enumerates
the sample's block structure; that an annotated retention is the route D-09 rejects, because the
destructive reading must be absent and not merely outvoted; and that R1, R2, R4, the projected-effect
table, the per-class characteristics section, the sequencing note and the out-of-scope section remain
untouched by this follow-through as they were by the amendment above it. Restate that list in the
follow-through rather than cross-referencing the paragraph above — a not-touched list a reader has to
resolve upwards is how the amendment record above it went stale, and the leg that checks this counts
occurrences of `the sequencing note`.

Then write `evidence/180-04-seed-regeneration-closed.txt` in the house transcript form — numbered
`== N label` sections, flat `key=value` scalars, an explicit `rc=` line after each command. Recompute
every scalar from the file; transcribe none of them from this plan. Record one scalar per removed
fragment naming the fragment and its measured count, the four preserved-fragment counts, the four region
`cmp` verdicts against the pinned blob, `frontmatter_fields=4`, `followthrough_recorded=1`, and the seed's
line count before and after.

Commit in `/workspaces` on `gsd/v1.36-dev-test-fidelity-planning`, naming the gap and D-09 in the
message.
  </action>
  <verify>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md; test -s "$S" && for f in 'Escalate to the full second read' 'only when the sample diverges' 'first_offset' '**Cost, stated:**' 'divergence metric becomes an' 'uno328pb' 'A fault confined entirely to' 'the bit-structured stride is' 'address-line-aligned' '256 B block at each' 'block 0 and the top block'; do test "$(/usr/bin/grep -cF "$f" "$S")" -eq 0 || { echo "STILL PRESENT: $f"; exit 1; }; done; echo all_removed_fragments=0</automated>
    <fails_when>The loop prints `STILL PRESENT: <fragment>` and exits 1 — that named fragment of the rejected sampler's escalation trigger, stated-cost argument or block enumeration is still in the seed, so the design remains regenerable from it. Also non-zero when the seed is missing or empty: the leading `test -s` guard is what stops eleven negative greps passing vacuously on an absent file, since `grep` on a missing path exits non-zero and the count would read 0.</fails_when>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md; test "$(/usr/bin/grep -cF 'Measured and rejected (Phase 180, PRUNE-08)' "$S")" -ge 1 && test "$(/usr/bin/grep -cF '10 connects' "$S")" -ge 1 && test "$(/usr/bin/grep -cF '_operation_context' "$S")" -ge 1 && test "$(/usr/bin/grep -cF '180-PRUNE-08-CLOSURE.md' "$S")" -ge 2 && test "$(/usr/bin/grep -cF 'MEAS-01' "$S")" -ge 2 && ! /usr/bin/grep -qF 'R3 remains for Phase 180' "$S" && ! /usr/bin/grep -qF 'Replace the read step' "$S" && ! /usr/bin/grep -qF 'Per-connect cost is unmeasured' "$S"</automated>
    <fails_when>non-zero exit — the rejection verdict lead-in was dropped or duplicated, the `10 connects` objection D-09 requires preserved was removed along with the paragraphs around it, the structural reason (`_operation_context`) or the closing-document pointer was lost, the measurement citation count fell below two, or one of plan `180-02`'s three already-removed false statements has come back.</fails_when>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md && D=$(mktemp -d) && git cat-file blob 7a47a1fd9236bb4068bc72be6c8f8ed35f86e300 > "$D/pre.md" && sed -n '1,6p' "$D/pre.md" > "$D/a1" && sed -n '1,6p' "$S" > "$D/a2" && sed -n '/^### R1 —/,/^### R3 —/p' "$D/pre.md" > "$D/b1" && sed -n '/^### R1 —/,/^### R3 —/p' "$S" > "$D/b2" && sed -n '/^### R4 —/,/^## Status: Phase 177 amendment$/p' "$D/pre.md" > "$D/c1" && sed -n '/^### R4 —/,/^## Status: Phase 177 amendment$/p' "$S" > "$D/c2" && sed -n '/^## Status: Phase 177 amendment$/,/^## Status: Phase 180 amendment$/p' "$D/pre.md" > "$D/d1" && sed -n '/^## Status: Phase 177 amendment$/,/^## Status: Phase 180 amendment$/p' "$S" > "$D/d2" && test "$(wc -l < "$D/b1")" -eq 45 && test "$(wc -l < "$D/c1")" -eq 72 && test "$(wc -l < "$D/d1")" -eq 18 && cmp "$D/a1" "$D/a2" && cmp "$D/b1" "$D/b2" && cmp "$D/c1" "$D/c2" && cmp "$D/d1" "$D/d2" && rm -rf "$D"</automated>
    <fails_when>`cmp` prints a `differ: byte N, line M` line and exits non-zero — the named region moved. Region a is the frontmatter's four fields, b is R1 through R3's heading inclusive, c is R4 through the out-of-scope section inclusive, d is the whole Phase 177 amendment section. Also non-zero when any of the three line-count guards fails (45, 72, 18): a zero-line extraction would make its `cmp` pass vacuously, which is exactly how a sed range that stopped matching would read as clean.</fails_when>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md; test "$(sed -n '2,5p' "$S" | /usr/bin/grep -cE '^(title|trigger_condition|planted_date|status):')" -ge 4 && /usr/bin/grep -qF '## Status: Phase 180 amendment' "$S" && /usr/bin/grep -qF '## Projected effect' "$S" && /usr/bin/grep -qxF '### R3 — Sample for a rate, sweep for a map' "$S" && /usr/bin/grep -qF '**Follow-through, gap closure.**' "$S" && test "$(/usr/bin/grep -cF 'the sequencing note' "$S")" -ge 2</automated>
    <fails_when>non-zero exit — the frontmatter no longer carries exactly the four expected keys, the Phase 180 amendment heading or the projected-effect heading was destroyed, R3's heading line is not byte-identical to the exact expected line, the exact bold lead-in `**Follow-through, gap closure.**` is absent, meaning the follow-through block was not appended, or `the sequencing note` occurs fewer than twice, meaning the follow-through was appended without restating its own not-touched list (it occurs exactly once at HEAD, in the amendment paragraph above it — measured this planning session, so this leg is reachable and not vacuous).</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-seed-regeneration-closed.txt; test -s "$E" && test "$(/usr/bin/grep -cE '^escalation_paragraph_present=0$|^stated_cost_paragraph_present=0$|^block_structure_enumerated=0$|^stride_rationale_present=0$|^divergence_keys_present=0$' "$E")" -ge 5 && /usr/bin/grep -qxF 'connect_objection_present=1' "$E" && /usr/bin/grep -qxF 'rejection_verdict_present=1' "$E" && /usr/bin/grep -qxF 'frontmatter_fields=4' "$E" && /usr/bin/grep -qxF 'followthrough_recorded=1' "$E" && test "$(/usr/bin/grep -cE '^region_cmp_[a-z0-9_]+=identical$' "$E")" -ge 4 && test "$(/usr/bin/grep -cE '^rc=0$' "$E")" -ge 1</automated>
    <fails_when>non-zero exit — the transcript is missing or empty; fewer than five of the removed-fragment scalars read zero; the preserved-objection, verdict, frontmatter or follow-through scalars are absent; fewer than four region comparisons read `identical`, meaning a byte-unchanged claim was asserted rather than measured; or no `rc=0` line was recorded.</fails_when>
    <automated>test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain)"</automated>
    <fails_when>non-zero exit — either submodule has a dirty working tree, meaning this prose-only task touched code it must not touch.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/seeds/dev-test-adaptive-sequencing.md` contains zero occurrences of each of these eleven literals, measured with `/usr/bin/grep -cF`: `Escalate to the full second read`, `only when the sample diverges`, `first_offset`, `**Cost, stated:**`, `divergence metric becomes an`, `uno328pb`, `A fault confined entirely to`, `the bit-structured stride is`, `address-line-aligned`, `256 B block at each`, `block 0 and the top block`.
    - The seed contains exactly one occurrence of `Measured and rejected (Phase 180, PRUNE-08)`, at least one of `10 connects`, at least one of `_operation_context`, at least two of `180-PRUNE-08-CLOSURE.md` and at least two of `MEAS-01`.
    - The seed still contains none of `R3 remains for Phase 180`, `Replace the read step`, `Per-connect cost is unmeasured`.
    - Against pinned blob `7a47a1fd9236bb4068bc72be6c8f8ed35f86e300`, `cmp` reports identical for all four regions: lines 1-6; `### R1 —` through `### R3 —` inclusive (45 lines in the pre-edit blob); `### R4 —` through `## Status: Phase 177 amendment` inclusive (72 lines); `## Status: Phase 177 amendment` through `## Status: Phase 180 amendment` inclusive (18 lines).
    - Line 70 of the seed reads exactly `### R3 — Sample for a rate, sweep for a map`, matched with `/usr/bin/grep -qxF`.
    - The `## Status: Phase 180 amendment` section contains the exact bold lead-in `**Follow-through, gap closure.**` (absent at HEAD), and `the sequencing note` occurs at least twice in the seed (exactly once at HEAD), proving the follow-through restates its own not-touched list naming R1, R2, R4, the projected-effect table, the per-class characteristics section, the sequencing note and the out-of-scope section.
    - No sentence anywhere in the seed states that `180-PRUNE-08-CLOSURE.md` records the escalation trigger or the stride rationale; the closing document was measured this planning session to contain neither.
    - `evidence/180-04-seed-regeneration-closed.txt` contains `escalation_paragraph_present=0`, `stated_cost_paragraph_present=0`, `block_structure_enumerated=0`, `stride_rationale_present=0`, `divergence_keys_present=0`, `connect_objection_present=1`, `rejection_verdict_present=1`, `frontmatter_fields=4`, `followthrough_recorded=1`, and four `region_cmp_*=identical` lines.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` both print nothing.
    - The SUMMARY quotes both removed paragraphs verbatim and records the disposition of the scattered-transport-fault sentence with its reason, so the removed text has a home outside git history.
  </acceptance_criteria>
  <done>A planner reading only the seed finds R3's verdict, the connect-count objection that produced it and a pointer to the closing document — and no escalation policy, no stride rationale, no block enumeration and no stated-cost argument to build against. Every region the amendment does not own is proven byte-identical to the pinned pre-edit blob, and the seed's own amendment section says which paragraphs moved.</done>
</task>

<task type="auto">
  <name>Task 2: Both structural pins hold their stated premises — a stray connect route and a `last_ok` reassignment each redden a pin that the pre-hardening version passes</name>
  <precondition>`/workspaces/firestarter_app` has a clean working tree and `./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q` reports `10 passed` before any edit. That measured 10 is what makes the 12 afterwards a measured delta rather than a guess; halt if either is false, because a dirty tree also means Task 1's commit did not land where it belongs.</precondition>
  <files>
    firestarter_app/tests/test_readback_inventory.py,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-one-connect-pin-hardened.txt,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-verdict-pin-hardened.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_readback_inventory.py` — the whole file (336 lines). In particular: the module docstring's last paragraph at :36-46 describing both Phase 180 pins; `_verdict_expression_names` at :139-163; `test_read_verdict_expression_reads_only_the_last_full_read_result` at :166-190 including the docstring sentence at :170 with the stale sibling citation; `test_a_planted_divergence_term_in_the_verdict_reddens_the_pin` at :193-212; `_CONTEXT_ANCHOR` at :236; `_operations_source` at :239; `_read_eprom_connect_shape` at :244-300; `test_one_read_eprom_call_costs_exactly_one_connect` at :303-318; and `test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin` at :321-336. Do not restructure any of it — both hardenings are additive to the existing helpers and assertions.
    - `/workspaces/firestarter_app/firestarter/eprom_operations.py:896-943` — `read_eprom` in full. Confirm for yourself that its whole body is one `with self._operation_context(...)` and that no direct `_setup_operation` or `find_and_connect` call appears anywhere inside it, because that is what makes 1 the right expected value for `connect_route_calls` rather than 0 or 2.
    - `/workspaces/firestarter_app/firestarter/eprom_operations.py:454-551` — `_setup_operation` and `_operation_context`. Note that `_operation_context` itself calls `_setup_operation`: the new count must be scoped to `read_eprom`'s own `FunctionDef` node, never to the module, or the callee's legitimate call would be counted as a stray and the pin would be wrong at HEAD.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:2767-2812` — `_dispatch_read` in full. Note the two assignments to `last_ok`: the `last_ok = True` initializer at the top of the function body and `last_ok = operator.read_eprom(...)` inside the single `for` loop. The review's suggested "exactly one assignment" is wrong against this source; the invariant is two, of two named shapes, and nothing else.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-REVIEW.md` — WR-01, WR-02, IN-01 in full, including WR-01's `ast.walk` snippet. Treat the snippet as one non-binding example route: its `stray_connects == 0` form happens to hold for `read_eprom` today, but the invariant this task pins is the stronger equality below.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-01-one-connect-pin.txt` and `180-01-verdict-pin-red.txt` — the transcript shape these two extend, including the `pin_at_head=GREEN` / `pin_at_planted=RED` key form.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-RESEARCH.md:423-455` — the three clauses an honest one-connect pin must assert and its stated ceiling; WR-01 is the fourth clause that section's own ceiling paragraph implies but does not list.
    - `/workspaces/CLAUDE.md` — the source-comments hard rule and the submodule commit rule. Every rationale below goes in a docstring; not one `#` line is added, including trailing ones.
  </read_first>
  <action>
Four additive changes to `firestarter_app/tests/test_readback_inventory.py`, then two transcripts. No
product source is touched, no existing test is deleted or renamed, and no `#` line is added anywhere.

**Change 1 — WR-01: `_read_eprom_connect_shape` gains a fourth clause.** Add one key,
`connect_route_calls`, to the dict it returns: the number of `ast.Call` nodes anywhere in `read_eprom`'s
own `FunctionDef` whose `func` is an `ast.Attribute` whose `attr` is one of `_operation_context`,
`_setup_operation` or `find_and_connect`. Scope the walk to the `read_eprom` node the function already
resolves out of its `functions` mapping — never to the module tree, because `_operation_context`'s own
legitimate `_setup_operation` call would then be counted and the pin would be false at HEAD. Keep the
existing `context_count`, `connect_chain` and `disconnect_in_finalbody` keys and their computations
exactly as they are: `context_count` is what the pre-hardening pin asserted and the new anti-vacuity leg
needs it to keep reading 1 on the mutant to prove the discrimination.

Then extend `test_one_read_eprom_call_costs_exactly_one_connect` with one assertion,
`shape["connect_route_calls"] == 1`, keeping its three existing assertions. Measured this planning
session: that value is 1 at HEAD, and it equals `context_count`, so the invariant the pin now carries is
that every connect-shaped call inside `read_eprom` *is* the single counted `_operation_context` item —
there is no second route, inside a `with` header or outside one. Rewrite the docstring's ceiling
paragraph accordingly: it must name `connect_route_calls` and state that a stray direct connect call
anywhere in the body is now caught statically, and its remaining ceiling is the runtime one it already
states honestly — that no serial open is observed and nothing is claimed about the retry-across-ports
walk inside `find_and_connect`.

**Change 2 — WR-01's discriminating RED leg.** Add module-level constant `_STRAY_CONNECT_ANCHOR`, whose
value is `read_eprom`'s output-filename line — the one assigning `actual_output_file` from `output_file`
or the upper-cased chip name — with its twelve leading spaces and its trailing newline, written as a
single-quoted Python literal so the f-string's own double quotes need no escaping. Add
`test_a_planted_stray_setup_operation_call_in_read_eprom_reddens_the_hardened_pin`, following the file's
established leg shape: assert the anchor occurs exactly once in `_operations_source()`; build the mutant
by a single bounded `replace` that appends one line after the anchor, a `self._setup_operation(...)` call
on `eprom_name`, `eprom_data_dict` and `COMMAND_READ` at twelve spaces of indentation; then assert three
things in order. First `mutant_shape["context_count"] == 1` — the pre-hardening claim still holds, which
is what makes this mutant a real discrimination rather than a re-proof of plan `180-01`'s leg. Second
`mutant_shape["connect_route_calls"] == 2`. Third, inside `pytest.raises(AssertionError)`, the hardened
pin's own claim `mutant_shape["connect_route_calls"] == 1`. Strings only; write no fixture file. Give the
leg a docstring naming WR-01 and stating in one sentence that the mutant is invisible to the `with`-header
count and visible only to the body-wide count.

**Change 3 — WR-02: a new helper and three new assertions.** Add module-level
`_last_ok_assignment_shape(source)`, mirroring `_read_eprom_connect_shape`'s dict-returning shape so the
module keeps one idiom. It resolves the single `ast.FunctionDef` named `_dispatch_read`, collects every
`ast.Assign`, `ast.AugAssign`, `ast.AnnAssign` and `ast.NamedExpr` node within it whose target is an
`ast.Name` with id `last_ok`, orders them by `(lineno, col_offset)`, and returns `tags`: one string per
node — `const_true` when the node is an `ast.Assign` whose `value` is an `ast.Constant` equal to `True`,
`read_eprom_call` when the value is an `ast.Call` whose `func` is an `ast.Attribute` with `attr` equal to
`read_eprom`, and `other` for anything else. Return two more keys: `for_loop_count`, the number of
`ast.For` nodes in the function, and `read_assign_in_for_loop`, true when the node tagged
`read_eprom_call` is reachable from an `ast.For` node in that function.

Then add three assertions to `test_read_verdict_expression_reads_only_the_last_full_read_result`, keeping
its existing set-equality assertion on `_verdict_expression_names` untouched:
`shape["tags"] == ["const_true", "read_eprom_call"]`, `shape["for_loop_count"] == 1`, and
`shape["read_assign_in_for_loop"] is True`. Measured this planning session: those are the values at HEAD.
The `other` tag is what makes the list a closed claim rather than a membership test — any third
assignment, any augmented assignment and any walrus on `last_ok` changes the list and reddens the pin.

**Change 4 — WR-02's discriminating RED leg, its docstring correction and IN-01.** Add module-level
constant `_LAST_OK_REASSIGN_ANCHOR`, whose value is `_dispatch_read`'s `reason` assignment line — the
conditional expression yielding the diverged-runs reason string — with its four leading spaces and its
trailing newline, again single-quoted so the inner double quotes need no escaping. Add
`test_a_planted_last_ok_reassignment_before_the_return_reddens_the_hardened_pin`: assert the anchor occurs
exactly once in `_engine_source()`; build the mutant by appending one line after it that rebinds `last_ok`
to the conjunction of itself and the negation of `divergence`, at four spaces of indentation; then assert
`_verdict_expression_names(mutant)` still equals exactly the three-name list `VERDICT_BAD`, `VERDICT_OK`,
`last_ok` — the pre-hardening pin's whole claim, unmoved, which is the discrimination WR-02 describes —
then that the mutant's `tags` read `const_true`, `read_eprom_call`, `other`, and finally, inside
`pytest.raises(AssertionError)`, the hardened claim that `tags` equals the two-element list.

In the same pass, correct the pinned test's docstring. Delete the sentence that markets the pin as the
structural half catching what a behavioural test cannot — WR-02 is right that a syntax-only claim on one
keyword expression cannot support that, and the check is now two claims rather than one. Replace it with
exactly what the hardened check proves: that the `verdict=` expression resolves to that three-name set and
nothing derived from the divergence comparison, and that within `_dispatch_read` `last_ok` is `assigned
exactly twice` — the literal phrase must appear — once from a `True` constant and once from the
whole-device read inside the run loop, so a later rebinding is caught structurally too. Keep an honest
ceiling sentence: both claims are static, neither observes a run, and neither says anything about what
`read_eprom` itself returns. And per IN-01, drop the parenthesised sibling line-number citation from the
same docstring, keeping the function name `test_read_step_last_run_failure_yields_bad` — a name cannot
drift, and the number was already 36 lines stale the day it was written.

Update the module docstring's Phase 180 paragraph and its anti-vacuity count in the same pass: it says
four anti-vacuity legs; there are six after this change, and the two new ones plant a stray connect route
and a `last_ok` rebinding respectively. Docstrings only.

Then write the two transcripts under
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/`, in the house form —
numbered `== N label` sections, flat scalars, explicit `rc=` after each command. Compute every scalar by
calling the committed helpers on the mutant strings rather than transcribing an expected value.
`180-04-one-connect-pin-hardened.txt` records `anchor_count`, `head_context_count`,
`head_connect_route_calls`, `planted_context_count`, `planted_connect_route_calls`,
`old_pin_at_planted=GREEN`, `new_pin_at_planted=RED`, `new_pin_at_head=GREEN`, and
`wr01_doc_names_hardened_claim=True`. `180-04-verdict-pin-hardened.txt` records `anchor_count`,
`head_names`, `head_tags`, `planted_names`, `planted_tags`, `old_pin_at_planted=GREEN`,
`new_pin_at_planted=RED`, `new_pin_at_head=GREEN`, `wr02_doc_drops_overclaim=True`,
`wr02_doc_names_assignment_claim=True`, `stale_line_citations=0`, and the full-module pytest tail showing
12 passed.

Commit the test-module change **inside** `/workspaces/firestarter_app` on
`gsd/v1.36-dev-test-fidelity`, and the two transcripts in `/workspaces`. Two repositories, two commits.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && T=tests/test_readback_inventory.py; test -s "$T" && /usr/bin/grep -qF 'connect_route_calls' "$T" && /usr/bin/grep -qF '_last_ok_assignment_shape' "$T" && /usr/bin/grep -qF '_STRAY_CONNECT_ANCHOR' "$T" && /usr/bin/grep -qF '_LAST_OK_REASSIGN_ANCHOR' "$T" && /usr/bin/grep -qF 'def test_a_planted_stray_setup_operation_call_in_read_eprom_reddens_the_hardened_pin' "$T" && /usr/bin/grep -qF 'def test_a_planted_last_ok_reassignment_before_the_return_reddens_the_hardened_pin' "$T" && /usr/bin/grep -qF 'test_read_step_last_run_failure_yields_bad' "$T" && test "$(/usr/bin/grep -cE '\(:[0-9]+' "$T")" -eq 0 && test "$(/usr/bin/grep -cF 'the structural half that catches' "$T")" -eq 0</automated>
    <fails_when>non-zero exit — the module is missing or empty; a named new helper, constant or test function is absent, meaning a change was skipped; the sibling function name was dropped along with its stale citation; a parenthesised line citation matching `\(:[0-9]+` survives (IN-01 open); or the docstring still carries the marketing sentence WR-02 called an overclaim.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q > /tmp/180-04-rbi.txt 2>&1; rc=$?; tail -2 /tmp/180-04-rbi.txt; test "$rc" -eq 0 && /usr/bin/grep -qE '^12 passed' /tmp/180-04-rbi.txt && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' /tmp/180-04-rbi.txt</automated>
    <fails_when>non-zero pytest exit; the summary line does not start `12 passed` — 10 means neither new leg was collected, 11 means one was, and anything above 12 means a test was added that this plan does not name; or the summary reports any `failed` or `error` count.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "
import ast, importlib.util, pathlib, sys
sys.path.insert(0, '.')
spec = importlib.util.spec_from_file_location('rbi_hardened', 'tests/test_readback_inventory.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
ops = m._operations_source(); eng = m._engine_source()
print('head_connect_route_calls=' + str(m._read_eprom_connect_shape(ops)['connect_route_calls']))
print('head_context_count=' + str(m._read_eprom_connect_shape(ops)['context_count']))
print('head_tags=' + ','.join(m._last_ok_assignment_shape(eng)['tags']))
print('head_for_loop_count=' + str(m._last_ok_assignment_shape(eng)['for_loop_count']))
print('head_read_assign_in_for_loop=' + str(m._last_ok_assignment_shape(eng)['read_assign_in_for_loop']))
assert ops.count(m._STRAY_CONNECT_ANCHOR) == 1
assert eng.count(m._LAST_OK_REASSIGN_ANCHOR) == 1
sm = ops.replace(m._STRAY_CONNECT_ANCHOR, m._STRAY_CONNECT_ANCHOR + '            self._setup_operation(eprom_name, eprom_data_dict, COMMAND_READ)\n', 1)
vm = eng.replace(m._LAST_OK_REASSIGN_ANCHOR, m._LAST_OK_REASSIGN_ANCHOR + '    last_ok = last_ok and not divergence\n', 1)
ss = m._read_eprom_connect_shape(sm); vs = m._last_ok_assignment_shape(vm)
print('planted_context_count=' + str(ss['context_count']))
print('planted_connect_route_calls=' + str(ss['connect_route_calls']))
print('planted_names=' + ','.join(m._verdict_expression_names(vm)))
print('planted_tags=' + ','.join(vs['tags']))
d1 = m.test_one_read_eprom_call_costs_exactly_one_connect.__doc__ or ''
d2 = m.test_read_verdict_expression_reads_only_the_last_full_read_result.__doc__ or ''
print('wr01_doc_names_hardened_claim=' + str('connect_route_calls' in d1))
print('wr02_doc_drops_overclaim=' + str('the structural half that catches' not in d2))
print('wr02_doc_names_assignment_claim=' + str('assigned exactly twice' in d2))
" > /tmp/180-04-discrim.txt 2>&1; cat /tmp/180-04-discrim.txt; /usr/bin/grep -qx 'head_connect_route_calls=1' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'head_context_count=1' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'head_tags=const_true,read_eprom_call' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'head_for_loop_count=1' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'head_read_assign_in_for_loop=True' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'planted_context_count=1' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'planted_connect_route_calls=2' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'planted_names=VERDICT_BAD,VERDICT_OK,last_ok' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'planted_tags=const_true,read_eprom_call,other' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'wr01_doc_names_hardened_claim=True' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'wr02_doc_drops_overclaim=True' /tmp/180-04-discrim.txt && /usr/bin/grep -qx 'wr02_doc_names_assignment_claim=True' /tmp/180-04-discrim.txt</automated>
    <fails_when>Any of the twelve exact scalar lines is absent from the printed output. `planted_context_count` other than 1 or `planted_names` other than the three-name list means the mutant is NOT invisible to the pre-hardening pin, so the leg re-proves plan 180-01's gate instead of the hardening and the RED discriminates nothing. `planted_connect_route_calls` of 1 or `planted_tags` without the trailing `other` means the hardened check does not see the mutant at all. `head_*` values other than 1 / 1 / `const_true,read_eprom_call` / 1 / `True` mean the pin's expected values are wrong against the real source. A `False` on any of the three docstring scalars means a docstring still overclaims or does not state the new claim. An `AssertionError` traceback in the output means an anchor is not unique and the mutation missed.</fails_when>
    <automated>cd /workspaces && for f in evidence/180-04-one-connect-pin-hardened.txt evidence/180-04-verdict-pin-hardened.txt; do E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/$f; test -s "$E" || { echo "MISSING $E"; exit 1; }; /usr/bin/grep -qxF 'old_pin_at_planted=GREEN' "$E" || { echo "NO DISCRIMINATION IN $E"; exit 1; }; /usr/bin/grep -qxF 'new_pin_at_planted=RED' "$E" || { echo "NO RED IN $E"; exit 1; }; /usr/bin/grep -qxF 'new_pin_at_head=GREEN' "$E" || { echo "NO HEAD GREEN IN $E"; exit 1; }; done && /usr/bin/grep -qE '^12 passed' .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-verdict-pin-hardened.txt && /usr/bin/grep -qxF 'stale_line_citations=0' .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-04-verdict-pin-hardened.txt</automated>
    <fails_when>The loop prints `MISSING`, `NO DISCRIMINATION IN`, `NO RED IN` or `NO HEAD GREEN IN` with a path and exits 1 — that transcript is absent or does not carry the three-way record a hardening proof requires: the pre-hardening pin green on the mutant, the hardened pin red on it, and the hardened pin green at HEAD. Also non-zero when the verdict transcript carries no `12 passed` module tail or no `stale_line_citations=0` scalar.</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff 93a1672 --quiet -- firestarter/ && git diff 93a1672 --quiet -- tests/fixtures/ && test "$(git diff 93a1672 --name-only)" = "tests/test_readback_inventory.py" && D=$(mktemp) && git diff 93a1672 -- tests/ > "$D" && test "$(/usr/bin/grep -E '^\+' "$D" | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && rm -f "$D" && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/ && test -z "$(git -C /workspaces/firestarter status --porcelain)"</automated>
    <fails_when>non-zero exit — the commit touched anything under `firestarter/` (product source or the generated database) or under `tests/fixtures/`; the changed-file set since pinned app anchor `93a1672` is not exactly `tests/test_readback_inventory.py`; it added a line whose first non-space character is a hash; ruff reports a rule code or names an unformatted file; or the firmware submodule is dirty.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import io,tokenize;B={'tests/test_chip_test.py':621,'tests/test_readback_inventory.py':0};bad=[p+':'+str(sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)) for p,e in B.items() if sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)!=e];print('phase_added_comments='+('0' if not bad else ','.join(bad)));raise SystemExit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file with its measured count is where the count moved off 621 or 0, in either direction. The `tokenize` gate is used rather than the inherited added-line regex because that regex cannot see a trailing comment on a code line.</fails_when>
  </verify>
  <acceptance_criteria>
    - `_read_eprom_connect_shape` returns a `connect_route_calls` key counting `ast.Call` nodes within `read_eprom`'s own `FunctionDef` whose `func.attr` is `_operation_context`, `_setup_operation` or `find_and_connect`, and still returns `context_count`, `connect_chain` and `disconnect_in_finalbody` unchanged.
    - `test_one_read_eprom_call_costs_exactly_one_connect` asserts `shape["connect_route_calls"] == 1` in addition to its three existing assertions.
    - `_last_ok_assignment_shape` returns `tags`, `for_loop_count` and `read_assign_in_for_loop`, and against the committed `chip_test.py` returns `tags == ["const_true", "read_eprom_call"]`, `for_loop_count == 1` and `read_assign_in_for_loop is True`.
    - `test_read_verdict_expression_reads_only_the_last_full_read_result` asserts all three of those alongside its existing `_verdict_expression_names` equality.
    - `test_a_planted_stray_setup_operation_call_in_read_eprom_reddens_the_hardened_pin` exists, asserts its anchor occurs exactly once, asserts `mutant_shape["context_count"] == 1` and `mutant_shape["connect_route_calls"] == 2`, and raises `AssertionError` on the hardened claim inside `pytest.raises`.
    - `test_a_planted_last_ok_reassignment_before_the_return_reddens_the_hardened_pin` exists, asserts its anchor occurs exactly once, asserts `_verdict_expression_names(mutant) == ["VERDICT_BAD", "VERDICT_OK", "last_ok"]` and `tags == ["const_true", "read_eprom_call", "other"]`, and raises `AssertionError` on the hardened claim inside `pytest.raises`.
    - `test_one_read_eprom_call_costs_exactly_one_connect.__doc__` contains `connect_route_calls`.
    - `test_read_verdict_expression_reads_only_the_last_full_read_result.__doc__` does not contain `the structural half that catches` and does contain `assigned exactly twice`.
    - `/usr/bin/grep -cE '\(:[0-9]+' tests/test_readback_inventory.py` returns 0, and the module still contains `test_read_step_last_run_failure_yields_bad`.
    - `./.venv311/bin/python -m pytest tests/test_readback_inventory.py -o addopts="" -q` reports `12 passed` with no `failed` or `error` count.
    - `evidence/180-04-one-connect-pin-hardened.txt` and `evidence/180-04-verdict-pin-hardened.txt` each contain `old_pin_at_planted=GREEN`, `new_pin_at_planted=RED` and `new_pin_at_head=GREEN`; the verdict transcript also contains a `12 passed` tail and `stale_line_citations=0`.
    - `git diff 93a1672 --name-only` in `firestarter_app` prints exactly `tests/test_readback_inventory.py`, and `git diff 93a1672 --quiet -- firestarter/` and `-- tests/fixtures/` both exit 0; `ruff check tests/` and `ruff format --check tests/` both pass.
    - The `tokenize` gate prints `phase_added_comments=0`, holding `tests/test_chip_test.py` at 621 and `tests/test_readback_inventory.py` at 0.
  </acceptance_criteria>
  <done>A stray `self._setup_operation(...)` anywhere in `read_eprom`'s body now reddens the one-connect pin that used to stay green on it, and a `last_ok` rebinding before the return now reddens the verdict pin that used to stay green on it — each proven by a leg that asserts, in the same test, that the pre-hardening claim still passes on the same mutant. Both docstrings say only what their checks prove, the stale sibling citation is a function name instead of a line number, and the module runs 12 green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths **created by this plan** (absent at meta HEAD `dd2a88eb` and app HEAD `93a1672`, so a
source-grounding drift check must not flag them as unresolved references):

**New test functions** in `firestarter_app/tests/test_readback_inventory.py` —
`test_a_planted_stray_setup_operation_call_in_read_eprom_reddens_the_hardened_pin`,
`test_a_planted_last_ok_reassignment_before_the_return_reddens_the_hardened_pin`.

**New test helper** — `_last_ok_assignment_shape`.

**New module-level constants** — `_STRAY_CONNECT_ANCHOR`, `_LAST_OK_REASSIGN_ANCHOR`.

**New dict key returned by an existing helper** — `connect_route_calls`, from
`_read_eprom_connect_shape`. Its sibling keys `context_count`, `connect_chain` and
`disconnect_in_finalbody` already exist (plan `180-01`).

**New evidence files**, all under
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/` —
`180-04-seed-regeneration-closed.txt`, `180-04-one-connect-pin-hardened.txt`,
`180-04-verdict-pin-hardened.txt`.

**New seed prose** — a dated follow-through block appended inside the existing
`## Status: Phase 180 amendment` section of `.planning/seeds/dev-test-adaptive-sequencing.md`. No new
heading is added.

**Pre-existing symbols this plan reads or extends, created earlier and NOT new** — `_engine_source`,
`_operations_source`, `_read_eprom_connect_shape`, `_verdict_expression_names`, `_CONTEXT_ANCHOR`,
`_VERDICT_ANCHOR`, `_PLANTED_ANCHOR`, `test_one_read_eprom_call_costs_exactly_one_connect`,
`test_read_verdict_expression_reads_only_the_last_full_read_result`,
`test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin`,
`test_a_planted_divergence_term_in_the_verdict_reddens_the_pin`,
`test_read_step_last_run_failure_yields_bad`,
`test_read_step_first_run_failure_with_passing_last_run_yields_ok`.

**Deliberately NOT produced** — no new test module; no new checker script under `tools/`; no runtime
backstop test for the one-connect premise (a real serial open cannot be observed without a board, and
this phase is not hardware-gated per D-02); no fixture file of any kind, because D-07's pattern is
in-memory string mutation; no re-key ledger row (the protocol was retired 2026-09-08); no edit to
`180-PRUNE-08-CLOSURE.md` or to plans `180-01`, `180-02`, `180-03`.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. This plan changes one Markdown file in the meta repo and one test module
in the app repo. There is no network path, no serial path, no untrusted input, no credential, no user
data, no dependency install and no schema. The classic web categories — injection, XSS, SSRF, broken
access control, path traversal on user input — have no surface here and are not modelled; asserting
otherwise would be exactly the kind of unbacked claim this milestone exists to catch.

The honest threat surface is the integrity of the phase's own records and gates: a pin that passes while
its premise is false, and a seed left in a state from which a rejected design can be regenerated.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → a future planner | The seed is read as instruction by the next milestone that opens the `dev test` engine. A live-reading design description left standing there is the only harm Task 1 can do, and it is the harm D-09 exists to prevent — measured as half-done by `180-VERIFICATION.md`. |
| test gate → the phase's own conclusion | Both pins are the machine half of the PRUNE-08 close. A gate that reads green while the invariant it names is false converts the close into an assertion. WR-01 is exactly this, with no runtime backstop anywhere in the phase's diff. |
| test docstring → the next reader of the gate | A docstring that oversells its check makes a later reader trust a claim the code does not carry, and stops them writing the test that would. WR-02's second half is this, and it is a finding in its own right. |
| meta repo ↔ both submodules | No data crosses. The boundary is asserted rather than assumed: Task 1 requires both submodules porcelain-clean, and Task 2 requires its commit's changed-file set to be exactly one test module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-13 | Tampering | `.planning/seeds/dev-test-adaptive-sequencing.md` R3 | high | mitigate | Removal, never annotation: eleven named fragments negative-grepped to zero, the connect-count objection positively required to survive, and four region `cmp` legs against pinned blob `7a47a1fd` proving nothing outside R3's body and the Phase 180 section moved. The judgment the verifier could not settle by grep becomes eleven scalars. |
| T-180-14 | Spoofing | `test_one_read_eprom_call_costs_exactly_one_connect` | high | mitigate | The pin is extended from the `with` header to every connect route in `read_eprom`'s body, and the new leg proves the discrimination in-process by asserting the pre-hardening claim still passes on the same mutant the hardened claim rejects. Rated `high` because the closure argument rests on this premise; mitigated, so nothing blocks. |
| T-180-15 | Spoofing | `test_read_verdict_expression_reads_only_the_last_full_read_result` and its docstring | medium | mitigate | `last_ok`'s assignment shape is pinned as a closed tag list, so a rebinding invisible to the `verdict=` expression reddens the pin; the marketing sentence is negative-grepped out and the two-claim statement positively required, checked by reading `__doc__` rather than by inspection. |
| T-180-16 | Repudiation | `evidence/180-04-*.txt` | low | mitigate | Flat `key=value` scalars a reader can diff, one per claim, each recomputed from the file or the helper rather than transcribed; every hardening transcript must carry all three of the pre-hardening-green, hardened-red and hardened-at-head-green records. |
| T-180-17 | Tampering | `firestarter_app/firestarter/`, the `firestarter` submodule, `chip_database.json` | medium | mitigate | `git diff HEAD~1 --quiet -- firestarter/`, an exact changed-file-set equality, and both submodule porcelain checks. The database is generated and a hand edit there is the recorded hazard. |
| T-180-18 | Tampering | the four pre-existing `#` comments at the sibling read-step legs | low | mitigate | The `tokenize` gate asserts equality with 621, which fires on removal as well as addition, so neither copying nor tidying them can pass. |
| T-180-SC | Tampering | npm / pip / cargo installs | low | accept | No package is installed by this plan; every tool invoked is already in the pinned 3.11 venv or is Python standard library. No Package Legitimacy Audit is required. |

Two threats are rated `high` (T-180-13, T-180-14) and both are dispositioned `mitigate` with measured
gates, so the `security_block_on: high` threshold is satisfied and nothing blocks.
</threat_model>

<verification>
1. The seed carries zero occurrences of each of the eleven named fragments, and still carries the
   rejection verdict, the `10 connects` objection, `_operation_context` and the closing-document pointer.
2. All four seed regions `cmp` identical against pinned blob `7a47a1fd9236bb4068bc72be6c8f8ed35f86e300`,
   with the three line-count guards holding at 45, 72 and 18.
3. The Phase 180 amendment section carries the dated follow-through; the Phase 177 section is byte-unchanged.
4. `connect_route_calls` reads 1 at HEAD and 2 on the planted stray-connect mutant, while `context_count`
   stays 1 on that mutant.
5. `tags` reads `const_true,read_eprom_call` at HEAD and gains `other` on the planted rebinding mutant,
   while `_verdict_expression_names` stays at the three-name list on that mutant.
6. Both docstring scalars hold: `connect_route_calls` present in one, the marketing sentence absent and
   `assigned exactly twice` present in the other.
7. Zero parenthesised line citations remain in the module; the sibling function name survives.
8. `tests/test_readback_inventory.py` reports 12 passed; `ruff check` and `ruff format --check` on
   `tests/` pass; the app commit's file set is exactly that module.
9. `phase_added_comments=0` under the `tokenize` gate; both submodules and `chip_database.json` clean.

**Do not** run the seven-leg battery here, do not touch `REQUIREMENTS.md` or `ROADMAP.md`, and do not
assert any suite floor. All three belong to plan `180-05`, which runs the battery once, after these
fixes land.
</verification>

<success_criteria>
- The blocking gap is closed by absence, not annotation, and the closure is settled by eleven measured
  scalars rather than a comprehension judgment (D-09, `gaps[0]`, `prohibitions_flagged[0]`,
  `human_verification[1]`).
- Nothing in the seed outside R3's body and the Phase 180 amendment section moved, proven by `cmp`.
- WR-01, WR-02 and IN-01 are closed, and each hardening was seen RED against a mutant the pre-hardening
  pin passes (D-07, `human_verification[2]` as the operator answered it).
- Zero product-source lines, zero firmware lines, zero `chip_database.json` bytes and zero `#` comments.
</success_criteria>

<output>
Create `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-04-SUMMARY.md` when done.
Record in it: both removed seed paragraphs quoted verbatim, so the text has a home outside git history;
the disposition of the scattered-transport-fault sentence with its reason; the exact seed line ranges as
edited, which will have shifted from the :75-99 written here; the measured `connect_route_calls` and
`tags` values at HEAD and on each mutant; the module's before and after test counts; and any claim in
this plan the executor found false.
</output>
