---
phase: 180-read-step-sampling-conditional-on-phase-176
plan: 02
type: execute
wave: 2
depends_on: ["180-01"]
files_modified:
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md
  - .planning/seeds/dev-test-adaptive-sequencing.md
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-closure-claims.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-seed-amendment.txt
autonomous: true
requirements: [PRUNE-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The closing document names the excluded thing and gives its reason — the bit-structured sample, excluded because 10 connects exceed 1 on both board classes at every reference size (D-01)"
    - "Roadmap criterion 4 carries an explicit Not Applicable verdict together with the quoted precondition that is false, rather than being silently unaddressed (D-10)"
    - "The size-gated variant is recorded as evidence and explicitly declined — no new requirement, backlog item or todo is created for it (D-05)"
    - "The close is stated as resting on today's one-connect-per-read design, naming R4-01 as the specific change that would require redoing it (D-08)"
    - "`_read_region` is recorded as the primitive a future attempt would use, as a note and not a licence to implement (D-11)"
    - "The corpus sentence states the supported counts per size bucket and carries no blanket claim that all 746 rows are supported (C-1)"
    - "A planner reading only `.planning/seeds/dev-test-adaptive-sequencing.md` can no longer regenerate the rejected sampling design, and can no longer read that the per-connect cost is unmeasured (D-09, Ruling 2)"
    - "The seed keeps exactly four frontmatter fields and an intact projected-effect table; the Phase 177 amendment section is left byte-unchanged and a sibling Phase 180 section is appended"
    - "Nothing under `firestarter_app/` and nothing in the `firestarter` firmware submodule changed in this plan"
  prohibitions:
    - "The seed must not be left in a state from which the rejected sampling design can be regenerated"
    - "The close must not overstate its evidence"
    - "The closing document must not present a modelled figure as a measured one"
  artifacts:
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      provides: "the completed close — exclusion, criterion 4 N/A, granted standing, and the condition that would invalidate it"
      contains: "## What would invalidate this close"
    - path: ".planning/seeds/dev-test-adaptive-sequencing.md"
      provides: "R3 amended in place, R4's falsified sentence corrected, the status line repointed, and a sibling Phase 180 amendment section"
      contains: "## Status: Phase 180 amendment"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-closure-claims.txt"
      provides: "scalar-per-claim proof that each required closing-document section and constraint is present"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-seed-amendment.txt"
      provides: "the Phase 177 key set with r3_intact inverted, plus r4_amended and the preserved-invariant scalars"
  key_links:
    - from: ".planning/seeds/dev-test-adaptive-sequencing.md"
      to: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      via: "the amended R3 and the Phase 180 amendment section point a future planner at the closing document"
      pattern: "180-PRUNE-08-CLOSURE"
    - from: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "names R4-01 in Future Requirements as the change that would invalidate the close"
      pattern: "R4-01"
---

<objective>
Finish the close and make the design record honest.

Plan `180-01` opened `180-PRUNE-08-CLOSURE.md` on the verdict and the connect arithmetic. This plan
completes it with the three moves `177-READBACK-INVENTORY.md` makes and one stronger fourth: name the
excluded thing and its reason, state the standing this milestone grants the close, and — instead of a
reddening gate alone — name **R4-01** as the design change that would invalidate the close outright
(D-08), because no test can detect a design change that has not happened yet. Roadmap criterion 4 gets
an explicit Not Applicable verdict with its unmet precondition quoted (D-10), the size-gated variant is
recorded as evidence and declined (D-05), and `_read_region` is recorded as the primitive a future
attempt would reach for (D-11).

Then the seed is amended **in place** (D-09), following Phase 177's R1 precedent exactly, so a planner
reading only the seed can no longer regenerate the rejected design — including R4's sentence that
Phase 176 falsified (Ruling 2).

Purpose: the two artifacts a future milestone actually reads first — the closing document and the
seed — stop carrying a live instruction to build the thing this phase measured as not worth building.
Output: a complete `180-PRUNE-08-CLOSURE.md`, an amended seed, and two evidence transcripts.
</objective>

<orchestrator_dispositions>
- **Ruling 2 — seed R4's now-false sentence is corrected, minimally.** D-09's own rule is that a
  statement this phase makes false must change; that is why it mandates the frontmatter `status:` edit.
  R4 (seed :105-107) asserts the per-connect cost has not been measured, which Phase 176 falsified, and
  D-09 attributes the "measure a connect first" instruction to R3 when it actually lives in R4. Leaving
  R4 standing recreates the precise regenerate-from-the-seed hazard D-09 exists to prevent. **Correct
  R4's factual claim only** — point it at MEAS-01's measurement and at this phase's closing document.
  **Do not touch R4's design substance, and do not scope, implement or re-file R4-01**, which the
  `<domain>` out-of-scope list forbids explicitly. This widens D-09's stated scope by one paragraph and
  is recorded here so the operator can override it at review time.
- **Ruling 3 (model-free form) and Ruling 1 (the one-connect pin)** were discharged in plan `180-01`;
  Ruling 3's constraint continues to bind every section this plan appends.
- **Correction C-1 (measured):** the corpus sentence CONTEXT.md authorises overstates by one word. Ten
  of 746 rows are not `supported`, all ten at or below 8 KiB, so the load-bearing buckets survive
  intact — 148 of 148 at 256 KiB, 114 of 114 at or above 512 KiB. Drop the blanket claim and state the
  supported counts per bucket. `support_status` honesty is a named binding constraint in this project.
- **Correction C-4 (measured):** the seed's own block range read literally yields 11 blocks and
  contradicts its stated 10. The enumeration written into the closing document by plan `180-01` is
  authoritative; the amended R3 must not re-quote the loose range notation.
</orchestrator_dispositions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-CONTEXT.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md
@/workspaces/.planning/seeds/dev-test-adaptive-sequencing.md
@/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-01-SUMMARY.md
</context>

<environment>
This plan touches the **meta repo only** — `/workspaces`, branch
`gsd/v1.36-dev-test-fidelity-planning`. Neither submodule is modified; both must stay porcelain-clean.

Shell `grep` here is **ugrep and honours `.gitignore`**, so it silently under-scans. Every
evidence-grade scan below uses `/usr/bin/grep`.

Edit `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` **by hand**, never with the `gsd-tools`
requirements or roadmap verbs — those reformat the whole file, and `roadmap.update-plan-progress`
additionally clobbers the dependency table. Neither file is touched by this plan at all; the note is
here because the seed edit is adjacent work in the same tree.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Complete the closing document — the exclusion, criterion 4's N/A verdict, the granted standing, and the condition that would invalidate the close</name>
  <files>
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-closure-claims.txt
  </files>
  <read_first>
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` — as left by plan `180-01`: the title, `## The verdict` and `## The connect arithmetic`. Append below them; do not rewrite them.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/177-READBACK-INVENTORY.md` — the whole file (77 lines), and in particular its `## PRUNE-04's closure` section at :66-77, whose four moves are the house form and whose closing sentence explicitly grants PRUNE-08 the same standing.
    - `/workspaces/.planning/REQUIREMENTS.md:122` — R4-01's text in Future Requirements, including its own stated reason for deferral.
    - `/workspaces/.planning/ROADMAP.md:445` — roadmap criterion 4, whose precondition must be quoted verbatim.
    - `/workspaces/firestarter_app/firestarter/chip_test.py:2851-2885` — `_read_region` and its docstring, which already records the absolute-offset hole-padding hazard criterion 4 names.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-RESEARCH.md` sections "Criterion 4" (line 904), "Closing-Document Form" (line 926) and correction C-1 (line 187).
    - `/workspaces/.planning/notes/dev-test-sequence-cost-model.md` — read its provenance section only, so you know what may not be published; no figure from it appears in this document.
  </read_first>
  <action>
Append exactly four sections to `180-PRUNE-08-CLOSURE.md`, in this order, below the two written by plan `180-01`.

`## What is excluded, and why` — name the excluded thing explicitly, as `177-READBACK-INVENTORY.md` names its row 6 rather than leaving the exclusion implicit. The excluded thing is the bit-structured sample described in seed R3; the reason is the arithmetic already stated above — 10 connects against 1, on both board classes, at every reference size this milestone tests. State the size axis without any modelled number (D-04): N grows with the base-two logarithm of the device size while the full read's wire cost grows linearly with size, so a crossover exists and it lies above every reference part this milestone tests. Give the corpus context in the corrected form (C-1): of 746 database rows, 484 are at or below 128 KiB, 148 are 256 KiB and 114 are at or above 512 KiB, and every row at 256 KiB and above is `supported`. Do not write that all 746 are supported — ten are not, all ten at or below 8 KiB, so the two load-bearing buckets are 148 of 148 and 114 of 114. Then record D-05 in the same section: the size-gated variant was offered to the operator as both a ship option and a re-file option and **declined both**; it is recorded here as evidence and is deliberately not a requirement, backlog item or todo.

`## Criterion 4 — Not Applicable` — quote roadmap criterion 4's precondition verbatim, state that sampling does not ship, and therefore that the precondition is false and the criterion is Not Applicable (D-10). Say explicitly that no hole-padded fixture and no block-wise comparator were built, and why building one to satisfy a criterion whose precondition is false would be dishonest. Then record D-11's forward note in the same section, as a note and not a licence to implement: `firestarter_app/firestarter/chip_test.py:2851` `_read_region` is the primitive a future attempt would use — a region read whose result is sliced at the **absolute** offset, because a region read produces a hole-padded file whose real bytes sit at offset `start` and never at 0, returning empty bytes rather than raising on any failure. Note that the hole-padding hazard criterion 4 names is therefore already solved in that primitive, so a future attempt inherits the fix rather than the bug, and that routing a future sampler through `_read_region` is also what would keep Phase 177's census at exactly two device-read call sites.

`## The standing this close is granted` — quote back `177-READBACK-INVENTORY.md`'s own sentence granting PRUNE-08 the same standing it took for PRUNE-04's measured-empty close, and state that this document is that close being taken.

`## What would invalidate this close` — state that the entire verdict is a consequence of one design fact, one connect per read, and name **R4-01** (`.planning/REQUIREMENTS.md` Future Requirements, line 122) as the specific change that would invert the arithmetic completely: leasing one validated link per plan would make N region reads cost roughly one connect plus N small transfers rather than N connects (D-08). Say plainly that this is an honest condition on the close and not a deferral, and that no new requirement is filed. Then name the gates that would redden if the read step's verdict source moved — the three D-06 tests and the one-connect pin added by plan `180-01`, by function name and module — while noting that a gate cannot detect a design change that has not happened, which is why R4-01 is stated as a condition rather than a test.

Throughout: **publish no figure from the cost-model note** (D-04, Ruling 3) — no read rate, no wire time, no per-operation overhead, and no factor-of-two-and-a-half illustration. Never blend the two per-board-class connect figures; `176-MEASUREMENT.md` forbids a combined number by name. Cite `chip_test.py:2767` and `:2851`, not 177's stale numbers.

Then write `evidence/180-02-closure-claims.txt` in the 177 transcript form — numbered section headers, flat key=value scalars, explicit rc after each command — recording one scalar per required claim: the six section headings present, `criterion4_verdict=NA`, `criterion4_precondition_quoted=1`, `r4_01_named=1`, `read_region_noted=1`, `size_gated_declined=1`, `blanket_supported_claim=0`, `modelled_rate_published=0`, and the count of D-06 test functions named by the document.
  </action>
  <verify>
    <automated>cd /workspaces && C=.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md; test "$(/usr/bin/grep -cE '^## ' "$C")" -ge 6 && /usr/bin/grep -qF '## What is excluded, and why' "$C" && /usr/bin/grep -qF '## Criterion 4 — Not Applicable' "$C" && /usr/bin/grep -qF '## The standing this close is granted' "$C" && /usr/bin/grep -qF '## What would invalidate this close' "$C"</automated>
    <fails_when>non-zero exit — fewer than six level-two headings, or any one of the four appended section headings is absent, which means the close is missing one of its required moves.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md; /usr/bin/grep -qF 'R4-01' "$C" && /usr/bin/grep -qF '_read_region' "$C" && /usr/bin/grep -qF 'every row at 256 KiB and above is' "$C" && /usr/bin/grep -qF 'chip_test.py:2851' "$C" && /usr/bin/grep -qF 'test_read_verdict_expression_reads_only_the_last_full_read_result' "$C"</automated>
    <fails_when>non-zero exit — the close does not name R4-01 as the invalidating change, does not record the region-read primitive, does not carry the corrected per-bucket corpus wording, cites a stale line number for the primitive, or does not name the reddening gate by function.</fails_when>
    <automated>cd /workspaces && C=.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md; test -s "$C" && ! /usr/bin/grep -qF 'KB/s' "$C" && ! /usr/bin/grep -qF '2.56' "$C" && ! /usr/bin/grep -qF 'all `supported`' "$C" && ! /usr/bin/grep -qiE '^[[:space:]]*(TBD|TODO|undecided)[[:space:]]*$' "$C"</automated>
    <fails_when>non-zero exit — the closing document is missing or empty (the leading existence guard is what stops the four negative greps passing vacuously on an absent file), or a modelled read-rate figure, a blended per-connect number, the blanket corpus over-claim, or a bare placeholder line is present in it.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-closure-claims.txt; test -s "$E" && /usr/bin/grep -qF 'criterion4_verdict=NA' "$E" && /usr/bin/grep -qF 'blanket_supported_claim=0' "$E" && /usr/bin/grep -qF 'modelled_rate_published=0' "$E"</automated>
    <fails_when>non-zero exit — the claims transcript is missing or does not record the criterion-4 verdict and the two honesty scalars, so the constraints were asserted but never measured.</fails_when>
    <automated>test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain)"</automated>
    <fails_when>non-zero exit — either submodule has a dirty working tree, meaning this documentary task touched code it must not touch.</fails_when>
  </verify>
  <acceptance_criteria>
    - `180-PRUNE-08-CLOSURE.md` contains at least six level-two headings, including `## What is excluded, and why`, `## Criterion 4 — Not Applicable`, `## The standing this close is granted` and `## What would invalidate this close`.
    - `180-PRUNE-08-CLOSURE.md` contains `R4-01`, `_read_region`, `chip_test.py:2851`, the literal `every row at 256 KiB and above is`, and the function name `test_read_verdict_expression_reads_only_the_last_full_read_result`.
    - `180-PRUNE-08-CLOSURE.md` quotes roadmap criterion 4's `If sampling ships` precondition verbatim and states the Not Applicable verdict.
    - `180-PRUNE-08-CLOSURE.md` does not contain `KB/s`, does not contain `2.56`, and does not contain the blanket corpus over-claim.
    - `evidence/180-02-closure-claims.txt` contains `criterion4_verdict=NA`, `blanket_supported_claim=0` and `modelled_rate_published=0`.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` both print nothing.
  </acceptance_criteria>
  <done>PRUNE-08's close is complete as a document: the verdict, a recomputable arithmetic, the named exclusion with its reason, criterion 4's explicit N/A with its unmet precondition, the standing Phase 177 already granted it, and R4-01 named as the one change that would require redoing the whole thing.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the seed in place so the rejected design cannot be regenerated from it</name>
  <reversibility rating="costly">The seed is the artifact a future planner reads first; if this amendment is skipped or later reverted, a subsequent milestone can regenerate the rejected sampling design from the seed alone with no trace of Phase 176's measurement. Rated `costly` by CONTEXT.md D-09 — flagged, not gated.</reversibility>
  <files>
    .planning/seeds/dev-test-adaptive-sequencing.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-seed-amendment.txt
  </files>
  <read_first>
    - `/workspaces/.planning/seeds/dev-test-adaptive-sequencing.md` — the whole file (177 lines). Note the four frontmatter fields at :2-5, R3's heading at :70 and its four paragraphs at :70-93, R4 at :95-107, the projected-effect table below it, and the `## Status: Phase 177 amendment` section at :162-177.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md` section 4 — the Phase 177 amendment quoted verbatim and the six moves to copy, with item 6 called out as the one to get right.
    - `/workspaces/.planning/phases/177-evidence-gated-read-back/evidence/177-03-seed-amendment.txt` — the evidence key set this transcript inverts.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` — as completed by Task 1; the amended seed points at it.
    - `/workspaces/.planning/REQUIREMENTS.md:103` and `:122` — MEAS-01's text and R4-01's, the two the amended R4 sentence points at.
  </read_first>
  <action>
Make three in-place edits to `.planning/seeds/dev-test-adaptive-sequencing.md` and append one new section. Hand edits only — never the `gsd-tools` requirements or roadmap verbs.

**Edit 1, frontmatter line 5.** The `status:` value's middle clause currently asserts that R3 is still outstanding for this phase. That statement becomes false the moment this phase closes PRUNE-08, so replace that clause with one recording that R3 is closed by Phase 180 as measured, not worth doing, citing PRUNE-08. Leave the R1/R2 clause and the R4 deferral clause as they are. The file must still have **exactly four** frontmatter fields — `title`, `trigger_condition`, `planted_date`, `status` — because Phase 177's evidence asserts that count. Line 3's `trigger_condition` is stale in a different way and is **out of scope; do not touch it**.

**Edit 2, R3's body at :75-81.** Replace that paragraph **in place**, not with an annotation beside it. It currently reads as a live imperative telling a planner to swap the read step's second full run for a bit-structured sample and describes the block structure. The replacement must state that the design was measured and rejected, and must **preserve the connect-count objection inside the replacement text** so the design cannot be regenerated without meeting it: at the 64 KiB reference size the sample is 10 whole device reads against the 1 it would replace, and because `EpromOperator._operation_context` connects on entry and disconnects in its `finally` block, each of those is a full connect — so the sample costs 10 connects where the sweep costs 1. Point the reader at `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` for the full argument, the per-board-class measured figures and the size-axis finding. Do **not** re-quote the loose block-range notation the original paragraph used — it reads literally as 11 blocks and contradicts the seed's own stated 10 (C-4); if a block count is stated at all, state 10 and point at the enumeration in the closing document. Leave R3's heading at :70, its opening paragraph, its escalation paragraph and its stated-cost paragraph as they are — the amendment targets the live instruction, not the whole rule.

**Edit 3, R4's sentence at :105-107 (Ruling 2).** Its opening assertion that the per-connect cost has not been measured is now false: Phase 176 measured it per board class under MEAS-01. Correct that factual claim **minimally** — state that the per-connect cost is now measured, cite MEAS-01 and `176-MEASUREMENT.md` sections 4a and 4b, and point at this phase's closing document for what the measurement decided about R1 to R3. Keep the sentence's forward-looking role for R4 itself intact. **Do not touch R4's design substance, its connect counts, or the folded-monitor-read sub-step, and do not scope, implement or re-file R4-01** — the phase boundary forbids that explicitly.

**Append one new section** at the very end of the file, after `## Explicitly out of scope`, as a **sibling** to the Phase 177 section. **Do not edit the Phase 177 section.** Head it `## Status: Phase 180 amendment`, open it with the bold phase number and date in the precedent's form, and follow the precedent's six moves: state what was wrong with the amended text and why, with the technical reason; name the deciding authority, which here is MEAS-01's measurement plus PRUNE-08's own close text in `.planning/REQUIREMENTS.md`; state the in-place principle in the precedent's own words, that this amendment replaces R3's paragraph in place rather than merely annotating it so the destructive reading is not outvoted but absent, and a planner reading only this seed can no longer regenerate it; and close with an explicit not-touched list. Get that last list right: Phase 177's own list says R3 and R4 were not touched, so Phase 180's section must state that R3 and one sentence of R4 **are** now amended, and its own not-touched list reads R1, R2, the projected-effect table, the per-class characteristics section, the sequencing note and the out-of-scope section.

Then write `evidence/180-02-seed-amendment.txt` in the 177 transcript form, inverting that phase's key set: `r3_intact=0`, `r3_amended=1`, `r4_amended=1`, `connect_objection_preserved=1`, `status_line_repointed=1`, `frontmatter_fields=4`, `projection_table_intact=1`, `phase177_section_bytes_unchanged=1`, `dated_block=1`, plus the closing document path the amended text points at.
  </action>
  <verify>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md; test -s "$S" && ! /usr/bin/grep -qF 'R3 remains for Phase 180' "$S" && ! /usr/bin/grep -qF 'Replace the read step' "$S" && ! /usr/bin/grep -qF 'Per-connect cost is unmeasured' "$S"</automated>
    <fails_when>non-zero exit — the seed is missing or empty (the leading existence guard is what stops the three negative greps passing vacuously on an absent file, since `grep` on a missing path exits non-zero and the negation would read that as clean), or one of the three now-false statements is still in it, which means a future planner reading only the seed can still regenerate the rejected design or still believe the cost is unmeasured.</fails_when>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md; /usr/bin/grep -qF '## Status: Phase 180 amendment' "$S" && /usr/bin/grep -qF '## Status: Phase 177 amendment' "$S" && /usr/bin/grep -qF '180-PRUNE-08-CLOSURE.md' "$S" && /usr/bin/grep -qF 'MEAS-01' "$S" && /usr/bin/grep -qF 'in place' "$S"</automated>
    <fails_when>non-zero exit — the Phase 180 amendment section is absent, the Phase 177 section was destroyed rather than left as a sibling, the amendment does not point at the closing document, does not cite the measurement requirement, or does not state the in-place principle.</fails_when>
    <automated>cd /workspaces && S=.planning/seeds/dev-test-adaptive-sequencing.md; test "$(sed -n '2,5p' "$S" | /usr/bin/grep -cE '^(title|trigger_condition|planted_date|status):')" -ge 4 && /usr/bin/grep -qF '## Projected effect' "$S" && test "$(/usr/bin/grep -c '10 connects' "$S")" -ge 1</automated>
    <fails_when>non-zero exit — the frontmatter no longer carries all four expected fields, the projected-effect heading was destroyed, or the connect-count objection was dropped from the amended R3 rather than preserved inside it.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-02-seed-amendment.txt; test -s "$E" && /usr/bin/grep -qF 'r3_intact=0' "$E" && /usr/bin/grep -qF 'r3_amended=1' "$E" && /usr/bin/grep -qF 'r4_amended=1' "$E" && /usr/bin/grep -qF 'frontmatter_fields=4' "$E" && /usr/bin/grep -qF 'connect_objection_preserved=1' "$E"</automated>
    <fails_when>non-zero exit — the amendment transcript is missing or empty, or does not record the inverted R3 keys, the R4 amendment, the preserved frontmatter field count or the preserved objection.</fails_when>
    <automated>test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain)"</automated>
    <fails_when>non-zero exit — either submodule has a dirty working tree, meaning this documentary task touched code it must not touch.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/seeds/dev-test-adaptive-sequencing.md` no longer contains the literals `R3 remains for Phase 180`, `Replace the read step` or `Per-connect cost is unmeasured`.
    - The seed contains both `## Status: Phase 177 amendment` and `## Status: Phase 180 amendment`, in that order, with the Phase 177 section byte-unchanged.
    - The seed contains `180-PRUNE-08-CLOSURE.md`, `MEAS-01`, `in place` and at least one occurrence of `10 connects`.
    - Frontmatter lines 2 through 5 still carry exactly the four keys `title`, `trigger_condition`, `planted_date`, `status`, and line 3 is byte-unchanged.
    - The `## Projected effect` heading and its table are still present.
    - The Phase 180 amendment section's not-touched list names R1, R2, the projected-effect table, the per-class characteristics section, the sequencing note and the out-of-scope section, and states that R3 and one sentence of R4 were amended.
    - `evidence/180-02-seed-amendment.txt` contains `r3_intact=0`, `r3_amended=1`, `r4_amended=1`, `frontmatter_fields=4`, `projection_table_intact=1` and `connect_objection_preserved=1`.
  </acceptance_criteria>
  <done>A planner who reads only this seed finds no instruction to build the bit-structured sample, no claim that the per-connect cost is unmeasured, the connect-count objection stated where the design used to be, and a dated amendment section that says which paragraphs moved and which did not.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths this phase **creates** (absent at meta HEAD `f031be81` and app HEAD `ffb0060`, so a
drift check must not flag them as unresolved references):

**New documents** —
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` (created by
plan `180-01`, completed by this plan).

**New seed section** — `## Status: Phase 180 amendment` in
`.planning/seeds/dev-test-adaptive-sequencing.md`.

**New evidence files** — `180-02-closure-claims.txt`, `180-02-seed-amendment.txt`, alongside plan
`180-01`'s `180-01-verdict-pin-red.txt`, `180-01-behavioural-legs.txt`, `180-01-one-connect-pin.txt`
and plan `180-03`'s `180-03-requirement-marking.txt`, `180-03-phase-seal.txt`, all under
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/`.

**New test functions referenced by name in the closing document** (created by plan `180-01`) —
`test_read_verdict_expression_reads_only_the_last_full_read_result`,
`test_a_planted_divergence_term_in_the_verdict_reddens_the_pin`,
`test_read_step_last_run_failure_yields_bad`,
`test_read_step_first_run_failure_with_passing_last_run_yields_ok`,
`test_one_read_eprom_call_costs_exactly_one_connect`,
`test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin`.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. This plan writes **only Markdown** in the meta repo — no code, no
dependency, no network or serial path, no untrusted input, no credential or user data. Its whole risk
surface is what a future reader is told.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → a future planner | The seed and the closing document are read as instruction by the next milestone. A false or destructive statement left standing here is the only harm this plan can do, and it is the harm D-09 exists to prevent. |
| closing document → outward-facing claim | The corpus counts and `support_status` wording in the close are the kind of statement v1.35's honesty constraint forbids upgrading. An over-claim here would be the exact false-PASS family this milestone was built to catch. |
| meta repo ↔ both submodules | No data crosses. The boundary is asserted, not merely assumed: both verify blocks require porcelain-clean submodules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-05 | Tampering | `.planning/seeds/dev-test-adaptive-sequencing.md` | medium | mitigate | The amendment is **in place**, not an annotation, so the rejected reading is absent rather than outvoted; three negative greps assert each now-false statement is gone, and the connect-count objection is required to survive inside the replacement text so the design cannot be regenerated without meeting it. |
| T-180-06 | Spoofing | the closing document's corpus and `support_status` claims | medium | mitigate | The blanket over-claim is negative-grepped out and the corrected per-bucket wording is positively required; the transcript records `blanket_supported_claim=0` as a measured scalar rather than a promise. |
| T-180-07 | Tampering | the Phase 177 amendment section (:162-177) | low | mitigate | Phase 180 appends a sibling section; a positive grep requires the Phase 177 heading to survive, and the transcript records that section byte-unchanged. |
| T-180-08 | Repudiation | `evidence/180-02-*.txt` | low | mitigate | Flat key=value scalars a reader can diff, one per claim, rather than prose. |
| T-180-SC | Tampering | npm / pip / cargo installs | low | accept | No package is installed by this plan; it writes Markdown only. No Package Legitimacy Audit is required. |

No threat is rated `high`, so nothing here blocks.
</threat_model>

<verification>
1. `180-PRUNE-08-CLOSURE.md` carries at least six level-two headings and all four appended sections.
2. It names R4-01, the region-read primitive at its current line number, and the D-06 gate by function name; it quotes criterion 4's precondition and states the N/A verdict.
3. It publishes no modelled read-rate figure, no blended per-connect number, and no blanket corpus claim.
4. The seed carries none of the three now-false statements, carries a sibling Phase 180 amendment section, keeps four frontmatter fields and an intact projected-effect table, and preserves the connect-count objection inside the amended R3.
5. Both submodules are porcelain-clean.

**Do not** run the eight-leg gate battery, invoke `check_rekey_ledger.py`, or assert a suite floor of
2241 or 2242. The re-key protocol and its checker were retired on 2026-09-08; **no re-key ledger row is
needed for this phase**. The battery is seven legs and runs once, in plan `180-03`.
</verification>

<success_criteria>
- The close is complete and honest: verdict, recomputable arithmetic, named exclusion, criterion 4 N/A with its unmet precondition, granted standing, and R4-01 as the invalidating condition (D-01, D-03, D-04, D-05, D-08, D-10, D-11).
- The seed can no longer regenerate the rejected design and no longer asserts the cost is unmeasured (D-09, Ruling 2).
- The corrected corpus wording ships; no claim is upgraded (C-1).
- Zero code, zero firmware, zero product-source lines changed.
</success_criteria>

<output>
Create `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-02-SUMMARY.md` when done.
Record in it: the exact seed line ranges as edited (they will have shifted from the :5 / :75-81 /
:105-107 written here), the not-touched list as written, and any prior claim this plan found false.
</output>

