---
phase: 180-read-step-sampling-conditional-on-phase-176
plan: 05
type: execute
wave: 2
gap_closure: true
depends_on: ["180-04"]
files_modified:
  - firestarter_app/tests/test_chip_test.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-side-effect-helper.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-requirement-reseal.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-phase-seal.txt
autonomous: true
requirements: [PRUNE-08]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The two read-step behavioural legs no longer each carry their own near-identical side-effect closure (IN-02): `_alternating_read_side_effect` is defined once beside the module's other read-side-effect builders and called by both, and neither test function body defines a nested `_read_side_effect` any more"
    - "The refactor changed no assertion: the diff since pinned app anchor `93a1672` removes zero lines whose first non-space token is `assert`, and `pytest tests/test_chip_test.py -k read_step` still reports 4 passed against the measured 4 at HEAD"
    - "The third, pre-existing `_read_side_effect` closure inside `test_read_step_disagreement_is_divergence_metric_not_marginal` is untouched — it writes different bytes per run and always returns True, so it is a different behaviour, not a third duplicate"
    - "PRUNE-08 reads Complete in both places `.planning/REQUIREMENTS.md` records it, and in neither place before this phase's gap fixes were committed: the v1 checkbox reads `- [x] **PRUNE-08**` and the traceability row reads `| PRUNE-08 | Phase 180 | Complete |`, with `Gaps Found` gone from the table"
    - "Exactly two lines of `.planning/REQUIREMENTS.md` changed: the ledger counts move from the measured 19 unchecked / 27 checked / 18 Pending / 27 Complete / 1 Gaps Found to 18 / 28 / 18 / 28 / 0, and MEAS-01, R4-01, the Coverage block and Phase 181's rows are all still present"
    - "`ROADMAP.md`'s Phase 180 section records five plans and carries a `**Gap closure**` grouping with `180-04-PLAN.md` and `180-05-PLAN.md` ticked, alongside the three already-ticked plan boxes"
    - "Nothing in `ROADMAP.md` outside the Phase 180 section moved: everything up to and including the `### Phase 180:` heading (434 lines at pinned blob `cc9499934b4757899719ed35acc8d44d9ac89b31`) and everything from `### Phase 181:` to end of file (5386 lines) are byte-identical, proven by `cmp`"
    - "The seven-leg green-tree battery passes with the full app suite at or above 2247 — the measured 2245 at Phase 180's original final HEAD plus plan 180-04's two additive legs — and zero failures"
    - "No `#` comment was added or removed anywhere in the phase: `tokenize` COMMENT counts are still exactly 621 for `tests/test_chip_test.py` and 0 for `tests/test_readback_inventory.py`"
    - "Zero lines under `firestarter_app/firestarter/` changed, the `firestarter` firmware submodule is byte-unchanged, and `firestarter/data/chip_database.json` is untouched — the changed-file set since app anchor `93a1672` is exactly `tests/test_chip_test.py` and `tests/test_readback_inventory.py`"
  prohibitions:
    - statement: "PRUNE-08 must not be re-marked Complete before this phase's gap fixes are committed. The requirement was reverted out of Complete for a reason; re-ticking it on the strength of a plan existing rather than the fixes landing would repeat the exact over-claim the verification caught, in the same ledger, one iteration later."
      status: required
      verification: judgment
    - statement: "No line of `ROADMAP.md` outside the Phase 180 section may move, and the phase-level checkbox in the `### Phases` list must stay unticked — that flip belongs to phase completion, which the orchestrator runs, not to a plan. The `gsd-tools` roadmap verbs must not be used at all: `roadmap.update-plan-progress` clobbers the dependency table by positional overwrite, destroying the phase name and requirement IDs."
      status: required
      verification: judgment
    - statement: "No file under `firestarter_app/firestarter/` is modified, no file in the `firestarter` firmware submodule is modified, and `firestarter/data/chip_database.json` is not touched. The database is generated and must never be hand-edited."
      status: required
      verification: judgment
    - statement: "No `#` comment line and no trailing `#` comment is written into any file this plan touches. Docstrings carry every rationale. This is `/workspaces/CLAUDE.md`'s hard rule and a plan cannot override it; the four pre-existing comments in `test_chip_test.py` at the sibling read-step legs — two of which cite a GSD decision identifier — are out of scope and must be neither copied nor removed, which is why the comment gate asserts equality with 621 rather than an upper bound."
      status: required
      verification: judgment
    - statement: "No requirement row other than PRUNE-08's two lines is edited. MEAS-01, R4-01, the Coverage block and every RPT and HYG row belong to other phases, and Phase 181 reads this ledger next."
      status: required
      verification: judgment
    - statement: "`180-PRUNE-08-CLOSURE.md` and plans 180-01, 180-02 and 180-03 with their SUMMARYs are not re-opened, rewritten or renumbered."
      status: required
      verification: judgment
    - statement: "No hardened gate is trusted on a green run, and no pre-existing failure outside this plan's scope is fixed or silenced inside a gap-closure diff."
      status: required
      verification: judgment
  artifacts:
    - path: "firestarter_app/tests/test_chip_test.py"
      provides: "one shared alternating read side-effect builder in the module's existing builder cluster, called by both read-step verdict legs"
      contains: "_alternating_read_side_effect"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PRUNE-08 recorded Complete again, in the v1 checkbox and the traceability table, after the gap fixes landed"
      contains: "| PRUNE-08 | Phase 180 | Complete |"
    - path: ".planning/ROADMAP.md"
      provides: "the Phase 180 section extended with a Gap closure grouping and a corrected plan count, with nothing outside the section moved"
      contains: "180-04-PLAN.md"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-side-effect-helper.txt"
      provides: "the helper's call sites, the two emptied bodies, the zero-assertions-removed scalar and the module's before and after counts"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-requirement-reseal.txt"
      provides: "the five measured ledger counts before and after, the changed-line count, and the gap-fix scalars the flip was gated on"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-phase-seal.txt"
      provides: "the seven battery legs with their rc values, the cleanliness legs, the comment delta gate and the two roadmap region comparisons"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/seeds/dev-test-adaptive-sequencing.md"
      via: "the Complete flip is gated on the seed's eleven removed fragments measuring zero, so the ledger cannot lead the fix this time"
      pattern: "PRUNE-08"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-04-PLAN.md"
      via: "the Gap closure grouping names both new plans, matching the form Phase 174's section already uses for 174-06"
      pattern: "Gap closure"
---

<objective>
Re-seal the phase after the gaps close.

Plan `180-04` removes the rejected sampler's operative text from the seed and hardens both structural
pins. This plan finishes the review's list with IN-02 — the last of the four items the operator answered
— then puts the requirement ledger and the roadmap back to the truth and proves the tree is green once,
in one place.

The ordering is deliberate and is the point of the plan. `180-VERIFICATION.md` reverted PRUNE-08 out of
Complete, `REQUIREMENTS.md` line 60 is back to an unticked box and its traceability row reads
`Gaps Found`. Re-ticking that box is a claim that the gap is closed, so Task 2 refuses to run until the
seed's eleven removed fragments measure zero and both hardened pin symbols are present and committed.
Plan `180-03` set this precedent by gating its own flip on the closing document already existing; this
plan gates on the fix rather than on the plan that promises it.

Purpose: the ledger, the roadmap and the gate battery say exactly what the tree can back, with the
requirement moving last. Output: one shared test helper, two changed ledger lines, a Gap closure grouping
in the roadmap, and three evidence transcripts.
</objective>

<assumption_delta_decision>
The assumption-delta detector fired on Phase 180 (`kind: pluralization`, term `second`, from the goal's
"the read step's **second** full sweep"). Orchestrator disposition: **`no-change`**.

- **Primary noun:** unchanged — the full read remains the sole verdict source.
- **Decision:** `no-change`.
- **Rationale:** the signal is a false positive — "second full sweep" names an existing behaviour of the
  read step, not a newly-introduced second case, and this phase's whole verdict (PRUNE-08 closed as
  measured-not-worth-doing) is that the second sweep stays exactly as it is. Nothing generalizes; no noun
  gains or loses a monopoly; no second representation is introduced.

Advisory; never blocks.
</assumption_delta_decision>

<flagged_assumptions>
The spec-less probe fallback is live for this phase. The full record — the one `unresolved` edge row for
PRUNE-08 and the §B prohibition recall with its two dropped candidates and their breadcrumbs — is stated
once, in `180-04-PLAN.md`'s `<flagged_assumptions>` block, and is not restated here. Nine prohibitions
survived Stage 2 across the plan set: seven authored in `180-04`, two authored here (the premature-flip
prohibition and the roadmap-scope prohibition), and five of `180-04`'s re-stated in this plan's
`must_haves.prohibitions` because they bind this plan's own tasks — a re-statement, not a tenth item. All
nine are descriptor-less: no `check_kind`, `check_target`, `check_rule`, `check_violation_fixture` or
`check_clean_fixture` scalar appears on any of them.

**No-silent-drop equality across the plan set:** edges, 1 probe-surfaced == 1 surfaced as a flagged
assumption (0 authored as truths, since no edge resolved). Prohibitions, 9 surfaced == 9 authored into
`must_haves.prohibitions`.
</flagged_assumptions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/REQUIREMENTS.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-CONTEXT.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-VERIFICATION.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-REVIEW.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-03-SUMMARY.md
</context>

<environment>
Meta repo `/workspaces`, branch `gsd/v1.36-dev-test-fidelity-planning`, owns `.planning/`. Submodule
`/workspaces/firestarter_app`, branch `gsd/v1.36-dev-test-fidelity`, is where Task 1 commits — inside the
submodule. Submodule `/workspaces/firestarter` is not touched at all.

Python is always `./.venv311/bin/python` from `/workspaces/firestarter_app`. The devcontainer default is
3.12; app CI runs 3.11 only. Never a bare `python` or `pytest`. Every command that reads a pytest count
line passes `-o addopts=""`, because the project's addopts is `-ra -q` and doubling the quiet flag hides
the count.

Shell `grep` here is **ugrep and honours `.gitignore`**, so it silently under-scans. Every
evidence-grade scan below uses `/usr/bin/grep`.

Edit `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` **by hand**. Never the `gsd-tools`
requirements or roadmap verbs — they reformat the whole file, and `roadmap.update-plan-progress`
clobbers the dependency table by positional overwrite.

`tests/test_flash_path_record_sync` asserts **whole-repo porcelain**, so commit everything before running
the full suite or a clean tree's red result will look like a real failure. The suite was measured at
503 s wall time at this phase's original final HEAD; budget for it.

**Two pinned blobs and one pinned commit, all load-bearing.** They are the pre-plan state of the files
whose untouched regions are asserted, so each check stays reproducible after the edits commit instead of
becoming a one-shot claim. Anchoring any of them to `HEAD~N` would be vacuous the moment the change lands,
and this project has a recorded case of exactly that.

- `.planning/ROADMAP.md` pre-edit blob: `cc9499934b4757899719ed35acc8d44d9ac89b31`
  (materialize with `git cat-file blob cc9499934b4757899719ed35acc8d44d9ac89b31`)
- `.planning/REQUIREMENTS.md` pre-edit blob: `6366d59da8cc946e60f7085092f0ef85c7cc4c75`
- `firestarter_app` pre-plan commit: `93a1672`, the app HEAD before plan `180-04` — every app-side diff
  scope check anchors here, so both this plan's and `180-04`'s app changes are in scope for it and the
  expected changed-file set is exactly two test modules.

Measured this planning session against the real files, so nothing below is assumed:
`tests/test_chip_test.py` reports `162 passed`, and `-k read_step` reports `4 passed, 158 deselected`;
`def _read_side_effect` occurs three times in it, at :2148 (pre-existing, a different behaviour), :2194
and :2223 (the two duplicates IN-02 names); `_writes_fill_at_requested_region` ends with its
`return _side_effect` at :1471 and `def test_runs_boundary_rejects_below_2_before_any_operator_call` opens
at :1474, so the builder cluster's insertion point is between them; the two test-function body
extractions used below are 34 and 30 lines at HEAD; `tokenize` COMMENT counts are 621 and 0;
`REQUIREMENTS.md` counts are 19 unchecked, 27 checked, 18 `Pending`, 27 `Complete`, 1 `Gaps Found`;
`v1 requirements: 46 total` sits at :197; the literal `**Plans**: 3 plans` occurs **eight** times across
`ROADMAP.md`, so a global replacement would corrupt seven other phases; and the ROADMAP regions used
below are 434 lines up to and including the `### Phase 180:` heading and 5386 lines from
`### Phase 181:` to end of file, out of 5845 total.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: One shared alternating read side-effect builder, called by both read-step verdict legs</name>
  <precondition>`/workspaces/firestarter_app` has a clean working tree with plan `180-04`'s commit landed, and `./.venv311/bin/python -m pytest tests/test_chip_test.py -o addopts="" -q` reports `162 passed` before any edit. That measured 162 is what makes the after-count a measured invariant rather than a guess; halt if the tree is dirty, because `tests/test_flash_path_record_sync` asserts whole-repo porcelain and Task 3's suite run would then redden for an unrelated reason.</precondition>
  <files>
    firestarter_app/tests/test_chip_test.py,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-side-effect-helper.txt
  </files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_chip_test.py:1426-1474` — `_writes_bytes_to_output_file` and `_writes_fill_at_requested_region`, the two existing read-side-effect builders. Copy their shape exactly: a module-level function taking the payload description, defining a nested `_side_effect` with the `(_name, _eprom_data, output_file=None, ...)` signature the mock is called with, and returning it. Note `_writes_bytes_to_output_file` always returns `True` and cannot express an alternating sequence, which is why it is a shape precedent and not a reuse target.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:2186-2240` — the two legs being refactored, `test_read_step_last_run_failure_yields_bad` and `test_read_step_first_run_failure_with_passing_last_run_yields_ok`. Read both docstrings in full; they are kept verbatim. Read both closures and confirm for yourself that they differ only in the two-element list's order.
    - `/workspaces/firestarter_app/tests/test_chip_test.py:2141-2166` — `test_read_step_disagreement_is_divergence_metric_not_marginal` and its own `_read_side_effect` at :2148. **This one is out of scope.** It writes different bytes on each call and always returns True — a divergence fixture, not a verdict-order fixture — and it carries four pre-existing `#` comments at :2143-2144 and :2160-2161, two of which cite a GSD decision identifier. Do not fold it into the new helper, do not touch its comments.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-REVIEW.md` — IN-02 in full, including its suggested signature.
    - `/workspaces/CLAUDE.md` — the source-comments hard rule and the submodule commit rule.
  </read_first>
  <action>
Add one module-level builder to `firestarter_app/tests/test_chip_test.py` and rewrite two test bodies to
use it. No assertion changes, no docstring changes, no `#` line anywhere.

**The builder.** Name it `_alternating_read_side_effect`, signature `(*call_returns: bool)`. Insert it
immediately after `_writes_fill_at_requested_region`'s closing `return _side_effect` and before
`def test_runs_boundary_rejects_below_2_before_any_operator_call`, so all three read-side-effect builders
sit in one cluster — that is where the module already keeps them and where IN-02's stated precedent
lives. It defines a nested `_side_effect` with the same parameter shape the two legs use today,
`(_name, _eprom_data, output_file=None, **_kwargs)`; keeps a call counter in a single-key dict, the idiom
both current closures already use; returns `call_returns[n % len(call_returns)]` for call `n` and
increments; and when `output_file` is given writes 64 zero bytes to it with `Path(output_file).write_bytes`.

The fixed 64 zero bytes are load-bearing and belong in the builder's docstring: every call writes the
same bytes, so the two runs agree and `_dispatch_read` records no divergence — which is exactly what
makes these legs isolate the verdict source from the divergence metric. A builder that varied its payload
would turn them into divergence tests, which is a different sibling's job. State that in the docstring,
name IN-02 as the reason the builder exists, and say plainly that `_writes_bytes_to_output_file` could not
be reused because it always returns True.

**The two call sites.** In `test_read_step_last_run_failure_yields_bad`, delete its local `call_returns`,
`call_count` and `_read_side_effect` and set `operator.read_eprom.side_effect =
_alternating_read_side_effect(True, False)`. In
`test_read_step_first_run_failure_with_passing_last_run_yields_ok`, do the same with
`_alternating_read_side_effect(False, True)`. The argument order is the call order and it is the only
difference between the two legs; keep each leg's docstring, its `_mock_operator()` call, its
`_plan_with_steps(...)` call, its `run_plan(plan, operator, _REAL_DB, runs=2)` call and both of its
assertions byte-identical. The verdict assertions are the whole point of the legs and this task must not
be able to weaken them, which is why the verify block counts removed assertion lines rather than trusting
a byte-identical claim about the file.

Then write `evidence/180-05-side-effect-helper.txt` in the house transcript form — numbered `== N label`
sections, flat `key=value` scalars, explicit `rc=` after each command. Recompute every scalar from the
file. Record `builder_call_sites`, `nested_read_side_effect_in_leg_one=0`,
`nested_read_side_effect_in_leg_two=0`, `pre_existing_divergence_closure_intact=1`,
`assert_lines_removed=0`, the module's before and after pytest counts, the `-k read_step` count, and the
two `tokenize` comment counts.

Commit **inside** `/workspaces/firestarter_app` on `gsd/v1.36-dev-test-fidelity`; commit the transcript
in `/workspaces`. Two repositories, two commits.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && T=tests/test_chip_test.py && D=$(mktemp -d) && test -s "$T" && sed -n '/^def test_read_step_last_run_failure_yields_bad(/,/^def test_read_step_first_run_failure_with_passing_last_run_yields_ok(/p' "$T" > "$D/leg1" && sed -n '/^def test_read_step_first_run_failure_with_passing_last_run_yields_ok(/,/^def test_write_step_attaches_fingerprint_with_region_start_addr_base(/p' "$T" > "$D/leg2" && test "$(wc -l < "$D/leg1")" -ge 10 && test "$(wc -l < "$D/leg2")" -ge 10 && test "$(/usr/bin/grep -cF 'def _read_side_effect' "$D/leg1")" -eq 0 && test "$(/usr/bin/grep -cF 'def _read_side_effect' "$D/leg2")" -eq 0 && test "$(/usr/bin/grep -cF '_alternating_read_side_effect(True, False)' "$D/leg1")" -ge 1 && test "$(/usr/bin/grep -cF '_alternating_read_side_effect(False, True)' "$D/leg2")" -ge 1 && /usr/bin/grep -qF 'def _alternating_read_side_effect' "$T" && rm -rf "$D"</automated>
    <fails_when>non-zero exit — the module is missing or empty; either body extraction came back under ten lines, which would let the two zero-counts pass vacuously on an empty range (the signature of a sed range that stopped matching after a rename); a nested `def _read_side_effect` survives in either refactored leg, so the duplication IN-02 names is still there; a leg does not call the builder with its own ordered pair, meaning the two legs were collapsed or the order was swapped; or the builder was never defined.</fails_when>
    <automated>cd /workspaces/firestarter_app && T=tests/test_chip_test.py; test "$(/usr/bin/grep -cF 'def _read_side_effect' "$T")" -ge 1 && /usr/bin/grep -qF 'def test_read_step_disagreement_is_divergence_metric_not_marginal' "$T" && test "$(/usr/bin/grep -cF '_alternating_read_side_effect' "$T")" -ge 3</automated>
    <fails_when>non-zero exit — the pre-existing divergence closure at :2148 was folded into the new helper or deleted along with the two duplicates (its `def _read_side_effect` must survive, since it writes different bytes per run and always returns True); its owning test was renamed or removed; or the builder appears fewer than three times, meaning it was defined without both call sites being wired.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_chip_test.py -o addopts="" -q > /tmp/180-05-mod.txt 2>&1; rc=$?; tail -2 /tmp/180-05-mod.txt; test "$rc" -eq 0 && /usr/bin/grep -qE '^162 passed' /tmp/180-05-mod.txt && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' /tmp/180-05-mod.txt && ./.venv311/bin/python -m pytest tests/test_chip_test.py -o addopts="" -q -k read_step > /tmp/180-05-rs.txt 2>&1 && /usr/bin/grep -qE '^4 passed' /tmp/180-05-rs.txt</automated>
    <fails_when>non-zero pytest exit; the module summary line does not start `162 passed` — a lower count means a test was lost to the refactor and a higher one means a test was added, which this task must not do; any `failed` or `error` count; or the `-k read_step` selection does not report `4 passed`, meaning one of the four read-step legs stopped being collected under that keyword.</fails_when>
    <automated>cd /workspaces/firestarter_app && D=$(mktemp) && git diff 93a1672 -- tests/test_chip_test.py > "$D" && test "$(/usr/bin/grep -cE '^-[[:space:]]*assert ' "$D")" -eq 0 && test "$(/usr/bin/grep -E '^\+' "$D" | /usr/bin/grep -vE '^\+\+\+' | /usr/bin/grep -cE '^\+[[:space:]]*#')" -eq 0 && rm -f "$D" && git diff 93a1672 --quiet -- firestarter/ && git diff 93a1672 --quiet -- tests/fixtures/ && ./.venv311/bin/python -m ruff check tests/ && ./.venv311/bin/python -m ruff format --check tests/</automated>
    <fails_when>non-zero exit — the diff since pinned anchor `93a1672` removes a line whose first non-space token is `assert `, meaning an assertion was weakened or dropped by a refactor that was supposed to touch only the fixture; it adds a line whose first non-space character is a hash; it touches anything under `firestarter/` or `tests/fixtures/`; or ruff reports a rule code or names an unformatted file.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-side-effect-helper.txt; test -s "$E" && /usr/bin/grep -qxF 'nested_read_side_effect_in_leg_one=0' "$E" && /usr/bin/grep -qxF 'nested_read_side_effect_in_leg_two=0' "$E" && /usr/bin/grep -qxF 'pre_existing_divergence_closure_intact=1' "$E" && /usr/bin/grep -qxF 'assert_lines_removed=0' "$E" && /usr/bin/grep -qE '^builder_call_sites=[0-9]+$' "$E" && /usr/bin/grep -qE '^162 passed' "$E"</automated>
    <fails_when>non-zero exit — the transcript is missing or empty, or does not record all four verdict scalars, a measured call-site count and the module's pytest tail, so the refactor's scope was asserted rather than measured.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_chip_test.py` defines `_alternating_read_side_effect` once, between `_writes_fill_at_requested_region`'s closing `return _side_effect` and `def test_runs_boundary_rejects_below_2_before_any_operator_call`, and the literal `_alternating_read_side_effect` occurs at least three times in the module.
    - The body of `test_read_step_last_run_failure_yields_bad` contains zero `def _read_side_effect` and at least one `_alternating_read_side_effect(True, False)`; the body of `test_read_step_first_run_failure_with_passing_last_run_yields_ok` contains zero `def _read_side_effect` and at least one `_alternating_read_side_effect(False, True)`.
    - `def _read_side_effect` still occurs at least once in the module, inside `test_read_step_disagreement_is_divergence_metric_not_marginal`, which is still present and unrenamed.
    - `./.venv311/bin/python -m pytest tests/test_chip_test.py -o addopts="" -q` reports `162 passed` with no `failed` or `error` count, and `-k read_step` reports `4 passed`.
    - `git diff 93a1672 -- tests/test_chip_test.py` removes zero lines whose first non-space token is `assert ` and adds zero lines whose first non-space character is a hash.
    - `git diff 93a1672 --quiet -- firestarter/` and `-- tests/fixtures/` both exit 0; `ruff check tests/` and `ruff format --check tests/` both pass.
    - `evidence/180-05-side-effect-helper.txt` contains `nested_read_side_effect_in_leg_one=0`, `nested_read_side_effect_in_leg_two=0`, `pre_existing_divergence_closure_intact=1`, `assert_lines_removed=0`, a `builder_call_sites=` scalar and a `162 passed` tail.
  </acceptance_criteria>
  <done>One builder expresses the alternating pass/fail sequence both verdict legs need, each leg calls it with its own ordered pair, the divergence fixture beside them is untouched, and no assertion line moved.</done>
</task>

<task type="auto">
  <name>Task 2: PRUNE-08 reads Complete again — exactly two lines, and only after the gap fixes are committed</name>
  <precondition>Every gap fix is on disk **and committed** before this task edits anything. Concretely: `.planning/seeds/dev-test-adaptive-sequencing.md` contains zero occurrences of `Escalate to the full second read`, `**Cost, stated:**` and `256 B block at each`; `firestarter_app/tests/test_readback_inventory.py` contains both `connect_route_calls` and `_last_ok_assignment_shape`; `firestarter_app/tests/test_chip_test.py` contains `_alternating_read_side_effect`; and both `git status --porcelain` in `/workspaces` and in `/workspaces/firestarter_app` are empty. Halt if any of those is false. The requirement was reverted out of Complete because a claim outran its evidence; re-ticking it on the strength of a plan rather than a landed fix would repeat that failure in the same ledger one iteration later.</precondition>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-requirement-reseal.txt
  </files>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` — the whole file (203 lines). The two edit sites are line 60, PRUNE-08's v1 checkbox under "No-Information Operations", currently `- [ ] **PRUNE-08**`, and line 175, its traceability row, currently `| PRUNE-08 | Phase 180 | Gaps Found |`. Line numbers may have shifted; locate by content and verify against these.
    - `/workspaces/.planning/REQUIREMENTS.md:103` MEAS-01, `:122` R4-01, `:195-199` the Coverage block. **Do not touch any of them**; the Coverage figures do not change when a box is ticked.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-requirement-marking.txt` — the transcript this one re-runs, and the counts it recorded the first time the flip landed.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-03-PLAN.md:120-180` — Task 1 as it was written, including its own blocking precondition. This task is that task re-run against a different precondition: not "the closing document exists" but "the gap fixes landed".
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-VERIFICATION.md` — the `gaps:` block, so the SUMMARY can state which artifact discharged which gap.
  </read_first>
  <action>
Make exactly two hand edits to `.planning/REQUIREMENTS.md`. Never the `gsd-tools` requirements verbs —
they reformat the whole file.

**Edit 1, line 60.** Tick PRUNE-08's v1 checkbox. Change nothing else on the line: the requirement text —
which already states that closing as *measured, not worth doing* with the measurement recorded is a
success and not a miss — stays byte-identical, because that sentence is exactly what this phase
discharged.

**Edit 2, line 175.** Change the traceability row's status cell from the reverted `Gaps Found` to
`Complete`, matching the spelling and spacing every other completed row in that table already uses.
Leave the requirement and phase cells alone. After this edit no row in the table reads `Gaps Found`.

Touch no other requirement row. In particular do not touch MEAS-01, R4-01, the Coverage block, or any RPT
or HYG row — those belong to Phase 181, which reads this ledger next.

Then write `evidence/180-05-requirement-reseal.txt` in the house transcript form. Record the five ledger
counts before and after, measured from the file rather than transcribed from this plan: unchecked
19 to 18, checked 27 to 28, `Pending` 18 to 18, `Complete` 27 to 28, `Gaps Found` 1 to 0. Record
`changed_lines=2`. Record the precondition scalars the flip was gated on as measured values, not as
promises: the three seed fragment counts at zero, the two hardened pin symbols present, the builder
present, and both trees porcelain-clean. Note in the transcript that `Pending` does not move, because the
row this phase flips was reading `Gaps Found` rather than `Pending` — a reader comparing against
`180-03`'s transcript, where `Pending` moved 19 to 18, would otherwise read the unchanged count as a
missed edit.

Commit in `/workspaces`, with a message naming PRUNE-08, the closed gap and the fixes it rests on.
  </action>
  <verify>
    <automated>cd /workspaces && R=.planning/REQUIREMENTS.md; test -s "$R" && /usr/bin/grep -qFe '- [x] **PRUNE-08**' "$R" && ! /usr/bin/grep -qFe '- [ ] **PRUNE-08**' "$R" && /usr/bin/grep -qxF '| PRUNE-08 | Phase 180 | Complete |' "$R" && ! /usr/bin/grep -qF '| PRUNE-08 | Phase 180 | Gaps Found |' "$R" && ! /usr/bin/grep -qF '| PRUNE-08 | Phase 180 | Pending |' "$R"</automated>
    <fails_when>non-zero exit — the ledger is missing or empty (the leading `test -s` guard is what stops the three negative greps passing vacuously on an absent file), the checkbox is still unticked, the traceability row still reads the reverted `Gaps Found` or reads `Pending`, or the row was ticked in only one of the two places PRUNE-08 is recorded.</fails_when>
    <automated>cd /workspaces && awk '/^- \[ \] \*\*/{u++} /^- \[x\] \*\*/{c++} /^\| .* \| Pending \|$/{p++} /^\| .* \| Complete \|$/{q++} /^\| .* \| Gaps Found \|$/{g++} END{printf "unchecked=%d checked=%d pending=%d complete=%d gapsfound=%d\n",u,c,p,q,g+0; exit !(u==18 && c==28 && p==18 && q==28 && g+0==0)}' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, and the printed line names all five measured counts. `unchecked=19`, `complete=27` or `gapsfound=1` means the flip did not land; `unchecked=17` or `checked=29` means a second requirement was ticked, which this phase must not do; `pending=17` means a row belonging to another phase was flipped as collateral.</fails_when>
    <automated>cd /workspaces && D=$(mktemp -d) && git cat-file blob 6366d59da8cc946e60f7085092f0ef85c7cc4c75 > "$D/pre.md" && test "$(diff "$D/pre.md" .planning/REQUIREMENTS.md | /usr/bin/grep -cE '^[<>]')" -le 4 && R=.planning/REQUIREMENTS.md && /usr/bin/grep -qFe '- [x] **MEAS-01**' "$R" && /usr/bin/grep -qF '**R4-01**' "$R" && /usr/bin/grep -qF 'v1 requirements: 46 total' "$R" && /usr/bin/grep -qxF '| RPT-A1 | Phase 181 | Pending |' "$R" && rm -rf "$D"</automated>
    <fails_when>non-zero exit — the diff against pinned blob `6366d59d` carries more than four changed-line markers, meaning more than the two intended lines moved (two lines changed produce exactly two `<` and two `>` markers); or MEAS-01, R4-01, the Coverage line or Phase 181's first traceability row was altered, meaning the edit reached beyond PRUNE-08.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-requirement-reseal.txt; test -s "$E" && /usr/bin/grep -qxF 'changed_lines=2' "$E" && /usr/bin/grep -qxF 'unchecked_after=18' "$E" && /usr/bin/grep -qxF 'checked_after=28' "$E" && /usr/bin/grep -qxF 'complete_after=28' "$E" && /usr/bin/grep -qxF 'gapsfound_after=0' "$E" && /usr/bin/grep -qxF 'pending_after=18' "$E" && /usr/bin/grep -qE '^gate_seed_fragments=0$' "$E" && /usr/bin/grep -qE '^gate_hardened_pins=(2|present)$' "$E"</automated>
    <fails_when>non-zero exit — the reseal transcript is missing, does not record all five after-counts and the changed-line count, or does not record the precondition gate as measured scalars, which is what distinguishes a gated flip from a flip that merely followed a plan that promised the fix.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **PRUNE-08**` and does not contain `- [ ] **PRUNE-08**`.
    - It contains the exact line `| PRUNE-08 | Phase 180 | Complete |` and neither the `Gaps Found` nor the `Pending` variant of that row.
    - The five measured ledger counts read 18 unchecked, 28 checked, 18 `Pending`, 28 `Complete`, 0 `Gaps Found`.
    - `diff` against pinned blob `6366d59da8cc946e60f7085092f0ef85c7cc4c75` shows at most four changed-line markers.
    - `- [x] **MEAS-01**`, `**R4-01**`, `v1 requirements: 46 total` and `| RPT-A1 | Phase 181 | Pending |` are all still present.
    - `evidence/180-05-requirement-reseal.txt` contains `changed_lines=2`, `unchecked_after=18`, `checked_after=28`, `complete_after=28`, `gapsfound_after=0`, `pending_after=18`, `gate_seed_fragments=0`, and a `gate_hardened_pins=` scalar recording the two pin symbols as measured present.
    - This task's own commit changes exactly two lines of `.planning/REQUIREMENTS.md` and no other file, checked with an explicit pinned range `<sha>^..<sha>` recorded in the SUMMARY — never a relative `HEAD~N`, which in a repository with parallel sessions can name another session's commit.
  </acceptance_criteria>
  <done>PRUNE-08 reads Complete in both places the ledger records it, no other row moved, and the flip is recorded as gated on measured fixes rather than on a plan that promised them.</done>
</task>

<task type="auto">
  <name>Task 3: The phase seal — the Gap closure grouping in the roadmap, then seven green legs with nothing outside the section moved</name>
  <precondition>Both repositories have clean working trees with every prior task's work committed. `tests/test_flash_path_record_sync` asserts whole-repo porcelain, so an uncommitted file turns the full suite red for a reason that has nothing to do with this phase, and this project has a recorded case of exactly that misreading.</precondition>
  <files>
    .planning/ROADMAP.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-phase-seal.txt
  </files>
  <read_first>
    - `/workspaces/.planning/ROADMAP.md:434-459` — the whole Phase 180 section: the goal line, the `**Depends on**` line, `**Requirements**: PRUNE-08`, the four success criteria, the plan-count line that currently reads three plans, and the three ticked plan boxes under their Wave headings. Read the section boundaries before editing so the edit stays inside them.
    - `/workspaces/.planning/ROADMAP.md` Phase 174's section, its `**Gap closure**` paragraph and its `- [x] 174-06-PLAN.md` line. **This is the form to copy**, including the sentence explaining that the gap plan's own `wave: 1` is that run's first wave and not a fifth wave of the original execution.
    - `/workspaces/.planning/ROADMAP.md:236-243` — the `### Phases` milestone checkbox list, where Phase 180's own box is still `- [ ]`. **Leave it unticked.** Flipping a phase-level box is phase completion, which the orchestrator runs; a plan that does it pre-empts the gate that decides it.
    - `/workspaces/.planning/MILESTONES.md:1-41` — the v1.36 section, aimed by name at a Phase 180 or 181 plan. It records that the re-key protocol, its ledger table and `tools/rekey/check_rekey_ledger.py` were all retired, that the ritual is **seven** legs and not eight, and that the app suite floor moved to 2239.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-phase-seal.txt` — the ten-section transcript this one re-runs, including its measured 2245 count, its `battery_legs=7` scalar and its `roadmap_lines_changed_outside_phase_180=0` scalar.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-04-SUMMARY.md` and `180-05`'s Task 1 record — the measured module counts, so the suite floor below is derived from what actually landed rather than from this plan's arithmetic.
  </read_first>
  <action>
Two things: extend the roadmap's Phase 180 section, then run the battery and record it.

**The roadmap edit, by hand, inside the Phase 180 section only.** Never the `gsd-tools` roadmap verbs,
and never `roadmap.update-plan-progress` — it clobbers the dependency table by positional overwrite and
destroys the phase name and requirement IDs. Snapshot the file before editing and diff after, so an
unintended edit outside the section is caught rather than discovered later.

Change the section's plan-count line, which currently reads three plans, to `**Plans**: 5 plans`
**inside the Phase 180 section only**. The three-plan form of that line occurs eight times across the
file — measured this planning session — so a global substitution would corrupt seven other phases.
Locate the one inside the section by its position between the four success criteria and the `**Wave 1**`
heading.

Then append a `**Gap closure**` grouping after the `**Wave 3**` block, copying Phase 174's form: a short
paragraph stating that these plans were planned after verification returned `gaps_found` at 23 of 24
must-haves, that each carries `gap_closure: true`, and that their own wave numbers are that run's waves
rather than further waves of the original execution — then a ticked box per plan,
`- [x] 180-04-PLAN.md` and `- [x] 180-05-PLAN.md`, each with a one-line objective. Do not alter the goal
line, the `**Depends on**` line, the `**Requirements**` line, the four success criteria, the three
existing plan boxes or their Wave headings, and do not touch any other phase's section or the `### Phases`
milestone list.

**The battery — seven legs, not eight.** Run from `/workspaces/firestarter_app` using
`./.venv311/bin/python`, and record each leg's output tail and its rc. Leg 1 `ruff check firestarter/
tests/`. Leg 2 `ruff format --check firestarter/ tests/`. Leg 3 `tools/check_mypy_watermark.py`, matching
the **exact** watermark line rather than a substring, because mypy prints an OK-looking line when it is
missing. Leg 4 `tools/snapshot_report_shapes.py --check`. Leg 5 `tools/check_devtest_orchestrator.py`.
Leg 6 `tools/check_diagnostic_report_claims.py`. Leg 7 the full suite,
`python -m pytest tests/ -o addopts="" -q`.

Do **not** invoke `tools/rekey/check_rekey_ledger.py` — it was deleted with the whole declared-re-key
protocol on 2026-09-08, and **no ledger row is required for this phase**. Do not assert a floor of 2239,
2241, 2242 or 2245: 2239 was the pre-phase floor, Phase 180's original final HEAD measured 2245, and plan
`180-04` adds two additive legs, so the expected count is at or above **2247**. Re-measure it and record
what you measured rather than transcribing the expectation.

Also run the cleanliness legs — the firmware submodule porcelain-clean, the app tree porcelain-clean,
`firestarter/data/chip_database.json` unmodified, and the changed-file set since pinned app commit
`93a1672` equal to exactly the two test modules — and the `tokenize` comment delta gate over both touched
test modules against their pinned baselines of 621 and 0. Use the `tokenize` gate, not the inherited
added-line regex: that regex matches only lines whose first non-space character is a hash, so a trailing
comment ships green past a non-overridable project rule. Assert equality with the baselines rather than
an upper bound, so a well-meant tidy-up of the four pre-existing comments fails too.

Then write `evidence/180-05-phase-seal.txt` in the house form: one numbered section per leg with the
verbatim tail of its output and an explicit rc line, then the cleanliness and comment scalars, then
`full_suite_passed=` with the measured count, `full_suite_failed=0`, `snapshots_passed=`,
`phase_added_comments=0`, `firmware_porcelain=clean`, `chip_database_unchanged=1`,
`roadmap_region_before_phase180=identical`, `roadmap_region_after_phase180=identical`,
`ticked_plan_boxes=5`, `phase_level_checkbox_ticked=0` and `battery_legs=7`.

Commit in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/ && ./.venv311/bin/python -m ruff format --check firestarter/ tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py > /tmp/180-05-mypy.txt 2>&1 && /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' /tmp/180-05-mypy.txt && ./.venv311/bin/python tools/snapshot_report_shapes.py --check && ./.venv311/bin/python tools/check_devtest_orchestrator.py && ./.venv311/bin/python tools/check_diagnostic_report_claims.py</automated>
    <fails_when>non-zero exit at any of legs 1 to 6 — ruff prints a rule code instead of the all-clear, the format check names a file, the watermark output is anything other than the exact `mypy errors: 35 (watermark: 35)` line (mypy prints an OK-looking line when it is missing, so a substring match would pass on absence), a snapshot reports drift, or either the orchestrator or the claims checker prints a non-PASS verdict.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q > /tmp/180-05-suite.txt 2>&1; rc=$?; tail -3 /tmp/180-05-suite.txt; test "$rc" -eq 0 && N=$(/usr/bin/grep -oE '^[0-9]+ passed' /tmp/180-05-suite.txt | /usr/bin/grep -oE '^[0-9]+') && test -n "$N" && test "$N" -ge 2247 && ! /usr/bin/grep -qE '[0-9]+ failed' /tmp/180-05-suite.txt && ! /usr/bin/grep -q 'no tests ran' /tmp/180-05-suite.txt</automated>
    <fails_when>non-zero pytest exit; the captured count line is absent, which means the `-o addopts=""` override was dropped and the count was suppressed; the passed count is below 2247, meaning plan 180-04's two additive legs were not collected; the summary reports any `failed` count; or the output says `no tests ran`.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import io,tokenize;B={'tests/test_chip_test.py':621,'tests/test_readback_inventory.py':0};bad=[p+':'+str(sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)) for p,e in B.items() if sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)!=e];print('phase_added_comments='+('0' if not bad else ','.join(bad)));raise SystemExit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file with its measured count is where the count moved off 621 or 0, in either direction, so a well-meant removal of the four pre-existing comments fails here too.</fails_when>
    <automated>cd /workspaces/firestarter_app && S=$(git diff 93a1672 --name-only) && test "$S" = "tests/test_chip_test.py
tests/test_readback_inventory.py" && git diff 93a1672 --quiet -- firestarter/ && test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git status --porcelain)" && test -z "$(git status --porcelain firestarter/data/chip_database.json)"</automated>
    <fails_when>non-zero exit — the changed-file set since pinned app commit `93a1672` is not exactly those two test modules in that order, meaning the phase reached into product source or a third file; anything under `firestarter/` moved; or the firmware submodule, the app tree, or the generated chip database is dirty at seal time, so the phase cannot claim a product-source-free close.</fails_when>
    <automated>cd /workspaces && D=$(mktemp -d) && git cat-file blob cc9499934b4757899719ed35acc8d44d9ac89b31 > "$D/pre.md" && sed -n '1,/^### Phase 180:/p' "$D/pre.md" > "$D/x1" && sed -n '1,/^### Phase 180:/p' .planning/ROADMAP.md > "$D/x2" && sed -n '/^### Phase 181:/,$p' "$D/pre.md" > "$D/y1" && sed -n '/^### Phase 181:/,$p' .planning/ROADMAP.md > "$D/y2" && test "$(wc -l < "$D/x1")" -eq 434 && test "$(wc -l < "$D/y1")" -eq 5386 && cmp "$D/x1" "$D/x2" && cmp "$D/y1" "$D/y2" && rm -rf "$D"</automated>
    <fails_when>`cmp` prints a `differ: byte N, line M` line and exits non-zero — a line outside the Phase 180 section moved, which includes the `### Phases` milestone checkbox list and every other phase's section, and is the signature of a roadmap verb reformatting the file. Also non-zero when either line-count guard fails (434 before the heading, 5386 from Phase 181 on): a zero-line extraction would make its `cmp` pass vacuously.</fails_when>
    <automated>cd /workspaces && sed -n '/^### Phase 180:/,/^### Phase 181:/p' .planning/ROADMAP.md > /tmp/180-05-sect.txt && test "$(wc -l < /tmp/180-05-sect.txt)" -ge 20 && /usr/bin/grep -qxF '**Plans**: 5 plans' /tmp/180-05-sect.txt && test "$(/usr/bin/grep -cxF '**Plans**: 3 plans' /tmp/180-05-sect.txt)" -eq 0 && test "$(/usr/bin/grep -cxF '**Plans**: 3 plans' .planning/ROADMAP.md)" -ge 7 && test "$(/usr/bin/grep -cE '^- \[x\] 180-0[12345]-PLAN\.md' /tmp/180-05-sect.txt)" -ge 5 && /usr/bin/grep -qF '**Gap closure**' /tmp/180-05-sect.txt && /usr/bin/grep -qxF '**Requirements**: PRUNE-08' /tmp/180-05-sect.txt && test "$(/usr/bin/grep -cE '^  [0-9]\. If |^  [0-9]\. Whichever ' /tmp/180-05-sect.txt)" -ge 4 && test "$(/usr/bin/grep -cE '^- \[ \] \*\*Phase 180' .planning/ROADMAP.md)" -ge 1</automated>
    <fails_when>non-zero exit — the section extraction came back under twenty lines, which would let the two zero-counts pass vacuously; the plan count line was not updated inside the section, or still reads three plans there; the global count of `**Plans**: 3 plans` fell below seven, the signature of a global substitution that hit the other seven phases; fewer than five ticked `180-0N-PLAN.md` boxes; the `**Gap closure**` grouping is absent; the requirements line was destroyed by a whole-file rewrite; fewer than four success criteria survive; or Phase 180's own box in the `### Phases` milestone list is no longer unticked, meaning a plan pre-empted phase completion.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-05-phase-seal.txt; test -s "$E" && /usr/bin/grep -qxF 'full_suite_failed=0' "$E" && /usr/bin/grep -qxF 'phase_added_comments=0' "$E" && /usr/bin/grep -qxF 'firmware_porcelain=clean' "$E" && /usr/bin/grep -qxF 'chip_database_unchanged=1' "$E" && /usr/bin/grep -qxF 'battery_legs=7' "$E" && /usr/bin/grep -qxF 'roadmap_region_before_phase180=identical' "$E" && /usr/bin/grep -qxF 'roadmap_region_after_phase180=identical' "$E" && /usr/bin/grep -qxF 'phase_level_checkbox_ticked=0' "$E" && /usr/bin/grep -qE '^full_suite_passed=22[4-9][0-9]$' "$E" && test "$(/usr/bin/grep -cE '^rc=0$' "$E")" -ge 7 && ! /usr/bin/grep -qiE 'rekey|check_rekey_ledger|2242|2241' "$E"</automated>
    <fails_when>non-zero exit — the seal transcript is missing; a verdict scalar is absent; `full_suite_passed=` does not carry a measured four-digit count in the 2240s or above; fewer than seven `rc=0` lines were recorded, meaning fewer than seven legs actually ran; or the transcript mentions the retired re-key checker, its ledger or a superseded suite floor, which are the three ways this seal could be copied forward into vacuity.</fails_when>
  </verify>
  <acceptance_criteria>
    - All six static battery legs exit 0, and `tools/check_mypy_watermark.py` prints exactly `mypy errors: 35 (watermark: 35)`.
    - `./.venv311/bin/python -m pytest tests/ -o addopts="" -q` exits 0 with a measured passed count at or above 2247, no `failed` count and no `no tests ran` line.
    - The `tokenize` gate prints `phase_added_comments=0`, holding `tests/test_chip_test.py` at exactly 621 and `tests/test_readback_inventory.py` at exactly 0.
    - `git diff 93a1672 --name-only` in `firestarter_app` prints exactly `tests/test_chip_test.py` then `tests/test_readback_inventory.py`; `git diff 93a1672 --quiet -- firestarter/` exits 0; the firmware submodule, the app tree and `firestarter/data/chip_database.json` are all porcelain-clean.
    - Against pinned blob `cc9499934b4757899719ed35acc8d44d9ac89b31`, `cmp` reports identical for lines 1 through the `### Phase 180:` heading (434 lines) and for `### Phase 181:` to end of file (5386 lines).
    - The extracted Phase 180 section contains `**Plans**: 5 plans`, zero occurrences of `**Plans**: 3 plans`, at least five ticked `180-0N-PLAN.md` boxes, a `**Gap closure**` grouping, `**Requirements**: PRUNE-08` and at least four success-criteria lines; the global count of `**Plans**: 3 plans` is still at least seven.
    - `ROADMAP.md` still carries at least one `- [ ] **Phase 180` line in the `### Phases` milestone list — the phase-level box stays unticked.
    - `evidence/180-05-phase-seal.txt` records `full_suite_passed=` with a measured count, `full_suite_failed=0`, `phase_added_comments=0`, `firmware_porcelain=clean`, `chip_database_unchanged=1`, `battery_legs=7`, `roadmap_region_before_phase180=identical`, `roadmap_region_after_phase180=identical`, `phase_level_checkbox_ticked=0`, at least seven `rc=0` lines, and no mention of the retired re-key checker, its ledger or a superseded floor.
  </acceptance_criteria>
  <done>Every gate this phase relies on is observed green in one place with a measured suite count, the roadmap records five plans and a Gap closure grouping with nothing outside the section moved, the phase-level box is left for phase completion to decide, and the transcript proves the whole phase cost zero product-source lines, zero firmware lines and zero source comments.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths **created by this plan** (absent at meta HEAD `dd2a88eb` and app HEAD `93a1672`, so a
source-grounding drift check must not flag them as unresolved references):

**New test helper** in `firestarter_app/tests/test_chip_test.py` — `_alternating_read_side_effect`, with
its nested `_side_effect`.

**New evidence files**, all under
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/` —
`180-05-side-effect-helper.txt`, `180-05-requirement-reseal.txt`, `180-05-phase-seal.txt`.

**New roadmap prose** — a `**Gap closure**` grouping inside `ROADMAP.md`'s Phase 180 section, with the
two ticked lines `- [x] 180-04-PLAN.md` and `- [x] 180-05-PLAN.md`. No new heading is added.

**Created by the sibling gap-closure plan `180-04`, not by this one** —
`test_a_planted_stray_setup_operation_call_in_read_eprom_reddens_the_hardened_pin`,
`test_a_planted_last_ok_reassignment_before_the_return_reddens_the_hardened_pin`,
`_last_ok_assignment_shape`, `_STRAY_CONNECT_ANCHOR`, `_LAST_OK_REASSIGN_ANCHOR`, the
`connect_route_calls` dict key, and the three `180-04-*.txt` transcripts. This plan's preconditions and
gates reference them by name and must not be read as creating them.

**Pre-existing symbols this plan reads or calls, created earlier and NOT new** —
`_writes_bytes_to_output_file`, `_writes_fill_at_requested_region`, `_mock_operator`, `_plan_with_steps`,
`_result`, `_REAL_DB`, `run_plan`, `test_read_step_last_run_failure_yields_bad`,
`test_read_step_first_run_failure_with_passing_last_run_yields_ok`,
`test_read_step_disagreement_is_divergence_metric_not_marginal`,
`test_write_step_attaches_fingerprint_with_region_start_addr_base`,
`test_runs_boundary_rejects_below_2_before_any_operator_call`.

**Deliberately NOT produced** — no re-key ledger row (the protocol and its checker were retired
2026-09-08); no new test function anywhere, which is why the module count must stay at exactly 162 and
the suite floor is 2247 rather than higher; no phase-level roadmap checkbox flip (that is phase
completion's call, run by the orchestrator); no `COVERAGE.md`; no bench measurement document, since this
phase is not hardware-gated per D-02; no edit to `180-PRUNE-08-CLOSURE.md` or to plans `180-01`,
`180-02`, `180-03`.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. This plan changes one test module, two lines of a requirements ledger and
one section of a roadmap, then runs read-only checks. There is no network path, no serial path, no
untrusted input, no credential, no user data, no dependency install and no schema. The classic web
categories — injection, XSS, SSRF, broken access control — have no surface here and are not modelled;
claiming otherwise would be the kind of unbacked statement this milestone exists to catch.

The honest threat surface is the integrity of the records: a ticked box that outruns its evidence, and a
whole-file rewrite that destroys rows nobody was looking at.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement ledger → every downstream reader | A ticked box is a claim that evidence exists. This box was already ticked once and then reverted, so the failure mode is not hypothetical here — it is recorded, in this phase, in this file. |
| roadmap → the milestone record | `ROADMAP.md` is a 5,845-line hand-authored file carrying the whole backlog and every archived milestone. A verb that rewrites it wholesale destroys rows far outside the diff a reviewer reads. |
| green-tree battery → the phase's own conclusion | The battery is the only thing between "the fixes were written" and "the fixes pass". A leg invoking a deleted script, or asserting a superseded floor, converts the seal into theatre. |
| test fixture → the assertions above it | A fixture refactor is the cheapest place to weaken a verdict assertion without anyone noticing, because the diff reads as tidy-up. |
| meta repo ↔ both submodules | No data crosses. The battery reads the app repo and writes nothing to it; the firmware submodule is read only for its porcelain status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-19 | Spoofing | PRUNE-08's checkbox and traceability row | high | mitigate | Task 2 carries a blocking precondition that measures the fix rather than trusting the plan: three seed fragment counts at zero, both hardened pin symbols present, the builder present, both trees porcelain-clean. The flip is the second-to-last task in the last plan, and the transcript records the gate as measured scalars. Rated `high` because the same box already outran its evidence once in this phase; mitigated, so nothing blocks. |
| T-180-20 | Tampering | `.planning/ROADMAP.md` outside the Phase 180 section | high | mitigate | Two `cmp` legs against pinned blob `cc9499934b` covering all 5,820 lines outside the section, each with a line-count guard so a collapsed sed range cannot pass vacuously; hand edits only; and a global-count floor on `**Plans**: 3 plans` that fires if a global substitution reached the other seven phases. |
| T-180-21 | Tampering | the two read-step verdict assertions | medium | mitigate | The diff since pinned app commit `93a1672` must remove zero lines whose first non-space token is `assert `, and both legs must still report through `-k read_step` at 4 passed. An assertions-unchanged check rather than a byte-identical-file claim, because a byte-identical criterion breaks on any legitimate reformatting. |
| T-180-22 | Tampering | `.planning/REQUIREMENTS.md` rows outside PRUNE-08 | medium | mitigate | Five absolute count assertions, a changed-marker ceiling against pinned blob `6366d59d`, and positive greps for MEAS-01, R4-01, the Coverage line and Phase 181's first row. |
| T-180-23 | Repudiation | the phase seal's own verdict | medium | mitigate | The transcript must carry a *measured* pass count and is negative-grepped for the retired re-key checker, its ledger and the superseded 2241/2242 floors. |
| T-180-24 | Tampering | the four pre-existing `#` comments beside the sibling read-step leg | low | mitigate | The `tokenize` gate asserts equality with 621, which fires on removal as well as addition, so neither copying nor tidying them can pass. |
| T-180-25 | Elevation of Privilege | a plan pre-empting phase completion | low | mitigate | Phase 180's own box in the `### Phases` milestone list is required to stay unticked, checked positively, and the region `cmp` covering that list would catch the flip anyway. |
| T-180-SC | Tampering | npm / pip / cargo installs | low | accept | No package is installed. Every tool invoked is already in the pinned 3.11 venv or is Python standard library. No Package Legitimacy Audit is required. |

Two threats are rated `high` (T-180-19, T-180-20) and both are dispositioned `mitigate` with measured
gates, so the `security_block_on: high` threshold is satisfied and nothing blocks.
</threat_model>

<verification>
1. `_alternating_read_side_effect` is defined once in the builder cluster and called by both legs with
   their own ordered pairs; neither refactored body defines a nested closure; the pre-existing divergence
   closure survives.
2. `tests/test_chip_test.py` reports 162 passed and `-k read_step` reports 4 passed; the diff since
   `93a1672` removes zero assertion lines.
3. PRUNE-08 reads Complete at both sites; the five ledger counts read 18 / 28 / 18 / 28 / 0; the diff
   against blob `6366d59d` carries at most four changed-line markers; MEAS-01, R4-01, the Coverage block
   and Phase 181's rows are untouched.
4. All seven battery legs exit 0, the watermark line matches exactly, and the suite reports a measured
   count at or above 2247 with zero failures.
5. `phase_added_comments=0` under the `tokenize` gate, asserted as equality with 621 and 0.
6. Both submodules and the generated chip database are clean, and the changed-file set since `93a1672`
   is exactly the two test modules.
7. Both roadmap regions `cmp` identical against blob `cc9499934b`, with the 434 and 5386 line-count
   guards holding; the Phase 180 section carries five plans, five ticked boxes and a `**Gap closure**`
   grouping; the phase-level box stays unticked.
8. The seal transcript mentions neither the retired re-key checker nor its ledger nor a superseded floor.

**Seven legs. Floor 2247** — 2239 before the phase, 2245 at its original final HEAD, plus plan `180-04`'s
two additive legs. No ledger row is required: the declared-re-key protocol was retired on 2026-09-08
along with `tools/rekey/check_rekey_ledger.py`, which no longer exists anywhere in the repository.
</verification>

<success_criteria>
- IN-02 is closed with one shared builder and not one assertion changed.
- PRUNE-08 is Complete in the ledger, and it moved after the fixes landed, not after a plan promised them.
- Exactly two ledger lines changed; nothing in the roadmap outside the Phase 180 section moved; the
  phase-level checkbox is left for phase completion.
- The tree is green on all seven legs with a measured suite count recorded rather than transcribed.
- Zero product-source lines, zero firmware lines, zero `chip_database.json` bytes and zero `#` comments
  across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-05-SUMMARY.md` when done.
Record in it: the pinned range `<sha>^..<sha>` proving the requirement commit changed exactly two lines;
the five measured ledger counts before and after, with a note that `Pending` does not move because the
row was reading `Gaps Found`; the measured full-suite count; the two roadmap region comparison results;
the module counts before and after the refactor; and any claim in this plan the executor found false.
</output>
