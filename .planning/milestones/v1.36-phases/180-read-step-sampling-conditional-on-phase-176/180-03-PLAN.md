---
phase: 180-read-step-sampling-conditional-on-phase-176
plan: 03
type: execute
wave: 3
depends_on: ["180-01", "180-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-requirement-marking.txt
  - .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-phase-seal.txt
autonomous: true
requirements: [PRUNE-08]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PRUNE-08 reads Complete in both places `.planning/REQUIREMENTS.md` records it — the v1 checkbox at line 60 and the traceability row at line 175 — and in neither place before its closing document was committed"
    - "Exactly two lines of `.planning/REQUIREMENTS.md` changed; every other requirement row, the Future Requirements block, the Out of Scope table and the Coverage block are byte-unchanged"
    - "The v1 checkbox counts move 19 unchecked to 18 and 27 checked to 28, and the traceability `Pending` count moves 19 to 18 — measured, not asserted"
    - "The seven-leg green-tree battery passes, with the full app suite at or above 2245 (the measured 2239 floor plus this phase's six additive tests) and zero failures"
    - "No `#` comment was added anywhere in this phase — `tokenize` COMMENT-token counts are still 621 for `tests/test_chip_test.py` and 0 for `tests/test_readback_inventory.py`"
    - "The `firestarter` firmware submodule and the generated `chip_database.json` are byte-unchanged across the whole phase"
    - "ROADMAP.md's Phase 180 plan checkboxes are ticked and its dependency table is byte-unchanged"
  prohibitions:
    - "PRUNE-08 must not be marked Complete before its closing evidence exists"
    - "The close must not overstate its evidence"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PRUNE-08 recorded Complete in the v1 checkbox and the traceability table"
      contains: "PRUNE-08"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-requirement-marking.txt"
      provides: "before and after checkbox and Pending counts, and the changed-line count"
    - path: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-phase-seal.txt"
      provides: "the seven-leg battery, the cleanliness legs and the comment delta gate, each with its rc"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md"
      via: "PRUNE-08 is flipped only once the closing document that is its evidence exists on disk"
      pattern: "PRUNE-08"
---

<objective>
Seal the phase: record PRUNE-08 as Complete, and prove the tree is green.

The ledger entry this task writes is the bookkeeping consequence of D-01 — PRUNE-08 closing as
*measured, not worth doing*, roadmap criterion 2 — resting on the closing document plan `180-02`
completed and the seed amendment D-09 required. Nothing here re-opens any of those decisions; this
plan only records them.

The marking is deliberately **last**. This project has a recorded, repeated executor failure mode of
flipping a requirement Complete before the artifact that justifies it exists, and PRUNE-08's whole
claim is that a documentary close is legitimate evidence — a checkbox ticked ahead of its document
would be exactly the over-claim this milestone was built to catch. Task 1 therefore refuses to run
until `180-PRUNE-08-CLOSURE.md` is on disk and complete.

The seal then runs the **seven**-leg green-tree battery. It is seven, not eight: the declared-re-key
protocol, its ledger table and `tools/rekey/check_rekey_ledger.py` were all retired on 2026-09-08, so
**no re-key ledger row is needed for this phase** and a plan copying Phase 179's eight-leg version
would invoke a script that no longer exists. The measured floor is **2239**, not the 2242 recorded in
Phase 179's plans, which reads RED against a healthy tree.

Purpose: the requirement ledger and the roadmap tell the truth, and every gate the phase relies on is
observed green in one place. Output: two changed lines in `.planning/REQUIREMENTS.md`, ticked Phase 180
plan checkboxes in `.planning/ROADMAP.md`, and two evidence transcripts.
</objective>

<orchestrator_dispositions>
All three orchestrator rulings were discharged upstream and are recorded here only so the phase's
dispositions read together in the last plan an operator reviews: **Ruling 1** (the one-connect
structural pin) landed in plan `180-01` Task 3; **Ruling 3** (lead with the model-free connect-count
form, omit the modelled illustration) bound plan `180-01` Task 1 and plan `180-02` Task 1; **Ruling 2**
(correct seed R4's now-false sentence, minimally, without touching R4's design substance or re-filing
R4-01) landed in plan `180-02` Task 2. Corrections **C-1** (per-bucket supported counts, no blanket
claim) and **C-4** (state the block list by enumeration) are folded into those same tasks.
</orchestrator_dispositions>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/REQUIREMENTS.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-CONTEXT.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-01-SUMMARY.md
@/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-02-SUMMARY.md
</context>

<environment>
Meta repo `/workspaces`, branch `gsd/v1.36-dev-test-fidelity-planning`, owns `.planning/`. Submodule
`/workspaces/firestarter_app`, branch `gsd/v1.36-dev-test-fidelity`, is where the battery runs but is
not modified by this plan. Submodule `/workspaces/firestarter` is not touched at all.

Always `./.venv311/bin/python` from `/workspaces/firestarter_app` — devcontainer default is 3.12, app
CI is 3.11 only.

Shell `grep` is **ugrep and honours `.gitignore`**. Every evidence-grade scan below uses
`/usr/bin/grep`.

Edit `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` **by hand**. The `gsd-tools` requirements
and roadmap verbs reformat the whole file, and `roadmap.update-plan-progress` clobbers the dependency
table by positional overwrite, destroying the phase name and requirement IDs.

`tests/test_flash_path_record_sync` asserts **whole-repo porcelain** — commit everything before running
the suite, or a clean tree's red result will look like a real failure. Suite wall time without coverage
was measured at roughly 345 s; budget for it.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Record PRUNE-08 Complete — exactly two lines, and only after its closing document exists</name>
  <precondition>`.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` exists, is non-empty, carries at least six level-two headings, and is committed. Halt if it is not: the requirement's evidence must precede the claim.</precondition>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-requirement-marking.txt
  </files>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` — the whole file (203 lines). The two edit sites are line 60 (PRUNE-08's v1 checkbox, under "No-Information Operations") and line 175 (its traceability row). Line numbers may have shifted; locate by content, then verify against these.
    - `/workspaces/.planning/REQUIREMENTS.md:103` — MEAS-01, already Complete. **Do not touch.**
    - `/workspaces/.planning/REQUIREMENTS.md:122` — R4-01 in Future Requirements. **Do not touch.**
    - `/workspaces/.planning/REQUIREMENTS.md:195-199` — the Coverage block. **Do not touch**; its "46 total / mapped 46 / unmapped 0" figures do not change when a box is ticked.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md` — the evidence this marking rests on; read it before ticking anything.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PATTERNS.md` section 5 — the measured before and after counts.
  </read_first>
  <action>
Make exactly two hand edits to `.planning/REQUIREMENTS.md`. Never use the `gsd-tools` requirements verbs — they reformat the whole file.

**Edit 1, line 60.** PRUNE-08's v1 checkbox is currently unticked. Tick it, recording D-01's verdict. Change nothing else on the line: the requirement text — which already states that closing as *measured, not worth doing* with the measurement recorded is a success and not a miss — stays byte-identical, because that sentence is precisely what this phase discharged, and D-05 forbids re-filing the size-gated variant that the same measurement surfaced.

**Edit 2, line 175.** PRUNE-08's traceability row currently reads its phase as 180 with a pending status. Change the status cell to `Complete`, matching the spelling and spacing every other completed row in that table already uses. Leave the requirement and phase cells alone.

Touch no other requirement row. In particular do not touch MEAS-01 (:103), R4-01 (:122), the Coverage block (:195-199), or any RPT or HYG row — those belong to Phase 181.

Then write `evidence/180-03-requirement-marking.txt` in the Phase 177 transcript form — numbered section headers, flat key=value scalars, explicit rc after each command — recording the measured before and after values, not the expected ones: `unchecked_before=19`, `unchecked_after=18`, `checked_before=27`, `checked_after=28`, `pending_before=19`, `pending_after=18`, `changed_lines=2`, plus the closure document's path and its heading count as the evidence the marking rests on. Recompute each scalar from the file rather than transcribing it from this plan — this project's recompute-never-transcribe rule is what makes the scope claim checkable at all.

Commit in `/workspaces` on `gsd/v1.36-dev-test-fidelity-planning`, with a message naming PRUNE-08 and the closing document.
  </action>
  <verify>
    <automated>cd /workspaces && R=.planning/REQUIREMENTS.md; ! /usr/bin/grep -qF '- [ ] **PRUNE-08**' "$R" && /usr/bin/grep -qF '- [x] **PRUNE-08**' "$R" && ! /usr/bin/grep -qF '| PRUNE-08 | Phase 180 | Pending |' "$R" && /usr/bin/grep -qF '| PRUNE-08 | Phase 180 | Complete |' "$R"</automated>
    <fails_when>non-zero exit — PRUNE-08 is still unticked, still pending in the traceability table, or was ticked in only one of the two places it is recorded.</fails_when>
    <automated>cd /workspaces && awk '/^- \[ \] \*\*/{u++} /^- \[x\] \*\*/{c++} /^\| .* \| Pending \|$/{p++} /^\| .* \| Complete \|$/{q++} END{printf "unchecked=%d checked=%d pending=%d complete=%d\n",u,c,p,q; exit !(u==18 && c==28 && p==18 && q==28)}' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, and the printed line names the four measured counts. `unchecked=19` or `pending=19` means the flip did not land; `unchecked=17` or `checked=29` means a second requirement was ticked, which this phase must not do.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-requirement-marking.txt; test -s "$E" && /usr/bin/grep -qF 'unchecked_after=18' "$E" && /usr/bin/grep -qF 'checked_after=28' "$E" && /usr/bin/grep -qF 'pending_after=18' "$E" && /usr/bin/grep -qF 'changed_lines=2' "$E"</automated>
    <fails_when>non-zero exit — the marking transcript is missing, or does not record all four measured scalars, so the two-line scope claim was asserted rather than measured.</fails_when>
    <automated>cd /workspaces && R=.planning/REQUIREMENTS.md; /usr/bin/grep -qF '- [x] **MEAS-01**' "$R" && /usr/bin/grep -qF '**R4-01**' "$R" && /usr/bin/grep -qF 'v1 requirements: 46 total' "$R" && /usr/bin/grep -qF '| RPT-A1 | Phase 181 | Pending |' "$R"</automated>
    <fails_when>non-zero exit — MEAS-01, R4-01, the Coverage block or Phase 181's first traceability row was altered, meaning the edit reached beyond PRUNE-08's two lines.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` line 60 starts `- [x] **PRUNE-08**` and its requirement text is otherwise byte-unchanged.
    - `.planning/REQUIREMENTS.md` line 175 reads `| PRUNE-08 | Phase 180 | Complete |`.
    - The v1 checkbox counts read 18 unchecked and 28 checked; the traceability table reads 18 `Pending` and 28 `Complete`.
    - `- [x] **MEAS-01**`, `**R4-01**`, `v1 requirements: 46 total` and `| RPT-A1 | Phase 181 | Pending |` are all still present.
    - This task's own commit changes exactly two lines of `.planning/REQUIREMENTS.md` and no other file, checked with an explicit pinned range `<sha>^..<sha>` recorded in the SUMMARY — never a relative `HEAD~N`, which in a repository with parallel sessions can name another session's commit.
    - `evidence/180-03-requirement-marking.txt` contains `unchecked_after=18`, `checked_after=28`, `pending_after=18` and `changed_lines=2`.
  </acceptance_criteria>
  <done>PRUNE-08 reads Complete in both places the ledger records it, nothing else in the ledger moved, and the flip happened after the document that justifies it — not before.</done>
</task>

<task type="auto">
  <name>Task 2: The phase seal — seven green legs, the cleanliness legs, the comment delta gate, and the roadmap checkboxes</name>
  <precondition>Both repositories have clean working trees with every prior task's work committed. `tests/test_flash_path_record_sync` asserts whole-repo porcelain, so an uncommitted file turns the suite red for a reason that has nothing to do with this phase.</precondition>
  <files>
    .planning/ROADMAP.md,
    .planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-phase-seal.txt
  </files>
  <read_first>
    - `/workspaces/.planning/MILESTONES.md:1-41` — the v1.36 section, which is aimed by name at a Phase 180 or 181 plan. It records that the re-key protocol, its ledger table and its checker were retired, that the ritual is seven legs and not eight, and that the suite floor moved to 2239.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-RESEARCH.md` section "Gate Battery" (line 745) — all seven legs as exact runnable command lines, each executed green this research session, with the expected output of each.
    - `/workspaces/.planning/ROADMAP.md` lines 433-446 — the Phase 180 section, including the four success criteria and the plan list. Read the section boundaries before editing so the edit stays inside them.
    - `/workspaces/.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-01-SUMMARY.md` and `180-02-SUMMARY.md` — the measured pytest counts and any line number that moved.
  </read_first>
  <action>
Run the **seven**-leg green-tree battery from `/workspaces/firestarter_app` using `./.venv311/bin/python`, and record each leg's output and rc.

Leg 1: `ruff check firestarter/ tests/`. Leg 2: `ruff format --check firestarter/ tests/`. Leg 3: `tools/check_mypy_watermark.py`, whose output must match the exact watermark line — mypy prints an OK-looking line when it is *missing*, so match the full line rather than a substring. Leg 4: `tools/snapshot_report_shapes.py --check`. Leg 5: `tools/check_devtest_orchestrator.py`. Leg 6: `tools/check_diagnostic_report_claims.py`. Leg 7: the full suite, `python -m pytest tests/ -o addopts="" -q`, whose count line requires the addopts override because the project's own addopts is quiet and would hide it.

**Seven legs, not eight.** Do not invoke `tools/rekey/check_rekey_ledger.py` — it was deleted along with the whole declared-re-key protocol on 2026-09-08, and **no ledger row is required for this phase**. Do not assert a suite floor of 2241 or 2242; those are Phase 179 assertions that read RED against a healthy tree. The measured floor is 2239 before this phase, and this phase adds six test functions, so the expected count is at or above 2245. Re-measure it and record what you measured, rather than transcribing the expected figure.

Also run the cleanliness legs — the firmware submodule porcelain-clean, and the generated `chip_database.json` unmodified — and the `tokenize` comment delta gate over both touched test modules against their pinned baselines of 621 and 0. Use the `tokenize` gate, not the inherited added-line regex: that regex matches only lines whose first non-space character is a hash, so a trailing comment ships green past a non-overridable project rule.

Then update `.planning/ROADMAP.md` by hand — never the `gsd-tools` roadmap verbs, and never `roadmap.update-plan-progress`, which clobbers the dependency table by positional overwrite and destroys the phase name and requirement IDs. Snapshot the file first and diff after, so an unintended edit outside the Phase 180 section is caught. Inside the Phase 180 section only, tick the three plan checkboxes for `180-01-PLAN.md`, `180-02-PLAN.md` and `180-03-PLAN.md`. Do not alter the goal line, the dependency line, the requirements line or the four success criteria, and do not touch any other phase's section.

Finally write `evidence/180-03-phase-seal.txt` in the Phase 177 transcript form: one numbered section per leg, the verbatim tail of each command's output, an explicit rc line after each, then the cleanliness and comment-gate scalars, then `full_suite_passed=` with the measured count, `full_suite_failed=0`, `snapshots_passed=`, `phase_added_comments=0`, `firmware_porcelain=clean`, `chip_database_unchanged=1`, and `roadmap_lines_changed_outside_phase_180=0`.

Commit in `/workspaces`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m ruff check firestarter/ tests/ && ./.venv311/bin/python -m ruff format --check firestarter/ tests/ && ./.venv311/bin/python tools/check_mypy_watermark.py | /usr/bin/grep -qx 'mypy errors: 35 (watermark: 35)' && ./.venv311/bin/python tools/snapshot_report_shapes.py --check && ./.venv311/bin/python tools/check_devtest_orchestrator.py && ./.venv311/bin/python tools/check_diagnostic_report_claims.py</automated>
    <fails_when>non-zero exit at any of legs 1 to 6 — ruff prints a rule code instead of the all-clear, the format check names a file, the watermark line is anything other than the exact 35-of-35 line (mypy prints an OK-looking line when it is missing, so a substring match would pass on absence), a snapshot reports drift, or either orchestrator or claims checker prints a non-PASS verdict.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q > /tmp/180-03-suite.txt 2>&1; rc=$?; tail -3 /tmp/180-03-suite.txt; test "$rc" -eq 0 && N=$(/usr/bin/grep -oE '^[0-9]+ passed' /tmp/180-03-suite.txt | /usr/bin/grep -oE '^[0-9]+') && test -n "$N" && test "$N" -ge 2245 && ! /usr/bin/grep -qE '[0-9]+ failed' /tmp/180-03-suite.txt</automated>
    <fails_when>non-zero pytest exit; or the captured count line is absent (the addopts override was dropped, so the count was suppressed); or the passed count is below 2245, which means fewer than this phase's six additive tests were collected; or the summary reports any `failed` count.</fails_when>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import io,tokenize;B={'tests/test_chip_test.py':621,'tests/test_readback_inventory.py':0};bad=[p for p,e in B.items() if sum(1 for t in tokenize.generate_tokens(io.StringIO(open(p,encoding='utf-8').read()).readline) if t.type==tokenize.COMMENT)!=e];print('phase_added_comments='+('0' if not bad else ','.join(bad)));raise SystemExit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed line is anything other than `phase_added_comments=0` — the named file is where a `#` comment was added, including a trailing one the inherited added-line regex cannot see.</fails_when>
    <automated>test -z "$(git -C /workspaces/firestarter status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain)" && test -z "$(git -C /workspaces/firestarter_app status --porcelain firestarter/data/chip_database.json)"</automated>
    <fails_when>non-zero exit — the firmware submodule, the app working tree, or the generated chip database is dirty at seal time, so the phase cannot claim a product-source-free close.</fails_when>
    <automated>cd /workspaces && sed -n '/^### Phase 180:/,/^### Phase 181:/p' .planning/ROADMAP.md > /tmp/180-03-roadmap-section.txt; awk '/^- \[x\] 180-0[123]-PLAN\.md/{n++} END{printf "ticked_plan_boxes=%d\n",n; exit !(n==3)}' /tmp/180-03-roadmap-section.txt && /usr/bin/grep -qF '**Requirements**: PRUNE-08' /tmp/180-03-roadmap-section.txt && test "$(/usr/bin/grep -cE '^### Phase (17[4-9]|18[01]):' .planning/ROADMAP.md)" -ge 8</automated>
    <fails_when>non-zero exit — fewer than three ticked Phase 180 plan checkboxes (the section still carries the placeholder or a plan was missed), the requirements line was destroyed by a whole-file rewrite, or the milestone's phase headings dropped below eight, which is the signature of a roadmap verb reformatting the file.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/180-03-phase-seal.txt; test -s "$E" && /usr/bin/grep -qF 'full_suite_failed=0' "$E" && /usr/bin/grep -qF 'phase_added_comments=0' "$E" && /usr/bin/grep -qF 'firmware_porcelain=clean' "$E" && /usr/bin/grep -qF 'chip_database_unchanged=1' "$E" && /usr/bin/grep -qE '^full_suite_passed=[0-9]+' "$E" && test "$(/usr/bin/grep -cE '^rc=0$' "$E")" -ge 7</automated>
    <fails_when>non-zero exit — the seal transcript is missing, does not record a measured pass count alongside the four verdict scalars, or carries fewer than seven zero-return-code lines, which means fewer than seven legs were actually run.</fails_when>
  </verify>
  <acceptance_criteria>
    - All six static legs exit 0, and `tools/check_mypy_watermark.py` prints exactly `mypy errors: 35 (watermark: 35)`.
    - `./.venv311/bin/python -m pytest tests/ -o addopts="" -q` exits 0 with a passed count at or above 2245 and no `failed` count.
    - The `tokenize` gate prints `phase_added_comments=0`.
    - `git -C /workspaces/firestarter status --porcelain`, `git -C /workspaces/firestarter_app status --porcelain` and the same scoped to `firestarter/data/chip_database.json` all print nothing.
    - The ROADMAP Phase 180 section carries three ticked plan checkboxes matching `- [x] 180-0N-PLAN.md`, still carries `**Requirements**: PRUNE-08`, and the milestone still has at least eight `### Phase 17x/18x:` headings.
    - `evidence/180-03-phase-seal.txt` records `full_suite_passed=` with a measured number, `full_suite_failed=0`, `phase_added_comments=0`, `firmware_porcelain=clean`, `chip_database_unchanged=1`, and at least seven `rc=0` lines — one per battery leg.
    - The seal transcript names no deleted tool and asserts no superseded suite floor; it records only the seven legs listed in this task's action.
  </acceptance_criteria>
  <done>Every gate this phase relies on has been observed green in one place, the roadmap records the three plans as done, and the transcript proves the close cost zero product-source lines, zero firmware lines and zero source comments.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths this phase **creates** (absent at meta HEAD `f031be81` and app HEAD `ffb0060`, so a
drift check must not flag them as unresolved references):

**New documents** —
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-PRUNE-08-CLOSURE.md`.

**New seed section** — `## Status: Phase 180 amendment` in
`.planning/seeds/dev-test-adaptive-sequencing.md`.

**New evidence files**, all under
`.planning/phases/180-read-step-sampling-conditional-on-phase-176/evidence/` (the directory itself does
not exist yet) — `180-01-verdict-pin-red.txt`, `180-01-behavioural-legs.txt`,
`180-01-one-connect-pin.txt`, `180-02-closure-claims.txt`, `180-02-seed-amendment.txt`,
`180-03-requirement-marking.txt`, `180-03-phase-seal.txt`.

**New test functions** (plan `180-01`) — `test_read_verdict_expression_reads_only_the_last_full_read_result`,
`test_a_planted_divergence_term_in_the_verdict_reddens_the_pin`,
`test_read_step_last_run_failure_yields_bad`,
`test_read_step_first_run_failure_with_passing_last_run_yields_ok`,
`test_one_read_eprom_call_costs_exactly_one_connect`,
`test_a_planted_second_operation_context_in_read_eprom_reddens_the_pin`.

**New test helpers and constants** (plan `180-01`) — `_verdict_expression_names`, `_VERDICT_ANCHOR`,
`_operations_source`, `_read_eprom_connect_shape`, `_CONTEXT_ANCHOR`.

**Deliberately NOT produced** — no re-key ledger row (the protocol was retired 2026-09-08), no
`COVERAGE.md` (no external API is integrated), no schema push task (there is no ORM or migration in
this project), no bench measurement document (this phase is not hardware-gated), no hole-padded region
fixture and no block-wise comparator (roadmap criterion 4's precondition is false).
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; block on `high`. This plan changes two lines of a requirements ledger, ticks three
roadmap checkboxes, and runs read-only checks. No code, no dependency, no network or serial path, no
untrusted input, no credential or user data.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement ledger → every downstream reader | A ticked box is a claim that evidence exists. If the box moves before the evidence does, the ledger lies, and every audit and milestone summary built on it inherits the lie. |
| green-tree battery → the phase's own conclusion | The battery is the only thing standing between "the tests were written" and "the tests pass". A leg that invokes a deleted script, or asserts a superseded floor, converts the seal into theatre. |
| meta repo ↔ both submodules | No data crosses. The battery reads the app repo and writes nothing to it; the firmware submodule is not read at all beyond its porcelain status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-180-09 | Spoofing | PRUNE-08's checkbox and traceability row | high | mitigate | Task 1 carries a blocking precondition requiring the closing document to exist, be non-empty and carry at least six headings before the flip; the marking is the last task in the last plan; and the transcript records the evidence path alongside the counts. This is a recorded, repeated executor failure mode in this project, which is why it is the one `high` in the register — and it is mitigated, so nothing blocks. |
| T-180-10 | Tampering | `.planning/REQUIREMENTS.md` rows outside PRUNE-08 | medium | mitigate | Four absolute count assertions (18 unchecked, 28 checked, 18 Pending, 28 Complete) plus positive greps for MEAS-01, R4-01, the Coverage line and Phase 181's first row; hand edits only, never the reformatting verbs. |
| T-180-11 | Tampering | `.planning/ROADMAP.md` dependency table | medium | mitigate | Hand edit inside the Phase 180 section only, snapshot-and-diff before committing, and a verify leg asserting the requirements line survived and the milestone still carries at least eight phase headings — the signature a positional-overwrite verb would destroy. |
| T-180-12 | Repudiation | the phase seal's own verdict | medium | mitigate | The seal transcript must carry a *measured* pass count, not a transcribed one, and is negative-grepped for any mention of the retired re-key checker, its ledger or the superseded 2242 floor — the three ways this seal could be copied forward into vacuity. |
| T-180-SC | Tampering | npm / pip / cargo installs | low | accept | No package is installed. Every tool invoked is already in the pinned py3.11 venv or is Python standard library. No Package Legitimacy Audit is required. |

One threat is rated `high` (T-180-09) and it is dispositioned `mitigate` with a blocking precondition,
so the `security_block_on: high` threshold is satisfied.
</threat_model>

<verification>
1. PRUNE-08 reads Complete at both sites; the four ledger counts read 18 / 28 / 18 / 28; MEAS-01, R4-01, the Coverage block and Phase 181's rows are untouched.
2. All seven battery legs exit 0, the watermark line matches exactly, and the suite reports a measured count at or above 2245 with zero failures.
3. `phase_added_comments=0` under the `tokenize` gate, not the added-line regex.
4. Both submodules and the generated chip database are clean.
5. ROADMAP's Phase 180 section carries three ticked plan checkboxes, its requirements line intact, and nothing outside the section moved.
6. The seal transcript mentions neither the retired re-key checker nor its ledger nor the superseded 2242 floor.

**Seven legs, floor 2239 before this phase's six additive tests.** No ledger row is required — the
declared-re-key protocol was retired on 2026-09-08 along with `tools/rekey/check_rekey_ledger.py`,
which no longer exists anywhere in the repository.
</verification>

<success_criteria>
- PRUNE-08 is Complete in the ledger, and it moved after its evidence, not before (Pitfall 6).
- Exactly two ledger lines changed; no other requirement was touched.
- The tree is green on all seven legs, with a measured suite count recorded rather than transcribed.
- Zero product-source lines, zero firmware lines and zero `#` comments were added by the whole phase.
- The roadmap records Phase 180's three plans as done without any collateral reformatting.
</success_criteria>

<output>
Create `.planning/phases/180-read-step-sampling-conditional-on-phase-176/180-03-SUMMARY.md` when done.
Record in it: the measured suite count and how it compares to the 2245 expectation, the four ledger
counts as measured, the pinned sha range used for the two-line scope check, and — per
`/workspaces/CLAUDE.md` — any rationale that would otherwise have been a source comment. Note any
prior claim in `.planning/` this plan found false, rather than absorbing the disagreement silently.
</output>

