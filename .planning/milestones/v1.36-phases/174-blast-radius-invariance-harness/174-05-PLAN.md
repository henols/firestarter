---
phase: 174-blast-radius-invariance-harness
plan: 05
type: execute
wave: 4
depends_on: [174-03, 174-04]
files_modified:
  - .github/workflows/rekey-ledger-check.yml
  - .planning/MILESTONES.md
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-05-checker-fires.txt
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-05-phase-closeout.txt
autonomous: true
requirements: [GATE-06]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The cross-tree ledger check has a concrete home that actually fires: the primary runner is a local gate — this phase's own verification command against the populated submodule — and a registered workflow at .github/workflows/rekey-ledger-check.yml is the additional leg, so the check does not depend on CI registration to exist at all — per D-13."
    - "The workflow's push and pull_request branch lists both name beta and the milestone branch glob, and neither list is scoped to main alone; the one registered meta workflow is main-scoped and its last run was RED, so copying its triggers would have made this checker fire zero times during a milestone that forks off beta."
    - "The workflow checks the app sub-repo out explicitly at a resolved ref rather than relying on recursive submodule fetching, copying the structural half of the one registered meta workflow — whose own comments record that submodule fetching re-armed a failure class where an accidental gitlink with no configuration entry killed the checkout before any assertion ran."
    - "The workflow is proven to fire rather than merely to exist: its trigger configuration is parsed and asserted, its checker invocation is run locally with the same arguments the workflow uses, and the run is recorded — a checker nothing invokes is this project's documented fail-open pattern and a file's existence is not evidence."
    - "A declared re-key's separate-commit rule is stated where Phases 175 through 181 will read it: the MILESTONES.md ledger section carries the rule in prose, and the ledger module's freedom from executable logic is what makes a one-line after_hash change a complete commit — per D-11."
    - "The phase's cost is measured rather than assumed: the added test modules' wall-clock is recorded against the measured 740.92 s full-suite baseline, and the derive_plan sweep's 4.26 s at 0.58 percent of that baseline is the recorded reason nothing in this phase carries a slow marker."
    - "The three pre-existing tests/test_skip_census.py failures are recorded as pre-existing with their measured cause — a 180 s child-run cap against a 740.92 s suite — and are shown untouched by this phase, so no later reader mistakes them for damage this phase did."
    - "Not one file this phase wrote carries a comment line beyond a script shebang, and no Markdown this phase wrote carries an HTML comment — audited across every new and modified file in both repositories."
    - "No production module in firestarter_app/firestarter/ was modified and no firmware file was touched at all, so the phase boundary CONTEXT.md states is proven rather than asserted."
  artifacts:
    - path: .github/workflows/rekey-ledger-check.yml
      provides: "The additional CI leg for the cross-tree ledger check, triggered on beta and the milestone branch glob"
      contains: "rekey"
    - path: .planning/MILESTONES.md
      provides: "The declared-re-key protocol stated in prose beside the ledger table"
      contains: "separate commit"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-05-checker-fires.txt
      provides: "Parsed trigger configuration and a local run with the workflow's own arguments"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-05-phase-closeout.txt
      provides: "Cost measurement, comment audit, phase-boundary proof and the pre-existing-RED record"
  key_links:
    - from: .github/workflows/rekey-ledger-check.yml
      to: tools/rekey/check_rekey_ledger.py
      via: "the workflow's run step invokes the checker with an explicit repo root, the same arguments the local gate uses"
      pattern: "check_rekey_ledger"
    - from: .github/workflows/rekey-ledger-check.yml
      to: firestarter_app
      via: "explicit sub-repo checkout at a resolved ref, never recursive submodule fetching"
      pattern: "firestarter_app"
  prohibitions:
    - statement: "No cross-tree checker is accepted on the strength of its file existing. Acceptance requires the invocation to have been run and recorded; a workflow whose triggers never match this milestone's branches is indistinguishable from no workflow at all."
      status: required
      verification: judgment
    - statement: "No pre-existing failure outside this phase's scope is fixed, silenced or marked as collateral. The three suite-duration failures are recorded with their cause and left exactly as found, because repairing them here would hide a real backlog item inside an unrelated phase's diff."
      status: required
      verification: judgment
---

<objective>
Give the cross-tree ledger check a home that actually fires, and close the phase by measuring what it
cost and proving what it did not touch.

Purpose: D-13 puts the ledger check on the meta side because only meta can see both trees. What D-13 does
not settle is what runs it — and the measured answer matters, because meta has exactly one registered
workflow, it is scoped to a branch this milestone never targets, and its last run was RED. A checker
registered the same way would fire zero times, which is this project's documented fail-open pattern under
a different name. This plan decides the runner concretely and proves the decision by invocation.

The closeout half exists because this phase's whole claim is measurement discipline. A phase that builds
an oracle and then asserts its own cost, its own comment hygiene and its own scope boundary without
measuring them would be making exactly the mistake it was built to catch.

Output: one registered workflow, the declared-re-key protocol in prose, and two evidence transcripts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/174-blast-radius-invariance-harness/174-CONTEXT.md
@.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md
@.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md
@.planning/phases/174-blast-radius-invariance-harness/174-01-SUMMARY.md
@.planning/phases/174-blast-radius-invariance-harness/174-03-SUMMARY.md
@.planning/phases/174-blast-radius-invariance-harness/174-04-SUMMARY.md
@CLAUDE.md
</context>

<environment_preamble>
Commands run from `/workspaces`. Python goes through `firestarter_app/.venv311/bin/python` for anything
importing the app package, and through the meta repo's `python3` for the meta-side checker, which is
standard library only. `/usr/bin/grep` for all gate evidence, never bare `grep`.

Measured facts about the meta repository's CI, all verified this planning session and all load-bearing
for Task 1:
- `.github/workflows/` holds exactly one file, `catalog-sync-check.yml`.
- Its triggers are `push` and `pull_request` on branch `main` only, with paths under `tools/catalog/`.
- Its last run was a push to `main` on 2026-08-31 with conclusion failure; 8 runs ever, 5 of which failed.
- `.planning/config.json` sets `git.base_branch` to `beta`; milestone work happens on
  `gsd/v1.36-dev-test-fidelity`. So a `main`-scoped trigger matches nothing this milestone does.
- `tools/wiki/`'s checkers were retired on 2026-09-02 and `wiki-check.yml` was never registered — the
  project's own recorded example of a gate that could not fire.
- `git ls-files tools/` returns four entries, of which `tools/catalog/codegen.py` is the only Python.

Zero comments in every file this plan writes. YAML comments are comments: the workflow file carries none.
The analog's explanatory comment blocks are quotations of an existing file, not a template. All reasoning
for the workflow goes in its `name` fields and step names, which are not comments.
</environment_preamble>

<tasks>

<task type="auto">
  <name>Task 1: Register the ledger check where it can fire, and prove it fires by invoking it</name>
  <files>.github/workflows/rekey-ledger-check.yml, .planning/MILESTONES.md, .planning/phases/174-blast-radius-invariance-harness/evidence/174-05-checker-fires.txt</files>
  <read_first>
    - `.github/workflows/catalog-sync-check.yml` (whole file) — copy its cross-tree checkout structure: check the meta repo out to a subdirectory, then check the sub-repo out explicitly at a resolved ref rather than relying on recursive submodule fetching. Do NOT copy its `branches: [main]` triggers, and note its own comment block recording that it had never once succeeded, so a red run there is not evidence a new leg fired.
    - `tools/rekey/check_rekey_ledger.py` as plan 174-01 left it and plan 174-03 exercised it — its `--repo-root`, `--ledger` and `--milestones` flags and its three exit codes
    - `.planning/config.json` — `git.base_branch` and `git.branching_strategy`, which decide which branch names a trigger must name
    - `.planning/MILESTONES.md` — the `### v1.36 Re-Key Ledger` section as plan 174-03 left it
    - `.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md` section "D-13 — where a meta-side checker can actually run" and "Pitfall 6: The meta-side checker never runs"
    - `.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md` section for `rekey-ledger-check.yml`
  </read_first>
  <behavior>
    - The workflow's `push` branch list contains `beta` and the glob matching milestone branches, and does not consist of `main` alone.
    - The workflow's `pull_request` branch list contains the same two entries.
    - The workflow's path filters cover `.planning/MILESTONES.md`, `tools/rekey/`, the app submodule gitlink, and the workflow's own file.
    - The workflow checks the meta repo out to a subdirectory and then checks `firestarter_app` out explicitly; it does not use recursive submodule fetching.
    - Its run step invokes `tools/rekey/check_rekey_ledger.py` with an explicit repository root and the app-side ledger path resolved inside the checked-out sub-repo.
    - Running that same invocation locally against the populated submodule exits 0 and prints one `OK:` line.
    - Running it locally against a ledger path inside a checkout that does not exist exits 2, so the workflow's own failure mode is the fail-closed one rather than a silent skip.
  </behavior>
  <action>
Write `.github/workflows/rekey-ledger-check.yml`. Its job is the additional leg, not the primary one:
the primary runner is the local gate this phase already has, which is why the check exists whether or not
CI ever fires. State that ordering in the workflow's `name` and in the `MILESTONES.md` prose, because the
opposite arrangement — CI as the only runner — is what produced this project's retired
never-registered checker.

Triggers. `push` and `pull_request`, each with a branch list naming `beta` and the glob that matches
milestone branches, so this milestone's `gsd/v1.36-dev-test-fidelity` work and the eventual merge to
`beta` both match. Do not scope to `main`: measurement found the one registered meta workflow does
exactly that, that its six historical pull-request runs fired only because those pull requests targeted
`main`, and that this project's base branch is `beta`. A `main`-only trigger here would match nothing for
the whole milestone. Path filters: `.planning/MILESTONES.md`, `tools/rekey/**`, the `firestarter_app`
gitlink path, and `.github/workflows/rekey-ledger-check.yml` itself, so a change to the checker or to its
own registration is covered.

Checkout. Copy the structural half of the analog: check the meta repository out to a `meta`
subdirectory, then check `firestarter_app` out explicitly at a resolved ref into a sibling path. Do not
use recursive submodule fetching — the analog's own comments record that submodule fetching re-armed a
failure class in which an accidentally committed gitlink with no configuration entry killed the checkout
before any assertion ran, and that is a failure mode that would read as a red gate rather than as a
broken one.

Run step. Invoke the checker with Python from the runner's default toolchain — the checker is standard
library only, which is the house shape for both repositories' scripts — passing an explicit repository
root and pointing `--ledger` at the ledger path inside the checked-out sub-repo. Do not add a step that
ignores a non-zero exit; the whole point is that the job's conclusion is the gate's verdict.

No YAML comments anywhere in the file. Where the analog explains itself in a comment block, put the same
information into step `name` values, which are not comments and which appear in the run log where a
reader will actually see them.

Then extend the `MILESTONES.md` `### v1.36 Re-Key Ledger` section with a short protocol paragraph stating
the three rules Phases 175 through 181 must follow, in prose, so the constraint lives where the next
phase's planner will read it:
first, a behaviour change lands on its own and turns the frozen gate RED;
second, a second commit touching only the row's after-hash and this table's matching row makes it green,
so the re-key is a reviewable unit and an executor cannot reflexively repair the test inside a large
change commit;
third, the before-hash is never overwritten, because the original value is the evidence that the change
was exactly what it declared and nothing more.
Name the two runners of the check in the same paragraph — the local gate as primary, the registered
workflow as additional — so a later reader knows where to look when a row goes red.

Finally, prove the registration by invocation rather than by existence. Parse the workflow file and print
its two branch lists and its path filter list; then run the checker locally with the exact argument shape
the workflow's run step uses; then run it once more against a ledger path inside a directory that does not
exist, to show the failure mode is exit 2 rather than a silent pass. Write all three into the evidence
transcript. A `gh run list` on this workflow is NOT acceptable evidence at this point, because a workflow
whose triggers have not yet matched has no runs, and because the one existing meta workflow's red history
means a red run proves nothing about whether a new leg fired.
  </action>
  <acceptance_criteria>
    - `.github/workflows/rekey-ledger-check.yml` exists and parses as YAML.
    - Its `push` branch list and its `pull_request` branch list each contain `beta` and the milestone-branch glob, and neither is `main` alone.
    - Its path filter list contains `.planning/MILESTONES.md`, a `tools/rekey` entry, a `firestarter_app` entry, and its own filename.
    - The file contains no line whose first non-whitespace character is a hash.
    - The file does not contain a recursive submodule setting, and it does contain an explicit `firestarter_app` checkout step.
    - Its run step names `tools/rekey/check_rekey_ledger.py`.
    - Running the checker locally with the workflow's argument shape exits 0 and prints an `OK:` line.
    - Running it with `--ledger` inside a nonexistent directory exits 2.
    - `.planning/MILESTONES.md` contains the phrase describing the separate-commit rule and names both runners.
    - `.github/workflows/catalog-sync-check.yml` is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && mkdir -p "$E" && EV=$E/174-05-checker-fires.txt && D=$(mktemp -d) && { echo "== workflow triggers, parsed"; python3 -c "import re,sys,pathlib; t=pathlib.Path('.github/workflows/rekey-ledger-check.yml').read_text(); print('has_beta='+str(bool(re.search(r'^\s*-\s*beta\s*$', t, re.M)))); print('has_gsd_glob='+str(bool(re.search(r'gsd/', t)))); print('has_milestones_path='+str('.planning/MILESTONES.md' in t)); print('has_rekey_path='+str('tools/rekey' in t)); print('has_app_path='+str('firestarter_app' in t)); print('has_self_path='+str('rekey-ledger-check.yml' in t)); print('names_checker='+str('check_rekey_ledger.py' in t)); print('recursive_submodules='+str('recursive' in t)); print('comment_lines='+str(len([l for l in t.splitlines() if l.lstrip().startswith('#')]))); print('push_and_pr='+str(t.count('branches')>=2))"; echo "rc=$?"; echo "== checker with the workflow argument shape"; python3 tools/rekey/check_rekey_ledger.py --repo-root . --ledger firestarter_app/tests/fixtures/rekey_ledger.py --milestones .planning/MILESTONES.md; echo "rc=$?"; echo "== the same invocation against a checkout that does not exist"; python3 tools/rekey/check_rekey_ledger.py --repo-root "$D/absent" --ledger "$D/absent/firestarter_app/tests/fixtures/rekey_ledger.py" --milestones .planning/MILESTONES.md; echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 2 && test "$(/usr/bin/grep -c '^rc=2$' "$EV")" -eq 1 && /usr/bin/grep -qx 'has_beta=True' "$EV" && /usr/bin/grep -qx 'has_gsd_glob=True' "$EV" && /usr/bin/grep -qx 'has_milestones_path=True' "$EV" && /usr/bin/grep -qx 'has_rekey_path=True' "$EV" && /usr/bin/grep -qx 'has_app_path=True' "$EV" && /usr/bin/grep -qx 'has_self_path=True' "$EV" && /usr/bin/grep -qx 'names_checker=True' "$EV" && /usr/bin/grep -qx 'recursive_submodules=False' "$EV" && /usr/bin/grep -qx 'comment_lines=0' "$EV" && /usr/bin/grep -qx 'push_and_pr=True' "$EV" && /usr/bin/grep -qE '^OK: ' "$EV" && /usr/bin/grep -qE '^ERROR: ' "$EV" && rm -rf "$D"</automated>
    <fails_when>The rc tally is not exactly two 0s and one 2; `has_beta` or `has_gsd_glob` reads False, meaning the triggers do not match any branch this milestone uses and the checker would fire zero times; any of the four path-filter probes reads False; `names_checker` reads False, meaning the workflow does not invoke the checker at all; `recursive_submodules` reads True, meaning the checkout re-armed the gitlink failure class the analog records; `comment_lines` is not 0; `push_and_pr` reads False, meaning only one of the two event types is scoped; or the transcript lacks either an `OK: ` line from the clean invocation or an `ERROR: ` line from the absent-checkout invocation, the latter meaning the failure mode is a silent pass rather than fail-closed.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -q 'separate commit' .planning/MILESTONES.md && /usr/bin/grep -q 'rekey-ledger-check.yml' .planning/MILESTONES.md && ! /usr/bin/grep -q '<!' .planning/MILESTONES.md && test -z "$(git status --porcelain .github/workflows/catalog-sync-check.yml)" && test "$(ls .github/workflows/ | wc -l)" -eq 2</automated>
    <fails_when>The separate-commit rule or the workflow filename is absent from MILESTONES.md, meaning the protocol Phases 175 through 181 must follow is not stated where their planners will read it; an HTML comment opener appears in MILESTONES.md; the pre-existing catalog workflow shows as modified; or the workflow directory holds a count other than two, meaning the new workflow was not added or an unrelated one was.</fails_when>
  </verify>
  <done>The cross-tree ledger check has a named local primary runner and a registered workflow whose triggers match this milestone's branches, both proven by invocation rather than by existence, and the declared-re-key protocol is stated in the ledger section where the next seven phases' planners will read it.</done>
</task>

<task type="auto">
  <name>Task 2: Measure what the phase cost, audit what it wrote, and prove what it did not touch</name>
  <files>.planning/phases/174-blast-radius-invariance-harness/evidence/174-05-phase-closeout.txt, .planning/MILESTONES.md</files>
  <read_first>
    - every file this phase created, listed in the four prior plans' "Artifacts this phase produces" sections
    - `.planning/phases/174-blast-radius-invariance-harness/174-01-SUMMARY.md`, `174-02-SUMMARY.md`, `174-03-SUMMARY.md` and `174-04-SUMMARY.md` — the transcribed RED evidence and the recorded costs
    - `firestarter_app/tests/test_skip_census.py` — enough of it to confirm the 180 second child-run cap is still what it was, so the recorded cause of the three pre-existing failures is current rather than inherited
    - `.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md` section "Cost measurements (the deferred slow marker question)" — the measured 740.92 s suite, the 4.26 s sweep, the 0.58 percent share, and the recorded reason nothing here is marked slow
    - `/workspaces/CLAUDE.md` and `/workspaces/firestarter_app/CLAUDE.md` — the project rules this audit checks against
  </read_first>
  <behavior>
    - Every new and modified source file in both repositories carries zero comment lines, except the three generator scripts and the meta checker, which carry exactly one each — their shebang.
    - No Markdown this phase wrote carries an HTML comment opener.
    - No file under `firestarter_app/firestarter/` is modified, and no file in the `firestarter` firmware submodule is modified at all.
    - `firestarter_app/firestarter/data/chip_database.json` is unmodified, so the generated database was not hand-edited.
    - The five new test modules run together in well under sixty seconds, which against the measured 740.92 s baseline is a share small enough that no slow marker is warranted, and the transcript records the measured number rather than the judgement alone.
    - `tests/test_skip_census.py` still fails exactly three tests for the timeout reason it failed for at the branch base, unchanged and untouched by this phase.
    - No new runtime dependency appears in the package metadata.
  </behavior>
  <action>
This is the phase's own measurement pass. Everything it asserts was asserted somewhere in plans 174-01
through 174-04 about a single file; this task asserts it across the whole phase at once, because a
per-file gate can be satisfied file by file while the phase as a whole drifts.

Run and record five audits into
`.planning/phases/174-blast-radius-invariance-harness/evidence/174-05-phase-closeout.txt`.

Audit one, the comment sweep. For every file this phase created or modified in the app submodule and in
meta, count the lines whose first non-whitespace character is a hash. Every fixture module, every test
module and every JSON artifact must count zero; the three app generators and the meta checker must count
exactly one, their shebang. Use `/usr/bin/grep`, never the bare command, because the devcontainer's
`grep` honors ignore files and silently under-scans — a sweep run with it can read clean while it is not.
Also assert no Markdown this phase wrote contains an HTML comment opener, since one of those in a
planning file has broken this project's own gates before.

Audit two, the phase boundary. Assert `git -C firestarter_app status --porcelain firestarter/` is empty,
which covers both the production package and the generated chip database. Assert the firmware submodule's
porcelain is empty, since this milestone makes no firmware change at all. Print the list of files the
phase actually changed in the app submodule, so the record shows the change set rather than only that a
forbidden directory was clean.

Audit three, the cost. Time the five new test modules together and record the wall clock. Record it beside
the measured baseline — 740.92 s for the full suite, 4.26 s for a whole-database `derive_plan` sweep, and
that sweep's 0.58 percent share — and state the conclusion the numbers support: nothing in this phase
carries a slow marker, because marking a gate slow recreates the never-runs failure mode this phase
exists to prevent, and because the measured share does not justify it. Record the number even if it is
small; the point is that the decision is measured.

Audit four, the pre-existing REDs. Run `tests/test_skip_census.py` on its own and record its output.
Assert exactly three failures and assert the timeout text appears, so the record shows the failures are
the same three with the same cause they had at the branch base. Assert the file's own porcelain is empty,
proving this phase did not touch it. State in the transcript that the cause is a 180 second cap on a child
whole-suite run against a suite measured at 740.92 s, that it is unrelated to
`dedup_fingerprint`, `build_db_diff`, `derive_plan` or the fixtures directory, and that it belongs in the
backlog rather than in this phase.

Audit five, the dependency set. Assert the app package's declared runtime dependencies are unchanged from
the branch base, so HYG-02's no-new-runtime-dependency rule is measured rather than assumed.

Then add a short closing paragraph to the `MILESTONES.md` re-key ledger section recording what the phase
measured about itself: the added test cost against the baseline, the fact that the three census failures
are pre-existing with a named cause, and the fact that no production or firmware file changed. Keep it to
a few lines; the ledger table and the corrections table are the section's substance and this is the
footer that makes the phase's own numbers auditable later.
  </action>
  <acceptance_criteria>
    - The evidence transcript records all five audits, each under its own numbered heading.
    - The comment sweep reports zero comment lines for every fixture, test and JSON artifact this phase wrote, and exactly one for each of the three app generators and the meta checker.
    - No Markdown file this phase wrote contains an HTML comment opener.
    - `git -C firestarter_app status --porcelain firestarter/` is empty and the firmware submodule's porcelain is empty.
    - The transcript records the measured wall clock of the five new test modules together, and states the no-slow-marker conclusion with the 740.92 s and 0.58 percent figures beside it.
    - The transcript records exactly three `tests/test_skip_census.py` failures with the timeout text present, and records that file's porcelain as empty.
    - The app package's declared runtime dependency list is byte-identical to the branch base.
    - `.planning/MILESTONES.md` gains a closing paragraph naming the measured cost and the pre-existing failure count.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && EV=$E/174-05-phase-closeout.txt && { echo "== 1 comment sweep, zero-comment files"; for f in firestarter_app/tests/fixtures/report_shapes.py firestarter_app/tests/fixtures/rekey_ledger.py firestarter_app/tests/fixtures/planted_rekey_mutation.py firestarter_app/tests/test_blast_radius_invariance.py firestarter_app/tests/test_rekey_ledger.py firestarter_app/tests/test_devtest_issue_corpus.py firestarter_app/tests/test_part_number_delta_drift.py; do echo "zero $f=$(/usr/bin/grep -cE '^\s*#' "$f")"; done; echo "== 1b comment sweep, shebang-only files"; for f in firestarter_app/tools/snapshot_report_shapes.py firestarter_app/tools/build_devtest_issue_corpus.py firestarter_app/tools/measure_part_number_delta.py tools/rekey/check_rekey_ledger.py; do echo "shebang $f=$(/usr/bin/grep -cE '^\s*#' "$f")"; done; echo "== 1c html comment openers in markdown this phase wrote"; echo "md_openers=$(/usr/bin/grep -cE '<[!]--' .planning/MILESTONES.md .planning/phases/174-blast-radius-invariance-harness/*.md | /usr/bin/grep -vE ':0$' | wc -l)"; echo "== 1d yaml comments in the new workflow"; echo "yaml_comments=$(/usr/bin/grep -cE '^\s*#' .github/workflows/rekey-ledger-check.yml)"; echo "== 2 phase boundary"; echo "app_prod_porcelain=$(git -C firestarter_app status --porcelain firestarter/ | wc -l)"; echo "fw_porcelain=$(git -C firestarter status --porcelain 2>/dev/null | wc -l)"; echo "app_changed_files:"; git -C firestarter_app diff --name-only origin/beta HEAD; echo "== 3 cost of the five new modules"; /usr/bin/time -f 'new_modules_seconds=%e' sh -c 'cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_devtest_issue_corpus.py tests/test_part_number_delta_drift.py'; echo "rc=$?"; echo "baseline_full_suite_seconds=740.92"; echo "derive_plan_sweep_seconds=4.26"; echo "sweep_share_percent=0.58"; echo "slow_marker=none"; echo "== 4 the three pre-existing census failures, untouched"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_skip_census.py); echo "rc=$?"; echo "census_porcelain=$(git -C firestarter_app status --porcelain tests/test_skip_census.py | wc -l)"; echo "== 5 runtime dependency set unchanged"; echo "deps_changed=$(git -C firestarter_app diff origin/beta HEAD -- pyproject.toml | wc -l)"; } > "$EV" 2>&1; test "$(/usr/bin/grep -cE '^zero .*=0$' "$EV")" -eq 7 && test "$(/usr/bin/grep -cE '^shebang .*=1$' "$EV")" -eq 4 && /usr/bin/grep -qx 'md_openers=0' "$EV" && /usr/bin/grep -qx 'yaml_comments=0' "$EV" && /usr/bin/grep -qx 'app_prod_porcelain=0' "$EV" && /usr/bin/grep -qx 'fw_porcelain=0' "$EV" && /usr/bin/grep -qE '^new_modules_seconds=[0-9.]+$' "$EV" && /usr/bin/grep -qx 'baseline_full_suite_seconds=740.92' "$EV" && /usr/bin/grep -qx 'slow_marker=none' "$EV" && /usr/bin/grep -qx 'census_porcelain=0' "$EV" && /usr/bin/grep -qx 'deps_changed=0' "$EV" && /usr/bin/grep -qE '^3 failed' "$EV" && /usr/bin/grep -q 'TimeoutExpired' "$EV" && test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 1</automated>
    <fails_when>Fewer than seven files report a zero comment count or fewer than four report exactly one, meaning a comment was written into a file this project's hard rule forbids it in; `md_openers` is not 0, meaning an HTML comment opener reached a planning Markdown file; `yaml_comments` is not 0; `app_prod_porcelain` or `fw_porcelain` is not 0, meaning a production or firmware file changed in a test-only phase; the timed line is absent, meaning the cost was asserted rather than measured; the baseline or the no-slow-marker conclusion line is absent; `census_porcelain` is not 0 or the census run does not report exactly three failures with the timeout text, meaning the pre-existing failures were altered, silenced or newly caused; `deps_changed` is not 0, meaning package metadata moved and HYG-02 may be breached; or the count of zero exit codes is not exactly one, since the census leg must be the one non-zero rc and the four new modules must be the one zero.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -qE 'pre-existing|preexisting' .planning/MILESTONES.md && /usr/bin/grep -q '740.92' .planning/MILESTONES.md && test "$(ls .planning/phases/174-blast-radius-invariance-harness/evidence/*.txt | wc -l)" -ge 8 && test "$(ls .planning/phases/174-blast-radius-invariance-harness/174-0*-SUMMARY.md | wc -l)" -eq 5</automated>
    <fails_when>MILESTONES.md does not record the pre-existing-failure framing or the measured 740.92 second baseline, meaning the phase's own numbers are not auditable from the ledger section; fewer than eight evidence transcripts exist across the phase, meaning a plan closed without writing its record; or fewer than five plan summaries exist, meaning a plan closed without a summary.</fails_when>
  </verify>
  <done>The phase's comment hygiene, scope boundary, cost, pre-existing-failure record and dependency set are all measured and transcribed, and the ledger section carries the phase's own numbers where a later reader can audit them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI registration to gate verdict | A workflow whose triggers never match this milestone's branches is indistinguishable from no workflow. This is the boundary the whole plan exists to close. |
| meta repo to app submodule in CI | The workflow must obtain both trees. How it does so decides whether a checkout failure reads as a red gate or as a broken one. |
| phase claim to phase evidence | This phase's product is measurement discipline, so its own claims about cost, hygiene and scope are a boundary it has to cross with evidence rather than assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-22 | Repudiation | `.github/workflows/rekey-ledger-check.yml` triggers | high | mitigate | Both branch lists are parsed and asserted to contain `beta` and the milestone glob, and both event types are asserted present. The one registered meta workflow is `main`-scoped with a red history, so its triggers are explicitly not copied and its red runs are explicitly not treated as evidence. Acceptance is by local invocation with the workflow's own argument shape, never by the file existing. |
| T-174-23 | Denial of service | the workflow's cross-tree checkout | medium | mitigate | Explicit sub-repo checkout at a resolved ref, asserted by grep, with recursive submodule fetching asserted absent — the analog's own record shows submodule fetching re-armed a class of checkout failure that killed the job before any assertion ran, which would present as a failing gate rather than a broken one. |
| T-174-24 | Repudiation | the checker's failure mode inside CI | high | mitigate | The absent-checkout invocation is run locally and asserted to exit 2 with an `ERROR: ` line, so the workflow's worst case is fail-closed rather than a silent pass over a tree it could not read. |
| T-174-25 | Tampering | the phase's own scope boundary | medium | mitigate | Audit two asserts both submodules' relevant porcelain is empty and prints the actual changed-file list, so the phase-boundary claim is a measurement over the whole phase rather than a per-file assertion that could hold file by file while the phase drifts. |
| T-174-26 | Repudiation | the phase's own cost and hygiene claims | medium | mitigate | Audit one uses `/usr/bin/grep` explicitly, because the devcontainer's default honors ignore files and a sweep run with it reads clean while under-scanning. Audit three records a timed number rather than a judgement. Audit four re-runs the pre-existing failures and asserts their count and cause unchanged. |
| T-174-27 | Tampering | package metadata | medium | mitigate | Audit five diffs the app package metadata against the branch base and asserts an empty diff, so HYG-02's no-new-runtime-dependency rule is measured for this phase rather than assumed. |
| T-174-SC | Tampering | package-manager installs | medium | accept | No package is installed by this plan. The workflow installs nothing beyond the runner's default toolchain because the checker is standard library only; HYG-02 forbids a new runtime dependency milestone-wide, and audit five measures that; 174-RESEARCH.md's Package Legitimacy Audit records zero packages and zero flagged rows for this phase. |
</threat_model>

<verification>
Run from `/workspaces`:

1. `python3 tools/rekey/check_rekey_ledger.py --repo-root . --ledger firestarter_app/tests/fixtures/rekey_ledger.py --milestones .planning/MILESTONES.md` — exits 0 with one `OK:` line. This is the primary runner and it does not depend on CI.
2. The trigger parse in Task 1's verify reports `has_beta=True`, `has_gsd_glob=True`, `recursive_submodules=False` and `comment_lines=0`.
3. Task 2's closeout transcript reports seven zero-comment files, four shebang-only files, `md_openers=0`, `app_prod_porcelain=0`, `fw_porcelain=0`, `census_porcelain=0`, `deps_changed=0`, a timed `new_modules_seconds` value, and exactly three census failures with the timeout text.
4. Eight or more evidence transcripts and five plan summaries exist in the phase directory.

The whole app suite is not a criterion and must not be used as one: it is RED at the branch base for the three pre-existing suite-duration reasons audit four records.
</verification>

<success_criteria>
- The cross-tree ledger check has a named primary runner that works today and a registered workflow whose triggers match this milestone's branches, both proven by invocation.
- The workflow carries no comment, does not rely on recursive submodule fetching, and invokes the checker directly so the job conclusion is the gate verdict.
- The declared-re-key protocol — behaviour change first, re-key in its own commit, before-hash never overwritten — is stated in the ledger section for Phases 175 through 181.
- The phase's comment hygiene, scope boundary, cost, pre-existing-failure record and dependency set are all measured and transcribed rather than asserted.
- No production module, no generated database row and no firmware file changed.
</success_criteria>

## Artifacts this phase produces

New in the meta repo (paths relative to `/workspaces`):

- `.github/workflows/rekey-ledger-check.yml` — the additional CI leg. Triggers on `push` and `pull_request` with branch lists naming `beta` and the milestone-branch glob; path filters covering `.planning/MILESTONES.md`, `tools/rekey/`, the `firestarter_app` gitlink and its own filename; explicit meta and sub-repo checkout steps; one run step invoking `tools/rekey/check_rekey_ledger.py`.
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-05-checker-fires.txt`
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-05-phase-closeout.txt`

Extended in the meta repo:

- `.planning/MILESTONES.md` — the `### v1.36 Re-Key Ledger` section gains the declared-re-key protocol paragraph naming both runners, and a closing paragraph recording the phase's measured cost, the pre-existing failure count and cause, and the empty production and firmware change sets.

This plan creates no new symbol in either repository's source and modifies no source file.

<output>
Create `.planning/phases/174-blast-radius-invariance-harness/174-05-SUMMARY.md` when done. It must state plainly which runner is primary and why, quote the parsed trigger lists, and carry the five audits' numbers so a later phase can compare against them rather than re-deriving them.
</output>

