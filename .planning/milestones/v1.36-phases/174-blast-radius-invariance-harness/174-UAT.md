---
status: complete
phase: 174-blast-radius-invariance-harness
source: [174-VERIFICATION.md]
started: 2026-09-03T18:47:33Z
updated: 2026-09-04T07:32:22Z
---

## Current Test

[testing complete]

## Tests

### 1. GitHub Actions actually schedules `rekey-ledger-check.yml`
expected: `gh run list --workflow=rekey-ledger-check.yml --limit 5` shows at least one run with a conclusion, after the phase's commits reach `beta` or a `gsd/**` branch. No network path exists in this sandbox to make GitHub Actions schedule a run; the workflow file, its trigger branches and its invocation of the now-fixed checker were all re-confirmed by direct reading. Carried forward unchanged from the initial verification.
result: pass
evidence: "Operator authorised the push. Pushed firestarter_app gsd/v1.36-dev-test-fidelity (gitlink e907e6d1, 15 commits) then meta gsd/v1.36-dev-test-fidelity (a11e89f0). GitHub Actions scheduled run 33804720664 on trigger=push, branch=gsd/v1.36-dev-test-fidelity; conclusion=success in 14s. Run log confirms the checker itself executed and emitted 'OK: 6 ledger row(s), 6 MILESTONES.md row(s) bound' — not a vacuous green."
verified_at: 2026-09-03T20:52:44Z

### 2. Post-gap-closure code review findings are unactioned — decide fix-now vs track-forward
expected: A decision on the four carried-forward findings from `174-REVIEW.md` (commit `b954d7cd`) — `db_diff` cache aliasing (CR-01), app-side duplicate-`ledger_id` diagnostic asymmetry (WR-02), 1-of-16 snapshot-drift coverage (WR-01), and the three lower-severity items — either closed before Phase 177 (the first consumer phase) via a 174-07 gap-closure-style plan, or explicitly accepted and tracked for a later phase. The verifier's adversarial testing found none of them defeats a roadmap success criterion today; this is a risk-acceptance call for Phase 177+, not a gap.
result: pass
reported: "fix CR-01 + WR-01"
severity: major
decided_at: 2026-09-03T20:56:56Z
resolution: "Decision made and the chosen work landed. CR-01 and WR-01 fixed in firestarter_app 7842357 (RED legs, 6 failed / 82 passed -- exactly the six functools.cache-decorated shape ids) and c134530 (fix, 88 passed). snapshot_report_shapes.py --check clean on all 16; no FROZEN_HASHES, LADDER_PINS or committed snapshot moved. Re-key ledger CI green on the bumped gitlink (run 33813986978). WR-02, WR-03, WR-04, IN-01 and IN-02 explicitly accepted and recorded under Deferred Follow-Ups."
resolved_at: 2026-09-04T07:32:22Z

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-174-2
  truth: "The post-gap-closure review's two coupled findings are closed before Phase 177 consumes the harness"
  status: resolved
  reason: "User decided: fix CR-01 + WR-01 now; WR-02, WR-03, WR-04, IN-01, IN-02 explicitly accepted and tracked forward."
  severity: major
  resolved_by: "direct fix at operator request (no gap-closure plan); firestarter_app commits 7842357 (RED legs) and c134530 (fix)"
  resolved_at: 2026-09-03T22:37:27Z
  test: 2
  root_cause: "Two coupled defects. CR-01: `render_shape()` (tools/snapshot_report_shapes.py:92) and `_to_dict_with_db_diff()` (tests/test_blast_radius_invariance.py:404) assign `report.db_diff = build_db_diff(...)` directly onto the object `build_shape()` returns. Six of sixteen builders in tests/fixtures/report_shapes.py are `@functools.cache`-decorated, so that object is shared across every call for those ids; the assignment permanently mutates the cached instance. This is the same aliasing class 174-06's `_clone_with_chip_override` deepcopy fix closed for `.results`/`.plan`, via a second path the fix did not cover. WR-01: `test_committed_snapshot_matches_a_fresh_regeneration` (tests/test_blast_radius_invariance.py:561) regenerates exactly one shape (`_TRACER_SHAPE_ID`, the uncached `sst27sf512-six-step`); the other 15 committed snapshots are checked only for filename-stem existence, never byte-identical content. The two are coupled: parametrizing the drift assertion over all 16 shapes routes it through the 6 cached ids CR-01 corrupts, making the fresh-vs-committed comparison order-dependent unless CR-01 lands first."
  artifacts:
    - path: "firestarter_app/tools/snapshot_report_shapes.py"
      issue: "render_shape() line 92 mutates a cached DiagnosticReport's db_diff in place"
    - path: "firestarter_app/tests/test_blast_radius_invariance.py"
      issue: "_to_dict_with_db_diff() line 404 same in-place mutation; test_committed_snapshot_matches_a_fresh_regeneration line 561 covers 1 of 16 shapes"
    - path: "firestarter_app/tests/fixtures/report_shapes.py"
      issue: "6 @functools.cache builders (lines 507, 518, 556, 566, 579, 590) make the returned report a shared instance"
  missing:
    - "CR-01: build off a copy at both call sites so build_shape()'s cached object is never assigned onto; mirror the existing _clone_with_chip_override deepcopy idiom"
    - "CR-01: a regression test asserting db_diff is None on a fresh build_shape() for a cached id AFTER render_shape() has run in-process (the ordering that currently masks the bug)"
    - "WR-01: parametrize the snapshot-drift assertion over all of SHAPE_IDS, not just the tracer, so all 16 committed snapshots are byte-compared against a fresh regeneration in pytest"
  evidence: "CR-01 reproduced live at HEAD a11e89f0 via the tool's own import path (tests.fixtures.report_shapes): bare build_shape('m27c512-full-all-ok').db_diff is None; after one render_shape() call in-process, a second build_shape() returns the same object (r1 is r2 True) with db_diff now a populated DbDiff. Verdict: LEAKED."
  debug_session: ""

## Deferred Follow-Ups

- test: 2
  idea: "WR-02 (check_rekey_ledger.py:129 app-side duplicate ledger_id collapses silently, asymmetric with the MILESTONES-side guard), WR-03 (_solve_row picks first matching candidate without ambiguity detection), WR-04 (module-level assert strippable under python -O), IN-01 (gh issue list --limit 300 unchecked truncation), IN-02 (undocumented last-wins JSON block selection) — explicitly ACCEPTED by the operator and tracked forward, not fixed in 174. Rationale: the verifier could not construct a case where any defeats a roadmap success criterion, and WR-02 is independently backstopped by the passing test_ledger_id_values_are_unique."
  deferred_at: 2026-09-03T20:56:56Z

