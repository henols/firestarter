---
phase: 174-blast-radius-invariance-harness
plan: 02
type: execute
wave: 2
depends_on: [174-01]
files_modified:
  - firestarter_app/tests/fixtures/report_shapes.py
  - firestarter_app/tests/fixtures/reports/sst27sf512-six-step-readback-gated.json
  - firestarter_app/tests/fixtures/reports/gh47-sst27sf512-pass.json
  - firestarter_app/tests/fixtures/reports/gh28-m27c512-fail.json
  - firestarter_app/tests/fixtures/reports/gh20-at28c256-fail.json
  - firestarter_app/tests/fixtures/reports/gh23-w27e257-fail.json
  - firestarter_app/tests/fixtures/reports/synthetic-arm4-no-ok.json
  - firestarter_app/tests/fixtures/reports/synthetic-arm4-empty-results.json
  - firestarter_app/tests/fixtures/reports/m27c512-full-all-ok.json
  - firestarter_app/tests/fixtures/reports/m27c512-full-blank-check-bad.json
  - firestarter_app/tests/fixtures/reports/m27c512-full-canonical-name.json
  - firestarter_app/tests/fixtures/reports/m27c512-full-comma-joined-name.json
  - firestarter_app/tests/fixtures/reports/m27c512-full-runs-1.json
  - firestarter_app/tests/fixtures/reports/at28c256-full-all-ok-sdp.json
  - firestarter_app/tests/fixtures/reports/sst27sf512-full-all-ok.json
  - firestarter_app/tests/fixtures/reports/w27e257-full-all-ok.json
  - firestarter_app/tests/test_blast_radius_invariance.py
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-02-frozen-table.txt
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-02-ladder-arms.txt
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The frozen table holds sixteen shape_id rows across two separate tables — eight hand-specified rows that pin the hash function and are immune to database regeneration, and eight rows built from real derive_plan and run_plan output that pin the shapes research measured — per D-02, so a generator fix in a later milestone reddens only the second table and that is a signal rather than noise."
    - "The four re-key paths D-05 names each have a row a named later phase will be measured against: read-back gating on sst27sf512-six-step, SDP-step pruning on m27c512-full-all-ok, canonical naming on m27c512-full-canonical-name, and the UV blank-check abort on m27c512-full-blank-check-bad."
    - "Builder shapes reproduce the filed twelve-hex hash for all four chips D-06 names: gh47-sst27sf512-pass returns f9dbc31dcd27, gh28-m27c512-fail returns 31547956e56b, gh20-at28c256-fail returns 00e121446ceb, and gh23-w27e257-fail returns 7a89fcea856a — each taken off a real DiagnosticReport by the real dedup_fingerprint, with the step vector hand-transcribed from the issue body rather than parsed at runtime, so no deserializer is introduced."
    - "All four build_db_diff disposition branches are pinned with their (proposed_disposition, ladder_state) pair, and the all-OK arm is reached by a non-SDP chip: sst27sf512-full-all-ok and w27e257-full-all-ok both land on community-reported, while at28c256-full-all-ok-sdp lands on the empty ladder_state because its SDP leg attaches indeterminate fingerprints in every arm — per D-08, whose blind spot is thereby measured rather than assumed."
    - "The UV run_count collapse row names the mechanism that actually operates: the collapse moves the hash from 6d3afbc52315 to 077a32d1a5c4 through the blank-check verdict triple, and repeat_policy_tag stays empty because the collapsed write and verify steps carry a run_count of zero rather than one."
    - "The m27c512-full-runs-1 shape is the only row whose fingerprint carries the degraded repeat marker, and it gets there by the real repeat_policy_tag firing on a stamped run_count of one — never by a tag string appended by the builder."
    - "The at28c256 all-OK shape is built with a stateful operator double that maintains a chip image and honours SDP semantics, not with the fixed-return double, because the fixed-return double's read-back oracle reports every SDP leg step BAD and would silently reduce an all-OK assertion to a false negative."
    - "No shape in either table is frozen from a hash this plan did not compute: every one of the sixteen literals was recomputed against the branch base in the same commit that adds its builder."
    - "Each of the sixteen shapes has a committed to_dict() output snapshot under tests/fixtures/reports/ named for its shape_id, generated by tools/snapshot_report_shapes.py and byte-identical to a fresh regeneration — per D-01 and GATE-05."
  artifacts:
    - path: firestarter_app/tests/fixtures/report_shapes.py
      provides: "All sixteen builders, the full SHAPE_IDS registry, FROZEN_HASHES, LADDER_PINS and the stateful SDP-aware operator double"
      contains: "at28c256-full-all-ok-sdp"
    - path: firestarter_app/tests/fixtures/reports/m27c512-full-all-ok.json
      provides: "Committed to_dict() snapshot for the real-path m27c512 all-OK shape"
    - path: firestarter_app/tests/fixtures/reports/sst27sf512-full-all-ok.json
      provides: "Committed to_dict() snapshot for the non-SDP all-OK shape that reaches the community-reported arm"
    - path: firestarter_app/tests/test_blast_radius_invariance.py
      provides: "The parametrized frozen-hash table over all sixteen shapes and the four-arm ladder pin"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-02-frozen-table.txt
      provides: "Computed fingerprint beside frozen literal for all sixteen shapes"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-02-ladder-arms.txt
      provides: "Measured disposition and ladder pair for every shape, showing all four arms reached"
  key_links:
    - from: firestarter_app/tests/fixtures/report_shapes.py
      to: firestarter_app/firestarter/chip_test.py
      via: "derive_plan and run_plan build the real-path table; repeat_policy_tag and coverage_tag are the only source of a tag suffix"
      pattern: "from firestarter.chip_test import"
    - from: firestarter_app/tests/fixtures/report_shapes.py
      to: firestarter_app/firestarter/diagnostic_report.py
      via: "build_db_diff supplies each shape's pinned disposition and ladder pair"
      pattern: "build_db_diff"
    - from: firestarter_app/tests/test_blast_radius_invariance.py
      to: firestarter_app/tests/fixtures/reports/
      via: "one committed snapshot per shape_id, compared element-wise against a fresh normalised to_dict()"
      pattern: "reports"
  prohibitions:
    - statement: "No frozen hash is taken from a shape built with the fixed-return operator double where that double cannot reach the step the shape claims to exercise. A hash that encodes a mock artifact is worse than no hash, because it reads green forever."
      status: required
      verification: judgment
    - statement: "No tag suffix is appended to a canonical string by a builder. The repeat-policy and coverage discriminators must come out of the production functions that compute them, or the frozen value pins the fixture rather than the product."
      status: required
      verification: judgment
---

<objective>
Expand the one-shape spine into the full sixteen-shape frozen table: eight hand-specified rows that pin
the hash function itself, and eight rows built through the real `derive_plan` and `run_plan` path that pin
the shapes measurement actually found.

Purpose: the tracer proved the architecture on one shape. This plan is what makes the oracle cover the
milestone — every one of the four measured re-key paths, every one of `build_db_diff`'s four disposition
arms including the non-SDP all-OK arm that AT28C256 cannot reach, and the four filed community hashes
D-06 names. Nothing here is new architecture; it is breadth over a proven spine.

Output: sixteen builders, sixteen committed snapshots, one parametrized frozen table, one four-arm ladder
pin, and two evidence transcripts showing computed beside frozen for every row.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/174-blast-radius-invariance-harness/174-CONTEXT.md
@.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md
@.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md
@.planning/phases/174-blast-radius-invariance-harness/174-01-SUMMARY.md
@firestarter_app/CLAUDE.md
</context>

<environment_preamble>
Same as plan 174-01: every command runs from `/workspaces`, every Python invocation goes through
`firestarter_app/.venv311/bin/python` (Python 3.11.16, pytest 9.1.1, verified this planning session), and
`/usr/bin/grep` is used for all gate evidence because the devcontainer's `grep` is ugrep and honors
`.gitignore`.

`pytest` is invoked as `-o addopts="" -q -p no:randomly` so exactly one level of quiet applies and the
`N passed` summary lands at the start of a line.

The three pre-existing `tests/test_skip_census.py` failures at the branch base are not this phase's and
must not be fixed as collateral. No command in this plan collects that file, and no command in this plan
uses the whole suite as a criterion.

Zero comments in every file this plan writes — not one `#` line, and no HTML comment in any Markdown.
All reasoning in docstrings. The `# Source:` lines in 174-RESEARCH.md and 174-PATTERNS.md are quotations
of existing code and must not be copied through.

App-side files are committed inside the `firestarter_app` submodule on `gsd/v1.36-dev-test-fidelity`; the
gitlink and the evidence files are committed in the meta repo.
</environment_preamble>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The hand-specified table — the four filed community hashes, the read-back-gated pair half, and the two arm-4 synthetics</name>
  <files>firestarter_app/tests/fixtures/report_shapes.py, firestarter_app/tests/fixtures/reports/sst27sf512-six-step-readback-gated.json, firestarter_app/tests/fixtures/reports/gh47-sst27sf512-pass.json, firestarter_app/tests/fixtures/reports/gh28-m27c512-fail.json, firestarter_app/tests/fixtures/reports/gh20-at28c256-fail.json, firestarter_app/tests/fixtures/reports/gh23-w27e257-fail.json, firestarter_app/tests/fixtures/reports/synthetic-arm4-no-ok.json, firestarter_app/tests/fixtures/reports/synthetic-arm4-empty-results.json, firestarter_app/tests/test_blast_radius_invariance.py</files>
  <read_first>
    - `firestarter_app/tests/fixtures/report_shapes.py` as plan 174-01 left it — the `build_shape_from_step_specs` signature, the `run_counts` and `coverage_policy` stamping seams, and the existing `sst27sf512-six-step` builder this task extends
    - `firestarter_app/tests/test_blast_radius_invariance.py` as plan 174-01 left it — the parametrized frozen-hash test, the twelve-hex contract test, and the module docstring's anti-vacuity and Reachability blocks
    - `firestarter_app/firestarter/diagnostic_report.py` lines 186 to 245 (`dedup_fingerprint`, and the two conditional tag appends at the end of `parts`) and lines 287 to 322 (`build_db_diff`'s four arms in order)
    - `firestarter_app/firestarter/chip_test.py` lines 1060 to 1130 (`_REPEAT_POLICY_OPS`, `REPEAT_POLICY_DEGRADED_TAG`, `repeat_policy_tag`, `COVERAGE_TAG_FULL_DEVICE`, `coverage_tag`) and lines 1022 to 1060 (the `StepResult` dataclass, whose `run_count` defaults to 0)
    - `firestarter_app/tests/test_diagnostic_report.py` lines 1311 to 1338 (`_coverage_report` — the post-construction `write_target` stamping seam and the docstring that licenses direct construction for a shape production cannot currently produce)
    - `.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md` sections "Re-key pair 1", "GATE-04 — raw-CLI-token to part_number delta" (its per-issue table, for the twelve-hex values and raw tokens), "D-06's four reproduction targets", and "The four ladder arms (measured)"
  </read_first>
  <behavior>
    - `sst27sf512-six-step-readback-gated` hashes to `60a031573aab`: the same six steps as the tracer shape with both `indeterminate` classifications emptied.
    - `gh47-sst27sf512-pass` hashes to `f9dbc31dcd27`.
    - `gh28-m27c512-fail` hashes to `31547956e56b`.
    - `gh20-at28c256-fail` hashes to `00e121446ceb`.
    - `gh23-w27e257-fail` hashes to `7a89fcea856a`.
    - `synthetic-arm4-no-ok` hashes to `f90dfe1a44f7` and reaches `build_db_diff`'s fourth arm.
    - `synthetic-arm4-empty-results` hashes to `8d6208d00be7` and reaches the same arm with an empty results list.
    - `00e121446ceb` is the fingerprint three filed issues share (gh#20, gh#21, gh#32) — one builder reproduces the identity of a real three-member dedup group, so a re-key of it is visibly a reset of that group's agreement count.
  </behavior>
  <action>
Extend `SHAPE_IDS` and `FROZEN_HASHES` with seven more hand-specified rows. Every one is directly
constructed through `build_shape_from_step_specs`, not through `derive_plan`, and the module docstring
must carry the reason — reuse the argument at `tests/test_diagnostic_report.py:1312-1326`: direct
construction is legitimate for a shape the current engine has no live path to produce.

`sst27sf512-six-step-readback-gated` at `60a031573aab`: `chip="SST27SF512"`, `protocol="7"`, the same six
ops in the same order as the tracer shape, with the write and verify classifications changed from
`indeterminate` to `None`. Freezing both halves of the pair means Phase 177's target is pinned from both
sides, so a change that lands on neither value is as visible as one that lands on the wrong one.

The four filed-community shapes. Their step vectors are hand-transcribed from the four issue bodies in
`henols/firestarter_prom` — transcription into a committed builder, not runtime parsing, so D-01's ban on
a deserializer is respected. Note the load-bearing detail: `steps[].fingerprint` serialises as a bare
classification string, not as an object, so a transcription that treats it as an object silently drops
every classification and the hash will not reproduce. The four vectors, verbatim:

`gh47-sst27sf512-pass`, hash `f9dbc31dcd27`, `chip="sst27sf512"`, `protocol="7"`, twelve steps:
`("id","OK",None,"")`, `("read","OK",None,"")`, `("write","OK","indeterminate","")`,
`("verify","OK","indeterminate","")`, `("erase","OK",None,"")`, `("blank-check","OK",None,"")`, then
`("write-baseline-b","NA",None,"")`, `("write-baseline-a","NA",None,"")`, `("sdp-lock","NA",None,"")`,
`("write-inhibited","NA",None,"")`, `("sdp-unlock","NA",None,"")`, `("write-restored","NA",None,"")`.
This one needs BOTH tags: pass `run_counts` setting `id`, `read`, `write`, `verify`, `erase` and
`blank-check` to 1 and every SDP-leg op to 0 so the real `repeat_policy_tag` emits the degraded marker,
and pass `coverage_policy` as the full-device region policy so the real `coverage_tag` emits its marker.
Do not append either tag as a string.

`gh28-m27c512-fail`, hash `31547956e56b`, `chip="m27c512"`, `protocol="7"`, six steps:
`("id","OK",None,"")`, `("read","OK",None,"")`, `("blank-check","OK",None,"")`,
`("write","BAD","indeterminate","")`, `("verify","BAD","indeterminate","")`, `("erase","NA",None,"")`.
No tags: this report was filed under schema 1.2 by host 3.0.0b15, before either discriminator existed.

`gh20-at28c256-fail`, hash `00e121446ceb`, `chip="at28c256"`, `protocol="13"`, six steps:
`("id","NA",None,"")`, `("read","OK",None,"")`, `("blank-check","BAD",None,"")`,
`("write","BAD","indeterminate","")`, `("verify","BAD","indeterminate","")`, `("erase","NA",None,"")`.
No tags. Record in the builder's docstring that this fingerprint is shared by gh#20, gh#21 and gh#32 —
a real three-member dedup group — and that `count_agreeing` reads the embedded hash and never re-hashes,
so a re-key resets that group's promotion count permanently.

`gh23-w27e257-fail`, hash `7a89fcea856a`, `chip="w27e257"`, `protocol="7"`, six steps:
`("id","OK",None,"")`, `("read","OK",None,"")`, `("blank-check","OK",None,"")`,
`("write","BAD","blank/contact","")`, `("verify","BAD","blank/contact","")`, `("erase","OK",None,"")`.
No tags. Note the classification here is the `blank/contact` bucket, not `indeterminate` — the two
`BAD` steps carry a different classification from gh#28's and gh#20's, and transcribing the wrong one is
the single most likely way this row fails to reproduce.

The two arm-4 synthetics. `build_db_diff`'s fourth arm is the fallback that fires when no step reports OK,
and no real `derive_plan` output reaches it — measurement found zero of 2031 plans with every step
unsupported — so a synthetic shape is the only way to pin it, and the `shape_id` prefix says so.
`synthetic-arm4-no-ok` at `f90dfe1a44f7`: `chip="M8720"`, `protocol="0x08"`, two steps
`("read","NA",None,"")` and `("write","SKIPPED",None,"")`, no tags.
`synthetic-arm4-empty-results` at `8d6208d00be7`: the same chip and protocol with an empty `step_specs`
list. Since `build_db_diff` reads `support_status` off the chip name and derives disposition and ladder
purely from the results list, call it for these two shapes with a chip name the shipped database
actually carries — use `m27c512` — while keeping the synthetic results. Record that in the docstring so
nobody later reads the name mismatch as a bug.

Generate the seven new snapshots by running `tools/snapshot_report_shapes.py` and commit them verbatim.
Do not hand-write a snapshot.

No test-module change is needed beyond what the parametrization already gives you: the frozen-hash test
iterates `sorted(FROZEN_HASHES.items())`, so seven new rows are seven new parametrized cases
automatically. Add one dedicated test asserting the gh#20 shape's fingerprint is the shared
`00e121446ceb` with a docstring naming the three issues, because that identity is the concrete form of
"a re-key forks history" and deserves to fail by name rather than as row eleven of a table.
  </action>
  <acceptance_criteria>
    - `SHAPE_IDS` has exactly eight members after this task, and `FROZEN_HASHES` has the same eight keys.
    - `dedup_fingerprint(build_shape(sid))` equals the frozen literal for each of: `sst27sf512-six-step-readback-gated` and `60a031573aab`; `gh47-sst27sf512-pass` and `f9dbc31dcd27`; `gh28-m27c512-fail` and `31547956e56b`; `gh20-at28c256-fail` and `00e121446ceb`; `gh23-w27e257-fail` and `7a89fcea856a`; `synthetic-arm4-no-ok` and `f90dfe1a44f7`; `synthetic-arm4-empty-results` and `8d6208d00be7`.
    - `build_db_diff` for both `synthetic-arm4-*` shapes returns `proposed_disposition == "no change suggested (advisory)"` and `ladder_state == ""`.
    - Seven new files exist under `firestarter_app/tests/fixtures/reports/`, one per new shape_id, each carrying the `_generated_by` key and the normalised `generated` sentinel.
    - `tools/snapshot_report_shapes.py --check` exits 0 across all eight shapes.
    - `firestarter_app/tests/fixtures/report_shapes.py` carries zero comment lines.
    - `git -C firestarter_app status --porcelain firestarter/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && mkdir -p "$E" && EV=$E/174-02-frozen-table.txt && { echo "== computed beside frozen, table 1"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.fixtures.report_shapes import build_shape, FROZEN_HASHES, SHAPE_IDS; from firestarter.diagnostic_report import dedup_fingerprint; print('n_shapes='+str(len(SHAPE_IDS))); [print(sid+' frozen='+FROZEN_HASHES[sid]+' computed='+dedup_fingerprint(build_shape(sid))) for sid in sorted(SHAPE_IDS)]"); echo "rc=$?"; echo "== snapshot check"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; echo "== the invariance module"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 3 && /usr/bin/grep -qx 'n_shapes=8' "$EV" && test "$(/usr/bin/grep -cE ' frozen=[0-9a-f]{12} computed=[0-9a-f]{12}$' "$EV")" -eq 8 && test "$(/usr/bin/grep -cE ' frozen=([0-9a-f]{12}) computed=\1$' "$EV")" -eq 8 && /usr/bin/grep -qx 'sst27sf512-six-step-readback-gated frozen=60a031573aab computed=60a031573aab' "$EV" && /usr/bin/grep -qx 'gh47-sst27sf512-pass frozen=f9dbc31dcd27 computed=f9dbc31dcd27' "$EV" && /usr/bin/grep -qx 'gh28-m27c512-fail frozen=31547956e56b computed=31547956e56b' "$EV" && /usr/bin/grep -qx 'gh20-at28c256-fail frozen=00e121446ceb computed=00e121446ceb' "$EV" && /usr/bin/grep -qx 'gh23-w27e257-fail frozen=7a89fcea856a computed=7a89fcea856a' "$EV" && /usr/bin/grep -qx 'synthetic-arm4-no-ok frozen=f90dfe1a44f7 computed=f90dfe1a44f7' "$EV" && /usr/bin/grep -qx 'synthetic-arm4-empty-results frozen=8d6208d00be7 computed=8d6208d00be7' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error|skipped)' "$EV" && test "$(ls firestarter_app/tests/fixtures/reports/*.json | wc -l)" -eq 8</automated>
    <fails_when>The transcript does not read `n_shapes=8`; fewer than eight rows carry both a frozen and a computed twelve-hex value; any row's computed value differs from its frozen value (the back-referenced count drops below eight); any of the seven named exact lines is absent, which means that specific transcription did not reproduce its filed hash; the snapshot check exits non-zero; the pytest leg reports any failed, error or skipped count or lacks a passed count; or the reports directory holds a number of JSON files other than eight.</fails_when>
    <automated>cd /workspaces && (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.fixtures.report_shapes import build_shape; from firestarter.diagnostic_report import build_db_diff; from firestarter.database import EpromDatabase; db=EpromDatabase(skip_local_override=True); [print(sid+' -> '+repr((build_db_diff('m27c512', db, build_shape(sid).results).proposed_disposition, build_db_diff('m27c512', db, build_shape(sid).results).ladder_state))) for sid in ('synthetic-arm4-no-ok','synthetic-arm4-empty-results')]") | tee /dev/stderr | test "$(/usr/bin/grep -c "-> ('no change suggested (advisory)', '')")" -eq 2 && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/fixtures/report_shapes.py)" -eq 0 && test -z "$(git -C firestarter_app status --porcelain firestarter/)"</automated>
    <fails_when>Either synthetic shape returns a disposition or ladder pair other than the fallback pair, so fewer than two matching lines are counted; the fixture module reports a non-zero comment-line count; or the production package shows as modified.</fails_when>
  </verify>
  <done>Eight hand-specified shapes are frozen against literals this task computed, the four filed community hashes reproduce byte-exactly from hand-transcribed step vectors, the arm-4 fallback is pinned by two synthetics, and eight committed snapshots regenerate byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The real-path table — eight derive_plan shapes, the stateful SDP-aware double, and all four ladder arms measured</name>
  <files>firestarter_app/tests/fixtures/report_shapes.py, firestarter_app/tests/fixtures/reports/m27c512-full-all-ok.json, firestarter_app/tests/fixtures/reports/m27c512-full-blank-check-bad.json, firestarter_app/tests/fixtures/reports/m27c512-full-canonical-name.json, firestarter_app/tests/fixtures/reports/m27c512-full-comma-joined-name.json, firestarter_app/tests/fixtures/reports/m27c512-full-runs-1.json, firestarter_app/tests/fixtures/reports/at28c256-full-all-ok-sdp.json, firestarter_app/tests/fixtures/reports/sst27sf512-full-all-ok.json, firestarter_app/tests/fixtures/reports/w27e257-full-all-ok.json, firestarter_app/tests/test_blast_radius_invariance.py, .planning/phases/174-blast-radius-invariance-harness/evidence/174-02-ladder-arms.txt</files>
  <read_first>
    - `firestarter_app/tests/fixtures/report_shapes.py` as Task 1 left it
    - `firestarter_app/firestarter/chip_test.py` lines 486 to 520 (`derive_plan(name, db, *, write_scope="none")`) and lines 1582 to 1620 (`run_plan(plan, operator, db, *, runs=2, allow_single_run=False, sampler=None)` and its non-fatal-step contract)
    - `firestarter_app/firestarter/cli_handlers.py` lines 2374 to 2431 — the SOLE production `DiagnosticReport` construction site. The real-path builders mirror this call sequence exactly: `chip` is the raw CLI token, and `protocol` is `str(prog["algorithm"])` off `db.convert_to_programmer(db.get_eprom(chip))`
    - `firestarter_app/tests/test_chip_test.py` lines 1009 to 1023 (`_mock_operator` — the fixed-return double and the `_OPERATOR_METHODS` spec it is built against) and lines 1025 to 1104 (`_sdp_leg_readback_operator` — the stateful, SDP-lock-aware double this task must model its own on, including its docstring's statement of exactly what the fixed-return double gets wrong)
    - `firestarter_app/tests/test_erase_flag_invariants.py` lines 96 to 98 (the shared deterministic database instance) and lines 108 to 131 (the two-level `db.proms` descent, for reference — a top-level scan iterates manufacturer keys and passes vacuously)
    - `firestarter_app/firestarter/diagnostic_report.py` lines 246 to 265 (the four `_DISPOSITION_*` constants and the four `_LADDER_*` constants, by name) and lines 287 to 322 (`build_db_diff`)
    - `firestarter_app/tests/test_diagnostic_report.py` lines 715 to 750 (`test_ladder_state_verdict_mapping` — it already pins all four `ladder_state` values absolutely against the module constants; do not duplicate those assertions, and copy the triple-equality idiom at `:734`)
    - `.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md` sections "Re-key pairs 2 and 3", "Re-key path 4", "GATE-03 — the four build_db_diff disposition branches, pinned", "Cost measurements", "Pitfall 2", "Pitfall 4", "Pitfall 5"
  </read_first>
  <behavior>
    - `m27c512-full-all-ok` hashes to `6d3afbc52315`, with canonical pre-image `m27c512|7|id=OK:|read=OK:|blank-check=OK:|write=SKIPPED:|verify=SKIPPED:|erase=NA:` followed by six `NA:` SDP-leg steps.
    - `m27c512-full-blank-check-bad` hashes to `077a32d1a5c4` and reaches `build_db_diff`'s first arm.
    - `m27c512-full-canonical-name` hashes to `776846bf2dc8` — the same shape with the chip token spelled `M27C512`.
    - `m27c512-full-comma-joined-name` hashes to `37ad34d39a19` — the same shape with the chip token spelled `M27C512,M27V512`.
    - `m27c512-full-runs-1` hashes to `e4838f7bb1d3`, and it is the only shape whose canonical string carries the degraded repeat marker.
    - `at28c256-full-all-ok-sdp` hashes to `52fb759dc48c` and lands on `build_db_diff`'s SECOND arm with an empty ladder state, even though every step is OK.
    - `sst27sf512-full-all-ok` hashes to `4b3e52cab987` and `w27e257-full-all-ok` hashes to `22908e2954c3`; both land on the third arm with ladder state `community-reported`.
    - Across the sixteen shapes, all four disposition and ladder pairs appear at least once.
    - The whole real-path table builds in well under ten seconds; a `derive_plan` sweep of the entire database costs 4.26 s, which is 0.58 percent of the measured 740.92 s suite, so nothing here needs a slow marker.
  </behavior>
  <action>
Add the eight real-path builders. Each one mirrors `cli_handlers.py:2374-2431`: call
`derive_plan(chip, db, write_scope=...)`, then `run_plan(plan, operator_double, db, runs=...)`, then
construct `AutoCapture(host_version=..., chip=<raw token>, protocol=str(prog["algorithm"]))` and the
`DiagnosticReport`. Read `prog` from `db.convert_to_programmer(db.get_eprom(chip))`. Use the module-level
`EpromDatabase(skip_local_override=True)`. Measured protocol values: `"7"` for m27c512, sst27sf512 and
w27e257; `"13"` for at28c256.

Use a module-scoped pytest fixture (or a module-level cached construction) so `derive_plan` and
`run_plan` are paid once per shape rather than once per parametrized case. The cost budget is settled by
measurement and needs no marker: a `derive_plan` sweep across all 677 distinct part numbers times three
scopes is 2031 plans in 4.26 s, which is 0.58 percent of the 740.92 s suite. Marking this slow would
recreate the gate-that-never-runs failure mode, which is the one thing this phase exists to prevent.

The five m27c512 rows, all `write_scope="full"`:
- `m27c512-full-all-ok` at `6d3afbc52315`, `runs=2`, fixed-return double, correct chip id. Its write and
  verify steps land as `SKIPPED` because m27c512 is a UV part and no slot satisfies the monotonicity
  witness under that double. That is the honest measured shape and the docstring must say so rather than
  imply a write occurred.
- `m27c512-full-blank-check-bad` at `077a32d1a5c4`, the same shape with the blank-check operator call
  returning a BAD outcome. This is the UV `run_count` collapse row, and the docstring must state the
  mechanism correctly: the collapse moves the hash through the `blank-check` verdict triple, NOT through
  `repeat_policy_tag`. The tag stays empty because the collapsed write and verify steps carry a
  `run_count` of zero, and `repeat_policy_tag` fires only on a `run_count` of exactly one. CONTEXT.md
  D-12 row 4 and the milestone research both name `repeat_policy_tag` as the mechanism; measurement
  found it does not fire, and the docstring records that correction so Phase 179 is measured against the
  mechanism that actually operates.
- `m27c512-full-canonical-name` at `776846bf2dc8` and `m27c512-full-comma-joined-name` at `37ad34d39a19`:
  the all-OK shape with `auto_capture.chip` overridden to `M27C512` and to `M27C512,M27V512`
  respectively. These are D-2's rejected canonical-naming alternative, frozen so a later phase that
  accidentally normalises `parts[0]` reddens against a row that already names the consequence. Their
  docstrings must record that these two pairs REPLACE CONTEXT.md D-12's inherited
  `a00791f1c2b4` to `a6f6c6354047` pair, and why: neither inherited value reproduces from any m27c512
  report shape, across roughly 2.1e8 candidate canonical strings, so both are unverified priors and
  neither may be frozen.
- `m27c512-full-runs-1` at `e4838f7bb1d3`: the all-OK shape run with `runs=1`, so the real
  `repeat_policy_tag` fires on the read step's `run_count` of one and appends the degraded marker. The
  marker must come from the production function, never from a builder-appended string.

The three ladder-arm rows plus the SDP row:
- `at28c256-full-all-ok-sdp` at `52fb759dc48c`, `write_scope="full"`, `runs=2`, built with a NEW stateful
  operator double in this fixture module, modelled on `_sdp_leg_readback_operator`
  (`tests/test_chip_test.py:1025`). Copy its design: an in-memory chip image, a `locked` flag, a
  `write_eprom` side effect that genuinely refuses a write carrying the skip-SDP-unlock flag while the
  chip is locked, a `read_eprom` that writes the held image to the output file, a `verify_eprom` that
  compares against the held image, and `sdp_lock`/`sdp_unlock` that flip the flag. The fixed-return
  double must NOT be used here: its `read_eprom` returns success while writing no file, so the SDP leg's
  read-back-equality oracle sees an empty read-back and reports all six leg steps BAD, silently reducing
  an all-OK assertion to a false negative that still reads green. Define the double in
  `tests/fixtures/report_shapes.py` rather than importing it from `tests/test_chip_test.py`, so D-03's
  contract holds — Phase 181 must be able to import shapes without pulling a large test module in.
  Record in the docstring what this shape proves: a genuinely all-OK AT28C256 run lands on
  `build_db_diff`'s SECOND arm with an empty ladder state, because its SDP leg attaches `indeterminate`
  fingerprints in every arm. That independently reproduces the open ladder-regression finding and it is
  exactly the blind spot D-08 forbids the harness from having.
- `sst27sf512-full-all-ok` at `4b3e52cab987` and `w27e257-full-all-ok` at `22908e2954c3`,
  `write_scope="full"`, `runs=2`, fixed-return double. These are the non-SDP all-OK shapes D-08 requires,
  and both reach the third arm with ladder state `community-reported`. Without at least one of them the
  harness cannot see the D-4/D-6 ladder flip at all.

Add the four-arm ladder pin to `tests/test_blast_radius_invariance.py`. Declare a module-level
`LADDER_PINS` mapping each shape_id to its expected `(proposed_disposition, ladder_state)` pair, one
entry per line, and parametrize over `sorted(LADDER_PINS.items())`. Assert the disposition against the
imported private constant AND the literal in one triple-equality expression, the idiom at
`tests/test_diagnostic_report.py:734`, so a constant rename and a value change are both caught.
The four expected pairs, measured:
first arm `("suggests: community-fail signal (advisory -- human triage required)", "community-fail")`;
second arm `("inconclusive -- needs N>=2 agreement (advisory)", "")`;
third arm `("suggests: candidate for community-reported (advisory)", "community-reported")`;
fourth arm `("no change suggested (advisory)", "")`.
Do not restate the four ladder-state values on their own — `test_ladder_state_verdict_mapping` already
pins those absolutely; the new work here is the disposition text and the shape_id binding.

Add a coverage sentinel asserting that the set of distinct `(disposition, ladder_state)` pairs across
`LADDER_PINS` has exactly four members, so a future shape addition cannot quietly leave an arm
unexercised, and so a shape deletion that drops the last member of an arm reddens.

Generate the eight new snapshots with `tools/snapshot_report_shapes.py` and commit them verbatim.
  </action>
  <acceptance_criteria>
    - `SHAPE_IDS` has exactly sixteen members and `FROZEN_HASHES` has the same sixteen keys.
    - Each of the eight new shapes' computed fingerprint equals its frozen literal: `m27c512-full-all-ok` and `6d3afbc52315`; `m27c512-full-blank-check-bad` and `077a32d1a5c4`; `m27c512-full-canonical-name` and `776846bf2dc8`; `m27c512-full-comma-joined-name` and `37ad34d39a19`; `m27c512-full-runs-1` and `e4838f7bb1d3`; `at28c256-full-all-ok-sdp` and `52fb759dc48c`; `sst27sf512-full-all-ok` and `4b3e52cab987`; `w27e257-full-all-ok` and `22908e2954c3`.
    - `build_db_diff` for `at28c256-full-all-ok-sdp` returns ladder state `""` while every step verdict is in the OK, NA and SKIPPED set — the D-08 blind spot, measured.
    - `build_db_diff` for `sst27sf512-full-all-ok` and for `w27e257-full-all-ok` both return ladder state `community-reported`.
    - `LADDER_PINS` covers exactly four distinct disposition-and-ladder pairs.
    - `firestarter_app/tests/fixtures/reports/` holds exactly sixteen JSON files and `tools/snapshot_report_shapes.py --check` exits 0.
    - `tests/test_blast_radius_invariance.py` runs in under sixty seconds and skips nothing.
    - `git -C firestarter_app status --porcelain firestarter/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && EV=$E/174-02-ladder-arms.txt && { echo "== all sixteen, computed beside frozen, with ladder"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.fixtures.report_shapes import build_shape, FROZEN_HASHES, SHAPE_IDS; from firestarter.diagnostic_report import dedup_fingerprint, build_db_diff; from firestarter.database import EpromDatabase; db=EpromDatabase(skip_local_override=True); print('n_shapes='+str(len(SHAPE_IDS))); pairs=set(); [ (lambda sid,r,d: (pairs.add((d.proposed_disposition,d.ladder_state)), print(sid+' frozen='+FROZEN_HASHES[sid]+' computed='+dedup_fingerprint(r)+' ladder='+repr((d.proposed_disposition,d.ladder_state)))))(sid, build_shape(sid), build_db_diff('m27c512', db, build_shape(sid).results)) for sid in sorted(SHAPE_IDS)]; print('distinct_arms='+str(len(pairs)))"); echo "rc=$?"; echo "== snapshot check across sixteen"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; echo "== the invariance module, timed"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly --durations=3 tests/test_blast_radius_invariance.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 3 && /usr/bin/grep -qx 'n_shapes=16' "$EV" && /usr/bin/grep -qx 'distinct_arms=4' "$EV" && test "$(/usr/bin/grep -cE ' frozen=([0-9a-f]{12}) computed=\1 ' "$EV")" -eq 16 && /usr/bin/grep -q '^m27c512-full-all-ok frozen=6d3afbc52315 computed=6d3afbc52315 ' "$EV" && /usr/bin/grep -q '^m27c512-full-blank-check-bad frozen=077a32d1a5c4 computed=077a32d1a5c4 ' "$EV" && /usr/bin/grep -q '^m27c512-full-canonical-name frozen=776846bf2dc8 computed=776846bf2dc8 ' "$EV" && /usr/bin/grep -q '^m27c512-full-comma-joined-name frozen=37ad34d39a19 computed=37ad34d39a19 ' "$EV" && /usr/bin/grep -q '^m27c512-full-runs-1 frozen=e4838f7bb1d3 computed=e4838f7bb1d3 ' "$EV" && /usr/bin/grep -q '^at28c256-full-all-ok-sdp frozen=52fb759dc48c computed=52fb759dc48c ' "$EV" && /usr/bin/grep -q '^sst27sf512-full-all-ok frozen=4b3e52cab987 computed=4b3e52cab987 ' "$EV" && /usr/bin/grep -q '^w27e257-full-all-ok frozen=22908e2954c3 computed=22908e2954c3 ' "$EV" && /usr/bin/grep -qE "^sst27sf512-full-all-ok .*ladder=\('suggests: candidate for community-reported \(advisory\)', 'community-reported'\)$" "$EV" && /usr/bin/grep -qE "^at28c256-full-all-ok-sdp .*ladder=\('inconclusive -- needs N>=2 agreement \(advisory\)', ''\)$" "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error|skipped)' "$EV" && test "$(ls firestarter_app/tests/fixtures/reports/*.json | wc -l)" -eq 16 && test -z "$(git -C firestarter_app status --porcelain firestarter/)"</automated>
    <fails_when>The transcript does not read `n_shapes=16`; it does not read `distinct_arms=4`, which means an arm went unexercised or a shape was added that widened the table; the back-referenced count of rows whose computed value equals its frozen value is not sixteen; any of the eight named exact prefixes is absent; the `sst27sf512-full-all-ok` row does not show the `community-reported` ladder pair or the `at28c256-full-all-ok-sdp` row does not show the empty-ladder inconclusive pair, either of which would mean the D-08 blind spot is not being measured; the pytest leg reports a failed, error or skipped count or lacks a passed count; the reports directory holds a count other than sixteen; or the production package shows as modified.</fails_when>
  </verify>
  <done>All sixteen shapes are frozen against literals this plan computed, all four ladder arms are pinned including the non-SDP all-OK arm, the stateful SDP-aware double makes the AT28C256 arm-2 landing measurable rather than a mock artifact, and sixteen committed snapshots regenerate byte-identically.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate to tree | A frozen row that encodes a fixture artifact rather than a product behaviour reads green forever. This is the whole threat surface of an expansion plan. |
| developer home to fixture | `~/.firestarter/database.json` can override the shipped chip database, so a real-path shape's plan could differ machine to machine and move a frozen hash. |
| filed issue body to committed builder | The four filed-community step vectors are transcribed from untrusted GitHub issue text into source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-06 | Repudiation | `at28c256-full-all-ok-sdp` and the operator double behind it | high | mitigate | A stateful, SDP-lock-aware double holding a real chip image, so the SDP leg's read-back oracle sees real bytes. The fixed-return double is explicitly forbidden for this shape because its success-without-a-file behaviour turns an all-OK assertion into a false negative that still reads green. The acceptance criterion asserts the arm-2 landing directly rather than asserting the shape merely built. |
| T-174-07 | Repudiation | `LADDER_PINS` coverage | high | mitigate | The four-arm coverage sentinel asserts exactly four distinct disposition-and-ladder pairs, so neither a shape addition that widens the table nor a deletion that empties an arm can pass silently. |
| T-174-08 | Tampering | the real-path builders' database access | medium | mitigate | The single module-level `EpromDatabase(skip_local_override=True)`, asserted present by grep in plan 174-01 and reused here, so no local override can move a frozen hash. |
| T-174-09 | Tampering | transcription of four filed issue bodies into source | medium | mitigate | Transcription is by hand into a committed builder, so no untrusted text is parsed at runtime and no issue text ever reaches a filesystem path. The reproduction assertion against the filed twelve-hex value is itself the integrity check on the transcription: a wrong verdict or classification does not reproduce. |
| T-174-10 | Spoofing | the inherited hash pair CONTEXT.md D-12 rows 2 and 3 assert | high | mitigate | Neither inherited value is frozen. `m27c512-full-canonical-name` at `776846bf2dc8` is a this-session measurement and its docstring records the substitution and the reason, so the tree contains no number nothing in the tree can compute. |
| T-174-SC | Tampering | package-manager installs | medium | accept | No package is installed by this plan; the stack is the already-pinned interpreter, the already-pinned test extras and the standard library, and HYG-02 forbids a new runtime dependency milestone-wide. 174-RESEARCH.md's Package Legitimacy Audit records zero packages and zero flagged rows. |
</threat_model>

<verification>
Run from `/workspaces`:

1. `cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py` — exits 0, prints a passed count, prints no failed, error or skipped count.
2. `cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check` — exits 0 across all sixteen shapes.
3. Both evidence transcripts exist, `174-02-ladder-arms.txt` reads `n_shapes=16` and `distinct_arms=4`, and every row's computed value equals its frozen value.
4. `test -z "$(git -C firestarter_app status --porcelain firestarter/)"`.

The whole app suite is not a criterion and must not be used as one.
</verification>

<success_criteria>
- Sixteen shape_ids, sixteen frozen twelve-hex literals, sixteen committed snapshots, every literal computed by this phase.
- The four filed community hashes `f9dbc31dcd27`, `31547956e56b`, `00e121446ceb` and `7a89fcea856a` each reproduce from a hand-transcribed builder.
- All four `build_db_diff` arms are pinned, the all-OK arm is reached by two non-SDP chips, and the AT28C256 arm-2 landing is measured rather than assumed.
- The UV collapse row names the `blank-check` verdict triple as its mechanism, not the repeat-policy tag.
- No inherited hash is frozen anywhere.
</success_criteria>

## Artifacts this phase produces

Extended in `firestarter_app` (paths relative to the submodule root):

- `tests/fixtures/report_shapes.py` — grows to sixteen builders; new module constant `LADDER_PINS` (dict shape_id to a two-tuple of str); a new stateful SDP-aware operator double defined in this module rather than imported from `tests/test_chip_test.py`.
- `tests/test_blast_radius_invariance.py` — new parametrized ladder pin over `LADDER_PINS`, a four-arm coverage sentinel, and a dedicated test for the three-issue shared fingerprint.

New `shape_id` string constants introduced by this plan (fifteen): `sst27sf512-six-step-readback-gated`, `gh47-sst27sf512-pass`, `gh28-m27c512-fail`, `gh20-at28c256-fail`, `gh23-w27e257-fail`, `synthetic-arm4-no-ok`, `synthetic-arm4-empty-results`, `m27c512-full-all-ok`, `m27c512-full-blank-check-bad`, `m27c512-full-canonical-name`, `m27c512-full-comma-joined-name`, `m27c512-full-runs-1`, `at28c256-full-all-ok-sdp`, `sst27sf512-full-all-ok`, `w27e257-full-all-ok`.

New committed snapshot files (fifteen), one per new shape_id, at `tests/fixtures/reports/<shape_id>.json`.

New in the meta repo:

- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-02-frozen-table.txt`
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-02-ladder-arms.txt`

<output>
Create `.planning/phases/174-blast-radius-invariance-harness/174-02-SUMMARY.md` when done. It must record the measured wall-clock cost of the real-path table so plan 174-05's cost audit has a number to check, and it must state explicitly that CONTEXT.md D-12's inherited rows 2 and 3 were replaced by measured values rather than frozen.
</output>

