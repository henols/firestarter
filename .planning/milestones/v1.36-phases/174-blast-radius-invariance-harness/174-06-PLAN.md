---
phase: 174-blast-radius-invariance-harness
plan: 06
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - tools/rekey/check_rekey_ledger.py
  - firestarter_app/tests/test_rekey_ledger.py
  - firestarter_app/tests/fixtures/report_shapes.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-duplicate-row-red-green.txt
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-undeclared-row-red-green.txt
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-shape-aliasing-red-green.txt
autonomous: true
requirements: [GATE-01, GATE-02, GATE-06]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Two MILESTONES.md rows carrying the same ledger_id COLLIDE rather than merge: parse_milestones_rows raises LedgerParseError and the checker exits 2 printing ERROR: duplicate MILESTONES.md row for ledger_id 'RK-174-01-p177-readback-gating', instead of silently keeping only the last-seen row — the fabricated declared re-key CR-02 leg (a) proved invisible is now named on stdout — per D-13."
    - "An undeclared ledger row whose MILESTONES.md row exists at all has its shape_id AND its before_hash compared against the ledger's own values, so TOTALLY-WRONG-SHAPE or before_hash 000000000000 on a row whose after cell still reads (undeclared) exits 1 with an ERROR line naming the mismatch — this is CR-02 leg (b) and WR-01, closed in one change — per D-09 and D-13."
    - "An after cell that is neither the exact literal (undeclared) nor a matching declared hash is an ERROR: the fourteen-character string ffffffffffffff, an eleven-character truncation, and an uppercase 4DC282A5D596 all exit 1, because the undeclared branch now requires the exact literal rather than merely failing a twelve-lowercase-hex regex — the boundary and precision edges resolved explicitly."
    - "A MILESTONES.md carrying zero RK-174- rows while the ledger declares six exits 1 with an ERROR naming both counts, so deleting the whole ledger table is not a route past the gate — the empty edge resolved explicitly, and the same escape D-10 already closes on the app side."
    - "The checker's ERROR output is order-stable: two consecutive runs over byte-identical inputs produce byte-identical output, asserted by cmp rather than by inspection — the ordering edge resolved explicitly."
    - "Every one of the six new checker legs was SEEN RED against the pinned pre-fix checker blob 5c0c7c97097f8148182d8df87c75b250c4c3d3d8 (exit 0 where non-zero is required) before being seen GREEN against the fixed checker, and both rc values of each pair are in a committed evidence transcript — a leg only ever seen green proves nothing."
    - "build_shape never hands two different shape_ids the same results or plan object: build_shape('m27c512-full-all-ok').results is not build_shape('m27c512-full-canonical-name').results, and mutating clone.results[0].verdict no longer moves m27c512-full-all-ok's frozen hash off 6d3afbc52315 — CR-01 closed before Phase 181 writes RK-174-03-p181-canonical-naming-avoided's anti-vacuity leg — per D-01, D-02 and D-04."
    - "Not one frozen value moved: all sixteen FROZEN_HASHES, all sixteen LADDER_PINS, shape_ids.json, the twenty-six-row issue corpus and part_number_delta.json are byte-identical to their pre-plan state, and the four-module phase suite still reports 110 passed — per D-12's rule that a hash moves only by a declared, ledgered decision."
    - "tools/rekey/check_rekey_ledger.py still carries exactly one line whose first non-whitespace character is a hash — its shebang — and every app-side fixture and test file this plan touches carries zero, per the project's standing no-comments-in-source rule."
  artifacts:
    - path: tools/rekey/check_rekey_ledger.py
      provides: "Duplicate-ledger_id fail-closed parsing, undeclared-row shape_id/before_hash/after-literal validation, and the zero-row fail-closed check"
      contains: "duplicate MILESTONES.md row for ledger_id"
    - path: firestarter_app/tests/test_rekey_ledger.py
      provides: "Six subprocess-level legs binding the new checker behaviour, in the module that already owns the checker's paired tests"
      contains: "test_duplicate_milestones_row_for_one_ledger_id_fails_closed"
    - path: firestarter_app/tests/fixtures/report_shapes.py
      provides: "_clone_with_chip_override no longer aliases the cached base's results and plan"
      contains: "deepcopy"
    - path: firestarter_app/tests/test_blast_radius_invariance.py
      provides: "Non-aliasing and cross-shape mutation-isolation regression legs"
      contains: "test_build_shape_never_shares_results_or_plan_between_shape_ids"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-duplicate-row-red-green.txt
      provides: "The duplicate-ledger_id leg seen RED against blob 5c0c7c9 then GREEN against the fixed checker"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-undeclared-row-red-green.txt
      provides: "Five undeclared-branch legs seen RED against blob 5c0c7c9 then GREEN, plus the order-stability comparison"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-shape-aliasing-red-green.txt
      provides: "CR-01 aliasing seen RED against the pinned pre-fix report_shapes blob dc7e40a then GREEN, with the frozen hashes unmoved"
  key_links:
    - from: tools/rekey/check_rekey_ledger.py
      to: .planning/MILESTONES.md
      via: "parse_milestones_rows now rejects a duplicate ledger_id instead of letting the last row silently win"
      pattern: "duplicate MILESTONES.md row"
    - from: tools/rekey/check_rekey_ledger.py
      to: firestarter_app/tests/fixtures/rekey_ledger.py
      via: "check()'s undeclared branch compares shape_id and before_hash against the authoritative ledger row, not only the after cell"
      pattern: "before_hash"
    - from: firestarter_app/tests/test_rekey_ledger.py
      to: tools/rekey/check_rekey_ledger.py
      via: "_run_checker subprocess seam with --milestones pointed at a mutated tmp_path copy, the idiom test_check_rekey_ledger_orphan_milestones_row_exits_one already uses"
      pattern: "_run_checker"
    - from: firestarter_app/tests/fixtures/report_shapes.py
      to: firestarter_app/tests/test_blast_radius_invariance.py
      via: "build_shape returns independent results and plan objects per shape_id, so a mutation leg on one shape cannot redden another's frozen hash"
      pattern: "is not"
  prohibitions:
    - statement: "No comment line is written into any source file this plan touches. check_rekey_ledger.py keeps exactly one such line, its shebang; report_shapes.py, test_rekey_ledger.py and test_blast_radius_invariance.py keep zero. Docstrings are not comments and may be edited where the file already uses them; an explanatory hash line may not be added anywhere, and a plan cannot override this."
      status: required
      verification: judgment
    - statement: "No frozen hash, LADDER_PIN entry, shape_ids.json member, issue-corpus row or part_number_delta value is re-keyed, re-measured or edited. The whole point of the phase is that these move only by a declared, ledgered decision, and this plan declares none."
      status: required
      verification: judgment
    - statement: "No new checker leg is trusted on the strength of a green run. Each must be seen RED against the pinned pre-fix blob first, with both rc values transcribed, because a gate authored before the content it guards can be unreachable and prove nothing."
      status: required
      verification: judgment
    - statement: "No gate evidence is gathered with the bare grep command. The devcontainer's grep is ugrep and honors ignore files, so a sweep run with it can read clean while silently under-scanning; /usr/bin/grep is used explicitly everywhere."
      status: required
      verification: judgment
    - statement: "No file under firestarter_app/firestarter/ and no file in the firestarter firmware submodule is modified. This remains a test-and-tooling-only phase."
      status: required
      verification: judgment
    - statement: "No pre-existing failure outside this plan's scope is fixed or silenced, and WR-02, IN-01 and IN-02 are left exactly as the review found them. Repairing them here would hide unrelated work inside a gap-closure diff."
      status: required
      verification: judgment
---

<objective>
Close the single recorded gap in Phase 174: make `tools/rekey/check_rekey_ledger.py` actually bind
`.planning/MILESTONES.md` to the app-side ledger, so a duplicated, fabricated or corrupted row cannot
exit 0. Then fix the one confirmed Critical defect the verifier carried forward — three frozen shapes
sharing one mutable `results` list — before a later phase's mutation leg trips over it.

Purpose: GATE-06's whole claim is that a declared re-key is a machine check rather than a sentence. The
checker is the machine. Verification reproduced twice that it exits 0 on a fabricated declared row and
again on a corrupted undeclared row, which means the mechanism named to prevent the silent accident
this milestone exists to prevent does not prevent it. Everything else in the phase held up under
independent re-execution; this is the one thing that did not.

Output: a fail-closed checker, six subprocess-level legs binding it, a non-aliasing shape registry, and
three evidence transcripts each carrying a RED-then-GREEN `rc=` pair against a pinned pre-fix blob.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/174-blast-radius-invariance-harness/174-CONTEXT.md
@.planning/phases/174-blast-radius-invariance-harness/174-VERIFICATION.md
@.planning/phases/174-blast-radius-invariance-harness/174-REVIEW.md
@.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md
@CLAUDE.md
</context>

<environment_preamble>
Commands run from `/workspaces` unless a fragment explicitly enters `firestarter_app`. The meta-side
checker is standard library only and runs under the meta repo's `python3`. Anything importing
`firestarter` or `tests.fixtures` goes through `firestarter_app/.venv311/bin/python`, never a bare
`python` or `python3` — the devcontainer default is 3.12 and this project has proven that hides breakage
which reddens beta CI at 3.11. `/usr/bin/grep` for all gate evidence, never bare `grep`.

Two blob shas are pinned by this plan and are load-bearing. They are the PRE-FIX state of the two files
being changed, and every RED leg materializes its subject from them rather than from `HEAD`, so the
RED half of each anti-vacuity pair stays reproducible forever instead of becoming a one-shot capture
that a later reader has to take on trust. Anchoring to `HEAD` would be vacuous once the fix commits,
and this project has a recorded case of exactly that: `git rev-list --count HEAD ^HEAD~1` is always 1.

- `tools/rekey/check_rekey_ledger.py` pre-fix blob: `5c0c7c97097f8148182d8df87c75b250c4c3d3d8`
  (materialize with `git cat-file blob 5c0c7c97097f8148182d8df87c75b250c4c3d3d8`)
- `firestarter_app/tests/fixtures/report_shapes.py` pre-fix blob: `dc7e40a3cb72e719bc5c1c343e0b24c0df083882`
  (materialize with `git -C firestarter_app cat-file blob dc7e40a3cb72e719bc5c1c343e0b24c0df083882`)

Measured this planning session against those exact blobs, so every RED below is confirmed reachable
rather than assumed: a duplicated `RK-174-01-p177-readback-gating` row exits 0 printing
`OK: 6 ledger row(s), 6 MILESTONES.md row(s) bound`; a `TOTALLY-WRONG-SHAPE` / `000000000000` corruption
exits 0; a `MILESTONES.md` with every `RK-174-` row deleted exits 0 printing
`OK: 6 ledger row(s), 0 MILESTONES.md row(s) bound`; a fourteen-character `after` cell exits 0; an
uppercase `before` cell exits 0; and `build_shape('m27c512-full-all-ok').results is
build_shape('m27c512-full-canonical-name').results` is `True`, with a poke through the clone moving the
base's fingerprint from `6d3afbc52315` to `e9df6ca4627c`.

Also measured: `copy.deepcopy` succeeds on both `report.results` (12 `StepResult` entries) and
`report.plan` (a `Plan`), and a report rebuilt from the deep copies fingerprints to the same
`6d3afbc52315`. The CR-01 fix therefore cannot move a frozen hash.

Repository discipline: `tools/rekey/check_rekey_ledger.py` and the three evidence transcripts are meta
commits. `firestarter_app/tests/...` are commits made INSIDE the submodule, on the milestone branch
`gsd/v1.36-dev-test-fidelity`, which is the branch both trees are already on. Meta and submodule
changes are separate commits.
</environment_preamble>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — a duplicate ledger_id row collides instead of merging, seen RED then GREEN</name>
  <files>tools/rekey/check_rekey_ledger.py, firestarter_app/tests/test_rekey_ledger.py, .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-duplicate-row-red-green.txt</files>
  <read_first>
    - `tools/rekey/check_rekey_ledger.py` in full (196 lines) — the module docstring's exit-code table at lines 28-33, `parse_milestones_rows` at 94-107 (the defect: `rows[ledger_id] = (...)` unconditional, last line silently wins), `check` at 110-147, and `main`'s `except LedgerParseError` at 178-180 which is what turns a raise into exit 2
    - `.planning/phases/174-blast-radius-invariance-harness/174-VERIFICATION.md` — the `gaps:` frontmatter block, specifically the three `missing:` items, which are this plan's acceptance surface
    - `.planning/phases/174-blast-radius-invariance-harness/174-REVIEW.md` section CR-02 — its reproduction transcript and its suggested `parse_milestones_rows` fix
    - `.planning/MILESTONES.md` `### v1.36 Re-Key Ledger` section — the seven-column table and its six `RK-174-` rows, so the mutation generator's cell arithmetic matches the real row shape
    - `firestarter_app/tests/fixtures/rekey_ledger.py` — the authoritative six-row `LEDGER`, its four-tuple row shape and its docstring's D-09/D-13 rules
    - `firestarter_app/tests/test_rekey_ledger.py` in full — `_run_checker` at lines 41-49 and `test_check_rekey_ledger_orphan_milestones_row_exits_one` at 129-147, which is the exact idiom to copy: build a mutated `MILESTONES.md` copy under `tmp_path`, pass it via `--milestones`, assert the return code and assert the offending id appears on stdout
    - `.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt` — the legs A through H transcript, for the `== LABEL` / `rc=` idiom this task's transcript follows
    - `/workspaces/CLAUDE.md` — the no-comments-in-source rule and the submodule commit rule
  </read_first>
  <behavior>
    - `parse_milestones_rows` raises `LedgerParseError` the moment a second table line yields a `ledger_id` already present in `rows`, naming that `ledger_id`.
    - The checker exits 2 on that raise, because `main` already routes `LedgerParseError` to exit 2.
    - The printed line is exactly `ERROR: duplicate MILESTONES.md row for ledger_id 'RK-174-01-p177-readback-gating'` when the real file gains a second row for that id.
    - The real, unmutated pair still exits 0 with `OK: 6 ledger row(s), 6 MILESTONES.md row(s) bound`, so the change does not make the gate accidentally always-red.
    - Against the pinned pre-fix blob the same duplicated file exits 0, and that exit-0 is recorded as the RED half of the pair.
    - `test_duplicate_milestones_row_for_one_ledger_id_fails_closed` in `tests/test_rekey_ledger.py` reproduces the same collision through the subprocess seam and asserts return code 2 plus the duplicate `ledger_id` in the output.
  </behavior>
  <action>
Change `parse_milestones_rows` in `tools/rekey/check_rekey_ledger.py` so the assignment
`rows[ledger_id] = (shape_id, before, after)` is guarded: when `ledger_id` is already a key in `rows`,
raise `LedgerParseError` with the message `duplicate MILESTONES.md row for ledger_id {ledger_id!r}`.
`main` already catches `LedgerParseError` and prints `ERROR: {exc}` to stderr before returning 2, so the
observable line becomes `ERROR: duplicate MILESTONES.md row for ledger_id 'RK-174-01-p177-readback-gating'`
and the exit code becomes 2. Do not add a counter, a warning mode or a de-duplication pass — the
requirement is fail closed, and a silent last-write-wins is the exact defect being removed.

Update the module docstring's exit-code table so exit 2 reads as covering an input that could not be
parsed, naming the duplicate-row case alongside the existing missing-path and missing-`LEDGER` cases.
Update the docstring's guarantee paragraph too: the sentence claiming a row cannot be declared on one
side and silently never appear on the other was falsified by CR-02 and is now true only because of this
change, so state that a duplicated `ledger_id` is rejected rather than merged. These are docstring
edits, which the project's no-comments rule permits; do not add a single hash-prefixed line anywhere in
the file, which must finish this plan with exactly one such line, its shebang.

Then add `test_duplicate_milestones_row_for_one_ledger_id_fails_closed` to
`firestarter_app/tests/test_rekey_ledger.py`, directly after
`test_check_rekey_ledger_orphan_milestones_row_exits_one` so the two reverse-direction legs sit
together. Copy that test's shape exactly: take the real `_REPO_ROOT / ".planning" / "MILESTONES.md"`
text, build a `tmp_path` copy in which a fabricated fully-declared row for
`RK-174-01-p177-readback-gating` — `shape_id` `sst27sf512-six-step`, `before` `4dc282a5d596`, `after`
`ffffffffffff`, a date in the `declared` cell — is inserted immediately BEFORE the real row for the
same id, pass it via `--milestones`, and assert `returncode == 2` and that
`RK-174-01-p177-readback-gating` appears in `result.stdout + result.stderr`. Assert against the combined
streams, not `stdout` alone: this message is printed to stderr by `main`, unlike the `check()` errors the
sibling tests read off stdout. Give the test a docstring naming CR-02 leg (a) and stating that the
fabricated row is invisible to the pre-fix checker; add no hash-prefixed line.

Finally write the RED-then-GREEN transcript. Do not capture the RED by hand before editing — materialize
the pre-fix checker from the pinned blob `5c0c7c97097f8148182d8df87c75b250c4c3d3d8` with
`git cat-file blob`, run the identical mutated file through both it and the fixed checker in the same
pass, and record both `rc` values. That makes the pair permanently re-runnable rather than a one-shot
claim, and it is what the verify block below actually executes.
  </action>
  <acceptance_criteria>
    - `parse_milestones_rows` in `tools/rekey/check_rekey_ledger.py` raises `LedgerParseError` when a `ledger_id` already present in `rows` is seen a second time, and the assignment is no longer unconditional.
    - Running the fixed checker against a `MILESTONES.md` copy carrying two rows for `RK-174-01-p177-readback-gating` exits 2 and prints exactly `ERROR: duplicate MILESTONES.md row for ledger_id 'RK-174-01-p177-readback-gating'`.
    - Running the pre-fix blob `5c0c7c97097f8148182d8df87c75b250c4c3d3d8` against that identical file exits 0 and prints an `OK: ... MILESTONES.md row(s) bound` line, and both `rc` values are in the committed transcript.
    - Running the fixed checker with no path overrides exits 0 with `OK: 6 ledger row(s), 6 MILESTONES.md row(s) bound`.
    - The module docstring's exit-code table names the duplicate-row case under exit 2, and its guarantee paragraph no longer claims a protection the pre-fix code did not provide.
    - `firestarter_app/tests/test_rekey_ledger.py` defines `test_duplicate_milestones_row_for_one_ledger_id_fails_closed`, which builds its mutated copy under `tmp_path`, passes it via `--milestones`, asserts `returncode == 2`, and asserts the offending `ledger_id` appears in the combined stdout and stderr.
    - The whole `tests/test_rekey_ledger.py` module passes.
    - `tools/rekey/check_rekey_ledger.py` carries exactly one line whose first non-whitespace character is a hash; `tests/test_rekey_ledger.py` carries zero.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && mkdir -p "$E" && EV=$E/174-06-duplicate-row-red-green.txt && D=$(mktemp -d) && git cat-file blob 5c0c7c97097f8148182d8df87c75b250c4c3d3d8 > "$D/checker_prefix.py" && python3 -c "
import pathlib, sys
d = pathlib.Path(sys.argv[1])
src = pathlib.Path('.planning/MILESTONES.md').read_text(encoding='utf-8')
fab = '| RK-174-01-p177-readback-gating | sst27sf512-six-step | fabricated declared re-key | Phase 177 | 4dc282a5d596 | ffffffffffff | 2026-09-03 |'
out = []
for line in src.splitlines():
    if line.startswith('| RK-174-01-p177-readback-gating |'):
        out.append(fab)
    out.append(line)
(d / 'dup.md').write_text('\n'.join(out) + '\n', encoding='utf-8')
print('dup_rows_for_that_id=' + str(sum(1 for l in out if l.startswith('| RK-174-01-p177-readback-gating |'))))
" "$D" > "$EV" 2>&1 && { echo "== LEG A1 duplicate ledger_id against PRE-FIX blob 5c0c7c9 -- RED expected, it exits 0 where non-zero is required"; python3 "$D/checker_prefix.py" --milestones "$D/dup.md"; echo "rc_prefix_dup=$?"; echo "== LEG A2 the same file against the FIXED checker -- GREEN expected, exit 2 naming the duplicate"; python3 tools/rekey/check_rekey_ledger.py --milestones "$D/dup.md"; echo "rc_fixed_dup=$?"; echo "== LEG A3 the real unmutated pair still binds"; python3 tools/rekey/check_rekey_ledger.py; echo "rc_fixed_clean=$?"; } >> "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc_prefix_dup=0$' "$EV")" -eq 1 && test "$(/usr/bin/grep -c '^rc_fixed_dup=2$' "$EV")" -eq 1 && test "$(/usr/bin/grep -c '^rc_fixed_clean=0$' "$EV")" -eq 1 && /usr/bin/grep -qx 'dup_rows_for_that_id=2' "$EV" && /usr/bin/grep -qxF "ERROR: duplicate MILESTONES.md row for ledger_id 'RK-174-01-p177-readback-gating'" "$EV" && test "$(/usr/bin/grep -cF 'MILESTONES.md row(s) bound' "$EV")" -eq 2 && test "$(/usr/bin/grep -cE '^\s*#' tools/rekey/check_rekey_ledger.py)" -eq 1 && rm -rf "$D"</automated>
    <fails_when>`rc_prefix_dup` is anything other than 0 — the RED leg is inverted, so a non-zero here means the pre-fix blob was not the one measured and the anti-vacuity pair proves nothing; `rc_fixed_dup` is not 2, which includes 0 (the defect survives) and 1 (the raise was replaced by an ordinary mismatch error, contradicting the documented parse-failure contract); `rc_fixed_clean` is not 0, meaning the change made the gate always-red against the real file; `dup_rows_for_that_id` is not 2, meaning the mutation did not actually plant a duplicate; the exact `ERROR: duplicate MILESTONES.md row for ledger_id 'RK-174-01-p177-readback-gating'` line is absent; the count of `MILESTONES.md row(s) bound` lines is not exactly 2 (one from the pre-fix RED run, one from the clean run) — a third would mean the fixed checker also bound the duplicated file; or the checker's hash-prefixed line count is not exactly 1, meaning a comment was added to a file the project's hard rule forbids it in.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -q 'def test_duplicate_milestones_row_for_one_ledger_id_fails_closed' firestarter_app/tests/test_rekey_ledger.py && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_rekey_ledger.py)" -eq 0 && (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_rekey_ledger.py)</automated>
    <fails_when>The function name `test_duplicate_milestones_row_for_one_ledger_id_fails_closed` is absent from the module, meaning the checker change has no paired test binding it; the module gains a hash-prefixed line; or pytest exits non-zero, meaning at least one leg in the module failed.</fails_when>
  </verify>
  <done>A second MILESTONES.md row for an existing ledger_id is rejected by name at exit 2 rather than silently overwriting the first, the paired subprocess test passes, and the transcript carries the pre-fix exit 0 beside the fixed exit 2 for the identical input.</done>
</task>

<task type="auto">
  <name>Task 2: The undeclared branch validates shape_id, before_hash and the after literal, and an emptied table fails closed</name>
  <files>tools/rekey/check_rekey_ledger.py, firestarter_app/tests/test_rekey_ledger.py, .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-undeclared-row-red-green.txt</files>
  <read_first>
    - `tools/rekey/check_rekey_ledger.py` as Task 1 leaves it — `check` at 110-147, particularly the `else` branch at 133-139 whose only test is `_HASH_RE.match(m_row[2])`, and `_HASH_RE` at line 51 whose anchored twelve-lowercase-hex pattern is why an eleven-character, a fourteen-character and an uppercase value all read as not-a-hash and are silently accepted
    - `.planning/phases/174-blast-radius-invariance-harness/174-REVIEW.md` section WR-01 — the narrower validation gap that exists even with no duplicate row involved, and its suggested fix
    - `.planning/phases/174-blast-radius-invariance-harness/174-VERIFICATION.md` `missing:` item 2, which is this task's acceptance surface verbatim
    - `.planning/MILESTONES.md` — all six `RK-174-` rows, confirming every `after` cell is the literal `(undeclared)` today and every `before` cell equals the ledger's `before_hash`, so requiring the exact literal breaks nothing that exists
    - `firestarter_app/tests/fixtures/rekey_ledger.py` — the six rows, all with `after_hash is None`
    - `firestarter_app/tests/test_rekey_ledger.py` as Task 1 leaves it — `_run_checker` and the tmp_path-copy idiom
    - `.planning/phases/174-blast-radius-invariance-harness/evidence/174-03-ledger-closure.txt` — the reverse-direction orphan-row RED, for the transcript idiom
  </read_first>
  <behavior>
    - For a ledger row with `after_hash is None` whose `MILESTONES.md` row exists, `check()` emits an ERROR when `m_row[0] != shape_id` or `m_row[1] != before_hash`, naming both the file's pair and the ledger's pair.
    - For that same row, `check()` emits an ERROR unless `m_row[2]` is exactly the literal `(undeclared)` — so `ffffffffffff`, `ffffffffffffff`, `4dc282a5d59`, `4DC282A5D596`, an empty cell and `TBD` are all errors, replacing the previous test that only caught a twelve-lowercase-hex string.
    - Comparison is exact and case-sensitive on the full string; no prefix match, no lowercase folding, no truncation.
    - When `ledger_rows` is non-empty and `milestones_rows` is empty, `check()` emits an ERROR naming both counts.
    - Every one of these exits 1, because they are `check()` mismatches rather than parse failures.
    - Two consecutive runs of the fixed checker over byte-identical inputs produce byte-identical output.
    - An undeclared ledger row with NO `MILESTONES.md` row at all remains legal and produces no error, preserving the contract D-13 states.
  </behavior>
  <action>
Rewrite `check()`'s `after_hash is None` branch in `tools/rekey/check_rekey_ledger.py`. Keep the existing
guard that a missing `m_row` is legal, then, whenever `m_row is not None`, run three assertions in order.
First, if `m_row[0] != shape_id` or `m_row[1] != before_hash`, append an ERROR naming the file's
`(shape_id, before)` pair and the ledger's `(shape_id, before_hash)` pair — write both tuples with
`!r` so a case difference or a trailing space is visible in the message rather than invisible. Second,
if `m_row[2]` is not exactly the string `(undeclared)`, append an ERROR quoting the offending cell and
stating that an undeclared ledger row's `after` cell must be that exact literal. Introduce a module-level
constant `_UNDECLARED = "(undeclared)"` and compare against it rather than repeating the literal, so the
narration token has one definition. Delete the `_HASH_RE.match(m_row[2])` test from this branch: the
exact-literal requirement strictly subsumes it and closes the boundary hole where an eleven-character or
fourteen-character hex string was neither a hash nor a rejection. Keep `_HASH_RE` itself — it stays in
use nowhere else only if nothing else references it, so if the branch was its sole consumer, remove the
constant too rather than leaving a dead pattern.

Add the zero-row check at the top of `check()`: if `ledger_rows` is non-empty and `milestones_rows` is
empty, append an ERROR stating that `MILESTONES.md` carries zero `RK-174-` rows while the ledger declares
the row count. This is the `empty` edge, and it is the one deletion the reverse-direction loop cannot
see — that loop iterates `milestones_rows`, so an emptied table gives it nothing to iterate. Deliberately
do NOT require a `MILESTONES.md` row for every individual undeclared ledger row: D-13's contract allows a
newly appended ledger row to precede its narration, and this task preserves that. The emptied-table case
is categorically different, because there the narration mechanism itself has vanished. State that
distinction in the docstring.

Update the module docstring's guarantee paragraph again so it describes what the undeclared branch now
checks — shape_id, before_hash and the exact `(undeclared)` literal — instead of the narrower after-cell
claim WR-01 called out as documented-but-insufficient. Docstring only; no hash-prefixed line.

Then add five tests to `firestarter_app/tests/test_rekey_ledger.py`, all through `_run_checker` with a
mutated `tmp_path` copy passed via `--milestones`, each asserting `returncode == 1` and asserting a
substring of its own ERROR message so the tests cannot pass on the wrong error:
`test_corrupted_undeclared_row_shape_id_exits_one` (row 01's `shape_id` cell set to
`TOTALLY-WRONG-SHAPE`), `test_corrupted_undeclared_row_before_hash_exits_one` (its `before` cell set to
`000000000000`), `test_uppercased_before_hash_exits_one` (its `before` cell uppercased to
`4DC282A5D596`, proving comparison is case-sensitive and exact rather than folded),
`test_after_cell_that_is_not_the_undeclared_literal_exits_one` (parametrized over `ffffffffffff`,
`ffffffffffffff`, `4dc282a5d59` and the empty string, proving the boundary one step either side of
twelve hex characters is covered), and `test_milestones_with_zero_rekey_rows_exits_one` (every line
beginning with the row prefix removed). Add a sixth, `test_checker_error_output_is_order_stable`, which
runs `_run_checker` twice against the same corrupted copy and asserts the two `stdout` values are equal
— the `ordering` edge, resolved as a byte-equality assertion rather than a claim.

Build the mutated copies by locating the real row line by its `| RK-174-01-p177-readback-gating |`
prefix and rebuilding it cell by cell, never by hand-typing a replacement row: a hand-typed row drifts
the moment the real table's `change` prose changes, and the test would then be asserting against a row
shape the file no longer has.

Finally write the second transcript, materializing the pre-fix checker from blob
`5c0c7c97097f8148182d8df87c75b250c4c3d3d8` and running all five mutated files through both it and the
fixed checker in one pass, followed by the two-run order-stability comparison.
  </action>
  <acceptance_criteria>
    - `tools/rekey/check_rekey_ledger.py` defines `_UNDECLARED` and the undeclared branch compares `m_row[2]` against it, with no remaining `_HASH_RE.match(m_row[2])` call.
    - Running the fixed checker against a copy whose row 01 `shape_id` reads `TOTALLY-WRONG-SHAPE` exits 1 and prints an ERROR containing both `TOTALLY-WRONG-SHAPE` and `sst27sf512-six-step`.
    - The same against `before` `000000000000` exits 1 and prints an ERROR containing both `000000000000` and `4dc282a5d596`.
    - The same against `before` `4DC282A5D596` exits 1, proving the comparison is case-sensitive.
    - The same against `after` cells `ffffffffffff`, `ffffffffffffff` and `4dc282a5d59` each exit 1, proving one character either side of the twelve-hex threshold is covered rather than silently accepted.
    - A `MILESTONES.md` with every `RK-174-` row deleted exits 1 with an ERROR naming zero rows against six ledger rows.
    - All five of those files exit 0 against the pre-fix blob, and both `rc` values of every pair are in the transcript.
    - Two consecutive fixed-checker runs over the same corrupted copy produce byte-identical output.
    - The real, unmutated pair still exits 0 with the `OK: 6 ledger row(s), 6 MILESTONES.md row(s) bound` line.
    - `tests/test_rekey_ledger.py` defines all six new test function names and the whole module passes.
    - `tools/rekey/check_rekey_ledger.py` carries exactly one hash-prefixed line; `tests/test_rekey_ledger.py` carries zero.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && mkdir -p "$E" && EV=$E/174-06-undeclared-row-red-green.txt && D=$(mktemp -d) && git cat-file blob 5c0c7c97097f8148182d8df87c75b250c4c3d3d8 > "$D/checker_prefix.py" && python3 -c "
import pathlib, sys
d = pathlib.Path(sys.argv[1])
src = pathlib.Path('.planning/MILESTONES.md').read_text(encoding='utf-8')
row = [l for l in src.splitlines() if l.startswith('| RK-174-01-p177-readback-gating |')][0]
c = [x.strip() for x in row.strip().strip('|').split('|')]
def w(name, cells):
    (d / name).write_text(src.replace(row, '| ' + ' | '.join(cells) + ' |', 1), encoding='utf-8')
w('wrongshape.md', [c[0], 'TOTALLY-WRONG-SHAPE', c[2], c[3], c[4], c[5], c[6]])
w('wrongbefore.md', [c[0], c[1], c[2], c[3], '000000000000', c[5], c[6]])
w('upperbefore.md', [c[0], c[1], c[2], c[3], c[4].upper(), c[5], c[6]])
w('wideafter.md', [c[0], c[1], c[2], c[3], c[4], 'ffffffffffffff', c[6]])
w('shortafter.md', [c[0], c[1], c[2], c[3], c[4], c[4][:-1], c[6]])
(d / 'emptytable.md').write_text('\n'.join(l for l in src.splitlines() if not l.startswith('| RK-174-')) + '\n', encoding='utf-8')
print('cells=' + str(len(c)))
print('remaining_rows=' + str(sum(1 for l in (d / 'emptytable.md').read_text(encoding='utf-8').splitlines() if l.startswith('| RK-174-'))))
" "$D" > "$EV" 2>&1 && { for m in wrongshape wrongbefore upperbefore wideafter shortafter emptytable; do echo "== $m against PRE-FIX blob 5c0c7c9 -- RED expected, it exits 0 where non-zero is required"; python3 "$D/checker_prefix.py" --milestones "$D/$m.md"; echo "rc_prefix_$m=$?"; echo "== $m against the FIXED checker -- GREEN expected, exit 1 with a naming ERROR"; python3 tools/rekey/check_rekey_ledger.py --milestones "$D/$m.md"; echo "rc_fixed_$m=$?"; done; echo "== order stability, two consecutive runs over the same corrupted copy"; python3 tools/rekey/check_rekey_ledger.py --milestones "$D/wrongshape.md" > "$D/o1.txt" 2>&1; python3 tools/rekey/check_rekey_ledger.py --milestones "$D/wrongshape.md" > "$D/o2.txt" 2>&1; if cmp -s "$D/o1.txt" "$D/o2.txt"; then echo "order_stable=identical"; else echo "order_stable=DIFFERS"; fi; echo "== the real unmutated pair still binds"; python3 tools/rekey/check_rekey_ledger.py; echo "rc_fixed_clean=$?"; } >> "$EV" 2>&1; test "$(/usr/bin/grep -cE '^rc_prefix_(wrongshape|wrongbefore|upperbefore|wideafter|shortafter|emptytable)=0$' "$EV")" -eq 6 && test "$(/usr/bin/grep -cE '^rc_fixed_(wrongshape|wrongbefore|upperbefore|wideafter|shortafter|emptytable)=1$' "$EV")" -eq 6 && /usr/bin/grep -qx 'cells=7' "$EV" && /usr/bin/grep -qx 'remaining_rows=0' "$EV" && /usr/bin/grep -qx 'order_stable=identical' "$EV" && /usr/bin/grep -qx 'rc_fixed_clean=0' "$EV" && /usr/bin/grep -qF 'TOTALLY-WRONG-SHAPE' "$EV" && /usr/bin/grep -qF '4DC282A5D596' "$EV" && /usr/bin/grep -qF 'ffffffffffffff' "$EV" && test "$(/usr/bin/grep -cE '^ERROR: ' "$EV")" -ge 6 && test "$(/usr/bin/grep -cE '^\s*#' tools/rekey/check_rekey_ledger.py)" -eq 1 && rm -rf "$D"</automated>
    <fails_when>Fewer than six `rc_prefix_*=0` lines — a leg that was not RED against the pre-fix blob is a leg that never proved anything, and a non-zero there means the mutation missed the row or the pinned blob is not the measured one; fewer than six `rc_fixed_*=1` lines, which includes an exit 0 (the hole survives) and an exit 2 (a parse failure where a mismatch was required); `cells` is not 7, meaning the real table's column count moved and the mutation arithmetic is wrong; `remaining_rows` is not 0, meaning the emptied-table copy still carries rows; `order_stable` reads `DIFFERS`, meaning two runs over identical inputs disagree; `rc_fixed_clean` is not 0, meaning the real file no longer binds; any of the three planted literals is missing from the transcript, meaning an ERROR fired without naming what it rejected; fewer than six `ERROR: ` lines; or the checker's hash-prefixed line count is not exactly 1.</fails_when>
    <automated>cd /workspaces && for n in test_corrupted_undeclared_row_shape_id_exits_one test_corrupted_undeclared_row_before_hash_exits_one test_uppercased_before_hash_exits_one test_after_cell_that_is_not_the_undeclared_literal_exits_one test_milestones_with_zero_rekey_rows_exits_one test_checker_error_output_is_order_stable; do /usr/bin/grep -q "def $n" firestarter_app/tests/test_rekey_ledger.py || { echo "MISSING $n"; exit 1; }; done && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_rekey_ledger.py)" -eq 0 && (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_rekey_ledger.py)</automated>
    <fails_when>The loop prints a `MISSING <name>` line and exits 1, meaning a named test function was not written; the module gains a hash-prefixed line; or pytest exits non-zero, meaning a leg in the module failed.</fails_when>
  </verify>
  <done>An undeclared row that misnames its shape, carries a wrong or differently-cased before-hash, or shows an after cell that is not the exact `(undeclared)` literal is an exit-1 ERROR naming the mismatch; an emptied table is an exit-1 ERROR; every leg was seen exit 0 against the pinned pre-fix blob first; and the checker's output is byte-stable across runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stop three frozen shapes sharing one mutable results list — CR-01, separately committable</name>
  <files>firestarter_app/tests/fixtures/report_shapes.py, firestarter_app/tests/test_blast_radius_invariance.py, .planning/phases/174-blast-radius-invariance-harness/evidence/174-06-shape-aliasing-red-green.txt</files>
  <read_first>
    - `firestarter_app/tests/fixtures/report_shapes.py` lines 40-70 (the module docstring's caching-is-safe claim, which this task falsifies and must correct), 478-543 (`_clone_with_chip_override`, `_build_m27c512_full_all_ok`, `_build_m27c512_full_canonical_name`, `_build_m27c512_full_comma_joined_name`), and the `_BUILDERS` registry so the three affected `shape_id`s are read off the real mapping rather than assumed
    - `.planning/phases/174-blast-radius-invariance-harness/174-REVIEW.md` section CR-01 — the reproduction transcript, the `6d3afbc52315` to `e9df6ca4627c` movement, and the two suggested fixes
    - `.planning/phases/174-blast-radius-invariance-harness/174-VERIFICATION.md` section "Confirmed Critical Defect Not Rising to a Gap" — the verifier's assessment that this is collateral false-RED risk and must be fixed before Phase 181 writes `RK-174-03-p181-canonical-naming-avoided`'s leg
    - `firestarter_app/tests/test_blast_radius_invariance.py` lines 1-60 (the module docstring's anti-vacuity and Reachability blocks) and `test_planted_mutation_clearing_write_fingerprint_reddens_the_gate` at 561-572 — the mutation-leg idiom to copy, and the reason the tracer shape is deliberately uncached
    - `firestarter_app/tests/fixtures/rekey_ledger.py` — rows `RK-174-02-rejected-sdp-step-pruning` and `RK-174-03-p181-canonical-naming-avoided`, whose `before_hash` values `6d3afbc52315` and `776846bf2dc8` are the two literals this task must leave unmoved
  </read_first>
  <behavior>
    - Test 1: `build_shape('m27c512-full-all-ok').results is not build_shape('m27c512-full-canonical-name').results`, and the same for `m27c512-full-comma-joined-name`, and the same for each pair's `.plan`.
    - Test 2: after `clone = build_shape('m27c512-full-canonical-name'); clone.results[0].verdict = 'BAD'`, `dedup_fingerprint(build_shape('m27c512-full-all-ok'))` is still exactly `6d3afbc52315` and `dedup_fingerprint(build_shape('m27c512-full-comma-joined-name'))` is unchanged from its frozen literal.
    - Test 2 mutates only through an UNCACHED derivative, never through `m27c512-full-all-ok` itself, whose builder stays `functools.cache`d — a poke through the cached base would leak to every later test in the process.
    - All sixteen `FROZEN_HASHES` and all sixteen `LADDER_PINS` still hold: the deep copy preserves values, so no hash moves.
    - The committed report snapshots still regenerate byte-identically.
  </behavior>
  <action>
Fix `_clone_with_chip_override` in `firestarter_app/tests/fixtures/report_shapes.py` by giving the clone
its own `results` and `plan` rather than the base's objects: `import copy` at module level and pass
`results=copy.deepcopy(report.results)` and `plan=copy.deepcopy(report.plan)` into the
`DiagnosticReport` it returns. Measured this planning session: both deep-copy cleanly, and a report
rebuilt from the copies fingerprints to `6d3afbc52315`, unchanged — so the fix cannot move a frozen hash.

Take the deep-copy fix rather than the review's alternative of dropping `@functools.cache` from
`_build_m27c512_full_all_ok`, and say so in the docstring. The reasons are three. First, deep-copying
targets the actual defect, which is aliasing ACROSS `shape_id`s, whereas removing the cache from exactly
one of eight real-path builders leaves a seven-to-one asymmetry a later reader has to explain. Second,
the cache is load-bearing for cost: the two derivatives are themselves uncached, so every call would pay
`derive_plan` plus `run_plan` again, and this phase's recorded 7.92-second figure for its 110 tests is a
number later phases compare against. Third, WR-02 — the in-place `db_diff` assignment on cached
real-path shapes — is explicitly out of this plan's scope, and dropping one builder's cache would half-fix
it in a way that makes the remaining seven look intentional rather than outstanding.

Correct the module docstring's caching paragraph, which currently argues caching is safe because nothing
mutates a real-path shape's `results` after construction. That argument did not survive contact with
`_clone_with_chip_override`, and the docstring must now say the clone owns its own `results` and `plan`,
naming CR-01 as the reason. Correct `_clone_with_chip_override`'s own docstring too — the sentence
claiming sharing `results` is safe because `dedup_fingerprint` and `build_db_diff` only read it is the
exact false premise the review disproved. Docstrings only; add no hash-prefixed line to this file, which
must finish at zero.

Add the two regression legs to `firestarter_app/tests/test_blast_radius_invariance.py`, beside the
existing planted-mutation tests: `test_build_shape_never_shares_results_or_plan_between_shape_ids`, an
identity sweep over the three affected `shape_id`s asserting `is not` on both attributes for every
ordered pair; and
`test_mutation_through_a_derived_shape_does_not_move_the_base_shapes_frozen_hash`, which mutates a
freshly built `m27c512-full-canonical-name` clone's first `StepResult.verdict` to `BAD` and then asserts
`dedup_fingerprint(build_shape('m27c512-full-all-ok'))` still equals
`FROZEN_HASHES['m27c512-full-all-ok']`. Read the expected literals out of `FROZEN_HASHES`, never
hand-type them, and mutate only through the uncached derivative. Extend the module docstring's
Reachability block with the new transcript's filename, matching how the existing legs cite
`174-01-anti-vacuity-red-green.txt`.

Then write the third transcript. Materialize the pre-fix `report_shapes.py` from the pinned blob
`dc7e40a3cb72e719bc5c1c343e0b24c0df083882` into a temp path, load it with
`importlib.util.spec_from_file_location`, and record the RED: the aliasing reads `True` and a poke
through the clone moves the base from `6d3afbc52315` to `e9df6ca4627c`. Then record the GREEN against
the committed module: aliasing reads `False` and the base stays `6d3afbc52315` after the identical poke.
Finish the transcript with a full run of the phase's four test modules so the 110-test count and the
unmoved frozen artifacts are in the same record.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/fixtures/report_shapes.py` imports `copy` and `_clone_with_chip_override` passes deep copies of both `results` and `plan`.
    - `build_shape('m27c512-full-all-ok').results is build_shape('m27c512-full-canonical-name').results` evaluates to `False`, and the same for `.plan` and for the comma-joined shape.
    - Against the pinned pre-fix blob the same expression evaluates to `True` and a poke through the clone moves the base's fingerprint to `e9df6ca4627c`; both facts are in the transcript.
    - Against the committed module the identical poke leaves `dedup_fingerprint(build_shape('m27c512-full-all-ok'))` at `6d3afbc52315`.
    - Both new test function names exist in `tests/test_blast_radius_invariance.py` and the four-module phase suite reports `110 passed` or more, with zero failures.
    - `git -C firestarter_app status --porcelain tests/fixtures/shape_ids.json tests/fixtures/devtest_issue_corpus.json tests/fixtures/part_number_delta.json tests/fixtures/reports/` is empty, proving no frozen artifact moved.
    - `git -C firestarter_app status --porcelain firestarter/` is empty and the firmware submodule's porcelain is empty.
    - `tests/fixtures/report_shapes.py` and `tests/test_blast_radius_invariance.py` each carry zero hash-prefixed lines.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && mkdir -p "$E" && EV=$E/174-06-shape-aliasing-red-green.txt && D=$(mktemp -d) && git -C firestarter_app cat-file blob dc7e40a3cb72e719bc5c1c343e0b24c0df083882 > "$D/report_shapes_prefix.py" && { echo "== RED against the PRE-FIX report_shapes blob dc7e40a -- aliasing True and the base hash moves"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys, importlib.util
sys.path.insert(0, '.')
spec = importlib.util.spec_from_file_location('rs_prefix', sys.argv[1])
m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)
from firestarter.diagnostic_report import dedup_fingerprint
b = m.build_shape('m27c512-full-all-ok')
c = m.build_shape('m27c512-full-canonical-name')
print('prefix_alias_results=' + str(b.results is c.results))
print('prefix_base_before=' + dedup_fingerprint(b))
c.results[0].verdict = 'BAD'
print('prefix_base_after=' + dedup_fingerprint(b))
" "$D/report_shapes_prefix.py"); echo "rc_prefix=$?"; echo "== GREEN against the committed module -- aliasing False and the base hash holds"; (cd firestarter_app && ./.venv311/bin/python -c "
import sys
sys.path.insert(0, '.')
from tests.fixtures.report_shapes import build_shape, FROZEN_HASHES
from firestarter.diagnostic_report import dedup_fingerprint
b = build_shape('m27c512-full-all-ok')
c = build_shape('m27c512-full-canonical-name')
j = build_shape('m27c512-full-comma-joined-name')
print('fixed_alias_results=' + str(b.results is c.results))
print('fixed_alias_plan=' + str(b.plan is c.plan))
print('fixed_alias_results_joined=' + str(b.results is j.results))
print('fixed_base_before=' + dedup_fingerprint(b))
c.results[0].verdict = 'BAD'
print('fixed_base_after=' + dedup_fingerprint(build_shape('m27c512-full-all-ok')))
print('fixed_base_matches_frozen=' + str(dedup_fingerprint(build_shape('m27c512-full-all-ok')) == FROZEN_HASHES['m27c512-full-all-ok']))
"); echo "rc_fixed=$?"; echo "== the four phase test modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_devtest_issue_corpus.py tests/test_part_number_delta_drift.py); echo "rc_suite=$?"; } > "$EV" 2>&1; /usr/bin/grep -qx 'prefix_alias_results=True' "$EV" && /usr/bin/grep -qx 'prefix_base_before=6d3afbc52315' "$EV" && /usr/bin/grep -qx 'prefix_base_after=e9df6ca4627c' "$EV" && /usr/bin/grep -qx 'fixed_alias_results=False' "$EV" && /usr/bin/grep -qx 'fixed_alias_plan=False' "$EV" && /usr/bin/grep -qx 'fixed_alias_results_joined=False' "$EV" && /usr/bin/grep -qx 'fixed_base_before=6d3afbc52315' "$EV" && /usr/bin/grep -qx 'fixed_base_after=6d3afbc52315' "$EV" && /usr/bin/grep -qx 'fixed_base_matches_frozen=True' "$EV" && /usr/bin/grep -qx 'rc_suite=0' "$EV" && /usr/bin/grep -qE '^1[0-9][0-9] passed' "$EV" && test "$(/usr/bin/grep -cE '^(FAILED|ERROR) ' "$EV")" -eq 0 && rm -rf "$D"</automated>
    <fails_when>`prefix_alias_results` is not `True` or `prefix_base_after` is not `e9df6ca4627c` — the RED was not reproduced against the pinned pre-fix blob, so the pair proves nothing; any `fixed_alias_*` line reads `True`, meaning two shape_ids still share an object; `fixed_base_after` is not `6d3afbc52315`, meaning a poke through the derivative still moves the base's frozen hash; `fixed_base_matches_frozen` is not `True`, meaning the fix moved a frozen literal — which would be an undeclared re-key and is forbidden outright; `rc_suite` is not 0; the transcript carries no three-digit `NNN passed` line at or above 100; or any `FAILED ` or `ERROR ` line appears.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -q 'def test_build_shape_never_shares_results_or_plan_between_shape_ids' firestarter_app/tests/test_blast_radius_invariance.py && /usr/bin/grep -q 'def test_mutation_through_a_derived_shape_does_not_move_the_base_shapes_frozen_hash' firestarter_app/tests/test_blast_radius_invariance.py && /usr/bin/grep -q 'deepcopy' firestarter_app/tests/fixtures/report_shapes.py && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/fixtures/report_shapes.py)" -eq 0 && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/test_blast_radius_invariance.py)" -eq 0 && test -z "$(git -C firestarter_app status --porcelain tests/fixtures/shape_ids.json tests/fixtures/devtest_issue_corpus.json tests/fixtures/part_number_delta.json tests/fixtures/reports/)" && test -z "$(git -C firestarter_app status --porcelain firestarter/)" && test -z "$(git -C firestarter status --porcelain 2>/dev/null)"</automated>
    <fails_when>Either new test function name is absent, meaning the fix landed without a regression leg binding it; `deepcopy` does not appear in `report_shapes.py`, meaning the aliasing fix was not applied there; either app file carries a hash-prefixed line; `git status --porcelain` is non-empty for `shape_ids.json`, `devtest_issue_corpus.json`, `part_number_delta.json` or the committed report snapshots, meaning a frozen artifact moved and an undeclared re-key occurred; or either the app production package or the firmware submodule shows a modified file in a test-and-tooling-only plan.</fails_when>
  </verify>
  <done>No two `shape_id`s share a `results` or `plan` object, a mutation through `m27c512-full-canonical-name` leaves `m27c512-full-all-ok` at `6d3afbc52315`, the aliasing was seen `True` against the pinned pre-fix blob before it was seen `False`, and every frozen artifact is byte-identical to its pre-plan state.</done>
  <reversibility rating="reversible">Choosing `copy.deepcopy` in `_clone_with_chip_override` over dropping `@functools.cache` is a one-function change with no external consumer; either fix can replace the other later without touching a ledger row, a frozen hash or another phase's test. Re-keying a frozen hash would be one-way, and this plan re-keys none.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/MILESTONES.md` narration to the authoritative app-side ledger | The human-edited narration is the untrusted side. A row there can be duplicated, fabricated or corrupted by hand or by a bad merge, and the checker is the only thing standing between that and a silently accepted declared re-key. This is the boundary the whole gap is about. |
| checker exit code to gate verdict | Anything that lets the checker exit 0 on a broken tree turns the gate into a sentence. Exit 0 is a claim, and the claim must be earned by comparison, not by a parse that quietly discarded a row. |
| one frozen `shape_id` to another | Three `shape_id`s sharing one mutable object means a test poking one is silently writing to two others. The `shape_id` is the published contract a ledger row names, so object identity leaking across that boundary corrupts the contract. |
| pre-fix blob to fixed source | The RED half of every anti-vacuity pair is produced from a pinned git blob. If that pin is wrong or drifts, the RED is fabricated and the whole proof collapses into a green-only claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-28 | Spoofing | `parse_milestones_rows` duplicate rows | critical | mitigate | A second row for an existing `ledger_id` raises `LedgerParseError` and exits 2 naming that id, rather than silently overwriting the first. Task 1 proves the pre-fix blob exits 0 on the identical file and the fixed checker exits 2, both `rc` values transcribed. |
| T-174-29 | Tampering | `check()` undeclared branch | critical | mitigate | The branch now compares `shape_id` and `before_hash` against the authoritative ledger row and requires the `after` cell to be the exact `(undeclared)` literal, so a misnamed shape, a wrong or differently-cased before-hash, and an eleven-, twelve- or fourteen-character `after` value are all exit-1 errors naming the mismatch. |
| T-174-30 | Repudiation | deletion of the `MILESTONES.md` ledger table | high | mitigate | A zero-row table against a non-empty ledger is an exit-1 error naming both counts, so the cheapest route past a failing gate — remove the row — is closed on the meta side as D-10 already closes it on the app side. |
| T-174-31 | Repudiation | the new checker legs themselves | high | mitigate | Every leg is materialized against the pinned pre-fix blob `5c0c7c9` and asserted to exit 0 there before being asserted non-zero against the fixed checker. A leg that has only ever been seen green is rejected by the verify block's `rc_prefix_*=0` counts. |
| T-174-32 | Tampering | shared mutable `results` across three `shape_id`s | critical | mitigate | `_clone_with_chip_override` deep-copies `results` and `plan`; two regression legs assert `is not` across every affected pair and assert that a poke through the derivative leaves `m27c512-full-all-ok` at `6d3afbc52315`. The RED is reproduced against the pinned pre-fix blob `dc7e40a`. |
| T-174-33 | Tampering | frozen artifacts during a fix that touches the fixture module | high | mitigate | Task 3's second verify asserts empty porcelain for `shape_ids.json`, `devtest_issue_corpus.json`, `part_number_delta.json` and the committed report snapshots, and the first asserts `fixed_base_matches_frozen=True` plus a clean four-module suite run, so an accidental re-key cannot pass as a fix. |
| T-174-34 | Information disclosure | error messages naming ledger contents | low | accept | The checker's ERROR lines quote `shape_id` and hash values from two files that are already committed in the open repositories. There is nothing secret to leak, and suppressing the values would defeat the purpose — an error that does not name what it rejected is the WR-01 failure mode in a different form. |
| T-174-35 | Elevation of privilege | the checker's parsing of `MILESTONES.md` and `rekey_ledger.py` | low | accept | Both inputs are parsed without execution: `MILESTONES.md` by a line-anchored regex, and the ledger by `ast.parse` plus `ast.literal_eval` on the `LEDGER` value, never `import`, per D-13. This plan changes neither parsing strategy, so no new execution path over untrusted input is introduced. |
| T-174-SC | Tampering | npm/pip/cargo installs | medium | accept | This plan installs no package. The checker remains standard library only; the app-side change adds only `copy`, which ships with Python. `174-RESEARCH.md`'s Package Legitimacy Audit records zero packages and zero flagged rows for this phase, and HYG-02's no-new-runtime-dependency rule is unaffected because nothing here is a runtime dependency. |
</threat_model>

<verification>
Run from `/workspaces`:

1. `python3 tools/rekey/check_rekey_ledger.py` — exit 0, one `OK: 6 ledger row(s), 6 MILESTONES.md row(s) bound` line. The real pair must still bind; a gate that is always red is as useless as one that is always green.
2. `test "$(/usr/bin/grep -cE '^\s*#' tools/rekey/check_rekey_ledger.py)" -eq 1` — the shebang and nothing else.
3. All three evidence transcripts exist under `.planning/phases/174-blast-radius-invariance-harness/evidence/` with the `174-06-` prefix, and between them carry seven `rc_prefix_*=0` RED lines against the two pinned blobs.
4. `(cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py tests/test_devtest_issue_corpus.py tests/test_part_number_delta_drift.py)` — exit 0, at least 110 passed, zero failed.
5. `git -C firestarter_app status --porcelain firestarter/` and `git -C firestarter status --porcelain` both empty.

Two things that are NOT criteria and must not be treated as regressions:

- The whole app suite is RED at the branch base for the three pre-existing `tests/test_skip_census.py`
  suite-duration failures recorded at phase close. Leave them exactly as found.
- Plan `174-05`'s Task 2 verify asserts `ls .planning/phases/174-blast-radius-invariance-harness/174-0*-SUMMARY.md | wc -l` equals 5 and that at least eight evidence transcripts exist. This plan adds a sixth summary and three transcripts, so re-running that historical leg will report 6 rather than 5. That is this plan landing, not damage — the count assertion belongs to a closed plan's own record, and it is not re-run as an acceptance criterion here.
</verification>

<success_criteria>
- A duplicated `ledger_id` in `MILESTONES.md` exits 2 naming the id, instead of the last row silently winning.
- A corrupted `shape_id`, a wrong or uppercased `before_hash`, and any `after` cell that is not the exact `(undeclared)` literal each exit 1 with an ERROR naming the mismatch — closing CR-02 leg (b) and WR-01 together.
- An emptied ledger table exits 1 naming zero rows against six.
- The checker's output is byte-stable across consecutive identical runs.
- Every new leg was seen exit 0 against a pinned pre-fix blob before being seen non-zero against the fix, with both `rc` values committed in an evidence transcript.
- No two `shape_id`s share a `results` or `plan` object, and a mutation through one no longer moves another's frozen hash.
- Every frozen hash, ladder pin, `shape_id`, corpus row and delta value is byte-identical to its pre-plan state, and the four-module suite still passes.
- No comment line was added to any source file; the checker still carries exactly its shebang.
</success_criteria>

## Flagged Assumptions

The deterministic edge probe surfaced eleven applicable rows. Five belong to GATE-06 and are resolved
explicitly in `must_haves.truths` above — `adjacency` as the duplicate-row collision, `empty` as the
zero-row table, `boundary` as the one-character-either-side `after` cell, `precision` as exact
case-sensitive full-string hash comparison, and `ordering` as byte-stable output across runs. None was
dismissed and none needed a backstop marker.

The remaining six rows belong to requirements this plan does not re-open, and are carried here as flagged
assumptions rather than silently dropped:

- **GATE-01 / boundary** — resolved by plans 174-01 and 174-02: the twelve-lowercase-hex frozen-hash form is asserted by `test_frozen_hashes_are_twelve_lowercase_hex_chars` and the truncation boundary by `test_dedup_fingerprint_truncation_is_a_plain_slice`. Not re-authored here.
- **GATE-01 / precision** — resolved by plan 174-01: `test_dedup_fingerprint_moves_one_step_either_side_of_the_frozen_shape` covers the one-step-either-side case. Not re-authored here.
- **GATE-02 / unclassified** — resolved by plans 174-01 through 174-03: every hash assertion compares against an absolute literal, verified in full by the verifier. Not re-authored here.
- **GATE-03 / unclassified** — resolved by plans 174-01 and 174-02: `LADDER_PINS` plus `test_ladder_pins_cover_all_four_build_db_diff_arms`. Not re-authored here.
- **GATE-04 / unclassified** — resolved by plan 174-04: `part_number_delta.json` with an independently re-run `--check`. Not re-authored here.
- **GATE-05 / unclassified** — resolved by plans 174-01 through 174-04: the sixteen snapshots and the twenty-six-row corpus, both under `firestarter_app/tests/fixtures/`. Not re-authored here.

Eleven surfaced, five authored into `must_haves`, six flagged — no silent drops.

Two capability detectors were run rather than assumed, and both no-opped:
`api-coverage.cjs` returned `detected:false` with an empty `signals` array over the phase scope, and
`assumption-delta scan 174 --json` returned `detected:false` with an empty `signals` array. No coverage
matrix and no assumption-delta decision block is required. The schema-push detector finds no ORM or
migration file in scope — this plan touches one standard-library Python checker and three test modules —
so it no-ops too.

## Artifacts this phase produces

Modified in the meta repo (paths relative to `/workspaces`):

- `tools/rekey/check_rekey_ledger.py` — new module constant `_UNDECLARED = "(undeclared)"`; `parse_milestones_rows` gains a duplicate-`ledger_id` guard raising `LedgerParseError("duplicate MILESTONES.md row for ledger_id {ledger_id!r}")`; `check()` gains a zero-row guard and an undeclared-branch comparison of `shape_id`, `before_hash` and the exact `(undeclared)` literal, and loses the `_HASH_RE.match(m_row[2])` test; the module docstring's exit-code table and guarantee paragraph are corrected. No new function or class is introduced.

New in the meta repo:

- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-06-duplicate-row-red-green.txt`
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-06-undeclared-row-red-green.txt`
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-06-shape-aliasing-red-green.txt`

New symbols in `firestarter_app` (committed inside the submodule, on `gsd/v1.36-dev-test-fidelity`):

- `tests/test_rekey_ledger.py` — `test_duplicate_milestones_row_for_one_ledger_id_fails_closed`, `test_corrupted_undeclared_row_shape_id_exits_one`, `test_corrupted_undeclared_row_before_hash_exits_one`, `test_uppercased_before_hash_exits_one`, `test_after_cell_that_is_not_the_undeclared_literal_exits_one`, `test_milestones_with_zero_rekey_rows_exits_one`, `test_checker_error_output_is_order_stable`
- `tests/test_blast_radius_invariance.py` — `test_build_shape_never_shares_results_or_plan_between_shape_ids`, `test_mutation_through_a_derived_shape_does_not_move_the_base_shapes_frozen_hash`

Modified in `firestarter_app`:

- `tests/fixtures/report_shapes.py` — module-level `import copy`; `_clone_with_chip_override` returns a `DiagnosticReport` carrying `copy.deepcopy(report.results)` and `copy.deepcopy(report.plan)`; the module docstring's caching-safety paragraph and the function's own docstring are corrected to name CR-01. No new function, class or `shape_id` is introduced, and `SHAPE_IDS`, `FROZEN_HASHES`, `LADDER_PINS` and `RESERVED_SHAPE_IDS` are unchanged.

No production module in `firestarter_app/firestarter/` and no firmware file is touched.

<output>
Create `.planning/phases/174-blast-radius-invariance-harness/174-06-SUMMARY.md` when done. It must
transcribe, verbatim, the `rc_prefix_*` RED value beside the `rc_fixed_*` GREEN value for every one of
the seven legs, quote the exact new `ERROR:` line texts, and state which of the two CR-01 fixes was taken
and why — so Phase 181's planner can read the reasoning without re-deriving it.
</output>
