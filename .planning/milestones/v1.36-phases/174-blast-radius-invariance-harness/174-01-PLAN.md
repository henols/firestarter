---
phase: 174-blast-radius-invariance-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/fixtures/report_shapes.py
  - firestarter_app/tests/fixtures/reports/sst27sf512-six-step.json
  - firestarter_app/tests/fixtures/rekey_ledger.py
  - firestarter_app/tests/fixtures/planted_rekey_mutation.py
  - firestarter_app/tools/snapshot_report_shapes.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_rekey_ledger.py
  - tools/rekey/check_rekey_ledger.py
  - .planning/MILESTONES.md
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-01-tracer-end-to-end.txt
  - .planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt
autonomous: false
requirements: [GATE-01, GATE-02, GATE-03, GATE-05, GATE-06]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One shape_id, sst27sf512-six-step, travels the whole harness: a builder in firestarter_app/tests/fixtures/report_shapes.py constructs a real DiagnosticReport, the real dedup_fingerprint returns the absolute literal 4dc282a5d596, the real build_db_diff returns a pinned (proposed_disposition, ladder_state) pair, its to_dict() output is committed as tests/fixtures/reports/sst27sf512-six-step.json, its ledger row is in tests/fixtures/rekey_ledger.py, and the meta-side checker tools/rekey/check_rekey_ledger.py reads that row and finds its counterpart row in .planning/MILESTONES.md — per D-01 and D-13."
    - "The frozen assertion is against the absolute string 4dc282a5d596 taken off a real DiagnosticReport by the real dedup_fingerprint; no assertion in tests/test_blast_radius_invariance.py compares one computed fingerprint against another — per D-01 and GATE-02."
    - "Every hash literal in tests/fixtures/report_shapes.py and tests/fixtures/rekey_ledger.py is produced by a builder committed in the same commit; a test resolves each ledger row's shape_id to a builder and recomputes it, so no number survives in the tree that nothing in the tree can compute — per D-09."
    - "The 12-hex contract is exact and asserted: dedup_fingerprint truncates hashlib.sha256(canonical).hexdigest() to its first 12 characters with no rounding and no tie-breaking, every frozen literal matches the twelve-lowercase-hex-character shape, and the assertion compares the function's return value to the full literal rather than to a prefix of it."
    - "A shape one step either side of the frozen input hashes differently: removing the trailing blank-check step from sst27sf512-six-step, and appending one further OK step to it, each produce a fingerprint that is not 4dc282a5d596 — the boundary either side of the frozen shape is measured, not assumed."
    - "tests/fixtures/report_shapes.py is importable as tests.fixtures.report_shapes from a sibling test module without pulling tests/test_diagnostic_report.py into the import graph — per D-03, so Phases 175 through 181 can import shapes without inheriting that module's 68 dedup_fingerprint call sites."
    - "Every frozen shape carries a stable string shape_id; the frozen-hash table, the ladder pin, the committed JSON filename and the ledger row all key off that same string, and no positional index appears in any of them — per D-04."
    - "The frozen table goes RED when the guarded behaviour moves: clearing the write step's fingerprint on a built sst27sf512-six-step report makes dedup_fingerprint return a value other than 4dc282a5d596, and that RED was observed and transcribed into evidence/174-01-anti-vacuity-red-green.txt before the phase closed."
    - "The meta-side checker exits non-zero on a ledger row whose declared after_hash has no matching .planning/MILESTONES.md row, exits non-zero when either input path is missing, and exits zero on the real pair — all three legs observed in evidence/174-01-anti-vacuity-red-green.txt."
    - "The meta-side checker reads the app ledger with ast rather than by importing it, and tests/fixtures/rekey_ledger.py's rows are plain tuples of strings and None so ast.literal_eval can evaluate them — per D-13."
    - "A declared re-key is expressible as a commit that touches only a ledger row's after_hash and the MILESTONES.md counterpart: the ledger module holds no executable logic that a behaviour change would have to move at the same time — per D-11, which binds Phases 175 through 181 as much as this one."
  artifacts:
    - path: firestarter_app/tests/fixtures/report_shapes.py
      provides: "Shape builders, the SHAPE_IDS registry and the FROZEN_HASHES table keyed by shape_id"
      contains: "sst27sf512-six-step"
    - path: firestarter_app/tests/fixtures/reports/sst27sf512-six-step.json
      provides: "Committed to_dict() output snapshot for the tracer shape (GATE-05)"
    - path: firestarter_app/tests/fixtures/rekey_ledger.py
      provides: "Append-only (shape_id, before_hash, after_hash, ledger_id) rows"
      contains: "RK-174-01-p177-readback-gating"
    - path: firestarter_app/tests/fixtures/planted_rekey_mutation.py
      provides: "Deliberately-broken ledger module fed to the meta checker through its path override"
    - path: firestarter_app/tools/snapshot_report_shapes.py
      provides: "Deterministic generator for the committed to_dict() snapshots, with --check drift mode"
    - path: firestarter_app/tests/test_blast_radius_invariance.py
      provides: "Absolute-hash, ladder, snapshot-drift and anti-vacuity assertions"
    - path: firestarter_app/tests/test_rekey_ledger.py
      provides: "Append-only ledger sweep and shape_id resolution"
    - path: tools/rekey/check_rekey_ledger.py
      provides: "Cross-tree checker binding the app ledger to .planning/MILESTONES.md"
    - path: .planning/MILESTONES.md
      provides: "The v1.36 re-key ledger section (GATE-06)"
      contains: "RK-174-01-p177-readback-gating"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-01-tracer-end-to-end.txt
      provides: "Transcript of the one shape travelling all eight layers"
    - path: .planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt
      provides: "Observed RED then GREEN for every anti-vacuity leg"
  key_links:
    - from: firestarter_app/tests/fixtures/report_shapes.py
      to: firestarter_app/firestarter/diagnostic_report.py
      via: "build_shape returns a real DiagnosticReport; the test calls the real dedup_fingerprint on it, never a reimplementation of the hash"
      pattern: "from firestarter.diagnostic_report import"
    - from: firestarter_app/tests/fixtures/rekey_ledger.py
      to: firestarter_app/tests/fixtures/report_shapes.py
      via: "every ledger row's shape_id is a member of SHAPE_IDS and resolves to a builder"
      pattern: "shape_id"
    - from: tools/rekey/check_rekey_ledger.py
      to: firestarter_app/tests/fixtures/rekey_ledger.py
      via: "ast parse of the ledger module from the meta side; never a cross-tree import"
      pattern: "ast"
    - from: tools/rekey/check_rekey_ledger.py
      to: .planning/MILESTONES.md
      via: "every declared after_hash must have a ledger_id row in the MILESTONES re-key table"
      pattern: "RK-174-"
  prohibitions:
    - statement: "No hash literal is written into the tree that no committed builder in the same commit can compute. An inherited number, a number copied from CONTEXT.md, and a number recalled rather than run are all forbidden."
      status: required
      verification: judgment
    - statement: "A declared re-key never lands in the same commit as the behaviour change that caused it. The behaviour change lands and turns the gate RED; a second commit touching only after_hash and its MILESTONES.md row makes it green."
      status: required
      verification: judgment
---

<objective>
Carry ONE shape_id — `sst27sf512-six-step` — through every layer of the blast-radius harness, end to end,
and watch each layer go RED before it goes green.

The layers, in order: a builder that constructs a real `DiagnosticReport`; the real `dedup_fingerprint`
returning the absolute literal `4dc282a5d596`; the real `build_db_diff` returning a pinned disposition and
ladder pair; a committed `to_dict()` JSON snapshot; a machine-readable ledger row; a `MILESTONES.md`
counterpart row; and a meta-side checker that reads both trees and binds them together.

Purpose: this is the thinnest complete path through the oracle the rest of v1.36 is measured against. If the
architecture is wrong — if the builder cannot reach the shape, if the snapshot cannot be made
byte-stable, if the meta checker cannot see the app tree — it is wrong after one commit, not after ten.
Every later plan in this phase is expansion across a spine this plan proves.

Output: seven new source files, one `MILESTONES.md` section, and two committed evidence transcripts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/174-blast-radius-invariance-harness/174-CONTEXT.md
@.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md
@.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<environment_preamble>
Every command in this plan runs from `/workspaces` unless it says otherwise, and every Python
invocation goes through the py3.11 CI-replica interpreter, never the devcontainer's default 3.12.

The replica venv already exists and was verified this planning session: `firestarter_app/.venv311`
is Python 3.11.16 with pytest 9.1.1, and `uv venv` wrote a `.gitignore` containing `*` inside it so
it does not dirty the submodule. Use `./.venv311/bin/python` from inside `firestarter_app`.

If and only if that venv is absent, rebuild it with `export UV_CACHE_DIR` pointing somewhere
writable (`/home/vscode/.cache/uv` is not writable in this devcontainer), then
`uv venv --python 3.11 .venv311`, then
`uv pip install --python .venv311/bin/python -e '.[test]'`. A bare `pip install` inside the
activated venv installs into system Python 3.12, because `uv venv` ships no pip.

Four further measured environment facts bind every command below:
- `grep` in this devcontainer is ugrep and honors `.gitignore`, so it silently under-scans. Use
  `/usr/bin/grep` for anything asserted as gate evidence.
- `pytest` `addopts` is `-ra -q`. Clear it with `-o addopts=""` and pass exactly one `-q` so the
  `N passed` summary line lands at the start of a line.
- The app suite is RED at the branch base for three pre-existing, unrelated reasons: all three
  failures are in `tests/test_skip_census.py` and all three are `subprocess.TimeoutExpired`, because
  that test caps a child whole-suite run at 180 s while the suite takes 740.92 s. None of the three
  touches `dedup_fingerprint`, `build_db_diff`, `derive_plan` or `tests/fixtures/`. They are not this
  phase's to fix, they must not be fixed as collateral, and no command in this phase may use "the
  whole suite is green" as its criterion.
- `tests/test_flash_path_record_sync.py` asserts whole-repo porcelain, so commit before running any
  command that would collect it. No command in this plan collects it.

Commits: the app-side files are committed INSIDE the `firestarter_app` submodule on branch
`gsd/v1.36-dev-test-fidelity` (both repos are already on that branch, both working trees were clean at
the branch base). The submodule gitlink, `tools/rekey/check_rekey_ledger.py`, `.planning/MILESTONES.md`
and the evidence files are committed in the meta repo.

Zero comments. Not one `#` line beyond a shebang in any file this plan writes, and no HTML comment in
any Markdown it writes. All reasoning goes in docstrings. The code excerpts in 174-RESEARCH.md and
174-PATTERNS.md carry `# Source:` lines that are quotations of existing code — they must not be copied
through. Docstrings are not comments and are required.
</environment_preamble>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the shape_id name set before it becomes a published contract</name>
  <decision>Freeze the concrete `shape_id` string set that Phases 175 through 181 and the `MILESTONES.md` re-key ledger will reference by name.</decision>
  <context>
D-04 locked the *mechanism* — every frozen shape carries a stable string `shape_id`, and positional
indices were rejected. What D-04 did not settle is the concrete list of names, and D-04's own
reversibility rating is one-way *once ledger rows exist*: a `shape_id` becomes a published contract
between the app-side table and `MILESTONES.md`, so renaming one after a re-key is declared orphans that
ledger row. 174-RESEARCH.md's Open Question 5 asks for exactly this list to be settled at plan time
rather than during execution.

The full sixteen-name set the plans propose, with the hash each one was measured to produce this
planning session (every value below was recomputed against `firestarter_app @ 49bac1a` and reproduces
byte-exactly):

Hand-specified table, immune to database regeneration (D-02 table 1):

| shape_id | frozen hash |
|---|---|
| `sst27sf512-six-step` | `4dc282a5d596` |
| `sst27sf512-six-step-readback-gated` | `60a031573aab` |
| `gh47-sst27sf512-pass` | `f9dbc31dcd27` |
| `gh28-m27c512-fail` | `31547956e56b` |
| `gh20-at28c256-fail` | `00e121446ceb` |
| `gh23-w27e257-fail` | `7a89fcea856a` |
| `synthetic-arm4-no-ok` | `f90dfe1a44f7` |
| `synthetic-arm4-empty-results` | `8d6208d00be7` |

Real-path table, built through `derive_plan` and `run_plan` (D-02 table 2):

| shape_id | frozen hash |
|---|---|
| `m27c512-full-all-ok` | `6d3afbc52315` |
| `m27c512-full-blank-check-bad` | `077a32d1a5c4` |
| `m27c512-full-canonical-name` | `776846bf2dc8` |
| `m27c512-full-comma-joined-name` | `37ad34d39a19` |
| `m27c512-full-runs-1` | `e4838f7bb1d3` |
| `at28c256-full-all-ok-sdp` | `52fb759dc48c` |
| `sst27sf512-full-all-ok` | `4b3e52cab987` |
| `w27e257-full-all-ok` | `22908e2954c3` |

Three further names are RESERVED, not frozen, because the fields they describe do not exist yet:
`prune03-synthesized-fingerprint-match` (Phase 177), `attr01-status-axis-transport-fault` (Phase 178),
`uv-slot-write-pass` (Phase 179). Reserving them now is what D-04 buys — the name is claimed before a
later phase needs it, and D-10's completeness pin stays exact because reserved names live in a separate
`RESERVED_SHAPE_IDS` set that is asserted disjoint from `SHAPE_IDS`.

Two of these names exist because measurement contradicted an inherited claim, and the reader should see
that before approving:
- `m27c512-full-canonical-name` at `776846bf2dc8` replaces CONTEXT.md D-12's inherited
  `a00791f1c2b4` to `a6f6c6354047` pair. Neither inherited value reproduces from any m27c512 report
  shape; 174-RESEARCH.md exhausted roughly 2.1e8 candidate canonical strings and got zero hits.
- `uv-slot-write-pass` is reserved rather than frozen because the m27c512 write step is unreachable with
  the shipped `_mock_operator` double, so any hash frozen for it today would encode a mock artifact
  rather than a UV write.
  </context>
  <options>
    <option id="full-sixteen">
      <name>Freeze all sixteen names, reserve the three later-phase names</name>
      <pros>Every one of D-05's named later phases has a row to be measured against; all four of D-12's re-key paths have a shape; D-08's four ladder arms are all reachable including the non-SDP all-OK arm; D-06's four filed-issue reproductions are each their own named shape. Every hash was measured this session and reproduces.</pros>
      <cons>Sixteen names is sixteen one-way contracts. A later phase that wants a shape not on this list has to add a name rather than rename one.</cons>
    </option>
    <option id="minimal-four">
      <name>Freeze only the four D-12 re-key-path shapes, drop the rest</name>
      <pros>Four names, four contracts. Matches the roadmap's literal floor of four measured re-key shapes.</pros>
      <cons>ATTR-04's criterion — the status axis is confirmed by this phase's oracle reporting zero unexpected hash changes — would have no row to confirm against, which is the exact reason D-05 rejected the floor of four. D-08's non-SDP all-OK arm and D-06's four filed-issue reproductions both become unreachable. This option cannot satisfy D-05, D-06 or D-08 as locked.</cons>
    </option>
  </options>
  <resume-signal>Reply "full-sixteen" to approve the proposed set, "minimal-four" to take the reduced set, or name the specific shape_id strings you want changed.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One shape_id through all eight layers — builder, hash, ladder, snapshot, ledger, MILESTONES row, meta checker</name>
  <reversibility rating="one-way">The `shape_id` strings and the four-tuple ledger row shape are read by Phases 175 through 181 and by the meta-side checker; renaming a shape_id after a re-key is declared orphans that ledger row, and changing the row shape means rewriting every declared row.</reversibility>
  <files>firestarter_app/tests/fixtures/report_shapes.py, firestarter_app/tools/snapshot_report_shapes.py, firestarter_app/tests/fixtures/reports/sst27sf512-six-step.json, firestarter_app/tests/fixtures/rekey_ledger.py, firestarter_app/tests/test_blast_radius_invariance.py, firestarter_app/tests/test_rekey_ledger.py, tools/rekey/check_rekey_ledger.py, .planning/MILESTONES.md, .planning/phases/174-blast-radius-invariance-harness/evidence/174-01-tracer-end-to-end.txt</files>
  <read_first>
    - `firestarter_app/firestarter/diagnostic_report.py` lines 40 to 60 (`SCHEMA_VERSION`), 66 to 120 (`AutoCapture`, `TransportHealth`), 180 to 265 (`dedup_fingerprint` and the four `_DISPOSITION_*` and four `_LADDER_*` constants), 287 to 325 (`build_db_diff`), 542 to 600 (the `DiagnosticReport` dataclass and `_utc_now`), 619 to 750 (`_voltage_dict`, `_banner_dict`, `_db_diff_dict`, `_step_dict`), 771 to 791 (`to_dict`)
    - `firestarter_app/tests/test_diagnostic_report.py` lines 151 to 198 (`_minimal_report` — the builder signature and the `step_specs` tuple contract to generalize), lines 1311 to 1338 (`_coverage_report` — the post-construction stamping seam and the docstring that licenses direct construction), lines 1359 to 1381 (the one shipped frozen-literal test and its "a GATE, not a claim" docstring), line 1373 (the deferred-import idiom at the assertion site), lines 715 to 750 (`test_ladder_state_verdict_mapping` and the triple-equality idiom against the `_LADDER_*` constants — do not duplicate its ladder-tag assertions)
    - `firestarter_app/tests/test_erase_flag_invariants.py` lines 1 to 68 (the module-docstring anti-vacuity and Reachability blocks), lines 96 to 98 (the shared deterministic database instance), lines 264 to 296 (the pinned constant and the element-wise list-equality assertion with a drift-naming message)
    - `firestarter_app/tests/test_dev_test_cmd.py` line 81 (the shipped `from .fixtures.<module> import` relative-import idiom — this is how a test module reaches `tests/fixtures/` today, and it works with no `__init__.py` in that directory)
    - `firestarter_app/tools/gen_sdp_bus_config.py` lines 1 to 36 (the module-docstring contract block) and lines 343 to 421 (the `--target` / input-override / `--check` argparse seams, validate-before-emit, exit-code discipline, and the `write_text(encoding="utf-8", newline="\n")` byte-identical rule)
    - `firestarter_app/tools/check_diagnostic_report_claims.py` lines 88 to 100 and 229 to 240 (the path-override seam and `main(argv)` shape a checker uses so a planted input can be fed to it)
    - `tools/catalog/codegen.py` lines 1 to 40 (the only tracked meta-side Python script — its determinism contract, its `--check` flag, and the stdlib-only plus `sys.exit(main())` house shape)
    - `.planning/MILESTONES.md` lines 1 to 12 (the milestone-section header format) and lines 361 to 400 (the v1.33 "Post-Close Correction: The Sweep's Oracle Was Blind" section — the house precedent for recording a measurement that falsified a prior claim, and the structure to copy)
    - `.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md` sections "Measured Facts", "The four ladder arms (measured)", "Schema pins available for D-07", "Pitfall 1", "Pitfall 3", "D-13"
    - `.planning/phases/174-blast-radius-invariance-harness/174-PATTERNS.md` sections for `report_shapes.py`, `test_blast_radius_invariance.py`, `test_rekey_ledger.py`, `rekey_ledger.py`, `check_rekey_ledger.py`, and "Shared Patterns"
  </read_first>
  <behavior>
    - Building `sst27sf512-six-step` and calling the real `dedup_fingerprint` on it returns exactly `4dc282a5d596`.
    - Removing the trailing `blank-check` step from that shape returns something other than `4dc282a5d596`.
    - Appending one further `("read", "OK", None, "")` step to that shape returns something other than `4dc282a5d596`.
    - `build_db_diff("sst27sf512", db, results)` on that shape returns `proposed_disposition == "inconclusive -- needs N>=2 agreement (advisory)"` and `ladder_state == ""`, because two of its steps carry an `indeterminate` fingerprint classification and `build_db_diff`'s second arm fires on that.
    - `sorted(report.to_dict())` equals the eleven-element list `["auto_capture", "banner", "db_diff", "dedup_fingerprint", "generated", "is_submittable", "schema_version", "sdp_hold_state", "steps", "transport_health", "voltage"]`.
    - `tools/snapshot_report_shapes.py --check` exits 0 against the committed snapshot and exits 1 when the committed snapshot is altered by one byte.
    - `tools/snapshot_report_shapes.py --check --target-dir <a directory holding no snapshot>` exits 1, never 0.
    - Every row in `LEDGER` resolves to a member of `SHAPE_IDS`, and recomputing that shape's fingerprint equals `after_hash` when `after_hash` is not `None` and `before_hash` otherwise.
    - `python3 tools/rekey/check_rekey_ledger.py` exits 0 against the real app ledger and the real `.planning/MILESTONES.md`.
    - The same checker exits non-zero when pointed at a ledger whose declared `after_hash` has no matching `MILESTONES.md` row, and exits non-zero when either input path does not exist.
  </behavior>
  <action>
Build the one-shape spine. Each layer is the thinnest thing that is production-quality, not a stub —
nothing here is placeholder work, and every file this task creates is the file later plans expand.

**Layer 1, `firestarter_app/tests/fixtures/report_shapes.py`.** Generalize `_minimal_report`
(`tests/test_diagnostic_report.py:151`), do not design fresh. Export:
`build_shape(shape_id)` dispatching on a module-level mapping; `build_shape_from_step_specs(*, chip, protocol, step_specs, run_counts=None, coverage_policy=None)`
as the general builder, where `step_specs` is the same `(op, verdict, fingerprint_classification, reason)`
tuple list the analog takes and `fingerprint_classification=None` means no `Fingerprint` is attached;
`SHAPE_IDS` as a module-level sorted `tuple[str, ...]`; `FROZEN_HASHES` as a `dict[str, str]`;
and `RESERVED_SHAPE_IDS` as a frozenset. In this task `SHAPE_IDS` holds exactly one member,
`"sst27sf512-six-step"`, and `FROZEN_HASHES` maps it to `"4dc282a5d596"`.

`run_counts` is an optional `dict[str, int]` stamped onto the built `StepResult.run_count` values after
construction, the same seam `_coverage_report` uses at `:1336`; it is what lets the real
`repeat_policy_tag` fire. `coverage_policy` is an optional `region_policy` string; when given, stamp a
real `WriteTarget` onto the write step's result so the real `coverage_tag` fires. Never append a tag
string by hand — the tags must come out of `firestarter.chip_test.repeat_policy_tag` and
`firestarter.chip_test.coverage_tag` as production computes them. Note `StepResult.run_count` defaults to
`0`, so an unstamped shape produces no tag at all.

The `sst27sf512-six-step` builder is hand-specified, not `derive_plan`-derived, and the module docstring
must say why: 174-RESEARCH.md recovered its full canonical pre-image
`SST27SF512|7|id=OK:|read=OK:|write=OK:indeterminate|verify=OK:indeterminate|erase=OK:|blank-check=OK:`
and the chip name in it is the uppercase `SST27SF512`, not the lowercase raw CLI token, so it is not any
real `run_plan` output. Build it with `chip="SST27SF512"`, `protocol="7"`, and step_specs in exactly this
order: `("id", "OK", None, "")`, `("read", "OK", None, "")`, `("write", "OK", "indeterminate", "")`,
`("verify", "OK", "indeterminate", "")`, `("erase", "OK", None, "")`, `("blank-check", "OK", None, "")`.
No `run_counts`, no `coverage_policy`.

Add the one shared deterministic database instance, copying `tests/test_erase_flag_invariants.py:96-98`:
a module-level `EpromDatabase(skip_local_override=True)`. Put the reason in the module docstring: without
`skip_local_override=True` a developer's `~/.firestarter/database.json` can move a frozen hash.

**Layer 2, `firestarter_app/tools/snapshot_report_shapes.py`.** Copy the shape of
`tools/gen_sdp_bus_config.py`: stdlib only, `argparse`, `sys.exit(main())`, validate before emit, and
`--target-dir` (default `tests/fixtures/reports`), `--shape` (repeatable, default all of `SHAPE_IDS`) and
`--check`. Emission is `json.dumps(payload, indent=2, sort_keys=True) + "\n"` written with
`write_text(encoding="utf-8", newline="\n")`, after `mkdir(parents=True, exist_ok=True)`.

The load-bearing detail the analog does not have: `DiagnosticReport.to_dict()` puts a live UTC timestamp
in its `generated` key via `_utc_now` (`diagnostic_report.py:587`), so a raw snapshot is not
byte-stable. Normalise it — replace the `generated` value with the fixed sentinel
`"1970-01-01T00:00:00Z"` before emission, keeping the key itself present so D-07's key-list pin still
sees it. The normalisation function is exported so the drift test applies exactly the same one; there
must be one normaliser, not two. Also compose a real `DbDiff` into the report via `build_db_diff` before
snapshotting, so `db_diff` is a populated object rather than `None`. Add a `_generated_by` key naming
this script — JSON has no comment syntax, so that key is the do-not-edit banner, and the drift test
asserts it absolutely. Exit codes: 0 on success or a clean `--check`, 1 on a `--check` drift or a missing
`--check` target, 2 on a shape_id that is not in `SHAPE_IDS`. `--check` against a nonexistent target
prints a drift line and returns 1; it must never return 0.

**Layer 3, `firestarter_app/tests/fixtures/reports/sst27sf512-six-step.json`.** Generated by running the
script, then committed verbatim. Do not hand-write it.

**Layer 4, `firestarter_app/tests/fixtures/rekey_ledger.py`.** A module-level
`LEDGER: tuple[tuple[str, str, str | None, str], ...]` of plain four-tuples, one row per line, in the
order `(shape_id, before_hash, after_hash, ledger_id)`. `after_hash` is `None` until a phase declares the
re-key. Plain tuples of `str` and `None` only, and no dataclass, no enum, no computed expression — the
meta-side checker parses this file with `ast` rather than importing it (D-13), so every row must be
reachable by `ast.literal_eval`.

This task seeds exactly one row:
`("sst27sf512-six-step", "4dc282a5d596", None, "RK-174-01-p177-readback-gating")`.

The `ledger_id` grammar is `RK-174-<NN>-<owner>-<slug>`, where `<owner>` is `p<phase-number>` for a row an
owning phase will be measured against, or the literal `rejected` for a re-key the milestone has decided
not to take. Document the grammar in the module docstring, together with per-row provenance: this row's
owner is Phase 177, its mechanism is the write and verify steps' `indeterminate` fingerprint
classification being emptied when the read-back is gated on failure, and its measured projected
`after_hash` is `60a031573aab`. The projected value belongs in the docstring and in `MILESTONES.md`, NOT
in the `after_hash` column — a filled `after_hash` means "declared", and Phase 177 has not landed.

Keep the module free of executable logic. D-11 requires a declared re-key to be expressible as a commit
touching only an `after_hash` and its `MILESTONES.md` row; logic in this module would drag a second
concern into that commit.

**Layer 5, `firestarter_app/tests/test_blast_radius_invariance.py`.** Reach the fixture module with the
shipped relative-import idiom, `from .fixtures.report_shapes import ...`, as
`tests/test_dev_test_cmd.py:81` does. Import `dedup_fingerprint` and `build_db_diff` at the assertion
site with the deferred-import idiom from `tests/test_diagnostic_report.py:1373`, not at module top.

Write a module docstring carrying two structural blocks copied from
`tests/test_erase_flag_invariants.py:1-68`: an anti-vacuity rule stated up front, naming the specific way
a lazy assertion passes vacuously here (a relational comparison of two computed fingerprints passes
through any change to the hash function, which is why eleven such comparisons ship today and why this
module exists); and a Reachability block that will carry the transcribed observed RED per leg.

The assertions in this task:
- A parametrized frozen-hash test over `sorted(FROZEN_HASHES.items())` asserting
  `dedup_fingerprint(build_shape(shape_id)) == expected`, with a failure message naming the shape_id, both
  values, and the D-11 separate-commit rule. Adapt the "a GATE, not a claim" docstring argument from
  `tests/test_diagnostic_report.py:1360`.
- A twelve-hex contract test: every value in `FROZEN_HASHES` matches a twelve-lowercase-hex-character
  regex, and `dedup_fingerprint` of the tracer shape equals the first twelve characters of
  `hashlib.sha256` over the recovered canonical pre-image string. State in the docstring that the
  truncation is a plain character slice with no rounding and no tie-breaking, and that the assertion
  compares the whole returned value rather than a prefix of it.
- A boundary test one step either side of the frozen shape: build the tracer shape's step_specs with the
  trailing `blank-check` step removed, and separately with one extra `("read", "OK", None, "")` step
  appended, and assert each fingerprint is not `4dc282a5d596`.
- A ladder pin for the tracer shape asserting `proposed_disposition` against the imported
  `_DISPOSITION_INCONCLUSIVE` constant and the literal `"inconclusive -- needs N>=2 agreement (advisory)"`
  in one triple-equality expression, the idiom at `tests/test_diagnostic_report.py:734`. Do not restate
  the four `ladder_state` values — `test_ladder_state_verdict_mapping` (`:715`) already pins all four
  absolutely; the new work here is the disposition text and the shape_id binding.
- A top-level `to_dict()` key-list pin: `sorted(report.to_dict())` equals the eleven-element pinned
  constant, element-wise, with a drift-naming failure message. Use list equality, never a membership or
  subset check.
- A snapshot drift test: the committed JSON parses, its `_generated_by` value is asserted absolutely, and
  it equals the freshly-built normalised `to_dict()` for the same shape_id. No skip marker of any kind on
  this test or any other in this module — the committed artifact is inside this repo, so the
  sibling-repo skip markers in `tests/test_sdp_bus_config_drift.py` must not be copied, and a skip here
  would build exactly the fail-open gate this phase exists to prevent.

**Layer 6, `firestarter_app/tests/test_rekey_ledger.py`.** The append-only sweep: for every row in
`LEDGER`, assert `row[0]` is in `SHAPE_IDS`, that `row[3]` is non-empty and matches the `RK-174-`
grammar, and that `dedup_fingerprint(build_shape(row[0]))` equals `row[2]` when `row[2]` is not `None`
and `row[1]` otherwise. Add a row-shape test asserting every row is a four-tuple of `str`, `str`,
`str | None`, `str` — this is what keeps the file `ast.literal_eval`-able for the meta checker. Add the
Pitfall-1 test: every hash literal appearing anywhere in `LEDGER` is produced by a builder in the same
tree, asserted by resolving each row's shape_id and recomputing, so no number can survive that nothing
can compute.

**Layer 7, `tools/rekey/check_rekey_ledger.py`** in the meta repo. Stdlib only, `argparse`,
`sys.exit(main())` — the house shape of `tools/catalog/codegen.py`. Two overridable input paths,
`--ledger` (default `firestarter_app/tests/fixtures/rekey_ledger.py`) and `--milestones` (default
`.planning/MILESTONES.md`), both resolved from an explicit `--repo-root` defaulting to the current working
directory, NOT from `__file__`'s parent. That last point is load-bearing: this project has a recorded
checker whose `_HERE` resolved to the checker's own directory, so it scanned nothing and exited 0.

Read the ledger with `ast.parse` plus `ast.literal_eval` on the `LEDGER` assignment's value. Never
import it — a cross-tree import would also violate the standing rule that a checker owns its own inputs.
Parse the `MILESTONES.md` re-key table by locating its header row and reading pipe-delimited rows below
it. The check: every ledger row whose `after_hash` is not `None` must have a `MILESTONES.md` row carrying
the same `ledger_id`, the same `before_hash` and the same `after_hash`. A ledger row with
`after_hash is None` requires no `MILESTONES.md` row, but if one exists for that `ledger_id` its
`after_hash` cell must read as undeclared. Also assert the reverse direction: every `RK-174-` row in
`MILESTONES.md` must have a ledger row with that `ledger_id`.

Exit codes: 0 when both directions hold, printing one `OK:` line naming both counts; 1 on any
mismatch, printing one `ERROR:` line per mismatch; 2 when either input path is missing or the `LEDGER`
assignment cannot be found or evaluated. Fail closed — a missing target is never a silent pass.

**Layer 8, `.planning/MILESTONES.md`.** Add a `### v1.36 Re-Key Ledger` section. Copy the structure of
the v1.33 "Post-Close Correction" section at `:361-400`, in its order: a `###` heading with the date
inline; a bolded one-line finding attribution; a bolded framing line; the falsified artifact quoted
literally; a before/after measurement table; a bolded statement of what the old number actually measured;
and a `**What was done about it.**` paragraph.

Its measurement table is the ledger itself, with the columns GATE-06 requires — change, before-hash,
after-hash, date — plus the `ledger_id` and shape_id the checker binds on:

`| ledger_id | shape_id | change | owner | before | after | declared |`

This task adds one row: `RK-174-01-p177-readback-gating`, shape `sst27sf512-six-step`, change "gate the
fingerprint read-back on step failure", owner Phase 177, before `4dc282a5d596`, after `(undeclared)`,
declared `(pending)`. State in prose that the measured projected after-value is `60a031573aab`, and that
a projected value is not a declared one.

The section's falsified-prior paragraph records what this planning session measured and CONTEXT.md D-12
asserts: the SST27SF512 pair reproduces byte-exactly, and three inherited hashes — `a00791f1c2b4`,
`7d1cd4157cfa` and `a6f6c6354047` — do not reproduce from any m27c512 report shape across roughly 2.1e8
candidate canonical strings. That discrepancy is itself the blast-radius finding this ledger exists to
surface, and it belongs in the record on the day the ledger is created.

Finally, write the end-to-end transcript. Run each layer in sequence into
`.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-tracer-end-to-end.txt` with an
`rc=` line after each step, so the evidence file shows the one shape travelling all eight layers in one
readable pass.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/fixtures/report_shapes.py` exists and defines `build_shape`, `build_shape_from_step_specs`, `SHAPE_IDS`, `FROZEN_HASHES` and `RESERVED_SHAPE_IDS`.
    - `FROZEN_HASHES["sst27sf512-six-step"]` is the string `4dc282a5d596`, and `dedup_fingerprint(build_shape("sst27sf512-six-step"))` returns that same string.
    - `firestarter_app/tests/fixtures/reports/sst27sf512-six-step.json` exists, parses as JSON, carries a `_generated_by` value naming `tools/snapshot_report_shapes.py`, and its `generated` value is exactly `1970-01-01T00:00:00Z`.
    - `firestarter_app/tools/snapshot_report_shapes.py --check` exits 0; the same command with `--target-dir` pointed at an empty temporary directory exits 1.
    - `firestarter_app/tests/fixtures/rekey_ledger.py` defines `LEDGER` with exactly one row, and `ast.literal_eval` on the parsed `LEDGER` assignment value succeeds.
    - `python3 tools/rekey/check_rekey_ledger.py` exits 0 and prints one line beginning `OK:`.
    - `python3 tools/rekey/check_rekey_ledger.py --ledger <nonexistent path>` exits 2 and prints a line beginning `ERROR:`.
    - `.planning/MILESTONES.md` contains the string `RK-174-01-p177-readback-gating` and the string `4dc282a5d596`.
    - `tests/test_blast_radius_invariance.py` and `tests/test_rekey_ledger.py` both pass under the py3.11 replica interpreter with no test skipped.
    - Neither new test module contains a comment line, and neither contains a skip marker.
    - `firestarter_app/firestarter/` is byte-unchanged: `git -C firestarter_app status --porcelain firestarter/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && mkdir -p "$E" && EV=$E/174-01-tracer-end-to-end.txt && D=$(mktemp -d) && { echo "== 1 shape builds and hashes"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.fixtures.report_shapes import build_shape, FROZEN_HASHES, SHAPE_IDS; from firestarter.diagnostic_report import dedup_fingerprint, build_db_diff; r=build_shape('sst27sf512-six-step'); fp=dedup_fingerprint(r); print('shape_ids='+repr(sorted(SHAPE_IDS))); print('frozen='+FROZEN_HASHES['sst27sf512-six-step']); print('computed='+fp); assert fp==FROZEN_HASHES['sst27sf512-six-step']"); echo "rc=$?"; echo "== 2 snapshot check clean"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check); echo "rc=$?"; echo "== 3 snapshot check against an empty dir"; (cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check --target-dir "$D/empty"); echo "rc=$?"; echo "== 4 ledger is literal_eval-able"; python3 -c "import ast; t=ast.parse(open('firestarter_app/tests/fixtures/rekey_ledger.py').read()); v=[n.value for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,'id','')=='LEDGER' for x in n.targets)]; rows=ast.literal_eval(v[0]); print('ledger_rows='+str(len(rows))); print('row0='+repr(rows[0]))"; echo "rc=$?"; echo "== 5 meta checker on the real pair"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; echo "== 6 meta checker on a missing ledger"; python3 tools/rekey/check_rekey_ledger.py --ledger "$D/nope.py"; echo "rc=$?"; echo "== 7 the two new test modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 5 && test "$(/usr/bin/grep -c '^rc=1$' "$EV")" -eq 1 && test "$(/usr/bin/grep -c '^rc=2$' "$EV")" -eq 1 && /usr/bin/grep -qx 'frozen=4dc282a5d596' "$EV" && /usr/bin/grep -qx 'computed=4dc282a5d596' "$EV" && /usr/bin/grep -qx "shape_ids=['sst27sf512-six-step']" "$EV" && /usr/bin/grep -qx 'ledger_rows=1' "$EV" && /usr/bin/grep -q "RK-174-01-p177-readback-gating" "$EV" && /usr/bin/grep -qE '^OK: ' "$EV" && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && test "$(/usr/bin/grep -cE '^(FAILED|ERROR) ' "$EV")" -eq 0 && ! /usr/bin/grep -q 'no tests ran' "$EV" && ! /usr/bin/grep -qE '[0-9]+ (failed|error)' "$EV" && ! /usr/bin/grep -qE '[0-9]+ skipped' "$EV" && rm -rf "$D"</automated>
    <fails_when>Any of the seven numbered steps records an rc other than the one it is asserted to produce (exactly five zeros, one 1 from the empty-directory snapshot check, one 2 from the missing-ledger checker); or the transcript lacks the exact lines `frozen=4dc282a5d596` and `computed=4dc282a5d596`; or the pytest leg emits a `no tests ran` line, any `FAILED `/`ERROR ` line, any `N failed`/`N error` count, or any `N skipped` count, or lacks a line starting with a passed count. The three known pre-existing REDs live in `tests/test_skip_census.py`, which this command never collects, so none of these signals can originate from them.</fails_when>
    <automated>cd /workspaces && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/fixtures/report_shapes.py firestarter_app/tests/fixtures/rekey_ledger.py firestarter_app/tests/test_blast_radius_invariance.py firestarter_app/tests/test_rekey_ledger.py | /usr/bin/grep -vE ':0$' | wc -l)" -eq 0 && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tools/snapshot_report_shapes.py)" -eq 1 && test "$(/usr/bin/grep -cE '^\s*#' tools/rekey/check_rekey_ledger.py)" -eq 1 && ! /usr/bin/grep -rqE 'from_dict|literal_eval\(.*to_dict' firestarter_app/tests/fixtures/report_shapes.py firestarter_app/tests/test_blast_radius_invariance.py && ! /usr/bin/grep -rqE 'pytest\.mark\.skip|skipif|requires_fw' firestarter_app/tests/test_blast_radius_invariance.py firestarter_app/tests/test_rekey_ledger.py && ! /usr/bin/grep -qE '== dedup_fingerprint\(' firestarter_app/tests/test_blast_radius_invariance.py && /usr/bin/grep -q 'skip_local_override=True' firestarter_app/tests/fixtures/report_shapes.py && test -z "$(git -C firestarter_app status --porcelain firestarter/)"</automated>
    <fails_when>Any file among the four new test and fixture modules reports a non-zero count of comment lines; either script reports a count other than exactly 1 (its shebang); a report-deserializer identifier is found in the fixture or test module; any skip marker is found in either test module; a comparison of two computed fingerprints is found in the invariance module; the deterministic-database flag is absent from the fixture module; or `git -C firestarter_app status --porcelain firestarter/` prints anything, meaning a production module was modified in a test-only phase.</fails_when>
  </verify>
  <done>One shape_id has travelled all eight layers with every rc as asserted, `4dc282a5d596` is pinned as an absolute literal taken off a real report by the real function, the meta-side checker reads both trees and exits 0, and the transcript is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Plant a mutation at each of the three legs, watch each one RED, restore, watch GREEN</name>
  <files>firestarter_app/tests/fixtures/planted_rekey_mutation.py, firestarter_app/tests/test_blast_radius_invariance.py, firestarter_app/tests/test_rekey_ledger.py, .planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt</files>
  <read_first>
    - `firestarter_app/tests/fixtures/planted_diagnostic_report_claim.py` (whole file, 23 lines — the docstring contract every planted fixture in that directory carries: which checker it feeds, that it is deliberately violating, which requirement authored it, the seam it is injected through, and that it must never be imported)
    - `firestarter_app/tests/test_check_diagnostic_report_claims.py` lines 33 to 100 (the three-leg contract in full: `:52` clean input passes, `:73` planted input fails naming its label, `:92` a nonexistent target fails closed — and the `_FA_DIR` path resolution and subprocess env-override plumbing)
    - `firestarter_app/tests/test_sdp_bus_config_drift.py` lines 101 to 163 (the planted-bad-input leg: the fixture-assumption guard, the deep copy, the deleted target file, and the paired assertions that the exit code is 1 AND that nothing was written)
    - `firestarter_app/tests/test_erase_flag_invariants.py` lines 64 to 68 (the Reachability block that transcribes the observed RED per leg and cites the summary that recorded it)
    - `firestarter_app/tests/test_blast_radius_invariance.py` and `firestarter_app/tests/test_rekey_ledger.py` as Task 2 left them
    - `tools/rekey/check_rekey_ledger.py` as Task 2 left it
    - `.planning/phases/174-blast-radius-invariance-harness/174-RESEARCH.md` sections "Pattern 4: Anti-vacuity via planted mutation, SEEN red" and "Pitfall 3: A gate that is unreachable and therefore proves nothing"
  </read_first>
  <behavior>
    - Leg 1, clean input passes: the frozen-hash test and the meta checker both succeed on the real tree, proving neither gate is accidentally always-red.
    - Leg 2, planted mutation fails: setting the built report's write-step fingerprint to `None` makes `dedup_fingerprint` return something other than `4dc282a5d596`; and pointing the meta checker at `tests/fixtures/planted_rekey_mutation.py` makes it exit 1 naming the offending `ledger_id`.
    - Leg 3, missing or unparsable input fails closed: the meta checker exits 2 for a nonexistent `--ledger`, 2 for a nonexistent `--milestones`, and 2 for a ledger file that parses as Python but has no `LEDGER` assignment.
  </behavior>
  <action>
This is the leg that decides whether the tracer proved anything. A gate authored before the content it
guards can be unreachable, and RED is not proven until it has been seen — this repository has a live
example of the failure mode in a parity test guarding a firmware definition that does not exist.

Write `firestarter_app/tests/fixtures/planted_rekey_mutation.py` as a standalone, never-imported module
carrying the same five-part docstring every planted fixture in that directory carries. Its content is a
`LEDGER` assignment with one four-tuple row whose `after_hash` is a filled twelve-hex string that has no
counterpart row in `.planning/MILESTONES.md` — a declared re-key nobody recorded. Use the shape_id
`sst27sf512-six-step`, the real `before_hash` `4dc282a5d596`, a plainly-fictional `after_hash` of
`000000000000`, and the `ledger_id` `RK-174-99-planted-undeclared`. It is fixture input, never project
source, and `tests/fixtures/` is already excluded from ruff so it is not linted.

Add the in-process planted-mutation test to `tests/test_blast_radius_invariance.py`: build
`sst27sf512-six-step`, set its write step's `fingerprint` attribute to `None`, and assert the resulting
fingerprint is not `4dc282a5d596`. That mutation is the exact change Phase 177 will make when it gates the
read-back on failure, so this leg is not a synthetic poke — it is a rehearsal of the declared re-key.
Add a second in-process leg that mutates the chip name in `auto_capture` and asserts the same
inequality, so the gate is shown sensitive on both axes `dedup_fingerprint` reads. Both legs assert
inequality against the frozen literal, never against a second computed value.

Add the three subprocess legs to `tests/test_rekey_ledger.py`, resolving the repository root from
`Path(__file__).parent.parent.parent` and invoking the meta checker with
`[sys.executable, str(_CHECKER), "--repo-root", str(_REPO_ROOT), "--ledger", ...]` through
`subprocess.run(capture_output=True, text=True)`: the clean-input leg asserting exit 0 and an `OK:` line;
the planted leg asserting exit 1 and that the output names `RK-174-99-planted-undeclared`; and three
fail-closed legs asserting exit 2 for a nonexistent ledger path, a nonexistent milestones path, and a
temporary Python file that parses but declares no ledger. No skip marker on any of them — the checker and
both inputs are inside this working tree, so there is nothing to conditionally skip on, and a skip here
would build the fail-open gate this phase exists to prevent.

Then run every leg into
`.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt` with an
`rc=` line after each, so the file shows each gate observed RED and then observed GREEN again in one
readable pass. Finally, transcribe the observed RED for each leg into the Reachability block of
`tests/test_blast_radius_invariance.py`'s module docstring, citing this evidence file by name — the same
discipline `tests/test_erase_flag_invariants.py:64-68` uses. The existence of an assertion is not the
evidence; the transcribed RED is.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/fixtures/planted_rekey_mutation.py` exists, contains the string `RK-174-99-planted-undeclared`, and is imported by no module in the tree.
    - `python3 tools/rekey/check_rekey_ledger.py --ledger firestarter_app/tests/fixtures/planted_rekey_mutation.py` exits 1 and its output names `RK-174-99-planted-undeclared`.
    - `python3 tools/rekey/check_rekey_ledger.py --milestones <nonexistent path>` exits 2.
    - `tests/test_blast_radius_invariance.py` contains a test whose body clears a built report's write-step fingerprint and asserts inequality against `4dc282a5d596`.
    - The module docstring of `tests/test_blast_radius_invariance.py` contains a Reachability block naming `174-01-anti-vacuity-red-green.txt`.
    - The evidence file records, for each of the five checker invocations and the two in-process mutations, an explicit rc line, with at least one 1 and at least three 2s present.
    - Both new test modules still pass end to end after the planted legs are added, and nothing is skipped.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/174-blast-radius-invariance-harness/evidence && EV=$E/174-01-anti-vacuity-red-green.txt && D=$(mktemp -d) && printf 'OTHER = 1\n' > "$D/noledger.py" && { echo "== A clean ledger, real milestones"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; echo "== B planted undeclared after_hash"; python3 tools/rekey/check_rekey_ledger.py --ledger firestarter_app/tests/fixtures/planted_rekey_mutation.py; echo "rc=$?"; echo "== C ledger path does not exist"; python3 tools/rekey/check_rekey_ledger.py --ledger "$D/nope.py"; echo "rc=$?"; echo "== D milestones path does not exist"; python3 tools/rekey/check_rekey_ledger.py --milestones "$D/nope.md"; echo "rc=$?"; echo "== E parses but declares no ledger"; python3 tools/rekey/check_rekey_ledger.py --ledger "$D/noledger.py"; echo "rc=$?"; echo "== F in-process mutations, both axes"; (cd firestarter_app && ./.venv311/bin/python -c "import sys; sys.path.insert(0,'.'); from tests.fixtures.report_shapes import build_shape; from firestarter.diagnostic_report import dedup_fingerprint; r=build_shape('sst27sf512-six-step'); print('clean='+dedup_fingerprint(r)); w=[x for x in r.results if x.op=='write'][0]; w.fingerprint=None; print('fp_cleared='+dedup_fingerprint(r)); r2=build_shape('sst27sf512-six-step'); r2.auto_capture.chip='sst27sf512'; print('chip_lowered='+dedup_fingerprint(r2))"); echo "rc=$?"; echo "== G clean again, nothing was left mutated on disk"; python3 tools/rekey/check_rekey_ledger.py; echo "rc=$?"; echo "== H both test modules"; (cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py); echo "rc=$?"; } > "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -eq 4 && test "$(/usr/bin/grep -c '^rc=1$' "$EV")" -eq 1 && test "$(/usr/bin/grep -c '^rc=2$' "$EV")" -eq 3 && /usr/bin/grep -q 'RK-174-99-planted-undeclared' "$EV" && /usr/bin/grep -qx 'clean=4dc282a5d596' "$EV" && ! /usr/bin/grep -qx 'fp_cleared=4dc282a5d596' "$EV" && ! /usr/bin/grep -qx 'chip_lowered=4dc282a5d596' "$EV" && /usr/bin/grep -qE '^fp_cleared=[0-9a-f]{12}$' "$EV" && /usr/bin/grep -qE '^chip_lowered=[0-9a-f]{12}$' "$EV" && test "$(/usr/bin/grep -cE '^ERROR: ' "$EV")" -ge 4 && /usr/bin/grep -qE '^[0-9]+ passed' "$EV" && test "$(/usr/bin/grep -cE '^(FAILED|ERROR) ' "$EV")" -eq 0 && ! /usr/bin/grep -qE '[0-9]+ (failed|error|skipped)' "$EV" && rm -rf "$D"</automated>
    <fails_when>The rc tally is anything other than exactly four 0s, one 1 and three 2s; or the transcript lacks the exact line `clean=4dc282a5d596`; or it contains `fp_cleared=4dc282a5d596` or `chip_lowered=4dc282a5d596`, meaning the mutation did not move the hash and the gate is vacuous; or either mutated line is absent or is not twelve lowercase hex characters; or fewer than four lines beginning `ERROR: ` appear, meaning a fail-closed leg exited non-zero without saying why; or the pytest leg lacks a passed count or reports any failed, error or skipped count. `tests/test_skip_census.py` is never collected here, so no signal can come from the three known pre-existing REDs.</fails_when>
    <automated>cd /workspaces && test -z "$(git -C firestarter_app status --porcelain firestarter/)" && ! /usr/bin/grep -rq 'planted_rekey_mutation' firestarter_app/firestarter/ && test "$(/usr/bin/grep -rlc 'import.*planted_rekey_mutation' firestarter_app/tests/ firestarter_app/firestarter/ 2>/dev/null | wc -l)" -eq 0 && /usr/bin/grep -q 'Reachability' firestarter_app/tests/test_blast_radius_invariance.py && /usr/bin/grep -q '174-01-anti-vacuity-red-green.txt' firestarter_app/tests/test_blast_radius_invariance.py && test "$(/usr/bin/grep -cE '^\s*#' firestarter_app/tests/fixtures/planted_rekey_mutation.py)" -eq 0</automated>
    <fails_when>The planted fixture is referenced from production code; any module imports it; the Reachability block or the evidence-file citation is absent from the invariance module's docstring; the planted fixture carries a comment line; or `git -C firestarter_app status --porcelain firestarter/` prints anything.</fails_when>
  </verify>
  <done>All three anti-vacuity legs have been observed RED and then GREEN, the transcript is committed, and the observed RED is transcribed into the test module's own Reachability block.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate to tree | The gate's own verdict crosses here. A gate that cannot fail, or a checker nothing invokes, asserts nothing while reading green — the only threat class that matters in a test-only phase. |
| meta repo to app submodule | The meta-side checker reads a file inside the `firestarter_app` submodule. It parses, never imports, so app-tree content is data on this boundary, not code. |
| developer home to fixture | `~/.firestarter/database.json` can override the shipped chip database and move a frozen hash on one machine only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-01 | Repudiation | `tests/test_blast_radius_invariance.py`, `tools/rekey/check_rekey_ledger.py` | high | mitigate | Task 3's three-leg contract: clean input passes, planted mutation fails, missing or unparsable input fails closed with exit 2 — every leg observed RED and transcribed into `evidence/174-01-anti-vacuity-red-green.txt` and into the module's Reachability block. |
| T-174-02 | Tampering | `tools/rekey/check_rekey_ledger.py` reading the app ledger | high | mitigate | `ast.parse` plus `ast.literal_eval` on the `LEDGER` assignment only. Never `import`, never `eval`, never `exec`. The ledger row shape is constrained to plain tuples of `str` and `None` so no evaluable expression can reach the parser. |
| T-174-03 | Spoofing | path resolution inside `tools/rekey/check_rekey_ledger.py` | high | mitigate | Both inputs resolve from an explicit `--repo-root`, never from `__file__`'s parent. This project has a recorded checker whose directory-relative resolution made it scan nothing and exit 0; Task 3's exit-2 legs prove this one cannot. |
| T-174-04 | Tampering | `tests/fixtures/report_shapes.py` database access | medium | mitigate | The single module-level `EpromDatabase(skip_local_override=True)`, asserted present by grep, so no developer's local override can move a frozen hash. |
| T-174-05 | Spoofing | the twelve-hex `dedup_fingerprint` value | low | accept | It is a non-secret dedup identifier, not an integrity token — its own docstring says so. This plan preserves that framing and neither reimplements the hash nor restates it as a security property. Accepting is correct because nothing in the system authenticates on this value. |
| T-174-SC | Tampering | package-manager installs | medium | accept | No package is installed. 174-RESEARCH.md's Package Legitimacy Audit records zero packages, zero `[ASSUMED]` and zero `[SUS]` rows for this phase; the stack is the already-pinned interpreter, the already-pinned test extras and the standard library, and HYG-02 forbids a new runtime dependency milestone-wide. The `.venv311` replica already exists at the branch base, so no install runs at all in the normal path. |
</threat_model>
<verification>
Run from `/workspaces`, in this order:

1. `cd firestarter_app && ./.venv311/bin/python -m pytest -o addopts="" -q -p no:randomly tests/test_blast_radius_invariance.py tests/test_rekey_ledger.py` — exits 0, prints a passed count, prints no failed, error or skipped count.
2. `python3 tools/rekey/check_rekey_ledger.py` — exits 0, prints one `OK:` line.
3. `cd firestarter_app && ./.venv311/bin/python tools/snapshot_report_shapes.py --check` — exits 0.
4. `test -z "$(git -C firestarter_app status --porcelain firestarter/)"` — no production module was touched.
5. Both evidence transcripts exist and are non-empty, and their rc tallies match the two verify blocks above.

The whole app suite is NOT a criterion here and must not be used as one: it is RED at the branch base for three pre-existing, unrelated reasons in `tests/test_skip_census.py`, all three `subprocess.TimeoutExpired` from that test's 180 s child cap against a 740.92 s suite.
</verification>

<success_criteria>
- `dedup_fingerprint(build_shape("sst27sf512-six-step"))` returns the absolute literal `4dc282a5d596`, asserted against that literal and not against a second computed hash.
- The committed `to_dict()` snapshot for that shape exists, is byte-stable across regeneration, and its drift test has no skip marker.
- One ledger row, one `MILESTONES.md` row, and a meta-side checker that binds them exit 0 together.
- Every gate in the plan has been seen RED: two in-process mutations and three fail-closed checker legs, all transcribed.
- No file this plan wrote carries a comment line beyond a script shebang, and no production module changed.
</success_criteria>

## Planner Assumptions — unresolved edge probes

The deterministic edge probe ran over GATE-01 through GATE-06 and surfaced nine items. Five had a
defensible acceptance criterion and were authored into `must_haves.truths` above as plain strings:
GATE-01's boundary and precision probes here, and GATE-06's adjacency, empty and ordering probes in
plan 174-03. The remaining four came back **unclassified**, so the probe could not say what the edge
even is. They are recorded here as explicitly flagged planner assumptions rather than converted into
truths, because an assumption dressed as a criterion is the failure this phase exists to prevent.

- **GATE-02, unclassified.** Assumption: a re-key of any frozen shape is visible as a non-zero pytest
  exit from `tests/test_blast_radius_invariance.py` alone, with no second surface needed. If GATE-02 has
  a failure surface outside the parametrized frozen table, this plan does not cover it.
- **GATE-03, unclassified.** Assumption: `build_db_diff`'s disposition and ladder output is a pure
  function of the results list plus the chip's `support_status`, so pinning one pair per arm covers the
  requirement. If the function grows a dependency on anything else, four pins are no longer four arms.
- **GATE-04, unclassified.** Carried in plan 174-04. Assumption: a byte-identical regeneration check plus
  an absolutely-asserted aggregate is the whole of what makes the delta measured rather than assumed.
- **GATE-05, unclassified.** Assumption: a committed `to_dict()` output snapshot per shape, with the
  timestamp field normalised, is the whole of what GATE-05 asks for. If the requirement wants report
  shapes the current engine cannot produce, the corpus is narrower than the requirement.

Nine probe items, five authored into `must_haves`, four flagged here. Nothing was dropped and nothing was
auto-dismissed.

## Artifacts this phase produces

New in `firestarter_app` (all paths relative to the submodule root):

- `tests/fixtures/report_shapes.py` — functions `build_shape(shape_id)`, `build_shape_from_step_specs(*, chip, protocol, step_specs, run_counts=None, coverage_policy=None)`; module constants `SHAPE_IDS` (sorted tuple of str), `FROZEN_HASHES` (dict str to str), `RESERVED_SHAPE_IDS` (frozenset); one module-level `EpromDatabase(skip_local_override=True)` instance.
- `tools/snapshot_report_shapes.py` — CLI flags `--target-dir`, `--shape` (repeatable), `--check`; exported snapshot normaliser replacing the `generated` value with the sentinel `1970-01-01T00:00:00Z`; emits a `_generated_by` key; exit codes 0, 1, 2.
- `tests/fixtures/reports/sst27sf512-six-step.json` — committed `to_dict()` output snapshot.
- `tests/fixtures/rekey_ledger.py` — module constant `LEDGER`, a tuple of four-tuples `(shape_id, before_hash, after_hash, ledger_id)`; the `ledger_id` grammar `RK-174-<NN>-<owner>-<slug>`.
- `tests/fixtures/planted_rekey_mutation.py` — a never-imported `LEDGER` carrying the deliberately-undeclared `ledger_id` `RK-174-99-planted-undeclared`.
- `tests/test_blast_radius_invariance.py` — new test module.
- `tests/test_rekey_ledger.py` — new test module.

New `shape_id` string constants introduced by this plan: `sst27sf512-six-step`.

New `ledger_id` string constants introduced by this plan: `RK-174-01-p177-readback-gating`, and the fixture-only `RK-174-99-planted-undeclared`.

New in the meta repo (paths relative to `/workspaces`):

- `tools/rekey/check_rekey_ledger.py` — CLI flags `--repo-root`, `--ledger`, `--milestones`; exit codes 0, 1, 2.
- `.planning/MILESTONES.md` — new section heading `### v1.36 Re-Key Ledger`, with the table header `| ledger_id | shape_id | change | owner | before | after | declared |`.
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-tracer-end-to-end.txt`
- `.planning/phases/174-blast-radius-invariance-harness/evidence/174-01-anti-vacuity-red-green.txt`

<output>
Create `.planning/phases/174-blast-radius-invariance-harness/174-01-SUMMARY.md` when done. It must transcribe the observed RED output for each of the five anti-vacuity legs verbatim, not merely state that the legs exist.
</output>
