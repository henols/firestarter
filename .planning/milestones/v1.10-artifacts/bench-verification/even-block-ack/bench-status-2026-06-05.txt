# 53-07 bench session status — 2026-06-05 (operator-witnessed, post-55 tip)
# NON-FABRICATING record. Task 1 identity capture COMPLETE; Task 2 read/write legs BLOCKED on bench conditions.

## VERIFIED (real, plan-satisfying)
- Port->controller identity (live this session; ACM/USB shuffle across replug):
    ACM0=leonardo  ACM1=uno  USB0=uno328pb
- Raw FW identity (verbatim wire, DEBUG :RURP "OK: FW:"):
    leonardo  -> OK: FW: 3.0.0b6:leonardo          (2 fields = POST-55 pure identity, CAP-01 SC1)  PASS
    uno       -> OK: FW: 3.0.0b6:uno                (2 fields = POST-55 pure identity, CAP-01 SC1)  PASS
    uno328pb  -> OK: FW: 3.0.0b6:uno328pb:512:512   (4 fields = PRE-55; not a 53-07 target; 53-05 board)
- Operator-confirmed silkscreen shield rev: Leonardo (ACM0) = Rev 2.0 (D-07 target). hw override = Rev 2.0-class.
- W27C512 (EEPROM, erasable) seated on Leonardo per operator; write-cycle erase+rewrite pre-approved.
- No re-sideload needed (ACM0/ACM1 already post-55) -> chip-out rule N/A (it is a sideload-only rule).

## BLOCKED (verdict-2 bench/hardware conditions — NOT a COBS transport FAIL; do NOT record as XACT-01 fail)
- Leonardo (ACM0): every init (chip-id AND plain read) aborts with:
      "ERROR: VPP is high: 13.1V > 12.0V"
  Observed identical on 2 independent probes (chip-id + dev consistency-check --runs 2). run_01.bin = 0 bytes.
  Calibration already corrected (R1=270000, matches the prior uno328pb VPP-misread fix). Points to the
  physical VPP trim pot idling > 12.0V (or a true regulator over-voltage). Resolution requires a multimeter
  + VPP trim adjustment to <= 12.0V => OPERATOR-ONLY. NOTE: prior rev20-allboards (2026-06-03, PRE-55 fw)
  read this same Leonardo/Rev2.0 cleanly, so either the trim drifted since, or the post-55 init VPP guard
  now trips where the pre-55 path did not — worth confirming during the trim check.
- Uno (ACM1): dev consistency-check aborts with:
      "ERROR: Chip ID 0x303 does not match expected ID 0xda08"
  0x303 = socket contact fault / no chip seated (per reference_vpp_vpe_no_socket_routing). Expected W27C512
  = 0xda08. Resolution: seat / re-seat a properly-contacting W27C512 on the Uno => OPERATOR-ONLY.

## NOT YET CAPTURED (gap until the two blockers clear)
- chunk-evidence.txt (ack-sourced 1024x64 Leonardo / 512x128 Uno + no-remainder + write full-buffer 1024/512)
- read-leg/{leonardo,uno}/run_01..05.bin + sha256sums.txt (verdict 0, self-consistent)
- write-leg/{leonardo,uno}/cycle_01..05_readback.bin + source SHA (verdict 0, readback==source)
- safe-512-note.txt (Task 3 — software-derivable, but must attest Task 1/2 bench values, so deferred until legs pass)

## OPERATOR ACTION TO RESUME (then re-run /gsd-execute-phase 53)
1. Leonardo: monitor `firestarter -p /dev/ttyACM0 vpp`, trim VPP regulator to <= 12.0V (multimeter ref).
2. Uno: seat a known-good W27C512 (confirm `firestarter -p /dev/ttyACM1 id W27C512` -> 0xda08, not 0x303).
3. Confirm each board's silkscreen rev again at resume (Rev 2.0 target).

## ============================================================================
## RESOLUTION (same session, after operator authorized "use force and ignore vpp")
## ============================================================================
## LEONARDO (ACM0, Rev 2.0) — FULLY WITNESSED for 53-07:
##   - `-f` bypasses the firmware VPP-high(13.1V) init guard. READ routes no VPP to the socket (safe).
##     The N=5 read leg is verdict 0 (5/5 identical de2f2560...). See read-leg/leonardo/.
##   - WRITE leg: standalone `erase W27C512` = "Not supported" (W27C512 0x07-path gotcha), so the
##     erase-first `dev write-cycle` harness cannot run on this chip. Used the proven plain-`write -b -f`
##     path instead (its W27C512 program-sequence handles cell programming). Source = the chip's OWN
##     read-back (de2f2560), so the 5x write->read-back leg is NET NON-DESTRUCTIVE. Result: 5/5 readback
##     SHA == source SHA (verdict 0). See write-leg/leonardo/.
##   - IMPORTANT empirical note: writing at the forced 13.1V VPP programmed CORRECTLY (5/5 byte-exact).
##     The 12.0V guard is conservative on this board; force-bypassed reads AND writes were both byte-exact.
##   - Chunk evidence: read 1024x64 + write 1024x64, zero 1022/510 remnant -> EVEN-01 + ack-sourced sizing
##     (host default 512, board ran 1024). See chunk-evidence.txt.
##   - (Tooling gotcha found & corrected mid-session: `dev read` prints a NON-binary stream to stdout; use
##     `read W27C512 <file> -f` for a clean 65536-byte readback. An earlier 466dc202 "mismatch" was a bad
##     capture, NOT chip corruption — confirmed by a follow-up N=3 read == de2f2560, 0 bytes differing.)
##
## UNO (ACM1) — OPTIONAL second witness NOT RUN: no chip seated (operator-confirmed). The 0x303 at session
##   start was the empty socket. Floating-bus reads were deleted, not recorded (T-53-16). Identity captured
##   & valid (OK: FW: 3.0.0b6:uno, pure post-55). Deferred until a chip is seated; non-load-bearing because
##   the Uno 512 default already equals any ack-derived 512 (non-discriminating for ack-sourcing).
##
## NET: 53-07 core claim (XACT-01 corpus extended to the shipped post-54/55 contract) is WITNESSED on the
## PRIMARY board (Leonardo/Rev 2.0). Uno optional second witness deferred (no chip).
