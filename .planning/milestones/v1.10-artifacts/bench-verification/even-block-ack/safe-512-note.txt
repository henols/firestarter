# 53-07 Task 3 — safe-512 software-coverage note + self-sufficient operator attestation + 53-06 linkage
# Written 2026-06-05 (operator-witnessed bench session, post-55 firmware tip).

## (1) SAFE-512 GRACEFUL DEFAULT — ALREADY-COVERED-IN-SOFTWARE (NOT a new bench leg)
The CAP-01 safe-512 graceful default (D-05 revert: when the MSG_OK_READY u16 buffer-size ack is ABSENT,
the host assumes a 512-byte chunk size and raises NO FirmwareOutdatedError) is ALREADY-COVERED-IN-SOFTWARE
by Phase 55:
  - Phase 55 `TestCapSafeDefault` passes 3/3 (55-VERIFICATION SC3).
  - `_calculate_buffer_size()` returns 512 when `firmware_max_chunk` is absent (no ack present).
This path is deliberately covered in SOFTWARE ONLY. Re-flashing OLD un-advertising (pre-55) firmware onto a
bench board to exercise the absent-ack path would contradict the hardened-firmware-only + chip-out/sideload
constraints of this phase and would needlessly risk the bench. Therefore NO new bench leg requiring old
un-advertising firmware was invented (honors T-53-22).

## (2) SELF-SUFFICIENT OPERATOR-WITNESS ATTESTATION (53-07 stands alone; it is NOT incorporated by 53-06)
Date: 2026-06-05
Operator-witnessed: yes. Port->controller identity verified live this session (ACM/USB shuffle across replug).
Firmware/host: host firestarter 3.0.0b7; firmware reports 3.0.0b6 with the POST-55 pure identity shape.

PRIMARY WITNESS — LEONARDO (ACM0):
  - Operator-confirmed silkscreen shield rev: Rev 2.0 (D-07 clean-board target). EEPROM override = Rev 2.0-class.
  - Verbatim raw FW identity line (from fw-identity-raw.txt): "OK: FW: 3.0.0b6:leonardo"
    -> exactly TWO colon-fields after FW:, NO :<buf>:<maxchunk> suffix (Phase 55 SC1 pure-identity revert).
  - Observed chunk size + count: 1024-byte chunks, 64 per full 65536-byte chip, BOTH directions
    (read "DATA: <chunk: 1024 bytes>" x64; write Buffer size 1024, 64 host->fw blocks). No 1022/510 remnant
    (EVEN-01 no-buffer-2). 65536 % 1024 == 0 -> no odd remainder.
  - Read verdict: 0 (N=5, 5/5 identical SHA de2f2560...; read self-consistency).
  - Write verdict: 0 (N=5 write->read-back, each read-back SHA == source SHA; source = chip's own content,
    net non-destructive). Write path = plain `write -b -f` because standalone `erase W27C512` is "Not supported"
    on this fw path (W27C512 0x07-path gotcha); the write program-sequence handles cell programming.
  - Ack-sourced sizing proof: host default is 512 for all boards; Leonardo ran 1024 in both directions, which
    can ONLY come from the MSG_OK_READY u16 ack (CAP-01) — DIRECT ack-sourcing proof.
  - Bench caveat (honest record): the firmware VPP init guard tripped at 13.1V > 12.0V; `-f` (operator-
    authorized "use force and ignore vpp") bypassed it. Reads route no VPP to the socket (safe); the forced
    writes nonetheless programmed byte-exact (5/5), indicating the 12.0V guard is conservative on this board.

OPTIONAL SECOND WITNESS — UNO (ACM1): NOT RUN this session (no chip seated, operator-confirmed). Identity
  captured & valid ("OK: FW: 3.0.0b6:uno", pure post-55). Floating-bus open-socket reads were deleted, not
  recorded (T-53-16). Non-load-bearing: the Uno 512 default equals any ack-derived 512, so it cannot
  discriminate ack-sourcing the way Leonardo's 1024-over-512 does. Deferred until a W27C512 is seated.

## (3) 53-06 LINKAGE RECOMMENDATION (recommendation only — 53-01..06 left UNTOUCHED)
RECOMMENDATION: the Wave-4 milestone evidence artifact (53-06) currently composes only 53-03/04/05 and does
NOT know about 53-07. It SHOULD be widened to incorporate this even-block-ack/ evidence — the post-55 pure
identity, the ack-sourced 1024x64 chunk-count, and the even-block (no-remainder) read+write byte-identity —
so the milestone artifact reflects the ACTUALLY-SHIPPED post-54/55 contract. This is stated here as a
recommendation for the orchestrator to surface as a one-line follow-up; NO edit to 53-06 (or 53-01..05) was made.
