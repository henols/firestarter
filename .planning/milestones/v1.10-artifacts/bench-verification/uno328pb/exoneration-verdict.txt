# 53-05 / XACT-03 — uno328pb structured transport-exoneration verdict (D-10)
# Operator-witnessed, 2026-06-05. Port /dev/ttyUSB0. HARDENED post-55 firmware
#   (OK: FW: 3.0.0b6:uno328pb — pure 2-field, no :buf:maxchunk suffix; sideloaded this session, D-09 honored).
# Shield rev: uno328pb (operator 'go'; Rev 2.2 typical per bench history). Data chip seated (NOT blank).

## (1) BEFORE shape — documented pre-hardening (v1.6-EVIDENCE.md Plan 27-04; cited, NOT re-run per D-09)
- ~99.4% 0xff (floating-bus / contact-fault read shape)
- 100% of byte positions unstable across N=5
- 4x N=5 timeouts
- 0.47% pairwise byte-divergence (run_01 vs run_02)

## (2) AFTER shape — observed on the HARDENED transport (5 retry attempts x up to 5 runs each)
Method: dev consistency-check W27C512 --runs 5 -f, repeated 5x (D-08: timeouts logged + retried,
never aborted). Raw evidence: timeout-retry-log.txt + attempt-{1..5}/ + read-runs/.
- VPP: 12.7V > 12.0V WARNING (non-fatal — uno328pb proceeds; contrast the Leonardo's 13.1V fatal guard).
- TIMEOUTS PERSIST: most attempts time out mid-read at a varying offset (partial reads of
  0 / 1024 / 22528 / 26624 B captured). A timeout maps to verdict 2 (hw-error); a full read takes ~18.2s.
- 0xff content ~8.8-9.0% (NOT ~99.4%). The catastrophic floating-bus mode is GONE — a real data chip
  is now being read. CAVEAT: this is partly confounded by chip state (the before-shape was a
  floating/blank read; a real data chip is seated now), so the 0xff drop is NOT solely a transport effect.
- READ JITTER PERSISTS: attempt 5 completed all 5 reads (no timeout) but produced 5 DISTINCT SHAs ->
  Consistency check: FAIL (verdict 1). Quantified on those 5 full reads:
    * positions unstable across 5 reads: 36.02% (was 100% before)
    * pairwise byte-divergence: mean 15.1%, min 2.27%, max 34.1% (was 0.47% before)
  i.e. the divergence MAGNITUDE is larger (real-data jitter, not floating-bus noise), position
  instability is lower (36% vs 100%).

## (3) SHAPE CHANGED? — YES (PARTIAL)
The catastrophic floating-bus failure mode (~99.4% 0xff, 100% positions unstable) is RESOLVED: a real
data chip now reads, position instability dropped 100% -> 36%. BUT severe read instability PERSISTS on
the hardened transport — intermittent mid-read timeouts (verdict 2) AND, when reads complete, run-to-run
data divergence (verdict 1; 5 distinct SHAs, mean 15% pairwise). The uno328pb is NOT byte-stable on the
hardened transport.

## (4) EXONERATION VERDICT
The hardened COBS transport did NOT make the uno328pb byte-stable: timeouts + read jitter persist. This
is consistent with the read-path instability being a HARDWARE/shield-level fault, not a serial-transport
fault — exactly what XACT-03 sets out to establish.

transport-exoneration per v1.9-COBS-DECISION §2.0 — NOT a per-shield hardware fix; the actual RCA stays deferred to v1.9 Phase 45+.

A green OR changed result is still exoneration, not a fix (per 53-05 objective): the persistent
timeouts + divergence on the hardened transport confirm serial framing is ruled OUT as the sole cause,
and the residual instability is handed to the v1.9 read-bug RCA (Phase 45+).

## (5) D-08 verdict-integrity confirmation
- 5 attempts run; timeouts LOGGED and RETRIED, never aborted (the overall N=5 effort completed).
- A timeout always surfaced as verdict 2 (hardware/serial error), NEVER collapsed to verdict 1.
- The single verdict-1 (attempt 5) was a GENUINE data divergence (5 full reads, 5 distinct SHAs), not a
  timeout mis-mapped to FAIL — the v1.6 RCA 3-way diagnostic (0=PASS/1=FAIL/2=hw-error) is intact.
