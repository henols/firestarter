# uno328pb (ATmega328PB, FT232R/urclock) /dev/ttyUSB0 — Rev 2.0 shield — hardened fw 8731017 — 2026-06-03
- FW identity: "FW: 3.0.0b6:uno328pb:512" (advertises buffer 512 -> host sizes chunks to 510).
- id: PASSED x3 (historically-unstable board ids cleanly here).
- blank -f: SUCCESSFUL (5.42s).
- N=5 consistency-check -f: PASS — 1 distinct SHA (71189f7f = all-0xff blank), no timeouts, no
  jitter, each ~18.2s. (Historically this board showed timeouts + ~99% 0xff drift; clean here.)
- verify -f: attempt 1 TIMEOUT (transient), attempts 2-3 SUCCESSFUL (13.86s). Retry-on-timeout
  per "never trust N=1" — transient bench flakiness, not a transport defect (retries pass).
- Includes the systemic com_mode buffer-and-flush fix (uno_rurp_shield.cpp compiles for ATmega328PB).

CAVEATS (do not over-read):
- Chip is BLANK (all 0xff) -> N=5-identical is a consistency proof on the EASY case, NOT a
  varied-data stress, and NOT a replication of the historical instability (which was on a DATA chip).
- Board is now on Rev 2.0 (vs the historically-unstable Rev 2.2/USB0 setup) AND the hardened
  transport AND a blank chip — cannot isolate which factor accounts for the clean result.
- This is NOT a substitute for XACT-03 (53-05), which needs the data chip + the documented v1.6
  before-shape comparison + structured transport-exoneration verdict.
