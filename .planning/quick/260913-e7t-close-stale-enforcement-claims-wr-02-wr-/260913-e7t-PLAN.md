---
phase: quick-260913-e7t
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WR-02, WR-03, WR-04, WR-05, WR-06]
files_modified:
  - firestarter_app/tests/test_sdp_db_invariant.py
  - firestarter_app/tests/test_numeric_schema_source_scan.py
  - firestarter_app/tests/test_build_db_inclusion.py
  - firestarter_app/CLAUDE.md
  - firestarter/.github/workflows/beta-build.yml
  - .planning/notes/host-tools-retirement.md
  - .planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md
  - .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No file in the tree claims a deleted `tools/check_*.py` script gates the SDP widening direction (WR-02)."
    - "test_numeric_schema_source_scan.py refers to its non-vacuity subject consistently as one test, matching its own module docstring (WR-03)."
    - "test_build_db_inclusion.py claims no enforcement for the SC#3 dispatch-safety invariant, and invents no replacement claim (WR-04)."
    - "The no-DEV_TOOLS CI leg in beta-build.yml still runs, and its comment stands on its own with no reference to deleted steps (WR-05)."
    - "firestarter_app/CLAUDE.md names only checks ci.yml actually runs as CI-enforced; mypy is described as the local pre-commit hook it really is (WR-06a)."
    - "host-tools-retirement.md records that CLAUDE.md kept advertising mypy as CI-enforced after the CI step was deleted, and that this was corrected on 2026-09-13 (WR-06b)."
    - "The four app suites stay at 2129 passed and the firmware script suite at 316 passed — no test deleted, skipped or weakened."
  artifacts:
    - firestarter_app/tests/test_sdp_db_invariant.py
    - firestarter_app/tests/test_numeric_schema_source_scan.py
    - firestarter_app/tests/test_build_db_inclusion.py
    - firestarter_app/CLAUDE.md
    - firestarter/.github/workflows/beta-build.yml
    - .planning/notes/host-tools-retirement.md
    - .planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md
  key_links:
    - "App suite -> firmware repo porcelain: six app test modules assert `_git_porcelain(FW_ROOT) == \"\"`, so Task 1 must run and commit while /workspaces/firestarter is still clean. Task 2 dirties it."
    - "CLAUDE.md Tooling gate line -> .pre-commit-config.yaml: mypy survives as a local hook, so the CI claim must move rather than vanish, or a new false claim replaces the old one."
    - "WR-01 -> the provenance-sweep todo: the sweep todo currently reads `firestarter_app/tools/` as DISCHARGED, which would swallow WR-01 unless a carry-over line is added."
---

<objective>
Close the five stale enforcement claims WR-02..WR-06 that Phase 188's gate retirement left
behind, across three repositories, as prose corrections only.

Purpose: the v1.37 milestone is named Claim Hygiene. Five places in the tree still describe
deleted `tools/check_*.py` gates, a deleted CI step, and deleted workflow steps as live
enforcement. Each is a false claim about what is enforced; none breaks a test.

Output: three atomic commits — one inside `firestarter_app`, one inside `firestarter`, one in
the meta repo — plus a durable record in `.planning/notes/host-tools-retirement.md` and an
honest disposition for the source todo.

TRACER N/A, stated deliberately: tracer-first decomposition assumes a layered path to wire
end-to-end. There is no path here. These are three independent prose corrections in three
separate git repositories with zero shared code, zero behavior change, and no architecture to
prove. The tasks are grouped by repository so each repository gets exactly one atomic commit,
which is the real constraint.

ORDERING IS LOAD-BEARING, not cosmetic. Execute Task 1, then Task 2, then Task 3, strictly in
order. Six app test modules (`test_json_key_parity.py`, `test_cap03_ack_layout_parity.py`,
`test_py32_asset_name_host.py`, `test_py32_flash_map_host.py`, `test_sdp_table_parity.py`,
`test_py32_packaging`-adjacent) assert `_git_porcelain(FW_ROOT) == ""` against the FIRMWARE
repository. Both sub-repos were measured clean at plan time. If Task 2 edits
`/workspaces/firestarter` before Task 1's suite run, those assertions go red on an uncommitted
workflow file and the red will look like a real regression.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md
@/workspaces/.planning/notes/host-tools-retirement.md

HARD RULE, NOT OVERRIDABLE by this plan or any task in it. See CLAUDE.md, "Source code comments
— hard rule". Write NO new comment into product source under `firestarter/` or
`firestarter_app/`. Every edit below either corrects an EXISTING docstring or comment, or
deletes text. Nothing here adds a new explanatory comment, and nothing may cite this quick task,
a WR id, a plan number, or any `.planning/` path inside a sub-repo source file. The two meta-repo
files in Task 3 are `.planning/` documents, not source, and carry citations normally.

Click docstrings in `firestarter_app` are user-facing `--help` text. None of these five touch a
Click docstring. Do not treat any docstring here as a comment to be stripped.

Branches are ALREADY CREATED and checked out in all three repositories:
`quick/260913-e7t-close-stale-claims-wr02-wr06`. Verified at plan time in both sub-repos. Do not
create or switch branches.

Use `/usr/bin/grep` for every verification grep. The devcontainer's bare `grep` is ugrep and
honors `.gitignore`, which silently under-scans and can make a gate pass on no evidence.

PINNED BASE SHAs, measured at plan time with all three working trees clean. The commit-scope gates
diff against these explicit shas rather than a relative `HEAD~1`, so a commit landing from another
session widens the diff and correctly fails the gate instead of hiding inside it. Local `beta` is
stale in all three repositories (107 / 20 / 53 commits behind the branch), so `merge-base beta HEAD`
is NOT a usable anchor here:
  - `/workspaces` (meta): `e5f1961451ae7e7a4a01fc67bcf47438a3e89a95`
  - `/workspaces/firestarter`: `8b1e90059cce6f8a0dfbb989f620e48819706f91`
  - `/workspaces/firestarter_app`: `6fa3a0542a5de22110874f82f6a8c10c4ceb88cc`
If any repository's HEAD does not match its sha above when you start, stop and report — the plan's
scope gates are calibrated to these and would misreport.

Every WR gate below was run against the unedited tree at plan time and confirmed RED. A gate that
is already green before the edit proves nothing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the four stale claims in firestarter_app (WR-02, WR-03, WR-04, WR-06a)</name>
  <files>firestarter_app/tests/test_sdp_db_invariant.py, firestarter_app/tests/test_numeric_schema_source_scan.py, firestarter_app/tests/test_build_db_inclusion.py, firestarter_app/CLAUDE.md</files>
  <precondition>`/workspaces/firestarter` has an empty `git status --porcelain`. If it is dirty, Task 2 has already run out of order — stop and report, because the app suite asserts firmware-repo porcelain and will produce a misleading red.</precondition>
  <read_first>
    - firestarter_app/tests/test_sdp_db_invariant.py lines 155-170 (the `_assert_partition_matches_committed` docstring)
    - firestarter_app/tests/test_numeric_schema_source_scan.py lines 40-48 (the module docstring, ALREADY correct — it is the reference both edits must match) and lines 212-238
    - firestarter_app/tests/test_build_db_inclusion.py lines 7-12
    - firestarter_app/CLAUDE.md line 127 (the "Tooling gate (v1.8)" line)
    - firestarter_app/.pre-commit-config.yaml (measured at plan time: hooks are ruff-check, ruff-format, mypy — mypy is STILL wired here)
    - firestarter_app/.github/workflows/ci.yml lines 58-65 (measured: ruff check, ruff format --check, pytest --cov-fail-under=70; zero mypy occurrences in the file)
  </read_first>
  <action>
Four prose edits in one repository, one commit. No behavior change. No assertion logic changes.
No test is deleted, skipped, renamed or weakened. Do not touch any `assert` expression — for
WR-03 you edit only the human-readable message string an assertion carries, never its condition.

EDIT 1 (WR-02) — `tests/test_sdp_db_invariant.py`, the `_assert_partition_matches_committed`
docstring around line 165. Its closing sentence attributes the widening direction to a host
checker script under `tools/` that no longer exists (`git ls-files 'tools/check_*.py'` returns 0
files). The narrowing direction genuinely is still gated, by this very test. Replace the whole
"Direction matters" paragraph with exactly this, preserving the 4-space docstring indent:

    Direction matters (P-10): a chip present in `committed_allow` but absent
    from `measured_allow` LEFT the allow-set -- the narrowing signal this
    gate exists to catch. A chip present in `measured_allow` but absent from
    `committed_allow` ENTERED it -- the widening signal, which nothing gates
    today. Named separately so a reader can tell which happened at a glance.

EDIT 2 (WR-03) — `tests/test_numeric_schema_source_scan.py`, two plural cross-references that
contradict the module docstring at line 43. The module docstring is already correct and is the
authority; make both of these agree with it. Change the singular/plural only — do not renumber
anything, do not touch the surrounding tokens or the f-string prefixes.

  2a, the `test_scan_helper_detects_planted_forbidden_tokens` docstring opener around line 215,
      becomes exactly:

          """Drives the SAME `_find_forbidden_tokens` helper test 1 calls,

  2b, the final segment of that function's assertion MESSAGE around line 235, becomes exactly:

          f"which means test 1 above proves nothing."

      Keep the `f` prefix exactly as it is. The literal has no placeholder of its own, but it is
      one segment of an implicitly concatenated group that does, so ruff's F541 does not fire.
      Dropping the prefix is an unnecessary diff; adding or removing placeholders is not allowed.

EDIT 3 (WR-04) — `tests/test_build_db_inclusion.py`, module docstring lines 9-11. The trailing
dispatch-safety clause names a plan as the enforcer; the test that did that enforcing was
deleted along with the `check_*` family. Delete that clause and nothing else. Do NOT invent a
replacement enforcement claim, do not add a "no longer enforced" note, and do not add a comment.
The two surviving lines become exactly:

    These tests assert the Phase 66 DB-01/02/03/05 behaviors implemented by
    build_db.py with chip_database.json regenerated at 744 chips (Plan 03).

Note the comma after `(Plan 03)` becomes a period. Leave the blank line and the "Taxonomy
strings (locked ...)" block that follow exactly as they are.

EDIT 4 (WR-06a) — `CLAUDE.md` line 127, the "Tooling gate (v1.8)" line. Phase 188-04 deleted the
mypy step from `ci.yml` under D-02; the file now contains zero mypy occurrences. But mypy is NOT
gone from the project — `.pre-commit-config.yaml` still wires it as the third local hook. So
deleting the word outright would replace one false claim with another, because the line's tail
also asserts that pre-commit mirrors the CI hook order, and pre-commit has no pytest hook.
Rewrite the single line to say what is measurably true of each side. Replace line 127 in full
with exactly this one line:

**Tooling gate (v1.8):** `ruff check` + `ruff format --check` + `pytest --cov-fail-under=70` — all enforced by `.github/workflows/ci.yml` on every PR. `mypy` (strict on 8 modules per Phase 42 D-06: `main.py`, `cli_handlers.py`, `chip_resolver.py`, `frame_parser.py`, `codec.py`, `address_parser.py`, `exceptions.py`, `serial_comm.py`) is wired in the local `pre-commit` config only and is **not** a CI gate; `pre-commit` runs `ruff-check` → `ruff-format` → `mypy` locally.

Then, in this order: run ruff check, run ruff format --check, run the full suite, review
`git diff` and confirm every hunk is inside a docstring, a comment, or markdown prose, then
`cd /workspaces/firestarter_app` and commit all four files as ONE commit on the already-checked-out
branch. Commit inside the sub-repo. Do not stage anything from the meta repo, and do not touch the
meta gitlinks — the orchestrator advances those after both sub-repo commits land.

Suggested message subject: `docs: correct four stale enforcement claims left by the tools/ retirement`
End the message with the required attribution trailer:
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m ruff check firestarter/ tests/ && python3 -m ruff format --check firestarter/ tests/</automated>
    <automated>cd /workspaces/firestarter_app && OUT=$(python3 -m pytest tests/ -o addopts="" -q 2>&1); rc=$?; printf '%s\n' "$OUT" | tail -3; test "$rc" -eq 0 || exit 1; printf '%s\n' "$OUT" | /usr/bin/grep -q '2129 passed' && echo APP-SUITE-2129-OK</automated>
    <automated>cd /workspaces/firestarter_app && test "$(/usr/bin/grep -c 'already gates elsewhere' tests/test_sdp_db_invariant.py)" -eq 0 && test "$(/usr/bin/grep -c 'the widening signal, which nothing gates' tests/test_sdp_db_invariant.py)" -ge 1 && echo WR-02-OK</automated>
    <automated>cd /workspaces/firestarter_app && test "$(/usr/bin/grep -c 'tests 1 and 2' tests/test_numeric_schema_source_scan.py)" -eq 0 && test "$(/usr/bin/grep -c 'test 1 calls' tests/test_numeric_schema_source_scan.py)" -ge 2 && test "$(/usr/bin/grep -c 'test 1 above proves nothing' tests/test_numeric_schema_source_scan.py)" -ge 1 && echo WR-03-OK</automated>
    <automated>cd /workspaces/firestarter_app && test "$(/usr/bin/grep -c 'SC#3' tests/test_build_db_inclusion.py)" -eq 0 && test "$(/usr/bin/grep -cF 'regenerated at 744 chips (Plan 03).' tests/test_build_db_inclusion.py)" -ge 1 && echo WR-04-OK</automated>
    <automated>cd /workspaces/firestarter_app && test "$(/usr/bin/grep -cE 'mypy.*all enforced by' CLAUDE.md)" -eq 0 && test "$(/usr/bin/grep -cF 'is **not** a CI gate' CLAUDE.md)" -ge 1 && test "$(/usr/bin/grep -c 'mypy' .pre-commit-config.yaml)" -ge 1 && echo WR-06a-OK</automated>
    <automated>cd /workspaces/firestarter_app && FILES=$(git diff 6fa3a0542a5de22110874f82f6a8c10c4ceb88cc..HEAD --name-only) || exit 1; printf '%s\n' "$FILES"; test "$(printf '%s\n' "$FILES" | wc -l)" -eq 4 && echo FOUR-FILES-SINCE-PINNED-BASE</automated>
  </verify>
  <done>
`pytest tests/ -o addopts="" -q` reports **2129 passed** — identical to the pre-edit baseline, no
skip, no deselect, no xfail added. ruff check clean; ruff format --check reports 155 files already
formatted. All four grep gates print their OK token. The `.pre-commit-config.yaml` mypy hook is
still present and untouched, proving the CLAUDE.md rewrite describes a real local hook rather than
a fabricated one. One commit exists on the branch inside `firestarter_app` touching exactly the
four named files, and `git diff` shows no change to any assertion condition, any function
signature, or any non-prose line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the no-DEV_TOOLS CI comment self-contained in firestarter (WR-05)</name>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <precondition>Task 1 has already committed inside `/workspaces/firestarter_app`, so the app suite has finished while the firmware tree was still clean. This task is what makes it dirty.</precondition>
  <read_first>
    - firestarter/.github/workflows/beta-build.yml lines 88-97 (the comment block and the `Run native unit tests (no DEV_TOOLS)` step it annotates)
  </read_first>
  <action>
One comment edit, one commit. The step stays, the substance stays, only the dead antecedent goes.

The three-line comment above the `Run native unit tests (no DEV_TOOLS)` step opens by pointing at
vector-gate steps that sat above it. Phase 188-06 deleted the vector gates and the codegen gates
went on 2026-09-13, so the comment now points at nothing — a reader scrolling up finds no referent
and cannot tell whether a step is missing.

KEEP the step `Run native unit tests (no DEV_TOOLS)` running `pio test -e native_nodevtools`
exactly as it is. It is correct and load-bearing. KEEP the comment's substance: `-D DEV_TOOLS`
lives in the shared `[env]` block, so without this leg the no-DEV_TOOLS build is never compiled on
the branch that ships. Only drop the back-reference so the comment stands alone.

Replace the three comment lines with exactly these two, at the same 6-space YAML indent:

      # -D DEV_TOOLS lives in the shared [env] block, so without this leg the
      # no-DEV_TOOLS build is never compiled on the branch that ships.

This is an edit to an existing comment, which the hard rule permits. Do not add any new comment
line, do not cite a phase, a WR id or a plan, and do not annotate why the back-reference went —
that record belongs in the SUMMARY, not in the workflow file.

Then `cd /workspaces/firestarter` and commit that single file as ONE commit on the
already-checked-out branch. Commit inside the sub-repo; do not advance the meta gitlink.

Suggested message subject: `ci: make the no-DEV_TOOLS gate comment self-contained`
End the message with:
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/beta-build.yml')); names=[s.get('name') for j in d['jobs'].values() for s in j.get('steps',[])]; assert 'Run native unit tests (no DEV_TOOLS)' in names, names; print('STEP-PRESENT', len(names), 'steps')"</automated>
    <automated>cd /workspaces/firestarter && test "$(/usr/bin/grep -c 'Same rationale as the vector gates' .github/workflows/beta-build.yml)" -eq 0 && test "$(/usr/bin/grep -cF 'pio test -e native_nodevtools' .github/workflows/beta-build.yml)" -ge 1 && test "$(/usr/bin/grep -cF 'shared [env] block' .github/workflows/beta-build.yml)" -ge 1 && echo WR-05-OK</automated>
    <automated>cd /workspaces/firestarter && OUT=$(python3 -m pytest tests/ -o addopts="" -q 2>&1); rc=$?; printf '%s\n' "$OUT" | tail -3; test "$rc" -eq 0 || exit 1; printf '%s\n' "$OUT" | /usr/bin/grep -q '316 passed' && echo FW-SCRIPT-SUITE-316-OK</automated>
    <automated>cd /workspaces/firestarter && FILES=$(git diff 8b1e90059cce6f8a0dfbb989f620e48819706f91..HEAD --name-only) || exit 1; printf '%s\n' "$FILES"; test "$FILES" = ".github/workflows/beta-build.yml" && echo ONE-FILE-SINCE-PINNED-BASE</automated>
    <automated>cd /workspaces/firestarter && D=$(git diff 8b1e90059cce6f8a0dfbb989f620e48819706f91..HEAD -- .github/workflows/beta-build.yml) || exit 1; printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][^-+]'; if printf '%s\n' "$D" | /usr/bin/grep -E '^[-+][^-+]' | /usr/bin/grep -qvE '^[-+][[:space:]]*#'; then echo NON-COMMENT-LINE-CHANGED; exit 1; else echo ONLY-COMMENT-LINES-CHANGED; fi</automated>
  </verify>
  <done>
`beta-build.yml` still parses as YAML and the `Run native unit tests (no DEV_TOOLS)` step is still
present in the parsed step list with its `pio test -e native_nodevtools` run line intact. Zero
occurrences of the dead back-reference remain; the `[env]` block rationale survives. The firmware
script suite reports **316 passed**. The commit touches exactly one file and its diff contains
only comment lines — no `-` or `+` line touches a `name:`, `run:`, `uses:` or any other YAML key.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the CLAUDE.md claim in the retirement note and dispose of the todo (WR-06b)</name>
  <files>.planning/notes/host-tools-retirement.md, .planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md, .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md</files>
  <precondition>Tasks 1 and 2 have both committed inside their sub-repos, so their commit SHAs exist and can be cited by the records this task writes.</precondition>
  <read_first>
    - .planning/notes/host-tools-retirement.md lines 78-86 (the paragraph beginning `**The mypy CI leg (D-02) — retired, no replacement:**`)
    - .planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md in full
    - .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md, the "STILL OPEN" section's `firestarter_app/tools/` bullet
  </read_first>
  <action>
Three meta-repo edits, one commit. These are `.planning/` documents, so normal GSD citation
applies here — the no-comments-in-source rule does not reach them.

LOCATION CORRECTION, apply it rather than hunting: both the source todo and the task brief say the
mypy-CI-leg paragraph is in "§2". It is not. It is measured in **§1** ("What was retired"), as the
last paragraph of that section, beginning `**The mypy CI leg (D-02) — retired, no replacement:**`.
Locate it by that bold lead string, never by section number.

EDIT 1 (WR-06b) — extend that existing §1 paragraph. It records that the CI step was deleted. It
does not record that the claim outlived the step, which is the fact 188-04's SUMMARY promised and
188-09 did not deliver. Append to the end of the paragraph, in its own prose voice, all of:
  - that `firestarter_app/CLAUDE.md`'s "Tooling gate (v1.8)" line went on advertising `mypy` as
    enforced by `ci.yml` on every PR after the step was deleted, so a reader trusting CLAUDE.md was
    misled with nothing in this note to correct them;
  - that the claim was corrected on **2026-09-13** by quick task `260913-e7t` (WR-06), citing the
    `firestarter_app` commit SHA from Task 1;
  - the measured nuance that makes the correction non-trivial: `mypy` was never removed from the
    project, only from CI — `.pre-commit-config.yaml` still wires `ruff-check` → `ruff-format` →
    `mypy`, so the accurate statement is "local hook, not a CI gate", and a flat deletion of the
    word from CLAUDE.md would have produced a second false claim rather than a fix.
Do not restructure the note, do not renumber sections, and do not touch §2's eight costs.

EDIT 2 — the source todo `2026-09-13-close-six-stale-claims-wr01-wr06.md`. Add a dated
`## Disposition 2026-09-13` section recording: WR-02, WR-03, WR-04 and WR-06a corrected in the
`firestarter_app` commit (cite the SHA); WR-05 corrected in the `firestarter` commit (cite the
SHA); WR-06b recorded in `.planning/notes/host-tools-retirement.md` §1. Mark those five resolved
in the six-row table, or beneath it — whichever keeps the table readable — without deleting the
original rows, since the table is the record of what was false.

Then record WR-01's disposition explicitly and state the reasoning, because it is the one item
deliberately NOT fixed here. WR-01 is a source comment at
`firestarter_app/tools/parse_devtest_issue.py:215`. The operator's hard rule is that no comment
belongs in product source at all, so rewording it to name a different enforcer would manufacture
exactly what the rule forbids. Its correct disposition is DELETION of the comment block, as part
of the existing sweep tracked by `2026-08-27-strip-gsd-provenance-comments-from-source.md` — not a
reword in this task.

EDIT 3 — the file disposition, and the carry-over that makes it safe. The brief allows moving this
todo to `.planning/todos/completed/` ONLY if WR-01's carry-over is unambiguously recorded in the
provenance-sweep todo. Measured at plan time, it is NOT: that todo's "STILL OPEN" section marks
`firestarter_app/tools/` as **DISCHARGED 2026-09-13 (Phase 188)** with a measured count of 0, and
that measurement covered `.planning/` CITATIONS only — WR-01 is not a `.planning/` citation, it is
a stale enforcement claim naming deleted checker scripts, so the discharge does not cover it and
would silently swallow it.

So do BOTH of these, and neither is optional:
  a) LEAVE `2026-09-13-close-six-stale-claims-wr01-wr06.md` in `.planning/todos/pending/`, with
     WR-01 as its sole open item. State in the Disposition section that this is the chosen
     disposition and why: five of six are closed, but the file's own "Done when" requires all six,
     and in a milestone named Claim Hygiene, filing a five-of-six todo under `completed/` would be
     the same species of over-claim this task exists to remove. Update the "Done when" block so it
     reflects the real remaining condition — WR-01 deleted by the provenance sweep — rather than
     still demanding six in-place corrections.
  b) Add a dated carry-over line to the provenance-sweep todo's `firestarter_app/tools/` bullet,
     qualifying the discharge: it measured `.planning/` citations only (0 hits) and does not
     discharge every comment under `tools/`; `tools/parse_devtest_issue.py:215` still carries a
     comment block whose enforcement claim died with the `check_*` family (WR-01 of the
     2026-09-13 todo), and under the broadened hard rule its disposition is deletion in this
     sweep, not a reword.

Then commit all three `.planning/` files as ONE commit in the meta repo. Stage by explicit path.
Do NOT stage `firestarter` or `firestarter_app` — the gitlinks stay where they are and the
orchestrator advances them afterwards. Leave the untracked `anything.txt` and `tmp/` alone.

Suggested message subject: `docs(quick-260913-e7t): record the mypy claim's survival and dispose of WR-01`
End the message with:
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
  </action>
  <verify>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c '260913-e7t' .planning/notes/host-tools-retirement.md)" -ge 1 && test "$(/usr/bin/grep -c 'pre-commit' .planning/notes/host-tools-retirement.md)" -ge 1 && test "$(/usr/bin/grep -c 'The mypy CI leg (D-02)' .planning/notes/host-tools-retirement.md)" -ge 1 && echo WR-06b-OK</automated>
    <automated>cd /workspaces && test -f .planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md && test ! -f .planning/todos/completed/2026-09-13-close-six-stale-claims-wr01-wr06.md && test "$(/usr/bin/grep -c 'Disposition 2026-09-13' .planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md)" -ge 1 && echo TODO-STAYS-PENDING</automated>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c 'parse_devtest_issue.py' .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md)" -ge 1 && test "$(/usr/bin/grep -c 'WR-01' .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md)" -ge 1 && echo CARRY-OVER-RECORDED</automated>
    <automated>cd /workspaces && FILES=$(git diff e5f1961451ae7e7a4a01fc67bcf47438a3e89a95..HEAD --name-only) || exit 1; printf '%s\n' "$FILES"; if printf '%s\n' "$FILES" | /usr/bin/grep -qv '^\.planning/'; then echo NON-PLANNING-PATH-STAGED; exit 1; else echo PLANNING-ONLY-NO-GITLINKS; fi</automated>
  </verify>
  <done>
`.planning/notes/host-tools-retirement.md` §1's mypy paragraph now carries three facts it lacked:
that CLAUDE.md kept advertising mypy as CI-enforced after the step went, that this was corrected on
2026-09-13 with the commit SHA, and that mypy survives as a local pre-commit hook so the fix had to
move the claim rather than delete the word. The source todo is still under `pending/`, carries a
dated Disposition section marking WR-02..WR-06 resolved with both sub-repo SHAs, and states WR-01
as the sole open item with deletion-not-reword as its disposition and the reason why. The
provenance-sweep todo's `tools/` bullet now names `parse_devtest_issue.py` and WR-01, so the
Phase 188 discharge can no longer be read as covering it. The meta commit touches only paths under
`.planning/`; both gitlinks are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly crossed | Every change is prose in a docstring, a YAML comment, or a markdown document. No input parsing, no network, no serial frame, no voltage-routing path is touched. |
| CI workflow file (firestarter) | `beta-build.yml` is executed by GitHub Actions, so a malformed edit is a live availability risk even when the intended change is a comment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e7t-01 | Tampering | firestarter_app/tests/*.py prose edits | medium | mitigate | Edits are confined to docstrings and one assertion MESSAGE; Task 1 asserts the suite stays at exactly 2129 passed (a weakened or skipped test changes that number) and requires a `git diff` review confirming no assertion condition changed. |
| T-e7t-02 | Denial of Service | firestarter/.github/workflows/beta-build.yml | high | mitigate | A comment edit that breaks YAML or drops a step disables the shipping branch's build. Task 2 verifies by parsing the file with `yaml.safe_load` and asserting `Run native unit tests (no DEV_TOOLS)` is still in the parsed step list, plus a diff check that no `-`/`+` line touches a YAML key. |
| T-e7t-03 | Repudiation | .planning/notes/host-tools-retirement.md | low | mitigate | The note is the durable record of what stopped being enforced; an incomplete entry lets the loss be rediscovered the hard way. Task 3 requires the CLAUDE.md-claim survival, the correction date, the commit SHA, and the surviving pre-commit hook all present, gated by grep. |
| T-e7t-04 | Information Disclosure | firestarter_app/CLAUDE.md | low | accept | The rewritten line names eight module paths and the local hook order, all already public in the repository's own config files. No secret, credential or host path is introduced. |
| T-e7t-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package in any repository. No task runs a package manager, so the Phase 188 package-legitimacy gate has no applicable surface here; `ruff`, `pytest` and `yaml` are all already installed in the devcontainer. |
</threat_model>

<verification>
Run after all three tasks, from a clean state in all three repositories.

1. `cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q` → **2129 passed**,
   the pre-edit baseline. Any other number means a test was added, deleted, skipped or renamed —
   all four are forbidden by this plan.
2. `cd /workspaces/firestarter_app && python3 -m ruff check firestarter/ tests/` → clean, and
   `python3 -m ruff format --check firestarter/ tests/` → 155 files already formatted.
3. `cd /workspaces/firestarter && python3 -m pytest tests/ -o addopts="" -q` → **316 passed**.
4. `cd /workspaces/firestarter && python3 -c "import yaml;yaml.safe_load(open('.github/workflows/beta-build.yml'))"` → parses without raising.
5. Tree-wide staleness sweep, all five literals gone:
   `cd /workspaces && /usr/bin/grep -rn 'already gates elsewhere\|tests 1 and 2\|Same rationale as the vector gates' firestarter_app/tests firestarter/.github 2>/dev/null | wc -l` → **0**.
6. Both sub-repos report an empty `git status --porcelain` and exactly one new commit each on
   `quick/260913-e7t-close-stale-claims-wr02-wr06`; the meta repo has one new commit touching only
   `.planning/` paths, with both gitlinks unadvanced.
</verification>

<success_criteria>
- All five stale enforcement claims (WR-02, WR-03, WR-04, WR-05, WR-06a) are corrected in place,
  and WR-06b is recorded in `.planning/notes/host-tools-retirement.md`.
- Zero behavior change: 2129 app tests and 316 firmware script tests pass, identical to baseline.
  No test deleted, skipped, weakened, or renamed. No CI step removed.
- Zero new comments in product source under `firestarter/` or `firestarter_app/`. Every sub-repo
  hunk either corrects existing docstring/comment prose or deletes it. No WR id, plan number, quick
  id or `.planning/` path appears in any sub-repo source file.
- The CLAUDE.md rewrite creates no new false claim: mypy is described as the local pre-commit hook
  `.pre-commit-config.yaml` actually wires, not deleted outright and not still claimed for CI.
- Three atomic commits, one per repository, each committed from inside that repository. Meta
  gitlinks unadvanced.
- WR-01's disposition is recorded in both todos, and the file-move decision is stated with its
  reason.
</success_criteria>

<output>
Create `.planning/quick/260913-e7t-close-stale-enforcement-claims-wr-02-wr-/260913-e7t-SUMMARY.md`
when done. Record in it: the three commit SHAs; the measured before/after test counts; the
`.pre-commit-config.yaml` finding that forced the WR-06a rewrite to move the mypy claim rather than
delete it; the §1-not-§2 location correction for the retirement note; and the todo disposition
chosen with its reasoning.
</output>
