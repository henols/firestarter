---
phase: 260917-co5-correct-factual-errors-in-the-three-clau
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/CLAUDE.md
  - firestarter_fw/CLAUDE.md
  - CLAUDE.md
autonomous: true
requirements:
  - QUICK-260917-co5
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The already-applied working-tree edits to the three CLAUDE.md files are committed, one commit per repository, and nothing in any of the three files is re-authored, re-derived or re-verified by this plan."
    - "Each of the three commits touches exactly one CLAUDE.md (the meta commit additionally carries the two gitlinks) and no other path."
    - "Every unrelated dirty or untracked path in all three working trees is still exactly as dirty or untracked after this plan as before it."
    - "The meta commit records the two advanced submodule SHAs, so `git submodule status` shows no `+` prefix for either submodule immediately after it."
    - "No branch is created, switched, rebased or pushed; all three repositories stay on `v1.39-protocol-0x05-write-correctness`."
    - "No commit message carries an attribution line, a Co-Authored-By trailer, or any GSD phase or plan identifier beyond the quick id `260917-co5`."
  artifacts:
    - "A commit in /workspaces/firestarter_app whose only changed path is CLAUDE.md"
    - "A commit in /workspaces/firestarter_fw whose only changed path is CLAUDE.md"
    - "A commit in /workspaces whose changed paths are exactly CLAUDE.md, firestarter_app, firestarter_fw"
  key_links:
    - "firestarter_app commit SHA -> meta gitlink `firestarter_app`"
    - "firestarter_fw commit SHA -> meta gitlink `firestarter_fw`"
---

<objective>
Commit the three already-applied, already-verified CLAUDE.md corrections — one atomic commit per
repository — and advance the two submodule gitlinks in the meta repository.

Purpose: the corrected content exists in all three working trees right now, uncommitted, mixed in
with unrelated dirty files. It needs to land as three clean, separable commits before anything else
touches those trees.

Output: three commits (firestarter_app, firestarter_fw, meta). Nothing is pushed. No PR.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md

Established by planning; do NOT re-derive and do NOT re-verify:

- The content edits are **already applied** in all three working trees. This plan does not author,
  improve, re-check, or lint a single line of them. Reading a diff is allowed only to compose a
  commit message.
- Measured working-tree state at planning time (2026-09-17):
  - `/workspaces` — `CLAUDE.md` +18/-1, branch `v1.39-protocol-0x05-write-correctness`.
    Also dirty and **must stay untouched**: `.devcontainer/devcontainer.json`, `.gitignore`,
    `.planning/graphs/.last-build-status.json`, `.planning/graphs/GRAPH_REPORT.md`,
    `.vscode/c_cpp_properties.json`, `.vscode/extensions.json`, `.vscode/launch.json`.
    Untracked and **must stay untracked**: `anything.txt`, `firestarter.wiki/`,
    `setup-claude-pr-policy.sh`, `tmp/`, `.planning/quick/260917-8pj-*/`.
  - `/workspaces/firestarter_app` — `CLAUDE.md` +58/-23, branch
    `v1.39-protocol-0x05-write-correctness`. Also dirty and **must stay untouched**:
    `firestarter/cli_handlers.py`, `tests/test_dev_test_cmd.py`. Untracked and **must stay
    untracked**: `datasheets/LST62832I.pdf`, `datasheets/MBM27128.pdf`,
    `datasheets/MBM27C4001.pdf`.
  - `/workspaces/firestarter_fw` — `CLAUDE.md` +44/-10, branch
    `v1.39-protocol-0x05-write-correctness`, otherwise clean.
- Submodule paths in `.gitmodules` are `firestarter_app` and `firestarter_fw`. Both gitlinks are
  already reported modified because of the dirty content inside them; staging them after the
  submodule commits records the new HEAD SHAs.
- Use plain `git` for every commit. Do **not** use `gsd-tools query commit` here — it scrapes
  ROADMAP prose and has been observed to switch branches.
- Submodule commits happen INSIDE the submodule working directory. Each Bash call resets cwd, so
  every command must either `cd` first in the same call or use `git -C <abs path>`.
- Never use `git add -A`, `git add .`, `git commit -a`, or `git commit` with no pathspec staged.
  Only explicit pathspecs.

Summary of what each diff contains, for writing the commit messages only:

- **app** — replaced the stale "Protocol overrides (WARNING-5)" description with the two real arms
  of `classify()` in `tools/build_db.py` and its measured scope (84 rows at `algorithm 13`, 15
  vendors, 4 pinouts, replacing "~23 chips across 6 manufacturers"); corrected the pinout key list
  from 5 keys to the real 16; recorded that `build_db.py` fetches `infoic.xml` from pinned minipro
  commit `a8efaedc` rather than parsing a non-existent vendored `tools/infoic.xml`; documented
  `tools/extra_chips.json`; documented that `part_number` can be a comma-joined alias list;
  repointed stale `.planning/` citations under `.planning/milestones/` and dropped the citation to
  the deleted phase-13 folder; added `pip install -e '.[test]'` and the CI-runs-Python-3.11 warning.
- **fw** — repointed the dead `.planning/REQUIREMENTS.md` "Evidence ceiling" citation to
  `.planning/milestones/v1.31-REQUIREMENTS.md` and two further archived-milestone paths; corrected
  `eprom_params.h:32-34` to `31-33`; added a "What CI runs" section and the `test/` (Unity) versus
  `tests/` (Python) two-test-tree distinction; recorded `include/messages.h` as generated,
  do-not-edit, sourced from the meta repo's `tools/catalog/messages.toml`; added the comment-rule
  loophole closer and the CI gate note.
- **meta** — corrected the `tools/` inventory from two occupants to three by adding
  `tools/citations/`, including that it scans C-like and Python-like extensions only and therefore
  covers no `CLAUDE.md`; added the missing branching rule (milestone work forks off `beta` in all
  three repos as `v1.X-slug`, never commit to `beta` or `main` directly); added the comment-rule
  loophole closer and the staged-diff greps.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the firestarter_app CLAUDE.md correction inside the app submodule</name>
  <files>/workspaces/firestarter_app/CLAUDE.md</files>
  <precondition>`/workspaces/firestarter_app` is on branch `v1.39-protocol-0x05-write-correctness` and its `CLAUDE.md` is modified but unstaged. Assert with `git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` and `git -C /workspaces/firestarter_app status --porcelain CLAUDE.md`; halt if either disagrees.</precondition>
  <action>Stage ONLY `CLAUDE.md` in the app submodule and commit it. Run `git -C /workspaces/firestarter_app add CLAUDE.md` then `git -C /workspaces/firestarter_app commit -m "docs(260917-co5): correct build_db.py, protocol-override and test-env facts in CLAUDE.md" -m "<body>"`. Do not edit the file. Do not stage `firestarter/cli_handlers.py`, `tests/test_dev_test_cmd.py`, or anything under `datasheets/`. Do not use `git add -A`, `git add .` or `git commit -a`. Body: three or four short lines drawn from the app bullet in the context section — the real `classify()` arms and measured scope replacing the stale WARNING-5 text, the 16-key pinout list, the fetched-from-pinned-minipro `infoic.xml` correction plus `tools/extra_chips.json` and the comma-joined `part_number` alias note, and the repointed `.planning/` citations plus the test-env and Python 3.11 CI note. No attribution line, no Co-Authored-By trailer, no phase or plan identifier beyond `260917-co5`.</action>
  <verify>
    <automated>test "$(git -C /workspaces/firestarter_app show --pretty=format: --name-only HEAD | grep -c .)" = "1" && git -C /workspaces/firestarter_app show --pretty=format: --name-only HEAD | grep -qx 'CLAUDE.md' && ! git -C /workspaces/firestarter_app log -1 --format=%B | grep -Eiq 'co-authored-by|generated with|ai-assisted|generated by ai|claude code|anthropic' && git -C /workspaces/firestarter_app status --porcelain | grep -q ' M firestarter/cli_handlers\.py' && git -C /workspaces/firestarter_app status --porcelain | grep -q ' M tests/test_dev_test_cmd\.py' && test "$(git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD)" = "v1.39-protocol-0x05-write-correctness"</automated>
  </verify>
  <done>One new commit exists in `/workspaces/firestarter_app` on `v1.39-protocol-0x05-write-correctness`, its only changed path is `CLAUDE.md`, `firestarter/cli_handlers.py` and `tests/test_dev_test_cmd.py` are still unstaged modifications, the three `datasheets/*.pdf` files are still untracked, and the message carries no attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the firestarter_fw CLAUDE.md correction inside the firmware submodule</name>
  <files>/workspaces/firestarter_fw/CLAUDE.md</files>
  <precondition>`/workspaces/firestarter_fw` is on branch `v1.39-protocol-0x05-write-correctness` and `CLAUDE.md` is its only modified path. Assert with `git -C /workspaces/firestarter_fw rev-parse --abbrev-ref HEAD` and `git -C /workspaces/firestarter_fw status --porcelain`; halt if either disagrees.</precondition>
  <action>Stage ONLY `CLAUDE.md` in the firmware submodule and commit it. Run `git -C /workspaces/firestarter_fw add CLAUDE.md` then `git -C /workspaces/firestarter_fw commit -m "docs(260917-co5): repoint dead citations and record CI, test trees and generated messages.h" -m "<body>"`. Do not edit the file. Do not use `git add -A`, `git add .` or `git commit -a`. Body: three or four short lines drawn from the fw bullet in the context section — the archived-milestone citation repoints and the `eprom_params.h` line-range correction, the "What CI runs" section and the `test/` versus `tests/` distinction, and `include/messages.h` being generated from the meta repo's `tools/catalog/messages.toml`. No attribution line, no Co-Authored-By trailer, no phase or plan identifier beyond `260917-co5`.</action>
  <verify>
    <automated>test "$(git -C /workspaces/firestarter_fw show --pretty=format: --name-only HEAD | grep -c .)" = "1" && git -C /workspaces/firestarter_fw show --pretty=format: --name-only HEAD | grep -qx 'CLAUDE.md' && ! git -C /workspaces/firestarter_fw log -1 --format=%B | grep -Eiq 'co-authored-by|generated with|ai-assisted|generated by ai|claude code|anthropic' && test -z "$(git -C /workspaces/firestarter_fw status --porcelain)" && test "$(git -C /workspaces/firestarter_fw rev-parse --abbrev-ref HEAD)" = "v1.39-protocol-0x05-write-correctness"</automated>
  </verify>
  <done>One new commit exists in `/workspaces/firestarter_fw` on `v1.39-protocol-0x05-write-correctness`, its only changed path is `CLAUDE.md`, the firmware working tree is clean, and the message carries no attribution.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the meta CLAUDE.md correction together with both advanced gitlinks</name>
  <files>/workspaces/CLAUDE.md, /workspaces/firestarter_app (gitlink), /workspaces/firestarter_fw (gitlink)</files>
  <precondition>Tasks 1 and 2 are committed, so `git -C /workspaces submodule status` reports a `+` prefix for both `firestarter_app` and `firestarter_fw`. Halt if either prefix is absent — a missing `+` means that submodule commit did not land.</precondition>
  <action>From `/workspaces`, stage EXACTLY three pathspecs and commit: `git -C /workspaces add CLAUDE.md firestarter_app firestarter_fw` then `git -C /workspaces commit -m "docs(260917-co5): correct tools/ inventory, add the branching rule and close the comment-rule loophole" -m "<body>"`. Do not edit any file. Do not stage `.devcontainer/devcontainer.json`, `.gitignore`, `.planning/graphs/*`, any `.vscode/*` file, or any untracked path. Do not use `git add -A`, `git add .` or `git commit -a`. Body: three or four short lines drawn from the meta bullet in the context section — the `tools/` inventory correction to three occupants including `tools/citations/` and its extension scope, the branching rule for milestone work across all three repositories, the comment-rule loophole closer and staged-diff greps, and one line stating that the commit also advances both submodule gitlinks. No attribution line, no Co-Authored-By trailer, no phase or plan identifier beyond `260917-co5`.</action>
  <verify>
    <automated>test "$(git -C /workspaces show --pretty=format: --name-only HEAD | grep -c .)" = "3" && test "$(git -C /workspaces show --pretty=format: --name-only HEAD | grep . | sort | tr '\n' ' ')" = "CLAUDE.md firestarter_app firestarter_fw " && ! git -C /workspaces log -1 --format=%B | grep -Eiq 'co-authored-by|generated with|ai-assisted|generated by ai|claude code|anthropic' && test -z "$(git -C /workspaces submodule status | grep '^+')" && git -C /workspaces status --porcelain | grep -q '^ M \.gitignore$' && git -C /workspaces status --porcelain | grep -q '^ M \.vscode/launch\.json$' && git -C /workspaces status --porcelain | grep -q '^ M \.devcontainer/devcontainer\.json$' && git -C /workspaces status --porcelain | grep -q '^?? anything\.txt$' && test "$(git -C /workspaces rev-parse --abbrev-ref HEAD)" = "v1.39-protocol-0x05-write-correctness"</automated>
  </verify>
  <done>One new commit exists in `/workspaces` on `v1.39-protocol-0x05-write-correctness` whose changed paths are exactly `CLAUDE.md`, `firestarter_app` and `firestarter_fw`; `git submodule status` shows no `+` prefix; the six unrelated tracked modifications and every untracked path are still uncommitted; nothing is pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git history | Unrelated dirty and untracked files sit beside the three intended files; a broad `git add` would publish them. |
| submodule HEAD -> meta gitlink | The meta commit pins whatever SHA each submodule HEAD holds at staging time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260917-co5-01 | Tampering | `git add` pathspec in all three tasks | medium | mitigate | Explicit pathspecs only; `git add -A`, `git add .` and `git commit -a` are forbidden by each action, and each task's verify asserts the exact changed-path set plus the survival of the unrelated dirty paths. |
| T-260917-co5-02 | Tampering | meta gitlink staging | medium | mitigate | Task 3's precondition requires a `+` prefix on both submodules before staging, and its verify asserts no `+` prefix remains afterwards. |
| T-260917-co5-03 | Repudiation | commit messages | low | mitigate | Each verify greps the message for attribution markers and fails if any is present. |
| T-260917-co5-04 | Information disclosure | untracked `firestarter.wiki/`, `tmp/`, `setup-claude-pr-policy.sh`, `datasheets/*.pdf` | low | mitigate | Never staged; Task 3's verify asserts at least one representative untracked path is still untracked. |
| T-260917-co5-SC | Tampering | package installs | low | accept | This plan installs no package and runs no package manager. |
</threat_model>

<verification>
- Three commits exist, one per repository, all on `v1.39-protocol-0x05-write-correctness`.
- `git -C /workspaces submodule status` shows no `+` prefix on either submodule.
- No repository has been pushed: `git -C <repo> status -sb | head -1` still shows the branch ahead of its upstream (or with no upstream), never in sync as a result of this plan.
- The content of the three `CLAUDE.md` files is byte-identical to what it was at planning time — this plan changes no file content.
</verification>

<success_criteria>
- Exactly three new commits, with changed-path sets `{CLAUDE.md}`, `{CLAUDE.md}` and `{CLAUDE.md, firestarter_app, firestarter_fw}`.
- Every unrelated dirty tracked file and every untracked path listed in `<context>` is still uncommitted in its original state.
- No branch created, switched, rebased or deleted; no push; no PR.
- No commit message contains an attribution line or a GSD phase or plan identifier beyond `260917-co5`.
</success_criteria>

<output>
Create `/workspaces/.planning/quick/260917-co5-correct-factual-errors-in-the-three-clau/260917-co5-SUMMARY.md` when done, recording the three commit SHAs.
</output>
