---
phase: quick-260920-ib5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [QUICK-260920-ib5]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 36000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A pull request is open against `main` in henols/firestarter that adds README.md and nothing else."
    - "The README.md on that PR branch is byte-identical to the copy already on `beta` — git blob b0e303e4ec13391bb9654678951cad7c50afdeec."
    - "The single commit on the PR branch touches exactly one path: README.md."
    - "Neither the commit message nor the PR body carries AI authorship attribution."
    - "The /workspaces checkout is still on v1.41-verification-to-host with its uncommitted milestone-v1.41 work untouched."
    - "No GitHub Release and no tag was created in the meta repository."
    - "The PR is left OPEN — not merged, no auto-merge enabled."
  artifacts:
    - "Remote branch origin/docs/readme-on-main carrying one commit on top of origin/main."
    - "An open pull request with base `main`, head `docs/readme-on-main`."
    - "Throwaway worktree removed from the scratchpad; `git worktree list` shows only /workspaces."
  key_links:
    - "Blob-hash equality (b0e303e4ec13391bb9654678951cad7c50afdeec) is the only proof that the copy is verbatim — a reworded README passes every other check."
    - "`gh pr create --base main` is what makes this land on the default branch; without --base it would target the repo default in the CLI's own resolution and could be silently wrong."
    - "Doing the work in a `git worktree` is what keeps the /workspaces submodule layout (firestarter_fw) from being shuffled into main's layout (firestarter)."
---

<objective>
github.com/henols/firestarter renders no README on its front page: `main` is the repository's
default branch and `main` carries no README.md, while the authored front-page README has only ever
lived on `beta`. This plan opens a pull request that puts the identical file on `main`.

Purpose: give visitors landing on the default branch the project description, the wiki link, and
the pointers to firestarter_fw and firestarter_app that already exist on `beta`.
Output: a pushed branch `docs/readme-on-main` with one commit, and an open PR against `main`.

Tracer-first decomposition does not apply here — this is a single-file documentation change with no
layered architecture to prove. The three tasks are one linear sequence: build the commit, publish
the PR, clean up.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/README.md
</context>

<facts_established_at_planning_time>
Verified live in /workspaces at planning time. Task verify steps re-check the ones that matter;
do not re-derive the rest.

- /workspaces is the meta repo `henols/firestarter`, on branch `v1.41-verification-to-host`.
- `git show origin/beta:README.md` and the working-tree `/workspaces/README.md` are IDENTICAL —
  both are blob `b0e303e4ec13391bb9654678951cad7c50afdeec`.
- `origin/main` has NO README.md. Its top-level tree is: .claude .devcontainer .github .gitignore
  .gitmodules .planning .vscode CLAUDE.md firestarter firestarter_app tools.
- `main` is the repo default branch and is protected: PR required, no direct push, no force-push,
  `current_user_can_bypass` is never. A branch + PR is the only route.
- The meta repo's only workflow on main is `.github/workflows/catalog-sync-check.yml`, path-filtered
  to `tools/catalog/**` and its own file. A README-only change triggers nothing and publishes
  nothing. The "a docs push publishes" hazard in CLAUDE.md is about the two SUB-repos, not this one.
- Submodule path differs across branches: `firestarter` on origin/main, `firestarter_fw` on the
  current branch. This is why the primary checkout must never be switched.
- git 2.55.0; `submodule.recurse` is unset; `commit.template` is unset; `git worktree list` shows
  only /workspaces; `gh` is authenticated as henols.
</facts_established_at_planning_time>

<shell_variables>
Export these once at the start of execution and reuse them in every task.

- `SCRATCH` = `/tmp/claude-1000/-workspaces/4a97b051-bcc5-4c47-bc16-881a0f1f506b/scratchpad`
- `WT` = `$SCRATCH/readme-main-wt`
- `META` = `/workspaces`
- `BR` = `docs/readme-on-main`
- `BLOB` = `b0e303e4ec13391bb9654678951cad7c50afdeec`
</shell_variables>

<tasks>

<task type="auto">
  <name>Task 1: Build the README commit in a throwaway worktree off origin/main</name>
  <files>$SCRATCH/readme-main-wt/README.md</files>
  <precondition>`$WT` does not already exist and `git -C /workspaces worktree list` shows only /workspaces; `git -C /workspaces status --porcelain` is non-empty (the milestone-v1.41 working-tree changes are present and must survive this plan).</precondition>
  <action>
First snapshot the primary checkout so Task 3 can prove it was untouched: write
`git -C "$META" status --porcelain` into `$SCRATCH/raw-pre-status.txt`, then filter it into
`$SCRATCH/pre-status.txt` with `{ grep -v '.planning/quick/' < "$SCRATCH/raw-pre-status.txt" || true; } > "$SCRATCH/pre-status.txt"`
(the `|| true` matters — a filter that matches nothing exits 1 and would abort the chain). Also
record `git -C "$META" rev-parse --abbrev-ref HEAD`.

Never switch the /workspaces checkout to another branch. The firmware submodule sits at a different
path on origin/main than on the current branch, and switching would shuffle submodule directories
under a working tree that holds uncommitted milestone-v1.41 work.

Fetch the remote (`git -C "$META" fetch origin main beta`), then create the throwaway worktree:
`git -C "$META" -c submodule.recurse=false worktree add -b "$BR" "$WT" origin/main`. Do NOT run any
submodule init, update or sync inside that worktree. Empty submodule directories there are expected
and correct; leave them alone.

Materialise the file by extracting the tracked blob, not by retyping it:
`git -C "$META" show origin/beta:README.md > "$WT/README.md"`. The content is locked — do not edit,
reword, reformat, retitle, or add anything to it. The point of this change is that `main` and `beta`
carry the same front page.

Stage only that one path (`git -C "$WT" add -- README.md` — never `add -A`, never `add .`) and
commit. Subject: `docs: add README to main`. Body: one sentence saying that main is the default
branch, renders the repository front page, and carried no README. Add no trailers of any kind
beyond what git writes itself.
  </action>
  <verify>
    <automated>cd "$WT" && git rev-parse --abbrev-ref HEAD > "$SCRATCH/v-head.txt" && grep -qx "docs/readme-on-main" < "$SCRATCH/v-head.txt" && [ "$(git rev-parse HEAD:README.md)" = "b0e303e4ec13391bb9654678951cad7c50afdeec" ] && git show --pretty=format: --name-only HEAD > "$SCRATCH/v-files.txt" && [ "$(sed '/^$/d' < "$SCRATCH/v-files.txt")" = "README.md" ] && [ "$(git rev-parse HEAD~1)" = "$(git -C /workspaces rev-parse origin/main)" ] && [ "$(git -C /workspaces rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && git log -1 --format='%B%n%an%n%ae' > "$SCRATCH/v-meta.txt" && if grep -qiE 'co-authored-by|generated with|claude|anthropic|ai-assisted|ai-generated' < "$SCRATCH/v-meta.txt"; then echo "FAIL: attribution present in commit"; exit 1; else echo "PASS"; fi</automated>
  </verify>
  <done>Branch `docs/readme-on-main` exists in the scratchpad worktree with exactly one commit whose parent is the current `origin/main` tip; that commit adds `README.md` and nothing else; the blob is `b0e303e4ec13391bb9654678951cad7c50afdeec`; the commit message and author fields carry no AI attribution; /workspaces is still on `v1.41-verification-to-host`.</done>
</task>

<task type="auto">
  <name>Task 2: Push the branch and open the pull request against main</name>
  <files>$SCRATCH/pr-body.md</files>
  <precondition>`gh auth status` reports an authenticated github.com account with repo scope, and Task 1's commit exists on `$BR`.</precondition>
  <action>
Write the PR description to `$SCRATCH/pr-body.md`, copied VERBATIM from the `<pr_description>`
block of this plan. Do not rewrite it, expand it, or add headings, checklists or tables — it is
already shaped to /workspaces/CLAUDE.md § "Pull requests and merge requests" (one short summary
paragraph plus three bullets, inside the 50-120 word band).

Push the branch: `git -C "$WT" push -u origin "$BR"`. Pushing a branch to the meta repo publishes
nothing — its only main-branch workflow is path-filtered to tools/catalog.

Open the PR from inside `$WT`:
`gh pr create -R henols/firestarter --base main --head docs/readme-on-main --title "docs: add README to main" --body-file "$SCRATCH/pr-body.md"`.
`--base main` is mandatory and must be passed explicitly.

Then STOP. Do not merge the PR. Do not run `gh pr merge`, do not enable auto-merge, do not approve
it, and do not push anything to `main`. Merging is the operator's call. Also create no tag and no
GitHub Release in this repository under any circumstances — a bare version-shaped tag here strands
every CLI's firmware-update check, and a Release is worse.

Record the PR number and URL for the summary.
  </action>
  <verify>
    <automated>cd "$WT" && PR=$(gh pr list -R henols/firestarter --head docs/readme-on-main --base main --state open --json number --jq '.[0].number') && [ -n "$PR" ] && gh pr view "$PR" -R henols/firestarter --json state,baseRefName,headRefName,mergedAt,files --jq '[.state,.baseRefName,.headRefName,(.mergedAt // "null"),(.files|map(.path)|join(","))]|join(" ")' > "$SCRATCH/v-pr.txt" && grep -qx "OPEN main docs/readme-on-main null README.md" < "$SCRATCH/v-pr.txt" && W=$(wc -w < "$SCRATCH/pr-body.md") && [ "$W" -ge 50 ] && [ "$W" -le 120 ] && gh pr view "$PR" -R henols/firestarter --json body --jq .body > "$SCRATCH/v-body.txt" && if grep -qiE 'co-authored-by|generated with|claude|anthropic|ai-assisted|ai-generated' < "$SCRATCH/v-body.txt"; then echo "FAIL: attribution in PR body"; exit 1; else echo "PASS pr=$PR words=$W"; fi</automated>
  </verify>
  <done>An OPEN pull request exists with base `main`, head `docs/readme-on-main`, `mergedAt` null, and a changed-files list of exactly `README.md`; its body is 50-120 words and free of AI attribution; no tag and no GitHub Release were created.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the worktree and prove the primary checkout is untouched</name>
  <files>$SCRATCH/post-status.txt</files>
  <action>
Remove the throwaway worktree: `git -C "$META" worktree remove --force "$WT"` then
`git -C "$META" worktree prune`. Deleting the local `$BR` ref afterwards is optional and harmless
because origin carries it — but NEVER delete the remote branch, the open PR depends on it.

Then prove the primary checkout survived. Capture
`git -C "$META" status --porcelain | grep -v '.planning/quick/' > $SCRATCH/post-status.txt` and diff
it against `$SCRATCH/pre-status.txt` from Task 1. The `.planning/quick/` filter exists because this
quick task's own plan and summary files legitimately change state there; everything else must be
bit-for-bit the same, including the untracked milestone-v1.41 entries.

If the diff is non-empty, stop and report rather than trying to repair it — something reached into
the primary working tree that should not have.
  </action>
  <verify>
    <automated>[ ! -d "$WT" ] && git -C /workspaces worktree list > "$SCRATCH/v-wt.txt" && [ "$(sed '/^$/d' < "$SCRATCH/v-wt.txt" | wc -l)" = "1" ] && [ "$(git -C /workspaces rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && git -C /workspaces status --porcelain > "$SCRATCH/v-raw-status.txt" && { grep -v '.planning/quick/' < "$SCRATCH/v-raw-status.txt" || true; } > "$SCRATCH/post-status.txt" && diff "$SCRATCH/pre-status.txt" "$SCRATCH/post-status.txt" && [ "$(git -C /workspaces hash-object README.md)" = "b0e303e4ec13391bb9654678951cad7c50afdeec" ] && git -C /workspaces ls-files --error-unmatch .gitmodules > /dev/null && echo "PASS"</automated>
  </verify>
  <done>The scratchpad worktree directory is gone, `git worktree list` shows only /workspaces, the primary checkout is still on `v1.41-verification-to-host`, its README.md is unchanged, and its working-tree status is identical to the pre-task snapshot outside `.planning/quick/`.</done>
</task>

</tasks>

<pr_description>
Copy the text between the markers verbatim into `$SCRATCH/pr-body.md`. Do not copy the marker
lines themselves. `wc -w` on the result is 87, inside the 50-120 band the Task 2 verify asserts.

--- BEGIN PR BODY ---
`main` is the repository's default branch, so github.com/henols/firestarter currently renders no README on its front page. This adds the README that already lives on `beta`, unchanged, so visitors landing on the default branch get the project description, a link to the wiki, and pointers to the firmware and CLI repositories.

- Adds `README.md` only; no other file is touched.
- Byte-identical to the copy on `beta`, so the two branches do not diverge.
- No workflow is triggered: the repository's only main workflow is path-filtered to `tools/catalog/**`.
--- END PR BODY ---
</pr_description>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> origin (github.com) | A push publishes whatever was staged; the meta repo's default branch is the project's public front page. |
| scratchpad worktree -> /workspaces checkout | Both share one `.git` dir; a careless submodule or branch operation in the worktree can reach the primary tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-ib5-01 | Tampering | the commit on `docs/readme-on-main` | high | mitigate | Stage only `README.md` by explicit path; Task 1 verify asserts `--name-only` output is exactly `README.md` and that the parent is the `origin/main` tip. |
| T-quick-ib5-02 | Information disclosure | uncommitted milestone-v1.41 files in /workspaces | high | mitigate | Work happens in a separate worktree checked out from `origin/main`, so the milestone files are not even present there; Task 3 diffs a pre/post `status --porcelain` snapshot. |
| T-quick-ib5-03 | Repudiation | commit author and PR body | medium | mitigate | Task 1 and Task 2 verify legs grep the commit message, author fields and PR body for attribution markers and fail closed. |
| T-quick-ib5-04 | Elevation of privilege | branch protection on `main` | medium | mitigate | No direct push to `main`; the only write is to a feature branch, and the merge decision is left to the operator. |
| T-quick-ib5-05 | Denial of service | downstream `fw` update check | critical | mitigate | Create no tag and no GitHub Release in this repository — a PEP 440-parseable tag here makes every CLI report firmware up to date forever. Stated as an explicit prohibition in Task 2. |
| T-quick-ib5-06 | Tampering | submodule directory layout | medium | mitigate | `-c submodule.recurse=false` on `worktree add`, an explicit ban on submodule init/update/sync in the worktree, and Task 3's status diff. |
</threat_model>

<verification>
- The PR is open against `main` with a one-file diff.
- `git -C /workspaces rev-parse origin/docs/readme-on-main:README.md` equals
  `b0e303e4ec13391bb9654678951cad7c50afdeec`.
- `gh release list -R henols/firestarter` and `git ls-remote --tags origin` show nothing new.
- `git -C /workspaces status --porcelain` outside `.planning/quick/` is unchanged from the start of
  the run, and HEAD is still `v1.41-verification-to-host`.
</verification>

<success_criteria>
- One commit on `origin/docs/readme-on-main`, adding only `README.md`, blob
  `b0e303e4ec13391bb9654678951cad7c50afdeec`.
- An OPEN, unmerged PR with base `main`, body 50-120 words, no AI attribution.
- No tag, no GitHub Release, no push to `main` or `beta`.
- Scratchpad worktree removed; /workspaces branch and working tree bit-for-bit as found.
</success_criteria>

<output>
Create `.planning/quick/260920-ib5-create-a-readme-md-in-meta-main-that-poi/260920-ib5-SUMMARY.md`
when done. Include the PR number and URL, the commit sha on `docs/readme-on-main`, and the
pre/post status diff result.
</output>
