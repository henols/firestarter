---
phase: 260917-8pj-dev-test-default-3-runs-for-read-and-wri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/submit.py
  - firestarter_app/tests/fixtures/report_shapes.py
  - firestarter_app/tests/test_chip_test.py
  - firestarter_app/tests/test_chip_test_cycle.py
  - firestarter_app/tests/test_chip_test_sdp_leg.py
  - firestarter_app/tests/test_dev_test_cmd.py
  - firestarter_app/tests/test_submit.py
autonomous: true
requirements:
  - QUICK-260917-8pj-A
  - QUICK-260917-8pj-B
estimate:
  tokens: 100000
  raw_tokens: 200000
  tasks: 4
  confidence: high
must_haves:
  truths:
    - "The three single-pass-read commits (aadd479, d8662ff, 86bb3c0) are undone by forward `git revert` commits, leaving the tracked tree byte-identical to `ff5dd1d` before any new work starts."
    - "`_REPEAT_POLICY_OPS` is `_MULTI_RUN_OPS | {OP_READ}` again — the read step is a normal repeat-policy op."
    - "The frozen hash for shape `m27c512-full-runs-1` is `e4838f7bb1d3` again."
    - "`dev test` exposes no two-pass-read opt-in flag; the read step and the write/verify/erase block are governed by one `runs` knob."
    - "A default `dev test` run passes `runs=3` to `run_plan`; `--fast` still passes `runs=1` with `allow_single_run=True`, and that is the only way to reach a single pass."
    - "`dedup_fingerprint` is byte-identical for a run at run_count 2 and the same run at run_count 3, and differs at run_count 1 — so no already-filed community report changes group."
    - "The four fixtures transcribed from real filed issues still reproduce their filed hashes: `gh47-sst27sf512-pass`, `gh28-m27c512-fail` (`31547956e56b`), `gh20-at28c256-fail` (`00e121446ceb`), `gh23-w27e257-fail` (`7a89fcea856a`)."
    - "`dev test --submit` files the report through the authenticated `gh` tier without ever calling `confirm_fn`, identically on a TTY and off one."
    - "`dev test --submit` files a `--fast` run too — the flag is never gated on the run being an accurate sweep (OP-1)."
    - "When the duplicate check could not run, `--submit` files anyway and discloses that fact in both the issue body and the console (OP-2)."
    - "When `gh` is absent or unauthenticated, `--submit` prints the prefilled URL, states plainly that nothing was filed, and never opens a browser."
    - "A `dev test` run WITHOUT `--submit` behaves exactly as it does today: it asks on a TTY and prints the URL while filing nothing off a TTY."
    - "The `is_submittable` refuse gate still refuses, naming the missing fields, before any `gh` call — including under `--submit`."
    - "`dev test`'s exit code is unchanged by the presence, success, or failure of `--submit`."
    - "Not one new comment line is added to any file under `firestarter_app`; comments the change falsifies are deleted, and surviving rationale lives in a docstring."
  artifacts:
    - "firestarter_app/firestarter/cli_handlers.py — `_DEFAULT_RUNS` constant, `--submit` option on `dev test`, `_ALWAYS_WRITES_PASS_COUNT` re-pinned"
    - "firestarter_app/firestarter/submit.py — `submit_report` consent parameter and its unattended `gh`-tier dispatch"
    - "firestarter_app/tests/test_submit.py — the unattended-path behaviour suite"
    - "firestarter_app/tests/test_dev_test_cmd.py — option-surface pin, help ceiling, run-count fallout, `--submit` wiring tests"
    - "firestarter_app/tests/test_blast_radius_invariance.py or tests/test_diagnostic_report.py — the run_count-invariance proof"
  key_links:
    - "`dev_test` -> `run_plan(runs=_DEFAULT_RUNS)`: the single place the default run count is decided."
    - "`dev_test` -> `submit_report(auto_submit=...)`: the only path by which explicit consent reaches the filer."
    - "`repeat_policy_tag` -> `dedup_fingerprint`: the tag fires only at run_count 1, which is why 2 and 3 hash alike."
    - "`submit_report` -> `gh_available` -> `submit_via_gh`: the only tier allowed to file unattended."
---

<objective>
Reverse the single-pass-read decision, raise the `dev test` default run count from 2 to 3 for
read and write/verify/erase alike, and give `dev test` a `--submit` flag that files the report
without a prompt.

Purpose: the operator has changed the read-depth decision, and the pending ask for an
unattended filing flag (todo `2026-08-30-dev-test-flag-to-auto-file-issue.md`) blocks scripted
multi-chip sweeps.
Output: revert commits plus two behaviour changes inside the `firestarter_app` submodule only.

Not a tracer-first plan, deliberately: every layer this touches is already proven end-to-end
(`dev test` runs, files reports, and has a live test suite around both), so a thin vertical
slice would carry no information a horizontal task split does not already give.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
@/workspaces/.planning/todos/pending/2026-08-30-dev-test-flag-to-auto-file-issue.md

Do NOT bare-`cat` `/workspaces/.planning/STATE.md` — one ~52,000-character line, ~125k tokens.

All work happens in `/workspaces/firestarter_app` on branch
`v1.39-protocol-0x05-write-correctness`, HEAD `86bb3c0`, comparison base `ff5dd1d`
(== `origin/beta`). Every commit lands INSIDE that submodule. No meta-repo commit, no gitlink
move, no `.planning/` edit, no push, no merge, no PR. `workflow.use_worktrees` is false — work
sequentially on the main tree.

Environment: use `/workspaces/firestarter_app/.venv311/bin/python` (3.11.16) for every pytest,
ruff and mypy leg. The bare `python3` is 3.12 and has masked a real CI break. `pytest` addopts
is `-ra -q`; pass `-o addopts=""` when a count must be read. A full `pytest tests/` run takes
~200s — allow 600s. Suite state on `86bb3c0`: 2013 passed, zero errors.

HARD RULE, not overridable by this plan: add no new comment lines to any source file under
`firestarter_app`, tests included. Comments this change falsifies must be DELETED; if the
rationale must survive, fold it into the adjacent docstring. Click docstrings are user-facing
`--help` text — update them, never strip them. Never write a phase number, plan id, quick-task
id, `.planning/` path or decision id into source.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert the three single-pass-read commits</name>
  <files>firestarter/chip_test.py, firestarter/cli_handlers.py, tests/fixtures/report_shapes.py, tests/test_chip_test.py, tests/test_chip_test_cycle.py, tests/test_chip_test_sdp_leg.py, tests/test_dev_test_cmd.py</files>
  <action>
Undo the read-once decision with forward revert commits, never history rewriting. Run
`git revert --no-edit` for `86bb3c0`, then `d8662ff`, then `aadd479` — newest first, three
separate commits, each keeping git's generated `Revert "..."` subject so the record states what
was undone and why it can be found. No AI attribution in any message.

`aadd479` is a direct child of `ff5dd1d`, so after the three reverts the tracked tree is
identical to `ff5dd1d` and every property the operator named is restored by construction: the
repeat-policy op set is `_MULTI_RUN_OPS | {OP_READ}` again, the `read_runs` kwarg and the
`_runs_for_step` helper are gone, the two-pass-read opt-in flag is gone from `dev test`, and the
frozen hash for shape `m27c512-full-runs-1` is `e4838f7bb1d3`.

If any revert conflicts, stop and report rather than hand-resolving — a conflict means the tree
is not what this plan measured, and the rest of the plan is anchored on `ff5dd1d`.

Do not start Task 2 until the whole suite is green here; the later tasks' fallout must be
distinguishable from revert fallout.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test -z "$(git diff ff5dd1d HEAD --name-only)" && test "$(git rev-list --count ff5dd1d..HEAD)" = "6" && echo "tree restored to ff5dd1d across 6 commits"</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "import firestarter.chip_test as ct, inspect; s=inspect.signature(ct.run_plan); assert 'read_runs' not in s.parameters; assert ct.OP_READ in ct._REPEAT_POLICY_OPS; from tests.fixtures.report_shapes import FROZEN_HASHES; assert FROZEN_HASHES['m27c512-full-runs-1'] == 'e4838f7bb1d3'; print('read_runs gone, OP_READ back in the repeat policy, frozen hash restored')"</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "
from firestarter.cli_handlers import dev_test
import click
names = [p.name for p in dev_test.params if isinstance(p, click.Option)]
assert names == ['fast'], names
print('dev test option surface:', names)"</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -3</automated>
  </verify>
  <done>Three revert commits exist inside the submodule; `git diff ff5dd1d HEAD` is empty; `run_plan` has no `read_runs` parameter; `OP_READ` is back in `_REPEAT_POLICY_OPS`; the `m27c512-full-runs-1` frozen hash is `e4838f7bb1d3`; `dev test` carries `--fast` only; the full suite passes with zero errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default the run count to 3, and prove the fingerprint does not move</name>
  <files>firestarter/cli_handlers.py, tests/test_dev_test_cmd.py, tests/test_blast_radius_invariance.py</files>
  <behavior>
    - A default `dev test` invocation passes `runs=3` and `allow_single_run=False` to `run_plan`.
    - `dev test --fast` still passes `runs=1` and `allow_single_run=True`.
    - `dedup_fingerprint` of a result set at run_count 3 equals the same set at run_count 2.
    - `dedup_fingerprint` at run_count 1 differs from both — the degraded marker still fires.
    - All four filed-issue fixtures still reproduce their filed hashes.
    - A full ALLOW-chip `dev test` run makes 7 `write_eprom` calls: three write passes plus the
      SDP leg's four single-run writes.
  </behavior>
  <action>
Introduce a module-level `_DEFAULT_RUNS = 3` in `cli_handlers.py`, placed beside
`_ALWAYS_WRITES_PASS_COUNT`, and change the `run_plan` call in `dev_test` from `runs=1 if fast
else 2` to `runs=1 if fast else _DEFAULT_RUNS`. One knob governs read and write/verify/erase
alike, exactly as it did before `aadd479`. `--fast` keeps meaning one run for every multi-run op
including read, and keeps passing `allow_single_run=True`. Do not touch `run_plan`'s own
signature default of 2 — it is the library default that many tests pass explicitly and the
whole-database sweep documents; only the CLI's choice changes.

Re-pin `_ALWAYS_WRITES_PASS_COUNT` to 7. It describes a real `dev test` sweep — three write
passes plus the SDP leg's four single-run writes — so it now keys off the CLI's default, not
`run_plan`'s. Update `TestWritePassCountDerivedFromLivePlanD09`'s derivation to read
`_DEFAULT_RUNS` instead of `inspect.signature(run_plan).parameters["runs"].default`, keeping the
test's own rule intact: it derives the number, it never restates it.

Update the user-facing text. The `--fast` help currently says "Run one write/verify cycle
instead of two" — it is three now. The `dev test` Click docstring currently says the write/verify
block "runs as a CYCLE, twice" — it is three times, and the read step runs three times too.
These are `--help` text: rewrite them, never strip them.

Delete the comment block above `_ALWAYS_WRITES_PASS_COUNT` that states the write/verify/erase
steps write twice; it is now false, and the invariant it describes is already stated in the
derivation test's docstring, which is where it survives. Correct
`make_restore_failed_operator`'s docstring in the same way — it names "the shipped write step's
two runs" as part of a fixed call order this change lengthens by one. Add no replacement
commentary anywhere.

Known fallout to repair, verified against the tree rather than assumed — re-check the list by
running the suite, do not trust it as exhaustive:
`tests/test_dev_test_cmd.py::TestSamplerBracketing::test_every_run_fills_split_voltage_slots`
feeds four `vpp_values`/`vpe_values` into a side_effect list sized for two bracketed writes; three
writes need six, and the asserted "last pair wins" values move with them. The
`make_restore_failed_operator` double gates persistence on `idx < _ALWAYS_WRITES_PASS_COUNT - 1`,
so its last-write index shifts with the constant. `tests/test_chip_test_cycle.py` passes `runs`
explicitly and should be unaffected — confirm that rather than assume it.

Then write the correctness proof the operator asked for, as a test rather than an assertion in
prose. `dedup_fingerprint` hashes the chip name, the protocol, each step's
`op=verdict:classification`, and `repeat_policy_tag`/`coverage_tag` only when non-empty;
`repeat_policy_tag` returns the degraded marker only at `run_count == 1`. So build one result set
twice — identical in every field except `run_count`, 2 versus 3 — and assert the two fingerprints
are equal, and that the same set at `run_count` 1 differs from both. Build it through
`tests/fixtures/report_shapes.py`'s existing `build_shape_from_step_specs` helper or an equivalent
direct construction; do NOT register a new shape id, because `SHAPE_IDS`, `FROZEN_HASHES` and the
pinned-set gate all count the registry and a new member would trip three unrelated assertions.
Put the test next to the existing fingerprint-invariance gates in
`tests/test_blast_radius_invariance.py`, which already re-derives every `FROZEN_HASHES` entry —
that parametrized gate is what proves the four filed community hashes are untouched, so name it
in the verify below rather than duplicating it.

Commit once, inside the submodule, with a message that states the default moved from two runs to
three and that the reverted read-once work is superseded by it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_blast_radius_invariance.py -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "
from unittest.mock import patch
from click.testing import CliRunner
import firestarter.cli_handlers as ch
assert ch._DEFAULT_RUNS == 3, ch._DEFAULT_RUNS
assert ch._ALWAYS_WRITES_PASS_COUNT == 7, ch._ALWAYS_WRITES_PASS_COUNT
print('default runs', ch._DEFAULT_RUNS, 'write passes', ch._ALWAYS_WRITES_PASS_COUNT)"</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_blast_radius_invariance.py -o addopts="" -q -k "frozen or fingerprint" 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && DIFF=$(git diff ff5dd1d -- firestarter tests) && test -z "$(printf '%s' "$DIFF" | /usr/bin/grep -E '^\+[[:space:]]*#')" && echo "no added comment lines"</automated>
  </verify>
  <done>`_DEFAULT_RUNS` is 3 and is the only place the CLI default is written; `_ALWAYS_WRITES_PASS_COUNT` is 7 and the derivation test computes it from `_DEFAULT_RUNS` and the live plan; the `--fast` help and the `dev test` docstring both say three; the falsified comment block is deleted with no replacement; a test proves run_count 2 and 3 hash identically while 1 differs; every `FROZEN_HASHES` entry including the four filed community hashes still re-derives; the diff against `ff5dd1d` adds zero comment lines; one commit exists inside the submodule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Give `submit_report` an explicit-consent unattended path</name>
  <files>firestarter/submit.py, tests/test_submit.py</files>
  <behavior>
    - Called without the consent argument, `submit_report` behaves byte-identically to today:
      asks on a TTY, prints the URL and files nothing off a TTY.
    - With consent, `isatty_fn` is never called at all — the path is identical on and off a TTY.
    - With consent, `confirm_fn` is never called on any branch.
    - With consent and `is_submittable` false, it still refuses, names the missing fields, and
      calls neither `run_fn`, `browser_open`, `find_prior_report_fn` nor `comment_via_gh_fn`.
    - With consent and a prior issue found, it comments on that issue and files no second one.
    - With consent and a failed comment, it states the failure and opens no browser.
    - With consent and a dedup check that could not run, it files anyway (OP-2) and the disclosure
      reaches both the console and the body handed to `gh`.
    - With consent and `gh` unavailable or unauthenticated, it prints the prefilled URL, states
      that nothing was filed, and never calls `browser_open`.
    - With consent and a successful file, it prints the created issue URL.
    - The create argv still carries nothing permission-gated — no label flag.
  </behavior>
  <action>
Add one keyword-only parameter to `submit_report`, `auto_submit: bool = False`, carrying the
explicit consent. Keep every existing injectable seam (`which_fn`, `run_fn`, `browser_open`,
`isatty_fn`, `confirm_fn`, `console`, `find_prior_report_fn`, `comment_via_gh_fn`) so the tests
keep driving it with no network and no subprocess.

Order of operations: the refuse gate (Step 1), the sanitize/build steps (Step 2) and the dedup
query (Step 3) all run first, unchanged, on every path. Then, BEFORE the TTY branch, take the
consent branch when `auto_submit` is true. Placing it there is what makes the flag behave the
same on a TTY and off one, and it means `isatty_fn` is never consulted — assert that in a test,
because it is the mechanical proof the two environments cannot diverge.

Inside the consent branch: when the dedup query found a prior issue, comment on it through
`comment_via_gh_fn` and file nothing new; on a `None` return print the failure reason and stop —
do not degrade to the browser, which cannot file anything unattended. When no prior issue was
found, file through `submit_via_gh` if and only if `gh_available` is true, printing the created
URL on success and the failure reason on a `None` return, and in neither case opening a browser.
When `gh` is absent or unauthenticated, print the prefilled `build_issue_url` output and a plain
statement that nothing was filed and why — mirroring the existing off-TTY print shape, which
already prints a URL for a human to use later.

Fail open on the dedup check, per the operator's decision (OP-2): when `find_prior_report_fn`
reports the check could not run, file anyway, and disclose it in two places — one console line,
and one sentence appended to the body that reaches `gh`, so the maintainer reading the issue
sees that the duplicate check did not run before it was filed. The appended sentence must only
be added on this branch; every other path's body stays exactly what `build_body` returned.

Rewrite `submit_report`'s docstring to match. Its Step 4 paragraph currently states that filing
nothing off a TTY is a standing ban that survives because no flag carries the consent — that is
now false, and the docstring is the one place in this package where the rationale legitimately
lives. State instead that the off-TTY path still files nothing WITHOUT consent, and that the
consent parameter is the seam that lets it file. Do not write a phase number, a decision id, or
a `.planning/` path into it.

Write the behaviours above as tests in `tests/test_submit.py`, reusing that module's existing
idioms: injected fakes for every seam, list-argv assertions, and the NEGATIVE assertion style
already used for permission-gated flags — the create argv must carry no label flag, because
`gh issue create --label` aborts unless the label pre-exists and the caller has write access,
and community testers have neither. Keep the repo target assertion on the pinned `SUBMIT_REPO`.

Commit once, inside the submodule.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_submit.py -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "
import inspect
from firestarter.submit import submit_report
p = inspect.signature(submit_report).parameters['auto_submit']
assert p.default is False and p.kind is inspect.Parameter.KEYWORD_ONLY
print('auto_submit is keyword-only and defaults to', p.default)"</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/ruff check firestarter/ tests/ && ./.venv311/bin/ruff format --check firestarter/ tests/</automated>
    <automated>cd /workspaces/firestarter_app && DIFF=$(git diff ff5dd1d -- firestarter tests) && test -z "$(printf '%s' "$DIFF" | /usr/bin/grep -E '^\+[[:space:]]*#')" && echo "no added comment lines"</automated>
  </verify>
  <done>`submit_report` takes a keyword-only `auto_submit` defaulting to False; with it false every existing test still passes unchanged; with it true `isatty_fn` and `confirm_fn` are never called, the refuse gate still refuses, a prior issue gets a comment instead of a duplicate, a dedup check that could not run files anyway with the disclosure in both the console and the body, an absent or unauthenticated `gh` prints the URL and files nothing without opening a browser, and the create argv still carries no label flag; the diff against `ff5dd1d` adds zero comment lines; one commit exists inside the submodule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire `--submit` onto `dev test` and re-pin the option surface</name>
  <files>firestarter/cli_handlers.py, tests/test_dev_test_cmd.py</files>
  <behavior>
    - `dev test --submit` reaches `submit_report` with the consent argument true.
    - `dev test` without the flag reaches `submit_report` with it false — on a TTY and off one.
    - `dev test --fast --submit` is accepted and files (OP-1): no mutual-exclusion refusal.
    - `dev test`'s exit code is identical with and without `--submit`, whether filing succeeds,
      fails, or is skipped for want of `gh`.
    - The option surface is exactly `--fast` and `--submit`; the two genuinely removed flags
      still error as unknown options.
  </behavior>
  <action>
Add a `--submit` flag to `dev test`: `is_flag=True`, `default=False`, help text naming what it
does and what it costs — it files a public issue in the project tracker with no human in the
loop, through the authenticated `gh` CLI, and does nothing but print a URL when `gh` is
unavailable. Pass it to `submit_report` as the consent argument at the existing call site.

The flag may file any run, including a `--fast` one (OP-1). Write no mutual-exclusion check and
no refusal for that combination; a fast run's `repeat_policy_tag` already re-keys its
`dedup_fingerprint` so it cannot join an accurate run's promotion group, which is the protection
that matters.

Delete the comment above the `submit_report` call that states every run reaches the filing ask
rather than only an explicit `--submit` run — the change makes it false, and the statement it was
protecting is now expressed by the flag itself. Add no replacement comment. If the `dev test`
docstring needs to mention that the run ends by offering to file the report, that belongs in the
docstring, which is `--help` text.

Repair the surface gates in `tests/test_dev_test_cmd.py`, which exist precisely to catch this:
`TestZeroOptionSurface` pins the option name list exactly — widen it to the two options and say
in the class docstring that this is a second, stated reversal of the zero-option decision, in the
same voice the `--fast` reversal already uses. Drop `submit` from the removed-flag parametrize
and its ids, leaving the two flags that really are gone; that parametrize is the gate that
currently asserts `--submit` errors as an unknown option, and it must not be left contradicting
the code. Re-measure the `dev test --help` line-count ceiling and re-pin it TIGHTLY at the real
number — a loose ceiling defeats the assertion's whole purpose.

Then write the wiring tests. Patch `firestarter.submit.submit_report` and assert the consent
argument's value for: a bare run, a `--submit` run, and a `--fast --submit` run. Prove the bare
run's default is unchanged in both environments — that this flag did not move the default is the
property most worth pinning, because the off-TTY silence was deliberate. Prove the exit code is
unchanged by the flag by running the same chip with and without it and comparing.

Commit once, inside the submodule, with a message that states `--submit` returns as an explicit
opt-in and that unattended filing now requires it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/test_dev_test_cmd.py -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -c "
import click
from click.testing import CliRunner
from firestarter.main import cli
import firestarter.cli_handlers as ch
names = [p.name for p in ch.dev_test.params if isinstance(p, click.Option)]
assert names == ['fast', 'submit'], names
r = CliRunner().invoke(cli, ['dev', 'test', '--help'])
assert r.exit_code == 0, r.output
print('options:', names, 'help lines:', len(r.output.strip().splitlines()))"</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python tools/planning_citation_gate.py firestarter tests tools && ./.venv311/bin/ruff check firestarter/ tests/ && ./.venv311/bin/ruff format --check firestarter/ tests/</automated>
    <automated>cd /workspaces/firestarter_app && ./.venv311/bin/python -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70 -o addopts="" 2>&1 | tail -5</automated>
    <automated>cd /workspaces/firestarter_app && DIFF=$(git diff ff5dd1d -- firestarter tests tools) && test -z "$(printf '%s' "$DIFF" | /usr/bin/grep -E '^\+[[:space:]]*#')" && DIRTY=$(git status --porcelain firestarter tests) && test -z "$DIRTY" && echo "clean tree, no added comment lines"</automated>
  </verify>
  <done>`dev test` exposes exactly `--fast` and `--submit`; the removed-flag gate lists only the two flags that are genuinely gone; the `--help` ceiling is re-pinned at the measured value; the consent argument reaches `submit_report` as true only under the flag, on a TTY and off one; `--fast --submit` is accepted; the exit code is unchanged by the flag; the citation gate, ruff check, ruff format and the full suite with the 70% coverage floor all pass on Python 3.11; the working tree under `firestarter/` and `tests/` is clean; the diff against `ff5dd1d` adds zero comment lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local run -> public issue tracker | `--submit` publishes a locally-generated report to a public GitHub repository with no human reading it first |
| host app -> `gh` subprocess | argv and stdin cross into an external binary that acts with the user's credentials |
| firmware/chip data -> issue body | measured values, paths and error prose collected on the bench end up in a public artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8pj-01 | Information disclosure | `submit_report` consent branch | high | mitigate | The consent branch reuses the SAME `sanitize_dict` -> `build_body` output every other path uses; Task 3 asserts the body handed to `gh` under consent is the sanitized one, so no unscrubbed PII vector reaches a public issue |
| T-8pj-02 | Elevation of privilege | `dev test` flag -> auto-file path | critical | mitigate | The auto path is reachable only when `auto_submit` is true; Task 3 asserts `confirm_fn` and `isatty_fn` are never called under it, and Task 4 asserts a bare run passes false on a TTY and off one — the outward-facing path cannot fire on a run the operator did not opt into |
| T-8pj-03 | Tampering | `gh issue create` argv | high | mitigate | Filing keeps going through the existing `submit_via_gh`: list argv, never a shell string, explicit `--repo`, body on stdin. Task 3 re-asserts the argv carries no permission-gated flag, since `--label` aborts for a community tester with read-only access |
| T-8pj-04 | Denial of service | duplicate public issues at sweep scale | medium | accept | Operator decision OP-2 is to fail open when the duplicate check cannot run. Residual risk is bounded by disclosing in the filed body that the check did not run, so a maintainer can recognise and close a duplicate |
| T-8pj-05 | Repudiation | unattended filing with no receipt | medium | mitigate | Every consent-branch outcome prints: the created issue URL on success, the comment URL on a duplicate, the captured failure reason on a `gh` error, and the prefilled URL plus a plain "nothing was filed" when `gh` is unavailable |
| T-8pj-06 | Spoofing | report attributed to a chip it did not test | low | accept | Unchanged by this plan — the report is built from the run that just executed and the refuse gate still requires chip, protocol and host version before anything is filed |
</threat_model>

<verification>
- Full suite on Python 3.11 with the coverage floor: `pytest tests/ --cov=firestarter --cov-fail-under=70` passes, at or above the 2013-test baseline, zero errors.
- `ruff check`, `ruff format --check` and `tools/planning_citation_gate.py` all pass over `firestarter/`, `tests/` and `tools/`.
- mypy error count over `firestarter/ tests/` on Python 3.11 has not risen above the count measured on the post-revert tree.
- `git diff ff5dd1d -- firestarter tests tools` adds zero lines whose first non-space character is `#`.
- Six commits exist between `ff5dd1d` and the final HEAD from Task 1's reverts plus one per subsequent task; nothing is committed in the meta repo, nothing is pushed, no gitlink moves.
</verification>

<success_criteria>
- The single-pass-read machinery is gone by revert, not by rewrite, and the three revert commits name what they undid.
- A default `dev test` run reads three times and runs the write/verify/erase block three times, from one `runs` knob.
- `--fast` remains the only route to a single pass, and still carries `allow_single_run=True`.
- Every already-filed community fingerprint is byte-identical under the new default, proven by a test that compares run_count 2 against run_count 3 and by the four filed-issue fixtures re-deriving their hashes.
- `dev test --submit` files unattended through `gh` alone, on a TTY and off one, for fast and accurate runs alike, discloses a dedup check that could not run, and never opens a browser.
- A `dev test` run without `--submit` is unchanged in every observable way, including its exit code.
- No new comment line exists anywhere under `firestarter_app`; the statements this change falsified are deleted or corrected in docstrings.
</success_criteria>

<output>
Create `.planning/quick/260917-8pj-dev-test-default-3-runs-for-read-and-wri/260917-8pj-SUMMARY.md` when done.

Record in it: the three revert shas, the final run-count constants, the `--submit` consent
parameter name, any test the run-count change broke that this plan did not predict, and any
place where a falsified comment was deleted rather than corrected.
</output>
