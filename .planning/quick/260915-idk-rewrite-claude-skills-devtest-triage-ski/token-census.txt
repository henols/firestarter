114	erase	| `NA` | step does not apply to this family (e.g. UV-EPROM erase) | not a failure — never report it as one |
174	blank-check	| 3 | Every failing step comes back **`OK`** | **`NA` does not count.** `NA` means the step stopped running, not that it started passing — #48 legitimately reports `blank-check NA`. Closing a `blank-check BAD` against a later `blank-check NA` hides a live defect behind a green title |
174	blank-check	| 3 | Every failing step comes back **`OK`** | **`NA` does not count.** `NA` means the step stopped running, not that it started passing — #48 legitimately reports `blank-check NA`. Closing a `blank-check BAD` against a later `blank-check NA` hides a live defect behind a green title |
174	blank-check	| 3 | Every failing step comes back **`OK`** | **`NA` does not count.** `NA` means the step stopped running, not that it started passing — #48 legitimately reports `blank-check NA`. Closing a `blank-check BAD` against a later `blank-check NA` hides a live defect behind a green title |
300	Read	Then use the Read tool on the PDF with `pages: "2"` for the pin-configuration page.
319	writes	| **VCC** | operating range (e.g. 5V ±10%) | `VCC:` | marginal/erratic writes |
320	Erase	| **Erase** | "electrically erasable" vs UV window | `Can be erased:` | `erase` reported NA/BAD wrongly |
320	erased	| **Erase** | "electrically erasable" vs UV window | `Can be erased:` | `erase` reported NA/BAD wrongly |
320	erase	| **Erase** | "electrically erasable" vs UV window | `Can be erased:` | `erase` reported NA/BAD wrongly |
321	ID	| **Chip ID** | device-identification / signature-byte section | `Chip ID:` | `id` step NA or mismatching |
321	ID	| **Chip ID** | device-identification / signature-byte section | `Chip ID:` | `id` step NA or mismatching |
321	id	| **Chip ID** | device-identification / signature-byte section | `Chip ID:` | `id` step NA or mismatching |
322	write	| **Program algorithm** | programming waveform: page-write, DQ7 polling, unlock sequence, single-pulse | `Protocol:` line | the whole write path is wrong for the part |
322	write	| **Program algorithm** | programming waveform: page-write, DQ7 polling, unlock sequence, single-pulse | `Protocol:` line | the whole write path is wrong for the part |
323	write	| **Page size** | page-write buffer size (e.g. 64-byte page) | `infoic_page_size_raw` in the DB entry | partial/corrupt page writes |
323	writes	| **Page size** | page-write buffer size (e.g. 64-byte page) | `infoic_page_size_raw` in the DB entry | partial/corrupt page writes |
324	Write	| **Write protection** | SDP / hardware data protection section | `Flags:` | a locked chip that silently refuses writes |
324	writes	| **Write protection** | SDP / hardware data protection section | `Flags:` | a locked chip that silently refuses writes |
325	write	| **Pulse timing** | write cycle / program pulse time | `pulse_duration` | under- or over-programming |
406	blank-check	are both `true`, so SDP handling *is* requested. With the data right, `blank-check BAD`
407	write	plus `write BAD` points at the SDP unlock sequence not taking effect on the wire — which
408	Write	is what open issue #12 ("AT28Cxxx Write Protection Enable/Disable missing") describes.
414	write	`page_size` or whether write protection is represented is wrong often enough to invert
434	Read	| `pdftoppm is not installed` from the Read tool | `sudo apt-get install -y poppler-utils` |
