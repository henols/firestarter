---
phase: quick-260912-mo6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/audit_coverage_matrix.py
  - firestarter_app/tools/check_no_exists_proxy.py
  - firestarter_app/tests/test_audit_coverage_matrix_default_paths.py
autonomous: true
requirements: [QUICK-260912-mo6]
commits_land_in: firestarter_app

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running the tool from a checkout whose parent has no planning directory exits non-zero, names --output and --ledger on stderr, and creates nothing."
    - "Running the tool the operator's way (parent HAS a planning directory) behaves exactly as before: same default paths, same bytes, exit 0."
    - "An explicitly-passed --output/--ledger is never blocked, including for paths outside the repo."
    - "Supplying only ONE of --output/--ledger still fails closed, because the other default would escape."
    - "--check and --all-algorithms keep their existing exit-code semantics."
  artifacts:
    - firestarter_app/tests/test_audit_coverage_matrix_default_paths.py
    - .planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/red-before-fix.txt
  key_links:
    - "main()'s default-selection chokepoint (both the normal and the --all-algorithms branch, both output AND ledger) is the only place defaults are resolved."
    - "The guard fires at invocation time, not import time — tests/test_audit_coverage_matrix.py imports the module and must keep working."
---

<objective>
`firestarter_app/tools/audit_coverage_matrix.py` derives `_REPO_ROOT` with three `dirname()`
hops from its own `__file__`, which lands on the PARENT of the `firestarter_app` checkout. In
the operator's devcontainer that parent is the meta repo (`/workspaces`) and is intended — the
tool regenerates a tracked planning artifact there. In a standalone clone of the published pip
repo the same arithmetic lands in the user's home directory, and the tool creates a planning
tree outside the repository entirely.

Fail closed: permit the escape only when a planning directory ALREADY exists at that root
(the operator's layout, behaviour byte-for-byte unchanged); otherwise exit non-zero, write
nothing, and tell the user to pass explicit paths. Explicit paths always win.

Purpose: the tool must be safe for anyone who is not this operator, without changing anything
for this operator.
Output: an invocation-time guard in the tool, a load-bearing test under `tests/`, and observed
evidence that the operator's own invocation still produces an empty `git diff`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
@/workspaces/.planning/notes/host-tools-checker-apparatus-audit.md

Source of the defect (read lines 1-70 and 1905-1924 only — the file is 1942 lines):
@/workspaces/firestarter_app/tools/audit_coverage_matrix.py

Test-suite conventions for this tool (subprocess invocation shape lives around lines 580-660):
@/workspaces/firestarter_app/tests/test_audit_coverage_matrix.py
</context>

<facts_established_during_planning>
Verified live, so the executor does not have to re-derive any of it:

- `_REPO_ROOT` → `/workspaces`; `DEFAULT_OUTPUT` → `/workspaces/.planning/v1.3-COVERAGE-MATRIX.md`
  (exists, tracked in the meta repo). Both meta files are CLEAN right now.
- Nothing anywhere imports `DEFAULT_OUTPUT`, `DEFAULT_LEDGER`, `DEFAULT_OUTPUT_ALL`,
  `DEFAULT_LEDGER_ALL` or `_REPO_ROOT` — the only reader is `main()` at lines 1913-1918.
  Keeping those four constants exactly as they are is therefore the minimum-risk shape; do not
  make them lazy and do not delete them.
- The ONLY other `__file__`-derived path in the tool is `_DATA_DIR` (line 49, ONE `..` hop,
  same-repo package data). `FIRESTARTER_DB_FILE` (lines 50-53) overrides the DB path at import
  time — that env seam is what lets a copy of the tool run from a scratch tree.
- `--check` is a true dry run: `generate_matrix` returns before any `write_text`/`save_ledger`.
- CI gates are `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/`,
  `python tools/check_mypy_watermark.py` (mypy scope is `firestarter/ tests/`, watermark 35),
  `pytest tests/ --cov=firestarter --cov-fail-under=70`. `tools/` is inside NO lint gate;
  `tests/` is inside all of them — the new test file must be ruff-clean, format-clean and add
  ZERO mypy errors.
- `tools/check_no_exists_proxy.py` forbids the MODULE-LEVEL `X = not path.exists()` shape only;
  an existence check inside a function body is explicitly permitted. Its `_DEFAULT_TARGETS` is a
  literal enumeration of top-level `tests/*.py`; no gate asserts its length.
- `tests/test_skip_census.py` allow-lists skip reasons. The new test must NOT skip.
- Submodule pre-task SHA: `612aa69` on branch
  `gsd/v1.37-operator-safety-answered-reports-claim-hygiene`. Working tree carries two UNTRACKED
  `datasheets/*.pdf` files that must stay untracked — never `git add -A`.
</facts_established_during_planning>

<house_rules_for_this_plan>
- **No comments.** `/workspaces/CLAUDE.md` and `firestarter_app/CLAUDE.md` forbid writing ANY
  comment into `firestarter_app/` — that includes `tests/`. Every line the executor adds must be
  code or a docstring. No `#` lines, no end-of-line `#` commentary, no phase/plan/decision IDs,
  no `.planning/` path literals in text the executor authors. Rationale goes in the SUMMARY and
  the commit message. Pre-existing comments and docstrings elsewhere in these files are out of
  scope — do not sweep them.
- Docstrings ARE the explanation vehicle here, and argparse help text is user-facing text, not a
  comment.
</house_rules_for_this_plan>

<interfaces>
Shape agreed with the operator. The observable contract is what the test pins; the internal
structure below is a recommendation the executor may vary only if every observable stays true.

- module level, next to the existing defaults: `PLANNING_DIR` — a plain `os.path.join` of
  `_REPO_ROOT` and the planning directory name. A join only; NO existence check at module level.
- `resolve_default_paths(all_algorithms)` → `(output, ledger)`. Returns the `*_ALL` pair when
  `all_algorithms` is true, the plain pair otherwise. When `PLANNING_DIR` is not an existing
  directory it writes one actionable line to stderr and raises `SystemExit(2)`. It must never
  create the directory.
- stderr text must contain the literal flag names `--output` and `--ledger`, and the resolved
  `PLANNING_DIR` value interpolated at runtime (never a hardcoded path literal).
- exit code `2` — deliberately distinct from `1`, which already means "DB parse error or
  `--check` drift".
- `main()` calls the resolver ONLY when at least one of `args.output` / `args.ledger` is `None`,
  then fills in each missing one. Both the normal and the `--all-algorithms` branch flow through
  that one call site.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the guard test and OBSERVE it red against the unfixed tool</name>
  <files>firestarter_app/tests/test_audit_coverage_matrix_default_paths.py, firestarter_app/tools/check_no_exists_proxy.py, .planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/red-before-fix.txt</files>
  <precondition>`git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` is `gsd/v1.37-operator-safety-answered-reports-claim-hygiene` and `git -C /workspaces status --porcelain -- .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json` prints nothing.</precondition>
  <behavior>
    The new module drives the REAL tool as a subprocess, from a COPY of it planted in a scratch
    tree, so `__file__` — and therefore `_REPO_ROOT` — is controlled without monkeypatching
    anything. `shutil.copyfile` the tool to `<tmp>/<case>/firestarter_app/tools/` and run it with
    `sys.executable`, `cwd` = the app repo root, and `FIRESTARTER_DB_FILE` pointed at the real
    `firestarter/data/chip_database.json`. Pointing the DB env var at the real database is what
    makes the fire-legs load-bearing: without it the copy would die on a missing database and
    every leg would pass for the wrong reason.

    - test_guard_fails_closed_when_no_planning_dir — parametrized over no extra args,
      `--all-algorithms`, and `--check`, against a scratch root with NO planning directory.
      Asserts exit code 2; asserts stderr contains both `--output` and `--ledger`; asserts no
      planning directory was created under the scratch root; asserts the only file under the
      scratch root is still the copied tool.
    - test_guard_fails_closed_when_only_one_path_is_explicit — same scratch layout, run once with
      only `--output` and once with only `--ledger`. Each must exit 2, and neither the explicit
      file nor a planning directory may appear.
    - test_defaults_resolve_when_planning_dir_exists — scratch root WITH an empty planning
      directory created first. Asserts exit 0 and that both the matrix markdown and the ledger
      JSON were written under it by their existing default names, and that the matrix body
      contains the section-3 header, proving a real generate rather than an empty file.
    - test_explicit_paths_bypass_the_guard — scratch root with NO planning directory, both
      `--output` and `--ledger` passed to tmp paths. Asserts exit 0, both files written, and that
      no planning directory was created under the scratch root.

    Expected colour against the UNFIXED tool, and this is the point of running it now:
    the two fails-closed tests go RED (the unfixed tool exits 0 and writes a planning tree into
    the scratch root; the `--check` leg is red on the stderr assertion, since pre-fix it exits 1
    for drift with no mention of the flags). The last two tests are GREEN both before and after —
    they are the do-not-weaken legs, and their staying green across the fix is their whole job.
  </behavior>
  <action>
Create `firestarter_app/tests/test_audit_coverage_matrix_default_paths.py` implementing the four
tests above. Module docstring carries the project's MIT header block in the same shape as the
neighbouring test modules, then explains the copy-the-tool-to-a-scratch-tree mechanism and why
`FIRESTARTER_DB_FILE` must point at the real database. Use `from __future__ import annotations`,
full return annotations (`-> None` on tests, real types on helpers) so the mypy watermark does
not move, and stdlib only: `os`, `shutil`, `subprocess`, `sys`, `pathlib.Path`, `pytest`. Give
the subprocess helper a `timeout` argument with real headroom (300s) so a hang fails loudly.
Anchor the app root from `__file__` with `.resolve().parent.parent` — never from cwd.

No `pytest.mark.skip`/`skipif` anywhere in the module: the skip census allow-lists reasons, and a
test whose job is to prove a gate can fail must not be gateable.

Then add `"tests/test_audit_coverage_matrix_default_paths.py",` to the `_DEFAULT_TARGETS` literal
in `firestarter_app/tools/check_no_exists_proxy.py`, immediately after the existing
`"tests/test_audit_coverage_matrix.py",` entry (byte-sort places it there). That list is the
enumeration of top-level test modules the absence-proxy lint scans; a new top-level test module
belongs in it. Add nothing else to that file.

Create the evidence directory
`/workspaces/.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/`,
run the new module against the STILL-UNFIXED tool, and capture the full output to
`evidence/red-before-fix.txt`. Confirm by reading it that the two fails-closed tests failed for
the reason predicted above and that the two do-not-weaken tests passed. If the red legs fail for
some OTHER reason (import error, timeout, missing database), fix the test — a test that is red by
accident proves nothing.

Commit inside the submodule only, with explicit paths, message
`test(quick-260912-mo6): add failing guard test for repo-escaping default output paths`.
Do not `git add -A` (untracked datasheet PDFs are present) and do not touch the meta repo's
submodule gitlink.
  </action>
  <verify>
    <automated>mkdir -p /workspaces/.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence && cd /workspaces/firestarter_app && python -m pytest tests/test_audit_coverage_matrix_default_paths.py -o addopts="" > /workspaces/.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/red-before-fix.txt 2>&1; test $? -ne 0 && /usr/bin/grep -qE "FAILED tests/test_audit_coverage_matrix_default_paths.py::test_guard_fails_closed_when_no_planning_dir" /workspaces/.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/red-before-fix.txt && /usr/bin/grep -qE "FAILED tests/test_audit_coverage_matrix_default_paths.py::test_guard_fails_closed_when_only_one_path_is_explicit" /workspaces/.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/red-before-fix.txt</automated>
    <automated>test -z "$(/usr/bin/grep -vE '^[[:space:]]*$' /workspaces/.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/red-before-fix.txt | /usr/bin/grep -E 'test_defaults_resolve_when_planning_dir_exists|test_explicit_paths_bypass_the_guard' | /usr/bin/grep -E '^FAILED|^ERROR')"</automated>
  </verify>
  <done>The new test module exists, is committed in the submodule, and `evidence/red-before-fix.txt` records both fails-closed tests failing against the unfixed tool while both do-not-weaken tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the invocation-time guard and turn the suite green</name>
  <files>firestarter_app/tools/audit_coverage_matrix.py</files>
  <action>
Implement the `<interfaces>` block in `tools/audit_coverage_matrix.py`.

1. Delete the four-line comment block above `_REPO_ROOT` (the one beginning "Pitfall 6 defense")
   without replacing it — it describes arithmetic that is no longer the whole story, and the
   no-comments rule forbids writing a successor. Leave `_REPO_ROOT` and the four `DEFAULT_*`
   constants exactly as they are; nothing else reads them, and keeping them import-time keeps
   `tests/test_audit_coverage_matrix.py`'s imports working.
2. Add `PLANNING_DIR` and `resolve_default_paths(all_algorithms)` per `<interfaces>`, placed
   directly after the `DEFAULT_*` constants. Give the function a docstring stating what it
   returns and that it refuses to invent a planning directory outside the repository. The
   existence test must live INSIDE the function body, never at module level.
3. Rewrite the default-selection block in `main()` (currently `output = args.output or
   DEFAULT_OUTPUT` and its `--all-algorithms` twin) so the resolver is consulted once, only when
   `args.output is None or args.ledger is None`, and each missing value is filled from the
   returned pair. Use `is None` tests, not truthiness — an explicitly-passed empty string is the
   user's business, not the guard's. Keep the two `generate_matrix` / `generate_matrix_all` call
   sites and the final `sys.exit(rc)` untouched.
4. Update two pieces of user-facing text in the module docstring, and nothing else in it: the
   sentence explaining how the default output path is computed (restate it as: defaults land in
   the planning directory at the repository root, and the tool exits rather than creating that
   directory when it is absent — no citations, and no hardcoded planning-path literal), and the
   "Exit codes" list, which must gain the new `2` case. Leave every other docstring line alone.

Keep lines at or under 88 columns to match house style even though `tools/` is outside ruff.
Add no comments of any kind.

Re-run the new test module and confirm all four tests pass, then run the wider gate set in the
verify block. Commit inside the submodule with explicit paths, message
`fix(quick-260912-mo6): fail closed when default output paths escape the repo`, with a body
explaining the three-hop arithmetic, why the escape is still permitted when a planning directory
already exists, and that explicit paths always win. Do not touch the meta gitlink.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python -m pytest tests/test_audit_coverage_matrix_default_paths.py tests/test_audit_coverage_matrix.py tests/test_numeric_schema_source_scan.py tests/test_check_no_exists_proxy.py tests/test_scan_paths_resolve.py -o addopts=""</automated>
    <automated>cd /workspaces/firestarter_app && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python tools/check_mypy_watermark.py</automated>
    <automated>cd /workspaces/firestarter_app && python -c "import importlib.util,sys; spec=importlib.util.spec_from_file_location('act','tools/audit_coverage_matrix.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('import-time clean', m.PLANNING_DIR)"</automated>
    <automated>test -z "$(git -C /workspaces/firestarter_app diff 612aa69..HEAD -- tools/ tests/ | /usr/bin/grep -E '^\+[[:space:]]*#')"</automated>
  </verify>
  <done>All four new tests pass, the pre-existing tool tests and the lint/type gates pass, importing the module still has no side effect and does not exit, and the committed diff added no comment lines.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the operator's invocation is unchanged and leave no state behind</name>
  <files>.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/evidence/operator-invocation.txt</files>
  <action>
This tool MUTATES tracked meta-repo state when run with defaults: it rewrites
`v1.3-defect-coverage-ids.json` and regenerates the 184 KB coverage matrix, both tracked in
`/workspaces`. A previous phase was caught out by exactly this. So: run it, then prove the tree
is clean, and restore if it is not.

Run three things from `/workspaces/firestarter_app`, capturing all output to
`evidence/operator-invocation.txt`:
  1. the dry-run drift gate with defaults — must exit 0, proving the guard stays silent on the
     operator's layout and the drift mode is unweakened, while writing nothing;
  2. a real default-path regeneration — must exit 0;
  3. `git -C /workspaces status --porcelain` scoped to the two tracked artifacts — must print
     nothing, proving the regenerated bytes and the resolved default paths are identical to what
     is committed.

If step 3 prints anything, capture `git -C /workspaces diff` for those paths into the evidence
file, restore both with `git -C /workspaces checkout --` on the two explicit paths, and record it
in the SUMMARY as a PRE-EXISTING drift finding — this change cannot alter output bytes, so a
dirty tree means the committed artifacts were already stale. Never leave the meta tree dirty.

Then confirm the submodule state: exactly two new commits since `612aa69`, HEAD still on
`gsd/v1.37-operator-safety-answered-reports-claim-hygiene`, and `git status --porcelain` in the
submodule showing nothing but the two untracked `datasheets/*.pdf` entries.

Commit the evidence file in the META repo only (it lives under `.planning/`), message
`docs(quick-260912-mo6): record operator-invocation no-mutation evidence`. Do not stage the
submodule gitlink in that commit — advancing the gitlink is the quick workflow's own step, not
part of this evidence commit.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python tools/audit_coverage_matrix.py --check && python tools/audit_coverage_matrix.py && test -z "$(git -C /workspaces status --porcelain -- .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json)"</automated>
    <automated>test "$(git -C /workspaces/firestarter_app rev-list --count 612aa69..HEAD)" = "2" && test "$(git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD)" = "gsd/v1.37-operator-safety-answered-reports-claim-hygiene" && test -z "$(git -C /workspaces/firestarter_app status --porcelain | /usr/bin/grep -v '^?? datasheets/')"</automated>
  </verify>
  <done>The operator's own invocation ran, produced an empty `git status` for both tracked artifacts, and the evidence file records it. The submodule carries exactly the two intended commits on the right branch with a clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool → host filesystem outside the repo | `_REPO_ROOT` is derived from `__file__`, not from a repository marker, so the tool's default write target is chosen by checkout layout rather than by anything it can verify |
| CLI args → filesystem | `--output` / `--ledger` are unconstrained paths supplied by the invoker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-mo6-01 | Tampering | `_REPO_ROOT` / `DEFAULT_OUTPUT` default resolution | medium | mitigate | `resolve_default_paths` refuses to use a default whose planning directory does not already exist, exits 2, and creates nothing — Task 2, proven by the fails-closed tests in Task 1 |
| T-mo6-02 | Tampering | already-present planning directory at `_REPO_ROOT` | low | accept | When the directory exists the tool still writes there — that is the operator's intended, tracked workflow, and Task 3 proves the bytes are unchanged. Narrowing further would break the only supported invocation |
| T-mo6-03 | Tampering | explicit `--output` / `--ledger` paths | low | accept | An explicitly-supplied path is the user's stated intent and is deliberately never blocked; the guard is only about paths the tool picks for itself |
| T-mo6-04 | Denial of Service | `--check` drift gate exit codes | low | mitigate | New failure mode uses exit `2`, distinct from `1`, so no caller that keys on the drift code can misread a guard refusal as drift — pinned by the parametrized `--check` leg in Task 1 |
</threat_model>

<verification>
- Fails-closed legs observed RED against the unfixed tool before the fix existed
  (`evidence/red-before-fix.txt`), GREEN after.
- Do-not-weaken legs GREEN both before and after.
- `pytest` over the tool's own pre-existing test module, the whole-file source scans and the
  scan-path inventory all pass.
- `ruff check` + `ruff format --check` + the mypy watermark gate all pass with the new
  `tests/` module in scope.
- Importing the tool module has no side effect and does not exit — the guard is invocation-time.
- The operator's default-path invocation leaves `.planning/v1.3-COVERAGE-MATRIX.md` and
  `.planning/v1.3-defect-coverage-ids.json` byte-identical to HEAD.
- The submodule diff added zero comment lines.
</verification>

<success_criteria>
- A standalone-layout invocation exits 2, names `--output` and `--ledger`, and creates nothing.
- The operator's invocation is unchanged, proven by an empty `git status` on both tracked files.
- Explicit paths, `--check` and `--all-algorithms` all keep working.
- Exactly two commits in `firestarter_app` since `612aa69`, on the milestone branch, and the meta
  tree carries only the evidence files.
</success_criteria>

<output>
Create `.planning/quick/260912-mo6-fail-closed-on-repo-escaping-default-out/260912-mo6-SUMMARY.md`
when done. Record there: why the three-hop arithmetic existed, why the escape is still permitted
when a planning directory is already present, the observed red-then-green transition, and any
pre-existing drift found in the two tracked artifacts.
</output>
