## Deferred Items

- `firestarter_app/firestarter/cli_handlers.py:1102` (inside `_erase_sector_blank_refusal_exit_code`'s docstring) cites `` `flash_nor_unlock.cpp:41` `` as "commented out" -- plan 205-03's task 2 deleted that commented-out dead assignment outright (205-03-SUMMARY.md, `flash_nor_unlock.cpp` entry), so the citation now names a line that no longer exists in that form. Not caused by this plan's (205-04) own edits -- `cli_handlers.py` is outside 205-04's declared file list and this docstring's staleness was introduced by a sibling plan's firmware-side deletion. `cli_handlers.py` was touched in 205-04 only for an unrelated, necessary edit (threading `blank_check_requested` through the `write()` handler's `write_eprom` call), so fixing this pre-existing, unrelated citation was out of scope for that edit.
  status: resolved
  **What:** Reworded to past tense -- "was commented-out dead code for its whole life, then deleted outright by `205-03`'s blank-check sweep" -- instead of describing the assignment as present-but-commented-out. Fixed in plan 205-06's added-scope citation-repair task.

- `firestarter_app/tests/test_uv_mask.py:201`'s docstring for `test_write_target_current_is_probe_read_defaults_to_false` still names `` `FLAG_SKIP_BLANK_CHECK` `` in prose ("keeps `FLAG_SKIP_BLANK_CHECK` off"), describing the now-retired wire bit rather than the `blank_check_requested` signal that replaced it (FWBLANK-04, Phase 205 Plan 04). The assertion itself (`target.current_is_probe_read is False`) is unaffected and still passes. Not fixed here: `test_uv_mask.py` is outside plan 205-04's declared `<files>` list for both its tasks, and the mention is prose-only -- no absence gate scans this file for the retired identifier.
  status: resolved
  **What:** Reworded to name `blank_check_requested` directly instead of the retired constant. Fixed in plan 205-06's added-scope citation-repair task.

- `firestarter_app/tests/fixtures/report_shapes.py:653` (inside `_build_uv_slot_write_pass`'s docstring) still names `` `FLAG_SKIP_BLANK_CHECK` `` in prose ("the real positional `FLAG_SKIP_BLANK_CHECK`"), describing the pre-205-04 mechanism `chip_test.py`'s masked UV slot write used to compose. Verified the underlying `dedup_fingerprint` for `uv-slot-write-pass` is UNCHANGED after the FWBLANK-04 re-plumb (`927571e5110f`, both before and after) -- the keyword-based signal preserves the exact same step-level outcome the wire bit produced, so no behaviour moved and no re-key was needed (see 205-04-SUMMARY.md's Task 3 finding). Not fixed here: this fixture module is outside both of plan 205-04's tasks' declared `<files>` lists (task 3's own acceptance criterion requires its commit to touch `tests/test_blast_radius_invariance.py` alone), and the mention is prose-only -- no absence gate scans this file for the retired identifier.
  status: resolved
  **What:** Reworded to describe the explicit `blank_check_requested=False` keyword instead of the retired wire bit. Fixed in plan 205-06's added-scope citation-repair task.
