---
phase: 02-naming-cleanup-wire-key-minipro-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/database.py
  - firestarter_app/CLAUDE.md
  - firestarter/src/json_parser.c
  - firestarter/CLAUDE.md
autonomous: true
requirements:
  - WIRE-01

must_haves:
  truths:
    - "Python emitter `convert_to_programmer` writes `\"vpp_mv\"` (not `\"vpp\"`) into the wire dict carrying the integer millivolt value."
    - "Firmware `json_parser.c` parses incoming JSON key `\"vpp_mv\"` into `handle->vpp_mv` (all three flip sites land in a single commit)."
    - "`firestarter_app/CLAUDE.md` example wire JSON shows exactly one VPP key — `\"vpp_mv\": 12000,` — with no `\"vpp\":` line remaining."
    - "`firestarter/CLAUDE.md` JSON-wire-protocol field list documents `vpp_mv` only (no `vpp /` legacy reference)."
  artifacts:
    - path: "firestarter_app/firestarter/database.py"
      provides: "Wire emitter with the renamed key string at :518"
      contains: '"vpp_mv": vpp_mv'
    - path: "firestarter/src/json_parser.c"
      provides: "Firmware parser registered for the renamed wire key (literal + dispatch + macro arg)"
      contains: 'key_vpp_mv'
    - path: "firestarter_app/CLAUDE.md"
      provides: "Wire-JSON example consistent with the new emitter"
      contains: '"vpp_mv": 12000'
    - path: "firestarter/CLAUDE.md"
      provides: "Field-list documentation collapsed to `vpp_mv` only"
  key_links:
    - from: "firestarter_app/firestarter/database.py:518"
      to: "firestarter/src/json_parser.c:62, :74, :309"
      via: "wire JSON over 250000 baud serial — key `\"vpp_mv\"` is the contract"
      pattern: '"vpp_mv"'
    - from: "firestarter/src/json_parser.c:503"
      to: "firestarter_handle_t.vpp_mv (include/firestarter.h:85, unchanged)"
      via: "extract_int macro writes the parsed int into the struct field"
      pattern: 'extract_int\("vpp_mv", handle->vpp_mv\)'
---

<objective>
WIRE-01 atomic wire-key flip across both sub-repos. Today the Python emitter at
`firestarter_app/firestarter/database.py:518` emits a single key `"vpp"` carrying
integer millivolts; the firmware parser at `firestarter/src/json_parser.c:62, :74,
:308-310` reads that same `"vpp"` token into `handle->vpp_mv`. After this plan:
emitter writes `"vpp_mv"`; firmware parser reads `"vpp_mv"`. Doc examples in both
sub-repo CLAUDE.md files updated to match. No backward-compat fallback on either
side (per CONTEXT.md D-01).

Purpose: Close MILESTONES.md WARNING-3 (semantic overload — the key name said
"volts" but the value was millivolts) and stop `firestarter_app/CLAUDE.md` from
documenting a phantom dual-key wire that never existed (per RESEARCH.md "Factual
Correction" — the live wire is single-key today, not dual).

Output: Two source-file edits (one Python, one C) plus two CLAUDE.md doc edits.
Three sites in `json_parser.c` MUST flip in a single firmware commit (per
RESEARCH.md "Pitfall #1" — a half-flipped state silently drops the field, leaves
`handle->vpp_mv = 0`, and trips SAF-04 on every Intel-flash write).
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-CONTEXT.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-VALIDATION.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
@firestarter/CLAUDE.md

<interfaces>
<!-- Key contracts the executor will modify. Extracted from PATTERNS.md + RESEARCH.md "Code Examples". -->

From firestarter_app/firestarter/database.py:501-540 (CURRENT state):
```python
def convert_to_programmer(self, full_eprom_data: dict) -> dict:
    if not full_eprom_data:
        return {}

    # :510 — local variable assignment (NOT a dict write — RESEARCH.md "Factual Correction")
    vpp_mv = full_eprom_data.get("vpp_mv") or int(full_eprom_data.get("vpp", 0) * 1000)

    programmer_data = {
        "memory-size": full_eprom_data.get("memory-size", 0),
        "type": full_eprom_data.get("type", 0),
        "algorithm": full_eprom_data.get("protocol-id", 0),
        "pin-count": full_eprom_data.get("pin-count", 0),
        "vpp": vpp_mv,                # :518 — THIS is the rename target. Single key today.
        "pulse-delay": full_eprom_data.get("pulse-delay", 0),
    }
```
Plan 02-01 only touches `:518` — the dict-write key string. Line `:510` (the local
variable `vpp_mv` and the float-volts fallback `get("vpp", 0) * 1000`) is the
INTERNAL `_map_data` consumer fallback and is renamed in Plan 02-02 (D-04
`vpp_volts` rename), NOT here.

From firestarter/src/json_parser.c (CURRENT state — three flip sites):
```c
// :62  — PROGMEM literal (variable name AND string content both flip)
const char key_vpp[] PROGMEM = "vpp";

// :71-75 — dispatch table; the row at :74 holds the var reference for the VPP key
static const key_parser_t key_parsers[] PROGMEM = {
    {key_mem_size, get_memory_size}, {key_address, get_address},     {key_flags, get_flags},
    {key_chip_id, get_chip_id},      {key_pin_count, get_pin_count}, {key_pulse_delay, get_delay},
    {key_vpp, get_vpp_mv},           {key_type, get_type},           {key_algorithm, get_algorithm},
};

// :308-310 — getter body; macro arg IS the parse key (re-checked via strncmp_P)
bool get_vpp_mv(const char* json, jsmntok_t* tokens, int pos, firestarter_handle_t* handle) {
    extract_int("vpp", handle->vpp_mv);
}
```
Sibling rows in the same table use the convention `key_<wire_key_underscored>[]
PROGMEM = "<wire-key>";` (e.g. `key_mem_size` ↔ `"memory-size"`, `key_pin_count`
↔ `"pin-count"`). Renamed PROGMEM var: `key_vpp_mv[] PROGMEM = "vpp_mv"`.

Sibling getter bodies (`:300-302 get_type`, `:304-306 get_delay`, `:312-314
get_algorithm`) all match the pattern `extract_<int|long>("<wire-key>",
handle-><field>);` where wire-key matches the field name. Post-flip:
`extract_int("vpp_mv", handle->vpp_mv);` — wire-key matches the C-struct field
name `vpp_mv` (RESEARCH.md D-08 verified).

From firestarter/include/firestarter.h:85 (UNCHANGED — out of scope per D-03):
```c
uint16_t vpp_mv;
```
The C-struct field name was already correctly named in v1.0; the rename is
parse-layer only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-01-01: Firmware atomic three-site flip in `json_parser.c`</name>
  <files>firestarter/src/json_parser.c, firestarter/CLAUDE.md</files>
  <read_first>
    - firestarter/src/json_parser.c (current state — lines 55-80 for the literal + table, lines 300-315 for the getter body)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md §"Firmware Parser Semantics — D-08 Validation" + §"Pitfall 1: Splitting the json_parser.c three-line flip across commits"
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md §7 (the three-site flip patterns 7a/7b/7c with sibling-row analogs verbatim)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-CONTEXT.md §D-01 and §D-08 (macro semantics)
    - firestarter/CLAUDE.md (current state — line 74 for field list `vpp / vpp_mv`, no other minipro/path edits in this plan)
  </read_first>
  <action>
**Working directory:** `firestarter/` (Arduino firmware sub-repo — independent git history).

Perform the three-site atomic flip per CONTEXT.md D-01, in a SINGLE git commit:

1. `firestarter/src/json_parser.c:62` — rename PROGMEM literal: `const char key_vpp[] PROGMEM = "vpp";` becomes `const char key_vpp_mv[] PROGMEM = "vpp_mv";`. Both the C identifier and the string content flip. Follow the sibling pattern at `:56` (`key_mem_size[] PROGMEM = "memory-size"`) — variable name matches the wire key, snake_case-underscored.

2. `firestarter/src/json_parser.c:164` — update the dispatch table row to reference the renamed variable: `{key_vpp, get_vpp_mv},` becomes `{key_vpp_mv, get_vpp_mv},`. Function pointer `get_vpp_mv` is unchanged.

3. `firestarter/src/json_parser.c:503` (inside `get_vpp_mv` body at `:308-310`) — flip the macro's first argument from `"vpp"` to `"vpp_mv"`: `extract_int("vpp", handle->vpp_mv);` becomes `extract_int("vpp_mv", handle->vpp_mv);`. The C-struct field `handle->vpp_mv` is unchanged (D-03; already correctly named in `firestarter/include/firestarter.h:85`).

Per RESEARCH.md "Pitfall #1", all three edits MUST land in one commit — a half-flip silently drops the parse and trips SAF-04 on every Intel-flash write. Verify with the grep gates below BEFORE staging.

Then, in the same firmware commit, update `firestarter/CLAUDE.md` line 74 — the JSON Wire Protocol field-list — collapsing `- `vpp` / `vpp_mv` — VPP voltage for ADC validation` to `- `vpp_mv` — VPP voltage in millivolts (used by SAF-04 ADC validation)`. This is the "Claude's Discretion" item from CONTEXT.md (implied by D-01 — firmware no longer reads `"vpp"`, so documenting it as a field misleads readers). Do NOT touch `firestarter/CLAUDE.md:30` (`minipro_complete_db.json` filename) or `:69` (`minipro protocol_id`) — those are Plan 02-02 and Plan 02-03 territory.

Build verification (per RESEARCH.md "Environment Availability"): compile both Arduino targets via PlatformIO. If `pio` is unavailable in the sandbox, document the unavailability in the commit message and rely on the host-side static greps from the verification block. Phase 4 / HW-* owns physical-hardware confirmation; this plan only owns the source-state assertion.

Commit message body in `firestarter/`: "WIRE-01: rename JSON wire key vpp -> vpp_mv (atomic three-site flip)" with body noting the three sites (`:62`, `:74`, `:309`) and the CLAUDE.md field-list collapse.
  </action>
  <verify>
    <automated>cd firestarter && grep -nv '^[[:space:]]*//' src/json_parser.c | grep -c 'key_vpp_mv' | { read n; test "$n" -ge 2 || { echo "FAIL: expected >=2 occurrences of key_vpp_mv (declaration + table row), got $n"; exit 1; }; } &amp;&amp; grep -nv '^[[:space:]]*//' src/json_parser.c | grep -c '"vpp_mv"' | { read n; test "$n" -ge 2 || { echo "FAIL: expected >=2 occurrences of \"vpp_mv\" literal (PROGMEM string + macro arg), got $n"; exit 1; }; } &amp;&amp; grep -nv '^[[:space:]]*//' src/json_parser.c | grep -c '"vpp"' | { read n; test "$n" -eq 0 || { echo "FAIL: legacy \"vpp\" literal still present in json_parser.c ($n occurrence(s))"; exit 1; }; } &amp;&amp; grep -nv '^[[:space:]]*//' src/json_parser.c | grep -c '\bkey_vpp\b' | { read n; test "$n" -eq 0 || { echo "FAIL: legacy key_vpp identifier still present in json_parser.c ($n occurrence(s))"; exit 1; }; } &amp;&amp; ! grep -nF '`vpp` /' CLAUDE.md &amp;&amp; ! grep -nF 'vpp / vpp_mv' CLAUDE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nv '^[[:space:]]*//' firestarter/src/json_parser.c | grep -c key_vpp_mv` >= 2 (one declaration at `:62` + at least one table-row reference at `:74`).
    - `grep -nv '^[[:space:]]*//' firestarter/src/json_parser.c | grep -c '"vpp_mv"'` >= 2 (PROGMEM string literal at `:62` + macro arg at `:309`).
    - `grep -nv '^[[:space:]]*//' firestarter/src/json_parser.c | grep -c '"vpp"'` == 0.
    - `grep -nv '^[[:space:]]*//' firestarter/src/json_parser.c | grep -c '\bkey_vpp\b'` == 0 (no naked legacy identifier).
    - `firestarter/CLAUDE.md` no longer contains the substring ``vpp` / `` or `vpp / vpp_mv` in the JSON-wire-protocol field-list section; it DOES contain `vpp_mv` as the documented VPP field.
    - All three source-edits AND the CLAUDE.md field-list collapse land in one git commit inside the `firestarter/` sub-repo.
    - If `pio run -e uno` and `pio run -e leonardo` are available in-environment, both exit 0; if unavailable, the commit message explicitly records the skip and links to Phase 4 / HW-* for hardware confirmation.
  </acceptance_criteria>
  <done>
    - All four grep gates above return their expected values.
    - One firmware-sub-repo commit lands containing exactly the three `json_parser.c` edits + the `firestarter/CLAUDE.md` field-list collapse, with no other changes mixed in.
    - The C-struct field `firestarter_handle_t.vpp_mv` at `firestarter/include/firestarter.h:85` is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 02-01-02: Python emitter wire-key rename at `database.py:518` + `firestarter_app/CLAUDE.md` wire-example fix</name>
  <files>firestarter_app/firestarter/database.py, firestarter_app/CLAUDE.md</files>
  <read_first>
    - firestarter_app/firestarter/database.py (current state — lines 501-540, with focus on `:518` where the dict-write key lives; lines 505-512 to confirm `:510` is the LOCAL VARIABLE assignment, not a dict write — Plan 02-02 owns `:510`)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md §"Factual Correction to CONTEXT.md" (CRITICAL — CONTEXT.md D-02 says "delete `\"vpp\": vpp_mv,`" but RESEARCH.md proves the live code emits ONLY `"vpp": vpp_mv` today, so the correct edit is a RENAME, not a delete)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md §1a (verbatim before/after for `:518`)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-CONTEXT.md §D-02 and §"Specific Ideas" (Wire JSON before/after diff)
    - firestarter_app/CLAUDE.md (current state — lines 46-58, the wire-JSON example block with both `"vpp"` and `"vpp_mv"` lines today)
  </read_first>
  <action>
**Working directory:** `firestarter_app/` (Python host sub-repo — independent git history).

This task lands in a SEPARATE commit from Task 02-01-01 (the two sub-repos have
independent git histories per RESEARCH.md §"Cross-Sub-Repo Coordination Pattern").
Commit order recommended by RESEARCH.md: firmware first (Task 02-01-01), then
this Python-side edit — but either order leaves the partial-upgrade state
covered by SAF-04 (Phase 1 already shipped) per RESEARCH.md "Pitfall #3".

Edits:

1. `firestarter_app/firestarter/database.py:518` — rename the dict-write key string ONLY. The line today reads `"vpp": vpp_mv,` (RESEARCH.md "Factual Correction" — there is no second `"vpp_mv": ...,` line; CONTEXT.md D-02's "delete" framing is inaccurate). After edit: `"vpp_mv": vpp_mv,`. The local variable `vpp_mv` (assigned at `:510`) is unchanged; only the dict key string flips. Note: line `:510` is `vpp_mv = full_eprom_data.get("vpp_mv") or int(full_eprom_data.get("vpp", 0) * 1000)` — this is a LOCAL ASSIGNMENT reading the internal `_map_data` output dict (which still has key `"vpp"` until Plan 02-02 lands D-04). Do NOT touch `:510` in this plan; that is Plan 02-02's job (D-04 rename to `"vpp_volts"`).

2. `firestarter_app/CLAUDE.md` lines 46-58 (the wire JSON example block) — delete the line `  "vpp": 12000,`. Keep the line `  "vpp_mv": 12000,` (already present). All other lines in the example block stay verbatim. This makes the documented wire match what the live emitter now produces (the example was the phantom — RESEARCH.md "Factual Correction").

Do NOT touch any other line in `database.py` (the consumer rename at `:417` / `:510` is Plan 02-02 / D-04 territory) and do NOT touch any other section of `firestarter_app/CLAUDE.md` (the filename/path/attribution edits at `:19, :36, :42, :69, :72` are Plan 02-02 / Plan 02-03 territory).

Commit inside `firestarter_app/`: "WIRE-01: emit \"vpp_mv\" wire key (rename from \"vpp\") + drop phantom dual-key line from CLAUDE.md example".
  </action>
  <verify>
    <automated>cd firestarter_app &amp;&amp; grep -nE '"vpp_mv":\s*vpp_mv' firestarter/database.py | grep -q ':518:' &amp;&amp; ! grep -nE '"vpp":\s*vpp_mv' firestarter/database.py &amp;&amp; ! grep -nF '"vpp": 12000' CLAUDE.md &amp;&amp; grep -qF '"vpp_mv": 12000' CLAUDE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/firestarter/database.py` line `:518` contains the literal substring `"vpp_mv": vpp_mv`.
    - `firestarter_app/firestarter/database.py` contains zero occurrences of `"vpp": vpp_mv` (regression-proof against accidental restoration).
    - `firestarter_app/firestarter/database.py` line `:510` is UNCHANGED — still reads the local variable assignment with `or int(full_eprom_data.get("vpp", 0) * 1000)` (Plan 02-02 owns this line).
    - `firestarter_app/CLAUDE.md` contains exactly one VPP-related JSON line in the wire-example block: `  "vpp_mv": 12000,`. The literal `  "vpp": 12000,` line is absent.
    - One commit inside `firestarter_app/` containing exactly the two edits above (one Python source line + one doc line), with no other changes mixed in.
  </acceptance_criteria>
  <done>
    - All four grep gates above return their expected results.
    - `firestarter_app/CLAUDE.md` wire example renders as documented in CONTEXT.md §"Specific Ideas" "After Phase 2" block (`"vpp_mv": 12000,` present, `"vpp": 12000,` absent).
    - The Python wire emitter signature, function name, and surrounding lines are unchanged; only the key-string at `:518` flipped.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the cross-sub-repo wire-key contract is consistent:

- `cd firestarter_app && grep -nE '"vpp_mv":\s*vpp_mv' firestarter/database.py` returns line `:518`.
- `cd firestarter && grep -nv '^[[:space:]]*//' src/json_parser.c | grep -c '"vpp_mv"'` returns 2 (PROGMEM literal + macro arg).
- `cd firestarter && grep -nv '^[[:space:]]*//' src/json_parser.c | grep -c '"vpp"'` returns 0.
- `! grep -F '"vpp": 12000' firestarter_app/CLAUDE.md` succeeds (line removed).
- `! grep -F 'vpp / vpp_mv' firestarter/CLAUDE.md` succeeds (field list collapsed).
- Partial-upgrade safe-fail is structurally intact: per RESEARCH.md "Pitfall #3", SAF-04 (shipped in Phase 1 per STATE.md) catches both `firmware-new + app-old` and `firmware-old + app-new` mismatches by zero-init of `handle->vpp_mv` → VPP HIGH guard fires. No new safety net required from this plan.

Plan 02-02 (file rename + `vpp_volts` internal rename) and Plan 02-03 (attribution
scrub + check_dispatch augmentation + CLI smoke) consume the renamed wire as
their post-state contract.
</verification>

<success_criteria>
- WIRE-01 partially closed at the source level: Python emits `"vpp_mv"`, firmware parses `"vpp_mv"`, doc examples match.
- Roadmap SC#1 source-state assertion holds for Phase 2: `firestarter_app/firestarter/database.py::convert_to_programmer` emits wire key `"vpp_mv"`; firmware JSON parser reads `"vpp_mv"` into `handle->vpp_mv`; `firestarter_app/CLAUDE.md` example wire JSON shows only `"vpp_mv"`.
- All eight grep gates across the two tasks pass.
- No new source files created; no test scaffolding added; no other lines of the four touched files modified beyond what each task enumerates.
- Roadmap SC#4 evidence (the host-side regression scan including the renamed wire) is OWNED BY Plan 02-03 — Plan 02-01 only asserts the source-state contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-01-SUMMARY.md` per `@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md`. Record:
- Both sub-repo commit SHAs.
- Confirmation that the three firmware sites landed in one commit (cite the firmware sub-repo SHA).
- Any environment-availability notes from `pio run` (if it ran, record the build sizes; if it didn't, note the skip).
- A pointer forward to Plan 02-03 / WIRE-02 for the dynamic regression-scan evidence (`check_dispatch.py` after augmentation).
</output>