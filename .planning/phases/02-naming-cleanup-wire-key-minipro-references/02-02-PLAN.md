---
phase: 02-naming-cleanup-wire-key-minipro-references
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - firestarter_app/firestarter/data/minipro_complete_db.json
  - firestarter_app/firestarter/data/chip_database.json
  - firestarter_app/firestarter/database.py
  - firestarter_app/firestarter/eprom_info.py
  - firestarter_app/firestarter/ic_layout.py
  - firestarter_app/tools/build_db.py
  - firestarter_app/tools/check_dispatch.py
  - firestarter_app/pyproject.toml
  - firestarter_app/MANIFEST.in
  - firestarter_app/CLAUDE.md
  - firestarter/CLAUDE.md
  - CLAUDE.md
autonomous: true
requirements:
  - CLEAN-01
  - WIRE-01  # D-04 "internal twin" — _map_data() output dict key vpp → vpp_volts (CONTEXT.md Domain item 2)

must_haves:
  truths:
    - "The chip-database file ships as `firestarter_app/firestarter/data/chip_database.json` with git blame preserved across the rename (`git log --follow` chains through the prior `minipro_complete_db.json` history)."
    - "Every reader / writer / docstring / config that referenced the old filename now references `chip_database.json`; zero residual `minipro_complete_db` substrings remain in source, tools, packaging, or any CLAUDE.md."
    - "`_map_data()` writes the float-volts key as `\"vpp_volts\"` (not `\"vpp\"`); both downstream consumers (`eprom_info.py:271`, `ic_layout.py:513`) and the emitter fallback (`database.py:510`) read `\"vpp_volts\"`. The upstream-schema READ path at `database.py:373-381` (which reads `electrical.vpp` from the on-disk DB string `\"12V\"`) is UNCHANGED — different layer, different concept."
    - "`firestarter_app/pyproject.toml [tool.setuptools.package-data]` and `firestarter_app/MANIFEST.in` declare the actual shipping data files (`chip_database.json`, `database_overrides.json`, `pinouts.json`, `avrdude.conf`) so a built wheel ships the renamed DB. `pip install -e .` and `firestarter info W27C512` succeed without `FileNotFoundError`."
  artifacts:
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "Renamed chip database (743 entries; content byte-identical to prior `minipro_complete_db.json` per D-16)."
    - path: "firestarter_app/firestarter/database.py"
      provides: "Default DB path string at :189 + docstring at :366 + _map_data dict-write key at :417 + emitter fallback at :510 all updated"
    - path: "firestarter_app/tools/build_db.py"
      provides: "`OUTPUT_FILE` constant at :12 references the new filename"
    - path: "firestarter_app/tools/check_dispatch.py"
      provides: "Docstring at :2 + `_DATA_DIR` glob default at :27 reference the new filename (Plan 02-03 augments this file further)"
    - path: "firestarter_app/pyproject.toml"
      provides: "`[tool.setuptools.package-data]` matches actual shipping files including `chip_database.json`"
    - path: "firestarter_app/MANIFEST.in"
      provides: "sdist file list matches actual shipping files including `chip_database.json`"
  key_links:
    - from: "firestarter_app/firestarter/database.py:189 (_initialize_database_core)"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "_read_config_file('chip_database.json')"
      pattern: '_read_config_file\("chip_database\.json"\)'
    - from: "firestarter_app/tools/build_db.py:12 (OUTPUT_FILE)"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "os.path.join(_DATA_DIR, 'chip_database.json')"
      pattern: 'OUTPUT_FILE\s*=\s*os\.path\.join\(.*"chip_database\.json"\)'
    - from: "firestarter_app/firestarter/database.py:417 (_map_data dict-write)"
      to: "firestarter_app/firestarter/eprom_info.py:271 + firestarter_app/firestarter/ic_layout.py:513 + firestarter_app/firestarter/database.py:510"
      via: "in-memory dict key `vpp_volts`"
      pattern: '"vpp_volts"'
---

<objective>
CLEAN-01 file rename (`minipro_complete_db.json` → `chip_database.json`) atomically
swept across all reader/writer/doc callsites; D-04 internal `_map_data()` float-volts
key renamed to `"vpp_volts"` with its two downstream consumers (`eprom_info.py:271`,
`ic_layout.py:513`); and the pre-existing v1.0 packaging drift in `pyproject.toml`
and `MANIFEST.in` aligned to the actual shipping data files.

Purpose: Stop the upstream-toolname leak in the data filename (the file is OUR
DB, not the minipro tool's artifact), remove the second WARNING-3 semantic
overload internally (`"vpp"` carried float volts next to `"vpp_mv"` int mV), and
close the v1.0 Phase 11 packaging-metadata drift so a built wheel ships the
renamed DB. All three live together because they ALL touch
`firestarter_app/` package-data — coupling them avoids two wheel-shipping
regressions across two phases.

Output: One `git mv` operation, eight in-place callsite touches across seven
source files, two stale-config rewrites (`pyproject.toml` + `MANIFEST.in`), and
five doc-line edits across three CLAUDE.md files. All staged in one
`firestarter_app/` sub-repo commit so the file-path is valid in every commit
(no broken-state intermediate per RESEARCH.md Pattern 2 / Phase 11 precedent).
The single meta-repo `CLAUDE.md:44` line edit lands in this same wave; the
meta-repo commit is a tiny doc-only change to the parent project instructions.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-CONTEXT.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-VALIDATION.md
@.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-01-PLAN.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
@firestarter/CLAUDE.md

<interfaces>
<!-- D-04 boundary contracts. RESEARCH.md "Pitfall #2" — three different `vpp` concepts in this codebase. -->

UPSTREAM-SCHEMA READ (NOT RENAMED — `database.py:373-381`):
```python
# Reads the on-disk DB string "12V" (emitted by build_db.py:255) OR a legacy
# user-override at ~/.firestarter/database.json. UPSTREAM-OWNED. Do not touch.
vpp_str = electrical.get("vpp", "0").replace("V", "")
```

INTERNAL DICT-WRITE (RENAMED in this plan — `database.py:411-426`):
```python
data = {
    "name": ic.get("part_number"),
    ...
    "vpp": vpp,         # :417 — float volts (4.5, 12.0, 21.0)  — RENAME KEY to "vpp_volts"
    "vpp_mv": vpp_mv,   # int millivolts (4500, 12000, 21000)   — UNCHANGED
    ...
}
```

INTERNAL DICT-READ (RENAMED in this plan — `database.py:510`):
```python
# After Plan 02-01 flipped :518's wire-write key, this line still reads the
# legacy internal dict key "vpp". The :417 rename above MUST be paired with
# this consumer fallback rename — both in the same commit.
vpp_mv = full_eprom_data.get("vpp_mv") or int(full_eprom_data.get("vpp_volts", 0) * 1000)
```

CONSUMER #1 (RENAMED — `eprom_info.py:271`):
```python
# Reads _map_data() output for the `firestarter info` listing.
vpp_str = f"{ic.get('vpp_volts', '-')}v" if ic.get("type") == 1 else "- "
```

CONSUMER #2 (RENAMED — `ic_layout.py:513`):
```python
# Reads _map_data() output for the DIP-layout renderer.
output_data["vpp_str"] = f"{eprom_data.get('vpp_volts', 'N/A')}v"
```

PRE-EXISTING STALE STATE (FIXED in this plan):
```toml
# firestarter_app/pyproject.toml:64-69 — currently lists three filenames that
# DON'T EXIST in firestarter/data/. RESEARCH.md "Critical: pyproject.toml
# package-data is stale". After fix, every entry maps to a real file.
[tool.setuptools.package-data]
"firestarter" = [
    "data/chip_database.json",
    "data/database_overrides.json",
    "data/pinouts.json",
]
```
```
# firestarter_app/MANIFEST.in — currently includes `database.json` and
# `pin-maps.json`, neither of which exists. After fix:
include firestarter/data/chip_database.json
include firestarter/data/pinouts.json
include firestarter/data/avrdude.conf
include firestarter/data/database_overrides.json
include firestarter/database.py
include firestarter/ic_layout.py
include firestarter/avr_tool.py
include firestarter/main.py
include firestarter/__init__.py
include README.md
include LICENSE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-02-01: `git mv` data file + flip all 7 path callsites in one `firestarter_app/` commit</name>
  <files>firestarter_app/firestarter/data/minipro_complete_db.json, firestarter_app/firestarter/data/chip_database.json, firestarter_app/firestarter/database.py, firestarter_app/tools/build_db.py, firestarter_app/tools/check_dispatch.py, firestarter_app/CLAUDE.md, firestarter/CLAUDE.md, CLAUDE.md</files>
  <read_first>
    - firestarter_app/firestarter/database.py (current state — line 189 default path; line 366 docstring referencing old filename)
    - firestarter_app/tools/build_db.py (current state — lines 10-13 with `MINIPRO_XML_URL` survivor at :10 and `OUTPUT_FILE` at :12)
    - firestarter_app/tools/check_dispatch.py (current state — line 2 docstring; line 27 `_DATA_DIR` glob default)
    - firestarter_app/CLAUDE.md (current state — line 19 data-flow diagram filename + line 36 Key Files bullet + line 69 Database Pipeline section)
    - firestarter/CLAUDE.md (current state — line 30 only; the `:69` minipro line is Plan 02-03's territory)
    - CLAUDE.md (meta — line 44, the single inline path reference)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md §6 (Phase 11 `git mv parse_db_2.py build_db.py` precedent for the atomic rename pattern)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-CONTEXT.md §D-05, §D-06, §D-07 (the 7-callsite list verbatim)
  </read_first>
  <action>
**Working directory:** `firestarter_app/` for all sub-repo edits; meta-repo root for the meta `CLAUDE.md:44` edit.

Inside `firestarter_app/` (one git commit for all of these — staging order avoids a broken-path intermediate, mirroring Phase 11's `git mv parse_db_2.py build_db.py` precedent):

1. `git -C firestarter_app mv firestarter/data/minipro_complete_db.json firestarter/data/chip_database.json` — preserves blame across the rename per D-06. Content stays byte-identical (per D-16; no `build_db.py` regeneration). Do this BEFORE editing the callsites so the rename and the path-flips stage in one commit; running readers between the rename and the edits would fail.

2. `firestarter_app/firestarter/database.py:189` — change the `_read_config_file(...)` argument from `"minipro_complete_db.json"` to `"chip_database.json"`. The default-path resolution at `:175-210` is unchanged in shape; only the filename literal flips.

3. `firestarter_app/firestarter/database.py:366` — update the `_map_data` docstring substring `'minipro_complete_db.json'` to `'chip_database.json'`. Surrounding prose unchanged.

4. `firestarter_app/tools/build_db.py:12` — flip `OUTPUT_FILE = os.path.join(_DATA_DIR, "minipro_complete_db.json")` to `OUTPUT_FILE = os.path.join(_DATA_DIR, "chip_database.json")`. The constant name `OUTPUT_FILE` stays. The `MINIPRO_XML_URL` constant at `:10` is **NOT touched in this task** (it's the surviving load-bearing attribution per D-09; comment scrub of `:23` is Plan 02-03's job).

5. `firestarter_app/tools/check_dispatch.py:2` — update the module docstring substring `minipro_complete_db.json` to `chip_database.json`. (The full D-10 wording rewrite at `:30` and the D-15 augmentation are Plan 02-03; this task only flips the filename string in the docstring.)

6. `firestarter_app/tools/check_dispatch.py:27` — find the `_DATA_DIR` candidate-list / glob-default that references `"minipro_complete_db.json"`; replace with `"chip_database.json"`. Surrounding logic and other candidates (e.g. `pinouts.json`) unchanged.

7. `firestarter_app/CLAUDE.md` — three single-line edits:
   - `:19` (the data-flow ASCII diagram inside the fenced code block) — replace the filename token `minipro_complete_db.json` with `chip_database.json`. The arrows and surrounding lines are unchanged.
   - `:36` (Key Files bullet) — replace `firestarter/data/minipro_complete_db.json` with `firestarter/data/chip_database.json`. Rest of the bullet (`— generated chip database (do NOT edit by hand)`) unchanged.
   - `:69` (Database Pipeline section) — replace the filename token `minipro_complete_db.json` with `chip_database.json`. **Do NOT touch the surrounding minipro attribution wording in this task** — Plan 02-03 owns the `minipro` → neutral / single-attribution scrub. This task ONLY flips the filename.

8. `firestarter/CLAUDE.md:30` — replace `regenerated minipro_complete_db.json` with `regenerated chip_database.json`. **Do not touch the `minipro` substring at `:69` here** — Plan 02-03 owns the firmware-side `minipro` scrub.

9. Meta `CLAUDE.md:44` (working directory: meta-repo root) — replace the inline path `firestarter_app/firestarter/data/minipro_complete_db.json` with `firestarter_app/firestarter/data/chip_database.json`. No other meta-repo line changes; lands as a separate meta-repo commit per the project's submodule-pointer convention.

Verify before staging that **no file in the `firestarter_app/` tree still references `minipro_complete_db`** via grep gate (see verify block). Stage everything (the renamed JSON + the seven Python/doc edits + the firmware sub-repo `CLAUDE.md:30` edit + the meta `CLAUDE.md:44` edit) and commit each sub-repo independently:

- `firestarter_app/` commit message: `CLEAN-01: rename minipro_complete_db.json -> chip_database.json (git mv preserves blame; 7 callsites flipped atomically)`
- `firestarter/` commit message: `docs: update CLAUDE.md filename reference to chip_database.json`
- meta-repo: `docs(02): update EPROM database filename reference to chip_database.json`
  </action>
  <verify>
    <automated>! grep -rn 'minipro_complete_db' firestarter_app/ firestarter/ CLAUDE.md 2>/dev/null &amp;&amp; test -f firestarter_app/firestarter/data/chip_database.json &amp;&amp; ! test -e firestarter_app/firestarter/data/minipro_complete_db.json &amp;&amp; grep -nF '_read_config_file("chip_database.json")' firestarter_app/firestarter/database.py &amp;&amp; grep -nE 'OUTPUT_FILE\s*=\s*os\.path\.join.*"chip_database\.json"' firestarter_app/tools/build_db.py &amp;&amp; grep -qF 'chip_database.json' firestarter_app/CLAUDE.md &amp;&amp; grep -qF 'chip_database.json' firestarter/CLAUDE.md &amp;&amp; grep -qF 'chip_database.json' CLAUDE.md &amp;&amp; ( cd firestarter_app &amp;&amp; git log --follow --format='%H' -- firestarter/data/chip_database.json | wc -l | { read n; test "$n" -ge 2 || { echo "FAIL: git history chain not preserved across rename ($n commits via --follow; expected >=2)"; exit 1; }; } ) &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `find firestarter_app/ firestarter/ CLAUDE.md -type f \( -name '*.py' -o -name '*.toml' -o -name '*.md' -o -name 'MANIFEST.in' \) -print0 | xargs -0 grep -l minipro_complete_db` returns empty (zero file matches).
    - `firestarter_app/firestarter/data/chip_database.json` exists; the prior `minipro_complete_db.json` no longer exists in that directory.
    - `git -C firestarter_app log --follow --format='%H' -- firestarter/data/chip_database.json` returns >= 2 commit hashes (the rename commit + the prior history under the old filename — proves `git mv` worked).
    - `firestarter_app/firestarter/database.py` contains `_read_config_file("chip_database.json")` at line `:189` AND the substring `'chip_database.json'` in the `_map_data` docstring near line `:366`.
    - `firestarter_app/tools/build_db.py:12` matches the regex `OUTPUT_FILE\s*=\s*os\.path\.join.*"chip_database\.json"`.
    - `firestarter_app/tools/check_dispatch.py` line `:2` docstring AND the `_DATA_DIR`-related glob/default near `:27` both reference `chip_database.json`.
    - `firestarter_app/CLAUDE.md` contains `chip_database.json` at the previously-`minipro_complete_db` sites (`:19`, `:36`, `:69`).
    - `firestarter/CLAUDE.md` contains `regenerated chip_database.json` at `:30`. Line `:69` (the `minipro protocol_id` substring) is UNCHANGED in this task — Plan 02-03 owns that.
    - Meta `CLAUDE.md:44` contains `firestarter_app/firestarter/data/chip_database.json`.
    - Each sub-repo has exactly one commit recording the rename batch; the meta-repo has one tiny doc-only commit for the `:44` edit.
    - `firestarter_app/tools/build_db.py:10` (`MINIPRO_XML_URL`) is UNCHANGED in this task.
  </acceptance_criteria>
  <done>
    - Grep gate passes (zero `minipro_complete_db` matches anywhere in the working tree).
    - All eight path-callsite assertions above hold.
    - The `firestarter_app/` commit graph shows the rename via `git mv` with `--follow` chaining through the prior history (blame preserved).
    - Each of the three repositories (meta-repo, `firestarter_app/`, `firestarter/`) has its own commit with the appropriate message above.
  </done>
</task>

<task type="auto">
  <name>Task 02-02-02: D-04 `_map_data` internal `vpp_volts` rename (dict-write + emitter fallback + two downstream consumers)</name>
  <files>firestarter_app/firestarter/database.py, firestarter_app/firestarter/eprom_info.py, firestarter_app/firestarter/ic_layout.py</files>
  <read_first>
    - firestarter_app/firestarter/database.py (current state — lines 411-426 for `_map_data` dict-write at :417; lines 505-512 for emitter fallback at :510; lines 373-381 for the UPSTREAM-SCHEMA read that is NOT renamed)
    - firestarter_app/firestarter/eprom_info.py (current state — lines 265-278, focus on `:271` consumer)
    - firestarter_app/firestarter/ic_layout.py (current state — lines 510-520, focus on `:516` consumer)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md §"Missed Callsites" (verbatim verification that `eprom_info.py:271` and `ic_layout.py:513` are the two and only two consumer sites; CONTEXT.md D-04 anticipated but did not enumerate them)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md §"Pitfall #2: Loading order" (the three-different-vpp-concepts distinction — RENAME internal dict, PRESERVE upstream-schema read)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md §1b, §1c (target patterns for the rename + the upstream-schema preservation)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-CONTEXT.md §D-04 + §"Specific Ideas" (`_map_data()` internal dict diff)
  </read_first>
  <action>
**Working directory:** `firestarter_app/` (lives in the same sub-repo commit as Task 02-02-01 if practical, or a sibling commit immediately after — both options preserve D-13's "naturally atomic" intent. The grep gate in this task does not require Task 02-02-01 to have landed; both tasks operate on disjoint lines).

This is the D-04 internal rename — symmetric naming with the `vpp_mv` int sibling. Per RESEARCH.md "Pitfall #2", the three different `"vpp"` concepts MUST be kept distinct: this task touches **only the internal `_map_data()` OUTPUT dict key and its consumers**, not the upstream-schema READ at `database.py:373-381` (which reads the on-disk DB string `"12V"` and legacy user-override DBs — that read path stays `electrical.get("vpp", "0")` per D-08-compat).

Edits (four sites, all renaming the same conceptual key from `"vpp"` to `"vpp_volts"`):

1. `firestarter_app/firestarter/database.py:417` — in the `_map_data()` dict literal, change the line `"vpp": vpp,` to `"vpp_volts": vpp,`. The local variable `vpp` (float volts, computed at `:373-381`) is unchanged; only the dict KEY string flips. The neighbouring `"vpp_mv": vpp_mv,` line at `:418` is unchanged (already correctly named).

2. `firestarter_app/firestarter/database.py:510` — in the `convert_to_programmer` local-variable assignment, change the fallback chain from `... or int(full_eprom_data.get("vpp", 0) * 1000)` to `... or int(full_eprom_data.get("vpp_volts", 0) * 1000)`. The primary `full_eprom_data.get("vpp_mv")` portion and the surrounding `or` shape (Reusable Asset per CONTEXT.md `<code_context>`) are preserved verbatim — only the second key-string flips. This is the consumer side of the `:417` write — both lines reference the SAME internal-dict key; they MUST rename together.

3. `firestarter_app/firestarter/eprom_info.py:271` — change `ic.get('vpp', '-')` to `ic.get('vpp_volts', '-')`. The surrounding line `vpp_str = f"{ic.get(...)}v" if ic.get("type") == 1 else "- "` is otherwise unchanged. Verified by RESEARCH.md as a `_map_data()` output consumer (caller chain `main.py:451 → db.get_eproms() → _map_data()`).

4. `firestarter_app/firestarter/ic_layout.py:513` — change `eprom_data.get('vpp', 'N/A')` to `eprom_data.get('vpp_volts', 'N/A')`. The surrounding line `output_data["vpp_str"] = f"{eprom_data.get(...)}v"` is otherwise unchanged. Verified by RESEARCH.md as a `_map_data()` output consumer (the docstring at `:482` confirms `eprom_data` is the `get_eprom(..., full=True)` output).

**Do NOT touch** `firestarter_app/firestarter/database.py:373-381` — specifically the `vpp_str = electrical.get("vpp", "0").replace("V", "")` line. That reads the upstream on-disk DB schema (the `"12V"` string format emitted by `build_db.py:255` and by legacy user-override files at `~/.firestarter/database.json`). Per D-08-compat, this read path is PRESERVED to keep legacy user-override DBs working. Confusion alert per RESEARCH.md Pitfall #2: same `"vpp"` substring, different layer.

Stage these four edits together. They can land in the same commit as Task 02-02-01's rename batch (preferred — natural atomicity per D-13), or in a sibling `firestarter_app/` commit immediately after if commit hygiene calls for two smaller commits.
  </action>
  <verify>
    <automated>cd firestarter_app &amp;&amp; grep -nE '"vpp_volts":\s*vpp,' firestarter/database.py | grep -q ':417:' &amp;&amp; grep -nE 'get\("vpp_volts",\s*0\)\s*\*\s*1000' firestarter/database.py | grep -q ':510:' &amp;&amp; grep -nE "ic\.get\('vpp_volts',\s*'-'\)" firestarter/eprom_info.py | grep -q ':271:' &amp;&amp; grep -nE "eprom_data\.get\('vpp_volts',\s*'N/A'\)" firestarter/ic_layout.py | grep -q ':516:' &amp;&amp; grep -nE 'electrical\.get\("vpp",\s*"0"\)\.replace\("V",\s*""\)' firestarter/database.py | grep -qE ':37[5-9]:|:38[0-1]:' &amp;&amp; ! grep -nE '"vpp":\s*vpp,' firestarter/database.py &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/firestarter/database.py:417` matches `"vpp_volts":\s*vpp,` (regex anchored at line 417).
    - `firestarter_app/firestarter/database.py:510` contains `int(full_eprom_data.get("vpp_volts", 0) * 1000)` and the surrounding `vpp_mv = full_eprom_data.get("vpp_mv") or ...` chain.
    - `firestarter_app/firestarter/database.py` contains zero occurrences of the regex `"vpp":\s*vpp,` (the `:417` line must NOT keep the legacy form).
    - `firestarter_app/firestarter/eprom_info.py:271` contains `ic.get('vpp_volts', '-')`.
    - `firestarter_app/firestarter/ic_layout.py:513` contains `eprom_data.get('vpp_volts', 'N/A')`.
    - `firestarter_app/firestarter/database.py` lines `:373-381` STILL contain `electrical.get("vpp", "0").replace("V", "")` — the upstream-schema read is UNCHANGED per D-08-compat (regression-proof against accidental "tidying").
    - No new files created. The two non-`database.py` edits are single-line touches.
  </acceptance_criteria>
  <done>
    - All seven grep-anchored assertions in `<verify>` and `<acceptance_criteria>` pass.
    - `firestarter info W27C512` runs without `KeyError` / formatting breakage (confirms `eprom_info.py:271` is wired correctly to the renamed dict key — exercised by Plan 02-03's SC#5 smoke).
    - The four lines that flipped are committed; no other lines in `database.py`, `eprom_info.py`, or `ic_layout.py` are modified.
  </done>
</task>

<task type="auto">
  <name>Task 02-02-03: Fix packaging metadata so the renamed DB ships in built wheels (`pyproject.toml` + `MANIFEST.in`)</name>
  <files>firestarter_app/pyproject.toml, firestarter_app/MANIFEST.in</files>
  <read_first>
    - firestarter_app/pyproject.toml (current state — lines 60-72, focus on the stale `[tool.setuptools.package-data]` block at `:64-69`)
    - firestarter_app/MANIFEST.in (current state — all 11 lines; the first two `include` lines are stale)
    - firestarter_app/firestarter/data/ directory listing (post-rename: `chip_database.json`, `database_overrides.json`, `pinouts.json` — verifiable via `ls firestarter_app/firestarter/data/` after Task 02-02-01 lands)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-RESEARCH.md §"Critical: pyproject.toml package-data is stale" + §"Pre-existing stale state" + Assumption A2 (the rationale for fixing in this phase)
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-PATTERNS.md §8 and §9 (target post-state for both files)
  </read_first>
  <action>
**Working directory:** `firestarter_app/`. Same commit as Tasks 02-02-01 and 02-02-02 if practical (one atomic CLEAN-01 commit landing the rename batch + the consumer rename + the packaging fix), or a sibling commit immediately after.

This task closes the v1.0 Phase 11 packaging-metadata drift identified in
RESEARCH.md "Critical: pyproject.toml package-data is stale". Today both files
declare data filenames that do NOT exist in `firestarter_app/firestarter/data/`
(`database_generated.json`, `pin-maps.json`, `database.json`). Without this fix,
`pip install -e .` works (editable install reads from the source tree via
`include-package-data = true`) but a built wheel — `pip install .` or PyPI —
would `FileNotFoundError` on the renamed DB. SC#5 may pass in dev and ship
broken to downstream consumers.

Edits:

1. `firestarter_app/pyproject.toml:64-69` — replace the `[tool.setuptools.package-data]` block. Replace the three stale entries (`data/database_generated.json`, `data/database_overrides.json`, `data/pin-maps.json`) with the three real shipping files post-rename: `data/chip_database.json`, `data/database_overrides.json`, `data/pinouts.json`. Surrounding TOML structure (the `[tool.setuptools]` block above and the `[tool.setuptools.dynamic]` block below) is unchanged. Indentation and string-quoting style match the file's existing convention (4-space indent + double-quoted strings).

2. `firestarter_app/MANIFEST.in` — replace the first two `include` lines (`include firestarter/data/database.json` and `include firestarter/data/pin-maps.json`, neither of which references a real file) with explicit `include` directives for the real data files: `include firestarter/data/chip_database.json`, `include firestarter/data/pinouts.json`, `include firestarter/data/database_overrides.json`. Keep the existing `include firestarter/data/avrdude.conf` line and all the `firestarter/<module>.py` + README/LICENSE lines that already exist (lines 3-10). Net result: 12 `include` lines (was 11), all pointing at files that actually exist.

Sanity-check by listing the data directory after Task 02-02-01's `git mv` lands: the file inventory MUST be `chip_database.json`, `database_overrides.json`, `pinouts.json`. If any other data file exists in that directory (e.g. `avrdude.conf` lives at `firestarter/data/avrdude.conf` per the existing MANIFEST line — preserve), include it; do not invent files that don't exist.

Stage with Tasks 02-02-01 and 02-02-02 if the timeline is tight; otherwise sibling commit message: `CLEAN-01: align pyproject.toml + MANIFEST.in package-data to actual shipping files (closes v1.0 Phase 11 packaging drift)`.
  </action>
  <verify>
    <automated>cd firestarter_app &amp;&amp; grep -qF 'data/chip_database.json' pyproject.toml &amp;&amp; grep -qF 'data/pinouts.json' pyproject.toml &amp;&amp; ! grep -qF 'data/database_generated.json' pyproject.toml &amp;&amp; ! grep -qF 'data/pin-maps.json' pyproject.toml &amp;&amp; grep -qF 'include firestarter/data/chip_database.json' MANIFEST.in &amp;&amp; grep -qF 'include firestarter/data/pinouts.json' MANIFEST.in &amp;&amp; ! grep -qF 'include firestarter/data/database.json' MANIFEST.in &amp;&amp; ! grep -qF 'include firestarter/data/pin-maps.json' MANIFEST.in &amp;&amp; for f in $(grep -hE '^include firestarter/data/' MANIFEST.in | awk '{print $2}'); do test -f "$f" || { echo "FAIL: MANIFEST.in references missing file $f"; exit 1; }; done &amp;&amp; for f in $(grep -E '^\s+"data/' pyproject.toml | tr -d '", '); do test -f "firestarter/$f" || { echo "FAIL: pyproject.toml package-data references missing file firestarter/$f"; exit 1; }; done &amp;&amp; pip install -e . >/tmp/pip-install.log 2>&amp;1 &amp;&amp; firestarter --help >/dev/null &amp;&amp; firestarter info W27C512 >/dev/null &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/pyproject.toml [tool.setuptools.package-data]` lists exactly three filenames, each of which exists on disk under `firestarter_app/firestarter/data/`: `chip_database.json`, `database_overrides.json`, `pinouts.json`. No stale entries (`database_generated.json`, `pin-maps.json`, `database.json`) remain.
    - `firestarter_app/MANIFEST.in` lists at least `include firestarter/data/chip_database.json`, `include firestarter/data/pinouts.json`, `include firestarter/data/avrdude.conf`, AND `include firestarter/data/database_overrides.json` (per the recommendation; the executor MAY also include the existing `firestarter/<module>.py` + README + LICENSE lines unchanged). No stale `database.json` or `pin-maps.json` entries remain.
    - Every `include firestarter/data/<file>` directive in `MANIFEST.in` and every `data/<file>` entry in `pyproject.toml` resolves to a real file on disk (verified by the `test -f` loop in `<verify>`).
    - `pip install -e .` exits 0 against the modified `pyproject.toml` (verified via `/tmp/pip-install.log`).
    - `firestarter --help` and `firestarter info W27C512` exit 0 against the editable install (no `FileNotFoundError`; the renamed DB resolves).
    - `firestarter_app/pyproject.toml` `[project.scripts]` block (the `firestarter = "firestarter.main:main"` line) is UNCHANGED. The `[tool.setuptools]` and `[tool.setuptools.dynamic]` blocks are UNCHANGED.
  </acceptance_criteria>
  <done>
    - All grep gates pass and the `test -f` loop confirms every declared package-data file exists.
    - `pip install -e .` succeeds.
    - `firestarter info W27C512` resolves the renamed DB without error — partial discharge of SC#5; the full SC#5 evidence (including `--adapter`) lives in Plan 02-03's task.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete (preferably in one or two `firestarter_app/` commits plus the small firmware-side and meta-repo doc commits):

- `! grep -rn 'minipro_complete_db' firestarter_app/ firestarter/ CLAUDE.md` succeeds (zero matches anywhere in the working tree).
- `test -f firestarter_app/firestarter/data/chip_database.json && ! test -e firestarter_app/firestarter/data/minipro_complete_db.json` (file rename complete).
- `git -C firestarter_app log --follow --format='%H' -- firestarter/data/chip_database.json | wc -l` returns >= 2 (blame preserved per D-06).
- `cd firestarter_app && grep -nE '"vpp_volts":\s*vpp,' firestarter/database.py` returns line `:417`; `grep -nE 'get\("vpp_volts",\s*0\)\s*\*\s*1000' firestarter/database.py` returns line `:510`.
- `cd firestarter_app && grep -nE "'vpp_volts'" firestarter/eprom_info.py firestarter/ic_layout.py` returns at least two hits (one per consumer file).
- `cd firestarter_app && grep -nE 'electrical\.get\("vpp",\s*"0"\)' firestarter/database.py` STILL returns a hit near `:375` (upstream-schema read preserved per D-08-compat — regression-proof).
- `cd firestarter_app && pip install -e . && firestarter --help && firestarter info W27C512` all exit 0.
- Plan 02-01's wire-emit contract still holds (`grep -nE '"vpp_mv":\s*vpp_mv' firestarter_app/firestarter/database.py` returns line `:518`) — this plan does NOT touch line `:518`.

Plan 02-03 consumes the renamed file and the renamed internal key as the
post-state contract for its `check_dispatch.py` augmentation and CLI smoke.
</verification>

<success_criteria>
- Roadmap SC#2 source-state assertion holds: chip-database file renamed to `chip_database.json`; all 7 readers/writer/docstrings (`build_db.py:12`, `database.py:189`, `database.py:366`, `check_dispatch.py:2`, `check_dispatch.py:27`, meta `CLAUDE.md:44`, `firestarter_app/CLAUDE.md` data-flow/Key Files/Pipeline) point at the new name; meta + sub-repo CLAUDE.md docs reflect the new name at every CLEAN-01 site.
- D-04 internal rename closed: `_map_data()` writes `"vpp_volts"`; emitter fallback at `:510` reads `"vpp_volts"`; both downstream consumers read `"vpp_volts"`; upstream-schema READ at `:373-381` is preserved (RESEARCH.md Pitfall #2).
- Packaging drift closed: `pyproject.toml` + `MANIFEST.in` reference the actual shipping files (RESEARCH.md "Critical: pyproject.toml package-data is stale") — built wheels would ship the renamed DB.
- `pip install -e .` succeeds; `firestarter --help` and `firestarter info W27C512` exit 0 against the renamed file (initial SC#5 discharge; Plan 02-03 closes SC#5 with the full smoke).
- The `MINIPRO_XML_URL` constant at `firestarter_app/tools/build_db.py:10` and the `minipro` substrings at `firestarter_app/CLAUDE.md:42, :69, :72`, `firestarter/CLAUDE.md:69`, `database.py:45, :389`, `check_dispatch.py:30, :23` are NOT touched in this plan — Plan 02-03 owns CLEAN-02 (the comment / attribution scrub).
</success_criteria>

<output>
After completion, create `.planning/phases/02-naming-cleanup-wire-key-minipro-references/02-02-SUMMARY.md` per `@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md`. Record:
- `firestarter_app/` rename commit SHA (and the `git log --follow` chain proving blame preservation).
- The meta-repo and `firestarter/` doc-only commit SHAs.
- Confirmation that `pip install -e .` + `firestarter info W27C512` exited 0 against the renamed file.
- A pointer forward to Plan 02-03 for the comment/attribution scrub + the `check_dispatch.py` D-15 augmentation + the full SC#5 CLI smoke.
</output>