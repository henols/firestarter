---
phase: 207.1-address-v1-41-tech-debt
plan: 07
type: execute
wave: 5
depends_on: ["207.1-01", "207.1-02", "207.1-03", "207.1-04", "207.1-05", "207.1-06"]
files_modified:
  - firestarter_app
  - firestarter_fw
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md
autonomous: true
requirements: []

estimate:
  tokens: 22000
  raw_tokens: 44000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-13: meta HEAD's `firestarter_app` and `firestarter_fw` gitlinks equal each sub-repo's HEAD, advanced once, in one commit that touches exactly those two paths, after every sub-repo commit of this phase."
    - "The closing ledger `207.1-LEDGER.md` has one row for each of the 26 open audit items plus one row for 202 WR-02; every row names its D-NN and one outcome out of fixed, accepted, closed on evidence; every fixed row cites at least one commit sha that resolves in its repository."
    - "D-04: the ledger records the read-only `gh` query and its result -- 7 issues, 9 filed reports (#65, #66 plus comment 5698646617, #70, #71, #83 plus comment 5713093962, #86, #90), all cycle-block shape, 0 tagged through the audit-named branch on the per-step path -- and states that D-03 stands at this count."
    - "D-03: the ledger records that the CONTEXT reversal note (set run_count to the nominal runs in both branches) would re-key only a per-step-path report, none of the nine filed ones; that refinement does not reopen D-03."
    - "D-18: the ledger gives each accepted-without-work item its own line citing its source: CMP-F2 (206 D-09), the lease not reaching HardwareManager (206 D-05), 205's accepted host-guard-only risk, the bench coverage limits, 203-03's RED/GREEN order, and the sub-repo branches with no upstream."
    - "D-19: the stale 202-205 digests and 206's source-free digest are accepted as benign; the ledger points to the audit's green gate run (§ Verification Integrity) and to this phase's own green gate, and no VERIFICATION.md in phases 202-207 changed during this phase (no `verification fingerprint` run)."
    - "D-20: 202 WR-02 is listed as closed by Phase 206, with no action."
    - "Nothing from this phase is on any remote: no repository's HEAD is an ancestor of its `origin/beta`, no `v1.41-verification-to-host` branch exists on any remote, and no `v1.41*` tag exists in meta."
  artifacts:
    - path: .planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md
      provides: "the phase's closing artifact: every audit item mapped to a decision, an outcome and evidence"
      contains: "| 26 |"
  key_links:
    - from: .planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md
      to: .planning/milestones/v1.41-MILESTONE-AUDIT.md
      via: "each ledger row is one entry of the audit's `tech_debt` frontmatter, in the audit's own order"
      pattern: "tech_debt"
    - from: firestarter_app
      to: .planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md
      via: "the gitlink commit is what makes every app sha the ledger cites reachable from meta HEAD"
      pattern: "advance firestarter_fw and firestarter_app gitlinks"
---

<objective>
Close the phase: D-13 (advance both sub-repo gitlinks in meta, once, after every sub-repo commit), and
write the closing ledger that maps every one of the 26 open audit items plus 202 WR-02 to its
decision, its outcome and its evidence, including the D-04 count and the D-18, D-19 and D-20 lines.
End with a proof that nothing was pushed.

Purpose: the next audit must be able to check, row by row, that nothing in the v1.41 close-time
audit was left without a disposition (207.1-CONTEXT.md § Specific Ideas). The gitlink commit is what
makes the app and fw commits the ledger cites reachable from meta.

Output: one gitlink commit, one ledger file and its commit.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/milestones/v1.41-MILESTONE-AUDIT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-01-SUMMARY.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-02-SUMMARY.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-03-SUMMARY.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-04-SUMMARY.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-05-SUMMARY.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-06-SUMMARY.md
</context>

## Spec-less probe fallback

Skipped, visibly: this phase has no requirement IDs, so there are no spec probes to generate. The
ledger below is the phase's coverage unit instead.

## Artifacts this phase produces (this plan)

- New file path: `.planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md`.
- No source symbol, CLI flag or dataclass field.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): D-13 end to end -- both gitlinks advanced once, after every sub-repo commit, and provably equal to each sub-repo HEAD</name>
  <files>firestarter_app, firestarter_fw</files>
  <read_first>
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md (§ Meta gitlink advance -- the `d347dd3f` precedent and its body format)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ Runtime State Inventory and § Commit Mechanics)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-03-SUMMARY.md and 207.1-04-SUMMARY.md (the last app and fw commits)
  </read_first>
  <action>
    Per D-13, extended to the fw gitlink because D-15 created an fw commit (207.1-RESEARCH.md Open Question 2); one meta commit advances both, following `d347dd3f`.

    1. Pre-checks, all from /workspaces: `git -C firestarter_app status --porcelain --untracked-files=no` and `git -C firestarter_fw status --porcelain` are both empty; both sub-repos and meta are on `v1.41-verification-to-host`; `git -C firestarter_app merge-base --is-ancestor 64da8f6 HEAD` and `git -C firestarter_fw merge-base --is-ancestor a55f2d8 HEAD` succeed; `git diff --cached --name-only` is empty. If any fails, stop and report.

    2. Build the commit body in the precedent's format: one line per sub-repo naming its new HEAD short sha and subject, followed by the commits since the gitlink meta HEAD records now, from `git -C firestarter_app log --format='%h %s' 2a08c7d..HEAD` (the three 206 review fixes plus this phase's app commits) and `git -C firestarter_fw log --format='%h %s' a55f2d8..HEAD` (the D-15 commit). Stage exactly `firestarter_app` and `firestarter_fw` by name with `git -C /workspaces add -- firestarter_app firestarter_fw`, confirm the staged list is those two, and commit with subject `chore(207.1-07): advance firestarter_fw and firestarter_app gitlinks` and that body. Never `git add -A`. Do not run `git submodule update` or `git submodule sync` (the gitmodules archaeology note warns both can undo or misdirect a checkout). Confirm the meta branch is unchanged. Do not push.

    3. Record in the SUMMARY: this is the phase's one gitlink advance. If a later step of the phase workflow (code review fixes, verification fixes) adds a sub-repo commit, the gitlink must be advanced again before the milestone close; say so in the SUMMARY so the orchestrator sees it.
  </action>
  <verify>
    <automated>cd /workspaces && G=$(git log -1 --format=%H --fixed-strings --grep='chore(207.1-07): advance firestarter_fw and firestarter_app gitlinks' HEAD) && [ -n "$G" ] && F=$(git show --name-only --format= "$G") && [ "$(printf '%s\n' "$F" | LC_ALL=C sort | tr '\n' ' ')" = "firestarter_app firestarter_fw " ] && TA=$(git ls-tree HEAD firestarter_app) && TF=$(git ls-tree HEAD firestarter_fw) && [ "$(printf '%s' "$TA" | awk '{print $3}')" = "$(git -C firestarter_app rev-parse HEAD)" ] && [ "$(printf '%s' "$TF" | awk '{print $3}')" = "$(git -C firestarter_fw rev-parse HEAD)" ] && git -C firestarter_app merge-base --is-ancestor 64da8f6 HEAD && git -C firestarter_fw merge-base --is-ancestor a55f2d8 HEAD && A=$(git -C firestarter_app log --format=%s 64da8f6..HEAD) && for d in D-08 D-07 D-06 D-11 D-05 D-12 D-03; do printf '%s\n' "$A" | /usr/bin/grep -qF -e "$d" || { echo "APP COMMIT MISSING $d"; exit 1; }; done && W=$(git -C firestarter_fw log --format=%s a55f2d8..HEAD) && printf '%s\n' "$W" | /usr/bin/grep -qF -e 'D-15' && [ -z "$(git -C firestarter_app status --porcelain --untracked-files=no)" ] && [ -z "$(git -C firestarter_fw status --porcelain)" ] && for r in /workspaces /workspaces/firestarter_app /workspaces/firestarter_fw; do [ "$(git -C $r rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] || { echo "BRANCH MOVED $r"; exit 1; }; done && echo PASS-207.1-07-D13</automated>
    <fails_when>PASS-207.1-07-D13 is not printed: no gitlink commit, the commit touches a path other than the two gitlinks, a gitlink differs from its sub-repo HEAD, a sub-repo HEAD does not descend from its phase-start sha, an "APP COMMIT MISSING" line, the fw D-15 commit absent from the range, a sub-repo tracked tree is dirty, or a "BRANCH MOVED" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces ls-tree HEAD firestarter_app` and `... firestarter_fw` name exactly `git -C firestarter_app rev-parse HEAD` and `git -C firestarter_fw rev-parse HEAD`.
    - The commit `chore(207.1-07): advance firestarter_fw and firestarter_app gitlinks` touches exactly `firestarter_app` and `firestarter_fw`, and its body lists the sub-repo commits since `2a08c7d` (app) and `a55f2d8` (fw).
    - The app range `64da8f6..HEAD` carries commits naming D-08, D-07, D-06, D-11, D-05, D-12 and D-03; the fw range `a55f2d8..HEAD` carries the D-15 commit.
    - All three repositories are on `v1.41-verification-to-host`; nothing is pushed.
  </acceptance_criteria>
  <done>Meta records both sub-repos at their current HEADs in one gitlink commit, made after the last sub-repo commit of the phase.</done>
</task>

<task type="auto">
  <name>Task 2: the closing ledger -- 27 rows, the D-04 count, the D-18 / D-19 / D-20 lines, and the no-push proof</name>
  <files>.planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md</files>
  <read_first>
    - /workspaces/.planning/milestones/v1.41-MILESTONE-AUDIT.md (the `tech_debt` frontmatter, lines 26-72, and § Verification Integrity)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ Item-to-Decision Map, § D-04, § D-03, § D-18 / D-19 / D-20 citations)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-01-SUMMARY.md through 207.1-06-SUMMARY.md (the commit shas and verdicts to cite)
    - /workspaces/.planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md (the per-item layout analog)
  </read_first>
  <action>
    Write `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md` with the Write tool. It is the phase's closing artifact (207.1-CONTEXT.md § Specific Ideas; the form is Claude's discretion). Cite only shas that exist: read them from the six SUMMARY files and confirm each with `git -C <repo> cat-file -e <sha>^{commit}`. Do not re-run the D-04 `gh` query: research ran it read-only, and this ledger records it.

    Sections, in order:

    1. A title and a two-sentence scope line: the list is the `tech_debt` frontmatter of `.planning/milestones/v1.41-MILESTONE-AUDIT.md`, 26 open items plus 202 WR-02 kept for the record; outcomes are fixed, accepted, or closed on evidence.

    2. `## Ledger`: one markdown table with the header `| # | Audit phase | Item | D-NN | Outcome | Evidence |`. Rows 1-26 follow the audit's own order exactly as in 207.1-RESEARCH.md § Item-to-Decision Map (rows 1-4 phase 202, then the 202 WR-02 row numbered with the single character `—`, rows 5-8 phase 203, 9-12 phase 204, 13-16 phase 205, 17-20 phase 206, 21 phase 207, 22-26 cross-phase). The D-NN cell holds the decision ids exactly as in that map (row 17 is `D-03, D-04`, row 18 `D-10, D-11`, row 25 `D-16, D-17`). The Outcome cell holds exactly one of `fixed`, `accepted`, `closed on evidence`: fixed for rows 1, 2, 4, 5, 6, 7, 11, 14, 15, 18, 21, 22, 25, 26; accepted for rows 3, 8, 10, 12, 13, 16, 17, 19, 20, 23, 24; closed on evidence for row 9 and the WR-02 row. The Evidence cell cites commits as a repo word followed by a backticked short sha -- `app` for firestarter_app, `fw` for firestarter_fw, `meta` for this repository -- plus a file and section where that is the evidence. Every fixed row cites at least one sha: row 1 the D-05 app commit and the meta todo commit; row 2 the D-03/D-09 app commit and the todo commit; rows 4, 11 and 15 the three `docs(phase-NNN)` security commits; rows 5, 6, 7 the D-06, D-07, D-08 app GREEN commits; row 14 the fw D-15 commit; row 18 the D-11 and D-05/D-10 app commits; row 21 the D-14 meta commit; row 22 the gitlink commit from Task 1; row 25 the D-16 meta commit plus the `check-ignore` result for D-17 (per-clone, not committed); row 26 the D-12 app commit. Row 9 cites the D-01 meta commit and `.planning/WINDOWS.md` entry 3. Accepted rows cite their source file and section (item 3 below). No cell may contain a pipe character, because the verify script splits rows on it; use a semicolon between two pieces of evidence.

    3. `## Accepted without work (D-18)`: one line per D-18 item, each citing its source, using the citations in 207.1-RESEARCH.md § D-18 / D-19 / D-20: CMP-F2 dead transport bucket (206-01-PLAN.md D-09; 206-VERIFICATION.md); the lease does not reach HardwareManager (206-03-PLAN.md D-05; 206-SESSION-COST.md § 3); 205's accepted risk that the host guard is the only safety net against half-programming a non-blank UV part (PROJECT.md D-2; REQUIREMENTS.md D-2); the bench coverage limits -- Leonardo only, no Uno-class board, a W27C512 as the UV proxy, no Intel-flash or NOR-unlock part (204-BENCH-MATRIX.md; 204-BENCH-TRACER.md; 205-BENCH-MATRIX.md § Stated coverage gaps; 206-SESSION-COST.md); 203-03's RED/GREEN order (203-03-SUMMARY.md Deviation 4); no upstream on the sub-repo milestone branches (root CLAUDE.md § Milestone close and branch protection: GSD pushes at `/gsd-ship`).

    4. `## Verification digests (D-19)`: 202, 203, 204 and 205 read `stale` and 206 covers no source; all are accepted as benign. 204's digest cannot be recomputed because fw `1cf1b22` deleted `tests/test_blank_check_region_source_contract.py`. D-16's citation repoint in 206-SESSION-COST.md (a 206 covered file) drifts 206's digest further, also accepted. The evidence instead is the audit's green gate run on the live tree (v1.41-MILESTONE-AUDIT.md § Verification Integrity) and this phase's own CI-equivalent gate (207.1-03-SUMMARY.md: the passed count, 36 snapshots, coverage). No `verification fingerprint` verb was run and no VERIFICATION.md changed.

    5. `## 202 WR-02 (D-20)`: closed by Phase 206 (verdict 2 maps to SKIPPED plus STATUS_ERROR), listed only for the record; no action (audit frontmatter, 202 item 5; 206-01-PLAN.md D-01).

    6. `## The D-04 count (for the record; D-03 stands)`: the method and result from 207.1-RESEARCH.md § D-04, transcribed: the two read-only commands (`gh issue list -R henols/firestarter --state all --limit 1000 --json number,title,state,createdAt,body` and `gh api --paginate 'repos/henols/firestarter/issues/comments?per_page=100'`, with `XDG_CACHE_HOME` pointed at a scratch directory), the note that `henols/firestarter_prom` resolves to `henols/firestarter` and that duplicates are filed as comments, the discriminator (a step with op in read, write, write-partial, verify, erase at `run_count == 1`, plus another such step at `run_count >= 2`, and every matching fingerprint reproduced from its own JSON), and the result: 64 issues (40 `[dev test]`), 197 comments, 42 reports with steps, 15 carrying `runs=1`; 7 issues and 9 reports are default-policy with a raised step (#65 closed; #66 plus comment 5698646617; #70; #71; #83 plus comment 5713093962; #86; #90); 6 `--fast`-shape issues not counted (#42, #45, #46, #47, #48, #50); 0 tagged through the audit-named `(SerialError, HardwareOperationError)` branch on the per-step path. Then the D-03 refinement: all nine are the cycle-block shape (the `hardware_refused` break plus `_aggregate_cycle_results` returning one result), so the CONTEXT reversal note would re-key none of them; reversing D-03 in full would need a separate change to the cycle path and a declared re-key record. D-03 stands at this count, as locked.

    7. `## D-02 security verdicts`: one line each for 202, 204 and 205 from 207.1-06-SUMMARY.md (commit sha, `status: verified`, `threats_open: 0`, audit route), noting T-205-10 and T-205-16 in 205's accepted-risks log.

    8. `## No push`: nothing from this phase was pushed from meta, fw, app or the wiki; the push happens at `/gsd-ship` (root CLAUDE.md: a `beta` push publishes). Include the verify block's `PASS-207.1-NO-PUSH` line.

    9. Close with `**Gaps:** none.` and `**Count check:**` stating 4 + 4 + 4 + 4 + 4 + 1 + 5 = 26 open rows plus the WR-02 row = 27.

    Then from /workspaces: confirm `git diff --cached --name-only` is empty, stage only the ledger path, and commit with `git -C /workspaces commit -m "docs(207.1-07): the 207.1 closing ledger"`. Confirm the branch. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "import re,subprocess as sp,sys; t=open('.planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md',encoding='utf-8').read(); R=[[c.strip() for c in l.strip().strip('|').split('|')] for l in t.splitlines() if re.match(r'^\| (\d+|—) \|',l)]; n=sorted(int(r[0]) for r in R if r[0].isdigit()); w=[r for r in R if r[0]=='—']; A={'fixed','accepted','closed on evidence'}; bad=[r[0] for r in R if len(r)<6 or not re.search(r'D-\d\d',r[3]) or r[4] not in A]; M={'app':'firestarter_app','fw':'firestarter_fw','meta':'.'}; S=[(k,s) for r in R for k,s in re.findall(r'\b(app|fw|meta) \x60?([0-9a-f]{7,40})\b',r[5])]; miss=[s for k,s in S if sp.run(['git','-C',M[k],'cat-file','-e',s+'^{commit}'],capture_output=True).returncode]; nosha=[r[0] for r in R if len(r)>=6 and r[4]=='fixed' and not re.search(r'\b(app|fw|meta) \x60?[0-9a-f]{7,40}\b',r[5])]; D=[d for d in ['D-%02d'%i for i in range(1,21)] if d not in t]; ok=n==list(range(1,27)) and len(w)==1 and w[0][4]=='closed on evidence' and not bad and not miss and not nosha and not D and '7 issues' in t and '9 reports' in t and 'T-205-16' in t; print(dict(rows=len(R),bad=bad,miss=miss,nosha=nosha,missingD=D,shas=len(S))); sys.exit(0 if ok else 1)" && C=$(git log -1 --format=%H --fixed-strings --grep='docs(207.1-07): the 207.1 closing ledger' HEAD) && [ -n "$C" ] && F=$(git show --name-only --format= "$C") && [ "$F" = ".planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md" ] && git diff --quiet 10c9b615 HEAD -- ':(glob).planning/phases/20[2-7]-*/*-VERIFICATION.md' && [ -z "$(git status --porcelain -- ':(glob).planning/phases/20[2-7]-*/*-VERIFICATION.md')" ] && echo PASS-207.1-07-LEDGER</automated>
    <fails_when>PASS-207.1-07-LEDGER is not printed: the python check exits 1 (its dict shows a row count other than 27, a row with a bad D-NN or outcome, a cited sha that does not resolve, a fixed row with no sha, a missing D-NN, or the D-04 count or T-205-16 absent), no ledger commit or it touches another path, or a 202-207 VERIFICATION.md changed since 10c9b615</fails_when>
    <automated>for r in /workspaces /workspaces/firestarter_app /workspaces/firestarter_fw; do git -C $r fetch -q origin beta || { echo "FETCH FAIL $r"; exit 1; }; if git -C $r merge-base --is-ancestor HEAD origin/beta; then echo "PUBLISHED $r"; exit 1; fi; [ -z "$(git -C $r ls-remote origin refs/heads/v1.41-verification-to-host)" ] || { echo "REMOTE BRANCH $r"; exit 1; }; [ "$(git -C $r rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] || { echo "BRANCH MOVED $r"; exit 1; }; done; [ -z "$(git -C /workspaces tag --list 'v1.41*')" ] || { echo "META TAG"; exit 1; }; [ "$(git -C /workspaces/firestarter.wiki rev-parse HEAD)" = "$(git -C /workspaces/firestarter.wiki rev-parse origin/master)" ] || { echo "WIKI HAS UNPUSHED OR NEW COMMITS"; exit 1; }; echo PASS-207.1-NO-PUSH</automated>
    <fails_when>PASS-207.1-NO-PUSH is not printed: a FETCH FAIL, PUBLISHED, REMOTE BRANCH, BRANCH MOVED, META TAG or WIKI line appears</fails_when>
  </verify>
  <acceptance_criteria>
    - `207.1-LEDGER.md` exists with 26 numbered rows and one `—` row, each with a D-NN, an outcome from the three allowed values, and evidence; every fixed row cites a sha that `git cat-file -e` resolves in its repo.
    - The ledger contains every id D-01 through D-20, the D-04 result (`7 issues`, `9 reports`, the issue numbers), the D-03 refinement, the D-18 lines, the D-19 note, the D-20 line, the D-02 verdicts naming T-205-16, and `**Gaps:** none.`.
    - The commit `docs(207.1-07): the 207.1 closing ledger` touches only the ledger.
    - No VERIFICATION.md in phases 202-207 changed since meta `10c9b615`.
    - The no-push leg prints `PASS-207.1-NO-PUSH`.
  </acceptance_criteria>
  <done>Every audit item has a recorded disposition with checkable evidence, and the phase is proven unpushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local milestone branches -> remotes | a push to `beta` in either sub-repo publishes (PyPI, fw pre-release) |
| meta gitlinks -> sub-repo history | the gitlink names the exact sub-repo commits the record depends on |
| ledger -> next audit | the next audit reads this ledger as the disposition of every item |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207.1-22 | Tampering (release) | any push from the phase | high | mitigate | No push step exists in any plan; Task 2's no-push leg fetches `origin/beta` read-only in all three repos and fails if any HEAD is an ancestor of it, if a milestone branch exists on a remote, if a `v1.41*` tag exists, or if the wiki clone has commits not on `origin/master`. |
| T-207.1-23 | Tampering (a gitlink that names the wrong commit) | the D-13 commit | medium | mitigate | `ls-tree` equals each sub-repo `rev-parse HEAD`; each HEAD descends from its phase-start sha; the commit touches exactly the two gitlinks; no `git submodule update` or `sync` is run. |
| T-207.1-24 | Repudiation (an outcome claimed without evidence) | `207.1-LEDGER.md` | medium | mitigate | The verify script counts 27 rows, checks each D-NN and outcome, resolves every cited sha with `git cat-file -e`, and fails a fixed row that cites none. |
</threat_model>

<verification>
- Task 1 prints `PASS-207.1-07-D13`; Task 2 prints `PASS-207.1-07-LEDGER` and `PASS-207.1-NO-PUSH`.
- `git -C /workspaces status --porcelain -- .planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md` is empty.
</verification>

<success_criteria>
- The cross-phase gitlink item is fixed (D-13), and every one of the 26 audit items plus 202 WR-02 has a ledger row with a decision, an outcome and evidence (D-04, D-18, D-19, D-20 recorded).
- Nothing is pushed from any repository.
</success_criteria>

<output>
Create `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-07-SUMMARY.md` when done. Record the gitlink commit sha, the ledger commit sha, the ledger check's printed dict, and the no-push line.
</output>
