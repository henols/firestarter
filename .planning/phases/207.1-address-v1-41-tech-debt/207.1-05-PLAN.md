---
phase: 207.1-address-v1-41-tech-debt
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md
autonomous: true
requirements: []
coupling_justified:
  - "207.1-04: both plans commit in meta, but on disjoint paths and with named-path staging only, and each checks the index is empty before staging; either order yields the same two commits"

estimate:
  tokens: 18000
  raw_tokens: 36000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-01: `.planning/WINDOWS.md` entry 3 reads `fixed` in both the JSON block and the table row, with an identical reason in both, `open_count: 0` and `fixed_count: 3`, and the ledger's own drift check (`extractTableRegion` equals `renderTable`) passes."
    - "D-01: entry 3's reason quotes the operator's same-physical-chip confirmation, cites 206-SESSION-COST.md's `chip_id_actual` 55816 (0xDA08) on all six runs on the same rig (Leonardo, /dev/ttyACM0, Rev 2.0), and gives the VPP miscalibration (13.0 V trimmed to 12.0 V) as the PROBABLE cause of the 204/205 0x1818 reads, not a measured one; no re-bench happens."
    - "D-14: `207-REVIEW-FIX.md` records wiki commit `13571d2` as pushed at 2026-09-23T20:25:36Z and as `origin/master`; its frontmatter `status: all_fixed` is unchanged."
    - "D-14: STATE.md's Phase 207 decision line records WR-01 as fixed and pushed; no line in STATE.md still describes WR-01 as unfixed, or the 206/207 secure-phase runs as outstanding; the frontmatter still parses as YAML."
    - "D-14: STATE.md is edited by hand only; no `state.*` gsd-tools writer is run by this plan."
    - "Nothing is pushed; both commits land on meta `v1.41-verification-to-host` and stage named paths only."
  artifacts:
    - path: .planning/WINDOWS.md
      provides: "entry 3 closed on evidence (D-01)"
      contains: "same physical W27C512"
    - path: .planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md
      provides: "the corrected WR-01 commit line (D-14)"
      contains: "2026-09-23T20:25:36Z"
    - path: .planning/STATE.md
      provides: "the corrected Phase 207 decision line (D-14)"
      contains: "Code-review WR-01 was fixed after the phase close"
  key_links:
    - from: .planning/WINDOWS.md
      to: .claude/gsd-core/bin/lib/broken-windows.cjs
      via: "the next `windows` verb write refuses with WINDOWS_LEDGER_TABLE_DRIFT unless the table re-renders byte-identically from the JSON"
      pattern: "renderTable"
---

<objective>
Correct the meta records the audit found wrong or open: D-01 (close WINDOWS entry 3 on the evidence
the 206-04 bench produced, with the operator's same-chip confirmation) and D-14 (the 207 record still
says the WR-01 wiki fix was never pushed, and STATE.md still says WR-01 was not fixed).

Purpose: a record that states something false is the failure this project's audits exist to catch.
Both corrections cite evidence already on disk; nothing is re-measured.

Output: one WINDOWS.md commit; one commit carrying 207-REVIEW-FIX.md and STATE.md.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md
</context>

## Spec-less probe fallback

Skipped, visibly: this phase has no requirement IDs, so there are no spec probes to generate.

## Artifacts this phase produces (this plan)

- No new file, symbol, flag or field. Edits only: WINDOWS.md entry 3, 207-REVIEW-FIX.md § WR-01, three STATE.md lines.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): D-01 end to end -- WINDOWS entry 3 closed on evidence through the verb, the reason written identically in both places, the drift guard green</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - /workspaces/.planning/WINDOWS.md (whole file: the frontmatter counts, table rows 1-3, the JSON block; entry 2 is the precedent format)
    - /workspaces/.planning/phases/204-the-command-surfaces-leave-the-firmware/204-BENCH-TRACER.md (lines 94-110 the 0x1818 observation and the array-data inference; lines 170-176 the VPP-high warning)
    - /workspaces/.planning/phases/205-the-pre-flights-leave-the-firmware/205-BENCH-MATRIX.md (lines 36-80, the same reading on the same part)
    - /workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md (lines 38-50 rig and pot trim; lines 95-102 and 285-292 chip_id_actual 55816)
    - /workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-04-SUMMARY.md (lines 96-100 and 128-132, the 13.0 V fault corrected to 12.0 V)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (Pitfalls 1 and 2)
  </read_first>
  <action>
    Per D-01. A `windows fixed` verb exists, so use it, then hand-edit the reason, because the verb takes no reason (207.1-RESEARCH.md finding 5).

    1. From /workspaces confirm `git diff --cached --name-only` is empty (stop and report if not) and that the drift check below prints `"ok":true` before any change.

    2. Run `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 3` from cwd /workspaces. It sets entry 3's `status` and `resolved_at`, recomputes `open_count`, `fixed_count` and `last_updated`, and re-renders the table and the JSON block. Do not hand-edit the counts or timestamps.

    3. With the Edit tool, set entry 3's JSON `"reason"` (currently the empty string) and the reason cell of table row 3 (the cell between `fixed` and entry 3's `recorded_at`) to exactly this one text, identical in both places:

    Closed on evidence in 207.1 (D-01), no re-bench. Operator confirmation on 2026-09-23 (answer: Same chip): the 206-04 bench used the same physical W27C512 as the 204 and 205 benches. 206-SESSION-COST.md reads chip_id_actual 55816 (0xDA08, matching chip_id_expected) on all six runs, on the same rig: Leonardo, /dev/ttyACM0, Rev 2.0 shield. Probable cause of the 204 and 205 0x1818 reads, stated as probable and not measured: the VPP miscalibration that 206-04 found and the operator's pot trim corrected (13.0 V to 12.0 V, 206-04-SUMMARY.md); a wrong VPP puts the wrong voltage on A9 for the ID-sense read, and the 204-BENCH-TRACER.md and 205-BENCH-MATRIX.md transcripts both carry the VPP is high 13.0V warning. The returned 18 18 bytes match the chip's array data, not an ID encoding (204-BENCH-TRACER.md).

    The text contains no pipe, no backslash and no double quote on purpose: `renderTable` escapes the first two in the table but not in the JSON, and a double quote would need JSON escaping, so any of them breaks the byte-identity the drift guard checks. Change nothing else in the file.

    4. Run the drift check from the verify block and require `"ok":true,"open":0,"fixed":3,"e3":"fixed"`. Stage `.planning/WINDOWS.md` by name, confirm it is the only staged path, and commit with `git -C /workspaces commit -m "docs(207.1-05): D-01 close WINDOWS entry 3 on evidence"`. Confirm the meta branch is still `v1.41-verification-to-host`. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && node -e 'const bw=require("/workspaces/.claude/gsd-core/bin/lib/broken-windows.cjs");const raw=require("fs").readFileSync(".planning/WINDOWS.md","utf8");const l=bw.parseLedger(raw);const e=l.entries[2];const r=e.reason||"";const need=["same physical W27C512","Same chip","55816","0xDA08","Leonardo","/dev/ttyACM0","Rev 2.0","13.0 V","12.0 V","probable","A9","18 18","204-BENCH-TRACER.md","205-BENCH-MATRIX.md","206-SESSION-COST.md","206-04-SUMMARY.md"];const miss=need.filter(s=>!r.includes(s));const ok=bw.extractTableRegion(raw,l.total_count)===bw.renderTable(l.entries)&&l.open_count===0&&l.fixed_count===3&&e.id===3&&e.status==="fixed"&&!!e.resolved_at&&miss.length===0;console.log(JSON.stringify({ok,open:l.open_count,fixed:l.fixed_count,e3:e.status,miss}));process.exit(ok?0:1)' && C=$(git log -1 --format=%H --fixed-strings --grep='docs(207.1-05): D-01 close WINDOWS entry 3 on evidence' HEAD) && [ -n "$C" ] && F=$(git show --name-only --format= "$C") && [ "$F" = ".planning/WINDOWS.md" ] && [ -z "$(git status --porcelain -- .planning/WINDOWS.md)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207.1-05-T1</automated>
    <fails_when>PASS-207.1-05-T1 is not printed: the JSON line shows ok false (table drift, counts not 0/3, entry 3 not fixed, no resolved_at) or a non-empty miss list, no D-01 commit, the commit touches more than WINDOWS.md, WINDOWS.md has uncommitted changes, or the meta branch moved</fails_when>
  </verify>
  <acceptance_criteria>
    - The drift check prints `{"ok":true,"open":0,"fixed":3,"e3":"fixed","miss":[]}`.
    - Entry 3's JSON `reason` and table-row reason cell are the identical text given in the action, and `resolved_at` is set by the verb.
    - The commit `docs(207.1-05): D-01 close WINDOWS entry 3 on evidence` touches only `.planning/WINDOWS.md`; nothing is pushed.
  </acceptance_criteria>
  <done>WINDOWS entry 3 is closed on the recorded evidence, the cause is stated as probable, and the ledger re-renders byte-identically.</done>
</task>

<task type="auto">
  <name>Task 2: D-14 -- the 207 record and STATE.md say what actually happened to WR-01 (hand edits only)</name>
  <files>.planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md, .planning/STATE.md</files>
  <read_first>
    - /workspaces/.planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md (frontmatter and § WR-01, lines 1-50)
    - /workspaces/.planning/STATE.md, NOT whole (618 KB): read lines 1-40 (frontmatter and the Current focus line) and lines 3250-3262 (the Phase 207 decision lines); locate the three targets with `/usr/bin/grep -n` first, because execute-phase's own state writers may have moved or rewritten lines since planning
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (Pitfalls 4 and 5)
  </read_first>
  <action>
    Per D-14. Edit STATE.md by hand with the Edit tool only; never run a `gsd-tools` `state.*` writer (memory `reference_state_planned_phase_underwrites_state_md`: they corrupt this file). The evidence for every correction: the wiki clone's reflog line `refs/remotes/origin/master@{2026-09-23T20:25:36Z}: update by push`, and `git -C /workspaces/firestarter.wiki rev-parse origin/master` = `13571d2fae9d99a3510d6ed20427123f63b49ccc`. Confirm both before editing (local refs only; no fetch, no push).

    1. 207-REVIEW-FIX.md § WR-01. Replace the `**Commit:**` paragraph (about lines 39-41: the sentence that records the commit as unpublished and a push as still pending) with a paragraph that reads, wrapped at the file's existing width: **Commit:** 13571d2 in firestarter.wiki (branch master), pushed at 2026-09-23T20:25:36Z. 13571d2 is origin/master, so the live wiki carries this fix (wiki clone reflog: refs/remotes/origin/master@{2026-09-23T20:25:36Z}: update by push). Corrected in Phase 207.1 (D-14); this line first recorded the commit as unpublished. Put `13571d2`, `firestarter.wiki`, `master`, `origin/master` and the reflog line in inline code, as the paragraph it replaces does. Keep the `**Files modified:**` and `**Applied fix:**` paragraphs and the frontmatter (`status: all_fixed`) unchanged.

    2. STATE.md, the Phase 207 decision line (about line 3258; the `- [Phase 207]:` line that says WR-01 was left unfixed and that fixing it needs another public wiki push). Replace the whole line with one line that reads: - [Phase 207]: Code-review WR-01 was fixed after the phase close. Wiki commit 13571d2 changed the Testing-Chips page from twice to three times (_DEFAULT_RUNS = 3 since b596249, 2026-09-17) and was pushed at 2026-09-23T20:25:36Z; it is origin/master (207-REVIEW-FIX.md § WR-01; corrected in Phase 207.1, D-14). Put the tokens `13571d2`, `_DEFAULT_RUNS = 3`, `b596249` and `origin/master` in inline code, as the neighbouring decision lines do; the line's prefix up to and including `after the phase close.` stays exactly as written here.

    3. STATE.md, the `**Current focus:**` line (about line 30). Execute-phase's begin-phase writer normally replaces this whole line with the 207.1 phase label before this plan runs. If the line now reads only that label, leave it. If it still carries the Phase 207 close text, make two replacements in it: the phrase `wiki record live at` followed by `880a59b` becomes `wiki record live at` followed by `13571d2`; and the sentence that lists the 206 and 207 secure-phase runs and WR-01 as still outstanding before the close becomes: Done since: `/gsd-secure-phase 206` (`d5dfe54d`), `/gsd-secure-phase 207` (`6326b92e`), WR-01 (wiki `13571d2`, pushed 2026-09-23T20:25:36Z) and the close-time `/gsd-audit-milestone` (`4bab20c9`, status `tech_debt`). Outstanding before the close: Phase 207.1 (the audit's tech debt), then a hand-archived close.

    4. STATE.md frontmatter `last_activity_desc` (line 11, a double-quoted YAML scalar). If it still carries the Phase 207 close text, make two replacements inside the quotes, adding no double quote and no backslash: the parenthetical that names WR-01 as the review's one warning becomes `(WR-01: the Testing-Chips page said dev test runs twice; the default is three since b596249; fixed afterwards by wiki 13571d2, pushed 2026-09-23T20:25:36Z)`, and the clause that lists the 206 and 207 secure-phase runs as outstanding becomes `/gsd-secure-phase 206 and 207 since done (d5dfe54d, 6326b92e)`. If a state writer has already replaced the value, leave it.

    5. Validate: the STATE.md frontmatter parses as YAML (verify block), and every stale phrase the verify block checks is gone. Then confirm `git -C /workspaces diff --cached --name-only` is empty, run `git -C /workspaces diff --stat -- .planning/STATE.md` and record it in the SUMMARY: STATE.md may already carry uncommitted hunks written by the 207.1 discuss, plan and execute workflows; those belong to this phase and are committed with this change. Stage exactly `.planning/STATE.md` and `.planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md` by name, confirm the staged list is those two, and commit with `git -C /workspaces commit -m "docs(207.1-05): D-14 correct the 207 record drift"`. Confirm the branch is still `v1.41-verification-to-host`. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && S=.planning/STATE.md && R=.planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md && [ "$(git -C /workspaces/firestarter.wiki rev-parse origin/master)" = "13571d2fae9d99a3510d6ed20427123f63b49ccc" ] && [ "$(/usr/bin/grep -cF -e 'locally only, not pushed' $R)" = "0" ] && /usr/bin/grep -qF -e 'pushed at 2026-09-23T20:25:36Z' $R && /usr/bin/grep -qF -e 'D-14' $R && /usr/bin/grep -qxF -e 'status: all_fixed' $R && [ "$(/usr/bin/grep -cF -e 'is pre-existing and not fixed in-phase' $S)" = "0" ] && [ "$(/usr/bin/grep -cF -e 'WR-01 (live Testing-Chips page says' $S)" = "0" ] && [ "$(/usr/bin/grep -cF -e 'secure-phase 206 and 207 outstanding' $S)" = "0" ] && [ "$(/usr/bin/grep -cF -e 'page says dev test runs twice' $S)" = "0" ] && /usr/bin/grep -qF -e '- [Phase 207]: Code-review WR-01 was fixed after the phase close.' $S && /usr/local/py-utils/venvs/bandit/bin/python -c "import yaml; t=open('/workspaces/.planning/STATE.md',encoding='utf-8').read(); assert t.startswith('---\n'); d=yaml.safe_load(t[4:t.index('\n---\n',4)]); assert isinstance(d,dict) and d.get('milestone')=='v1.41', d; print('YAML-OK')" && C=$(git log -1 --format=%H --fixed-strings --grep='docs(207.1-05): D-14 correct the 207 record drift' HEAD) && [ -n "$C" ] && F=$(git show --name-only --format= "$C") && [ "$(printf '%s\n' "$F" | LC_ALL=C sort | tr '\n' ' ')" = ".planning/STATE.md .planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md " ] && [ -z "$(git status --porcelain -- $R)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207.1-05-T2</automated>
    <fails_when>PASS-207.1-05-T2 is not printed: the wiki ref is not 13571d2 (the evidence no longer holds), a stale phrase is still present in either file, the corrected decision line or the D-14 note is missing, `status: all_fixed` was lost, the YAML parse raises (a traceback instead of YAML-OK), no D-14 commit, the commit touches paths other than the two, 207-REVIEW-FIX.md is dirty, or the meta branch moved</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter.wiki rev-parse origin/master` still prints `13571d2fae9d99a3510d6ed20427123f63b49ccc` (the evidence the correction relies on).
    - 207-REVIEW-FIX.md contains `pushed at 2026-09-23T20:25:36Z` and `D-14`, no longer records the commit as local-only, and keeps `status: all_fixed`.
    - STATE.md contains the line starting `- [Phase 207]: Code-review WR-01 was fixed after the phase close.`, and none of the four stale phrases the verify block counts.
    - STATE.md's frontmatter parses as YAML with `milestone: v1.41`.
    - The commit `docs(207.1-05): D-14 correct the 207 record drift` touches exactly `.planning/STATE.md` and `.planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md`; no `state.*` verb was run; nothing is pushed.
  </acceptance_criteria>
  <done>Neither the 207 fix report nor STATE.md claims any longer that the WR-01 wiki fix is unpushed or unfixed, and STATE.md still parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench evidence -> defect ledger | a WINDOWS entry closure is a claim about hardware the next audit relies on |
| hand edit -> GSD state readers | STATE.md's frontmatter is parsed by every GSD verb; a bad edit breaks them silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207.1-16 | Repudiation (closure claims more than the evidence) | WINDOWS.md entry 3 | medium | mitigate | The reason states the cause as probable and not measured, names every evidence file, and quotes the operator's confirmation; the drift check requires each citation to be present. |
| T-207.1-17 | Tampering (state file corruption) | STATE.md frontmatter | medium | mitigate | Hand Edit only, no `state.*` writer; the replacement text inside the quoted `last_activity_desc` adds no quote or backslash; a YAML parse runs after the edit. |
| T-207.1-18 | Tampering (a stray commit of unrelated meta changes) | meta staging | medium | mitigate | Each commit starts from an empty index, stages named paths only, and the verify checks the exact path list of each commit. |
</threat_model>

<verification>
- The WINDOWS drift check prints `"ok":true` with `open` 0 and `fixed` 3.
- `git -C /workspaces log -2 --format=%s` shows the two 207.1-05 commits (in either order relative to 207.1-04's commit).
- `git -C /workspaces diff --quiet 10c9b615 HEAD -- ':(glob).planning/phases/20[2-7]-*/*-VERIFICATION.md'` succeeds (D-19: no verification digest touched).
</verification>

<success_criteria>
- Audit item 204 WINDOWS #3 is closed on evidence (D-01); audit item 207 record drift is fixed (D-14).
- Only the named files changed; nothing is pushed.
</success_criteria>

<output>
Create `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-05-SUMMARY.md` when done. Record both commit shas, the drift-check JSON, which of the three STATE.md sites still carried stale text at edit time, and the STATE.md `diff --stat`.
</output>
