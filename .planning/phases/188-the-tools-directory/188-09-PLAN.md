---
phase: 188-the-tools-directory
plan: 09
type: execute
wave: 8
depends_on: ["188-08"]
commits_in_repo: meta (/workspaces)
autonomous: true
requirements: [TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04, TOOLS-05, TOOLS-06, TOOLS-07]
files_modified:
  - .planning/notes/host-tools-retirement.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-09-12-retire-two-orphaned-host-tools.md
  - .planning/todos/completed/2026-09-12-retire-two-orphaned-host-tools.md
  - .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md
  - .planning/seeds/phase-gate-expiry-discipline.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every retired requirement is marked RETIRED with its cause and its checkbox stays unchecked — a decision not to build stays distinguishable from a thing that was built, at milestone audit, without reading the phase history."
    - "One verdict document names every retired item, what it guarded, and why it went, and states the four disclosed costs in one place — so a later reader cannot mistake any of them for an oversight."
    - "No successor guard exists anywhere: this phase authored no new check script, no new CI step and no new hook in any of the three repositories, and filed no backlog item for any named gap."
    - "The retired consumer-declaration convention left nothing half-present: no script declares a consumer and no check asserts a declaration, and the audit's blind spot is carried forward as a stated cost rather than quietly closed."
    - "The phase's own success criteria are amended in place with the conflict quoted, never deleted and never silently rewritten to match what happened."
    - "The three-repo configuration is intact: the planning configuration still lists all four sub-repositories, so a covered-file prune cannot be mistaken for a real change and block the phase transition."
  artifacts:
    - path: ".planning/notes/host-tools-retirement.md"
      provides: "The verdict document: one line per retired item naming what it guarded and why it went, plus the four disclosed costs in one place"
      min_lines: 80
    - path: ".planning/REQUIREMENTS.md"
      provides: "Seven amended traceability rows, five RETIRED with cause, one satisfied by placement, one Complete"
      contains: "RETIRED"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/notes/host-tools-retirement.md"
      via: "each RETIRED row names its cause and its decision identifier; the note carries the long form"
      pattern: "RETIRED"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the seven amended success criteria and the seven amended traceability rows describe the same end state"
      pattern: "AMENDED by Phase 188"
  prohibitions:
    - requirement_id: TOOLS-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT mark a retired requirement Complete, and MUST NOT flip a retired requirement's checkbox — 'we decided not to' and 'we built it' must stay distinguishable at milestone audit, which is the entire point of the RETIRED disposition this phase mints."
    - requirement_id: TOOLS-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT author a successor guard for anything this phase retired — no new check script, no new CI step, no pre-commit hook, no backlog item filed for a named gap, in any of the three repositories, for any reason. Naming a gap and filing a gap are different acts; this phase performs only the first."
---

<objective>
Write the retirement on the record: one verdict document, seven amended requirement rows, seven
amended success criteria, and the three folded planning items closed or amended with the distinction
that actually applies.

Purpose: six of this phase's seven requirements were answered by subtraction. If they are checked off
as Complete, the milestone audit cannot tell them apart from requirements that were built, and the
four disclosed costs — the cross-repo wire-byte contract, the consumer-declaration blind spot, the
firmware-rename masking guard, and a tool retired on judgment rather than on evidence — become
invisible. This phase therefore mints a disposition that has no precedent in this project's ledger,
and records every cost in one place, with no successor guard filed for any of them.

Output: a verdict note, an amended ledger, amended criteria, and three folded planning items settled.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/188-the-tools-directory/188-CONTEXT.md
@.planning/phases/188-the-tools-directory/188-RESEARCH.md
@.planning/phases/188-the-tools-directory/188-PATTERNS.md
@.planning/notes/catalog-sync-check-retirement.md
@.planning/notes/dispatch-invariant-retirement-verdict.md

**Do NOT bare-`cat` `.planning/ROADMAP.md` (937 KB) or `.planning/STATE.md` (518 KB, one
~52,000-character line).** Use `/usr/bin/grep -n` to find the section and `sed -n '<start>,<end>p'`
to read it. `.planning/REQUIREMENTS.md` is 301 lines and may be read whole.

**Never run `roadmap.update-plan-progress` against this phase**, and never run any `gsd-tools`
state-writing verb during it. The roadmap verb overwrites the dependency table positionally, killing
the phase name and its requirement identifiers; several other verbs prune the planning
configuration's sub-repository list as a side effect, and that file is verification-covered, so a
prune alone reads stale and blocks the phase transition. Every edit in this plan is by hand.

**Read the eight prior SUMMARY files before writing the note.** The measured figures the verdict
document must carry — the coverage-matrix checker's exit behaviour, what the wire contract asserted,
the re-recorded native figures, the live citation counts — were captured by the plans that did the
work, not by this one.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write the verdict document</name>
  <files>.planning/notes/host-tools-retirement.md (created)</files>
  <read_first>
    - `.planning/notes/catalog-sync-check-retirement.md` — the shape this note copies: YAML frontmatter with title, date and context; an H1 repeating the title; a `## VERDICT` section stating the outcome in bold BEFORE any evidence; numbered sections, one per thing the note must carry; and a closing questions section. Note especially its section 5's formulation about naming a gap versus filing one.
    - `.planning/notes/dispatch-invariant-retirement-verdict.md` — the second instance, for two further section headings worth reusing: what the deleted fixtures proved, and what the phase deliberately did not do.
    - All eight prior SUMMARY files in this phase directory — every measured figure the note carries comes from them.
    - `.planning/notes/host-tools-checker-apparatus-audit.md` — the audit this phase was scoped from, including both of its own corrections.
  </read_first>
  <action>
    Write `.planning/notes/host-tools-retirement.md` on the shape of the catalog-sync retirement note.

    Open with a VERDICT section stating the outcome in bold before any evidence: that the host tools
    directory went from twenty-six scripts to six on the operator's own decision, that six of seven
    requirements resolve to retired rather than delivered, and that no successor guard was authored
    for any of it.

    Then one line per retired item, naming what it guarded and why it went. Cover all of it: the ten
    gates by name, the six process tools by name, the two CI mirrors, the one orphan, the frame-vector
    apparatus on both sides, the scan-path pair, and the mypy CI leg. Beside each, the measured line
    count from the plan that deleted it.

    Then the four disclosed costs, in one place, each stated as a loss rather than as a decision:
    that nothing now proves host and firmware agree on the same wire bytes; that a code-level scan
    still cannot distinguish a live operator tool from a dead one, on a smaller directory; that the
    measured defect where a firmware rename silently flips host test modules from pass to skip is no
    longer guarded; and that a tool with no test and no code reference was retired on judgment about
    value rather than on the reference-count evidence the audit had already retracted. Add the two
    costs the execution surfaced: the electrical-safety dispatch invariant that went with the gate
    family, and the surviving AST leg in the lock-status module that is now decorative because the
    planted-fixture pair proving it could fire was deleted with the gate.

    Quote, verbatim in a fenced block, the prose this phase deleted that a reader can no longer
    recover from the tree: the regression-guard sentences removed from the host repository's own
    agent-facing file. The analog note does exactly this, for exactly this reason — the source is
    gone and the note is the only place the correction can land.

    Close with the residual-gap formulation the analog uses: that no successor guard and no backlog
    item are filed for any of these gaps, that this is deliberate on the operator's own decision, and
    that naming a gap and filing a gap are different acts, of which this note performs only the first.

    Record two further facts a later reader will otherwise rediscover the hard way: that the
    coverage-matrix checker's last measured behaviour in check mode was to exit non-zero printing
    nothing at all on either failure path, captured before deletion; and that the chip-dispatch
    baseline data file under the tools directory is now orphaned with zero consumers, retained
    because this phase decided scripts and not data.

    Author no successor guard, no replacement check, no CI step and no backlog item anywhere in this
    note or alongside it — D-23 forbids it for any reason.
  </action>
  <acceptance_criteria>
    - `.planning/notes/host-tools-retirement.md` exists with YAML frontmatter carrying a title, a date and a context line, and a `## VERDICT` section appearing before any evidence section.
    - Every one of the ten gates, the six process tools, the two CI mirrors, the orphan, the scan-path pair and both halves of the frame-vector apparatus is named in the note.
    - The four disclosed costs from the phase decisions plus the two surfaced during execution are all present and each is stated as a loss.
    - The deleted regression-guard prose appears verbatim in a fenced block.
    - The note states that no successor guard and no backlog item are filed, and says so in the naming-versus-filing form.
    - No new check script, CI step or hook exists in any of the three repositories as a result of this phase.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test -f .planning/notes/host-tools-retirement.md && head -6 .planning/notes/host-tools-retirement.md && /usr/bin/grep -c '^## VERDICT' .planning/notes/host-tools-retirement.md</automated>
    <fails_when>non-zero exit, or the printed count is 0 — the verdict section is the one structural feature the analog shape requires first</fails_when>
    <automated>cd /workspaces && for n in check_devtest_orchestrator check_diagnostic_report_claims check_dispatch check_is_memory_cmd_no_ifdef check_mypy_watermark check_no_community_support_status_write check_no_exists_proxy check_no_log_in_sdp_window check_protection_readability_invariants check_sdp_capability_invariants audit_coverage_matrix diff_db measure_plan_shapes measure_part_number_delta snapshot_report_shapes build_devtest_issue_corpus ci_parity ci_replica_venv derive_sdp_partition codegen_vectors scan_paths; do /usr/bin/grep -q -e "$n" .planning/notes/host-tools-retirement.md || echo "MISSING $n"; done | wc -l</automated>
    <fails_when>the printed count is anything other than 0; each missing line names a retired item the note does not mention</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -ci -e 'naming a gap' -e 'no successor guard' .planning/notes/host-tools-retirement.md</automated>
    <fails_when>the printed count is 0 — the residual-gap formulation is what keeps a later reader from filing the successor this phase forbids</fails_when>
    <automated>cd /workspaces && git ls-files -- '.github/workflows/*' > /tmp/188-09-meta-wf.txt; echo "meta-rc=$?"; wc -l < /tmp/188-09-meta-wf.txt; (cd firestarter_app && git fetch origin --quiet; git diff --name-only "origin/beta...HEAD" -- .github/ > /tmp/188-09-app-wf.txt; echo "app-rc=$?"; /usr/bin/grep -cv 'ci.yml' /tmp/188-09-app-wf.txt); (cd firestarter && git fetch origin --quiet; git diff --name-only "origin/beta...HEAD" -- .github/ > /tmp/188-09-fw-wf.txt; echo "fw-rc=$?"; /usr/bin/grep -cvE 'build.yml|beta-build.yml' /tmp/188-09-fw-wf.txt)</automated>
    <fails_when>any `-rc=` is non-zero, or the meta line count is anything other than 0 (the meta repository runs no CI and no workflow may be added), or either sub-repo count is anything other than 0 (a workflow file other than the three this phase edits was touched on this branch, which would mean a successor gate was authored)</fails_when>
  </verify>
  <done>The verdict document exists in the established shape, names every retired item, states all six disclosed costs, quotes the deleted prose, and files no successor.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the requirement ledger and the seven success criteria, by hand</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md (Phase 188 section only — scoped edits, never a whole-file write)</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read it whole (301 lines). The TOOLS section is at lines 202-232 and the seven traceability rows at lines 278-284. Read the FLOOR rows nearby too: they are the `<word> — <cause> (<plan id>)` shape the RETIRED rows copy, and this project has no prior RETIRED row anywhere.
    - `.planning/ROADMAP.md` — read only the Phase 188 section, located with `/usr/bin/grep -n 'Phase 188'` and read with `sed -n`. It carries the goal, the seven success criteria, the explicitly-not-in-scope paragraph, the plans line and the key-context list.
    - `.planning/ROADMAP.md` around the Phase 187 section — one criterion there carries the in-place amendment shape this task copies: the original text retained, followed by a bold amendment marker naming the phase and the decision, then the correction.
  </read_first>
  <action>
    Amend `.planning/REQUIREMENTS.md` by hand, per D-22. Rewrite the disposition cell of all seven traceability
    rows in the existing `<word> — <cause> (<plan id>)` form, substituting the retirement word for the
    completion word where the requirement was answered by subtraction:

    - The coverage-matrix default-path requirement: retired, dissolved by tool deletion — the tool no
      longer exists, so the repo-escaping default path it guarded cannot occur.
    - The consumer-declaration requirement: retired per D-12 — declaration layer and its check both
      dropped on operator decision; the audit's blind spot is carried, not closed.
    - The ten-gates requirement: satisfied by family retirement — all ten decided by name, all ten
      retired, with the four orphaned library symbols relocated into the test tier.
    - The process-tool placement requirement: satisfied by placement — five retired, one relocated
      into the skill that consumes it, all six placed by name.
    - The citation requirement: complete — the six survivors swept by hand, asserted across the
      directory with a positive control.
    - The vector-sync requirement: retired, both files deleted, so there is nothing left to sync.
    - The checker-exit requirement: retired, dissolved by tool deletion, with the last measured
      behaviour recorded in the verdict note.

    Leave every retired requirement's checkbox in the TOOLS section unchecked, as D-22 requires. That is the whole
    point of the disposition: a checkbox flip would make a decision not to build indistinguishable
    from a thing that was built.

    Then amend the Phase 188 section of `.planning/ROADMAP.md`, by hand and with scoped edits only —
    never a whole-file write, and never the roadmap progress verb. For each of the seven success
    criteria, retain the original text and append a bold amendment marker naming this phase and the
    governing decision, followed by what actually happened. Criterion 2 needs particular care: it
    asserts that no deletion may precede the declaration layer, and the operator dropped the
    declaration layer and proceeded with the deletions — quote that conflict rather than smoothing
    it. Criterion 6's no-new-CI-gate constraint was honoured in a stronger form than it asked for, and
    the amendment should say so. Also amend the explicitly-not-in-scope paragraph, which forbids
    deleting a tool on reference-count evidence alone: the tool it was written to protect was deleted,
    on operator judgment about value rather than on reference counts, and that distinction is the
    thing to record.

    The plans line and its nine wave-block entries were written at planning time and already name what
    each plan set out to do. Do not rewrite them: mark each entry complete by flipping its checkbox,
    and append a short clause to any entry whose outcome differed from what the line predicted.

    Finally, assert that the planning configuration still lists all four sub-repositories. Several
    tool verbs prune that array as a side effect, and it is a verification-covered file, so a prune
    alone reads stale and blocks this phase's transition.
  </action>
  <acceptance_criteria>
    - All seven traceability rows name Phase 188 and carry a disposition with a cause; five read RETIRED, one reads satisfied by placement, one reads Complete.
    - Every retired requirement's checkbox in the TOOLS section is still unchecked, and no TOOLS checkbox anywhere is checked.
    - Each of the seven success criteria in the roadmap's Phase 188 section retains its original text and carries an amendment marker naming this phase.
    - The explicitly-not-in-scope paragraph carries an amendment recording that a tool it protected was deleted on judgment rather than on reference counts.
    - The plans line names nine plans, nine plan entries exist, and every one of the nine checkboxes is checked.
    - The Phase 188 dependency and requirement information elsewhere in the roadmap is unchanged — the positional-overwrite verb was not run.
    - The planning configuration still lists all four sub-repositories.
    - No other phase's section in the roadmap was modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -cE '^\| TOOLS-0[1-7] \| Phase 188 \| (RETIRED|Complete|Satisfied|satisfied)' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is anything other than 7 — a row is missing, still reads Pending, or does not follow the disposition-then-cause shape</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'RETIRED' .planning/REQUIREMENTS.md; /usr/bin/grep -c '\[x\] \*\*TOOLS-' .planning/REQUIREMENTS.md; echo "checked-rc=$?"</automated>
    <fails_when>the first printed count is below 5, or the second is anything other than 0 — a checked TOOLS box means a retired requirement was marked as built</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'AMENDED by Phase 188' .planning/ROADMAP.md</automated>
    <fails_when>the printed count is below 8 — seven success criteria plus the not-in-scope paragraph</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '188-0[1-9]-PLAN.md' .planning/ROADMAP.md; /usr/bin/grep -c '^- \[x\] 188-0[1-9]-PLAN.md' .planning/ROADMAP.md</automated>
    <fails_when>either printed count is anything other than 9 — the first means a plan entry is missing, the second means a plan is not marked complete</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'firestarter_py32_ci' .planning/config.json; /usr/bin/grep -c 'firestarter_app_py32' .planning/config.json</automated>
    <fails_when>either printed count is 0 — the sub-repository list was pruned from four entries, which reads as a stale covered file and blocks this phase's transition</fails_when>
    <automated>cd /workspaces && git diff --stat -- .planning/ROADMAP.md | tail -1</automated>
    <fails_when>the reported changed-line total exceeds 80 — a diff that large means something outside the Phase 188 section moved, which is the positional-overwrite failure this plan forbids by hand-editing</fails_when>
  </verify>
  <done>Seven ledger rows and eight roadmap passages are amended by hand, no retired checkbox is flipped, the sub-repository list is intact, and the roadmap diff is confined to this phase's section.</done>
</task>

<task type="auto">
  <name>Task 3: Settle the three folded planning items</name>
  <files>.planning/todos/pending/2026-09-12-retire-two-orphaned-host-tools.md (moved to .planning/todos/completed/), .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md, .planning/seeds/phase-gate-expiry-discipline.md</files>
  <read_first>
    - `.planning/todos/pending/2026-09-12-retire-two-orphaned-host-tools.md` — the todo this phase is. Its recommendation was to declare consumers and then delete safely; the operator dropped the declaration layer.
    - `.planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md` — its tools half is discharged here, its tests half stays open.
    - `.planning/seeds/phase-gate-expiry-discipline.md` — its premise is answered by deletion rather than by discipline.
    - `.planning/todos/completed/` — list it, to match the naming and placement convention for a closed todo.
    - `.planning/phases/188-the-tools-directory/188-07-SUMMARY.md` — the measured remaining citation count, which the provenance todo's amendment must carry.
  </read_first>
  <action>
    Close the orphaned-host-tools todo by moving it into the completed directory, following whatever
    naming convention the files already there use. Record in it the distinction that actually applies:
    the two tools it named split — the ones it wrongly called dead were both deleted here, but on
    operator judgment about value rather than on the reference-count evidence the todo itself had
    retracted. State that the declaration layer it recommended was dropped, not implemented, and point
    at the verdict note for the cost.

    Amend, do NOT close, the provenance-comment todo. Its tools half is discharged — mostly by
    deletion, and for the six survivors by the hand sweep in plan 188-07. Replace its stale tools-half
    figure with the measured live count from that plan's summary, and state that the figure is now
    zero for that directory. Leave its tests half open with its own count, and leave the todo in the
    pending directory.

    Mark the gate-expiry-discipline seed resolved rather than leaving it dormant: its premise was that
    one-shot phase gates need an expiry discipline, and there is no gate family left to expire. Record
    what answered it.

    Do not file a backlog item for any gap this phase named. The verdict note performs the naming; the
    filing is forbidden by decision.
  </action>
  <acceptance_criteria>
    - The orphaned-host-tools todo is no longer in the pending directory and is present in the completed directory, carrying the judgment-versus-reference-count distinction and a pointer to the verdict note.
    - The provenance todo is still in the pending directory, its tools-half figure replaced with the measured live value from plan 188-07, and its tests half untouched and still open.
    - The gate-expiry seed is marked resolved and names what answered it.
    - No new backlog item was filed for any gap this phase named.
    - The whole meta working tree is clean after commit, apart from files this phase deliberately leaves untracked.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && ls .planning/todos/pending/ | /usr/bin/grep -c 'retire-two-orphaned-host-tools'; echo "pending-rc=$?"; ls .planning/todos/completed/ | /usr/bin/grep -c 'retire-two-orphaned-host-tools'</automated>
    <fails_when>the first printed count is other than 0, or the second is 0 — the todo must have moved rather than been copied or left behind</fails_when>
    <automated>cd /workspaces && ls .planning/todos/pending/ | /usr/bin/grep -c 'strip-gsd-provenance-comments-from-source'; /usr/bin/grep -ci 'tests' .planning/todos/pending/2026-08-27-strip-gsd-provenance-comments-from-source.md</automated>
    <fails_when>the first printed count is anything other than 1 (the todo must stay open), or the second is 0 (its tests half was removed along with its tools half)</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -ci -e 'resolved' .planning/seeds/phase-gate-expiry-discipline.md</automated>
    <fails_when>the printed count is 0 — the seed was left dormant instead of being marked resolved</fails_when>
    <automated>cd /workspaces && git fetch origin --quiet; git diff --name-only "origin/beta...HEAD" -- .planning/backlog .planning/backlog.md > /tmp/188-09-backlog.txt; echo "git-rc=$?"; wc -l < /tmp/188-09-backlog.txt</automated>
    <fails_when>`git-rc=` is non-zero, or the line count is anything other than 0 — a backlog file changed on this branch means an item was filed for a gap this phase is forbidden to file</fails_when>
    <automated>cd /workspaces && git status --porcelain > /tmp/188-09-st.txt; echo "git-rc=$?"; /usr/bin/grep -v '^??' /tmp/188-09-st.txt | wc -l</automated>
    <fails_when>`git-rc=` is non-zero (a failed status call writes an empty file and would read as a pass), or the printed count is anything other than 0 after the final commit — a tracked file was modified and not committed</fails_when>
  </verify>
  <done>The three folded planning items are settled with the distinctions stated, no backlog item is filed, and the meta tree is clean.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the requirement ledger → the milestone audit | the audit reads dispositions, not phase history; a wrong word there is a permanently wrong record |
| the verdict note → a future reader | the note is the only surviving record of what several deleted files guarded |
| hand-edited roadmap → the automated roadmap verbs | a positional-overwrite verb run against this phase destroys the phase name and its requirement identifiers |
| the planning configuration → the phase-transition gate | the configuration is a verification-covered file; a side-effect prune reads as a stale change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-188-38 | Repudiation | a retired requirement recorded as Complete | high | mitigate | The plan carries an explicit prohibition; a verify leg asserts zero checked TOOLS boxes and at least five retirement dispositions; and the disposition word carries a cause in every row, so the audit reads the reason and not just the verdict. |
| T-188-39 | Tampering | a roadmap verb overwriting the phase's dependency table positionally | high | mitigate | The context block forbids every state-writing verb for this phase by name; all edits are by hand and scoped; a diff-size leg fails if the change reaches beyond this phase's section. |
| T-188-40 | Tampering | the sub-repository list pruned as a side effect, blocking the transition | high | mitigate | A verify leg asserts both of the entries that are routinely pruned are still present, reusing the exact check the previous phase used. |
| T-188-41 | Repudiation | a disclosed cost becoming invisible after the phase closes | high | mitigate | The verdict note states all six costs in one place as losses, quotes the deleted prose verbatim, and a verify leg requires every retired item to be named in it. |
| T-188-42 | Tampering | a successor guard authored after the fact | medium | mitigate | A verify leg asserts the meta repository still has zero workflow files and that no workflow other than the three this phase edits was touched in either sub-repo; the note itself states that naming a gap and filing one are different acts. |
| T-188-SC | Tampering | package-manager installs | low | accept | This plan touches only markdown under the planning directory and installs nothing. No package-legitimacy audit is required, and none is claimed. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:**

| Artifact | What it carries |
|---|---|
| `.planning/notes/host-tools-retirement.md` | the verdict; one line per retired item with what it guarded and why it went; six disclosed costs; the deleted regression-guard prose quoted verbatim; the coverage-matrix checker's last measured behaviour; the newly orphaned baseline data file; and the explicit statement that no successor guard and no backlog item are filed |
| a completed-directory copy of the orphaned-host-tools todo | the judgment-versus-reference-count distinction |

**Destroyed by this plan (blast radius):**

| Destroyed | Consequence |
|---|---|
| the seven Pending dispositions in the traceability table | replaced by five retirements with cause, one placement, one completion |
| the stale tools-half figure in the provenance todo | replaced by the measured live value; the tests half stays open |
| the dormant state of the gate-expiry seed | marked resolved; there is no gate family left to expire |
| the pending status of the orphaned-host-tools todo | closed, with the distinction its own retraction implies |

## Probe Coverage (this plan's share)

| Requirement | Probe category | Disposition | Where it landed |
|---|---|---|---|
| TOOLS-02 | adjacency | resolved (explicit) | truth 4 — the convention and its enforcer are both absent rather than one surviving the other; a half-present declaration layer is exactly the merge-or-separate case this row asks about |
| TOOLS-02 | empty | resolved (explicit) | truth 4 — with the declaration set empty there is no gate for it to redden, because no check asserting a declaration exists |
| TOOLS-02 | ordering | resolved (explicit) | truth 1 — retired and built requirements stay in a stable, distinguishable order in the ledger: the disposition word carries the distinction and the checkbox state preserves it |

<verification>
All three tasks' automated legs in order, then two read-backs. First, read the verdict note end to
end and confirm it reads as a record a stranger could use — that each cost is stated as a loss rather
than as a justified decision, which is the difference between a record and an apology. Second, read
the seven amended success criteria and confirm each one's original text is still legible beneath its
amendment; a criterion silently rewritten to match what happened is the failure this shape prevents.
</verification>

<success_criteria>
1. A verdict note exists in the established shape, naming every retired item and all six disclosed costs.
2. Five requirements read RETIRED with cause, one satisfied by placement, one Complete; no TOOLS checkbox is checked.
3. All seven success criteria and the not-in-scope paragraph are amended in place with the conflict quoted.
4. The roadmap diff is confined to this phase's section; no state-writing verb was run.
5. The planning configuration still lists all four sub-repositories.
6. No successor guard and no backlog item exist for any gap this phase named.
7. The three folded planning items are settled with their distinctions stated.
</success_criteria>


<output>
Create `.planning/phases/188-the-tools-directory/188-09-SUMMARY.md` when done. Record: the seven
final disposition strings verbatim; the eight amended roadmap passages by criterion number; the
measured live citation remainder carried into the provenance todo; and the confirmation that zero
successor guards and zero backlog items were filed. This is also the plan that must record the
phase-level totals — the measured deleted line count across all three repositories, against
CONTEXT.md's estimate — for the milestone audit.
</output>


