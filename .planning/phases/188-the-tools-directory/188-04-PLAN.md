---
phase: 188-the-tools-directory
plan: 04
type: execute
wave: 4
depends_on: ["188-03"]
commits_in_repo: firestarter_app (submodule; executors commit INSIDE it on the milestone branch)
autonomous: true
requirements: [TOOLS-03]
files_modified:
  - firestarter_app/tests/report_shape_renderer.py
  - firestarter_app/tests/devtest_handler_names.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_op_registration_parity.py
  - firestarter_app/tests/fixtures/reports/
  - firestarter_app/tools/check_devtest_orchestrator.py
  - firestarter_app/tools/check_dispatch.py
  - firestarter_app/tools/check_is_memory_cmd_no_ifdef.py
  - firestarter_app/tools/check_mypy_watermark.py
  - firestarter_app/tools/check_no_community_support_status_write.py
  - firestarter_app/tools/check_no_exists_proxy.py
  - firestarter_app/tools/check_no_log_in_sdp_window.py
  - firestarter_app/tools/check_protection_readability_invariants.py
  - firestarter_app/tools/check_sdp_capability_invariants.py
  - firestarter_app/tests/test_check_devtest_orchestrator.py
  - firestarter_app/tests/test_check_dispatch_invariants.py
  - firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py
  - firestarter_app/tests/test_check_mypy_watermark.py
  - firestarter_app/tests/test_check_no_community_support_status_write.py
  - firestarter_app/tests/test_check_no_exists_proxy.py
  - firestarter_app/tests/test_check_no_log_in_sdp_window.py
  - firestarter_app/tests/test_check_protection_readability.py
  - firestarter_app/tests/test_check_sdp_capability.py
  - firestarter_app/tests/test_lock_status_class_partition.py
  - firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h
  - firestarter_app/tests/fixtures/planted_log_in_window.cpp
  - firestarter_app/tests/fixtures/planted_no_exists_proxy.py
  - firestarter_app/tests/fixtures/planted_permit_by_default.py
  - firestarter_app/tests/fixtures/planted_widenable_allowset.py
  - firestarter_app/tests/fixtures/planted_protection_widenable_tokenset.py
  - firestarter_app/tests/fixtures/planted_protection_permit_by_default.py
  - firestarter_app/tests/scan_paths.py
  - firestarter_app/tests/test_scan_paths_resolve.py
  - firestarter_app/.github/workflows/ci.yml
  - firestarter_app/CLAUDE.md

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Zero files matching the retired gate-family naming pattern remain under firestarter_app/tools/ — the family is retired whole, with no exceptions and no survivor kept back for being load-bearing."
    - "The mypy step and the script it invokes leave CI in the same commit, so host CI is never red between two commits of this phase; nothing replaces the step — not a plain mypy step, not an inline error count."
    - "The 12 V-on-a-part-with-no-VPP-pin hazard description survives its retired guard: the hazard paragraph in firestarter_app/CLAUDE.md stays, and only the sentences claiming a live regression guard are removed."
    - "The nineteen committed report snapshots differ from their pre-phase state in exactly one JSON key each — the generator-name banner — and each one still byte-matches a fresh regeneration from the relocated renderer."
    - "The fail-closed scan-path resolver and the tools it indexes disappear in the same commit; the resolver never spends a commit boundary pointing at a file that is gone."
  artifacts:
    - path: "firestarter_app/tests/report_shape_renderer.py"
      provides: "The single normaliser + deterministic renderer for the report-shape corpus, relocated out of the retired snapshot tool"
      exports: ["render_shape", "normalise_snapshot"]
    - path: "firestarter_app/tests/devtest_handler_names.py"
      provides: "The dev-test handler-function allow-list, relocated out of the retired orchestrator gate"
      exports: ["_HANDLER_FUNCTION_NAMES"]
  key_links:
    - from: "firestarter_app/tests/test_blast_radius_invariance.py"
      to: "firestarter_app/tests/report_shape_renderer.py"
      via: "both parametrized sites import render_shape from the relocated module"
      pattern: "from tests.report_shape_renderer import render_shape"
    - from: "firestarter_app/tests/fixtures/reports/*.json"
      to: "firestarter_app/tests/report_shape_renderer.py"
      via: "the _generated_by banner key names the module that produced them"
      pattern: "tests/report_shape_renderer.py"
    - from: "firestarter_app/tests/test_op_registration_parity.py"
      to: "firestarter_app/tests/devtest_handler_names.py"
      via: "the non-registry locator resolves the constant in the relocated module"
      pattern: "devtest_handler_names"
  prohibitions:
    - requirement_id: TOOLS-03
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT delete the electrical-hazard description in firestarter_app/CLAUDE.md along with the retired guard that referenced it — the paragraph explaining that 12 V on a pin which is not VPP is a hardware-damage path across roughly 23 chips stays in full; only the sentences asserting that a live regression guard enforces it come out."
---

<objective>
Retire the remaining nine gates of the checker family whole — with their tests, their planted-violation
fixtures, the CI step that named one of them, the fail-closed scan-path resolver that indexed six of
them, and the prose in two markdown files that told future readers a guard was live — after first
relocating the last two orphaned symbols so the suite never loses its signal.

Purpose: D-01 retires all ten `check_*.py` gates, no exceptions, after the operator was shown that
one of them is the CI-named type-error floor and another was extended eight days earlier. D-02 takes
mypy out of CI entirely with nothing in its place. D-13 deletes the scan-path pair outright. Each of
those has a same-commit constraint the others do not: the CI step dies with the script it invokes or
CI is red between commits; the resolver fails closed the moment any indexed tool disappears. They
therefore land together, in one commit, after the repairs.

Output: `firestarter_app/tools/` with no checker family left in it, a CI file whose header no longer
describes a step it does not run, a suite that is green, and two more relocated symbols.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/188-the-tools-directory/188-CONTEXT.md
@.planning/phases/188-the-tools-directory/188-RESEARCH.md
@.planning/phases/188-the-tools-directory/188-PATTERNS.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md

**`/workspaces/CLAUDE.md` §"Source code comments — hard rule" binds every edit here.** Delete
comments; never author one. A carried comment loses its process identifiers and any clause naming a
deleted file, by deletion rather than rewriting.

**Commit inside the `firestarter_app` submodule**, on the `v1.37`-slug branch.

**`/usr/bin/grep` or `git grep` only** — the bare devcontainer `grep` is ugrep, honors `.gitignore`,
and under-scans silently. Pass `-o addopts=""` to pytest when the count line is the measurement.

**Re-verify the CI line ranges before editing.** The ranges below were confirmed live on 2026-09-12,
but a single commit to the repo moves them. Locate each step by its `- name:` text, not by line
number.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Relocate the report-shape renderer and the handler allow-list, and re-banner the nineteen snapshots</name>
  <files>firestarter_app/tests/report_shape_renderer.py (created), firestarter_app/tests/devtest_handler_names.py (created), firestarter_app/tests/test_blast_radius_invariance.py, firestarter_app/tests/test_op_registration_parity.py, firestarter_app/tests/fixtures/reports/ (all 19 JSON files, banner key only)</files>
  <read_first>
    - `firestarter_app/tools/snapshot_report_shapes.py` lines 1-110 in full — the module docstring, the import block (note it prepends the app root to `sys.path` and then imports `EpromDatabase`, `build_db_diff`, and the fixture shape builders), the four module constants including the generator-name banner, the module-level database instance, `normalise_snapshot()`, and `render_shape()`. Everything below `_build_argparser()` is the CLI half and does not move.
    - `firestarter_app/tests/test_blast_radius_invariance.py` lines 745-835 — both `render_shape` import sites, the banner assertion, the drift-failure message that names the tool, and the second parametrized sweep.
    - `firestarter_app/tools/check_devtest_orchestrator.py` lines 138-168 — the allow-list constant and the comment block above it, whose last clause names the gate's own test file.
    - `firestarter_app/tests/test_op_registration_parity.py` lines 105-120 and 355-375 — the module-level import and the non-registry locator entry.
    - `firestarter_app/tests/fixtures/reports/` — list it; confirm the file count before and after.
  </read_first>
  <action>
    Create `firestarter_app/tests/report_shape_renderer.py` carrying, moved from the snapshot tool:
    `normalise_snapshot()`, `render_shape()`, the timestamp sentinel, the duration sentinel, the
    generator-name banner constant, the module-level `EpromDatabase(skip_local_override=True)`
    instance, and the imports those need. The module now lives inside the `tests` package, so it does
    not need the app-root `sys.path` prepend the tool carried — drop it and import directly. Set the
    banner constant to this module's own path, `tests/report_shape_renderer.py`: the banner is the
    do-not-edit marker in a JSON file that has no comment syntax, and a banner naming a file that no
    longer exists is a lie in committed data. Carry `normalise_snapshot`'s docstring, which explains
    why the duration field is normalised and why an already-`None` duration is left alone — strip only
    its phase and plan identifiers. Do not carry `_build_argparser`, `main`, the `--check` mode, the
    target-writing code or the exit-code block.

    Create `firestarter_app/tests/devtest_handler_names.py` carrying `_HANDLER_FUNCTION_NAMES` and the
    comment above it explaining why the allow-list is not merely documentation — that a violating
    helper in a function not listed there passes the gate while the identical violation inside the
    handler trips it. The final clause of that comment names the gate's own test file as the thing
    making the invariant permanent; that file is deleted in Task 2, so delete the clause.

    Repair both consumers. In `test_blast_radius_invariance.py`: repoint both `render_shape` imports at
    the relocated module, update the banner assertion to the new value, and rewrite the drift-failure
    message so it stops instructing the reader to re-run a deleted script — say instead that the
    snapshots are regenerated from the relocated renderer. In `test_op_registration_parity.py`: replace
    the module-level import of the orchestrator gate with one of the relocated module and repoint the
    non-registry locator's first argument at it; the locator's own explanatory string stays.

    Re-banner the nineteen committed snapshots under `tests/fixtures/reports/` by regenerating each one
    through the relocated `render_shape`, with a one-off inline python invocation. Do not author a
    replacement snapshot tool, a script, a CI step or a make target — D-23 forbids any successor, and
    the drift test that already exists is the oracle proving the regeneration is correct.

    Do not delete the snapshot tool or the orchestrator gate in this task. Both stay until Task 2, so
    this commit leaves a green suite.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/report_shape_renderer.py` exports `render_shape` and `normalise_snapshot`, defines no `main`, and contains no argparse.
    - `firestarter_app/tests/devtest_handler_names.py` exports `_HANDLER_FUNCTION_NAMES` with membership identical, as a set, to the gate's copy.
    - All nineteen files under `tests/fixtures/reports/` carry the new banner value and none carries the old one.
    - The diff over `tests/fixtures/reports/` touches exactly one line per file — the banner key. Any other changed line means a builder or serializer moved, which is not this phase's business.
    - `test_blast_radius_invariance.py` passes in full, including both parametrized sweeps over the registered shape ids and the fresh-regeneration byte comparison.
    - `test_op_registration_parity.py` passes in full.
    - No comment line in either new module carries a phase number, a `D-NN`, a `GATE-NN`, a `.planning/` path, or the name of a file this phase deletes.
    - The whole suite passes and both ruff legs are green.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import sys; sys.path.insert(0,'tools')
import check_devtest_orchestrator as old
import tests.devtest_handler_names as new
assert set(old._HANDLER_FUNCTION_NAMES) == set(new._HANDLER_FUNCTION_NAMES), 'allow-list drift'
print('handler-names', len(new._HANDLER_FUNCTION_NAMES))
"</automated>
    <fails_when>non-zero exit, an `AssertionError` naming allow-list drift, or a printed `handler-names` count of 0</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -rlF -e 'tests/report_shape_renderer.py' tests/fixtures/reports/ | wc -l; /usr/bin/grep -rlF -e 'tools/snapshot_report_shapes.py' tests/fixtures/reports/ | wc -l</automated>
    <fails_when>the first count is anything other than 19, or the second is anything other than 0</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff --numstat -- tests/fixtures/reports/ | awk '{a+=$1; d+=$2; n+=1} END {print "files="n, "added="a, "deleted="d}'</automated>
    <fails_when>`files` is not 19, or `added` is not 19, or `deleted` is not 19 — any other figure means something beyond the one banner key moved in the snapshots</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_blast_radius_invariance.py tests/test_op_registration_parity.py -o addopts="" -q 2>&1 | tail -4</automated>
    <fails_when>non-zero exit, any `error`/`ERROR` line, or a reported passed count below 113</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -nE '[Pp]hase[ -]?[0-9]|\bD-[0-9]+|GATE-[0-9]|\.planning' tests/report_shape_renderer.py tests/devtest_handler_names.py | wc -l</automated>
    <fails_when>the count is anything other than 0</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -5 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit, any `error`/`ERROR` line in the collection summary, `Found N errors.`, or `N files would be reformatted`</fails_when>
  </verify>
  <done>Both remaining orphaned symbols live under `tests/`, both consumers are repaired, the nineteen snapshots carry an honest banner and still byte-match a fresh regeneration, and the suite is green with both generators still present.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the nine remaining gates, the CI step, the scan-path resolver and the guard prose — one commit</name>
  <reversibility rating="costly">D-01 and D-02 together remove ten gates, 3,537 lines of gate tests, and the CI-named type-error floor. Re-adding the floor later means first re-establishing a watermark against whatever the error count has drifted to, which is the work the original gate existed to prevent; and restoring the scan-path inventory means reading each resolver's source again, which is what building it the first time required.</reversibility>
  <files>firestarter_app/tools/check_devtest_orchestrator.py, check_dispatch.py, check_is_memory_cmd_no_ifdef.py, check_mypy_watermark.py, check_no_community_support_status_write.py, check_no_exists_proxy.py, check_no_log_in_sdp_window.py, check_protection_readability_invariants.py, check_sdp_capability_invariants.py (all deleted); firestarter_app/tests/test_check_devtest_orchestrator.py, test_check_dispatch_invariants.py, test_check_is_memory_cmd_no_ifdef.py, test_check_mypy_watermark.py, test_check_no_community_support_status_write.py, test_check_no_exists_proxy.py, test_check_no_log_in_sdp_window.py, test_check_protection_readability.py, test_check_sdp_capability.py (all deleted); firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h, planted_log_in_window.cpp, planted_no_exists_proxy.py, planted_permit_by_default.py, planted_widenable_allowset.py, planted_protection_widenable_tokenset.py, planted_protection_permit_by_default.py (all deleted); firestarter_app/tests/scan_paths.py, firestarter_app/tests/test_scan_paths_resolve.py (deleted); firestarter_app/tests/test_lock_status_class_partition.py, firestarter_app/.github/workflows/ci.yml, firestarter_app/CLAUDE.md (edited)</files>
  <read_first>
    - `firestarter_app/.github/workflows/ci.yml` — read lines 1-10 and 50-100. Line 3 is a header comment enumerating the gate steps; the mypy step and the two catalog steps that STAY are in the second window.
    - `firestarter_app/CLAUDE.md` lines 110-130 — the hazard paragraph and, after it, the `Regression guard:` sentences. The boundary between the two is the thing this task must get exactly right.
    - `firestarter_app/tests/test_lock_status_class_partition.py` lines 560-650 — the AST leg, the subprocess helper, and the two tests that route a planted fixture through the gate's environment-override seam.
    - `firestarter_app/tests/scan_paths.py` lines 90-250 — the resolver table, so you can see which eight of the eleven indexed tools this phase removes.
    - Each of the seven planted-violation fixtures named below, before deleting it. A fixture with a surviving consumer stays; confirm each one's consumers with `git grep -ln` over `tests/ tools/ .github/ pyproject.toml` rather than trusting this list.
  </read_first>
  <action>
    In one commit, delete the nine remaining gates and the nine test files that exercise them:
    the dev-test orchestrator gate, the dispatch gate, the ifdef-in-predicate gate, the mypy
    watermark gate, the community-support-status-write gate, the exists-proxy gate, the
    log-in-SDP-window gate, the protection-readability-invariants gate and the SDP-capability-
    invariants gate. Three of the nine test files do not follow the `test_<gate>.py` convention (D-03) —
    the dispatch gate's test is named for invariants, and the protection-readability and
    SDP-capability tests drop the `_invariants` suffix. Do not assume the naming; confirm each
    mapping by reading the test file's own imports before deleting it.

    Delete the seven planted-violation fixtures whose only consumers are in that set: the
    ifdef-in-predicate header, the log-in-window source, the exists-proxy module, the
    permit-by-default module, the widenable-allowset module, the protection-widenable-tokenset
    module, and — after the repair below — the protection-permit-by-default module. Keep the
    unparsable fixture, whose live consumer is a `pyproject.toml` rationale, and keep the
    part-number-delta fixture, which a surviving test loads directly.

    Repair `tests/test_lock_status_class_partition.py` in the same commit: delete the subprocess
    helper and the two tests that route a planted fixture through the retired gate, since their
    subject ceases to exist; delete the clause in the surviving AST test's docstring that names
    that pair as the thing making it non-decorative, because it becomes false. Remove any import
    left unused by the deletion — `ruff check` will name them. Record in the SUMMARY that the
    surviving AST leg is now decorative, as a disclosed cost of D-01, so plan 188-09 can carry it
    into the verdict note.

    Delete `tests/scan_paths.py` and `tests/test_scan_paths_resolve.py` in the same commit, per D-13. The
    resolver fails closed when an entry names a missing file, so it goes red the moment the first
    indexed tool disappears; deleting the pair alongside them is what keeps every commit boundary
    green. No partial edit of the table is needed — the whole module goes.

    Delete the mypy step from `firestarter_app/.github/workflows/ci.yml` — its `- name:` line, its
    `run:` line, and the single blank separator line before the next step, leaving no double blank.
    Nothing replaces it: not a plain mypy invocation, not an inline error count. Amend the header
    comment at the top of the same file, which enumerates the gate steps and names the mypy
    watermark among them; it becomes false, so remove that item from the enumeration. Leave the two
    catalog steps, both ruff steps, the pytest step and the smoke test exactly as they are.

    Edit `firestarter_app/CLAUDE.md`: remove only the sentences beginning `Regression guard:` and
    the two lettered clauses describing what that gate asserted. The hazard paragraph above them —
    explaining that a chip on a pinout with no VPP pin must never route to the EPROM configure path,
    and naming the manufacturers and the seven electrically-erasable exceptions — stays in full. It
    is the first thing an agent reads about this hazard and it outlives its guard.

    Do not delete `tools/baseline/dispatch_baseline.json`: it becomes orphaned data with zero
    consumers, but D-14 puts non-script files under `tools/` out of scope. Note it for the verdict
    note instead.
  </action>
  <acceptance_criteria>
    - Zero files matching `tools/check_*.py` remain tracked in `firestarter_app`.
    - Zero files matching `tests/test_check_*.py` remain tracked in `firestarter_app`, across all three naming conventions D-03 names.
    - `tests/scan_paths.py` and `tests/test_scan_paths_resolve.py` are untracked; nothing imports them.
    - `firestarter_app/.github/workflows/ci.yml` contains no mypy step and no mention of a mypy watermark in its header comment, and still contains the catalog validity step, the messages drift gate, both ruff steps, the pytest-with-coverage step and the smoke test.
    - The CI file has no double blank line introduced by the deletion.
    - `firestarter_app/CLAUDE.md` still contains the hazard paragraph naming the manufacturers and the seven electrically-erasable exceptions, and contains no sentence asserting that a regression guard enforces it.
    - `tests/test_lock_status_class_partition.py` collects and passes, with the two gate-seam tests and their helper gone and no unused import left.
    - The whole app suite passes; its collected count equals 188-02's baseline minus exactly the tests in the deleted modules, with no module reporting a collection error.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both pass.
    - `tools/baseline/dispatch_baseline.json` is still tracked.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && git ls-files -- 'tools/check_*.py' 'tests/test_check_*.py' tests/scan_paths.py tests/test_scan_paths_resolve.py > /tmp/188-04-gone.txt; echo "git-rc=$?"; wc -l < /tmp/188-04-gone.txt; git ls-files -- 'tools/*.py' | wc -l</automated>
    <fails_when>`git-rc=` is non-zero (a failed listing would print 0 and read as a pass), or the first count is anything other than 0 (a gate, a gate test or a scan-path module survived), or the second count is 0 (a zero here would mean the whole tools listing failed, so the first zero proved nothing)</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'mypy' .github/workflows/ci.yml; echo "grep-rc=$?"</automated>
    <fails_when>the printed count is anything other than 0 (grep-rc=1 with count 0 is the passing shape) — D-02 removes mypy from CI entirely, including from the header enumeration</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c -e 'Catalog validity check' -e 'ruff lint' -e 'ruff format check' -e 'Run pytest with coverage' -e 'Smoke test' .github/workflows/ci.yml</automated>
    <fails_when>the printed count is below 5 — a step that must stay was deleted with the ones that go</fails_when>
    <automated>cd /workspaces/firestarter_app && awk 'BEGIN{p=0} /^[[:space:]]*$/{if(p){print NR; c++} p=1; next} {p=0} END{print "doubleblanks="c+0}' .github/workflows/ci.yml | tail -1</automated>
    <fails_when>`doubleblanks=` reports anything other than 0</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'Regression guard' CLAUDE.md; echo "grep-rc=$?"; /usr/bin/grep -c 'hardware-damage path' CLAUDE.md</automated>
    <fails_when>the first count is other than 0, or the second is 0 — the second is the hazard sentence that MUST survive its retired guard</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tools/baseline/dispatch_baseline.json | wc -l</automated>
    <fails_when>the count is anything other than 1 — D-14 puts non-script data out of scope</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -5 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit, any `error`/`ERROR` line in the collection summary, `Found N errors.`, or `N files would be reformatted`</fails_when>
  </verify>
  <done>`firestarter_app/tools/` holds no checker family, CI runs no mypy step and says so, the scan-path pair is gone, the hazard description outlives its guard, and the suite is green.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip database record → firmware configure path | the retired dispatch gate was the only automated control stopping a 12 V handler being selected for a part whose pinout has no VPP pin |
| host CI → the published package | the retired mypy watermark was the CI-named type-error floor; after D-02 nothing bounds the error count |
| committed JSON fixture → the drift oracle | the report snapshots' banner key is the only do-not-edit marker a JSON file can carry |
| `firestarter_app/CLAUDE.md` → a future agent | it is the first thing an agent reads about the 12 V hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-188-12 | Information Disclosure (of a hazard) | the 12 V / no-VPP-pin hazard paragraph in `firestarter_app/CLAUDE.md` | high | mitigate | Task 2 removes only the `Regression guard:` sentences; a verify leg requires the hazard sentence to still be present and the guard claim to be absent, and the plan carries an explicit prohibition against deleting the paragraph. |
| T-188-13 | Repudiation | the retired dispatch gate's electrical-safety invariant | high | accept | Disclosed, accepted cost of D-01: the operator was shown that one of the ten is the CI-named type floor and chose "retire the whole family, no exceptions". The invariant's loss is recorded by name in plan 188-09's verdict note alongside the other disclosed costs; D-23 forbids a successor guard for any reason, so no mitigation may be authored here. |
| T-188-14 | Tampering | unbounded type-error drift after the mypy floor leaves CI | high | accept | Disclosed, accepted cost of D-02: the operator was shown that the tree carries roughly 33 mypy errors, that a must-exit-0 step would be red on day one, and that dropping the gate lets the count drift upward unnoticed, and chose "nothing — mypy leaves CI". Recorded in the verdict note; no replacement is authored. |
| T-188-15 | Tampering | a snapshot re-banner silently rewriting report content | medium | mitigate | The numstat leg requires exactly one added and one deleted line per file across exactly nineteen files, and the pre-existing fresh-regeneration drift test byte-compares each snapshot against the relocated renderer's output. |
| T-188-16 | Repudiation | the fail-closed scan-path resolver reporting a false green between commits | medium | mitigate | D-13's pair is deleted in the same commit as the indexed tools, so it never spends a commit boundary pointing at a missing file; the whole-suite leg would show a collection error if it did. |
| T-188-17 | Tampering | host CI red between two commits of this phase | medium | mitigate | D-21(a): the mypy step and the script it invokes are deleted in one commit; a verify leg asserts zero mypy mentions remain and that the five surviving steps are still present. |
| T-188-SC | Tampering | package-manager installs | low | accept | No package is added and no install task exists; the suite runs against the already-installed editable package. No package-legitimacy audit is required, and none is claimed. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:**

| Artifact | Symbols |
|---|---|
| `firestarter_app/tests/report_shape_renderer.py` | `render_shape`, `normalise_snapshot`, the two normalisation sentinels, the generator banner constant |
| `firestarter_app/tests/devtest_handler_names.py` | `_HANDLER_FUNCTION_NAMES` |

**Destroyed by this plan (blast radius):**

| Destroyed | Consequence |
|---|---|
| nine `tools/check_*.py` gates | the checker family ceases to exist; the electrical-safety dispatch invariant, the SDP-window logging invariant, the exists-proxy invariant, the protection-readability invariant, the SDP-capability invariants, the ifdef-in-predicate invariant, the community-support-status-write invariant, the dev-test orchestrator coverage invariant and the mypy type-error floor all lose their only automated control |
| nine `tests/test_check_*.py` modules | 3,537 lines of gate tests, including the planted-violation legs proving each gate had teeth |
| seven planted-violation fixtures | the known-bad subjects those legs used |
| `tests/scan_paths.py` + `tests/test_scan_paths_resolve.py` | the fail-closed cross-repo path inventory, both its eleven tool resolvers and its six cross-repo test paths; the measured defect it guarded — seven host test modules silently flipping from pass to skip on a firmware rename — is no longer guarded |
| the mypy CI step and the header clause naming it | host CI runs ruff and pytest only |
| the `Regression guard:` sentences in `firestarter_app/CLAUDE.md` | the hazard paragraph survives; the claim that a live guard enforces it does not |
| two gate-seam tests and their subprocess helper in `tests/test_lock_status_class_partition.py` | the surviving AST leg in that module becomes decorative — it asserts a rule that nothing now proves can fire |
| `_build_argparser`, `main`, the `--check` mode of the snapshot tool; the scan function and `main` of the diagnostic-claims gate | deliberately NOT relocated |

Newly orphaned but deliberately retained (D-14 scopes this phase to scripts, not data):
`firestarter_app/tools/baseline/dispatch_baseline.json` — 158 KB, zero consumers after this plan.

## Probe Coverage (this plan's share)

| Requirement | Probe category | Disposition | Where it landed |
|---|---|---|---|
| TOOLS-03 | empty | resolved (explicit) | truth 1 — zero files matching the gate-family pattern remain; the empty set is the end state, and the verify leg asserts it as a count rather than inferring it |
| TOOLS-03 | adjacency | resolved (explicit) | truth 3 — the boundary between the hazard description and its guard claim is exactly where two things that touch must separate: one stays, one goes |

<verification>
Both tasks' automated legs in order, then one read-back: open `firestarter_app/CLAUDE.md` around the
hazard paragraph and confirm by eye that the manufacturer list and the seven-exception sentence are
intact and that the paragraph reads coherently without the removed guard sentences. A grep can prove
a string is present; only a reader can tell whether the paragraph still makes sense.
</verification>

<success_criteria>
1. Zero `check_*.py` gates and zero `test_check_*.py` modules remain tracked.
2. The mypy step and the script it invokes leave in the same commit; nothing replaces it; the CI header no longer claims it.
3. The scan-path pair dies in the same commit as the tools it indexed.
4. The 12 V hazard description survives in full; only the guard claim is removed.
5. Nineteen snapshots carry an honest banner and still byte-match a fresh regeneration.
6. The suite collects and passes at every commit in this plan; both ruff legs green.
</success_criteria>

## Gate markers

<!-- planner-discipline-allow: mypy -->
<!-- planner-discipline-allow: Regression guard -->

<output>
Create `.planning/phases/188-the-tools-directory/188-04-SUMMARY.md` when done. Record: the nine
gate-to-test mappings as confirmed by reading, not assumed; the collected-count delta against
188-02's baseline; that the surviving AST leg in the lock-status module is now decorative (a
disclosed D-01 cost for the verdict note); and that `tools/baseline/dispatch_baseline.json` is now
orphaned data retained by D-14. Rationale belongs here and in the commit message — never in a source
comment.
</output>


