---
phase: 188-the-tools-directory
plan: 05
type: execute
wave: 5
depends_on: ["188-01", "188-04"]
commits_in_repo: firestarter_app (submodule; executors commit INSIDE it on the milestone branch)
autonomous: true
requirements: [TOOLS-01, TOOLS-04, TOOLS-06, TOOLS-07]
files_modified:
  - firestarter_app/tools/audit_coverage_matrix.py
  - firestarter_app/tools/diff_db.py
  - firestarter_app/tools/measure_plan_shapes.py
  - firestarter_app/tools/measure_part_number_delta.py
  - firestarter_app/tools/snapshot_report_shapes.py
  - firestarter_app/tools/build_devtest_issue_corpus.py
  - firestarter_app/tests/test_audit_coverage_matrix.py
  - firestarter_app/tests/test_audit_coverage_matrix_default_paths.py
  - firestarter_app/tests/test_diff_db_gate.py
  - firestarter_app/tests/test_plan_shapes_drift.py
  - firestarter_app/tests/test_part_number_delta_drift.py
  - firestarter_app/tests/fixtures/plan_shapes.json
  - firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md
  - firestarter_app/tests/test_numeric_schema_source_scan.py
  - firestarter_app/pyproject.toml
  - firestarter_app/tools/ci_parity.sh
  - firestarter_app/tools/ci_replica_venv.sh
  - firestarter_app/tools/derive_sdp_partition.py
  - firestarter_app/tools/catalog/codegen_vectors.py
  - firestarter_app/tools/catalog/frame-vectors.toml
  - firestarter_app/firestarter/frame_vectors.py
  - firestarter_app/tests/test_frame_vectors.py
  - firestarter_app/.github/workflows/ci.yml

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the six GSD-process tools is placed by name and none is left sitting in the published package repo: five are deleted outright and the sixth is deleted here only because plan 188-01 already landed its relocated copy in the skill that consumes it."
    - "The host half of the frame-vector apparatus leaves whole — generator, catalog, generated module and consuming test — with no fragment kept back under any name, and the two CI steps that invoked the generator leave in the same commit so host CI is never red between commits."
    - "firestarter_app/tools/ ends at exactly the six scripts D-14 names, and the wheel no longer ships the frame-vector module."
    - "A tool whose deletion dissolves a requirement leaves the requirement's last measured state on the record rather than silently taking it away: the coverage-matrix checker's literal behaviour is captured before the tool is removed."
  artifacts:
    - path: "firestarter_app/tools"
      provides: "Exactly six surviving scripts: the database builder, the catalog code generator, the SDP bus-config generator, the validation-header generator, the dev-test issue parser and the test-image generator"
  key_links:
    - from: "firestarter_app/.github/workflows/ci.yml"
      to: "firestarter_app/tools/catalog/codegen.py"
      via: "the two surviving catalog steps, untouched by this plan"
      pattern: "tools/catalog/codegen.py"
    - from: ".claude/skills/devtest-rootcause/scripts/diff_db.py"
      to: "firestarter_app/tools/baseline/chip_database.baseline.json"
      via: "the relocated copy from plan 188-01 reads the baseline that stays in this repo"
      pattern: "FIRESTARTER_BASELINE_FILE"
  prohibitions:
    - requirement_id: TOOLS-06
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT quietly preserve a fragment of the frame-vector apparatus under any name — no generator, no catalog, no generated module, no consuming test, and no re-homed helper carrying its encode or decode logic survives on the host side. D-08 removes the only mechanism proving host and firmware agree on the same wire bytes, and the operator was shown that cost and chose it; a preserved fragment would misrepresent the decision as half-taken."
---

<objective>
Take the GSD-process tooling, the two CI mirrors, the one orphan and the host half of the
frame-vector apparatus out of the published package repo, leaving `firestarter_app/tools/` at
exactly the six scripts that have a named automated consumer.

Purpose: TOOLS-04 asks that each GSD-process tool be placed by name — stays with a product-facing
rationale, moves to the meta repo, or is retired. D-04 places all six: five retired, one relocated
(which plan 188-01 has already landed). D-06 dissolves TOOLS-01 and TOOLS-07 with the coverage-matrix
tool. D-07 retires both CI mirrors, D-15 the one tool with no test and no code reference anywhere,
and D-08 the frame-vector apparatus on both sides — the host side here. D-09's two CI steps leave in
the same commit as the generator they invoke, because a step naming a deleted script turns host CI
red on every pull request.

Output: six scripts left under `tools/`, a wheel one module lighter, and a CI file running only the
gates that still have a script behind them.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/188-the-tools-directory/188-CONTEXT.md
@.planning/phases/188-the-tools-directory/188-RESEARCH.md
@.planning/phases/188-the-tools-directory/188-PATTERNS.md
@/workspaces/CLAUDE.md

**`/workspaces/CLAUDE.md` §"Source code comments — hard rule" binds every edit here.** Delete
comments; never author one.

**Commit inside the `firestarter_app` submodule**, on the `v1.37`-slug branch.

**`/usr/bin/grep` or `git grep` only.** Pass `-o addopts=""` to pytest when the count line matters.

**This plan may not start before plan 188-01 has landed.** Deleting `tools/diff_db.py` before the
skill owns its copy breaks the documented root-cause procedure — D-21 makes that the phase's first
ordering constraint. Assert it rather than assuming it; the precondition on Task 1 is there for that.

**Re-verify the CI line ranges by step name before editing** — they were confirmed live on
2026-09-12 and a single commit moves them.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Retire the six GSD-process tools, their tests and their orphaned data</name>
  <precondition>`.claude/skills/devtest-rootcause/scripts/diff_db.py` exists in the meta repo and is tracked by git — plan 188-01's relocated copy. Deleting the host original before that copy exists breaks the skill's documented root-cause procedure (D-05/D-21).</precondition>
  <reversibility rating="costly">The coverage-matrix tool mints defect-coverage identifiers into a committed ledger; the ledger and the generated matrix stay in `.planning/` as frozen artifacts, but no further identifiers can be minted without restoring the tool.</reversibility>
  <files>firestarter_app/tools/audit_coverage_matrix.py, diff_db.py, measure_plan_shapes.py, measure_part_number_delta.py, snapshot_report_shapes.py, build_devtest_issue_corpus.py (all deleted); firestarter_app/tests/test_audit_coverage_matrix.py, test_audit_coverage_matrix_default_paths.py, test_diff_db_gate.py, test_plan_shapes_drift.py, test_part_number_delta_drift.py (all deleted); firestarter_app/tests/fixtures/plan_shapes.json, firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md (deleted); firestarter_app/tests/test_numeric_schema_source_scan.py, firestarter_app/pyproject.toml (edited)</files>
  <read_first>
    - `firestarter_app/tests/test_numeric_schema_source_scan.py` lines 15-30, 65-90 and 165-185 — the module docstring's enumeration of its two tests, the path constant naming the coverage-matrix tool, and the whole-file scan leg that reads it. That leg is the one repair this task makes.
    - `firestarter_app/pyproject.toml` lines 70-80, 105-125, 140-160 and 190-200 — five comment blocks that name tools this phase deletes. Read line 74 especially closely: it is the rationale for a live version upper bound, not decoration.
    - `firestarter_app/tests/golden/` — list it. Confirm what remains after the coverage matrix is removed, so you can tell whether the exclude glob in `pyproject.toml` stays valid.
    - `firestarter_app/tests/test_devtest_issue_corpus.py` — read enough to confirm it reads a committed artifact and does NOT invoke its generator. It survives; do not delete it with the generator.
  </read_first>
  <action>
    Before deleting anything, capture the coverage-matrix checker's literal current behaviour for the
    record: run it in check mode and record the exit code and the exact stdout and stderr, including
    that both streams are empty. TOOLS-07 asks that the checker exit 0 or that the staleness be
    recorded with its cause; D-06 dissolves the requirement by deleting the tool, and the honest
    discharge is a measurement taken before the tool goes, carried into plan 188-09's verdict note.
    Write it to the SUMMARY, not to a committed evidence file — D-19 puts evidence artifacts out of
    scope for this phase.

    Then delete the six GSD-process tools: the coverage-matrix auditor, the database differ, the
    plan-shape measurer, the part-number-delta measurer, the report-shape snapshotter and the
    dev-test issue-corpus builder. The database differ is deleted here and only here, because plan
    188-01 already placed the skill's own copy; the other five are retired outright.

    Delete their tests in the same commit: the coverage-matrix test and its default-paths sibling,
    the database-differ gate test, the plan-shapes drift test and the part-number-delta drift test.
    The last two invoke their generators by subprocess, so they die with them. Note that the
    database-differ gate test is named by none of the decisions — D-04 says "with their tests" and
    never enumerates it; it is that tool's only test, it invokes the tool, and it goes.

    Delete two now-orphaned data artifacts: the plan-shapes fixture, whose only consumers were the
    measurer and its drift test; and the committed coverage-matrix golden file under `tests/golden/`,
    whose only consumer was the byte-identity test being deleted. Keep the part-number-delta fixture —
    a surviving test loads it directly, so it stays as a frozen artifact whose generator is gone.
    Keep the dev-test issue-corpus fixture and the module that reads it: that test reads the committed
    artifact and does not invoke its generator.

    Repair `tests/test_numeric_schema_source_scan.py`: its whole-file scan leg reads the
    coverage-matrix tool by path and would raise a file-not-found error. Delete that leg, its path
    constant and its distinctive-identifier constant if nothing else uses them, and delete the clause
    in the module docstring that enumerates it. The module's other leg is unaffected.

    Amend the five stale comment blocks in `pyproject.toml`. In each, delete the clause naming a tool
    this phase deletes and keep the surviving rationale. At line 74 that means keeping the version
    upper bound and its constraint text and deleting only the reason clause that cites the retired
    watermark gate — do not change any version specifier, and do not remove any dependency. At the
    lint-exclude block, keep the exclude entries and delete justification (1), which asserts a
    byte-identity relationship between two files that will both be gone.
  </action>
  <acceptance_criteria>
    - The six process-tool paths are untracked in `firestarter_app`, and `.claude/skills/devtest-rootcause/scripts/diff_db.py` is tracked in the meta repo.
    - The five named test files are untracked; `tests/test_devtest_issue_corpus.py` and `tests/test_canonical_part_number.py` are still tracked and still pass.
    - `tests/fixtures/part_number_delta.json` and `tests/fixtures/devtest_issue_corpus.json` are still tracked; `tests/fixtures/plan_shapes.json` and `tests/golden/v1.3-COVERAGE-MATRIX.md` are not.
    - `tests/golden/` still holds its seven other files, so `pyproject.toml`'s exclude glob still resolves to a real directory.
    - `pyproject.toml` names none of the deleted tools anywhere, and its dependency specifiers are byte-identical to their pre-task values.
    - `tests/test_numeric_schema_source_scan.py` collects and passes with its remaining leg.
    - The whole suite collects and passes; both ruff legs on `firestarter/ tests/` are green.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && git -C . ls-files -- .claude/skills/devtest-rootcause/scripts/diff_db.py | wc -l</automated>
    <fails_when>the count is 0 — plan 188-01's relocated copy is absent, so this plan's precondition is unmet and Task 1 must halt rather than delete the original</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tools/audit_coverage_matrix.py tools/diff_db.py tools/measure_plan_shapes.py tools/measure_part_number_delta.py tools/snapshot_report_shapes.py tools/build_devtest_issue_corpus.py tests/test_audit_coverage_matrix.py tests/test_audit_coverage_matrix_default_paths.py tests/test_diff_db_gate.py tests/test_plan_shapes_drift.py tests/test_part_number_delta_drift.py tests/fixtures/plan_shapes.json tests/golden/v1.3-COVERAGE-MATRIX.md > /tmp/188-05-gone.txt; echo "git-rc=$?"; wc -l < /tmp/188-05-gone.txt</automated>
    <fails_when>`git-rc=` is non-zero (a failed listing would print 0 and read as a pass), or the line count is anything other than 0 — a deletion target survived</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tests/fixtures/part_number_delta.json tests/fixtures/devtest_issue_corpus.json tests/test_devtest_issue_corpus.py tests/test_canonical_part_number.py | wc -l; git ls-files -- tests/golden/ | wc -l</automated>
    <fails_when>the first count is anything other than 4 (a surviving consumer's data or module was deleted), or the second is anything other than 7</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -cE 'audit_coverage_matrix|check_mypy_watermark|check_diagnostic_report_claims' pyproject.toml; echo "grep-rc=$?"</automated>
    <fails_when>the printed count is anything other than 0 (grep-rc=1 with count 0 is the passing shape) — a comment block still names a deleted tool</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff -U0 -- pyproject.toml | /usr/bin/grep -E '^[+-][^+-]' | /usr/bin/grep -cE '(>=|<=|==|~=|<[0-9]|>[0-9])'</automated>
    <fails_when>the printed count is anything other than 0 — a changed line carrying a version specifier means a dependency pin moved, which is outside this phase's scope</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -5 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit, any `error`/`ERROR` line in the collection summary, `Found N errors.`, or `N files would be reformatted`</fails_when>
  </verify>
  <done>All six GSD-process tools are placed by name, their tests and orphaned data are gone, the one surviving consumer is repaired, `pyproject.toml` names no deleted tool and no pin moved, and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the two CI mirrors, the orphan, and the host half of the frame-vector apparatus — with its CI steps, one commit</name>
  <reversibility rating="one-way">D-08 removes the only mechanism proving host and firmware agree on the same wire bytes. Unlike the ten AST gates it is not a zero-findings source checker; it is a live byte-level cross-repo contract test, and once both catalogs are gone there is no record from which to re-derive the agreed vectors. The operator was shown this cost in the option text and chose it; it is recorded here as a disclosed, accepted decision, not an oversight, and no checkpoint re-asks it.</reversibility>
  <files>firestarter_app/tools/ci_parity.sh, tools/ci_replica_venv.sh, tools/derive_sdp_partition.py, tools/catalog/codegen_vectors.py, tools/catalog/frame-vectors.toml, firestarter/frame_vectors.py, tests/test_frame_vectors.py (all deleted); firestarter_app/.github/workflows/ci.yml (edited)</files>
  <read_first>
    - `firestarter_app/.github/workflows/ci.yml` lines 50-100 — read the surviving catalog pair and the vector pair beside it. The vector pair is the exact mirror of the surviving pair with different targets; delete the mirror, change nothing about the twin.
    - `firestarter_app/firestarter/frame_vectors.py` and `firestarter_app/tests/test_frame_vectors.py` — read both headers, so the SUMMARY can record what the contract test actually asserted.
    - `firestarter_app/tests/test_cobs.py` header — confirm by reading that it independently covers the COBS and CRC8 path, so this deletion strands no product coverage. Do not duplicate any of its cases anywhere.
    - `firestarter_app/pyproject.toml` lines 88-101 — the package declaration, to confirm the frame-vector module is not named individually and no packaging edit is needed.
  </read_first>
  <action>
    In one commit, delete the two CI mirror shell scripts (the parity runner and the replica-venv
    builder) and the SDP-partition derivation tool. The replica-venv builder exists solely to build a
    numpy-free interpreter so the mypy leg is trustworthy, and D-02 removed that leg's reason to
    exist; the parity runner's fourth leg is the mypy leg. The partition tool has no test and no code
    reference in any of the three repositories — its only consumer record is planning prose, and D-15
    retires it on operator judgment about value rather than on that evidence.

    In the same commit, delete the host half of the frame-vector apparatus whole: the vector code
    generator under the catalog directory, the vector catalog beside it, the generated module that
    ships in the wheel, and the consuming test. Preserve no fragment — not the encoder, not the
    decoder, not a re-homed helper, not a "just the CRC" copy. The firmware half is plan 188-06's and
    has already landed.

    In the same commit, delete the two vector CI steps: the vector catalog validity check and the
    vector codegen drift gate. Each is a `- name:` line with its `run:` body plus the single blank
    separator line before the next step; leave no double blank. Leave the two catalog steps for the
    message generator exactly as they are — D-10 keeps them, and they are the model the vector pair
    was copied from.

    Make no packaging edit: the package is declared at package level, the frame-vector module is not
    named individually anywhere in the build configuration, and no packaging test names it. Deleting
    it silently shrinks the wheel by one module, which is the intended outcome.

    Author no successor: no replacement wire-contract test, no "lite" vector check, no CI step, no
    backlog-filed guard. D-23 forbids it for any reason, and D-08 records the residual gap as
    accepted.
  </action>
  <acceptance_criteria>
    - The three shell/python retirement targets and the four frame-vector host paths are all untracked in `firestarter_app`.
    - `firestarter_app` contains no tracked file whose name or path contains the vector-catalog or frame-vector tokens.
    - `.github/workflows/ci.yml` contains no vector step and no reference to the vector generator, still contains the catalog validity step and the messages drift gate, and has no double blank line.
    - `pyproject.toml` is byte-unchanged by this task.
    - `tests/test_cobs.py` is still tracked and still passes, and no case from it has been duplicated into another module.
    - The whole suite collects and passes; the coverage total against `--cov=firestarter` is still at or above the 70 percent floor.
    - `ruff check tools/` passes clean — a free acceptance signal, since the six errors it reports today all live in files this phase deletes. This invokes an existing tool with a wider scope; it authors no new gate.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tools/ci_parity.sh tools/ci_replica_venv.sh tools/derive_sdp_partition.py tools/catalog/codegen_vectors.py tools/catalog/frame-vectors.toml firestarter/frame_vectors.py tests/test_frame_vectors.py > /tmp/188-05-fv.txt; echo "git-rc=$?"; wc -l < /tmp/188-05-fv.txt</automated>
    <fails_when>`git-rc=` is non-zero (a failed listing would print 0 and read as a pass), or the line count is anything other than 0</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files | /usr/bin/grep -cE 'codegen_vectors|frame.vectors'; echo "grep-rc=$?"</automated>
    <fails_when>the printed count is anything other than 0 (grep-rc=1 with count 0 is the passing shape) — any surviving path means a fragment of the apparatus was kept back</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'codegen_vectors' .github/workflows/ci.yml; echo "grep-rc=$?"; /usr/bin/grep -c -e 'Catalog validity check' -e 'Codegen drift gate (messages.py)' .github/workflows/ci.yml</automated>
    <fails_when>the first count is other than 0, or the second is below 2 — the vector steps must be gone and the message steps must remain</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain -- pyproject.toml > /tmp/188-05-pp.txt; echo "git-rc=$?"; wc -l < /tmp/188-05-pp.txt</automated>
    <fails_when>`git-rc=` is non-zero (a failed status call prints nothing and would read as a pass), or the line count is anything other than 0 in this task's commit — no packaging edit is required and none may be made</fails_when>
    <automated>cd /workspaces/firestarter_app && ruff check tools/</automated>
    <fails_when>non-zero exit; the output ends `Found N errors.`</fails_when>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70 -o addopts="" -q 2>&1 | tail -6</automated>
    <fails_when>non-zero exit, any `error`/`ERROR` line in the collection summary, or a line reading `FAIL Required test coverage of 70% not reached`</fails_when>
  </verify>
  <done>The two CI mirrors, the orphan and the host half of the frame-vector apparatus are gone with their two CI steps in one commit; the wheel is one module lighter; the coverage floor holds; `ruff check tools/` is clean.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host serial framing ↔ firmware serial framing | the frame-vector catalog was the only artifact asserting both sides encode and decode the same bytes |
| published wheel → end user | deleting a module changes what `pip install firestarter` ships |
| the skill's documented procedure → a future agent | deleting the differ before its relocated copy exists silently breaks a procedure an agent will follow verbatim |
| `pyproject.toml` dependency specifiers → the installed environment | a comment sweep adjacent to version pins can move a pin by accident |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-188-18 | Tampering | host/firmware wire-format divergence after the vector contract is removed | high | accept | Disclosed, accepted cost of D-08. The operator was shown in the option text that this is the only mechanism proving the two sides agree on the same wire bytes and chose it anyway; CONTEXT.md instructs that it not be re-litigated and that no fragment be quietly preserved. The residual gap is named — not filed — in plan 188-09's verdict note, and `/workspaces/CLAUDE.md`'s standing rule that protocol changes keep `serial_comm.py` and `firestarter.cpp` in sync survives as a human discipline. D-23 forbids authoring a successor. |
| T-188-19 | Repudiation | the skill's root-cause procedure pointing at a deleted differ | high | mitigate | Task 1 carries a `<precondition>` asserting the relocated copy is tracked in the meta repo, and its first verify leg fails the task outright if that copy is absent. D-21 orders 188-01 first for this reason. |
| T-188-20 | Tampering | a dependency pin moving during the `pyproject.toml` comment sweep | medium | mitigate | A verify leg greps the task's own diff for any changed line carrying a version specifier and fails if one exists; the action forbids removing a dependency or changing a specifier. |
| T-188-21 | Tampering | host CI red between two commits because a step outlives its script | medium | mitigate | D-21(a): Task 2 deletes the two vector steps in the same commit as the generator, and a verify leg asserts both that the vector references are gone and that the two message steps survive. |
| T-188-22 | Information Disclosure | product coverage stranded by deleting a shipped module | low | accept | The COBS and CRC8 path is independently covered by a surviving test module; the only product-source coverage this deletion removes is the frame-vector module's own seven statements, against fifteen percentage points of headroom over the seventy percent floor. The coverage leg measures it rather than assuming it. |
| T-188-SC | Tampering | package-manager installs | low | accept | No package is added to `pyproject.toml` and no install task exists in this plan; a pin-movement guard is in place precisely so none happens by accident. No package-legitimacy audit is required, and none is claimed. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** nothing. This plan is subtraction only; the one artifact its deletions
depend on — the relocated database differ — was created by plan 188-01.

**Destroyed by this plan (blast radius):**

| Destroyed | Consequence |
|---|---|
| `tools/audit_coverage_matrix.py` (1,952 lines) + its two tests | no further defect-coverage identifiers can be minted; the committed ledger and matrix stay in `.planning/` as frozen artifacts. Dissolves TOOLS-01 and TOOLS-07 outright. |
| `tools/diff_db.py` + `tests/test_diff_db_gate.py` | the host repo no longer carries the differ; the skill owns it. The gate test is named by no decision and dies with its subject. |
| `tools/measure_plan_shapes.py`, `tools/measure_part_number_delta.py` + their drift tests + the plan-shapes fixture | two measurement tools and the subprocess drift tests that invoked them |
| `tools/snapshot_report_shapes.py` | the snapshot CLI and its `--check` drift mode; its renderer half survives in the test tier from plan 188-04 |
| `tools/build_devtest_issue_corpus.py` | the corpus generator; the committed corpus and the test reading it both survive |
| `tests/golden/v1.3-COVERAGE-MATRIX.md` (186 KB) | orphaned the moment its only consumer test goes |
| `tools/ci_parity.sh`, `tools/ci_replica_venv.sh` | local CI mirroring; after this phase CI is ruff plus pytest, which a developer runs directly in two commands |
| `tools/derive_sdp_partition.py` | a tool with no test and no code reference in any repo, retired on operator judgment about value |
| `tools/catalog/codegen_vectors.py`, `tools/catalog/frame-vectors.toml`, `firestarter/frame_vectors.py`, `tests/test_frame_vectors.py` | the host half of the cross-repo wire-byte contract, whole |
| the two vector CI steps in `ci.yml` | host CI stops invoking a generator that no longer exists |
| the whole-file scan leg of `tests/test_numeric_schema_source_scan.py` | one test that read the coverage-matrix tool by path |

## Known structural warning — deliberate, not a defect

`verify.plan-structure` warns that Task 2 is rated `one-way` with no preceding `checkpoint:decision`.
That is intentional and was ruled on before this plan was written. CONTEXT.md records that the
operator was shown this exact cost in the option text and chose it anyway, and instructs that it not
be re-litigated and that no fragment of the apparatus be quietly preserved. Inserting a decision gate
here would re-ask a disclosed, accepted decision. The rating stays on the task so the signal survives
in the record; the gate does not. The same applies to plan 188-06's firmware half.

## Flagged Planner Assumptions

- **The committed coverage-matrix golden file is deleted, not kept.** RESEARCH records this as an
  open judgment (its assumption A4) on the grounds that keeping it is harmless. This plan deletes
  it: its only consumer is the test being deleted in the same commit, and the `pyproject.toml`
  justification naming it becomes false either way. If the operator prefers to retain it as frozen
  data, restoring it is one `git checkout` and changes nothing else in the plan.

## Probe Coverage (this plan's share)

| Requirement | Probe category | Disposition | Where it landed |
|---|---|---|---|
| TOOLS-04 | empty | resolved (explicit) | truth 3 — the end state is an exact set of six, asserted as a listing rather than inferred from what was removed |
| TOOLS-04 | adjacency | resolved (explicit) | truth 1 — the one tool that touches both categories (retired *and* relocated) is disambiguated by ordering: the copy exists before the original goes |
| TOOLS-06 | empty | resolved (explicit) | truth 2 — after deletion there is nothing left to sync, which is exactly how D-11 dissolves the requirement |

<verification>
Both tasks' automated legs in order, then one read-back: list `firestarter_app/tools/` and confirm by
eye that the six survivors D-14 names are present and that the remaining entries are the non-script
data files D-14 puts out of scope. A count alone cannot tell a survivor from a stray.
</verification>

<success_criteria>
1. Six scripts remain under `firestarter_app/tools/`, matching D-14's table exactly.
2. The database differ is deleted only after the skill's copy is tracked; the precondition asserts it.
3. The frame-vector host half leaves whole, with its two CI steps in the same commit and no fragment kept back.
4. `pyproject.toml` names no deleted tool and no dependency specifier moved.
5. The suite is green and the coverage floor holds; `ruff check tools/` is clean.
</success_criteria>

## Gate markers

<!-- planner-discipline-allow: codegen_vectors -->
<!-- planner-discipline-allow: audit_coverage_matrix -->
<!-- planner-discipline-allow: check_mypy_watermark -->
<!-- planner-discipline-allow: check_diagnostic_report_claims -->

<output>
Create `.planning/phases/188-the-tools-directory/188-05-SUMMARY.md` when done. Record: the
coverage-matrix checker's measured exit code and empty output, captured before deletion, for the
verdict note; what the frame-vector contract test actually asserted, for the same note; the measured
coverage total after the deletion; and the final listing of `firestarter_app/tools/`. Rationale goes
here and in the commit message — never in a source comment.
</output>


