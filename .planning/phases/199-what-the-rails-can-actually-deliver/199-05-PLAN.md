---
phase: 199-what-the-rails-can-actually-deliver
plan: 05
type: execute
wave: 3
depends_on: [199-01, 199-02, 199-03, 199-04]
files_modified:
  - firestarter_app/tools/DECODE-NOTES.md
  - .planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md
  - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md
autonomous: true
requirements: [RAIL-01, RAIL-02, RAIL-04, RAIL-05]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "RAIL-02 / D-01 / D-03: `tools/DECODE-NOTES.md` section 10 records that `RURP_VPP_CEILING_MV = 25000` is a regulator's theoretical figure, kept as the refusal gate, and that the deliverable maximum at the socket is a separate, measured, host-side number. No row's `support_status` moved and nothing started refusing."
    - "RAIL-01 / D-03: section 10 carries the measured per-rail figures with their method — a meter at socket pin 1, Rev 2.0, pot at maximum, chip out, both control-register composites named — and the paired ADC figures with the measured discrepancy."
    - "D-05 / D-03: section 10 describes the ADC-versus-meter discrepancy as a COMBINED ADC-plus-P1-switch-path figure that this session cannot decompose, not as an ADC calibration, and cites backlog 999.38's ratiometric figure rather than replacing it."
    - "RAIL-02 / D-03: section 10 carries the 30-row classification table in its post-override state, with a reason per row and the 10 against 20 path split, ordered by VPP path and then by voltage descending and then by manufacturer."
    - "D-06 / D-03: section 10 states that the 20 direct-VPE rows are classified against a pin-1 figure taken through the pin-1 routing bit while they actually receive the rail through a different routing bit to pin 21 — a different physical destination, and therefore an approximation stated as one."
    - "D-12 / D-03: section 10 records the posture change — the VPE-as-VPP flag's own firmware documentation calls it a pure human override set by no database entry, and after this phase it is also set by a host rule, while its resolution order is unchanged. The firmware documentation is NOT edited; the record carries the change."
    - "RAIL-04 / D-09: section 10 records the routing decision as route-it, with the rule stated, and records that the mechanism it drives already shipped end to end before this phase began."
    - "D-18 / C14: section 10 carries the classification test's own limit — it mirrors a firmware table the host cannot read and therefore detects a database-side change only."
    - "D-08 proven end to end: the integer on the bench record's `DELIVERABLE_MAX_DROP_PATH_MV` line, the host gate module's deliverable-maximum constant, and the figure written into section 10 are all the same number, asserted by a three-way equality check rather than by inspection."
    - "RAIL-05 / D-19: `199-GH71-ANSWER.md` exists, holds the five-section draft shape, and its comment body claims only the database correction, credits the reporter, and answers the VPE-as-VPP proposal directly as now automatic above the measured threshold. It advances nothing about causation and it does not claim the part will program."
    - "RAIL-05 / D-19: the draft states that it does not close the report, under an explicit heading, and RAIL-05 is left Pending in the requirements ledger exactly as PULSE-04 and VOLT-04 are."
    - "D-20: gh#71 joins gh#70 and gh#66 on the ONE consolidated held-pending list in `197-GH70-ANSWER.md`. No second list is started. The list's sibling sentence, its what-is-held bullet, its release trigger, its four posting steps and its requirement-status bullet all name three issues after this plan rather than two."
    - "D-20 accuracy: gh#71's held-state line does NOT copy the other two issues' claim that no comment on the issue comes from this project. Two of gh#71's three comments are from the maintainer, so that clause is false for gh#71 and must be written correctly rather than pattern-matched."
    - "Nothing is posted to GitHub. Every read of gh#71 in this plan is read-only, and the draft is held pending the milestone's beta cut."
  artifacts:
    - firestarter_app/tools/DECODE-NOTES.md
    - .planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md
    - .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md
  key_links:
    - "Section 10 is where this phase's record goes because the source cannot carry it. The generator holds a hash-prefixed line directly above the ceiling constant, and editing it stages as an added annotation, which the project's pre-commit check refuses. D-03 is decided by that rule rather than merely constrained by it."
    - "The same rule decides D-12. The firmware's route resolver documents the flag as a pure human override; that sentence no longer describes the only caller, and the only legal move is to record the change elsewhere rather than to edit the sentence."
    - "Section 10 follows sections 8 and 9 beat for beat: a bolded verdict, a sequence of bolded evidence paragraphs each carrying a provenance marker, a named-limits bullet list, a closing sentence naming which plans established which part, and a semicolon-separated sources run. Section 6 is the template for the limits list."
    - "The provenance markers use two forms: a file-and-line citation at a pinned sha for an external pinned file, and a content-addressed prose locator for in-repository files that move. Use the prose form for the generator and the firmware, matching this project's cited-by-content instruction and its line-citation repair rule."
    - "The 198 draft defers to gh#70's list as canonical rather than duplicating the reasoning, and 199 does the same: it carries the pointer and the four steps, and gh#70's file remains the single consolidated list."
  prohibitions:
    - "Nothing is posted to any GitHub issue, and no comment, label or state change is made on gh#71. The draft is held by explicit operator decision and the branch has no upstream, so no version or commit a reader could look up exists yet."
    - "No hash-prefixed line is added to any file under `firestarter_app/` or `firestarter_fw/`. This plan's source-side change is a markdown file under the generator's tooling directory."
    - "The generator's hash-prefixed line above the ceiling constant is not edited, reflowed or deleted. Its content is recorded in section 10 instead."
    - "The firmware's documentation of the VPE-as-VPP flag as a pure human override is not edited. The posture change is recorded in section 10 instead."
    - "No second held-pending list is created. gh#71 joins the one consolidated list in `197-GH70-ANSWER.md`."
    - "The draft's held-state line for gh#71 does not claim that no comment on the issue comes from this project. Two of its three comments are from the maintainer."
    - "The draft does not re-announce the 21 V and 1 ms mismatches or the voltage-cap explanation. The maintainer published all three on the issue on 2026-09-16, and repeating them reads as a project that does not read its own issues."
    - "The draft advances nothing about causation. The last-block boundary hypothesis is the maintainer's standing position across three part sizes and this phase does not add to it."
    - "The draft does not claim the part will program, or that the correction makes it program. Nothing in this phase was bench-proven against a real part of that type."
    - "No figure the bench session did not produce appears in section 10 or in the draft."
    - "RAIL-05 is not marked complete. It is left Pending and carried to the milestone close, exactly as two sibling requirements already are."
    - "`RURP_VPP_CEILING_MV` is not changed, and no row's `support_status` is changed. D-01 holds through the record as it held through the correction."
    - "`.planning/milestones/v1.40-artifacts/` is not created. That directory belongs to the milestone close."
    - "No commit lands on `beta` or `main` in any repository, and nothing is pushed. A push to `beta` in the host repository publishes to PyPI with no path filter."
  flagged_assumptions:
    - "RAIL-04 / unclassified probe row — UNRESOLVED, surfaced rather than resolved. The edge probe classified RAIL-04's requirement text as `unclassified` and its own guidance for that case is manual review. The reviewed reading is that RAIL-04's edge surface is the decision record itself — a decision not to route is explicitly an acceptable outcome, so there is no input boundary to probe — and this plan discharges it by recording the decision and its rule in section 10. The probe row is NOT auto-resolved, because a requirement satisfied by a recorded decision has no testable boundary and pretending otherwise would manufacture a criterion. A verifier that cannot confirm this row should escalate it for human judgement rather than pass it."
    - "RAIL-05 / unclassified probe row — UNRESOLVED, surfaced rather than resolved. The edge probe classified RAIL-05's requirement text as `unclassified`. The reviewed reading is that RAIL-05 has no input boundary at all: it is satisfied by a comment existing on a public issue, which this phase deliberately does not do, because the draft is held pending the milestone's beta cut and there is no version a reader could look up. RAIL-05 is therefore left Pending by design, and its probe row cannot be resolved by anything inside this phase. A verifier must escalate it rather than pass it or dismiss it."
---

<objective>
Write down what was found, and answer the person who reported it — without claiming more than was
proved.

Four plans produced four pieces of evidence: a measured pair of rail figures at socket pin 1, one
corrected database row, a shipped routing rule, and a census that can fail. This plan assembles them
into `tools/DECODE-NOTES.md` section 10, which is where this phase's record has to go, because the
generator carries an annotation directly above the ceiling constant and editing it would stage as an
added annotation the project's own pre-commit check refuses. D-03 is decided by that rule, not merely
constrained by it — and the same rule is why the firmware's description of the VPE-as-VPP flag as a
pure human override, which this phase makes untrue, is recorded here rather than corrected in place.

Then it writes the gh#71 draft and holds it. The draft has exactly one genuinely new thing to say:
`dim20` proposed routing VPE as VPP, and it is now automatic above the measured threshold. It claims
the database correction, credits him, and stops. It does not re-announce the three findings the
maintainer already published on the issue, it advances nothing about the last-block boundary
hypothesis, and it does not claim the part will program — nothing in this phase was proved against a
real part of that type. Everything sits under a heading that says it does not close the report, and
RAIL-05 stays Pending.

**Edge-probe accounting for this plan: 2 of the phase's 8 probe-surfaced items.** RAIL-04's and
RAIL-05's `unclassified` probe rows both stay UNRESOLVED and are surfaced as flagged assumptions in
the frontmatter, with the reviewed reading of each recorded, rather than quietly resolved. Neither
has an input boundary a criterion could be written against. The other six are carried by plans
`199-02` (2), `199-03` (3) and `199-04` (1).

Purpose: satisfy the recording halves of RAIL-01, RAIL-02 and RAIL-04, and produce RAIL-05's held
draft on the terms two sibling phases already established.
Output: one markdown section, one held draft, one consolidated list that now names three issues, and
a three-way proof that the threshold in the record, the threshold in the code and the threshold on
the bench are one number.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-what-the-rails-can-actually-deliver/199-CONTEXT.md
@.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md
@.planning/phases/199-what-the-rails-can-actually-deliver/199-PATTERNS.md
@.planning/phases/199-what-the-rails-can-actually-deliver/199-BENCH-RECORD.md
@.planning/phases/199-what-the-rails-can-actually-deliver/199-REGEN-DIFF.md
@CLAUDE.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DECODE-NOTES section 10 — the record of what the rails deliver</name>
  <files>firestarter_app/tools/DECODE-NOTES.md</files>
  <precondition>`.planning/phases/199-what-the-rails-can-actually-deliver/199-BENCH-RECORD.md` exists and carries all four named figure lines. Plans `199-02` produces it; section 10 cannot be written against figures that do not exist.</precondition>
  <precondition>Plans `199-01`, `199-03` and `199-04` are committed, so the corrected row, the shipped threshold constant and the census counts all exist and can be cited as measured rather than intended.</precondition>
  <precondition>`firestarter_app` HEAD is on branch `v1.40-program-parameter-fidelity`. A commit that reaches `beta` publishes to PyPI.</precondition>
  <read_first>
    - `firestarter_app/tools/DECODE-NOTES.md` section 9 in full — the heading format with its requirement ids, phase number and one-clause verdict in parentheses; the bolded verdict sentence; the sequence of bolded evidence paragraphs; the named-limits bullet list; the closing sentence naming which plans established which part; and the semicolon-separated sources run. Also read section 8 for the second instance and section 6 for the honest-gaps template.
    - `firestarter_app/tools/build_db.py` — the ceiling constant and the annotation directly above it, to quote its content into section 10 without editing it. Cited by content, not line number.
    - `firestarter_app/firestarter/vpp_rail_gate.py` as plan `199-03` left it — the deliverable-maximum constant's name and value, and the docstring statements section 10 must be consistent with.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-BENCH-RECORD.md` in full — the four figure lines, the method, the operator statements and the limits, all of which section 10 restates for a reader who has no access to the planning directory.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md` section F26 — the section 10 template, the two provenance-marker forms, and the enumerated list of what section 10 must contain.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md` section C13 — the 30-row table with algorithm, path, pin count, manufacturer and part number per row, to be re-measured post-override before it is written down.
    - `firestarter_app/firestarter/diagnostic_report.py` — the single-sourced rail-reading disclosure sentence, the honesty voice section 10's limits should match.
    - `firestarter_fw/src/proms/eprom.cpp` — the route resolver's documentation of the VPE-as-VPP flag, read as a reference to quote. Do not edit this file; this phase is host-only.
  </read_first>
  <action>
    Append a new numbered section 10 at the end of `firestarter_app/tools/DECODE-NOTES.md`, after
    section 9. Follow sections 8 and 9 beat for beat.

    Head it with a numbered heading naming the subject, then in parentheses the requirement ids
    RAIL-01, RAIL-02 and RAIL-04, the phase number, and a one-clause verdict.

    Open with a bolded verdict sentence: the ceiling constant is a regulator's theoretical figure,
    kept as the refusal gate, and the deliverable maximum at the socket is a separate, measured,
    host-side number. Then a short paragraph saying which plans produced which piece of the finding.

    Write the evidence as a sequence of bolded lead-in paragraphs, each carrying one kind of evidence
    with a provenance marker. Use the content-addressed prose locator form for in-repository files
    that move, naming the site by what it is rather than by a line number, matching this project's
    cited-by-content instruction and its line-citation repair rule.

    The paragraphs section 10 owes its reader are these. What the ceiling is and why it stays: the
    constant's value, the fact that its comparison is strict so the rows sitting exactly on it remain
    supported, and the reason replacing it was declined — a measured figure would flip all thirty rows
    to a status the chip resolver turns into an unimplemented-chip error, which is the silent refusal
    the milestone's own decision forbids. What was measured: the meter figures at socket pin 1 on both
    VPP paths, on a Rev 2.0 board identified by silkscreen, chip out, pot at maximum, with the two
    control-register composites named and their bit derivations given, and the held-rail method named
    together with why a plain register-set command does not work. What the ADC said: the two bounded
    monitor readings, the sampling rule, and the discrepancy against each meter figure. What the
    routing decision is: route it, with the rule stated as required voltage strictly greater than the
    measured deliverable maximum, and the finding that the mechanism it drives already shipped end to
    end on both sides before this phase began, reachable only by an operator who knew the flag's name.
    And the classification: the census's five properties and the counts.

    Write the 30-row table in its post-override state, re-measured against the live database rather
    than copied from research. Order it by VPP path, then by voltage descending, then by manufacturer,
    so the ten rows the threshold actually strands read as one block. Give a reason per row. Include
    the algorithm, the path, the pin count, the manufacturer and the part number per row, and state
    the 10 against 20 split.

    Write the limits as a bulleted list in the register section 6 uses — a bolded claim sentence, the
    evidence, and an explicit cross-reference naming whatever owns the gap. The list must name at
    least these seven. The ADC-versus-meter figure is a combined ADC-plus-P1-switch-path discrepancy
    that this session cannot decompose, because the monitor reads assert no socket-routing bit while
    the meter reads at socket pin 1 through the pin-1 routing bit; backlog 999.38's ratiometric figure
    with its range remains the only ADC-only number and is cited rather than replaced, and its
    standing operational rule stands, that a pot target is set from a meter reading and never from the
    firmware's own figure; the monitor's wire format carries whole volts plus one tenths digit, so
    every firmware figure sits on a hundred-millivolt grid and is never finer, independently of
    accuracy; Rev 2.2 and Modified Rev 0 are unmeasured and named as such rather than assumed,
    cross-referenced to the backlog item that owns the unfinished sweep, and Modified Rev 0 could
    contribute no ADC figure in any case because the voltage read refuses on that revision and the
    acceptance check returns early there; the direct-VPE to pin-21 destination was not measured, so
    the twenty direct-VPE rows are classified against a pin-1 figure taken through a different routing
    bit to a different physical destination, which is an approximation and is stated as one; the
    classification test mirrors a firmware table the host cannot read and therefore detects a
    database-side change only; and the two supplement rows at a hardcoded twenty-five volts are
    unverified and not write-graduated, and reachable by neither rail at pot maximum.

    Record the posture change, D-12, as its own bolded paragraph. Quote the firmware route resolver's
    own description of the flag as a pure human override set by no database entry, state that after
    this phase it is also set by a host rule, state that the flag's resolution order is unchanged
    because it still wins over the table with no table read, and state plainly that the description no
    longer describes the only caller. Then state why the description is not corrected in place: the
    project forbids annotations in product source outright, so the only legal moves are to leave it
    byte-unchanged or to delete it whole, and recording the change here is what the rule leaves
    available. Make the same point about the generator's annotation above the ceiling constant, quoting
    its content, since that annotation is the reason this whole section exists rather than a label in
    the source.

    Close with a sentence naming which plans established which part, then a semicolon-separated
    sources run in the form sections 8 and 9 use.

    Write the deliverable-maximum figure into section 10 as the same integer the bench record and the
    gate module carry, and in the same millivolt unit, so the three-way equality check in Task 3 can
    find it. Put it on its own line in the form the bench record uses, inside a fenced block, in
    addition to prose that states it in volts for a reader.

    Edit no source file. Do not touch the generator's annotation, the firmware's documentation, the
    ceiling constant, or any `support_status`.

    Commit inside `firestarter_app` on `v1.40-program-parameter-fidelity`, citing D-03, D-01, D-06,
    D-12 and D-18.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && B=$(git rev-parse --abbrev-ref HEAD) && printf '%s\n' "$B" && test "$B" = 'v1.40-program-parameter-fidelity'</automated>
    <fails_when>the branch name printed is anything other than `v1.40-program-parameter-fidelity` — `beta` in particular means the next push publishes to PyPI</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -qE '^## *10\.' tools/DECODE-NOTES.md && /usr/bin/grep -qE '^## *10\..*RAIL-01' tools/DECODE-NOTES.md && /usr/bin/grep -qE '^## *10\..*Phase 199' tools/DECODE-NOTES.md</automated>
    <fails_when>non-zero exit — section 10 is absent, or its heading does not carry the requirement id and the phase number in the parenthetical form sections 8 and 9 both use</fails_when>
    <automated>cd /workspaces/firestarter_app && L=$(/usr/bin/grep -nE '^## *[0-9]+\.' tools/DECODE-NOTES.md) && printf '%s\n' "$L" && test "$(printf '%s\n' "$L" | wc -l)" -eq 11</automated>
    <fails_when>the printed list of numbered-section heading lines does not have exactly 11 entries — the file held ten numbered sections, 0 through 9, before this task, so fewer than 11 means section 10 was not added as a top-level numbered section and more than 11 means an extra section was introduced. The heading lines are printed with their line numbers so a miscount is readable rather than inferred</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -c "
import pathlib
s = pathlib.Path('tools/DECODE-NOTES.md').read_text(encoding='utf-8')
sec = s.split('## 10.', 1)[1] if '## 10.' in s else ''
assert sec, 'section 10 not found'
for tok in ('RURP_VPP_CEILING_MV', '25000', 'socket pin 1', '0x188', '0x088', 'Rev 2.0', 'Rev 2.2', 'Modified Rev 0', '999.38', '999.42', 'pin 21', 'FLAG_VPE_AS_VPP', 'VERIFIED', 'Sources'):
    assert tok in sec, tok
print('SECTION10_CONTENT_OK')" | /usr/bin/grep -qx 'SECTION10_CONTENT_OK'</automated>
    <fails_when>the output is not `SECTION10_CONTENT_OK` — the AssertionError names the first required token absent from section 10, which means the ceiling and its value, the measurement node, one of the two composites, one of the three shield revisions, one of the two cited backlog items, the unmeasured pin-21 destination, the routing flag, a provenance marker or the sources run was not written</fails_when>
    <automated>cd /workspaces/firestarter_app && S=$(.venv/ci-replica/bin/python -c "
import pathlib
s = pathlib.Path('tools/DECODE-NOTES.md').read_text(encoding='utf-8').split('## 10.', 1)[1]
print(sum(1 for line in s.splitlines() if line.lstrip().startswith('|') and line.count('|') >= 6))") && printf 'section 10 table rows: %s\n' "$S" && test "$S" -ge 32</automated>
    <fails_when>fewer than 32 pipe-delimited table lines inside section 10 — the classification table needs a header row, a separator row and thirty data rows, so a smaller count means rows were summarised away rather than disposed individually</fails_when>
    <automated>cd /workspaces && M=$(/usr/bin/grep -oP '^DELIVERABLE_MAX_DROP_PATH_MV = \K[0-9]+$' .planning/phases/199-what-the-rails-can-actually-deliver/199-BENCH-RECORD.md) && printf 'bench: %s\n' "$M" && test -n "$M" && cd /workspaces/firestarter_app && /usr/bin/grep -qFx -e "DELIVERABLE_MAX_DROP_PATH_MV = $M" tools/DECODE-NOTES.md</automated>
    <fails_when>an empty bench figure printed, meaning the bench record's figure line is absent or malformed, or a non-zero exit from the final grep, meaning section 10 does not carry the same figure line as the bench record and the record and the measurement have diverged</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain -- tools/build_db.py firestarter/data/chip_database.json && test -z "$(git status --porcelain -- tools/build_db.py firestarter/data/chip_database.json)"</automated>
    <fails_when>a non-empty porcelain listing printed immediately above — the generator or the generated database was modified by this task, which means the ceiling constant or its annotation was touched when D-01 and D-03 both say the record carries the change instead</fails_when>
    <automated>cd /workspaces/firestarter_fw && git status --porcelain && test -z "$(git status --porcelain)"</automated>
    <fails_when>a non-empty porcelain listing printed immediately above — a firmware file was modified, and this phase is host-only; in particular the route resolver's documentation of the flag must be left byte-unchanged and the posture change recorded in section 10 instead</fails_when>
    <automated>cd /workspaces/firestarter_app && L=$(git diff --cached --name-only -- '*.py' '*.c' '*.cpp' '*.h') && printf 'staged source files:\n%s\n' "$L" && test -z "$L"</automated>
    <fails_when>a non-empty staged source file list printed immediately above, or a non-zero exit from `git diff --cached` itself — this task stages one markdown file, so any staged source file means an edit reached a file the no-annotation rule governs. This leg is deliberately range-free because it is about the INDEX immediately before the commit; it must run after `git add` and before the commit, and it reports nothing if run after</fails_when>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/tools/DECODE-NOTES.md` carries a new top-level numbered section 10, bringing the numbered-section count to exactly 11, and its heading names RAIL-01 and Phase 199 in the parenthetical form sections 8 and 9 use.
    - Section 10 opens with a bolded verdict stating that the ceiling is a theoretical regulator figure kept as the refusal gate and that the deliverable maximum at the socket is a separate measured host-side number.
    - Section 10 contains the ceiling constant name and its value, the measurement node, both control-register composites, all three shield revisions, both cited backlog items, the unmeasured pin-21 destination, the routing flag name, at least one provenance marker and a sources run.
    - Section 10 carries the classification table with at least 32 pipe-delimited lines — a header, a separator and thirty data rows — ordered by VPP path then voltage descending then manufacturer, with a reason per row.
    - Section 10 carries a figure line identical to the bench record's `DELIVERABLE_MAX_DROP_PATH_MV` line, so the record and the measurement are the same integer.
    - Section 10 carries a named-limits list naming all seven required limits, each with its evidence and, where one exists, a cross-reference to the item that owns the gap.
    - Section 10 records the posture change, quoting the firmware's own description of the flag, and states why that description is not corrected in place.
    - `tools/build_db.py`, `firestarter/data/chip_database.json` and the entire firmware repository are unmodified.
    - No source file is staged in this task's commit.
  </acceptance_criteria>
  <done>
    The phase's findings are written where a reader who has no access to the planning directory will
    find them, in the shape two prior phases established, with every limit named rather than hedged —
    and the two annotations this phase makes untrue are recorded rather than edited.
  </done>
  <reversibility rating="reversible">Section 10 is an append to a markdown file with no code dependants. Removing it restores the file, and nothing in the source was changed to accommodate it.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The gh#71 draft, held — and the one consolidated held-pending list now names three issues</name>
  <files>.planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md, .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md</files>
  <precondition>Task 1 is committed, so section 10 exists and the draft can cite the measured threshold as a published figure rather than as an intention.</precondition>
  <precondition>`gh` is authenticated for read access to the public issue tracker, and a writable cache directory is provided inline on the command, because this devcontainer's default cache path is not writable and the tool fails without it.</precondition>
  <read_first>
    - `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md` in full — all five sections in order, the standing warning at the top about which section may be posted, the status section's dated operator decision, the internal-provenance section's rule about keeping internal identifiers out of the public body, the comment body's delimiters and bolded lead-ins, and the held-pending deferral section's four labelled bullets with their exact current wording.
    - `.planning/phases/198-the-two-voltage-nibbles/198-GH66-ANSWER.md` in full — the smaller instance of the same five-section shape, how it folds the does-not-close clause into the what-changed heading, and how it defers to gh#70's list as canonical rather than duplicating the reasoning.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md` section E25 — gh#71's measured current state, the reporter's proposal verbatim, both maintainer comments in full, and the two-column table of what the draft may and may not claim.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md` section E23 — the enumerated structure of the consolidated list and the five concrete edits D-20 requires.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-BENCH-RECORD.md` and `firestarter_app/tools/DECODE-NOTES.md` section 10 — the figures the draft may quote, and only those.
    - `/workspaces/CLAUDE.md` section "Milestone close and branch protection" — the publishing consequence that is the reason the hold exists.
  </read_first>
  <action>
    Create `.planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md` in the
    five-section shape both sibling drafts use, in the same order.

    Open with the title and immediately the bold standing warning that this is an internal
    draft-tracking file, that only the comment-body section is intended for posting, and that the
    header above it and the notes below it are internal bookkeeping that must never be pasted onto the
    issue.

    Write the status section: draft, approved, held pending the beta cut, not posted. Then the dated
    operator decision as a numbered list, carried forward from the two sibling phases without
    re-asking, and then the statement that nothing has been posted, pointing at the deferral section.

    Write the internal-provenance section: which plan produced the draft and which requirement it
    answers, where the internal identifiers live together with the explicit statement that they are
    kept out of the public body because they are meaningless to a reader without this project's
    planning directory, which phase owns each separately-worked claim, and the technical cross-checks
    for whoever resolves the version placeholder.

    Write the comment body between bare delimiters, preceded by the instruction to post that section
    verbatim once the version line is resolved and to include neither the header nor the footnotes.
    Inside it, four bolded lead-ins and nothing more.

    First, what changed, with the does-not-close clause folded into the heading the way the 198 draft
    does: the part's programming voltage is corrected in the database from eighteen volts to
    twenty-one volts, from the vendored datasheet the reporter attached, credited to him by name. That
    is the only database claim.

    Second, the answer to the reporter's proposal, which is the draft's one genuinely new fact: he
    proposed routing VPE as VPP, and it is now automatic — the host compares the part's required
    voltage against the measured deliverable maximum of the standard path and routes the undropped
    rail itself when the part asks for more, warning with both voltages and proceeding. State the
    measured deliverable maximum at socket pin 1 and the measured undropped figure, with the method in
    one clause: a meter at the socket on a Rev 2.0 board with the chip out and the pot at maximum.
    State that the manual option remains for anything the rule does not catch.

    Third, the version line, carrying the same held-pending placeholder the two sibling drafts use,
    and saying that it will be resolved at the milestone's beta cut.

    Fourth, what this does not do. It does not claim the part will program: nothing here was proved
    against a real part of that type. It does not advance the last-block boundary question, which is
    the maintainer's standing position across three part sizes and which this work does not touch. And
    it states that the issue stays open.

    Say nothing else. Do not re-announce the twenty-one-volt and one-millisecond mismatches or the
    voltage-cap explanation: the maintainer published all three on the issue on 2026-09-16, and
    repeating them reads as a project that does not read its own issues. Quote no figure the bench
    session did not produce.

    Write the held-pending deferral section, deferring to gh#70's file as the canonical consolidated
    list rather than duplicating its reasoning, exactly as the 198 draft defers. Carry the pointer and
    the four posting steps so a reader who finds this file alone reaches complete instructions, and
    say that the canonical list lives in the other file.

    Then edit `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md`'s
    held-pending deferral section so it names three issues rather than two. Five concrete edits. Add
    this phase's draft to the sibling-file sentence in the opening paragraph, which currently names one
    sibling. Extend the what-is-held bullet with this issue's line and its measured current comment
    state — and write that state correctly rather than copying the pattern: two of this issue's three
    comments are from the maintainer, so the other two issues' clause that no comment comes from this
    project is false here and must not be reused. Extend the release-trigger bullet to say the same cut
    releases all three. Change the posting-steps preamble so it names all three issues, and add this
    issue's number to the step that gives the posting command. Extend the requirement-status bullet to
    carry three requirement ids rather than two, with this phase's artifact paths.

    Do not create a second list, and do not move the canonical list into this phase's directory. One
    list, in gh#70's file, is what D-20 established and what whoever closes the milestone will read.

    Read the issue read-only once, before writing, to confirm its state and comment count have not
    moved since the research session — a new comment would make the draft's what-is-held claim stale
    before it is written. Provide a writable cache directory inline on the command. Post nothing:
    no comment, no label, no state change.

    Leave RAIL-05 Pending in `.planning/REQUIREMENTS.md`. It is not satisfied by this plan and it is
    carried to the milestone close on the same terms as its two siblings.

    Commit in the meta repository at `/workspaces` on `v1.40-program-parameter-fidelity`, citing D-19
    and D-20.
  </action>
  <verify>
    <automated>cd /workspaces && XDG_CACHE_HOME="${TMPDIR:-/tmp}/ghcache" gh issue view 71 --repo henols/firestarter --json state,comments --jq '{state, comments: (.comments|length)}' | /usr/bin/grep -qF -e '"state":"OPEN"'</automated>
    <fails_when>non-zero exit — the issue is no longer open, meaning someone closed it and the draft's premise has moved, or the command itself failed, most likely because the writable cache directory was not provided inline and the default cache path is not writable in this devcontainer</fails_when>
    <automated>cd /workspaces && for H in '## Status' '## Internal provenance' '## Comment Body' '## Held-pending deferral'; do /usr/bin/grep -qF -e "$H" .planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md || { echo "MISSING: $H"; exit 1; }; done && echo DRAFT_SECTIONS_OK</automated>
    <fails_when>the output is not `DRAFT_SECTIONS_OK` — a `MISSING:` line names the section of the five-section shape that was not written, which means a reader who finds this file alone would not reach complete instructions</fails_when>
    <automated>cd /workspaces && /workspaces/firestarter_app/.venv/ci-replica/bin/python -c "
import pathlib
s = pathlib.Path('.planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md').read_text(encoding='utf-8')
assert 'NOT POSTED' in s.upper(), 'status does not say not posted'
assert 'dim20' in s, 'reporter not credited'
low = s.lower()
assert 'does not close' in low, 'no does-not-close statement'
body = s.split('## Comment Body', 1)[1].split('## Held-pending deferral', 1)[0]
bl = body.lower()
for bad in ('pulse_duration_us', '0xf0', 'last 256', 'boundary hypothesis', 'will program', 'now programs', 'fixes the chip'):
    assert bad not in bl, bad
for bad in ('999.', 'phase 199', 'd-17', 'd-19', 'rail-0'):
    assert bad not in bl, bad
print('DRAFT_CLAIMS_OK')" | /usr/bin/grep -qx 'DRAFT_CLAIMS_OK'</automated>
    <fails_when>the output is not `DRAFT_CLAIMS_OK` — the AssertionError names the failing leg: a status that does not say the draft is unposted, a missing credit to the reporter, a missing does-not-close statement, a comment body re-announcing a finding the maintainer already published or claiming the part will program, or a comment body leaking an internal backlog number, phase number, decision id or requirement id that is meaningless to a reader without this project's planning directory</fails_when>
    <automated>cd /workspaces && .planning/../firestarter_app/.venv/ci-replica/bin/python -c "
import pathlib
s = pathlib.Path('.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md').read_text(encoding='utf-8')
sec = s.split('## Held-pending deferral', 1)[1]
for tok in ('gh#70', 'gh#66', 'gh#71', '199-GH71-ANSWER.md', 'RAIL-05', 'PULSE-04', 'VOLT-04'):
    assert tok in sec, tok
print('CONSOLIDATED_LIST_OK')" | /usr/bin/grep -qx 'CONSOLIDATED_LIST_OK'</automated>
    <fails_when>the output is not `CONSOLIDATED_LIST_OK` — the AssertionError names the first required token absent from the consolidated list, which means the third issue, this phase's draft file, or the third requirement id was not added, and whoever closes the milestone would read a list that omits one held answer</fails_when>
    <automated>cd /workspaces && H=$(/usr/bin/grep -nF -e 'Held-pending deferral' .planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md) && printf 'headings:\n%s\n' "$H" && test "$(printf '%s\n' "$H" | wc -l)" -eq 1 && F=$(/usr/bin/grep -rlF -e 'This is now the consolidated list' .planning/phases/) && printf 'claimants:\n%s\n' "$F" && test "$(printf '%s\n' "$F" | wc -l)" -eq 1</automated>
    <fails_when>either printed list does not have exactly one entry — more than one deferral heading in the canonical file means the section was duplicated rather than extended, and more than one file claiming to be the consolidated list means a second list was started, which is exactly what D-20 exists to prevent. Both lists are printed so which file or line offends is readable</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -nE '^- \[[ x]\] \*\*RAIL-05\*\*' .planning/REQUIREMENTS.md && /usr/bin/grep -qE '^- \[ \] \*\*RAIL-05\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed line shows a ticked checkbox, or the final grep exits non-zero — RAIL-05 was marked complete when D-19 leaves it Pending, because nothing has been posted to the issue and the draft is held pending the beta cut</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md` exists and carries all four named sections plus the standing warning at the top naming which section may be posted.
    - Its status section states that the draft is held and not posted, and carries the dated operator decision.
    - Its comment body credits the reporter by name, claims the database correction from the vendored datasheet, answers the VPE-as-VPP proposal as now automatic above the measured threshold, carries the held-pending version placeholder, and states under an explicit heading that it does not close the report.
    - Its comment body contains no re-announcement of the pulse-width mismatch or the voltage-cap explanation, no claim that the part will program, no advance on the boundary hypothesis, and no internal backlog number, phase number, decision id or requirement id.
    - The consolidated held-pending list in `197-GH70-ANSWER.md` names all three issues, this phase's draft file, and all three requirement ids, and its what-is-held line for the new issue does not claim that no comment on it comes from this project.
    - Exactly one held-pending deferral heading exists in the canonical file, and exactly one file across the phases directory claims to be the consolidated list.
    - gh#71 is still open and nothing was posted to it: no comment, no label change, no state change.
    - RAIL-05 is still an unticked checkbox in `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>
    The person who reported the problem has an answer written, crediting him, claiming the one
    correction and the one genuinely new fact and nothing else — and it is held, on one list, with
    complete instructions for whoever releases it.
  </done>
  <reversibility rating="reversible">Two markdown files, one new and one extended by five scoped edits. Nothing is posted, so no public statement exists to retract, which is precisely what the hold buys.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The phase gate — one threshold in three places, and the CI gate as CI runs it</name>
  <files>firestarter_app/tools/DECODE-NOTES.md</files>
  <precondition>All four prior plans and both prior tasks of this plan are committed, so the bench record, the gate module's constant and section 10 all exist and can be compared.</precondition>
  <precondition>`firestarter_app/.venv/ci-replica/bin/python` resolves to Python 3.11, matching the CI floor. A run under the devcontainer default 3.12 proves nothing about CI.</precondition>
  <read_first>
    - `firestarter_app/.github/workflows/ci.yml` — the `ci` job's steps in order, so the gate run below matches what CI actually executes, including the coverage floor and the exact lint scope.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md` section C16 — the measured CI step list, and the finding that the type checker is not a CI step in this repository so no plan may claim CI enforces it.
    - `.planning/phases/199-what-the-rails-can-actually-deliver/199-RESEARCH.md` section F27 — the commit unit, the branch state, the publishing hazard, and the porcelain-assertion hazard from a stray untracked file.
    - `firestarter_app/tools/DECODE-NOTES.md` section 10 as Task 1 left it, and `firestarter/vpp_rail_gate.py` as plan `199-03` left it — the two places the threshold now lives besides the bench record.
  </read_first>
  <action>
    Prove D-08's mechanism end to end, then run the gate the way CI runs it.

    The threshold exists in three places by design: the bench record's figure line, which is the
    measurement; the gate module's constant, which is what ships; and section 10's figure line, which
    is what a reader finds. Assert all three are the same integer in one check. That is what makes the
    no-hardcoded-threshold discipline verifiable rather than merely intended — a plan can promise not
    to write a literal, but only an equality check proves the number that shipped is the number that
    was measured.

    If the three disagree, do not adjust whichever one is convenient. Report which two agree and which
    one differs: a mismatch between the module and the bench record means the wrong figure shipped, and
    a mismatch between section 10 and the other two means the record misstates what shipped. They are
    different findings with different fixes.

    Dispose of the stray untracked file before the gate run. The host repository carries one untracked
    file under its datasheets directory, `datasheets/LST62832I.pdf`, which predates this phase and is
    unrelated to it, and the suite contains a test that asserts whole-repository porcelain, so it
    reddens a leg that has nothing to do with this work. Move it to a path outside the repository,
    run the gate, then move it back to its original path immediately afterwards, and record both moves
    and the destination path in this plan's summary. Do not stage it, do not commit it, and do not
    delete it — it is not this phase's file. Do not use a stash, because a stash including untracked
    files would sweep up this phase's own work as well.

    Run the full suite with the coverage report and the coverage floor, on the CI interpreter, exactly
    as the CI job's pytest step invokes it. Run the lint and format checks over the two directories CI
    gates and no others. The generator's tooling directory is deliberately outside that scope, which is
    the reason this phase's coverage lives in the tests directory.

    Run the type checker locally if convenient, and claim nothing about CI enforcing it — it is not a
    step in this repository's CI job, and project experience records that the pre-release ships even
    when the host CI job fails.

    Confirm the commit topology is what this phase intends: every source, test, fixture and tooling
    change inside the host sub-repository on the milestone branch, every planning artifact in the meta
    repository on the same branch, the firmware sub-repository untouched, and nothing pushed anywhere.

    Advance the meta repository's pointer to the host sub-repository as the final act of the phase,
    per this project's per-phase convention, and commit that in the meta repository on the milestone
    branch.

    This task changes no file content. If it needs to amend section 10 — because the three-way check
    found the record's figure wrong — make that the one scoped edit and re-run the check.
  </action>
  <verify>
    <automated>cd /workspaces && M=$(/usr/bin/grep -oP '^DELIVERABLE_MAX_DROP_PATH_MV = \K[0-9]+$' .planning/phases/199-what-the-rails-can-actually-deliver/199-BENCH-RECORD.md) && N=$(/usr/bin/grep -oP '^DELIVERABLE_MAX_DROP_PATH_MV = \K[0-9]+$' firestarter_app/tools/DECODE-NOTES.md) && C=$(cd firestarter_app && .venv/ci-replica/bin/python -c "
from firestarter import vpp_rail_gate as g
names = [n for n in dir(g) if n.isupper() and 'DELIVERABLE' in n]
assert len(names) == 1, names
print(getattr(g, names[0]))") && printf 'bench=%s notes=%s module=%s\n' "$M" "$N" "$C" && test -n "$M" && test "$M" = "$N" && test "$M" = "$C"</automated>
    <fails_when>the three printed figures are not identical, or any one of them is empty — a bench-versus-module mismatch means the shipped threshold is not the measured one, a notes-versus-other-two mismatch means section 10 misstates what shipped, and an empty value names the artifact whose figure line is absent or unparseable</fails_when>
    <automated>cd /workspaces/firestarter_app && test -f datasheets/LST62832I.pdf && S=$(git status --porcelain -- datasheets/LST62832I.pdf) && printf '%s\n' "$S" && test "$S" = '?? datasheets/LST62832I.pdf'</automated>
    <fails_when>the file is absent, meaning the move-aside was not reversed or the file was deleted, or the porcelain line printed is anything other than the untracked marker followed by that path, meaning the stray file was staged or committed as part of this phase</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -V | /usr/bin/grep -q '^Python 3\.11\.'</automated>
    <fails_when>non-zero exit — the replica interpreter drifted off the CI floor of 3.11, which makes every other result in this task untrustworthy as a statement about CI</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70 -o addopts="" -q 2>&1 | tail -6 | /usr/bin/grep -qE '^[0-9]+ passed'</automated>
    <fails_when>any failure reported, no bare `N passed` summary line, or a coverage total below the floor which makes the command exit non-zero — this is the exact step CI runs, so a red here is a red in CI. Run it with the stray untracked file moved aside, because a repo-porcelain assertion in the suite reddens on unrelated dirt</fails_when>
    <automated>cd /workspaces/firestarter_app && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit — a lint error or an unformatted file under the two directories CI gates; the generator's tooling directory is deliberately out of scope and a finding there is not this leg's business</fails_when>
    <automated>cd /workspaces && for R in . firestarter_app firestarter_fw; do B=$(git -C "$R" rev-parse --abbrev-ref HEAD); printf '%s %s\n' "$R" "$B"; test "$B" = 'v1.40-program-parameter-fidelity' || exit 1; done && echo ALL_THREE_ON_MILESTONE_BRANCH</automated>
    <fails_when>the output is not `ALL_THREE_ON_MILESTONE_BRANCH`, with the offending repository and branch printed immediately above — any repository off the milestone branch, and `beta` in particular, means the next push publishes to PyPI</fails_when>
    <automated>cd /workspaces/firestarter_fw && S=$(git status --porcelain) && printf 'fw porcelain:\n%s\n' "$S" && test -z "$S" && H=$(git rev-parse HEAD) && printf '%s\n' "$H" && printf '%s\n' "$H" | /usr/bin/grep -qE '^[0-9a-f]{40}$'</automated>
    <fails_when>a non-empty porcelain listing printed immediately above — the firmware sub-repository was modified during a host-only phase, and the milestone's only firmware change belongs to a later phase</fails_when>
    <automated>cd /workspaces && L=$(git log --oneline origin/beta..HEAD -- .planning/phases/199-what-the-rails-can-actually-deliver/) && printf 'phase commits:\n%s\n' "$L" && test -n "$L"</automated>
    <fails_when>non-zero exit — no commit on the milestone branch ahead of the base branch touches this phase's directory, which means the phase's planning artifacts were never committed or were committed to the wrong branch</fails_when>
    <automated>cd /workspaces && test "$(git rev-list --count origin/beta..HEAD)" -gt 0 && test "$(git rev-list --count HEAD..origin/beta)" -eq 0</automated>
    <fails_when>the first test fails, meaning nothing from this phase is ahead of the base branch, or the second fails, meaning the local branch is behind the base branch and the phase was built on a stale base. Both figures are explicit ranges, so neither reads as clean when the range is empty for the wrong reason</fails_when>
  </verify>
  <acceptance_criteria>
    - The bench record's `DELIVERABLE_MAX_DROP_PATH_MV` figure, section 10's figure line and the gate module's deliverable-maximum constant are all the same non-empty integer.
    - `datasheets/LST62832I.pdf` is present at its original path and still untracked at the end of the task, and was neither staged, committed nor deleted at any point.
    - `.venv/ci-replica/bin/python` reports a 3.11 version.
    - The full test suite passes under that interpreter with the coverage report and the coverage floor CI uses.
    - `ruff check` and `ruff format --check` are clean over the two directories CI gates.
    - All three repositories are on `v1.40-program-parameter-fidelity`.
    - The firmware sub-repository's working tree is clean and it was not modified by this phase.
    - At least one commit ahead of the base branch touches this phase's planning directory, and the local branch is not behind the base branch.
    - The meta repository's pointer to the host sub-repository has been advanced, and nothing has been pushed to any remote.
  </acceptance_criteria>
  <done>
    The number that was measured on the bench is the number that ships in the code and the number a
    reader finds in the record, proved by equality rather than by inspection. The gate CI will run has
    been run, on the interpreter CI uses, with the coverage floor CI applies — and the stray file that
    would have reddened it is back where its owner left it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal planning record to public issue comment | Text written with the planning directory in view is read by strangers who have none of it |
| measured figure to published figure | A number a human read off a meter becomes a claim on a public issue and a constant in shipped code |
| held draft to accidental publication | A file whose body is meant to be posted verbatim sits beside instructions for posting it |
| milestone branch to the base branch | A push to the base branch of the host repository publishes to a public package index with no path filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-32 | Repudiation | a public claim the work does not support | high | mitigate | The comment body is asserted to contain no claim that the part will program, no advance on the boundary hypothesis, and no re-announcement of the three findings the maintainer already published; the two-column may-and-may-not table in RESEARCH is the contract |
| T-199-33 | Information Disclosure | internal identifiers leaking into a public comment | high | mitigate | The comment body is asserted to contain no backlog number, phase number, decision id or requirement id, and the internal-provenance section states the rule explicitly |
| T-199-34 | Tampering | a shipped threshold that is not the measured one | high | mitigate | A three-way equality check across the bench record, section 10 and the module constant, with a stated rule that a mismatch is reported by which two agree rather than patched to the convenient value |
| T-199-35 | Repudiation | a held answer lost because it is on the wrong list | high | mitigate | One consolidated list, in the canonical file, extended by five scoped edits; a check asserts exactly one deferral heading in that file and exactly one file across the phases directory claiming to be the consolidated list |
| T-199-36 | Repudiation | a requirement marked satisfied by a draft nobody posted | high | mitigate | A check asserts the requirement's checkbox is still unticked, and the plan states that it is carried to the milestone close on the same terms as its two siblings |
| T-199-37 | Information Disclosure | an approximation published as a measurement | medium | mitigate | Section 10 is asserted to name the unmeasured pin-21 destination and the two unmeasured shield revisions, and to describe the ADC-versus-meter figure as a combined discrepancy citing the ratiometric backlog figure rather than replacing it |
| T-199-38 | Tampering | an annotation edited in place instead of recorded | medium | mitigate | The generator, the generated database and the entire firmware repository are asserted unmodified, and no source file is staged in the record task's commit |
| T-199-39 | Elevation of Privilege | an unintended publish to the package index | high | mitigate | All three repositories are asserted on the milestone branch, the local branch is asserted not behind the base branch, pushing is named as a prohibition, and nothing in the plan pushes |
| T-199-40 | Spoofing | a stale claim about the issue's comment state | medium | mitigate | The issue is read read-only immediately before the draft is written, and the plan states that a new comment would make the what-is-held claim stale before it is written |
| T-199-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package; the only external tool it invokes is the already-authenticated issue-tracker client, read-only |
</threat_model>

## Artifacts this phase produces (plan 199-05)

**New files**
- `.planning/phases/199-what-the-rails-can-actually-deliver/199-GH71-ANSWER.md`

**New documentation section**
- `firestarter_app/tools/DECODE-NOTES.md` section 10, whose heading names RAIL-01, RAIL-02 and RAIL-04 and Phase 199, bringing the file's numbered-section count to 11

**New content inside section 10**
- the bolded verdict on the ceiling's theoretical status
- the measured per-rail figures with the method and both control-register composites
- a figure line identical to the bench record's `DELIVERABLE_MAX_DROP_PATH_MV` line
- the paired ADC figures and the measured discrepancy
- the 30-row classification table, post-override, ordered by path then voltage then manufacturer, with a reason per row
- the named-limits list carrying seven limits
- the posture-change paragraph quoting the firmware's own description of the routing flag
- the sources run

**New sections inside the held draft**
- the standing warning, `## Status`, `## Internal provenance`, `## Comment Body` between bare delimiters, and `## Held-pending deferral` deferring to the canonical list

**Extended, not duplicated**
- the consolidated held-pending list in `.planning/phases/197-the-override-mechanism-and-the-program-pulse/197-GH70-ANSWER.md`, by five scoped edits, now naming three issues and three requirement ids

**Deliberately unchanged, asserted rather than assumed**
- `tools/build_db.py` and its annotation above the ceiling constant
- `firestarter/data/chip_database.json`
- the entire firmware sub-repository, including the route resolver's documentation of the routing flag
- RAIL-05's unticked checkbox in `.planning/REQUIREMENTS.md`
- gh#71's state, labels and comment count

<verification>
- Section 10 exists as the eleventh numbered section, names RAIL-01 and Phase 199 in its heading, and opens with a bolded verdict on the ceiling's theoretical status.
- Section 10 contains the ceiling constant and value, the measurement node, both composites, all three shield revisions, both cited backlog items, the pin-21 gap, the routing flag, a provenance marker and a sources run.
- Section 10's classification table carries at least 32 pipe-delimited lines.
- The bench record's figure, section 10's figure and the module constant are one identical integer.
- The generator, the generated database and the whole firmware repository are unmodified, and no source file is staged in the record commit.
- The held draft carries all four sections plus the standing warning, credits the reporter, states that it does not close the report, and its comment body carries no over-claim and no internal identifier.
- The consolidated list names three issues, this phase's draft file and three requirement ids, and exactly one file claims to be that list.
- gh#71 is still open and nothing was posted to it.
- RAIL-05 is still unticked.
- The full suite passes on the 3.11 replica with the CI coverage floor; ruff is clean; all three repositories are on the milestone branch; the local branch is ahead of and not behind the base branch; nothing was pushed.
</verification>

<success_criteria>
The recording halves of RAIL-01, RAIL-02 and RAIL-04 all hold: the deliverable maximum and its method
are recorded with the ADC error carried and honestly described, the 30 rows are classified against
the ceiling that stands with a reason each, and the routing decision is recorded as route-it with its
rule. RAIL-05 has its answer written, credited, held and listed — and left Pending, because nothing
has been posted. The threshold that shipped is provably the threshold that was measured.
</success_criteria>

<output>
Create `.planning/phases/199-what-the-rails-can-actually-deliver/199-05-SUMMARY.md` when done. Record
the numbered-section count in `DECODE-NOTES.md` before and after, the classification table's row
count and its chosen ordering, the seven named limits as written, the three-way threshold figures as
printed by the gate check, gh#71's state and comment count as read immediately before the draft was
written, the five edits actually made to the consolidated list, the full-suite pass count and
coverage total from the CI-replica run, the disposition of the stray untracked file including the path
it was moved to and back from, and confirmation that RAIL-05 is still Pending and that nothing was
pushed to any remote.
</output>
