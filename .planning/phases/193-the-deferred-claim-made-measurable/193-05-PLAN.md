---
phase: 193-the-deferred-claim-made-measurable
plan: 05
type: execute
wave: 3
depends_on: ["193-01", "193-02", "193-04"]
files_modified:
  - CLAUDE.md
  - .planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md
autonomous: true
requirements: [GATE-02, GATE-03]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The standing rule — the meta repository never publishes a GitHub Release — is recorded in `CLAUDE.md`, the one file an agent reads before acting, and in the section a milestone close actually visits (D-13). It is not recorded only in the seed or the impact-analysis note, because both of those are reached only by someone already working on the rename, and a close that publishes a Release for unrelated reasons meets neither."
    - "The rule carries its mechanism, not just its instruction: a `v1.36` tag parses as PEP 440 `1.36`, so `Version(\"3.0.0b29\") >= Version(\"1.36\")` reads true and `fw` reports the firmware already up to date — for every stranded CLI, silently, forever. A reader can see WHY the rule exists without re-deriving it."
    - "The mechanism is CITED and not restated (D-14). `CLAUDE.md` points at `.planning/notes/999.9-repo-rename-impact-analysis.md` and its section by heading, so there is one source of truth and no second copy that can drift."
    - "The `.gitmodules` archaeology trap has a pointer in `CLAUDE.md`'s repository-structure area (D-15), where an archaeology session starts, pointing at the note that carries both workarounds and their transcripts."
    - "The same edit corrects the sentence this phase itself falsified: the repository-structure paragraph's inventory of meta `tools/` now names both occupants, because adding `tools/adoption/` made the previous single-occupant claim untrue. A phase does not leave behind a stale reference it created."
    - "Every citation added by this plan is by path and section heading, never by line number — the in-file precedent already states why, and the cited note's own line-numbered citation has already gone stale across three refs."
    - "The four ROADMAP success criteria are each answered by a named artefact and, where the claim is a measurement, by the transcript that recorded it."
  artifacts:
    - path: "CLAUDE.md"
      provides: "The two standing rules an agent meets before acting: no GitHub Release on the meta repository, with its mechanism cited; and the .gitmodules archaeology trap, with its note cited"
      contains: "999.9-repo-rename-impact-analysis.md"
    - path: ".planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md"
      provides: "A criterion-by-criterion record answering the phase's four ROADMAP success criteria, each claim naming the artefact or transcript that supports it"
      contains: "## Criterion 1"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/notes/999.9-repo-rename-impact-analysis.md"
      via: "the no-Releases bullet cites the analysis section carrying the version-comparison walkthrough, by heading"
      pattern: "Standing rule this must produce"
    - from: "CLAUDE.md"
      to: ".planning/notes/gitmodules-archaeology-trap.md"
      via: "the repository-structure paragraph points at the note carrying both workarounds and their transcripts"
      pattern: "gitmodules-archaeology-trap.md"
  prohibitions:
    - requirement_id: GATE-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT restate the version-comparison walkthrough inside `CLAUDE.md`. A second copy of a mechanism can drift from the note that owns it, and the whole point of citing is that there is one source of truth."
    - requirement_id: GATE-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT cite a note, workflow or source file by line number. Line references in this repository have already gone stale across refs, and the file being edited already states the house idiom."
    - requirement_id: GATE-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT leave standing a sentence that this phase's own work made false. Correcting it belongs in the same edit that falsified it, not in a later cleanup."
    - requirement_id: GATE-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT state the rule without its reason. An instruction a reader cannot justify is an instruction a future milestone will talk itself out of, which is precisely the failure this requirement exists to prevent."
---

<objective>
Put the two rules that make the eventual failure survivable where the person about to act will meet
them, and answer the phase's four ROADMAP criteria on the record.

`CLAUDE.md` is the only location guaranteed to be in front of an agent before it acts, and it already
runs exactly this pattern — a rule stated in-line with its mechanism in a cited note. D-13 chooses
it; D-14 makes GATE-02's mechanism a citation rather than a second copy; D-15 puts GATE-03's pointer
in the repository-structure area, whose reader is the archaeologist rather than the closer. Neither
`REQUIREMENTS.md` nor `ROADMAP.md` would do: both are archived at close, where they are
historical-by-intent, and anything recorded only there is gone.

D-18 is honoured by omission: the 37 todos reviewed at discussion time were not folded, and no plan
in this phase acts on one.

Purpose: GATE-02 in full, GATE-03's `CLAUDE.md` pointer, and ROADMAP criteria 3 and 4.
Output: `CLAUDE.md` with one new bullet and one new paragraph, and
`evidence/193-disposition.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-the-deferred-claim-made-measurable/193-CONTEXT.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md
</context>

<environment_notes>
PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, so it silently under-scans. Every
evidence-bearing command in this plan uses the absolute `/usr/bin/grep`.

`/usr/bin/grep -c` exits 1 when the count is 0, so a leg that legitimately expects 0 carries a
trailing `|| true` and is judged on its printed number, not on its exit status.

`CLAUDE.md` is 77 lines before this plan. Its `## Repository Structure` paragraph is a single
unwrapped line, and its `## Milestone close and branch protection` section is four single-line
bullets ending at the last line of the file. This plan adds exactly three lines: one bullet in the
close section, and a blank line plus one paragraph line after the structure paragraph — 80 lines
after both tasks. Task 1 runs before task 2 so the close-section insert cannot move the structure
paragraph under task 2's feet; anchor every edit by content rather than by line number regardless.

`.planning/config.json` and `tools/catalog/messages.toml` are dirty in the working tree before this
plan starts. That is pre-existing and must not be staged or committed by any task here.
</environment_notes>

<artifacts_this_phase_produces>
Symbols created by phase 193. Newly-created names are not drift against any existing source.

- `tools/adoption/` and `tools/adoption/pypi_version_share.sh` — new; no arguments, no CLI flags.
  Stdout keys `window:`, `fixed_downloads_ge_2_0_9:`, `at_risk_downloads_2_0_7:`,
  `fixed_share_pct:`, `threshold:`, `TRIGGER:`, banner `WHAT THIS DOES NOT MEASURE`; exit codes
  0, 1, 2.
- `.planning/notes/gitmodules-archaeology-trap.md` — new note with headings `## The trap`,
  `## Workaround A — an existing clone`, `## Workaround B — a fresh clone at a pre-rename ref`,
  `## A one-shot variant, supplementary`, `## The sync hazard`,
  `## Banked evidence — the executed transcripts`, `## Honest limits`.
- `.planning/phases/193-the-deferred-claim-made-measurable/evidence/` — new directory holding
  `193-gate-01-instrument-run.txt`, `193-gate-03-fresh-clone.txt`,
  `193-gate-03-existing-clone.txt`, `193-gate-03-submodule-sync-hazard.txt` and
  **`193-disposition.md`, created by this plan**, whose headings are `## Criterion 1` through
  `## Criterion 4`.
- `.planning/phases/193-the-deferred-claim-made-measurable/COVERAGE.md` — planner-authored.
- `CLAUDE.md` gains **one bullet** in the existing `## Milestone close and branch protection`
  section and **one paragraph** after the existing `## Repository Structure` paragraph. **No new
  heading is added to `CLAUDE.md` by this phase.**
- `.planning/seeds/SEED-claim-firestarter-slug.md` keeps its four frontmatter keys; no new key.
</artifacts_this_phase_produces>

<flagged_assumptions>
Surfaced and deliberately left unresolved — recorded rather than dropped:

- **GATE-02, edge-probe category `unclassified`.** The deterministic edge taxonomy could not
  classify this requirement. Manual review found no boundary, precision, ordering, adjacency or
  empty-input dimension in it: its deliverable is a recorded prose rule and a citation, not a
  computation over inputs, so none of the eight shape categories applies. It is carried forward as
  `unresolved` rather than auto-resolved with a fabricated criterion or dismissed. If a later reader
  finds a shape dimension here, it is unrecorded by this plan.
</flagged_assumptions>

<planner-discipline-allow-markers>
<!-- planner-discipline-allow: now holds `catalog/` alone -->
</planner-discipline-allow-markers>

<tasks>

<task type="auto">
  <name>Task 1: The no-Releases rule, with its mechanism cited rather than copied</name>
  <read_first>
    - /workspaces/CLAUDE.md — the file being modified; read the whole `## Milestone close and branch protection` section so the bullet shape is copied rather than guessed, including the in-file citation idiom on its third bullet
    - /workspaces/.planning/notes/999.9-repo-rename-impact-analysis.md — the section carrying the version-comparison walkthrough; read it in full so it is cited accurately and not duplicated
    - /workspaces/.planning/REQUIREMENTS.md — GATE-02 verbatim, and decision D-4 at activation
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md — the five structural rules for a bullet in this section
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Add one bullet to `CLAUDE.md`'s `## Milestone close and branch protection` section, inserted
    **between** the existing bullet about recreating local `beta` and the existing final bullet that
    points at the close-procedure note. The "see the note for mechanics" bullet stays last — that is
    the section's existing rhythm and the template D-13 says to follow.

    **Shape**, copied from the four bullets already there: a single line, no wrapping; a bold
    imperative opener followed by an em-dash clause giving the reason; backticks around every path,
    branch, command and version token. Wrapped bold labels break a downstream gate in this project,
    so the bullet must be one line.

    **Content.** The rule: the meta repository must never publish a GitHub Release; bare milestone
    tags only. The reason, stated so a reader can see why it is not cosmetic rather than having to
    re-derive it: the repository's zero-Releases state is what keeps the post-claim failure a clean
    404 instead of a silent wrong answer, because a tag like `v1.36` parses as PEP 440 `1.36` and
    `Version("3.0.0b29") >= Version("1.36")` evaluates true, so `fw` would report the firmware already
    up to date for every stranded CLI, silently and permanently. Use `3.0.0b29`, the value the
    requirement and the ROADMAP criterion carry, so criterion 3 matches literally — the cited note
    uses a different beta number and both evaluate the same way, but the requirement's value is the
    one to write here. State that the risk is latent today and armed by a single future action.

    **Cite, do not restate.** Point at `.planning/notes/999.9-repo-rename-impact-analysis.md` and
    name its section by heading for the full walkthrough. Do not reproduce the walkthrough: a second
    copy in a 77-line onboarding brief both bloats it and can drift from the note that owns it. Cite
    by content and not by line number, using the same idiom the section's third bullet already states
    and for the same reason — that note's own line-numbered citation of the host source has already
    gone stale across three refs.

    Change nothing else in the file. The section must hold five bullets after this edit, and the
    file must be 78 lines.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md` is 78 lines after this task
    - The `## Milestone close and branch protection` section holds exactly 5 bullets
    - The new bullet is a single line and the section's final bullet is still the one pointing at the close-procedure note
    - The new bullet contains `3.0.0b29`, `1.36`, and the path `.planning/notes/999.9-repo-rename-impact-analysis.md`
    - The new bullet names the cited section by its heading text `Standing rule this must produce`
    - The new bullet states both the rule and its consequence — that a stranded CLI would be told the firmware is current
    - No line in `CLAUDE.md` carries a `path:NN` line-numbered citation introduced by this task
    - The four pre-existing bullets in the section are byte-identical
  </acceptance_criteria>
  <verify>
    <automated>wc -l < /workspaces/CLAUDE.md</automated>
    <fails_when>prints any number other than 78</fails_when>
    <automated>awk '/^## Milestone close and branch protection$/,0' /workspaces/CLAUDE.md | /usr/bin/grep -c '^- '</automated>
    <fails_when>prints any number other than 5, or exits non-zero — a 4 means the bullet was not added, and a 6 or more means the mechanism was restated across extra bullets instead of cited</fails_when>
    <automated>/usr/bin/grep -c -F -e '3.0.0b29' -e 'Standing rule this must produce' -e '.planning/notes/999.9-repo-rename-impact-analysis.md' /workspaces/CLAUDE.md</automated>
    <fails_when>prints a number below 3, or exits non-zero — a shortfall means the requirement's version token, the cited section heading or the note path is missing</fails_when>
    <automated>awk '/^## Milestone close and branch protection$/,0' /workspaces/CLAUDE.md | /usr/bin/grep -c 'v135-close-procedure-under-protection.md'</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>tail -1 /workspaces/CLAUDE.md | /usr/bin/grep -cF 'v135-close-procedure-under-protection.md'</automated>
    <fails_when>prints any number other than 1, or exits non-zero — the "see the note for mechanics" bullet must remain the section's last line</fails_when>
    <automated>/usr/bin/grep -cE '\.(md|py|sh|cpp|h):[0-9]+' /workspaces/CLAUDE.md || true</automated>
    <fails_when>prints any number other than 0 — a line-numbered citation is exactly what the file's own third bullet says not to write</fails_when>
    <automated>/usr/bin/grep -c -F -e '`main` is protected in all three repositories' -e "This project's close targets" -e 'recreate local `beta` from `origin/beta`' /workspaces/CLAUDE.md</automated>
    <fails_when>prints a number below 3, or exits non-zero — the pre-existing bullets must survive the insert untouched</fails_when>
  </verify>
  <done>An agent opening `CLAUDE.md` before a milestone close meets the no-Releases rule and can see, in one line, why publishing one would make every stranded CLI believe its firmware is current — without opening another file to find out.</done>
</task>

<task type="auto">
  <name>Task 2: The archaeology pointer, and the sentence this phase made false</name>
  <read_first>
    - /workspaces/CLAUDE.md — the file being modified; read the `## Repository Structure` paragraph in full, including its inventory of the meta `tools/` directory and its closing bolded clause
    - /workspaces/.planning/notes/gitmodules-archaeology-trap.md — the note being pointed at; read its headings so the pointer describes what is actually there
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md — the conventions of that paragraph: one dense unwrapped line, facts chained with semicolons and em-dashes, bold reserved for the load-bearing negative
    - /workspaces/tools/ — the directory listing, so the corrected inventory names what is actually there
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Two changes in the `## Repository Structure` area, both in one edit.

    **The correction.** The existing paragraph's inventory of the meta `tools/` directory asserts a
    single occupant. Plan 193-01 added a second, `tools/adoption/`, so that clause is now false.
    Rewrite the clause so it names both occupants — the catalog tooling and the adoption instrument —
    and says in a few words what each is for, keeping the paragraph's style: one line, no wrapping,
    facts chained with semicolons and em-dashes, the closing bolded clause about the absent wiki
    guard preserved. Leave every other fact in the paragraph byte-identical, including the retirement
    dates and the short sha it carries. A phase that leaves a freshly-false sentence in the file it is
    editing has authored a stale reference one milestone after this project spent a phase removing
    them.

    **The pointer.** Add a new single-line paragraph immediately after that paragraph, separated by
    one blank line. It states the trap in one clause and points at the note. Content:
    `.gitmodules` records a submodule URL per commit, so checking out a pre-rename ref such as `v1.35`
    or bisecting firmware history resurrects the old firmware URL from that commit; fixing the live
    branches does not fix history. Bold the load-bearing negative — the clause a skimming reader must
    not miss — in the same way the existing paragraph bolds its own. Point at
    `.planning/notes/gitmodules-archaeology-trap.md` for both workarounds, their executed transcripts,
    and the `git submodule sync` hazard that silently undoes one of them. State in the same line that
    the trap does not bite today, because the old slug still redirects, and that it arms only if the
    freed slug is ever claimed — a reader who meets this pointer must not conclude their clone is
    already broken.

    Cite the note by path, and by section heading where a specific section is meant. No line numbers.

    Change nothing else. The file must be 80 lines after this task, and the close section must still
    hold five bullets.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md` is 80 lines after this task
    - The repository-structure paragraph names both `catalog/` and `adoption/` as occupants of meta `tools/`
    - The previous single-occupant claim about meta `tools/` no longer appears anywhere in the file
    - The paragraph's retirement dates and its short sha `5426d7ef` are unchanged, and its closing bolded clause about the absent wiki guard survives
    - A new single-line paragraph follows it, separated by one blank line, naming `.gitmodules`, `v1.35` and `.planning/notes/gitmodules-archaeology-trap.md`
    - The new paragraph states that the trap does not bite today because the old slug still redirects
    - The new paragraph contains a bolded clause
    - The `## Milestone close and branch protection` section still holds exactly 5 bullets, and `## System Overview` is still present
    - No `path:NN` line-numbered citation was introduced
  </acceptance_criteria>
  <verify>
    <automated>wc -l < /workspaces/CLAUDE.md</automated>
    <fails_when>prints any number other than 80</fails_when>
    <automated>/usr/bin/grep -cF 'now holds `catalog/` alone' /workspaces/CLAUDE.md || true</automated>
    <fails_when>prints any number other than 0 — the single-occupant claim is false once the adoption instrument exists</fails_when>
    <automated>/usr/bin/grep -c -F -e 'tools/adoption' -e 'tools/catalog' /workspaces/CLAUDE.md</automated>
    <fails_when>prints a number below 2, or exits non-zero — the corrected inventory must name both occupants</fails_when>
    <automated>/usr/bin/grep -c -F -e '.planning/notes/gitmodules-archaeology-trap.md' -e 'v1.35' -e '.gitmodules' /workspaces/CLAUDE.md</automated>
    <fails_when>prints a number below 3, or exits non-zero — the pointer must name the note, the concrete ref a reader will hit, and the file that causes the trap</fails_when>
    <automated>/usr/bin/grep -c -F -e '5426d7ef' -e 'no automated wiki guard exists now' /workspaces/CLAUDE.md</automated>
    <fails_when>prints a number below 2, or exits non-zero — the surrounding facts in the rewritten paragraph must survive the edit</fails_when>
    <automated>/usr/bin/grep -n 'gitmodules-archaeology-trap.md' /workspaces/CLAUDE.md | cut -d: -f1</automated>
    <fails_when>prints anything other than a single number between 13 and 15 — the pointer belongs in the repository-structure area, not in the close section at the end of the file</fails_when>
    <automated>awk '/^## Milestone close and branch protection$/,0' /workspaces/CLAUDE.md | /usr/bin/grep -c '^- '</automated>
    <fails_when>prints any number other than 5, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c '^## ' /workspaces/CLAUDE.md</automated>
    <fails_when>prints any number other than 5, or exits non-zero — this phase adds no heading to the file</fails_when>
  </verify>
  <done>Someone starting an archaeology session from `CLAUDE.md` learns the trap exists, learns it is not biting them today, and is one click from the note with both workarounds — and the file no longer contains a claim this phase falsified.</done>
</task>

<task type="auto">
  <name>Task 3: The phase answered against its own four criteria</name>
  <read_first>
    - /workspaces/.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-disposition.md — the analog: one section per criterion, every claim naming the evidence file that supports it
    - /workspaces/.planning/ROADMAP.md — the Phase 193 section and its four success criteria, verbatim; scope the read with a targeted search rather than reading the whole file
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-01-instrument-run.txt — the instrument readings cited by criteria 1 and 2
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt — cited by criterion 4
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt — cited by criterion 4
  </read_first>
  <files>.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md</files>
  <action>
    Write `evidence/193-disposition.md`: a criterion-by-criterion record in the shape of the two
    prior phases' disposition files.

    A `#` title and a `**Date:**` line, then four sections headed `## Criterion 1` through
    `## Criterion 4`, each carrying the criterion's text verbatim from the ROADMAP's Phase 193
    section, then the disposition, then the evidence.

    - **Criterion 1** — an instrument reports per-version download share, and the seed's trigger names
      a threshold and an observation window. Cite `tools/adoption/pypi_version_share.sh` by path, the
      live reading in `193-gate-01-instrument-run.txt` by filename and reading number, and the seed's
      rewritten `trigger_condition` by file. State the threshold and the window as they were written.
    - **Criterion 2** — the record states what the instrument cannot see. Cite the caveat block's
      banner line in the instrument's output and the corresponding prose in the seed, and state the
      three limits: installed base is not observable, download share is a proxy, and the never-upgrade
      residual cannot be driven to zero by any threshold.
    - **Criterion 3** — the no-Releases rule recorded with its mechanism. Cite the `CLAUDE.md` section
      and the cited note section by heading, and reproduce the one-line mechanism so the disposition
      is readable standalone.
    - **Criterion 4** — the trap carries a workaround demonstrated for both cases. Cite both
      transcripts by filename, name the pre-rename ref used and why it was chosen, and state that the
      third transcript records the hazard that undoes one workaround.

    Where a claim is a measurement, name the transcript and the reading that recorded it. Where a
    claim is a projection, say so — do not let a criterion's disposition launder a projection into an
    observation.

    Close with a short honest-limits paragraph: the instrument's integers move daily, so the figures
    in this file are a capture and not a standing fact; and the post-claim failure shape remains
    unverified by construction.

    Commit the disposition and `CLAUDE.md`'s two edits together on the current branch.
  </action>
  <acceptance_criteria>
    - The file carries the four headings `## Criterion 1` through `## Criterion 4`
    - Each section reproduces its criterion's text and names at least one artefact path or evidence filename
    - Criterion 1's section names both `tools/adoption/pypi_version_share.sh` and the seed file, and states the threshold and the window
    - Criterion 2's section states all three limits
    - Criterion 3's section names the cited note section by heading and reproduces the one-line mechanism
    - Criterion 4's section names both workaround transcripts, the pre-rename ref, and the hazard transcript
    - The file carries a closing statement that the instrument's figures are a capture rather than a standing fact
    - The file names no criterion as satisfied without naming what satisfies it
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c -E '^## Criterion [1-4]' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md</automated>
    <fails_when>prints any number other than 4, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c -F -e 'tools/adoption/pypi_version_share.sh' -e 'SEED-claim-firestarter-slug.md' -e '193-gate-01-instrument-run.txt' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md</automated>
    <fails_when>prints a number below 3, or exits non-zero — criteria 1 and 2 must name the artefacts that satisfy them</fails_when>
    <automated>/usr/bin/grep -c -F -e '193-gate-03-fresh-clone.txt' -e '193-gate-03-existing-clone.txt' -e '193-gate-03-submodule-sync-hazard.txt' -e 'v1.35' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md</automated>
    <fails_when>prints a number below 4, or exits non-zero — criterion 4 must name both demonstrations, the hazard transcript and the ref they were run against</fails_when>
    <automated>/usr/bin/grep -c -F -e 'Standing rule this must produce' -e '3.0.0b29' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md</automated>
    <fails_when>prints a number below 2, or exits non-zero — criterion 3 must be readable standalone, which means carrying the mechanism and the cited section heading</fails_when>
    <automated>/usr/bin/grep -ciE 'move daily|a capture|not a standing fact' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-disposition.md</automated>
    <fails_when>prints 0, or exits non-zero — a disposition that reads its figures as permanent misleads the next reader</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>The phase's four ROADMAP criteria each have a written disposition naming the artefact or transcript that answers them, and the disposition is explicit about which of its figures are captures rather than standing facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CLAUDE.md` → every agent that acts in this repository | The last instruction read before an irreversible outward-facing act |
| `CLAUDE.md` → the notes it cites | A citation that goes stale misdirects the reader who needed it most |
| a published GitHub Release on the meta repository → every stranded CLI's `fw` command | A single future action inverts a loud 404 into a silent, permanent wrong answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-50 | Spoofing | `fw`'s update check against the meta repository after the claim | high | mitigate | A `v1.36` tag parses as PEP 440 `1.36` and `3.0.0b29 >= 1.36` reads true, so one published Release makes `fw` report firmware current forever. Mitigated by recording the rule with its mechanism in the one file an agent reads before acting, in the section a milestone close visits, with a grep-checked presence of the version token, the note path and the cited section heading. |
| T-193-51 | Repudiation | the cited mechanism | medium | mitigate | The mechanism is cited, never copied, so there is one source of truth and no second copy to drift. Bounded by a verify leg requiring the close section to hold exactly five bullets. |
| T-193-52 | Tampering | citations added by this plan | medium | mitigate | Every citation is by path and section heading. A scan for line-numbered source citations must return 0; the cited note's own line-numbered citation is already stale across three refs, which is the demonstration of the risk. |
| T-193-53 | Repudiation | the repository-structure paragraph | medium | mitigate | This phase falsified the meta tooling inventory by adding a second occupant. The correction lands in the same edit as the pointer, checked by a negative grep for the single-occupant claim and a positive grep for both occupants. |
| T-193-54 | Denial of service | a reader misreading the pointer | low | accept | A reader could conclude their clone is already broken. Mitigated in wording — the pointer says the trap does not bite today — and accepted beyond that, because the remaining risk is a moment of unnecessary caution rather than damage. |
| T-193-SC | Tampering | npm/pip/cargo installs | high | mitigate | No plan in this phase runs a package-manager install. 193-RESEARCH.md records the Package Legitimacy Audit as not applicable; this plan edits one markdown file and writes one record. |
</threat_model>

<verification>
- The no-Releases rule and its reason are one line in the section a close visits, and the mechanism is cited rather than copied.
- The archaeology pointer sits in the repository-structure area, names the concrete ref, and says the trap is not biting today.
- The sentence this phase falsified is corrected in the same edit that falsified it.
- No line-numbered citation is introduced anywhere.
- Each of the four ROADMAP criteria has a written disposition naming what answers it.
</verification>

<success_criteria>
`CLAUDE.md` is 80 lines, its close section holds five single-line bullets with the new rule between
the fourth-from-last and the last, its repository-structure area carries a corrected tooling
inventory and a new archaeology pointer, and
`evidence/193-disposition.md` answers the phase's four criteria with named artefacts.
</success_criteria>

<output>
Create `.planning/phases/193-the-deferred-claim-made-measurable/193-05-SUMMARY.md` when done.
Record the rationale and decision identifiers behind both `CLAUDE.md` edits there — they do not
belong in `CLAUDE.md` itself.
</output>
