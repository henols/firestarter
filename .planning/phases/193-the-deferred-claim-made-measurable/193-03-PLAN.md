---
phase: 193-the-deferred-claim-made-measurable
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt
  - .planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt
  - .planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt
autonomous: true
requirements: [GATE-03]

estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Both `.gitmodules` workarounds are demonstrated by execution, not by argument (D-16): a fresh clone at a pre-rename ref initialised with `--no-recurse-submodules` plus a manual URL set, and an existing clone whose `.git/config` override survives a checkout back to a pre-rename ref."
    - "The pre-rename ref is one a stranger can actually reach. `v1.35` is a published, immutable tag whose `.gitmodules` still names the old firmware slug; the Phase 189 parent commit is NOT used, because it lives only on an unpushed milestone branch and `git cat-file -t` on it fails in a fresh clone."
    - "Order is load-bearing and the transcripts record it: the `.git/config` override must be set BEFORE `git submodule update --init`. `git submodule init` preserves an existing `submodule.<name>.url` rather than re-copying it from `.gitmodules`, so setting the override first works and setting it afterwards would already have cloned the wrong repository."
    - "No transcript claims a breakage that has not happened. The old firmware slug still redirects today, so a plain `git submodule update --init` at the pre-rename ref succeeds; that benign reading is captured in the same file as the workaround, labelled as such, and the transcripts state that the failure the workaround prevents arms only when the claim fires."
    - "The empty-versus-populated submodule directory edge is handled rather than tripped over: `git -C firestarter <cmd>` on an empty directory silently walks up and answers for the PARENT repository, and a leftover `firestarter/` or `.git/modules/firestarter` makes `submodule update` abort with a second-clone error that reads exactly like the trap and is not. Every transcript starts from a freshly created temporary directory and asserts the child checkout exists before reading anything from it."
    - "`git submodule sync` destroys the existing-clone workaround, and the transcript proves it with a before-and-after reading plus the two-command repair. It clobbers both `.git/config`'s `submodule.<name>.url` AND the child's own `origin` remote, so a repair that sets only one of them leaves the clone still pointing at the wrong place."
    - statement: "The workaround and the repair are ordered procedures whose steps cannot be reordered without changing the outcome, and each transcript records its commands in executed order with a per-reading capture timestamp, so two independently produced transcripts are comparable line for line."
      verification: backstop
    - "The operator's working clone at `/workspaces` is not modified by any of this work: every command runs inside a disposable clone under a `mktemp -d` directory, and the working clone's `submodule.firestarter.url` reads the same value before and after."
  artifacts:
    - path: ".planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt"
      provides: "The executed fresh-clone workaround at the published pre-rename ref, plus the labelled benign-today reading taken with no override"
      contains: "capture_date:"
    - path: ".planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt"
      provides: "The executed existing-clone workaround — the .git/config override surviving a checkout back to the pre-rename ref and being honoured by submodule update"
      contains: "capture_date:"
    - path: ".planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt"
      provides: "The executed reproduction of the sync hazard and its two-command repair, with before-and-after readings of both the config override and the child's origin"
      contains: "capture_date:"
  key_links:
    - from: ".git/config submodule.firestarter.url"
      to: "the URL git actually fetches from"
      via: "the config override takes precedence over the .gitmodules value recorded at the checked-out ref"
      pattern: "submodule.firestarter.url"
    - from: "git submodule sync"
      to: "both .git/config and the child's origin remote"
      via: "sync re-reads .gitmodules at the current HEAD and overwrites the override in two places"
      pattern: "remote (get-url|set-url) origin"
  prohibitions:
    - requirement_id: GATE-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT record a breakage that has not happened. The old slug still redirects, so no transcript may show or describe a failed clone as today's behaviour, and the projected post-claim failure must not appear in a transcript as an observation."
    - requirement_id: GATE-03
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT mutate the operator's working clone at `/workspaces` — not its `.git/config`, not its `.git/modules/`, not its submodule checkouts. Every demonstration runs inside a disposable clone."
    - requirement_id: GATE-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present a dirty-state artifact as the trap. A second-clone abort caused by a leftover directory looks like the failure being documented and is not; a transcript that cannot prove it started clean proves nothing."
    - requirement_id: GATE-03
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT commit credential material into a transcript. Record URLs, refs, shas and git's own output; do not dump the environment, a full `git config --list`, or any SSH or token material."
---

<objective>
Execute both `.gitmodules` workarounds for real and commit the transcripts, so GATE-03 rests on
observation rather than on an argument.

D-16 settles that "demonstrated" means executed, with transcripts under the phase's `evidence/`
directory, and that the pre-rename ref must be one that actually reproduces the trap. It also
settles the honesty constraint: the trap does not bite today, so these transcripts show the
mechanism and its fix and say plainly that the failure arms only when the claim fires.

This plan produces the raw readings. Plan 193-04 turns them into the note that `CLAUDE.md` will
cite.

Purpose: GATE-03's "demonstrated for both cases" clause, and ROADMAP criterion 4.
Output: three transcripts under
`.planning/phases/193-the-deferred-claim-made-measurable/evidence/`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-the-deferred-claim-made-measurable/193-CONTEXT.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md
</context>

<environment_notes>
PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, so it silently under-scans. Every
evidence-bearing command in this plan uses the absolute `/usr/bin/grep`.

`/usr/bin/grep -c` exits 1 when the count is 0, so a leg that legitimately expects 0 carries a
trailing `|| true` and is judged on its printed number, not on its exit status.

The meta repository clones over HTTPS at `https://github.com/henols/firestarter_prom.git`. Its
submodule URLs are SSH-form (`git@github.com:…`), so the submodule fetches need working SSH
authentication to github.com; research confirmed it is authenticated as `henols` in this container.

Every disposable clone lives under a directory created with `mktemp -d` and is removed at the end of
its task. Nothing in this plan runs a `git` mutation against `/workspaces`.
</environment_notes>

<artifacts_this_phase_produces>
Symbols created by phase 193. Newly-created names are not drift against any existing source.

- `tools/adoption/` and `tools/adoption/pypi_version_share.sh` — new; no arguments, no CLI flags.
  Stdout keys `window:`, `fixed_downloads_ge_2_0_9:`, `at_risk_downloads_2_0_7:`,
  `fixed_share_pct:`, `threshold:`, `TRIGGER:`, banner `WHAT THIS DOES NOT MEASURE`; exit codes
  0, 1, 2.
- `.planning/notes/gitmodules-archaeology-trap.md` — new note (plan 193-04).
- `.planning/phases/193-the-deferred-claim-made-measurable/evidence/` — new directory holding
  `193-gate-01-instrument-run.txt`, `193-gate-03-fresh-clone.txt`,
  `193-gate-03-existing-clone.txt`, `193-gate-03-submodule-sync-hazard.txt` and
  `193-disposition.md`.
- Transcript key names introduced by this plan: `capture_date:`, `capture_host_commit:`,
  `meta_source:`, `meta_ref:`, `meta_commit:`, `gitmodules_url(firestarter):`,
  `config_url(firestarter):`, `remote_origin(firestarter):`, `firestarter_head:`,
  `clean_state_proof:`, and the terminal verdict lines `FRESH CLONE WORKAROUND OK`,
  `EXISTING CLONE WORKAROUND OK` and `SYNC HAZARD REPRODUCED AND REPAIRED`.
- `.planning/phases/193-the-deferred-claim-made-measurable/COVERAGE.md` — planner-authored.
- `CLAUDE.md` gains one bullet and one paragraph (plan 193-05). No new headings.
- `.planning/seeds/SEED-claim-firestarter-slug.md` keeps its four frontmatter keys; no new key.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: A fresh clone at the published pre-rename ref, worked around and recorded</name>
  <precondition>`git ls-remote --tags https://github.com/henols/firestarter_prom.git v1.35` prints a sha, and `git ls-remote git@github.com:henols/firestarter_fw.git HEAD` succeeds — the tag must be reachable in a fresh clone and SSH to github.com must authenticate, or the demonstration cannot be executed and must not be narrated instead.</precondition>
  <read_first>
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-2 (why `v1.35` and not the Phase 189 parent commit), §F-5 (today's benign behaviour and the two dirty-state artifacts), §Code Examples 4 and 7
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md — §`${phase_dir}/evidence/*.txt` for the transcript register and naming convention
    - /workspaces/.planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt — the analog transcript, including how it labels an expected-and-not-a-regression reading
    - /workspaces/.planning/notes/999.9-repo-rename-impact-analysis.md — the section on the archaeology trap, which names `v1.35` and prescribes the two workarounds this plan executes
  </read_first>
  <files>.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt</files>
  <action>
    Execute the fresh-clone workaround and capture it. Work entirely inside a directory created with
    `mktemp -d`; remove it when the transcript is written.

    **Why `v1.35` and not the obvious commit.** The commit that landed the `.gitmodules` repoint is
    `5aba9dbc8d760e530638931d19ede0a227dd1a60` and its parent `9ccf0414d7a2d86e5e97b6cf1db2da01737bfc81`
    still carries the old URL — but that parent lives only on the unpushed milestone branch, and
    `git cat-file -t` on it fails in a fresh clone. `v1.35` is a published, immutable tag whose
    `.gitmodules` still reads `url = git@github.com:henols/firestarter.git`, and it is the very ref
    the 999.9 note names when it describes the trap. Use it, and state this reasoning in the
    transcript so a later reader does not "improve" the ref back into unreachability.

    **READING 1 — the workaround.** In the temp directory:
    clone with `git clone --no-recurse-submodules https://github.com/henols/firestarter_prom.git`;
    `git checkout v1.35`; record the resulting HEAD sha and the verbatim `.gitmodules` at that ref;
    record `git config --get submodule.firestarter.url` **before** the override, which is unset;
    set `git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git`; re-read it;
    run `git submodule update --init --depth 1 firestarter`; then — only after confirming the child
    directory is non-empty — read `git -C firestarter remote get-url origin` and the child's HEAD sha.

    The order is the point: the override is set before the update. `git submodule init` preserves an
    existing `submodule.<name>.url` instead of re-copying it from `.gitmodules`, which is why setting
    it first works. Record the readings in that executed order and say so.

    **READING 2 — today's behaviour with no override, labelled.** In a second, separate temp clone at
    the same ref, run `git submodule update --init --depth 1 firestarter` with no override and record
    what happens. It succeeds and fetches from the old slug, because the redirect is still live.
    Label this reading plainly: this is the state of the world on the capture date; the workaround
    exists for the state that arrives when the freed slug is claimed, and that state has not arrived.
    Do not write, or imply, a failure that did not occur.

    **Clean-state discipline.** Before each `submodule update`, record a `clean_state_proof:` line
    showing the child path and `.git/modules/firestarter` are absent. A leftover directory makes
    `submodule update` abort with a second-clone error that reads exactly like the trap and is not,
    and `git -C firestarter …` on an empty directory silently answers for the parent repository. A
    transcript that cannot prove it started clean proves nothing.

    Write `evidence/193-gate-03-fresh-clone.txt` in the 189 register: an ALL-CAPS title line with a
    `=====` underline; `capture_date:` and `capture_host_commit:`; a `Captured by:` line naming the
    commands and the `/usr/bin/grep` caveat; the two readings separated by a `====` banner, each with
    its own `capture_date:` and `snake_case_key(qualifier): value` observables using full 40-character
    shas; prose after each reading saying which line is the discriminator; and the terminal verdict
    line `FRESH CLONE WORKAROUND OK`.

    Remove the temp directory. Commit the transcript on the current branch.
  </action>
  <acceptance_criteria>
    - `git show v1.35:.gitmodules` names `git@github.com:henols/firestarter.git` for the firmware submodule — the ref genuinely reproduces the pre-rename state
    - The transcript records the override being set before `submodule update --init`, in that order
    - After the override, the child's `origin` reads `git@github.com:henols/firestarter_fw.git` and the child's HEAD is a full 40-character sha
    - READING 2 records that a plain update with no override succeeds today, and labels it as the current state of the world rather than as a failure
    - Every `submodule update` in the transcript is preceded by a `clean_state_proof:` line
    - The transcript carries two `capture_date:` lines and the terminal line `FRESH CLONE WORKAROUND OK`
    - `/workspaces` is untouched: `git -C /workspaces config --get submodule.firestarter.url` reads `git@github.com:henols/firestarter_fw.git` after the task, the same value as before
    - No temp clone remains on disk
  </acceptance_criteria>
  <verify>
    <automated>git -C /workspaces show v1.35:.gitmodules > "${TMPDIR:-/tmp}/193-v135-gitmodules.txt"; echo "git_show_rc=$?"; /usr/bin/grep -cF 'url = git@github.com:henols/firestarter.git' "${TMPDIR:-/tmp}/193-v135-gitmodules.txt"</automated>
    <fails_when>the first printed line is not `git_show_rc=0`, or the count on the second line is anything other than 1 — a 0 count means the chosen ref does not carry the pre-rename URL and the demonstration would prove nothing</fails_when>
    <automated>git ls-remote --tags https://github.com/henols/firestarter_prom.git v1.35 > "${TMPDIR:-/tmp}/193-v135-tag.txt"; echo "ls_remote_rc=$?"; wc -l < "${TMPDIR:-/tmp}/193-v135-tag.txt"</automated>
    <fails_when>the first printed line is not `ls_remote_rc=0`, or the line count on the second line is anything other than 1 — a 0 means the ref is unreachable from a fresh clone, which is exactly the defect that disqualified the milestone-branch commit</fails_when>
    <automated>/usr/bin/grep -c '^capture_date:' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt</automated>
    <fails_when>prints a number below 2, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c '^clean_state_proof:' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt</automated>
    <fails_when>prints a number below 2, or exits non-zero — a reading without a clean-state proof cannot distinguish the trap from a leftover directory</fails_when>
    <automated>/usr/bin/grep -cF 'remote_origin(firestarter): git@github.com:henols/firestarter_fw.git' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt</automated>
    <fails_when>prints a number below 1, or exits non-zero — the whole point of the workaround is that the child ends up on the new URL despite the ref declaring the old one</fails_when>
    <automated>/usr/bin/grep -cE '^[a-z_]+\(firestarter\): [0-9a-f]{40}$' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt</automated>
    <fails_when>prints 0, or exits non-zero — abbreviated shas are not reproducible readings</fails_when>
    <automated>/usr/bin/grep -cF 'FRESH CLONE WORKAROUND OK' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>git -C /workspaces config --get submodule.firestarter.url</automated>
    <fails_when>prints anything other than `git@github.com:henols/firestarter_fw.git`, or exits non-zero — the working clone must be exactly as it was before this task</fails_when>
  </verify>
  <done>A stranger can reproduce the fresh-clone workaround from the transcript: a published ref, the commands in the order they must be run, the readings that show the child ended up on the right repository, and an explicit statement that today's un-worked-around behaviour still succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: An existing clone — the override outlives the checkout</name>
  <read_first>
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-3 (the override survives a checkout; `submodule init` does not clobber it) and §Code Examples 5
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt — the transcript written in task 1; match its register and key names exactly
    - /workspaces/.planning/notes/999.9-repo-rename-impact-analysis.md — the archaeology-trap section, which prescribes `git config submodule.firestarter.url` for this case
  </read_first>
  <files>.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt</files>
  <action>
    Demonstrate the second of D-16's two cases: a clone that already exists and then travels back to a
    pre-rename ref. Work in a fresh `mktemp -d` directory, not in `/workspaces`.

    Clone the meta repository with `--no-recurse-submodules` and leave HEAD on the default branch,
    which is a post-rename state. Record `.gitmodules`' firmware URL at that ref and set the
    `.git/config` override to `git@github.com:henols/firestarter_fw.git` — this is the standing state
    of a maintained clone, and it is what the operator's own `/workspaces` clone carries.

    Then `git checkout v1.35` — the pre-rename ref — and take the discriminating readings:
    `.gitmodules` at the new HEAD now declares the **old** URL, while
    `git config --get submodule.firestarter.url` still reads the **new** one. That divergence is the
    whole mechanism: when the two disagree, `.git/config` wins; when they agree, nothing observable
    changes and the override is invisible. Say that in prose beside the readings, because a reader who
    sees only matching values cannot tell which one was in force.

    Then run `git submodule update --init --depth 1 firestarter`, preceded by a `clean_state_proof:`
    line, and confirm the child was fetched from `firestarter_fw` despite the checked-out ref
    declaring otherwise. Read the child's `origin` only after confirming the child directory is
    non-empty — on an empty directory `git -C firestarter …` walks up and answers for the parent
    repository, which would put a false reading into the file.

    Write `evidence/193-gate-03-existing-clone.txt` in the same register as task 1's transcript: title
    and `=====` underline, `capture_date:`, `capture_host_commit:`, a `Captured by:` line, the
    readings in executed order with full 40-character shas, prose naming the discriminator, and the
    terminal verdict line `EXISTING CLONE WORKAROUND OK`.

    Remove the temp directory. Commit the transcript on the current branch.
  </action>
  <acceptance_criteria>
    - The transcript records `.gitmodules` at `v1.35` naming the old slug and `.git/config` naming `firestarter_fw` at the same moment — the divergence is visible in two adjacent readings
    - The transcript states in prose that `.git/config` wins when the two disagree and is invisible when they agree
    - `submodule update` at the pre-rename ref fetches from `firestarter_fw`, and the child's `origin` is read only after the child directory is confirmed non-empty
    - Exactly one `clean_state_proof:` line precedes the update
    - The transcript carries the terminal line `EXISTING CLONE WORKAROUND OK` and uses full 40-character shas
    - `/workspaces` is untouched: its `submodule.firestarter.url` reads `git@github.com:henols/firestarter_fw.git` after the task
    - No temp clone remains on disk
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c -F -e 'gitmodules_url(firestarter): git@github.com:henols/firestarter.git' -e 'config_url(firestarter): git@github.com:henols/firestarter_fw.git' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt</automated>
    <fails_when>prints a number below 2, or exits non-zero — both halves of the divergence must appear, or the transcript does not show the mechanism</fails_when>
    <automated>/usr/bin/grep -cF 'remote_origin(firestarter): git@github.com:henols/firestarter_fw.git' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt</automated>
    <fails_when>prints a number below 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c '^clean_state_proof:' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cE '^[a-z_]+\(firestarter\): [0-9a-f]{40}$' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt</automated>
    <fails_when>prints 0, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cF 'EXISTING CLONE WORKAROUND OK' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>git -C /workspaces config --get submodule.firestarter.url</automated>
    <fails_when>prints anything other than `git@github.com:henols/firestarter_fw.git`, or exits non-zero</fails_when>
  </verify>
  <done>The existing-clone case is on the record as an execution: two adjacent readings show `.gitmodules` and `.git/config` disagreeing at the pre-rename ref, and the fetch follows `.git/config`.</done>
</task>

<task type="auto">
  <name>Task 3: The hazard that quietly undoes the workaround, reproduced and repaired</name>
  <read_first>
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-4 (the reproduction and the verified two-command repair) and §Common Pitfalls 4
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt — the transcript written in task 2; this one continues from the same state and must match its register
    - /workspaces/.planning/notes/999.9-repo-rename-impact-analysis.md — the ordered procedure whose Phase A step 2 instructs `git submodule sync --recursive` as routine hygiene, which is what makes this hazard reachable
  </read_first>
  <files>.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt</files>
  <action>
    Reproduce the failure mode of the workaround itself, and its repair. Work in a fresh `mktemp -d`
    directory.

    Rebuild the existing-clone state from task 2 — clone with `--no-recurse-submodules`, set the
    `.git/config` override to `git@github.com:henols/firestarter_fw.git`, check out `v1.35`, and run
    `git submodule update --init --depth 1 firestarter` so the child exists and points at
    `firestarter_fw`.

    **READING 1 — before.** Record `git config --get submodule.firestarter.url` and
    `git -C firestarter remote get-url origin`. Both read `firestarter_fw`.

    **READING 2 — the hazard.** Run `git submodule sync firestarter` while HEAD is still at the
    pre-rename ref. Record its output line and then re-read both values. Both now read the **old**
    slug: `sync` re-reads `.gitmodules` at the current HEAD and overwrites the override, and it
    rewrites the child's own `origin` as well. State in prose why this matters: the 999.9 note's own
    ordered procedure instructs `git submodule sync --recursive` as routine hygiene, so an operator
    who has internalised that step will undo the workaround with no warning and no error.

    **READING 3 — the repair.** Run both repair commands —
    `git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git` and
    `git -C firestarter remote set-url origin git@github.com:henols/firestarter_fw.git` — and re-read
    both values. Then state the discriminator explicitly: `sync` clobbers **two** places, so a repair
    that sets only the config override leaves the child's own remote still pointing at the old slug,
    and a reader who checks only one of the two readings would conclude the repair worked when it
    half did.

    Note in prose that re-running `sync` at the same HEAD immediately re-clobbers both, so the repair
    is a repair and not a fix — the only durable answer at a pre-rename ref is not to run `sync`
    there.

    Write `evidence/193-gate-03-submodule-sync-hazard.txt` in the same register as tasks 1 and 2,
    with the terminal verdict line `SYNC HAZARD REPRODUCED AND REPAIRED`.

    Remove the temp directory. Commit the transcript on the current branch.
  </action>
  <acceptance_criteria>
    - The transcript carries three readings: both values on the new slug, both values on the old slug after `sync`, and both back on the new slug after the two-command repair
    - Both clobbered locations are recorded separately — the `.git/config` override and the child's `origin` remote — so a partial repair is visibly insufficient
    - The transcript states that the neighbouring documented procedure instructs the very command that causes this, and that re-running it re-clobbers
    - The transcript carries three `capture_date:` lines and the terminal line `SYNC HAZARD REPRODUCED AND REPAIRED`
    - `/workspaces` is untouched: its `submodule.firestarter.url` reads `git@github.com:henols/firestarter_fw.git` and its firmware child's `origin` reads the same
    - No temp clone remains on disk
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c '^capture_date:' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt</automated>
    <fails_when>prints a number below 3, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cF 'config_url(firestarter): git@github.com:henols/firestarter.git' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt</automated>
    <fails_when>prints a number below 1, or exits non-zero — without the clobbered reading the hazard is asserted rather than shown</fails_when>
    <automated>/usr/bin/grep -cF 'remote_origin(firestarter): git@github.com:henols/firestarter.git' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt</automated>
    <fails_when>prints a number below 1, or exits non-zero — the second clobbered location is what makes a one-command repair insufficient</fails_when>
    <automated>/usr/bin/grep -c -F -e 'git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git' -e 'git -C firestarter remote set-url origin git@github.com:henols/firestarter_fw.git' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt</automated>
    <fails_when>prints a number below 2, or exits non-zero — both repair commands must appear literally, because the repair needs both</fails_when>
    <automated>/usr/bin/grep -cF 'SYNC HAZARD REPRODUCED AND REPAIRED' /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>git -C /workspaces config --get submodule.firestarter.url; git -C /workspaces/firestarter remote get-url origin</automated>
    <fails_when>either printed line is anything other than `git@github.com:henols/firestarter_fw.git`, or the command exits non-zero</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>The workaround's own failure mode is on the record with the command that causes it, the two places it damages, and the two commands that undo the damage — so the note plan 193-04 writes can warn about it from evidence rather than from memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disposable clone → the operator's working clone at `/workspaces` | A git mutation escaping the temp directory would damage live submodule wiring |
| local git → github.com over SSH and HTTPS | Untrusted remote content is fetched into a checkout |
| executed output → a committed transcript | A reading that is wrong becomes an asserted fact in the record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-30 | Tampering | `/workspaces` submodule wiring | high | mitigate | Every command runs inside a `mktemp -d` clone; no task runs a git mutation against `/workspaces`. Each task's verify block re-reads `/workspaces`' `submodule.firestarter.url` and the firmware child's `origin` and requires the unchanged value. |
| T-193-31 | Spoofing | the recorded readings | high | mitigate | A leftover `firestarter/` or `.git/modules/firestarter` produces a second-clone abort that reads exactly like the trap. Every update is preceded by a recorded `clean_state_proof:` line, and the child's remote is read only after the child directory is confirmed non-empty, because `git -C` on an empty directory answers for the parent. |
| T-193-32 | Repudiation | the transcripts | high | mitigate | The benign-today reading is captured in the same file as the workaround and labelled; no transcript narrates a failure that did not occur, and the projected post-claim behaviour is left to the note, where it is marked a projection. |
| T-193-33 | Information disclosure | committed transcripts | medium | mitigate | Transcripts record URLs, refs, shas and git's own stdout only. No environment dump, no `git config --list`, no SSH or token material. |
| T-193-34 | Tampering | code fetched from github.com | low | accept | The fetched content is the project's own firmware repository at a pinned sha, checked out but never built or executed. Accepted because nothing from the remote is run and the checkout is deleted with its temp directory. |
| T-193-SC | Tampering | npm/pip/cargo installs | high | mitigate | No plan in this phase runs a package-manager install. 193-RESEARCH.md records the Package Legitimacy Audit as not applicable; this plan uses only the pre-existing system `git` 2.55.0. |
</threat_model>

<verification>
- Both of D-16's cases are executed, not narrated, and each transcript carries the commands that produced it.
- The pre-rename ref is published and immutable, and is proved to still carry the old URL.
- Every update is preceded by a recorded clean-state proof, so no dirty-state artifact is mistaken for the trap.
- The operator's working clone reads identically before and after every task.

<human-check>
Read `evidence/193-gate-03-fresh-clone.txt` READING 2 and confirm it reads as a statement about
today's world rather than as a demonstration of a failure. This is the one claim in the phase that a
grep cannot judge, and D-16 makes it binding.
</human-check>
</verification>

<success_criteria>
Three transcripts exist under the phase's `evidence/` directory, each carrying its capture date, its
commands, full-sha observables, a clean-state proof before every submodule update, and a terminal
verdict line. Together they demonstrate both workarounds, today's benign behaviour and the hazard
that silently undoes the workaround.
</success_criteria>

<output>
Create `.planning/phases/193-the-deferred-claim-made-measurable/193-03-SUMMARY.md` when done.
Record the shas actually observed and any divergence from the research transcripts.
</output>
