---
phase: 190-endpoints-that-do-not-depend-on-a-redirect
plan: 04
type: execute
wave: 3
depends_on: ["190-01", "190-02", "190-03"]
files_modified:
  - .planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh
  - .planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt
  - .planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt
  - .planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt
autonomous: true
requirements: [URL-01, URL-03, URL-04]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-14, the end-to-end demonstration is four live checks against henols/firestarter_fw: resolve the stable channel and print its version and asset URL; resolve the pre channel and print the same; download one real .hex through _download_firmware_file and confirm it parses as Intel HEX; and demonstrate both URL-04 failures for real, showing exit 1 and the named message for each. No bench and no avrdude — the download is the last network step before hardware."
    - "Per RESEARCH Finding 1, resolving successfully is not evidence for URL-01. The bare slug answers with HTTP 301, requests follows redirects by default, and the response body already names firestarter_fw in every asset URL, so the before and after transcripts are identical. The distinguishing observable is the redirect count, and the fixture asserts len(response.history) == 0 on the API call."
    - "That assertion is scoped to the API call only. The asset download fetches a browser_download_url on github.com which legitimately redirects to a release-assets host, so asserting redirect-freedom on the download would fail check 3 for an unrelated and correct reason."
    - "Per D-15, the asset-less case needs no mock on the network side: stable 2.0.6 ships only the leonardo and uno assets while pre 3.0.0b29 ships all four, so uno328pb against the stable channel is a genuine no-asset-for-this-board result from a reachable, correctly-named endpoint. Only the board identity is simulated, because no programmer is attached and the ROADMAP states Bench: none — and the transcript says so in as many words."
    - "Per D-15 and RESEARCH Pitfall 7, the unreachable case is produced by pointing the constant at a non-existent slug in a throwaway process, and the patch must target firestarter.firmware.FIRESTARTER_* rather than firestarter.constants.FIRESTARTER_*: firmware.py imports the constants by name, so rebinding the constants module has no effect on the already-bound names. This is the likeliest way that leg silently demonstrates nothing."
    - "Per D-16, nothing live enters pytest and nothing enters CI. The demonstration is a re-runnable script committed under the phase directory plus its captured transcript, so the suite stays deterministic and offline; Phase 191 re-runs it against the stable it cuts, and a post-claim milestone re-runs it to watch the 404 arrive."
    - "Per RESEARCH Open Question 2, the fixture's redirect assertion is not the standing guard D-05 defers to Phase 192. D-05 defers a repository-wide source-scanning regression check; a live network assertion inside an on-demand, non-CI evidence script is a different instrument with a different failure mode, and D-16 already establishes that such scripts live here."
    - "Per D-17, the Host-CI half of criterion 4 is satisfied by running the five ci.yml gate steps verbatim inside a Python 3.11 environment and committing the transcript. The py3.11 pin is the substance of the decision, not a detail: the devcontainer is py3.12 and has previously been proven to mask real py3.11 CI breakage on beta."
    - "Per D-17, this is the local equivalent and the phase record must say so plainly. The milestone branch is not on origin, Host CI triggers on push to any branch, and a push is outward-facing and operator-gated — so the real Actions run happens when the operator pushes at ship, not here."
    - "D-17 names five steps, which is exactly ci.yml's primary ci job. ci.yml also defines a second job, ci-py32, with two further steps. The breaking test RESEARCH Finding 4 identified lives in the pyusb-absence family but is inside the primary job's scope, so step 4 does catch it; ci-py32 is noted rather than reproduced."
    - "Per D-05, cleanliness is proved with a boundary-aware sweep at verification time rather than a standing guard, and every sweep reading is paired with a henols/firestarter_prom positive control. A zero bare-slug count next to a zero control is a failed gate, not a pass."
    - "The firestarter_app gitlink is advanced in the meta repository as the phase's last act, per the per-phase gitlink norm in force since v1.36. It will name an unpushed commit until the milestone is pushed, exactly as Phase 189 left the firmware gitlink, and the phase record states that rather than leaving a reader to discover it."
  prohibitions:
    - statement: "MUST NOT record any transcript that was not produced by an actual run of the committed script or the actual command it names. A hand-assembled or partially edited transcript is indistinguishable from a real one to every later reader, and this phase's entire criterion-4 claim rests on these files being what they say they are."
      status: resolved
      verification: judgment
    - statement: "MUST NOT push any branch, open any pull request, cut any release, or touch main in any repository. Every outward-facing step in this milestone is operator-gated, a merge to beta fires a pre-release cut in both sub-repositories and publishes the host one to PyPI, and D-17 was chosen precisely so this phase needs no push to prove CI green."
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the evidence script leave the downloaded firmware image, or any scratch tree, behind inside either repository. Removal is by explicit path only — never a wildcard clean — because ignored GSD state and bench artifacts live alongside and a broad clean destroys them."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh"
      provides: "Re-runnable live demonstration of all four D-14 checks against the renamed repository, printing every resolved value on a labelled line before its verdict"
      contains: "ENDPOINT CONTRACT OK"
      min_lines: 150
    - path: ".planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt"
      provides: "The captured fixture run: both channels resolved, one real .hex downloaded and format-checked, and both URL-04 failure modes shown exiting 1 with distinct named messages"
      contains: "redirects: 0"
    - path: ".planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt"
      provides: "The five ci.yml gate steps run verbatim on Python 3.11 with their raw output, and the statement that this is the local equivalent of an Actions run that happens at ship"
      contains: "cov-fail-under=70"
    - path: ".planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt"
      provides: "The boundary-aware bare-slug sweep across the host repository with its non-vacuity positive control"
      contains: "henols/firestarter_prom"
  key_links:
    - from: ".planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh"
      to: "api.github.com/repos/henols/firestarter_fw"
      via: "the installed package's own constants and FirmwareManager methods, driven through a Python 3.11 interpreter resolved from the app working tree"
      pattern: "redirects: 0"
    - from: "the meta repository's firestarter_app gitlink"
      to: "the phase's final app-repository commit"
      via: "a git add of the submodule path in /workspaces followed by a commit there"
      pattern: "firestarter_app"
---

<objective>
Prove the two things criterion 4 asks for: that `fw` resolves a real firmware release end to end
against the renamed repository **without a redirect in the path**, and that Host CI is green on the
milestone branch. Both as captured, re-runnable evidence rather than assertion.

Purpose: everything before this plan is deterministic and offline. This is where the phase meets the
live endpoint, and it is the only place the distinguishing observable exists — a successful resolve
proves nothing about URL-01, because the old slug resolves just as successfully through a transparent
301. The fixture asserts the redirect count, which is the one reading that can tell the two apart.
Output: one committed script, three committed transcripts, and the gitlink advance that pins the
phase's app-repository work into the meta repository.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/190-CONTEXT.md
@.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/190-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

Deliberately NOT `@`-included: `.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/PROJECT.md`.
Everything needed is quoted in `190-CONTEXT.md`.

**Where the work happens.** Every file this plan creates lives in the **meta** repository under the
phase directory, and is committed in `/workspaces` on `v1.38-repository-rename`. The script *reads
from and executes against* the `firestarter_app` submodule but writes nothing into it — except that
Task 3 advances the meta repository's gitlink for that submodule, which is a meta-repository commit.
**Nothing in this plan pushes.**

State measured 2026-09-13 and re-confirmed at planning time — cite it, do not re-measure:

- **Live release state on `henols/firestarter_fw`:** the stable channel resolves to `2.0.6` carrying
  only `firestarter_leonardo.hex` and `firestarter_uno.hex`; the pre channel resolves to `3.0.0b29`
  carrying all four board assets. That asymmetry is what makes the asset-less demonstration genuine
  rather than mocked.
- **Redirect behaviour, measured:** the bare slug returns 200 with a one-hop history whose `Location`
  resolves to the numeric-id form for repository id `810276812`; `firestarter_fw` returns 200 with an
  empty history; a non-existent slug returns 404 with an empty history. Re-confirmed at planning time
  by running the installed CLI, which printed seven stable rows whose asset URLs **already** read
  `henols/firestarter_fw` — through the old constant. The transcript is identical before and after.
- **The real asset download:** `firestarter_uno.hex` at `2.0.6` is 60 768 bytes, returns 200, every
  non-empty line begins with a colon, and the final record is the Intel HEX EOF record. Its final host
  after redirects is a release-assets host — a **legitimate** redirect.
- `_download_firmware_file` writes the fetched file into the app's home directory under the user's
  home, deriving the filename from the URL. That directory also holds the app's own config; **remove
  the downloaded image by explicit path and never remove the directory.**
- The `.venv311` tree at `/workspaces/firestarter_app/.venv311` is Python **3.11.16**, holds an
  editable install pointing at the working tree, and is self-ignoring — its own `.gitignore` contains
  a bare wildcard, so it cannot be committed. Versions in it: `pytest` 9.1.1, `ruff` 0.16.5, `mypy`
  2.3.1, `click` 8.5.0. `uv` 0.12.6 is at `/usr/local/bin/uv` if a fresh environment is ever needed.
- The five `ci.yml` gate steps, verbatim and in order: `pip install -e .[test]`;
  `ruff check firestarter/ tests/`; `ruff format --check firestarter/ tests/`;
  `pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70`;
  `pip install -e . && firestarter --help`. Baseline before this phase: 2129 passed, coverage 84.74%
  against the 70% floor, about six minutes for step 4.
- `ci.yml`'s `paths-ignore` excludes markdown, so a phase changing `.py` files would fire CI. It also
  defines a second job, `ci-py32`, which D-17's five steps do not cover; note its existence in the
  transcript rather than reproducing it.
- `tests/test_flash_path_record_sync.py` asserts whole-repo porcelain, so the app repository must be
  committed clean before step 4 runs or it goes red for unrelated reasons.

The precedent to mirror is Phase 189's, read before writing anything:
`.planning/phases/189-free-the-name/fresh-clone-fixture.sh` for the script skeleton, and
`.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` and
`189-rename-03-fresh-clone.txt` for the transcript shape.

Devcontainer hazards that decide how this is written:

- The default `grep` here is **ugrep** and honours `.gitignore`. The script spells `/usr/bin/grep` in
  full everywhere, exactly as `fresh-clone-fixture.sh` does.
- `grep -qF` with a dash-leading pattern exits 2 and the gate then fails **open**; use `-qFe` for any
  fixed-string match on such a pattern.
- Any scratch directory lives **outside** `/workspaces` via `mktemp -d`, removed by an `EXIT` trap by
  explicit path. A `git clean -Xdf` aimed at this tree destroys ignored GSD state and bench artifacts
  by design, and a scratch tree parked inside the repository would be collateral.

A shell script under `.planning/` is **not** `firestarter_app/` source, so the no-comments rule does
not reach it — it carries the full comment header the house style uses. Nothing in this plan writes
into `firestarter_app/` at all.

Follow the repository's standing commit-attribution convention for every commit this plan makes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run the live endpoint-contract fixture</name>
  <precondition>`curl -s -o /dev/null -w '%{http_code}' https://api.github.com/repos/henols/firestarter_fw/releases/latest` prints `200` — `api.github.com` is reachable from this container and the renamed repository answers, which is an external fact no plan ordering guarantees.</precondition>
  <files>.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh, .planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</files>
  <read_first>
    - `.planning/phases/189-free-the-name/fresh-clone-fixture.sh` — **read it in full.** It is the
      shape to mirror wholesale: shebang plus `set -euo pipefail`; a header block stating what it
      proves, `Usage:`, `Environment:`, `Dependencies:`, `Failure style:`, `Exit codes:` and
      `Re-runnable:`; a `--help` arm that prints its own header; a fail-closed default arm exiting 2 on
      an unrecognised argument; `SCRIPT_DIR` derived from `BASH_SOURCE` with every root derived
      relative to it and no hardcoded absolute path; `${VAR:-default}` environment overrides; a
      `mktemp -d` scratch tree outside `/workspaces` with a `trap cleanup EXIT`; labelled `key: value`
      emission of every resolved value **before** the verdict; bail-on-first assertions each naming the
      expectation, the pre-state and the next action; and a single all-caps verdict token with an
      explicit exit.
    - `.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` and
      `189-rename-03-fresh-clone.txt` — the transcript shape: titled header, `capture_date:` in
      ISO-8601 Z, `capture_commit:`, the exact command in a block, numbered readings with raw output,
      a non-vacuity control, and a closing paragraph on what the reading does and does not prove.
    - `/workspaces/firestarter_app/firestarter/firmware.py` — `fetch_release_info`,
      `fetch_latest_release_info`, `list_releases`, `_download_firmware_file`, `_asset_label`, and
      `manage_firmware_update`'s identity seam `check_current_firmware`. The fixture drives these
      directly; read their signatures rather than guessing them.
    - `/workspaces/firestarter_app/firestarter/constants.py` — the three constants as `190-01` left
      them.
    - `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/190-CONTEXT.md` — D-14, D-15,
      D-16 and the `<specifics>` requirement that the script prints what it resolved rather than merely
      exiting 0.
  </read_first>
  <action>
Write `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh`,
then run it once and capture its full output. Mirror `fresh-clone-fixture.sh`'s skeleton rather than
inventing one; the conventions listed in `<read_first>` are not suggestions.

**Contract.**

- Environment overrides: `PYTHON_BIN` defaulting to the app working tree's `.venv311/bin/python`
  derived from `SCRIPT_DIR`; `NOPE_SLUG` defaulting to a repository name under `henols` that does not
  exist, used to produce the unreachable case; `KEEP_DOWNLOAD` defaulting to `0`, where `1` leaves the
  downloaded image in place for inspection.
- Arguments: `--help` prints the header block and exits 0; anything else prints usage to stderr and
  exits 2.
- Preconditions checked before any work, each reporting to stderr: `PYTHON_BIN` exists and is
  executable; it can import `firestarter`, `requests` and `click`; and the app root derived from
  `SCRIPT_DIR` is a git repository.
- Exit codes, documented in the header: 0 with the verdict token `ENDPOINT CONTRACT OK`; 1 for a
  failed assertion; 2 for bad usage; and **3 for the one named, expected non-failure case — the
  network is unavailable.** A live-network script that cannot reach `api.github.com` must say so
  distinctly rather than read as a URL regression. Exit 3 must be reachable code, not a documented
  intention: detect it by a pre-flight reachability probe whose failure is transport-level rather than
  an HTTP status.

**The four checks (D-14), each printing labelled values before asserting.**

1. **Stable channel.** Through the package's own stable-resolution method with `board=uno`, print
   `channel:`, `board:`, `version:` and `asset_url:`. Assert the version is non-empty and the asset URL
   contains `henols/firestarter_fw`. Separately, and this is the load-bearing part, issue the API
   request for `FIRESTARTER_RELEASE_URL` directly and print `requested:`, `resolved:`, `status:` and
   `redirects:`. **Assert `redirects:` is 0 and that `resolved:` equals `requested:`.** Do the same for
   `FIRESTARTER_RELEASES_URL`. Without these two readings the whole transcript is compatible with the
   pre-change state and proves nothing, because the old slug returns byte-identical data through a
   transparent 301.
2. **Pre channel.** The same resolution with `channel=pre` and a board the pre channel carries, printing
   the same labelled keys and asserting the same slug property.
3. **Real asset download.** Download one real `.hex` through the package's own download helper — not a
   hand-rolled `requests.get`, because D-14 names that helper and using anything else would prove a
   different code path. Print `hex_path:`, `hex_bytes:`, `hex_first_line:` and `hex_eof:`. Assert the
   file is non-empty, that every non-empty line begins with a colon, and that the final record is the
   Intel HEX EOF record. **Do not assert redirect-freedom on this request** — a release asset URL
   legitimately redirects to a release-assets host, and asserting otherwise would fail this check for a
   correct reason. Then remove the downloaded file **by explicit path** unless `KEEP_DOWNLOAD=1`; never
   remove the directory it sits in, which also holds the app's own configuration.
4. **Both URL-04 failure modes, for real.**
   - *Asset-less:* drive the `fw` command end to end for `--board uno328pb --stable`, with **only** the
     board-identity seam simulated — patch `check_current_firmware` to return an identified triple —
     while the release resolution runs live against the real, correctly-named endpoint. Print
     `case:`, `exit_code:` and the emitted message. Assert the exit code is 1 and that the message
     names both the board and an endpoint containing `henols/firestarter_fw`. **Print an explicit
     caveat line stating that the board identity is simulated because no programmer is attached and the
     milestone requires no bench, while the endpoint resolution is live.** An honest transcript names
     what was simulated.
   - *Unreachable:* in a throwaway child process, rebind the endpoint constants **on the
     `firestarter.firmware` module** — not on `firestarter.constants`, which has no effect because
     `firmware.py` imports them by name — to point at `NOPE_SLUG`, then run `fw --list`. Print `case:`,
     `exit_code:`, the stdout and the stderr. Assert the exit code is 1, that the stderr names the
     nonexistent endpoint, and that **no JSON document appears on stdout**.
   - Assert the two cases' messages are **different from each other**, and that neither contains the
     wording the already-up-to-date branch prints. That is criterion 3's "not mistakable for" made
     checkable.

Drive the Python work through heredoc child programs invoked with `PYTHON_BIN`, in the style of the
child-program template the app's own subprocess tests use. Keep each child small and have it print
labelled lines the shell then echoes and asserts on, so the transcript carries the readings rather than
only the verdict.

**Then run it once**, with no arguments, and capture the complete output to
`.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt`.
Prepend the transcript header the Phase 189 evidence files use — a title, `capture_date:` in ISO-8601
Z, `capture_commit:` naming the meta repository HEAD and the app repository HEAD, and the exact command
— and append a closing paragraph stating what the transcript proves and what it does not: it proves the
endpoints resolve directly and both failure modes are loud, and it does **not** prove anything about
`main`, about the stable channel PyPI ships, or about any board, because no hardware was involved.

Commit the script and the transcript in `/workspaces`. Confirm the branch afterwards.
  </action>
  <verify>
    <automated>bash /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh --help</automated>
    <fails_when>exits non-zero, or its output contains no `Exit codes:` line — the script must document its own contract</fails_when>
    <automated>bash /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh --nonsense</automated>
    <fails_when>exits with anything other than status 2, or prints no usage line to stderr</fails_when>
    <automated>bash /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh</automated>
    <fails_when>exits non-zero, or its output does not contain the token `ENDPOINT CONTRACT OK`. An exit status of 3 means the network was unreachable and is not a URL regression — re-run when connectivity returns rather than treating it as a failure.</fails_when>
    <automated>/usr/bin/grep -c '^redirects: 0$' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</automated>
    <fails_when>prints a number smaller than `2`, or exits non-zero because the transcript is absent — both API endpoints must be shown resolving with no redirect hop, and this is the only reading that distinguishes URL-01 done from URL-01 undone</fails_when>
    <automated>/usr/bin/grep -c 'henols/firestarter_fw' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</automated>
    <fails_when>prints a number smaller than `4` — the renamed slug must appear in the stable resolution, the pre resolution, the asset URL and the asset-less failure message</fails_when>
    <automated>/usr/bin/grep -c '^hex_eof: ' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</automated>
    <fails_when>prints anything other than `1` — the real asset download and its Intel HEX format reading must both be in the captured run</fails_when>
    <automated>/usr/bin/grep -c '^exit_code: 1$' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</automated>
    <fails_when>prints a number smaller than `2` — both URL-04 failure modes must be shown exiting 1, not merely described</fails_when>
    <automated>/usr/bin/grep -c '^capture_commit: ' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</automated>
    <fails_when>prints `0` — without the pinned commits a later reader cannot tell which tree the demonstration was taken against</fails_when>
    <automated>/usr/bin/grep -cE 'simulated' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt</automated>
    <fails_when>prints `0` — the asset-less case simulates the board identity and the transcript must say so; a transcript that omits what it faked is not honest evidence</fails_when>
    <automated>/usr/bin/grep -c 'firestarter.constants' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh</automated>
    <fails_when>prints anything other than `0` in the context of a rebinding assignment — the unreachable case must rebind the endpoint on the firmware module, since the constants module's names are already bound elsewhere. A read-only import for printing a value is acceptable; if this leg prints non-zero, inspect the matches and confirm none of them is an assignment.</fails_when>
    <automated>ls /workspaces/firestarter_app | wc -l</automated>
    <fails_when>the count differs from its value before the run, or `git -C /workspaces/firestarter_app status --porcelain` is non-empty — the fixture must leave the app repository untouched</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `v1.38-repository-rename`</fails_when>
  </verify>
  <acceptance_criteria>
    - `endpoint-contract-fixture.sh` exists, is committed, is executable, begins `#!/usr/bin/env bash`,
      sets `set -euo pipefail`, derives every root from `SCRIPT_DIR` with no hardcoded `/workspaces`
      path, documents exit codes 0, 1, 2 and 3 in its header, and rejects an unrecognised argument with
      exit 2.
    - Exit 3 is reachable code guarded by a real pre-flight reachability probe, not a documented
      intention.
    - The script spells `/usr/bin/grep` in full at every filesystem match.
    - The captured transcript contains labelled lines for `requested:`, `resolved:`, `status:`,
      `redirects:`, `version:`, `asset_url:`, `hex_bytes:`, `hex_eof:`, `case:` and `exit_code:`.
    - Both API endpoints show `redirects: 0` and a `resolved:` value equal to their `requested:` value.
    - The asset download's readings show a non-zero byte count and the Intel HEX EOF record, and the
      transcript does **not** claim redirect-freedom for that request.
    - Both failure cases show exit code 1, carry different messages from each other, and neither message
      contains the substring `already up to date`.
    - The transcript states plainly that the board identity in the asset-less case was simulated and
      why, and that the release resolution was live.
    - The downloaded firmware image is no longer present after the run, and the directory it was written
      into still exists and still contains the app's own configuration file.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty after the run.
  </acceptance_criteria>
  <done>
A committed, re-runnable script resolves both channels against the renamed repository with zero
redirect hops, downloads and format-checks a real firmware image, and demonstrates both URL-04 failure
modes exiting 1 with distinct named messages — and its captured run is in the phase record with what
it simulated stated on its face.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reproduce Host CI verbatim on Python 3.11, and prove the repository is slug-clean</name>
  <files>.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt, .planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt</files>
  <read_first>
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` — **read it in full.** Take the five
      primary-job `run:` steps verbatim; do not paraphrase them, do not add flags, and do not drop the
      coverage thresholds. Note the second job, `ci-py32`, and note the `paths-ignore` list.
    - `.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` — the sweep-transcript
      shape, including the `command:` / `matching files:` / `result:` triple and the non-vacuity
      positive control. Reproduce that control; the naive sweep is falsely clean here.
    - `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/190-CONTEXT.md` — D-05 and D-17
      in full, including D-17's instruction that the phase record must state this is the local
      equivalent.
  </read_first>
  <action>
Two transcripts, both captured from real runs.

**1. The CI-equivalent run (D-17).** From the app repository root, inside the Python 3.11 environment,
run the five primary-job gate steps **verbatim and in order**, capturing each command and its complete
output. The py3.11 pin is the substance of this decision: the devcontainer's default interpreter is
py3.12 and has provably masked real py3.11 CI breakage on `beta`, so a run under the default
interpreter does not satisfy D-17 no matter how green it is. Use the existing `.venv311` — activate it
or prefix every command with its `bin/` — or create a fresh 3.11 environment with `uv` if `.venv311`
has drifted.

Commit the app repository **before** step 4: one of its tests asserts whole-repository porcelain and
will otherwise go red for reasons that have nothing to do with this phase.

Write the result to `190-url-01-ci-equivalent.txt` with the Phase 189 transcript header — title,
`capture_date:` in ISO-8601 Z, `capture_commit:` for both repositories — then one numbered section per
step containing the exact command and its raw output. Record the interpreter version as read from the
interpreter itself, not as assumed. Record the test count and the coverage percentage, and compare both
against the pre-phase baseline of 2129 tests and 84.74% coverage against a 70% floor.

Close with two paragraphs that D-17 requires and that a later reader will need:

- **This is the local equivalent, not an Actions run.** The milestone branch is not on origin; Host CI
  triggers on a push to any branch; a push is outward-facing and operator-gated; and the real Actions
  run happens when the operator pushes at ship. Say that plainly rather than letting a green transcript
  imply CI has run.
- **D-17's five steps are the primary `ci` job only.** `ci.yml` also defines `ci-py32`, whose two
  further steps are not reproduced here. Note that the one test this phase broke and adapted lives in
  the pyusb-absence family but is inside the primary job's scope, so step 4 does exercise it.

**2. The slug sweep (D-05).** Run the boundary-aware sweep across the whole host repository together
with its positive control, and capture both into `190-slug-sweep.txt` in the Phase 189 sweep-transcript
shape: the exact command, the matching files, and the result, for each. The bare-slug reading must be
zero and the `henols/firestarter_prom` control must be 8 files. **Record both numbers and state in the
transcript that a zero paired with a zero control is a failed gate, not a pass** — the naive
`grep -v`-chained pipeline was measured to return six of the seven original sites, dropping the one
line that names two slugs at once, and reads falsely clean.

Also record what the sweep deliberately did **not** cover: the meta repository, which is Phase 192's
(SWEEP-01), and anything under `.planning/milestones/`, which is historical-by-intent and which Phase
192 must prove untouched.

Commit both transcripts in `/workspaces`.
  </action>
  <verify>
    <automated>/usr/bin/grep -c 'cov-fail-under=70' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints `0`, or exits non-zero because the transcript is absent — the coverage step must be recorded with the threshold ci.yml actually sets, which is how a paraphrased step is caught</fails_when>
    <automated>/usr/bin/grep -cE '^Python 3\.11\.' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints `0` — the interpreter version must be recorded as read from the interpreter, and a 3.12 reading means the run did not reproduce CI at all</fails_when>
    <automated>/usr/bin/grep -cE 'ruff check firestarter/ tests/|ruff format --check firestarter/ tests/' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints a number smaller than `2` — both lint steps must appear verbatim as ci.yml spells them</fails_when>
    <automated>/usr/bin/grep -cE '[0-9]+ passed' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints `0` — the pytest step's summary line must be in the transcript; a transcript with no count is not evidence the suite ran</fails_when>
    <automated>/usr/bin/grep -ci 'ci-py32' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints `0` — the record must note that D-17's five steps are the primary job only, or a later reader will believe the whole workflow was reproduced</fails_when>
    <automated>/usr/bin/grep -ci 'local equivalent' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints `0` — D-17 requires the record to state plainly that this is not an Actions run</fails_when>
    <automated>/usr/bin/grep -c 'henols/firestarter_prom' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt</automated>
    <fails_when>prints `0` — the non-vacuity positive control must be in the sweep transcript, not merely run</fails_when>
    <automated>git -C /workspaces/firestarter_app grep -lE 'henols/firestarter([^_a-zA-Z0-9]|$)' -- . | wc -l</automated>
    <fails_when>prints anything other than `0`; if non-zero, re-run with `-nE` to see which lines still match</fails_when>
    <automated>git -C /workspaces/firestarter_app grep -lE 'henols/firestarter_prom' -- . | wc -l</automated>
    <fails_when>prints anything other than `8` — a zero means the scan matched nothing and the clean reading above proves nothing; a different non-zero means the control's baseline moved and must be re-established first</fails_when>
    <automated>git -C /workspaces/firestarter_app status --porcelain</automated>
    <fails_when>prints any line — the app repository must be committed clean before the coverage step, or its porcelain-asserting test goes red for unrelated reasons</fails_when>
  </verify>
  <acceptance_criteria>
    - `190-url-01-ci-equivalent.txt` contains all five primary-job commands **verbatim** as `ci.yml`
      spells them, each followed by its raw captured output.
    - The recorded interpreter is Python 3.11.x, read from the interpreter rather than asserted.
    - The recorded test count and coverage percentage are both stated and both compared against the
      pre-phase baseline of 2129 tests and 84.74%.
    - The transcript states that this is the local equivalent and that the real Actions run happens when
      the operator pushes at ship.
    - The transcript notes that `ci-py32` exists and was not reproduced, and that the adapted
      pyusb-absence test is nonetheless inside the primary job's scope.
    - `190-slug-sweep.txt` records the bare-slug sweep and its positive control as two separate
      `command:` / `result:` readings, with values 0 and 8 respectively.
    - The sweep transcript states that a zero paired with a zero control is a failed gate.
    - The sweep transcript names what it did not cover: the meta repository (Phase 192) and
      `.planning/milestones/` (historical-by-intent).
    - `git -C /workspaces/firestarter_app status --porcelain` is empty.
  </acceptance_criteria>
  <done>
The five Host CI gate steps have been run verbatim on Python 3.11 and captured with their real output
and real counts, the record says plainly that this is the local equivalent of a run that happens at
ship, and the host repository is proved free of bare-slug references by a sweep whose own non-vacuity
is demonstrated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Advance the firestarter_app gitlink and record that it names an unpushed commit</name>
  <precondition>`git -C /workspaces/firestarter_app status --porcelain` is empty and `git -C /workspaces/firestarter_app branch --show-current` prints `v1.38-repository-rename` — every app-repository commit this phase makes is already in, because the gitlink pins a specific commit and advancing it early would pin the wrong one.</precondition>
  <files>.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</files>
  <read_first>
    - `.planning/phases/189-free-the-name/189-03-SUMMARY.md` — how Phase 189 advanced the **firmware**
      gitlink, why it did so last, and what it recorded about the result naming an unpushed commit.
      This task is the same operation on the other submodule and must reach the same standard of
      record.
    - `.planning/phases/189-free-the-name/evidence/189-rename-02-branch-disposition.md` — the shape of a
      disposition note stating what is landed, what is carried, and what resolves only at push.
  </read_first>
  <action>
This is the phase's last act, and it is deliberately last: the gitlink pins a specific commit, so
advancing it before every app-repository commit is in would pin a tree that does not carry the phase's
work.

From `/workspaces`, stage the `firestarter_app` submodule path and commit the gitlink advance on
`v1.38-repository-rename`. Use plain `git -C /workspaces add` and `git -C /workspaces commit`; a commit
helper that scrapes the roadmap for a milestone name has been observed switching branches in this
repository, so confirm `git -C /workspaces branch --show-current` afterwards. Commit only the submodule
path — no other file rides along in this commit.

Then append a short **disposition** section to `190-url-01-ci-equivalent.txt` recording three facts a
later reader will otherwise have to derive:

- The app-repository commit the gitlink now names, by sha, and the fact that it is **not on origin**.
  Until the milestone is pushed, a fresh clone of the meta repository at this tip cannot fetch it —
  exactly the state Phase 189 left the firmware gitlink in, and for the same reason.
- That this resolves at the milestone push, which is operator-gated, and that nothing in this phase
  pushes.
- That Phase 189's fresh-clone fixture is expected to report the unfetchable-gitlink case rather than a
  URL regression while that is true, and that both submodules are now in that state rather than one.

Record the same three facts in `190-04-SUMMARY.md`.
  </action>
  <verify>
    <automated>git -C /workspaces diff-index --quiet HEAD -- firestarter_app</automated>
    <fails_when>non-zero exit — the gitlink is still ahead of or behind what is committed, meaning the advance was not made or not committed</fails_when>
    <automated>git -C /workspaces ls-tree HEAD firestarter_app</automated>
    <fails_when>prints a line whose object sha does not equal the output of `git -C /workspaces/firestarter_app rev-parse HEAD`, or prints nothing at all</fails_when>
    <automated>git -C /workspaces show --stat --format= HEAD</automated>
    <fails_when>lists any path other than `firestarter_app` — the gitlink advance must be its own commit with nothing riding along</fails_when>
    <automated>git -C /workspaces/firestarter_app branch --contains HEAD</automated>
    <fails_when>does not list `v1.38-repository-rename` — the commit the gitlink names must be on the milestone branch, not on a detached head or a stray branch</fails_when>
    <automated>/usr/bin/grep -ci 'not on origin' /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt</automated>
    <fails_when>prints `0` — the disposition must state that the newly pinned commit is unpushed, or a later reader will read a fresh-clone failure as a regression</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `v1.38-repository-rename`</fails_when>
    <automated>git -C /workspaces status --porcelain</automated>
    <fails_when>prints any line naming `firestarter_app` — the submodule pointer must be committed, not left dirty in the meta working tree</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces ls-tree HEAD firestarter_app` names exactly the sha that
      `git -C /workspaces/firestarter_app rev-parse HEAD` prints.
    - That sha is reachable from `v1.38-repository-rename` in the app repository.
    - The gitlink advance is its own commit touching no path other than `firestarter_app`.
    - The disposition section names the pinned sha, states that it is not on origin, states that this
      resolves at the operator-gated milestone push, and states that both submodule gitlinks are now in
      that state rather than one.
    - `190-04-SUMMARY.md` records the same three facts.
    - Nothing was pushed: `git -C /workspaces log origin/beta..HEAD --oneline` is non-empty, confirming
      local-only commits, and no `git push` appears anywhere in this phase's record.
  </acceptance_criteria>
  <done>
The meta repository pins the app-repository commit carrying this phase's work, the advance is its own
commit, and the record states plainly that the pinned commit is unpushed and why that is expected
rather than broken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `api.github.com` -> the fixture | Every reading in the transcript comes from an untrusted remote; the transcript's value depends entirely on the fixture asserting rather than merely reporting. |
| a downloaded `.hex` -> the local filesystem | A real binary from the network is written into the user's home directory by the package's own download helper. |
| the committed transcript -> every later reader | Phase 191 re-runs this fixture against the stable it cuts and a post-claim milestone re-runs it to watch the 404 arrive; both trust that this transcript is what it says it is. |
| the meta gitlink -> every future clone | The sha recorded here is the app-repository tree every clone of the milestone tip will resolve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-190-17 | Spoofing | a transcript that cannot distinguish direct resolution from a redirect | high | mitigate | The fixture asserts `len(response.history) == 0` and prints `requested:` alongside `resolved:` for both API endpoints, and two verify legs count those readings in the committed transcript. A successful resolve alone is explicitly recorded as non-evidence. |
| T-190-18 | Repudiation | evidence that was not produced by a run | high | mitigate | The transcript is the fixture's captured output, the fixture is committed and re-runnable, and its verdict token, its labelled readings and its `capture_commit:` pins are each asserted independently. Fabrication is named as a prohibition. |
| T-190-19 | Tampering | a downloaded firmware image left on disk | medium | mitigate | The image is removed by explicit path after the format check unless `KEEP_DOWNLOAD=1`, and the directory it sits in — which also holds the app's own configuration — is never removed. A verify leg confirms the app repository is untouched. |
| T-190-20 | Tampering | a scratch tree parked inside the repository | medium | mitigate | Scratch lives outside `/workspaces` via `mktemp -d` with an `EXIT` trap removing it by explicit path, mirroring the Phase 189 fixture, because a later broad clean aimed at this tree would otherwise take ignored GSD state with it. |
| T-190-21 | Elevation of privilege | an outward-facing step performed to make CI green | high | mitigate | D-17 exists so the phase needs no push: the five gate steps run locally on py3.11 and the record states plainly that the real Actions run happens at ship. No push, no PR, no release is named as a prohibition, and a merge to `beta` would fire a pre-release cut in both sub-repositories and a PyPI publish. |
| T-190-22 | Tampering | the downloaded asset flashed without provenance verification | medium | accept | **Pre-existing and out of scope.** No signature or checksum verification exists on any downloaded release asset, and the Releases API exposes none for these files. Recorded here because this plan downloads a real image; adding provenance is a firmware-release-process change, not a host change, and is not this phase's to make. |
| T-190-23 | Information disclosure | committed transcripts | low | mitigate | The fixture emits URLs, version strings, byte counts, exit codes and commit shas only. It must not print environment, tokens or the home directory's other contents; the API is used unauthenticated so no credential exists on this path. |
| T-190-SC | Tampering | npm/pip/cargo installs | low | accept | The only install commands are `pip install -e .[test]` and `pip install -e .`, both of which install the project itself from the local working tree against the already-pinned dependency set, and both are run because `ci.yml` runs them verbatim. No new distribution is introduced. `190-RESEARCH.md` § Package Legitimacy Audit records the phase as not applicable with an empty table. |
</threat_model>

## Spec-less probe coverage

No SPEC exists for this phase. The deterministic edge probe surfaced **7 applicable rows** across
URL-01, URL-03 and URL-04; `190-01` authors the three URL-04 rows and `190-02` authors the four URL-01
and URL-03 rows into their own `must_haves`. **This plan authors none of them and drops none** — its
`must_haves.truths` are derived from the locked decisions and the measured live state rather than from
the probe.

**Phase-wide arithmetic: 7 probe-surfaced rows == 7 authored into `must_haves` (3 in `190-01`, 4 in
`190-02`) + 0 surfaced as flagged assumptions.** No row was dropped and none was dismissed.

Prohibition recall's two-stage pass kept three survivors for this plan, authored descriptor-less above:
fabricated evidence, outward-facing action, and a broad clean destroying ignored state. All three are
values or safety items rather than routine engineering. Stage 2 dropped the routine ones (must not
hardcode the container's paths, must not leave the fixture non-executable). One candidate — "must not
download and execute an unverified binary" — is genuine but is **canon supply-chain security**, and is
**dropped with this breadcrumb** rather than minted as a bespoke prohibition: it is recorded instead as
threat `T-190-22` with an explicit `accept` disposition and a statement that it is pre-existing and
outside this phase's remit.

### Planner assumptions carried forward, stated so a reviewer can reject them

Three questions `190-RESEARCH.md` left open are **decided here** rather than left to the executor:

1. **"No document on stdout" is implemented as "no JSON document", not "empty stdout."** That is the
   literal reading of D-11, it preserves D-07's retained `logger.error` (which reaches stdout in
   production), and it needs no change to the logging handler. The residue — that
   `fw --list --json` stdout is still not clean JSON whenever any log line fires, which is a
   pre-existing defect this phase narrows but does not resolve — must be recorded explicitly in
   `190-01-SUMMARY.md` so a future JSON-schema decision inherits the measurement.
2. **The fixture's redirect assertion is included and is not the standing guard D-05 defers.** D-05
   defers a repository-wide *source-scanning* regression check; this is a live network assertion inside
   an on-demand, non-CI evidence script — a different instrument with a different failure mode, and one
   D-16 already establishes belongs here.
3. **The pin is written to be portable to `main`.** It imports the three constants individually by name
   so Phase 191 can delete the line for any constant `main` lacks — `FIRESTARTER_RELEASE_BY_TAG_URL`
   may post-date that fork point, which was **not** measured and remains Phase 191's to check. It is
   deliberately not a directory scan or an attribute walk, either of which would pass vacuously on a
   branch carrying fewer constants.

Two further discretionary choices, recorded for the same reason: the D-11 stderr write uses
`click.echo(..., err=True)` rather than a direct `print` to `sys.stderr` — the codebase has precedent
for neither, and this is the smaller conceptual jump inside a Click handler; and "parses as Intel HEX"
is checked by line-prefix plus EOF record rather than by adding a HEX-parsing dependency for one
evidence script.

## Artifacts this phase produces

Every symbol, file and flag created by Phase 190, listed so a drift check can exclude newly-created
names. Nothing in this list exists on `beta` today.

| Artifact | Created by | Notes |
|---|---|---|
| `firestarter_app/tests/test_fw_list_failure_vs_empty.py` | 190-01 | New test module; the `fw --list` failure/empty split on the no-`obj=` CliRunner harness |
| `firestarter_app/tests/test_endpoint_constants.py` | 190-02 | New test module; the URL-03 slug pin over all three endpoint constants |
| `firestarter_app/tests/test_fw_update_dead_endpoint.py` | 190-03 | New test module; the D-06 update-path guard and the no-double-emit proof |
| `firestarter._endpoint_for_channel` | 190-03 | New module-level private helper in `firestarter_app/firestarter/firmware.py`; maps a channel plus optional pinned version to the endpoint URL actually addressed |
| `FirmwareManager.list_releases` return type | 190-01 | Widened, not new: `List[ReleaseInfo]` becomes `List[ReleaseInfo] \| None` |
| `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/endpoint-contract-fixture.sh` | 190-04 | New re-runnable evidence script; env vars `PYTHON_BIN`, `KEEP_DOWNLOAD`, `NOPE_SLUG`; exit codes 0/1/2/3; labelled output keys `requested:`, `resolved:`, `status:`, `redirects:`, `version:`, `asset_url:`, `hex_path:`, `hex_bytes:`, `hex_first_line:`, `hex_eof:`, `case:`, `exit_code:`; verdict token `ENDPOINT CONTRACT OK` |
| `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-04-endpoint-contract.txt` | 190-04 | Captured fixture run, all four D-14 checks |
| `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-01-ci-equivalent.txt` | 190-04 | The five `ci.yml` gate steps run verbatim on py3.11, plus the gitlink disposition |
| `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-url-03-pin-falsification.txt` | 190-02 | The pin proved red on a single-constant revert |
| `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt` | 190-04 | Boundary-aware bare-slug sweep with its positive control |
| `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/COVERAGE.md` | planning | GitHub Releases API coverage matrix |

No new CLI flag is added by this phase. `fw`'s option surface is unchanged.

## Verification

- **The live findings are volatile.** The 301 on `henols/firestarter` persists only until the deferred
  claim fires, at which point it becomes a 404 or, worse, a 200 from the meta repository. Re-probe
  before relying on the redirect readings in any later milestone; that is exactly why the fixture is
  re-runnable and committed rather than a one-off transcript.
- **Exit 3 is not a failure.** A run that cannot reach `api.github.com` reports the network, not a URL
  regression. Re-run when connectivity returns.
- **Both submodule gitlinks now name unpushed commits.** Phase 189 left the firmware one in that state
  deliberately, and this phase puts the host one in the same state. Phase 189's fresh-clone fixture is
  expected to report that named case until the milestone is pushed.

## Success criteria

- ROADMAP criterion 4 is satisfied in both halves: `fw` resolves a real firmware release end to end
  against the renamed repository, proved with the redirect count that is the only reading capable of
  distinguishing it from the pre-change state; and the five Host CI gate steps are green on the
  milestone branch under the interpreter CI actually uses.
- The phase record states plainly what this does **not** establish: it is the local equivalent of an
  Actions run that happens at ship, it says nothing about `main` or about the stable channel PyPI
  ships, and no hardware was involved at any point.

<output>
Create `.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/190-04-SUMMARY.md` when done.
</output>
