---
phase: 207-the-version-and-the-record
plan: 02
type: execute
wave: 1
depends_on: []
commits_land_in: "the wiki clone /workspaces/firestarter.wiki on master, on top of the unpushed f967398; local only, nothing is pushed in this plan"
files_modified:
  - firestarter.wiki/Breaking-Changes.md
  - firestarter.wiki/Writing-and-Verifying.md
  - firestarter.wiki/Home.md
  - firestarter.wiki/_Sidebar.md
  - firestarter.wiki/Install-Beta.md
  - firestarter.wiki/Testing-Chips.md
autonomous: true
requirements: [REL-04]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-02: the wiki work is committed in the existing /workspaces/firestarter.wiki clone on master directly on top of the unpushed f967398, so git rev-list --reverse origin/master..HEAD starts with f967398 and holds exactly four commits"
    - "D-03: every statement this plan adds is labelled from 3.1.0b1 — the new Breaking-Changes heading and each of its sub-sections, every ## section of Writing-and-Verifying.md, and each new line on Install-Beta.md and Testing-Chips.md contains the literal 3.1.0b1"
    - "Breaking-Changes.md states that commands 4 and 6 are removed from the firmware from 3.1.0b1 and never reused, and quotes an older CLI's refusals verbatim: `ERROR: Unknown command: 6` for verify and `ERROR: Unknown command: 4` for blank"
    - "The older-CLI-on-3.1.0b1-firmware sub-section opens with the unguarded write: an older CLI's write no longer refuses a non-blank UV EPROM"
    - "Breaking-Changes.md tells the user what to do: upgrade the CLI first with `pip install --pre --upgrade firestarter`, then the firmware with `firestarter fw -i`, or pin older firmware with `firestarter fw -i --firmware-version 3.0.0b35`"
    - "Lines 1-25 of Breaking-Changes.md state compatibility per entry and no longer make a page-wide claim that every mixed pair fails"
    - "The new Writing-and-Verifying page documents write (-b/--no-blank-check, --skip-erase, which parts the CLI checks), write --verify and --full with their exit codes and verdict lines, verify and blank with --full, and erase -b, and it is linked from Home, _Sidebar.md and the Breaking-Changes 3.1.0b1 entry"
    - "The two code-derived compatibility statements (an older CLI's erase -b not checking, and a 3.1.0b1 CLI's plain erase blank-checking on older firmware) carry the phrase `not yet observed on hardware`"
    - "No page text and no new wiki commit message carries a phase number, a D-NN id, a .planning/ path or a v1.4x milestone label"
    - "Every relative ](Page-Name) link in the wiki resolves to an existing Page-Name.md, and the claim-stamp line at the end of Breaking-Changes.md is byte-identical to origin/master's"
  artifacts:
    - path: "firestarter.wiki/Breaking-Changes.md"
      provides: "the 3.1.0b1 entry: retired commands, the mixed-version matrix in both directions, and what to do"
      contains: "## 3.1.0b1 — verify and blank check run on the host"
    - path: "firestarter.wiki/Writing-and-Verifying.md"
      provides: "the only wiki page documenting write, write --verify / --full, verify, blank and erase -b"
      contains: "# Writing and Verifying"
    - path: "firestarter.wiki/Home.md"
      provides: "Reference-list entry for the new page"
      contains: "[Writing-and-Verifying](Writing-and-Verifying)"
    - path: "firestarter.wiki/_Sidebar.md"
      provides: "sidebar entry for the new page"
      contains: "- [Writing and Verifying](Writing-and-Verifying)"
  key_links:
    - from: "firestarter.wiki/Breaking-Changes.md"
      to: "firestarter.wiki/Writing-and-Verifying.md"
      via: "a relative link in the 3.1.0b1 entry"
      pattern: "](Writing-and-Verifying)"
    - from: "firestarter.wiki/Writing-and-Verifying.md"
      to: "firestarter.wiki/Breaking-Changes.md"
      via: "the mixed-version pointer section"
      pattern: "](Breaking-Changes)"
    - from: "firestarter.wiki/Writing-and-Verifying.md"
      to: "firestarter_app/tests/__snapshots__/test_characterization.ambr"
      via: "every flag and exit code on the page is transcribed from the snapshot-pinned --help text"
      pattern: "--no-blank-check"
  prohibitions:
    - statement: "This plan pushes nothing. The live wiki stays at 81229d8 when the plan ends; publication is plan 207-03's operator-gated step (D-03)."
      requirement_id: REL-04
      status: enforced
      verification: "Task 3 sweep leg: git ls-remote origin refs/heads/master in the wiki clone prints 81229d8ed8a280b935cf84c4c74f4c97afcdf475."
    - statement: "No catalog message id is added for a retired command (research F9). The wiki quotes the verbatim Unknown command text instead, because the only sender of 4 or 6 is a pre-3.1.0 CLI that renders firmware messages through its own shipped messages.py."
      requirement_id: REL-04
      status: enforced
      verification: "Task 3 sweep leg: git -C /workspaces status --porcelain -- tools/catalog prints nothing, and no commit of this plan lands outside the wiki clone."
    - statement: "The existing v1.32, v1.20 and v1.10 headings of Breaking-Changes.md are not relabelled, and its claim-stamp last line is not edited, because editing the stamp would claim a re-verification nobody performed."
      requirement_id: REL-04
      status: enforced
      verification: "Task 1 leg: the last line equals origin/master's last line, and the heading order check finds ## v1.32 — chip database directly after the new entry."
---

<objective>
Write the user-facing record of the v1.41 release in the wiki: the breaking change and what a user does
about it (ROADMAP criterion 2), and the `write --verify` / `--full` / host-side `-b` surfaces where a
user will look for them (criterion 3). Commit it locally in the existing clone, on top of `f967398`
(D-02), labelled "from 3.1.0b1" throughout (D-03). Publishing is plan 207-03's job.

Purpose: REL-04 — "the wiki, which is the only documentation home". Today no wiki page documents
`write`, `verify`, `blank`, `-b`, `--verify` or `--full` at all, and the Breaking-Changes preamble makes
a page-wide mixed-version claim that 3.1.0b1 makes false. Three earlier phases filed obligations here:
Phase 204 accepted the bare `Unknown command: 6` / `Unknown command: 4` refusal on condition that this
phase names the version boundary for a human; Phase 205 asked for "a 3.1.0 CLI wants 3.1.0 firmware for
`write -b`" and the `erase -b` 0/1/2 contract with the `-s`/`-b` refusal; Phase 203 named the
`write --verify` exit contract among the published surfaces this phase documents.

Output: three local wiki commits — the Breaking-Changes 3.1.0b1 entry; the new `Writing-and-Verifying`
page with its Home and sidebar lines; and version-pairing notes on Install-Beta and Testing-Chips.

Tracer note: Task 1 is the thin end-to-end slice for this plan — the one entry that carries criterion 2,
authored, committed, and checked from the committed blob rather than the working file. The far end, the
live wiki, is deliberately gated behind plan 207-03's operator checkpoint.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/207-the-version-and-the-record/207-CONTEXT.md
@.planning/phases/207-the-version-and-the-record/207-RESEARCH.md
@.planning/phases/207-the-version-and-the-record/207-PATTERNS.md

Standing rules for every task in this plan:
- The wiki is public, and so is its git history. Page text AND wiki commit messages carry no phase
  number, no decision id, no `.planning/` path and no milestone label such as the v1.4x series. Refer to
  releases by the product version the user sees (`3.1.0b1`, `3.0.0b35`), never by milestone.
- Work in `/workspaces/firestarter.wiki` on `master`, with the clone's configured identity (Henrik
  Olsson). Never rebase, amend or drop `f967398`. Never push.
- Relative wiki links take the bare page name with no anchor: `[Text](Page-Name)`.
- Every fact on these pages is transcribed from the code or from a bench transcript, never from memory.
  The snapshot-pinned `--help` text in `firestarter_app/tests/__snapshots__/test_characterization.ambr`
  is the source of truth for flags and exit codes, because the Click docstrings ARE the help text.
- Use `/usr/bin/grep` (the shell's `grep` is ugrep and honours ignore files), and pass every
  dash-leading pattern through `-e`.
- The wiki wraps prose at about 80 columns. Never break a line inside a backticked span, inside a bold
  lead sentence, or inside the phrase `not yet observed on hardware`. Every string this plan's checks
  quote (the refusal lines, the commands, the bold leads, the headings) sits on one physical line;
  a wrapped copy reads the same in the browser and fails the check.
</context>

## Decisions carried into this plan

- **D-02 (locked, operator):** work in the existing clone on top of the unpushed `f967398`; the push
  checkpoint in plan 207-03 shows the full outgoing log with `f967398` named.
- **D-03 (locked, operator):** every new statement is labelled "from 3.1.0b1"; the push happens in-phase
  behind a `checkpoint:human-action` (plan 207-03).
- **F3, option A (Claude's discretion):** a new page `Writing-and-Verifying`, because no page documents
  any command and a user looking for "how do I verify a write" will not look in Breaking Changes.
- **F4:** the new heading is `## 3.1.0b1 — verify and blank check run on the host`, in the page's
  `## <label> — <topic>` shape, using the product version. The existing milestone-labelled headings stay.
- **F5:** the page-wide preamble becomes a per-entry statement. **Departure from research, recorded
  here:** research said keep the preamble's install one-liner unchanged; this plan adds `--upgrade` to
  it. The page's reader is upgrading an existing install, and plain `pip install --pre firestarter`
  leaves an already-installed older CLI in place and then flashes new firmware — exactly the unguarded
  pairing the new entry warns about.
- **F9:** no catalog id for a retired command; quote the verbatim `Unknown command: N` text so a search
  lands on the explanation. This closes the deferral chain from Phase 204 through Phase 205.
- **F10:** the full compatibility matrix in both directions, leading with the unguarded write. The
  code-derived rows are stated in the "not yet observed on hardware" register, and plan 207-03's
  checkpoint shows them to the operator by name.

<assumption_delta_decision>
The assumption-delta scan flagged the pluralization term "alongside" in the phase goal ("the wiki
documents the breaking change alongside the two new user-facing surfaces"). Decision: **no-change**.
"Alongside" describes co-location of documentation — the Breaking-Changes entry and the new
command-reference page link to each other and ship in one push — not a second or alternative
implementation of any capability. There is no assumption to promote.
</assumption_delta_decision>

## Artifacts this phase produces

Phase-wide list. Items marked **(this plan)** are produced here.

- (plan 207-01) `firestarter_fw` and `firestarter_app`: a merge of `origin/beta` and a single-file
  `chore(207-01): bump … -> 3.1.0b1` commit in each; an app README commit; one meta gitlink commit;
  `evidence/207-01-oracles.txt`; `207-01-SUMMARY.md`.
- **(this plan)** Wiki commit 1, subject `Document the 3.1.0b1 compatibility break between the CLI and the firmware`, touching only `Breaking-Changes.md`.
- **(this plan)** Wiki commit 2, subject `Add Writing and Verifying: write, verify, blank and erase -b from 3.1.0b1`, touching `Writing-and-Verifying.md` (new), `Home.md`, `_Sidebar.md` and `Breaking-Changes.md`.
- **(this plan)** Wiki commit 3, subject `Say which CLI and firmware versions go together when installing and testing`, touching `Install-Beta.md` and `Testing-Chips.md`.
- **(this plan)** `.planning/phases/207-the-version-and-the-record/207-02-SUMMARY.md`, recording the three wiki shas, the F3/F4/F5/F9/F10 choices and the F5 departure.
- (plan 207-03) The live wiki `master` fast-forwarded from `81229d8` after operator approval; `evidence/207-03-wiki-prepush.txt`; `evidence/207-03-wiki-postpush-freshclone.txt`; `207-03-SUMMARY.md`.
- Nothing else: no tag, no Release, no push, no catalog change.

<tasks>

<task type="tracer">
  <name>Task 1: Breaking-Changes end to end: the 3.1.0b1 entry authored, the page-wide claim replaced, committed, checked from the committed blob</name>
  <reversibility rating="reversible">A local wiki commit; nothing is published until plan 207-03's checkpoint.</reversibility>
  <precondition>`git -C /workspaces/firestarter.wiki rev-parse --abbrev-ref HEAD` prints `master`, `git -C /workspaces/firestarter.wiki rev-parse HEAD` prints `f967398fdf653f4ee77f7ad07b37a416927763be`, its porcelain is empty, and after `git -C /workspaces/firestarter.wiki fetch origin` the ref `origin/master` is `81229d8ed8a280b935cf84c4c74f4c97afcdf475`.</precondition>
  <files>firestarter.wiki/Breaking-Changes.md</files>
  <read_first>
    - `firestarter.wiki/Breaking-Changes.md` in full (102 lines): the logo and title block (lines 1-7), the preamble (lines 9-19), the `---` rule (line 21), the `## v1.32` entry's sub-entry shape (lines 23-69), the `## v1.20` mixed-version sentence shape (lines 84-86), and the claim-stamp last line (line 102). This is the file being modified.
    - `207-RESEARCH.md` § Q3 (the compatibility matrix in both directions, with evidence per row), § Wiki Content Specification W1-W6, § Open Design Forks F4, F5, F9, F10, and § Assumptions Log A1-A4 (which rows are code-derived).
    - `207-PATTERNS.md` § "firestarter.wiki/Breaking-Changes.md" (insertion point, heading shape, sub-entry shape, hedge register).
    - `.planning/phases/204-the-command-surfaces-leave-the-firmware/204-BENCH-MATRIX.md` lines 350-400: the verbatim `ERROR: Unknown command: 6` / `ERROR: Unknown command: 4` transcripts, their exit code 1, and the no-side-effect proof.
    - `.planning/phases/205-the-pre-flights-leave-the-firmware/205-BENCH-MATRIX.md` lines 245-270: the verbatim `ERROR: Not blank, at 0x000000, v: 0x11` refusal a 3.1.0b1 CLI gets from older firmware on `write -b`.
    - `firestarter_fw/src/proms/eprom.cpp` lines 40-59, and `git -C /workspaces/firestarter_fw show 3.0.0b35:src/proms/eprom.cpp` lines 40-62: the `CMD_ERASE` arm with and without its end-of-erase blank check, which is the whole basis of the two code-derived erase rows.
    - `firestarter_app/firestarter/cli_handlers.py` lines 300-320 (a beta CLI's `fw` auto-routes to `--pre`) and the `--firmware-version` option near line 1542.
  </read_first>
  <action>
One commit in `/workspaces/firestarter.wiki`, touching only `Breaking-Changes.md`.

1. Run `git -C /workspaces/firestarter.wiki fetch origin` and confirm `origin/master` is still `81229d8ed8a280b935cf84c4c74f4c97afcdf475`. If it moved, STOP and report: someone edited the live wiki and this plan's base is stale.

2. Preamble (F5). Replace the four-line bold paragraph on lines 9-12 — the page-wide claim that every wire-protocol change breaks every mixed CLI and firmware pair and that a mismatch always fails with a timeout or decode error — with a per-entry statement: the bold lead `**Each entry says which mixed CLI and firmware pairings work.**` followed by `When in doubt, upgrade the CLI first, then the firmware:`. In the bash block that follows (lines 14-16), add `--upgrade` so it reads `pip install --pre --upgrade firestarter && firestarter fw -i --pre` (the recorded F5 departure). Keep lines 18-19 and the line-21 rule unchanged.

3. New entry. Insert directly after the line-21 `---` rule and before `## v1.32 — chip database`, followed by its own `---` rule. Heading (F4): `## 3.1.0b1 — verify and blank check run on the host`. Content, in this order; every `###` sub-section carries the literal `3.1.0b1` in its heading or body (D-03):
   a. Opening paragraph (W1): from CLI and firmware 3.1.0b1, `verify` and `blank` read the chip with an ordinary read and compare on your computer. The firmware's own verify command (command 6) and blank-check command (command 4) are removed, and those two numbers will never be reused for another command. The CLI now also runs the check that a chip is blank before a write, which the firmware used to run; `write -b` still means "skip that check", and the erase still runs.
   b. `### An older CLI on 3.1.0b1 firmware`. Open with a bold sentence (W3, F10): an older CLI's `write` no longer refuses a non-blank UV EPROM. Then one plain paragraph: before 3.1.0b1 the firmware refused that write; from 3.1.0b1 the CLI refuses it, and an older CLI has no such check, so the write starts on a chip that is not blank and fails partway, leaving it partly programmed. Do not name an error code for that failure (research A1). Then a two-column table `| Command | What happens |` with a `|---|---|` rule, rows in this order: `verify` — refused before the chip is touched, printing `ERROR: Unknown command: 6`, exit 1; `blank` — refused the same way, printing `ERROR: Unknown command: 4`, exit 1; `erase -b` — erases, then does not check, and reports success on the erase alone (from the code; not yet observed on hardware — A2); `dev test` — its verify and blank-check steps are refused, so the report describes the version mismatch rather than the chip, and must not be submitted (A4); `fw` and `fw -i` — offer to install 3.1.0b1 firmware, which must not be accepted before the CLI is upgraded. Quote both `ERROR: Unknown command: N` lines verbatim inside backticks so a search for them lands here (F9).
   c. `### A 3.1.0b1 CLI on older firmware`. `verify` and `blank` work. `write -b` onto a non-blank UV EPROM is refused by the older firmware, quoting `ERROR: Not blank, at 0x000000, v: 0x11` and saying the address and value vary; `dev test`'s UV slot writes are refused the same way. Plain `erase`, without `-b`, on electrically erasable parts that use the UV-EPROM programming path (the W27C512, for example) now also runs the older firmware's blank check after erasing, so an erase that leaves stray bits fails where it used to report success (from the code; not yet observed on hardware — A3). The fix for every row: `firestarter fw -i`.
   d. `### What to do (from 3.1.0b1)`. A `**Upgrade:**` paragraph in the § v1.32 register (W5): upgrade the CLI first with `pip install --pre --upgrade firestarter`, confirm `firestarter --version` prints 3.1.0b1 or later, then flash the firmware with `firestarter fw -i`; `firestarter fw` then shows the firmware version. To stay on an older CLI, pin the firmware instead with `firestarter fw -i --firmware-version 3.0.0b35`. Put each command in a bash code block, as the page's existing preamble does.
   State the two code-derived rows in the § v1.32 hedge register (its "software-proven and unvalidated on silicon" sentence), never with the confidence of the bench-observed rows. Do not link to the new page yet: Task 2 adds that link in the same commit that creates the page, so the link check stays green at every commit.

4. Do not touch the last line (the claim stamp; editing it would claim a re-verification nobody performed). Do not relabel the `## v1.32`, `## v1.20` or `## v1.10` headings.

5. Commit only `Breaking-Changes.md` with the subject `Document the 3.1.0b1 compatibility break between the CLI and the firmware` and a one-to-two sentence plain body (what a user with an older CLI or older firmware should expect, and to upgrade the CLI first). No phase number, decision id, `.planning/` path or milestone label in the message.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter.wiki && C=$(git log -1 --format=%H --fixed-strings --grep='Document the 3.1.0b1 compatibility break between the CLI and the firmware' HEAD) && [ -n "$C" ] && [ "$(git rev-parse "$C^1")" = "f967398fdf653f4ee77f7ad07b37a416927763be" ] && [ "$(git show --name-only --format= "$C")" = "Breaking-Changes.md" ] && B=$(git show "$C:Breaking-Changes.md") && for p in '## 3.1.0b1 — verify and blank check run on the host' '### An older CLI on 3.1.0b1 firmware' '### A 3.1.0b1 CLI on older firmware' 'ERROR: Unknown command: 6' 'ERROR: Unknown command: 4' 'ERROR: Not blank, at 0x000000, v: 0x11' 'pip install --pre --upgrade firestarter' 'firestarter fw -i --firmware-version 3.0.0b35' '**Each entry says which mixed CLI and firmware pairings work.**'; do printf '%s\n' "$B" | /usr/bin/grep -qF -e "$p" || { echo "MISSING: $p"; exit 1; }; done && [ "$(printf '%s\n' "$B" | /usr/bin/grep -cF -e 'not yet observed on hardware')" -ge 2 ] && [ "$(printf '%s\n' "$B" | sed -n 1,25p | /usr/bin/grep -cF -e 'cannot drive old firmware')" = "0" ] && [ "$(printf '%s\n' "$B" | tail -n 1)" = "$(git show 81229d8ed8a280b935cf84c4c74f4c97afcdf475:Breaking-Changes.md | tail -n 1)" ] && [ "$(printf '%s\n' "$B" | /usr/bin/grep -e '^## ' | head -2 | tr '\n' '|')" = "## 3.1.0b1 — verify and blank check run on the host|## v1.32 — chip database|" ] && echo PASS-207-02-T1</automated>
    <fails_when>stdout prints `MISSING: <text>`, or lacks `PASS-207-02-T1`: the commit's parent is not f967398, it touches more than Breaking-Changes.md, fewer than two hedged rows exist, the retired page-wide claim survives in lines 1-25, the claim-stamp last line changed, or the new entry is not the first `## ` heading above `## v1.32 — chip database`</fails_when>
    <automated>cd /workspaces/firestarter.wiki && C=$(git log -1 --format=%H --fixed-strings --grep='Document the 3.1.0b1 compatibility break between the CLI and the firmware' HEAD) && git show "$C:Breaking-Changes.md" | sed -n '/^## 3\.1\.0b1 /,/^## v1\.32 /p' | awk '/^### /{if(s!=""&&!f){print "UNLABELLED: " s; bad=1} s=$0; f=0; n++} /3\.1\.0b1/{f=1} END{if(s!=""&&!f){print "UNLABELLED: " s; bad=1} if(n<3){print "TOO FEW SUBSECTIONS: " n; bad=1} exit bad}' && echo PASS-207-02-T1-LABELS</automated>
    <fails_when>stdout prints `UNLABELLED: ### <heading>` or `TOO FEW SUBSECTIONS: N`, and `PASS-207-02-T1-LABELS` is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - The commit with subject `Document the 3.1.0b1 compatibility break between the CLI and the firmware` exists, its parent is `f967398fdf653f4ee77f7ad07b37a416927763be` (D-02), and it touches only `Breaking-Changes.md`.
    - The committed page contains `## 3.1.0b1 — verify and blank check run on the host`, and it is the first `## ` heading, directly above `## v1.32 — chip database`.
    - The committed page contains, verbatim: `ERROR: Unknown command: 6`, `ERROR: Unknown command: 4`, `ERROR: Not blank, at 0x000000, v: 0x11`, `pip install --pre --upgrade firestarter`, `firestarter fw -i --firmware-version 3.0.0b35`.
    - `### An older CLI on 3.1.0b1 firmware` exists and the first paragraph under it is bold and states that an older CLI's `write` does not refuse a non-blank UV EPROM.
    - `/usr/bin/grep -cF -e 'not yet observed on hardware'` on the committed page prints 2 or more.
    - Every `###` sub-section of the new entry contains `3.1.0b1` (D-03), and there are at least three.
    - Lines 1-25 of the committed page contain `**Each entry says which mixed CLI and firmware pairings work.**` and no longer carry the retired page-wide sentence.
    - The committed page's last line is byte-identical to `git show 81229d8ed8a280b935cf84c4c74f4c97afcdf475:Breaking-Changes.md | tail -n 1`.
    - `git -C /workspaces/firestarter.wiki status --porcelain` prints nothing, and nothing was pushed.
  </acceptance_criteria>
  <done>The 3.1.0b1 entry names the retired commands, quotes every refusal verbatim, leads with the one pairing that can harm a chip, says what to do, and replaces the page-wide claim, all committed locally on top of f967398.</done>
</task>

<task type="auto">
  <name>Task 2: The Writing-and-Verifying page, reachable from Home, the sidebar and the Breaking-Changes entry</name>
  <reversibility rating="reversible">A local wiki commit adding one page and three one-line links; nothing is published here.</reversibility>
  <files>firestarter.wiki/Writing-and-Verifying.md, firestarter.wiki/Home.md, firestarter.wiki/_Sidebar.md, firestarter.wiki/Breaking-Changes.md</files>
  <read_first>
    - `firestarter.wiki/Testing-Chips.md` in full (the structural analog: logo block, intro, prerequisite link, bash block, `---` between sections, a bold safety sentence, bold-family bullet lists, a flag paragraph).
    - `firestarter.wiki/Install-Beta.md` lines 15-30 (the table shape to copy for exit codes).
    - `firestarter.wiki/Home.md` in full and `firestarter.wiki/_Sidebar.md` in full (both modified: one line each).
    - `firestarter.wiki/Breaking-Changes.md` as committed by Task 1 (modified: one linking sentence).
    - `firestarter_app/tests/__snapshots__/test_characterization.ambr` lines 97-118 (`blank`), 164-205 (`erase`), 351-373 (`verify`) and 375-451 (`write`): the pinned `--help` text, the source of truth for every flag and exit code on the page.
    - `firestarter_app/firestarter/write_blank_guard.py` lines 40-75 (`GUARDED_PROTOCOL_IDS` and its docstring), lines 110-125 (the refusal line format), and `is_erase_exempt` (lines 171-235): which parts the CLI checks and when an erase exempts them.
    - `firestarter_app/firestarter/database.py` lines 553-582: `FLAG_CAN_ERASE` comes from the chip's `EEPROM` or `Flash/EEPROM` type, which is what makes a part "electrically erasable" on this page.
    - `firestarter_app/firestarter/cli_handlers.py` lines 580-600 (the four `write --verify` verdict lines) and lines 735-745 (`--full` refused without `--verify`).
    - `firestarter_app/firestarter/compare.py` line 33 (`MAX_RETAINED_RANGES = 64`), lines 46-50 (the classification labels) and `render_compare_lines` (lines 516-548: the range lines, the tail line, the summary line).
    - `firestarter_app/firestarter/eprom_operations.py` `verify_eprom` (grep `def verify_eprom`): how the default path stops at the first mismatch and how `--full` differs.
    - `207-RESEARCH.md` § Q5 and § Wiki Content Specification W7-W10; `207-PATTERNS.md` § "firestarter.wiki/Writing-and-Verifying.md" and § "Home.md and _Sidebar.md".
  </read_first>
  <action>
One commit in `/workspaces/firestarter.wiki` touching exactly these four files (the `bf787fb` precedent: a new page and its navigation lines land together).

1. Create `Writing-and-Verifying.md` (F3, option A). Line 1 is a byte-for-byte copy of line 1 of `Home.md` (the logo line every page carries), then an empty line, `---`, an empty line, and `# Writing and Verifying`. Then a short intro: from CLI and firmware 3.1.0b1, the CLI — not the firmware — checks that a chip is blank before a write, can read a write back and compare it, and runs `verify` and `blank`; you need the beta, with the link `[Install Beta](Install-Beta)`; and a bash block showing `firestarter write <chip> <file>`, `firestarter verify <chip> <file>`, `firestarter blank <chip>` and `firestarter erase <chip> -b`. Then the six `##` sections below, a `---` rule between each, and each `##` heading or its body carrying the literal `3.1.0b1` (D-03). No footer, no claim stamp and no HTML comment: this is a wiki-native page. Transcribe every flag, exit code and quoted line from the files in read_first; where this action's summary and the code disagree, write what the code does and record the difference in the SUMMARY.
   - `## Writing a chip (from 3.1.0b1)`: before the write reaches the programmer, the CLI reads exactly the bytes about to be written and refuses if any is not blank, printing a line of the form `Refusing write to <chip>: not blank at 0x…, v: 0x….`. Then a bullet list with bold family names, in the Testing-Chips style: **UV-erasable EPROMs** — always checked; **Electrically erasable parts** (EEPROM-type 27-series parts such as the W27C512, and flash) — the pre-write erase runs first, so they are checked only with `--skip-erase`, and NOR flash is also checked when the write starts at a non-zero address, because that erases one sector rather than the chip; **Never checked** — 28C EEPROMs (protocol `0x0D`) and page-write flash (`0x05`), which erase each page as they write it, and SRAM and FRAM, which have no blank state. Then a paragraph opening with `` `-b` / `--no-blank-check` ``: skips that check only; the erase still runs; it means what it always meant, and from 3.1.0b1 it skips the CLI's check instead of the firmware's. Then a paragraph opening with `` `--skip-erase` ``: skips the erase too, with a bold safety sentence carrying the help text's warning that skipping erase on a non-blank electrically erasable chip leaves un-erased bits that cannot be reprogrammed.
   - `## Checking a write as it lands: --verify (from 3.1.0b1)`: opt-in; after a successful write it reads the written region back and compares it through the same engine `verify` uses; plain `write` is not slowed. A table of `write --verify` exit codes with the help text's three meanings (0 landed and matched; 1 ended for a reason the CLI or the firmware decided — a blank-check refusal, a firmware error during the write, a malformed address, or a read-back that completed and disagreed; 2 the connection or the hardware failed at any step). A table of the four verdict lines, verbatim with `<chip>` in place of the format field: `Write to <chip>: verified -- the read-back matches.`, `Write to <chip>: landed, but the read-back did not verify.`, `Write to <chip>: landed, but could not be verified -- the read-back failed.`, `Write to <chip>: did not complete -- nothing was verified.`, each with when it appears. Then `--full`: with `--verify`, report every mismatching range of the read-back; refused without `--verify`. Plain `write` keeps its 0 (success) / 1 (any failure) contract.
   - `## verify and blank (from 3.1.0b1)`: both read the chip and compare on your computer, so they also work against older firmware (link `[Breaking-Changes](Breaking-Changes)`). Options `-a`/`--address`, `-s`/`--size`, `-f`/`--force`, `--full`. The compared region: `verify` defaults to the input file's length, `blank` to the whole chip, and `--size` wins; a region that runs past the chip's end (or, for `verify`, a `--size` longer than the file) is refused before the serial port opens. Exit codes 0 match or blank, 1 mismatch or not blank, 2 transport, hardware, setup or region failure. Output: by default the first mismatching range; with `--full` every range, at most 64 listed, then a line of the form `… and N more ranges, M bytes`, then one summary line naming a classification — `blank/contact`, `address-line`, `match` or `indeterminate` — with the counts and the span compared. State the default-versus-`--full` behaviour exactly as `verify_eprom` and `render_compare_lines` implement it.
   - `## Erasing with a check: erase -b (from 3.1.0b1)`: `-b` / `--blank-check` checks after the erase, through the same engine as `blank` — the opposite sense of `write -b`, which skips a check before the write. Without `-b`: 0 erased, 1 not. With `-b`: 0 erased and blank, 1 erased but not blank, 2 the check itself failed on transport, hardware or setup. `-s` / `--sector-address` together with `-b` is refused, exit 2, before anything is erased. For every non-blank range afterwards, run `firestarter blank <chip> --full`.
   - `## Exit codes at a glance (from 3.1.0b1)`: one table, `| Command | 0 | 1 | 2 |`, rows `write`, `write --verify`, `verify`, `blank`, `erase`, `erase -b`, with a dash where a command has no code 2.
   - `## Mixed CLI and firmware versions (from 3.1.0b1)`: one paragraph — upgrade the CLI first, then the firmware — and the link `[Breaking-Changes](Breaking-Changes)` for what each mixed pairing does.

2. `Home.md` § Reference: insert as the first bullet, above the `Programming-Protocols` line, `- [Writing-and-Verifying](Writing-and-Verifying) — writing a chip, checking it afterwards, and what each exit code means`.

3. `_Sidebar.md`: insert `- [Writing and Verifying](Writing-and-Verifying)` as a new line directly after `- [Testing Chips](Testing-Chips)`.

4. `Breaking-Changes.md`: at the end of the 3.1.0b1 entry's opening paragraph, add one sentence linking `[Writing and Verifying](Writing-and-Verifying)` for the full description of `write -b`, `--verify`, `--full`, `verify`, `blank` and `erase -b`. Change nothing else on that page.

5. Commit the four files with the subject `Add Writing and Verifying: write, verify, blank and erase -b from 3.1.0b1` and a one-sentence plain body. No planning identifiers in the message.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter.wiki && P=Writing-and-Verifying.md && [ "$(sed -n 1p $P)" = "$(sed -n 1p Home.md)" ] && [ "$(sed -n 2,5p $P | tr '\n' '|')" = "|---||# Writing and Verifying|" ] && for p in '--no-blank-check' '--skip-erase' '--verify' '--full' '--blank-check' '--sector-address' 'Refusing write to' 'verified -- the read-back matches.' 'landed, but the read-back did not verify.' 'landed, but could not be verified -- the read-back failed.' 'did not complete -- nothing was verified.' 'blank/contact' 'address-line' 'indeterminate' '64' '0x0D' '0x05' 'SRAM' 'FRAM' 'W27C512' '](Install-Beta)' '](Breaking-Changes)'; do /usr/bin/grep -qF -e "$p" $P || { echo "MISSING: $p"; exit 1; }; done && [ "$(/usr/bin/grep -cF -e '<!-' $P)" = "0" ] && echo PASS-207-02-T2-CONTENT</automated>
    <fails_when>stdout prints `MISSING: <text>` naming an absent flag, verdict line, classification label or link, or lacks `PASS-207-02-T2-CONTENT` because the logo line or the title block differs from the house shape or the page carries an HTML comment</fails_when>
    <automated>cd /workspaces/firestarter.wiki && awk '/^## /{if(s!=""&&!f){print "UNLABELLED: " s; bad=1} s=$0; f=0; n++} /3\.1\.0b1/{f=1} END{if(s!=""&&!f){print "UNLABELLED: " s; bad=1} if(n<6){print "TOO FEW SECTIONS: " n; bad=1} exit bad}' Writing-and-Verifying.md && [ "$(/usr/bin/grep -cxF -e '- [Writing and Verifying](Writing-and-Verifying)' _Sidebar.md)" = "1" ] && [ "$(/usr/bin/grep -cF -e '- [Writing-and-Verifying](Writing-and-Verifying) — ' Home.md)" = "1" ] && /usr/bin/grep -qF -e '](Writing-and-Verifying)' Breaking-Changes.md && bad=0 && for l in $(/usr/bin/grep -ohE '\]\([A-Za-z-]+\)' *.md | sed -E 's/^\]\(|\)$//g' | sort -u); do [ -f "$l.md" ] || { echo "BROKEN: $l"; bad=1; }; done && [ "$bad" = "0" ] && C=$(git log -1 --format=%H --fixed-strings --grep='Add Writing and Verifying: write, verify, blank and erase -b from 3.1.0b1' HEAD) && [ -n "$C" ] && [ "$(git show --name-only --format= "$C" | LC_ALL=C sort | tr '\n' ' ')" = "Breaking-Changes.md Home.md Writing-and-Verifying.md _Sidebar.md " ] && [ -z "$(git status --porcelain)" ] && echo PASS-207-02-T2-NAV</automated>
    <fails_when>stdout prints `UNLABELLED: ## <heading>`, `TOO FEW SECTIONS: N` or `BROKEN: <Page>`, or lacks `PASS-207-02-T2-NAV` because a navigation line is missing or duplicated, or the commit touches a file other than the four named</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n 1p Writing-and-Verifying.md` equals `sed -n 1p Home.md`, and lines 2-5 are an empty line, `---`, an empty line, and `# Writing and Verifying`.
    - The page contains `--no-blank-check`, `--skip-erase`, `--verify`, `--full`, `--blank-check`, `--sector-address`, `Refusing write to`, all four verdict lines verbatim, the labels `blank/contact`, `address-line` and `indeterminate`, the figure `64`, `0x0D`, `0x05`, `SRAM`, `FRAM` and `W27C512`.
    - The page has at least six `## ` sections and every one of them contains `3.1.0b1` (D-03).
    - `/usr/bin/grep -cF -e '<!-' Writing-and-Verifying.md` prints `0`.
    - `_Sidebar.md` contains exactly one line `- [Writing and Verifying](Writing-and-Verifying)`, directly after `- [Testing Chips](Testing-Chips)`.
    - `Home.md` contains exactly one `- [Writing-and-Verifying](Writing-and-Verifying) — ` line, as the first bullet of § Reference.
    - `Breaking-Changes.md` links `](Writing-and-Verifying)` inside the 3.1.0b1 entry.
    - The link check prints no `BROKEN:` line.
    - The commit `Add Writing and Verifying: write, verify, blank and erase -b from 3.1.0b1` touches exactly `Breaking-Changes.md`, `Home.md`, `Writing-and-Verifying.md` and `_Sidebar.md`, and the clone's porcelain is empty.
  </acceptance_criteria>
  <done>A user who opens the wiki can find, from Home, the sidebar or the breaking-change entry, one page that says what write -b, --skip-erase, --verify, --full, verify, blank and erase -b do and what every exit code means, transcribed from the pinned help text.</done>
</task>

<task type="auto">
  <name>Task 3: Version-pairing notes on Install-Beta and Testing-Chips, and the whole-wiki pre-publication sweep</name>
  <reversibility rating="reversible">A local wiki commit touching two sentences; nothing is published here.</reversibility>
  <files>firestarter.wiki/Install-Beta.md, firestarter.wiki/Testing-Chips.md</files>
  <read_first>
    - `firestarter.wiki/Install-Beta.md` in full (101 lines). Modified: the `--version` example on line 47, and one sentence in § 3 after the "fetches beta firmware automatically" paragraph (lines 61-62).
    - `firestarter.wiki/Testing-Chips.md` in full (107 lines). Modified: one paragraph in § "How to report it" (lines 83-103). Its last line (the relocation footer) is not touched.
    - `207-RESEARCH.md` § Q4 items 3 and 4, § Verification Commands V7 and V8, and § Common Pitfalls 3 and 7.
  </read_first>
  <action>
One commit in `/workspaces/firestarter.wiki` touching exactly `Install-Beta.md` and `Testing-Chips.md`.

1. `Install-Beta.md` line 47: change the example version in the `--version` sentence to `3.1.0b1`, so it reads that `--version` must print a beta version such as `3.1.0b1`. Leave the rest of that sentence and the `--force-reinstall` advice unchanged.

2. `Install-Beta.md` § 3 "Flash the firmware": after the paragraph that says the beta install fetches beta firmware automatically, add one sentence: from 3.1.0b1, install or upgrade the CLI before flashing, because an older CLI on 3.1.0b1 firmware cannot run `verify` or `blank` and does not stop a write to a non-blank UV EPROM, with the link `[Breaking-Changes](Breaking-Changes)`.

3. `Testing-Chips.md` § "How to report it": before the paragraph beginning "If a chip fails", add one paragraph in the plain tone of lines 102-103: from 3.1.0b1 a report is only valid when the CLI and the firmware are on the same side of 3.1.0b1 — both 3.1.0b1 or later, or both older — because a mixed pair fails steps for reasons that are not the chip's; `firestarter --version` shows the CLI and `firestarter fw` shows the firmware. Do not touch the relocation footer on the last line.

4. Commit both files with the subject `Say which CLI and firmware versions go together when installing and testing` and a one-sentence plain body. No planning identifiers in the message.

5. Run the whole-wiki sweep below. It proves the outgoing commit list is exactly `f967398` plus this plan's three commits (D-02), that every required fact is present, that no page and no outgoing commit message carries a planning identifier, that every internal link resolves, that the live wiki is still at `81229d8` (nothing pushed), and that no catalog file changed (F9). Paste its full output into the SUMMARY with the three wiki commit shas, the F3/F4/F5/F9/F10 choices, the F5 departure, and any place where the code disagreed with this plan's summary of it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter.wiki && /usr/bin/grep -qF -e 'must print a beta version such as `3.1.0b1`' Install-Beta.md && S3=$(sed -n '/^## 3\. Flash the firmware/,/^## 4\. Check that it worked/p' Install-Beta.md) && printf '%s\n' "$S3" | /usr/bin/grep -qF -e '3.1.0b1' && printf '%s\n' "$S3" | /usr/bin/grep -qF -e '](Breaking-Changes)' && sed -n '/^## How to report it/,$p' Testing-Chips.md | /usr/bin/grep -qF -e '3.1.0b1' && [ "$(tail -n 1 Testing-Chips.md)" = "$(git show f967398fdf653f4ee77f7ad07b37a416927763be:Testing-Chips.md | tail -n 1)" ] && C=$(git log -1 --format=%H --fixed-strings --grep='Say which CLI and firmware versions go together when installing and testing' HEAD) && [ -n "$C" ] && [ "$(git show --name-only --format= "$C" | LC_ALL=C sort | tr '\n' ' ')" = "Install-Beta.md Testing-Chips.md " ] && echo PASS-207-02-T3</automated>
    <fails_when>stdout lacks `PASS-207-02-T3`: the Install-Beta example is missing, or its § 3 lacks either `3.1.0b1` or the Breaking-Changes link, the Testing-Chips report section carries no `3.1.0b1` note, its footer changed, or the commit touches other files</fails_when>
    <automated>cd /workspaces/firestarter.wiki && git fetch -q origin && for p in 'Unknown command: 6' 'Unknown command: 4' '3.1.0b1' 'pip install --pre --upgrade firestarter' '--verify' '--full' '--no-blank-check' 'Refusing write to'; do /usr/bin/grep -qF -e "$p" *.md || { echo "MISSING: $p"; exit 1; }; done; if /usr/bin/grep -nE 'Phase [0-9]{3}|\bD-[0-9]+\b|\.planning/|v1\.4[0-9]' *.md; then echo "PLANNING ID IN PAGE"; exit 1; fi; if git log --format=%B origin/master..HEAD | /usr/bin/grep -nE 'Phase [0-9]{3}|\bD-[0-9]+\b|\.planning/|v1\.4[0-9]|\b20[0-9]-[0-9]{2}\b'; then echo "PLANNING ID IN COMMIT"; exit 1; fi; bad=0; for l in $(/usr/bin/grep -ohE '\]\([A-Za-z-]+\)' *.md | sed -E 's/^\]\(|\)$//g' | sort -u); do [ -f "$l.md" ] || { echo "BROKEN: $l"; bad=1; }; done; [ "$bad" = "0" ] || exit 1; [ "$(git rev-list --count origin/master..HEAD)" = "4" ] || { echo "OUTGOING COUNT"; exit 1; }; [ "$(git rev-list --reverse origin/master..HEAD | head -1)" = "f967398fdf653f4ee77f7ad07b37a416927763be" ] || { echo "F967398 NOT OLDEST"; exit 1; }; [ "$(git ls-remote origin refs/heads/master | cut -f1)" = "81229d8ed8a280b935cf84c4c74f4c97afcdf475" ] || { echo "LIVE WIKI MOVED"; exit 1; }; [ -z "$(git -C /workspaces status --porcelain -- tools/catalog)" ] || { echo "CATALOG TOUCHED"; exit 1; }; [ -z "$(git status --porcelain)" ] || { echo "DIRTY CLONE"; exit 1; }; echo PASS-207-02-SWEEP</automated>
    <fails_when>stdout prints `MISSING:`, `PLANNING ID IN PAGE` (with the offending lines above it), `PLANNING ID IN COMMIT`, `BROKEN:`, `OUTGOING COUNT`, `F967398 NOT OLDEST`, `LIVE WIKI MOVED`, `CATALOG TOUCHED` or `DIRTY CLONE`, and `PASS-207-02-SWEEP` is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `Install-Beta.md` contains ``must print a beta version such as `3.1.0b1` ``, and § 3 ("Flash the firmware", up to § 4) contains both `3.1.0b1` and `](Breaking-Changes)`.
    - `Testing-Chips.md` § "How to report it" contains `3.1.0b1`, and its last line is byte-identical to `f967398`'s.
    - The commit `Say which CLI and firmware versions go together when installing and testing` touches exactly `Install-Beta.md` and `Testing-Chips.md`.
    - D-02: `git rev-list --count origin/master..HEAD` prints `4`, and `git rev-list --reverse origin/master..HEAD | head -1` prints `f967398fdf653f4ee77f7ad07b37a416927763be`.
    - No page matches `Phase [0-9]{3}`, `\bD-[0-9]+\b`, `\.planning/` or `v1\.4[0-9]`, and no outgoing commit message matches those or a `20N-NN` plan id.
    - The link check prints no `BROKEN:` line.
    - `git -C /workspaces/firestarter.wiki ls-remote origin refs/heads/master` still prints `81229d8ed8a280b935cf84c4c74f4c97afcdf475` (nothing pushed).
    - `git -C /workspaces status --porcelain -- tools/catalog` prints nothing (F9: no catalog id).
    - `207-02-SUMMARY.md` records the three wiki commit shas, the fork choices, the F5 departure, the F9 decision, and the full sweep output ending `PASS-207-02-SWEEP`.
  </acceptance_criteria>
  <done>Installers and testers are told which CLI and firmware versions go together, and the whole outgoing wiki change — f967398 plus three commits — is proven complete, link-clean, free of planning identifiers and still unpublished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wiki clone to public wiki | Anything committed here becomes public, page text and commit messages alike, the moment plan 207-03 pushes. |
| code and bench record to wiki prose | The executor turns code and transcripts into user-facing claims; a mis-transcription becomes a false public statement. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-07 | Information Disclosure | wiki page text and commit messages | medium | mitigate | Task 3 sweep leg rejects phase numbers, decision ids, `.planning/` paths, v1.4x labels in every page and in every outgoing commit message; wiki commit subjects are fixed in this plan. |
| T-207-08 | Information Disclosure (omission) | Breaking-Changes 3.1.0b1 entry | high | mitigate | F10: the older-CLI sub-section opens with the unguarded write to a non-blank UV EPROM (Task 1 acceptance), so the page never claims less danger than the code carries. The preamble's install one-liner gains `--upgrade` so following it cannot leave an older CLI in place. |
| T-207-09 | Tampering (false claim) | code-derived compatibility rows | medium | mitigate | The two rows not observed on hardware carry `not yet observed on hardware` (Task 1 leg counts at least 2); every flag and exit code is transcribed from the snapshot-pinned help text; plan 207-03's checkpoint shows the hedged rows to the operator by name. |
| T-207-10 | Repudiation | Breaking-Changes claim stamp | low | mitigate | The last line is asserted byte-identical to `81229d8`'s, so no re-verification is claimed that nobody performed. |
| T-207-11 | Elevation of Privilege | accidental push of the wiki | high | mitigate | No task runs `git push`; the sweep asserts the live `master` is still `81229d8`. Publication happens only behind plan 207-03's `checkpoint:human-action`. |
</threat_model>

<verification>
- Task 1: `PASS-207-02-T1` and `PASS-207-02-T1-LABELS`.
- Task 2: `PASS-207-02-T2-CONTENT` and `PASS-207-02-T2-NAV`.
- Task 3: `PASS-207-02-T3` and `PASS-207-02-SWEEP`.
- ROADMAP criteria 2 and 3 are met in the committed (unpublished) wiki; plan 207-03 publishes and re-proves them from a fresh clone.
</verification>

<success_criteria>
- The wiki states that commands 4 and 6 are retired from 3.1.0b1 and never reused, what an older CLI does against 3.1.0b1 firmware (with the verbatim refusals), and what a user does about it.
- `write --verify`, `--full` and the now-CLI-side `-b` semantics are documented on a page reachable from Home and the sidebar.
- Every new statement is labelled from 3.1.0b1 (D-03), and the work sits on top of `f967398` in the existing clone (D-02).
- Nothing is pushed.
</success_criteria>

<output>
Create `.planning/phases/207-the-version-and-the-record/207-02-SUMMARY.md` when done. Record the three
wiki commit shas and subjects; the F3, F4, F5, F9 and F10 choices and the F5 `--upgrade` departure; the
F9 decision stated so the deferral chain closes ("no catalog id: the only sender of commands 4 and 6 is a
pre-3.1.0 CLI rendering through its own shipped messages; the wiki quotes the verbatim text"); any place
the code disagreed with this plan's summary of it; and the full sweep output. Commit the SUMMARY with
explicit `--files`, then confirm `git -C /workspaces rev-parse --abbrev-ref HEAD` prints
`v1.41-verification-to-host`.
</output>
