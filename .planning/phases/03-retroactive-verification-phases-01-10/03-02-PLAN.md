---
phase: 03-retroactive-verification-phases-01-10
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md
  - .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md
  - .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md
  - .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md
  - .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md
  - .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md
autonomous: true
requirements: [VERIF-03, VERIF-05, VERIF-06, VERIF-07, VERIF-10]
tags: [verification, retroactive-audit, docs-only, sc3-lock, sc4-lock]

must_haves:
  truths:
    - "03-VERIFICATION.md exists at .planning/milestones/v1.0-phases/03-eprom-algorithms/ with frontmatter status: passed, requirements_verified subset of {REQ-FW-01, REQ-SAF-01}, follow_ups carrying WARNING-5 with in_scope: false and v1.2 deferral note."
    - "05-VERIFICATION.md exists at .planning/milestones/v1.0-phases/05-intel-flash/ with frontmatter status: passed, requirements_verified subset of {REQ-FW-02, REQ-SAF-01}, body containing the verbatim phrase 'Cross-Milestone Closure — REQ-SAF-01' (SC#3 lock) and citing flash_intel.cpp:25-50 (flash_intel_check_vpp) + :77 (call-site) + Unity 6/6 PASS + cross-link to 01-VERIFICATION.md Truth #1 + #3."
    - "06-VERIFICATION.md exists at .planning/milestones/v1.0-phases/06-eeprom-page-write/ with frontmatter status: passed, requirements_verified = [REQ-FW-03], follow_ups carrying WARNING-5 with in_scope: false and v1.2 deferral note."
    - "07-VERIFICATION.md exists at .planning/milestones/v1.0-phases/07-chip-id-safety/ with frontmatter status: passed, requirements_verified = [REQ-SAF-02], Cross-Milestone Closure subsection citing v1.1 Plan 01-02 SAF-05 (eeprom_28c.cpp:55-77 helper + :82-83 call-site + Unity 4/4 PASS + 01-VERIFICATION.md Truth #2 + #4), follow_ups carrying WARNING-5."
    - "10-VERIFICATION.md exists at .planning/milestones/v1.0-phases/10-static-pins/ with frontmatter status: passed, requirements_verified subset of {REQ-FW-05, REQ-FW-06}, body containing the verbatim term 'static_high_mask' AND 'pins < 32' (SC#4 lock) plus the 5-hop chain citations (pinouts.json + database.py:get_bus_config + json_parser.c:parse_bus_config + memory.cpp:mem_util_remap_address_bus + memory.cpp:mem_util_calculate_top_address_register), and INFO-3 follow_up."
    - "Each file's body cites file:line evidence that resolves via grep -n against the live source tree at execution time."
    - "No firestarter/ or firestarter_app/ source code is modified — files_modified contains only .planning/ paths."
  artifacts:
    - path: ".planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md"
      provides: "Phase 03 retroactive verification (REQ-FW-01, REQ-SAF-01 UV-EPROM portion) + WARNING-5 follow_up"
      contains: "WARNING-5"
    - path: ".planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md"
      provides: "Phase 05 retroactive verification (REQ-FW-02, REQ-SAF-01 Intel portion) — SC#3 SAF-04 closure record"
      contains: "Cross-Milestone Closure — REQ-SAF-01"
    - path: ".planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md"
      provides: "Phase 06 retroactive verification (REQ-FW-03) + WARNING-5 follow_up"
      contains: "REQ-FW-03"
    - path: ".planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md"
      provides: "Phase 07 retroactive verification (REQ-SAF-02) + SAF-05 closure + WARNING-5 follow_up"
      contains: "eeprom28c_check_chip_id"
    - path: ".planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md"
      provides: "Phase 10 retroactive verification (REQ-FW-05, REQ-FW-06) — SC#4 static_high_mask + pins<32 lock"
      contains: "static_high_mask"
    - path: ".planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md"
      provides: "Plan 03-02 closure summary; Phase 3 fully discharged"
  key_links:
    - from: ".planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md"
      to: ".planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md"
      via: "Cross-Milestone Closure subsection citing SAF-04 (Truth #1 + #3)"
      pattern: "flash_intel_check_vpp"
    - from: ".planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md"
      to: ".planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md"
      via: "Cross-Milestone Closure subsection citing SAF-05 (Truth #2 + #4)"
      pattern: "eeprom28c_check_chip_id"
    - from: ".planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md"
      to: "firestarter/src/proms/memory.cpp::mem_util_calculate_top_address_register"
      via: "SC#4 explicit lock — pins<32 guard"
      pattern: "pins < 32"
---

<objective>
Author the "closure-and-hazard batch" of 5 retroactive VERIFICATION.md audit artifacts for v1.0 phases 03, 05, 06, 07, 10 — the phases that either carry an open hazard (`follow_ups` entry for WARNING-5 or INFO-3) OR record a v1.1 cross-milestone closure (SAF-04 for REQ-SAF-01 Intel portion in Phase 05, SAF-05 for REQ-SAF-02 28C portion in Phase 07). Two explicit ROADMAP success-criterion locks land in this plan: **SC#3** ("05-VERIFICATION.md records SAF-04 closure of REQ-SAF-01") and **SC#4** ("10-VERIFICATION.md confirms static_high_mask end-to-end + pins<32 VPE_TO_VPP guard intact in current memory.cpp").

Purpose: Close the remaining 8 PARTIAL findings from `v1.0-MILESTONE-AUDIT.md` `gaps.requirements` (REQ-FW-01 / REQ-SAF-01 UV-EPROM portion in Phase 03, REQ-FW-02 / REQ-SAF-01 Intel portion in Phase 05, REQ-FW-03 in Phase 06, REQ-SAF-02 in Phase 07, REQ-FW-05 / REQ-FW-06 in Phase 10) per ROADMAP Phase 3 SC#1 + SC#2 + SC#3 + SC#4. Together with Plan 03-01, the 10-file batch fully discharges Phase 3.

Output: 5 markdown verification files under `.planning/milestones/v1.0-phases/{NN-slug}/` plus a `03-02-SUMMARY.md` recording what landed. No source code changes anywhere; all writes confined to `.planning/`.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-retroactive-verification-phases-01-10/03-CONTEXT.md
@.planning/phases/03-retroactive-verification-phases-01-10/03-RESEARCH.md
@.planning/phases/03-retroactive-verification-phases-01-10/03-VALIDATION.md
@.planning/phases/03-retroactive-verification-phases-01-10/03-01-SUMMARY.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@.planning/milestones/v1.0-INTEGRATION-CHECK.md
@.planning/milestones/v1.0-phases/11-build-db-cleanup/11-VERIFICATION.md
@.planning/milestones/v1.0-phases/12-close-gap-blocker-1-algorithm-based-dispatch-for-protocols-0/12-VERIFICATION.md
@.planning/milestones/v1.0-phases/13-close-gap-warning-5-at28c256-64-5v-eeprom-override-12v-on-we/13-VERIFICATION.md
@.planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md
</context>

<threat_model>
severity: informational; no_threats: true. Plan writes only markdown documents under `.planning/`. These are not internet-reachable, not parsed by any runtime, not granted any capability. No attack surface introduced.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write 03-VERIFICATION.md (Phase 03 — UV-EPROM; REQ-FW-01 + REQ-SAF-01 UV-EPROM portion; Phase 12 inline + WARNING-5 follow_up)</name>
  <files>.planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md</files>

  <read_first>
    - .planning/milestones/v1.0-phases/13-close-gap-warning-5-at28c256-64-5v-eeprom-override-12v-on-we/13-VERIFICATION.md — canonical `follow_ups` block shape for WARNING-5 entries (use verbatim schema)
    - .planning/milestones/v1.0-phases/12-close-gap-blocker-1-algorithm-based-dispatch-for-protocols-0/12-VERIFICATION.md — Phase 12 algo-first dispatch closure for REQ-FW-01 (0x07/0x08/0x0B UV-EPROM protocols); inline-cite target
    - .planning/milestones/v1.0-phases/11-build-db-cleanup/11-VERIFICATION.md — template + 4-entry follow_ups block
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md — v1.1 form
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md `gaps.requirements` entry for REQ-SAF-01 (PARTIAL, attributed to "03-eprom-algorithms (and 05-intel-flash)") + WARNING-5 integration block (AT28C256/64 30-chip hazard; affects REQ-FW-03 + REQ-SAF-01)
    - .planning/milestones/v1.0-INTEGRATION-CHECK.md row 20 (Phase 12 algo-first dispatch 0x07/0x08/0x0B) + REQ-SAF-01 wire-trace row
    - .planning/milestones/v1.0-phases/03-eprom-algorithms/03-01-PLAN.md + 03-01-SUMMARY.md
    - .planning/phases/03-retroactive-verification-phases-01-10/03-RESEARCH.md §"Phase 03 — UV-EPROM Algorithm Correctness" — current line cites (eprom.cpp:40/68-74/144/199/207/250-251, memory.cpp:92-95) + §"Cross-Milestone Closure Narrative Sources — 03-VERIFICATION.md" recommendation (inline cite, not dedicated subsection)
    - Live grep targets: firestarter/src/proms/eprom.cpp, firestarter/src/proms/memory.cpp
  </read_first>

  <action>
    Author `.planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md`. Frontmatter `phase: 03-eprom-algorithms`, `requirements_verified: [REQ-FW-01, REQ-SAF-01]` (REQ-SAF-01 = UV-EPROM portion; the Intel portion is owned by 05-VERIFICATION.md per RESEARCH.md Open Question #2), `status: passed`, `score: 2/2 must-haves verified`, `overrides_applied: 0`.

    Required `follow_ups` frontmatter entry for WARNING-5 (per CONTEXT.md `<specifics>` lines 483-503 + RESEARCH.md §"`follow_ups` Schema" + 13-VERIFICATION.md canonical form):
    ```yaml
    follow_ups:
      - source: v1.0-MILESTONE-AUDIT.md WARNING-5 (escalated by Phase 12)
        item: "AT28C256/64 family (30 chips, algo=0x07 + electrical.type='Flash/EEPROM') route through configure_eprom and apply 12V P1_VPP to socket pin 1 (= /WE on DIP28_2764) during write. Reads safe."
        severity: warning
        in_scope: false
        note: "Deferred to v1.2 per MILESTONES.md. Upstream-DB classification issue (minipro tags AT28C256 as EPROM_STD); not a v1.0 Phase 03 defect. Tracked as a per-chip override candidate."
    ```

    Required high-value file:line citations:
    - `firestarter/src/proms/eprom.cpp:40` — `configure_eprom` entry — REQ-FW-01
    - `firestarter/src/proms/eprom.cpp:68-74` — `pulse_delay` default switch on `handle->protocol` — REQ-FW-01 protocol semantics
    - `firestarter/src/proms/eprom.cpp:144, :207` — VPE_AS_VPP routing (`protocol == 0x0B || FLAG_VPE_AS_VPP`) — REQ-FW-01 wire detail
    - `firestarter/src/proms/eprom.cpp:199` — `eprom_check_vpp` definition (calls `rurp_read_voltage_mv`) — REQ-SAF-01 UV-EPROM evidence
    - `firestarter/src/proms/eprom.cpp:250-251` — `eprom_check_vpp` unconditional call in `eprom_generic_init` — REQ-SAF-01 UV-EPROM lock
    - `firestarter/src/proms/memory.cpp:92-95` — Algo-first dispatch for 0x07/0x08/0x0B (Phase 12 closure for FW-01) — cite v1.0-INTEGRATION-CHECK row 20 + `12-VERIFICATION.md` Truth #1 inline (NOT in a Cross-Milestone Closure subsection per CONTEXT.md "Claude's Discretion" + RESEARCH.md recommendation: Phase 12 is v1.0, not v1.1)

    Cross-Milestone Closure subsection: OMIT (Phase 12 inline cite suffices; no v1.1 work closed REQ-FW-01 or REQ-SAF-01 UV-EPROM portion — UV-EPROM was always WIRED).

    Body sections: Goal → Observable Truths (REQ-FW-01 protocol-aware dispatch WIRED + REQ-SAF-01 UV-EPROM VPP-ADC compare unconditional) → Required Artifacts (eprom.cpp) → Key Link Verification (eprom.cpp ↔ memory.cpp 0x07/0x08/0x0B dispatch) → Data-Flow Trace (REQ-FW-01 has a multi-hop chain: DB algorithm int → wire JSON → handle->protocol → memory.cpp dispatch → configure_eprom; INCLUDE) → Behavioral Spot-Checks (cite `01-VERIFICATION.md` 25/25 native + `02-VERIFICATION.md` SC4 743/743 dispatch) → Requirements Coverage (REQ-FW-01 SATISFIED with inline Phase-12 reachability cite; REQ-SAF-01 UV-EPROM portion SATISFIED — note Intel portion owned by 05-VERIFICATION.md per audit attribution split) → Anti-Patterns Found (pre-existing only) → Gaps Summary (WARNING-5 deferred to v1.2).

    Body length 100-160 lines.

    Commit: `docs(03-02): retroactive verification — Phase 03 (REQ-FW-01, REQ-SAF-01 UV-EPROM)`.
  </action>

  <verify>
    <automated>test -f .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && head -1 .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md | grep -q '^---' && grep -q '^phase: 03-eprom-algorithms' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && grep -q '^status: passed' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && grep -q 'REQ-FW-01' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && grep -q 'REQ-SAF-01' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && grep -q 'WARNING-5' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && grep -q 'in_scope: false' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md && grep -q 'eprom_check_vpp\|eprom.cpp' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at exact path `.planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md`.
    - Frontmatter opens/closes with `---`.
    - Frontmatter `phase: 03-eprom-algorithms`.
    - Frontmatter `requirements_verified: [REQ-FW-01, REQ-SAF-01]` (subset of audit attribution — REQ-SAF-01 UV-EPROM portion; Intel portion owned by 05).
    - Frontmatter `status: passed`, `overrides_applied: 0`.
    - Frontmatter `follow_ups:` contains an entry with `source` naming `WARNING-5`, `in_scope: false`, and `note:` mentioning v1.2 (deferral target).
    - Body cites `eprom.cpp:250-251` (eprom_check_vpp unconditional call), `eprom.cpp:199` (definition), AND `memory.cpp:92-95` (Phase 12 algo-first 0x07/0x08/0x0B dispatch).
    - Body inline-cites Phase 12 for REQ-FW-01 reachability (one-line "Reachability closed by Phase 12 — see `12-VERIFICATION.md`" inside Requirements Coverage row).
    - Every `file:line` citation resolves via `grep -n` at write time.
    - Body length 100-160 lines.
    - Footer present.
    - Commit message matches `docs(03-02): retroactive verification — Phase 03 (REQ-FW-01, REQ-SAF-01 UV-EPROM)` or close variant.
  </acceptance_criteria>

  <done>03-VERIFICATION.md committed; REQ-FW-01 + REQ-SAF-01 UV-EPROM portion SATISFIED + WARNING-5 carried as follow_up.</done>
</task>

<task type="auto">
  <name>Task 2: Write 05-VERIFICATION.md (Phase 05 — Intel Flash; REQ-FW-02 + REQ-SAF-01 Intel portion; SC#3 SAF-04 closure lock)</name>
  <files>.planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md — **primary source** for SC#3 SAF-04 closure narrative (Truth #1 + Truth #3 + Post-Review Fixes CR-01 regression test record); cite this verbatim by Truth # and section
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-01-PLAN.md + 01-01-SUMMARY.md — SAF-04 implementation detail (inline-copy static helper, NOT shared extract — STATE.md D-04 decision)
    - .planning/milestones/v1.0-phases/12-close-gap-blocker-1-algorithm-based-dispatch-for-protocols-0/12-VERIFICATION.md — 8/8 dense reference (template)
    - .planning/milestones/v1.0-phases/11-build-db-cleanup/11-VERIFICATION.md — template + follow_ups schema (this file has NO follow_ups, but read for body shape)
    - .planning/milestones/v1.0-phases/13-close-gap-warning-5-at28c256-64-5v-eeprom-override-12v-on-we/13-VERIFICATION.md — closure-style reference (Phase 13 closed WARNING-5 at data layer; useful for Cross-Milestone Closure framing precedent)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md `gaps.requirements` entries for REQ-FW-02 (PARTIAL — `configure_flash_intel` + 0x10 dispatch WIRED but WARNING-1 missing VPP ADC) and REQ-SAF-01 (UNSATISFIED at v1.0 close for Intel-flash family, closed by SAF-04)
    - .planning/milestones/v1.0-INTEGRATION-CHECK.md row 8 (Intel 0x10 dispatch) + REQ-SAF-01 wire-trace row
    - .planning/milestones/v1.0-phases/05-intel-flash/05-01-PLAN.md + 05-01-SUMMARY.md
    - .planning/phases/03-retroactive-verification-phases-01-10/03-RESEARCH.md §"Phase 05 — Intel Flash" + §"SC#3 (Intel-flash REQ-SAF-01 closure recorded in 05-VERIFICATION.md)" + §"Cross-Milestone Closure Narrative Sources — 05-VERIFICATION.md" (primary cite = 01-VERIFICATION.md Truths #1 + #3; secondary = 01-01-SUMMARY.md; spot-check NEW state = flash_intel.cpp:25-50/77/79-85; Unity = test_flash_intel_vpp/ 6/6 PASS; commits firestarter@f6480f2 + meta 83c37b7)
    - Live grep targets: firestarter/src/proms/flash_intel.cpp, firestarter/src/proms/memory.cpp, firestarter/test/native/avr/test_flash_intel_vpp/
  </read_first>

  <action>
    Author `.planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md`. This is one of two SC-lock files in this plan (SC#3). Frontmatter `phase: 05-intel-flash`, `requirements_verified: [REQ-FW-02, REQ-SAF-01]` (REQ-SAF-01 = Intel portion; UV-EPROM portion owned by 03-VERIFICATION.md per RESEARCH.md Open Question #2), `status: passed`, `score: N/N must-haves verified` (likely 3 Truths: REQ-FW-02 handler WIRED + 0x10 dispatch WIRED + REQ-SAF-01 Intel VPP-ADC compare via SAF-04), `overrides_applied: 0`, no `follow_ups` block (Phase 05 carries no open hazard — SAF-04 closed REQ-SAF-01 fully for the Intel family).

    **SC#3 lock — required body subsection** (per ROADMAP SC#3 "05-VERIFICATION.md explicitly records that the SAF-04 closure from Phase 1 satisfies the Intel-flash REQ-SAF-01 gap"):

    Subsection title MUST contain the verbatim phrase `Cross-Milestone Closure — REQ-SAF-01` (acceptance gate string-matches this substring). Suggested full heading: `### Cross-Milestone Closure — REQ-SAF-01 (Intel-flash VPP ADC compare)`.

    Subsection content (model on CONTEXT.md `<specifics>` lines 445-468 sample text, validated correct by RESEARCH.md §"SC#3"):
    - Narrative: v1.0 `flash_intel_write_init` shipped without `rurp_read_voltage_mv()` pre-pulse VPP ADC compare → flagged WARNING-1 in v1.0-MILESTONE-AUDIT.md → escalated to REQ-SAF-01 UNSATISFIED at v1.0 milestone close (39 algo=0x10 Intel-flash chips affected).
    - Closure: "Closed by v1.1 Plan 01-01 (SAF-04)". Cite `flash_intel.cpp:25-50` (helper), `:77` (call-site, before chip_id branch at :87 and before any write command), `:79-85` (CR-01 regression fix — VPP error path clears `REGULATOR | P1_VPP_ENABLE` before returning).
    - Unity coverage: 6/6 PASS in `test_flash_intel_vpp/test_flash_intel_vpp.cpp`; specifically `test_flash_intel_high_vpp_error_clears_regulator` covers CR-01.
    - Cross-link: "See `.planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md` Truth #1 + #3 for the canonical v1.1 verification record."
    - Verdict: "REQ-SAF-01 is SATISFIED for the Intel-flash family as of 2026-05-12."
    - Commit refs: `firestarter@f6480f2` (CR-01 fix); meta-repo `83c37b7` (state record).

    Required high-value file:line citations to embed elsewhere in body:
    - `firestarter/src/proms/flash_intel.cpp:25-50` — `flash_intel_check_vpp` static helper (SAF-04, v1.1 Plan 01-01) — SC#3 lock evidence
    - `firestarter/src/proms/flash_intel.cpp:74` (write_init def), `:77` (helper call-site) — SC#3 lock
    - `firestarter/src/proms/flash_intel.cpp:87` (call), `:152` (def) — `flash_intel_check_chip_id` (REQ-SAF-02 Intel portion, also relevant to Phase 07 — but Phase 05's scope is REQ-FW-02 + REQ-SAF-01)
    - `firestarter/src/proms/flash_intel.cpp:96-98` — `flash_intel_write_init` blank-check site (REQ-SAF-03 cross-handler — owned by 08; cite as supporting evidence)
    - `firestarter/src/proms/memory.cpp:72-75` — Intel 0x10 dispatch — REQ-FW-02 evidence
    - `firestarter/test/native/avr/test_flash_intel_vpp/` directory — Unity 6/6 PASS (cite 01-VERIFICATION.md Truth #3 for execution record; do NOT re-run per RESEARCH.md Pitfall #3)

    Body sections: Goal → Observable Truths (3: configure_flash_intel WIRED + 0x10 dispatch WIRED + SAF-04 VPP ADC compare unconditional pre-write) → Required Artifacts (flash_intel.cpp + memory.cpp + Unity suite) → Key Link Verification (memory.cpp 0x10 → configure_flash_intel → flash_intel_write_init → flash_intel_check_vpp → rurp_read_voltage_mv) → Data-Flow Trace (Level 4 — handle->vpp_mv flows from DB through wire to ADC compare; INCLUDE — this is the SC#3 lock data path) → Behavioral Spot-Checks (cite `01-VERIFICATION.md` Truth #3 native suite 6/6 PASS for Unity coverage; do NOT re-run) → Requirements Coverage (REQ-FW-02 SATISFIED with row cite to memory.cpp:72-75 + Phase 12; REQ-SAF-01 Intel portion SATISFIED with row cite to flash_intel.cpp:77 SAF-04 call) → Anti-Patterns Found (pre-existing only; note D-04 decision — `eprom_check_vpp` left byte-identical, helper extraction deferred to cleanup) → **Cross-Milestone Closure — REQ-SAF-01 (the SC#3 subsection above)** → Gaps Summary (no gaps).

    Body length 120-160 lines (highest content density of all 10 files because SC#3 closure narrative is mandatory and detailed).

    Commit: `docs(03-02): retroactive verification — Phase 05 (REQ-FW-02, REQ-SAF-01 Intel) — SC#3 SAF-04 closure`.
  </action>

  <verify>
    <automated>test -f .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && head -1 .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md | grep -q '^---' && grep -q '^phase: 05-intel-flash' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q '^status: passed' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q 'REQ-FW-02' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q 'REQ-SAF-01' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q 'Cross-Milestone Closure — REQ-SAF-01\|Cross-Milestone Closure -- REQ-SAF-01' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q 'flash_intel_check_vpp' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q 'flash_intel.cpp' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && grep -q 'SAF-04' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at exact path `.planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md`.
    - Frontmatter opens/closes with `---`.
    - Frontmatter `phase: 05-intel-flash`.
    - Frontmatter `requirements_verified: [REQ-FW-02, REQ-SAF-01]` (Intel portion).
    - Frontmatter `status: passed`, `overrides_applied: 0`, no `follow_ups` block.
    - **SC#3 lock — body contains the verbatim phrase `Cross-Milestone Closure — REQ-SAF-01`** (em-dash or double-hyphen variant acceptable — grep gate string-matches both).
    - Body cites `flash_intel.cpp:25-50` (helper def) AND `flash_intel.cpp:77` (call-site) AND mentions `SAF-04` AND cross-links `01-VERIFICATION.md` Truth #1 or Truth #3.
    - Body mentions Unity test asset (`test_flash_intel_vpp` or "6/6 PASS").
    - Body cites `memory.cpp:72-75` (Intel 0x10 dispatch) for REQ-FW-02.
    - Every `file:line` citation resolves via `grep -n` at write time.
    - Body length 120-160 lines (this is the highest-density file in the plan — do NOT trim below 120 lines without escalation).
    - Footer present.
    - Commit message matches `docs(03-02): retroactive verification — Phase 05 (REQ-FW-02, REQ-SAF-01 Intel) — SC#3 SAF-04 closure` or close variant naming SC#3.
  </acceptance_criteria>

  <done>05-VERIFICATION.md committed; REQ-FW-02 + REQ-SAF-01 Intel portion SATISFIED; SC#3 SAF-04 closure narrative locked with grep-verifiable evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Write 06-VERIFICATION.md (Phase 06 — EEPROM Page Write; REQ-FW-03; WARNING-5 follow_up; no v1.1 closure)</name>
  <files>.planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md</files>

  <read_first>
    - .planning/milestones/v1.0-phases/13-close-gap-warning-5-at28c256-64-5v-eeprom-override-12v-on-we/13-VERIFICATION.md — canonical `follow_ups` shape for WARNING-5 + optional sidebar cite for Phase 13 data-layer closure of WARNING-5 (per CONTEXT.md D-04 and RESEARCH.md §"Cross-Milestone Closure Narrative Sources — 06-VERIFICATION.md")
    - .planning/milestones/v1.0-phases/11-build-db-cleanup/11-VERIFICATION.md — template
    - .planning/milestones/v1.0-phases/12-close-gap-blocker-1-algorithm-based-dispatch-for-protocols-0/12-VERIFICATION.md — dense reference
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md — v1.1 form (Phase 1 left eeprom_28c.cpp internal logic untouched for REQ-FW-03; only added SAF-05 chip-id helper which is REQ-SAF-02 scope, owned by 07)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md `gaps.requirements` entry for REQ-FW-03 (PARTIAL — handler correct + 0x0D dispatch WIRED + WARNING-5 AT28C256 mistag; 30 chips affected) + WARNING-5 integration block
    - .planning/milestones/v1.0-INTEGRATION-CHECK.md row 9 (0x0D dispatch)
    - .planning/milestones/v1.0-phases/06-eeprom-page-write/06-01-PLAN.md + 06-01-SUMMARY.md
    - .planning/phases/03-retroactive-verification-phases-01-10/03-RESEARCH.md §"Phase 06 — EEPROM Page Write" + §"Cross-Milestone Closure Narrative Sources — 06-VERIFICATION.md" (NO Cross-Milestone Closure subsection; WARNING-5 carried as follow_up; OPTIONAL sidebar cite of 13-VERIFICATION.md) + Pitfall #10 (don't conflate REQ-FW-03 SATISFIED with AT28C256 hazard closed)
    - Live grep targets: firestarter/src/proms/eeprom_28c.cpp, firestarter/src/proms/memory.cpp
  </read_first>

  <action>
    Author `.planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md`. Frontmatter `phase: 06-eeprom-page-write`, `requirements_verified: [REQ-FW-03]` (just one; REQ-SAF-02 is owned by 07-VERIFICATION.md per RESEARCH.md Open Question split), `status: passed`, `score: 1/1 must-haves verified` (handler is correct for the algo=0x0D dispatch path), `overrides_applied: 0`.

    Required `follow_ups` frontmatter entry for WARNING-5 (verbatim shape same as Task 1 / 03-VERIFICATION.md but `not a v1.0 Phase 06 defect`):
    ```yaml
    follow_ups:
      - source: v1.0-MILESTONE-AUDIT.md WARNING-5 (escalated by Phase 12)
        item: "AT28C256/64 family (30 chips, algo=0x07 + electrical.type='Flash/EEPROM') route through configure_eprom, not the 0x0D handler. Handler logic itself is correct."
        severity: warning
        in_scope: false
        note: "Deferred to v1.2 per MILESTONES.md. Upstream-DB classification issue (minipro tags AT28C256 as EPROM_STD with algo=0x07); not a v1.0 Phase 06 defect — chips never reach this handler. Tracked as a per-chip override candidate."
    ```

    Required high-value file:line citations:
    - `firestarter/src/proms/eeprom_28c.cpp:34` — `configure_eeprom28c` entry — REQ-FW-03
    - `firestarter/src/proms/eeprom_28c.cpp:23` — `EEPROM_SDP_DISABLE` byte-flip table — REQ-FW-03 SDP handling
    - `firestarter/src/proms/eeprom_28c.cpp:75` — `eeprom28c_write_init` — REQ-FW-03
    - `firestarter/src/proms/eeprom_28c.cpp:91` — SDP_DISABLE call (`flash_execute_command(EEPROM_SDP_DISABLE)`) — REQ-FW-03 page-write protocol
    - `firestarter/src/proms/eeprom_28c.cpp:91-93` — blank-check site (cross-handler scope owned by 08, cite as supporting evidence)
    - `firestarter/src/proms/memory.cpp:77-80` — 0x0D dispatch — REQ-FW-03 wire

    Cross-Milestone Closure subsection: OMIT (no v1.1 work touched eeprom_28c handler internals; SAF-05 added the chip-id helper which is REQ-SAF-02 scope, owned by 07). Optional one-paragraph sidebar pointer to 13-VERIFICATION.md ("Phase 13 closed WARNING-5 at the *data* layer for the canonical 23 AT28C256/64 chips via `_28C_EEPROM_HAZARD_PINOUT` guard; v1.1 inherits the override") — discretionary per RESEARCH.md §"06-VERIFICATION.md".

    Body sections: Goal → Observable Truths (1: REQ-FW-03 28C handler WIRED + 0x0D dispatch reachable) → Required Artifacts (eeprom_28c.cpp) → Key Link Verification (memory.cpp 0x0D → configure_eeprom28c → eeprom28c_write_init → SDP_DISABLE → page-write) → Data-Flow Trace (OPTIONAL — single-handler change, may omit) → Behavioral Spot-Checks (cite `01-VERIFICATION.md` 25/25 native + `13-VERIFICATION.md` for the WARNING-5 data-layer override evidence) → Requirements Coverage (REQ-FW-03 SATISFIED with handler-logic-correct narrative; WARNING-5 explicit caveat — handler logic is correct, AT28C256 hazard is upstream-data classification, not handler-logic defect, per RESEARCH.md Pitfall #10) → Anti-Patterns Found (pre-existing only) → Gaps Summary (WARNING-5 deferred to v1.2).

    Body length 100-160 lines.

    Commit: `docs(03-02): retroactive verification — Phase 06 (REQ-FW-03; WARNING-5 follow_up)`.
  </action>

  <verify>
    <automated>test -f .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md && head -1 .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md | grep -q '^---' && grep -q '^phase: 06-eeprom-page-write' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md && grep -q '^status: passed' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md && grep -q 'REQ-FW-03' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md && grep -q 'WARNING-5' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md && grep -q 'in_scope: false' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md && grep -q 'eeprom_28c.cpp\|configure_eeprom28c' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at exact path `.planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md`.
    - Frontmatter opens/closes with `---`.
    - Frontmatter `phase: 06-eeprom-page-write`.
    - Frontmatter `requirements_verified: [REQ-FW-03]` exactly.
    - Frontmatter `status: passed`, `overrides_applied: 0`.
    - Frontmatter `follow_ups:` contains an entry with `source` naming `WARNING-5`, `in_scope: false`, `note:` mentioning v1.2, and `note:` explicitly stating "not a v1.0 Phase 06 defect" or equivalent.
    - Body does NOT contain a `Cross-Milestone Closure` heading (per CONTEXT.md D-02 + RESEARCH.md §"06-VERIFICATION.md") — optional sidebar paragraph to 13-VERIFICATION.md is acceptable.
    - Body cites `eeprom_28c.cpp:34` (configure_eeprom28c) AND `eeprom_28c.cpp:75` (write_init) AND `memory.cpp:77-80` (0x0D dispatch).
    - Body explicitly notes the WARNING-5 caveat: handler-logic-correct + AT28C256-hazard-is-upstream (per RESEARCH.md Pitfall #10).
    - Every `file:line` citation resolves via `grep -n` at write time.
    - Body length 100-160 lines.
    - Footer present.
    - Commit message matches `docs(03-02): retroactive verification — Phase 06 (REQ-FW-03; WARNING-5)` or close variant.
  </acceptance_criteria>

  <done>06-VERIFICATION.md committed; REQ-FW-03 SATISFIED + WARNING-5 carried with correct attribution.</done>
</task>

<task type="auto">
  <name>Task 4: Write 07-VERIFICATION.md (Phase 07 — Chip-ID Safety; REQ-SAF-02; SAF-05 closure; WARNING-5 follow_up)</name>
  <files>.planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md — **primary source** for SAF-05 closure narrative (Truth #2 + Truth #4 + Post-Review Fixes CR-02 mem_size < 64 underflow guard); cite verbatim
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-02-PLAN.md + 01-02-SUMMARY.md — SAF-05 implementation detail (A9-12V identification per RESEARCH.md datasheet evidence, NOT the AMD/SST JEDEC sequence proposed in CONTEXT.md D-05 — STATE.md "D-05 override (SAF-05, load-bearing)" decision)
    - .planning/milestones/v1.0-phases/13-close-gap-warning-5-at28c256-64-5v-eeprom-override-12v-on-we/13-VERIFICATION.md — WARNING-5 follow_up shape
    - .planning/milestones/v1.0-phases/11-build-db-cleanup/11-VERIFICATION.md — template
    - .planning/milestones/v1.0-phases/12-close-gap-blocker-1-algorithm-based-dispatch-for-protocols-0/12-VERIFICATION.md — dense reference
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md `gaps.requirements` entry for REQ-SAF-02 (PARTIAL — Intel + UV-EPROM + flash3 WIRED; WARNING-2 = eeprom_28c ignored chip_id forward-compat hazard, closed by SAF-05)
    - .planning/milestones/v1.0-INTEGRATION-CHECK.md row for REQ-SAF-02 (chip-id wire-trace)
    - .planning/milestones/v1.0-phases/07-chip-id-safety/07-01-PLAN.md + 07-01-SUMMARY.md
    - .planning/phases/03-retroactive-verification-phases-01-10/03-RESEARCH.md §"Phase 07 — Chip-ID Safety" + §"Cross-Milestone Closure Narrative Sources — 07-VERIFICATION.md" (primary cite = 01-VERIFICATION.md Truths #2 + #4 + CR-02; spot-check NEW state = eeprom_28c.cpp:55-77 helper + :82-83 call-site gated on chip_id>0 BEFORE SDP_DISABLE at :91 + :60-64 CR-02 underflow guard; Unity = test_eeprom28c_chip_id/ 4/4 PASS)
    - Live grep targets: firestarter/src/proms/flash_intel.cpp, firestarter/src/proms/eprom.cpp, firestarter/src/proms/eeprom_28c.cpp, firestarter/test/native/avr/test_eeprom28c_chip_id/
  </read_first>

  <action>
    Author `.planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md`. Frontmatter `phase: 07-chip-id-safety`, `requirements_verified: [REQ-SAF-02]`, `status: passed`, `score: N/N must-haves verified` (likely 3 Truths: Intel chip-id check + UV-EPROM chip-id branch + 28C chip-id check forward-compat closed by SAF-05), `overrides_applied: 0`.

    Required `follow_ups` frontmatter entry for WARNING-5 (28C path not reached for AT28C256 today — related but distinct from the handler-logic WARNING-5 in 03/06; per CONTEXT.md `<specifics>` row 7 "WARNING-5 (related: 28C path not reached for AT28C256)"):
    ```yaml
    follow_ups:
      - source: v1.0-MILESTONE-AUDIT.md WARNING-5 (escalated by Phase 12)
        item: "AT28C256/64 family routes via algo=0x07 → configure_eprom, not the 0x0D handler. SAF-05's 28C chip-id check (eeprom_28c.cpp:55-77) is forward-compat ready but vacuous for the AT28C-family today (no 0x0D chip in DB has chip_id_value populated; AT28C256 doesn't reach 0x0D at all)."
        severity: warning
        in_scope: false
        note: "Deferred to v1.2 per MILESTONES.md. Closing the AT28C-family routing is an upstream-DB classification fix, not a chip-id-validation defect. SAF-05 forward-compat scope is satisfied."
    ```

    **Required body subsection: Cross-Milestone Closure — REQ-SAF-02 (28C SAF-05 closure)** — narrative per RESEARCH.md §"07-VERIFICATION.md":
    - Narrative: v1.0 audit WARNING-2 flagged `eeprom_28c.cpp::eeprom28c_write_init` for ignoring `handle->chip_id` — vacuous-today / forward-compat hazard. REQ-SAF-02 was PARTIAL because the 28C family path lacked chip-id validation.
    - Closure: "Closed by v1.1 Plan 01-02 (SAF-05)". Cite `eeprom_28c.cpp:55-77` (helper definition), `:82-83` (call-site, gated on `chip_id > 0`, BEFORE `flash_execute_command(EEPROM_SDP_DISABLE)` at `:91`), `:60-64` (CR-02 underflow guard for `mem_size < 64`).
    - Override note: per STATE.md "D-05 override (SAF-05, load-bearing)", implementation uses A9-12V identification (datasheet evidence), NOT the AMD/SST JEDEC AA/55/90 sequence proposed in CONTEXT.md D-05. JEDEC sequence would corrupt address 0x5555 on SDP-disabled AT28C parts.
    - Unity coverage: 4/4 PASS in `test_eeprom28c_chip_id/`.
    - Cross-link: `01-VERIFICATION.md` Truth #2 + #4 + Post-Review Fixes CR-02.
    - Verdict: "REQ-SAF-02 is SATISFIED for the 28C family as of 2026-05-12 (forward-compat ready; vacuous-today caveat covered by WARNING-5 follow_up)."

    Required high-value file:line citations to embed in body:
    - `firestarter/src/proms/flash_intel.cpp:87` — Intel `chip_id > 0` branch in write_init — REQ-SAF-02 (Intel)
    - `firestarter/src/proms/flash_intel.cpp:152` — `flash_intel_check_chip_id` definition — REQ-SAF-02 (Intel) evidence
    - `firestarter/src/proms/eprom.cpp:250` (inside `eprom_generic_init`) — UV-EPROM chip-id branch — REQ-SAF-02 (UV-EPROM)
    - `firestarter/src/proms/eeprom_28c.cpp:55-77` — `eeprom28c_check_chip_id` static helper (SAF-05, v1.1 Plan 01-02) — REQ-SAF-02 (28C) closure evidence
    - `firestarter/src/proms/eeprom_28c.cpp:78-79` — call-site gated on `chip_id > 0` (BEFORE `:91` SDP_DISABLE) — SAF-05 ordering
    - `firestarter/test/native/avr/test_eeprom28c_chip_id/` directory — Unity 4/4 PASS (cite 01-VERIFICATION.md Truth #4)

    Body sections: Goal → Observable Truths (3: Intel chip-id check WIRED + UV-EPROM chip-id branch WIRED + 28C chip-id check WIRED via SAF-05) → Required Artifacts (flash_intel.cpp + eprom.cpp + eeprom_28c.cpp + Unity suite) → Key Link Verification (each handler's chip_id branch) → Data-Flow Trace (chip_id from DB → wire JSON → handle->chip_id → handler-specific check; INCLUDE) → Behavioral Spot-Checks (cite 01-VERIFICATION.md Truth #4 native 4/4 PASS) → Requirements Coverage (REQ-SAF-02 SATISFIED across 3 families) → Anti-Patterns Found (pre-existing only) → **Cross-Milestone Closure — REQ-SAF-02 (the SAF-05 subsection above)** → Gaps Summary (WARNING-5 deferred to v1.2).

    Body length 120-160 lines (heavy content like 05 — SAF-05 closure narrative is detailed).

    Commit: `docs(03-02): retroactive verification — Phase 07 (REQ-SAF-02 + SAF-05 closure; WARNING-5 follow_up)`.
  </action>

  <verify>
    <automated>test -f .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && head -1 .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md | grep -q '^---' && grep -q '^phase: 07-chip-id-safety' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && grep -q '^status: passed' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && grep -q 'REQ-SAF-02' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && grep -q 'eeprom28c_check_chip_id' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && grep -q 'SAF-05' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && grep -q 'Cross-Milestone Closure' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md && grep -q 'WARNING-5' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at exact path `.planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md`.
    - Frontmatter opens/closes with `---`.
    - Frontmatter `phase: 07-chip-id-safety`.
    - Frontmatter `requirements_verified: [REQ-SAF-02]` exactly.
    - Frontmatter `status: passed`, `overrides_applied: 0`.
    - Frontmatter `follow_ups:` contains an entry with `source` naming `WARNING-5`, `in_scope: false`, and `note:` mentioning v1.2 deferral.
    - Body contains a `Cross-Milestone Closure` heading (SAF-05 closure narrative).
    - Body cites `eeprom_28c.cpp:55-77` (helper) AND `eeprom_28c.cpp:78-79` (call-site) AND mentions `SAF-05` AND cross-links `01-VERIFICATION.md` Truth #2 or Truth #4.
    - Body mentions Unity test asset (`test_eeprom28c_chip_id` or "4/4 PASS").
    - Body cites at least two of the three handler-family chip_id sites (Intel `flash_intel.cpp:87` or `:152`, UV-EPROM `eprom.cpp:250`, 28C `eeprom_28c.cpp:55-77`).
    - Body notes the STATE.md "D-05 override (SAF-05, load-bearing)" decision (A9-12V, NOT JEDEC AA/55/90).
    - Every `file:line` citation resolves via `grep -n` at write time.
    - Body length 120-160 lines.
    - Footer present.
    - Commit message matches `docs(03-02): retroactive verification — Phase 07 (REQ-SAF-02 + SAF-05 closure; WARNING-5)` or close variant.
  </acceptance_criteria>

  <done>07-VERIFICATION.md committed; REQ-SAF-02 SATISFIED across Intel + UV-EPROM + 28C families; SAF-05 closure recorded; WARNING-5 carried as follow_up.</done>
</task>

<task type="auto">
  <name>Task 5: Write 10-VERIFICATION.md (Phase 10 — Static Pins; REQ-FW-05 + REQ-FW-06; SC#4 static_high_mask + pins<32 LOCK)</name>
  <files>.planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md</files>

  <read_first>
    - .planning/milestones/v1.0-phases/12-close-gap-blocker-1-algorithm-based-dispatch-for-protocols-0/12-VERIFICATION.md — dense reference 8/8 template
    - .planning/milestones/v1.0-phases/11-build-db-cleanup/11-VERIFICATION.md — template + follow_ups schema (INFO-3 carried as follow_up needs the same shape)
    - .planning/phases/01-safety-closure-intel-flash-vpp-28c-chip-id/01-VERIFICATION.md — v1.1 form
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md `gaps.requirements` entries for REQ-FW-05 (PARTIAL — static-high-mask end-to-end WIRED; INFO-3 = only DIP24 populated) and REQ-FW-06 (PARTIAL — dead READ_WRITE==WRITE_FLAG removed, replaced with `pins < 32` + VPE_TO_VPP comment)
    - .planning/milestones/v1.0-INTEGRATION-CHECK.md rows 10 + 11 (the 5-hop static_high_mask chain — cite by row number, do NOT duplicate)
    - .planning/milestones/v1.0-phases/10-static-pins/10-01-PLAN.md + 10-01-SUMMARY.md + 10-CONTEXT.md (stray; READ for evidence per CONTEXT.md `<deferred>` + RESEARCH.md Pitfall #6 — has rich background on static_high_mask rationale; do NOT move or delete it)
    - .planning/phases/03-retroactive-verification-phases-01-10/03-RESEARCH.md §"Phase 10 — Static Pins (SC#4 evidence)" + §"SC#4 (static_high_mask end-to-end + pins<32 guard in 10-VERIFICATION.md)" — verbatim 5-hop chain + guard cites with live line numbers
    - Live grep targets: firestarter_app/firestarter/data/pinouts.json, firestarter_app/firestarter/database.py, firestarter/src/json_parser.c, firestarter/src/proms/memory.cpp, firestarter/include/firestarter.h
  </read_first>

  <action>
    Author `.planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md`. This is the second SC-lock file in this plan (SC#4). Frontmatter `phase: 10-static-pins`, `requirements_verified: [REQ-FW-05, REQ-FW-06]`, `status: passed`, `score: N/N must-haves verified` (likely 2 Truths: `static_high_mask` 5-hop chain WIRED + `pins < 32` VPE_TO_VPP guard intact), `overrides_applied: 0`.

    Required `follow_ups` frontmatter entry for INFO-3 (per CONTEXT.md `<specifics>` row 10 + RESEARCH.md §"10-VERIFICATION.md"):
    ```yaml
    follow_ups:
      - source: v1.0-MILESTONE-AUDIT.md INFO-3 / FW-07 (v1.2 candidate)
        item: "pinouts.json static-high-pins coverage is only populated for DIP24_2716 and DIP24_2732 today; DIP28 and DIP32 quirk pins (CE2 / JEDEC-tied NC) are not declared. REQ-FW-05 wire is correct; coverage data is the gap."
        severity: info
        in_scope: false
        note: "Deferred to v1.2 (FW-07 in REQUIREMENTS.md Future Requirements). Coverage extension, not a wire defect — the static_high_mask plumbing handles any pin populated upstream."
    ```

    **SC#4 lock — required body Observable Truths** (per ROADMAP SC#4 "10-VERIFICATION.md confirms `static_high_mask` end-to-end wiring and the `pins < 32` VPE_TO_VPP guard are intact in the current `memory.cpp`"):

    The body MUST contain the verbatim term `static_high_mask` (acceptance gate string-matches this token, NOT just "static high mask") AND the verbatim phrase `pins < 32` (or `pins<32` with optional whitespace — acceptance gate accepts both via grep `pins *< *32`). Both terms must appear in Observable Truths or Required Artifacts sections (not just citations).

    Required 5-hop `static_high_mask` chain (each hop is a separate cited row in either Data-Flow Trace or Key Link Verification table; cite v1.0-INTEGRATION-CHECK rows 10 + 11 + live grep):
    1. `firestarter_app/firestarter/data/pinouts.json:10` AND `:21` — `"static-high-pins":[24]` for DIP24_2716 + DIP24_2732
    2. `firestarter_app/firestarter/database.py:309-319` — `get_bus_config` `static-high-pins` translation block (via `pin_conversions`); `pin_conversions[24][24] = 13` at `:90`
    3. Wire JSON `"static-high":[13]` (post-translation) — note as the wire-level representation (no fresh file:line; this is the bus-config sub-object value)
    4. `firestarter/src/json_parser.c:426` — `handle->bus_config.static_high_mask |= 1UL << line;` inside `parse_bus_config`
    5. `firestarter/src/proms/memory.cpp:319` — `reorg_address |= config.static_high_mask;` inside `mem_util_remap_address_bus` (function opens at `:226`)
    Plus the field declaration: `firestarter/include/firestarter.h:72` — `bus_config_t::static_high_mask` (wire contract).

    Required `pins < 32` VPE_TO_VPP guard evidence:
    - `firestarter/src/proms/memory.cpp:140` — `if (handle->pins < 32) { mask |= VPE_TO_VPP; }` inside `mem_util_calculate_top_address_register` (function opens at `:137`).
    - `firestarter/src/proms/memory.cpp:141-142` — explanatory comment about VPE_TO_VPP and ADDRESS_LINE_16 sharing the same CONTROL bit; preserving VPE_TO_VPP would corrupt A16 for 32-pin (512KB) chips.
    - Dead `READ_WRITE == WRITE_FLAG` check: confirmed ABSENT. Body MUST note this explicitly (the verification gate that prompted REQ-FW-06) — cite via `grep -n "READ_WRITE == WRITE_FLAG" firestarter/src/proms/memory.cpp` returning no matches.

    Body sections: Goal → Observable Truths (Truth #1: `static_high_mask` 5-hop chain WIRED end-to-end; Truth #2: `pins < 32` VPE_TO_VPP guard intact + dead READ_WRITE check absent) → Required Artifacts (pinouts.json + database.py + json_parser.c + memory.cpp + firestarter.h) → Key Link Verification (5-hop chain in a 5-row table) → Data-Flow Trace (Level 4 — `static-high-pins` value flows from DB through bus-config translation, wire JSON, parse_bus_config, into `mem_util_remap_address_bus`; INCLUDE — this is the SC#4 lock data path) → Behavioral Spot-Checks (cite `02-VERIFICATION.md` SC4 743/743 dispatch — DIP24 pinout-bearing chips parsed end-to-end without regression) → Requirements Coverage (REQ-FW-05 SATISFIED with 5-hop chain row; REQ-FW-06 SATISFIED with guard + dead-check-absent row) → Anti-Patterns Found (pre-existing only) → Gaps Summary (INFO-3 deferred to v1.2 / FW-07).

    Body length 130-160 lines (highest content density along with 05 — SC#4 chain is intentionally explicit).

    Commit: `docs(03-02): retroactive verification — Phase 10 (REQ-FW-05, REQ-FW-06) — SC#4 static_high_mask + pins<32 lock`.
  </action>

  <verify>
    <automated>test -f .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && head -1 .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md | grep -q '^---' && grep -q '^phase: 10-static-pins' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q '^status: passed' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'REQ-FW-05' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'REQ-FW-06' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'static_high_mask' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -qE 'pins *< *32' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'pinouts.json' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'get_bus_config' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'parse_bus_config' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'mem_util_remap_address_bus' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'mem_util_calculate_top_address_register' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && grep -q 'INFO-3\|FW-07' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at exact path `.planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md`.
    - Frontmatter opens/closes with `---`.
    - Frontmatter `phase: 10-static-pins`.
    - Frontmatter `requirements_verified: [REQ-FW-05, REQ-FW-06]` exactly.
    - Frontmatter `status: passed`, `overrides_applied: 0`.
    - Frontmatter `follow_ups:` contains an INFO-3 / FW-07 entry with `severity: info`, `in_scope: false`, `note:` deferring to v1.2.
    - **SC#4 lock — body contains the verbatim token `static_high_mask`** (grep gate string-matches).
    - **SC#4 lock — body contains the verbatim phrase `pins < 32`** (with optional whitespace — grep regex `pins *< *32` accepts both `pins < 32` and `pins<32`).
    - Body cites ALL FIVE hops of the static_high_mask chain by name/path:
      a) `pinouts.json` (with `:10` or `:21` line cite for `static-high-pins`)
      b) `database.py:get_bus_config` (e.g., `:309-319` or named function)
      c) `json_parser.c:parse_bus_config` (with `:239` line cite for the OR)
      d) `memory.cpp:mem_util_remap_address_bus` (with `:247` line cite for the mask application)
      e) `memory.cpp:mem_util_calculate_top_address_register` (with `:140` line cite for the `pins < 32` guard)
    - Body explicitly notes the dead `READ_WRITE == WRITE_FLAG` check is ABSENT in current memory.cpp.
    - Body cross-links v1.0-INTEGRATION-CHECK rows 10 + 11 (cite by row number; do NOT duplicate).
    - Every `file:line` citation resolves via `grep -n` at write time.
    - Body length 130-160 lines (do NOT trim below 130 lines — SC#4 chain is mandatory and explicit).
    - Footer present.
    - Commit message matches `docs(03-02): retroactive verification — Phase 10 (REQ-FW-05, REQ-FW-06) — SC#4 static_high_mask + pins<32 lock` or close variant naming SC#4.
  </acceptance_criteria>

  <done>10-VERIFICATION.md committed; REQ-FW-05 + REQ-FW-06 SATISFIED; SC#4 static_high_mask 5-hop chain + pins<32 VPE_TO_VPP guard locked with grep-verifiable evidence; INFO-3 carried as follow_up.</done>
</task>

<task type="auto">
  <name>Task 6: Write Plan 03-02 SUMMARY.md (closure record for the closure/hazard batch; Phase 3 fully discharged)</name>
  <files>.planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md</files>

  <read_first>
    - .planning/phases/02-naming-cleanup-wire-key-minipro-references/02-03-SUMMARY.md — most-recent SUMMARY.md form (closure narrative + commit-hash list + follow-ups carry-forward summary + operator-next-steps)
    - .planning/phases/03-retroactive-verification-phases-01-10/03-01-SUMMARY.md — the just-written Plan 03-01 summary (will be cross-referenced for the 5 clean-batch files)
    - .planning/phases/03-retroactive-verification-phases-01-10/03-CONTEXT.md "Commit Protocol" + D-13 (one commit per VERIFICATION.md)
    - The 5 just-written VERIFICATION.md files from Tasks 1-5 of this plan
    - .planning/ROADMAP.md §"Phase 3" — 4 success criteria (SC#1: 10 files exist; SC#2: every missing-VERIFICATION finding resolved or carried; SC#3: 05-VERIFICATION records SAF-04; SC#4: 10-VERIFICATION confirms static_high_mask + pins<32)
  </read_first>

  <action>
    Author `.planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md` summarising what landed in Plan 03-02 AND closing Phase 3 overall. Include:

    1. List of the 5 VERIFICATION.md files written by this plan (03/05/06/07/10) with their REQ-IDs, the closure or hazard each carries, and commit hashes for the 5 atomic commits.
    2. SC#3 confirmation: 05-VERIFICATION.md contains the `Cross-Milestone Closure — REQ-SAF-01` subsection citing flash_intel_check_vpp helper + call-site + Unity 6/6 + 01-VERIFICATION.md cross-link.
    3. SC#4 confirmation: 10-VERIFICATION.md contains the 5-hop `static_high_mask` chain (pinouts.json → get_bus_config → wire → parse_bus_config → mem_util_remap_address_bus) AND the `pins < 32` VPE_TO_VPP guard at memory.cpp:140 AND the absent dead READ_WRITE check.
    4. WARNING-5 follow_ups carried by 03/06/07-VERIFICATION.md (each `in_scope: false`, deferred to v1.2 per MILESTONES.md).
    5. INFO-3 follow_up carried by 10-VERIFICATION.md (deferred to v1.2 as FW-07).
    6. Phase 3 overall closure: 10 VERIFICATION.md files now exist across the v1.0 phases 01..10 directories. Combined with Plan 03-01's 5 files, Phase 3 SC#1 + SC#2 + SC#3 + SC#4 all satisfied.
    7. WARNING-4 from 08-VERIFICATION.md (Plan 03-01) noted as carried for Phase 4 / HW-01.
    8. Confirmation that no source code under `firestarter/` or `firestarter_app/` was modified across the 10-file batch.
    9. Operator next step: "Run `/gsd-verify-work 03` to confirm the 10 VERIFICATION.md files satisfy VERIF-01..VERIF-10 and trigger STATE.md transition to phase_3_complete; then Phase 4 (Hardware Validation) is unblocked." Also note Phase 4 owns WARNING-4 (HW-01) and the deferred-to-v1.2 items remain in MILESTONES.md Known Gaps.

    Commit: `docs(03-02): plan 03-02 summary — closure/hazard batch (5 VERIFICATION.md files, VERIF-03/05/06/07/10; SC#3 + SC#4 locked); Phase 3 discharged`.
  </action>

  <verify>
    <automated>test -f .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q '03-VERIFICATION' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q '05-VERIFICATION' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q '06-VERIFICATION' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q '07-VERIFICATION' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q '10-VERIFICATION' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q 'SC#3\|SC#4' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md && grep -q 'VERIF-03\|VERIF-05\|VERIF-06\|VERIF-07\|VERIF-10' .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists at exact path `.planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md`.
    - Lists all 5 VERIFICATION.md files written by Plan 03-02 with their REQ-ID coverage.
    - References all 5 requirement IDs from this plan's frontmatter `requirements` (VERIF-03, VERIF-05, VERIF-06, VERIF-07, VERIF-10).
    - Explicitly confirms SC#3 (05-VERIFICATION.md SAF-04 closure record) AND SC#4 (10-VERIFICATION.md static_high_mask + pins<32 lock) — both terms named.
    - Lists WARNING-5 follow_ups in 03/06/07-VERIFICATION.md as carried (in_scope: false, deferred to v1.2).
    - Lists INFO-3 follow_up in 10-VERIFICATION.md as carried (deferred to v1.2 as FW-07).
    - Notes Phase 3 overall closure: 10 VERIFICATION.md files exist; SC#1 + SC#2 + SC#3 + SC#4 all satisfied.
    - Confirms no source code under `firestarter/` or `firestarter_app/` was modified.
    - Provides operator-next-step pointer to `/gsd-verify-work 03` and Phase 4 (Hardware Validation).
    - Commit message matches `docs(03-02): plan 03-02 summary — closure/hazard batch ... Phase 3 discharged` or close variant.
  </acceptance_criteria>

  <done>03-02-SUMMARY.md committed; Phase 3 fully discharged with SC#1 + SC#2 + SC#3 + SC#4 satisfied.</done>
</task>

</tasks>

<verification>
After all 6 tasks complete, run:

```bash
# All 5 VERIFICATION.md files exist
for f in 03-eprom-algorithms/03 05-intel-flash/05 06-eeprom-page-write/06 07-chip-id-safety/07 10-static-pins/10; do
  test -f ".planning/milestones/v1.0-phases/${f}-VERIFICATION.md" || echo "MISSING: $f"
done

# Plan summary exists
test -f .planning/phases/03-retroactive-verification-phases-01-10/03-02-SUMMARY.md || echo "MISSING: 03-02-SUMMARY.md"

# SC#3 lock present in 05-VERIFICATION.md
grep -q 'Cross-Milestone Closure — REQ-SAF-01\|Cross-Milestone Closure -- REQ-SAF-01' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && echo "SC#3 string-match OK" || echo "SC#3 string-match FAIL"
grep -q 'flash_intel_check_vpp' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md && echo "SC#3 helper cite OK" || echo "SC#3 helper cite FAIL"

# SC#4 lock present in 10-VERIFICATION.md
grep -q 'static_high_mask' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && echo "SC#4 mask token OK" || echo "SC#4 mask token FAIL"
grep -qE 'pins *< *32' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md && echo "SC#4 guard OK" || echo "SC#4 guard FAIL"
for name in pinouts.json get_bus_config parse_bus_config mem_util_remap_address_bus mem_util_calculate_top_address_register; do
  grep -q "$name" .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md || echo "SC#4 chain hop MISSING: $name"
done

# WARNING-5 follow_ups in 03/06/07-VERIFICATION.md
for f in 03-eprom-algorithms/03 06-eeprom-page-write/06 07-chip-id-safety/07; do
  grep -A 5 'follow_ups:' ".planning/milestones/v1.0-phases/${f}-VERIFICATION.md" | grep -q 'WARNING-5' || echo "WARNING-5 missing in $f"
  grep -A 6 'follow_ups:' ".planning/milestones/v1.0-phases/${f}-VERIFICATION.md" | grep -q 'in_scope: false' || echo "in_scope: false missing in $f"
done

# INFO-3 follow_up in 10-VERIFICATION.md
grep -A 6 'follow_ups:' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md | grep -qE 'INFO-3|FW-07' || echo "INFO-3/FW-07 missing in 10"

# requirements_verified subsets correctly assigned (no fabricated REQs)
grep -A 4 'requirements_verified:' .planning/milestones/v1.0-phases/03-eprom-algorithms/03-VERIFICATION.md | grep -E 'REQ-FW-01|REQ-SAF-01' | wc -l   # expect 2
grep -A 4 'requirements_verified:' .planning/milestones/v1.0-phases/05-intel-flash/05-VERIFICATION.md | grep -E 'REQ-FW-02|REQ-SAF-01' | wc -l       # expect 2
grep -A 3 'requirements_verified:' .planning/milestones/v1.0-phases/06-eeprom-page-write/06-VERIFICATION.md | grep 'REQ-FW-03'                       # expect 1 match
grep -A 3 'requirements_verified:' .planning/milestones/v1.0-phases/07-chip-id-safety/07-VERIFICATION.md | grep 'REQ-SAF-02'                         # expect 1 match
grep -A 4 'requirements_verified:' .planning/milestones/v1.0-phases/10-static-pins/10-VERIFICATION.md | grep -E 'REQ-FW-05|REQ-FW-06' | wc -l         # expect 2

# No source code modified — only .planning/ paths in this plan's commits
git log --since="1 day ago" --name-only --pretty=format: | grep -v '^$' | grep -v '^\.planning/' | head -1   # expect empty
```
</verification>

<success_criteria>
- 5 VERIFICATION.md files exist at the exact paths listed in `files_modified`.
- Each frontmatter has `status: passed`, ISO8601 `verified:` timestamp, `requirements_verified` subset of audit's `gaps.requirements` for that phase, `overrides_applied: 0`.
- 05-VERIFICATION.md contains the verbatim phrase `Cross-Milestone Closure — REQ-SAF-01` (SC#3 lock) AND cites `flash_intel_check_vpp` + `flash_intel.cpp:25-50` + `:77` + Unity 6/6 + 01-VERIFICATION.md cross-link.
- 10-VERIFICATION.md contains the verbatim token `static_high_mask` AND the verbatim phrase `pins < 32` (or `pins<32`) AND cites all 5 hops (pinouts.json + get_bus_config + parse_bus_config + mem_util_remap_address_bus + mem_util_calculate_top_address_register).
- 03/06/07-VERIFICATION.md each carry a WARNING-5 `follow_ups` entry with `in_scope: false` and v1.2 deferral note.
- 10-VERIFICATION.md carries an INFO-3 / FW-07 `follow_ups` entry with `in_scope: false` and v1.2 deferral note.
- 07-VERIFICATION.md contains a `Cross-Milestone Closure` heading citing SAF-05 + `eeprom_28c.cpp:55-77` + `:82-83` + Unity 4/4 + 01-VERIFICATION.md cross-link.
- Every `file:line` citation resolves via `grep -n` against the live tree at write time.
- One atomic commit per VERIFICATION.md plus one commit for 03-02-SUMMARY.md (6 commits total).
- `files_modified` lists ONLY paths under `.planning/` (no firestarter/ or firestarter_app/ paths).
- After this plan completes, all 10 v1.0-phase VERIFICATION.md files exist; Phase 3 SC#1 + SC#2 + SC#3 + SC#4 all satisfied.
</success_criteria>

<output>
After completion, this plan produces 5 VERIFICATION.md files and 1 SUMMARY.md, all under `.planning/`. Combined with Plan 03-01's 5 files + 1 SUMMARY.md, the 10-file Phase 3 batch is fully discharged. Operator next step: `/gsd-verify-work 03` to confirm VERIF-01..VERIF-10 coverage and trigger STATE.md transition to `phase_3_complete`; then Phase 4 (Hardware Validation) is unblocked.
</output>
