---
phase: 194-real-page-size-reaches-the-firmware
plan: 07
type: execute
wave: 4
depends_on: [194-06]
files_modified:
  - .planning/v1.39/194-w29c020-bench-transcript.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PAGE-03]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-11: a contiguous multi-page write to W29C020 -- a part from the correct 18, and the part both reported defects were reproduced on -- reads back byte-identical on real silicon with this phase's firmware and host."
    - "The silicon run proves no regression on the 18 parts whose derived page size was already correct. It does not and cannot prove any of the 9 previously under-sized parts were fixed."
    - "D-10: PAGE-03 cannot be met from current bench inventory, because every protocol 0x05 part with bench history on this rig sits in the correct 18. A W29C512 is ordered and is one of the 9."
    - "The seated part is identified by chip-ID, never by its marking, because three distinct parts share a confusable 512 designation across three different programming algorithms and two different high-voltage rails."
    - "PAGE-03 stays open after this plan, with its status note naming what was exercised on hardware and what rests on the database comparison."
  artifacts:
    - path: .planning/v1.39/194-w29c020-bench-transcript.md
      provides: "the committed no-regression silicon transcript, with the measured rig identity, the firmware and host versions, and the read-back comparison result"
      contains: "W29C020"
  key_links:
    - from: "the phase firmware built in plan 194-02"
      to: "the board under test"
      via: "a flash of this phase's build, with the resulting version recorded in the transcript so the run is attributable to it"
    - from: "the transcript"
      to: "PAGE-03's status note in .planning/REQUIREMENTS.md"
      via: "the note cites the transcript for the no-regression half and the 27-row record for the evidence-class split"
  prohibitions:
    - "Must NOT claim the silicon run closes PAGE-03. The part exercised is from the correct 18, so the run proves the fix did not regress the parts that already worked. It proves nothing about the 9, and presenting it otherwise would be the exact conflation the phase's success criterion 4 forbids."
    - "Must NOT identify the seated part by its printed marking. Three distinct parts share a confusable 512 designation, across three programming algorithms and two different high-voltage rails, and seating the wrong one against the wrong rail destroys it. Confirm by chip-ID."
    - "Must NOT assume which shield revision is mounted. The reported hardware revision cannot distinguish the three boards the operator owns, so the revision is asked, never inferred."
    - "Must NOT run any write with the blank-check-skip flag, because that flag also skips the erase on this path and corrupts a non-blank chip while still reporting a successful result."
    - "Must NOT run an unattended monitoring loop against the board, and must NOT ask the operator to adjust a voltage rail. Nothing in this plan requires a rail change."
    - "Must NOT mark PAGE-03 Complete. Its hardware leg is held OPEN per D-11 until the ordered part arrives."
    - "Must NOT edit any source file in either sub-repo. This plan runs hardware and records the result."
    - "Must NOT run any gsd state-sync verb against STATE.md. Four such verbs corrupt its frontmatter, and the requirements note in this plan is a hand edit."
    - "Must NOT push any branch."
---

<objective>
Run the no-regression write on real silicon, record it honestly, and leave PAGE-03 open with the
reason.

Purpose: D-4 of the milestone makes bench validation required rather than optional, and D-11 lands
the software half of this phase together with a silicon run on a part from the correct 18. The part
is `W29C020`, which is the part both reported defects were reproduced on, and whose derived page
size was already right. That makes the run a real no-regression proof and makes it incapable of
proving the 9 were fixed. Both halves of that sentence go into the record, because success criterion
4 requires the two evidence classes to stay apart.

Output: a committed bench transcript with the measured rig identity, the firmware and host versions
under test and the read-back result, plus a status note beside PAGE-03 that cites it without
claiming completion.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-RESEARCH.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-02-SUMMARY.md
@.planning/phases/194-real-page-size-reaches-the-firmware/194-06-SUMMARY.md
@.planning/v1.39/194-page-size-27-row-record.md
</context>

## Spec-less edge-coverage fallback -- no-silent-drop arithmetic

The edge probe surfaced **5** items over PAGE-01/02/03.
**5 surfaced == 3 authored into `must_haves.truths` + 2 surfaced as flagged assumptions.**
The full table is recorded once in `194-01-PLAN.md`. This plan restates the second flagged
assumption, which is its own subject.

**Flagged planner assumption 2, restated (PAGE-03, unclassified).** I assume PAGE-03 cannot be
closed in this phase, on D-10's measured grounds: every protocol `0x05` part with bench history on
this rig sits in the correct 18, so writing to one proves the 18 did not regress and cannot prove
the 9 were fixed. The risk if wrong is that an operator with an unlisted part on hand could close it
sooner. Mitigation: task 1 asks which parts are actually available before the run, so an unlisted
member of the 9 would be discovered rather than assumed absent, and task 3 records the evidence-class
split either way.

**Prohibition probe** -- the kept values and safety items are authored descriptor-less in
`must_haves.prohibitions` above. **Canon-referral breadcrumb:** untrusted-wire-field input
validation is canon ASVS V5 -- covered by `<threat_model>` and `/gsd-secure-phase`, not minted as a
prohibition.

## Artifacts this phase produces

Repeated verbatim in every plan of this phase so a source-grounding pass can exclude newly-created
symbols from drift verification and not flag deleted ones.

**CREATED:**

| Artifact | Repo | Plan |
|---|---|---|
| `MSG_ERR_FL4_PAGE_SIZE` = `0xBF`, severity ERROR, one `u16` param, `wire_format` `id_frame` | meta catalog, generated into fw + app | 01 |
| `FLASH_5V_PAGE_SIZE_MAX` (512) -- a `#define` local to `src/proms/flash_5v_page.cpp` | fw | 01 |
| `flash_5v_page_mask(uint16_t requested, uint32_t* out_mask)` -> `bool` -- the validator, out-param plus success return | fw | 01 |
| `bus_recording_saturated()` and an `#ifndef`-guarded `HOST_STUBS_MAX_RECORDING` (default 4096) | fw | 01 |
| `test_5v_page_write_execute_page_starts_at_real_page_not_derived` -- the tracer boundary case, (65536, 128) | fw | 01 |
| the 7-distinct-pair boundary cases, the deliberately-wrong-`mem_size` case, and the four refusal mismatch cases plus their positive control | fw | 02 |
| the 27-row `algorithm: 5` host table leg, both halves (real page for all 27, the old derivation's value for the 18) | app | 03 |
| `tests/golden/wire_dict_expected_deltas_194.json` -- 25 entries, indices generated programmatically | app | 04 |
| `PageSizeUnavailableError` (subclass of `EpromOperationError`) | app | 05 |
| `firestarter/page_size_gate.py` -- `requires_page_size()` predicate and `require_page_size()` raiser | app | 05 |
| `tests/test_page_size_write_refusal.py` | app | 05 |
| `.planning/v1.39/194-page-size-27-row-record.md` | meta | 06 |
| two pending todos -- the new-host/old-firmware skew (D-12) and the ERROR band exhaustion (U2) | meta | 06 |
| `.planning/v1.39/194-w29c020-bench-transcript.md` | meta | 07 |

**DELETED:**

| Artifact | Repo | Plan |
|---|---|---|
| `flash_5v_page_page_size()` and its block comment | fw | 01 |
| `_PAGE_SIZE_BY_PART`, its two entries, its leading comment block and its inline citations | app | 01 |
| the two-arm emit comment's falsified clauses and the curated-dict-naming clause beside `raw_page_size` | app | 01 |
| the falsified single-algorithm clauses in `constants.py`, `database.py` (two blocks), `json_parser.c` and `include/firestarter.h` | app + fw | 06 |

**REGENERATED, never hand-edited:** `firestarter_app/firestarter/data/chip_database.json`,
`firestarter_fw/include/messages.h`, `firestarter_app/firestarter/messages.py`.

**Deliberately unchanged:** `firestarter_fw/src/proms/eeprom_28c.cpp` (reference only),
`firestarter_app/firestarter/database.py` and `constants.py` logic, `firestarter_fw/src/json_parser.c`
logic, `tests/golden/wire_dict_baseline.json`, `tests/golden/wire_dict_expected_deltas_149.json`,
`tests/test_lock_status_class_partition.py`, `tests/test_b15_page_size_corroboration.py`,
`.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md`.

## The bench facts that govern this plan

| Fact | Consequence |
|---|---|
| `W29C020` sits in the correct 18 | The run is a no-regression proof. It cannot prove any of the 9 were fixed |
| Both reported defects were reproduced on this exact part, on a Leonardo with a Rev 2.0-class shield | Running the same part on the same class of rig is what makes a before-and-after comparison meaningful |
| `W29C512` is a third distinct part from two other confusable 512 designations | Identify the seated part by chip-ID. One of the three wants 12 V and another wants 13 V |
| The reported hardware revision cannot distinguish the three shields the operator owns | Ask which shield is mounted. Never infer it |
| Serial device numbers shuffle across a replug | Confirm the controller identity per port before every operation |
| A plain write on this part does erase first, and the blank-check-skip flag also skips the erase | Run a plain write. The skip flag corrupts a non-blank chip and still reports success |
| Chip removal before a firmware sideload applies to the Uno class only | A Leonardo is exempt, so the part may stay seated across the flash |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Seat the part, identify the rig, and report both</name>
  <files>(no file is edited by this task)</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-10 -- the name-collision hazard in full, and the reason the ordered part was chosen
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-11 -- which part this phase runs and why
    - `.planning/v1.39/194-page-size-27-row-record.md` -- the 27-row table, so the operator can see which parts belong to the 9 and which to the 18
  </read_first>
  <action>
This task needs the operator, because seating a chip and identifying a physical board are the two
things the agent cannot do.

Ask for four things, and wait.

1. **Seat a `W29C020` in the socket.** This is the Winbond page-write flash part both reported
   defects were reproduced on, and it belongs to the 18 whose derived page size was already correct.
2. **Say which shield revision is mounted.** The reported hardware revision value cannot distinguish
   the three boards you own, so the answer has to come from you rather than from a probe. The
   reproduction rig for both defects was a Rev 2.0-class shield on a Leonardo.
3. **Say which serial port the board is on**, so the run can confirm the controller identity on that
   exact port before it does anything. Device numbers shuffle across a replug.
4. **Say whether any part from the 9 previously under-sized parts is on hand.** The 9 are the two
   Atmel `AT29BV`/`AT29LV` pairs at 256 and 512, `AT29C020`, `AT29C040`, `AT29C512`, `AT29LV512`,
   `SST29EE512`, the `SST29LE512` pair and the `W29C512` pair. The plan assumes none is available and
   that the ordered `W29C512` has not arrived. If one is on hand, say so, because then PAGE-03's
   hardware leg could close in this phase rather than waiting.

Nothing in this plan needs a voltage-rail change, so no adjustment is requested. A Leonardo is
exempt from the chip-out-before-sideload rule, so the part may stay seated while the phase firmware
is flashed in task 2.

Record all four answers before proceeding.
  </action>
  <instructions>
Everything automatable has already run. Plans 194-01 through 194-06 built and tested the firmware
and the host, regenerated the database, and committed the 27-row record. Nothing further can be
automated until a physical part is in the socket and the board is described.

Four things need you, and only you can supply them:

1. Seat a **W29C020** in the socket. This is the Winbond page-write flash part both reported defects
   were reproduced on. Its page size was already correct, which is what makes it the no-regression
   control rather than a proof of the fix.
2. Tell me **which shield revision is mounted**. The reported hardware revision value cannot
   distinguish the three boards you own, so I must not guess it.
3. Tell me **which serial port the board is on**. I will confirm the controller identity on that
   exact port before anything else, because device numbers shuffle across a replug.
4. Tell me **whether any part from the 9 previously under-sized parts is on hand**, and whether the
   ordered W29C512 has arrived. The plan assumes neither. If one is available, PAGE-03's hardware
   leg could close sooner than assumed.

No voltage-rail change is needed for this run, so I am not asking you to adjust anything. The part
may stay seated while I flash the phase firmware, because the chip-out-before-sideload rule applies
to the Uno class and this rig is a Leonardo.
  </instructions>
  <verification>After you reply, I confirm the controller identity on the port you name, read the chip-ID and compare it against the W29C020 row's expected value in the database, and stop before writing if the identity does not match. I do not identify the part by its marking.</verification>
  <resume-signal>Reply with the four answers: the seated part, the mounted shield revision, the serial port, and whether any part from the 9 is on hand. Say "none of the 9" if that is the answer.</resume-signal>
  <acceptance_criteria>
    - The operator has confirmed a `W29C020` is seated.
    - The mounted shield revision is recorded as an operator statement rather than as a probe result.
    - The serial port is recorded, and task 2 will confirm the controller identity on that port before any operation.
    - The availability answer for the 9 is recorded, including the case where none is available.
    - No operation has been run against the board yet.
  </acceptance_criteria>
  <verify>
    <human-check>The operator has stated: the seated part, the mounted shield revision, the serial port, and whether any part from the 9 is on hand.</human-check>
  </verify>
  <done>The rig is described by the operator rather than inferred. The seated part, the shield revision, the port and the availability of any part from the 9 are all recorded, so the transcript in task 2 can attribute its result to a known configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Flash this phase's firmware, confirm the part by chip-ID, then write and read back</name>
  <precondition>The operator has confirmed in task 1 which port the board is on and which shield revision is mounted, and has seated a `W29C020`. The port identity is confirmed on that exact port before any write, because serial device numbers shuffle across a replug.</precondition>
  <files>(no tracked file is edited by this task -- it drives hardware and captures output for task 3)</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-02-SUMMARY.md` -- the firmware build figures and the version this phase produces, so the flash is attributable
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-06-SUMMARY.md` -- confirmation that the comment-clause deletions did not move either build
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-10's name-collision warning and D-11's scope
    - `firestarter_app/firestarter/data/chip_database.json` -- the `W29C020` row, for its expected chip-ID, capacity and page size
    - `firestarter_app` command help for the write, read and firmware subcommands, so every flag used is one that exists
  </read_first>
  <action>
Drive the rig. Capture every command and its full output, because task 3 commits the transcript and
every figure in it must carry the command that produced it.

STEP 1 -- confirm the port identity on the exact port the operator named. Report the controller the
board identifies as, and stop if it is not the expected one. A wrong port means the rest of the run
describes a different board.

STEP 2 -- flash this phase's firmware to the attached board, and record the version the board
reports afterwards. The standing rig permission covers flashing these boards freely, so no
per-flash approval is needed. The part may stay seated, because the chip-out rule before a sideload
applies to the Uno class only and this rig is a Leonardo. Record the host version too, so the
transcript names both halves of the pair under test.

STEP 3 -- confirm the seated part by **chip-ID**, never by its marking. Report the read ID and
compare it against the database row's expected value. Three distinct parts carry a confusable 512
designation across three different programming algorithms, and two of them want different
high-voltage rails, so a marking-based identification risks destroying a part. If the ID does not
match `W29C020`, stop and report rather than writing.

STEP 4 -- the write. Generate a deterministic test pattern that spans several physical pages at the
part's real page size of 128 bytes, and that would expose a page boundary landing in the wrong
place. A pattern whose value varies within each 128-byte page, and differs between adjacent pages,
is what makes a mis-placed boundary visible in the comparison rather than invisible. Record the
pattern's generation rule and its size.

Run a plain write. Do not pass the blank-check-skip flag: on this path that flag also skips the
erase, which corrupts a non-blank chip while still reporting a successful result. Record the full
command, the elapsed time and the reported outcome.

STEP 5 -- the read-back comparison. Read the device and compare it byte-for-byte against the written
pattern. Record whether the comparison is byte-identical, and if it is not, record every mismatched
range rather than only the first. A mis-placed page boundary shows up as a run of erased bytes at a
regular interval, and the interval is the diagnostic.

STEP 6 -- one negative control, if it can be run without risk to the part. The refusal path is
already proven natively in plan 02 over four rejected classes, so a silicon repeat is optional. If a
supported route exists to drive a protocol `0x05` write against a chip carrying no page size, record
the refusal message the operator sees. If no such route exists without editing the shipped database,
skip it and say so, because the host guard in plan 05 already refuses before any serial byte and the
database no longer contains such a row.

STEP 7 -- hand every captured command and output to task 3. Do not summarise a figure away.
  </action>
  <acceptance_criteria>
    - The controller identity was confirmed on the operator-named port before any other operation, and the reported identity is recorded.
    - This phase's firmware is on the board, and the version the board reports is recorded alongside the host version.
    - The seated part was confirmed by chip-ID, and the read ID matches the `W29C020` row's expected value.
    - The write was run without the blank-check-skip flag, and the full command and its reported outcome are recorded.
    - The test pattern spans several 128-byte physical pages, varies within each page and differs between adjacent pages, and its generation rule is recorded.
    - The read-back comparison result is recorded as byte-identical or with every mismatched range named.
    - The negative control is either recorded with its refusal message or explicitly recorded as skipped with the reason.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "
import json, pathlib
db = json.loads(pathlib.Path('firestarter/data/chip_database.json').read_text(encoding='utf-8'))
row = [c for c in db['WINBOND'] if c['part_number'].startswith('W29C020')][0]
p = row['programming']
assert p['algorithm'] == 5, p['algorithm']
assert p['page_size'] == 128, p['page_size']
assert row['electrical']['size_bytes'] == 262144, row['electrical']['size_bytes']
print('BENCH-TARGET-OK part=' + row['part_number'] + ' page=128 size=262144 chip_id=' + str(p.get('chip_id_value')))
"</automated>
    <fails_when>`BENCH-TARGET-OK` is absent from the output. A raised assertion prints the offending value, which means the database row this run is aimed at is not the expected protocol, page size or capacity</fails_when>
    <human-check>The transcript shows the confirmed controller identity, the firmware and host versions, the read chip-ID matching the expected value, the plain write command with no blank-check-skip flag, and a byte-identical read-back comparison.</human-check>
  </verify>
  <done>This phase's firmware and host wrote a multi-page pattern to a `W29C020` identified by chip-ID on an operator-described rig, and the device read back byte-identical. Every figure is captured with the command that produced it, ready to be committed.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the transcript and record PAGE-03's status without closing it</name>
  <precondition>Task 2's captured commands and outputs are available, because every figure in the transcript must carry the command that produced it.</precondition>
  <files>.planning/v1.39/194-w29c020-bench-transcript.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/194-real-page-size-reaches-the-firmware/194-CONTEXT.md` D-11 -- the hardware-gated-deferral shape, and the statement that landing the software half does not license marking PAGE-03 Complete
    - `.planning/v1.39/194-page-size-27-row-record.md` -- the evidence-class split this transcript pairs with
    - `.planning/v1.33/sweep-outcome-record.md` -- the record convention, including the `pairs_with` frontmatter key that links two halves of one measurement
    - `.planning/REQUIREMENTS.md` -- the PAGE-03 entry and the coverage table, as plan 06 left them
  </read_first>
  <action>
Two artifacts: the transcript, and a status note that does not overstate it.

STEP 1 -- write `.planning/v1.39/194-w29c020-bench-transcript.md`. Frontmatter with a title, the
phase, the plan, the measured date, a status line, and a `pairs_with` key naming the 27-row record,
because the two are halves of one measurement. Body sections: the rig as the operator described it,
the firmware and host versions under test, the confirmed controller identity and port, the read
chip-ID against its expected value, the pattern generation rule and size, the write command and its
outcome, the read-back comparison result, and the negative control with its result or its skip
reason.

Every figure carries the verbatim command that produced it. Do not restate a number without its
command, and do not carry a number forward from research or from another plan without saying where
it came from.

STEP 2 -- state the scope of the claim, in the transcript, in its own section. This run used a part
from the correct 18. It proves the change did not regress the parts whose derived page size was
already right. It proves nothing about the 9 previously under-sized parts, because none was on the
rig. Write both sentences. A reader who takes this transcript as evidence for the 9 has been misled
by the transcript rather than by their own reading.

STEP 3 -- the PAGE-03 status note in `.planning/REQUIREMENTS.md`. Plan 06 left PAGE-03 unmarked with
a note. Extend that note to cite this transcript for the no-regression half, and keep the
evidence-class split exactly as the record states it: none of the 9 exercised on hardware, all 9
resting on the database comparison. Leave the checkbox open.

If task 1 discovered that a part from the 9 **is** on hand, do not improvise. Record the discovery
in the SUMMARY, leave PAGE-03 open, and state that the hardware leg is now runnable sooner than D-10
assumed. Closing PAGE-03 needs a run on one of the 9, and that run is a scope decision for the
operator rather than an extension of this plan.

Make the requirements edit by hand. Do not run a state-sync verb.

STEP 4 -- commit in the meta repo, one commit covering the transcript and the requirements note.
Snapshot the roadmap before and after, and diff the two, because a completion verb in this project
has been observed to overwrite an unrelated phase's plan line. Restore anything that moved outside
this phase's own section.
  </action>
  <acceptance_criteria>
    - `.planning/v1.39/194-w29c020-bench-transcript.md` exists, is tracked, and its frontmatter carries a `pairs_with` key naming the 27-row record.
    - The transcript records the rig as an operator statement, the firmware and host versions, the confirmed controller identity, the read chip-ID against its expected value, the pattern rule, the write command, the read-back result and the negative control's outcome or skip reason.
    - The transcript contains a section stating that the run proves no regression on the 18 and proves nothing about the 9.
    - `.planning/REQUIREMENTS.md` still leaves PAGE-03 unmarked, and its note cites both the transcript and the 27-row record.
    - The PAGE-03 note states that none of the 9 was exercised on hardware and that all 9 rest on the database comparison.
    - PAGE-01 and PAGE-02 remain marked Complete from plan 06, and no other requirement's state changed.
    - `.planning/ROADMAP.md` is unchanged outside this phase's own section.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && git ls-files --error-unmatch .planning/v1.39/194-w29c020-bench-transcript.md >/dev/null && /usr/bin/grep -q 'pairs_with' .planning/v1.39/194-w29c020-bench-transcript.md && /usr/bin/grep -q '194-page-size-27-row-record' .planning/v1.39/194-w29c020-bench-transcript.md && /usr/bin/grep -qi 'W29C020' .planning/v1.39/194-w29c020-bench-transcript.md && echo TRANSCRIPT-OK</automated>
    <fails_when>`TRANSCRIPT-OK` is absent from the output. The transcript is missing or untracked, or it does not pair itself with the 27-row record, or it does not name the part under test</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -A3 'PAGE-03' .planning/REQUIREMENTS.md | /usr/bin/grep -q '\[ \]' && /usr/bin/grep -q '\[x\] \*\*PAGE-01' .planning/REQUIREMENTS.md && /usr/bin/grep -q '\[x\] \*\*PAGE-02' .planning/REQUIREMENTS.md && echo REQ-HONEST</automated>
    <fails_when>`REQ-HONEST` is absent from the output. PAGE-03 was marked complete on a run that used a part from the 18, or PAGE-01 or PAGE-02 lost the state plan 06 set</fails_when>
    <automated>cd /workspaces && SHA=$(git log --format=%H -1) && echo "meta commit: $SHA" && NAMES=$(git show --name-only --format= "$SHA") && printf '%s\n' "$NAMES" && ! printf '%s\n' "$NAMES" | /usr/bin/grep -qx '.planning/ROADMAP.md' && echo ROADMAP-UNTOUCHED</automated>
    <fails_when>`ROADMAP-UNTOUCHED` is absent from the output, which means this task's own commit names the roadmap among its changed paths, and this task does not edit the roadmap</fails_when>
    <human-check>The transcript's scope section states plainly that the run proves no regression on the 18 and proves nothing about the 9, and PAGE-03 is still open.</human-check>
  </verify>
  <done>Per D-11 the silicon run is committed as a transcript paired with the 27-row record, and its scope is stated rather than implied. PAGE-03 is left open with a note that names what was exercised on hardware and what rests on the database comparison, so the two evidence classes stay apart as success criterion 4 requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| printed marking to part identity | Three distinct parts share a confusable 512 designation across three programming algorithms and two high-voltage rails. A marking-based identification can destroy a part |
| reported hardware revision to the mounted board | The reported value cannot distinguish the three shields the operator owns, so the revision must be asked |
| serial device number to the board | Device numbers shuffle across a replug, so the port must be re-identified before every run |
| transcript to a future reader | A transcript that does not state its own scope becomes evidence for a claim nobody made |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-43 | Repudiation | a no-regression run presented as proof for the 9 | high | mitigate | The transcript carries a scope section stating both what the run proves and what it does not, and PAGE-03 stays open with the evidence-class split restated |
| T-194-44 | Tampering | the wrong part seated against the wrong rail | high | mitigate | The seated part is confirmed by chip-ID against the database row's expected value, and the run stops before writing when the ID does not match |
| T-194-45 | Tampering | a write that skips the erase and still reports success | high | mitigate | The blank-check-skip flag is prohibited on this path, because it also skips the erase and corrupts a non-blank chip while reporting a successful result |
| T-194-46 | Tampering | an operation aimed at the wrong board | medium | mitigate | The controller identity is confirmed on the operator-named port before any other operation, and the run stops when it is not the expected one |
| T-194-47 | Repudiation | a read-back reporting only the first mismatch | medium | mitigate | Every mismatched range is recorded, because a mis-placed page boundary appears as erased runs at a regular interval and the interval is the diagnostic |
| T-194-48 | Repudiation | a figure carried forward without its command | medium | mitigate | The transcript copies the milestone-record convention that every number carries the verbatim command that produced it |
| T-194-41 | Tampering | a planning verb clobbering unrelated roadmap content | medium | mitigate | The requirements note is a hand edit, state-sync verbs are prohibited, and a verify leg asserts the commit does not name the roadmap |
| T-194-09 | Tampering | npm, pip or cargo installs | high | accept | This plan installs zero packages and changes no dependency manifest. RESEARCH records no package to audit, so no legitimacy checkpoint is required |
| T-194-49 | Denial of service | the ordered part has not arrived | medium | accept | Accepted and planned for. D-10 measured that no part from the 9 has bench history on this rig, and D-11 holds PAGE-03's hardware leg open rather than substituting weaker evidence |
</threat_model>

<verification>
- The operator has stated the seated part, the shield revision, the port and the availability of any part from the 9.
- The controller identity was confirmed on the named port before any other operation.
- The read chip-ID matches the expected value for the part under test.
- The write ran without the blank-check-skip flag, and the read-back comparison is byte-identical.
- `.planning/v1.39/194-w29c020-bench-transcript.md` is tracked and pairs itself with the 27-row record.
- `.planning/REQUIREMENTS.md` leaves PAGE-03 open, and PAGE-01 and PAGE-02 keep the state plan 06 set.
- `.planning/ROADMAP.md` is unchanged by this plan.
</verification>

<success_criteria>
A contiguous multi-page write to a chip-ID-confirmed `W29C020`, with this phase's firmware and host
on an operator-described rig, reads back byte-identical. The transcript is committed, pairs itself
with the 27-row record, and states plainly that it proves no regression on the 18 and proves nothing
about the 9. PAGE-03 is open, with a note naming the two evidence classes separately. PAGE-01 and
PAGE-02 keep the Complete state plan 06 set.
</success_criteria>

<output>
Create `.planning/phases/194-real-page-size-reaches-the-firmware/194-07-SUMMARY.md` when done.
Record the operator-stated rig configuration, the versions under test, the read-back result, the
negative control's disposition, and whether any part from the 9 turned out to be on hand.
</output>
