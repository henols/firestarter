---
phase: 196-adoption-instrument-disposition
plan: 03
type: execute
wave: 3
depends_on: ["196-02"]
files_modified:
  - .gitignore
  - .planning/graphs/GRAPH_REPORT.md
  - .planning/graphs/.last-build-status.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INSTR-01, INSTR-02]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "One expression over every tracked file, with six named exclusions, prints `INSTR02_GATE: GREEN` — zero residual references. That is D-12's choice of a single grep gate over a hand-enumerated site checklist, and it is the phase's proof that the sweep landed."
    - "The expression is trusted because it has been seen to fail. It printed `INSTR02_GATE: RED (11 residual reference(s))` against the pre-sweep tree, naming four live documents plus the instrument itself — recorded in `196-RESEARCH.md` §R2.3, independently reproduced by the orchestrator at planning time, and reproduced once more by this plan against the pre-phase commit. That is D-13: a gate only ever seen green cannot distinguish swept from matches-nothing."
    - "The expression computes its `v1.38` boundary from the `^## v1\\.38 ` section header and aborts loudly with exit 3 if that header is ever renamed. It never hardcodes a line number, because line numbers drift and section headers do not — and an empty boundary would make its filter true for every line and silently drop every ROADMAP hit, which is a fail-open."
    - "Six exclusion classes, each with its reason recorded: `.planning/STATE.md` (D-08, session records of what was true when written), `.planning/milestones/` (D-09 broadened to the whole archive, historical-by-intent), this phase's own directory (its decision records quote the terms deliberately and are the evidence for the sweep, not a subject of it), the seed (D-14 freezes two path citations on purpose), `.planning/graphs/` (a generated distillation of the whole tree, including the frozen archive), and `.claude/` (installer-owned)."
    - "D-07's measurement was corrected before planning. It named two surviving live references; re-measurement over all 5,207 tracked files found 21 more in this phase's own decision records and two more in archived v1.38 material outside the Phase 193 directory. The decision stands and the count did not — which is why the exclusion set has six classes and not three."
    - "The knowledge graph is rebuilt after the deletion and the sweep, never before, because it reads the working tree and a rebuild run first would faithfully re-encode the stale text. That is D-11, and its ordering is the one genuine constraint on task order in this phase."
    - "D-11's premise that the rebuild clears `GRAPH_REPORT.md` does not hold and the plan does not rely on it. The report carries two references: a file node for the deleted script, which the rebuild removes, and a community node whose label is sourced from a frozen archived file, which the rebuild re-emits. The rebuild is kept because it is still correct and honours this project's rule that generated artifacts are regenerated and never hand-edited — but `.planning/graphs/` is excluded from the proof, so the acceptance criterion is about the file node being gone, not about the gate."
    - "The 3 MB HTML artifact the rebuild also writes — untracked, unignored, and unmentioned by D-11 — is gitignored in this phase rather than avoided by discipline, because avoidance relies on every future commit being careful and an ignore rule does not."
    - "The INSTR-01 and INSTR-02 traceability rows read `Complete` only after the proof is green and the graph is rebuilt, never before. The proof is re-run once more after that edit, because `.planning/REQUIREMENTS.md` is itself a scanned file."
  artifacts:
    - path: ".planning/graphs/GRAPH_REPORT.md"
      provides: "The regenerated knowledge-graph report, no longer carrying a file node for the deleted instrument"
      contains: "Nodes"
    - path: ".gitignore"
      provides: "A permanent ignore rule for the untracked, unignored 3 MB HTML artifact the graph rebuild writes"
      contains: ".planning/graphs/graph.html"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Both INSTR requirements and both traceability rows recorded complete, after their proof"
      contains: "| INSTR-02 | Phase 196 | Complete |"
  key_links:
    - from: "the INSTR-02 proof expression"
      to: ".planning/ROADMAP.md"
      via: "the boundary computed from the `^## v1.38 ` section header, which excludes the closed milestone section by line while keeping the live Phase 196 block in scope (D-10)"
      pattern: "\\^## v1\\\\\\.38 "
    - from: ".planning/graphs/GRAPH_REPORT.md"
      to: ".planning/milestones/v1.38-phases/193-the-deferred-claim-made-measurable/"
      via: "the community node label sourced from a frozen archived heading, which regenerates on every rebuild and is why `.planning/graphs/` is a gate exclusion rather than a sweep site"
      pattern: "Nodes"
  prohibitions:
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT weaken the proof expression to reach green — no dropped term, no widened exclusion, no replaced boundary anchor, no removed fail-closed guard. If the expression is red, the document it names is what changes. An expression edited until it passes proves only that it was edited."
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT commit the proof expression as a standalone script or a document anywhere outside the six excluded paths. Its own term set matches its own text, so a copy in `tools/`, at the repository root, or in `.planning/notes/` would turn the proof red on itself. It lives inside this phase's directory, which is excluded, and it is run from there."
    - requirement_id: INSTR-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT edit `.planning/STATE.md`, any file under `.planning/milestones/`, or `.planning/graphs/GRAPH_REPORT.md` by hand. One archived file is the *source* of a surviving reference in the generated report; editing it to make a check pass is forbidden, and excluding the generated directory is the sanctioned route."
    - requirement_id: INSTR-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT mark either requirement complete before its proof has run. The traceability flip is the last edit of the phase, and the proof is re-run after it."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT use a GSD verb to edit `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`. The requirements and roadmap verbs reformat the whole file, and the plan-progress verb overwrites this repository's phase block positionally. These are hand edits."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT stage with `git add -A`, `git add .`, or `git commit -a`. The graph rebuild writes a 3 MB untracked, unignored HTML artifact and a 105 MB JSON file; a broad stage would commit generated blobs. Every commit stages explicit paths only."
    - requirement_id: INSTR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT run the graph rebuild inside a sub-agent or in the background. Sub-agent isolation has previously truncated a build mid-write, and a truncated generated artifact is worse than a stale one because it looks current."
    - requirement_id: INSTR-01
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT add any comment to source under `firestarter_fw/` or `firestarter_app/`, and MUST NOT make the existence of a comment an acceptance criterion. This plan touches neither sub-repo."
    - requirement_id: INSTR-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT add AI or Claude attribution to any commit, in the subject, the body, or a trailer."
    - requirement_id: INSTR-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT commit to `beta` or to `main` in any repository, MUST NOT create a GitHub Release in the meta repository, and MUST NOT create or push a tag. Milestone work stays on the `v1.39-*` branch."
    - requirement_id: INSTR-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT touch either sub-repo, advance a gitlink, or enter a submodule. `firestarter_app`'s gitlink is dirty before this phase starts and is left exactly as found."
---

<objective>
Prove the sweep landed, regenerate what the deletion made stale, and record the two requirements as
complete — in that order, because each depends on the one before it.

The proof is a single expression, chosen over a hand-enumerated site checklist, and it is trusted only
because it has been seen to fail: red at eleven residual references against the pre-sweep tree, naming
four live documents plus the instrument itself.

This plan implements D-07 (the corrected reference measurement), D-08 and D-09 (the frozen classes the
expression excludes rather than edits), D-11 (the graph is rebuilt in-phase, after the sweep), D-12
(one gate, no hand enumeration) and D-13 (the gate is proven red before it is trusted).

Purpose: the closing half of INSTR-01 and INSTR-02 — the disposition is proved consistent across every
live document, and both requirements are recorded complete against that proof.
Output: a green proof reading, a regenerated knowledge-graph report, a permanent ignore rule for the
rebuild's HTML artifact, and two flipped traceability rows.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/196-adoption-instrument-disposition/196-CONTEXT.md
@.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md
@.planning/phases/196-adoption-instrument-disposition/196-01-SUMMARY.md
@.planning/phases/196-adoption-instrument-disposition/196-02-SUMMARY.md
</context>

<environment_notes>
**Never full-read `.planning/ROADMAP.md` (983 KB), `.planning/PROJECT.md` (452 KB) or
`.planning/STATE.md` (555 KB, one line of roughly 52,000 characters).** `.planning/STATE.md` is not
read and not edited at all in this phase. `.planning/REQUIREMENTS.md` is 7.7 KB and is safe to read
whole.

**`grep` on PATH in this devcontainer is `ugrep`.** It honours `.gitignore` and silently under-scans,
so every command here calls `/usr/bin/grep` by absolute path. `/usr/bin/grep -c` is never a pass
signal. Always pass the pattern with `-e`.

**The proof expression's own exit code is meaningless without the capture.** `xargs` returns 123 when
any `grep` batch exits 1, and 5,207 files span several batches, so the pipeline's status says nothing.
Emptiness of the captured text is the only honest signal, and a positive token is echoed on success so
that a pass is observable rather than inferred from silence.

**The graph rebuild takes about 212 seconds**, not the 15 to 60 seconds the command documentation
claims. Run it in the **foreground**, from `/workspaces`, **never inside a sub-agent and never in the
background** — sub-agent isolation has truncated a build mid-write before. Use a 600000 ms timeout.

**No spontaneous rebuild can race this plan.** The auto-update hook exits 0 unless the current branch
equals the repository default branch, and HEAD is on `v1.39-protocol-0x05-write-correctness`. The
rebuild this plan runs is the only rebuild.

**The rebuild writes four things.** Two are tracked and are committed here by explicit path:
`.planning/graphs/GRAPH_REPORT.md` (1.1 MB) and `.planning/graphs/.last-build-status.json`. Both were
already dirty before this phase started, so their diffs carry pre-existing churn that does not belong
to this phase — do not assert a clean baseline and do not try to make one. Two are not:
`.planning/graphs/graph.json` (105 MB, gitignored) and `graphify-out/` (gitignored). A fifth,
`.planning/graphs/graph.html` (3 MB), is untracked **and not gitignored** — this plan fixes that.

**`.gitignore` already carries an unrelated uncommitted edit** adding a marketplace-installed skill
directory. `git add` is file-granular, so staging `.gitignore` carries that hunk too. That is
accepted deliberately and disclosed in the commit body; see Task 2.

**Any `gsd-tools` call — even a read-only one — can silently prune `.planning/config.json`'s
`planning.sub_repos` list.** This plan runs at least one GSD verb, so the check is not optional: after
every GSD invocation, run `git status --porcelain .planning/config.json` and `git checkout --` the
file if it changed. Never commit that pruning.

**Branch.** HEAD must be on `v1.39-protocol-0x05-write-correctness`. Never commit to `beta` or `main`,
and create no tag and no release. Commit convention: `docs(196): <subject>`.
</environment_notes>

<artifacts_this_phase_produces>
- The token `INSTR02_GATE: GREEN — zero residual references (boundary=N)`, printed when the proof
  expression finds nothing. Its failure forms are `INSTR02_GATE: RED (N residual reference(s))
  boundary=N`, printed with the offending `file:line:text` lines above it, and
  `INSTR02_GATE: ABORT` with exit 3, printed when the `^## v1\.38 ` section header cannot be found.
- One new line in `.gitignore`: `.planning/graphs/graph.html`.
- Check tokens emitted by this plan: `GATE-HAS-TEETH`, `GRAPH-FILENODE-GONE`, `GRAPH-FRESH`,
  `HTML-IGNORED-NOT-TRACKED`, `GRAPH-COMMIT-SCOPE-OK`, `INSTR-ROWS-COMPLETE`,
  `FINAL-COMMIT-SCOPE-OK`.
- **The proof expression itself is not committed as an artifact.** It lives in this file and in
  `196-RESEARCH.md` §R2.1, both inside this phase's directory, which is one of the six paths it
  excludes. Its term set matches its own text, so a copy anywhere outside those six paths would turn
  it red on itself. It does not persist beyond this phase, and that is deliberate.
</artifacts_this_phase_produces>

<flagged_assumptions>
Carried forward. Not resolved, not dismissed, and not to be silently dropped.

- **Probe rows INSTR-01 and INSTR-02 — both `unclassified`, both `unresolved`.** No SPEC exists, so the
  deterministic edge probe ran against the requirement texts under the spec-less fallback and returned
  `category: unclassified`, `status: unresolved`, `verification: null` for both. An `unclassified` row
  is never auto-resolved with a backstop marker, so both are surfaced here rather than silently
  dropped. Practical reading: neither requirement has an enumerable data-shape edge — one is a prose
  record, the other a semantic consistency property — and their acceptance rests on the criteria
  authored across these three plans.
- **The proof expression is a proxy, and its blind spots are known.** It matches identifiers, not
  meaning. A live document could say the instrument that gated the slug claim is still running,
  without naming it, and pass. Six exclusion classes are six holes — the seed exclusion is the
  sharpest, because D-14 requires an annotation there that the expression cannot verify. Untracked
  files are outside it by design. Phrase-level residue is deliberately outside its term set, because
  broad phrases match five archived milestones and would make it permanently red. Each of these is
  covered by a criterion written by hand somewhere in this phase, and none of them is covered twice.
- **Assumption A4 (`196-RESEARCH.md`).** The positional-overwrite hazard in the GSD roadmap
  plan-progress verb was not re-tested during research, because running it would have mutated files.
  Its structural precondition — that the v1.39 milestone section carries no table for a positional
  writer to anchor on — was verified. The mitigation is free: hand-edit.
- **`reference_pypi_per_version_downloads_clickhouse` is a Claude memory outside this repository.**
  It records the method D-06 deletes, it is not a document in this repository, and no task here can
  reach it. Surface it to the operator at phase close as a question, not as work.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The proof — one expression, seen to fail before it is trusted, then green</name>
  <precondition>HEAD is on the milestone branch: `git -C /workspaces rev-parse --abbrev-ref HEAD` prints a name beginning `v1.39-`. If it prints `beta` or `main`, halt.</precondition>
  <read_first>
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md — §R2 in full. §R2.1 is the authoritative text of the expression; §R2.2 tabulates why each mechanic is what it is and which local failure each guards against; §R2.3 is the recorded red proof and its eleven hits by file; §R2.4 is why the generated graph directory is excluded; §R2.5 is the honest list of what the expression does not catch
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-CONTEXT.md — D-07 through D-13
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-01-SUMMARY.md and 196-02-SUMMARY.md — the recorded pre-task SHAs. The pre-phase commit is the parent of Plan 196-01's commit, and Task 1 needs it to reproduce the red reading
  </read_first>
  <files>.planning/phases/196-adoption-instrument-disposition/196-03-SUMMARY.md</files>
  <action>
    Run the proof twice: once against the pre-phase commit, where it must be red, and once against the
    working tree, where it must be green.

    **Use the expression exactly as authored in `196-RESEARCH.md` §R2.1.** Do not retype it from
    memory and do not simplify it. Every mechanic in it is load-bearing and each guards against a
    failure this repository has actually had: the absolute `/usr/bin/grep` because the one on PATH
    honours `.gitignore`; the `-e` before the pattern so a future dash-leading term cannot exit 2 and
    be read as not-found; `-I` to skip binaries and `-d skip` so the two submodule gitlinks do not
    emit directory errors; the capture into a variable tested for emptiness, because the pipeline's
    own exit code is meaningless once `xargs` spans several batches; a single pipeline with no
    semicolon chain and no or-grep, both of which have failed open here; the boundary computed from
    the `^## v1\.38 ` section header rather than a literal line number; the fail-closed guard that
    aborts with exit 3 when that header cannot be found, because an empty boundary makes the filter
    true for every line and would silently drop every hit in the largest file; and the echoed positive
    token, so a pass is observable rather than inferred from silence.

    **Do not weaken it to reach green.** No term dropped, no exclusion widened, no anchor replaced, no
    guard removed. If it comes back red, the document it names is what changes. An expression edited
    until it passes proves only that it was edited.

    **Do not commit it anywhere.** Its own term set matches its own text, so a copy under `tools/`, at
    the repository root, or in `.planning/notes/` would turn it red on itself. It belongs in this
    phase's directory, which is one of the six paths it excludes, and it is run ad hoc from there.
    Record the reading in the summary, not the expression in a script.

    **First run — the red proof (D-13).** A gate only ever seen green cannot distinguish swept from
    matches-nothing, so reproduce the failure on this machine before trusting the pass. Add a detached
    worktree at the pre-phase commit — the parent of Plan 196-01's commit, whose SHA is recorded in
    `196-01-SUMMARY.md` — under the session scratch directory, run the expression there, confirm it
    prints `INSTR02_GATE: RED` and that its printed hit list names all four of `CLAUDE.md`,
    `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, then remove the
    worktree. Do not initialise submodules in it; the expression skips gitlinks and all eleven expected
    hits are in meta-repository files. This reading is expected to match the eleven recorded in
    §R2.3 — four live documents and seven inside the instrument itself — but the assertion is that it
    is red and names those four documents, not that the count is exactly eleven, because the count
    moves if anything else in the repository gains a matching term.

    **Second run — the green reading.** Run the same expression against the working tree from
    `/workspaces`. It must print `INSTR02_GATE: GREEN`. If it prints red, read the file and line it
    names and fix that document; the four expected sites were all swept by Plan 196-02, so a red
    reading here names either a missed site or a new one introduced by this phase — most likely a live
    document that named the retired artifact by path instead of describing it.

    **If it prints `INSTR02_GATE: ABORT` with exit 3**, the `^## v1\.38 ` section header in
    `.planning/ROADMAP.md` has been renamed. Do not work around it and do not substitute a line
    number: find the new header text, confirm it still denotes the closed v1.38 milestone section, and
    report the rename — the anchor is part of the proof and changing it silently changes what was
    proved.

    **Record both readings verbatim in the summary**, each with the command's own printed token, so the
    red-before-green evidence survives the phase rather than living only in this plan.
  </action>
  <acceptance_criteria>
    - The expression run against a detached worktree at the pre-phase commit prints a line containing `INSTR02_GATE: RED`, and its printed hit list contains all four of `CLAUDE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`. This is the D-13 evidence and it is reproduced on this machine, not inherited from the research document.
    - The temporary worktree is removed afterwards: `git worktree list` names no path under the session scratch directory.
    - The expression run against the working tree prints a line containing `INSTR02_GATE: GREEN` and prints no `file:line:text` hit lines.
    - Neither run prints `INSTR02_GATE: ABORT`, and neither exits 3.
    - The boundary the green run reports is a line number greater than 300 — the `^## v1\.38 ` header computed from the anchor, never a hardcoded value.
    - No file outside `.planning/phases/196-adoption-instrument-disposition/` was created or modified by this task. In particular no script carrying the expression exists anywhere: `git status --porcelain tools/ .planning/notes/` prints nothing, and there is no new untracked file at the repository root.
    - `.planning/config.json` is unchanged after every GSD or git invocation in this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && PRE="$(git rev-parse "$PRE_PHASE_SHA")" && WT="${TMPDIR:-/tmp}/instr02-presweep-$$" && git worktree add --detach --no-checkout "$WT" "$PRE" >/dev/null 2>&1 && git -C "$WT" checkout "$PRE" -- . >/dev/null 2>&1; OUT="$(cd "$WT" && GATE_TERMS='tools/adoption|pypi_version_share|[Cc]lick[Hh]ouse|splitByChar|toUInt32OrZero|pypi_raw|pypistats'; RM=.planning/ROADMAP.md; V138="$(/usr/bin/grep -nE '^## v1\.38 ' "$RM" | head -1 | cut -d: -f1)"; if [ -z "$V138" ]; then echo 'INSTR02_GATE: ABORT'; else HITS="$(git ls-files -z -- ':!:.planning/STATE.md' ':!:.planning/milestones' ':!:.planning/phases/196-adoption-instrument-disposition' ':!:.planning/seeds/SEED-claim-firestarter-slug.md' ':!:.planning/graphs' ':!:.claude' | xargs -0 /usr/bin/grep -nIE -d skip -e "$GATE_TERMS" | awk -F: -v f="$RM" -v n="$V138" '"'"'!($1 == f && $2 + 0 >= n)'"'"')"; if [ -n "$HITS" ]; then printf '%s\n' "$HITS"; printf 'INSTR02_GATE: RED (%s residual reference(s)) boundary=%s\n' "$(printf '%s\n' "$HITS" | wc -l)" "$V138"; else printf 'INSTR02_GATE: GREEN (boundary=%s)\n' "$V138"; fi; fi)"; git worktree remove --force "$WT" >/dev/null 2>&1; printf '%s\n' "$OUT" | tail -20; printf '%s\n' "$OUT" | /usr/bin/grep -qFe 'INSTR02_GATE: RED' && for f in CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md; do printf '%s\n' "$OUT" | /usr/bin/grep -qFe "$f" || { echo "PRE-SWEEP-HIT-MISSING: $f"; exit 1; }; done && echo GATE-HAS-TEETH</automated>
    <fails_when>non-zero exit, `GATE-HAS-TEETH` absent, a `PRE-SWEEP-HIT-MISSING: <path>` line naming a live document the pre-sweep reading failed to flag, or `INSTR02_GATE: GREEN` in the captured output — a green reading at the pre-phase commit means the expression matches nothing and proves nothing, which is the exact D-13 failure. `INSTR02_GATE: ABORT` in the captured output means the ROADMAP section header was already renamed at that commit. `$PRE_PHASE_SHA` must be set to the parent of Plan 196-01's commit, taken from `196-01-SUMMARY.md`.</fails_when>
    <automated>cd /workspaces && GATE_TERMS='tools/adoption|pypi_version_share|[Cc]lick[Hh]ouse|splitByChar|toUInt32OrZero|pypi_raw|pypistats' && RM=.planning/ROADMAP.md && V138="$(/usr/bin/grep -nE '^## v1\.38 ' "$RM" | head -1 | cut -d: -f1)" && { [ -n "$V138" ] || { echo 'INSTR02_GATE: ABORT - v1.38 ROADMAP boundary not found'; exit 3; }; } && test "$V138" -gt 300 && HITS="$(git ls-files -z -- ':!:.planning/STATE.md' ':!:.planning/milestones' ':!:.planning/phases/196-adoption-instrument-disposition' ':!:.planning/seeds/SEED-claim-firestarter-slug.md' ':!:.planning/graphs' ':!:.claude' | xargs -0 /usr/bin/grep -nIE -d skip -e "$GATE_TERMS" | awk -F: -v f="$RM" -v n="$V138" '!($1 == f && $2 + 0 >= n)')" && { if [ -n "$HITS" ]; then printf '%s\n' "$HITS"; printf 'INSTR02_GATE: RED (%s residual reference(s)) boundary=%s\n' "$(printf '%s\n' "$HITS" | wc -l)" "$V138"; exit 1; else printf 'INSTR02_GATE: GREEN - zero residual references (boundary=%s)\n' "$V138"; fi; }</automated>
    <fails_when>exit 1 with `INSTR02_GATE: RED (N residual reference(s))` and one `file:line:text` line per residual reference, each naming the document that still carries one; exit 3 with `INSTR02_GATE: ABORT` when the `^## v1\.38 ` header cannot be found, which means the anchor was renamed and the expression refused to guess a boundary; a boundary at or below 300, which fails the `-gt 300` test and signals the anchor matched something unexpected; or the absence of any `INSTR02_GATE:` line at all, which means the command did not run.</fails_when>
    <automated>cd /workspaces && W="$(git worktree list | /usr/bin/grep -Fe 'instr02-presweep' || true)" && { [ -z "$W" ] && true || { printf '%s\n' "$W"; false; }; } && T="$(git status --porcelain tools/ .planning/notes/)" && CFG="$(git status --porcelain .planning/config.json)" && test -z "$T" && test -z "$CFG" && echo NO-GATE-ARTIFACT-LEFT</automated>
    <fails_when>non-zero exit, `NO-GATE-ARTIFACT-LEFT` absent, or a printed `git worktree list` line showing a temporary worktree that was not removed. A non-empty `tools/` or `.planning/notes/` porcelain means the expression was written to a file outside the excluded paths, which would turn the proof red on itself. A non-empty `.planning/config.json` porcelain means a GSD verb pruned its sub-repository list.</fails_when>
  </verify>
  <done>The expression has been seen red on this machine against the pre-phase commit, naming all four live documents, and green against the working tree with a boundary computed from the section-header anchor. Both readings are recorded verbatim in the summary, no temporary worktree survives, and the expression exists in no committed file outside this phase's directory.</done>
</task>

<task type="auto">
  <name>Task 2: The regeneration — rebuild the graph after the sweep, and stop the 3 MB artifact being trackable</name>
  <precondition>`graphify` is on PATH and no rebuild is already in flight: `command -v graphify` prints a path, and `test ! -e /workspaces/.planning/graphs/.rebuild.lock`. If the lock exists, halt rather than racing a build — a truncated generated artifact is worse than a stale one, because it looks current.</precondition>
  <reversibility rating="reversible">A regenerated artifact and one ignore line. The report is rebuildable from the tree at any time, and an ignore rule is removed by deleting a line.</reversibility>
  <read_first>
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md — §R5.2 in full (the documented command chain, the proof that the build reads the working tree rather than git, the measured 212-second duration, the tracked-versus-ignored output table, and the branch gate that makes a spontaneous rebuild impossible here) and §R2.4 (why one of the report's two references regenerates and the other does not)
    - /workspaces/.claude/commands/gsd-graphify.md lines 150-200 only — the build chain and its anti-patterns section, which states plainly that the build is never spawned into an agent and never run in the background
    - /workspaces/.gitignore — the whole file, 60-odd lines. Read the knowledge-graph block near the end; the new rule belongs in it, and the file already carries one unrelated uncommitted hunk further up
    - /workspaces/.planning/graphs/.last-build-status.json — the previous build's recorded outcome, so the new one can be compared against it
  </read_first>
  <files>.gitignore, .planning/graphs/GRAPH_REPORT.md, .planning/graphs/.last-build-status.json</files>
  <action>
    Rebuild the knowledge graph now that the deletion and the sweep are both on disk, and close the
    hazard the rebuild itself creates.

    **Ordering is the whole point and it is already satisfied.** The build is given a filesystem path
    and reads the working tree, not git, so it encodes whatever text is on disk at the moment it runs.
    Run before the sweep it would faithfully re-encode the stale text; run now it encodes the swept
    text. This is D-11's constraint and it is the one genuine ordering constraint in the phase.

    **Capture a marker first.** Touch a marker file under the session scratch directory immediately
    before starting the build, so the report's freshness can be proved by comparison afterwards rather
    than assumed.

    **Run the documented chain from `196-RESEARCH.md` §R5.2, in the foreground, from `/workspaces`.**
    Update the graph against the current directory, copy the JSON payload, the HTML view and the
    report from the scratch output directory into `.planning/graphs/`, then take the build snapshot and
    read the status back. Allow a 600000 ms timeout: the last real build took about 212 seconds, not
    the 15 to 60 seconds the command documentation claims. **Never spawn this into a sub-agent and
    never background it** — sub-agent isolation has truncated a build mid-write before.

    **Expect the report to keep one reference, and do not chase it.** The report carries two. One is a
    file node for the deleted script and the rebuild removes it; that is the criterion below. The other
    is a community node whose label is sourced from a heading inside the frozen Phase 193 archive, and
    the rebuild re-emits it every time. D-11 assumed the rebuild would clear both; it does not, and the
    plan does not depend on it doing so. `.planning/graphs/` is excluded from the proof precisely
    because the only way to clear that second reference would be to edit a frozen archived file, which
    is forbidden. Do not hand-edit the report for any reason — generated artifacts are regenerated in
    this project, never edited.

    **Add one line to `.gitignore`.** The build also writes a 3 MB HTML view into `.planning/graphs/`
    that is untracked **and not ignored**, which D-11 does not mention. Add the path
    `.planning/graphs/graph.html` to the knowledge-graph block near the end of the file, beside the
    two entries already there. This is a deliberate choice over avoiding it by discipline: avoidance
    relies on every future commit being careful, and an ignore rule does not. Leave the block's
    explanatory comment alone — its stated payload size is already out of date and correcting it is not
    this phase's work.

    **The `.gitignore` rider, disclosed rather than hidden.** That file already carries an unrelated
    uncommitted hunk adding a marketplace-installed skill directory, and `git add` is file-granular, so
    staging `.gitignore` carries that hunk too. Accept it and say so in the commit body — one sentence
    naming the rider and that it is unrelated to this phase. Do not attempt to split it out.

    **Commit exactly three paths**: `.gitignore`, `.planning/graphs/GRAPH_REPORT.md` and
    `.planning/graphs/.last-build-status.json`. Never `git add -A`, never `git add .`, never
    `git commit -a` — the build has just written a 3 MB HTML file and a 105 MB JSON file into that same
    directory, and a broad stage would commit generated blobs. Both tracked graph outputs were already
    dirty before this phase started, so their diffs carry pre-existing churn that is not this phase's
    work; that is expected and is not a reason to try to clean it up. Subject on the repository
    convention, `docs(196): <subject>`, and no AI or Claude attribution of any kind.

    The chain runs at least one GSD verb, so afterwards check `git status --porcelain
    .planning/config.json` and `git checkout --` that one path if it changed. Never commit that
    pruning.
  </action>
  <acceptance_criteria>
    - `.planning/graphs/GRAPH_REPORT.md` is newer than the marker file touched immediately before the build — proving this build ran, rather than a stale report being committed.
    - `.planning/graphs/.last-build-status.json` records `"status": "ok"` and `"exit_code": 0`.
    - `.planning/graphs/GRAPH_REPORT.md` contains no whole line that is a file node for the deleted instrument script.
    - `.planning/graphs/GRAPH_REPORT.md` is still a substantial report — at least 5,000 lines — so a truncated or empty build cannot satisfy the previous criterion by being empty.
    - `git check-ignore -q .planning/graphs/graph.html` exits 0.
    - `git ls-files -- .planning/graphs/graph.html` prints nothing, and `git ls-files -- .planning/graphs/graph.json` prints nothing — neither generated payload became tracked.
    - `git ls-files -- .planning/graphs` prints at least one path, so the previous criterion is not vacuously satisfied by a pathspec that matches nothing.
    - `graphify-out/` at the repository root is untracked and ignored.
    - Exactly one new commit exists on top of the pre-task HEAD, and its diff touches exactly three paths: `.gitignore`, `.planning/graphs/GRAPH_REPORT.md` and `.planning/graphs/.last-build-status.json`.
    - The commit message contains no AI or Claude attribution, and its body names the unrelated `.gitignore` rider hunk.
    - HEAD is still on a branch whose name begins `v1.39-`, and `.planning/config.json` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test -n "$GRAPH_MARKER" && test -e "$GRAPH_MARKER" && test .planning/graphs/GRAPH_REPORT.md -nt "$GRAPH_MARKER" && test "$(wc -l < .planning/graphs/GRAPH_REPORT.md)" -ge 5000 && ST="$(cat .planning/graphs/.last-build-status.json)" && printf '%s\n' "$ST" | /usr/bin/grep -qFe '"status": "ok"' && printf '%s\n' "$ST" | /usr/bin/grep -qFe '"exit_code": 0' && echo GRAPH-FRESH</automated>
    <fails_when>non-zero exit, or `GRAPH-FRESH` absent. An unset or missing `$GRAPH_MARKER` fails the first two tests and means the marker was not touched before the build, so freshness cannot be proved and must not be assumed. A report older than the marker means the build did not run or did not copy; a report under 5,000 lines means it was truncated; a status other than `ok` or an exit code other than 0 fails its own `grep -qFe`.</fails_when>
    <automated>cd /workspaces && H="$(/usr/bin/grep -nxE -e '- pypi_version_share\.sh' .planning/graphs/GRAPH_REPORT.md || true)" && { [ -z "$H" ] && echo GRAPH-FILENODE-GONE || { printf '%s\n' "$H"; false; }; }</automated>
    <fails_when>non-zero exit, `GRAPH-FILENODE-GONE` absent, or a printed `<line>:<text>` line showing the file node for the deleted script still present — which means the build ran before the deletion reached the working tree, or did not run at all. Note this is a whole-line match against the file-node form only: the community node sourced from the frozen archive legitimately survives and must not be chased.</fails_when>
    <automated>cd /workspaces && git check-ignore -q .planning/graphs/graph.html && HT="$(git ls-files -- .planning/graphs/graph.html)" && JS="$(git ls-files -- .planning/graphs/graph.json)" && DIR="$(git ls-files -- .planning/graphs)" && OUT="$(git ls-files -- graphify-out)" && test -z "$HT" && test -z "$JS" && test -z "$OUT" && test -n "$DIR" && /usr/bin/grep -qFxe '.planning/graphs/graph.html' .gitignore && echo HTML-IGNORED-NOT-TRACKED</automated>
    <fails_when>non-zero exit, or `HTML-IGNORED-NOT-TRACKED` absent. `git check-ignore -q` exits 1 when the path is not ignored, which is the state this task exists to fix. A non-empty listing for the HTML view, the JSON payload or the scratch output directory means a generated blob became tracked — the exact outcome a broad `git add` produces. An empty `.planning/graphs` listing fails the `-n` test and is the guard against a pathspec that matches nothing.</fails_when>
    <automated>cd /workspaces && CNT="$(git rev-list --count "$PRE_TASK_SHA"..HEAD)" && NAMES="$(git diff --name-only "$PRE_TASK_SHA"..HEAD)" && MSG="$(git log -1 --format='%B')" && BR="$(git rev-parse --abbrev-ref HEAD)" && CFG="$(git status --porcelain .planning/config.json)" && test "$CNT" = 1 && test -n "$NAMES" && test "$(printf '%s\n' "$NAMES" | wc -l)" = 3 && for p in .gitignore .planning/graphs/GRAPH_REPORT.md .planning/graphs/.last-build-status.json; do printf '%s\n' "$NAMES" | /usr/bin/grep -qFe "$p" || { echo "MISSING-FROM-COMMIT: $p"; exit 1; }; done && A="$(printf '%s\n' "$MSG" | /usr/bin/grep -niE -e 'co-authored-by.*(claude|anthropic|ai)|generated (with|by).*(claude|ai)|claude\.ai|ai-assisted' || true)" && { [ -z "$A" ] && true || { printf '%s\n' "$A"; false; }; } && printf '%s\n' "$BR" | /usr/bin/grep -qe '^v1\.39-' && test -z "$CFG" && echo GRAPH-COMMIT-SCOPE-OK</automated>
    <fails_when>non-zero exit, `GRAPH-COMMIT-SCOPE-OK` absent, a `MISSING-FROM-COMMIT: <path>` line naming an expected path that was not staged, or a printed attribution line. `$PRE_TASK_SHA` must be captured with `git rev-parse HEAD` before the commit — `git rev-list --count HEAD ^HEAD~1` is a tautology that always prints 1. A name count above 3 means a generated blob or a stray dirty path was swept in; a branch other than `v1.39-*` fails the branch grep; a pruned `.planning/config.json` fails the final `-z` test.</fails_when>
  </verify>
  <done>The report is provably newer than the pre-build marker, records a clean build, is full-length, and no longer carries a file node for the deleted script. The 3 MB HTML view is ignored and untracked, the JSON payload and the scratch output directory are untracked, and one attribution-free commit on a `v1.39-*` branch carries exactly three paths with the unrelated `.gitignore` rider disclosed in its body.</done>
</task>

<task type="auto">
  <name>Task 3: The bookkeeping — both requirements recorded complete, then the proof run once more over the file that records it</name>
  <reversibility rating="reversible">Four checkbox and table-cell edits in one 7.7 KB tracked file, recoverable from the previous commit.</reversibility>
  <read_first>
    - /workspaces/.planning/REQUIREMENTS.md — the whole file, 7.7 KB. Read the INSTR section as Plan 196-02 left it, the Traceability table, and the already-complete sibling requirements above it: they carry a `**Complete:**` clause naming the evidence, and that is the house pattern this task follows
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-PATTERNS.md — §2 S3, which names the already-`Complete` rows in the same table as the format analog for the flip
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-RESEARCH.md — §R5.3, for the two documented GSD-verb hazards and why both edits here are by hand
    - /workspaces/.planning/phases/196-adoption-instrument-disposition/196-03-SUMMARY.md — the green proof reading recorded by Task 1, which is the evidence the `**Complete:**` clauses cite
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Record both requirements complete, now that their proof has run and the graph has been rebuilt —
    and not one step earlier. A requirement marked complete ahead of its proof is a failure mode this
    project has watched executors reach for, which is why the flip is the last edit of the phase.

    **Four edits, all by hand.** Flip INSTR-01's checkbox from unchecked to `[x]` and INSTR-02's the
    same. Flip the two Traceability rows from `Pending` to `Complete`, matching the cell format of the
    already-complete sibling rows in the same table exactly — same pipes, same spacing.

    **Add a `**Complete:**` clause to each requirement**, in the voice the sibling requirements above
    already use: a short sentence naming the evidence, not a restatement of the requirement.
    INSTR-01's evidence is the retirement record at `.planning/notes/adoption-instrument-retirement.md`
    carrying the full causal chain, the two declined alternatives and the accepted cost, plus the
    instrument's removal. INSTR-02's evidence is the green proof reading over every tracked file
    outside its six named exclusions, recorded verbatim in `196-03-SUMMARY.md`, and the annotation in
    the seed that the six exclusions leave the expression unable to see.

    **Do not name the retired artifact by path or by filename in either clause.** This file is one of
    the files the proof expression scans, and a path citation here turns it red — which the final leg
    of this task would catch, but only after the edit. Describe it.

    **Hand-edit. Do not run a GSD verb on this file or on the roadmap.** The requirements and roadmap
    verbs reformat the whole file, and the plan-progress verb overwrites this repository's phase block
    positionally — it has destroyed a phase name, its requirement IDs and its dependency line in one
    call. Leave the Coverage totals at the foot of the file alone: eight requirements, eight mapped,
    zero unmapped is still true and none of those numbers changes.

    **Then run the proof once more.** `.planning/REQUIREMENTS.md` is a scanned file, so the edit just
    made could in principle reintroduce a residual reference. Re-run the expression exactly as in
    Task 1's second leg and confirm it still prints green. This re-run is the phase's closing
    assertion: everything that was going to change has changed, and the property still holds.

    **Commit one path.** Stage only `.planning/REQUIREMENTS.md`. Never `git add -A`, never `git add .`,
    never `git commit -a`. Subject on the repository convention, `docs(196): <subject>`, with no AI or
    Claude attribution. Check `git status --porcelain .planning/config.json` before committing and
    `git checkout --` that one path if a GSD verb pruned it.

    Create no tag and no GitHub Release. The meta repository must never publish a release — a
    version-shaped tag published as one makes every stranded CLI report firmware up to date, silently
    and permanently.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains exactly one line matching `- \[x\] \*\*INSTR-01\*\*: ` and exactly one matching `- \[x\] \*\*INSTR-02\*\*: `, and contains no line matching `- \[ \] \*\*INSTR-0`.
    - The file contains `| INSTR-01 | Phase 196 | Complete |` and `| INSTR-02 | Phase 196 | Complete |`, and contains neither `| INSTR-01 | Phase 196 | Pending |` nor `| INSTR-02 | Phase 196 | Pending |`.
    - Both INSTR requirements carry a `**Complete:**` clause.
    - Neither `tools/adoption` nor `pypi_version_share` appears anywhere in the file.
    - The Coverage totals are unchanged: the file still states 8 total requirements, 8 mapped and 0 unmapped.
    - Every hunk in `git diff` for this file begins within lines 70 to 110, proving no other requirement and no other table row was touched.
    - The proof expression, re-run after this edit, prints `INSTR02_GATE: GREEN` and no hit lines.
    - Exactly one new commit exists on top of the pre-task HEAD, its diff touches exactly one path, and its message carries no AI or Claude attribution.
    - HEAD is still on a branch whose name begins `v1.39-`, no tag was created, and `.planning/config.json` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && Q=.planning/REQUIREMENTS.md && test "$(/usr/bin/grep -nE -e '^- \[x\] \*\*INSTR-01\*\*: ' "$Q" | wc -l)" = 1 && test "$(/usr/bin/grep -nE -e '^- \[x\] \*\*INSTR-02\*\*: ' "$Q" | wc -l)" = 1 && test "$(/usr/bin/grep -nE -e '^- \[ \] \*\*INSTR-0' "$Q" | wc -l)" = 0 && /usr/bin/grep -qFe '| INSTR-01 | Phase 196 | Complete |' "$Q" && /usr/bin/grep -qFe '| INSTR-02 | Phase 196 | Complete |' "$Q" && P="$(/usr/bin/grep -nFe '| INSTR-01 | Phase 196 | Pending |' -e '| INSTR-02 | Phase 196 | Pending |' -e 'tools/adoption' -e 'pypi_version_share' "$Q" || true)" && { [ -z "$P" ] && true || { printf '%s\n' "$P"; false; }; } && test "$(/usr/bin/grep -nFe '**Complete:**' "$Q" | wc -l)" -ge 5 && /usr/bin/grep -qFe 'Unmapped: 0' "$Q" && echo INSTR-ROWS-COMPLETE</automated>
    <fails_when>non-zero exit, `INSTR-ROWS-COMPLETE` absent, or a printed `<line>:<text>` line showing a traceability row still reading `Pending` or a path citation reintroduced into the file. A checkbox left unchecked fails its own `= 1` or `= 0` count; fewer than five `**Complete:**` clauses means one of the two new ones is missing, since three sibling requirements already carry one; a changed Coverage total fails the `Unmapped: 0` grep.</fails_when>
    <automated>cd /workspaces && D="$(git diff -U0 -- .planning/REQUIREMENTS.md)" && test -n "$D" && B="$(printf '%s\n' "$D" | /usr/bin/grep -e '^@@' | awk '{split($2,a,","); n=substr(a[1],2)+0; if (n < 70 || n > 110) print $0}')" && { [ -z "$B" ] && echo REQUIREMENTS-SCOPE-OK || { printf '%s\n' "$B"; false; }; }</automated>
    <fails_when>non-zero exit, `REQUIREMENTS-SCOPE-OK` absent, or a printed `@@` hunk header showing an edit outside lines 70 to 110 — the most likely symptom of a GSD verb having reformatted the whole file. An empty diff fails the `-n` test and means neither the checkboxes nor the rows were edited. This leg compares the working tree against the index, so it must run before this task's commit.</fails_when>
    <automated>cd /workspaces && GATE_TERMS='tools/adoption|pypi_version_share|[Cc]lick[Hh]ouse|splitByChar|toUInt32OrZero|pypi_raw|pypistats' && RM=.planning/ROADMAP.md && V138="$(/usr/bin/grep -nE '^## v1\.38 ' "$RM" | head -1 | cut -d: -f1)" && { [ -n "$V138" ] || { echo 'INSTR02_GATE: ABORT - v1.38 ROADMAP boundary not found'; exit 3; }; } && HITS="$(git ls-files -z -- ':!:.planning/STATE.md' ':!:.planning/milestones' ':!:.planning/phases/196-adoption-instrument-disposition' ':!:.planning/seeds/SEED-claim-firestarter-slug.md' ':!:.planning/graphs' ':!:.claude' | xargs -0 /usr/bin/grep -nIE -d skip -e "$GATE_TERMS" | awk -F: -v f="$RM" -v n="$V138" '!($1 == f && $2 + 0 >= n)')" && { if [ -n "$HITS" ]; then printf '%s\n' "$HITS"; printf 'INSTR02_GATE: RED (%s residual reference(s)) boundary=%s\n' "$(printf '%s\n' "$HITS" | wc -l)" "$V138"; exit 1; else printf 'INSTR02_GATE: GREEN - zero residual references (boundary=%s)\n' "$V138"; fi; }</automated>
    <fails_when>exit 1 with `INSTR02_GATE: RED (N residual reference(s))` and one `file:line:text` line per residual reference — after this task the overwhelmingly likely culprit is this task's own edit having cited the retired path in a `**Complete:**` clause; exit 3 with `INSTR02_GATE: ABORT` when the `^## v1\.38 ` header cannot be found; or the absence of any `INSTR02_GATE:` line at all, which means the command did not run.</fails_when>
    <automated>cd /workspaces && CNT="$(git rev-list --count "$PRE_TASK_SHA"..HEAD)" && NAMES="$(git diff --name-only "$PRE_TASK_SHA"..HEAD)" && MSG="$(git log -1 --format='%B')" && BR="$(git rev-parse --abbrev-ref HEAD)" && CFG="$(git status --porcelain .planning/config.json)" && TAGS="$(git tag --points-at HEAD)" && test "$CNT" = 1 && test "$NAMES" = '.planning/REQUIREMENTS.md' && test -z "$TAGS" && A="$(printf '%s\n' "$MSG" | /usr/bin/grep -niE -e 'co-authored-by.*(claude|anthropic|ai)|generated (with|by).*(claude|ai)|claude\.ai|ai-assisted' || true)" && { [ -z "$A" ] && true || { printf '%s\n' "$A"; false; }; } && printf '%s\n' "$BR" | /usr/bin/grep -qe '^v1\.39-' && test -z "$CFG" && echo FINAL-COMMIT-SCOPE-OK</automated>
    <fails_when>non-zero exit, `FINAL-COMMIT-SCOPE-OK` absent, or a printed attribution line. `$PRE_TASK_SHA` must be captured with `git rev-parse HEAD` before the commit — `git rev-list --count HEAD ^HEAD~1` is a tautology that always prints 1. Any path other than `.planning/REQUIREMENTS.md` fails the string equality; a tag pointing at HEAD fails the `-z "$TAGS"` test, which is the guard against the meta repository publishing anything; a branch other than `v1.39-*` fails the branch grep; a pruned `.planning/config.json` fails the final `-z` test.</fails_when>
  </verify>
  <done>Both INSTR checkboxes read `[x]`, both traceability rows read `Complete`, both requirements carry a `**Complete:**` clause naming their evidence without citing the retired path, every hunk is inside the INSTR region, the proof re-run after the edit is still green, and one attribution-free, tag-free commit on a `v1.39-*` branch carries exactly that one file.</done>
</task>

</tasks>

<threat_model>
Security enforcement is treated as enabled (`security_enforcement` is absent from
`.planning/config.json`). Resolved configuration for this run: `security_asvs_level: 1`,
`security_block_on: high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the proof expression → the six excluded paths | Six exclusions are six holes by construction. Each carries a recorded reason, and each is the boundary at which the expression stops being evidence — what lies beyond it is covered by a hand-written criterion somewhere in this phase, or by nothing, and §R2.5 says which. |
| generated artifacts → the git index | The rebuild writes 108 MB across five files into a directory that holds two tracked ones. The boundary between what is regenerated and what is committed is held by explicit-path staging and by the `.gitignore` rule this plan adds. |
| this repository → the frozen v1.38 archive | Read-only in one direction. The generated report re-emits a node sourced from inside the archive on every rebuild, and the sanctioned response is to exclude the generated directory, never to edit the archived source. |
| a requirement's recorded status → its evidence | `Complete` is an assertion about proof. Flipping it before the proof runs converts a bookkeeping row into a false claim that later readers have no way to detect. |

No network boundary is crossed by any task here; the graph build reads the local filesystem. No
credential, secret, token or user input is read, written or transported. ASVS L1 categories V2 through
V7 and V12 have no surface. **V14 (configuration) is the one category with any surface in this phase**,
and it is touched twice: one added `.gitignore` rule, and the `.planning/config.json` pruning hazard
that any GSD verb can trigger. Both are covered below.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-08 | Spoofing | the INSTR-02 proof expression | medium | mitigate | An expression that matches nothing is indistinguishable from a repository with nothing to match, and a green reading from one would be a false claim of proof. Mitigated by the D-13 red-first requirement, reproduced on the executor's own machine against the pre-phase commit and asserted to name all four live documents — not merely to be non-empty. |
| T-196-09 | Tampering | the INSTR-02 proof expression | medium | mitigate | The cheapest route from red to green is to edit the expression — drop a term, widen an exclusion, replace the anchor, remove the fail-closed guard. Mitigated by an explicit prohibition, by the assertion that the reported boundary is anchor-computed and above 300, and by the rule that a red reading changes the document it names, never the expression. |
| T-196-10 | Information disclosure | `tools/`, the repository root, `.planning/notes/` | low | mitigate | The expression's own term set matches its own text, so committing it as a script outside the six excluded paths would turn the proof red on itself and reintroduce the identifiers the phase removed. Mitigated by keeping it inside this phase's directory, and by a porcelain assertion that `tools/` and `.planning/notes/` are untouched by Task 1. |
| T-196-02 | Repudiation | `.planning/STATE.md`, `.planning/milestones/`, `.planning/graphs/GRAPH_REPORT.md` | medium | mitigate | One archived file is the *source* of a reference that survives in the generated report, so the shortest path to a clean report runs through editing frozen evidence. Mitigated by excluding the generated directory instead, by a prohibition against hand-editing either the archive or the report, and by scoping the report criterion to the file node the rebuild genuinely removes. |
| T-196-11 | Repudiation | `.planning/REQUIREMENTS.md` traceability rows | medium | mitigate | A requirement recorded `Complete` before its proof is a false claim no later reader can detect. Mitigated by ordering the flip last, by Plan 196-02 asserting both rows still read `Pending` when it ends, and by re-running the proof after the flip. |
| T-196-03 | Tampering | git index | medium | mitigate | The rebuild writes a 3 MB unignored HTML view and a 105 MB JSON payload beside two tracked files; a broad `git add` commits generated blobs permanently. Mitigated by explicit-path staging, a three-path commit-scope assertion, untracked assertions on all three generated payloads, and a permanent ignore rule for the HTML view. |
| T-196-04 | Tampering | `beta` and `main` branches; the meta-repository release surface | medium | mitigate | A commit on `beta` in either sub-repo publishes and can reach PyPI; a GitHub Release in the meta repository makes every stranded CLI report firmware up to date. Mitigated by a branch precondition, branch assertions in all three commit legs, a `git tag --points-at HEAD` assertion in the final leg, and an explicit prohibition on tags and releases. |
| T-196-05 | Tampering | `.planning/config.json` | low | mitigate | Any `gsd-tools` invocation, including a read-only one, can silently prune the `planning.sub_repos` list — and this plan runs at least one. Mitigated by a porcelain check on that one path in every verify leg of all three tasks and a `git checkout --` recovery instruction. |
| T-196-12 | Denial of service | the graph build | low | mitigate | A build spawned into a sub-agent or backgrounded has previously been truncated mid-write, leaving a generated artifact that looks current and is not. Mitigated by a foreground-only instruction, a lock-file precondition, a 600000 ms timeout sized against the measured 212-second duration, and a minimum-length assertion on the report so a truncated build cannot pass. |
| T-196-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this phase and no dependency is added, upgraded or resolved, so `196-RESEARCH.md` carries no Package Legitimacy Audit — correctly, because there is no package to audit. The graph build runs an already-installed binary at `/usr/local/bin/graphify` and resolves nothing. Accepted with that as the stated rationale. |

No threat is rated `high` or `critical`, so `security_block_on: high` is satisfied with zero blocking
threats and no security checkpoint is inserted.
</threat_model>

<verification>
Run the tasks in order. The order is load-bearing twice over: the graph build encodes whatever is on
disk when it runs, and a requirement recorded complete before its proof is a false claim.

Seven positive tokens must appear, in this order: `GATE-HAS-TEETH`, then a line containing
`INSTR02_GATE: GREEN`, then `NO-GATE-ARTIFACT-LEFT`, then `GRAPH-FRESH`, `GRAPH-FILENODE-GONE`,
`HTML-IGNORED-NOT-TRACKED` and `GRAPH-COMMIT-SCOPE-OK`, then `INSTR-ROWS-COMPLETE`,
`REQUIREMENTS-SCOPE-OK`, a second line containing `INSTR02_GATE: GREEN`, and `FINAL-COMMIT-SCOPE-OK`.

Both hunk-band legs and `REQUIREMENTS-SCOPE-OK` compare the working tree against the index, so each
must run before its own task's commit. Each carries a `test -n` guard on the captured diff, so running
one after its commit fails loudly rather than passing vacuously — but the guard is a backstop, not a
licence to reorder.

Three properties no automated leg in this plan can reach:

1. **What the proof expression does not see.** It matches identifiers, not meaning. Six exclusion
   classes are six holes, and the sharpest is the seed: D-14 requires an annotation there that the
   expression cannot verify, which is why Plan 196-02 asserts it by hand. Untracked files are outside
   it by design. Phrase-level residue is deliberately outside its term set. `196-RESEARCH.md` §R2.5 is
   the authoritative list; read it once before signing the phase off, and confirm every hole named
   there is covered by a criterion somewhere in these three plans.
2. **Whether the rebuild ran against the right tree.** The freshness leg proves the report is newer
   than a marker; it cannot prove the sweep was on disk at that moment. Confirm by reading: the file
   node for the deleted script is gone, which is only possible if the deletion had landed.
3. **The one reference that legitimately survives.** The generated report still carries a community
   node sourced from a frozen archived heading. It is not a miss, it is not fixable without editing
   the archive, and it must not be chased. Confirm it is the community node and not a second file
   node before accepting it.
</verification>

<success_criteria>
- The proof expression has been seen red against the pre-phase commit on the executor's own machine, naming all four live documents, and green against the working tree with an anchor-computed boundary above 300.
- No temporary worktree survives, and the expression exists in no committed file outside this phase's directory — `tools/` and `.planning/notes/` are untouched by the proof task.
- `.planning/graphs/GRAPH_REPORT.md` is provably newer than the pre-build marker, records a clean build, is at least 5,000 lines, and carries no file node for the deleted script.
- `.planning/graphs/graph.html` is gitignored and untracked; `graph.json` and `graphify-out/` are untracked; `.planning/graphs` still has tracked content.
- `.gitignore` carries `.planning/graphs/graph.html`, and the unrelated rider hunk it drags along is disclosed in the commit body rather than hidden.
- Both INSTR checkboxes read `[x]`, both traceability rows read `Complete`, both requirements carry a `**Complete:**` clause naming their evidence, and neither clause cites the retired path.
- The proof, re-run after the traceability flip, is still green.
- Three attribution-free commits on a `v1.39-*` branch, staging two, three and one explicit paths respectively. No tag, no release, and `.planning/config.json` unchanged throughout.
</success_criteria>

<output>
Create `.planning/phases/196-adoption-instrument-disposition/196-03-SUMMARY.md` when done.

Record in it, verbatim:

- **Both proof readings** — the red line from the pre-phase commit with its count and its four named
  documents, and the green line with its computed boundary. These are the D-13 evidence and they must
  survive the phase rather than living only in a plan file.
- The final green reading from the re-run after the traceability flip, separately, so the closing
  assertion is distinguishable from the first one.
- The measured duration of the graph build, and whether the report's line count moved materially —
  both are calibration data for the next phase that rebuilds it.
- The exact `**Complete:**` clause written for each requirement.
- The pre-task SHA used for each of the three commit-scope assertions.
- **Two things to carry to phase close, as questions and not as work:** first, that
  `reference_pypi_per_version_downloads_clickhouse` is a Claude memory outside this repository
  recording the method D-06 deleted, and the operator should be asked whether it goes too, since the
  instruction was that none of it survives. Second, that a fired seed still matches the seed glob
  `/gsd-new-milestone` reads, so `SEED-claim-firestarter-slug.md` will keep surfacing to milestone
  creation although it can never fire again — that is GSD tooling behaviour, deferred in
  `196-CONTEXT.md`, and worth a todo rather than a fix here.
- One observation for the record, not an action: the knowledge-graph block in `.gitignore` states a
  payload size that is out of date by roughly a factor of four. It was deliberately left alone as
  outside this phase's scope.
</output>
