---
status: testing
phase: 200-an-elevated-programming-supply-is-stated
source: [200-VERIFICATION.md]
started: 2026-09-19T18:20:00Z
updated: 2026-09-19T18:20:00Z
---

## Current Test

number: 1
name: The shipped wording reads as intended — both voltage spellings and the amended verb
expected: |
  Run, with a clean config dir so no `~/.firestarter/database.json` override interferes:

      firestarter info MBM27C1000

  The output carries, in this order:

      VCC:                5.0v
      Programming VCC:    6.0v
      VPP:                12.5v

  ...and further down, after a blank separator line:

      WARNING: this part's programming supply decodes to 6.0 V; the shield supplies a fixed 5.0 V.
      Programming will be attempted at 5.0 V.

  Two things are yours to confirm, and only yours:

  1. **The two spellings are deliberate and both wanted.** The field-row cell reads `6.0v`
     (via `format_mv`, matching every other voltage row in the table). The warning prose reads
     `6.0 V` (via `_format_v_prose`, which wraps `format_mv` and re-spells only the unit). One
     number, one formatter, two renderings. D-04's mock-up showed both. Confirm that is what you
     want, or say which should change.

  2. **The amended verb reads the way you meant it.** You amended D-04 on 2026-09-19 from an
     assertion about what the part *does* ("programs at") to one about what the datum *decodes
     to". The measurement behind that amendment: only **3** of the 284 elevated rows carry a
     datasheet-cited `vdd_mv` (the three Fujitsu overrides); the other **281** carry a pure
     `VCC_VOLTAGES` rail-table slot, and DECODE-NOTES § 9 is this milestone's own verdict that
     those nibbles select a programmer rail index, not a chip requirement. `MBM27C1000` above is
     one of the 281. Confirm the sentence says what you intended it to say.

  `MBM27C4001` is the other pinned part and is one of the 3 datasheet-cited rows, if you want to
  read the same wording on a row where the value does have datasheet backing.
awaiting: user response

## Tests

### 1. The shipped wording reads as intended — both voltage spellings and the amended verb
expected: Operator confirms both spellings (`6.0v` in the cell, `6.0 V` in the prose) and the amended verb read as intended, or states which should change.
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

None. The verifier scored 9/10 must-haves VERIFIED with zero behaviour unverified, zero orphaned
requirements and zero stub artifacts. The single outstanding item is this wording sign-off, which
plan 200-02 Task 2 reserved for the operator by design — it was never an automatable check.

Carried alongside, not blocking: the code review's CR-01. `programming_vcc_over_rail_mv` raises
`AttributeError` rather than returning `None` when `electrical` is a truthy non-dict (e.g.
`{"electrical": "bogus"}` from a hand-edited `~/.firestarter/database.json`). Every D-06 case the
plans actually enumerate returns `None` correctly, and the shape is unreachable through `info`
because `database.py` calls `electrical.get("pin_count")` on the same raw record one layer
earlier. The docstring's unconditional "must fail open" over-claims against the narrower `except
(TypeError, ValueError)`. Verifier ruled it a backlog hardening item, not a phase gap.
