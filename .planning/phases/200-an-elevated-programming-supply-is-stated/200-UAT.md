---
status: complete
phase: 200-an-elevated-programming-supply-is-stated
source: [200-VERIFICATION.md, 200-01-SUMMARY.md, 200-02-SUMMARY.md, 200-03-SUMMARY.md]
started: 2026-09-19T18:20:00Z
updated: 2026-09-19T19:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. The shipped wording reads as intended — both voltage spellings and the amended verb
expected: Operator confirms both spellings (`6.0v` in the cell, `6.0 V` in the prose) and the amended verb read as intended, or states which should change.
result: pass
reported: "To long and complicated warning"
severity: minor
resolution: |
  Operator rejected the shipped two-line warning as too long and complicated, then chose the
  shortest of three offered one-line replacements. Applied in firestarter_app b3a777e:

      WARNING: Programming VCC decodes to 6.0 V; using 5.0 V.

  Down from 92+40 chars across two lines to 55 chars on one. Verified live on both pinned
  parts (MBM27C1000, MBM27C4001), both exit 0; the 5.0 V negative case (W27C512) still shows
  neither the row nor the warning.

  Spelling split confirmed intact: the field row still reads `6.0v` via `format_mv`, the prose
  still reads `6.0 V` via `_format_v_prose`, and both numbers in the new line still route
  through `_format_v_prose`. The amended D-04 verb ("decodes to") is preserved verbatim; D-05's
  state-and-proceed signal is now carried by "using" instead of a follow-on sentence.

### 2. Elevated part shows Programming VCC: row + WARNING; non-elevated shows neither; info exits 0
expected: A part whose electrical.vdd_mv decodes above 5000 mV shows a Programming VCC: row between VCC: and VPP:, and a WARNING naming both numbers (one line as of G-200-1; two lines as originally shipped), on `firestarter info`; a part at or below 5000 mV (or with absent/null/zero/non-numeric vdd_mv) shows neither; info exits 0 in both cases.
result: pass
source: automated
coverage_id: 200-01-D1

### 3. Shortfall-statement shape generalises no_pinout_warning and is pinned by tests proven capable of failing
expected: The shortfall-statement shape (blank line, WARNING: condition+consequence, follow-on line) generalises the existing no_pinout_warning advisory and is pinned by regression tests proven capable of failing.
result: pass
source: automated
coverage_id: 200-01-D2

### 4. Rendered info output pinned byte-for-byte for both elevated classes through the real entry point
expected: The rendered `info` output an operator actually sees is pinned byte-for-byte for two elevated parts, through the real installed entry point, for both the rail-slot class (281/284 rows) and the datasheet-cited class (3/284 rows).
result: pass
source: automated
coverage_id: 200-02-D1

### 5. Snapshot change is purely additive — zero deletions against the phase base
expected: The snapshot change to tests/__snapshots__/test_characterization.ambr is purely additive -- git diff --numstat against the phase base 0d6be3f reports insertions with exactly zero deletions, a byte-identity proof for every pre-existing line.
result: pass
source: automated
coverage_id: 200-02-D2

### 6. Census: shipped predicate selects exactly 284 of 746 rows, all sub-counts pinned as equalities
expected: The shipped predicate programming_vcc_over_rail_mv selects exactly 284 of 746 rows, asserted as an equality derived from the live database, with vdd_mv/algorithm/type/status histograms, vendor count, VOLT-03 disjointness, and the 3-versus-281 datasheet-provenance split all pinned as exact equalities rather than floors.
result: pass
source: automated
coverage_id: 200-03-D1

### 7. Every census count proved capable of failing; database confirmed byte-identical
expected: Every count in the module is proved capable of failing via three in-module planted mutations plus an external planted mutation on _EXPECTED_TOTAL_ROWS, all observed to fail; the on-disk chip_database.json is confirmed byte-identical to the phase base (0d6be3f) before and after a full module run.
result: pass
source: automated
coverage_id: 200-03-D2

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0
issues_found_and_resolved: 1  # G-200-1, closed in-session by firestarter_app b3a777e

## Gaps

- gap_id: G-200-1
  truth: "The elevated-VCC warning on `firestarter info` reads as the operator intended — concise enough for the console, in the house advisory style."
  status: resolved
  resolved_by: "firestarter_app b3a777e -- fix(200): shorten the elevated-VCC warning to one line"
  resolved_at: 2026-09-19
  reason: "User reported: To long and complicated warning"
  severity: minor
  test: 1
  root_cause: "Authored string, not a defect. firestarter/eprom_info.py:343-347 emits a 92-char first line that spells the rail value twice ('the shield supplies a fixed 5.0 V' then 'Programming will be attempted at 5.0 V.'), and opens with the abstract subject phrase 'this part's programming supply decodes to'. The sibling no_pinout_warning advisory eleven lines below is 76 chars and uses an em-dash condition—consequence shape. The two constraints the replacement must keep are D-04's amended 'decodes to' hedge (not 'requires'/'programs at') and D-05's state-and-proceed signal (the warning must never read as a refusal)."
  artifacts:
    - path: "firestarter_app/firestarter/eprom_info.py"
      issue: "Warning text at lines 343-347 — verbose first line, duplicated rail value across the two lines"
    - path: "firestarter_app/tests/test_cli_handlers.py"
      issue: "test_info_elevated_programming_vcc_warns asserts the current wording via caplog.text"
    - path: "firestarter_app/tests/__snapshots__/test_characterization.ambr"
      issue: "Four snapshot entries (stdout+stderr for MBM27C1000 and MBM27C4001) pin the current wording byte-for-byte"
  missing: []
  debug_session: ""
  evidence:
    - "Full suite: 2100 passed, 36 snapshots passed, exit 0"
    - "Snapshot narrowness: git diff --numstat = 2 insertions / 4 deletions, both hunks the warning block"
    - "ruff check + ruff format --check: clean on both changed files"
    - "mypy (py3.11 venv, CI's pinned version): 32 errors before and after -- zero delta, neither changed file in the list"
    - "Live render verified on MBM27C1000 and MBM27C4001 (exit 0) and negative case W27C512"

Follow-on record note (not a gap): plan 200-01's coverage entry D-02 describes the shortfall
shape as "(blank line, WARNING: condition+consequence, follow-on line)". The follow-on line is
gone as of b3a777e -- the consequence is now folded into the single WARNING line. The SUMMARY is
left unedited as the historical record of what plan 01 shipped; this note is the current-state
correction. VCC-02 is still satisfied: the shape still generalises the no_pinout_warning
advisory (blank line, then a WARNING: condition+consequence line), it is simply one line rather
than two, and both regression tests still pin it.

Carried alongside, not blocking: the code review's CR-01. `programming_vcc_over_rail_mv` raises
`AttributeError` rather than returning `None` when `electrical` is a truthy non-dict (e.g.
`{"electrical": "bogus"}` from a hand-edited `~/.firestarter/database.json`). Every D-06 case the
plans actually enumerate returns `None` correctly, and the shape is unreachable through `info`
because `database.py` calls `electrical.get("pin_count")` on the same raw record one layer
earlier. The docstring's unconditional "must fail open" over-claims against the narrower `except
(TypeError, ValueError)`. Verifier ruled it a backlog hardening item, not a phase gap.
