name: PY32F071 toolchain

on:
  push:
    branches:
      - agent/py32f071-toolchain
      - feature/py32f071-full-support
  pull_request:
    paths:
      - "platform/py32f071/**"
      - ".github/workflows/py32f071.yml"
      - "include/rurp_platform_compat.h"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install GNU Arm toolchain and build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            gcc-arm-none-eabi \
            binutils-arm-none-eabi

      - name: Record tool versions
        run: |
          {
            arm-none-eabi-gcc --version
            arm-none-eabi-ld --version
            cmake --version
            ninja --version
          } | tee tool-versions.txt

      - name: Configure
        run: |
          cmake \
            -S platform/py32f071 \
            -B build/py32f071 \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            2>&1 | tee configure.log

      - name: Build
        run: |
          cmake --build build/py32f071 2>&1 | tee build.log

      - name: Report size
        run: |
          arm-none-eabi-size \
            build/py32f071/firestarter-py32f071.elf \
            | tee size.txt

      - name: Verify generated checksums
        working-directory: build/py32f071
        run: sha256sum --check firestarter-py32f071.sha256

      - name: Create artifact checksum manifest
        run: |
          sha256sum \
            build/py32f071/firestarter-py32f071.elf \
            build/py32f071/firestarter-py32f071.bin \
            build/py32f071/firestarter-py32f071.hex \
            build/py32f071/firestarter-py32f071.map \
            configure.log \
            build.log \
            size.txt \
            tool-versions.txt \
            > SHA256SUMS

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firestarter-py32f071
          if-no-files-found: error
          path: |
            build/py32f071/firestarter-py32f071.elf
            build/py32f071/firestarter-py32f071.bin
            build/py32f071/firestarter-py32f071.hex
            build/py32f071/firestarter-py32f071.map
            build/py32f071/firestarter-py32f071.sha256
            configure.log
            build.log
            size.txt
            tool-versions.txt
            SHA256SUMS
