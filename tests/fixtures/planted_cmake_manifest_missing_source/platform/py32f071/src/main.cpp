// Fixture input for tests/test_check_cmake_manifest.py — NOT real firmware
// source. Stands in for platform/py32f071/src/main.cpp so this fixture's
// PY32_PLATFORM_SOURCES entry resolves (the planted violation in this
// fixture is exclusively the missing FIRESTARTER_COMMON_SOURCES entry).
int main() { return 0; }
