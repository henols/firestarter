{
  "meta": {
    "sources": [
      "src/proms/eprom.cpp",
      "src/proms/eprom_params.cpp"
    ],
    "blob_shas": {
      "src/proms/eprom.cpp": "d3aef013b556ad6b5cc34fc54e446a370d5c54c2",
      "src/proms/eprom_params.cpp": "7817c1422d698547e5da5e0bee197b9951fb4465"
    },
    "recorded_at_head": "6bc3ed3371915a38f71ab8d92c593a6913603dfc",
    "recorded_by": "Phase 142 Plan 04 -- because L-2 requires this golden and the eprom.cpp source change to land in the SAME commit (the one-commit property that keeps the D-18 gate RED once, for one reason), recorded_at_head necessarily names this commit's PARENT rather than the commit that actually carries this file. Do not read that as a mistake: it is the deliberate one-commit offset this plan's own objective documents. Phase 143 Plan 05 -- the same one-commit property, under D-23's name this phase: this file's blob_shas['src/proms/eprom.cpp'] is the SHA of the working-tree file as it will read once committed (git hash-object src/proms/eprom.cpp, run before staging), and recorded_at_head is this commit's PARENT (a5a39d92128edbdb97f425e1538227a85a257e57, the tip of plan 143-03) rather than the commit that actually carries this file -- deliberate, for the identical structural reason Phase 142 recorded above, not a second mistake. Debug session w27c512-program-fail-byte0 (Phase 145 Gate 2 root-cause fix) -- re-derived a THIRD time, under the identical one-commit property: blob_shas['src/proms/eprom.cpp'] is `git hash-object src/proms/eprom.cpp` run on the working tree before staging, and recorded_at_head names this commit's PARENT (a594173d2bbbabe74e6a470b4751528435246326). The fix adds one file-static helper, eprom_internal_program_pulse, which asserts the program-voltage route around each program pulse -- the assert Phase 141 dropped when it deleted the old block-mismatch reporter, and whose absence made every 27C write fail at its first non-0xFF byte. That helper contains NO conditional at all, so this re-derivation moved ONLY line numbers: all 26 sites keep their predicate, keyed_on and tier byte-identically (machine-checked, not asserted by hand), counts stay 26 / 1 / 25, and the single tier-1 protocol-keyed site remains configure_eprom's pulse-fallback switch, now at line 70 (was 70). 15 of 26 sites shifted; every shifted site sits below the inserted helper, and every site above it is line-identical. Debug session w27c512-devtest-all-bad -- re-derived a FOURTH time, under the identical one-commit property: blob_shas['src/proms/eprom.cpp'] is `git hash-object src/proms/eprom.cpp` run on the working tree before staging, and recorded_at_head names this commit's PARENT (88d204a5a023bcad6f708b33150502ba90fdec2b). The fix changes ONE expression -- eprom_internal_erase's CE pulse width goes from handle->pulse_delay (the per-byte PROGRAM width, ~100 us) to EPROM_ERASE_PULSE_US (100 ms, the W27C512 datasheet T_PWE) -- plus the comment that explains it. It adds NO conditional and NO function, so this re-derivation moved ONLY line numbers: all 26 sites keep their predicate, keyed_on and tier verbatim, and the 5 sites below eprom_internal_erase's pulse shift by exactly 11 lines 649->660, 652->663, 653->664, 660->671, 677->688. RE-RECORDED by debug session w27c512-write-slow-3x (2026-08-22), which replaced eprom.cpp's per-BYTE program loop with a pass-batched one (a scan pass alternating with a pulse pass) to fix a measured 3.2x write-speed regression on protocol 0x07 -- 105.89 s -> 33.51 s for a 64 KiB W27C512 on a leonardo, byte-exact. WHAT MOVED AND WHY, per this gate's own how_to_update: total_sites 26 -> 23 and other_sites 25 -> 22. Three branch sites were REMOVED and none was added: two `i < handle->data_size` loop conditions became `i < block_len` (a hoisted uint16_t block length, which also saved AVR flash) and the final full-block verify pass, which was a byte-identical inline copy of memory_verify_execute, became a CALL to it -- taking its `firestarter_get_data(...) == expected` predicate out of this file. Every remaining predicate's spelling, keyed_on and tier are unchanged; only line numbers shifted. THE INVARIANT THIS GATE EXISTS FOR IS INTACT AND WAS RE-CHECKED, NOT ASSUMED: protocol_keyed_sites is still exactly 1, still `switch (handle->protocol)` at line 70 (configure_eprom's pulse-fallback switch), so TABLE-05's 'no second algorithm selector' holds -- the pass-batched loop reads no handle->protocol at all, and the one new build-time switch it did add (EPROM_OVERPROGRAM_SUPPORTED, a leonardo-only flash-funding guard) is a preprocessor conditional keyed on a target, never on a protocol. src/proms/eprom_params.cpp is byte-unchanged and its blob SHA is untouched. Re-derived, never hand-edited: sites and counts come from this gate module's OWN _extract_predicates() run against the live file, and blob_shas from `git hash-object` on the working tree before staging. DEVIATION FROM THE ONE-COMMIT PROPERTY, stated rather than hidden: the earlier re-records landed the source change and this golden in the SAME commit so the gate went RED once, for one reason. That was not possible here -- the eprom.cpp change landed at 071d505/5882548 and was PUSHED to an open pull request (#55) before this gate's failure was discovered, so folding this record into those commits would mean rewriting published history under a live PR. recorded_at_head therefore names the actual HEAD at recording time (273eedb), NOT this commit's parent, and the gate was legitimately RED in between. Also note the stale clause earlier in this field asserting a 'byte-unchanged guarantee for src/proms/eprom.cpp' (D-10, Phase 140): that guarantee was already void long before this session -- Phases 141/142/143 and 145 all edited this file -- and the assertion message that repeats it is inherited text, not a live claim. Phase 156 Plan 03 (DEDUP-01) -- re-derived a FIFTH time, under the identical one-commit property: blob_shas['src/proms/eprom.cpp'] is `git hash-object src/proms/eprom.cpp` run on the working tree before staging, and recorded_at_head names this commit's PARENT (3d0b73d). The change replaces eprom_check_vpp's over-voltage arm's inline eight-assignment VPP-mismatch packing block with a call to the new shared helper mem_util_report_voltage (src/proms/memory.cpp); the arm's `if (is_flag_set(FLAG_FORCE))` branch, which selected between MSG_WARN_VPP_HIGH/RESPONSE_CODE_WARNING and MSG_ERR_VPP_HIGH/RESPONSE_CODE_ERROR, becomes a `bool force = is_flag_set(FLAG_FORCE);` assignment feeding two parallel ternaries passed as call arguments -- this is a GENUINE branch removal, not a relocation: the fork survives as a call-site parameter pair rather than as a source-level `if`, and this gate's own extractor correctly does not count a plain assignment statement as a branch predicate. WHAT MOVED AND WHY, per this gate's own how_to_update: total_sites 23 -> 22 and other_sites 22 -> 21; protocol_keyed_sites stays exactly 1, still `switch (handle->protocol)` at line 70. No site was added and no surviving site was re-keyed or re-tiered: all 22 surviving entries were machine-compared field by field against the prior 23-site inventory (predicate, keyed_on, tier) and found byte-identical apart from `line`, which is the only field this re-derivation touched -- five sites below the edited region shifted up (736->718, 787->753, 790->756, 791->757, 798->764, 815->781; six listed, one of which, 736->718, sits just below the edited block and the rest sit inside or after the eprom_check_chip_id arm further down the file). The second FLAG_FORCE-keyed site in this file (the chip-ID mismatch ternary, now at line 757) is UNCHANGED and belongs to DEDUP-02 (Phase 156 Plan 04), which will re-derive this golden a SIXTH time in its own commit when it removes that site. src/proms/eprom_params.cpp is byte-unchanged and its blob SHA is untouched. Re-derived, never hand-edited: sites and counts come from this gate module's own _extract_predicates() run against the live file, and blob_shas from `git hash-object` on the working tree before staging. Phase 156 Plan 04 (DEDUP-02) -- re-derived a SIXTH time, under the identical one-commit property: blob_shas['src/proms/eprom.cpp'] is `git hash-object src/proms/eprom.cpp` run on the working tree before staging, and recorded_at_head names this commit's PARENT (6bc3ed3). The change replaces eprom_internal_check_chip_id's inline chip-ID-mismatch guard-plus-four-byte-payload block with a call to the new shared helper mem_util_report_chip_id (src/proms/memory.cpp); the removed predicate -- `if (chip_id != handle->chip_id)` -- did NOT disappear: it was RELOCATED into mem_util_report_chip_id's early return in src/proms/memory.cpp, a file this gate does not scan. This is a relocation, not a deletion of a chip_id safety branch from eprom.cpp -- the same relocation pattern tests/test_hv_routing_source_contract_v142.py exists to catch, which is why it is named rather than glossed. The surviving `is_flag_set(FLAG_FORCE)` ternary at line 757 is a DIFFERENT site -- eprom_generic_init's own caller-policy decision, feeding mem_util_report_chip_id's warn_only parameter -- and is untouched; folding it into the helper is exactly what this plan's own prohibitions forbid, because eprom_check_chip_id_execute (the standalone CMD_CHECK_CHIP_ID path) keeps passing an unconditional RESPONSE_CODE_ERROR regardless of FLAG_FORCE. WHAT MOVED AND WHY, per this gate's own how_to_update: total_sites 22 -> 21 and other_sites 21 -> 20; protocol_keyed_sites stays exactly 1, still `switch (handle->protocol)` at line 70. No site was added and no surviving site was re-keyed or re-tiered: all 20 surviving `other`-tier entries plus the one `protocol`-tier entry were machine-compared field by field (predicate, keyed_on, tier), positionally aligned against the prior 22-site inventory, and found byte-identical apart from `line`, which is the only field this re-derivation touched -- one site shifted, 781 -> 775 (the using_p1_as_vpp pin-routing fork, which sits below the edited region). Positional alignment (not a predicate-text dict lookup) was required because three sites share the identical predicate/keyed_on/tier signature `if (handle->response_code == RESPONSE_CODE_ERROR)` / [response_code] / other (lines 146, 177, 676) -- a naive dict keyed on that signature would collapse them and lose their distinct class/reason metadata; each was matched to its live counterpart by ascending line order instead. This closes the phase's two-commit sequence begun by Plan 03: DEDUP-01 removed one site (23 -> 22) and DEDUP-02 removes this one (22 -> 21), so the gate reads GREEN at every commit boundary. Divergence from research's own predicted final survivor shifts, stated rather than forced into agreement: research (cited in 156-04-PLAN.md) predicted :787/:790/:791/:815 landing at :758/:761/:762/:773; the machine-derived result instead lands the equivalent surviving sites at 753/756/757/775 -- the prediction was a sanity check, not a substitute for live extraction, and this re-derivation used the live extraction. src/proms/eprom_params.cpp is byte-unchanged and its blob SHA is untouched. Re-derived, never hand-edited: sites and counts come from this gate module's own _extract_predicates() run against the live file, and blob_shas from `git hash-object` on the working tree before staging.",
    "requirement": "TABLE-05",
    "decision": "D-12, D-13",
    "why_two_checks": "A blob-SHA match alone cannot distinguish 'unchanged' from 'a branch deleted together with the code that consumed it' -- both leave the file's other bytes identical to some other prior state's bytes, so the live re-parsed site-by-site inventory (predicate text + keyed_on + tier, compared positionally) is what makes a deletion, or a new insertion, visible even when nothing else about the blob changed.",
    "how_to_update": "If eprom.cpp or eprom_params.cpp legitimately change in a way that moves this inventory, re-derive it by running an independent parse against the new file (never hand-edit a line number, a keyed_on set, a class, or a count merely to make a surprise disappear), and state in the commit message which site changed, or was added or removed, and why. Diffing the extractor's live output against this JSON is the only sanctioned way to update it.",
    "frozen_for": "Phase 142 (plan 142-04) replaced the two duplicated protocol-keyed VPP-route predicates (formerly :190 in eprom_write_execute and :340 in eprom_check_vpp, both `if (handle->protocol == 0x0B || is_flag_set(FLAG_VPE_AS_VPP))`) with a single exposed resolver, eprom_hv_route_mask, driven by the eprom_params table's vpp_path column (D-05); removed the explicit `pins >= 32` drop-bit clear this file carried since Phase 141, now that plan 142-02's revision-gated preserve mask makes an explicit clear actively wrong on Rev 2-class hardware (D-04); deleted the dead eprom_internal_ensure_regulator_enabled helper, which had zero callers (Q6); and added two conditional single-exit wrappers (eprom_write_init, eprom_write_execute) that clear the shared EPROM_HV_ALL_OFF_MASK composite whenever response_code == RESPONSE_CODE_ERROR (D-10 as amended, D-12). Net effect: tier-1 protocol-keyed sites fell from 3 to exactly 1 (only :70's pulse-fallback switch survives -- D-01 forbids touching it), and total sites fell from 27 to 26 (4 removed, 3 added). No phase in this milestone is currently scheduled to rewrite src/proms/eprom.cpp again; if one does, an unchanged site count across that phase would itself be suspicious and should be checked by hand, exactly as this note warned before Phase 142 landed. Phase 143 (plan 143-05, HOST-02) added exactly ONE new code element inside eprom_internal_write_execute_body: a time-keyed (millis()), #ifndef SERIAL_ON_IO-guarded MSG_DATA_PROGRESS emission at the top of the per-byte loop body (immediately after `addr` is computed, before the LOOP-06 skips), plus its own guarded state variable (`last_emit_ms`) declared just above the loop. Net effect on THIS inventory: zero sites added, zero removed, zero re-keyed -- the new predicate compares two millis()-derived values against a named constant and reads no handle-> field and none of the three predicate helpers, so `_is_relevant` correctly excludes it; `counts` stays 26 / 1 / 25 and `protocol_lines` stays exactly [70]. Every site whose original line number was >= 321 (inside or after the outer per-byte loop) shifted down by the inserted line count; sites above that point are byte- and line-identical. This was checked by hand, per the note above, and confirmed unchanged in kind (only line numbers moved).",
    "allowlist_rationale": "protocol is the sole ALGORITHM key end to end (v1.20 protocol-only dispatch; TABLE-05): only the pulse-fallback switch (handle->protocol) at line 70 -- together with the eprom_params.cpp table, itself asserted switch-free below -- selects programming-algorithm SHAPE (max_pulses / overprogram_factor / overprogram_cap_us / energy_cap_us / verify_mode / vpp_path). Every tier-2 site pinned here is a pre-existing operation-flag (a ctrl_flags bit read via is_flag_set), a hardware-routing predicate (the resolver's own FLAG_VPE_AS_VPP override, which no longer co-occurs with a handle->protocol read in the same predicate span now that Phase 142 moved the table lookup into a separate statement; or the pin_routing predicate keyed on pins+bus_config.vpp_line), a disable-guarantee gate (the two single-exit wrappers' response_code == RESPONSE_CODE_ERROR checks, Phase 142), a loop-bounds check, a status/idempotency check, or a plain data comparison (voltage, chip ID, verify byte, or a pulse_delay-vs-zero sentinel test) -- never a second axis that chooses algorithm shape. The tier-2 set is PINNED, not FORBIDDEN: D-13's rule taken literally ('a branch keyed on any other handle field fails the gate') would make this gate RED against roughly twenty-five pre-existing sites on arrival, and a gate that can never be seen to pass proves nothing (D-15 trap 2). Pinning the inventory instead means a NEW site, or an existing site's predicate/keyed_on/tier changing, fails the gate, while today's arrival state is GREEN and non-vacuous. CORRECTED (142-04, C-6): the previous text here cited three stale colon-prefixed line numbers plus a stale ordinal reference to the algorithm-selector switch, all left over from an earlier renumbering that the 141-05 re-derivation never caught; the authoritative numbers are, and have only ever been, in the sites array below -- this prose intentionally names no line numbers at all now, so it cannot go stale the same way again.",
    "sweep_note": "The src/proms/eprom_params.cpp blob SHA below was re-derived by the provenance comment sweep (SWEEP-01, SWEEP-06) using `git hash-object` on the swept working-tree file, per this gate's own \"never hand-edit the SHA\" instruction. The sweep changed comment text ONLY: no cell value, no recorded site, no count, no table content and no other blob_shas entry changed. Verified for this record: the comment-stripped text of src/proms/eprom_params.cpp is byte-identical before and after the sweep."
  },
  "sites": [
    {
      "line": 45,
      "predicate": "switch (handle->cmd)",
      "keyed_on": [
        "cmd"
      ],
      "tier": "other",
      "class": "command_dispatch",
      "reason": "Selects which CMD_* operation callback (init/main/end) to install on handle; it dispatches among read/write/erase/blank-check/check-chip-id actions, not among programming algorithms -- handle->protocol is untouched by this switch."
    },
    {
      "line": 52,
      "predicate": "if (!is_flag_set(FLAG_SKIP_BLANK_CHECK))",
      "keyed_on": [
        "ctrl_flags"
      ],
      "tier": "other",
      "class": "operation_flag",
      "reason": "Reads the caller-supplied FLAG_SKIP_BLANK_CHECK ctrl_flags bit to decide whether a post-erase blank-check callback is attached; a per-call opt-out flag, independent of which protocol/algorithm is in effect."
    },
    {
      "line": 69,
      "predicate": "if (handle->pulse_delay == 0)",
      "keyed_on": [
        "pulse_delay"
      ],
      "tier": "other",
      "class": "data_compare",
      "reason": "Tests whether the host supplied a pulse width at all (0 = not supplied). It gates entry to the algorithm-shape switch at :70 but is itself a data-value-vs-sentinel comparison, not an algorithm selection -- pulse_delay is DATA, not a protocol axis (TABLE-02/D-03)."
    },
    {
      "line": 70,
      "predicate": "switch (handle->protocol)",
      "keyed_on": [
        "protocol"
      ],
      "tier": "protocol",
      "class": "algorithm_selector",
      "reason": "THE tier-1 algorithm-shape selector this gate exists to keep unique: chooses the pulse_delay==0 fallback constant (1ms/100us/500us) per protocol. D-01 keeps this switch in place deliberately, byte-unchanged, through every phase that has touched this file so far -- no pulse number enters the eprom_params table -- so its continued presence here, alone, is the expected and required state, not a violation. Phase 142 leaves it untouched; it is now the ONLY tier-1 site in this file."
    },
    {
      "line": 106,
      "predicate": "if (energy_cap_us > 0 && handle->pulse_delay > energy_cap_us)",
      "keyed_on": [
        "pulse_delay"
      ],
      "tier": "other",
      "class": "budget_refusal",
      "reason": "D-03's pre-flight refusal in configure_eprom: a pulse wider than this row's per-byte energy cap (energy_cap_us, read from the eprom_params table via eprom_params_for()) is refused before any high voltage is enabled, rather than silently clamped. Keyed on handle->pulse_delay -- the requested pulse width, a data value -- not on handle->protocol; the row lookup one statement above IS keyed on protocol, but that comparison's own text is not part of this predicate span, so this site is tier-2."
    },
    {
      "line": 144,
      "predicate": "if (!is_operation_in_progress(handle))",
      "keyed_on": [
        "operation_state"
      ],
      "tier": "other",
      "class": "status_check",
      "reason": "Checks whether a multi-call write operation is already mid-flight (operation_state), so one-time init (erase / blank-check) is not repeated on a resumed call; an operation-lifecycle guard, unrelated to which programming algorithm is selected. Lives inside eprom_internal_write_init_body (Phase 142 Plan 04 renamed this static, wrapped, body -- see eprom_write_init below for the wrapper); the predicate itself is byte-unchanged, only its line number and enclosing function name moved."
    },
    {
      "line": 146,
      "predicate": "if (handle->response_code == RESPONSE_CODE_ERROR)",
      "keyed_on": [
        "response_code"
      ],
      "tier": "other",
      "class": "status_check",
      "reason": "Early-exits eprom_internal_write_init_body's init sequence when a prior step (VPP check) already set an error response code; a generic error-propagation guard, not an algorithm choice. Byte-unchanged from before Phase 142; only its enclosing function was renamed static (see :177 for the new, structurally distinct, wrapper-level gate on the same predicate text)."
    },
    {
      "line": 150,
      "predicate": "if (is_flag_set(FLAG_CAN_ERASE))",
      "keyed_on": [
        "ctrl_flags"
      ],
      "tier": "other",
      "class": "operation_flag",
      "reason": "Reads the caller-supplied FLAG_CAN_ERASE ctrl_flags bit to decide whether an erase step may run before write; a per-chip capability flag set by the host database, not a protocol/algorithm selector."
    },
    {
      "line": 151,
      "predicate": "if (!is_flag_set(FLAG_SKIP_ERASE))",
      "keyed_on": [
        "ctrl_flags"
      ],
      "tier": "other",
      "class": "operation_flag",
      "reason": "Reads the caller-supplied FLAG_SKIP_ERASE ctrl_flags bit to decide whether the (already-permitted) erase step actually runs; independent of handle->protocol."
    },
    {
      "line": 158,
      "predicate": "if (!is_flag_set(FLAG_SKIP_BLANK_CHECK))",
      "keyed_on": [
        "ctrl_flags"
      ],
      "tier": "other",
      "class": "operation_flag",
      "reason": "Reads the caller-supplied FLAG_SKIP_BLANK_CHECK ctrl_flags bit to decide whether a blank-check runs before write; independent of the programming algorithm."
    },
    {
      "line": 176,
      "predicate": "if (handle->response_code == RESPONSE_CODE_ERROR)",
      "keyed_on": [
        "response_code"
      ],
      "tier": "other",
      "class": "hv_disable_gate",
      "reason": "NEW (Phase 142 Plan 04, D-10 as amended, D-12): the single-exit wrapper's own gate for eprom_write_init. After eprom_internal_write_init_body (:144-:158's enclosing function) returns from either of its two exits, this conditional is the ONLY place that decides whether to clear the shared EPROM_HV_ALL_OFF_MASK composite -- structural, not remembered. Defensive rather than corrective here: neither of that body's own exits leaks a route today (unlike eprom_write_execute's identical wrapper gate at :426, which closes a real, pre-existing gap)."
    },
    {
      "line": 290,
      "predicate": "if (is_flag_set(FLAG_VPE_AS_VPP))",
      "keyed_on": [
        "ctrl_flags"
      ],
      "tier": "other",
      "class": "vpp_route",
      "reason": "NEW shape, same decision (Phase 142 Plan 04, D-05/D-06): the FLAG_VPE_AS_VPP half of what used to be the duplicated protocol-keyed forks at (what were) :190 and :340 -- `if (handle->protocol == 0x0B || is_flag_set(FLAG_VPE_AS_VPP))`. Both call sites now resolve the route through the single exposed function eprom_hv_route_mask, which checks this flag FIRST (forcing the direct-VPE path on top of whatever the eprom_params table's vpp_path column says) and only then reads the table. Because the flag check and the table read are now two separate statements, this site's predicate span no longer contains a handle->protocol reference at all -- DEMOTING it from a tier-1 protocol-keyed site to a tier-2 flag site (D-06's own framing: FLAG_VPE_AS_VPP is a live user-facing escape hatch set by no database entry, a pure human override for the 25V NMOS parts and the manual-pot workflow, not a second orthogonal dispatch axis)."
    },
    {
      "line": 319,
      "predicate": "if (handle->firestarter_get_control_register(handle, CTRL_VPP_REGULATOR_ENABLE) == 0)",
      "keyed_on": [
        "firestarter_get_control_register"
      ],
      "tier": "other",
      "class": "status_check",
      "reason": "Checks whether the VPP regulator control-register bit is already set before running the enable sequence and its settle delay, so that sequence runs at most once; a hardware-state idempotency guard -- the VPP ROUTE itself is chosen by eprom_hv_route_mask(handle) (Phase 142 Plan 04), called immediately inside this guard, not here. CORRECTED (142-04): the reason previously recorded here cited 'the allowlisted :145 branch immediately inside this guard' -- a line-number cross-reference that was already stale before this phase (the fork it meant was at :190, not :145) and is now doubly wrong, because there is no branch immediately inside this guard any more at all: the route decision is a single function call. This text intentionally names no line number for that call, so it cannot go stale the same way twice."
    },
    {
      "line": 543,
      "predicate": "if (handle->firestarter_get_data(handle, addr) == expected)",
      "keyed_on": [
        "firestarter_get_data"
      ],
      "tier": "other",
      "class": "skip_check",
      "reason": "LOOP-06's already-matching skip: reads the byte back from the chip before pulsing and skips straight to the next byte if it already matches the expected value; a pre-pulse optimisation read-compare, not a protocol/algorithm branch. Runs immediately after the (unrelated, not handle->-keyed) expected == 0xFF skip just above it."
    },
    {
      "line": 672,
      "predicate": "if (handle->response_code == RESPONSE_CODE_ERROR)",
      "keyed_on": [
        "response_code"
      ],
      "tier": "other",
      "class": "hv_disable_gate",
      "reason": "NEW (Phase 142 Plan 04, VPP-02's headline requirement; D-10 as amended, correction C-1, D-12): the single-exit wrapper's own gate for eprom_write_execute. After eprom_internal_write_execute_body returns -- from any of its four error exits (row == NULL; MSG_ERR_MAX_PULSES; MSG_ERR_ENERGY_CAP; MSG_ERR_VERIFY, the pre-existing headline gap that used to disable nothing at all) or its success fall-through -- this conditional is the ONLY place that decides whether to clear the shared EPROM_HV_ALL_OFF_MASK composite: structural, not remembered, so a future `return` added inside the body cannot silently escape the guarantee. Conditional rather than unconditional specifically because test_loop_eprom_v131.cpp's test_loop05_a_successful_block_does_not_disable_the_route requires a successful block to leave CTRL_VPP_REGULATOR_ENABLE SET (an unconditional clear would re-arm the once-per-block guard and re-pay delay(500) on the next block too)."
    },
    {
      "line": 709,
      "predicate": "if (vpp_mv > (uint32_t)handle->vpp_mv + 500)",
      "keyed_on": [
        "vpp_mv"
      ],
      "tier": "other",
      "class": "data_compare",
      "reason": "Compares a measured VPP millivolt reading against the caller-supplied vpp_mv target plus a fixed margin to detect an over-voltage fault; a voltage data comparison, not a protocol selector."
    },
    {
      "line": 714,
      "predicate": "if (vpp_mv < (uint32_t)handle->vpp_mv * 95 / 100)",
      "keyed_on": [
        "vpp_mv"
      ],
      "tier": "other",
      "class": "data_compare",
      "reason": "Compares a measured VPP millivolt reading against 95% of the caller-supplied vpp_mv target to detect an under-voltage condition; a voltage data comparison, not a protocol selector."
    },
    {
      "line": 749,
      "predicate": "if (handle->response_code == RESPONSE_CODE_ERROR)",
      "keyed_on": [
        "response_code"
      ],
      "tier": "other",
      "class": "status_check",
      "reason": "Early-exits eprom_generic_init when the preceding VPP check already set an error response code; a generic error-propagation guard, not an algorithm choice."
    },
    {
      "line": 752,
      "predicate": "if (handle->chip_id > 0)",
      "keyed_on": [
        "chip_id"
      ],
      "tier": "other",
      "class": "operation_flag",
      "reason": "Tests whether the caller supplied a non-zero expected chip_id (0 means 'skip the check'); a caller opt-in gate for an optional verification step, not a protocol/algorithm branch."
    },
    {
      "line": 753,
      "predicate": "is_flag_set(FLAG_FORCE)",
      "keyed_on": [
        "ctrl_flags"
      ],
      "tier": "other",
      "class": "operation_flag",
      "reason": "Ternary reading the caller-supplied FLAG_FORCE ctrl_flags bit to choose whether a chip-ID mismatch is reported as a warning or an error; independent of the programming algorithm."
    },
    {
      "line": 771,
      "predicate": "if (bit & CTRL_VPE_ENABLE && using_p1_as_vpp(handle))",
      "keyed_on": [
        "pins+bus_config.vpp_line"
      ],
      "tier": "other",
      "class": "pin_routing",
      "reason": "Reroutes the CTRL_VPE_ENABLE control bit to CTRL_VPP_P1_ENABLE when this package's VPP line physically lands on pin 1 (keyed on pins + bus_config.vpp_line, using_p1_as_vpp() in memory_utils.h). A pre-existing package pin-map routing decision that predates this milestone and does not read handle->protocol at all -- not an algorithm selector of any kind. Byte-unchanged by Phase 142 (D-12/H: this function is deliberately left untouched so its VPE-to-P1 remap keeps converting the shared EPROM_HV_ALL_OFF_MASK composite correctly); only its line number moved because of edits earlier in the file."
    }
  ],
  "counts": {
    "total_sites": 21,
    "protocol_keyed_sites": 1,
    "other_sites": 20
  },
  "params_table": {
    "keys": [
      "0x07",
      "0x08",
      "0x0B"
    ],
    "switch_statements": 0,
    "key_comparisons": 1
  }
}
